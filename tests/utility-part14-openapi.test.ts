import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{resolve}from'node:path';import{describe,it}from'node:test';import{parse}from'yaml';import{api}from'./helpers/http';const d=parse(readFileSync(resolve(__dirname,'../docs/api/openapi.yaml'),'utf8'))as any;const ops=['GET /utility/meters/{id}/reading-dues','POST /utility/meters/{id}/reading-dues','GET /buildings/{buildingId}/utility-reading-dues','GET /clients/{clientId}/utility-reading-dues','GET /utility/reading-dues/{id}','POST /utility/reading-dues/{id}/complete','POST /utility/reading-dues/{id}/cancel','POST /utility/meter-reading-evidence/{evidenceId}/ocr-candidates','GET /utility/ocr-candidates/{id}','POST /utility/ocr-candidates/{id}/accept','POST /utility/ocr-candidates/{id}/reject'];describe('PART 14 Utility OpenAPI',()=>{it('publishes only registered Reading Due and OCR endpoints',async()=>{for(const x of ops){const[m,p]=x.split(' ');assert.ok(d.paths[p]?.[m.toLowerCase()],x);const q=api(),r=m==='GET'?await q.get('/api/v1'+p):await q.post('/api/v1'+p).send({});assert.equal(r.status,401,x)}});it('documents lifecycle, lineage and candidate-only semantics',()=>{assert.deepEqual(d.components.schemas.UtilityReadingDueStatus.enum,['DUE','COMPLETED','OVERDUE','CANCELLED']);assert.deepEqual(d.components.schemas.UtilityOcrStatus.enum,['PENDING_REVIEW','ACCEPTED','REJECTED']);for(const x of['meterId','scheduleDefinitionId','generatedTaskId','meterReadingId'])assert.ok(d.components.schemas.UtilityReadingDue.properties[x]);for(const x of['evidenceId','meterId','candidateReadingValue','confidence','acceptedReadingId','verifiedByUserId','verifiedAt'])assert.ok(d.components.schemas.UtilityOcrCandidate.properties[x]);assert.match(d.paths['/utility/meter-reading-evidence/{evidenceId}/ocr-candidates'].post.description,/never changes a Meter Reading/)});});
