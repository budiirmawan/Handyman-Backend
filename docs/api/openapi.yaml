# Asentra Backend API — OpenAPI Contract
#
# CR-BE-API-01 PART 01 — OpenAPI Contract Foundation.
#
# This document is the AUTHORITATIVE Web/Mobile API contract for the
# Asentra Backend platform (see README "REST API").
#
# Scope note:
#   The contract is maintained incrementally by capability waves. CR-BE-PRO-02
#   PART 01–06 RFQ procurement surfaces are closed here; absence of an
#   unrelated BE-02…BE-23 path does not imply that the runtime route does not
#   exist.
#
# Source of truth: registered routes in src/modules/**/*.routes.ts and
# the DTOs in src/modules/**/*.types.ts. This file must never invent an
# endpoint that is not registered in the backend router.
#
# Server base path: the versioned API is mounted at the configured
# API_PREFIX (default /api/v1). All paths below are relative to it.
#
# Extending this file (incremental rule):
#   1. Add paths only for endpoints already registered in the router.
#   2. Reuse the shared envelope, error and pagination schemas below;
#      introduce new schemas only for genuinely new domain shapes.
#   3. Keep `servers` and `securitySchemes` untouched unless the
#      platform's base path or auth mechanism changes.

openapi: 3.0.3

info:
  title: Asentra Backend API
  version: 0.1.0
  description: |
    Asentra Facility & Housekeeping Management backend — final authority for
    persistence, security authorization, workflow, business rules, data scope
    and available actions. Consumed by asentra-web and asentra-mobile.

    Cross-cutting contract conventions (CR-BE-R2P-CONTRACT-01 PART 01):
    - versioned API prefix: API_PREFIX, default `/api/v1`;
    - every success uses `SuccessEnvelope`; every error uses `ErrorEnvelope`;
    - every HTTP response after request entry carries the server-generated
      `X-Request-ID` response header; error responses also carry the same value
      as `error.requestId`;
    - protected Building-scoped operations require an ACTIVE caller Building
      assignment; inaccessible scope returns 403 `BUILDING_ACCESS_DENIED` and
      list operations are constrained to the caller's accessible Buildings;
    - on PART 01 Purchase Order, Work Contract, procurement-binding, and Vendor
      Invoice paths, `x-required-permission` records the exact permission and
      `x-building-scoped: true` records Building isolation;
    - PART 01 PO/SPK available-actions responses use `{ <resource>Id, state,
      availableActions }`; action tokens are upper-case command names, are
      filtered for the calling user's existing manage permission and current
      business authority, and map only to registered command endpoints.
  license:
    name: Proprietary

servers:
  - url: /api/v1
    description: Versioned API base path (API_PREFIX; default /api/v1)

tags:
  - name: Utility
    description: >
      Existing BE-18/BE-19 and CR-BE-UTL-01 Utility contracts. Tenant lineage
      and Building analytics reuse authoritative Meter, Reading, Evidence,
      Consumption, Approval and Bill records; no parallel engine.
  - name: FX
    description: >
      CR-BE-FX-01 PART 02 — FX Rate Authority lifecycle and Client FX Policy
      governance.

      CANONICAL CONVENTION (frozen): **1 BASE = RATE x QUOTE**. `rate` is the
      number of QUOTE units equal to ONE unit of BASE, so `baseCurrencyCode:
      USD`, `quoteCurrencyCode: IDR`, `rate: "16500"` means 1 USD = 16,500 IDR.
      The rate is a ratio, not a monetary amount, and is stored as
      NUMERIC(24,12); it is always returned as a string to avoid float drift.

      LIFECYCLE (truthful, frozen): `PENDING_APPROVAL` -> `ACTIVE` | `REJECTED`;
      `ACTIVE` -> `SUPERSEDED` | `INACTIVE`; `REJECTED`, `SUPERSEDED` and
      `INACTIVE` are terminal and can never return to `ACTIVE`. A rate is created
      `PENDING_APPROVAL` and cannot be used by any consumer until a DIFFERENT
      authorized user approves it (maker-checker). A stored rate value is never
      modified: correction is a new rate plus supersession.

      This surface performs NO conversion. There is no convert/quote endpoint,
      no reporting conversion, no provider ingestion and no FX dashboard in
      FX-01. PART 03 owns the single governed conversion authority.

  - name: Health
    description: Runtime / readiness probes (public).
  - name: Authentication
    description: >
      Login, logout and the opaque session model (CR-BE-MOB-CONTRACT-01
      PART 01). `POST /auth/login` returns a cryptographically random,
      base64url `sessionToken`; only its SHA-256 hash is stored and the raw
      token is never persisted or returned again. Session lifetime is bounded
      by `expiresAt` (SESSION_TTL_MINUTES, default 480). On every protected
      request the backend hashes the presented `Authorization: Bearer <token>`,
      verifies the session is ACTIVE and unexpired, then re-validates the
      user's account state. `POST /auth/logout` revokes the current session
      (the row is marked REVOKED, never deleted). There is NO session-state /
      validate endpoint and NO token refresh/rotation endpoint: session
      validation is implicit in the authentication middleware on every
      protected route, and expiry is handled by re-login — mobile must treat
      any 401 as "re-authenticate". 401 semantics: missing/invalid
      Authorization header → `AUTHENTICATION_REQUIRED`; unknown or non-ACTIVE
      session → `INVALID_SESSION`; expired ACTIVE session → `SESSION_EXPIRED`;
      bad credentials at login → `INVALID_CREDENTIALS` (unknown email and
      wrong password are indistinguishable to avoid account enumeration).
      Repeated login failures are throttled per IP → 429 `AUTH_RATE_LIMITED`.
  - name: Effective Context
    description: Authoritative context of the authenticated user.
  - name: Client Configuration
    description: >
      BE-27A Client-scoped key/value configuration administration and ACTIVE
      effective read. No Building overrides, versioning, preview, or publish
      lifecycle.
  - name: Building Configuration
    description: >
      BE-27B Building-scoped key/value override administration and effective
      Client→Building resolution. No Module configuration, versioning, preview,
      or publish lifecycle.
  - name: Module Configuration
    description: >
      BE-27C Client/Building scoped Module enablement configuration. Effective
      state is intersected with the existing Subscription/License/Entitlement
      authority; configuration never grants commercial availability.
  - name: Feature Entitlement Configuration
    description: >
      BE-27D Client/Building Feature state under an existing Module. Effective
      Feature state cannot exceed BE-27C effective Module availability.
  - name: Navigation Registry
    description: >
      BE-27E server-driven navigation metadata filtered by scope, Module,
      Feature and Permission authority. No frontend menu implementation.
  - name: Workspace Registry
    description: >
      BE-27F server-driven workspace metadata filtered by Module, Feature,
      Navigation and Permission authority. No workspace UI implementation.
  - name: Dashboard Configuration
    description: >
      BE-27G Dashboard/Widget composition referencing approved BE-23/BE-24
      read models. Configuration only; no KPI calculation or chart rendering.
  - name: Form Administration
    description: >
      BE-27H scope-safe administration facade over existing BE-07 Form
      Templates, Sections and Fields. No second Form execution engine.
  - name: Checklist Administration
    description: >
      BE-27I scope-safe administration facade over existing BE-07 Checklist
      Templates and Items. Measurement, evidence, verification, and execution
      remain with their existing backend authorities.
  - name: Operational Settings
    description: >
      BE-27J typed, allowlisted operational metadata over BE-27A/BE-27B.
      Owning domain workflows and validation remain authoritative.
  - name: Organization Presentation
    description: >
      BE-27K Client/Building display labels and metadata referencing the
      authoritative BE-03 Organization hierarchy without changing it.
  - name: CMS Content
    description: >
      BE-27L scoped safe-text/Markdown content. Raw executable HTML/JS and
      Branding/White Label behavior are not supported.
  - name: Branding
    description: >
      BE-27M constrained Client/Building brand metadata and theme tokens using
      opaque storage references. No arbitrary CSS/JS or logo generation.
  - name: Configuration Versioning
    description: >
      BE-27N/O immutable snapshots and controlled Draft/Validate/Publish lifecycle.
  - name: Configuration Preview
    description: >
      BE-27P caller-bound, expiring PREVIEW contexts for validated non-active
      versions. Preview never changes publication or ACTIVE state.
  - name: Configuration Audit
    description: >
      BE-27Q read-only, scope-safe projection of append-only configuration
      events from the existing Operational Event authority.
  - name: BAST Lifecycle
    description: >
      CR-BE-BAST-01 canonical BE-22 BAST identity and lifecycle commands,
      immutable submissions, acceptance Sign-Off, rejection Finding linkage,
      read-only legacy compatibility/reconciliation, and Work Order closure
      readiness. Work Order remains the sole closure authority.
  - name: Vendor Invoice
    description: >
      CR-BE-COM-02 Vendor Invoice lifecycle, verification/matching,
      payment status, settlement readiness, vendor consistency cross-check,
      BAST hard gate, and end-to-end traceability.
  - name: Evidence
    description: >
      Evidence submission metadata, file upload / metadata / download, and
      removal (CR-BE-API-01 PART 03 + CR-BE-MOB-CONTRACT-01 PART 04). The
      evidence chain for mobile is: Checklist/Form execution → Evidence
      Requirement (required rules) → Evidence submission metadata → binary
      file (upload/download). Binary upload IS supported
      (`POST /evidence/:id/file` and the single-call `POST /mobile/evidence`);
      there is no offline byte-queue here (BE-25G sync carries metadata only).
  - name: Checklist & Measurement
    description: >
      Checklist template / item definition, UOM registry, and the
      measurement/UOM binding on numeric checklist items / form fields
      (CR-BE-MOB-CONTRACT-01 PART 04). These are the definitional authorities
      behind the mobile checklist execution read model
      (`GET /mobile/checklist-executions/:id` embeds items + measurement + UOM).
  - name: Evidence Requirements
    description: >
      Evidence requirement definitions (required/optional, type, min/max
      count) bound to checklist templates / items and form templates/fields
      (CR-BE-MOB-CONTRACT-01 PART 04). The required-evidence rules mobile
      enforces come from these records (also embedded in the mobile checklist
      execution read model).
  - name: Management Read Models
    description: Shared Management & Owner read-scope and response contracts (BE-24).
  - name: Operational Finance
    description: Building-scoped Operational Finance budget, binding and read-time aggregation contracts (CR-BE-FIN-01).
  - name: Reporting Export
    description: >
      Governed JSON projections, CSV/XLSX/PDF archive generation, and scoped
      report archive metadata/download contracts. Source KPIs/read models remain
      the calculation authorities; renderers do not recalculate.
  - name: Mobile Execution
    description: >
      Mobile worker execution surface (BE-25A): tasks, checklist executions,
      evidence, reviews, operational events, finding workflow read models and
      QR resolution. List endpoints support the opt-in mobile pagination
      convention (?page=&pageSize=) described in docs/api/mobile-contract.md.
  - name: Work Orders
    description: >
      Work Order read surface for mobile operational users
      (CR-BE-MOB-CONTRACT-01 PART 03): the authoritative Work Order record,
      its assignee/personnel, location/asset context, priority, lifecycle
      status, completion subset, and action history. Building isolation
      (BE-02G) is enforced on every route — by-id reads resolve the Work
      Order's Building and require an ACTIVE caller assignment (403
      BUILDING_ACCESS_DENIED otherwise); the list route is Building-nested.
      `GET /work-orders/:id/actions` returns the append-only ACTION HISTORY,
      NOT an available-actions read model: there is no direct
      Work-Order available-actions endpoint — the authoritative per-caller
      `availableActions` for Work Orders is exposed only through the BE-25C
      unified feed `GET /mobile/assignments`.
  - name: Notifications
    description: >
      BE-26A notification foundation: the authenticated user's own in-app
      inbox (record, type, recipient, source/domain reference, channel,
      status, read state). Foundation only — no push/email/WhatsApp/SMS
      delivery and no create endpoint (creation is internal to the backend).
  - name: Notification Templates
    description: >
      BE-26B reusable notification templates (platform configuration): template
      key, notification type, channel, subject/title and message/body
      templates, declared template variables, and active status. Foundation
      only — no recipient resolution and no sending.
  - name: Notification Subscriptions
    description: >
      BE-26D notification event subscriptions (platform configuration): maps
      existing backend domain events to notification triggers — event type,
      notification template reference, recipient resolution rule, enabled
      status, and optional Client/Building context. Foundation only — no
      delivery and no new domain events.
  - name: Notification Reminders
    description: >
      BE-26H notification reminders (platform configuration): a time-deferred
      notification trigger — reminder reference, source/resource reference,
      recipient resolution rule, reminder_at, notification template reference,
      status and sent_at. Foundation only — no scheduler engine and no
      escalation.
  - name: Notification Escalations
    description: >
      BE-26I notification escalations (platform configuration): a time-deferred
      escalation trigger — escalation reference, source/resource reference,
      current recipient, escalation recipient rule, escalation_at/triggered_at,
      notification template reference, status and reason/notes. Foundation
      only — no scheduler engine, no workflow/approval engine, and no
      business-resource state changes.
  - name: Notification Secure Links
    description: >
      BE-26J secure notification links: an opaque, recipient-bound,
      single-purpose reference to a target resource + action. The raw token is
      returned once and never stored (only its hash); expired/revoked/exhausted
      links are rejected. Resolution does not bypass backend authorization or
      workflow rules. Foundation only — no Notification History.
  - name: Notification History
    description: >
      BE-26K notification delivery/history read model: a unified,
      chronological, recipient-scoped view over the in-app, email and
      WhatsApp delivery records. Read-only — no second audit engine, no
      credential/token exposure, and no sensitive payloads.

  - name: Material Requests
    description: >
      CR-BE-MAT-01 — BE-17B Material Request lines on a Purchase Request: the
      REQUEST AUTHORITY of the material fulfilment chain. Carries requested
      quantity, the PART 02 approved quantity (fulfilment cap) and the UOM
      context captured at creation. APPROVED lines are frozen (no silent
      mutation); correction goes through cancellation.
  - name: Material Reservations
    description: >
      CR-BE-INV-CONTROL-01 PART 01 — allocation records against an existing
      APPROVED Material Request. Reservations update the existing stock balance
      reserved quantity but do not create a demand or stock authority.
  - name: Procurement Approvals
    description: >
      BE-17D procurement approval bindings. Approving a MATERIAL_REQUEST
      binding may set an explicit approved quantity (CR-BE-MAT-01 PART 02);
      approval without an explicit quantity defaults approved = requested.
  - name: RFQ Procurement
    description: >
      CR-BE-PRO-02 RFQ, invitation-scoped Vendor responses, immutable
      comparison/evaluation evidence, manual recommendation, approval, award,
      and explicit conversion to an existing DRAFT Purchase Order. Vendor
      external sessions cannot access peer quotations, comparison, award, PO,
      or commitment data.
  - name: Receivings
    description: >
      BE-17G Receiving — the RECEIVING AUTHORITY. Material receivings bind to
      a Material Request line (PART 01), are capped by the approved quantity
      (PART 02, cumulative over-receipt rejected), snapshot the UOM (PART 04)
      and post their STOCK_IN atomically through the inventory movement
      ledger.
  - name: Inventory Stock
    description: >
      BE-16 stock movements and balances. `inventory_stock_movements` is the
      append-only STOCK-LEDGER AUTHORITY (PART 03 — a balance never changes
      without a movement); `inventory_stock_balances` is the derived current
      stock state per warehouse + item.
  - name: Work Order Material Usage
    description: >
      BE-16I Work Order Material Usage — the USAGE AUTHORITY. Controlled
      issue flow (PART 06): validated, atomic STOCK_OUT + usage with stable
      movement linkage, UOM snapshot (PART 04) and operational unit/total
      cost snapshot (PART 05 — no valuation/GL/accounting).

  - name: Service Requests
    description: >
      BE-17C Service Request lines on a Purchase Request — the service-side
      counterpart of the Material Request. Client and Building are derived
      from the parent Purchase Request.
  - name: Vendor Selection Readiness
    description: >
      BE-17E deterministic verdict on whether a Vendor may be selected for a
      Purchase or Service Request, with its individual checks.
  - name: Purchase Order Readiness
    description: >
      BE-17F — THE readiness authority of the R2P chain. READY means "may
      commit"; an ISSUED Purchase Order means "committed". No downstream
      domain re-decides this verdict.
  - name: Purchase Order
    description: >
      CR-BE-R2P-01 PART 01–03 — the COMMITMENT authority of the R2P chain.
      A Purchase Order is committed against a READY BE-17F PO Readiness
      (which remains the sole readiness authority and a precondition only),
      carries commercial lines over MR/SR request lines without creating a
      competing quantity ledger, and is ISSUED to the Vendor through a
      deterministic DRAFT → ISSUED transition.
  - name: Price Catalog
    description: >
      CR-BE-PRICE-01 — the internal reference PRICE AUTHORITY: effective-
      dated, overlap-excluded catalog entries (MATERIAL only) in fixed
      Vendor+Building → Vendor → Building → Client-wide tiers with
      exact-match UOM/currency selection. Change is governed lifecycle
      (activate / replace / deactivate / override-correct with mandatory
      reason under `price_catalog.override`), never in-place mutation.
      Internal-only surface — RFQ Vendor sessions receive nothing from it;
      usage elsewhere is advisory evidence (RFQ comparison snapshots, PO
      price-deviation projection), never a gate.
  - name: Service Catalog
    description: >
      CR-BE-SVC-01 — the governed SERVICE master identity: a Client-scoped
      reference master (the SERVICE analog of `inventory_items`) with an
      immutable code, a category, and ACTIVE/INACTIVE lifecycle. It is the
      governed anchor for Service Requests (PART 02), Vendor capability
      matching (PART 03), the SERVICE procurement lineage (PART 04), and the
      SERVICE price subject (PART 05). Identity only — no demand/price/
      quantity/UOM surface. Created ACTIVE; deactivation is terminal.
  - name: Work Contract (SPK)
    description: >
      CR-BE-R2P-01 PART 04 — the SPK (Surat Perintah Kerja) EXECUTION MANDATE
      raised against an ISSUED Purchase Order, inheriting that PO's Client,
      Building and Vendor. Its own entity — it does not duplicate the BE-17H
      Work Order Procurement Binding domain. Lifecycle
      DRAFT → ACTIVE → COMPLETED / CANCELLED.
  - name: Work Order Procurement Binding
    description: >
      BE-17H binding of a Work Order to its Purchase Request / Material
      Request / Service Request / Receiving, EXTENDED by CR-BE-R2P-01 PART 05
      with the authoritative SPK → PO → Vendor chain. Exactly one binding per
      Work Order; no parallel SPK ↔ Work Order binding domain.
  - name: Users
    description: >
      User identity and account lifecycle (CR-BE-MOB-CONTRACT-01 PART 02).
      The authenticated user's own identity is read via `GET /auth/me`
      (Effective Context tag); these management routes require `user.read` /
      `user.manage` and are the backend-authoritative source for user records
      the mobile app may need to resolve (e.g. assignee display names).
  - name: Roles & Permissions
    description: >
      Roles, permissions and their assignments (CR-BE-MOB-CONTRACT-01 PART 02).
      Default-deny RBAC is enforced by `requirePermission` (permission codes
      are `<domain>.<verb>`). The effective set for the authenticated user is
      the single source of truth exposed in `GET /auth/me` →
      `access.roles` / `access.permissions`; these routes administer the
      underlying assignments and are read-only for mobile context resolution.
  - name: Clients
    description: >
      Client (Company & Client) records (CR-BE-MOB-CONTRACT-01 PART 02).
      The reachable Client → Property → Building hierarchy for the
      authenticated user is exposed authoritatively in `GET /auth/me` →
      `context.clients`; these routes administer the Client registry.
  - name: Properties
    description: >
      Property records under a Client (CR-BE-MOB-CONTRACT-01 PART 02).
      Reachable properties are exposed in `GET /auth/me` →
      `context.clients[].properties`; these routes administer the registry.
  - name: Buildings
    description: >
      Building records under a Property (CR-BE-MOB-CONTRACT-01 PART 02).
      Building isolation (BE-02G) is enforced: by-id reads assert the caller's
      ACTIVE Building assignment (403 BUILDING_ACCESS_DENIED otherwise).
      Reachable buildings are exposed in `GET /auth/me` → `scope.buildingIds`
      and `GET /auth/me/buildings`.
  - name: Building Assignments
    description: >
      User ↔ Building assignment administration and the current user's own
      accessible Building context (CR-BE-MOB-CONTRACT-01 PART 02).
      `GET /auth/me/buildings` is authentication-only (no management
      permission); the `/users/:userId/buildings` routes require
      `building.read` / `building.manage`.
  - name: Findings
    description: >
      Finding, Rework, and Supervisor Verification surface for mobile
      operational users (CR-BE-MOB-CONTRACT-01 PART 05). The Finding record
      (BE-09) with its authoritative `status`, source binding
      (FORM_INSTANCE / CHECKLIST_EXECUTION / WORK_ORDER), location/asset
      context via Building, assignment, rework cycle, supervisor review +
      verification, closure, and audit/history. Building isolation (BE-02G)
      is enforced on every route. Workflow authority is NEVER inferred from
      `status`: the backend-authoritative `availableActions` comes from
      `GET /findings/:id/available-actions`, and supervisor verification from
      `GET/POST /mobile/verification/:targetType/:targetId` (which reuses the
      same BE-09 action authority). Supervisor capability is RBAC-based
      (`finding.read` / `finding.review` / `finding.manage` / `finding.close` /
      `finding.assign`) — there is NO hard-coded mobile role logic.
  - name: Location & Asset Context
    description: >
      QR/tag resolution and the Building → Floor → Area → Room → Space →
      Functional Location → Asset/Equipment operational context for mobile
      (CR-BE-MOB-CONTRACT-01 PART 06). QR/tag resolution is provided by the
      backend (`GET /mobile/qr/resolve/:identifier` and
      `GET /assets/resolve/:identifier`, both over the BE-05H Asset Identifier
      authority — no separate QR engine); physical device GPS/geolocation and
      geofencing are NOT backend responsibilities and are NOT modeled here.
      Location and asset references use canonical backend ids only; Building
      isolation (BE-02G) is enforced on every route (cross-Building access →
      403 BUILDING_ACCESS_DENIED; unknown/inaccessible QR → the same 404 as
      unknown, hiding existence).

  - name: Inventory Master
    description: >-
      CR-BE-MOB-01 PART 04 — existing BE-16A Inventory Item master, BE-16B
      Warehouse master and BE-16H Asset ↔ spare-part binding **reads**: the
      reference data a mobile field picker needs before issuing material
      against a Work Order. It is reference data only and carries no stock,
      price or workflow. The Work Order material chain itself was already
      published and is deliberately not repeated here: issue/usage under
      `Work Order Material Usage` (`recordWorkOrderMaterialUsage`,
      `listWorkOrderMaterialUsages`, `getWorkOrderMaterialUsage`,
      `getWorkOrderMaterialCostSummary`), the authoritative ledger and
      availability under `Inventory Stock` (stock movements STOCK_IN /
      STOCK_OUT and stock balances), the approved-quantity chain under
      `Material Requests` (`quantity`, `approvedQuantity`, `receivedQuantity`,
      `remainingQuantity`) plus `Work Order Procurement Binding`, and
      supervisor verification under `Work Orders`
      (`getWorkOrderVerification` / `submitWorkOrderVerification` /
      `closeWorkOrder…`, over the BE-07 review primitive). Building isolation
      (BE-02G) and default-deny RBAC are enforced on every route. This tag
      invents no material workflow, no inventory engine, no verification
      engine and no mobile-specific API.
  - name: Engineering
    description: >-
      CR-BE-MOB-01 PART 03 — existing BE-10 Engineering field / reading
      contract for mobile. Every BE-10 record is a binding or read model over
      an existing authority, never a second engine: equipment inspection and
      engineering checklist bindings start a shared BE-07 checklist execution;
      meter-reading and log-sheet bindings start a shared BE-07 form instance
      and write into BE-07 `form_responses`; planned maintenance (BE-10G)
      references a BE-07 Schedule, BE-07 generated Tasks and a BE-08 Work
      Order; corrective maintenance is a BE-10F breakdown plus a linked BE-08
      Work Order. PM/CM field execution is therefore the already published
      BE-08 Work Order action set and the BE-07 task operations — this tag
      adds no execution path. Evidence is the published BE-07 evidence API on
      the returned execution id (`executionType` CHECKLIST_EXECUTION or
      FORM_INSTANCE); supervisor review is the published review / mobile
      verification / Work Order verification surface; finding workflow stays
      on BE-09 keyed by `findingId`. Building isolation (BE-02G) and
      default-deny RBAC are enforced on every route. This tag invents no
      diagnosis or test-result domain, no per-reading review workflow, and no
      mobile-specific engineering API.
  - name: Security
    description: >-
      CR-BE-MOB-01 PART 02 — existing BE-12 Security and BE-21A/B Incident
      operational contract for mobile. Security Posts, Patrol Routes and
      Patrol Route Points (the checkpoints) are operational context over the
      BE-04 structure — no second location hierarchy and no checkpoint
      master. A Patrol Execution is the Security view of a BE-07 generated
      task (`id` = `taskId`); start/complete delegate to the shared BE-07
      task-execution engine and patrol assignment is the published BE-07
      task-assignment surface. Checkpoint confirmation is by canonical
      `pointId` — there is no checkpoint QR/identifier authority. Patrol
      checklist bindings only start the shared BE-07 checklist execution;
      responses, completion, evidence and verification stay on the published
      BE-07 operations. Security findings bind a Security source to an
      authoritative BE-09 Finding (workflow keyed by `findingId`), and field
      reporting for a standalone event is the BE-21A/BE-21B Incident chain.
      Building isolation (BE-02G) and default-deny RBAC are enforced on
      every route. This tag invents no patrol engine, no checkpoint domain
      and no "occurrence" domain.
  - name: Housekeeping
    description: >-
      CR-BE-MOB-01 PART 01 — existing BE-11 Housekeeping operational contract
      for mobile. Daily cleaning is a read view of BE-07 generated tasks;
      execution writes remain `startTask` / `completeTask` / `cancelTask`.
      Toilet and public-area inspections bind a Cleaning Area to a BE-07
      checklist template and start a shared checklist execution. Supervisor
      inspection and quality audit provide pass/fail/rework decisions via the
      BE-07 review primitive. Housekeeping findings bind HK sources to a
      BE-09 Finding (rework stays on finding operations). Evidence is a
      BE-11I binding over BE-07 evidence metadata. Building isolation
      (BE-02G) is enforced on every route. This tag does not invent a
      housekeeping engine, reinspection lifecycle, or consumable stock-out.
  - name: Workforce
    description: >-
      CR-BE-MOB-03 PART 01 — existing supervisor team workload & status
      contract (BE-03F reporting lines, BE-03I2 workforce reporting,
      BE-23G workforce KPI). Publish-only: every operation below is an
      existing runtime route, read-only, reusing the BE-03 Workforce /
      BE-07 Task authorities. No new domain, no mobile facade, no
      duplicate endpoint. BE-03F write routes (assign/update a reporting
      line) and the generic BE-03B/C/E administration surfaces are
      deliberately not published here — the mobile supervisor surface is
      read-only. Building isolation (BE-02G) is enforced on the KPI and
      reporting reads; the BE-03F reporting-line reads are profile-resolved
      (404 on unknown profile) and carry no BE-02G gate today — documented
      honestly, not claimed.
  - name: Attendance
    description: >-
      CR-BE-MOB-05 PART 02 — Workforce attendance self-service
      (clock-in / clock-out / current status). The backend-authoritative
      attendance record binds the authenticated Workforce Profile (BE-03C),
      the Building the profile clocked into (BE-02F/G access-asserted), and
      — when an applicable ACTIVE roster row exists — the authoritative
      `workforce_shift_assignments.id` / `shifts.id`. Identity is resolved
      from the authenticated session, never from the client; timestamps are
      set by the backend database clock only. Lifecycle: CLOCKED_IN →
      CLOCKED_OUT, at most one open clock-in per profile. Deliberately no
      payroll, timesheet, overtime, leave, absence, GPS/geofence, biometric,
      QR, offline, or supervisor-administration surface.
  - name: Security Logbook
    description: >-
      CR-BE-MOB-05 PART 03 — Security Operational Logbook. Backend-
      authoritative, Building-scoped Security logbook entries for mobile
      Security operations: create / list / detail / update (OPEN only).
      Each entry binds the authenticated Workforce Profile (BE-03C), the
      Building it belongs to (BE-02F/G access-asserted), and an optional
      existing authoritative `shift_handovers.id` in the SAME Building.
      Identity is resolved from the authenticated session, never from the
      client; `recordedAt` is set by the backend database clock only.
      Lifecycle: OPEN → CLOSED (CLOSED is terminal). Category is a fixed
      classification vocabulary only — no patrol / incident / finding
      engine is implied. Deliberately no patrol, checkpoint, incident,
      visitor, attendance, QR, GPS, push, offline, dashboard, or
      Management Read Model surface.
  - name: Corrective Action Verification
    description: >-
      CR-BE-MOB-03 PART 03 — existing BE-21J corrective-action verification
      contract for the mobile Teknisi supervisor. Publish-only: every
      operation below is an existing runtime route; a verification IS a
      shared BE-07 `reviews` row with target_type CORRECTIVE_ACTION — there
      is no new verification engine, no redesigned workflow, and the
      decision vocabulary (APPROVED / REJECTED / REWORK_REQUIRED) is the
      shared review primitive's. `corrective_action_verification.read`
      sees the context/result/history; only `.manage` may open a
      verification or record a decision. Building isolation (BE-02G) is
      asserted on the corrective action's Building for every operation.
  - name: Integration Webhooks
    description: >
      CR-BE-INTEG-01 outbound integration platform. Client-scoped webhook
      endpoint configuration (optional Building narrowing) plus the read-only
      per-endpoint delivery history over the transactional-outbox pipeline
      (`operational_events` → outbox snapshot → fan-out → HMAC-signed POST
      with bounded retry). SIGNING SECRETS are server-generated (`whsec_` +
      32 random bytes, base64url) and disclosed EXACTLY ONCE — in the create
      (201) and rotate-secret responses; no other read/list/update response
      ever carries a secret, and delivery history exposes state / attempt /
      timing / response metadata only (never payload bodies).

      Outbound request contract (receiver side): every delivery is an HTTPS
      POST whose body is the canonical event envelope
      `{ id, type, occurredAt, clientId, buildingId, entity{type,id},
      summary, metadata }` serialized once at enqueue time (byte-stable
      across attempts and endpoints), with headers
      `Content-Type: application/json`, `User-Agent: Asentra-Webhook/1`,
      `X-Asentra-Timestamp` (Unix seconds at send time),
      `X-Asentra-Delivery-Id` (stable idempotency key across retries),
      `X-Asentra-Event-Id`, `X-Asentra-Event-Type`, and
      `X-Asentra-Signature: v1=<hex>`.

      Receiver verification recipe: (1) read the RAW request body bytes —
      never re-serialize parsed JSON; (2) compute
      `HMAC_SHA256(secret, "<X-Asentra-Timestamp>.<X-Asentra-Delivery-Id>."
      + raw_body)` and hex-encode it; (3) compare against the value after
      `v1=` with a constant-time comparison; (4) reject when
      `|now − X-Asentra-Timestamp|` exceeds 300 seconds (replay protection);
      (5) de-duplicate on `X-Asentra-Delivery-Id` — retries reuse the same
      delivery id and byte-identical body by design (at-least-once
      delivery); (6) respond 2xx to acknowledge. Any non-2xx is a failure:
      408/425/429/5xx and timeouts are retried with exponential backoff
      (5 attempts); other 4xx and 3xx (redirects are never followed) are
      permanent. Per-endpoint ordering is NOT guaranteed — key on
      `occurredAt`/event id, not arrival order.

  - name: Basic Financial Reporting
    description: >
      CR-BE-FIN-RPT-01 — Currency-safe basic financial reporting. BE-19I
      lightweight query/read-model aggregation only; no persisted ledger or
      accounting model. All monetary totals are grouped by exact
      currency_code (currency for utility_bills). No cross-currency
      arithmetic without explicit conversion. UNKNOWN currency (historical
      NULL) is readable, countable, amount-measurable, source-traceable,
      excluded from known-currency totals and net arithmetic, surfaced
      explicitly as reporting gap. Payment/outstanding amounts inherit invoice
      currency and are grouped by that currency. Single known currency + zero
      UNKNOWN may populate legacy scalar convenience fields; otherwise those
      scalars are null and monetarySummary.byCurrency is authoritative. No FX
      conversion in base report; FX remains read-side optional additive only.

# No global security requirement: public endpoints (health, login,
# invitation acceptance) opt out explicitly. All other documented
# endpoints require the bearer session token.
security: []

paths:
  /health:
    get:
      operationId: getHealth
      summary: Process liveness probe
      tags: [Health]
      responses:
        "200":
          description: Process is alive.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/HealthStatus"

  /health/database:
    get:
      operationId: getDatabaseHealth
      summary: Database readiness probe (SELECT 1)
      tags: [Health]
      responses:
        "200":
          description: Database connection verified.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/DatabaseHealth"
        "503":
          $ref: "#/components/responses/DatabaseUnavailable"

  /auth/login:
    post:
      operationId: login
      summary: Authenticate with email and password
      description: >
        Public. Returns an opaque session token plus the user. The token is
        sent on subsequent requests as `Authorization: Bearer <token>`; only
        its SHA-256 hash is stored server-side and the raw token is never
        returned again. Session lifetime is bounded by `expiresAt`
        (SESSION_TTL_MINUTES, default 480). The password policy is NOT
        enforced at login (policy applies only at set/change time). Unknown
        email, wrong password and non-login-allowed account states all return
        the same 401 `INVALID_CREDENTIALS` to avoid account enumeration.
      tags: [Authentication]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Authenticated successfully.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/LoginResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/InvalidCredentials"
        "429":
          $ref: "#/components/responses/RateLimited"

  /auth/me:
    get:
      operationId: getEffectiveUserContext
      summary: Effective user context
      description: >
        The single authoritative context of the authenticated user: identity,
        effective roles and permissions, reachable Client/Property/Building
        hierarchy and effective module entitlements. Web and Mobile must treat
        this as the source of truth for what the user can do and reach. This
        is also the authenticated-user / session-validation probe: it requires
        only a valid bearer session (no permission) and returns 401 with
        `AUTHENTICATION_REQUIRED` / `INVALID_SESSION` / `SESSION_EXPIRED` when
        the session is missing, invalid, or expired. There is no separate
        session-state endpoint.
      tags: [Effective Context]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Effective context resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EffectiveUserContext"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /auth/logout:
    post:
      operationId: logout
      summary: Revoke the current session
      description: >
        Revokes the calling session (the row is marked REVOKED, never
        deleted); the token becomes unusable immediately. A re-used or
        already-revoked token yields 401. There is no token refresh endpoint:
        to continue working the client re-authenticates via `POST /auth/login`.
      tags: [Authentication]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Session revoked.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/LogoutResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /auth/audit-events:
    get:
      operationId: listAuthAuditEvents
      summary: List authentication audit events
      description: >
        Paginated authentication audit trail. Requires the
        `auth.audit.read` permission. `meta.limit`, `meta.offset` and
        `meta.total` follow the platform pagination contract.
      tags: [Authentication]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/OffsetParam"
      responses:
        "200":
          description: Audit events page.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/AuditEvent"
                      meta:
                        $ref: "#/components/schemas/PaginationMeta"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /auth/me/buildings:
    get:
      operationId: getMyBuildings
      summary: Current user building assignments
      description: >
        The authenticated user's own active Building contexts
        (Building → Property → Client). Authentication only; no management
        permission required.
      tags: [Effective Context]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Accessible building contexts.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/UserBuildingContext"
        "401":
          $ref: "#/components/responses/Unauthorized"

  # --------------------------------------------------------------------
  # CR-BE-MOB-CONTRACT-01 PART 02 — User, Role, Permission & Building
  # Context. Existing backend-authoritative management routes, published
  # for the mobile context-resolution chain
  # User → Role → Permission → Client → Property → Building.
  # --------------------------------------------------------------------
  /users:
    post:
      operationId: createUser
      summary: Create a user
      description: >
        Requires `user.manage`. Creates a user with a normalized email
        (trimmed + lowercased); status defaults to ACTIVE.
      tags: [Users]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateUserRequest" }
      responses:
        "201":
          description: User created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicUser" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /users/{userId}:
    get:
      operationId: getUser
      summary: Get a user by id
      description: Requires `user.read`. Returns the public user identity.
      tags: [Users]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
      responses:
        "200":
          description: User record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicUser" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /users/{userId}/deactivate:
    post:
      operationId: deactivateUser
      summary: Deactivate a user
      description: Requires `user.manage`. Revokes active sessions for the user.
      tags: [Users]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
      responses:
        "200":
          description: User deactivated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicUser" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /users/{userId}/suspend:
    post:
      operationId: suspendUser
      summary: Suspend a user
      description: Requires `user.manage`. Revokes active sessions for the user.
      tags: [Users]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
      responses:
        "200":
          description: User suspended.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicUser" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /users/{userId}/reactivate:
    post:
      operationId: reactivateUser
      summary: Reactivate a user
      description: Requires `user.manage`.
      tags: [Users]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
      responses:
        "200":
          description: User reactivated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicUser" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /users/{userId}/whatsapp-contact:
    patch:
      operationId: updateUserWhatsAppContact
      summary: Update a user's WhatsApp contact and consent
      description: >
        CR-BE-NOTIFY-PROV-01 PART 06. Requires `user.manage`. Sets the
        authoritative E.164 WhatsApp number and/or stamps explicit consent
        (OPT_IN / OPT_OUT). OPT_IN requires a phone; clearing the phone
        clears consent; one account per number (409
        USER_WHATSAPP_PHONE_ALREADY_EXISTS). Consent gates outbound WhatsApp
        intents: only ACTIVE users with active consent are messaged.
      tags: [Users]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              { $ref: "#/components/schemas/UpdateUserWhatsAppContactRequest" }
      responses:
        "200":
          description: Contact/consent updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicUser" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /roles:
    post:
      operationId: createRole
      summary: Create a role
      description: Requires `role.manage`. Code is normalized to uppercase.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateRoleRequest" }
      responses:
        "201":
          description: Role created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicRole" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listRoles
      summary: List roles
      description: Requires `role.read`.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Role list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicRole" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /roles/{roleId}:
    get:
      operationId: getRole
      summary: Get a role by id
      description: Requires `role.read`.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/RoleIdPath"
      responses:
        "200":
          description: Role record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicRole" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /users/{userId}/roles:
    post:
      operationId: assignRoleToUser
      summary: Assign an ACTIVE role to a user
      description: >
        Requires `role.manage`. Returns the assigned role. Rejects unknown
        users, unknown/inactive roles, and duplicate active assignments.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AssignRoleRequest" }
      responses:
        "201":
          description: Role assigned.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicRole" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listUserRoles
      summary: List the ACTIVE roles assigned to a user
      description: >
        Requires `role.read`. For the authenticated user's own effective
        roles, prefer `GET /auth/me` → `access.roles`.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
      responses:
        "200":
          description: User roles.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicRole" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /permissions:
    post:
      operationId: createPermission
      summary: Create a permission
      description: Requires `permission.manage`. Code is normalized to lowercase.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePermissionRequest" }
      responses:
        "201":
          description: Permission created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicPermission" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listPermissions
      summary: List permissions
      description: Requires `permission.read`.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Permission list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicPermission" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /permissions/{permissionId}:
    get:
      operationId: getPermission
      summary: Get a permission by id
      description: Requires `permission.read`.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermissionIdPath"
      responses:
        "200":
          description: Permission record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicPermission" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /roles/{roleId}/permissions:
    post:
      operationId: assignPermissionToRole
      summary: Assign an ACTIVE permission to an ACTIVE role
      description: >
        Requires `permission.manage`. Returns the assigned permission.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/RoleIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AssignPermissionRequest" }
      responses:
        "201":
          description: Permission assigned.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicPermission" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listRolePermissions
      summary: List the ACTIVE permissions assigned to a role
      description: Requires `permission.read`.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/RoleIdPath"
      responses:
        "200":
          description: Role permissions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicPermission" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /clients:
    post:
      operationId: createClient
      summary: Create a Client
      description: Requires `client.manage`.
      tags: [Clients]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateClientRequest" }
      responses:
        "201":
          description: Client created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicClient" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listClients
      summary: List Clients
      description: >
        Requires `client.read`. For the authenticated user's reachable
        hierarchy prefer `GET /auth/me` → `context.clients`.
      tags: [Clients]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Client list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicClient" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /clients/{clientId}:
    get:
      operationId: getClient
      summary: Get a Client by id
      description: Requires `client.read`.
      tags: [Clients]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      responses:
        "200":
          description: Client record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicClient" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateClient
      summary: Update a Client
      description: Requires `client.manage`.
      tags: [Clients]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateClientRequest" }
      responses:
        "200":
          description: Client updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicClient" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /properties:
    post:
      operationId: createProperty
      summary: Create a Property
      description: Requires `property.manage`.
      tags: [Properties]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePropertyRequest" }
      responses:
        "201":
          description: Property created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicProperty" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listProperties
      summary: List Properties
      description: Requires `property.read`. Optionally filtered by `clientId`.
      tags: [Properties]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdQuery"
      responses:
        "200":
          description: Property list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicProperty" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /properties/{propertyId}:
    get:
      operationId: getProperty
      summary: Get a Property by id
      description: Requires `property.read`.
      tags: [Properties]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PropertyIdPath"
      responses:
        "200":
          description: Property record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicProperty" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /properties/{propertyId}/status:
    patch:
      operationId: updatePropertyStatus
      summary: Update a Property status
      description: Requires `property.manage`.
      tags: [Properties]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PropertyIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdatePropertyStatusRequest" }
      responses:
        "200":
          description: Property updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicProperty" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /clients/{clientId}/properties:
    get:
      operationId: listClientProperties
      summary: List Properties under a Client
      description: Requires `property.read`.
      tags: [Properties]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      responses:
        "200":
          description: Property list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicProperty" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /buildings:
    post:
      operationId: createBuilding
      summary: Create a Building
      description: Requires `building.manage`.
      tags: [Buildings]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateBuildingRequest" }
      responses:
        "201":
          description: Building created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicBuilding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildings
      summary: List Buildings
      description: >
        Requires `building.read`. Optionally filtered by `propertyId`. For the
        authenticated user's accessible set prefer `GET /auth/me` →
        `scope.buildingIds` and `GET /auth/me/buildings`.
      tags: [Buildings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PropertyIdQuery"
      responses:
        "200":
          description: Building list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicBuilding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /buildings/{buildingId}:
    get:
      operationId: getBuilding
      summary: Get a Building by id
      description: >
        Requires `building.read` AND an ACTIVE Building assignment for the
        caller (BE-02G). Inaccessible Building → 403 BUILDING_ACCESS_DENIED.
      tags: [Buildings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Building record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicBuilding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/status:
    patch:
      operationId: updateBuildingStatus
      summary: Update a Building status
      description: Requires `building.manage`.
      tags: [Buildings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateBuildingStatusRequest" }
      responses:
        "200":
          description: Building updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicBuilding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /properties/{propertyId}/buildings:
    get:
      operationId: listPropertyBuildings
      summary: List Buildings under a Property
      description: Requires `building.read`.
      tags: [Buildings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PropertyIdPath"
      responses:
        "200":
          description: Building list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicBuilding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /users/{userId}/buildings:
    post:
      operationId: assignBuildingToUser
      summary: Assign a Building to a user
      description: >
        Requires `building.manage`. Creates an ACTIVE User ↔ Building
        assignment (the BE-02G source for `GET /auth/me/buildings`).
      tags: [Building Assignments]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateBuildingAssignmentRequest" }
      responses:
        "201":
          description: Assignment created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicUserBuildingAssignment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listUserBuildings
      summary: List a user's Building assignments
      description: >
        Requires `building.read`. For the authenticated user's own accessible
        set prefer `GET /auth/me/buildings` (authentication only).
      tags: [Building Assignments]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
      responses:
        "200":
          description: Assignment list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicUserBuildingAssignment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /users/{userId}/buildings/{buildingId}:
    delete:
      operationId: deactivateBuildingAssignment
      summary: Deactivate a user's Building assignment
      description: Requires `building.manage`.
      tags: [Building Assignments]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Assignment deactivated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicUserBuildingAssignment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # --------------------------------------------------------------------
  # BE-27A — Client Configuration. Client scope only; no Building
  # override, configuration version, preview, or publish lifecycle.
  # --------------------------------------------------------------------
  /clients/{clientId}/configurations:
    post:
      operationId: createClientConfiguration
      summary: Create a Client configuration record
      description: >
        Creates one stable key/value record for an accessible Client. Keys are
        normalized to uppercase and unique per Client. Requires
        `client_configuration.manage`; permission alone does not bypass the
        caller's BE-02G Client scope.
      tags: [Client Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateClientConfigurationRequest"
      responses:
        "201":
          description: Client configuration created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ClientConfiguration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
    get:
      operationId: listClientConfigurations
      summary: List Client configuration records
      description: >
        Lists configuration administration records for one accessible Client,
        ordered by key. Requires `client_configuration.read` and Client scope.
      tags: [Client Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/ClientConfigurationStatus"
      responses:
        "200":
          description: Client configuration records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/ClientConfiguration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /clients/{clientId}/configurations/effective:
    get:
      operationId: getEffectiveClientConfiguration
      summary: Resolve effective Client configuration
      description: >
        Returns only ACTIVE key/value records for one accessible Client as an
        authoritative key/value object. BE-27A has no Building override,
        entitlement projection, version, preview, or publish behavior.
        Requires `client_configuration.read` and Client scope.
      tags: [Client Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      responses:
        "200":
          description: Effective Client configuration.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EffectiveClientConfiguration"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /client-configurations/{clientConfigurationId}:
    get:
      operationId: getClientConfiguration
      summary: Get a Client configuration record
      description: >
        Reads one administration record after checking its owning Client
        against the caller's scope. Requires `client_configuration.read`.
      tags: [Client Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ClientConfigurationIdPath"
      responses:
        "200":
          description: Client configuration record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ClientConfiguration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateClientConfiguration
      summary: Update a Client configuration value or status
      description: >
        Updates only `value` and/or record availability status. Client and key
        are immutable. Requires `client_configuration.manage` and Client scope.
      tags: [Client Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ClientConfigurationIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateClientConfigurationRequest"
      responses:
        "200":
          description: Client configuration updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ClientConfiguration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # --------------------------------------------------------------------
  # BE-27B — Building Configuration. ACTIVE Building values override the
  # effective BE-27A Client values for the same normalized key.
  # --------------------------------------------------------------------
  /buildings/{buildingId}/configurations:
    post:
      operationId: createBuildingConfiguration
      summary: Create a Building configuration record
      description: >
        Creates one key/value record for an accessible Building. Client context
        is derived through Building → Property → Client and is never accepted
        from the request body. Requires `building_configuration.manage` and
        explicit BE-02G Building scope.
      tags: [Building Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateBuildingConfigurationRequest"
      responses:
        "201":
          description: Building configuration created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/BuildingConfiguration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
    get:
      operationId: listBuildingConfigurations
      summary: List Building configuration records
      description: >
        Lists administration records for one explicitly accessible Building,
        ordered by key. Requires `building_configuration.read`.
      tags: [Building Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/ClientConfigurationStatus"
      responses:
        "200":
          description: Building configuration records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/BuildingConfiguration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /buildings/{buildingId}/configurations/effective:
    get:
      operationId: getEffectiveBuildingConfiguration
      summary: Resolve effective Building configuration
      description: >
        Resolves ACTIVE BE-27A Client values and overlays ACTIVE values for the
        selected Building; the Building value wins on key collision. Inactive
        Building records do not suppress Client fallback. Requires
        `building_configuration.read` and explicit Building scope.
      tags: [Building Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Effective Client→Building configuration.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EffectiveBuildingConfiguration"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /building-configurations/{buildingConfigurationId}:
    get:
      operationId: getBuildingConfiguration
      summary: Get a Building configuration record
      description: >
        Reads one administration record after checking its owning Building
        against the caller's explicit scope. Requires
        `building_configuration.read`.
      tags: [Building Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingConfigurationIdPath"
      responses:
        "200":
          description: Building configuration record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/BuildingConfiguration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateBuildingConfiguration
      summary: Update a Building configuration value or status
      description: >
        Updates only value and/or record availability status. Building, derived
        Client, and key are immutable. Requires `building_configuration.manage`
        and explicit Building scope.
      tags: [Building Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingConfigurationIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateBuildingConfigurationRequest"
      responses:
        "200":
          description: Building configuration updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/BuildingConfiguration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # --------------------------------------------------------------------
  # BE-27C — Client / Building scoped Module Configuration.
  # --------------------------------------------------------------------
  /clients/{clientId}/module-configurations:
    post:
      operationId: createClientModuleConfiguration
      summary: Configure a Module for a Client
      description: >
        Binds an existing ACTIVE Module key to an accessible Client with an
        enabled/disabled intent. Requires `module_configuration.manage`.
        This does not create a Module or Entitlement.
      tags: [Module Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateModuleConfigurationRequest"
      responses:
        "201":
          description: Client Module configuration created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ModuleConfiguration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
    get:
      operationId: listClientModuleConfigurations
      summary: List Client Module configuration records
      description: Requires `module_configuration.read` and accessible Client scope.
      tags: [Module Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      responses:
        "200":
          description: Client Module configuration records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/ModuleConfiguration"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /clients/{clientId}/module-configurations/effective:
    get:
      operationId: getEffectiveClientModuleConfiguration
      summary: Resolve effective Client Module configuration
      description: >
        Intersects configured Client enablement with the existing effective
        Subscription/License/Module Entitlement resolver. Unconfigured Modules
        are not implicitly enabled. Requires `module_configuration.read`.
      tags: [Module Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      responses:
        "200":
          description: Effective Client Module configuration.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EffectiveModuleConfiguration"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /buildings/{buildingId}/module-configurations:
    post:
      operationId: createBuildingModuleConfiguration
      summary: Configure a Module for a Building
      description: >
        Binds an existing ACTIVE Module key to an explicitly accessible
        Building. Client context is derived through Building → Property →
        Client. Requires `module_configuration.manage`.
      tags: [Module Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateModuleConfigurationRequest"
      responses:
        "201":
          description: Building Module configuration created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ModuleConfiguration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
    get:
      operationId: listBuildingModuleConfigurations
      summary: List Building Module configuration records
      description: Requires `module_configuration.read` and explicit Building scope.
      tags: [Module Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Building Module configuration records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/ModuleConfiguration"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /buildings/{buildingId}/module-configurations/effective:
    get:
      operationId: getEffectiveBuildingModuleConfiguration
      summary: Resolve effective Building Module configuration
      description: >
        Applies Building Module intent over Client Module intent for the same
        Module, then intersects the result with existing effective commercial
        Entitlements. Requires `module_configuration.read` and explicit
        Building scope.
      tags: [Module Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Effective Client→Building Module configuration.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EffectiveModuleConfiguration"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /module-configurations/{moduleConfigurationId}:
    get:
      operationId: getModuleConfiguration
      summary: Get a Module configuration record
      description: >
        Requires `module_configuration.read` and the record's Client or
        Building scope.
      tags: [Module Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ModuleConfigurationIdPath"
      responses:
        "200":
          description: Module configuration record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ModuleConfiguration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateModuleConfiguration
      summary: Enable or disable a Module configuration
      description: >
        Updates configured intent only. Scope and Module reference are
        immutable. Requires `module_configuration.manage` and record scope.
      tags: [Module Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ModuleConfigurationIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateModuleConfigurationRequest"
      responses:
        "200":
          description: Module configuration updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ModuleConfiguration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # BE-27D — Feature Entitlement Configuration
  /clients/{clientId}/feature-entitlements:
    post:
      operationId: createClientFeatureEntitlement
      summary: Configure a Feature for a Client
      description: Requires `feature_entitlement_configuration.manage`; references an existing ACTIVE Module.
      tags: [Feature Entitlement Configuration]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateFeatureEntitlementRequest" }
      responses:
        "201":
          description: Client Feature configuration created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FeatureEntitlementConfiguration" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listClientFeatureEntitlements
      summary: List Client Feature configurations
      tags: [Feature Entitlement Configuration]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      responses:
        "200":
          description: Client Feature configuration records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/FeatureEntitlementConfiguration" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /clients/{clientId}/feature-entitlements/effective:
    get:
      operationId: getEffectiveClientFeatureEntitlements
      summary: Resolve effective Client Feature entitlements
      description: Feature ENABLED requires its BE-27C effective Module to be enabled.
      tags: [Feature Entitlement Configuration]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      responses:
        "200":
          description: Effective Client Feature entitlements.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/EffectiveFeatureEntitlementConfiguration" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /buildings/{buildingId}/feature-entitlements:
    post:
      operationId: createBuildingFeatureEntitlement
      summary: Configure a Feature for a Building
      description: Client context is derived from the explicitly accessible Building.
      tags: [Feature Entitlement Configuration]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateFeatureEntitlementRequest" }
      responses:
        "201":
          description: Building Feature configuration created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FeatureEntitlementConfiguration" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildingFeatureEntitlements
      summary: List Building Feature configurations
      tags: [Feature Entitlement Configuration]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Building Feature configuration records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/FeatureEntitlementConfiguration" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /buildings/{buildingId}/feature-entitlements/effective:
    get:
      operationId: getEffectiveBuildingFeatureEntitlements
      summary: Resolve effective Building Feature entitlements
      description: Building state overrides Client state, then BE-27C Module availability is enforced.
      tags: [Feature Entitlement Configuration]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Effective Building Feature entitlements.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/EffectiveFeatureEntitlementConfiguration" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /feature-entitlements/{featureEntitlementConfigurationId}:
    get:
      operationId: getFeatureEntitlementConfiguration
      summary: Get a Feature entitlement configuration
      tags: [Feature Entitlement Configuration]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/FeatureEntitlementConfigurationIdPath"
      responses:
        "200":
          description: Feature entitlement configuration record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FeatureEntitlementConfiguration" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateFeatureEntitlementConfiguration
      summary: Update configured Feature state
      tags: [Feature Entitlement Configuration]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/FeatureEntitlementConfigurationIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateFeatureEntitlementRequest" }
      responses:
        "200":
          description: Feature entitlement configuration updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FeatureEntitlementConfiguration" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # BE-27E — Navigation Registry
  /clients/{clientId}/navigation-items:
    post:
      operationId: createClientNavigationItem
      summary: Create Client navigation metadata
      tags: [Navigation Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CreateNavigationItemRequest" } } }
      responses:
        "201": { description: Navigation item created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/NavigationItem" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listClientNavigationItems
      summary: List Client navigation metadata
      tags: [Navigation Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Navigation items, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/NavigationItem" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /clients/{clientId}/navigation/effective:
    get:
      operationId: getEffectiveClientNavigation
      summary: Resolve effective Client navigation
      description: Filters enabled items by effective Module, Feature, Permission and parent visibility.
      tags: [Navigation Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Effective navigation, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveNavigation" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /buildings/{buildingId}/navigation-items:
    post:
      operationId: createBuildingNavigationItem
      summary: Create Building navigation metadata
      tags: [Navigation Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CreateNavigationItemRequest" } } }
      responses:
        "201": { description: Navigation item created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/NavigationItem" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildingNavigationItems
      summary: List Building navigation metadata
      tags: [Navigation Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Navigation items, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/NavigationItem" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /buildings/{buildingId}/navigation/effective:
    get:
      operationId: getEffectiveBuildingNavigation
      summary: Resolve effective Building navigation
      description: Building keys override Client keys before authority filtering.
      tags: [Navigation Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Effective navigation, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveNavigation" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /navigation-items/{navigationItemId}:
    get:
      operationId: getNavigationItem
      summary: Get navigation metadata
      tags: [Navigation Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/NavigationItemIdPath" }]
      responses:
        "200": { description: Navigation item, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/NavigationItem" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateNavigationItem
      summary: Update navigation presentation metadata
      tags: [Navigation Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/NavigationItemIdPath" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/UpdateNavigationItemRequest" } } }
      responses:
        "200": { description: Navigation item updated, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/NavigationItem" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # BE-27F — Workspace Registry
  /clients/{clientId}/workspaces:
    post:
      operationId: createClientWorkspace
      summary: Create Client workspace metadata
      tags: [Workspace Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateWorkspaceRequest" } } } }
      responses:
        "201": { description: Workspace created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/Workspace" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listClientWorkspaces
      summary: List Client workspaces
      tags: [Workspace Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Workspaces, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/Workspace" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /clients/{clientId}/workspaces/effective:
    get:
      operationId: getEffectiveClientWorkspaces
      summary: Resolve effective Client workspaces
      tags: [Workspace Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Effective workspaces, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveWorkspaces" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /buildings/{buildingId}/workspaces:
    post:
      operationId: createBuildingWorkspace
      summary: Create Building workspace metadata
      tags: [Workspace Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateWorkspaceRequest" } } } }
      responses:
        "201": { description: Workspace created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/Workspace" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildingWorkspaces
      summary: List Building workspaces
      tags: [Workspace Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Workspaces, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/Workspace" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /buildings/{buildingId}/workspaces/effective:
    get:
      operationId: getEffectiveBuildingWorkspaces
      summary: Resolve effective Building workspaces
      tags: [Workspace Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Effective workspaces, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveWorkspaces" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /workspaces/{workspaceId}:
    get:
      operationId: getWorkspace
      summary: Get workspace metadata
      tags: [Workspace Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkspaceIdPath" }]
      responses:
        "200": { description: Workspace, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/Workspace" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateWorkspace
      summary: Update workspace metadata
      tags: [Workspace Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkspaceIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateWorkspaceRequest" } } } }
      responses:
        "200": { description: Workspace updated, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/Workspace" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # BE-27G — Dashboard / Widget Configuration
  /clients/{clientId}/dashboards:
    post:
      operationId: createClientDashboard
      summary: Create Client Dashboard configuration
      tags: [Dashboard Configuration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateDashboardRequest" } } } }
      responses:
        "201": { description: Dashboard created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/Dashboard" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listClientDashboards
      summary: List Client Dashboards
      tags: [Dashboard Configuration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Dashboards, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/Dashboard" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /clients/{clientId}/dashboards/effective:
    get:
      operationId: getEffectiveClientDashboards
      summary: Resolve effective Client Dashboard/Widget composition
      tags: [Dashboard Configuration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Effective Dashboards, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveDashboards" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /buildings/{buildingId}/dashboards:
    post:
      operationId: createBuildingDashboard
      summary: Create Building Dashboard configuration
      tags: [Dashboard Configuration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateDashboardRequest" } } } }
      responses:
        "201": { description: Dashboard created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/Dashboard" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildingDashboards
      summary: List Building Dashboards
      tags: [Dashboard Configuration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Dashboards, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/Dashboard" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /buildings/{buildingId}/dashboards/effective:
    get:
      operationId: getEffectiveBuildingDashboards
      summary: Resolve effective Building Dashboard/Widget composition
      tags: [Dashboard Configuration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Effective Dashboards, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveDashboards" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /dashboards/{dashboardId}:
    get:
      operationId: getDashboard
      summary: Get Dashboard configuration
      tags: [Dashboard Configuration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/DashboardIdPath" }]
      responses:
        "200": { description: Dashboard, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/Dashboard" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateDashboard
      summary: Update Dashboard metadata
      tags: [Dashboard Configuration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/DashboardIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateDashboardRequest" } } } }
      responses:
        "200": { description: Dashboard updated, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/Dashboard" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /dashboards/{dashboardId}/widgets:
    post:
      operationId: createDashboardWidget
      summary: Add Widget composition metadata
      description: sourceReference must be an approved existing BE-23/BE-24 read model; no calculation is executed.
      tags: [Dashboard Configuration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/DashboardIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateDashboardWidgetRequest" } } } }
      responses:
        "201": { description: Widget created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/DashboardWidget" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listDashboardWidgets
      summary: List Dashboard Widgets
      tags: [Dashboard Configuration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/DashboardIdPath" }]
      responses:
        "200": { description: Widgets, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/DashboardWidget" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /dashboard-widgets/{dashboardWidgetId}:
    get:
      operationId: getDashboardWidget
      summary: Get Dashboard Widget metadata
      tags: [Dashboard Configuration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/DashboardWidgetIdPath" }]
      responses:
        "200": { description: Widget, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/DashboardWidget" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateDashboardWidget
      summary: Update Dashboard Widget metadata/order/state
      tags: [Dashboard Configuration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/DashboardWidgetIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateDashboardWidgetRequest" } } } }
      responses:
        "200": { description: Widget updated, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/DashboardWidget" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # BE-27H — Dynamic Form Administration over BE-07
  /clients/{clientId}/form-administration/templates:
    post:
      operationId: createClientAdminForm
      summary: Create Client-bound Form Template definition
      tags: [Form Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateAdminFormTemplateRequest" } } } }
      responses:
        "201": { description: Form definition created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/AdminFormDefinition" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listClientAdminForms
      summary: List Client-bound Form definitions
      tags: [Form Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Form definitions, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/AdminFormDefinition" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /buildings/{buildingId}/form-administration/templates:
    post:
      operationId: createBuildingAdminForm
      summary: Create Building-bound Form Template definition
      tags: [Form Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateAdminFormTemplateRequest" } } } }
      responses:
        "201": { description: Form definition created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/AdminFormDefinition" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    get:
      operationId: listBuildingAdminForms
      summary: List Building-bound Form definitions
      tags: [Form Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Form definitions, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/AdminFormDefinition" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /clients/{clientId}/form-administration/templates/{formTemplateId}/binding:
    post:
      operationId: bindClientAdminForm
      summary: Bind existing Form Template to Client configuration
      tags: [Form Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }, { $ref: "#/components/parameters/FormTemplateIdPath" }]
      responses:
        "201": { description: Binding created }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /buildings/{buildingId}/form-administration/templates/{formTemplateId}/binding:
    post:
      operationId: bindBuildingAdminForm
      summary: Bind existing Form Template to Building configuration
      tags: [Form Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }, { $ref: "#/components/parameters/FormTemplateIdPath" }]
      responses:
        "201": { description: Binding created }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /form-administration/templates/{formTemplateId}:
    get:
      operationId: getAdminFormDefinition
      summary: Get Form definition with bindings, Sections and Fields
      tags: [Form Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/FormTemplateIdPath" }]
      responses:
        "200": { description: Form definition, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/AdminFormDefinition" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateAdminFormTemplate
      summary: Update Form metadata/status through existing Form Template service
      tags: [Form Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/FormTemplateIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateAdminFormTemplateRequest" } } } }
      responses:
        "200": { description: Updated Form definition }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /form-administration/templates/{formTemplateId}/sections:
    post:
      operationId: createAdminFormSection
      summary: Add existing-model Form Section
      tags: [Form Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/FormTemplateIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateAdminFormSectionRequest" } } } }
      responses:
        "201": { description: Section created }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /form-administration/sections/{formSectionId}:
    patch:
      operationId: updateAdminFormSection
      summary: Update existing-model Form Section
      tags: [Form Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/FormSectionIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateAdminFormSectionRequest" } } } }
      responses:
        "200": { description: Section updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /form-administration/sections/{formSectionId}/fields:
    post:
      operationId: createAdminFormField
      summary: Add existing-model Form Field configuration
      tags: [Form Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/FormSectionIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateAdminFormFieldRequest" } } } }
      responses:
        "201": { description: Field created }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /form-administration/fields/{formFieldId}:
    patch:
      operationId: updateAdminFormField
      summary: Update existing-model Form Field configuration
      tags: [Form Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/FormFieldIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateAdminFormFieldRequest" } } } }
      responses:
        "200": { description: Field updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  # BE-27I — Checklist Administration over the existing BE-07 Checklist engine
  /clients/{clientId}/checklist-administration/templates:
    post:
      operationId: createClientAdminChecklist
      summary: Create a Client-bound Checklist definition
      tags: [Checklist Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateAdminChecklistTemplateRequest" } } } }
      responses:
        "201": { description: Checklist definition created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/AdminChecklistDefinition" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listClientAdminChecklists
      summary: List Client-bound Checklist definitions
      tags: [Checklist Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Checklist definitions, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/AdminChecklistDefinition" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /buildings/{buildingId}/checklist-administration/templates:
    post:
      operationId: createBuildingAdminChecklist
      summary: Create a Building-bound Checklist definition
      tags: [Checklist Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateAdminChecklistTemplateRequest" } } } }
      responses:
        "201": { description: Checklist definition created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/AdminChecklistDefinition" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildingAdminChecklists
      summary: List Building-bound Checklist definitions
      tags: [Checklist Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Checklist definitions, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/AdminChecklistDefinition" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /clients/{clientId}/checklist-administration/templates/{checklistTemplateId}/binding:
    post:
      operationId: bindClientAdminChecklist
      summary: Bind an existing Checklist Template to Client configuration
      tags: [Checklist Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }, { $ref: "#/components/parameters/ChecklistTemplateIdPath" }]
      responses:
        "201": { description: Binding created }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /buildings/{buildingId}/checklist-administration/templates/{checklistTemplateId}/binding:
    post:
      operationId: bindBuildingAdminChecklist
      summary: Bind an existing Checklist Template to Building configuration
      tags: [Checklist Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }, { $ref: "#/components/parameters/ChecklistTemplateIdPath" }]
      responses:
        "201": { description: Binding created }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /checklist-administration/templates/{checklistTemplateId}:
    get:
      operationId: getAdminChecklistDefinition
      summary: Get Checklist definition with bindings and Items
      tags: [Checklist Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ChecklistTemplateIdPath" }]
      responses:
        "200": { description: Checklist definition, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/AdminChecklistDefinition" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateAdminChecklistTemplate
      summary: Update existing Checklist metadata or status
      tags: [Checklist Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ChecklistTemplateIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateAdminChecklistTemplateRequest" } } } }
      responses:
        "200": { description: Checklist definition updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /checklist-administration/templates/{checklistTemplateId}/items:
    post:
      operationId: createAdminChecklistItem
      summary: Add an Item to the existing Checklist definition
      description: Measurement, evidence, and verification configuration remain authoritative in their existing APIs.
      tags: [Checklist Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ChecklistTemplateIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateAdminChecklistItemRequest" } } } }
      responses:
        "201": { description: Checklist Item created }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /checklist-administration/items/{checklistItemId}:
    patch:
      operationId: updateAdminChecklistItem
      summary: Update core existing Checklist Item configuration
      description: Measurement metadata is read-only here; evidence and verification remain in their existing APIs.
      tags: [Checklist Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ChecklistItemIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateAdminChecklistItemRequest" } } } }
      responses:
        "200": { description: Checklist Item updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # BE-27J — typed Operational Settings facade over BE-27A/BE-27B
  /clients/{clientId}/operational-settings:
    post:
      operationId: createClientOperationalSetting
      summary: Create an allowlisted Client Operational Setting
      description: Requires `client_configuration.manage`. Settings are metadata and never bypass owning-domain validation.
      tags: [Operational Settings]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateOperationalSettingRequest" } } } }
      responses:
        "201": { description: Operational Setting created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/OperationalSetting" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listClientOperationalSettings
      summary: List Client Operational Settings
      tags: [Operational Settings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
        - $ref: "#/components/parameters/ConfigurationStatusQuery"
      responses:
        "200": { description: Operational Settings, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/OperationalSetting" } } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /clients/{clientId}/operational-settings/effective:
    get:
      operationId: getEffectiveClientOperationalSettings
      summary: Get effective ACTIVE Client Operational Settings
      tags: [Operational Settings]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Effective Operational Settings, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveOperationalSettings" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /client-operational-settings/{operationalSettingId}:
    get:
      operationId: getClientOperationalSetting
      summary: Get a Client Operational Setting
      tags: [Operational Settings]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/OperationalSettingIdPath" }]
      responses:
        "200": { description: Operational Setting, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/OperationalSetting" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateClientOperationalSetting
      summary: Update value, enabled state, or status
      tags: [Operational Settings]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/OperationalSettingIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateOperationalSettingRequest" } } } }
      responses:
        "200": { description: Operational Setting updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /buildings/{buildingId}/operational-settings:
    post:
      operationId: createBuildingOperationalSetting
      summary: Create an allowlisted Building Operational Setting
      description: Requires `building_configuration.manage`; Building ownership is derived through Property to Client.
      tags: [Operational Settings]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateOperationalSettingRequest" } } } }
      responses:
        "201": { description: Operational Setting created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/OperationalSetting" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildingOperationalSettings
      summary: List Building Operational Settings
      tags: [Operational Settings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - $ref: "#/components/parameters/ConfigurationStatusQuery"
      responses:
        "200": { description: Operational Settings, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/OperationalSetting" } } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /buildings/{buildingId}/operational-settings/effective:
    get:
      operationId: getEffectiveBuildingOperationalSettings
      summary: Get effective Client to Building Operational Settings
      description: ACTIVE Building records replace ACTIVE Client records by key; enabled false remains an explicit effective disable.
      tags: [Operational Settings]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Effective Operational Settings, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveOperationalSettings" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /building-operational-settings/{operationalSettingId}:
    get:
      operationId: getBuildingOperationalSetting
      summary: Get a Building Operational Setting
      tags: [Operational Settings]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/OperationalSettingIdPath" }]
      responses:
        "200": { description: Operational Setting, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/OperationalSetting" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateBuildingOperationalSetting
      summary: Update Building value, enabled state, or status
      tags: [Operational Settings]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/OperationalSettingIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateOperationalSettingRequest" } } } }
      responses:
        "200": { description: Operational Setting updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # BE-27K — presentation metadata over authoritative BE-03 entities
  /clients/{clientId}/organization-presentation:
    post:
      operationId: createClientOrganizationPresentation
      summary: Create Client Organization Presentation Configuration
      description: Stores display-only labels and entity references. Requires `client_configuration.manage`.
      tags: [Organization Presentation]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateOrganizationPresentationRequest" } } } }
      responses:
        "201": { description: Presentation Configuration created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/OrganizationPresentationConfiguration" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: getClientOrganizationPresentation
      summary: Get Client Organization Presentation Configuration
      tags: [Organization Presentation]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Presentation Configuration, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/OrganizationPresentationConfiguration" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /clients/{clientId}/organization-presentation/effective:
    get:
      operationId: getEffectiveClientOrganizationPresentation
      summary: Get effective Client Organization Presentation
      description: Resolves label/display-name fallbacks from authoritative BE-03 entities; inactive hierarchy entries cannot be made visible.
      tags: [Organization Presentation]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Effective Presentation, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveOrganizationPresentation" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /client-organization-presentation/{organizationPresentationId}:
    get:
      operationId: getClientOrganizationPresentationById
      summary: Get Client Presentation Configuration by id
      tags: [Organization Presentation]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/OrganizationPresentationIdPath" }]
      responses:
        "200": { description: Presentation Configuration, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/OrganizationPresentationConfiguration" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateClientOrganizationPresentation
      summary: Update Client Presentation metadata/status
      tags: [Organization Presentation]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/OrganizationPresentationIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateOrganizationPresentationRequest" } } } }
      responses:
        "200": { description: Presentation Configuration updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /buildings/{buildingId}/organization-presentation:
    post:
      operationId: createBuildingOrganizationPresentation
      summary: Create Building Organization Presentation Configuration
      description: Building ownership is derived through Property to Client. Requires `building_configuration.manage`.
      tags: [Organization Presentation]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateOrganizationPresentationRequest" } } } }
      responses:
        "201": { description: Presentation Configuration created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/OrganizationPresentationConfiguration" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: getBuildingOrganizationPresentation
      summary: Get Building Organization Presentation Configuration
      tags: [Organization Presentation]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Presentation Configuration, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/OrganizationPresentationConfiguration" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /buildings/{buildingId}/organization-presentation/effective:
    get:
      operationId: getEffectiveBuildingOrganizationPresentation
      summary: Get effective Client to Building Organization Presentation
      description: An ACTIVE Building artifact replaces the ACTIVE Client artifact as a whole; hierarchy authority remains BE-03.
      tags: [Organization Presentation]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Effective Presentation, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveOrganizationPresentation" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /building-organization-presentation/{organizationPresentationId}:
    get:
      operationId: getBuildingOrganizationPresentationById
      summary: Get Building Presentation Configuration by id
      tags: [Organization Presentation]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/OrganizationPresentationIdPath" }]
      responses:
        "200": { description: Presentation Configuration, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/OrganizationPresentationConfiguration" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateBuildingOrganizationPresentation
      summary: Update Building Presentation metadata/status
      tags: [Organization Presentation]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/OrganizationPresentationIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateOrganizationPresentationRequest" } } } }
      responses:
        "200": { description: Presentation Configuration updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # BE-27L — scoped safe CMS content foundation
  /clients/{clientId}/cms-content:
    post:
      operationId: createClientCmsContent
      summary: Create Client CMS Content
      description: Accepts safe plain text/Markdown only. Requires `client_configuration.manage`.
      tags: [CMS Content]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateCmsContentRequest" } } } }
      responses:
        "201": { description: CMS Content created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/CmsContent" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listClientCmsContent
      summary: List Client CMS Content administration records
      tags: [CMS Content]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
        - $ref: "#/components/parameters/CmsContentTypeQuery"
        - $ref: "#/components/parameters/CmsContentStatusQuery"
      responses:
        "200": { description: CMS Content list, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/CmsContent" } } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /clients/{clientId}/cms-content/effective:
    get:
      operationId: getEffectiveClientCmsContent
      summary: Get PUBLISHED effective Client CMS Content
      tags: [CMS Content]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
        - $ref: "#/components/parameters/CmsContentTypeQuery"
      responses:
        "200": { description: Effective CMS Content, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveCmsContent" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /client-cms-content/{cmsContentId}:
    get:
      operationId: getClientCmsContent
      summary: Get Client CMS Content by id
      tags: [CMS Content]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/CmsContentIdPath" }]
      responses:
        "200": { description: CMS Content, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/CmsContent" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateClientCmsContent
      summary: Update or publish Client CMS Content
      tags: [CMS Content]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/CmsContentIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateCmsContentRequest" } } } }
      responses:
        "200": { description: CMS Content updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /buildings/{buildingId}/cms-content:
    post:
      operationId: createBuildingCmsContent
      summary: Create Building CMS Content
      description: Building ownership is derived through Property to Client. Requires `building_configuration.manage`.
      tags: [CMS Content]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateCmsContentRequest" } } } }
      responses:
        "201": { description: CMS Content created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/CmsContent" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildingCmsContent
      summary: List Building CMS Content administration records
      tags: [CMS Content]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - $ref: "#/components/parameters/CmsContentTypeQuery"
        - $ref: "#/components/parameters/CmsContentStatusQuery"
      responses:
        "200": { description: CMS Content list, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/CmsContent" } } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /buildings/{buildingId}/cms-content/effective:
    get:
      operationId: getEffectiveBuildingCmsContent
      summary: Get effective Client to Building CMS Content
      description: PUBLISHED Building content replaces PUBLISHED Client content with the same contentType and slug.
      tags: [CMS Content]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - $ref: "#/components/parameters/CmsContentTypeQuery"
      responses:
        "200": { description: Effective CMS Content, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveCmsContent" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /building-cms-content/{cmsContentId}:
    get:
      operationId: getBuildingCmsContent
      summary: Get Building CMS Content by id
      tags: [CMS Content]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/CmsContentIdPath" }]
      responses:
        "200": { description: CMS Content, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/CmsContent" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateBuildingCmsContent
      summary: Update or publish Building CMS Content
      tags: [CMS Content]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/CmsContentIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateCmsContentRequest" } } } }
      responses:
        "200": { description: CMS Content updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # BE-27M — constrained Branding & White Label configuration
  /clients/{clientId}/branding:
    post:
      operationId: createClientBranding
      summary: Create Client Branding Configuration
      description: Uses constrained metadata/theme tokens and an opaque logo storage reference. Requires `client_configuration.manage`.
      tags: [Branding]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateBrandingRequest" } } } }
      responses:
        "201": { description: Branding Configuration created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/BrandingConfiguration" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: getClientBranding
      summary: Get Client Branding Configuration
      tags: [Branding]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Branding Configuration, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/BrandingConfiguration" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /clients/{clientId}/branding/effective:
    get:
      operationId: getEffectiveClientBranding
      summary: Get effective ACTIVE Client Branding
      tags: [Branding]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Effective Branding, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveBranding" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /client-branding/{brandingConfigurationId}:
    get:
      operationId: getClientBrandingById
      summary: Get Client Branding Configuration by id
      tags: [Branding]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BrandingConfigurationIdPath" }]
      responses:
        "200": { description: Branding Configuration, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/BrandingConfiguration" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateClientBranding
      summary: Update Client Branding metadata/status
      tags: [Branding]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BrandingConfigurationIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateBrandingRequest" } } } }
      responses:
        "200": { description: Branding Configuration updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /buildings/{buildingId}/branding:
    post:
      operationId: createBuildingBranding
      summary: Create Building Branding Configuration
      description: Building ownership is derived through Property to Client. Requires `building_configuration.manage`.
      tags: [Branding]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateBrandingRequest" } } } }
      responses:
        "201": { description: Branding Configuration created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/BrandingConfiguration" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: getBuildingBranding
      summary: Get Building Branding Configuration
      tags: [Branding]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Branding Configuration, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/BrandingConfiguration" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /buildings/{buildingId}/branding/effective:
    get:
      operationId: getEffectiveBuildingBranding
      summary: Get effective Client to Building Branding
      description: An ACTIVE Building profile replaces the ACTIVE Client profile as a whole.
      tags: [Branding]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Effective Branding, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveBranding" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /building-branding/{brandingConfigurationId}:
    get:
      operationId: getBuildingBrandingById
      summary: Get Building Branding Configuration by id
      tags: [Branding]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BrandingConfigurationIdPath" }]
      responses:
        "200": { description: Branding Configuration, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/BrandingConfiguration" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateBuildingBranding
      summary: Update Building Branding metadata/status
      tags: [Branding]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BrandingConfigurationIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateBrandingRequest" } } } }
      responses:
        "200": { description: Branding Configuration updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # BE-27N — immutable version history; no lifecycle mutation endpoints
  /configuration-sources/{sourceType}/{sourceConfigurationId}/versions:
    get:
      operationId: listConfigurationVersions
      summary: List immutable versions for a configuration source
      description: Requires the existing source-domain read permission and Client/Building Data Scope.
      tags: [Configuration Versioning]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ConfigurationVersionSourceTypePath"
        - $ref: "#/components/parameters/SourceConfigurationIdPath"
      responses:
        "200": { description: Configuration version history, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/ConfigurationVersion" } } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /configuration-versions/{configurationVersionId}:
    get:
      operationId: getConfigurationVersion
      summary: Get one immutable configuration version
      tags: [Configuration Versioning]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ConfigurationVersionIdPath" }]
      responses:
        "200": { description: Configuration version, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/ConfigurationVersion" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /configuration-versions/{configurationVersionId}/validations:
    get:
      operationId: listConfigurationValidations
      summary: List append-only validation results
      tags: [Configuration Versioning]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ConfigurationVersionIdPath" }]
      responses:
        "200": { description: Validation results, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/ConfigurationValidation" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /configuration-versions/{configurationVersionId}/draft:
    post:
      operationId: createConfigurationDraft
      summary: Clone an immutable version into a new DRAFT
      description: Configuration changes continue through existing source-domain APIs, which save new DRAFT snapshots.
      tags: [Configuration Versioning]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ConfigurationVersionIdPath" }]
      responses:
        "201": { description: New DRAFT version, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/ConfigurationVersion" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /configuration-versions/{configurationVersionId}/validate:
    post:
      operationId: validateConfigurationVersion
      summary: Validate a DRAFT against its authoritative source and scope
      tags: [Configuration Versioning]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ConfigurationVersionIdPath" }]
      responses:
        "200": { description: Validation outcome; failed validation leaves lifecycleStatus DRAFT, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/ConfigurationValidationOutcome" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /configuration-versions/{configurationVersionId}/publish:
    post:
      operationId: publishConfigurationVersion
      summary: Publish a successfully VALIDATED version
      tags: [Configuration Versioning]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ConfigurationVersionIdPath" }]
      responses:
        "200": { description: PUBLISHED version }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /configuration-versions/{configurationVersionId}/activate:
    post:
      operationId: activateConfigurationVersion
      summary: Activate PUBLISHED version and supersede prior ACTIVE version
      tags: [Configuration Versioning]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ConfigurationVersionIdPath" }]
      responses:
        "200": { description: ACTIVE version }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  # BE-27P — safe non-active configuration preview
  /configuration-versions/{configurationVersionId}/preview-contexts:
    post:
      operationId: createConfigurationPreview
      summary: Create a caller-bound preview for a VALIDATED/PUBLISHED version
      tags: [Configuration Preview]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ConfigurationVersionIdPath" }]
      requestBody: { required: false, content: { application/json: { schema: { $ref: "#/components/schemas/CreateConfigurationPreviewRequest" } } } }
      responses:
        "201": { description: Preview context, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/ConfigurationPreviewContext" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /configuration-preview-contexts/{configurationPreviewId}:
    get:
      operationId: getConfigurationPreview
      summary: Get PREVIEW status and metadata
      tags: [Configuration Preview]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ConfigurationPreviewIdPath" }]
      responses:
        "200": { description: Preview metadata, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/ConfigurationPreviewContext" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /configuration-preview-contexts/{configurationPreviewId}/effective:
    get:
      operationId: getEffectiveConfigurationPreview
      summary: Resolve effective PREVIEW configuration/navigation/workspace
      description: Clearly isolated from ACTIVE context; this endpoint has no publication side effects.
      tags: [Configuration Preview]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ConfigurationPreviewIdPath" }]
      responses:
        "200": { description: Effective PREVIEW projection, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveConfigurationPreview" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "410": { description: Preview context expired or revoked }
  /configuration-preview-contexts/{configurationPreviewId}/revoke:
    post:
      operationId: revokeConfigurationPreview
      summary: Revoke a caller-owned preview context
      tags: [Configuration Preview]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ConfigurationPreviewIdPath" }]
      responses:
        "200": { description: REVOKED preview context }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # BE-27Q — read-only projection over append-only operational_events
  /configuration-audit:
    get:
      operationId: listConfigurationAudit
      summary: List scoped configuration audit/history events
      description: Requires `operational_event.read`; no update/delete endpoints exist.
      tags: [Configuration Audit]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: configurationId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: configurationVersionId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: sourceType
          in: query
          schema: { $ref: "#/components/schemas/ConfigurationVersionSourceType" }
        - name: action
          in: query
          schema: { $ref: "#/components/schemas/ConfigurationAuditAction" }
        - name: buildingId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: from
          in: query
          schema: { type: string, format: date-time }
        - name: to
          in: query
          schema: { type: string, format: date-time }
      responses:
        "200": { description: Configuration audit events, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/ConfigurationAuditEvent" } } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /configuration-audit/{configurationAuditEventId}:
    get:
      operationId: getConfigurationAudit
      summary: Get one scoped configuration audit event
      tags: [Configuration Audit]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ConfigurationAuditEventIdPath" }]
      responses:
        "200": { description: Configuration audit event, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/ConfigurationAuditEvent" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /reports/export:
    get:
      operationId: getReportingExport
      summary: Export an authoritative reporting dataset as structured JSON
      description: >
        Neutral KPI/table projection over an owning BE-23 KPI or the BE-24
        Operations Command Center. Values are copied without alternate
        calculations. The endpoint never renders charts or generates PDF/Excel
        files. Requires `reporting_export.read`; the Management dataset also
        requires `management_read_model.read`.
      tags: [Reporting Export]
      security:
        - bearerAuth: []
      parameters:
        - name: dataset
          in: query
          required: true
          schema:
            $ref: "#/components/schemas/ReportArchiveDataset"
        - name: clientId
          in: query
          required: false
          description: BE-24 Management dataset Client narrowing.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Accessible single Building; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: BE-24 comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: date
          in: query
          required: false
          description: BE-24 Daily Operations UTC date.
          schema:
            type: string
            format: date
        - name: dateFrom
          in: query
          required: false
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          schema:
            type: string
        - name: graceMinutes
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 10080
        - name: overdueAfterDays
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 365
        - name: expiringWithinDays
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 365
        - name: interval
          in: query
          required: false
          schema:
            type: string
            enum: [DAY, MONTH, YEAR]
        - name: includeSubMeters
          in: query
          required: false
          schema:
            type: boolean
      responses:
        "200":
          description: Export-ready structured JSON dataset resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ReportingExport"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /reporting/exports:
    post:
      operationId: createReportExport
      x-required-permission: report_export.generate
      x-building-scoped: true
      summary: Create and synchronously generate a governed report archive
      description: >
        Creates one governed export request, resolves the authenticated
        caller's Client/Building scope, builds one canonical reporting
        snapshot, renders exactly the requested format, and returns the
        archive metadata. The request is idempotent when `Idempotency-Key` is
        supplied: the same key and canonical request fingerprint return the
        same archive execution; reusing the key with different request data
        returns 409. A deliberate regeneration is a new request/archive ID.
        No storage reference or public storage URL is returned.
      tags: [Reporting Export]
      security:
        - bearerAuth: []
      parameters:
        - name: Idempotency-Key
          in: header
          required: false
          description: >
            Optional client request key. It is hashed server-side and is not
            returned. Same key plus the same canonical request returns the
            existing execution; same key plus different request data conflicts.
          schema:
            type: string
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateReportArchiveRequest"
      responses:
        "201":
          description: >
            The durable archive request. PART 05 normally returns COMPLETED
            for a successful synchronous generation or FAILED with safe
            failure metadata when generation did not succeed.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ReportArchive"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /reporting/exports/{id}:
    get:
      operationId: getReportExport
      x-required-permission: report_archive.read
      x-building-scoped: true
      summary: Get one scoped report export archive status
      description: >
        Returns the same durable archive resource used by the export request.
        The record is visible only when the caller has current access to its
        Client and complete recorded Building scope. Inaccessible or unknown
        archive IDs use the same scoped not-found boundary.
      tags: [Reporting Export]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Scoped report archive status and provenance.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ReportArchive"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /reporting/archives:
    get:
      operationId: listReportArchives
      x-required-permission: report_archive.read
      x-building-scoped: true
      summary: List accessible report archives
      description: >
        Lists only archives whose Client is reachable and whose complete
        recorded Building scope remains within the caller's current accessible
        Building set. Results are newest first and always paginated. A
        clientId/buildingId filter only narrows the scoped result; it never
        widens access.
      tags: [Reporting Export]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          description: Matches single- or multi-Building archives containing this Building.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dataset
          in: query
          schema: { $ref: "#/components/schemas/ReportArchiveDataset" }
        - name: format
          in: query
          schema: { $ref: "#/components/schemas/ReportArchiveFormat" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/ReportArchiveStatus" }
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: pageSize
          in: query
          schema: { type: integer, minimum: 1, maximum: 200, default: 50 }
      responses:
        "200":
          description: Scoped paginated report archive list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportArchiveListEnvelope"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /reporting/archives/{id}:
    get:
      operationId: getReportArchive
      x-required-permission: report_archive.read
      x-building-scoped: true
      summary: Get one scoped report archive
      description: >
        Returns immutable request, source/as-of provenance, generation status,
        safe artifact metadata, retention state, and safe failure metadata.
        Internal storage references and public storage URLs are omitted.
      tags: [Reporting Export]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Scoped report archive.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ReportArchive"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /reporting/archives/{id}/download:
    get:
      operationId: downloadReportArchive
      x-required-permission: report_archive.read
      x-building-scoped: true
      summary: Download one completed report archive
      description: >
        Serves the already-stored completed artifact after the same current
        Client/Building authorization and stored size/SHA-256 verification as
        archive read. The operation never regenerates the report. The response
        uses `Content-Disposition: attachment` with a backend-generated safe
        filename (`filename*` UTF-8 encoding) and never exposes an internal
        storage key, permanent URL, or anonymous download URL. REQUESTED,
        GENERATING, FAILED, and PURGED archives are not downloadable.
      tags: [Reporting Export]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: >
            Stored report bytes. The concrete media type is the archive's
            requested format: JSON, CSV, XLSX, or PDF.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
            Content-Disposition:
              required: true
              description: attachment with the stored backend-generated safe filename.
              schema: { type: string }
            Content-Length:
              required: true
              description: Byte length verified against archive metadata.
              schema: { type: integer, format: int64 }
          content:
            application/json:
              schema: { type: string, format: binary }
            text/csv:
              schema: { type: string, format: binary }
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet:
              schema: { type: string, format: binary }
            application/pdf:
              schema: { type: string, format: binary }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "500":
          description: Stored artifact failed integrity verification.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"

  /management/read-scope:
    get:
      operationId: getManagementReadScope
      summary: Resolve Management & Owner read scope
      description: >
        Resolves the authenticated caller's Client and single/multi-Building
        read scope from the same hierarchy as the effective-user context.
        Selectors only narrow explicit Building assignments. Requires
        `management_read_model.read`. This foundation endpoint returns no
        dashboard, summary, or KPI data.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          description: Optional Client narrowing within accessible Buildings.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: >
            Explicit accessible Building set. Accepts comma-separated values or
            repeated query parameters; mutually exclusive with buildingId.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          description: BE-23-compatible ISO-8601 lower reporting boundary (UTC).
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          description: >
            BE-23-compatible ISO-8601 upper boundary. A date-only value includes
            the whole UTC day; a datetime is an exclusive upper instant.
          schema:
            type: string
      responses:
        "200":
          description: Authorized read scope and reporting period resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementReadScopeContext"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/daily-operations:
    get:
      operationId: getManagementDailyOperations
      summary: Management / Owner daily operations read model
      description: >
        Read-only daily aggregate for the authorized Client/Building scope.
        Scheduled/completed/in-progress/open-finding values reuse BE-10I;
        overdue reuses BE-23G; critical source counters reuse BE-23F2 and
        BE-23H. Requires `management_read_model.read`. This is not the
        Operations Command Center.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: date
          in: query
          required: false
          description: UTC operational date; defaults to today (UTC).
          schema:
            type: string
            format: date
        - name: graceMinutes
          in: query
          required: false
          description: BE-23G overdue tolerance; defaults to 0.
          schema:
            type: integer
            minimum: 0
            maximum: 10080
            default: 0
      responses:
        "200":
          description: Daily Operations read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementDailyOperations"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/work-order-summary:
    get:
      operationId: getManagementWorkOrderSummary
      summary: Management / Owner Work Order Summary read model
      description: >
        Read-only BE-08 Work Order aggregate for the authorized Client/Building
        scope. Status and priority counters use exact authoritative values;
        verification uses APPROVED BE-08I reviews. Because BE-08 has no due
        date or SLA, overdue is an explicit elapsed-created-age reporting
        threshold compatible with BE-23H, not a Work Order lifecycle state.
        Requires `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          description: BE-23-compatible ISO-8601 lower created-at boundary (UTC).
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          description: BE-23-compatible inclusive date or exclusive datetime boundary.
          schema:
            type: string
        - name: overdueAfterDays
          in: query
          required: false
          description: >
            Elapsed created-age threshold for unfinished Work Orders. Reporting
            filter only; not a persisted due date or SLA. Defaults to 7.
          schema:
            type: integer
            minimum: 0
            maximum: 365
            default: 7
      responses:
        "200":
          description: Work Order Summary read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementWorkOrderSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/pending-approvals:
    get:
      operationId: getManagementPendingApprovals
      summary: Management / Owner Pending Approval read model
      description: >
        Read-only composition of existing Permit, Procurement, Tenant, and
        Document approval foundations. The endpoint does not create or decide
        approvals. Available actions are delegated to source authorities where
        those read models exist. Requires `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          description: BE-23-compatible ISO-8601 lower submitted-at boundary (UTC).
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          description: BE-23-compatible inclusive date or exclusive datetime boundary.
          schema:
            type: string
      responses:
        "200":
          description: Pending Approval read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementPendingApproval"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/critical-findings:
    get:
      operationId: getManagementCriticalFindings
      summary: Management / Owner Critical Findings read model
      description: >
        Read-only BE-09 Finding projection for the authorized Client/Building
        scope. A Client's critical tier is its highest ACTIVE configured
        finding-severity rank; no global severity code or threshold is
        invented. Outstanding statuses reuse BE-23F2. Because BE-09 has no due
        date/SLA, overdue is an explicit elapsed-reported-age reporting state.
        Requires `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          description: BE-23-compatible ISO-8601 lower reported-at boundary (UTC).
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          description: BE-23-compatible inclusive date or exclusive datetime boundary.
          schema:
            type: string
        - name: overdueAfterDays
          in: query
          required: false
          description: >
            Elapsed reported-age threshold for outstanding Findings. Reporting
            filter only; not a persisted due date or SLA. Defaults to 7.
          schema:
            type: integer
            minimum: 0
            maximum: 365
            default: 7
      responses:
        "200":
          description: Critical Findings read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementCriticalFindings"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/workforce-summary:
    get:
      operationId: getManagementWorkforceSummary
      summary: Management / Owner Workforce Summary read model
      description: >
        Read-only workforce aggregate for the authorized Client/Building scope.
        Headcount, assignment, overdue, and man-hour values are projected by
        BE-23G's authoritative KPI helpers. Team/Department breakdowns use the
        same ACTIVE BE-03 Workforce-Building binding. Requires
        `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          description: BE-23-compatible ISO-8601 assignment-period lower boundary (UTC).
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          description: BE-23-compatible inclusive date or exclusive datetime boundary.
          schema:
            type: string
        - name: graceMinutes
          in: query
          required: false
          description: BE-23G overdue tolerance; defaults to 0.
          schema:
            type: integer
            minimum: 0
            maximum: 10080
            default: 0
      responses:
        "200":
          description: Workforce Summary read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementWorkforceSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/vendor-summary:
    get:
      operationId: getManagementVendorSummary
      summary: Management / Owner Vendor Summary and Performance read model
      description: >
        Read-only Vendor aggregate for the authorized Client/Building scope.
        Assigned/completed/overdue work, completion rate, and per-vendor
        performance are projected by BE-23H. Active Vendors use the
        authoritative ACTIVE Vendor-Building relationship. Requires
        `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          description: BE-23-compatible ISO-8601 assignment-period lower boundary (UTC).
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          description: BE-23-compatible inclusive date or exclusive datetime boundary.
          schema:
            type: string
        - name: overdueAfterDays
          in: query
          required: false
          description: BE-23H elapsed assignment-age threshold; defaults to 7.
          schema:
            type: integer
            minimum: 0
            maximum: 365
            default: 7
      responses:
        "200":
          description: Vendor Summary and Performance read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementVendorSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/tenant-service-summary:
    get:
      operationId: getManagementTenantServiceSummary
      summary: Management / Owner Tenant Service Summary read model
      description: >
        Read-only Tenant Service and Complaint aggregate for the authorized
        Client/Building scope. Total/open/completed/completion-rate values are
        projected by BE-23H. In-progress fulfilment uses authoritative BE-08
        Work Order status; complaint counts use exact BE-14 states. Requires
        `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          description: BE-23-compatible ISO-8601 request-period lower boundary (UTC).
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          description: BE-23-compatible inclusive date or exclusive datetime boundary.
          schema:
            type: string
        - name: overdueAfterDays
          in: query
          required: false
          description: >
            Elapsed requested-age threshold for outstanding requests. Reporting
            filter only; not a persisted due date or SLA. Defaults to 7.
          schema:
            type: integer
            minimum: 0
            maximum: 365
            default: 7
      responses:
        "200":
          description: Tenant Service Summary read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementTenantServiceSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/asset-registry-compliance:
    get:
      operationId: getManagementAssetRegistryCompliance
      summary: Management / Owner Asset Registry and Compliance read model
      description: >
        Read-only BE-05 registry, warranty, certification, compliance, and
        location snapshot for the authorized Client/Building scope. Current
        coverage/effectiveness delegates to existing BE-05 helpers. No
        maintenance or reliability KPI is included. Requires
        `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: expiringWithinDays
          in: query
          required: false
          description: Forward compliance-expiry window; defaults to 30 days.
          schema:
            type: integer
            minimum: 0
            maximum: 365
            default: 30
      responses:
        "200":
          description: Asset Registry and Compliance read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementAssetRegistryCompliance"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/asset-reliability-work:
    get:
      operationId: getManagementAssetReliabilityWork
      summary: Management / Owner Asset Reliability and Work read model
      description: >
        Read-only BE-05/07/08/10/21 reliability and maintenance projection for
        the authorized Client/Building scope. Work Order and task statuses stay
        authoritative; percentage rounding and overdue grace reuse BE-23.
        PART 05A warranty/certification compliance is not repeated. Requires
        `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          description: BE-23-compatible ISO-8601 operational-period lower boundary (UTC).
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          description: BE-23-compatible inclusive date or exclusive datetime boundary.
          schema:
            type: string
        - name: graceMinutes
          in: query
          required: false
          description: BE-23 overdue tolerance for maintenance tasks; defaults to 0.
          schema:
            type: integer
            minimum: 0
            maximum: 10080
            default: 0
      responses:
        "200":
          description: Asset Reliability and Work read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementAssetReliabilityWork"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/utility-summary:
    get:
      operationId: getManagementUtilitySummary
      summary: Management / Owner Utility Summary read model
      description: >
        Read-only Utility projection for the authorized Client/Building scope.
        Electricity, water, gas, abnormality, verification and period values
        are returned by BE-23I, which delegates consumption calculations to
        BE-18M. Tenant rows are direct BE-18M grouped results. No billing,
        tariff, or cost logic is included. Requires `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          description: BE-23-compatible ISO-8601 consumption-period lower boundary (UTC).
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          description: BE-23-compatible inclusive date or datetime boundary.
          schema:
            type: string
        - name: interval
          in: query
          required: false
          description: BE-23I comparison/trend interval; defaults to MONTH.
          schema:
            type: string
            enum: [DAY, MONTH, YEAR]
            default: MONTH
        - name: includeSubMeters
          in: query
          required: false
          description: >
            Includes Sub Meters when true. Defaults to false to preserve
            BE-18M double-counting protection.
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: Utility Summary read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementUtilitySummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/financial-summary:
    get:
      operationId: getManagementFinancialSummary
      summary: Management / Owner Financial Summary read model
      description: >
        Read-only composition of authoritative BE-19I Building summaries.
        Multi-Building monetary totals are partitioned by Client because BE-19
        stores no currency dimension; values are never summed across Clients.
        No ledger, tax, accounting, payroll, or billing mutation is introduced.
        Requires `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          description: BE-23-compatible lower financial calendar boundary (UTC date).
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          description: BE-23-compatible upper financial calendar boundary (UTC date).
          schema:
            type: string
      responses:
        "200":
          description: Financial Summary read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementFinancialSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  # ------------------------------------------------------------------
  # CR-BE-FIN-RPT-01 — Currency-safe basic financial reporting
  # ------------------------------------------------------------------
  /buildings/{buildingId}/financial-summary:
    get:
      operationId: getBasicFinancialSummary
      summary: Currency-safe basic financial summary per Building
      description: >
        CR-BE-FIN-RPT-01 PART 05 — Currency-safe basic financial reporting.
        BE-19I lightweight query/read-model aggregation only; no persisted
        ledger or accounting model. All monetary totals are grouped by exact
        currency_code (currency for utility_bills). No cross-currency
        arithmetic without explicit conversion. UNKNOWN currency (historical
        NULL) is readable, countable, amount-measurable, source-traceable,
        excluded from known-currency totals and net arithmetic, surfaced
        explicitly as reporting gap. Payment/outstanding amounts inherit
        invoice currency and are grouped by that currency. Single known
        currency + zero UNKNOWN may populate legacy scalar convenience fields;
        otherwise those scalars are null and monetarySummary.byCurrency is
        authoritative. No FX conversion in base report; FX remains read-side
        optional additive only. Requires `basic_financial_reporting.read` and
        an ACTIVE Building assignment.

        UNKNOWN definition: historical monetary data without a governed
        currency snapshot. It is not IDR, not Client base/default currency,
        not reporting currency, excluded from byCurrency arithmetic and net
        calculations, never inferred.

        Legacy scalar compatibility: single known currency + zero UNKNOWN →
        legacy monetary scalar fields may contain values from that single
        currency; multiple known currencies OR any UNKNOWN → unsafe legacy
        monetary scalar fields are null. Counts remain scalar.
      tags: [Basic Financial Reporting]
      security:
        - bearerAuth: []
      parameters:
        - name: buildingId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: periodFrom
          in: query
          required: false
          description: YYYY-MM-DD inclusive lower bound for business dates.
          schema:
            type: string
            format: date
        - name: periodTo
          in: query
          required: false
          description: YYYY-MM-DD inclusive upper bound for business dates.
          schema:
            type: string
            format: date
        - name: tenantCompanyId
          in: query
          required: false
          description: Optional Tenant Company filter (UUID).
          schema:
            $ref: "#/components/schemas/Uuid"
      x-required-permission: basic_financial_reporting.read
      x-building-scoped: true
      responses:
        "200":
          description: Currency-safe financial summary resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/BasicFinancialSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  # ------------------------------------------------------------------
  # CR-BE-FIN-01 — Operational Finance budget, binding and aggregation API
  # ------------------------------------------------------------------
  /buildings/{buildingId}/operational-budgets:
    post:
      operationId: createOperationalBudget
      summary: Create an Operational Budget
      description: >
        Creates a Building-scoped DRAFT Operational Budget. Client ownership is
        derived from Building → Property → Client. Currency is explicit and no
        source transaction is created or bound by this operation.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: buildingId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateOperationalBudgetRequest" }
      responses:
        "201": { $ref: "#/components/responses/OperationalBudgetSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildingOperationalBudgets
      summary: List Operational Budgets for a Building
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: buildingId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/OperationalBudgetStatus" }
        - name: periodFrom
          in: query
          schema: { type: string, format: date }
        - name: periodTo
          in: query
          schema: { type: string, format: date }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetListSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /operational-budgets:
    get:
      operationId: listOperationalBudgets
      summary: List Accessible Operational Budgets
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: buildingId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/OperationalBudgetStatus" }
        - name: periodFrom
          in: query
          schema: { type: string, format: date }
        - name: periodTo
          in: query
          schema: { type: string, format: date }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetListSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /operational-budgets/{id}:
    get:
      operationId: getOperationalBudget
      summary: Get an Operational Budget
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateOperationalBudget
      summary: Update a Draft Operational Budget
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateOperationalBudgetRequest" }
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "404": { $ref: "#/components/responses/NotFound" }

  /operational-budgets/{id}/activate:
    post:
      operationId: activateOperationalBudget
      summary: Activate an Operational Budget
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /operational-budgets/{id}/close:
    post:
      operationId: closeOperationalBudget
      summary: Close an Operational Budget
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /operational-budgets/{id}/cancel:
    post:
      operationId: cancelOperationalBudget
      summary: Cancel an Operational Budget
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /operational-budgets/{budgetId}/categories:
    post:
      operationId: createOperationalBudgetCategory
      summary: Create an Operational Budget Category
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateOperationalBudgetCategoryRequest" }
      responses:
        "201": { $ref: "#/components/responses/OperationalBudgetCategorySuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listOperationalBudgetCategories
      summary: List Operational Budget Categories
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetCategoryListSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /operational-budget-categories/{id}:
    get:
      operationId: getOperationalBudgetCategory
      summary: Get an Operational Budget Category
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetCategorySuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateOperationalBudgetCategory
      summary: Update a Draft Operational Budget Category
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateOperationalBudgetCategoryRequest" }
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetCategorySuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      operationId: deleteOperationalBudgetCategory
      summary: Delete a Draft Operational Budget Category
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      responses:
        "204": { description: Category deleted. }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /operational-budgets/{budgetId}/source-bindings:
    post:
      operationId: createOperationalBudgetSourceBinding
      summary: Bind an Authoritative Operational Cost Source
      description: >
        Creates typed lineage only. Exactly one supported source reference is
        accepted. No source amount is stored or aggregated by this operation.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateOperationalBudgetSourceBindingRequest" }
      responses:
        "201": { $ref: "#/components/responses/OperationalBudgetSourceBindingSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listOperationalBudgetSourceBindings
      summary: List Operational Budget Source Bindings
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetSourceBindingListSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /operational-budget-source-bindings/{id}:
    get:
      operationId: getOperationalBudgetSourceBinding
      summary: Get an Operational Budget Source Binding
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetSourceBindingSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /operational-budget-source-bindings/{id}/remove:
    post:
      operationId: removeOperationalBudgetSourceBinding
      summary: Remove an Operational Budget Source Binding
      description: Removes the active binding while retaining lineage history.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetSourceBindingSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /operational-budgets/{budgetId}/aggregation:
    get:
      operationId: getOperationalBudgetAggregation
      summary: Get Read-Time Operational Budget Aggregation
      description: >
        Read-time aggregation over active typed source bindings. Amounts are
        read from authoritative source modules and are never persisted here.
        Ambiguous and currency-incompatible contributions fail closed.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetAggregationSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /operational-budgets/{budgetId}/summary:
    get:
      operationId: getOperationalBudgetAggregationSummary
      summary: Get Operational Budget Aggregation Summary
      description: Alias of the read-time PART 04 aggregation contract.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetAggregationSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # ── CR-BE-COMM-VAR-01 — Operational commitment ledger & variance ──────────
  /operational-budgets/{id}/overspend-policy:
    post:
      operationId: changeOperationalBudgetOverspendPolicy
      summary: Change Operational Budget Overspend Policy
      description: >
        Narrow, separately authorised overspend-policy transition for a DRAFT or
        ACTIVE budget. It requires BOTH operational_budget.manage and
        operational_budget.override, plus a mandatory reason, and is audited
        through the existing OPERATIONAL_BUDGET_OVERSPEND_POLICY_CHANGED event.
        The generic budget PATCH remains DRAFT-only. Administering a budget must
        never imply the authority to exceed approved spending.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.override
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ChangeOperationalBudgetOverspendPolicyRequest" }
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetOverspendPolicySuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /operational-budgets/{budgetId}/commitments:
    post:
      operationId: createOperationalCommitment
      summary: Create Manual Operational Commitment
      description: >
        Creates a manual/estimated commitment against an ACTIVE budget. The
        decision runs in one transaction opened with a budget-row lock, and
        available budget is re-read inside that lock, so two concurrent
        approvals can never consume the same remainder. Overspend is rejected
        (409 OPERATIONAL_BUDGET_OVERSPEND_REJECTED) unless the budget policy is
        ALLOW_WITH_OVERRIDE, the caller holds operational_budget.override, and
        an explicit overspendOverrideReason is supplied. Creation is idempotent
        per (budget, idempotencyKey).
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateOperationalCommitmentRequest" }
      responses:
        "201": { $ref: "#/components/responses/OperationalCommitmentSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listOperationalCommitments
      summary: List Operational Commitments
      description: Commitment ledger headers for one budget.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: budgetCategoryId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/OperationalCommitmentStatus" }
        - name: origin
          in: query
          required: false
          schema: { $ref: "#/components/schemas/OperationalCommitmentOrigin" }
        - name: workOrderId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalCommitmentListSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /operational-budgets/{budgetId}/commitments/from-purchase-order-line:
    post:
      operationId: createPurchaseOrderLineCommitment
      summary: Create Commitment From An Issued Purchase Order Line
      description: >
        Raises the commitment for one priced, ISSUED Purchase Order line — the
        only automatic commitment authority in the repository. Amount, currency,
        Client, Building, vendor and Material Request lineage are DERIVED from
        the line and must not be supplied. The caller provides only the explicit
        cost category and an idempotency key. A Purchase Order already
        represented by a CR-BE-FIN-01 source binding, or already carrying a live
        commitment, is refused so one obligation is never counted twice.
        Approved Material Requests, reservations and receivings carry no price
        and never create a commitment.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePurchaseOrderLineCommitmentRequest" }
      responses:
        "201": { $ref: "#/components/responses/OperationalCommitmentSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /operational-commitments/{id}:
    get:
      operationId: getOperationalCommitment
      summary: Get Operational Commitment With Its Ledger Entries
      description: >
        The commitment header plus its append-only ledger entries. signedAmount
        is the effect on the open amount, so the entries always sum to
        openAmount.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalCommitmentDetailSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /operational-commitments/{id}/adjust:
    post:
      operationId: adjustOperationalCommitment
      summary: Adjust An Open Operational Commitment
      description: >
        Appends a signed adjustment entry: a positive amount increases the
        obligation (subject to the same overspend control as creation), a
        negative amount decreases it. A decrease below the already-actualized
        amount is rejected. Amounts are never edited in place.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AdjustOperationalCommitmentRequest" }
      responses:
        "200": { $ref: "#/components/responses/OperationalCommitmentSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /operational-commitments/{id}/release:
    post:
      operationId: releaseOperationalCommitment
      summary: Release The Remainder Of An Operational Commitment
      description: >
        Releases exactly the remaining open amount and closes the commitment.
        This is the correct path for a partially actualized obligation that will
        not complete.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CloseOperationalCommitmentRequest" }
      responses:
        "200": { $ref: "#/components/responses/OperationalCommitmentSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /operational-commitments/{id}/cancel:
    post:
      operationId: cancelOperationalCommitment
      summary: Cancel An Operational Commitment
      description: >
        Withdraws an obligation that never became actual. Rejected once anything
        has been actualized (409) — release the remainder instead.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CloseOperationalCommitmentRequest" }
      responses:
        "200": { $ref: "#/components/responses/OperationalCommitmentSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /operational-budgets/{budgetId}/variance:
    get:
      operationId: getOperationalBudgetVariance
      summary: Get Operational Budget Variance
      description: >
        Derived at read time; nothing is persisted or cached. Ledger and legacy
        contributions are reported separately as well as combined. A legacy
        source binding contributes only while no ledger commitment represents
        the same Purchase Order. Unallocated actual (costed material usages and
        verified vendor invoices with no commitment) is a BUDGET-scope figure
        only, because an unmatched source carries no cost category. Currency and
        period mismatches fail closed and are listed in controls.exclusions.
        Recorded gaps (B-02, B-03) are described in gaps and never valued.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetVarianceSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /operational-budget-variance:
    get:
      operationId: listOperationalBudgetVariance
      summary: List Operational Budget Variance By Building And Period
      description: >
        Portfolio breakdown: one variance summary per accessible budget. Building
        and period are the budget's own dimensions, so no new aggregation axis is
        introduced.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/OperationalBudgetStatus" }
        - name: periodFrom
          in: query
          required: false
          schema: { type: string, format: date }
        - name: periodTo
          in: query
          required: false
          schema: { type: string, format: date }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetVarianceListSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /buildings/{buildingId}/operational-budget-variance:
    get:
      operationId: listBuildingOperationalBudgetVariance
      summary: List Operational Budget Variance For One Building
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: buildingId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/OperationalBudgetStatus" }
        - name: periodFrom
          in: query
          required: false
          schema: { type: string, format: date }
        - name: periodTo
          in: query
          required: false
          schema: { type: string, format: date }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetVarianceListSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /operational-budgets/{budgetId}/traceability:
    get:
      operationId: getOperationalBudgetTraceability
      summary: Get Operational Budget Source Traceability
      description: >
        Flat source-transaction rows behind the variance figures, each with its
        classification, commitment/binding identity and lineage keys (purchase
        order, purchase order line, vendor, work order, material request, vendor
        invoice, material usage) and an exclusion reason when it does not count.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetTraceabilitySuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /management/buildings/{buildingId}/operational-finance-summary:
    get:
      operationId: getManagementBuildingOperationalFinanceSummary
      summary: Building Operational Finance Management Summary
      description: >
        Read-only Building-level Operational Finance summary for an explicit
        period. Budget amounts and category amounts are delegated to the
        existing CR-BE-FIN-01 PART 04 aggregation through the PART 05
        management contracts. No source transaction payloads, accounting,
        currency conversion or fiscal-year logic is introduced.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: buildingId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: periodStart
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: periodEnd
          in: query
          required: true
          schema:
            type: string
            format: date
      x-required-permission: management_read_model.read
      x-building-scoped: true
      responses:
        "200":
          description: Building Operational Finance summary resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementBuildingOperationalFinance"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"

  /management/operational-finance/budgets/{budgetId}/summary:
    get:
      operationId: getManagementOperationalBudgetSummary
      summary: Management Operational Budget Summary
      description: >
        Read-only Operational Finance budget summary delegated to CR-BE-FIN-01
        PART 04. Returns one Building budget with planned, actual, committed,
        remaining and variance amounts. Ambiguous, unresolved or currency-
        incompatible source contributions remain excluded by the PART 04
        fail-closed rules. This is additive and does not modify the semantics
        of `/management/financial-summary`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      x-required-permission: management_read_model.read
      x-building-scoped: true
      responses:
        "200":
          description: Operational Budget summary resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementOperationalBudgetSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /management/operational-finance/budgets/{budgetId}/categories:
    get:
      operationId: getManagementOperationalBudgetCategories
      summary: Management Operational Budget Category Breakdown
      description: >
        Read-only category breakdown delegated to CR-BE-FIN-01 PART 04. Each
        category preserves the authoritative planned, actual, committed,
        remaining and variance values. No source transaction payloads are
        duplicated in this response.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      x-required-permission: management_read_model.read
      x-building-scoped: true
      responses:
        "200":
          description: Operational Budget category breakdown resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementOperationalBudgetCategories"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /management/operational-kpi:
    get:
      operationId: getManagementOperationalKpi
      summary: Management / Owner Operational KPI read model
      description: >
        Read-only operational KPI for the authorized Client/Building scope.
        Assignment counts and completion rate reuse BE-23G; critical source
        counters reuse BE-23F2/BE-23H. Work Order, Finding and Incident values
        read authoritative source statuses. No source workflow is duplicated.
        Requires `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          description: BE-23-compatible ISO-8601 operational-period lower boundary (UTC).
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          description: BE-23-compatible inclusive date or exclusive datetime boundary.
          schema:
            type: string
        - name: graceMinutes
          in: query
          required: false
          description: BE-23G overdue tolerance; defaults to 0.
          schema:
            type: integer
            minimum: 0
            maximum: 10080
            default: 0
      responses:
        "200":
          description: Operational KPI read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementOperationalKpi"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/building-performance:
    get:
      operationId: getManagementBuildingPerformance
      summary: Management / Owner Building Performance read model
      description: >
        Lightweight per-Building composition of existing BE-24 summaries and
        BE-23/domain reports. No score, weighting, ranking, domain formula, or
        Portfolio aggregation is introduced. Requires
        `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          schema:
            type: string
        - name: graceMinutes
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 10080
            default: 0
        - name: overdueAfterDays
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 365
            default: 7
        - name: expiringWithinDays
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 365
            default: 30
        - name: interval
          in: query
          required: false
          schema:
            type: string
            enum: [DAY, MONTH, YEAR]
            default: MONTH
        - name: includeSubMeters
          in: query
          required: false
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: Per-Building Performance rows resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementBuildingPerformance"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/portfolio-overview:
    get:
      operationId: getManagementPortfolioOverview
      summary: Management / Owner Portfolio Overview read model
      description: >
        Multi-Building composition of exact scope-wide BE-24 summaries plus
        PART 08A per-Building rows. Rates are copied from scope-wide KPI owners,
        not averaged. Monetary summaries remain partitioned by Client. No
        composite score, ranking, chart, or UI metadata is introduced.
        Requires `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Accessible single Building; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          schema:
            type: string
        - name: graceMinutes
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 10080
            default: 0
        - name: overdueAfterDays
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 365
            default: 7
        - name: expiringWithinDays
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 365
            default: 30
        - name: interval
          in: query
          required: false
          schema:
            type: string
            enum: [DAY, MONTH, YEAR]
            default: MONTH
        - name: includeSubMeters
          in: query
          required: false
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: Portfolio Overview resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementPortfolioOverview"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/operations-command-center:
    get:
      operationId: getManagementOperationsCommandCenter
      summary: Management / Owner Operations Command Center read model
      description: >
        Thin facade over completed BE-24 read models for an accessible single-
        or multi-Building scope. Component values are copied without direct
        domain queries, alternate KPI formulas, cross-Client monetary totals,
        chart metadata, or UI metadata. Requires `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Accessible single Building; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: date
          in: query
          required: false
          description: UTC operational day delegated to Daily Operations.
          schema:
            type: string
            format: date
        - name: dateFrom
          in: query
          required: false
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          schema:
            type: string
        - name: graceMinutes
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 10080
            default: 0
        - name: overdueAfterDays
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 365
            default: 7
        - name: expiringWithinDays
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 365
            default: 30
        - name: interval
          in: query
          required: false
          schema:
            type: string
            enum: [DAY, MONTH, YEAR]
            default: MONTH
        - name: includeSubMeters
          in: query
          required: false
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: Operations Command Center read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementOperationsCommandCenter"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /evidence/{evidenceId}/file:
    post:
      operationId: uploadEvidenceFile
      summary: Upload an evidence file
      description: >
        Attaches the file bytes of an existing evidence submission
        (multipart field `file`). The submission row is the file metadata
        record: `fileReference` becomes the backend-generated storage key and
        `originalFileName`, `mimeType`, `fileSize` are updated. Requires
        `evidence.manage`. File access is bound to the evidence record's
        Client scope (BE-02G).
        CR-BE-DOC-CONTROL-01: the server computes and persists the SHA-256
        of the exact stored bytes atomically with this upload
        (`contentSha256` / `contentHashedAt` / `hashAlgorithm`); callers can
        never supply a trusted hash. A PURGED retention tombstone is
        terminal and rejects re-upload with 400.
      tags: [Evidence]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/EvidenceIdPathParam"
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                  description: Max 50 MB; MIME type must match the evidence type.
      responses:
        "201":
          description: File uploaded and metadata updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EvidenceFileMetadata"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    get:
      operationId: getEvidenceFileMetadata
      summary: Resolve evidence file metadata
      description: >
        Returns the metadata of an evidence submission (the evidence file
        metadata record). Requires `evidence.read`. Access is bound to
        the evidence record's Client scope.
      tags: [Evidence]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/EvidenceIdPathParam"
      responses:
        "200":
          description: Evidence file metadata.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EvidenceFileMetadata"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /evidence/{evidenceId}/file/content:
    get:
      operationId: downloadEvidenceFile
      summary: Download an evidence file
      description: >
        Streams the stored evidence file bytes (`Content-Type` and
        `Content-Disposition` reflect the stored metadata). Requires
        `evidence.read`. Access is bound to the evidence record's Client
        scope; internal storage paths are never exposed.
        CR-BE-DOC-CONTROL-01: for a PURGED retention tombstone the binary
        was disposed — this endpoint returns 404 while the metadata record
        remains readable.
      tags: [Evidence]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/EvidenceIdPathParam"
      responses:
        "200":
          description: File bytes.
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # ------------------------------------------------------------------
  # CR-BE-DOC-CONTROL-01 — Evidence integrity verification + retention
  # hold + retention policy administration.
  # ------------------------------------------------------------------
  /evidence/{evidenceId}/integrity-verification:
    post:
      operationId: verifyEvidenceIntegrity
      summary: Verify evidence integrity
      description: >
        Server-side integrity verification: re-reads the stored binary
        through the storage abstraction, recomputes its SHA-256, and
        compares it against the authoritative hash persisted at upload time.
        Persists `lastIntegrityStatus` / `lastIntegrityCheckedAt` and
        records an operational event (`EVIDENCE_INTEGRITY_VERIFIED` on
        success, `EVIDENCE_INTEGRITY_FAILED` otherwise). Outcomes: VERIFIED,
        MISMATCH (stored bytes changed outside Asentra), NOT_HASHED
        (legacy/unhashed evidence — never backfilled), FILE_UNAVAILABLE
        (stored object unreadable). Verification NEVER mutates or deletes
        evidence. The request carries no body — a caller-supplied hash is
        never accepted. Requires `evidence.manage`; access is bound to the
        evidence record's Client/Building scope. A PURGED retention
        tombstone is not verifiable (400).
      tags: [Evidence]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/EvidenceIdPathParam"
      responses:
        "200":
          description: Verification outcome (persisted and audited).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EvidenceIntegrityVerification"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /evidence/{evidenceId}/retention-hold:
    post:
      operationId: setEvidenceRetentionHold
      summary: Set a retention hold on evidence
      description: >
        Places a minimal legal/operational hold on the evidence row. A held
        row is never purged by retention execution (the hold blocks only the
        final purge — due marking still happens). Audited as
        EVIDENCE_RETENTION_HOLD_SET. Requires `evidence.manage`; PURGED
        tombstones and already-held rows are rejected with 400.
      tags: [Evidence]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/EvidenceIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason:
                  type: string
                  maxLength: 500
      responses:
        "200":
          description: Evidence with the hold set.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EvidenceResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      operationId: clearEvidenceRetentionHold
      summary: Clear a retention hold on evidence
      description: >
        Clears the retention hold; the next retention execution pass may
        purge the row if it is due. Audited as
        EVIDENCE_RETENTION_HOLD_CLEARED. Requires `evidence.manage`; a row
        that is not on hold is rejected with 400.
      tags: [Evidence]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/EvidenceIdPathParam"
      responses:
        "200":
          description: Evidence with the hold cleared.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EvidenceResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /clients/{clientId}/evidence-retention-policies:
    post:
      operationId: createEvidenceRetentionPolicy
      summary: Create an evidence retention policy
      description: >
        Creates a Client-scoped (optionally Building-scoped) evidence
        retention policy. Administration reuses the existing
        `client_configuration.manage` permission (SLA-definition precedent);
        the Building must belong to the Client. Duplicate code per Client →
        409. Activation is prospective only: existing evidence is never
        swept (deferred legacy adoption is a separate future CR).
      tags: [Configuration]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EvidenceRetentionPolicyCreateRequest"
      responses:
        "201":
          description: Created retention policy.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EvidenceRetentionPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
    get:
      operationId: listEvidenceRetentionPolicies
      summary: List evidence retention policies
      description: >
        Lists the Client's evidence retention policies (optional
        `buildingId` / `status` filters). Requires
        `client_configuration.read` and Client access.
      tags: [Configuration]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [ACTIVE, INACTIVE]
      responses:
        "200":
          description: Retention policies of the Client.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/EvidenceRetentionPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /evidence-retention-policies/{id}:
    get:
      operationId: getEvidenceRetentionPolicy
      summary: Get an evidence retention policy
      description: Requires `client_configuration.read` and access to the policy's Client/Building scope.
      tags: [Configuration]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The retention policy.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EvidenceRetentionPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateEvidenceRetentionPolicy
      summary: Update an evidence retention policy
      description: >
        Updates policy fields (name, applicability, retentionDays, status,
        effective window). Requires `client_configuration.manage`. Edits are
        prospective only: evidence already governed keeps its immutable
        snapshot (`retentionDaysSnapshot` / `retainedUntil` never change).
      tags: [Configuration]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EvidenceRetentionPolicyUpdateRequest"
      responses:
        "200":
          description: The updated retention policy.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EvidenceRetentionPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # ------------------------------------------------------------------
  # BE-25A — Mobile execution surface (task / checklist / evidence /
  # review / operational event / finding workflow / QR resolution).
  # List endpoints support the opt-in mobile pagination convention
  # (?page=&pageSize=) → meta: MobilePaginationMeta.
  # ------------------------------------------------------------------
  /tasks:
    get:
      operationId: listTasks
      summary: List generated tasks
      description: >
        Lists generated tasks within the caller's accessible Buildings
        (BE-02G). Optional `clientId` narrows within the accessible set.
        Pagination is opt-in via `page`/`pageSize`; without them the full
        scoped list is returned (Web compatibility). Requires `task.read`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          description: Optional Client filter (must be within the accessible scope).
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/PageSizeParam"
      responses:
        "200":
          description: Scoped task list (full list or requested page).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/Task"
                      meta:
                        $ref: "#/components/schemas/MobilePaginationMeta"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /tasks/{taskId}:
    get:
      operationId: getTask
      summary: Get a generated task
      description: Requires `task.read` and Building-scope access to the task.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TaskIdPathParam"
      responses:
        "200":
          description: The task.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/Task"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /tasks/{taskId}/start:
    post:
      operationId: startTask
      summary: Start an assigned task
      description: >
        Transitions an OPEN/ASSIGNED task to IN_PROGRESS. The active WORKFORCE
        assignee must be the calling user. Requires `task.manage`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TaskIdPathParam"
      responses:
        "200":
          description: The updated task.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/Task"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /tasks/{taskId}/complete:
    post:
      operationId: completeTask
      summary: Complete an in-progress task
      description: >
        Transitions an IN_PROGRESS task to COMPLETED. Requires `task.manage`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TaskIdPathParam"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                completionNotes:
                  type: string
                  nullable: true
      responses:
        "200":
          description: The completed task.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/Task"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /tasks/{taskId}/cancel:
    post:
      operationId: cancelTask
      summary: Cancel a task
      description: Requires `task.manage`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TaskIdPathParam"
      responses:
        "200":
          description: The cancelled task.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/Task"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /workforce/{workforceId}/tasks:
    get:
      operationId: listWorkforceTasks
      summary: List active task assignments for a workforce profile
      description: >
        Active task assignments (WORKFORCE) with their generated tasks, scoped
        to the caller's accessible Buildings. Pagination is opt-in.
        Requires `task.read`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkforceIdPathParam"
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/PageSizeParam"
      responses:
        "200":
          description: Scoped assignment list (full list or requested page).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/TaskAssignment"
                      meta:
                        $ref: "#/components/schemas/MobilePaginationMeta"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /teams/{teamId}/tasks:
    get:
      operationId: listTeamTasks
      summary: List active task assignments for a team
      description: >
        Active task assignments (TEAM) with their generated tasks, scoped to
        the caller's accessible Buildings. Pagination is opt-in.
        Requires `task.read`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TeamIdPathParam"
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/PageSizeParam"
      responses:
        "200":
          description: Scoped assignment list (full list or requested page).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/TaskAssignment"
                      meta:
                        $ref: "#/components/schemas/MobilePaginationMeta"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /tasks/{taskId}/assignments:
    get:
      operationId: listTaskAssignments
      summary: List assignments for a task
      description: >
        The assigned personnel for a generated task (BE-07 assignment
        authority). Scoped to the task's accessible Building (BE-02G): a task
        outside the caller's scope yields 403 BUILDING_ACCESS_DENIED.
        Requires `task.read`. NOTE: this read returns the assignment records;
        the authoritative per-caller `availableActions` for the task is
        exposed through `GET /mobile/assignments`, not here.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TaskIdPathParam"
      responses:
        "200":
          description: Task assignment records (ACTIVE and historical).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/TaskAssignment"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      operationId: assignTask
      summary: Assign a task
      description: >
        MUTATION — assigns a task to a WORKFORCE / TEAM assignee. Requires
        `task.manage` and Building-scope access to the task.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TaskIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AssignTaskRequest" }
      responses:
        "201":
          description: The created task assignment.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TaskAssignment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # --------------------------------------------------------------------
  # CR-BE-MOB-CONTRACT-01 PART 03 — Work Order mobile read surface.
  # Read-only projection of the BE-08 authorities; no lifecycle changes.
  # --------------------------------------------------------------------
  /buildings/{buildingId}/work-orders:
    get:
      operationId: listBuildingWorkOrders
      summary: List Work Orders for a Building
      description: >
        Work Orders within one explicitly accessible Building (BE-02G:
        `requireBuildingAccess`). Requires `work_order.read`. Optional
        filters: `status`, `workType`, `workRequestId`.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/WorkOrderStatus"
        - name: workType
          in: query
          required: false
          schema:
            type: string
        - name: workRequestId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: Work Order list for the accessible Building.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/WorkOrder"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: createWorkOrder
      summary: Create a Work Order for a Building
      description: >
        MUTATION — creates a Work Order in the accessible Building. Requires
        `work_order.manage` and an ACTIVE Building assignment
        (`requireBuildingAccess`). `createdByUserId` is derived from the
        session.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateWorkOrderRequest" }
      responses:
        "201":
          description: The created Work Order.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrder" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /work-orders/team:
    get:
      operationId: listTeamWorkOrders
      x-required-permission: work_order.read
      x-building-scoped: true
      summary: List Work Orders assigned to the caller's team
      description: >-
        CR-BE-MOB-03 PART 04 (TMW-04) — self-service team-scoped Work Order
        read for supervisors. The caller's team is derived from the
        authenticated session: the linked, ACTIVE BE-03C Workforce Profile's
        `team_id` (the same authority `getMobileMyTeam` uses) — never from a
        caller-supplied id. A Work Order is included when its ACTIVE BE-08E
        assignment targets the team itself (`assignee_type = 'TEAM'`) or an
        ACTIVE member of the team (`assignee_type = 'WORKFORCE'` whose
        profile is in the team). Results are constrained in SQL to the
        caller's BE-02G accessible Building set, so no cross-Building /
        cross-Client Work Order can be returned even though no building
        parameter is supplied. Requires `work_order.read` (existing RBAC).
        Read-only: Work Order ids, statuses and lifecycle are the existing
        authoritative BE-08 ones; nothing is written and no mobile facade is
        created. Optional `status` / `workType` / `workRequestId` filters
        narrow the list (same vocabulary as `listBuildingWorkOrders`). A
        caller with no linked ACTIVE profile, no team, or no accessible
        Building gets an empty list.
      tags: [Work Orders]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/WorkOrderStatus" }
        - name: workType
          in: query
          required: false
          schema: { type: string }
          description: Data-driven work type code (uppercase).
        - name: workRequestId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: The team-scoped Work Order list (empty when the caller has no team).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/WorkOrder"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /work-orders/{workOrderId}:
    get:
      operationId: getWorkOrder
      summary: Get a Work Order by id
      description: >
        The authoritative Work Order record. Requires `work_order.read` AND an
        ACTIVE Building assignment for the Work Order's Building (BE-02G) —
        permission alone is never enough; inaccessible → 403
        BUILDING_ACCESS_DENIED. `status` is authoritative; clients must not
        infer transitions from it (per-caller actions come from
        `GET /mobile/assignments`).
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      responses:
        "200":
          description: The Work Order.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/WorkOrder"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateWorkOrder
      summary: Update Work Order metadata
      description: >
        MUTATION — updates title/description/workType. Requires
        `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateWorkOrderRequest" }
      responses:
        "200":
          description: The updated Work Order.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrder" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/assignments:
    get:
      operationId: listWorkOrderAssignments
      summary: List assignments for a Work Order
      description: >
        The assigned personnel/vendor records for a Work Order (BE-08E).
        Requires `work_order.read` and Building access to the Work Order.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      responses:
        "200":
          description: Work Order assignment records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/WorkOrderAssignment"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      operationId: assignWorkOrder
      summary: Assign a Work Order
      description: >
        MUTATION — assigns the Work Order to a WORKFORCE / TEAM / VENDOR /
        VENDOR_WORKFORCE assignee. Requires `work_order.manage` and Building
        access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AssignWorkOrderRequest" }
      responses:
        "201":
          description: The created assignment.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderAssignment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/assignments/current:
    get:
      operationId: getCurrentWorkOrderAssignment
      summary: Get the current (ACTIVE) Work Order assignment
      description: >
        The single ACTIVE assignment for the Work Order, or `null` when none.
        Requires `work_order.read` and Building access to the Work Order.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      responses:
        "200":
          description: The current assignment (or null).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        allOf:
                          - $ref: "#/components/schemas/WorkOrderAssignment"
                        nullable: true
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /work-orders/{workOrderId}/actions:
    get:
      operationId: listWorkOrderActions
      summary: List Work Order action history
      description: >
        The append-only execution action HISTORY (BE-08F): acknowledge, start,
        hold, resume, note, cancel. This is NOT an available-actions read
        model — clients must not derive valid next actions from this list; the
        authoritative per-caller `availableActions` is in
        `GET /mobile/assignments`. Requires `work_order.read` and Building
        access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      responses:
        "200":
          description: Work Order action history.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/WorkOrderAction"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /work-orders/{workOrderId}/context:
    get:
      operationId: getWorkOrderContext
      summary: Get the Work Order location / asset / operational context
      description: >
        The authoritative resolved context (BE-08D): the bound Asset,
        Functional Location, and the projected operational hierarchy
        (Client → Property → Building → …). Projected through the BE-04H
        resolver, never stored. Requires `work_order.read` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      responses:
        "200":
          description: The resolved Work Order context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/WorkOrderContext"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateWorkOrderContext
      summary: Bind/clear Work Order asset/location context
      description: >
        MUTATION — binds or clears the Work Order's Asset / Functional
        Location (BE-08D). Requires `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateWorkOrderContextRequest" }
      responses:
        "200":
          description: The updated Work Order context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/completion:
    get:
      operationId: getWorkOrderCompletion
      summary: Get the Work Order completion subset
      description: >
        The completion subset of a Work Order (BE-08H): status plus completion
        provenance/summary/notes. Requires `work_order.read` and Building
        access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      responses:
        "200":
          description: The Work Order completion subset.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/WorkOrderCompletion"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # --------------------------------------------------------------------
  # CR-BE-MOB-CONTRACT-01 PART 07 — Work Order execution actions,
  # evidence, verification, history, and management mutations (BE-08),
  # Task assignment mutations (BE-07), Entitlement (BE-02C), and Asset
  # sub-resource reads (BE-05). All already implemented; published for
  # completeness.
  # --------------------------------------------------------------------
  /tasks/{taskId}/assignments/{assignmentId}:
    patch:
      operationId: updateTaskAssignment
      summary: Update a task assignment status
      description: >
        MUTATION — updates a task assignment status (ACTIVE/INACTIVE).
        Requires `task.manage` and Building-scope access to the task.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TaskIdPathParam"
        - name: assignmentId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status:
                  type: string
                  enum: [ACTIVE, INACTIVE]
      responses:
        "200":
          description: The updated task assignment.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TaskAssignment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/priority:
    patch:
      operationId: updateWorkOrderPriority
      summary: Update Work Order priority
      description: MUTATION — requires `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [priority]
              properties:
                priority: { $ref: "#/components/schemas/WorkOrderPriority" }
      responses:
        "200":
          description: The updated Work Order.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrder" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/status:
    patch:
      operationId: updateWorkOrderStatus
      summary: Transition the Work Order status
      description: >
        MUTATION — applies a lifecycle transition via the BE-08C transition
        engine (`WORK_ORDER_TRANSITIONS`). Invalid transitions are rejected
        deterministically. Requires `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status: { $ref: "#/components/schemas/WorkOrderStatus" }
      responses:
        "200":
          description: The updated Work Order.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrder" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/bast-requirement:
    patch:
      operationId: updateWorkOrderBastRequirement
      summary: Update the Work Order BAST requirement policy
      description: MUTATION — requires `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [bastRequirement]
              properties:
                bastRequirement: { $ref: "#/components/schemas/BastRequirement" }
      responses:
        "200":
          description: The updated Work Order.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrder" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/assignments/{assignmentId}:
    patch:
      operationId: updateWorkOrderAssignment
      summary: Deactivate or reassign a Work Order assignment
      description: Requires `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
        - name: assignmentId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - { $ref: "#/components/schemas/AssignWorkOrderRequest" }
                - $ref: "#/components/schemas/DeactivateWorkOrderAssignmentRequest"
      responses:
        "200":
          description: The updated assignment.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderAssignment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/acknowledge:
    post:
      operationId: acknowledgeWorkOrder
      summary: Acknowledge the Work Order
      description: >
        MUTATION — records an ACKNOWLEDGED execution action (BE-08F).
        Requires `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/WorkOrderActionRequest" }
      responses:
        "201":
          description: The recorded action.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderAction" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/start:
    post:
      operationId: startWorkOrder
      summary: Start the Work Order
      description: >
        MUTATION — records a STARTED execution action. Requires
        `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/WorkOrderActionRequest" }
      responses:
        "201":
          description: The recorded action.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderAction" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/hold:
    post:
      operationId: holdWorkOrder
      summary: Hold the Work Order
      description: >
        MUTATION — records an ON_HOLD execution action. Requires
        `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/WorkOrderActionRequest" }
      responses:
        "201":
          description: The recorded action.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderAction" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/resume:
    post:
      operationId: resumeWorkOrder
      summary: Resume the Work Order
      description: >
        MUTATION — records a RESUMED execution action. Requires
        `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/WorkOrderActionRequest" }
      responses:
        "201":
          description: The recorded action.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderAction" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/notes:
    post:
      operationId: addWorkOrderNote
      summary: Add a note to the Work Order
      description: >
        MUTATION — records a NOTE_ADDED execution action (requires `notes`).
        Requires `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/WorkOrderActionRequest" }
      responses:
        "201":
          description: The recorded action.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderAction" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/cancel:
    post:
      operationId: cancelWorkOrder
      summary: Cancel the Work Order
      description: >
        MUTATION — records a CANCELLED execution action. Requires
        `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/WorkOrderActionRequest" }
      responses:
        "201":
          description: The recorded action.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderAction" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/complete:
    post:
      operationId: completeWorkOrder
      summary: Complete the Work Order
      description: >
        MUTATION — completes the Work Order (BE-08H), with optional
        `completionSummary` / `completionNotes`. Requires `work_order.manage`
        and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CompleteWorkOrderRequest" }
      responses:
        "200":
          description: The completed Work Order.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrder" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/evidence-requirements:
    get:
      operationId: listWorkOrderEvidenceRequirements
      summary: List evidence requirements for a Work Order
      description: >
        The BE-07 evidence requirements bound to a Work Order (required/type/
        min/max). Requires `work_order.read` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      responses:
        "200":
          description: Work Order evidence requirements.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/WorkOrderEvidenceRequirement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/evidence:
    get:
      operationId: listWorkOrderEvidence
      summary: List evidence for a Work Order
      description: >
        The BE-07 evidence submissions bound to a Work Order. Requires
        `work_order.read` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      responses:
        "200":
          description: Work Order evidence submissions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/WorkOrderEvidence" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: submitWorkOrderEvidence
      summary: Submit evidence for a Work Order
      description: >
        MUTATION — creates a BE-07 evidence submission bound to the Work
        Order (execution_type WORK_ORDER). Requires `work_order.manage` and
        Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitWorkOrderEvidenceRequest" }
      responses:
        "201":
          description: The created evidence submission.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderEvidence" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/evidence/{evidenceId}:
    patch:
      operationId: removeWorkOrderEvidence
      summary: Remove Work Order evidence
      description: >
        MUTATION — marks the evidence submission REMOVED. Requires
        `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
        - name: evidenceId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: The removed evidence submission.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderEvidence" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/verification:
    get:
      operationId: getWorkOrderVerification
      summary: Get the Work Order verification state
      description: >
        The Work Order verification state (BE-08I): latest verification +
        all verification records. Requires `work_order.read` and Building
        access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      responses:
        "200":
          description: The verification state.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderVerificationState" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: submitWorkOrderVerification
      summary: Submit a Work Order verification decision
      description: >
        MUTATION — verifies a COMPLETED Work Order with
        APPROVED / REJECTED / REWORK_REQUIRED (BE-08I, reusing the BE-07
        review primitive). Requires `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitWorkOrderVerificationRequest" }
      responses:
        "201":
          description: The verification result.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderVerificationState" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/history:
    get:
      operationId: getWorkOrderHistory
      summary: Get the Work Order audit/history
      description: >
        The append-only Work Order history (BE-08J, reusing BE-07 operational
        events). Requires `work_order.read` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      responses:
        "200":
          description: Work Order history events.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/WorkOrderHistoryEvent" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # Entitlement (BE-02C) — module entitlements are resolved for the
  # authenticated user via /auth/me → entitlements; these are the
  # administration reads.
  /entitlements:
    get:
      operationId: listEntitlements
      summary: List module entitlements
      description: Requires `entitlement.read`.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Entitlement list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Entitlement" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /entitlements/{entitlementId}:
    get:
      operationId: getEntitlement
      summary: Get an entitlement by id
      description: Requires `entitlement.read`.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/EntitlementIdPath"
      responses:
        "200":
          description: Entitlement record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Entitlement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /entitlements/{entitlementId}/status:
    patch:
      operationId: updateEntitlementStatus
      summary: Update an entitlement status
      description: Requires `entitlement.manage`.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/EntitlementIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status: { $ref: "#/components/schemas/EntitlementStatus" }
      responses:
        "200":
          description: The updated entitlement.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Entitlement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /subscriptions/{subscriptionId}/entitlements:
    get:
      operationId: listSubscriptionEntitlements
      summary: List entitlements for a subscription
      description: Requires `entitlement.read`.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/SubscriptionIdPath"
      responses:
        "200":
          description: Subscription entitlements.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Entitlement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: createEntitlement
      summary: Create an entitlement for a subscription
      description: Requires `entitlement.manage`.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/SubscriptionIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [moduleId]
              properties:
                moduleId: { $ref: "#/components/schemas/Uuid" }
                startsAt: { type: string, format: date-time }
                endsAt: { type: string, format: date-time, nullable: true }
                status: { $ref: "#/components/schemas/EntitlementStatus" }
      responses:
        "201":
          description: The created entitlement.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Entitlement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /subscriptions/{subscriptionId}/entitlements/effective:
    get:
      operationId: resolveEffectiveEntitlements
      summary: Resolve effective entitlements for a subscription
      description: >
        The BE-02C effective entitlements (the same resolver /auth/me uses).
        Requires `entitlement.read`.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/SubscriptionIdPath"
      responses:
        "200":
          description: Effective entitlements.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Entitlement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # Asset sub-resource reads (BE-05).
  /assets/{assetId}/status:
    get:
      operationId: getAssetStatus
      summary: Get the asset lifecycle status
      description: Requires `asset.read` and Building access.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: The asset status.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicAsset" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /assets/{assetId}/identifiers:
    get:
      operationId: listAssetIdentifiers
      summary: List asset identifiers (QR/tag/barcode)
      description: Requires `asset_identifier.read` and Building access.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: Asset identifier list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/AssetIdentifier" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /assets/{assetId}/history:
    get:
      operationId: getAssetHistory
      summary: Get the asset history
      description: Requires `asset_history.read` and Building access.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: Asset history events.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/AssetHistoryEvent" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /assets/{assetId}/warranties:
    get:
      operationId: listAssetWarranties
      summary: List asset warranties
      description: Requires `asset_warranty.read` and Building access.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: Asset warranty list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/AssetWarranty" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /assets/{assetId}/certifications:
    get:
      operationId: listAssetCertifications
      summary: List asset certifications
      description: Requires `asset_certification.read` and Building access.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: Asset certification list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/AssetCertification" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /operational-events/{eventId}:
    get:
      operationId: getOperationalEvent
      x-required-permission: operational_event.read
      x-building-scoped: true
      summary: Get one operational audit event
      description: >
        Returns one event only when its Client/Building is inside the caller's
        current accessible scope. An inaccessible event is indistinguishable
        from an unknown event at this read boundary. Requires
        `operational_event.read`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - name: eventId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: The scoped operational audit event.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/OperationalEvent" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /checklist-executions:
    get:
      operationId: listChecklistExecutions
      summary: List checklist executions
      description: >
        Checklist executions within the caller's accessible Clients. Pagination
        is opt-in via `page`/`pageSize`. Requires `checklist.read`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/PageSizeParam"
      responses:
        "200":
          description: Scoped execution list (full list or requested page).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/ChecklistExecution"
                      meta:
                        $ref: "#/components/schemas/MobilePaginationMeta"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /checklist-executions/{executionId}:
    get:
      operationId: getChecklistExecution
      summary: Get a checklist execution
      description: Requires `checklist.read` and Client-scope access.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ExecutionIdPathParam"
      responses:
        "200":
          description: The checklist execution.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ChecklistExecution"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /checklist-executions/{executionId}/start:
    post:
      operationId: startChecklistExecution
      summary: Start a draft checklist execution
      description: Requires `checklist.manage`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ExecutionIdPathParam"
      responses:
        "200":
          description: The started execution.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ChecklistExecution"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /checklist-executions/{executionId}/responses:
    get:
      operationId: getChecklistResponses
      summary: Get saved checklist item responses
      description: Requires `checklist.read` and Client-scope access.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ExecutionIdPathParam"
      responses:
        "200":
          description: Saved responses for the execution.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/ChecklistItemResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      operationId: saveChecklistResponses
      summary: Save checklist item responses
      description: >
        MUTATION — saves one or more item responses (single object or array of
        `{ itemId, value, result?, notes? }`). Requires `checklist.manage`.
        Backend-authoritative validation: the execution must be in-scope and
        non-terminal, each item must belong to the execution's template, and
        each `value` must match the item's `itemType` (CHECK/BOOLEAN → boolean,
        NUMBER → number, TEXT → string). Returns `data: {}` on success.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ExecutionIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ChecklistResponsesRequest" }
      responses:
        "200":
          description: Responses saved (empty payload).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: object
                        additionalProperties: false
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /checklist-executions/{executionId}/complete:
    post:
      operationId: completeChecklistExecution
      summary: Complete an in-progress checklist execution
      description: Requires `checklist.manage`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ExecutionIdPathParam"
      responses:
        "200":
          description: The completed execution.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ChecklistExecution"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /checklist-executions/{executionId}/cancel:
    post:
      operationId: cancelChecklistExecution
      summary: Cancel a non-terminal checklist execution
      description: Requires `checklist.manage`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ExecutionIdPathParam"
      responses:
        "200":
          description: The cancelled execution.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ChecklistExecution"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # --------------------------------------------------------------------
  # CR-BE-MOB-CONTRACT-01 PART 04 — Checklist definition, UOM,
  # measurement binding, and evidence requirement authorities.
  # --------------------------------------------------------------------
  /checklist-templates/{templateId}/executions:
    post:
      operationId: createChecklistExecution
      summary: Create a checklist execution from a template
      description: >
        MUTATION — creates a DRAFT checklist execution for an ACTIVE template.
        Requires `checklist.manage` and Client-scope access to the template.
        This is the entry point of the mobile checklist chain
        (Task/Work Order → Checklist execution).
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ChecklistTemplateIdPath"
      responses:
        "201":
          description: Checklist execution created (DRAFT).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ChecklistExecution" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /clients/{clientId}/checklist-templates:
    get:
      operationId: listChecklistTemplates
      summary: List checklist templates for a Client
      description: Requires `checklist.read` and accessible Client scope.
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      responses:
        "200":
          description: Checklist template list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/ChecklistTemplate" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createChecklistTemplate
      summary: Create a checklist template
      description: Requires `checklist.manage` and accessible Client scope.
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateChecklistTemplateRequest" }
      responses:
        "201":
          description: Checklist template created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ChecklistTemplate" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /checklist-templates/{checklistTemplateId}:
    get:
      operationId: getChecklistTemplate
      summary: Get a checklist template
      description: Requires `checklist.read` and Client-scope access.
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ChecklistTemplateIdPath"
      responses:
        "200":
          description: Checklist template.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ChecklistTemplate" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateChecklistTemplate
      summary: Update checklist template metadata/status
      description: Requires `checklist.manage` and Client-scope access.
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ChecklistTemplateIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateChecklistTemplateRequest" }
      responses:
        "200":
          description: Checklist template updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ChecklistTemplate" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /checklist-templates/{checklistTemplateId}/items:
    get:
      operationId: listChecklistItems
      summary: List items of a checklist template
      description: >
        The ordered checklist items of a template. Requires `checklist.read`.
        Item `required` and `itemType` are the backend-authoritative response
        and validation rules (mirrored in the mobile checklist execution read
        model).
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ChecklistTemplateIdPath"
      responses:
        "200":
          description: Checklist items (ordered).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/ChecklistItem" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: createChecklistItem
      summary: Add an item to a checklist template
      description: Requires `checklist.manage`.
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ChecklistTemplateIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateChecklistItemRequest" }
      responses:
        "201":
          description: Checklist item created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ChecklistItem" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /checklist-items/{checklistItemId}:
    patch:
      operationId: updateChecklistItem
      summary: Update a checklist item
      description: Requires `checklist.manage`.
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ChecklistItemIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateChecklistItemRequest" }
      responses:
        "200":
          description: Checklist item updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ChecklistItem" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /clients/{clientId}/uoms:
    get:
      operationId: listUoms
      summary: List UOMs for a Client
      description: >
        The Client's units of measure (measurement/UOM authority). Requires
        `uom.read` and accessible Client scope.
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      responses:
        "200":
          description: UOM list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Uom" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createUom
      summary: Create a UOM
      description: Requires `uom.manage` and accessible Client scope.
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateUomRequest" }
      responses:
        "201":
          description: UOM created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Uom" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /uoms/{uomId}:
    get:
      operationId: getUom
      summary: Get a UOM by id
      description: Requires `uom.read` and Client-scope access.
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/UomIdPath"
      responses:
        "200":
          description: UOM record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Uom" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateUom
      summary: Update a UOM
      description: Requires `uom.manage` and Client-scope access.
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/UomIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateUomRequest" }
      responses:
        "200":
          description: UOM updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Uom" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /checklist-items/{checklistItemId}/measurement:
    patch:
      operationId: bindChecklistItemMeasurement
      summary: Bind measurement/UOM to a checklist item
      description: >
        MUTATION — binds a UOM plus min/max/decimal precision to a numeric
        checklist item (measurement authority). Requires `uom.manage`. The
        item must be NUMBER-typed, the UOM ACTIVE and in the item's Client
        scope, and `minimumValue ≤ maximumValue`. Mirrored in the mobile
        checklist execution read model's `measurement` object.
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ChecklistItemIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BindMeasurementRequest" }
      responses:
        "200":
          description: Measurement bound to the checklist item.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/MeasurementBoundItem" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /form-fields/{formFieldId}/measurement:
    patch:
      operationId: bindFormFieldMeasurement
      summary: Bind measurement/UOM to a form field
      description: >
        MUTATION — binds a UOM plus min/max/decimal precision to a numeric
        form field (the form counterpart of the checklist measurement
        authority). Requires `uom.manage`. Same rules as the checklist item
        binding.
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/FormFieldIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BindMeasurementRequest" }
      responses:
        "200":
          description: Measurement bound to the form field.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/MeasurementBoundItem" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /evidence-requirements:
    get:
      operationId: listEvidenceRequirements
      summary: List evidence requirements
      description: >
        Evidence requirement definitions within the caller's accessible
        Clients. Optional `targetType` / `targetId` / `clientId` filters.
        Requires `evidence.read`. These are the required-evidence rules
        mobile enforces (also embedded in the mobile checklist execution
        read model).
      tags: [Evidence Requirements]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: targetType
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/EvidenceRequirementTargetType"
        - name: targetId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: clientId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Evidence requirement list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/EvidenceRequirement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createEvidenceRequirement
      summary: Create an evidence requirement
      description: >
        Requires `evidence.manage`. Binds a required/optional evidence rule
        (type PHOTO/DOCUMENT/SIGNATURE, min/max count) to a checklist
        template/item or form template/version/section/field.
      tags: [Evidence Requirements]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateEvidenceRequirementRequest" }
      responses:
        "201":
          description: Evidence requirement created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/EvidenceRequirement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /evidence-requirements/{evidenceRequirementId}:
    get:
      operationId: getEvidenceRequirement
      summary: Get an evidence requirement
      description: Requires `evidence.read` and Client-scope access.
      tags: [Evidence Requirements]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/EvidenceRequirementIdPath"
      responses:
        "200":
          description: Evidence requirement record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/EvidenceRequirement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateEvidenceRequirement
      summary: Update an evidence requirement
      description: Requires `evidence.manage` and Client-scope access.
      tags: [Evidence Requirements]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/EvidenceRequirementIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateEvidenceRequirementRequest" }
      responses:
        "200":
          description: Evidence requirement updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/EvidenceRequirement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # CR-BE-EVD-01 PART 05 — Finding/Rework/Verification Evidence
  /findings/{findingId}/evidence:
    parameters:
      - $ref: "#/components/parameters/FindingIdPathParam"
    get:
      operationId: listFindingEvidence
      summary: List Finding Evidence
      description: Requires `evidence.read`; scope is derived from the Finding Building and Client.
      tags: [Findings]
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Finding Evidence list, content: { application/json: { schema: { $ref: "#/components/schemas/EvidenceListResponse" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: createFindingEvidence
      summary: Create Finding Evidence metadata
      description: Creates `FINDING` Evidence with executionId equal to findingId. Requires `evidence.manage`; bytes use the shared file endpoint.
      tags: [Findings]
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/FindingEvidenceRequest" } } } }
      responses:
        "201": { description: Created, content: { application/json: { schema: { $ref: "#/components/schemas/EvidenceResponse" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /findings/{findingId}/rework/{reworkId}/evidence:
    parameters:
      - $ref: "#/components/parameters/FindingIdPathParam"
      - name: reworkId
        in: path
        required: true
        schema: { $ref: "#/components/schemas/Uuid" }
    get: { operationId: listFindingReworkEvidence, summary: List Rework Evidence, tags: [Findings], security: [{ bearerAuth: [] }], responses: { "200": { description: Evidence list, content: { application/json: { schema: { $ref: "#/components/schemas/EvidenceListResponse" } } } }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } } }
    post: { operationId: createFindingReworkEvidence, summary: Create Rework Evidence metadata, description: Creates `FINDING_REWORK` Evidence. Requires `evidence.manage`; the cycle must be REQUESTED., tags: [Findings], security: [{ bearerAuth: [] }], requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/FindingEvidenceRequest" } } } }, responses: { "201": { description: Created, content: { application/json: { schema: { $ref: "#/components/schemas/EvidenceResponse" } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } } }
  /findings/{findingId}/verification/{verificationId}/evidence:
    parameters:
      - $ref: "#/components/parameters/FindingIdPathParam"
      - name: verificationId
        in: path
        required: true
        schema: { $ref: "#/components/schemas/Uuid" }
    get: { operationId: listFindingVerificationEvidence, summary: List Verification Evidence, tags: [Findings], security: [{ bearerAuth: [] }], responses: { "200": { description: Evidence list, content: { application/json: { schema: { $ref: "#/components/schemas/EvidenceListResponse" } } } }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } } }
    post: { operationId: createFindingVerificationEvidence, summary: Create Verification Evidence metadata, description: Creates `FINDING_VERIFICATION` Evidence for a PENDING Finding review. Requires `finding.review`., tags: [Findings], security: [{ bearerAuth: [] }], requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/FindingEvidenceRequest" } } } }, responses: { "201": { description: Created, content: { application/json: { schema: { $ref: "#/components/schemas/EvidenceResponse" } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } } }

  /evidence:
    post:
      operationId: submitEvidence
      summary: Submit evidence metadata
      description: >
        MUTATION — creates an evidence submission metadata record (BE-07).
        Requires `evidence.manage`. Body carries the evidence type, the owning
        execution (FORM_INSTANCE / CHECKLIST_EXECUTION) and the file metadata
        (`fileReference` + `originalFileName` / `mimeType` / `fileSize`);
        optional `evidenceRequirementId` is validated against the ACTIVE
        requirement (type + Client match). Terminal executions are rejected.
        The actual bytes are attached afterwards via
        `POST /evidence/:id/file` (or the single-call `POST /mobile/evidence`).
      tags: [Evidence]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitEvidenceRequest" }
      responses:
        "201":
          description: Evidence submission metadata created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/EvidenceFileMetadata" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    get:
      operationId: listEvidence
      summary: List evidence submissions
      description: >
        Evidence submissions within the caller's accessible Clients. Optional
        `executionId` / `requirementId` filters. Pagination is opt-in via
        `page`/`pageSize`. Requires `evidence.read`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - name: executionId
          in: query
          description: Filter by owning execution (form instance / checklist execution).
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: requirementId
          in: query
          description: Filter by evidence requirement.
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/PageSizeParam"
      responses:
        "200":
          description: Scoped evidence list (full list or requested page).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/EvidenceFileMetadata"
                      meta:
                        $ref: "#/components/schemas/MobilePaginationMeta"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /evidence/{evidenceId}:
    get:
      operationId: getEvidence
      summary: Get an evidence submission (metadata record)
      description: Requires `evidence.read` and Client-scope access.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/EvidenceIdPathParam"
      responses:
        "200":
          description: The evidence submission metadata.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EvidenceFileMetadata"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: removeEvidence
      summary: Remove an evidence submission
      description: >
        MUTATION — sets the evidence submission `status` to REMOVED (never a
        delete). Requires `evidence.manage` and Client-scope access to the
        submission's Client.
      tags: [Evidence]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/EvidenceIdPathParam"
      responses:
        "200":
          description: Evidence submission removed.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EvidenceFileMetadata"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /reviews:
    get:
      operationId: listReviews
      summary: List reviews
      description: >
        Reviews within the caller's accessible Clients. Optional `targetType` /
        `targetId` filters. Pagination is opt-in via `page`/`pageSize`.
        Requires `review.read`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - name: targetType
          in: query
          description: Filter by review target type.
          required: false
          schema:
            type: string
            enum: [FORM_INSTANCE, CHECKLIST_EXECUTION]
        - name: targetId
          in: query
          description: Filter by review target id.
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/PageSizeParam"
      responses:
        "200":
          description: Scoped review list (full list or requested page).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/Review"
                      meta:
                        $ref: "#/components/schemas/MobilePaginationMeta"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /operational-events:
    get:
      operationId: listOperationalEvents
      x-required-permission: operational_event.read
      x-building-scoped: true
      summary: Search operational audit events
      description: >
        Bounded, newest-first operational audit search over the append-only
        `operational_events` authority. Results are always paginated with a
        default page size of 50 and a maximum of 200. The caller's accessible
        Client/Building scope is applied in SQL; an explicit out-of-scope
        Client or Building filter is denied. Ordinary reads do not create
        audit events. Requires `operational_event.read`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          description: Optional accessible Client filter.
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          description: Optional accessible Building filter.
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: actorUserId
          in: query
          description: Filter by the event actor User.
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: eventType
          in: query
          description: Filter by event type.
          required: false
          schema:
            type: string
            maxLength: 128
        - name: entityType
          in: query
          description: Filter by entity type.
          required: false
          schema:
            type: string
            maxLength: 128
        - name: entityId
          in: query
          description: Filter by entity id.
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: requestId
          in: query
          description: Filter by the server request or background-run UUID.
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: source
          in: query
          description: Filter by execution source; null historical sources are not selected by this filter.
          required: false
          schema:
            $ref: "#/components/schemas/OperationalEventSource"
        - name: from
          in: query
          description: Inclusive lower bound for occurredAt (ISO-8601).
          required: false
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          description: Inclusive upper bound for occurredAt (ISO-8601).
          required: false
          schema:
            type: string
            format: date-time
        - name: page
          in: query
          description: 1-based page number; defaults to 1. Audit search is always bounded.
          required: false
          schema:
            type: integer
            minimum: 1
            default: 1
        - name: pageSize
          in: query
          description: Page size; defaults to 50 and is capped at 200.
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
      responses:
        "200":
          description: Scoped operational audit events, newest first.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/OperationalEvent"
                      meta:
                        $ref: "#/components/schemas/MobilePaginationMeta"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /findings/{findingId}/available-actions:
    get:
      operationId: getFindingAvailableActions
      summary: Get backend-authoritative available actions for a finding
      description: >
        Returns the actions the calling user may perform on the finding in its
        current state. Mobile renders only these actions — it never infers
        workflow authority. Requires `finding.read`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: Backend-authoritative available actions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/FindingAvailableActions"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /findings/{findingId}/state:
    get:
      operationId: getFindingState
      summary: Get the current finding workflow state
      description: Requires `finding.read`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: The finding state.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/FindingState"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: transitionFindingState
      summary: Transition the finding state
      description: >
        MUTATION — transitions the finding to a new `state`. The transition is
        validated by the BE-09 transition authority
        (`assertFindingTransitionAllowed`): an invalid/unauthorized transition
        yields 403 `FINDING_ACTION_NOT_ALLOWED` (deterministic). Requires the
        action authority (not a bare `finding.manage`) — the same authority
        `available-actions` reports. No lifecycle states or transitions are
        invented here.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/TransitionFindingStateRequest" }
      responses:
        "200":
          description: The finding after the transition.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Finding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # --------------------------------------------------------------------
  # CR-BE-MOB-CONTRACT-01 PART 05 — Finding, Rework & Verification.
  # Read-only projections + the existing BE-09 mutations; no invented
  # transitions (workflow authority is the action/transition authority).
  # --------------------------------------------------------------------
  /buildings/{buildingId}/findings:
    get:
      operationId: listFindings
      summary: List findings for a Building
      description: >
        Findings within one explicitly accessible Building (BE-02G:
        `requireBuildingAccess`). Requires `finding.read`. Optional `status`
        filter.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/FindingStatus"
      responses:
        "200":
          description: Finding list for the accessible Building.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/Finding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createFinding
      summary: Create a finding
      description: >
        MUTATION — creates a finding in the accessible Building. Requires
        `finding.manage` and an ACTIVE Building assignment
        (`requireBuildingAccess`). `reportedByUserId` is derived from the
        session; status starts OPEN.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateFindingRequest" }
      responses:
        "201":
          description: Finding created (OPEN).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Finding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /findings/{findingId}:
    get:
      operationId: getFinding
      summary: Get a finding by id
      description: >
        The authoritative Finding record. Requires `finding.read` AND an ACTIVE
        Building assignment for the finding's Building (BE-02G). `status` is
        authoritative; clients must not infer transitions — use
        `GET /findings/:id/available-actions`.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: The finding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Finding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateFinding
      summary: Update finding metadata
      description: >
        MUTATION — updates title/description/classification/severity. Requires
        `finding.manage` and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateFindingRequest" }
      responses:
        "200":
          description: The updated finding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Finding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/source:
    get:
      operationId: getFindingSource
      summary: Get the finding source context
      description: >
        The finding's source binding (FORM_INSTANCE / CHECKLIST_EXECUTION /
        WORK_ORDER) and its resolved source context (reference type, id,
        code, title). Requires `finding.read` and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: The finding source context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FindingSourceState" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateFindingSource
      summary: Bind/clear the finding source
      description: >
        MUTATION — binds or clears the finding's source (sourceType/sourceId).
        Requires `finding.manage` and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateFindingSourceRequest" }
      responses:
        "200":
          description: The updated finding source.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FindingSourceState" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/cancel:
    post:
      operationId: cancelFinding
      summary: Cancel a finding
      description: >
        MUTATION — cancels a non-terminal finding. Requires `finding.manage`
        and Building access. Terminal states are rejected.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: The cancelled finding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Finding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/rework:
    get:
      operationId: getFindingRework
      summary: Get the finding rework context
      description: >
        The finding's rework context: current cycle + all rework cycles
        (REQUESTED / RESUBMITTED). Requires `finding.read` and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: The rework context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FindingReworkContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: requestFindingRework
      summary: Request rework on a finding
      description: >
        MUTATION — requests rework with a required `reason`. Requires
        `finding.manage` and Building access (the BE-09 rework authority).
        Does not invent transitions — it delegates to the existing rework
        workflow.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RequestReworkRequest" }
      responses:
        "201":
          description: Rework requested.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FindingReworkContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateFindingReworkNotes
      summary: Update rework notes
      description: >
        MUTATION — updates the current rework cycle's notes. Requires
        `finding.manage` and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateReworkNotesRequest" }
      responses:
        "200":
          description: The updated rework context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FindingReworkContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/reject:
    post:
      operationId: rejectFinding
      summary: Reject a finding (supervisor)
      description: >
        MUTATION — rejects a finding under review with a required `reason`.
        Requires `finding.review` and Building access. This is the existing
        BE-09 rejection authority; no transition is invented.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RequestReworkRequest" }
      responses:
        "200":
          description: The finding after rejection.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Finding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/resubmit:
    post:
      operationId: resubmitFinding
      summary: Resubmit a reworked finding
      description: >
        MUTATION — resubmits a finding after rework. Requires `finding.manage`
        and Building access (BE-09 resubmission authority).
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: The resubmitted finding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Finding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/reviews:
    get:
      operationId: listFindingReviews
      summary: List finding reviews
      description: >
        The finding's supervisor reviews. Requires `finding.read` and Building
        access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: Finding reviews.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/FindingReview" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: openFindingReview
      summary: Open a finding review (supervisor)
      description: >
        MUTATION — opens a review on the finding. Requires `finding.review`
        and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/OpenFindingReviewRequest" }
      responses:
        "201":
          description: Review opened.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FindingReview" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/reviews/current:
    get:
      operationId: getCurrentFindingReview
      summary: Get the current finding review
      description: Requires `finding.read` and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: The current review (or null).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        allOf:
                          - $ref: "#/components/schemas/FindingReview"
                        nullable: true
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/verification:
    get:
      operationId: getFindingVerification
      summary: Get the finding verification state
      description: >
        The finding's verification state: current review, latest verification
        decision, and all reviews. Requires `finding.read` and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: The verification state.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FindingVerificationState" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: submitFindingVerification
      summary: Submit a finding verification decision (supervisor)
      description: >
        MUTATION — submits a supervisor verification decision
        (APPROVED / REJECTED / REWORK_REQUIRED) via the BE-09 verification
        workflow. Requires `finding.review` AND the BE-09 action authority
        (the same authority `available-actions` reports). A completed
        verification is immutable.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitFindingVerificationRequest" }
      responses:
        "200":
          description: The verification state after the decision.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FindingVerificationState" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/assignments:
    get:
      operationId: listFindingAssignments
      summary: List finding assignments
      description: >
        The finding's assignee records (WORKFORCE / TEAM / VENDOR /
        VENDOR_WORKFORCE). Requires `finding.read` and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: Finding assignments.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/FindingAssignment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: assignFinding
      summary: Assign a finding
      description: >
        MUTATION — assigns the finding to a workforce/team/vendor assignee.
        Requires `finding.assign` and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AssignFindingRequest" }
      responses:
        "201":
          description: Assignment created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FindingAssignment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/assignments/current:
    get:
      operationId: getCurrentFindingAssignment
      summary: Get the current finding assignment
      description: Requires `finding.read` and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: The current assignment (or null).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        allOf:
                          - $ref: "#/components/schemas/FindingAssignment"
                        nullable: true
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/assignments/{assignmentId}:
    patch:
      operationId: updateFindingAssignment
      summary: Update a finding assignment
      description: >
        MUTATION — deactivates or reassigns a finding assignment. Requires
        `finding.assign` and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
        - name: assignmentId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - { $ref: "#/components/schemas/AssignFindingRequest" }
                - $ref: "#/components/schemas/DeactivateFindingAssignmentRequest"
      responses:
        "200":
          description: The updated assignment.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FindingAssignment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/history:
    get:
      operationId: getFindingHistory
      summary: Get the finding audit/history
      description: >
        The append-only finding history (audit events). Requires `finding.read`
        and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: Finding history events.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/FindingHistoryEvent" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/closure:
    get:
      operationId: getFindingClosure
      summary: Get the finding closure readiness
      description: >
        The finding's closure info: state, readiness, latest verification
        decision, pending rework, and closure provenance. Requires
        `finding.read` and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: Finding closure info.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FindingClosureInfo" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/close:
    post:
      operationId: closeFinding
      summary: Close a verified finding
      description: >
        MUTATION — closes a VERIFIED finding (optional `closureNotes`).
        Requires `finding.close` and Building access (BE-09 closure authority).
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CloseFindingRequest" }
      responses:
        "200":
          description: The closed finding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Finding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /assets/resolve/{identifier}:
    get:
      operationId: resolveAssetByIdentifier
      summary: Resolve an asset identifier (QR-ready)
      description: >
        Resolves a printed/label identifier (QR) to its asset. The asset's
        Building must be within the caller's accessible scope.
        Requires `asset_identifier.read`.

        CR-BE-MOB-01 PART 05 — this is the single identifier authority behind
        `resolveMobileQr`; the payload is the same opaque BE-05H
        `identifier_value`. The registry is asset-scoped, so no other
        operational context (location, functional location, patrol checkpoint,
        Work Order) can be resolved here. Unchanged by PART 05.
      x-required-permission: asset_identifier.read
      x-building-scoped: true
      x-qr-supported-target-types: [ASSET]
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/IdentifierPathParam"
      responses:
        "200":
          description: The resolved identifier and asset.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/AssetResolution"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /mobile/app-version/{platform}:
    get:
      operationId: getAppVersionMetadata
      summary: Get mobile app version metadata
      description: >
        BE-25M app version metadata contract (public, no authentication — the
        app can check for a required update before login). Returns the
        platform's current supported version, minimum supported version,
        update-required / update-available flags, and release metadata where
        applicable. The optional `appVersion` query param (the caller's
        running version) is used to compute the flags; a client that omits it
        receives the metadata with both flags false. Read-only — no app
        distribution/update delivery and no Flutter UI behavior.
      tags: [Mobile Execution]
      parameters:
        - name: platform
          in: path
          description: Target platform.
          required: true
          schema:
            type: string
            enum: [ANDROID, IOS]
        - name: appVersion
          in: query
          description: The caller's running app version (e.g. 1.2.3). Used to compute the update flags.
          required: false
          schema:
            type: string
      responses:
        "200":
          description: The app version metadata contract.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileAppVersionMetadata"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
  /mobile/diagnostics:
    get:
      operationId: getMobileDiagnostics
      summary: Get mobile API diagnostics metadata
      description: >
        BE-25N basic mobile API health/diagnostic metadata (authenticated):
        correlation id (requestId — the same value as the X-Request-ID
        response header), server time, process uptime, and the sanitized
        mobile device/app context echoed from the request headers
        (X-Device-Id / X-Platform / X-App-Version) when present. Lightweight
        by design — no monitoring platform, no sensitive data.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Mobile API diagnostics metadata.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileDiagnostics"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /mobile/push-tokens:
    post:
      operationId: registerPushToken
      summary: Register (or rotate) a mobile push token
      description: >
        BE-25L push token registration. Registers the authenticated user's
        device push token. Re-registering the same `deviceId` ROTATES the
        token on the existing ACTIVE registration; a token re-registered
        from another device of the same user replaces the old device's
        registration. Returns the ACTIVE registration with registered_at /
        last_seen_at / status.

        CR-BE-MOB-01 PART 07 / CR-BE-PUSH-01 PART 05 — registration is NOT
        delivery, and provider acceptance is NOT delivery. Storing a token
        creates no notification and sends nothing: a successful registration
        must never be presented to the user as "push enabled". A push is only
        ever produced by the shared outbound delivery engine from an existing
        notification, never by this endpoint. Even when a push is sent, an
        accepted attempt means the provider QUEUED it — the backend cannot
        observe whether the device received, displayed or read it, so no
        acceptance may be reported as "delivered". BE-26 delivers IN_APP (the
        authoritative mobile path, `listNotifications` /
        `markNotificationRead`), EMAIL and WHATSAPP; PUSH delivery now exists
        server-side but is deliberately absent from the history read model —
        see `x-push-delivery-lifecycle`. The registration is self-scoped: it
        is bound to the authenticated `userId` and requires no permission
        code, and a user can only ever see or deactivate their own device
        registrations. Registrations the provider rejected are returned with
        status INVALID and should be re-registered.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      x-recipient-scoped: true
      x-push-delivery-lifecycle:
        - stage: 1
          name: TOKEN_REGISTRATION
          status: EXISTING
          authority: BE-25L mobile_push_tokens
          operationIds: [registerPushToken, listPushTokens, deactivatePushToken]
          notes: >-
            Authoritative per user + device, with rotation and deactivation.
            Tokens are stored only; nothing reads them to send anything.
        - stage: 2
          name: NOTIFICATION_CREATION
          status: EXISTING
          authority: BE-26A record + BE-26D subscription + BE-26B template + BE-26C recipient resolution
          operationIds: [listNotifications, getNotification, markNotificationRead]
          notes: >-
            A domain event resolves matching subscriptions, renders the
            template and creates notification records for authoritative
            recipient user ids. The notification record channel enum is
            IN_APP only — creating a notification is not a push.
        - stage: 3
          name: DELIVERY_ATTEMPT
          status: EXISTING
          authority: BE-26E in-app, BE-26F email, BE-26G WhatsApp, BE-26K history, CR-BE-PUSH-01 push attempt evidence
          operationIds: [listNotificationHistory, getNotificationHistoryItem]
          implementedChannels: [IN_APP, EMAIL, WHATSAPP, PUSH]
          missingChannels: []
          notes: >-
            Every implemented channel persists an attempt row with status,
            sent/failed timestamps, provider and failure reason. IN_APP, EMAIL
            and WHATSAPP are surfaced by the BE-26K history read model. PUSH
            attempts are recorded per device as internal, append-only evidence
            (CR-BE-PUSH-01) and are deliberately NOT exposed through the
            history read model or any other endpoint — recorded and auditable
            server-side, but not client-readable.
        - stage: 4
          name: PROVIDER_DELIVERY
          status: EXISTING
          authority: CR-BE-PUSH-01 governed provider adapter (email and WhatsApp have their own adapters)
          operationIds: []
          notes: >-
            A governed, provider-neutral push adapter now exists behind a
            single internal port, invoked only by the shared outbound delivery
            engine over the user's ACTIVE registrations. It is not reachable
            from any endpoint: `operationIds` is empty because no route sends,
            tests, resends or reports a push. Provider acceptance means the
            provider QUEUED the message — it is NOT proof the device received,
            displayed or read it, and neither the backend nor a client may
            present it as such. Mobile continues to rely on the IN_APP inbox
            as the authoritative record, and must never simulate a push
            locally and report it as delivered.
      x-push-delivery-failure-behavior: >-
        For the history-backed channels a failed attempt is persisted as a
        FAILED row carrying `failedAt`, `provider` and `failureReason`, and is
        readable through the history model — failures are recorded, never
        silently dropped, and never retried into a fake success. A failed PUSH
        attempt is likewise recorded per device as internal append-only
        evidence with a sanitized error, and is retried by the SHARED outbound
        retry engine under the same budget as the other channels; it is not
        client-readable. When the provider reports that a registration is no
        longer valid, that device's registration is retired to INVALID and is
        visible as such through `listPushTokens`. A push that the provider
        accepted but the device never displayed remains unobservable to the
        backend: acceptance is not delivery confirmation.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [deviceId, pushToken, platform]
              properties:
                deviceId:
                  type: string
                  maxLength: 128
                  description: Stable client-generated device identifier.
                pushToken:
                  type: string
                  maxLength: 512
                  description: Provider push token (FCM/APNs style opaque token).
                platform:
                  type: string
                  enum: [ANDROID, IOS]
                appVersion:
                  type: string
                  maxLength: 64
                  nullable: true
                deviceModel:
                  type: string
                  maxLength: 128
                  nullable: true
                deviceOsVersion:
                  type: string
                  maxLength: 64
                  nullable: true
      responses:
        "201":
          description: The ACTIVE push token registration.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PushTokenRegistration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    get:
      operationId: listPushTokens
      summary: List the authenticated user's push token registrations
      description: Returns the user's own registrations (newest first). Scoped to the authenticated user only.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: The user's push token registrations.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/PushTokenRegistration"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /mobile/push-tokens/{tokenId}:
    delete:
      operationId: deactivatePushToken
      summary: Deactivate / unregister a push token
      description: >
        Deactivates (unregisters) the authenticated user's push token
        registration. Only the token's owner can deactivate it. The row is
        kept as INACTIVE history.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - name: tokenId
          in: path
          description: Push token registration id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The deactivated registration.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PushTokenRegistration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /notification-templates:
    post:
      operationId: createNotificationTemplate
      summary: Create a notification template
      description: >
        BE-26B notification template foundation. Creates a reusable rendering
        recipe (platform configuration). Requires `notification_template.manage`.
        Every `{{variable}}` used in subject/body must be declared in
        `variables`. The template key is unique and immutable after creation.
      tags: [Notification Templates]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [key, type, channel, subject]
              properties:
                key:
                  type: string
                  maxLength: 64
                  description: Stable, unique template key (uppercase code).
                type:
                  type: string
                  maxLength: 64
                  description: Notification type stamped on rendered notifications.
                channel:
                  type: string
                  enum: [IN_APP]
                subject:
                  type: string
                  maxLength: 500
                  description: Subject/title template with {{variable}} placeholders.
                body:
                  type: string
                  maxLength: 4000
                  nullable: true
                  description: Message/body template with {{variable}} placeholders.
                variables:
                  type: array
                  maxItems: 50
                  description: Declared template variable names.
                  items:
                    type: string
                    pattern: "^[A-Za-z][A-Za-z0-9_]*$"
      responses:
        "201":
          description: The created notification template.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationTemplate"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
    get:
      operationId: listNotificationTemplates
      summary: List notification templates
      description: >
        Lists notification templates (platform configuration), ordered by key.
        Optional `status` filter (ACTIVE / INACTIVE). Requires
        `notification_template.read`.
      tags: [Notification Templates]
      security:
        - bearerAuth: []
      parameters:
        - name: status
          in: query
          description: Optional status filter.
          required: false
          schema:
            type: string
            enum: [ACTIVE, INACTIVE]
      responses:
        "200":
          description: The notification templates.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/NotificationTemplate"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /notification-templates/{notificationTemplateId}:
    get:
      operationId: getNotificationTemplate
      summary: Get a notification template
      description: >
        Returns a single notification template by id. Requires
        `notification_template.read`.
      tags: [Notification Templates]
      security:
        - bearerAuth: []
      parameters:
        - name: notificationTemplateId
          in: path
          description: Notification template id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The notification template.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationTemplate"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateNotificationTemplate
      summary: Update or deactivate a notification template
      description: >
        Updates a notification template's type/channel/subject/body/variables
        or sets `status` (ACTIVE / INACTIVE). The template key is immutable.
        Requires `notification_template.manage`.
      tags: [Notification Templates]
      security:
        - bearerAuth: []
      parameters:
        - name: notificationTemplateId
          in: path
          description: Notification template id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                type:
                  type: string
                  maxLength: 64
                channel:
                  type: string
                  enum: [IN_APP]
                subject:
                  type: string
                  maxLength: 500
                body:
                  type: string
                  maxLength: 4000
                  nullable: true
                variables:
                  type: array
                  maxItems: 50
                  items:
                    type: string
                    pattern: "^[A-Za-z][A-Za-z0-9_]*$"
                status:
                  type: string
                  enum: [ACTIVE, INACTIVE]
      responses:
        "200":
          description: The updated notification template.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationTemplate"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /notification-subscriptions:
    post:
      operationId: createNotificationSubscription
      summary: Create a notification event subscription
      description: >
        BE-26D notification event subscription. Maps an existing backend
        domain event type to a notification trigger: a BE-26B template plus a
        BE-26C recipient rule, with optional Client/Building context. The
        referenced template must exist; the subscription key is unique and
        immutable after creation. Requires `notification_subscription.manage`.
      tags: [Notification Subscriptions]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [key, eventType, templateKey, recipientRule]
              properties:
                key:
                  type: string
                  maxLength: 128
                  description: Stable, unique subscription key (uppercase code).
                eventType:
                  type: string
                  maxLength: 128
                  description: Existing domain event type (e.g. WORK_ORDER_ASSIGNED).
                templateKey:
                  type: string
                  maxLength: 64
                  description: BE-26B notification template key.
                recipientRule:
                  $ref: "#/components/schemas/RecipientRule"
                clientId:
                  allOf:
                    - $ref: "#/components/schemas/Uuid"
                  nullable: true
                  description: Optional Client context (null = any Client).
                buildingId:
                  allOf:
                    - $ref: "#/components/schemas/Uuid"
                  nullable: true
                  description: Optional Building context (null = any Building).
                status:
                  type: string
                  enum: [ACTIVE, INACTIVE]
      responses:
        "201":
          description: The created notification event subscription.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationEventSubscription"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
    get:
      operationId: listNotificationSubscriptions
      summary: List notification event subscriptions
      description: >
        Lists notification event subscriptions (platform configuration),
        ordered by key. Optional `status` filter (ACTIVE / INACTIVE).
        Requires `notification_subscription.read`.
      tags: [Notification Subscriptions]
      security:
        - bearerAuth: []
      parameters:
        - name: status
          in: query
          description: Optional status filter.
          required: false
          schema:
            type: string
            enum: [ACTIVE, INACTIVE]
      responses:
        "200":
          description: The notification event subscriptions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/NotificationEventSubscription"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /notification-subscriptions/{notificationSubscriptionId}:
    get:
      operationId: getNotificationSubscription
      summary: Get a notification event subscription
      description: >
        Returns a single notification event subscription by id. Requires
        `notification_subscription.read`.
      tags: [Notification Subscriptions]
      security:
        - bearerAuth: []
      parameters:
        - name: notificationSubscriptionId
          in: path
          description: Notification event subscription id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The notification event subscription.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationEventSubscription"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateNotificationSubscription
      summary: Update or deactivate a notification event subscription
      description: >
        Updates a notification event subscription's event type, template,
        recipient rule, Client/Building context, or `status` (ACTIVE /
        INACTIVE). The subscription key is immutable. Requires
        `notification_subscription.manage`.
      tags: [Notification Subscriptions]
      security:
        - bearerAuth: []
      parameters:
        - name: notificationSubscriptionId
          in: path
          description: Notification event subscription id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                eventType:
                  type: string
                  maxLength: 128
                templateKey:
                  type: string
                  maxLength: 64
                recipientRule:
                  $ref: "#/components/schemas/RecipientRule"
                clientId:
                  allOf:
                    - $ref: "#/components/schemas/Uuid"
                  nullable: true
                buildingId:
                  allOf:
                    - $ref: "#/components/schemas/Uuid"
                  nullable: true
                status:
                  type: string
                  enum: [ACTIVE, INACTIVE]
      responses:
        "200":
          description: The updated notification event subscription.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationEventSubscription"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /notification-reminders:
    post:
      operationId: createNotificationReminder
      summary: Create a notification reminder
      description: >
        BE-26H notification reminder. A time-deferred notification trigger:
        at `reminderAt` the reminder renders its template, resolves its
        recipients, and triggers in-app notification delivery. The referenced
        template must exist; the reminder key is unique and immutable after
        creation. Requires `notification_reminder.manage`.
      tags: [Notification Reminders]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                [key, clientId, sourceEntityType, sourceEntityId,
                 recipientRule, templateKey, reminderAt]
              properties:
                key:
                  type: string
                  maxLength: 128
                  description: Stable, unique reminder key (uppercase code).
                clientId:
                  $ref: "#/components/schemas/Uuid"
                  description: Client context (Client isolation).
                sourceEntityType:
                  type: string
                  maxLength: 128
                  description: Source/resource domain type being reminded about.
                sourceEntityId:
                  $ref: "#/components/schemas/Uuid"
                  description: Source/resource id being reminded about.
                recipientRule:
                  $ref: "#/components/schemas/RecipientRule"
                templateKey:
                  type: string
                  maxLength: 64
                  description: BE-26B notification template key.
                reminderAt:
                  type: string
                  format: date-time
                  description: When the reminder becomes due.
                variables:
                  type: object
                  additionalProperties: true
                  description: Template variable values snapshot.
      responses:
        "201":
          description: The created notification reminder.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationReminder"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
    get:
      operationId: listNotificationReminders
      summary: List notification reminders
      description: >
        Lists notification reminders (platform configuration), ordered by
        `reminderAt`. Optional `status` filter (PENDING / SENT / CANCELLED).
        Requires `notification_reminder.read`.
      tags: [Notification Reminders]
      security:
        - bearerAuth: []
      parameters:
        - name: status
          in: query
          description: Optional status filter.
          required: false
          schema:
            type: string
            enum: [PENDING, SENT, CANCELLED]
      responses:
        "200":
          description: The notification reminders.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/NotificationReminder"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /notification-reminders/{notificationReminderId}:
    get:
      operationId: getNotificationReminder
      summary: Get a notification reminder
      description: Requires `notification_reminder.read`.
      tags: [Notification Reminders]
      security:
        - bearerAuth: []
      parameters:
        - name: notificationReminderId
          in: path
          description: Notification reminder id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The notification reminder.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationReminder"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateNotificationReminder
      summary: Update a notification reminder
      description: >
        Reschedules/updates a PENDING reminder's `reminderAt`, template,
        recipient rule, or variables. The reminder key is immutable. Requires
        `notification_reminder.manage`.
      tags: [Notification Reminders]
      security:
        - bearerAuth: []
      parameters:
        - name: notificationReminderId
          in: path
          description: Notification reminder id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                reminderAt:
                  type: string
                  format: date-time
                templateKey:
                  type: string
                  maxLength: 64
                recipientRule:
                  $ref: "#/components/schemas/RecipientRule"
                variables:
                  type: object
                  additionalProperties: true
      responses:
        "200":
          description: The updated notification reminder.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationReminder"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /notification-reminders/{notificationReminderId}/cancel:
    post:
      operationId: cancelNotificationReminder
      summary: Cancel a notification reminder
      description: >
        Cancels a PENDING reminder (terminal: PENDING → CANCELLED). Requires
        `notification_reminder.manage`.
      tags: [Notification Reminders]
      security:
        - bearerAuth: []
      parameters:
        - name: notificationReminderId
          in: path
          description: Notification reminder id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The cancelled notification reminder.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationReminder"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /notification-escalations:
    post:
      operationId: createNotificationEscalation
      summary: Create a notification escalation
      description: >
        BE-26I notification escalation. A time-deferred escalation trigger:
        when the current recipient has not resolved the source resource by
        `escalationAt`, the escalation renders its template and triggers
        notification delivery to the escalation recipients. The referenced
        template must exist; the escalation key is unique and immutable after
        creation. Requires `notification_escalation.manage`.
      tags: [Notification Escalations]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                [key, clientId, sourceEntityType, sourceEntityId,
                 escalationRule, templateKey, escalationAt]
              properties:
                key:
                  type: string
                  maxLength: 128
                  description: Stable, unique escalation key (uppercase code).
                clientId:
                  $ref: "#/components/schemas/Uuid"
                  description: Client context (Client isolation).
                sourceEntityType:
                  type: string
                  maxLength: 128
                  description: Source/resource domain type being escalated.
                sourceEntityId:
                  $ref: "#/components/schemas/Uuid"
                  description: Source/resource id being escalated.
                currentRecipientUserId:
                  allOf:
                    - $ref: "#/components/schemas/Uuid"
                  nullable: true
                  description: The current (pre-escalation) recipient.
                escalationRule:
                  $ref: "#/components/schemas/RecipientRule"
                  description: The escalation recipient resolution rule.
                templateKey:
                  type: string
                  maxLength: 64
                  description: BE-26B notification template key.
                escalationAt:
                  type: string
                  format: date-time
                  description: When the escalation becomes due.
                reason:
                  type: string
                  maxLength: 2000
                  nullable: true
                  description: Escalation reason/notes.
      responses:
        "201":
          description: The created notification escalation.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationEscalation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
    get:
      operationId: listNotificationEscalations
      summary: List notification escalations
      description: >
        Lists notification escalations (platform configuration), ordered by
        `escalationAt`. Optional `status` filter (PENDING / TRIGGERED /
        CANCELLED). Requires `notification_escalation.read`.
      tags: [Notification Escalations]
      security:
        - bearerAuth: []
      parameters:
        - name: status
          in: query
          description: Optional status filter.
          required: false
          schema:
            type: string
            enum: [PENDING, TRIGGERED, CANCELLED]
      responses:
        "200":
          description: The notification escalations.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/NotificationEscalation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /notification-escalations/{notificationEscalationId}:
    get:
      operationId: getNotificationEscalation
      summary: Get a notification escalation
      description: Requires `notification_escalation.read`.
      tags: [Notification Escalations]
      security:
        - bearerAuth: []
      parameters:
        - name: notificationEscalationId
          in: path
          description: Notification escalation id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The notification escalation.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationEscalation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateNotificationEscalation
      summary: Update a notification escalation
      description: >
        Reschedules/updates a PENDING escalation's `escalationAt`,
        currentRecipientUserId, escalation rule, template, or reason. The
        escalation key is immutable. Requires `notification_escalation.manage`.
      tags: [Notification Escalations]
      security:
        - bearerAuth: []
      parameters:
        - name: notificationEscalationId
          in: path
          description: Notification escalation id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                escalationAt:
                  type: string
                  format: date-time
                currentRecipientUserId:
                  allOf:
                    - $ref: "#/components/schemas/Uuid"
                  nullable: true
                escalationRule:
                  $ref: "#/components/schemas/RecipientRule"
                templateKey:
                  type: string
                  maxLength: 64
                reason:
                  type: string
                  maxLength: 2000
                  nullable: true
      responses:
        "200":
          description: The updated notification escalation.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationEscalation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /notification-escalations/{notificationEscalationId}/cancel:
    post:
      operationId: cancelNotificationEscalation
      summary: Cancel a notification escalation
      description: >
        Cancels a PENDING escalation (terminal: PENDING → CANCELLED). Requires
        `notification_escalation.manage`.
      tags: [Notification Escalations]
      security:
        - bearerAuth: []
      parameters:
        - name: notificationEscalationId
          in: path
          description: Notification escalation id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The cancelled notification escalation.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationEscalation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /notification-links:
    post:
      operationId: createSecureLink
      summary: Create a secure notification link
      description: >
        BE-26J secure link creation. Generates an opaque, recipient-bound,
        single-purpose token for a target resource + action. The raw token is
        returned exactly once (in `data.token`) and never stored — only its
        SHA-256 hash is persisted. Requires `notification_secure_link.manage`.
      tags: [Notification Secure Links]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                [clientId, recipientUserId, targetEntityType, targetEntityId,
                 action, expiresAt]
              properties:
                clientId:
                  $ref: "#/components/schemas/Uuid"
                  description: Client context (Client isolation).
                recipientUserId:
                  $ref: "#/components/schemas/Uuid"
                  description: The bound recipient (must be an active user).
                targetEntityType:
                  type: string
                  maxLength: 64
                  description: Target resource type (uppercase code).
                targetEntityId:
                  $ref: "#/components/schemas/Uuid"
                  description: Target resource id.
                action:
                  type: string
                  maxLength: 64
                  description: Target action (uppercase code).
                expiresAt:
                  type: string
                  format: date-time
                  description: Link expiry (future timestamp).
                maxUses:
                  type: integer
                  minimum: 1
                  default: 1
                  description: One-time (1) or limited-use (N) behavior.
      responses:
        "201":
          description: The created secure link (raw token in `data.token`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        allOf:
                          - $ref: "#/components/schemas/NotificationSecureLink"
                          - type: object
                            required: [token]
                            properties:
                              token:
                                type: string
                                description: The raw token (returned exactly once).
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /notification-links/resolve:
    post:
      operationId: resolveSecureLink
      summary: Resolve and consume a secure notification link
      description: >
        Validates the token against the authenticated user (recipient binding),
        expiry, revocation, and usage, then consumes a use and returns the
        target resource + action. Expired / revoked / exhausted links are
        rejected. Resolution does NOT perform the action — the client uses the
        normal authorized endpoints afterwards.
      tags: [Notification Secure Links]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [token]
              properties:
                token:
                  type: string
                  description: The opaque secure link token.
      responses:
        "200":
          description: The validated target resource + action.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationSecureLink"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /notification-links/{secureLinkId}:
    get:
      operationId: getSecureLink
      summary: Get a secure link status
      description: >
        Returns a secure link's validation/revocation status (non-consuming).
        Requires `notification_secure_link.read`.
      tags: [Notification Secure Links]
      security:
        - bearerAuth: []
      parameters:
        - name: secureLinkId
          in: path
          description: Secure link record id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The secure link status.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationSecureLink"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /notification-links/{secureLinkId}/revoke:
    post:
      operationId: revokeSecureLink
      summary: Revoke a secure link
      description: >
        Revokes an ACTIVE secure link (ACTIVE → REVOKED). Revoked links are
        rejected at resolution. Requires `notification_secure_link.manage`.
      tags: [Notification Secure Links]
      security:
        - bearerAuth: []
      parameters:
        - name: secureLinkId
          in: path
          description: Secure link record id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The revoked secure link.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationSecureLink"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /notification-history:
    get:
      operationId: listNotificationHistory
      summary: List the authenticated user's notification history
      description: >
        BE-26K notification history read model: a unified, chronological view
        of the recipient's own delivery records across IN_APP / EMAIL /
        WHATSAPP channels, newest first. Optional `channel` and `status`
        filters; pagination is opt-in via `page`/`pageSize`. Read-only —
        no second audit engine, no credential/token exposure.

        CR-BE-MOB-01 PART 07 — the channel enum is exhaustive: it lists every
        channel that can produce a delivery attempt record. PUSH is absent
        because no push delivery attempt exists in the backend, so the absence
        of a PUSH row is not a delivery failure — it means the capability is
        not implemented. Rows are recipient-scoped to the authenticated user.
      tags: [Notification History]
      security:
        - bearerAuth: []
      x-recipient-scoped: true
      x-delivery-channels-implemented: [IN_APP, EMAIL, WHATSAPP]
      x-delivery-channels-missing: [PUSH]
      parameters:
        - name: channel
          in: query
          description: Optional channel filter.
          required: false
          schema:
            type: string
            enum: [IN_APP, EMAIL, WHATSAPP]
        - name: status
          in: query
          description: Optional status filter (channel-specific values).
          required: false
          schema:
            type: string
            enum: [UNREAD, READ, SENT, FAILED]
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/PageSizeParam"
      responses:
        "200":
          description: The recipient's notification history (full list or page).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/NotificationHistoryItem"
                      meta:
                        $ref: "#/components/schemas/MobilePaginationMeta"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /notification-history/{channel}/{historyId}:
    get:
      operationId: getNotificationHistoryItem
      summary: Get one notification history item
      description: >
        Returns a single history item owned by the authenticated user, scoped
        by channel. Missing items (or items belonging to another user) return
        404 NOT_FOUND — no cross-user data is reachable.
      tags: [Notification History]
      security:
        - bearerAuth: []
      parameters:
        - name: channel
          in: path
          description: Delivery channel (IN_APP / EMAIL / WHATSAPP).
          required: true
          schema:
            type: string
            enum: [IN_APP, EMAIL, WHATSAPP]
        - name: historyId
          in: path
          description: The underlying delivery record id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The notification history item.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationHistoryItem"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /notifications:
    get:
      operationId: listNotifications
      summary: List the authenticated user's notifications
      description: >
        BE-26A notification foundation: the recipient's own in-app inbox,
        newest first. Self-scoped to the authenticated user only. Optional
        `status` filter (UNREAD / READ). Pagination is opt-in via
        `page`/`pageSize`; without them the full inbox is returned.
      tags: [Notifications]
      security:
        - bearerAuth: []
      parameters:
        - name: status
          in: query
          description: Optional read-state filter.
          required: false
          schema:
            type: string
            enum: [UNREAD, READ]
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/PageSizeParam"
      responses:
        "200":
          description: The recipient's notifications (full inbox or requested page).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/Notification"
                      meta:
                        $ref: "#/components/schemas/MobilePaginationMeta"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /notifications/{notificationId}:
    get:
      operationId: getNotification
      summary: Get the authenticated user's notification
      description: >
        Returns a single notification owned by the authenticated user.
        Missing notifications (or notifications belonging to another user)
        return 404 NOT_FOUND — no cross-user data is reachable.
      tags: [Notifications]
      security:
        - bearerAuth: []
      parameters:
        - name: notificationId
          in: path
          description: Notification id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The notification record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/Notification"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /notifications/{notificationId}/read:
    patch:
      operationId: markNotificationRead
      summary: Mark the authenticated user's notification as read
      description: >
        Sets the notification to READ and stamps `readAt`. Idempotent — the
        original `readAt` is preserved on repeat calls. Only the recipient
        can mark their own notification read.
      tags: [Notifications]
      security:
        - bearerAuth: []
      parameters:
        - name: notificationId
          in: path
          description: Notification id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The updated (READ) notification record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/Notification"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /mobile/verification/{targetType}/{targetId}:
    get:
      operationId: getMobileVerification
      summary: Get the mobile supervisor verification contract
      description: >
        BE-25J mobile supervisor verification read model: reviewable resource
        reference, supervisor/reviewer context, current verification state,
        verification decision, notes, verified_at, and backend-authoritative
        available actions. Target types: CHECKLIST_EXECUTION / FORM_INSTANCE
        (shared BE-07 review authority), FINDING (BE-09 finding
        verification), and WORK_ORDER (BE-08I verification lifecycle —
        COMPLETED-only, already-APPROVED immutable, REWORK_REQUIRED moves the
        Work Order back to IN_PROGRESS; CR-BE-MOB-03 PART 05). Requires the
        target-type read permission (review.read / finding.read /
        work_order.read) and the BE-02G scope of the target.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/VerificationTargetTypeParam"
        - $ref: "#/components/parameters/VerificationTargetIdParam"
      responses:
        "200":
          description: The mobile supervisor verification contract.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileVerification"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      operationId: submitMobileVerification
      summary: Submit a supervisor verification decision from mobile
      description: >
        BE-25J mobile supervisor verification submission. Accepts
        `{ decision: APPROVED | REJECTED | REWORK_REQUIRED, notes? }` and
        executes through the SAME services as the Web endpoints
        (review.manage for CHECKLIST_EXECUTION / FORM_INSTANCE, finding.review
        + the BE-09 action authority for FINDING, work_order.manage for
        WORK_ORDER — delegating to the BE-08I verification service so the
        mobile and Web surfaces share one lifecycle). A completed
        verification is immutable and is never silently overwritten. Returns
        the refreshed verification contract.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/VerificationTargetTypeParam"
        - $ref: "#/components/parameters/VerificationTargetIdParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [decision]
              properties:
                decision:
                  type: string
                  enum: [APPROVED, REJECTED, REWORK_REQUIRED]
                notes:
                  type: string
                  maxLength: 1000
      responses:
        "201":
          description: The refreshed verification contract after the decision.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileVerification"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /mobile/sync:
    post:
      operationId: processSyncBatch
      summary: Process a mobile offline sync batch
      description: >
        BE-25G offline sync contract. A lightweight batch of
        client-generated operations; every operation is executed through the
        SAME shared services as the REST endpoints — validation, per-item
        RBAC, data scope, and workflow rules are never bypassed and no
        separate mobile business engine exists. Each operation returns a
        per-item result with server status, server timestamp, and the written
        result or a failure code.

        Supported resources (already-implemented mobile operational
        resources only): TASK_EXECUTION (START/COMPLETE/CANCEL),
        CHECKLIST_RESPONSES (SAVE), EVIDENCE_SUBMISSION (SUBMIT metadata
        reference), TASK_ASSIGNMENT (UPDATE status).

        BE-25H idempotency: every operation is keyed by (user, operationId).
        A duplicate retry of the same operationId (in the same or a later
        batch) replays the ORIGINAL stored result — success or failure —
        without executing the write again; the stored result/reference is
        returned as the safe replay response. Concurrent duplicate batches
        are guarded by the unique key.

        BE-25I conflict handling: when an operation carries
        `data.baseVersion` (the resource's `updatedAt` the client last saw)
        and it is older than the CURRENT server version, the item returns a
        SYNC_CONFLICT failure with the current server state/reference and
        retry/reload guidance (`error.conflict`); the write is NOT executed
        and newer server data is never silently overwritten. Operations
        without `baseVersion` behave exactly as before.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      x-required-permission: per-resourceType (see x-sync-supported-resource-types)
      x-building-scoped: true
      x-sync-supported-resource-types:
        - resourceType: TASK_EXECUTION
          operations: [START, COMPLETE, CANCEL]
          operationIds: [startTask, completeTask, cancelTask]
          permission: task.manage
        - resourceType: CHECKLIST_RESPONSES
          operations: [SAVE]
          operationIds: [saveChecklistResponses]
          permission: checklist.manage
        - resourceType: EVIDENCE_SUBMISSION
          operations: [SUBMIT]
          operationIds: [submitEvidence]
          permission: evidence.manage
        - resourceType: TASK_ASSIGNMENT
          operations: [UPDATE]
          operationIds: [updateTaskAssignment]
          permission: task.manage
        - resourceType: PATROL_EXECUTION
          operations: [START, COMPLETE]
          operationIds: [startPatrolExecution, completePatrolExecution]
          permission: patrol_execution.manage
        - resourceType: PATROL_POINT_VISIT
          operations: [SUBMIT]
          operationIds: [recordPatrolPointVisit]
          permission: patrol_execution.manage
        - resourceType: METER_READING
          operations: [SUBMIT]
          operationIds: [submitMeterReading]
          permission: meter_reading_binding.manage
      x-sync-unsupported-resource-types:
        - resourceType: WORK_ORDER_ACTION
          status: MISSING
          reason: >-
            The BE-08 action set is acknowledge / start / hold / resume / note
            / complete / cancel, but the sync envelope has no verb for
            acknowledge, hold, resume or note. Adding a kind that could carry
            only part of the action set would advertise a half-contract, and
            adding verbs is a separate decision.
          useInstead: [acknowledgeWorkOrder, startWorkOrder, completeWorkOrder]
        - resourceType: WORK_ORDER_MATERIAL_USAGE
          status: MISSING
          reason: >-
            `recordWorkOrderMaterialUsage` is a stock-ledger write (STOCK_OUT
            + balance + cost snapshot). Queued offline issues would be applied
            against availability that may no longer exist, and the contract
            has no reservation concept. This needs an explicit product
            decision, not a dispatcher entry.
          useInstead: [recordWorkOrderMaterialUsage]
        - resourceType: INCIDENT
          status: MISSING
          reason: >-
            Incident and Operational Incident creation requires a
            client-supplied `incidentNumber` that must be unique per Client;
            offline devices cannot allocate one safely, and the sync envelope
            keys every operation to an EXISTING authoritative `resourceId`.
          useInstead: [createIncident, createOperationalIncident]
        - resourceType: SECURITY_FINDING
          status: MISSING
          reason: >-
            Same create-shaped gap as INCIDENT — `createSecurityFinding`
            creates a new BE-09 Finding, so there is no prior authoritative
            resourceId to key the operation to.
          useInstead: [createSecurityFinding, createEngineeringFinding]
        - resourceType: SUPERVISOR_DECISION
          status: MISSING
          reason: >-
            Supervisor verification is an online reviewer act on a completed
            execution; queueing a stale approve/reject offline would let a
            decision be applied to state the reviewer never saw. Deliberately
            excluded.
          useInstead:
            [submitSupervisorInspectionDecision, submitMobileVerification,
             submitWorkOrderVerification]
        - resourceType: EVIDENCE_BYTES
          status: NOT_REQUIRED
          reason: >-
            The backend must not hold an offline byte queue. EVIDENCE_SUBMISSION
            syncs metadata only; bytes go to the evidence file API.
          useInstead: [uploadMobileEvidence, uploadEvidenceFile]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MobileSyncBatchRequest"
      responses:
        "200":
          description: The batch result with per-item results.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileSyncBatchResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /mobile/qr/resolve/{identifier}:
    get:
      operationId: resolveMobileQr
      summary: Resolve a QR/identifier from mobile
      description: >
        BE-25F mobile QR resolution contract. Resolves an opaque identifier
        (QR label, tag, barcode) through the existing BE-05H Asset Identifier
        authority and returns the discriminated target with Building /
        Functional-Location context, Asset / Equipment context where
        applicable, and available mobile action hints. Requires
        `asset_identifier.read`.

        Access is restricted to the BE-02G accessible set: an identifier whose
        Asset's Building is not accessible yields the SAME 404 as an unknown
        value (existence is hidden). No separate QR engine; the existing
        resolve endpoint remains unchanged.

        CR-BE-MOB-01 PART 05 — frozen QR payload convention. The scanned
        payload is the OPAQUE `identifier_value` of a row in the BE-05H
        `asset_identifiers` registry (`identifierType` QR / TAG / BARCODE /
        LEGACY). It is deliberately meaningless: no Client, Building, URL or
        security data is encoded, so a label read by anyone leaks nothing. It
        is NOT a canonical UUID and NOT a structured deep link. The identifier
        registry is asset-scoped — identifiers are registered only through
        `POST /assets/{assetId}/identifiers` — therefore ASSET is the only
        target type this operation can ever return today
        (`x-qr-supported-target-types`).

        Scanning is never anonymous: the caller must present a valid session
        and hold `asset_identifier.read`, and the result is filtered by the
        caller's accessible Buildings. The backend records no GPS coordinate
        and performs no geofence check — scan position is not an input to,
        or an output of, this contract.

        Operational contexts that are NOT scan-resolvable are listed in
        `x-qr-target-type-boundary`. For each of them the client must use the
        authoritative canonical-id read named there; the backend must not be
        asked to guess, and such a target must never be mapped onto ASSET.
      x-required-permission: asset_identifier.read
      x-building-scoped: true
      x-qr-supported-target-types: [ASSET]
      x-qr-target-type-boundary:
        - targetType: LOCATION
          status: MISSING
          reason: >-
            BE-04 Floor / Area / Room / Space rows have a `code` that is
            unique per parent, but there is no location identifier registry
            and no resolve-by-identifier route. Nothing exists to resolve a
            scanned label to a location.
          authority: BE-04 Structure
          canonicalIdReads:
            [getBuildingHierarchy, listBuildingFloors, getFloor, getArea,
             getRoom, getSpace]
        - targetType: FUNCTIONAL_LOCATION
          status: MISSING
          reason: >-
            Same as LOCATION — BE-04 Functional Locations carry no identifier
            registry. The context projection is already published.
          authority: BE-04 Functional Locations
          canonicalIdReads:
            [getFunctionalLocation, getFunctionalLocationContext,
             listBuildingFunctionalLocations]
        - targetType: CHECKPOINT
          status: MISSING
          reason: >-
            BE-12B patrol route points are identified by canonical UUID plus
            sequence and location FKs; they carry no identifier value.
            Checkpoint confirmation is therefore by canonical `pointId`
            (`recordPatrolPointVisit`), not by scan.
          authority: BE-12B Patrol Route Points + BE-12D Patrol Execution
          canonicalIdReads:
            [listPatrolRoutePoints, getPatrolExecution, listPatrolPointVisits]
        - targetType: WORK_ORDER
          status: MISSING
          reason: >-
            BE-08 Work Orders have a human-readable `workOrderNumber` but no
            identifier registry and no resolve-by-number route. Mobile reaches
            a Work Order through its assignment feed or canonical id.
          authority: BE-08 Work Order
          canonicalIdReads:
            [listMobileAssignments, getWorkOrder, listBuildingWorkOrders]
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/IdentifierPathParam"
      responses:
        "200":
          description: The mobile QR resolution contract.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileQrResolution"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # --------------------------------------------------------------------
  # CR-BE-MOB-CONTRACT-01 PART 06 — Location hierarchy & operational
  # context. Read-only projections of the BE-04 / BE-05 authorities.
  # --------------------------------------------------------------------
  /buildings/{buildingId}/hierarchy:
    get:
      operationId: getBuildingHierarchy
      summary: Get the Building digital structure
      description: >
        The complete authoritative digital structure of one Building (BE-04H):
        floors → areas → rooms (with Room Type) → spaces (with pinned
        Functional Locations), plus Building-level Functional Locations.
        Requires `building.read` and an ACTIVE Building assignment
        (`requireBuildingAccess`). Read-only projection — never stored.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: The Building digital structure.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/BuildingHierarchy" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /functional-locations/{functionalLocationId}/context:
    get:
      operationId: getFunctionalLocationContext
      summary: Get the operational context of a Functional Location
      description: >
        The authoritative operational location context (BE-04H) behind one
        Functional Location — location hierarchy only, never Asset/Equipment
        records. Requires `functional_location.read` AND an ACTIVE Building
        assignment for the Functional Location's Building (403
        BUILDING_ACCESS_DENIED otherwise). Only levels that exist are present.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FunctionalLocationIdPath"
      responses:
        "200":
          description: The functional location operational context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/OperationalContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/floors:
    get:
      operationId: listBuildingFloors
      summary: List floors for a Building
      description: Requires `floor.read` and an ACTIVE Building assignment.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Floor list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicFloor" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /floors/{floorId}:
    get:
      operationId: getFloor
      summary: Get a floor by id
      description: Requires `floor.read` and Building access (resolved from the floor).
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FloorIdPath"
      responses:
        "200":
          description: Floor record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicFloor" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /floors/{floorId}/areas:
    get:
      operationId: listFloorAreas
      summary: List areas for a floor
      description: Requires `area.read` and Building access (resolved from the floor).
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FloorIdPath"
      responses:
        "200":
          description: Area list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicArea" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /areas/{areaId}:
    get:
      operationId: getArea
      summary: Get an area by id
      description: Requires `area.read` and Building access (resolved from the area).
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/AreaIdPath"
      responses:
        "200":
          description: Area record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicArea" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /areas/{areaId}/rooms:
    get:
      operationId: listAreaRooms
      summary: List rooms for an area
      description: Requires `room.read` and Building access (resolved from the area).
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/AreaIdPath"
      responses:
        "200":
          description: Room list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicRoom" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /rooms/{roomId}:
    get:
      operationId: getRoom
      summary: Get a room by id
      description: Requires `room.read` and Building access (resolved from the room).
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/RoomIdPath"
      responses:
        "200":
          description: Room record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicRoom" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /rooms/{roomId}/spaces:
    get:
      operationId: listRoomSpaces
      summary: List spaces for a room
      description: Requires `space.read` and Building access (resolved from the room).
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/RoomIdPath"
      responses:
        "200":
          description: Space list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicSpace" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /spaces/{spaceId}:
    get:
      operationId: getSpace
      summary: Get a space by id
      description: Requires `space.read` and Building access (resolved from the space).
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/SpaceIdPath"
      responses:
        "200":
          description: Space record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicSpace" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/functional-locations:
    get:
      operationId: listBuildingFunctionalLocations
      summary: List Functional Locations for a Building
      description: Requires `functional_location.read` and an ACTIVE Building assignment.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Functional Location list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicFunctionalLocation" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /functional-locations/{functionalLocationId}:
    get:
      operationId: getFunctionalLocation
      summary: Get a Functional Location by id
      description: Requires `functional_location.read` and Building access.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FunctionalLocationIdPath"
      responses:
        "200":
          description: Functional Location record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicFunctionalLocation" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/assets:
    get:
      operationId: listBuildingAssets
      summary: List assets for a Building
      description: >
        Requires `asset.read` and an ACTIVE Building assignment. Optional
        `status` filter.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/AssetStatus" }
      responses:
        "200":
          description: Asset list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicAsset" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /assets/{assetId}:
    get:
      operationId: getAsset
      summary: Get an asset by id
      description: >
        The authoritative Asset record (BE-05A). Requires `asset.read` and an
        ACTIVE Building assignment for the asset's Building (403
        BUILDING_ACCESS_DENIED otherwise).
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: Asset record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicAsset" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /assets/{assetId}/location:
    get:
      operationId: getAssetLocation
      summary: Get the asset location / operational context
      description: >
        The asset plus its authoritative resolved operational location context
        (BE-05C). Requires `asset.read` and Building access.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: The asset and its location context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/AssetLocation" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /assets/{assetId}/equipment-profile:
    get:
      operationId: getEquipmentProfile
      summary: Get the asset's equipment profile
      description: >
        The Equipment Profile bound to an asset (BE-05D), or null when none.
        Requires `equipment_profile.read` and Building access.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: The equipment profile (or null).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        allOf:
                          - $ref: "#/components/schemas/PublicEquipmentProfile"
                        nullable: true
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /mobile/evidence:
    post:
      operationId: uploadMobileEvidence
      summary: Upload evidence from mobile (single call)
      description: >
        BE-25E mobile evidence upload contract. One multipart request that
        validates the evidence requirement/type/context, stores the file bytes
        through the CR-BE-API-01 PART 03 storage abstraction (never in
        PostgreSQL) and creates the evidence submission in a single step.
        Requires `evidence.manage`.

        Multipart fields: `file` (max 50 MB), `evidenceType`
        (PHOTO/DOCUMENT/SIGNATURE), `executionType`
        (FORM_INSTANCE/CHECKLIST_EXECUTION), `executionId`,
        optional `evidenceRequirementId`, `capturedAt` (ISO-8601),
        `originalFileName`. Internal storage paths are never exposed.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file, evidenceType, executionType, executionId]
              properties:
                file:
                  type: string
                  format: binary
                  description: Max 50 MB; MIME type must match the evidence type.
                evidenceType:
                  type: string
                  enum: [PHOTO, DOCUMENT, SIGNATURE]
                executionType:
                  type: string
                  enum: [FORM_INSTANCE, CHECKLIST_EXECUTION]
                executionId:
                  type: string
                  format: uuid
                evidenceRequirementId:
                  type: string
                  format: uuid
                capturedAt:
                  type: string
                  format: date-time
                originalFileName:
                  type: string
      responses:
        "201":
          description: The created mobile evidence contract.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileEvidence"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /mobile/evidence/{evidenceId}:
    get:
      operationId: getMobileEvidence
      summary: Get mobile evidence reference
      description: >
        BE-25E mobile evidence reference read model for an existing evidence
        submission: requirement reference, target task/checklist/work
        reference, file metadata, evidence type, captured_at, upload status,
        and Client/Building context. Requires `evidence.read`; access is bound
        to the evidence record's Client scope (BE-02G).
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/EvidenceIdPathParam"
      responses:
        "200":
          description: The mobile evidence contract.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileEvidence"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /mobile/checklist-executions/{executionId}:
    get:
      operationId: getMobileChecklistExecution
      summary: Get a checklist execution for mobile (checklist mobile contract)
      description: >
        BE-25D mobile checklist execution read model. Returns the execution
        with its checklist/task reference, checklist items (values/status),
        measurement/UOM where applicable, evidence requirements, execution
        status, and backend-authoritative available actions. Requires
        `checklist.read`; the execution's Client must be inside the caller's
        accessible scope (BE-02G). Composition over the BE-07 authorities
        only — no separate mobile checklist engine. Evidence submission is
        the BE-25E contract.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ExecutionIdPathParam"
      responses:
        "200":
          description: The mobile checklist execution contract.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileChecklistExecution"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /mobile/assignments:
    get:
      operationId: listMobileAssignments
      summary: List my assignments (mobile field-user feed)
      description: >
        BE-25C unified mobile assignment feed. Returns the authenticated
        user's active Task (BE-07) and Work Order (BE-08) assignments — direct
        WORKFORCE assignments of the user's linked Workforce Profile and TEAM
        assignments of the user's Team — restricted to the accessible
        Client/Building scope (BE-02G). Requires `task.read` and
        `work_order.read`.

        `availableActions` is backend-authoritative (the same authorities the
        execution endpoints enforce) and gated on the caller's
        `task.manage` / `work_order.manage` permissions. Work orders expose
        execution actions (ACKNOWLEDGE, START, HOLD, RESUME, ADD_NOTE,
        CANCEL) plus CLOSE only when every closure rule is satisfied;
        `schedule.dueAt` is always null (no authoritative due date
        exists yet). Pagination is opt-in via `page`/`pageSize` (BE-25A
        convention).
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/PageSizeParam"
      responses:
        "200":
          description: Scoped mobile assignment feed (full list or requested page).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/MobileAssignment"
                      meta:
                        $ref: "#/components/schemas/MobilePaginationMeta"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  # --------------------------------------------------------------------
  # CR-BE-MOB-02 PART 07C — Mobile Current Shift effective-context contract.
  #
  # A small self-service effective-context endpoint. The backend derives the
  # authenticated user's current shift(s) from the linked BE-03C Workforce
  # Profile, the BE-03E Shift roster and the BE-02F/G Building access
  # authority — never from caller-supplied identity and never the generic
  # Shift CRUD surface. Authentication-only (same posture as GET /auth/me).
  # --------------------------------------------------------------------
  /mobile/current-shift:
    get:
      operationId: getMobileCurrentShift
      summary: Resolve the authenticated user's effective current shift
      description: >-
        BE-25M self-service effective-context read. Derives the caller's
        current shift(s) from the linked BE-03C Workforce Profile, the BE-03E
        Shift roster (`workforce_shift_assignments`) and the BE-02F/G
        accessible-Building authority — never from caller-supplied identity
        and never from the generic Shift CRUD surface. A Shift is current
        only when: its roster assignment is ACTIVE and its optional
        `effectiveFrom`/`effectiveUntil` window contains the current instant;
        the Shift itself is ACTIVE and in an accessible Building that carries
        an IANA `timezone`; and the current time-of-day in that timezone falls
        inside the Shift's `startTime`/`endTime` window (overnight-aware).
        A Building without a timezone cannot affirm "current", so its shifts
        are excluded rather than guessed. Requires only a valid bearer session
        (self-service, like `GET /auth/me`); an empty `shifts` array means the
        caller is not on shift at the derivation instant. Authoritative ids:
        `shiftId` = `shifts.id`, `assignmentId` =
        `workforce_shift_assignments.id`, `workforceProfileId` =
        `workforce_profiles.id`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Effective current-shift context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileCurrentShiftContext"
        "401":
          $ref: "#/components/responses/Unauthorized"

  # --------------------------------------------------------------------
  # CR-BE-MOB-05 PART 01 — Mobile Upcoming Shifts self-service contract.
  #
  # A read-only, caller-scoped schedule of the authenticated user's current
  # and future shift assignments, derived from the same authorities as
  # `GET /mobile/current-shift` (BE-25M family): the linked BE-03C Workforce
  # Profile, the BE-03E Shift roster (`workforce_shift_assignments`), the
  # BE-03E `shifts` and `buildings` records, and the BE-02F/G accessible
  # Building authority — never from caller-supplied identity and never the
  # generic Shift CRUD surface. No attendance, clock, roster, handover, or
  # schedule state is invented: only ACTIVE roster assignments of ACTIVE
  # Shifts in accessible Buildings whose effective window overlaps the
  # queried window are returned. Authentication-only self-service (same
  # posture as GET /auth/me and GET /mobile/current-shift).
  # --------------------------------------------------------------------
  /mobile/upcoming-shifts:
    get:
      operationId: getMobileUpcomingShifts
      summary: Resolve the authenticated user's current and future shift assignments
      description: >-
        CR-BE-MOB-05 PART 01 self-service read. Derives the caller's
        current/future shift schedule from the linked BE-03C Workforce
        Profile, the BE-03E Shift roster (`workforce_shift_assignments`) and
        the BE-02F/G accessible-Building authority — never from
        caller-supplied identity and never from the generic Shift CRUD
        surface. A roster assignment is included only when it is ACTIVE, its
        Shift is ACTIVE, its Building is in the caller's accessible set and
        carries a valid IANA `timezone`, and its optional
        `effectiveFrom`/`effectiveUntil` window overlaps the queried window
        (default: from the current instant, unbounded into the future).
        A Building without a valid timezone cannot be exposed safely, so its
        shifts are excluded rather than guessed. The daily wall-clock window
        is NOT evaluated here — `GET /mobile/current-shift` remains the
        single authority for "what is live right now"; this endpoint lists
        the caller's rostered current/future assignments. Optional
        `dateFrom` / `dateTo` bound the roster effective window (ISO-8601
        date or datetime; a date-only `dateTo` is inclusive of that whole
        UTC day; `dateFrom` must not exceed `dateTo`; range capped at 366
        days). Requires only a valid bearer session (self-service, like
        `GET /auth/me` and `GET /mobile/current-shift`); an empty `shifts`
        array means no matching roster. Authoritative ids: `shiftId` =
        `shifts.id`, `assignmentId` = `workforce_shift_assignments.id`,
        `workforceProfileId` = `workforce_profiles.id`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
          description: >-
            Optional inclusive lower bound on the roster effective window
            (ISO-8601 date YYYY-MM-DD treated as a UTC day, or datetime).
            Defaults to the current instant.
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
          description: >-
            Optional upper bound on the roster effective window (ISO-8601
            date YYYY-MM-DD inclusive of that whole UTC day, or datetime).
            Omitted = unbounded.
      responses:
        "200":
          description: Effective upcoming-shifts schedule context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileUpcomingShiftsContext"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  # --------------------------------------------------------------------
  # CR-BE-MOB-02 PART 07D — Mobile My Team effective-context contract.
  #
  # A small self-service effective-context endpoint. The backend derives the
  # authenticated user's own team and its ACTIVE members from the linked
  # BE-03C Workforce Profile (`team_id`) and the BE-03B Team hierarchy
  # (Team → Department → Organization → Client) — never from caller-supplied
  # workforceProfileId / teamId and never the generic Team CRUD surface.
  # Authentication-only (same posture as GET /auth/me).
  # --------------------------------------------------------------------
  /mobile/my-team:
    get:
      operationId: getMobileMyTeam
      summary: Resolve the authenticated user's team context and members
      description: >-
        BE-25N self-service effective-context read. Derives the caller's own
        team (the team of their linked BE-03C Workforce Profile) and its ACTIVE
        members from the session identity — never from caller-supplied
        `workforceProfileId` or `teamId` and never from the generic Team CRUD
        surface. The team hierarchy (Team → Department → Organization →
        Client) is exposed only when the profile's Client is inside the
        caller's accessible scope (BE-02F/G). `team` is null when the caller
        has no linked ACTIVE profile, no team, or a profile whose Client is
        outside the accessible scope; `members` lists ACTIVE profiles in that
        team (the caller included). Requires only a valid bearer session
        (self-service, like `GET /auth/me` and `GET /mobile/current-shift`).
        Authoritative ids: team = `teams.id`, members = `workforce_profiles.id`
        (+ `positions.id`), hierarchy refs = `departments.id` /
        `organizations.id` / `clients.id`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Effective my-team context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileMyTeamContext"
        "401":
          $ref: "#/components/responses/Unauthorized"

  # --------------------------------------------------------------------
  # CR-BE-MOB-05 PART 02 — Workforce Attendance self-service contract.
  #
  # Backend-authoritative clock-in / clock-out for the authenticated
  # workforce profile. Identity is resolved from the authenticated session
  # (linked BE-03C Workforce Profile) and never accepted from the client;
  # timestamps are set by the backend database clock (NOW()) and never
  # accepted from the client. Clock-in asserts BE-02F/G Building access and
  # binds the applicable ACTIVE BE-03E roster row when one exists (optional
  # "when available" binding — never invented, never derived from Current
  # Shift / task / handover / Team). Lifecycle: CLOCKED_IN → CLOCKED_OUT;
  # at most one open clock-in per profile (409 on duplicate); clock-out
  # without an open record → 409. Self-service gated by the dedicated
  # `attendance.manage` (clock-in/out) and `attendance.read` (current)
  # codes — no attendance administration surface exists.
  # --------------------------------------------------------------------
  /attendance/clock-in:
    post:
      operationId: clockInAttendance
      summary: Clock the authenticated workforce profile into a Building
      description: >-
        CR-BE-MOB-05 PART 02. Opens a CLOCKED_IN attendance record for the
        authenticated Workforce Profile (resolved from the session, never
        from the request) at the supplied `buildingId`. The Building is
        access-asserted (BE-02F/G, 403 when inaccessible), must be ACTIVE
        and must resolve to the profile's own Client. When the profile holds
        an applicable ACTIVE roster assignment at the Building (effective
        window containing now, wall-clock-current preferred among several),
        the record binds the authoritative `workforce_shift_assignment_id`
        and `shiftId`; otherwise the binding is null — attendance is never
        derived from Current Shift, tasks, handovers, Team, or local state.
        `clockInAt` is the backend database timestamp. Duplicate open
        clock-in → 409 `ATTENDANCE_ACTIVE_ALREADY_EXISTS`. Requires
        `attendance.manage`.
      tags: [Attendance]
      security:
        - bearerAuth: []
      x-required-permission: attendance.manage
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ClockInRequest" }
      responses:
        "201":
          description: The authoritative attendance record (CLOCKED_IN).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/AttendanceRecord" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /attendance/clock-out:
    post:
      operationId: clockOutAttendance
      summary: Clock the authenticated workforce profile out
      description: >-
        CR-BE-MOB-05 PART 02. Closes the authenticated profile's open
        CLOCKED_IN attendance record: `clockOutAt` is the backend database
        timestamp and the status becomes CLOCKED_OUT (terminal, preserved).
        The record is resolved from the session — no payload and no
        caller-supplied identity or timestamp. Clock-out without an open
        record → 409 `ATTENDANCE_NOT_ACTIVE`. Requires `attendance.manage`.
      tags: [Attendance]
      security:
        - bearerAuth: []
      x-required-permission: attendance.manage
      responses:
        "200":
          description: The authoritative attendance record (CLOCKED_OUT).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/AttendanceRecord" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "409": { $ref: "#/components/responses/Conflict" }

  /attendance/current:
    get:
      operationId: getCurrentAttendance
      summary: Resolve the authenticated profile's open attendance record
      description: >-
        CR-BE-MOB-05 PART 02. Returns the authenticated profile's open
        CLOCKED_IN attendance record, or null when the profile has no linked
        ACTIVE Workforce Profile or no open record. Self-service: only the
        caller's own record is ever returned. Requires `attendance.read`.
        Authoritative ids: `id` = `attendance_records.id`,
        `workforceProfileId` = `workforce_profiles.id`, `buildingId` =
        `buildings.id`, `workforceShiftAssignmentId` =
        `workforce_shift_assignments.id`, `shiftId` = `shifts.id`.
      tags: [Attendance]
      security:
        - bearerAuth: []
      x-required-permission: attendance.read
      responses:
        "200":
          description: The open attendance record, or null.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        nullable: true
                        allOf:
                          - $ref: "#/components/schemas/AttendanceRecord"
        "401": { $ref: "#/components/responses/Unauthorized" }

  # --------------------------------------------------------------------
  # CR-BE-MOB-05 PART 03 — Security Operational Logbook contract.
  #
  # Backend-authoritative, Building-scoped Security logbook entries. Each
  # entry binds the authenticated Workforce Profile (resolved from the
  # session, never from the request), the Building it belongs to (BE-02F/G
  # access-asserted on every operation — no cross-Building reads or writes),
  # and an optional existing authoritative `shift_handovers.id` in the SAME
  # Building (unknown → 404; cross-Building → 400). `recordedAt` is set by
  # the backend database clock only. Lifecycle OPEN → CLOSED: updates are
  # allowed only while OPEN; CLOSED is terminal (400
  # SECURITY_LOGBOOK_ENTRY_IMMUTABLE). Category is a fixed classification
  # vocabulary; the backend branches no behavior on it.
  # --------------------------------------------------------------------
  /buildings/{buildingId}/security/logbook:
    post:
      operationId: createSecurityLogbookEntry
      summary: Create a Security logbook entry at a Building
      description: >-
        CR-BE-MOB-05 PART 03. Records a Building-scoped Security logbook
        entry for the authenticated Workforce Profile (session-resolved,
        never caller-supplied) at the supplied Building (BE-02F/G access-
        asserted; 403 outside scope). The optional `shiftHandoverId` must
        reference an existing authoritative `shift_handovers.id` in the SAME
        Building — unknown → 404 `SHIFT_HANDOVER_NOT_FOUND`, cross-Building →
        400 `SECURITY_LOGBOOK_HANDOVER_BUILDING_MISMATCH`. Outgoing/incoming
        Shift IDs are never invented. `recordedAt` is the backend database
        timestamp. Status is OPEN on creation. Requires
        `security_logbook.manage`.
      tags: [Security Logbook]
      security:
        - bearerAuth: []
      x-required-permission: security_logbook.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSecurityLogbookEntryRequest"
      responses:
        "201":
          description: The authoritative logbook entry (OPEN).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SecurityLogbookEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    get:
      operationId: listSecurityLogbookEntries
      summary: List Security logbook entries of a Building
      description: >-
        CR-BE-MOB-05 PART 03. Lists the Building's logbook entries, newest
        first, constrained to the caller's accessible Buildings (BE-02F/G;
        Building-nested route with `requireBuildingAccess`). Optional
        `status` / `category` filters and a `dateFrom` / `dateTo` window on
        `recordedAt` (ISO-8601 date or datetime; a date-only `dateTo` is
        inclusive of that whole UTC day; range capped at 366 days). Requires
        `security_logbook.read`.
      tags: [Security Logbook]
      security:
        - bearerAuth: []
      x-required-permission: security_logbook.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/SecurityLogbookStatus" }
        - name: category
          in: query
          required: false
          schema: { $ref: "#/components/schemas/SecurityLogbookCategory" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
          description: Inclusive lower bound on recordedAt (ISO-8601 date or datetime).
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
          description: >-
            Upper bound on recordedAt (ISO-8601 date inclusive of that whole
            UTC day, or datetime).
      responses:
        "200":
          description: The Building's logbook entries.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/SecurityLogbookEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /security/logbook/{id}:
    get:
      operationId: getSecurityLogbookEntry
      summary: Resolve a Security logbook entry
      description: >-
        CR-BE-MOB-05 PART 03. Returns one authoritative logbook entry; the
        caller must have BE-02F/G access to the entry's Building (an unknown
        id → 404; an entry in an inaccessible Building → 403
        `BUILDING_ACCESS_DENIED` — never a cross-Building leak). Requires
        `security_logbook.read`. Authoritative id: `id` =
        `security_logbook_entries.id`.
      tags: [Security Logbook]
      security:
        - bearerAuth: []
      x-required-permission: security_logbook.read
      x-building-scoped: true
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: The authoritative logbook entry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SecurityLogbookEntry" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateSecurityLogbookEntry
      summary: Update a Security logbook entry while OPEN
      description: >-
        CR-BE-MOB-05 PART 03. Updates category / summary / detail / status
        / optional handover binding of an OPEN entry. CLOSED entries are
        terminal → 400 `SECURITY_LOGBOOK_ENTRY_IMMUTABLE`. The entry's
        Building is access-asserted (BE-02F/G): an unknown id → 404; an
        entry in an inaccessible Building → 403 `BUILDING_ACCESS_DENIED`. A
        supplied `shiftHandoverId` must reference an existing authoritative
        `shift_handovers.id` in the SAME Building (unknown → 404;
        cross-Building → 400). Timestamps are never accepted from the
        client. Requires `security_logbook.manage`.
      tags: [Security Logbook]
      security:
        - bearerAuth: []
      x-required-permission: security_logbook.manage
      x-building-scoped: true
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSecurityLogbookEntryRequest"
      responses:
        "200":
          description: The updated authoritative logbook entry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SecurityLogbookEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # --------------------------------------------------------------------
  # CR-BE-BAST-01 — canonical BE-22 lifecycle, compatibility and closure
  # --------------------------------------------------------------------
  /bast-documents:
    post:
      operationId: createCanonicalBastDocument
      summary: Create a canonical BAST and its shared Document/Version
      description: >
        Creates the sole authoritative BAST identity (`bast_documents.id`).
        `documentType` must be BAST. Work Order remains closure authority.
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateBastDocumentRequest" }
      responses:
        "201":
          description: Canonical BAST created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/BastDocument" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listCanonicalBastDocuments
      summary: List canonical BAST documents in accessible scope
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: workOrderId, in: query, schema: { $ref: "#/components/schemas/Uuid" } }
        - { name: vendorWorkId, in: query, schema: { $ref: "#/components/schemas/Uuid" } }
        - { name: workCompletionDocumentId, in: query, schema: { $ref: "#/components/schemas/Uuid" } }
        - { name: buildingId, in: query, schema: { $ref: "#/components/schemas/Uuid" } }
        - { name: clientId, in: query, schema: { $ref: "#/components/schemas/Uuid" } }
        - { name: contextType, in: query, schema: { $ref: "#/components/schemas/BastContextType" } }
        - { name: acceptanceStatus, in: query, schema: { $ref: "#/components/schemas/BastStatus" } }
        - { name: bastNumber, in: query, schema: { type: string } }
      responses:
        "200":
          description: Scope-filtered canonical BAST list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/BastDocument" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /bast-documents/reconciliation:
    get:
      operationId: getBastReconciliationInventory
      summary: Read non-destructive canonical/legacy BAST reconciliation inventory
      description: >
        Returns read-only classifications and safe identifiers. It never links,
        renumbers, changes lifecycle state, deletes legacy data, or resolves a
        quarantine.
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Read-only reconciliation inventory.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/BastReconciliationInventory" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /bast-documents/{id}:
    get:
      operationId: getCanonicalBastDocument
      summary: Get one canonical BAST by authoritative identity
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BastDocumentIdPath"
      responses:
        "200":
          description: Canonical BAST.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/BastDocument" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /bast-documents/{id}/submit:
    post:
      operationId: submitCanonicalBastDocument
      summary: Create the initial immutable BAST submission attempt
      description: >
        DRAFT → SUBMITTED. Pins the selected immutable Document Version,
        Completion/Service Report references, latest completed approved Work
        Order/Vendor Work reviews, readiness snapshot, and current evidence.
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BastDocumentIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitBastDocumentRequest" }
      responses:
        "200":
          description: Initial submission attempt created atomically.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/BastLifecycleCommandResult" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /bast-documents/{id}/resubmit:
    post:
      operationId: resubmitCanonicalBastDocument
      summary: Submit a corrected BAST as a new immutable attempt
      description: >
        REJECTED → SUBMITTED. Requires the rejection-linked Finding to be
        VERIFIED or CLOSED, a newer immutable Document Version, and current
        readiness/evidence. Historical attempts, Sign-Offs and Finding links
        are preserved; current acceptance projection fields are cleared.
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BastDocumentIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitBastDocumentRequest" }
      responses:
        "200":
          description: Corrected immutable submission attempt created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/BastLifecycleCommandResult" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /bast-documents/{id}/decisions:
    post:
      operationId: decideCanonicalBastDocument
      summary: Accept or reject the current BAST submission attempt
      description: >
        Requires `bast.accept`. SUBMITTED → ACCEPTED creates one immutable
        acceptance Sign-Off. SUBMITTED → REJECTED creates one rejection
        Sign-Off and one linked Finding. The current attempt and Document
        Version are always used; clients cannot supply competing audit links.
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BastDocumentIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DecideBastDocumentRequest" }
      responses:
        "200":
          description: Atomic acceptance or rejection decision.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/BastLifecycleCommandResult" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /vendor-basts:
    post:
      deprecated: true
      operationId: createLegacyVendorBast
      summary: Deprecated legacy BAST create (always restricted)
      description: >
        Compatibility contract only. Returns BAST_LEGACY_WRITE_RESTRICTED and
        directs callers to POST /bast-documents; it cannot create a competing
        lifecycle authority.
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateLegacyVendorBastRequest" }
      responses:
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      deprecated: true
      operationId: listLegacyVendorBasts
      summary: List scope-safe legacy BAST compatibility projections
      description: >
        Safely linked rows project canonical `bast_documents.id`, lifecycle and
        Sign-Off state. Legacy-only/divergent rows remain labelled fallbacks.
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: vendorWorkId, in: query, schema: { $ref: "#/components/schemas/Uuid" } }
        - { name: vendorId, in: query, schema: { $ref: "#/components/schemas/Uuid" } }
        - { name: buildingId, in: query, schema: { $ref: "#/components/schemas/Uuid" } }
      responses:
        "200":
          description: Legacy compatibility projections.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/LegacyVendorBastProjection" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /vendor-basts/{bastId}:
    get:
      deprecated: true
      operationId: getLegacyVendorBast
      summary: Get one legacy BAST compatibility projection
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/LegacyVendorBastIdPath"
      responses:
        "200":
          description: Legacy compatibility projection.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/LegacyVendorBastProjection" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /vendor-basts/{bastId}/submit:
    post:
      deprecated: true
      operationId: submitLegacyVendorBastProjection
      summary: Delegate a safe linked legacy submit/resubmit to canonical BE-22
      description: >
        Never mutates legacy lifecycle columns. Safe context-consistent links
        delegate to the canonical submit or resubmit command; all other writes
        are restricted pending reconciliation.
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/LegacyVendorBastIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitBastDocumentRequest" }
      responses:
        "200":
          description: Updated compatibility projection from canonical state.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/LegacyVendorBastProjection" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /vendor-basts/{bastId}/accept:
    post:
      deprecated: true
      operationId: acceptLegacyVendorBastProjection
      summary: Delegate a safe linked legacy acceptance to canonical BE-22
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/LegacyVendorBastIdPath"
      requestBody:
        content:
          application/json:
            schema: { $ref: "#/components/schemas/LegacyBastDecisionRequest" }
      responses:
        "200":
          description: Updated compatibility projection from canonical state.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/LegacyVendorBastProjection" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /vendor-basts/{bastId}/reject:
    post:
      deprecated: true
      operationId: rejectLegacyVendorBastProjection
      summary: Delegate a safe linked legacy rejection to canonical BE-22
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/LegacyVendorBastIdPath"
      requestBody:
        content:
          application/json:
            schema: { $ref: "#/components/schemas/LegacyBastDecisionRequest" }
      responses:
        "200":
          description: Updated compatibility projection from canonical state.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/LegacyVendorBastProjection" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /work-orders/{id}/close:
    post:
      operationId: closeWorkOrderAfterCanonicalBastReadiness
      summary: Close a completed and approved Work Order
      description: >
        Work Order is the sole closure authority. Existing completion/latest
        approval rules are enforced before canonical BAST cardinality
        (`NONE`, `WORK_ORDER`, `EACH_VENDOR_WORK`), accepted traceability,
        Finding/Rework and reconciliation-quarantine readiness. Failure is
        non-mutating and legacy BAST state never independently satisfies closure.
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Work Order transitioned to CLOSED.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderCloseResult" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  # ==================================================================
  # CR-BE-COM-02 — Vendor Invoice (PART 01–05)
  # ==================================================================

  /vendors/{vendorId}/invoices:
    post:
      operationId: createVendorInvoice
      x-required-permission: vendor_invoice.manage
      x-building-scoped: true
      summary: Create a Vendor Invoice
      description: >
        Creates a DRAFT Vendor Invoice for an external Vendor. The Vendor must
        be ACTIVE and hold an ACTIVE relationship to the invoice's Building.
        `purchaseOrderId` is optional; when supplied it must reference an
        existing ISSUED PO with the same Client, Building, and Vendor.
        `workContractId` is optional but requires `purchaseOrderId`; it must
        reference that PO's ACTIVE or COMPLETED SPK with the same scope and
        Vendor. PO/SPK IDs are persisted as immutable authoritative references.
        Existing operational references must share the invoice Vendor, Client,
        Building, and their actual Work Order/Vendor Work/report/BAST chain.
        This creation gate does not persist matching or quantity truth.
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: vendorId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateVendorInvoiceRequest" }
      responses:
        "201":
          description: Vendor Invoice created as DRAFT.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorInvoice" }
        "400":
          description: >
            Invalid request/linkage (`VALIDATION_ERROR`), unknown PO
            (`VENDOR_INVOICE_PURCHASE_ORDER_INVALID`), unknown SPK
            (`VENDOR_INVOICE_WORK_CONTRACT_INVALID`), SPK/PO mismatch
            (`VENDOR_INVOICE_WORK_CONTRACT_PO_MISMATCH`), Vendor mismatch
            (`VENDOR_INVOICE_PROCUREMENT_VENDOR_MISMATCH`), Building/Client
            mismatch (`VENDOR_INVOICE_PROCUREMENT_SCOPE_MISMATCH`), invalid
            existing operational context (`VENDOR_INVOICE_CONTEXT_INVALID`),
            inactive Vendor (`VENDOR_INACTIVE`), or invalid Vendor/Building
            relationship (`VENDOR_INVOICE_VENDOR_INVALID`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Route Vendor not found (`VENDOR_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            Duplicate invoice number within the Client
            (`VENDOR_INVOICE_NUMBER_ALREADY_EXISTS`), PO state is not ISSUED
            (`VENDOR_INVOICE_PURCHASE_ORDER_NOT_ISSUED`), or SPK state is not
            ACTIVE/COMPLETED
            (`VENDOR_INVOICE_WORK_CONTRACT_NOT_ELIGIBLE`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /vendor-invoices:
    get:
      operationId: listVendorInvoices
      x-required-permission: vendor_invoice.read
      x-building-scoped: true
      summary: List Vendor Invoices
      description: >
        Lists Vendor Invoices scoped to the caller's accessible Buildings,
        optionally filtered by vendor, building, work order, status, or date range.
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: vendorId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: workOrderId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: purchaseOrderId
          in: query
          description: CR-BE-R2P-01 PART 06 — filter by the linked Purchase Order.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: workContractId
          in: query
          description: CR-BE-R2P-01 PART 06 — filter by the linked SPK.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          schema: { type: string, enum: [DRAFT, FINALIZED, CANCELLED] }
        - name: invoiceDateFrom
          in: query
          schema: { type: string, format: date }
        - name: invoiceDateTo
          in: query
          schema: { type: string, format: date }
      responses:
        "200":
          description: List of Vendor Invoices.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/VendorInvoice" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /vendor-invoices/{id}:
    get:
      operationId: getVendorInvoice
      x-required-permission: vendor_invoice.read
      x-building-scoped: true
      summary: Get a Vendor Invoice
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Vendor Invoice.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorInvoice" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateVendorInvoice
      x-required-permission: vendor_invoice.manage
      x-building-scoped: true
      summary: Update a DRAFT Vendor Invoice
      description: >
        Requires `vendor_invoice.manage` and access to the invoice Building.
        Only DRAFT commercial fields may change. Invoice identity, Vendor,
        Client/Building, operational references, `purchaseOrderId`, and
        `workContractId` are immutable. FINALIZED and CANCELLED invoices reject
        updates without changing payment, verification, or linkage authority.
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateVendorInvoiceRequest" }
      responses:
        "200":
          description: DRAFT Vendor Invoice updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorInvoice" }
        "400":
          description: >
            Invalid/immutable input (`VALIDATION_ERROR`), a FINALIZED invoice
            (`VENDOR_INVOICE_FINALIZED_PROTECTED`), or a non-DRAFT invoice
            (`VENDOR_INVOICE_NOT_DRAFT`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Vendor Invoice not found (`VENDOR_INVOICE_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /vendor-invoices/{id}/available-actions:
    get:
      operationId: getVendorInvoiceAvailableActions
      x-required-permission: vendor_invoice.read
      x-building-scoped: true
      summary: Resolve caller-specific Vendor Invoice actions
      description: >
        Requires invoice Building access. Uses existing invoice lifecycle,
        canonical matching, verification, settlement readiness and the caller's
        `vendor_invoice.manage` permission. Every returned token maps to an
        existing command. Read-only callers and terminal invoices receive an
        empty list; frontend must not infer additional actions from status.
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Caller-specific authoritative actions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/VendorInvoiceAvailableActions"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Vendor Invoice not found (`VENDOR_INVOICE_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /vendor-invoices/{id}/finalize:
    post:
      operationId: finalizeVendorInvoice
      x-required-permission: vendor_invoice.manage
      x-building-scoped: true
      summary: Finalize a draft Vendor Invoice
      description: >
        Transitions a DRAFT Vendor Invoice to FINALIZED. Finalized invoices are
        immutable and eligible for verification.
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Vendor Invoice finalized.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorInvoice" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /vendor-invoices/{id}/cancel:
    post:
      operationId: cancelVendorInvoice
      x-required-permission: vendor_invoice.manage
      x-building-scoped: true
      summary: Cancel a Vendor Invoice
      description: >
        Transitions a DRAFT or FINALIZED Vendor Invoice to CANCELLED (terminal).
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Vendor Invoice cancelled.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorInvoice" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /vendor-invoices/{id}/verify:
    post:
      operationId: verifyVendorInvoice
      x-required-permission: vendor_invoice.manage
      x-building-scoped: true
      summary: Verify a finalized Vendor Invoice
      description: >
        Consumes the exact canonical result returned by GET /matching.
        MATCHED promotes the FINALIZED invoice to VERIFIED. MISMATCH records
        DISCREPANCY with deterministic reasons. NOT_READY rejects with 409
        `VENDOR_INVOICE_MATCHING_NOT_READY` and does not mutate verification
        state. A still-MATCHED VERIFIED invoice is idempotent.
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                notes: { type: string, nullable: true, maxLength: 2000 }
      responses:
        "200":
          description: Verification result.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorInvoiceVerifyResult" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: >
            Canonical matching status is NOT_READY
            (`VENDOR_INVOICE_MATCHING_NOT_READY`); verification state is not
            mutated and reasons are returned in `error.details`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /vendor-invoices/{id}/matching:
    get:
      operationId: getVendorInvoiceMatching
      x-required-permission: vendor_invoice.read
      x-building-scoped: true
      summary: Get matching evaluation (read-only diagnostic)
      description: >
        Returns the canonical MATCHED / MISMATCH / NOT_READY result without
        mutation. For linked invoices it compares immutable PO/SPK context,
        invoice amount/currency to committed PO Lines, authoritative MR
        received quantities or finalized service Receiving, and existing
        Work Order/Vendor Work/report/BAST evidence with its actual scope and
        relationships. Unlinked legacy invoices retain the prior operational-
        evidence path with procurement checks not applicable.
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Matching result.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/InvoiceMatchingResult" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /vendor-invoices/{id}/payment:
    post:
      operationId: recordVendorPayment
      x-required-permission: vendor_invoice.manage
      x-building-scoped: true
      summary: Record a payment against a Vendor Invoice
      description: >
        Records a positive, finite payment only when the canonical settlement
        readiness authority is READY. This enforces FINALIZED + VERIFIED,
        canonical MATCHED, valid scope/amount/currency and coherent existing
        payment state. The amount must fit NUMERIC(18,2), have at most two
        decimal places, and keep cumulative paidAmount at or below invoiceAmount. A cumulative amount
        below invoiceAmount produces PARTIALLY_PAID; an exact full payment
        produces PAID; overpayment is rejected with 422
        VENDOR_INVOICE_OVERPAYMENT. PostgreSQL derives outstandingAmount from
        invoiceAmount minus the cumulative paidAmount. A supplied paymentDate
        becomes lastPaymentDate; when omitted, an existing lastPaymentDate is
        preserved. Optional notes are retained in the payment audit event.
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RecordVendorPaymentRequest" }
      responses:
        "200":
          description: Payment recorded.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorInvoice" }
        "400":
          description: >
            Invalid amount/date/notes (`VALIDATION_ERROR`), invoice not VERIFIED
            (`VENDOR_INVOICE_NOT_VERIFIED_FOR_PAYMENT`), or payment prohibited by
            its lifecycle (`VENDOR_INVOICE_PAYMENT_NOT_ALLOWED`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              examples:
                invalidAmount:
                  value:
                    success: false
                    error:
                      code: VALIDATION_ERROR
                      message: Request validation failed.
                notVerified:
                  value:
                    success: false
                    error:
                      code: VENDOR_INVOICE_NOT_VERIFIED_FOR_PAYMENT
                      message: Only a verified Vendor Invoice can receive payments.
                paymentNotAllowed:
                  value:
                    success: false
                    error:
                      code: VENDOR_INVOICE_PAYMENT_NOT_ALLOWED
                      message: Payments cannot be recorded against this invoice.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            Missing `vendor_invoice.manage` permission (`PERMISSION_DENIED`) or
            no access to the invoice Building (`BUILDING_ACCESS_DENIED`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: Vendor Invoice not found (`VENDOR_INVOICE_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            Canonical settlement readiness is NOT_READY
            (`VENDOR_INVOICE_PAYMENT_NOT_READY`). Existing lifecycle-specific
            errors remain 400; fully paid duplicate attempts remain 422.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "422":
          description: >
            The payment would make cumulative paidAmount exceed invoiceAmount
            (`VENDOR_INVOICE_OVERPAYMENT`). No payment state is committed.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              examples:
                overpayment:
                  value:
                    success: false
                    error:
                      code: VENDOR_INVOICE_OVERPAYMENT
                      message: Payment would exceed the outstanding amount on this invoice.

  /vendor-invoices/{id}/settlement-readiness:
    get:
      operationId: getVendorInvoiceSettlementReadiness
      x-required-permission: vendor_invoice.read
      x-building-scoped: true
      summary: Evaluate settlement readiness
      description: >
        Returns canonical READY / NOT_READY / SETTLED payment eligibility.
        READY requires FINALIZED + VERIFIED, canonical MATCHED, valid scope,
        amount/currency and a coherent unpaid/partial payment state. SETTLED is
        derived from existing PAID + zero outstanding state. NOT_READY includes
        deterministic reasons. The projection is read-only and advertises only
        `RECORD_PAYMENT`, the existing payment command, when READY.
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Settlement readiness result.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorSettlementReadinessResult" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /vendor-invoices/{id}/consistency:
    get:
      operationId: getVendorInvoiceConsistency
      x-required-permission: vendor_invoice.read
      x-building-scoped: true
      summary: Evaluate vendor consistency cross-check
      description: >
        Evaluates vendor consistency across the commercial-to-operational chain.
        Checks invoice vendor vs vendor work vendor, vendor work vendor vs work
        order assigned vendor, procurement vendor match, and client/building
        isolation. Pure read-only diagnostic — does not mutate any records.
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Vendor consistency result.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorConsistencyResult" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /vendor-invoices/{id}/trace:
    get:
      operationId: getVendorInvoiceTrace
      x-required-permission: vendor_invoice.read
      x-building-scoped: true
      summary: Canonical Request-to-Pay trace from a Vendor Invoice
      description: >
        Returns one read-only graph derived from existing foreign keys and
        authoritative projections. Documents expose type/id/number/status;
        relationships expose source/target ids. Optional PO/SPK, material or
        service Receiving, Work/Vendor Work, report and BAST paths appear only
        when real records exist. Matching, verification, payment and settlement
        readiness are current projections, never persisted graph state. Requires
        invoice Building access; no cross-Building records are returned.
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Trace result.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorInvoiceTrace" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Vendor Invoice not found (`VENDOR_INVOICE_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-R2P-01 PART 07 — upstream chain links.
  #
  # Documented here so the authoritative chain is traversable end to end from
  # the contract alone. These endpoints predate CR-BE-R2P-01; their behaviour
  # is unchanged and is described exactly as implemented.
  # ---------------------------------------------------------------------------

  /purchase-requests/{purchaseRequestId}/service-requests:
    post:
      operationId: createServiceRequest
      summary: Create a Service Request line on a Purchase Request
      description: >
        Requires `service_request.manage` and access to the Purchase Request's
        Building. Client and Building are derived from the parent Purchase
        Request, which must be OPEN. The optional Vendor must belong to the
        same Client and the optional Functional Location to the same Building.
      tags: [Service Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: purchaseRequestId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateServiceRequestRequest" }
      responses:
        "201":
          description: Service Request created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ServiceRequest" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /vendor-selections:
    post:
      operationId: createVendorSelection
      summary: Evaluate Vendor Selection Readiness for a request
      description: >
        Requires `vendor_selection.manage`. BE-17E — evaluates whether a Vendor
        may be selected for a Purchase or Service Request and persists the
        deterministic verdict with its individual checks. Client and Building
        are derived from the referenced request. Backend-authoritative: the
        caller supplies no readiness value.
      tags: [Vendor Selection Readiness]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateVendorSelectionRequest" }
      responses:
        "201":
          description: Vendor Selection Readiness evaluated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorSelection" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /vendor-selections/{id}:
    get:
      operationId: getVendorSelection
      summary: Get a Vendor Selection Readiness record
      description: Requires `vendor_selection.read`.
      tags: [Vendor Selection Readiness]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Vendor Selection Readiness.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorSelection" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /po-readiness:
    post:
      operationId: createPOReadiness
      summary: Evaluate Purchase Order Readiness for a request
      description: >
        Requires `po_readiness.manage`. BE-17F — THE readiness authority of the
        R2P chain. Evaluates whether a request may be committed to a Purchase
        Order and persists the deterministic verdict (READY / NOT_READY /
        BLOCKED) with its individual checks. READY means "may commit"; an
        ISSUED Purchase Order means "committed".


        Client, Building and the request reference are derived from the
        referenced request. Backend-authoritative: the caller supplies no
        readiness value, and no downstream domain re-decides this verdict.
      tags: [Purchase Order Readiness]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePOReadinessRequest" }
      responses:
        "201":
          description: PO Readiness evaluated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/POReadiness" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /po-readiness/{id}:
    get:
      operationId: getPOReadiness
      summary: Get a Purchase Order Readiness record
      description: >
        Requires `po_readiness.read`. This is the record a Purchase Order
        references through `poReadinessId`.
      tags: [Purchase Order Readiness]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: PO Readiness.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/POReadiness" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updatePOReadiness
      summary: Update the editable detail of a PO Readiness record
      description: >
        Requires `po_readiness.manage`. Only `requiredDate` and `notes` may
        change — the readiness verdict and its checks are backend-derived and
        never caller-supplied.
      tags: [Purchase Order Readiness]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdatePOReadinessRequest" }
      responses:
        "200":
          description: PO Readiness updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/POReadiness" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # ---------------------------------------------------------------------------
  # CR-BE-R2P-01 — Request-to-Procurement / Request-to-Pay chain (PART 01–06)
  #
  # Authoritative chain:
  #   Request → Selected Vendor → PO Readiness (BE-17F, "may commit")
  #   → ISSUED Purchase Order (committed) → ACTIVE Work Contract / SPK
  #   → Work Order → BAST → Vendor Invoice → Matching / Verification
  #   → Settlement Readiness → Payment State
  #
  # Context rule: PO/SPK/Work Order binding scope is DERIVED from its
  # authoritative parent (PO Readiness, PO, or SPK) and cannot be overridden.
  # Vendor Invoice Vendor comes from the route and its Building from the body;
  # both are validated against every supplied PO/SPK/operational reference.
  # BE-02G Building isolation is enforced after context resolution, so an actor
  # without access to the resolved Building receives 403 BUILDING_ACCESS_DENIED.
  # ---------------------------------------------------------------------------

  /price-catalog/lookup:
    get:
      operationId: lookupPriceCatalogEntry
      x-required-permission: price_catalog.read
      x-building-scoped: true
      summary: Resolve the applicable reference price (deterministic §8 probe)
      description: >
        Requires `price_catalog.read` plus access to the anchor Building.
        Internal-only probe — RFQ Vendor sessions never receive this surface.


        Answers one governed question: for a subject (an item + required UOM
        for MATERIAL, or a governed service for SERVICE), a currency, a
        Building and an optional Vendor as-of `asOf`, which ACTIVE price
        authority applies. `sourceMode` defaults to MATERIAL; SERVICE uses
        `serviceId` and has no UOM/quantity. Exact match only (no FX, no UOM
        conversion); precedence is fixed: Vendor+Building → Vendor → Building
        → Client-wide. Typed outcomes only: `MATCHED` carries the full
        authority snapshot; `NO_REFERENCE_PRICE`, `UOM_INCOMPATIBLE`
        (MATERIAL only) and `CURRENCY_INCOMPATIBLE` are explicit absence
        classifications, never fabricated prices. The defensive `AMBIGUOUS`
        trip fails closed as 409 `PRICE_CATALOG_LOOKUP_AMBIGUOUS` (and is
        audited), never a 200 price.
      tags: [Price Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          description: Scope anchor; derives the governed Client.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: sourceMode
          in: query
          description: >
            Defaults to MATERIAL. SERVICE resolves by `serviceId` with no
            UOM/quantity (CR-BE-SVC-01 PART 05).
          schema: { $ref: "#/components/schemas/PriceCatalogSourceMode" }
        - name: itemId
          in: query
          description: MATERIAL subject (required when sourceMode is MATERIAL).
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: uomId
          in: query
          description: >
            Exact-match UOM (required when sourceMode is MATERIAL); no
            conversion exists or is inferred.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: serviceId
          in: query
          description: SERVICE subject (required when sourceMode is SERVICE).
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: currency
          in: query
          required: true
          schema: { $ref: "#/components/schemas/PriceCatalogCurrency" }
        - name: asOf
          in: query
          required: true
          description: ISO-8601 selection instant (half-open window `[)`).
          schema: { type: string, format: date-time }
        - name: vendorId
          in: query
          description: Optional vendor context; enables the vendor tiers.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Typed resolution outcome (never a bare null).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PriceCatalogLookupResult" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /price-catalog/entries:
    post:
      operationId: createPriceCatalogEntry
      x-required-permission: price_catalog.manage
      x-building-scoped: true
      summary: Create a DRAFT price-catalog entry (idempotent)
      description: >
        Requires `price_catalog.manage` plus access to the entry's scope.
        Creates a DRAFT (no effectiveness; never selected until activation).
        MATERIAL-only authority: one Inventory Item priced per 1 unit of an
        explicit UOM in one supported currency. Scope tiers are expressed via
        nullable keys: Client-wide / Building / Vendor / Vendor+Building;
        `entryKind` is derived (`VENDOR_CONTRACT` exactly when `vendorId` is
        set). Approved-by attribution is optional and must reference an
        ACTIVE user. `unitPrice` must be > 0 with at most two decimals;
        `effectiveTo` must be after `effectiveFrom` when given.
      tags: [Price Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/IdempotencyKeyHeader"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePriceCatalogEntryRequest" }
      responses:
        "201":
          description: DRAFT entry created (or idempotent replay).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PriceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listPriceCatalogEntries
      x-required-permission: price_catalog.read
      x-building-scoped: true
      summary: List price-catalog entries (scope-filtered, no existence oracle)
      description: >
        Requires `price_catalog.read`. Scoped to the caller's accessible
        Clients/Buildings; filters outside that reach yield an empty page,
        never an existence oracle.
      tags: [Price Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: clientId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: uomId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: currency
          in: query
          schema: { $ref: "#/components/schemas/PriceCatalogCurrency" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/PriceCatalogEntryStatus" }
      responses:
        "200":
          description: Entries visible to the caller.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PriceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /price-catalog/entries/{id}:
    get:
      operationId: getPriceCatalogEntry
      x-required-permission: price_catalog.read
      x-building-scoped: true
      summary: Read one price-catalog entry (any lifecycle state)
      description: >
        Requires `price_catalog.read` plus access to the entry's scope.
        Historical rows stay queryable: replaced/deactivated entries remain
        byte-intact evidence with their lifecycle closure fields.
      tags: [Price Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PriceCatalogEntryIdPath"
      responses:
        "200":
          description: The entry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PriceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updatePriceCatalogEntry
      x-required-permission: price_catalog.manage
      x-building-scoped: true
      summary: Edit a DRAFT entry (DRAFT only — never ACTIVE/INACTIVE facts)
      description: >
        Requires `price_catalog.manage`. Edits DRAFT fields only — 409
        `PRICE_CATALOG_NOT_DRAFT` otherwise. Re-validates every changed
        reference, scope and window; `entryKind` is re-derived from the final
        Vendor key. ACTIVE and INACTIVE rows are never updated in place.
      tags: [Price Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PriceCatalogEntryIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdatePriceCatalogEntryRequest" }
      responses:
        "200":
          description: Updated DRAFT entry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PriceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /price-catalog/entries/{id}/activate:
    post:
      operationId: activatePriceCatalogEntry
      x-required-permission: price_catalog.manage
      x-building-scoped: true
      summary: Activate a DRAFT entry
      description: >
        Requires `price_catalog.manage`. DRAFT → ACTIVE with its
        `effectiveFrom`/`effectiveTo` window; the per-tier exclusion
        constraint rejects overlaps at activation instant (409
        `PRICE_CATALOG_WINDOW_OVERLAP`). Future-dated windows are supported —
        selection as-of `T` simply ignores them until `T` arrives; nothing is
        flipped by a scheduler.
      tags: [Price Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PriceCatalogEntryIdPath"
      responses:
        "200":
          description: Activated entry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PriceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /price-catalog/entries/{id}/replace:
    post:
      operationId: replacePriceCatalogEntry
      x-required-permission: price_catalog.manage
      x-building-scoped: true
      summary: Atomically close an ACTIVE entry and activate its successor
      description: >
        Requires `price_catalog.manage`. The governed change command: the
        ACTIVE predecessor is closed (INACTIVE, actor/time +
        `replaced_by_entry_id`) and a successor is activated in the same
        transaction. The successor inherits subject, tier keys, UOM and
        currency — only price facts and window change, so a replacement can
        never silently re-scope an authority. The successor
        `effectiveFrom` must not open earlier than the predecessor's window
        start (400 `PRICE_CATALOG_EFFECTIVE_WINDOW_INVALID`) — writing into
        the already-entered past is reserved for the `/correct` override
        lane. One successor per predecessor; overlapping an adjacent ACTIVE
        same-tier window fails 409 `PRICE_CATALOG_WINDOW_OVERLAP`.
      tags: [Price Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PriceCatalogEntryIdPath"
        - $ref: "#/components/parameters/IdempotencyKeyHeader"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ReplacePriceCatalogEntryRequest" }
      responses:
        "201":
          description: Successor created and activated (or idempotent replay).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PriceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /price-catalog/entries/{id}/correct:
    post:
      operationId: correctPriceCatalogEntry
      x-required-permission: price_catalog.override
      x-building-scoped: true
      summary: Governed retroactive correction of an ACTIVE window (override)
      description: >
        Requires `price_catalog.override` — the exceptional authority, never
        implied by `price_catalog.read` or `price_catalog.manage` and
        unassigned by default (even the platform admin role must be granted it
        explicitly). Plus access to the entry's scope (BE-02G) and the
        mandatory `Idempotency-Key`.


        The retroactive-correction lane for an already-entered ACTIVE window.
        The predecessor is closed exactly like a replacement (lifecycle
        linkage only; its price/window facts are never mutated and stay
        queryable), and a NEW ACTIVE successor is created inheriting the
        subject, scope, UOM and currency. Unlike `/replace`, the successor
        window MAY open before the predecessor's start — that exceptional
        capability is precisely why this lane requires the override
        permission. The corrected authority resolves only under its own new
        effective window; existing RFQ comparison snapshots referencing the
        predecessor stay byte-stable historical evidence.


        Governance evidence is mandatory: `reason` is required, non-blank and
        length-checked (≤ 1000 chars) — 400 `VALIDATION_ERROR` when missing
        or blank. Every accepted correction emits exactly one
        `PRICE_CATALOG_ENTRY_OVERRIDE_CORRECTED` operational event carrying
        the reason, the permission code used and both windows. The reason
        joins the idempotency fingerprint: a replayed key with altered
        evidence conflicts (409 `PRICE_CATALOG_IDEMPOTENCY_CONFLICT`).


        Lifecycle guards: target must be ACTIVE (409 `PRICE_CATALOG_NOT_ACTIVE`
        for DRAFT/INACTIVE/already-replaced); an inverted window is 400
        `PRICE_CATALOG_EFFECTIVE_WINDOW_INVALID`; overlapping an adjacent
        ACTIVE same-tier window stays structurally fail-closed (409
        `PRICE_CATALOG_WINDOW_OVERLAP`).
      tags: [Price Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PriceCatalogEntryIdPath"
        - $ref: "#/components/parameters/IdempotencyKeyHeader"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CorrectPriceCatalogEntryRequest" }
      responses:
        "201":
          description: Corrective successor created and activated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PriceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /price-catalog/entries/{id}/deactivate:
    post:
      operationId: deactivatePriceCatalogEntry
      x-required-permission: price_catalog.manage
      x-building-scoped: true
      summary: Deactivate an ACTIVE entry (terminal close, no successor)
      description: >
        Requires `price_catalog.manage`. ACTIVE → INACTIVE, actor/time
        recorded; the row and its window remain as history and historical
        snapshots referencing the entry stay valid.
      tags: [Price Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PriceCatalogEntryIdPath"
      responses:
        "200":
          description: Deactivated entry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PriceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /service-catalog/entries:
    post:
      operationId: createServiceCatalogEntry
      x-required-permission: service_catalog.manage
      x-building-scoped: true
      summary: Create a Service Catalog entry (governed Service identity)
      description: >
        CR-BE-SVC-01 PART 01. Requires `service_catalog.manage` plus access to
        the entry's Client (reached through a Building assignment). Creates a
        governed Service master entry as ACTIVE: a Client-scoped `code`
        (unique per Client, immutable), a `category`, and a `name`. Audit:
        `SERVICE_CATALOG_ENTRY_CREATED` (transaction-atomic).
      tags: [Service Catalog]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateServiceCatalogEntryRequest" }
      responses:
        "201":
          description: Created entry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ServiceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listServiceCatalogEntries
      x-required-permission: service_catalog.read
      x-building-scoped: true
      summary: List Service Catalog entries (Client-scoped, no existence oracle)
      description: >
        CR-BE-SVC-01 PART 01. Requires `service_catalog.read`. Lists entries
        bound to the caller's accessible Client set; an out-of-reach
        `clientId` filter returns an empty page (no existence oracle).
        Optional filters: `clientId`, `status`, `category`, `search`.
      tags: [Service Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: clientId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/ServiceCatalogStatus" }
        - name: category
          in: query
          schema: { type: string }
        - name: search
          in: query
          schema: { type: string }
      responses:
        "200":
          description: Scoped entry list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/ServiceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /service-catalog/entries/{id}:
    get:
      operationId: getServiceCatalogEntry
      x-required-permission: service_catalog.read
      x-building-scoped: true
      summary: Read one Service Catalog entry
      description: >
        CR-BE-SVC-01 PART 01. Requires `service_catalog.read` plus Client
        access (the catalog is Client-scoped — a caller reaches a Client
        through their Building assignments).
      tags: [Service Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ServiceCatalogEntryIdPath"
      responses:
        "200":
          description: The entry (any status).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ServiceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateServiceCatalogEntry
      x-required-permission: service_catalog.manage
      x-building-scoped: true
      summary: Edit a Service Catalog entry (non-identity fields only)
      description: >
        CR-BE-SVC-01 PART 01. Requires `service_catalog.manage`. Edits
        `name`, `description`, `category` only — `code` and `clientId` are
        immutable. Audit: `SERVICE_CATALOG_ENTRY_UPDATED`.
      tags: [Service Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ServiceCatalogEntryIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateServiceCatalogEntryRequest" }
      responses:
        "200":
          description: Updated entry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ServiceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /service-catalog/entries/{id}/deactivate:
    post:
      operationId: deactivateServiceCatalogEntry
      x-required-permission: service_catalog.manage
      x-building-scoped: true
      summary: Deactivate a Service Catalog entry (terminal; no reactivation lane)
      description: >
        CR-BE-SVC-01 PART 01. Requires `service_catalog.manage`. ACTIVE →
        INACTIVE (terminal); the row is retained for history and existing
        references stay valid (governed snapshots/anchors are preserved).
        Audit: `SERVICE_CATALOG_ENTRY_DEACTIVATED`.
      tags: [Service Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ServiceCatalogEntryIdPath"
      responses:
        "200":
          description: Deactivated entry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ServiceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /purchase-orders:
    post:
      operationId: createPurchaseOrder
      x-required-permission: purchase_order.manage
      x-building-scoped: true
      summary: Commit a Purchase Order against a READY PO Readiness
      description: >
        Requires `purchase_order.manage`. Creates the PO as DRAFT.


        `poReadinessId` is the ONLY context input: Client, Building, Vendor and
        the request reference are derived from that BE-17F readiness record,
        which must be READY (409 `PURCHASE_ORDER_READINESS_NOT_READY`
        otherwise). PO Readiness remains the sole readiness authority — this
        domain evaluates no readiness of its own. Supplying `clientId`,
        `buildingId`, `vendorId`, `requestType`, `purchaseRequestId`,
        `serviceRequestId` or `status` is rejected as caller-supplied context.


        At most one live (non-CANCELLED) PO may exist per readiness record; the
        PO number is unique within a Client.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePurchaseOrderRequest" }
      responses:
        "201":
          description: Purchase Order committed as DRAFT.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PurchaseOrder" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listPurchaseOrders
      x-required-permission: purchase_order.read
      x-building-scoped: true
      summary: List Purchase Orders
      description: >
        Requires `purchase_order.read`. Scoped to the caller's accessible
        Buildings — rows outside that scope are never returned.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: vendorId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: purchaseRequestId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: serviceRequestId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/PurchaseOrderStatus" }
        - name: poDateFrom
          in: query
          schema: { type: string, format: date }
        - name: poDateTo
          in: query
          schema: { type: string, format: date }
      responses:
        "200":
          description: List of Purchase Orders.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PurchaseOrder" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /purchase-orders/{id}:
    get:
      operationId: getPurchaseOrder
      x-required-permission: purchase_order.read
      x-building-scoped: true
      summary: Get a Purchase Order
      description: Requires `purchase_order.read` and access to the PO's Building.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseOrderIdPath"
      responses:
        "200":
          description: Purchase Order.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PurchaseOrder" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updatePurchaseOrder
      x-required-permission: purchase_order.manage
      x-building-scoped: true
      summary: Update a DRAFT Purchase Order
      description: >
        Requires `purchase_order.manage`. DRAFT only — a non-DRAFT PO yields
        400 `PURCHASE_ORDER_NOT_DRAFT`.


        Only commercial detail may change. Identity (`poNumber`), commitment
        context (`poReadinessId`, `vendorId`, request references), derived scope
        (`clientId`, `buildingId`), `status` and the issuance/cancellation
        provenance columns are immutable; supplying any of them yields
        400 VALIDATION_ERROR. RFQ-derived DRAFT POs are additionally guarded
        with 409 `PURCHASE_ORDER_RFQ_DERIVED_IMMUTABLE`; commercial changes
        must return to the RFQ/award process.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseOrderIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdatePurchaseOrderRequest" }
      responses:
        "200":
          description: Purchase Order updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PurchaseOrder" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /purchase-orders/{id}/available-actions:
    get:
      operationId: getPurchaseOrderAvailableActions
      x-required-permission: purchase_order.read
      x-building-scoped: true
      summary: Resolve the caller's available Purchase Order actions
      description: >
        Requires `purchase_order.read` and access to the PO's Building. Uses
        the established `{ purchaseOrderId, state, availableActions }` shape.
        Actions are upper-case command tokens and are returned only when the
        caller also holds `purchase_order.manage` and the existing command
        preconditions pass. `ISSUE` reuses issue-readiness; `CANCEL` mirrors
        the current DRAFT-only cancel command. No lifecycle rule is added.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseOrderIdPath"
      responses:
        "200":
          description: Caller-specific available actions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PurchaseOrderAvailableActions"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /purchase-orders/{id}/cancel:
    post:
      operationId: cancelPurchaseOrder
      x-required-permission: purchase_order.manage
      x-building-scoped: true
      summary: Cancel a DRAFT Purchase Order
      description: >
        Requires `purchase_order.manage`. DRAFT → CANCELLED only; any other
        source state yields 400 `PURCHASE_ORDER_CANCEL_NOT_ALLOWED`. RFQ-derived
        DRAFT POs cannot be cancelled through this generic route and yield 409
        `PURCHASE_ORDER_RFQ_DERIVED_IMMUTABLE`; commercial changes must return
        to the RFQ/award process. Non-RFQ DRAFT cancellation retains its normal
        behavior.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseOrderIdPath"
      responses:
        "200":
          description: Purchase Order cancelled.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PurchaseOrder" }
        "400":
          description: >
            The Purchase Order is not DRAFT
            (`PURCHASE_ORDER_CANCEL_NOT_ALLOWED`) or request validation failed.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /purchase-orders/{id}/issue-readiness:
    get:
      operationId: getPurchaseOrderIssueReadiness
      x-required-permission: purchase_order.read
      x-building-scoped: true
      summary: Evaluate whether a Purchase Order may be issued
      description: >
        Requires `purchase_order.read`. Read-only projection — evaluating it
        mutates nothing.


        `poReadiness` reports the BE-17F verdict VERBATIM; this endpoint is not
        a second readiness authority. `availableActions` contains `ISSUE` only
        when `issuable` is true and the caller holds `purchase_order.manage`;
        a read-only caller receives an empty action list. Blockers are
        deterministic facts resolved from existing authorities and are never
        persisted on the PO.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseOrderIdPath"
      responses:
        "200":
          description: Issue-readiness projection.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PurchaseOrderIssueReadiness"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /purchase-orders/{id}/issue:
    post:
      operationId: issuePurchaseOrder
      x-required-permission: purchase_order.manage
      x-building-scoped: true
      summary: Issue a Purchase Order (DRAFT → ISSUED)
      description: >
        Requires `purchase_order.manage`. Commits the order to the Vendor and
        stamps `issuedAt` / `issuedByUserId`.


        Preconditions, all deterministic: the PO is DRAFT; its BE-17F readiness
        is READY; it has at least one line; request linkage, vendor and
        Client/Building scope are consistent. A non-DRAFT PO yields 409
        `PURCHASE_ORDER_NOT_ISSUABLE_STATE` (this is also how repeated issuance
        is rejected); an empty PO yields 409 `PURCHASE_ORDER_NO_LINES`; any
        other blocker set yields 409 `PURCHASE_ORDER_NOT_ISSUABLE` with the
        blocker codes in `error.details[].message`.


        Issuance is non-mutating on failure and freezes the PO's lines, since
        every line mutation requires a DRAFT parent.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseOrderIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/IssuePurchaseOrderRequest" }
      responses:
        "200":
          description: Purchase Order issued.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PurchaseOrder" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /purchase-orders/{id}/price-deviation:
    get:
      operationId: getPurchaseOrderPriceDeviation
      x-required-permission: purchase_order.read + price_catalog.read
      x-building-scoped: true
      summary: Advisory reference-price deviation projection for a PO (§13.3)
      description: >
        Conjunctive permission: requires BOTH `purchase_order.read` and
        `price_catalog.read`, plus access to the PO's Building — the whole
        payload is price-authority data, so a caller lacking either code
        receives 403, never a degraded variant. This is the only PO surface
        carrying reference-price fields; PO and PO-line reads never grow
        them, so no masked field can leak to a `purchase_order.read`-only
        caller.


        Advisory, COMPUTED, read-only, warn-only (§13.3/§13.4): for each line
        it resolves the CURRENT applicable reference price through the frozen
        §8 semantics (as-of = request time; precedence Vendor+Building →
        Vendor → Building → Client-wide with the PO's own Vendor as tier
        context; exact UOM/currency match) and reports the PO line's committed
        price against it: resolution outcome, matched authority id + price
        facts + tier provenance + effective window, PO unit price, unit/total
        variance (PO − reference, 2dp), variance percent and ABOVE/BELOW/EQUAL
        position. Explicit non-fabricated outcomes — `NO_REFERENCE_PRICE`,
        `UOM_INCOMPATIBLE`, `CURRENCY_INCOMPATIBLE`, `NOT_REQUESTED` (SERVICE
        lines) and the fail-closed `AMBIGUOUS` (audited) — carry null facts.
        Nothing is persisted: PO facts are immutable, catalog
        replace/correct afterwards rewrites nothing, and this advisory
        projection never gates creation, approval, acknowledgement or
        issuance, never selects winners and never settles.
      tags: [Purchase Order, Price Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseOrderIdPath"
      responses:
        "200":
          description: Advisory deviation projection.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PurchaseOrderPriceDeviation" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /purchase-orders/{id}/lines:
    post:
      operationId: addPurchaseOrderLine
      x-required-permission: purchase_order.manage
      x-building-scoped: true
      summary: Commit a Purchase Order line against an MR/SR line
      description: >
        Requires `purchase_order.manage`. The parent PO must be DRAFT (400
        `PURCHASE_ORDER_LINE_NOT_DRAFT` otherwise).


        Material Request / Service Request remain the SINGLE quantity
        authority: `quantitySnapshot` is a frozen READ of the originating
        line's approved (or requested) quantity, and `lineAmount` is derived
        from it. A caller may never supply a quantity, an amount, `itemId`,
        `uomId` or `lineNumber` — doing so yields 400 VALIDATION_ERROR. The PO
        creates no ordered/received/remaining ledger.


        A MATERIAL line requires an item and a positive quantity; a SERVICE
        line carries neither, and its unit price IS the line amount. Each
        request line may be committed at most once per PO. RFQ-derived DRAFT
        POs reject generic line additions with 409
        `PURCHASE_ORDER_RFQ_DERIVED_IMMUTABLE`; only the RFQ conversion path
        creates their lines.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseOrderIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AddPurchaseOrderLineRequest" }
      responses:
        "201":
          description: Purchase Order line committed.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PurchaseOrderLine" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listPurchaseOrderLines
      x-required-permission: purchase_order.read
      x-building-scoped: true
      summary: List the lines of a Purchase Order
      description: >
        Requires `purchase_order.read`. Ordered deterministically by
        `lineNumber`.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseOrderIdPath"
      responses:
        "200":
          description: Purchase Order lines.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/PurchaseOrderLine"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /purchase-order-lines/{lineId}:
    get:
      operationId: getPurchaseOrderLine
      x-required-permission: purchase_order.read
      x-building-scoped: true
      summary: Get a Purchase Order line
      description: Requires `purchase_order.read` and access to the line's Building.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseOrderLineIdPath"
      responses:
        "200":
          description: Purchase Order line.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PurchaseOrderLine" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updatePurchaseOrderLine
      x-required-permission: purchase_order.manage
      x-building-scoped: true
      summary: Update a Purchase Order line
      description: >
        Requires `purchase_order.manage`. The parent PO must be DRAFT.


        Only commercial detail changes: `lineAmount` is always RE-DERIVED from
        the frozen quantity snapshot, never accepted from the caller, so the
        quantity authority cannot be bypassed through the amount. RFQ-derived
        DRAFT lines are immutable here and return 409
        `PURCHASE_ORDER_RFQ_DERIVED_IMMUTABLE`; changes require a new RFQ award.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseOrderLineIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePurchaseOrderLineRequest"
      responses:
        "200":
          description: Purchase Order line updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PurchaseOrderLine" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      operationId: removePurchaseOrderLine
      x-required-permission: purchase_order.manage
      x-building-scoped: true
      summary: Remove a Purchase Order line
      description: >
        Requires `purchase_order.manage` and access to the line's Building. The
        parent PO must be DRAFT. The line is deleted, its originating request
        line becomes eligible for a future live commitment, and the response is
        a compact removal acknowledgement. RFQ-derived DRAFT lines cannot be
        removed here and return 409 `PURCHASE_ORDER_RFQ_DERIVED_IMMUTABLE`.
        The surviving removal audit is the operational event; the deleted line
        is not returned.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseOrderLineIdPath"
      responses:
        "200":
          description: Purchase Order line removed.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/RemovePurchaseOrderLineResult"
        "400":
          description: >
            The parent Purchase Order is not DRAFT
            (`PURCHASE_ORDER_LINE_NOT_DRAFT`) or the line id is invalid
            (`VALIDATION_ERROR`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Purchase Order Line not found (`PURCHASE_ORDER_LINE_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /work-contracts:
    post:
      operationId: createWorkContract
      x-required-permission: work_contract.manage
      x-building-scoped: true
      summary: Raise a Work Contract (SPK) against an ISSUED Purchase Order
      description: >
        Requires `work_contract.manage`. Creates the SPK as DRAFT.


        `purchaseOrderId` is the ONLY context input: Client, Building and
        Vendor are INHERITED from that Purchase Order and can never be supplied
        or overridden (400 VALIDATION_ERROR). The PO must be ISSUED — a DRAFT
        commitment is not yet a mandate to execute and a CANCELLED one never
        will be (409 `WORK_CONTRACT_PURCHASE_ORDER_NOT_ISSUED`).


        At most one live (DRAFT or ACTIVE) SPK may occupy a Purchase Order (409
        `WORK_CONTRACT_ALREADY_EXISTS_FOR_PO`); completing or cancelling
        releases the PO for a follow-up mandate. The SPK number is unique
        within a Client.
      tags: [Work Contract (SPK)]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateWorkContractRequest" }
      responses:
        "201":
          description: Work Contract created as DRAFT.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkContract" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listWorkContracts
      x-required-permission: work_contract.read
      x-building-scoped: true
      summary: List Work Contracts (SPK)
      description: >
        Requires `work_contract.read`. Scoped to the caller's accessible
        Buildings.
      tags: [Work Contract (SPK)]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: purchaseOrderId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/WorkContractStatus" }
        - name: spkDateFrom
          in: query
          schema: { type: string, format: date }
        - name: spkDateTo
          in: query
          schema: { type: string, format: date }
      responses:
        "200":
          description: List of Work Contracts.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/WorkContract" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /work-contracts/{id}:
    get:
      operationId: getWorkContract
      x-required-permission: work_contract.read
      x-building-scoped: true
      summary: Get a Work Contract (SPK)
      description: Requires `work_contract.read` and access to the SPK's Building.
      tags: [Work Contract (SPK)]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkContractIdPath"
      responses:
        "200":
          description: Work Contract.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkContract" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateWorkContract
      x-required-permission: work_contract.manage
      x-building-scoped: true
      summary: Update a DRAFT Work Contract (SPK)
      description: >
        Requires `work_contract.manage`. DRAFT only — once ACTIVE the mandate
        is in force and immutable (400 `WORK_CONTRACT_NOT_DRAFT`).


        Identity (`spkNumber`), the commitment reference (`purchaseOrderId`),
        the inherited scope (`clientId`, `buildingId`, `vendorId`) and all
        lifecycle provenance are immutable.
      tags: [Work Contract (SPK)]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkContractIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateWorkContractRequest" }
      responses:
        "200":
          description: Work Contract updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkContract" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-contracts/{id}/available-actions:
    get:
      operationId: getWorkContractAvailableActions
      x-required-permission: work_contract.read
      x-building-scoped: true
      summary: Resolve the caller's available Work Contract actions
      description: >
        Requires `work_contract.read` and access to the SPK's Building. Uses
        the established `{ workContractId, state, availableActions }` shape.
        Actions are upper-case command tokens and are returned only when the
        caller also holds `work_contract.manage` and the existing transition
        preconditions pass. DRAFT can expose `ACTIVATE` and `CANCEL`; ACTIVE
        can expose `COMPLETE` and `CANCEL`; terminal states expose none.
        Vendor usability is rechecked before advertising `ACTIVATE`.
      tags: [Work Contract (SPK)]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkContractIdPath"
      responses:
        "200":
          description: Caller-specific available actions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/WorkContractAvailableActions"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-contracts/{id}/activate:
    post:
      operationId: activateWorkContract
      x-required-permission: work_contract.manage
      x-building-scoped: true
      summary: Activate a Work Contract (DRAFT → ACTIVE)
      description: >
        Requires `work_contract.manage`. Puts the mandate in force, after which
        it becomes immutable.


        The inherited Vendor is re-validated at this moment (it must still be
        ACTIVE, belong to the Client and hold an ACTIVE relationship to the
        Building) — 400 `WORK_CONTRACT_VENDOR_INVALID` otherwise. An invalid or
        repeated transition yields 409 `WORK_CONTRACT_TRANSITION_INVALID`.
      tags: [Work Contract (SPK)]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkContractIdPath"
      responses:
        "200":
          description: Work Contract activated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkContract" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /work-contracts/{id}/complete:
    post:
      operationId: completeWorkContract
      x-required-permission: work_contract.manage
      x-building-scoped: true
      summary: Complete a Work Contract (ACTIVE → COMPLETED)
      description: >
        Requires `work_contract.manage`. Terminal state, reachable ONLY from
        ACTIVE — a DRAFT SPK cannot be completed without having been authorized
        (409 `WORK_CONTRACT_TRANSITION_INVALID`). Completion releases the
        Purchase Order for a follow-up mandate and preserves the activation
        stamp.


        Unlike activation, this wind-down transition does NOT re-validate the
        Vendor, so a mandate can never be stranded by a vendor deactivation.
      tags: [Work Contract (SPK)]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkContractIdPath"
      responses:
        "200":
          description: Work Contract completed.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkContract" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /work-contracts/{id}/cancel:
    post:
      operationId: cancelWorkContract
      x-required-permission: work_contract.manage
      x-building-scoped: true
      summary: Cancel a Work Contract (DRAFT | ACTIVE → CANCELLED)
      description: >
        Requires `work_contract.manage`. Terminal state, reachable from DRAFT
        or ACTIVE; a COMPLETED or already CANCELLED SPK yields 409
        `WORK_CONTRACT_TRANSITION_INVALID`. Cancelling releases the Purchase
        Order and preserves whatever lifecycle stamps the SPK had reached.
      tags: [Work Contract (SPK)]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkContractIdPath"
      responses:
        "200":
          description: Work Contract cancelled.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkContract" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /work-order-procurement-bindings:
    post:
      operationId: createWorkOrderProcurementBinding
      x-required-permission: wo_procurement.manage
      x-building-scoped: true
      summary: Bind a Work Order to its procurement context
      description: >
        Requires `wo_procurement.manage`. Binds a Work Order to its Purchase
        Request and, optionally, to a specific Material or Service Request.
        The Work Order and every referenced record must share one Client and
        Building (400 `WO_PROCUREMENT_BUILDING_MISMATCH`).


        Exactly ONE procurement binding may exist per Work Order (409
        `WO_PROCUREMENT_ALREADY_BOUND`).


        CR-BE-R2P-01 PART 05 — an optional `workContractId` binds the ACTIVE
        SPK chain in the same call. When supplied, the Purchase Order and
        Vendor are DERIVED from that SPK; supplying `purchaseOrderId`,
        `vendorId`, `clientId` or `buildingId` is rejected as an override
        attempt. The same chain rules as
        `POST /work-order-procurement-bindings/{id}/bind-work-contract` apply.
      tags: [Work Order Procurement Binding]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWorkOrderProcurementBindingRequest"
      responses:
        "201":
          description: Binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/WorkOrderProcurementBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /work-order-procurement-bindings/{id}:
    get:
      operationId: getWorkOrderProcurementBinding
      x-required-permission: wo_procurement.read
      x-building-scoped: true
      summary: Get a Work Order procurement binding
      description: >
        Requires `wo_procurement.read`. Returns the binding with its resolved
        Work Order, Purchase Request, Material/Service Request, Receiving and —
        when the chain is bound — Work Contract and Purchase Order summaries.
      tags: [Work Order Procurement Binding]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkOrderProcurementBindingIdPath"
      responses:
        "200":
          description: Binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/WorkOrderProcurementBinding"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-order-procurement-bindings/{id}/resolve-readiness:
    post:
      operationId: resolveWorkOrderProcurementReadiness
      x-required-permission: wo_procurement.manage
      x-building-scoped: true
      summary: Resolve the procurement readiness of a binding
      description: >
        Requires `wo_procurement.manage`. Backend-authoritative: a READY BE-17F
        PO Readiness for the binding's request promotes `procurementStatus`
        from BOUND to READY. This reads the readiness authority — it does not
        re-evaluate readiness.
      tags: [Work Order Procurement Binding]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkOrderProcurementBindingIdPath"
      responses:
        "200":
          description: Readiness resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/WorkOrderProcurementBinding"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-order-procurement-bindings/{id}/link-receiving:
    post:
      operationId: linkReceivingToWorkOrderProcurementBinding
      x-required-permission: wo_procurement.manage
      x-building-scoped: true
      summary: Link a Receiving to a binding
      description: >
        Requires `wo_procurement.manage`. The Receiving must belong to the same
        Client and Building and to the binding's own request (400
        `WO_PROCUREMENT_RECEIVING_MISMATCH`). Linking promotes
        `procurementStatus` to RECEIVED. Receiving quantity, inventory and
        over-receipt rules are owned by BE-17G and are not affected.
      tags: [Work Order Procurement Binding]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkOrderProcurementBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/LinkReceivingRequest" }
      responses:
        "200":
          description: Receiving linked.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/WorkOrderProcurementBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-order-procurement-bindings/{id}/bind-work-contract:
    post:
      operationId: bindWorkContractToProcurementBinding
      x-required-permission: wo_procurement.manage
      x-building-scoped: true
      summary: Bind the ACTIVE SPK chain to a Work Order's binding
      description: >
        Requires `wo_procurement.manage`. Completes the authoritative execution
        chain — Request → Vendor → ISSUED PO → ACTIVE SPK → Work Order — by
        EXTENDING the existing binding row. No parallel SPK ↔ Work Order
        binding domain exists, and the one-binding-per-Work-Order guarantee is
        unchanged.


        `workContractId` is the ONLY input: the Purchase Order and Vendor are
        DERIVED from the SPK. Supplying `purchaseOrderId`, `vendorId`,
        `clientId` or `buildingId` yields 400 VALIDATION_ERROR.


        Enforced: the SPK must be ACTIVE (409
        `WO_PROCUREMENT_WORK_CONTRACT_NOT_ACTIVE` for DRAFT / COMPLETED /
        CANCELLED); its Purchase Order must still be ISSUED (409
        `WO_PROCUREMENT_PURCHASE_ORDER_NOT_ISSUED`); the Work Order's
        Client/Building must match the SPK (400
        `WO_PROCUREMENT_BUILDING_MISMATCH`); and the Vendor holding the ACTIVE
        BE-15A assignment on that Work Order must be the SPK's Vendor (400
        `WO_PROCUREMENT_VENDOR_MISMATCH`, also returned when no vendor is
        assigned). An already-chained binding is never silently overwritten
        (409 `WO_PROCUREMENT_WORK_CONTRACT_ALREADY_BOUND`).
      tags: [Work Order Procurement Binding]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkOrderProcurementBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BindWorkContractRequest" }
      responses:
        "200":
          description: SPK chain bound to the Work Order's procurement binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/WorkOrderProcurementBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /work-orders/{workOrderId}/procurement-bindings:
    get:
      operationId: listWorkOrderProcurementBindings
      x-required-permission: wo_procurement.read
      x-building-scoped: true
      summary: List the procurement context bound to a Work Order
      description: >
        Requires `wo_procurement.read` and access to the Work Order's Building.
        At most one binding exists per Work Order.
      tags: [Work Order Procurement Binding]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: workOrderId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Bindings for the Work Order.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/WorkOrderProcurementBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  # ---------------------------------------------------------------------------
  # CR-BE-MAT-01 — Material fulfilment chain
  # Material Request → MR Line → Receiving → STOCK_IN → Movement/Balance →
  # STOCK_OUT → Work Order Material Usage → operational material cost.
  # ---------------------------------------------------------------------------

  /purchase-requests/{purchaseRequestId}/material-requests:
    post:
      operationId: createMaterialRequest
      summary: Create a Material Request line on a Purchase Request
      description: >
        Requires `material_request.manage` and access to the Purchase
        Request's Building. `clientId`/`buildingId` derive from the Purchase
        Request; `uomId` is captured from the Item Master (a supplied `uomId`
        must match the item UOM exactly — no conversion).
      tags: [Material Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseRequestIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MaterialRequestCreateRequest" }
      responses:
        "201":
          description: Material Request line created (status OPEN).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/MaterialRequest" }
        "400":
          description: >
            Validation failed, or a deterministic domain rejection:
            `MATERIAL_REQUEST_INVALID_QUANTITY`,
            `MATERIAL_REQUEST_PURCHASE_REQUEST_NOT_OPEN`,
            `MATERIAL_REQUEST_ITEM_CLIENT_MISMATCH`,
            `MATERIAL_REQUEST_WAREHOUSE_CLIENT_MISMATCH`,
            `MATERIAL_REQUEST_WAREHOUSE_BUILDING_MISMATCH`,
            `MATERIAL_REQUEST_ITEM_UOM_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    get:
      operationId: listMaterialRequestsByPurchaseRequest
      summary: List Material Request lines of a Purchase Request
      description: Requires `material_request.read` and Building access.
      tags: [Material Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseRequestIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/MaterialRequestStatus" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Material Request lines.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/MaterialRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/material-requests:
    get:
      operationId: listMaterialRequestsByBuilding
      summary: List Material Requests of a Building
      description: >
        Requires `material_request.read`; Building access enforced
        (Client/Building isolation). Optional `status` filter.
      tags: [Material Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/MaterialRequestStatus" }
      responses:
        "200":
          description: Material Request lines.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/MaterialRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /items/{itemId}/material-requests:
    get:
      operationId: listMaterialRequestsByItem
      summary: List Material Requests referencing an inventory item
      description: Requires `material_request.read`. Optional `status` filter.
      tags: [Material Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/InventoryItemIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/MaterialRequestStatus" }
      responses:
        "200":
          description: Material Request lines.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/MaterialRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /material-requests/{id}:
    get:
      operationId: getMaterialRequest
      summary: Get a Material Request line (with fulfilment quantities)
      description: >
        Requires `material_request.read` and Building access. The detail read
        additionally derives `receivedQuantity` (cumulative valid receipts)
        and `remainingQuantity` = (`approvedQuantity` ?? `quantity`) −
        `receivedQuantity`. No separate remaining-quantity store exists.
      tags: [Material Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/MaterialRequestIdPath"
      responses:
        "200":
          description: Material Request line.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/MaterialRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateMaterialRequest
      summary: Update an OPEN Material Request line
      description: >
        Requires `material_request.manage` and Building access. Only OPEN
        lines are editable — the PART 02 freeze rejects updates to APPROVED
        or CANCELLED lines with `MATERIAL_REQUEST_NOT_OPEN` (item identity,
        requested/approved quantity, UOM and warehouse scope are
        fulfilment-critical and cannot be silently changed after approval).
      tags: [Material Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/MaterialRequestIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MaterialRequestUpdateRequest" }
      responses:
        "200":
          description: Updated Material Request line.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/MaterialRequest" }
        "400":
          description: >
            Validation failed, or `MATERIAL_REQUEST_NOT_OPEN` (frozen line),
            `MATERIAL_REQUEST_INVALID_QUANTITY`,
            `MATERIAL_REQUEST_ITEM_UOM_MISMATCH`,
            `MATERIAL_REQUEST_WAREHOUSE_CLIENT_MISMATCH`,
            `MATERIAL_REQUEST_WAREHOUSE_BUILDING_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              examples:
                frozen:
                  value:
                    success: false
                    error:
                      code: MATERIAL_REQUEST_NOT_OPEN
                      message: Material Request is not OPEN.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /material-requests/{id}/cancel:
    post:
      operationId: cancelMaterialRequest
      summary: Cancel a Material Request line (OPEN or APPROVED)
      description: >
        Requires `material_request.manage` and Building access. Cancellation
        is the correction path after approval (PART 02); an already-cancelled
        line is rejected with `MATERIAL_REQUEST_NOT_OPEN`. Not a delete —
        history is preserved.
      tags: [Material Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/MaterialRequestIdPath"
      responses:
        "200":
          description: Cancelled Material Request line.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/MaterialRequest" }
        "400":
          description: "`MATERIAL_REQUEST_NOT_OPEN` — already cancelled."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "`MATERIAL_REQUEST_ACTIVE_RESERVATION` — cancellation is blocked while an ACTIVE reservation remains."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /material-requests/{materialRequestId}/reservations:
    post:
      operationId: createMaterialReservation
      summary: Reserve stock for an approved Material Request
      description: >
        Requires `inventory_stock.manage` and access to the Material Request's
        Building. The Material Request must be APPROVED. The server derives the
        item, Client, Building and UOM from the existing authorities; the
        selected warehouse must be in scope and have sufficient available stock.
        Demand is calculated under a Material Request lock as
        `approvedQuantity ?? quantity - cumulativeIssued - activeReserved`.
        The reservation and existing balance reserved-quantity update commit
        atomically. No stock movement is created because on-hand does not
        change.
      tags: [Material Reservations]
      security: [{ bearerAuth: [] }]
      x-required-permission: inventory_stock.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MaterialReservationSourceIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MaterialReservationCreateRequest" }
      responses:
        "201":
          description: Active reservation created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/MaterialReservation" }
        "400":
          description: >
            Validation failed (`INVENTORY_MATERIAL_RESERVATION_INVALID_QUANTITY`),
            or one of `INVENTORY_MATERIAL_RESERVATION_CLIENT_MISMATCH`,
            `INVENTORY_MATERIAL_RESERVATION_BUILDING_MISMATCH`,
            `INVENTORY_MATERIAL_RESERVATION_ITEM_MISMATCH`,
            `INVENTORY_MATERIAL_RESERVATION_WAREHOUSE_MISMATCH`, or
            `INVENTORY_MATERIAL_RESERVATION_UOM_INCOMPATIBLE` applies.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: >
            `INVENTORY_MATERIAL_RESERVATION_DEMAND_NOT_APPROVED`,
            `INVENTORY_MATERIAL_RESERVATION_DEMAND_EXCEEDED`, or
            `INVENTORY_MATERIAL_RESERVATION_INSUFFICIENT_STOCK`. Nothing is
            committed.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listMaterialReservations
      summary: List reservations of a Material Request
      description: >
        Requires `inventory_stock.read` and Building access. Results are scoped
        to the Material Request and may be filtered by reservation `status`.
      tags: [Material Reservations]
      security: [{ bearerAuth: [] }]
      x-required-permission: inventory_stock.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MaterialReservationSourceIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/MaterialReservationStatus" }
      responses:
        "200":
          description: Material reservations.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/MaterialReservation" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /material-reservations/{id}:
    get:
      operationId: getMaterialReservation
      summary: Get a material reservation
      description: >
        Requires `inventory_stock.read` and Building access.
        `INVENTORY_MATERIAL_RESERVATION_NOT_FOUND` is returned when the
        reservation does not exist.
      tags: [Material Reservations]
      security: [{ bearerAuth: [] }]
      x-required-permission: inventory_stock.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MaterialReservationIdPath"
      responses:
        "200":
          description: Material reservation.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/MaterialReservation" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /material-reservations/{id}/release:
    post:
      operationId: releaseMaterialReservation
      summary: Release an active material reservation
      description: >
        Requires `inventory_stock.manage` and Building access. Only ACTIVE
        reservations can be released. The existing stock balance's reserved
        quantity is reduced exactly once; on-hand is unchanged.
      tags: [Material Reservations]
      security: [{ bearerAuth: [] }]
      x-required-permission: inventory_stock.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MaterialReservationIdPath"
      responses:
        "200":
          description: Reservation released.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/MaterialReservation" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "`INVENTORY_MATERIAL_RESERVATION_NOT_ACTIVE` — reservation is already released, cancelled, or consumed."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /material-reservations/{id}/cancel:
    post:
      operationId: cancelMaterialReservation
      summary: Cancel an active material reservation
      description: >
        Requires `inventory_stock.manage` and Building access. Only ACTIVE
        reservations can be cancelled. The existing stock balance's reserved
        quantity is reduced exactly once; on-hand is unchanged.
      tags: [Material Reservations]
      security: [{ bearerAuth: [] }]
      x-required-permission: inventory_stock.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MaterialReservationIdPath"
      responses:
        "200":
          description: Reservation cancelled.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/MaterialReservation" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "`INVENTORY_MATERIAL_RESERVATION_NOT_ACTIVE` — reservation is already released, cancelled, or consumed."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /procurement-approvals:
    post:
      operationId: createProcurementApproval
      summary: Create a procurement approval binding
      description: >
        Requires `procurement_approval.manage` and Building access. Binds one
        PENDING approval decision to a PURCHASE_REQUEST, MATERIAL_REQUEST or
        SERVICE_REQUEST that is still OPEN. For RFQ awards, the target is a
        CLOSED RFQ and the request must include `requestType: RFQ`,
        `approvalType: RFQ_AWARD`, and the exact `recommendationId`. The
        approver must hold `procurement_approval.manage` in the target
        Building.
      tags: [Procurement Approvals]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ProcurementApprovalCreateRequest" }
      responses:
        "201":
          description: PENDING approval binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ProcurementApproval" }
        "400":
          description: >
            Validation failed, or `PROCUREMENT_APPROVAL_REQUEST_INVALID`,
            `PROCUREMENT_APPROVAL_APPROVER_INVALID`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409":
          description: "`PROCUREMENT_APPROVAL_ALREADY_PENDING` — duplicate pending binding."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /procurement-approvals/pending:
    get:
      operationId: listPendingProcurementApprovals
      summary: List pending procurement approvals in the accessible scope
      description: >
        Requires `procurement_approval.read`. Results are limited to the
        caller's accessible Buildings. Filters: `buildingId`,
        `approverUserId`, `requestType`, `approvalType`.
      tags: [Procurement Approvals]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: approverUserId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: requestType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ProcurementApprovalRequestType" }
        - name: approvalType
          in: query
          required: false
          schema: { type: string }
      responses:
        "200":
          description: Pending approval bindings.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/ProcurementApproval" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /procurement-approvals/{id}:
    get:
      operationId: getProcurementApproval
      summary: Get a procurement approval binding
      description: Requires `procurement_approval.read` and Building access.
      tags: [Procurement Approvals]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ProcurementApprovalIdPath"
      responses:
        "200":
          description: Approval binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ProcurementApproval" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /procurement-approvals/{id}/available-actions:
    get:
      operationId: getProcurementApprovalAvailableActions
      summary: Resolve the caller's available approval actions
      description: >
        Requires `procurement_approval.read` and Building access. Only the
        assigned approver of a PENDING binding whose target request is still
        OPEN receives `APPROVE` / `REJECT`.
      tags: [Procurement Approvals]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ProcurementApprovalIdPath"
      responses:
        "200":
          description: Available actions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ProcurementApprovalAvailableActions" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /procurement-approvals/{id}/approve:
    post:
      operationId: approveProcurementApproval
      summary: Approve (optionally with an explicit approved quantity)
      description: >
        Requires `procurement_approval.manage`; only the assigned approver may
        decide. CR-BE-MAT-01 PART 02 — the decision and the Material Request
        promotion commit atomically: approving a MATERIAL_REQUEST binding sets
        the line's authoritative `approvedQuantity` (explicit value, or
        default approved = requested) and freezes the line; approving a
        PURCHASE_REQUEST binding defaults approved = requested on every
        still-OPEN line (already APPROVED lines keep their approved quantity).
        `approvedQuantity` is only applicable when APPROVING a
        MATERIAL_REQUEST binding, must be > 0 and must not exceed the
        requested line quantity.
      tags: [Procurement Approvals]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ProcurementApprovalIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ProcurementApprovalDecisionRequest" }
      responses:
        "200":
          description: Approval decided (APPROVED).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ProcurementApproval" }
        "400":
          description: >
            Validation failed, or a deterministic approved-quantity rejection:
            `MATERIAL_REQUEST_APPROVED_QUANTITY_INVALID`,
            `MATERIAL_REQUEST_APPROVED_QUANTITY_EXCEEDS_REQUESTED`,
            `PROCUREMENT_APPROVAL_APPROVED_QUANTITY_NOT_APPLICABLE`. The
            binding stays PENDING and no Material Request is mutated.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              examples:
                exceedsRequested:
                  value:
                    success: false
                    error:
                      code: MATERIAL_REQUEST_APPROVED_QUANTITY_EXCEEDS_REQUESTED
                      message: Approved quantity must not exceed the requested Material Request quantity.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            Missing permission, `PROCUREMENT_APPROVAL_UNAUTHORIZED_APPROVER`,
            `PROCUREMENT_APPROVAL_ACTION_NOT_ALLOWED`, or Building access
            denied.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "`PROCUREMENT_APPROVAL_ALREADY_DECIDED` — decisions are append-only."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /procurement-approvals/{id}/reject:
    post:
      operationId: rejectProcurementApproval
      summary: Reject a pending procurement approval
      description: >
        Requires `procurement_approval.manage`; only the assigned approver may
        decide. Rejection never mutates the target request;
        `approvedQuantity` is not applicable
        (`PROCUREMENT_APPROVAL_APPROVED_QUANTITY_NOT_APPLICABLE`).
      tags: [Procurement Approvals]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ProcurementApprovalIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ProcurementApprovalDecisionRequest" }
      responses:
        "200":
          description: Approval decided (REJECTED).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ProcurementApproval" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "`PROCUREMENT_APPROVAL_ALREADY_DECIDED`."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /receivings:
    post:
      operationId: createReceiving
      summary: Record a receiving (material posts STOCK_IN through the ledger)
      description: >
        Requires `receiving.manage` and access to the request's Building. The
        request + vendor must have a READY purchase-order readiness. MATERIAL
        receiving validates item/warehouse Client-Building scope and, when
        `materialRequestId` is supplied (CR-BE-MAT-01 PART 01), binds the
        receiving to that Material Request line: same Purchase Request,
        Client/Building scope, item identity and target warehouse are
        enforced, the UOM snapshot is taken from the line (PART 04, no
        conversion), and the cumulative over-receipt guard caps
        received + new ≤ (approvedQuantity ?? quantity) under a line lock
        (PART 02). Line guard, STOCK_IN movement and receiving row commit in
        ONE transaction — a rejected receiving mutates nothing. SERVICE
        receiving records acceptance without stock movement.
      tags: [Receivings]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ReceivingCreateRequest" }
      responses:
        "201":
          description: Receiving recorded (status RECEIVED).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Receiving" }
        "400":
          description: >
            Validation failed, or a deterministic domain rejection:
            `RECEIVING_REQUEST_INVALID`, `RECEIVING_VENDOR_INVALID`,
            `RECEIVING_READINESS_INVALID`, `RECEIVING_INVALID_QUANTITY`,
            `RECEIVING_ITEM_CLIENT_MISMATCH`,
            `RECEIVING_WAREHOUSE_CLIENT_MISMATCH`,
            `RECEIVING_WAREHOUSE_BUILDING_MISMATCH`,
            `RECEIVING_MATERIAL_REQUEST_INVALID` (unknown/cancelled line or
            different Purchase Request),
            `RECEIVING_MATERIAL_REQUEST_ITEM_MISMATCH`,
            `RECEIVING_MATERIAL_REQUEST_SCOPE_MISMATCH` (cross Client/Building
            or different target warehouse), `RECEIVING_UOM_INCOMPATIBLE`
            (PART 04 — differing unit without a conversion authority).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              examples:
                uomIncompatible:
                  value:
                    success: false
                    error:
                      code: RECEIVING_UOM_INCOMPATIBLE
                      message: The receiving UOM is incompatible with the Material Request line / item UOM and no conversion authority exists.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409":
          description: >
            `RECEIVING_OVER_RECEIPT` — cumulative received quantity would
            exceed the Material Request line's approved (or requested)
            quantity. No STOCK_IN, balance change or receiving row is
            created.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              examples:
                overReceipt:
                  value:
                    success: false
                    error:
                      code: RECEIVING_OVER_RECEIPT
                      message: Cumulative received quantity would exceed the Material Request line quantity.

  /receivings/{id}:
    get:
      operationId: getReceiving
      summary: Get a receiving record
      description: Requires `receiving.read` and Building access.
      tags: [Receivings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ReceivingIdPath"
      responses:
        "200":
          description: Receiving record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Receiving" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateReceiving
      summary: Update notes on a RECEIVED record
      description: >
        Requires `receiving.manage` and Building access. Only `notes` is
        mutable; a FINALIZED record is immutable
        (`RECEIVING_ALREADY_FINALIZED`).
      tags: [Receivings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ReceivingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ReceivingUpdateRequest" }
      responses:
        "200":
          description: Updated receiving record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Receiving" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "`RECEIVING_ALREADY_FINALIZED`."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /receivings/{id}/finalize:
    post:
      operationId: finalizeReceiving
      summary: Finalize a receiving (RECEIVED → FINALIZED)
      description: >
        Requires `receiving.manage` and Building access. Idempotency guard: an
        already-FINALIZED record cannot be finalized again
        (`RECEIVING_ALREADY_FINALIZED`). FINALIZED receipts still count toward
        the PART 01 cumulative over-receipt guard.
      tags: [Receivings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ReceivingIdPath"
      responses:
        "200":
          description: Finalized receiving record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Receiving" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "`RECEIVING_ALREADY_FINALIZED`."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /buildings/{buildingId}/receivings:
    get:
      operationId: listReceivingsByBuilding
      summary: List receivings of a Building
      description: >
        Requires `receiving.read` and Building access. Filters: `status`,
        `receivingType`.
      tags: [Receivings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReceivingStatus" }
        - name: receivingType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReceivingType" }
      responses:
        "200":
          description: Receiving records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Receiving" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /purchase-requests/{purchaseRequestId}/receivings:
    get:
      operationId: listReceivingsByPurchaseRequest
      summary: List receivings of a Purchase Request
      description: >
        Requires `receiving.read` and access to the request's Building.
        Filters: `status`, `receivingType`.
      tags: [Receivings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseRequestIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReceivingStatus" }
        - name: receivingType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReceivingType" }
      responses:
        "200":
          description: Receiving records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Receiving" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /service-requests/{serviceRequestId}/receivings:
    get:
      operationId: listReceivingsByServiceRequest
      summary: List receivings of a Service Request
      description: >
        Requires `receiving.read` and access to the request's Building.
        Filters: `status`, `receivingType`.
      tags: [Receivings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ServiceRequestIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReceivingStatus" }
        - name: receivingType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReceivingType" }
      responses:
        "200":
          description: Receiving records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Receiving" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /vendors/{vendorId}/receivings:
    get:
      operationId: listReceivingsByVendor
      summary: List receivings of a Vendor within the accessible scope
      description: >
        Requires `receiving.read`. Results are limited to the caller's
        accessible Buildings. Filters: `status`, `receivingType`.
      tags: [Receivings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VendorIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReceivingStatus" }
        - name: receivingType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReceivingType" }
      responses:
        "200":
          description: Receiving records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Receiving" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /warehouses/{warehouseId}/stock-movements:
    post:
      operationId: postWarehouseStockMovement
      summary: Post a STOCK_IN / STOCK_OUT movement (ledger authority)
      description: >
        Requires `inventory_stock.manage` and access to the warehouse's
        Building. The movement and its balance update commit atomically under
        a `FOR UPDATE` lock (CR-BE-MAT-01 PART 03 — a balance never changes
        without a movement; a failed movement changes nothing). The item UOM
        is snapshotted on the movement (PART 04). STOCK_OUT requires
        sufficient available stock; negative resulting stock is impossible.
      tags: [Inventory Stock]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WarehouseIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StockMovementCreateRequest" }
      responses:
        "201":
          description: Movement posted; balance updated atomically.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/StockMovement" }
        "400":
          description: >
            Validation failed, or `INVENTORY_STOCK_MOVEMENT_INVALID_QUANTITY`,
            `INVENTORY_STOCK_MOVEMENT_CLIENT_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: >
            `INVENTORY_STOCK_MOVEMENT_INSUFFICIENT_STOCK` — STOCK_OUT above
            available stock, or `INVENTORY_STOCK_MOVEMENT_WORK_ORDER_BYPASS`
            when the exact Work Order material source convention is supplied.
            Nothing is committed.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listWarehouseStockMovements
      summary: List movements of a warehouse
      description: >
        Requires `inventory_stock.read` and Building access. Filters:
        `itemId`, `movementType`, `dateFrom`, `dateTo`, `reference`.
      tags: [Inventory Stock]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WarehouseIdPath"
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: movementType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/StockMovementType" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string, format: date-time }
        - name: dateTo
          in: query
          required: false
          schema: { type: string, format: date-time }
        - name: reference
          in: query
          required: false
          schema: { type: string }
      responses:
        "200":
          description: Append-only movement ledger entries (newest first).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/StockMovement" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /clients/{clientId}/stock-movements:
    post:
      operationId: postClientStockMovement
      summary: Post a movement (warehouse supplied in the body)
      description: >
        Same contract as the warehouse-scoped POST; `warehouseId` is required
        in the body. Requires `inventory_stock.manage` and access to the
        warehouse's Building.
      tags: [Inventory Stock]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StockMovementCreateRequest" }
      responses:
        "201":
          description: Movement posted; balance updated atomically.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/StockMovement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "`INVENTORY_STOCK_MOVEMENT_INSUFFICIENT_STOCK`."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listClientStockMovements
      summary: List movements in a Client scope
      description: >
        Requires `inventory_stock.read` and Client access. Filters:
        `buildingId`, `warehouseId`, `itemId`, `movementType`, `dateFrom`,
        `dateTo`, `reference`.
      tags: [Inventory Stock]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: movementType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/StockMovementType" }
      responses:
        "200":
          description: Movement ledger entries.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/StockMovement" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /buildings/{buildingId}/stock-movements:
    get:
      operationId: listBuildingStockMovements
      summary: List movements of a Building
      description: >
        Requires `inventory_stock.read` and Building access. Filters:
        `warehouseId`, `itemId`, `movementType`, `dateFrom`, `dateTo`.
      tags: [Inventory Stock]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: movementType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/StockMovementType" }
      responses:
        "200":
          description: Movement ledger entries.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/StockMovement" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /stock-movements/{id}:
    get:
      operationId: getStockMovement
      summary: Get a single ledger movement
      description: >
        Requires `inventory_stock.read` and Building access. Movements are
        immutable; historical rows without a UOM snapshot return
        `uomId: null` (fall back to the item's current UOM).
      tags: [Inventory Stock]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/StockMovementIdPath"
      responses:
        "200":
          description: Movement ledger entry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/StockMovement" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /warehouses/{warehouseId}/stock-balances:
    post:
      operationId: initializeWarehouseStockBalance
      summary: Initialize a stock balance (non-zero seeds enter the ledger)
      description: >
        Requires `inventory_stock.manage` and Building access. One balance per
        warehouse + item. Zero-quantity initialization stays movement-free;
        a NON-ZERO initialization writes a `BALANCE_INITIALIZATION` STOCK_IN
        movement atomically with the balance (CR-BE-MAT-01 PART 03 — no
        silent seed outside the ledger; an unattributable non-zero seed is
        rejected with `INVENTORY_STOCK_BALANCE_INIT_ACTOR_REQUIRED`).
      tags: [Inventory Stock]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WarehouseIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StockBalanceInitializeRequest" }
      responses:
        "201":
          description: Balance initialized.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/StockBalance" }
        "400":
          description: >
            Validation failed, or `INVENTORY_STOCK_BALANCE_NEGATIVE_QUANTITY`,
            `INVENTORY_STOCK_BALANCE_RESERVED_EXCEEDS_ON_HAND`,
            `INVENTORY_STOCK_BALANCE_CLIENT_MISMATCH`,
            `INVENTORY_STOCK_BALANCE_INIT_ACTOR_REQUIRED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "`INVENTORY_STOCK_BALANCE_ALREADY_EXISTS` — one balance per warehouse + item."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listWarehouseStockBalances
      summary: List balances of a warehouse
      description: >
        Requires `inventory_stock.read` and Building access. Filter: `itemId`.
        Balances are the derived current stock state — there is no direct
        balance mutation endpoint; stock changes only through movements.
      tags: [Inventory Stock]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WarehouseIdPath"
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Stock balances.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/StockBalance" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /clients/{clientId}/stock-balances:
    post:
      operationId: initializeClientStockBalance
      summary: Initialize a stock balance (warehouse supplied in the body)
      description: >
        Same contract as the warehouse-scoped POST; `warehouseId` is required
        in the body.
      tags: [Inventory Stock]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StockBalanceInitializeRequest" }
      responses:
        "201":
          description: Balance initialized.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/StockBalance" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "`INVENTORY_STOCK_BALANCE_ALREADY_EXISTS`."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listClientStockBalances
      summary: List balances in a Client scope
      description: >
        Requires `inventory_stock.read` and Client access. Filters:
        `buildingId`, `warehouseId`, `itemId`.
      tags: [Inventory Stock]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Stock balances.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/StockBalance" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /buildings/{buildingId}/stock-balances:
    get:
      operationId: listBuildingStockBalances
      summary: List balances of a Building
      description: >
        Requires `inventory_stock.read` and Building access. Filters:
        `warehouseId`, `itemId`, `clientId`.
      tags: [Inventory Stock]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Stock balances.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/StockBalance" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /stock-balances/{id}:
    get:
      operationId: getStockBalance
      summary: Get a single stock balance
      description: Requires `inventory_stock.read` and Building access.
      tags: [Inventory Stock]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/StockBalanceIdPath"
      responses:
        "200":
          description: Stock balance.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/StockBalance" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/material-usages:
    post:
      operationId: recordWorkOrderMaterialUsage
      summary: Issue material to a Work Order (controlled issue flow)
      description: >
        Requires `inventory_stock.manage` and access to the Work Order's
        Building. CR-BE-INV-CONTROL-01 PART 02 — the Work Order must have an
        existing procurement binding to an APPROVED Material Request whose item,
        UOM, Client, and Building match the issue. The issue quantity is capped
        by `approved_quantity ?? quantity - cumulative controlled issues`;
        receiving quantity is not used as issued quantity. An optional
        `reservationId` consumes only that ACTIVE reservation allocation.
        Unreserved issues use existing available stock. STOCK_OUT, balance,
        usage, and reservation progress commit or fail TOGETHER. Existing UOM,
        cost, movement linkage, and duplicate-reference protections remain.
      tags: [Work Order Material Usage]
      security: [{ bearerAuth: [] }]
      x-required-permission: inventory_stock.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/WorkOrderMaterialUsageCreateRequest" }
      responses:
        "201":
          description: Material issued; STOCK_OUT, balance and usage committed atomically.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderMaterialUsage" }
        "400":
          description: >
            Validation failed, or a deterministic domain rejection:
            `INVENTORY_WO_MATERIAL_USAGE_INVALID_QUANTITY`,
            `WO_MATERIAL_USAGE_INVALID_UNIT_COST`,
            `WO_MATERIAL_USAGE_UOM_INCOMPATIBLE`,
            `INVENTORY_WO_MATERIAL_USAGE_CLIENT_MISMATCH`,
            `INVENTORY_WO_MATERIAL_USAGE_BUILDING_MISMATCH`,
            `INVENTORY_WO_MATERIAL_USAGE_MATERIAL_REQUEST_INVALID`, or
            `INVENTORY_WO_MATERIAL_USAGE_RESERVATION_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              examples:
                uomIncompatible:
                  value:
                    success: false
                    error:
                      code: WO_MATERIAL_USAGE_UOM_INCOMPATIBLE
                      message: The supplied UOM is incompatible with the item UOM and no conversion authority exists.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: >
            `WORK_ORDER_NOT_FOUND`, `INVENTORY_ITEM_NOT_FOUND`,
            `INVENTORY_WAREHOUSE_NOT_FOUND`, `MATERIAL_REQUEST_NOT_FOUND`, or
            `INVENTORY_MATERIAL_RESERVATION_NOT_FOUND`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `INVENTORY_WO_MATERIAL_USAGE_INSUFFICIENT_STOCK` (issue above
            available or physical stock — negative resulting stock is
            impossible), `INVENTORY_WO_MATERIAL_USAGE_DEMAND_EXCEEDED`,
            `INVENTORY_WO_MATERIAL_USAGE_MATERIAL_REQUEST_REQUIRED`,
            `INVENTORY_WO_MATERIAL_USAGE_MATERIAL_REQUEST_NOT_APPROVED`,
            `INVENTORY_MATERIAL_RESERVATION_NOT_ACTIVE`, or
            `INVENTORY_MATERIAL_RESERVATION_ALLOCATION_EXCEEDED`. The existing
            `WO_MATERIAL_USAGE_WORK_ORDER_STATE_INVALID` and
            `WO_MATERIAL_USAGE_DUPLICATE_REFERENCE` protections remain. Nothing
            is committed.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              examples:
                duplicateIssue:
                  value:
                    success: false
                    error:
                      code: WO_MATERIAL_USAGE_DUPLICATE_REFERENCE
                      message: A material usage with this reference already exists for the work order (duplicate issue protection).
    get:
      operationId: listWorkOrderMaterialUsages
      summary: List material usages of a Work Order
      description: >
        Requires `inventory_stock.read` and Building access. Filters:
        `warehouseId`, `itemId`, `materialRequestId`, `reservationId`,
        `dateFrom`, `dateTo`.
      tags: [Work Order Material Usage]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: materialRequestId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
          description: Filter by the controlled Material Request demand source.
        - name: reservationId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
          description: Filter by the reservation allocation consumed by the usage.
        - name: dateFrom
          in: query
          required: false
          schema: { type: string, format: date-time }
        - name: dateTo
          in: query
          required: false
          schema: { type: string, format: date-time }
      responses:
        "200":
          description: Material usage records (append-only).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/WorkOrderMaterialUsage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/material-cost-summary:
    get:
      operationId: getWorkOrderMaterialCostSummary
      summary: Deterministic Work Order material-cost aggregation
      description: >
        Requires `inventory_stock.read` and access to the Work Order's
        Building. CR-BE-MAT-01 PART 05 — aggregates the immutable operational
        cost snapshots: Work Order → Material Usage → total material cost
        (NUMERIC sum, with a per-currency breakdown). Material cost only — no
        labour/service cost, no valuation/GL. Pure read: never mutates stock.
      tags: [Work Order Material Usage]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      responses:
        "200":
          description: Material cost summary.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderMaterialCostSummary" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/work-order-material-usages:
    get:
      operationId: listBuildingWorkOrderMaterialUsages
      summary: List Work Order material usages of a Building
      description: >
        Requires `inventory_stock.read` and Building access. Filters:
        `workOrderId`, `warehouseId`, `itemId`, `materialRequestId`,
        `reservationId`, `dateFrom`, `dateTo`.
      tags: [Work Order Material Usage]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: workOrderId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: materialRequestId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
          description: Filter by the controlled Material Request demand source.
        - name: reservationId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
          description: Filter by the reservation allocation consumed by the usage.
      responses:
        "200":
          description: Material usage records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/WorkOrderMaterialUsage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /clients/{clientId}/work-order-material-usages:
    get:
      operationId: listClientWorkOrderMaterialUsages
      summary: List Work Order material usages in a Client scope
      description: >
        Requires `inventory_stock.read` and Client access. Filters:
        `buildingId`, `workOrderId`, `warehouseId`, `itemId`,
        `materialRequestId`, `reservationId`, `dateFrom`, `dateTo`.
      tags: [Work Order Material Usage]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: workOrderId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: materialRequestId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
          description: Filter by the controlled Material Request demand source.
        - name: reservationId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
          description: Filter by the reservation allocation consumed by the usage.
      responses:
        "200":
          description: Material usage records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/WorkOrderMaterialUsage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /work-order-material-usages/{id}:
    get:
      operationId: getWorkOrderMaterialUsage
      summary: Get a single Work Order material usage
      description: >
        Requires `inventory_stock.read` and Building access. Usage records
        are immutable; UOM and cost snapshots never change after issue.
        Historical rows without snapshots return null fields (fall back to
        the item's current UOM for legacy reads).
      tags: [Work Order Material Usage]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkOrderMaterialUsageIdPath"
      responses:
        "200":
          description: Material usage record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderMaterialUsage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # --------------------------------------------------------------------
  # CR-BE-MOB-01 PART 01 — Housekeeping operational contract.
  # Existing BE-11 routes published for mobile. Execution writes stay on
  # BE-07 task/checklist endpoints; rework stays on BE-09 findings.
  # --------------------------------------------------------------------
  /buildings/{buildingId}/housekeeping/daily-cleaning:
    get:
      operationId: listBuildingDailyCleaning
      x-required-permission: daily_cleaning.read
      x-building-scoped: true
      summary: List daily cleaning tasks for a Building
      description: >-
        BE-11C read view of BE-07 generated tasks bound to a Cleaning Area.
        Requires `daily_cleaning.read` and BE-02G Building access.
        Execution writes are `startTask` / `completeTask` / `cancelTask`
        using `taskId` (same as `id`). Mobile relevance: cleaning work list.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: date
          in: query
          required: false
          schema: { type: string, format: date }
          description: Operational date YYYY-MM-DD.
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/DailyCleaningStatus" }
      responses:
        "200":
          description: Daily cleaning list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/DailyCleaning" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/daily-cleaning/{id}:
    get:
      operationId: getDailyCleaning
      x-required-permission: daily_cleaning.read
      x-building-scoped: true
      summary: Get a daily cleaning task by id
      description: >-
        Authoritative daily-cleaning read. `id` is the BE-07 generated task
        id (`taskId`). Requires `daily_cleaning.read` and Building access
        on the task's Building.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DailyCleaningIdPath"
      responses:
        "200":
          description: Daily cleaning record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/DailyCleaning" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/cleaning-areas/{id}/daily-cleaning:
    get:
      operationId: listCleaningAreaDailyCleaning
      x-required-permission: daily_cleaning.read
      x-building-scoped: true
      summary: List daily cleaning tasks for a Cleaning Area
      description: >-
        Requires `daily_cleaning.read` and Building access on the area's
        Building. Path `{id}` is the Cleaning Area id.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/CleaningAreaIdPath"
        - name: date
          in: query
          required: false
          schema: { type: string, format: date }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/DailyCleaningStatus" }
      responses:
        "200":
          description: Daily cleaning list for the area.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/DailyCleaning" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/daily-cleaning/{id}/assignments:
    get:
      operationId: listDailyCleaningAssignments
      x-required-permission: cleaning_assignment.read
      x-building-scoped: true
      summary: List assignments for a daily cleaning task
      description: >-
        BE-11D reuse of BE-07 task assignment. Requires
        `cleaning_assignment.read` and Building access. Path `{id}` is the
        daily-cleaning / task id.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DailyCleaningIdPath"
      responses:
        "200":
          description: Assignment list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/CleaningAssignment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: assignDailyCleaning
      x-required-permission: cleaning_assignment.manage
      x-building-scoped: true
      summary: Assign a daily cleaning task
      description: >-
        MUTATION — binds WORKFORCE or TEAM via BE-07 task assignment.
        Requires `cleaning_assignment.manage` and Building access.
        Terminal tasks (COMPLETED/CANCELLED) are rejected. Replaces any
        ACTIVE assignment.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DailyCleaningIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateCleaningAssignmentRequest" }
      responses:
        "201":
          description: Assignment created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/CleaningAssignment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /workforce/{workforceId}/housekeeping/daily-cleaning:
    get:
      operationId: listWorkforceDailyCleaning
      x-required-permission: cleaning_assignment.read
      x-building-scoped: true
      summary: List daily cleaning assigned to a workforce profile
      description: >-
        Requires `cleaning_assignment.read`. Optional date/status filters.
        Mobile relevance: my-cleaning feed keyed to workforceProfileId
        from `GET /auth/me`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkforceIdPathParam"
        - name: date
          in: query
          required: false
          schema: { type: string, format: date }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/DailyCleaningStatus" }
      responses:
        "200":
          description: Assigned daily cleaning list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/DailyCleaning" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /teams/{teamId}/housekeeping/daily-cleaning:
    get:
      operationId: listTeamDailyCleaning
      x-required-permission: cleaning_assignment.read
      x-building-scoped: true
      summary: List daily cleaning assigned to a Team
      description: Requires `cleaning_assignment.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TeamIdPathParam"
        - name: date
          in: query
          required: false
          schema: { type: string, format: date }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/DailyCleaningStatus" }
      responses:
        "200":
          description: Team daily cleaning list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/DailyCleaning" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/toilet-inspection-bindings:
    get:
      operationId: listToiletInspectionBindings
      x-required-permission: toilet_inspection.read
      x-building-scoped: true
      summary: List toilet inspection bindings
      description: >-
        BE-11E binding of a Cleaning Area to a BE-07 checklist template.
        Requires `toilet_inspection.read`. Pass `buildingId` for scoped
        mobile lists (Building access asserted).
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/HousekeepingBindingStatus" }
      responses:
        "200":
          description: Binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/ToiletInspectionBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createToiletInspectionBinding
      x-required-permission: toilet_inspection.manage
      x-building-scoped: true
      summary: Create a toilet inspection binding
      description: >-
        Requires `toilet_inspection.manage` and Building access on the
        cleaning area. Does not execute an inspection.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateToiletInspectionBindingRequest" }
      responses:
        "201":
          description: Binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ToiletInspectionBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/toilet-inspection-bindings/{id}:
    get:
      operationId: getToiletInspectionBinding
      x-required-permission: toilet_inspection.read
      x-building-scoped: true
      summary: Get a toilet inspection binding
      description: Requires `toilet_inspection.read` and Building access.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ToiletInspectionBindingIdPath"
      responses:
        "200":
          description: Binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ToiletInspectionBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateToiletInspectionBinding
      x-required-permission: toilet_inspection.manage
      x-building-scoped: true
      summary: Update a toilet inspection binding
      description: Requires `toilet_inspection.manage` and Building access.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ToiletInspectionBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateToiletInspectionBindingRequest" }
      responses:
        "200":
          description: Binding updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ToiletInspectionBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/toilet-inspection-bindings/{id}/start:
    post:
      operationId: startToiletInspectionExecution
      x-required-permission: toilet_inspection.manage
      x-building-scoped: true
      summary: Start a toilet inspection checklist execution
      description: >-
        Creates a BE-07 checklist execution for the binding. Complete/cancel
        and responses stay on published checklist endpoints using the
        returned execution `id`. Requires `toilet_inspection.manage`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ToiletInspectionBindingIdPath"
      responses:
        "201":
          description: Execution started.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ToiletInspectionExecutionContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/toilet-inspection-executions/{id}:
    get:
      operationId: getToiletInspectionExecution
      x-required-permission: toilet_inspection.read
      x-building-scoped: true
      summary: Get toilet inspection execution context
      description: >-
        Path `{id}` is the BE-07 checklist execution id. Requires
        `toilet_inspection.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ToiletInspectionExecutionIdPath"
      responses:
        "200":
          description: Execution plus binding context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ToiletInspectionExecutionContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/public-area-inspection-bindings:
    get:
      operationId: listPublicAreaInspectionBindings
      x-required-permission: public_area_inspection.read
      x-building-scoped: true
      summary: List public-area inspection bindings
      description: Requires `public_area_inspection.read`. Pass `buildingId` for scoped mobile lists.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/HousekeepingBindingStatus" }
      responses:
        "200":
          description: Binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicAreaInspectionBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createPublicAreaInspectionBinding
      x-required-permission: public_area_inspection.manage
      x-building-scoped: true
      summary: Create a public-area inspection binding
      description: Requires `public_area_inspection.manage` and Building access.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePublicAreaInspectionBindingRequest" }
      responses:
        "201":
          description: Binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicAreaInspectionBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/public-area-inspection-bindings/{id}:
    get:
      operationId: getPublicAreaInspectionBinding
      x-required-permission: public_area_inspection.read
      x-building-scoped: true
      summary: Get a public-area inspection binding
      description: Requires `public_area_inspection.read` and Building access.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PublicAreaInspectionBindingIdPath"
      responses:
        "200":
          description: Binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicAreaInspectionBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updatePublicAreaInspectionBinding
      x-required-permission: public_area_inspection.manage
      x-building-scoped: true
      summary: Update a public-area inspection binding
      description: Requires `public_area_inspection.manage` and Building access.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PublicAreaInspectionBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdatePublicAreaInspectionBindingRequest" }
      responses:
        "200":
          description: Binding updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicAreaInspectionBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/public-area-inspection-bindings/{id}/start:
    post:
      operationId: startPublicAreaInspectionExecution
      x-required-permission: public_area_inspection.manage
      x-building-scoped: true
      summary: Start a public-area inspection checklist execution
      description: >-
        Creates a BE-07 checklist execution. Complete/responses stay on
        published checklist endpoints. Requires `public_area_inspection.manage`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PublicAreaInspectionBindingIdPath"
      responses:
        "201":
          description: Execution started.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicAreaInspectionExecutionContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/public-area-inspection-executions/{id}:
    get:
      operationId: getPublicAreaInspectionExecution
      x-required-permission: public_area_inspection.read
      x-building-scoped: true
      summary: Get public-area inspection execution context
      description: Path `{id}` is the BE-07 checklist execution id. Requires `public_area_inspection.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PublicAreaInspectionExecutionIdPath"
      responses:
        "200":
          description: Execution plus binding context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicAreaInspectionExecutionContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/supervisor-inspections:
    get:
      operationId: listSupervisorInspections
      x-required-permission: supervisor_inspection.read
      x-building-scoped: true
      summary: List supervisor inspections
      description: >-
        BE-11G verification records over daily cleaning / toilet / public-area
        executions, using the BE-07 review primitive. Requires
        `supervisor_inspection.read`. Pass `buildingId` for scoped lists.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: targetType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/SupervisorInspectionTargetType" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/SupervisorInspectionStatus" }
      responses:
        "200":
          description: Supervisor inspection list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/SupervisorInspection" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createSupervisorInspection
      x-required-permission: supervisor_inspection.manage
      x-building-scoped: true
      summary: Open a supervisor inspection
      description: >-
        Requires `supervisor_inspection.manage`. `supervisorUserId` is the
        authenticated user. Decision is submitted separately.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateSupervisorInspectionRequest" }
      responses:
        "201":
          description: Inspection opened.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SupervisorInspection" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/supervisor-inspections/{id}:
    get:
      operationId: getSupervisorInspection
      x-required-permission: supervisor_inspection.read
      x-building-scoped: true
      summary: Get a supervisor inspection
      description: Requires `supervisor_inspection.read` and Building access.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/SupervisorInspectionIdPath"
      responses:
        "200":
          description: Supervisor inspection.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SupervisorInspection" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/supervisor-inspections/{id}/decision:
    post:
      operationId: submitSupervisorInspectionDecision
      x-required-permission: supervisor_inspection.manage
      x-building-scoped: true
      summary: Submit a supervisor inspection decision
      description: >-
        MUTATION — APPROVED / REJECTED / REWORK_REQUIRED via BE-07 review.
        Requires `supervisor_inspection.manage`. There is no reinspection
        engine; FAIL/REWORK is followed by a new checklist start plus BE-09
        finding rework when a finding exists.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/SupervisorInspectionIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitSupervisorInspectionDecisionRequest" }
      responses:
        "200":
          description: Decision recorded.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SupervisorInspection" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/quality-audits:
    get:
      operationId: listQualityAudits
      x-required-permission: quality_audit.read
      x-building-scoped: true
      summary: List quality audits
      description: >-
        BE-11K PASS / FAIL / REWORK_REQUIRED scoring layer. Requires
        `quality_audit.read`. Pass `buildingId` for scoped mobile lists.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: sourceType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/QualityAuditSourceType" }
        - name: result
          in: query
          required: false
          schema: { $ref: "#/components/schemas/QualityAuditResult" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/QualityAuditStatus" }
      responses:
        "200":
          description: Quality audit list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/QualityAudit" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createQualityAudit
      x-required-permission: quality_audit.manage
      x-building-scoped: true
      summary: Create a quality audit
      description: Requires `quality_audit.manage`. `auditorUserId` is the authenticated user.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateQualityAuditRequest" }
      responses:
        "201":
          description: Audit created (DRAFT).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/QualityAudit" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/quality-audits/{id}:
    get:
      operationId: getQualityAudit
      x-required-permission: quality_audit.read
      x-building-scoped: true
      summary: Get a quality audit
      description: Requires `quality_audit.read` and Building access.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/QualityAuditIdPath"
      responses:
        "200":
          description: Quality audit.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/QualityAudit" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateQualityAudit
      x-required-permission: quality_audit.manage
      x-building-scoped: true
      summary: Update a draft quality audit
      description: Requires `quality_audit.manage`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/QualityAuditIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateQualityAuditRequest" }
      responses:
        "200":
          description: Audit updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/QualityAudit" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/quality-audits/{id}/complete:
    post:
      operationId: completeQualityAudit
      x-required-permission: quality_audit.manage
      x-building-scoped: true
      summary: Complete a quality audit
      description: >-
        Records PASS / FAIL / REWORK_REQUIRED. Requires `quality_audit.manage`.
        Not a reinspection engine.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/QualityAuditIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CompleteQualityAuditRequest" }
      responses:
        "200":
          description: Audit completed.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/QualityAudit" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/findings:
    get:
      operationId: listHousekeepingFindings
      x-required-permission: housekeeping_finding.read
      x-building-scoped: true
      summary: List housekeeping finding bindings
      description: >-
        BE-11H links HK sources to a BE-09 Finding. Lifecycle/rework stay on
        published finding operations using `findingId`. Requires
        `housekeeping_finding.read`. Pass `buildingId` for scoped lists.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: sourceType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/HousekeepingFindingSourceType" }
        - name: status
          in: query
          required: false
          schema: { type: string }
          description: Finding status filter (BE-09 status).
      responses:
        "200":
          description: Housekeeping finding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HousekeepingFinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createHousekeepingFinding
      x-required-permission: housekeeping_finding.manage
      x-building-scoped: true
      summary: Create a housekeeping finding (BE-09 Finding + HK link)
      description: >-
        Requires `housekeeping_finding.manage` and Building access.
        Creates an authoritative BE-09 Finding and the HK source binding.
        Rework uses `requestFindingRework` / `resubmitFinding` on `findingId`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateHousekeepingFindingRequest" }
      responses:
        "201":
          description: Housekeeping finding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HousekeepingFinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/findings/{id}:
    get:
      operationId: getHousekeepingFinding
      x-required-permission: housekeeping_finding.read
      x-building-scoped: true
      summary: Get a housekeeping finding binding
      description: >-
        Path `{id}` is the HK link id, not the BE-09 finding id. The payload
        includes `findingId` and `availableActions` from BE-09. Requires
        `housekeeping_finding.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HousekeepingFindingIdPath"
      responses:
        "200":
          description: Housekeeping finding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HousekeepingFinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/{sourceType}/{sourceId}/evidence-requirements:
    get:
      operationId: listHousekeepingEvidenceRequirements
      x-required-permission: housekeeping_evidence.read
      x-building-scoped: true
      summary: List evidence requirements for a housekeeping source
      description: >-
        BE-11I binding over BE-07 evidence requirements. Requires
        `housekeeping_evidence.read` and Building access on the source.
        `sourceType` is a kebab path segment (daily-cleaning, toilet-inspections,
        public-area-inspections, supervisor-inspections, findings).
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HousekeepingEvidenceSourceTypePath"
        - $ref: "#/components/parameters/HousekeepingEvidenceSourceIdPath"
      responses:
        "200":
          description: Requirement list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HousekeepingEvidenceRequirement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/{sourceType}/{sourceId}/evidence:
    get:
      operationId: listHousekeepingEvidence
      x-required-permission: housekeeping_evidence.read
      x-building-scoped: true
      summary: List evidence submissions for a housekeeping source
      description: Requires `housekeeping_evidence.read` and Building access.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HousekeepingEvidenceSourceTypePath"
        - $ref: "#/components/parameters/HousekeepingEvidenceSourceIdPath"
      responses:
        "200":
          description: Submission list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HousekeepingEvidenceSubmission" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: submitHousekeepingEvidence
      x-required-permission: housekeeping_evidence.manage
      x-building-scoped: true
      summary: Submit evidence metadata for a housekeeping source
      description: >-
        BE-07 evidence metadata (bytes stay on `uploadEvidenceFile` /
        `uploadMobileEvidence`). Requires `housekeeping_evidence.manage`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HousekeepingEvidenceSourceTypePath"
        - $ref: "#/components/parameters/HousekeepingEvidenceSourceIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitHousekeepingEvidenceRequest" }
      responses:
        "201":
          description: Evidence submitted.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HousekeepingEvidenceSubmission" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # --------------------------------------------------------------------
  # CR-BE-MOB-03 PART 01 — Workforce team workload & status contract.
  #
  # Existing read-only routes published for the mobile supervisor:
  #   BE-23G  GET /workforce/reports/kpi          — team workload KPI
  #   BE-03I2 GET /workforce/reporting[/{id}]     — workforce reporting reads
  #   BE-03F  GET /workforce/{id}/supervisor      — current supervisor
  #   BE-03F  GET /workforce/{id}/direct-reports  — direct reports
  # Nothing new is introduced; the KPI and reporting reads enforce BE-02G
  # Building scope, the BE-03F reads are profile-resolved (404 on unknown
  # profile) and are documented without a building-scope claim.
  # --------------------------------------------------------------------

  /workforce/reports/kpi:
    get:
      operationId: getWorkforceKpi
      summary: Workforce team workload KPI for a Building or accessible scope
      description: >-
        BE-23G read-only KPI projection over the BE-03 Workforce records and
        the BE-07 Task assignment store. `buildingId` is access-asserted when
        supplied; omitted, the KPI rolls up across exactly the Buildings the
        caller can reach (BE-02G), so cross-Building / cross-Client leakage is
        impossible. Optional `workforceId` / `teamId` / `workforceType`
        narrow the numbers; `dateFrom` / `dateTo` bound the assignment window
        (UTC day convention); `graceMinutes` is the overdue tolerance.
        Requires `workforce_kpi.read`. Read-only — no ETL, no mutation of
        BE-03/BE-07 state.
      tags: [Workforce]
      security: [{ bearerAuth: [] }]
      x-required-permission: workforce_kpi.read
      x-building-scoped: true
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
          description: Narrow to one Building; access-asserted (403 outside scope).
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
          description: BE-03C workforce profile narrowing.
        - name: teamId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
          description: BE-03B team narrowing.
        - name: workforceType
          in: query
          required: false
          schema:
            type: string
            enum: [INTERNAL, OUTSOURCED, CONTRACT]
          description: BE-03C workforce_type narrowing.
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
          description: ISO date (YYYY-MM-DD) or datetime; day windows are UTC, matching BE-07.
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
          description: ISO date (YYYY-MM-DD) or datetime; must not precede dateFrom and range is capped at 366 days.
        - name: graceMinutes
          in: query
          required: false
          schema: { type: integer, minimum: 0, maximum: 10080 }
          description: Minutes of tolerance before a past-due assignment counts as overdue (default 0).
      responses:
        "200":
          description: The workforce KPI projection.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/WorkforceKpi"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound", description: Unknown `buildingId`. }

  /workforce/reporting:
    get:
      operationId: listWorkforceReporting
      summary: List workforce reporting records in the caller's accessible scope
      description: >-
        BE-03I2 read-only reporting query over the BE-03I1 read model. Every
        returned record is connected to at least one Building in the caller's
        accessible set (BE-02G `requireAccessibleBuilding`), so
        organization/department/team filters can never widen access beyond
        Building isolation. Optional `organizationId` / `departmentId` /
        `teamId` / `positionId` / `buildingId` / `shiftId` /
        `externalOrganizationId` / `workforceType` / `status` /
        `includeInactive` filters narrow the page. Requires
        `workforce.read`.
      tags: [Workforce]
      security: [{ bearerAuth: [] }]
      x-required-permission: workforce.read
      x-building-scoped: true
      parameters:
        - name: organizationId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: departmentId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: teamId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: positionId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: shiftId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: externalOrganizationId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: workforceType
          in: query
          required: false
          schema:
            type: string
            enum: [INTERNAL, OUTSOURCED, CONTRACT, EXTERNAL]
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [ACTIVE, INACTIVE]
          description: Do not combine with includeInactive=true.
        - name: includeInactive
          in: query
          required: false
          schema: { type: boolean }
        - name: page
          in: query
          required: false
          schema: { type: integer, minimum: 1, default: 1 }
        - name: limit
          in: query
          required: false
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
      responses:
        "200":
          description: Paginated workforce reporting records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/WorkforceReportingRecord"
                      meta:
                        type: object
                        required: [page, limit, total]
                        properties:
                          page: { type: integer }
                          limit: { type: integer }
                          total: { type: integer }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /workforce/reporting/{id}:
    get:
      operationId: getWorkforceReporting
      summary: Get one workforce reporting record by Workforce Profile id
      description: >-
        BE-03I2 read of a single Workforce Reporting record by its
        authoritative `workforce_profiles.id`. The record must be connected to
        at least one Building in the caller's accessible set (BE-02G); an
        unknown id or a profile outside the accessible scope is a 404, never a
        cross-Client leak. Requires `workforce.read`.
      tags: [Workforce]
      security: [{ bearerAuth: [] }]
      x-required-permission: workforce.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/WorkforceReportingIdPath"
      responses:
        "200":
          description: The workforce reporting record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/WorkforceReportingRecord"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /workforce/{workforceId}/supervisor:
    get:
      operationId: getWorkforceCurrentSupervisor
      summary: Resolve the current Supervisor of a Workforce Profile
      description: >-
        BE-03F read. Resolves the reporting line genuinely in force for the
        profile at the current instant (ACTIVE line, ACTIVE supervisor,
        effective window). The profile id is resolved through the
        Organization → Client chain, so an unknown profile is a controlled
        404. The reporting line is structurally same-Client (the BE-03F write
        path enforces the cross-Client rule), but this read path carries no
        BE-02G accessible-scope gate today — it is documented as-is, not
        claimed as building-scoped. Requires `workforce.read`.
      tags: [Workforce]
      security: [{ bearerAuth: [] }]
      x-required-permission: workforce.read
      parameters:
        - $ref: "#/components/parameters/WorkforceIdPathParam"
      responses:
        "200":
          description: The current supervisor of the profile.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/CurrentSupervisor"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /workforce/{supervisorId}/direct-reports:
    get:
      operationId: listWorkforceDirectReports
      summary: List the Workforce Profiles reporting to a Supervisor
      description: >-
        BE-03F read. Lists the ACTIVE direct reports of the Supervisor
        (ACTIVE line, ACTIVE subordinate, effective window), ordered by
        employee code. The supervisor id is resolved through the Organization
        → Client chain, so an unknown supervisor is a controlled 404. The
        reporting lines are structurally same-Client (the BE-03F write path
        enforces the cross-Client rule), but this read path carries no BE-02G
        accessible-scope gate today — it is documented as-is, not claimed as
        building-scoped. Requires `workforce.read`.
      tags: [Workforce]
      security: [{ bearerAuth: [] }]
      x-required-permission: workforce.read
      parameters:
        - $ref: "#/components/parameters/SupervisorIdPathParam"
      responses:
        "200":
          description: The direct reports of the supervisor.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/DirectReport"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # --------------------------------------------------------------------
  # CR-BE-MOB-03 PART 02 — Housekeeping supervisor supporting contract.
  #
  # Existing Housekeeping supervisor-supporting routes published for the
  # mobile Cleaning Service supervisor:
  #   BE-11J  consumable requirements + readiness checks
  #   BE-16J  housekeeping consumable bindings (item + warehouse + stock)
  #   BE-11L  housekeeping complaint bindings
  #   BE-11M  housekeeping operational / report datasets
  # Publish-only: every operation below is an existing runtime route.
  # Nothing new is introduced, no housekeeping workflow is redesigned, no
  # stock-usage engine and no reinspection lifecycle is invented (HK-04/06
  # stay NOT_REQUIRED). Building isolation (BE-02G) is asserted per request
  # on every write and single-record read; list endpoints assert the
  # supplied buildingId/clientId (pass buildingId for scoped supervisor
  # lists — the same convention as the CR-BE-MOB-01 Housekeeping tag).
  # --------------------------------------------------------------------

  /housekeeping/consumable-requirements:
    get:
      operationId: listConsumableRequirements
      x-required-permission: consumable_readiness.read
      x-building-scoped: true
      summary: List housekeeping consumable requirements
      description: >-
        BE-11J read. Consumable requirements with their latest readiness
        check and resolved Cleaning Area. Optional `buildingId` /
        `cleaningAreaId` / `status` filters narrow the list; when
        `buildingId` is supplied it is access-asserted (BE-02G) — pass
        `buildingId` for scoped supervisor lists. Requires
        `consumable_readiness.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ConsumableRequirementStatus" }
      responses:
        "200":
          description: Consumable requirement list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/ConsumableRequirement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createConsumableRequirement
      x-required-permission: consumable_readiness.manage
      x-building-scoped: true
      summary: Create a housekeeping consumable requirement
      description: >-
        BE-11J write. Defines the consumable requirement (code / name /
        required quantity / unit) for a Building and optional Cleaning
        Area. `buildingId` is access-asserted (BE-02G). Requires
        `consumable_readiness.manage`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateConsumableRequirementRequest" }
      responses:
        "201":
          description: Consumable requirement created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ConsumableRequirement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /housekeeping/consumable-requirements/{id}:
    get:
      operationId: getConsumableRequirement
      x-required-permission: consumable_readiness.read
      x-building-scoped: true
      summary: Get a housekeeping consumable requirement by id
      description: >-
        BE-11J read. The requirement is loaded then Building access is
        asserted on its `buildingId` (BE-02G); an unknown id or a
        requirement outside the caller's accessible scope is a 404/403.
        Requires `consumable_readiness.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ConsumableRequirementIdPath"
      responses:
        "200":
          description: The consumable requirement.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ConsumableRequirement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateConsumableRequirement
      x-required-permission: consumable_readiness.manage
      x-building-scoped: true
      summary: Update a housekeeping consumable requirement
      description: >-
        BE-11J write. Partial update of `name` / `cleaningAreaId` /
        `requiredQuantity` / `unit` / `status`; Building access asserted on
        the existing requirement (BE-02G). Requires
        `consumable_readiness.manage`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ConsumableRequirementIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateConsumableRequirementRequest" }
      responses:
        "200":
          description: The updated consumable requirement.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ConsumableRequirement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /housekeeping/consumable-requirements/{id}/readiness:
    post:
      operationId: recordConsumableReadiness
      x-required-permission: consumable_readiness.manage
      x-building-scoped: true
      summary: Record a housekeeping consumable readiness check
      description: >-
        BE-11J write. Records the daily operational readiness check
        (READY / LOW / NOT_READY / UNKNOWN + available quantity + notes) for
        a requirement; `checkedByUserId` is the authenticated session, never
        the caller. Building access asserted on the requirement (BE-02G).
        Requires `consumable_readiness.manage`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ConsumableRequirementIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RecordConsumableReadinessRequest" }
      responses:
        "201":
          description: The readiness check recorded.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ConsumableReadiness" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/consumable-readiness:
    get:
      operationId: listConsumableReadiness
      x-required-permission: consumable_readiness.read
      x-building-scoped: true
      summary: List housekeeping consumable readiness checks
      description: >-
        BE-11J read. Readiness checks with resolved requirement and Cleaning
        Area. Optional `buildingId` / `cleaningAreaId` / `readinessStatus` /
        `date` filters narrow the list; when `buildingId` is supplied it is
        access-asserted (BE-02G) — pass `buildingId` for scoped supervisor
        lists. Requires `consumable_readiness.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: readinessStatus
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReadinessStatus" }
        - name: date
          in: query
          required: false
          schema: { type: string }
          description: Operational date (YYYY-MM-DD) to narrow the checks.
      responses:
        "200":
          description: Readiness check list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/ConsumableReadiness" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/consumable-requirements/{requirementId}/bindings:
    get:
      operationId: listConsumableRequirementBindings
      x-required-permission: consumable_readiness.read
      x-building-scoped: true
      summary: List housekeeping consumable bindings of a requirement
      description: >-
        BE-16J read. Consumable bindings of one BE-11J requirement, each
        with the BE-16A item, BE-16B warehouse and the derived readiness
        (READY / LOW / NOT_READY / UNKNOWN from authoritative stock).
        Building access is asserted on the requirement (BE-02G). Requires
        `consumable_readiness.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HkConsumableRequirementIdPath"
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/HkConsumableBindingStatus" }
        - name: readiness
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReadinessStatus" }
      responses:
        "200":
          description: Binding list for the requirement.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HkConsumableBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: createConsumableRequirementBinding
      x-required-permission: consumable_readiness.manage
      x-building-scoped: true
      summary: Bind a consumable item and warehouse to a requirement
      description: >-
        BE-16J write. Binds a BE-16A CONSUMABLE item + BE-16B warehouse to a
        BE-11J requirement. Cross-Client and requirement/warehouse Building
        mismatches are rejected; Building access asserted on the requirement
        (BE-02G). Requires `consumable_readiness.manage`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HkConsumableRequirementIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateHkConsumableBindingRequest" }
      responses:
        "201":
          description: Binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HkConsumableBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /housekeeping/consumable-bindings:
    get:
      operationId: listHousekeepingConsumableBindings
      x-required-permission: consumable_readiness.read
      x-building-scoped: true
      summary: List housekeeping consumable bindings with optional filters
      description: >-
        BE-16J read. Consumable bindings across the accessible scope.
        Optional `clientId` / `buildingId` / `cleaningAreaId` /
        `consumableRequirementId` / `itemId` / `warehouseId` / `status` /
        `readiness` filters narrow the list; `buildingId` and `clientId`
        are access-asserted (BE-02G) — pass `buildingId` for scoped
        supervisor lists. Requires `consumable_readiness.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: clientId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: consumableRequirementId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/HkConsumableBindingStatus" }
        - name: readiness
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReadinessStatus" }
      responses:
        "200":
          description: Binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HkConsumableBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/consumable-bindings/{id}:
    get:
      operationId: getHousekeepingConsumableBinding
      x-required-permission: consumable_readiness.read
      x-building-scoped: true
      summary: Get a housekeeping consumable binding by id
      description: >-
        BE-16J read. The binding is loaded then Building access is asserted
        on its `buildingId` (BE-02G); unknown id or out-of-scope binding is
        a 404/403. Includes the derived readiness and current stock context.
        Requires `consumable_readiness.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HkConsumableBindingIdPath"
      responses:
        "200":
          description: The binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HkConsumableBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateHousekeepingConsumableBinding
      x-required-permission: consumable_readiness.manage
      x-building-scoped: true
      summary: Update a housekeeping consumable binding
      description: >-
        BE-16J write. Partial update of `requiredQuantity` / `status` /
        `notes`; Building access asserted on the existing binding (BE-02G).
        Requires `consumable_readiness.manage`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HkConsumableBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateHkConsumableBindingRequest" }
      responses:
        "200":
          description: The updated binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HkConsumableBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/housekeeping-consumable-bindings:
    get:
      operationId: listBuildingHousekeepingConsumableBindings
      x-required-permission: consumable_readiness.read
      x-building-scoped: true
      summary: List housekeeping consumable bindings in a Building
      description: >-
        BE-16J read, Building-nested. `requireBuildingAccess` is enforced on
        the path (BE-02G). Optional `cleaningAreaId` / `consumableRequirementId`
        / `itemId` / `warehouseId` / `status` / `readiness` filters narrow
        the list. Requires `consumable_readiness.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: consumableRequirementId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/HkConsumableBindingStatus" }
        - name: readiness
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReadinessStatus" }
      responses:
        "200":
          description: Binding list for the Building.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HkConsumableBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /cleaning-areas/{cleaningAreaId}/housekeeping-consumable-bindings:
    get:
      operationId: listCleaningAreaHousekeepingConsumableBindings
      x-required-permission: consumable_readiness.read
      x-building-scoped: true
      summary: List housekeeping consumable bindings of a Cleaning Area
      description: >-
        BE-16J read, Cleaning-Area-nested. Optional `buildingId` /
        `consumableRequirementId` / `itemId` / `warehouseId` / `status` /
        `readiness` filters; `buildingId` is access-asserted when supplied
        (BE-02G) — pass it for scoped supervisor lists. Requires
        `consumable_readiness.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HkBindingCleaningAreaIdPath"
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: consumableRequirementId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/HkConsumableBindingStatus" }
        - name: readiness
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReadinessStatus" }
      responses:
        "200":
          description: Binding list for the Cleaning Area.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HkConsumableBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /clients/{clientId}/housekeeping-consumable-bindings:
    get:
      operationId: listClientHousekeepingConsumableBindings
      x-required-permission: consumable_readiness.read
      x-building-scoped: true
      summary: List housekeeping consumable bindings of a Client
      description: >-
        BE-16J read, Client-nested. Client access is asserted (BE-02F);
        optional `buildingId` / `cleaningAreaId` / `consumableRequirementId`
        / `itemId` / `warehouseId` / `status` / `readiness` filters.
        Requires `consumable_readiness.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: consumableRequirementId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/HkConsumableBindingStatus" }
        - name: readiness
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReadinessStatus" }
      responses:
        "200":
          description: Binding list for the Client.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HkConsumableBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /warehouses/{warehouseId}/housekeeping-consumable-bindings:
    get:
      operationId: listWarehouseHousekeepingConsumableBindings
      x-required-permission: consumable_readiness.read
      x-building-scoped: true
      summary: List housekeeping consumable bindings of a Warehouse
      description: >-
        BE-16J read, Warehouse-nested. Building access is asserted on the
        warehouse's Building (BE-02G); optional `buildingId` /
        `cleaningAreaId` / `consumableRequirementId` / `itemId` / `status` /
        `readiness` filters. Requires `consumable_readiness.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WarehouseIdPath"
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: consumableRequirementId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/HkConsumableBindingStatus" }
        - name: readiness
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReadinessStatus" }
      responses:
        "200":
          description: Binding list for the Warehouse.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HkConsumableBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /inventory-items/{itemId}/housekeeping-consumable-bindings:
    get:
      operationId: listItemHousekeepingConsumableBindings
      x-required-permission: consumable_readiness.read
      x-building-scoped: true
      summary: List housekeeping consumable bindings of an Inventory Item
      description: >-
        BE-16J read, Item-nested. Client access is asserted on the item
        (BE-02F); optional `buildingId` / `cleaningAreaId` /
        `consumableRequirementId` / `warehouseId` / `status` / `readiness`
        filters. Requires `consumable_readiness.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/InventoryItemIdPath"
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: consumableRequirementId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/HkConsumableBindingStatus" }
        - name: readiness
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReadinessStatus" }
      responses:
        "200":
          description: Binding list for the Inventory Item.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HkConsumableBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/complaint-bindings:
    get:
      operationId: listHousekeepingComplaintBindings
      x-required-permission: housekeeping_complaint.read
      x-building-scoped: true
      summary: List housekeeping complaint bindings
      description: >-
        BE-11L read. Complaint / service-request bindings to Housekeeping
        operations. Optional `buildingId` / `cleaningAreaId` / `findingId` /
        `workRequestId` / `complaintReference` / `status` filters; when
        `buildingId` is supplied it is access-asserted (BE-02G) — pass
        `buildingId` for scoped supervisor lists. Requires
        `housekeeping_complaint.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: findingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: workRequestId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: complaintReference
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/HousekeepingComplaintBindingStatus" }
      responses:
        "200":
          description: Complaint binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HousekeepingComplaintBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createHousekeepingComplaintBinding
      x-required-permission: housekeeping_complaint.manage
      x-building-scoped: true
      summary: Create a housekeeping complaint binding
      description: >-
        BE-11L write. Binds a customer complaint / service request to
        Housekeeping operations (source pair must be both-provided or both
        omitted). `buildingId` is access-asserted (BE-02G); `createdByUserId`
        is the authenticated session. Requires `housekeeping_complaint.manage`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateHousekeepingComplaintBindingRequest" }
      responses:
        "201":
          description: Complaint binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HousekeepingComplaintBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /housekeeping/complaint-bindings/{id}:
    get:
      operationId: getHousekeepingComplaintBinding
      x-required-permission: housekeeping_complaint.read
      x-building-scoped: true
      summary: Get a housekeeping complaint binding by id
      description: >-
        BE-11L read. The binding is loaded then Building access is asserted
        on its `buildingId` (BE-02G); unknown id or out-of-scope binding is
        a 404/403. Requires `housekeeping_complaint.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HousekeepingComplaintBindingIdPath"
      responses:
        "200":
          description: The complaint binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HousekeepingComplaintBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateHousekeepingComplaintBinding
      x-required-permission: housekeeping_complaint.manage
      x-building-scoped: true
      summary: Update a housekeeping complaint binding
      description: >-
        BE-11L write. Partial update of `description` / `findingId` /
        `status`; Building access asserted on the existing binding (BE-02G).
        Requires `housekeeping_complaint.manage`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HousekeepingComplaintBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateHousekeepingComplaintBindingRequest" }
      responses:
        "200":
          description: The updated complaint binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HousekeepingComplaintBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/reports/summary:
    get:
      operationId: getHousekeepingReportSummary
      x-required-permission: housekeeping_report.read
      x-building-scoped: true
      summary: Housekeeping summary report for a Building
      description: >-
        BE-11M read. One summary projection (daily cleaning, inspections,
        supervisor inspections, findings, consumables, quality audits,
        complaints) for a Building. `buildingId` is required and
        access-asserted (BE-02G); optional `cleaningAreaId` / `dateFrom` /
        `dateTo` narrow the window. Requires `housekeeping_report.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
          description: YYYY-MM-DD window start.
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
          description: YYYY-MM-DD window end; must not precede dateFrom.
      responses:
        "200":
          description: The summary report.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HousekeepingReportSummary" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/reports/cleaning:
    get:
      operationId: getHousekeepingReportCleaning
      x-required-permission: housekeeping_report.read
      x-building-scoped: true
      summary: Housekeeping cleaning dataset for a Building
      description: >-
        BE-11M read. Daily-cleaning report rows (task, Cleaning Area,
        schedule, assignee, completion) for a Building. `buildingId` is
        required and access-asserted (BE-02G); optional `cleaningAreaId` /
        `dateFrom` / `dateTo` / `status` / `workforceId` / `teamId` narrow
        the dataset. Requires `housekeeping_report.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: teamId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Cleaning dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HousekeepingCleaningReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/reports/inspections:
    get:
      operationId: getHousekeepingReportInspections
      x-required-permission: housekeeping_report.read
      x-building-scoped: true
      summary: Housekeeping inspection dataset for a Building
      description: >-
        BE-11M read. Toilet + public-area inspection report rows for a
        Building. `buildingId` is required and access-asserted (BE-02G);
        optional `cleaningAreaId` / `dateFrom` / `dateTo` / `status` /
        `workforceId` / `teamId` narrow the dataset. Requires
        `housekeeping_report.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: teamId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Inspection dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HousekeepingInspectionReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/reports/supervisor-inspections:
    get:
      operationId: getHousekeepingReportSupervisorInspections
      x-required-permission: housekeeping_report.read
      x-building-scoped: true
      summary: Housekeeping supervisor-inspection dataset for a Building
      description: >-
        BE-11M read. Supervisor inspection report rows (decision, status,
        notes) for a Building. `buildingId` is required and access-asserted
        (BE-02G); optional `cleaningAreaId` / `dateFrom` / `dateTo` /
        `status` / `workforceId` / `teamId` narrow the dataset. Requires
        `housekeeping_report.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: teamId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Supervisor-inspection dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HousekeepingSupervisorInspectionReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/reports/findings:
    get:
      operationId: getHousekeepingReportFindings
      x-required-permission: housekeeping_report.read
      x-building-scoped: true
      summary: Housekeeping finding dataset for a Building
      description: >-
        BE-11M read. Housekeeping finding report rows (BE-09 `findingId`,
        source, Cleaning Area) for a Building. `buildingId` is required and
        access-asserted (BE-02G); optional `cleaningAreaId` / `dateFrom` /
        `dateTo` / `status` / `workforceId` / `teamId` narrow the dataset.
        Requires `housekeeping_report.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: teamId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Finding dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HousekeepingFindingReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/reports/consumables:
    get:
      operationId: getHousekeepingReportConsumables
      x-required-permission: housekeeping_report.read
      x-building-scoped: true
      summary: Housekeeping consumable dataset for a Building
      description: >-
        BE-11M read. Consumable report rows (requirement, readiness,
        available quantity) for a Building. `buildingId` is required and
        access-asserted (BE-02G); optional `cleaningAreaId` / `dateFrom` /
        `dateTo` / `status` / `workforceId` / `teamId` narrow the dataset.
        Requires `housekeeping_report.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: teamId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Consumable dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HousekeepingConsumableReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/reports/quality-audits:
    get:
      operationId: getHousekeepingReportQualityAudits
      x-required-permission: housekeeping_report.read
      x-building-scoped: true
      summary: Housekeeping quality-audit dataset for a Building
      description: >-
        BE-11M read. Quality-audit report rows (score, result, status) for a
        Building. `buildingId` is required and access-asserted (BE-02G);
        optional `cleaningAreaId` / `dateFrom` / `dateTo` / `status` /
        `workforceId` / `teamId` narrow the dataset. Requires
        `housekeeping_report.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: teamId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Quality-audit dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HousekeepingQualityAuditReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/reports/complaints:
    get:
      operationId: getHousekeepingReportComplaints
      x-required-permission: housekeeping_report.read
      x-building-scoped: true
      summary: Housekeeping complaint dataset for a Building
      description: >-
        BE-11M read. Complaint-binding report rows for a Building.
        `buildingId` is required and access-asserted (BE-02G); optional
        `cleaningAreaId` / `dateFrom` / `dateTo` / `status` / `workforceId`
        / `teamId` narrow the dataset. Requires `housekeeping_report.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: teamId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Complaint dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HousekeepingComplaintReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  # --------------------------------------------------------------------
  # CR-BE-MOB-03 PART 03 — Engineering & corrective-action verification
  # contract.
  #
  # Existing engineering-supervisor supporting routes published for the
  # mobile Teknisi supervisor:
  #   BE-10I  GET /engineering/reports/*          — engineering report datasets
  #   BE-21J  /corrective-actions/{id}/verification* — corrective-action
  #           verification (a shared BE-07 `reviews` row with target_type
  #           CORRECTIVE_ACTION; no new verification engine)
  # Publish-only: every operation below is an existing runtime route.
  # Nothing new is introduced, no verification workflow is redesigned, the
  # finding/rework lifecycle is untouched, and /mobile/verification is not
  # extended. Building isolation (BE-02G) is asserted per request on every
  # operation (buildingId required + access-asserted on the report reads;
  # assertBuildingAccess on the corrective action's Building for the
  # verification surface).
  # --------------------------------------------------------------------

  /engineering/reports/technical-summary:
    get:
      operationId: getEngineeringReportTechnicalSummary
      x-required-permission: engineering_report.read
      x-building-scoped: true
      summary: Engineering technical summary for a Building
      description: >-
        BE-10I read. One summary projection (operations, inspections, meter
        readings, equipment logs, checklists, breakdowns, maintenance,
        findings) for a Building. `buildingId` is required and
        access-asserted (BE-02G); optional `assetId` / `dateFrom` / `dateTo`
        / `workforceId` / `vendorId` narrow the window. Requires
        `engineering_report.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: assetId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
          description: ISO date/datetime; day windows are UTC (BE-07 convention), range capped at 366 days.
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
          description: ISO date/datetime; must not precede dateFrom.
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: The engineering technical summary.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/EngineeringTechnicalSummary" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/reports/inspections:
    get:
      operationId: getEngineeringReportInspections
      x-required-permission: engineering_report.read
      x-building-scoped: true
      summary: Engineering inspection dataset for a Building
      description: >-
        BE-10I read. Equipment-inspection binding report rows for a Building.
        `buildingId` is required and access-asserted (BE-02G); optional
        `assetId` / `dateFrom` / `dateTo` / `status` (ACTIVE | INACTIVE) /
        `workforceId` / `vendorId` narrow the dataset. Requires
        `engineering_report.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: assetId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string, enum: [ACTIVE, INACTIVE] }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Inspection dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/EngineeringInspectionReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/reports/meter-readings:
    get:
      operationId: getEngineeringReportMeterReadings
      x-required-permission: engineering_report.read
      x-building-scoped: true
      summary: Engineering meter-reading dataset for a Building
      description: >-
        BE-10I read. Meter-reading binding report rows (BE-10C, with the
        BE-07 UOM and effective min/max range) for a Building. `buildingId`
        is required and access-asserted (BE-02G); optional `assetId` /
        `dateFrom` / `dateTo` / `status` (ACTIVE | INACTIVE) / `uomId` /
        `workforceId` / `vendorId` narrow the dataset. Requires
        `engineering_report.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: assetId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string, enum: [ACTIVE, INACTIVE] }
        - name: uomId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Meter-reading dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/EngineeringMeterReadingReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/reports/equipment-logs:
    get:
      operationId: getEngineeringReportEquipmentLogs
      x-required-permission: engineering_report.read
      x-building-scoped: true
      summary: Engineering equipment-log dataset for a Building
      description: >-
        BE-10I read. Equipment log-sheet binding report rows for a Building.
        `buildingId` is required and access-asserted (BE-02G); optional
        `assetId` / `dateFrom` / `dateTo` / `status` (DRAFT | IN_PROGRESS |
        COMPLETED | CANCELLED) / `workforceId` / `vendorId` narrow the
        dataset. Requires `engineering_report.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: assetId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string, enum: [DRAFT, IN_PROGRESS, COMPLETED, CANCELLED] }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Equipment-log dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/EngineeringEquipmentLogReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/reports/checklists:
    get:
      operationId: getEngineeringReportChecklists
      x-required-permission: engineering_report.read
      x-building-scoped: true
      summary: Engineering checklist dataset for a Building
      description: >-
        BE-10I read. Engineering-checklist binding report rows for a
        Building. `buildingId` is required and access-asserted (BE-02G);
        optional `assetId` / `dateFrom` / `dateTo` / `status` (DRAFT |
        IN_PROGRESS | COMPLETED | CANCELLED) / `workforceId` / `vendorId`
        narrow the dataset. Requires `engineering_report.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: assetId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string, enum: [DRAFT, IN_PROGRESS, COMPLETED, CANCELLED] }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Checklist dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/EngineeringChecklistReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/reports/breakdowns:
    get:
      operationId: getEngineeringReportBreakdowns
      x-required-permission: engineering_report.read
      x-building-scoped: true
      summary: Engineering breakdown dataset for a Building
      description: >-
        BE-10I read. Breakdown report rows (with linked corrective Work
        Order context when present) for a Building. `buildingId` is required
        and access-asserted (BE-02G); optional `assetId` / `dateFrom` /
        `dateTo` / `status` (OPEN | CLOSED) / `workforceId` / `vendorId`
        narrow the dataset. Requires `engineering_report.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: assetId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string, enum: [OPEN, CLOSED] }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Breakdown dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/EngineeringBreakdownReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/reports/maintenance:
    get:
      operationId: getEngineeringReportMaintenance
      x-required-permission: engineering_report.read
      x-building-scoped: true
      summary: Engineering maintenance dataset for a Building
      description: >-
        BE-10I read. Planned-maintenance binding report rows (with linked
        schedule / Work Order context) for a Building. `buildingId` is
        required and access-asserted (BE-02G); optional `assetId` /
        `dateFrom` / `dateTo` / `status` (ACTIVE | INACTIVE) / `workforceId`
        / `vendorId` narrow the dataset. Requires `engineering_report.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: assetId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string, enum: [ACTIVE, INACTIVE] }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Maintenance dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/EngineeringMaintenanceReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/reports/findings:
    get:
      operationId: getEngineeringReportFindings
      x-required-permission: engineering_report.read
      x-building-scoped: true
      summary: Engineering finding dataset for a Building
      description: >-
        BE-10I read. Engineering finding report rows (BE-09 `findingId` via
        the BE-10H link, with classification / severity context) for a
        Building. `buildingId` is required and access-asserted (BE-02G);
        optional `assetId` / `dateFrom` / `dateTo` / `status` (BE-09 finding
        statuses) / `sourceType` / `workforceId` / `vendorId` narrow the
        dataset. Requires `engineering_report.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: assetId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum:
              [OPEN, ASSIGNED, IN_PROGRESS, PENDING_REVIEW, REJECTED,
               REWORK_REQUIRED, RESUBMITTED, VERIFIED, CLOSED, CANCELLED]
        - name: sourceType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/FindingSourceType" }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Finding dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/EngineeringFindingReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /corrective-action-verifications:
    get:
      operationId: listCorrectiveActionVerifications
      x-required-permission: corrective_action_verification.read
      x-building-scoped: true
      summary: List corrective-action verifications
      description: >-
        BE-21J read. Verification records over corrective actions (each a
        shared BE-07 `reviews` row with target_type CORRECTIVE_ACTION).
        Optional `correctiveActionId` / `incidentId` / `buildingId` /
        `status` (PENDING | COMPLETED) / `decision` (APPROVED | REJECTED |
        REWORK_REQUIRED) filters narrow the list; `buildingId` and
        `incidentId` are access-asserted (BE-02G), and the result is always
        constrained to the caller's accessible Buildings. Requires
        `corrective_action_verification.read`.
      tags: [Corrective Action Verification]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: correctiveActionId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: incidentId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/CorrectiveActionVerificationStatus" }
        - name: decision
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReviewDecision" }
      responses:
        "200":
          description: Corrective-action verification list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/CorrectiveActionVerification" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /corrective-actions/{id}/verification:
    get:
      operationId: getCorrectiveActionVerificationContext
      x-required-permission: corrective_action_verification.read
      x-building-scoped: true
      summary: Get the corrective-action verification context
      description: >-
        BE-21J read. The backend-authoritative verification context for one
        Corrective Action: its lifecycle status, whether it is verifiable
        (only COMPLETED is), the blockers when it is not, the open (PENDING)
        verification, the latest COMPLETED verification (the authoritative
        result), and whether it is finalized (an APPROVED decision has been
        recorded). Building access is asserted on the action's Building
        (BE-02G). Requires `corrective_action_verification.read`.
      tags: [Corrective Action Verification]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/CorrectiveActionIdPath"
      responses:
        "200":
          description: The verification context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/CorrectiveActionVerificationContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: openCorrectiveActionVerification
      x-required-permission: corrective_action_verification.manage
      x-building-scoped: true
      summary: Open a corrective-action verification
      description: >-
        BE-21J write. Claims the verification for the authenticated reviewer
        (the reviewer is always the session user, never a caller-supplied
        id). Only a COMPLETED Corrective Action is verifiable; one open
        PENDING verification per action (409). Building access is asserted
        on the action's Building (BE-02G). Requires
        `corrective_action_verification.manage`.
      tags: [Corrective Action Verification]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/CorrectiveActionIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/OpenCorrectiveActionVerificationRequest" }
      responses:
        "201":
          description: The opened verification.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/CorrectiveActionVerification" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /corrective-actions/{id}/verification/decision:
    post:
      operationId: submitCorrectiveActionVerification
      x-required-permission: corrective_action_verification.manage
      x-building-scoped: true
      summary: Submit a corrective-action verification decision
      description: >-
        BE-21J write. Records the reviewer decision (APPROVED | REJECTED |
        REWORK_REQUIRED) against the open verification. The decision is
        validated against the SHARED review vocabulary and its lifecycle
        outcome is derived by the backend (APPROVED → VERIFIED,
        REWORK_REQUIRED → IN_PROGRESS, REJECTED → status unchanged); the
        caller can never assert the resulting status. The reviewer is the
        authenticated session user. Building access is asserted on the
        action's Building (BE-02G). Requires
        `corrective_action_verification.manage`.
      tags: [Corrective Action Verification]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/CorrectiveActionIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitCorrectiveActionVerificationRequest" }
      responses:
        "200":
          description: The completed verification.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/CorrectiveActionVerification" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /corrective-actions/{id}/verification/latest:
    get:
      operationId: getLatestCorrectiveActionVerification
      x-required-permission: corrective_action_verification.read
      x-building-scoped: true
      summary: Get the latest completed corrective-action verification
      description: >-
        BE-21J read. The most recent COMPLETED verification (the
        authoritative result) of a Corrective Action, or null when no
        decision has been recorded yet. Building access is asserted on the
        action's Building (BE-02G). Requires
        `corrective_action_verification.read`.
      tags: [Corrective Action Verification]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/CorrectiveActionIdPath"
      responses:
        "200":
          description: The latest verification (null when none recorded).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: object
                        nullable: true
                        allOf:
                          - $ref: "#/components/schemas/CorrectiveActionVerification"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /corrective-actions/{id}/verification/history:
    get:
      operationId: listCorrectiveActionVerificationHistory
      x-required-permission: corrective_action_verification.read
      x-building-scoped: true
      summary: List corrective-action verification history
      description: >-
        BE-21J read. Full verification history of a Corrective Action,
        oldest first — every attempt is preserved, none replaced. Building
        access is asserted on the action's Building (BE-02G). Requires
        `corrective_action_verification.read`.
      tags: [Corrective Action Verification]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/CorrectiveActionIdPath"
      responses:
        "200":
          description: Verification history (oldest first).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/CorrectiveActionVerification" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # --------------------------------------------------------------------
  # CR-BE-MOB-01 PART 02 — Security operational contract.
  #
  # Existing BE-12 (Security) and BE-21A/B (Incident) routes published for
  # mobile. Nothing new is introduced: patrol execution start/complete
  # delegate to the BE-07 task engine, patrol checklist execution stays on
  # BE-07 checklist endpoints, and finding workflow stays on BE-09.
  # --------------------------------------------------------------------

  /buildings/{buildingId}/security-posts:
    post:
      operationId: createSecurityPost
      summary: Create a Security Post in a Building
      description: >-
        BE-12A. A Security Post is Security operational context anchored to
        the BE-04 building structure (Floor / Area / Room / Space /
        Functional Location references only — never a second hierarchy).
        Requires `security_post.manage` and BE-02G Building access.
        `clientId` is derived through Building → Property → Client and is
        never accepted from the caller.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_post.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateSecurityPostRequest" }
      responses:
        "201":
          description: Security Post created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SecurityPost" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildingSecurityPosts
      summary: List Security Posts in a Building
      description: >-
        BE-12A read. Requires `security_post.read` and BE-02G Building
        access. Mobile relevance: duty-post picker and patrol start-post
        context.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_post.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/SecurityPostStatus" }
        - name: postType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/SecurityPostType" }
        - name: floorId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: areaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: roomId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: spaceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: functionalLocationId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Security Post list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/SecurityPost" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /security/posts/{id}:
    get:
      operationId: getSecurityPost
      summary: Get a Security Post by id
      description: >-
        BE-12A read. Requires `security_post.read`; the service walks
        Security Post → Building and asserts BE-02G access, so an
        inaccessible post is rejected with 403 `BUILDING_ACCESS_DENIED`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_post.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/SecurityPostIdPath"
      responses:
        "200":
          description: Security Post.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SecurityPost" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateSecurityPost
      summary: Update a Security Post
      description: >-
        BE-12A. Requires `security_post.manage` and Building access.
        `code`, `clientId` and `buildingId` are immutable.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_post.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/SecurityPostIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateSecurityPostRequest" }
      responses:
        "200":
          description: Updated Security Post.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SecurityPost" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/security/patrol-routes:
    post:
      operationId: createPatrolRoute
      summary: Create a Patrol Route in a Building
      description: >-
        BE-12B. A Patrol Route is the ordered patrol definition for one
        Building, optionally starting at a BE-12A Security Post in the same
        Building. It carries no schedule, task, or execution semantics.
        Requires `patrol_route.manage` and BE-02G Building access.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_route.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePatrolRouteRequest" }
      responses:
        "201":
          description: Patrol Route created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PatrolRoute" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildingPatrolRoutes
      summary: List Patrol Routes in a Building
      description: >-
        BE-12B read. Requires `patrol_route.read` and BE-02G Building
        access. Mobile relevance: route picker and checkpoint context.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_route.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Patrol Route list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PatrolRoute" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /security/patrol-routes/{id}:
    get:
      operationId: getPatrolRoute
      summary: Get a Patrol Route by id
      description: >-
        BE-12B read. Requires `patrol_route.read`; Building access is
        asserted through Patrol Route → Building.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_route.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolRouteIdPath"
      responses:
        "200":
          description: Patrol Route.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PatrolRoute" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updatePatrolRoute
      summary: Update a Patrol Route
      description: >-
        BE-12B. Requires `patrol_route.manage`. `code`, `clientId` and
        `buildingId` are immutable; a start post must belong to the same
        Building (`PATROL_ROUTE_START_POST_MISMATCH`).
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_route.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolRouteIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdatePatrolRouteRequest" }
      responses:
        "200":
          description: Updated Patrol Route.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PatrolRoute" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /security/patrol-routes/{id}/points:
    post:
      operationId: addPatrolRoutePoint
      summary: Add a checkpoint (patrol route point) to a Patrol Route
      description: >-
        BE-12B. A patrol route point IS the Security checkpoint: an ordered
        (`sequence`) reference to an existing BE-04 location (Floor / Area /
        Room / Space / Functional Location). There is no separate checkpoint
        master and no checkpoint identifier/QR authority — scan-to-confirm
        remains unavailable (see the QR boundary in CR-BE-MOB-01 PART 05).
        Requires `patrol_route.manage`; duplicate sequences are rejected with
        `PATROL_ROUTE_POINT_DUPLICATE_SEQUENCE`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_route.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolRouteIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePatrolRoutePointRequest"
      responses:
        "201":
          description: Checkpoint added.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PatrolRoutePoint" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listPatrolRoutePoints
      summary: List the checkpoints of a Patrol Route
      description: >-
        BE-12B read, ordered by `sequence`. Requires `patrol_route.read`.
        Mobile relevance: the checkpoint list a guard must confirm during a
        patrol execution.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_route.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolRouteIdPath"
      responses:
        "200":
          description: Checkpoint list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/PatrolRoutePoint"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /security/patrol-route-points/{id}:
    patch:
      operationId: updatePatrolRoutePoint
      summary: Update a checkpoint (patrol route point)
      description: >-
        BE-12B. Requires `patrol_route.manage`. The point stays inside its
        route's Building (`PATROL_ROUTE_POINT_LOCATION_MISMATCH`).
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_route.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolRoutePointIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePatrolRoutePointRequest"
      responses:
        "200":
          description: Updated checkpoint.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PatrolRoutePoint" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /security/patrol-routes/{id}/schedule-bindings:
    post:
      operationId: createPatrolScheduleBinding
      summary: Bind a Patrol Route to a BE-07 Schedule Definition
      description: >-
        BE-12C. The binding is the patrol *assignment of work over time*:
        it links a Patrol Route to a shared BE-07 Schedule Definition, which
        generates the tasks that become Patrol Executions. Recurrence,
        occurrence preview and task generation stay in BE-07 — this module
        never re-implements them. Assignment of a generated patrol to a
        workforce profile or team is the published BE-07 task assignment
        surface (`POST /tasks/{taskId}/assignments`), where `taskId` is the
        Patrol Execution id. Requires `patrol_schedule.manage`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_schedule.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolRouteIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePatrolScheduleBindingRequest"
      responses:
        "201":
          description: Patrol Schedule Binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PatrolScheduleBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listPatrolRouteScheduleBindings
      summary: List the Schedule Bindings of a Patrol Route
      description: >-
        BE-12C read. Requires `patrol_schedule.read`. Each row resolves the
        BE-07 schedule and recurrence context without copying it.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_schedule.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolRouteIdPath"
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/PatrolScheduleBindingStatus"
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: patrolRouteId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: scheduleDefinitionId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Patrol Schedule Binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/PatrolScheduleBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /security/patrol-schedule-bindings/{id}:
    get:
      operationId: getPatrolScheduleBinding
      summary: Get a Patrol Schedule Binding by id
      description: >-
        BE-12C read. Requires `patrol_schedule.read`; Building access is
        asserted through the binding's Building.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_schedule.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolScheduleBindingIdPath"
      responses:
        "200":
          description: Patrol Schedule Binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PatrolScheduleBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updatePatrolScheduleBinding
      summary: Update a Patrol Schedule Binding
      description: >-
        BE-12C. Requires `patrol_schedule.manage`. Only the Security-side
        binding fields are mutable; the BE-07 Schedule Definition is edited
        through BE-07.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_schedule.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolScheduleBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePatrolScheduleBindingRequest"
      responses:
        "200":
          description: Updated Patrol Schedule Binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PatrolScheduleBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/security/patrol-executions:
    get:
      operationId: listBuildingPatrolExecutions
      summary: List Patrol Executions for a Building
      description: >-
        BE-12D read. A Patrol Execution is the Security view of an
        authoritative BE-07 generated task bound to a Patrol Route through a
        BE-12C Schedule Binding; `id` equals the BE-07 `taskId` and `status`
        is the shared task status (no second lifecycle). Requires
        `patrol_execution.read` and BE-02G Building access. Mobile
        relevance: the guard's patrol work list.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_execution.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: date
          in: query
          required: false
          schema: { type: string, format: date }
          description: Operational date YYYY-MM-DD.
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/PatrolExecutionStatus" }
        - name: patrolRouteId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Patrol Execution list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/PatrolExecution"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /security/patrol-executions/{id}:
    get:
      operationId: getPatrolExecution
      summary: Get a Patrol Execution by id
      description: >-
        BE-12D read. `id` is the BE-07 task id. `pointProgress` is derived
        from `patrol_point_visits` versus the route's ACTIVE checkpoints and
        is backend-authoritative. Requires `patrol_execution.read`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_execution.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolExecutionIdPath"
      responses:
        "200":
          description: Patrol Execution.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PatrolExecution" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /security/patrol-executions/{id}/start:
    post:
      operationId: startPatrolExecution
      summary: Start a Patrol Execution
      description: >-
        BE-12D. Delegates to the shared BE-07 task-execution engine, which
        owns the transition (`OPEN|ASSIGNED → IN_PROGRESS`) and rejects an
        illegal one. Prefer this over the generic `startTask` on a patrol so
        the Security route/binding checks run. Requires
        `patrol_execution.manage`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_execution.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolExecutionIdPath"
      responses:
        "200":
          description: Started Patrol Execution.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PatrolExecution" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /security/patrol-executions/{id}/points/{pointId}/visit:
    post:
      operationId: recordPatrolPointVisit
      summary: Confirm a checkpoint during a Patrol Execution
      description: >-
        BE-12D. Records a `patrol_point_visits` row (`VISITED`) for the
        checkpoint `pointId` of the execution's route. This is the
        checkpoint-confirmation authority; confirmation is by canonical
        `pointId`, NOT by QR scan (no checkpoint identifier authority
        exists). Requires `patrol_execution.manage`; the execution must be
        IN_PROGRESS and the point must belong to the execution's route.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_execution.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolExecutionIdPath"
        - $ref: "#/components/parameters/PatrolPointIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordPatrolPointVisitRequest"
      responses:
        "201":
          description: Checkpoint visit recorded.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PatrolPointVisit" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /security/patrol-executions/{id}/points:
    get:
      operationId: listPatrolPointVisits
      summary: List checkpoint visits of a Patrol Execution
      description: >-
        BE-12D read of `patrol_point_visits` for the execution, ordered by
        checkpoint `sequence`. Requires `patrol_execution.read`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_execution.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolExecutionIdPath"
      responses:
        "200":
          description: Checkpoint visit list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/PatrolPointVisit"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /security/patrol-executions/{id}/complete:
    post:
      operationId: completePatrolExecution
      summary: Complete a Patrol Execution
      description: >-
        BE-12D. Applies the Security checkpoint-progress rules and then
        delegates the status transition to the shared BE-07 task-execution
        engine. Mobile must call this rather than the generic `completeTask`
        on a patrol, otherwise the patrol-specific checks are bypassed.
        Requires `patrol_execution.manage`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_execution.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolExecutionIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CompletePatrolExecutionRequest"
      responses:
        "200":
          description: Completed Patrol Execution.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PatrolExecution" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /security/patrol-point-visits/{id}:
    patch:
      operationId: updatePatrolPointVisit
      summary: Update the notes of a checkpoint visit
      description: >-
        BE-12D. Only `notes` are mutable — the visit time and actor are
        history. Requires `patrol_execution.manage`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_execution.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolPointVisitIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePatrolPointVisitRequest"
      responses:
        "200":
          description: Updated checkpoint visit.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PatrolPointVisit" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /security/patrol-checklist-bindings:
    post:
      operationId: createPatrolChecklistBinding
      summary: Bind a BE-07 Checklist Template to a Patrol Route
      description: >-
        BE-12E. The checklist linkage for Security: a join row associating a
        Checklist Template with a Patrol Route (+ optional start post) in one
        Building. Checklist execution itself stays BE-07 — responses,
        completion, evidence and verification use the published checklist
        and evidence operations. Requires
        `patrol_checklist_binding.manage`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_checklist_binding.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePatrolChecklistBindingRequest"
      responses:
        "201":
          description: Patrol Checklist Binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PatrolChecklistBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listPatrolChecklistBindings
      summary: List Patrol Checklist Bindings
      description: >-
        BE-12E read, constrained to the caller's accessible Buildings.
        Requires `patrol_checklist_binding.read`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_checklist_binding.read
      x-building-scoped: true
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: patrolRouteId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: checklistTemplateId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/PatrolChecklistBindingStatus"
      responses:
        "200":
          description: Patrol Checklist Binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/PatrolChecklistBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /security/patrol-checklist-bindings/{id}:
    get:
      operationId: getPatrolChecklistBinding
      summary: Get a Patrol Checklist Binding by id
      description: >-
        BE-12E read. Requires `patrol_checklist_binding.read` and Building
        access on the binding's Building.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_checklist_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolChecklistBindingIdPath"
      responses:
        "200":
          description: Patrol Checklist Binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PatrolChecklistBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updatePatrolChecklistBinding
      summary: Update a Patrol Checklist Binding
      description: >-
        BE-12E. Only the Security binding fields are mutable; the Checklist
        Template is edited through BE-07. Requires
        `patrol_checklist_binding.manage`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_checklist_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolChecklistBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePatrolChecklistBindingRequest"
      responses:
        "200":
          description: Updated Patrol Checklist Binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PatrolChecklistBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /security/patrol-checklist-bindings/{id}/start:
    post:
      operationId: startPatrolChecklistExecution
      summary: Start the BE-07 checklist execution for a patrol binding
      description: >-
        BE-12E. Creates the shared BE-07 checklist execution row for the
        binding and returns it. No request body. Everything after this —
        saving responses, completing or cancelling, attaching evidence,
        supervisor verification — uses the published BE-07 checklist,
        evidence and verification operations on the returned execution
        `id`. Requires `patrol_checklist_binding.manage`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_checklist_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolChecklistBindingIdPath"
      responses:
        "201":
          description: Shared BE-07 checklist execution started.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PatrolChecklistExecution"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /security/patrol-checklist-executions/{id}:
    get:
      operationId: getPatrolChecklistExecutionContext
      summary: Resolve the Security context of a patrol checklist execution
      description: >-
        BE-12E read. Projects the authoritative Building / Template / Patrol
        Route / start post for a BE-07 checklist execution that was started
        from a patrol binding. The execution state itself stays with BE-07.
        Requires `patrol_checklist_binding.read`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_checklist_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolChecklistExecutionIdPath"
      responses:
        "200":
          description: Patrol checklist execution context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PatrolChecklistExecutionContext"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/security/daily-activity:
    get:
      operationId: getSecurityDailyActivity
      summary: Security daily activity read model for a Building
      description: >-
        BE-12F. A per-Building, per-operational-date composition of existing
        authoritative records (BE-12A/B/C/D/E + BE-07 + BE-09). It creates no
        state: it is the Security field-reporting *read* surface. Each open
        finding carries BE-09's backend-authoritative `availableActions`.
        Requires `security_daily_activity.read` and BE-02G Building access.
        `date` defaults to the current operational date when omitted.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_daily_activity.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: date
          in: query
          required: false
          schema: { type: string, format: date }
          description: Operational date YYYY-MM-DD.
        - name: shiftId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: securityPostId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Security daily activity.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/SecurityDailyActivity"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /security/findings:
    post:
      operationId: createSecurityFinding
      summary: Report a Security finding (creates the linked BE-09 Finding)
      description: >-
        BE-12H. The Security field-reporting write for an issue observed on
        a Security source (`PATROL_EXECUTION`, `PATROL_CHECKLIST`,
        `SECURITY_DAILY_ACTIVITY`, `SHIFT_HANDOVER`, `SECURITY_POST`). It
        creates the authoritative BE-09 Finding and records the Security
        context link — it does not fork Finding workflow. Classification,
        severity, assignment, state transitions, review, rework, closure and
        `availableActions` all remain on the published BE-09 finding
        operations, keyed by `findingId` (NOT by this link's `id`).
        Requires `security_finding.manage`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_finding.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSecurityFindingRequest"
      responses:
        "201":
          description: Security finding link + authoritative BE-09 Finding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SecurityFinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listSecurityFindings
      summary: List Security findings
      description: >-
        BE-12H read, constrained to the caller's accessible Buildings. Each
        row embeds the authoritative BE-09 Finding and its per-caller
        `availableActions`. Requires `security_finding.read`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_finding.read
      x-building-scoped: true
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: startSecurityPostId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: patrolRouteId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: sourceType
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/SecurityFindingSourceType"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/FindingStatus" }
      responses:
        "200":
          description: Security finding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/SecurityFinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /security/findings/{id}:
    get:
      operationId: getSecurityFinding
      summary: Get a Security finding link by id
      description: >-
        BE-12H read. `{id}` is the Security link id; use the embedded
        `findingId` for every BE-09 workflow operation. Requires
        `security_finding.read`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_finding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/SecurityFindingIdPath"
      responses:
        "200":
          description: Security finding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SecurityFinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /incidents:
    post:
      operationId: createIncident
      summary: Report an Incident (shared BE-21A foundation)
      description: >-
        BE-21A. ONE Incident foundation serves Operational Incident,
        Asset Failure and Finding Escalation, distinguished only by
        `incidentType`. For a standalone Security event, prefer
        `createOperationalIncident` with `operationalCategory: SECURITY`,
        which pins `incidentType` to OPERATIONAL and adds the operational
        handling status. `clientId` is derived from the Building and
        `reportedByUserId` is the authenticated actor — neither is accepted
        from the caller. Requires `incident.manage` and Building access.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: incident.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateIncidentRequest" }
      responses:
        "201":
          description: Incident reported.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Incident" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listIncidents
      summary: List Incidents
      description: >-
        BE-21A read, constrained to the caller's accessible Buildings.
        Requires `incident.read`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: incident.read
      x-building-scoped: true
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: incidentType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/IncidentType" }
        - name: severity
          in: query
          required: false
          schema: { $ref: "#/components/schemas/IncidentSeverity" }
        - name: priority
          in: query
          required: false
          schema: { $ref: "#/components/schemas/IncidentPriority" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/IncidentStatus" }
      responses:
        "200":
          description: Incident list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Incident" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /incidents/{id}:
    get:
      operationId: getIncident
      summary: Get an Incident by id
      description: BE-21A read. Requires `incident.read` and Building access.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: incident.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/IncidentIdPath"
      responses:
        "200":
          description: Incident.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Incident" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateIncident
      summary: Update descriptive Incident metadata
      description: >-
        BE-21A. Only `title`, `description`, `severity` and `priority` are
        mutable; type, context, number and reporter are the identity of the
        record. Requires `incident.manage`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: incident.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/IncidentIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateIncidentRequest" }
      responses:
        "200":
          description: Updated Incident.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Incident" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /incidents/{id}/cancel:
    post:
      operationId: cancelIncident
      summary: Cancel (withdraw) an Incident
      description: >-
        BE-21A. `CANCELLED` is terminal and mutually exclusive with the
        BE-21K `CLOSED` end state: a withdrawn incident was never resolved.
        Requires `incident.manage`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: incident.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/IncidentIdPath"
      responses:
        "200":
          description: Cancelled Incident.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Incident" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /operational-incidents:
    post:
      operationId: createOperationalIncident
      summary: Report an Operational Incident (Security field report)
      description: >-
        BE-21B. The authoritative field-report write for a standalone
        operational event; use `operationalCategory: SECURITY` (or `SAFETY`
        / `ACCESS`) for Security. It is a specialization of the BE-21A
        foundation with `incidentType` pinned to OPERATIONAL — identity,
        Building/Client context, location, severity, priority and reporter
        stay in BE-21A and are never stored twice. There is no separate
        "occurrence" domain: a patrol/checklist issue becomes a Security
        finding (`createSecurityFinding`), a standalone event becomes an
        Operational Incident. Requires `operational_incident.manage`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: operational_incident.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOperationalIncidentRequest"
      responses:
        "201":
          description: Operational Incident reported.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/OperationalIncident"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listOperationalIncidents
      summary: List Operational Incidents
      description: >-
        BE-21B read, constrained to the caller's accessible Buildings.
        Requires `operational_incident.read`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: operational_incident.read
      x-building-scoped: true
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: locationType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/IncidentLocationType" }
        - name: locationId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: operationalCategory
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/OperationalIncidentCategory"
        - name: severity
          in: query
          required: false
          schema: { $ref: "#/components/schemas/IncidentSeverity" }
        - name: priority
          in: query
          required: false
          schema: { $ref: "#/components/schemas/IncidentPriority" }
        - name: operationalStatus
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/OperationalIncidentStatus"
        - name: incidentStatus
          in: query
          required: false
          schema: { $ref: "#/components/schemas/IncidentStatus" }
        - name: occurredFrom
          in: query
          required: false
          schema: { type: string, format: date-time }
        - name: occurredTo
          in: query
          required: false
          schema: { type: string, format: date-time }
      responses:
        "200":
          description: Operational Incident list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/OperationalIncident"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /operational-incidents/{id}:
    get:
      operationId: getOperationalIncident
      summary: Get an Operational Incident by id
      description: >-
        BE-21B read. `id` is the shared BE-21A Incident id;
        `operationalIncidentId` is the internal specialization id.
        `availableActions` is backend-resolved per request from state ×
        permission × foundation lifecycle and must never be recomputed by
        the client. Requires `operational_incident.read`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: operational_incident.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/OperationalIncidentIdPath"
      responses:
        "200":
          description: Operational Incident.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/OperationalIncident"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateOperationalIncident
      summary: Update an Operational Incident (incl. handling status)
      description: >-
        BE-21B. Foundation metadata and operational fields are accepted on
        one request and applied atomically. `operationalStatus` follows the
        closed transition table (OPEN → IN_PROGRESS|RESOLVED, IN_PROGRESS →
        RESOLVED|OPEN, RESOLVED → IN_PROGRESS); an illegal transition is
        rejected. `RESOLVED` means operational handling finished — NOT
        verified or closed. Requires `operational_incident.manage`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: operational_incident.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/OperationalIncidentIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateOperationalIncidentRequest"
      responses:
        "200":
          description: Updated Operational Incident.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/OperationalIncident"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  # --------------------------------------------------------------------
  # CR-BE-MOB-01 PART 03 — Engineering field / reading contract.
  #
  # Existing BE-10 routes published for mobile. Nothing new is introduced:
  # every binding is a join row over an existing authority. Executions are
  # BE-07 checklist executions / form instances, PM & corrective execution
  # is the published BE-08 Work Order action set, evidence is the published
  # BE-07 evidence API on the returned execution id, and finding workflow
  # stays on BE-09 keyed by findingId.
  # --------------------------------------------------------------------

  /assets/{assetId}/inspection-bindings:
    post:
      operationId: createInspectionBinding
      summary: Bind a BE-07 Checklist Template to an Asset (equipment inspection)
      description: >-
        BE-10B. The equipment-inspection binding is a join row between a BE-05
        Asset and a BE-07 Checklist Template, optionally refined by a BE-04
        Functional Location. It is not an inspection engine and duplicates no
        Asset / Checklist / Evidence / Verification data. `clientId` and
        `buildingId` are derived from the Asset. Requires
        `inspection_binding.manage` and BE-02G Building access on the Asset's
        Building.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: inspection_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateInspectionBindingRequest"
      responses:
        "201":
          description: Inspection binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/InspectionBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listAssetInspectionBindings
      summary: List the equipment-inspection bindings of an Asset
      description: >-
        BE-10B read. Requires `inspection_binding.read` and Building access on
        the Asset's Building. Mobile relevance: which inspection checklists
        apply to the scanned/selected equipment.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: inspection_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: Inspection binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/InspectionBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/engineering/inspection-bindings:
    get:
      operationId: listBuildingInspectionBindings
      summary: List equipment-inspection bindings in a Building
      description: >-
        BE-10B read. Requires `inspection_binding.read` and BE-02G Building
        access.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: inspection_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Inspection binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/InspectionBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/inspection-bindings/{id}:
    get:
      operationId: getInspectionBinding
      summary: Get an equipment-inspection binding by id
      description: >-
        BE-10B read. Requires `inspection_binding.read`; Building access is
        asserted through the binding's Building.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: inspection_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/InspectionBindingIdPath"
      responses:
        "200":
          description: Inspection binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/InspectionBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateInspectionBinding
      summary: Update an equipment-inspection binding
      description: >-
        BE-10B. Only the Functional Location refinement and the binding status
        are mutable; the Asset and Checklist Template are the identity of the
        binding. Requires `inspection_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: inspection_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/InspectionBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateInspectionBindingRequest"
      responses:
        "200":
          description: Updated inspection binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/InspectionBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /engineering/inspection-bindings/{id}/start:
    post:
      operationId: startInspectionExecution
      summary: Start the BE-07 checklist execution for an inspection binding
      description: >-
        BE-10B. Creates the shared BE-07 checklist execution for the binding
        and returns it. No request body. Everything after this — saving
        responses, completing or cancelling, attaching evidence and supervisor
        verification — uses the published BE-07 checklist, evidence
        (`executionType: CHECKLIST_EXECUTION`) and verification operations on
        the returned execution `id`. Requires `inspection_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: inspection_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/InspectionBindingIdPath"
      responses:
        "201":
          description: Shared BE-07 checklist execution started.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/InspectionExecution"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /engineering/inspection-executions/{id}:
    get:
      operationId: getInspectionExecutionContext
      summary: Resolve the Engineering context of an inspection execution
      description: >-
        BE-10B read. Projects the authoritative Asset / Building / Functional
        Location for a BE-07 checklist execution started from an inspection
        binding. The execution state itself stays with BE-07. Requires
        `inspection_binding.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: inspection_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/InspectionExecutionIdPath"
      responses:
        "200":
          description: Inspection execution context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/InspectionExecutionContext"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /assets/{assetId}/meter-reading-bindings:
    post:
      operationId: createMeterReadingBinding
      summary: Bind a numeric BE-07 Form Field + UOM to an Asset (meter reading)
      description: >-
        BE-10C. The technical meter-reading definition for a piece of
        equipment: a join row between a BE-05 Asset, a numeric BE-07 Form
        Field, a BE-07 UOM and an optional BE-04 Functional Location, with an
        optional binding-level tightening of the field's measurement range.
        Readings are written into BE-07's own `form_responses` through a shared
        Form Instance — there is no second measurement store. NOTE: this is the
        *engineering/technical* meter authority; BE-18 utility meters are a
        separate billing-oriented authority and are not published here.
        Requires `meter_reading_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: meter_reading_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateMeterReadingBindingRequest"
      responses:
        "201":
          description: Meter reading binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MeterReadingBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listAssetMeterReadingBindings
      summary: List the meter-reading bindings of an Asset
      description: >-
        BE-10C read. Requires `meter_reading_binding.read` and Building access
        on the Asset's Building. Mobile relevance: which readings a technician
        must take on this equipment, with the authoritative UOM and range.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: meter_reading_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: Meter reading binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/MeterReadingBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/engineering/meter-reading-bindings:
    get:
      operationId: listBuildingMeterReadingBindings
      summary: List meter-reading bindings in a Building
      description: >-
        BE-10C read. Requires `meter_reading_binding.read` and BE-02G Building
        access.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: meter_reading_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Meter reading binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/MeterReadingBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/meter-reading-bindings/{id}:
    get:
      operationId: getMeterReadingBinding
      summary: Get a meter-reading binding by id
      description: >-
        BE-10C read. Requires `meter_reading_binding.read`; Building access is
        asserted through the binding's Building.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: meter_reading_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MeterReadingBindingIdPath"
      responses:
        "200":
          description: Meter reading binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MeterReadingBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateMeterReadingBinding
      summary: Update a meter-reading binding
      description: >-
        BE-10C. UOM, location refinement, measurement range and status are
        mutable; the Asset and Form Field are the identity of the binding. The
        range must stay inside the BE-07 field's configured range. Requires
        `meter_reading_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: meter_reading_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MeterReadingBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateMeterReadingBindingRequest"
      responses:
        "200":
          description: Updated meter reading binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MeterReadingBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /engineering/meter-reading-bindings/{id}/start:
    post:
      operationId: startMeterReadingExecution
      summary: Start the BE-07 form instance for a meter-reading binding
      description: >-
        BE-10C. Creates the shared BE-07 form instance that will carry the
        reading. No request body. Requires `meter_reading_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: meter_reading_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MeterReadingBindingIdPath"
      responses:
        "201":
          description: Shared BE-07 form instance started.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MeterReadingExecution"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /engineering/meter-reading-executions/{id}/reading:
    put:
      operationId: submitMeterReading
      summary: Submit the technical reading value for a reading execution
      description: >-
        BE-10C. Idempotent upsert of the reading into BE-07's `form_responses`
        row for the execution's numeric field. The value is validated against
        the effective measurement range (binding range tightened over the BE-07
        field range) and stored with the binding's UOM. This is the only
        reading write — there is no second measurement engine and no
        per-reading review workflow (review a completed reading through the
        published BE-07 review / mobile verification operations on the form
        instance). Requires `meter_reading_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: meter_reading_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MeterReadingExecutionIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubmitMeterReadingRequest"
      responses:
        "200":
          description: Submitted reading.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/MeterReading" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /engineering/meter-reading-executions/{id}:
    get:
      operationId: getMeterReadingContext
      summary: Resolve the context and current value of a reading execution
      description: >-
        BE-10C read. Projects the authoritative Asset / Building / Functional
        Location / UOM, the effective measurement range and the current stored
        value. Derived from the binding and the BE-07 stores. Requires
        `meter_reading_binding.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: meter_reading_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MeterReadingExecutionIdPath"
      responses:
        "200":
          description: Meter reading context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MeterReadingContext"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /assets/{assetId}/log-sheet-bindings:
    post:
      operationId: createLogSheetBinding
      summary: Bind a BE-07 Form Template to an Asset (equipment log sheet)
      description: >-
        BE-10D. The equipment log-sheet definition: a join row between a BE-05
        Asset, a BE-07 Form Template, an optional published Template Version
        snapshot and an optional BE-04 Functional Location. Log-sheet rows are
        BE-07 Form Instances; responses live in `form_responses`. Requires
        `log_sheet_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: log_sheet_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateLogSheetBindingRequest"
      responses:
        "201":
          description: Log sheet binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/LogSheetBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listAssetLogSheetBindings
      summary: List the log-sheet bindings of an Asset
      description: >-
        BE-10D read. Requires `log_sheet_binding.read` and Building access on
        the Asset's Building.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: log_sheet_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: Log sheet binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/LogSheetBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/engineering/log-sheet-bindings:
    get:
      operationId: listBuildingLogSheetBindings
      summary: List log-sheet bindings in a Building
      description: >-
        BE-10D read. Requires `log_sheet_binding.read` and BE-02G Building
        access.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: log_sheet_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Log sheet binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/LogSheetBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/log-sheet-bindings/{id}:
    get:
      operationId: getLogSheetBinding
      summary: Get a log-sheet binding by id
      description: >-
        BE-10D read. Requires `log_sheet_binding.read`; Building access is
        asserted through the binding's Building.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: log_sheet_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/LogSheetBindingIdPath"
      responses:
        "200":
          description: Log sheet binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/LogSheetBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateLogSheetBinding
      summary: Update a log-sheet binding
      description: >-
        BE-10D. Template version pin, location refinement and status are
        mutable; the Asset and Form Template are the identity of the binding.
        Requires `log_sheet_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: log_sheet_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/LogSheetBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateLogSheetBindingRequest"
      responses:
        "200":
          description: Updated log sheet binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/LogSheetBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /engineering/log-sheet-bindings/{id}/start:
    post:
      operationId: startLogSheetExecution
      summary: Start a BE-07 form instance for a log-sheet binding
      description: >-
        BE-10D. Creates one shared BE-07 form instance (a log-sheet row). No
        request body. Responses are written through the published BE-07 form
        operations. Requires `log_sheet_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: log_sheet_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/LogSheetBindingIdPath"
      responses:
        "201":
          description: Shared BE-07 form instance started.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/LogSheetExecution" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /engineering/log-sheet-bindings/{id}/executions:
    get:
      operationId: listLogSheetExecutions
      summary: List the log-sheet rows (form instances) of a binding
      description: >-
        BE-10D read, newest first. Requires `log_sheet_binding.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: log_sheet_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/LogSheetBindingIdPath"
      responses:
        "200":
          description: Log sheet execution list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/LogSheetExecution"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /engineering/log-sheet-executions/{id}:
    get:
      operationId: getLogSheetExecutionContext
      summary: Resolve the Engineering context of a log-sheet row
      description: >-
        BE-10D read. Projects the authoritative Asset / Building / Functional
        Location plus the BE-07 template and version the row is tied to.
        Requires `log_sheet_binding.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: log_sheet_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/LogSheetExecutionIdPath"
      responses:
        "200":
          description: Log sheet execution context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/LogSheetExecutionContext"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /engineering/checklist-bindings:
    post:
      operationId: createEngineeringChecklistBinding
      summary: Bind a BE-07 Checklist Template to an Engineering context
      description: >-
        BE-10E. A join row between a BE-07 Checklist Template and a Building
        plus an optional BE-05 Asset and/or BE-04 Functional Location target
        inside that Building. Executions stay BE-07 checklist executions.
        Requires `engineering_checklist_binding.manage` and Building access.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: engineering_checklist_binding.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateEngineeringChecklistBindingRequest"
      responses:
        "201":
          description: Engineering checklist binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringChecklistBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listEngineeringChecklistBindings
      summary: List Engineering checklist bindings
      description: >-
        BE-10E read, constrained to the caller's accessible Buildings.
        Requires `engineering_checklist_binding.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: engineering_checklist_binding.read
      x-building-scoped: true
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: assetId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Engineering checklist binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/EngineeringChecklistBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/checklist-bindings/{id}:
    get:
      operationId: getEngineeringChecklistBinding
      summary: Get an Engineering checklist binding by id
      description: >-
        BE-10E read. Requires `engineering_checklist_binding.read` and Building
        access on the binding's Building.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: engineering_checklist_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/EngineeringChecklistBindingIdPath"
      responses:
        "200":
          description: Engineering checklist binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringChecklistBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateEngineeringChecklistBinding
      summary: Update an Engineering checklist binding
      description: >-
        BE-10E. Asset / Functional Location targets and status are mutable; the
        Building and Checklist Template are the identity of the binding.
        Requires `engineering_checklist_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: engineering_checklist_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/EngineeringChecklistBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateEngineeringChecklistBindingRequest"
      responses:
        "200":
          description: Updated Engineering checklist binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringChecklistBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /engineering/checklist-bindings/{id}/start:
    post:
      operationId: startEngineeringChecklistExecution
      summary: Start the BE-07 checklist execution for an Engineering binding
      description: >-
        BE-10E. Creates the shared BE-07 checklist execution for the binding
        and returns it. No request body. Responses, completion, evidence and
        verification stay on the published BE-07 operations. Requires
        `engineering_checklist_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: engineering_checklist_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/EngineeringChecklistBindingIdPath"
      responses:
        "201":
          description: Shared BE-07 checklist execution started.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringChecklistExecution"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /engineering/checklist-executions/{id}:
    get:
      operationId: getEngineeringChecklistExecutionContext
      summary: Resolve the Engineering context of a checklist execution
      description: >-
        BE-10E read. Projects the authoritative Building / Template plus the
        bound Asset / Functional Location targets. Requires
        `engineering_checklist_binding.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: engineering_checklist_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/EngineeringChecklistExecutionIdPath"
      responses:
        "200":
          description: Engineering checklist execution context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringChecklistExecutionContext"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /assets/{assetId}/breakdowns:
    post:
      operationId: createBreakdown
      summary: Report an equipment breakdown on an Asset
      description: >-
        BE-10F. Records the breakdown/corrective event against a BE-05 Asset.
        It is the entry point of corrective maintenance; the corrective work
        itself is a BE-08 Work Order linked through
        `linkBreakdownCorrectiveWorkOrder`, and that Work Order's lifecycle,
        assignment and verification remain BE-08's. Requires
        `breakdown.manage` and Building access on the Asset's Building.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: breakdown.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateBreakdownRequest" }
      responses:
        "201":
          description: Breakdown recorded.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/BreakdownBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listAssetBreakdowns
      summary: List the breakdowns of an Asset
      description: >-
        BE-10F read. Each row projects the current BE-08 status of its linked
        corrective Work Order (never stored). Requires `breakdown.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: breakdown.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: Breakdown list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/BreakdownBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/engineering/breakdowns:
    get:
      operationId: listBuildingBreakdowns
      summary: List breakdowns in a Building
      description: >-
        BE-10F read. Requires `breakdown.read` and BE-02G Building access.
        Mobile relevance: the corrective-maintenance backlog for the site.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: breakdown.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Breakdown list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/BreakdownBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/breakdowns/{id}:
    get:
      operationId: getBreakdown
      summary: Get a breakdown by id
      description: >-
        BE-10F read. Requires `breakdown.read`; Building access is asserted
        through the breakdown's Building.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: breakdown.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BreakdownIdPath"
      responses:
        "200":
          description: Breakdown.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/BreakdownBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: closeBreakdown
      summary: Close a breakdown
      description: >-
        BE-10F. `status: CLOSED` is the only accepted transition — the
        breakdown record has no other lifecycle, and closing it does not close
        the corrective Work Order (that stays a BE-08 decision). Requires
        `breakdown.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: breakdown.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BreakdownIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CloseBreakdownRequest" }
      responses:
        "200":
          description: Closed breakdown.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/BreakdownBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /engineering/breakdowns/{id}/work-order:
    post:
      operationId: linkBreakdownCorrectiveWorkOrder
      summary: Link or create the corrective Work Order for a breakdown
      description: >-
        BE-10F. Either links an existing BE-08 Work Order (`workOrderId`) or
        asks the shared BE-08 service to create one (`title`). The corrective
        execution itself — acknowledge / start / hold / resume / note /
        complete, assignment, evidence and verification — is the already
        published BE-08 Work Order surface. This endpoint only records the
        link. Requires `breakdown.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: breakdown.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BreakdownIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LinkCorrectiveWorkOrderRequest"
      responses:
        "201":
          description: Breakdown with its linked corrective Work Order.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/BreakdownBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /assets/{assetId}/maintenance-bindings:
    post:
      operationId: createMaintenanceBinding
      summary: Create a planned-maintenance binding for an Asset
      description: >-
        BE-10G. The PM context for a BE-05 Asset (`maintenanceType`:
        PREVENTIVE / PREDICTIVE / CONDITION_BASED / CALIBRATION). The binding
        does not execute work: it references a shared BE-07 Schedule
        Definition, the BE-07 generated Tasks and an optional BE-08 Work
        Order. Scheduling, task generation, checklist, evidence, assignment,
        finding and verification all remain owned by BE-07 / BE-08 / BE-09.
        Requires `maintenance_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: maintenance_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateMaintenanceBindingRequest"
      responses:
        "201":
          description: Maintenance binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MaintenanceBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listAssetMaintenanceBindings
      summary: List the maintenance bindings of an Asset
      description: >-
        BE-10G read. Each row projects the linked BE-07 schedule, the newest
        BE-07 generated tasks and the BE-08 Work Order status (never stored).
        Requires `maintenance_binding.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: maintenance_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: Maintenance binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/MaintenanceBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/engineering/maintenance-bindings:
    get:
      operationId: listBuildingMaintenanceBindings
      summary: List maintenance bindings in a Building
      description: >-
        BE-10G read. Requires `maintenance_binding.read` and BE-02G Building
        access. Mobile relevance: PM context for the site's equipment.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: maintenance_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Maintenance binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/MaintenanceBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/maintenance-bindings/{id}:
    get:
      operationId: getMaintenanceBinding
      summary: Get a maintenance binding by id
      description: >-
        BE-10G read. Requires `maintenance_binding.read`; Building access is
        asserted through the binding's Building.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: maintenance_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MaintenanceBindingIdPath"
      responses:
        "200":
          description: Maintenance binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MaintenanceBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateMaintenanceBinding
      summary: Update a maintenance binding
      description: >-
        BE-10G. Descriptive fields, maintenance type, location refinement and
        status are mutable; the Asset is the identity of the binding.
        Requires `maintenance_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: maintenance_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MaintenanceBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateMaintenanceBindingRequest"
      responses:
        "200":
          description: Updated maintenance binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MaintenanceBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /engineering/maintenance-bindings/{id}/schedule:
    post:
      operationId: linkMaintenanceSchedule
      summary: Link (or create) the BE-07 Schedule of a maintenance binding
      description: >-
        BE-10G. Either links an existing `scheduleDefinitionId` or asks the
        shared BE-07 scheduler to create one. Recurrence and task generation
        are BE-07's — this endpoint never re-implements them. Requires
        `maintenance_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: maintenance_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MaintenanceBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LinkMaintenanceScheduleRequest"
      responses:
        "200":
          description: Maintenance binding with its linked schedule.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MaintenanceBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /engineering/maintenance-bindings/{id}/task:
    post:
      operationId: linkMaintenanceTask
      summary: Link a BE-07 generated Task to a maintenance binding
      description: >-
        BE-10G. Records that an existing BE-07 generated task belongs to this
        PM binding. Execution of that task is the published BE-07 task surface
        (`startTask` / `completeTask` / `cancelTask`, assignment via
        `assignTask`); the id in this request is the authoritative `taskId`.
        Requires `maintenance_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: maintenance_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MaintenanceBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LinkMaintenanceTaskRequest"
      responses:
        "200":
          description: Maintenance binding with its linked tasks.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MaintenanceBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /engineering/maintenance-bindings/{id}/work-order:
    post:
      operationId: linkMaintenanceWorkOrder
      summary: Link (or create) the BE-08 Work Order of a maintenance binding
      description: >-
        BE-10G. Either links an existing `workOrderId` or asks the shared BE-08
        service to create one. PM field execution is then the published BE-08
        action set on that Work Order — this binding adds no second execution
        path. Requires `maintenance_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: maintenance_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MaintenanceBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LinkMaintenanceWorkOrderRequest"
      responses:
        "200":
          description: Maintenance binding with its linked Work Order.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MaintenanceBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /engineering/findings:
    post:
      operationId: createEngineeringFinding
      summary: Report an Engineering finding (or link an existing BE-09 Finding)
      description: >-
        BE-10H. Records the Engineering context (Asset / Functional Location /
        `operationType` / source execution) for an authoritative BE-09 Finding.
        Supply `findingId` to link an existing Finding, or the create fields to
        have the shared BE-09 service create one. Classification, severity,
        assignment, state transitions, review, rework, closure and
        `availableActions` all remain on the published BE-09 operations, keyed
        by `findingId` (NOT by this link's `id`). Requires
        `engineering_finding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: engineering_finding.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateEngineeringFindingRequest"
      responses:
        "201":
          description: Engineering finding link + authoritative BE-09 Finding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringFinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listEngineeringFindings
      summary: List Engineering findings
      description: >-
        BE-10H read, constrained to the caller's accessible Buildings. Each row
        embeds the authoritative BE-09 Finding and its per-caller
        `availableActions`. Requires `engineering_finding.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: engineering_finding.read
      x-building-scoped: true
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: assetId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: sourceType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/FindingSourceType" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/FindingStatus" }
      responses:
        "200":
          description: Engineering finding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/EngineeringFinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/findings/{id}:
    get:
      operationId: getEngineeringFinding
      summary: Get an Engineering finding link by id
      description: >-
        BE-10H read. `{id}` is the Engineering link id; use the embedded
        `findingId` for every BE-09 workflow operation. Requires
        `engineering_finding.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: engineering_finding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/EngineeringFindingIdPath"
      responses:
        "200":
          description: Engineering finding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringFinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/engineering/daily-operations:
    get:
      operationId: getEngineeringDailyOperations
      summary: Daily Engineering operations read model for a Building
      description: >-
        BE-10A. A consolidated per-Building, per-operational-date (+ optional
        Shift) view of BE-07 generated Tasks, BE-08 Work Orders and BE-09
        Findings. Every row references its authoritative source id and carries
        the source domain's own status, untranslated; `availableActions` is
        present for FINDING rows only, because Tasks and Work Orders expose no
        authoritative available-actions service. Nothing is copied into a new
        master table. `date` is REQUIRED. Requires `engineering.read` and
        BE-02G Building access.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: engineering.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: date
          in: query
          required: true
          schema: { type: string, format: date }
          description: Operational date YYYY-MM-DD (required).
        - name: shiftId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Daily Engineering operations.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringDailyOperations"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/engineering/overview:
    get:
      operationId: getEngineeringOverview
      summary: Engineering supervisor overview for a Building
      description: >-
        BE-10K. A concise supervisor read model composed from the BE-10A daily
        operations, BE-09 finding counts, BE-08 active Work Orders and the
        BE-10J handover dataset. Derived at read time — no analytics engine and
        no duplicated operational data. `date` defaults to today. Requires
        `engineering_overview.read` and BE-02G Building access.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: engineering_overview.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: date
          in: query
          required: false
          schema: { type: string, format: date }
        - name: shiftId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Engineering overview.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringOverview"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/engineering/shift-handovers:
    post:
      operationId: createEngineeringShiftHandover
      summary: Create an Engineering shift handover
      description: >-
        BE-10J. Links an outgoing and an incoming BE-03 Shift for one Building
        and date, with a DRAFT → READY → ACKNOWLEDGED lifecycle. The
        operational `dataset` is resolved live from the authoritative
        Engineering records at read time and is never copied into the handover
        row. Requires `shift_handover.manage` and BE-02G Building access.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: shift_handover.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateEngineeringShiftHandoverRequest"
      responses:
        "201":
          description: Shift handover created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringShiftHandover"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listEngineeringShiftHandovers
      summary: List Engineering shift handovers for a Building
      description: >-
        BE-10J read, optionally filtered by handover date. Requires
        `shift_handover.read` and BE-02G Building access.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: shift_handover.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: date
          in: query
          required: false
          schema: { type: string, format: date }
      responses:
        "200":
          description: Shift handover list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/EngineeringShiftHandover"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/shift-handovers/{id}:
    get:
      operationId: getEngineeringShiftHandover
      summary: Get an Engineering shift handover by id
      description: >-
        BE-10J read, including the live authoritative handover dataset.
        Requires `shift_handover.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: shift_handover.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/EngineeringShiftHandoverIdPath"
      responses:
        "200":
          description: Shift handover.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringShiftHandover"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateEngineeringShiftHandover
      summary: Update the summary of a shift handover
      description: >-
        BE-10J. Only the free-text `summary` is mutable, and only while the
        handover is still DRAFT. Requires `shift_handover.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: shift_handover.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/EngineeringShiftHandoverIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateEngineeringShiftHandoverRequest"
      responses:
        "200":
          description: Updated shift handover.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringShiftHandover"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /engineering/shift-handovers/{id}/ready:
    post:
      operationId: markEngineeringShiftHandoverReady
      summary: Mark a shift handover READY
      description: >-
        BE-10J lifecycle transition DRAFT → READY. Requires
        `shift_handover.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: shift_handover.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/EngineeringShiftHandoverIdPath"
      responses:
        "200":
          description: Shift handover marked READY.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringShiftHandover"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /engineering/shift-handovers/{id}/acknowledge:
    post:
      operationId: acknowledgeEngineeringShiftHandover
      summary: Acknowledge a shift handover
      description: >-
        BE-10J lifecycle transition READY → ACKNOWLEDGED, recording the
        acknowledging user. This is the supervisor-facing acceptance of the
        outgoing shift's open work. Requires `shift_handover.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: shift_handover.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/EngineeringShiftHandoverIdPath"
      responses:
        "200":
          description: Shift handover acknowledged.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringShiftHandover"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  # --------------------------------------------------------------------
  # CR-BE-MOB-02 PART 07B — Security Shift Handover contract publication.
  #
  # Existing BE-12G binding + BE-12M reporting dataset **routes** published
  # for mobile. The Security binding attaches an existing BE-10J
  # shift_handovers row to a Security operational context (a BE-12A start
  # Security Post + optional BE-12B Patrol Route) under the same Building;
  # the BE-10J DRAFT → READY → ACKNOWLEDGED lifecycle stays the only
  # authority on the handover, and the binding's ACTIVE/INACTIVE status is
  # independent of it. No runtime, table, or duplicate mobile endpoint is
  # introduced — these are documentation-only publications of registered
  # handlers.
  # --------------------------------------------------------------------

  /buildings/{buildingId}/security/shift-handovers:
    post:
      operationId: createSecurityShiftHandoverBinding
      summary: Create a Security shift handover binding in a Building
      description: >-
        BE-12G. Links an existing BE-10J `shift_handovers` row to a Security
        operational context — a BE-12A start Security Post and an optional
        BE-12B Patrol Route — under the same Building. The BE-10J lifecycle
        (DRAFT → READY → ACKNOWLEDGED) remains authoritative on the handover;
        the binding's own ACTIVE/INACTIVE status is independent of it and
        never affects the handover. Requires `security_shift_handover.manage`
        and BE-02G Building access. `clientId` is derived through Building →
        Property → Client and is never accepted from the caller. One ACTIVE
        binding per handover (409).
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_shift_handover.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSecurityShiftHandoverBindingRequest"
      responses:
        "201":
          description: Security shift handover binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/SecurityShiftHandoverBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildingSecurityShiftHandoverBindings
      summary: List Security shift handover bindings in a Building
      description: >-
        BE-12G read, scoped to one Building. Optional `shiftHandoverId`,
        `startSecurityPostId`, `patrolRouteId` and `status` filters narrow the
        result. Requires `security_shift_handover.read` and BE-02G Building
        access.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_shift_handover.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: shiftHandoverId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: startSecurityPostId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: patrolRouteId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/SecurityShiftHandoverBindingStatus"
      responses:
        "200":
          description: Security shift handover binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/SecurityShiftHandoverBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /security/shift-handovers/{id}:
    get:
      operationId: getSecurityShiftHandoverBinding
      summary: Get a Security shift handover binding by id
      description: >-
        BE-12G read. `{id}` is the Security binding id (never the BE-10J
        handover id — that stays `shiftHandoverId`). Requires
        `security_shift_handover.read`; the service walks binding → Building
        and asserts BE-02G access, so an inaccessible binding is rejected with
        403 `BUILDING_ACCESS_DENIED`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_shift_handover.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/SecurityShiftHandoverBindingIdPath"
      responses:
        "200":
          description: Security shift handover binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/SecurityShiftHandoverBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateSecurityShiftHandoverBinding
      summary: Update a Security shift handover binding
      description: >-
        BE-12G. Mutable fields are `startSecurityPostId`, `patrolRouteId` and
        `status`; `shiftHandoverId`, `clientId`, `buildingId` and
        `createdByUserId` are immutable. Deactivation is `status: INACTIVE`
        (the row is retained, never deleted). Reactivating to ACTIVE is
        rejected while another ACTIVE binding exists for the same handover.
        Requires `security_shift_handover.manage` and BE-02G Building access.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_shift_handover.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/SecurityShiftHandoverBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSecurityShiftHandoverBindingRequest"
      responses:
        "200":
          description: Updated Security shift handover binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/SecurityShiftHandoverBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /security/reports/shift-handovers:
    get:
      operationId: listSecurityShiftHandoverDataset
      summary: Security shift handover reporting dataset
      description: >-
        BE-12M read-only reporting dataset over the authoritative BE-12G
        `security_shift_handover_bindings` joined to the BE-10J
        `shift_handovers` (with start Security Post / Patrol Route codes).
        `bindingStatus` is the binding's own ACTIVE/INACTIVE state;
        `handoverStatus` is the authoritative BE-10J lifecycle status. Scoped
        to the caller's accessible Buildings; `buildingId` narrows to one
        Building and, when omitted, the dataset rolls up across every
        accessible Building. Requires `security_report.read`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_report.read
      x-building-scoped: true
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
          description: Narrow to one Building (optional; rolls up across accessible Buildings when omitted).
        - name: securityPostId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: patrolRouteId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          description: Filter on the authoritative BE-10J handover status.
          schema:
            type: string
            enum: [DRAFT, READY, ACKNOWLEDGED, CANCELLED]
        - name: dateFrom
          in: query
          required: false
          description: Inclusive lower bound on the handover creation time (ISO-8601 date or datetime, UTC).
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          description: Exclusive upper bound on the handover creation time (ISO-8601 date or datetime, UTC).
          schema: { type: string }
      responses:
        "200":
          description: Security shift handover reporting dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/SecurityShiftHandoverDatasetRow"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # --------------------------------------------------------------------
  # CR-BE-MOB-01 PART 04 — Work Order material context reference data.
  #
  # Existing BE-16A / BE-16B / BE-16H **read** routes published for mobile.
  # The Work Order material *write* chain is already published elsewhere and
  # is NOT repeated here: `recordWorkOrderMaterialUsage`,
  # `listWorkOrderMaterialUsages`, `getWorkOrderMaterialCostSummary`
  # (Work Order Material Usage), the stock ledger (Inventory Stock), the
  # approved-quantity chain (Material Requests, Work Order Procurement
  # Binding) and Work Order verification (Work Orders). This block only adds
  # the item / warehouse / spare-part reference reads a field picker needs.
  # --------------------------------------------------------------------

  /clients/{clientId}/inventory-items:
    get:
      operationId: listClientInventoryItems
      summary: List the Inventory Item master of a Client
      description: >-
        BE-16A read. The authoritative material / spare-part catalogue owned by
        one Client; `itemType` is data (SPARE_PART / MATERIAL / CONSUMABLE) and
        drives no behaviour. `uomId` reuses the BE-07 UOM foundation and is the
        UOM snapshotted onto a Work Order material usage. Mobile relevance: the
        item picker behind `recordWorkOrderMaterialUsage`. Requires
        `inventory_item.read`; results are constrained to the caller's
        accessible Clients. This route carries no stock — availability is the
        published stock-balance surface.
      tags: [Inventory Master]
      security: [{ bearerAuth: [] }]
      x-required-permission: inventory_item.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/InventoryItemStatus" }
        - name: itemType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/InventoryItemType" }
        - name: category
          in: query
          required: false
          schema: { type: string }
        - name: search
          in: query
          required: false
          schema: { type: string }
          description: Free-text match over item code and name.
        - name: uomId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Inventory item list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/InventoryItem" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /inventory-items/{itemId}:
    get:
      operationId: getInventoryItem
      summary: Get an Inventory Item by id
      description: >-
        BE-16A read, including the resolved BE-07 UOM context when present.
        `itemId` is the authoritative id embedded in material usages, material
        requests, stock balances and stock movements. Requires
        `inventory_item.read`.
      tags: [Inventory Master]
      security: [{ bearerAuth: [] }]
      x-required-permission: inventory_item.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/InventoryItemIdPath"
      responses:
        "200":
          description: Inventory item.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/InventoryItem" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/warehouses:
    get:
      operationId: listBuildingWarehouses
      summary: List the Warehouses / stores of a Building
      description: >-
        BE-16B read. A Warehouse is operated within exactly one Building
        (Client → Property → Building → Warehouse) and may bind to a BE-04
        Functional Location. Mobile relevance: the issuing-warehouse picker
        behind `recordWorkOrderMaterialUsage` and the scope of the published
        stock-balance reads. Requires `inventory_warehouse.read` and BE-02G
        Building access.
      tags: [Inventory Master]
      security: [{ bearerAuth: [] }]
      x-required-permission: inventory_warehouse.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/InventoryWarehouseStatus" }
        - name: search
          in: query
          required: false
          schema: { type: string }
          description: Free-text match over warehouse code and name.
        - name: functionalLocationId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Warehouse list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/InventoryWarehouse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /clients/{clientId}/warehouses:
    get:
      operationId: listClientWarehouses
      summary: List the Warehouses / stores of a Client
      description: >-
        BE-16B read across the Client's Buildings, constrained to the caller's
        accessible scope. Requires `inventory_warehouse.read`.
      tags: [Inventory Master]
      security: [{ bearerAuth: [] }]
      x-required-permission: inventory_warehouse.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/InventoryWarehouseStatus" }
        - name: search
          in: query
          required: false
          schema: { type: string }
      responses:
        "200":
          description: Warehouse list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/InventoryWarehouse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /warehouses/{warehouseId}:
    get:
      operationId: getWarehouse
      summary: Get a Warehouse / store by id
      description: >-
        BE-16B read, including the resolved Functional Location when bound.
        `warehouseId` is the authoritative id embedded in material usages and
        stock records. Requires `inventory_warehouse.read` and Building access
        on the warehouse's Building.
      tags: [Inventory Master]
      security: [{ bearerAuth: [] }]
      x-required-permission: inventory_warehouse.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/WarehouseIdPath"
      responses:
        "200":
          description: Warehouse.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/InventoryWarehouse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /assets/{assetId}/spare-parts:
    get:
      operationId: listAssetSpareParts
      summary: List the spare-part Items bound to an Asset
      description: >-
        BE-16H read. The many-to-many binding between a BE-05 Asset and
        SPARE_PART Inventory Items, with the `requiredQuantity` planned per
        asset. Mobile relevance: the expected parts for corrective / planned
        work on this equipment, before issuing them through
        `recordWorkOrderMaterialUsage`. `requiredQuantity` is a planning
        reference only — it is neither an approved nor an issued quantity.
        Requires `asset.read` and Building access on the Asset's Building.
      tags: [Inventory Master]
      security: [{ bearerAuth: [] }]
      x-required-permission: asset.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/AssetSparePartStatus" }
        - name: clientId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Asset spare-part binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/AssetSparePart" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /inventory-items/{itemId}/asset-bindings:
    get:
      operationId: listInventoryItemAssetBindings
      summary: List the Assets an Inventory Item is a spare part for
      description: >-
        BE-16H reverse read of the same binding. Requires
        `inventory_item.read`.
      tags: [Inventory Master]
      security: [{ bearerAuth: [] }]
      x-required-permission: inventory_item.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/InventoryItemIdPath"
        - name: assetId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/AssetSparePartStatus" }
        - name: clientId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Asset spare-part binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/AssetSparePart" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /asset-spare-parts/{id}:
    get:
      operationId: getAssetSparePart
      summary: Get an Asset ↔ spare-part Item binding by id
      description: >-
        BE-16H read, including the resolved Asset and Item context. Requires
        `asset.read`.
      tags: [Inventory Master]
      security: [{ bearerAuth: [] }]
      x-required-permission: asset.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/AssetSparePartIdPath"
      responses:
        "200":
          description: Asset spare-part binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/AssetSparePart" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # CR-BE-UTL-01 PART 13 — existing Utility domain contract.
  /buildings/{buildingId}/utility-meters:
    parameters:
      - $ref: "#/components/parameters/BuildingIdPath"
    get:
      operationId: listBuildingUtilityMeters
      summary: List Utility meters in an accessible Building
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/UtilityMeterListSuccess" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createUtilityMeter
      summary: Create a Utility meter with governed purpose and UOM
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateUtilityMeterRequest" }
      responses:
        "201": { $ref: "#/components/responses/UtilityMeterSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/meters/{id}:
    parameters:
      - $ref: "#/components/parameters/UtilityResourceIdPath"
    get:
      operationId: getUtilityMeter
      summary: Get a Utility meter
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/UtilityMeterSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateUtilityMeter
      summary: Update mutable Utility meter master fields
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateUtilityMeterRequest" }
      responses:
        "200": { $ref: "#/components/responses/UtilityMeterSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /utility/meters/{id}/sub-meters:
    parameters:
      - $ref: "#/components/parameters/UtilityResourceIdPath"
    get:
      operationId: listUtilitySubMeters
      summary: List active/history-filtered sub-meter relationships
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/UtilityHierarchyListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: bindUtilitySubMeter
      summary: Bind an existing meter as a sub-meter
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateUtilityMeterHierarchyRequest" }
      responses:
        "201": { $ref: "#/components/responses/UtilityHierarchySuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/meters/{id}/readings:
    parameters:
      - $ref: "#/components/parameters/UtilityResourceIdPath"
    get:
      operationId: listUtilityMeterReadings
      summary: List append-only readings for a Utility meter
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/UtilityReadingListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: recordUtilityMeterReading
      summary: Record an ACTUAL or ESTIMATED Utility meter reading
      description: >
        Optionally links the BE-10C reading execution through
        meterReadingBindingId/formInstanceId. Readings are append-only; no
        update/delete lifecycle exists. PHOTO OCR candidate extraction is not
        currently an API capability and must not be inferred from evidence.
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RecordUtilityReadingRequest" }
      responses:
        "201": { $ref: "#/components/responses/UtilityReadingSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/meter-readings/{id}:
    get:
      operationId: getUtilityMeterReading
      summary: Get one Utility meter reading and lineage references
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityReadingSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /utility/meter-readings/{id}/evidence:
    parameters:
      - $ref: "#/components/parameters/UtilityResourceIdPath"
    get:
      operationId: listUtilityMeterReadingEvidence
      summary: List PHOTO/DOCUMENT/SIGNATURE evidence for a reading
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/UtilityEvidenceListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: submitUtilityMeterReadingEvidence
      summary: Submit a storage reference through the shared Evidence engine
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitUtilityReadingEvidenceRequest" }
      responses:
        "201": { $ref: "#/components/responses/UtilityEvidenceSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /utility/meter-readings/{id}/evidence-validation:
    get:
      operationId: validateUtilityMeterReadingEvidence
      summary: Resolve shared Evidence readiness for a reading
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityEvidenceReadinessSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /utility/meters/{id}/consumptions:
    parameters:
      - $ref: "#/components/parameters/UtilityResourceIdPath"
    get:
      operationId: listUtilityMeterConsumptions
      summary: List authoritative consumption deltas for a meter
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/UtilityConsumptionListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: calculateUtilityMeterConsumption
      summary: Derive consumption from two authoritative readings
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CalculateUtilityConsumptionRequest" }
      responses:
        "201": { $ref: "#/components/responses/UtilityConsumptionSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/meter-consumptions/{id}:
    get:
      operationId: getUtilityMeterConsumption
      summary: Get one consumption with reading lineage
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityConsumptionSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/utility-tariffs:
    parameters:
      - $ref: "#/components/parameters/BuildingIdPath"
    get:
      operationId: listBuildingUtilityTariffs
      summary: List effective-dated Electricity/Water tariffs
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/UtilityTariffListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createBuildingUtilityTariff
      summary: Configure a Building tariff
      description: >
        The explicit `currency` is required and validated as an ACTIVE Currency
        Master entry allowed for the resolved Building Client
        (CURRENCY_INACTIVE_OR_UNKNOWN / CLIENT_CURRENCY_NOT_ALLOWED). No
        IDR/base/default inference or currency conversion. The tariff currency
        is the immutable snapshot that propagates exactly to calculation and
        bill (tariff → calculation → bill).
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateUtilityTariffRequest" }
      responses:
        "201": { $ref: "#/components/responses/UtilityTariffSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/meters/{id}/reading-dues:
    parameters:
      - $ref: "#/components/parameters/UtilityResourceIdPath"
    get:
      operationId: listMeterUtilityReadingDues
      summary: List expected reading periods for one Utility Meter
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/UtilityReadingDueListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createUtilityReadingDue
      summary: Create an expected reading period, optionally linked to shared scheduler records
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateUtilityReadingDueRequest" }
      responses:
        "201": { $ref: "#/components/responses/UtilityReadingDueSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /buildings/{buildingId}/utility-reading-dues:
    get:
      operationId: listBuildingUtilityReadingDues
      summary: List Reading Due records in one Building
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityReadingDueListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /clients/{clientId}/utility-reading-dues:
    get:
      operationId: listClientUtilityReadingDues
      summary: List Reading Due records restricted to accessible Client Buildings
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityReadingDueListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /utility/reading-dues/{id}:
    get:
      operationId: getUtilityReadingDue
      summary: Get Reading Due status and Meter Reading linkage
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityReadingDueSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /utility/reading-dues/{id}/complete:
    post:
      operationId: completeUtilityReadingDue
      summary: Link a Due/Overdue expectation to an authoritative Meter Reading
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CompleteUtilityReadingDueRequest" }
      responses:
        "200": { $ref: "#/components/responses/UtilityReadingDueSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/reading-dues/{id}/cancel:
    post:
      operationId: cancelUtilityReadingDue
      summary: Cancel a Due/Overdue reading expectation with reason
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CancelUtilityReadingDueRequest" }
      responses:
        "200": { $ref: "#/components/responses/UtilityReadingDueSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/meter-reading-evidence/{evidenceId}/ocr-candidates:
    post:
      operationId: createUtilityOcrCandidate
      summary: Stage one OCR reading candidate from ACTIVE PHOTO evidence
      description: The PHOTO remains authoritative evidence; this never changes a Meter Reading.
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      parameters:
        - name: evidenceId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateUtilityOcrCandidateRequest" }
      responses:
        "201": { $ref: "#/components/responses/UtilityOcrCandidateSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/ocr-candidates/{id}:
    get:
      operationId: getUtilityOcrCandidate
      summary: Get OCR candidate and immutable human decision
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityOcrCandidateSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /utility/ocr-candidates/{id}/accept:
    post:
      operationId: acceptUtilityOcrCandidate
      summary: Human-accept OCR and link/create an authoritative validated reading
      description: >
        With meterReadingId, links an existing equal-value reading. With
        readingAt, calls the existing Meter Reading workflow using the
        candidate value. With neither, verifies against the PHOTO's reading.
        Optional readingDueId completes that expectation through its normal gate.
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      requestBody:
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AcceptUtilityOcrCandidateRequest" }
      responses:
        "200": { $ref: "#/components/responses/UtilityOcrCandidateSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/ocr-candidates/{id}/reject:
    post:
      operationId: rejectUtilityOcrCandidate
      summary: Human-reject OCR with a preserved reason
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RejectUtilityOcrCandidateRequest" }
      responses:
        "200": { $ref: "#/components/responses/UtilityOcrCandidateSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /buildings/{buildingId}/utility-summary:
    get:
      operationId: getBuildingUtilityOperationalSummary
      summary: Read the governed Building Utility operational summary
      description: >
        Read-only exact-period projection over persisted reconciliation,
        Meter/Reading Due, Utility exception, approval/calculation and Utility
        Bill records. It does not recalculate or mutate historical snapshots.
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: management_read_model.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: periodStart
          in: query
          required: true
          schema: { type: string, format: date-time }
        - name: periodEnd
          in: query
          required: true
          schema: { type: string, format: date-time }
      responses:
        "200": { $ref: "#/components/responses/BuildingUtilityOperationalSummarySuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /utility/exceptions:
    get:
      operationId: listUtilityOperationalExceptions
      summary: List Utility exceptions within accessible Buildings
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      parameters:
        - name: buildingId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: utilityType
          in: query
          schema: { $ref: "#/components/schemas/UtilityType" }
        - name: exceptionType
          in: query
          schema: { $ref: "#/components/schemas/UtilityExceptionType" }
        - name: severity
          in: query
          schema: { $ref: "#/components/schemas/UtilityExceptionSeverity" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/UtilityExceptionStatus" }
        - name: reconciliationId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200": { $ref: "#/components/responses/UtilityExceptionListSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createUtilityOperationalException
      summary: Open an operational exception against authoritative Utility references
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateUtilityExceptionRequest" }
      responses:
        "201": { $ref: "#/components/responses/UtilityExceptionSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/exceptions/{id}:
    get:
      operationId: getUtilityOperationalException
      summary: Get one auditable Utility operational exception
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityExceptionSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /utility/exceptions/{id}/start-review:
    post:
      operationId: startUtilityOperationalExceptionReview
      summary: Transition an OPEN Utility exception to UNDER_REVIEW
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      requestBody:
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StartUtilityExceptionReviewRequest" }
      responses:
        "200": { $ref: "#/components/responses/UtilityExceptionSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/exceptions/{id}/resolve:
    post:
      operationId: resolveUtilityOperationalException
      summary: Resolve an UNDER_REVIEW Utility exception with notes
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ResolveUtilityExceptionRequest" }
      responses:
        "200": { $ref: "#/components/responses/UtilityExceptionSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/exceptions/{id}/cancel:
    post:
      operationId: cancelUtilityOperationalException
      summary: Cancel an OPEN or UNDER_REVIEW Utility exception with reason
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CancelUtilityExceptionRequest" }
      responses:
        "200": { $ref: "#/components/responses/UtilityExceptionSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/consumptions/{id}/calculations:
    parameters:
      - $ref: "#/components/parameters/UtilityResourceIdPath"
    get:
      operationId: listUtilityConsumptionCalculations
      summary: List calculation history for one consumption
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/UtilityCalculationListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: calculateUtilityCharge
      summary: Resolve tariff and calculate an immutable Utility charge snapshot
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      requestBody:
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CalculateUtilityChargeRequest" }
      responses:
        "201": { $ref: "#/components/responses/UtilityCalculationSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/calculations/{id}:
    get:
      operationId: getUtilityCalculation
      summary: Get a tariff calculation and immutable snapshot lineage
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityCalculationSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /utility/calculations/{id}/finalize:
    post:
      operationId: finalizeUtilityCalculation
      summary: Freeze a DRAFT Utility calculation
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityCalculationSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /tenant-approvals/pending:
    get:
      operationId: listPendingTenantUtilityApprovals
      summary: List Building-scoped pending Tenant approvals
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: tenant_company.read
      x-building-scoped: true
      x-tenant-pic-self-service: false
      responses:
        "200": { $ref: "#/components/responses/TenantUtilityApprovalListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /tenant-approvals/{id}:
    get:
      operationId: getTenantUtilityApproval
      summary: Get a Tenant approval and immutable Utility snapshot when applicable
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: tenant_company.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/TenantUtilityApprovalSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /tenant-approvals/{id}/available-actions:
    get:
      operationId: getTenantUtilityApprovalAvailableActions
      summary: Resolve APPROVE/REJECT actions for the assigned approver
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: tenant_company.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/TenantApprovalActionsSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /tenant-approvals:
    post:
      operationId: createTenantUtilityApproval
      summary: Create an approval binding, including Utility Calculation targets
      description: >
        For requestType UTILITY_CALCULATION, snapshots the finalized TENANT
        meter charge. This endpoint also serves existing non-Utility Tenant
        request types and is not a Utility-specific approval engine.
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: tenant_company.manage
      x-building-scoped: true
      x-tenant-pic-self-service: false
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateTenantApprovalRequest" }
      responses:
        "201": { $ref: "#/components/responses/TenantUtilityApprovalSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /tenant-approvals/{id}/approve:
    post:
      operationId: approveTenantUtilityApproval
      summary: Approve the assigned Tenant approval
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: tenant_company.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      requestBody:
        content:
          application/json:
            schema: { $ref: "#/components/schemas/TenantApprovalDecisionRequest" }
      responses:
        "200": { $ref: "#/components/responses/TenantUtilityApprovalSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /tenant-approvals/{id}/reject:
    post:
      operationId: rejectTenantUtilityApproval
      summary: Reject the assigned Tenant approval with required reason
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: tenant_company.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/TenantApprovalDecisionRequest" }
      responses:
        "200": { $ref: "#/components/responses/TenantUtilityApprovalSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/calculations/{id}/tenant-approvals:
    get:
      operationId: getUtilityCalculationTenantApprovals
      summary: Get approval context/history for one finalized Tenant calculation
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: tenant_company.read
      x-building-scoped: true
      x-tenant-pic-self-service: false
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityApprovalContextSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /tenant-companies/{tenantCompanyId}/utility-bills:
    post:
      operationId: generateApprovedUtilityBill
      summary: Create a bill when no approval is pending and the latest terminal decision is APPROVED
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_bill.manage
      x-building-scoped: true
      x-tenant-context: historical-calculation-and-approval-snapshot
      x-tenant-pic-self-service: false
      parameters:
        - $ref: "#/components/parameters/TenantCompanyIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/GenerateUtilityBillRequest" }
      responses:
        "201": { $ref: "#/components/responses/UtilityBillSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility-bills:
    get:
      operationId: listUtilityBills
      summary: List Utility Bills within accessible Buildings
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_bill.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/UtilityBillListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /utility-bills/{id}:
    get:
      operationId: getUtilityBill
      summary: Get an immutable-snapshot Utility Bill
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_bill.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityBillSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

    patch:
      operationId: updateUtilityBill
      summary: Update due date or transition DRAFT/ISSUED/CANCELLED lifecycle
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_bill.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateUtilityBillRequest" }
      responses:
        "200": { $ref: "#/components/responses/UtilityBillSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility-bills/{id}/invoice-ready:
    get:
      operationId: getUtilityBillInvoiceReady
      summary: Project a non-invoice, invoice-ready Utility charge line
      description: No invoice numbering, tax, payment, AR, receipt, or accounting side effect.
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_bill.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityInvoiceReadySuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/utility-reconciliations:
    parameters:
      - $ref: "#/components/parameters/BuildingIdPath"
    get:
      operationId: listBuildingUtilityReconciliations
      summary: List immutable Building reconciliation and IKE/IKA snapshots
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/UtilityReconciliationListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createBuildingUtilityReconciliation
      summary: Calculate Building Electricity/Water reconciliation and IKE/IKA
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      x-tenant-billing-side-effects: false
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateUtilityReconciliationRequest" }
      responses:
        "201": { $ref: "#/components/responses/UtilityReconciliationSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/reconciliations/{id}:
    get:
      operationId: getBuildingUtilityReconciliation
      summary: Get a Building reconciliation and IKE/IKA snapshot
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityReconciliationSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /clients/{clientId}/sla-definitions:
    parameters:
      - { name: clientId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }
    post:
      summary: Create an SLA definition
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CreateSlaDefinitionRequest" } } }
      responses:
        "201": { description: Created }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    get:
      summary: List SLA definitions in a client scope
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: buildingId, in: query, schema: { $ref: "#/components/schemas/Uuid" } }
        - { name: status, in: query, schema: { type: string, enum: [ACTIVE, INACTIVE] } }
        - { name: operationalType, in: query, schema: { type: string, enum: [WORK_ORDER] } }
        - { name: priority, in: query, schema: { type: string, enum: [LOW, MEDIUM, HIGH, CRITICAL] } }
      responses:
        "200": { description: Definitions listed }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /work-orders/{id}/sla:
    parameters:
      - { name: id, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }
    get:
      summary: Get the immutable applied SLA and clocks for a Work Order
      description: Returns null when the prospective Work Order has no applicable SLA definition.
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Applied SLA returned, or null when none applies }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /work-orders/{id}/sla/escalations:
    parameters:
      - { name: id, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }
    get:
      summary: List the SLA escalation history of a Work Order
      description: >
        Read-only history of the escalation levels materialized from this Work Order's SLA
        breaches, ordered by clock type then level. Returns an empty array when the Work Order
        never breached, matched no escalation policy, or has no SLA. Recipient identities are
        never exposed — only the resolved recipient count and the created in-app notification
        count. There is no manual trigger, retry, or cancel endpoint: execution belongs solely
        to the due-job dispatcher.
      security: [{ bearerAuth: [] }]
      x-required-permission: work_order.read
      x-building-scoped: true
      responses:
        "200":
          description: Escalation history returned (possibly empty)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/SlaEscalationAction" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /sla-definitions/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }
    get:
      summary: Get an SLA definition
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Definition returned }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update or enable/disable an SLA definition
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/UpdateSlaDefinitionRequest" } } }
      responses:
        "200": { description: Definition updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /clients/{clientId}/sla-escalation-policies:
    parameters:
      - { name: clientId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }
    post:
      summary: Create an SLA escalation policy
      description: Configuration only — creating a policy schedules and sends nothing.
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CreateSlaEscalationPolicyRequest" } } }
      responses:
        "201": { description: Created }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      summary: List SLA escalation policies in a client scope
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: buildingId, in: query, schema: { $ref: "#/components/schemas/Uuid" } }
        - { name: status, in: query, schema: { type: string, enum: [ACTIVE, INACTIVE] } }
        - { name: clockType, in: query, schema: { type: string, enum: [RESPONSE, RESOLUTION, ANY] } }
        - { name: priority, in: query, schema: { type: string, enum: [LOW, MEDIUM, HIGH, CRITICAL] } }
      responses:
        "200": { description: Policies listed }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /sla-escalation-policies/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }
    get:
      summary: Get an SLA escalation policy
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Policy returned }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update or enable/disable an SLA escalation policy
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/UpdateSlaEscalationPolicyRequest" } } }
      responses:
        "200": { description: Policy updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /sla-escalation-policies/{id}/levels:
    parameters:
      - { name: id, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }
    post:
      summary: Add an ordered escalation level to a policy
      description: >
        Stores the level configuration only. The recipient rule is validated for shape and
        vocabulary; it is never resolved, and no notification is created by this endpoint.
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CreateSlaEscalationLevelRequest" } } }
      responses:
        "201": { description: Created }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      summary: List the escalation levels of a policy in level order
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Levels listed }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /sla-escalation-levels/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }
    get:
      summary: Get an SLA escalation level
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Level returned }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update or enable/disable an SLA escalation level
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/UpdateSlaEscalationLevelRequest" } } }
      responses:
        "200": { description: Level updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  # ---------------------------------------------------------------------
  # CR-BE-INTEG-01 PART 06 — Integration Webhooks (endpoint configuration
  # + delivery history). See the "Integration Webhooks" tag for the
  # outbound signature headers and the receiver verification recipe.
  # ---------------------------------------------------------------------
  /integration/webhook-endpoints:
    post:
      operationId: createIntegrationWebhookEndpoint
      summary: Create a webhook endpoint (secret disclosed once)
      description: >
        Creates a Client-scoped webhook endpoint (optional Building
        narrowing). The server generates the `whsec_` signing secret and
        returns it in THIS response ONLY — it can never be read back; use
        rotate-secret to replace a lost secret. `url` must be HTTPS without
        embedded credentials and must not target localhost/loopback/
        private/link-local/metadata addresses (SSRF guard; redirects are
        never followed at delivery time). `eventTypes` are uppercase
        BE-07 event-type codes; `INTEGRATION_*` and `NOTIFICATION_OUTBOUND_*`
        families are rejected (recursion guard). Activation is prospective:
        only events recorded AFTER the endpoint exists can ever be
        delivered to it — there is no historical replay or backfill.
        Requires `integration_webhook.manage` plus BE-02G access to the
        Client.
      tags: [Integration Webhooks]
      security:
        - bearerAuth: []
      x-required-permission: integration_webhook.manage
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [clientId, name, url, eventTypes]
              properties:
                clientId:
                  $ref: "#/components/schemas/Uuid"
                buildingId:
                  allOf:
                    - $ref: "#/components/schemas/Uuid"
                  nullable: true
                  description: >
                    Optional Building narrowing (must belong to the Client).
                    Null = all events of the Client.
                name:
                  type: string
                  maxLength: 200
                url:
                  type: string
                  maxLength: 2000
                  description: HTTPS receiver URL (SSRF-validated).
                eventTypes:
                  type: array
                  minItems: 1
                  maxItems: 100
                  items:
                    type: string
                    maxLength: 128
                  description: Subscribed BE-07 event-type codes (uppercased).
                status:
                  type: string
                  enum: [ACTIVE, INACTIVE]
                  default: ACTIVE
                timeoutMs:
                  type: integer
                  minimum: 1000
                  maximum: 30000
                  default: 10000
                  description: Per-delivery receiver timeout.
      responses:
        "201":
          description: >
            The created endpoint INCLUDING the one-time `signingSecret`
            disclosure (never returned again by any endpoint).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/IntegrationWebhookEndpointWithSecret"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    get:
      operationId: listIntegrationWebhookEndpoints
      summary: List webhook endpoints (secret-free)
      description: >
        Lists webhook endpoints of the caller's accessible Clients (BE-02G
        intersection — an explicit `clientId` outside the caller's scope is
        rejected with 403). Responses NEVER carry signing secrets. Requires
        `integration_webhook.read`.
      tags: [Integration Webhooks]
      security:
        - bearerAuth: []
      x-required-permission: integration_webhook.read
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [ACTIVE, INACTIVE]
        - name: eventType
          in: query
          required: false
          schema:
            type: string
            maxLength: 128
          description: Only endpoints subscribed to this event type.
      responses:
        "200":
          description: The accessible webhook endpoints (secret-free).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/IntegrationWebhookEndpoint"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /integration/webhook-endpoints/{integrationWebhookEndpointId}:
    get:
      operationId: getIntegrationWebhookEndpoint
      summary: Get a webhook endpoint (secret-free)
      description: >
        Returns one webhook endpoint by id — WITHOUT the signing secret.
        Requires `integration_webhook.read` plus BE-02G access to the
        endpoint's Client.
      tags: [Integration Webhooks]
      security:
        - bearerAuth: []
      x-required-permission: integration_webhook.read
      parameters:
        - name: integrationWebhookEndpointId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The webhook endpoint (secret-free).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/IntegrationWebhookEndpoint"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateIntegrationWebhookEndpoint
      summary: Update webhook endpoint configuration
      description: >
        Updates `name`, `url`, `eventTypes`, `timeoutMs` and/or `status`
        (ACTIVE/INACTIVE — deactivation is the retirement path; there is NO
        delete operation, so delivery history always keeps a valid endpoint
        reference). `clientId`/`buildingId` are immutable; secret material
        in the body is rejected (use rotate-secret). Config edits apply to
        FUTURE behavior only — already-snapshotted payloads and existing
        delivery records are never rewritten. Requires
        `integration_webhook.manage` plus BE-02G access.
      tags: [Integration Webhooks]
      security:
        - bearerAuth: []
      x-required-permission: integration_webhook.manage
      parameters:
        - name: integrationWebhookEndpointId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              minProperties: 1
              properties:
                name:
                  type: string
                  maxLength: 200
                url:
                  type: string
                  maxLength: 2000
                eventTypes:
                  type: array
                  minItems: 1
                  maxItems: 100
                  items:
                    type: string
                    maxLength: 128
                status:
                  type: string
                  enum: [ACTIVE, INACTIVE]
                timeoutMs:
                  type: integer
                  minimum: 1000
                  maximum: 30000
      responses:
        "200":
          description: The updated endpoint (secret-free).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/IntegrationWebhookEndpoint"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /integration/webhook-endpoints/{integrationWebhookEndpointId}/rotate-secret:
    post:
      operationId: rotateIntegrationWebhookEndpointSecret
      summary: Rotate the signing secret (new secret disclosed once)
      description: >
        Replaces the endpoint's signing secret with a newly generated
        `whsec_` value and returns it in THIS response ONLY. Single-active-
        secret model: the previous secret stops signing immediately (no
        overlap window) — update the receiver first, then rotate. Rotation
        is audited (`INTEGRATION_ENDPOINT_SECRET_ROTATED`) without secret
        material. Requires `integration_webhook.manage` plus BE-02G access.
      tags: [Integration Webhooks]
      security:
        - bearerAuth: []
      x-required-permission: integration_webhook.manage
      parameters:
        - name: integrationWebhookEndpointId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: >
            The endpoint INCLUDING the one-time `signingSecret` disclosure
            of the NEW secret.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/IntegrationWebhookEndpointWithSecret"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /integration/webhook-deliveries:
    get:
      operationId: listIntegrationWebhookDeliveries
      summary: List webhook delivery history
      description: >
        Read-only per-endpoint delivery history over the caller's accessible
        Clients (BE-02G intersection — an explicit `clientId` outside the
        caller's scope is rejected with 403), newest first. Each row exposes
        delivery state, attempt accounting, timing, and the last HTTP
        response status plus a sanitized error — NEVER signing secrets and
        NEVER payload bodies. Optional shared pagination (`page`,
        `pageSize`); optional filters below. There is no manual retry and no
        manual event-injection operation. Requires
        `integration_webhook.read`.
      tags: [Integration Webhooks]
      security:
        - bearerAuth: []
      x-required-permission: integration_webhook.read
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: endpointId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [PENDING, SENDING, DELIVERED, RETRY_SCHEDULED, FAILED_PERMANENT, EXHAUSTED]
        - name: eventType
          in: query
          required: false
          schema:
            type: string
            maxLength: 128
        - name: from
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Lower bound on the delivery's created timestamp.
        - name: to
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Upper bound on the delivery's created timestamp.
        - name: page
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
        - name: pageSize
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
      responses:
        "200":
          description: The accessible webhook deliveries (newest first).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/IntegrationWebhookDelivery"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /integration/webhook-deliveries/{integrationWebhookDeliveryId}:
    get:
      operationId: getIntegrationWebhookDelivery
      summary: Get one webhook delivery
      description: >
        Returns one delivery ledger row by id (state / attempt / timing /
        response metadata only — never secrets, never payload bodies).
        Requires `integration_webhook.read` plus BE-02G access to the
        delivery's Client.
      tags: [Integration Webhooks]
      security:
        - bearerAuth: []
      x-required-permission: integration_webhook.read
      parameters:
        - name: integrationWebhookDeliveryId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The webhook delivery.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/IntegrationWebhookDelivery"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  # ---------------------------------------------------------------------------
  # CR-BE-PRO-02 — RFQ → Vendor response → comparison → award → PO conversion
  # ---------------------------------------------------------------------------
  /rfqs:
    post:
      operationId: createRfq
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Create a DRAFT RFQ
      description: >
        Internal RFQ foundation. The typed Material/Service demand lineage and
        Client/Building scope are derived from the existing Purchase Request.
        RFQ creation has no quotation, award, PO, budget, or commitment effect.
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateRfqRequest" }
      responses:
        "201":
          description: DRAFT RFQ created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties: { data: { $ref: "#/components/schemas/Rfq" } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listRfqs
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: List RFQs in accessible Buildings
      description: RFQs are Client/Building isolated and never expose inaccessible rows.
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: RFQ list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { type: array, items: { $ref: "#/components/schemas/Rfq" } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /rfqs/{rfqId}:
    parameters:
      - name: rfqId
        in: path
        required: true
        schema: { $ref: "#/components/schemas/Uuid" }
    get:
      operationId: getRfq
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: Get an RFQ
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: RFQ.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties: { data: { $ref: "#/components/schemas/Rfq" } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateRfq
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Update a DRAFT RFQ
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateRfqRequest" }
      responses:
        "200":
          description: Updated RFQ.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties: { data: { $ref: "#/components/schemas/Rfq" } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /rfqs/{rfqId}/available-actions:
    get:
      operationId: getRfqAvailableActions
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: Resolve RFQ lifecycle actions
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Caller-specific RFQ actions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties: { data: { $ref: "#/components/schemas/RfqAvailableActions" } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /rfqs/{rfqId}/open:
    post:
      operationId: openRfq
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Open an RFQ
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: RFQ opened., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/Rfq" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /rfqs/{rfqId}/close:
    post:
      operationId: closeRfq
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Close an RFQ before evaluation/recommendation
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: RFQ closed., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/Rfq" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /rfqs/{rfqId}/cancel:
    post:
      operationId: cancelRfq
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Cancel an RFQ
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: RFQ cancelled., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/Rfq" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /rfqs/{rfqId}/lines:
    post:
      operationId: addRfqLine
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Add a typed demand line to a DRAFT RFQ
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json: { schema: { $ref: "#/components/schemas/CreateRfqLineRequest" } }
      responses:
        "201":
          description: RFQ line added.
          content:
            application/json:
              schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqLine" } } }] }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listRfqLines
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: List RFQ demand lines
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: RFQ lines.
          content:
            application/json:
              schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/RfqLine" } } } }] }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /rfq-lines/{lineId}:
    get:
      operationId: getRfqLine
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: Get an RFQ demand line
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: lineId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200": { description: RFQ line., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqLine" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /rfqs/{rfqId}/invitations:
    post:
      operationId: createRfqVendorInvitation
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Invite a Vendor to an RFQ
      description: >
        Internal invitation management. The returned one-time invitation token
        is disclosed only at issuance. It creates no quotation, PO, budget, or
        commitment.
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CreateRfqInvitationRequest" } } }
      responses:
        "201": { description: Invitation created., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqInvitationIssued" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listRfqVendorInvitations
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: List RFQ Vendor Invitations
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Invitations., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/RfqInvitation" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /rfq-vendor-invitations:
    get:
      operationId: listAccessibleRfqVendorInvitations
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: List accessible RFQ Vendor Invitations
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Invitations., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/RfqInvitation" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /rfq-vendor-invitations/{invitationId}:
    get:
      operationId: getRfqVendorInvitation
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: Get an RFQ Vendor Invitation
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: invitationId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200": { description: Invitation., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqInvitation" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /rfq-vendor-invitations/{invitationId}/resend:
    post:
      operationId: resendRfqVendorInvitation
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Resend an RFQ Vendor Invitation
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: invitationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/IdempotencyRequest" } } } }
      responses: { "200": { description: Invitation resent., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqInvitationIssued" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "409": { $ref: "#/components/responses/Conflict" } }
  /rfq-vendor-invitations/{invitationId}/revoke:
    post:
      operationId: revokeRfqVendorInvitation
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Revoke an RFQ Vendor Invitation
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: invitationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Invitation revoked., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqInvitation" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "409": { $ref: "#/components/responses/Conflict" } }

  /vendor-rfq-access/exchange:
    post:
      operationId: exchangeRfqVendorInvitationToken
      summary: Exchange a one-time RFQ invitation token
      description: >
        Public issuance boundary. The returned bearer is a dedicated,
        invitation-scoped Vendor RFQ session, not an internal User session and
        not an RBAC grant. Raw invitation/session tokens are never persisted.
      tags: [RFQ Procurement]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/RfqTokenExchangeRequest" } } } }
      responses: { "200": { description: External RFQ session issued., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqSessionExchange" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "409": { $ref: "#/components/responses/Conflict" } }
  /vendor-rfq-access/me:
    get:
      operationId: getVendorRfqMe
      summary: Get the current invitation-scoped Vendor RFQ session
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      responses: { "200": { description: Scoped Vendor session., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqSession" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "404": { $ref: "#/components/responses/NotFound" } }
  /vendor-rfq-access/rfqs/{rfqId}:
    get:
      operationId: getVendorSafeRfq
      summary: Get only the RFQ content permitted to this Vendor invitation
      description: No peer quotation, comparison, recommendation, approval, award, PO, or commitment data is exposed.
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: rfqId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Vendor-safe RFQ., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/VendorSafeRfq" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "404": { $ref: "#/components/responses/NotFound" } }
  /vendor-rfq-access/invitations/{invitationId}:
    get:
      operationId: getVendorSafeInvitation
      summary: Get the current Vendor's own RFQ invitation
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: invitationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Own invitation., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqInvitation" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "404": { $ref: "#/components/responses/NotFound" } }
  /vendor-rfq-access/invitations/{invitationId}/accept:
    post:
      operationId: acceptVendorRfqInvitation
      summary: Accept own RFQ invitation
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: invitationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Invitation accepted., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqInvitation" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "404": { $ref: "#/components/responses/NotFound" }, "409": { $ref: "#/components/responses/Conflict" } }
  /vendor-rfq-access/invitations/{invitationId}/decline:
    post:
      operationId: declineVendorRfqInvitation
      summary: Decline own RFQ invitation
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: invitationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: false, content: { application/json: { schema: { $ref: "#/components/schemas/RfqResponseReasonRequest" } } } }
      responses: { "200": { description: Invitation declined., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqInvitation" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "404": { $ref: "#/components/responses/NotFound" }, "409": { $ref: "#/components/responses/Conflict" } }
  /vendor-rfq-access/invitations/{invitationId}/no-bid:
    post:
      operationId: noBidVendorRfqInvitation
      summary: Record a Vendor no-bid response
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: invitationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: false, content: { application/json: { schema: { $ref: "#/components/schemas/RfqResponseReasonRequest" } } } }
      responses: { "200": { description: No-bid recorded., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqInvitation" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "404": { $ref: "#/components/responses/NotFound" }, "409": { $ref: "#/components/responses/Conflict" } }

  /vendor-rfq-access/invitations/{invitationId}/quotations:
    post:
      operationId: createVendorQuotation
      summary: Create the current Vendor's quotation draft
      description: Vendor session only; the quotation belongs to the invitation and is not visible to peer Vendors.
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: invitationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateVendorQuotationRequest" } } } }
      responses: { "201": { description: Quotation draft., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/VendorQuotation" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "404": { $ref: "#/components/responses/NotFound" }, "409": { $ref: "#/components/responses/Conflict" } }
  /vendor-rfq-access/quotations/current:
    get:
      operationId: getVendorCurrentQuotation
      summary: Get the current Vendor's own quotation
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      responses: { "200": { description: Own quotation., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/VendorQuotation" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" } }
  /vendor-rfq-access/quotations/{quotationId}:
    get:
      operationId: getVendorQuotation
      summary: Get the current Vendor's own quotation
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: quotationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Own quotation., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/VendorQuotation" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "404": { $ref: "#/components/responses/NotFound" } }
  /vendor-rfq-access/quotations/{quotationId}/revisions:
    get:
      operationId: listVendorQuotationRevisions
      summary: List the current Vendor's quotation revisions
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: quotationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Own revisions., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/VendorQuotationRevision" } } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "404": { $ref: "#/components/responses/NotFound" } }
    post:
      operationId: createVendorQuotationRevision
      summary: Create a new Vendor quotation revision
      description: Submitted revisions are immutable; new commercial terms require a new sequential revision.
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: quotationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateVendorQuotationRevisionRequest" } } } }
      responses: { "201": { description: Draft revision., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/VendorQuotationRevision" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "409": { $ref: "#/components/responses/Conflict" } }
  /vendor-rfq-access/quotation-revisions/{revisionId}:
    patch:
      operationId: updateVendorQuotationRevision
      summary: Update the current Vendor's DRAFT revision
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: revisionId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateVendorQuotationRevisionRequest" } } } }
      responses: { "200": { description: Draft revision., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/VendorQuotationRevision" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "409": { $ref: "#/components/responses/Conflict" } }
  /vendor-rfq-access/quotation-revisions/{revisionId}/lines:
    post:
      operationId: addVendorQuotationLine
      summary: Add an RFQ-mapped line to a Vendor DRAFT revision
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: revisionId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateVendorQuotationLineRequest" } } } }
      responses: { "201": { description: Quotation line., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/VendorQuotationLine" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "409": { $ref: "#/components/responses/Conflict" } }
  /vendor-rfq-access/quotation-lines/{lineId}:
    patch:
      operationId: updateVendorQuotationLine
      summary: Update a Vendor quotation DRAFT line
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: lineId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateVendorQuotationLineRequest" } } } }
      responses: { "200": { description: Quotation line., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/VendorQuotationLine" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "409": { $ref: "#/components/responses/Conflict" } }
  /vendor-rfq-access/quotation-revisions/{revisionId}/submit:
    post:
      operationId: submitVendorQuotationRevision
      summary: Submit and freeze a Vendor quotation revision
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: revisionId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Submitted immutable revision., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/VendorQuotationRevision" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "409": { $ref: "#/components/responses/Conflict" } }
  /vendor-rfq-access/quotation-revisions/{revisionId}/attachments:
    post:
      operationId: addVendorQuotationAttachment
      summary: Link an attachment to a Vendor DRAFT quotation revision
      description: Uses shared Documents, Versions and Supporting Documents; fileReference remains opaque and no binary content is stored in PostgreSQL.
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: revisionId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateQuotationAttachmentRequest" } } } }
      responses: { "201": { description: Attachment linked., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/QuotationAttachment" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "409": { $ref: "#/components/responses/Conflict" } }
    get:
      operationId: listVendorQuotationAttachments
      summary: List attachments on a Vendor quotation revision
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: revisionId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Attachment references., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/QuotationAttachment" } } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "404": { $ref: "#/components/responses/NotFound" } }
  /vendor-rfq-access/quotation-attachments/{attachmentId}:
    get:
      operationId: getVendorQuotationAttachment
      summary: Get the current Vendor's own attachment reference
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: attachmentId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Attachment reference., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/QuotationAttachment" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "404": { $ref: "#/components/responses/NotFound" } }

  /rfqs/{rfqId}/quotations:
    get:
      operationId: listRfqQuotations
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: List internal quotations invited to an RFQ
      description: Internal RFQ read only. All Vendor quotations are hidden from Vendor external sessions.
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: rfqId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Internal quotations., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/VendorQuotation" } } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" } }
  /vendor-quotations/{quotationId}:
    get:
      operationId: getInternalQuotation
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: Get an internal Vendor quotation
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: quotationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Quotation., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/VendorQuotation" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }
  /vendor-quotations/{quotationId}/revisions:
    get:
      operationId: listInternalQuotationRevisions
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: List immutable quotation revisions internally
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: quotationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Revisions., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/VendorQuotationRevision" } } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }
  /quotation-revisions/{revisionId}/attachments:
    get:
      operationId: listInternalQuotationAttachments
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: List internal quotation attachment references
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: revisionId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Attachment references., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/QuotationAttachment" } } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }

  /rfqs/{rfqId}/comparisons:
    post:
      operationId: createRfqComparison
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Create an immutable RFQ comparison snapshot
      description: >
        Internal only. The snapshot uses exact SUBMITTED quotation revisions,
        never drafts or mutable draft state. It creates no recommendation,
        award, PO, budget reservation, or commitment.
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: rfqId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateComparisonRequest" } } } }
      responses: { "201": { description: Comparison snapshot., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqComparison" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "409": { $ref: "#/components/responses/Conflict" } }
    get:
      operationId: listRfqComparisons
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: List immutable RFQ comparison snapshots
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: rfqId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Comparisons., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/RfqComparison" } } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" } }
  /rfq-comparisons/{comparisonId}:
    get:
      operationId: getRfqComparison
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: Get an immutable RFQ comparison
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: comparisonId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Comparison., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqComparison" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }
  /rfq-comparisons/{comparisonId}/evaluations:
    get:
      operationId: listRfqComparisonEvaluations
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: List human comparison evaluations
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: comparisonId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Evaluations., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/RfqComparisonEvaluation" } } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" } }
    post:
      operationId: createRfqComparisonEvaluation
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Record a human comparison evaluation
      description: Observations only; no weighted score or automatic winner is calculated.
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: comparisonId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateComparisonEvaluationRequest" } } } }
      responses: { "201": { description: Evaluation., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqComparisonEvaluation" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "409": { $ref: "#/components/responses/Conflict" } }
  /rfq-comparison-evaluations/{evaluationId}:
    get:
      operationId: getRfqComparisonEvaluation
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: Get a comparison evaluation
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: evaluationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Evaluation., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqComparisonEvaluation" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }
    patch:
      operationId: updateRfqComparisonEvaluation
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Update human evaluation observations
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: evaluationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateComparisonEvaluationRequest" } } } }
      responses: { "200": { description: Evaluation updated., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqComparisonEvaluation" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }

  /rfqs/{rfqId}/recommendations:
    post:
      operationId: createRfqRecommendation
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Create a human RFQ recommendation
      description: >
        Requires a CLOSED RFQ and a current immutable comparison. This is an
        accountable human proposal, not automatic scoring or winner selection.
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: rfqId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateRfqRecommendationRequest" } } } }
      responses: { "201": { description: Recommendation pending approval., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqRecommendation" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "409": { $ref: "#/components/responses/Conflict" } }
    get:
      operationId: getRfqRecommendationForRfq
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: Get the RFQ recommendation
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: rfqId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Recommendation or null., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { allOf: [{ $ref: "#/components/schemas/RfqRecommendation" }], nullable: true } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" } }
  /rfq-recommendations/{recommendationId}:
    get:
      operationId: getRfqRecommendation
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: Get an RFQ recommendation
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: recommendationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Recommendation., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqRecommendation" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }
  /rfq-recommendations/{recommendationId}/award:
    post:
      operationId: awardRfqRecommendation
      x-required-permission: rfq.award
      x-building-scoped: true
      summary: Finalize an approved RFQ award
      description: >
        Requires an APPROVED `RFQ_AWARD` procurement approval. `rfq.award` is a
        sensitive permission and is not granted by default. Award creates no
        PO or commitment; PO conversion is a separate explicit command.
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: recommendationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: false, content: { application/json: { schema: { $ref: "#/components/schemas/CreateRfqAwardRequest" } } } }
      responses: { "201": { description: Immutable award., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqAward" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "409": { $ref: "#/components/responses/Conflict" } }
  /rfq-awards/{awardId}:
    get:
      operationId: getRfqAward
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: Get an immutable RFQ award
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: awardId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Award., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqAward" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }
  /rfq-awards/{awardId}/convert-to-po:
    post:
      operationId: createRfqPoConversion
      x-required-permission: rfq.award
      x-building-scoped: true
      summary: Convert a finalized RFQ award into an existing DRAFT Purchase Order
      description: >
        Requires an exact finalized Vendor award and a matching READY existing
        PO Readiness. Vendor, currency, and PO Lines are derived from the
        immutable awarded quotation revision. Creates no commitment; PO
        issuance remains explicit and the ISSUED PO Line remains the only
        automatic priced commitment source.
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: awardId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateRfqPoConversionRequest" } } } }
      responses: { "201": { description: DRAFT PO conversion and provenance., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqPoConversion" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "409": { $ref: "#/components/responses/Conflict" } }
  /rfq-po-conversions/{conversionId}:
    get:
      operationId: getRfqPoConversion
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: Get RFQ-to-PO conversion provenance
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: conversionId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Conversion provenance., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqPoConversion" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }
  /purchase-orders/{purchaseOrderId}/rfq-provenance:
    get:
      operationId: getRfqPoProvenance
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: Get RFQ provenance for a Purchase Order
      description: >
        Returns typed RFQ → Award → Vendor → Quotation → Revision → PO → PO
        Line references. Vendor external sessions cannot access this projection.
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: purchaseOrderId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Provenance., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqPoConversion" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }

  # ------------------------------------------------------------------
  # CR-BE-FX-01 PART 02 — FX Rate Authority + Client FX Policy
  #
  # CANONICAL CONVENTION (frozen): 1 BASE = RATE x QUOTE.
  # This surface performs NO conversion of any kind.
  # ------------------------------------------------------------------
  /fx-rates:
    get:
      operationId: listFxRates
      summary: List FX rates
      description: >
        Platform-global rate authority. Rates contain no Client data, so no
        Client scoping applies; `fx_rate.read` is the whole authorization
        boundary. Filters are optional and an unrecognized value is ignored
        rather than guessed.
      tags: [FX]
      security: [{ bearerAuth: [] }]
      x-required-permission: fx_rate.read
      parameters:
        - name: baseCurrencyCode
          in: query
          schema: { type: string, pattern: "^[A-Z]{3}$" }
        - name: quoteCurrencyCode
          in: query
          schema: { type: string, pattern: "^[A-Z]{3}$" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/FxRateStatus" }
        - name: source
          in: query
          schema: { $ref: "#/components/schemas/FxRateSource" }
        - name: effectiveAt
          in: query
          description: Only rates whose closed-open window covers this instant.
          schema: { type: string, format: date-time }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 500, default: 100 }
        - name: offset
          in: query
          schema: { type: integer, minimum: 0, default: 0 }
      responses:
        "200": { $ref: "#/components/responses/FxRateListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createFxRate
      summary: Propose an FX rate (PENDING_APPROVAL)
      description: >
        Creates a rate in `PENDING_APPROVAL`. It is NOT usable by any consumer
        until a different authorized user approves it. Both currency legs must
        be ACTIVE in the Currency Master. `rate` is the number of QUOTE units
        per ONE unit of BASE (1 BASE = RATE x QUOTE) and must be a positive
        plain decimal — exponent notation is rejected. The effective window is
        closed-open `[effectiveFrom, effectiveTo)`; a future `effectiveFrom` is
        accepted as scheduling. Only `MANUAL_TREASURY` exists in FX-01: there is
        no provider ingestion.
      tags: [FX]
      security: [{ bearerAuth: [] }]
      x-required-permission: fx_rate.manage
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateFxRateRequest" }
      responses:
        "201": { $ref: "#/components/responses/FxRateSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /fx-rates/{rateId}:
    parameters:
      - $ref: "#/components/parameters/FxRateIdPath"
    get:
      operationId: getFxRate
      summary: Read one FX rate
      tags: [FX]
      security: [{ bearerAuth: [] }]
      x-required-permission: fx_rate.read
      responses:
        "200": { $ref: "#/components/responses/FxRateSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /fx-rates/{rateId}/events:
    parameters:
      - $ref: "#/components/parameters/FxRateIdPath"
    get:
      operationId: listFxRateEvents
      summary: Read the append-only audit trail of one FX rate
      description: >
        The five governed lifecycle events. Rows are append-only: the database
        rejects both UPDATE and DELETE, so this trail cannot be rewritten.
      tags: [FX]
      security: [{ bearerAuth: [] }]
      x-required-permission: fx_rate.read
      responses:
        "200": { $ref: "#/components/responses/FxRateEventListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /fx-rates/{rateId}/approve:
    parameters:
      - $ref: "#/components/parameters/FxRateIdPath"
    post:
      operationId: approveFxRate
      summary: Activate a proposed FX rate (checker)
      description: >
        `PENDING_APPROVAL` -> `ACTIVE`. MAKER-CHECKER: the approver must not be
        the maker, otherwise 409 `FX_RATE_SELF_APPROVAL`.

        ACTIVE WINDOW: at most one ACTIVE rate may cover any instant for a
        (base, quote, rateType). If activation would overlap an existing ACTIVE
        rate the request fails closed with 409 `FX_RATE_ACTIVE_WINDOW_CONFLICT`.
        The incumbent window is never shortened, the incumbent is never silently
        superseded, and no latest-rate fallback is applied.
      tags: [FX]
      security: [{ bearerAuth: [] }]
      x-required-permission: fx_rate.approve
      responses:
        "200": { $ref: "#/components/responses/FxRateSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /fx-rates/{rateId}/reject:
    parameters:
      - $ref: "#/components/parameters/FxRateIdPath"
    post:
      operationId: rejectFxRate
      summary: Reject a proposed FX rate (terminal)
      description: >
        `PENDING_APPROVAL` -> `REJECTED`. `REJECTED` is terminal: a rejected
        rate can never be approved or revived. The actor is recorded and an
        `FX_RATE_REJECTED` audit row is written in the same transaction.
      tags: [FX]
      security: [{ bearerAuth: [] }]
      x-required-permission: fx_rate.approve
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FxRateReasonRequest" }
      responses:
        "200": { $ref: "#/components/responses/FxRateSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /fx-rates/{rateId}/supersede:
    parameters:
      - $ref: "#/components/parameters/FxRateIdPath"
    post:
      operationId: supersedeFxRate
      summary: Correct an ACTIVE FX rate by supersession
      description: >
        The ONLY sanctioned correction path. A stored rate value is never
        modified. One transaction creates an ACTIVE successor and moves the
        incumbent to `SUPERSEDED`, linking both directions exactly
        (`successor.supersedesRateId` = incumbent id,
        `incumbent.supersededByRateId` = successor id). If the successor cannot
        be inserted the whole transaction rolls back and the incumbent stays
        ACTIVE.

        The currency pair is inherited and cannot be changed. Omitting `rate`
        performs a window-only correction that reuses the incumbent's exact
        stored decimal. The successor is authored by the incumbent's maker and
        approved by the superseding actor, so the existing maker-checker
        invariant requires the two to differ (409 `FX_RATE_SELF_APPROVAL`).
      tags: [FX]
      security: [{ bearerAuth: [] }]
      x-required-permission: fx_rate.approve
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SupersedeFxRateRequest" }
      responses:
        "200": { $ref: "#/components/responses/FxRateSupersessionSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /fx-rates/{rateId}/deactivate:
    parameters:
      - $ref: "#/components/parameters/FxRateIdPath"
    post:
      operationId: deactivateFxRate
      summary: Withdraw an ACTIVE FX rate (terminal)
      description: >
        `ACTIVE` -> `INACTIVE`, with no replacement. `INACTIVE` is terminal.
        The actor is recorded and an `FX_RATE_DEACTIVATED` audit row is written
        in the same transaction.
      tags: [FX]
      security: [{ bearerAuth: [] }]
      x-required-permission: fx_rate.approve
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FxRateReasonRequest" }
      responses:
        "200": { $ref: "#/components/responses/FxRateSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /clients/{clientId}/fx-policy:
    parameters:
      - $ref: "#/components/parameters/FxClientIdPath"
    get:
      operationId: getClientFxPolicy
      summary: Read a Client FX policy
      description: >
        FAIL-CLOSED READ MODEL. `policy` is `null` when the Client has no
        policy, and `fxAvailable` is then `false` — absence is never treated as
        permission. `fxAvailable` is also `false` when the policy is disabled or
        permits no rate source. `baseCurrencyCode` is returned for display only
        and is never injected into a write. Requires Client reachability; an
        unreachable Client returns 403.
      tags: [FX]
      security: [{ bearerAuth: [] }]
      x-required-permission: client_fx_policy.read
      responses:
        "200": { $ref: "#/components/responses/ClientFxPolicyReadSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      operationId: setClientFxPolicy
      summary: Set or replace a Client FX policy
      description: >
        FAIL-CLOSED WRITE. `fxEnabled` and `inversePermitted` default to `false`
        when omitted; an empty `permittedSources` permits nothing.

        NO SILENT INJECTION: `reportingCurrencyCode` must be stated explicitly
        and must equal the Client's `base_currency_code`
        (400 `FX_POLICY_REPORTING_CURRENCY_NOT_BASE` otherwise). It is never
        copied from the base or the default transaction currency — FX-01 does
        not add a second reporting axis. There is no Building override.

        Writes exactly one Client-scoped audit event
        (`CLIENT_FX_POLICY_SET` on first write, `CLIENT_FX_POLICY_CHANGED`
        afterwards) through the existing operational-events architecture.
      tags: [FX]
      security: [{ bearerAuth: [] }]
      x-required-permission: client_fx_policy.manage
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SetClientFxPolicyRequest" }
      responses:
        "200": { $ref: "#/components/responses/ClientFxPolicySuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }


components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: opaque session token
      description: >
        Opaque internal User session token returned by `POST /auth/login` and
        carried as `Authorization: Bearer <token>`.
    vendorRfqSession:
      type: http
      scheme: bearer
      bearerFormat: invitation-scoped RFQ session token
      description: >
        Dedicated external Vendor RFQ session issued by
        `POST /vendor-rfq-access/exchange`. This is not an internal User
        session, does not grant RBAC permissions, and is limited to one Vendor,
        invitation and RFQ.

  headers:
    RequestId:
      description: >-
        Server-generated UUID identifying this HTTP request. The value is
        authoritative for this response and is never taken from a caller's
        `X-Request-ID` header.
      required: true
      schema:
        type: string
        format: uuid

  parameters:
    # CR-BE-MOB-CONTRACT-01 PART 02 — identity & Building context identifiers.
    UserIdPath:
      name: userId
      in: path
      description: User id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    RoleIdPath:
      name: roleId
      in: path
      description: Role id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    PermissionIdPath:
      name: permissionId
      in: path
      description: Permission id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    PropertyIdPath:
      name: propertyId
      in: path
      description: Property id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    ClientIdQuery:
      name: clientId
      in: query
      description: Optional Client filter.
      required: false
      schema: { $ref: "#/components/schemas/Uuid" }
    PropertyIdQuery:
      name: propertyId
      in: query
      description: Optional Property filter.
      required: false
      schema: { $ref: "#/components/schemas/Uuid" }
    # CR-BE-R2P-01 — Request-to-Pay chain identifiers.
    PurchaseOrderIdPath:
      name: id
      in: path
      description: Authoritative `purchase_orders.id`.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    PurchaseOrderLineIdPath:
      name: lineId
      in: path
      description: Authoritative `purchase_order_lines.id`.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    PriceCatalogEntryIdPath:
      name: id
      in: path
      description: Authoritative `price_catalog_entries.id`.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    ServiceCatalogEntryIdPath:
      name: id
      in: path
      description: Authoritative `service_catalog.id`.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    IdempotencyKeyHeader:
      name: Idempotency-Key
      in: header
      description: >
        Required idempotency key (≤ 200 chars). A replayed key with an
        identical payload returns the original result; a replayed key with a
        different payload fails 409 `PRICE_CATALOG_IDEMPOTENCY_CONFLICT`.
      required: true
      schema:
        type: string
        maxLength: 200
    WorkContractIdPath:
      name: id
      in: path
      description: Authoritative `work_contracts.id` (SPK).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    WorkOrderProcurementBindingIdPath:
      name: id
      in: path
      description: Authoritative `work_order_procurement_bindings.id`.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    BastDocumentIdPath:
      name: id
      in: path
      description: Authoritative canonical `bast_documents.id`.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    LegacyVendorBastIdPath:
      name: bastId
      in: path
      description: Legacy `vendor_bast_bindings.id` compatibility identifier.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    ClientIdPath:
      name: clientId
      in: path
      description: Client id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    ClientConfigurationIdPath:
      name: clientConfigurationId
      in: path
      description: Client configuration record id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    BuildingIdPath:
      name: buildingId
      in: path
      description: Building id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    UtilityResourceIdPath:
      name: id
      in: path
      description: Authoritative Utility resource id for the path.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    TenantCompanyIdPath:
      name: tenantCompanyId
      in: path
      description: Authoritative Tenant Company id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    FloorIdPath:
      name: floorId
      in: path
      description: Floor id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    AreaIdPath:
      name: areaId
      in: path
      description: Area id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    RoomIdPath:
      name: roomId
      in: path
      description: Room id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    SpaceIdPath:
      name: spaceId
      in: path
      description: Space id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    FunctionalLocationIdPath:
      name: functionalLocationId
      in: path
      description: Functional Location id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    AssetIdPath:
      name: assetId
      in: path
      description: Asset id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    EntitlementIdPath:
      name: entitlementId
      in: path
      description: Module entitlement id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    SubscriptionIdPath:
      name: subscriptionId
      in: path
      description: Subscription id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    BuildingConfigurationIdPath:
      name: buildingConfigurationId
      in: path
      description: Building configuration record id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    ModuleConfigurationIdPath:
      name: moduleConfigurationId
      in: path
      description: Module configuration record id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    FeatureEntitlementConfigurationIdPath:
      name: featureEntitlementConfigurationId
      in: path
      description: Feature entitlement configuration record id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    NavigationItemIdPath:
      name: navigationItemId
      in: path
      description: Navigation item id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    WorkspaceIdPath:
      name: workspaceId
      in: path
      description: Workspace id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    DashboardIdPath:
      name: dashboardId
      in: path
      description: Dashboard configuration id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    DashboardWidgetIdPath:
      name: dashboardWidgetId
      in: path
      description: Dashboard Widget id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    FormTemplateIdPath:
      name: formTemplateId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    FormSectionIdPath:
      name: formSectionId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    FormFieldIdPath:
      name: formFieldId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    ChecklistTemplateIdPath:
      name: checklistTemplateId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    ChecklistItemIdPath:
      name: checklistItemId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    UomIdPath:
      name: uomId
      in: path
      description: Unit of measure id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    EvidenceRequirementIdPath:
      name: evidenceRequirementId
      in: path
      description: Evidence requirement id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    OperationalSettingIdPath:
      name: operationalSettingId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    OrganizationPresentationIdPath:
      name: organizationPresentationId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    CmsContentIdPath:
      name: cmsContentId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    BrandingConfigurationIdPath:
      name: brandingConfigurationId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    ConfigurationVersionSourceTypePath:
      name: sourceType
      in: path
      required: true
      schema: { $ref: "#/components/schemas/ConfigurationVersionSourceType" }
    SourceConfigurationIdPath:
      name: sourceConfigurationId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    ConfigurationVersionIdPath:
      name: configurationVersionId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    ConfigurationPreviewIdPath:
      name: configurationPreviewId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    ConfigurationAuditEventIdPath:
      name: configurationAuditEventId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    CmsContentTypeQuery:
      name: contentType
      in: query
      required: false
      schema: { $ref: "#/components/schemas/CmsContentType" }
    CmsContentStatusQuery:
      name: status
      in: query
      required: false
      schema: { $ref: "#/components/schemas/CmsContentStatus" }
    ConfigurationStatusQuery:
      name: status
      in: query
      required: false
      schema: { type: string, enum: [ACTIVE, INACTIVE] }
    LimitParam:
      name: limit
      in: query
      description: Maximum number of items to return (default 50, max 100).
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 50
    OffsetParam:
      name: offset
      in: query
      description: Number of items to skip before the page.
      required: false
      schema:
        type: integer
        minimum: 0
        default: 0
    VerificationTargetTypeParam:
      name: targetType
      in: path
      description: >-
        Reviewable resource type (CHECKLIST_EXECUTION, FORM_INSTANCE,
        FINDING, WORK_ORDER). WORK_ORDER (CR-BE-MOB-03 PART 05) delegates to
        the existing BE-08I verification lifecycle — COMPLETED-only,
        already-APPROVED immutable, REWORK_REQUIRED moves the Work Order
        back to IN_PROGRESS.
      required: true
      schema:
        type: string
        enum: [CHECKLIST_EXECUTION, FORM_INSTANCE, FINDING, WORK_ORDER]
    VerificationTargetIdParam:
      name: targetId
      in: path
      description: Id of the reviewable resource (checklist execution / form instance / finding / work order).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    PageParam:
      name: page
      in: query
      description: >
        1-based page number (mobile opt-in pagination convention, BE-25A).
        Ignored when absent; without pagination parameters the full scoped
        list is returned.
      required: false
      schema:
        type: integer
        minimum: 1
        default: 1
    PageSizeParam:
      name: pageSize
      in: query
      description: >
        Page size (mobile opt-in pagination convention, BE-25A; default 50,
        max 200).
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 200
        default: 50
    TaskIdPathParam:
      name: taskId
      in: path
      description: Generated task id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    ExecutionIdPathParam:
      name: executionId
      in: path
      description: Checklist execution id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    WorkforceIdPathParam:
      name: workforceId
      in: path
      description: Workforce profile id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    SupervisorIdPathParam:
      name: supervisorId
      in: path
      description: Supervisor Workforce Profile id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    WorkforceReportingIdPath:
      name: id
      in: path
      description: Workforce Profile id of the reporting record.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    TeamIdPathParam:
      name: teamId
      in: path
      description: Team id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    DailyCleaningIdPath:
      name: id
      in: path
      required: true
      description: Daily cleaning id — the BE-07 generated task id.
      schema: { $ref: "#/components/schemas/Uuid" }
    CleaningAreaIdPath:
      name: id
      in: path
      required: true
      description: Cleaning Area id.
      schema: { $ref: "#/components/schemas/Uuid" }
    HkBindingCleaningAreaIdPath:
      name: cleaningAreaId
      in: path
      required: true
      description: Cleaning Area id (BE-11A).
      schema: { $ref: "#/components/schemas/Uuid" }
    ConsumableRequirementIdPath:
      name: id
      in: path
      required: true
      description: Consumable requirement id (BE-11J).
      schema: { $ref: "#/components/schemas/Uuid" }
    HkConsumableRequirementIdPath:
      name: requirementId
      in: path
      required: true
      description: Consumable requirement id (BE-11J) of the binding.
      schema: { $ref: "#/components/schemas/Uuid" }
    HkConsumableBindingIdPath:
      name: id
      in: path
      required: true
      description: Housekeeping consumable binding id (BE-16J).
      schema: { $ref: "#/components/schemas/Uuid" }
    HousekeepingComplaintBindingIdPath:
      name: id
      in: path
      required: true
      description: Housekeeping complaint binding id (BE-11L).
      schema: { $ref: "#/components/schemas/Uuid" }
    CorrectiveActionIdPath:
      name: id
      in: path
      required: true
      description: Corrective Action id (BE-21G).
      schema: { $ref: "#/components/schemas/Uuid" }
    ToiletInspectionBindingIdPath:
      name: id
      in: path
      required: true
      description: Toilet inspection binding id.
      schema: { $ref: "#/components/schemas/Uuid" }
    ToiletInspectionExecutionIdPath:
      name: id
      in: path
      required: true
      description: BE-07 checklist execution id started from a toilet binding.
      schema: { $ref: "#/components/schemas/Uuid" }
    PublicAreaInspectionBindingIdPath:
      name: id
      in: path
      required: true
      description: Public-area inspection binding id.
      schema: { $ref: "#/components/schemas/Uuid" }
    PublicAreaInspectionExecutionIdPath:
      name: id
      in: path
      required: true
      description: BE-07 checklist execution id started from a public-area binding.
      schema: { $ref: "#/components/schemas/Uuid" }
    SupervisorInspectionIdPath:
      name: id
      in: path
      required: true
      description: Supervisor inspection id.
      schema: { $ref: "#/components/schemas/Uuid" }
    QualityAuditIdPath:
      name: id
      in: path
      required: true
      description: Quality audit id.
      schema: { $ref: "#/components/schemas/Uuid" }
    HousekeepingFindingIdPath:
      name: id
      in: path
      required: true
      description: Housekeeping finding link id (not the BE-09 finding id).
      schema: { $ref: "#/components/schemas/Uuid" }
    HousekeepingEvidenceSourceTypePath:
      name: sourceType
      in: path
      required: true
      schema: { $ref: "#/components/schemas/HousekeepingEvidenceSourceType" }
    HousekeepingEvidenceSourceIdPath:
      name: sourceId
      in: path
      required: true
      description: Source record id matching sourceType.
      schema: { $ref: "#/components/schemas/Uuid" }
    SecurityPostIdPath:
      name: id
      in: path
      required: true
      description: Security Post id (BE-12A).
      schema: { $ref: "#/components/schemas/Uuid" }
    PatrolRouteIdPath:
      name: id
      in: path
      required: true
      description: Patrol Route id (BE-12B).
      schema: { $ref: "#/components/schemas/Uuid" }
    PatrolRoutePointIdPath:
      name: id
      in: path
      required: true
      description: Patrol Route Point (checkpoint) id (BE-12B).
      schema: { $ref: "#/components/schemas/Uuid" }
    PatrolScheduleBindingIdPath:
      name: id
      in: path
      required: true
      description: Patrol Schedule Binding id (BE-12C).
      schema: { $ref: "#/components/schemas/Uuid" }
    PatrolExecutionIdPath:
      name: id
      in: path
      required: true
      description: Patrol Execution id — the BE-07 generated task id.
      schema: { $ref: "#/components/schemas/Uuid" }
    PatrolPointIdPath:
      name: pointId
      in: path
      required: true
      description: Patrol Route Point (checkpoint) id being confirmed.
      schema: { $ref: "#/components/schemas/Uuid" }
    PatrolPointVisitIdPath:
      name: id
      in: path
      required: true
      description: Patrol Point Visit id (BE-12D).
      schema: { $ref: "#/components/schemas/Uuid" }
    PatrolChecklistBindingIdPath:
      name: id
      in: path
      required: true
      description: Patrol Checklist Binding id (BE-12E).
      schema: { $ref: "#/components/schemas/Uuid" }
    PatrolChecklistExecutionIdPath:
      name: id
      in: path
      required: true
      description: BE-07 checklist execution id started from a patrol binding.
      schema: { $ref: "#/components/schemas/Uuid" }
    SecurityFindingIdPath:
      name: id
      in: path
      required: true
      description: Security finding link id (not the BE-09 finding id).
      schema: { $ref: "#/components/schemas/Uuid" }
    IncidentIdPath:
      name: id
      in: path
      required: true
      description: Incident id (BE-21A foundation).
      schema: { $ref: "#/components/schemas/Uuid" }
    OperationalIncidentIdPath:
      name: id
      in: path
      required: true
      description: Operational Incident id — the shared BE-21A Incident id.
      schema: { $ref: "#/components/schemas/Uuid" }
    InspectionBindingIdPath:
      name: id
      in: path
      required: true
      description: Equipment inspection binding id (BE-10B).
      schema: { $ref: "#/components/schemas/Uuid" }
    InspectionExecutionIdPath:
      name: id
      in: path
      required: true
      description: BE-07 checklist execution id started from an inspection binding.
      schema: { $ref: "#/components/schemas/Uuid" }
    MeterReadingBindingIdPath:
      name: id
      in: path
      required: true
      description: Meter reading binding id (BE-10C).
      schema: { $ref: "#/components/schemas/Uuid" }
    MeterReadingExecutionIdPath:
      name: id
      in: path
      required: true
      description: BE-07 form instance id started from a meter reading binding.
      schema: { $ref: "#/components/schemas/Uuid" }
    LogSheetBindingIdPath:
      name: id
      in: path
      required: true
      description: Equipment log sheet binding id (BE-10D).
      schema: { $ref: "#/components/schemas/Uuid" }
    LogSheetExecutionIdPath:
      name: id
      in: path
      required: true
      description: BE-07 form instance id for one log sheet row.
      schema: { $ref: "#/components/schemas/Uuid" }
    EngineeringChecklistBindingIdPath:
      name: id
      in: path
      required: true
      description: Engineering checklist binding id (BE-10E).
      schema: { $ref: "#/components/schemas/Uuid" }
    EngineeringChecklistExecutionIdPath:
      name: id
      in: path
      required: true
      description: BE-07 checklist execution id started from an engineering binding.
      schema: { $ref: "#/components/schemas/Uuid" }
    BreakdownIdPath:
      name: id
      in: path
      required: true
      description: Breakdown / corrective event id (BE-10F).
      schema: { $ref: "#/components/schemas/Uuid" }
    MaintenanceBindingIdPath:
      name: id
      in: path
      required: true
      description: Planned maintenance binding id (BE-10G).
      schema: { $ref: "#/components/schemas/Uuid" }
    EngineeringFindingIdPath:
      name: id
      in: path
      required: true
      description: Engineering finding link id (not the BE-09 finding id).
      schema: { $ref: "#/components/schemas/Uuid" }
    EngineeringShiftHandoverIdPath:
      name: id
      in: path
      required: true
      description: Engineering shift handover id (BE-10J).
      schema: { $ref: "#/components/schemas/Uuid" }
    SecurityShiftHandoverBindingIdPath:
      name: id
      in: path
      required: true
      description: Security shift handover binding id (BE-12G).
      schema: { $ref: "#/components/schemas/Uuid" }
    AssetSparePartIdPath:
      name: id
      in: path
      required: true
      description: Asset ↔ spare-part Item binding id (BE-16H).
      schema: { $ref: "#/components/schemas/Uuid" }
    FindingIdPathParam:
      name: findingId
      in: path
      description: Finding id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    IdentifierPathParam:
      name: identifier
      in: path
      description: Asset identifier value (printed label / QR payload).
      required: true
      schema:
        type: string
    EvidenceIdPathParam:
      name: evidenceId
      in: path
      description: Evidence submission (file metadata record) id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"

    PurchaseRequestIdPath:
      name: purchaseRequestId
      in: path
      description: Purchase Request id (BE-17A).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    ServiceRequestIdPath:
      name: serviceRequestId
      in: path
      description: Service Request id (BE-17C).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    MaterialRequestIdPath:
      name: id
      in: path
      description: Material Request line id (BE-17B).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    MaterialReservationSourceIdPath:
      name: materialRequestId
      in: path
      description: Existing Material Request line id used as reservation demand source.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    MaterialReservationIdPath:
      name: id
      in: path
      description: Material reservation id (CR-BE-INV-CONTROL-01 PART 01).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    ProcurementApprovalIdPath:
      name: id
      in: path
      description: Procurement approval binding id (BE-17D).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    ReceivingIdPath:
      name: id
      in: path
      description: Receiving record id (BE-17G).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    InventoryItemIdPath:
      name: itemId
      in: path
      description: Inventory item id (BE-16A material / spare part identity).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    WarehouseIdPath:
      name: warehouseId
      in: path
      description: Inventory warehouse / store id (BE-16B).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    StockMovementIdPath:
      name: id
      in: path
      description: Inventory stock movement id (BE-16D ledger entry).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    StockBalanceIdPath:
      name: id
      in: path
      description: Inventory stock balance id (BE-16C).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    WorkOrderIdPath:
      name: workOrderId
      in: path
      description: Work Order id (BE-08).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    WorkOrderMaterialUsageIdPath:
      name: id
      in: path
      description: Work Order material usage id (BE-16I).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    VendorIdPath:
      name: vendorId
      in: path
      description: Vendor id (BE-13).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"

    FxRateIdPath:
      name: rateId
      in: path
      required: true
      description: FX rate id.
      schema: { type: string, format: uuid }
    FxClientIdPath:
      name: clientId
      in: path
      required: true
      description: Client id whose FX policy is addressed.
      schema: { type: string, format: uuid }
  responses:
    BuildingUtilityOperationalSummarySuccess:
      description: Building Utility operational summary response.
      content: { application/json: { schema: { $ref: "#/components/schemas/BuildingUtilityOperationalSummaryEnvelope" } } }
    UtilityExceptionSuccess:
      description: Utility operational exception response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityExceptionEnvelope" } } }
    UtilityExceptionListSuccess:
      description: Utility operational exception list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityExceptionListEnvelope" } } }
    UtilityReadingDueSuccess:
      description: Utility Reading Due response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityReadingDueEnvelope" } } }
    UtilityReadingDueListSuccess:
      description: Utility Reading Due list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityReadingDueListEnvelope" } } }
    UtilityOcrCandidateSuccess:
      description: Utility OCR candidate response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityOcrCandidateEnvelope" } } }
    UtilityMeterSuccess:
      description: Utility Meter response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityMeterEnvelope" } } }
    UtilityMeterListSuccess:
      description: Utility Meter list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityMeterListEnvelope" } } }
    UtilityHierarchySuccess:
      description: Utility Meter hierarchy response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityHierarchyEnvelope" } } }
    UtilityHierarchyListSuccess:
      description: Utility Meter hierarchy list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityHierarchyListEnvelope" } } }
    UtilityReadingSuccess:
      description: Utility Meter Reading response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityReadingEnvelope" } } }
    UtilityReadingListSuccess:
      description: Utility Meter Reading list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityReadingListEnvelope" } } }
    UtilityEvidenceSuccess:
      description: Utility Reading Evidence response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityEvidenceEnvelope" } } }
    UtilityEvidenceListSuccess:
      description: Utility Reading Evidence list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityEvidenceListEnvelope" } } }
    UtilityEvidenceReadinessSuccess:
      description: Utility Reading Evidence readiness response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityEvidenceReadinessEnvelope" } } }
    UtilityConsumptionSuccess:
      description: Utility Consumption response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityConsumptionEnvelope" } } }
    UtilityConsumptionListSuccess:
      description: Utility Consumption list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityConsumptionListEnvelope" } } }
    UtilityTariffSuccess:
      description: Utility Tariff response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityTariffEnvelope" } } }
    UtilityTariffListSuccess:
      description: Utility Tariff list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityTariffListEnvelope" } } }
    UtilityCalculationSuccess:
      description: Utility Calculation response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityCalculationEnvelope" } } }
    UtilityCalculationListSuccess:
      description: Utility Calculation list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityCalculationListEnvelope" } } }
    TenantUtilityApprovalSuccess:
      description: Tenant Approval response.
      content: { application/json: { schema: { $ref: "#/components/schemas/TenantUtilityApprovalEnvelope" } } }
    TenantUtilityApprovalListSuccess:
      description: Tenant Approval list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/TenantUtilityApprovalListEnvelope" } } }
    TenantApprovalActionsSuccess:
      description: Tenant Approval available-actions response.
      content: { application/json: { schema: { $ref: "#/components/schemas/TenantApprovalActionsEnvelope" } } }
    UtilityApprovalContextSuccess:
      description: Utility Calculation approval-context response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityApprovalContextEnvelope" } } }
    UtilityBillSuccess:
      description: Utility Bill response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityBillEnvelope" } } }
    UtilityBillListSuccess:
      description: Utility Bill list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityBillListEnvelope" } } }
    UtilityInvoiceReadySuccess:
      description: Invoice-ready Utility projection response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityInvoiceReadyEnvelope" } } }
    UtilityReconciliationSuccess:
      description: Building Utility reconciliation response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityReconciliationEnvelope" } } }
    UtilityReconciliationListSuccess:
      description: Building Utility reconciliation list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityReconciliationListEnvelope" } } }
    OperationalBudgetSuccess:
      description: Operational Budget response.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalBudgetEnvelope" } } }
    OperationalBudgetListSuccess:
      description: Operational Budget list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalBudgetListEnvelope" } } }
    OperationalBudgetCategorySuccess:
      description: Operational Budget Category response.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalBudgetCategoryEnvelope" } } }
    OperationalBudgetCategoryListSuccess:
      description: Operational Budget Category list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalBudgetCategoryListEnvelope" } } }
    OperationalBudgetSourceBindingSuccess:
      description: Operational Budget source binding response.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalBudgetSourceBindingEnvelope" } } }
    OperationalBudgetSourceBindingListSuccess:
      description: Operational Budget source binding list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalBudgetSourceBindingListEnvelope" } } }
    OperationalBudgetOverspendPolicySuccess:
      description: Operational Budget overspend policy transition response.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalBudgetOverspendPolicyEnvelope" } } }
    OperationalCommitmentSuccess:
      description: Operational Commitment response.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalCommitmentEnvelope" } } }
    OperationalCommitmentListSuccess:
      description: Operational Commitment list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalCommitmentListEnvelope" } } }
    OperationalCommitmentDetailSuccess:
      description: Operational Commitment with its append-only ledger entries.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalCommitmentDetailEnvelope" } } }
    OperationalBudgetVarianceSuccess:
      description: Derived Operational Budget variance response.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalBudgetVarianceEnvelope" } } }
    OperationalBudgetVarianceListSuccess:
      description: Derived Operational Budget variance summaries.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalBudgetVarianceListEnvelope" } } }
    OperationalBudgetTraceabilitySuccess:
      description: Operational Budget source traceability response.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalBudgetTraceabilityEnvelope" } } }
    OperationalBudgetAggregationSuccess:
      description: Read-time Operational Budget aggregation response.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalBudgetAggregationEnvelope" } } }
    BadRequest:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: Request validation failed.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
          examples:
            validation:
              value:
                success: false
                error:
                  code: VALIDATION_ERROR
                  message: Request validation failed.
                  details:
                    - field: email
                      message: Email is required and must be a valid email address.
    Unauthorized:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: >
        Missing, invalid or expired session (protected routes). Login failures
        use `InvalidCredentials` (401 `INVALID_CREDENTIALS`) instead. Mobile
        must treat any 401 as "re-authenticate".
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
          examples:
            missingSession:
              value:
                success: false
                error:
                  code: AUTHENTICATION_REQUIRED
                  message: Authentication is required.
            invalidSession:
              value:
                success: false
                error:
                  code: INVALID_SESSION
                  message: The session is not valid.
            sessionExpired:
              value:
                success: false
                error:
                  code: SESSION_EXPIRED
                  message: The session has expired.
    InvalidCredentials:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: >
        Login failed: the email is unknown, the password is wrong, or the
        account is not in a login-allowed state. Deliberately generic so the
        three cases are indistinguishable (no account enumeration).
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
          examples:
            invalidCredentials:
              value:
                success: false
                error:
                  code: INVALID_CREDENTIALS
                  message: Invalid email or password.
    Forbidden:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: >
        Authenticated but the required permission is missing, or the caller
        lacks an ACTIVE assignment to the operation's resolved Building.
        Building-scoped resources do not widen access through a Client,
        Property, Vendor, or sibling Building relationship.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
          examples:
            permissionDenied:
              value:
                success: false
                error:
                  code: PERMISSION_DENIED
                  message: You do not have permission to perform this action.
            buildingAccessDenied:
              value:
                success: false
                error:
                  code: BUILDING_ACCESS_DENIED
                  message: Access to the requested building context is denied.
    NotFound:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: The requested resource does not exist.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
          examples:
            notFound:
              value:
                success: false
                error:
                  code: NOT_FOUND
                  message: Resource not found.
    Conflict:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: The request conflicts with existing state (e.g. a duplicate key).
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    UnprocessableEntity:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: The request is well-formed but semantically invalid (e.g. overpayment, business rule violation).
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    RateLimited:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: Too many attempts (login throttle).
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
          examples:
            rateLimited:
              value:
                success: false
                error:
                  code: AUTH_RATE_LIMITED
                  message: Too many login attempts. Please try again later.
    InternalServerError:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: Unhandled server error. Stack traces are never returned.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
          examples:
            internal:
              value:
                success: false
                error:
                  code: INTERNAL_SERVER_ERROR
                  message: An unexpected server error occurred.
    DatabaseUnavailable:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: Database connection is unavailable.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
          examples:
            database:
              value:
                success: false
                error:
                  code: DATABASE_UNAVAILABLE
                  message: Database connection is unavailable.

    # ------------------------------------------------------------------
    # CR-BE-FX-01 PART 02 — FX Rate Authority + Client FX Policy
    # ------------------------------------------------------------------
    FxRateSuccess:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: One FX rate.
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/SuccessEnvelope"
              - type: object
                properties:
                  data:
                    $ref: "#/components/schemas/FxRate"
    FxRateListSuccess:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: FX rates matching the filters.
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/SuccessEnvelope"
              - type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FxRate"
    FxRateEventListSuccess:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: >
        The append-only audit trail of one FX rate, oldest first. Rows can never
        be updated or deleted.
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/SuccessEnvelope"
              - type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FxRateEvent"
    FxRateSupersessionSuccess:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: >
        The superseded incumbent and its ACTIVE successor, linked exactly in
        both directions (`supersededByRateId` / `supersedesRateId`).
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/SuccessEnvelope"
              - type: object
                properties:
                  data:
                    $ref: "#/components/schemas/FxRateSupersessionResult"
    ClientFxPolicyReadSuccess:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: >
        The Client FX policy read model. `policy` is `null` when no policy
        exists, and `fxAvailable` is `false` in that case — absence of a policy
        is never treated as permission.
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/SuccessEnvelope"
              - type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ClientFxPolicyReadModel"
    ClientFxPolicySuccess:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: The stored Client FX policy.
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/SuccessEnvelope"
              - type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ClientFxPolicy"

  schemas:
    # ------------------------------------------------------------------
    # CR-BE-UTL-01 PART 13 — Utility domain
    # ------------------------------------------------------------------
    UtilityType:
      type: string
      enum: [ELECTRICITY, WATER, GAS]
    UtilityBillableType:
      type: string
      enum: [ELECTRICITY, WATER]
    UtilityMeterPurpose:
      type: string
      enum: [TENANT, BUILDING, COMMON_AREA, ENERGY_SOURCE]
    UtilityMeterStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    UtilityReadingSource:
      type: string
      enum: [MANUAL, ENGINEERING, IMPORT, SYSTEM]
    UtilityReadingType:
      type: string
      enum: [ACTUAL, ESTIMATED]
    UtilityExceptionType:
      type: string
      enum: [ABNORMAL_CONSUMPTION, MISSING_OR_LATE_READING, OCR_MANUAL_FOLLOW_UP, RECONCILIATION_VARIANCE, UNALLOCATED_CONSUMPTION, OTHER]
    UtilityExceptionSeverity:
      type: string
      enum: [LOW, MEDIUM, HIGH, CRITICAL]
    UtilityExceptionStatus:
      type: string
      enum: [OPEN, UNDER_REVIEW, RESOLVED, CANCELLED]
    UtilityReadingDueStatus:
      type: string
      enum: [DUE, COMPLETED, OVERDUE, CANCELLED]
    UtilityOcrStatus:
      type: string
      enum: [PENDING_REVIEW, ACCEPTED, REJECTED]
    UtilityCalculationStatus:
      type: string
      enum: [DRAFT, FINALIZED, SUPERSEDED]
    TenantApprovalStatus:
      type: string
      enum: [PENDING, APPROVED, REJECTED]
    UtilityBillStatus:
      type: string
      enum: [DRAFT, ISSUED, CANCELLED]
    UtilityTariffStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    UtilityErrorCode:
      type: string
      description: Stable Utility errors; all are returned in ErrorEnvelope.error.code.
      enum:
        - UTILITY_METER_NOT_FOUND
        - UTILITY_METER_READING_NOT_FOUND
        - UTILITY_METER_READING_ALREADY_EXISTS
        - UTILITY_METER_READING_IMMUTABLE
        - UTILITY_METER_CONSUMPTION_READING_INVALID
        - UTILITY_METER_CONSUMPTION_PERIOD_INVALID
        - UTILITY_METER_CONSUMPTION_UOM_MISMATCH
        - UTILITY_METER_CONSUMPTION_TENANT_MISMATCH
        - UTILITY_EXCEPTION_NOT_FOUND
        - UTILITY_EXCEPTION_REFERENCE_INVALID
        - UTILITY_EXCEPTION_ALREADY_OPEN
        - UTILITY_EXCEPTION_TRANSITION_INVALID
        - UTILITY_READING_DUE_NOT_FOUND
        - UTILITY_READING_DUE_ALREADY_EXISTS
        - UTILITY_READING_DUE_SCHEDULE_INVALID
        - UTILITY_READING_DUE_TRANSITION_INVALID
        - UTILITY_READING_DUE_READING_MISMATCH
        - UTILITY_OCR_CANDIDATE_NOT_FOUND
        - UTILITY_OCR_EVIDENCE_INVALID
        - UTILITY_OCR_CANDIDATE_ALREADY_EXISTS
        - UTILITY_OCR_DECISION_FINAL
        - UTILITY_OCR_READING_MISMATCH
        - UTILITY_TARIFF_NOT_FOUND
        - UTILITY_TARIFF_INVALID
        - UTILITY_TARIFF_PERIOD_OVERLAP
        - TENANT_APPROVAL_CONTEXT_MISMATCH
        - TENANT_APPROVAL_ALREADY_DECIDED
        - UTILITY_BILL_APPROVAL_REQUIRED
        - UTILITY_BILL_APPROVAL_REJECTED
        - UTILITY_BILL_ALREADY_EXISTS
        - BUILDING_UTILITY_RECONCILIATION_NO_SOURCE
        - BUILDING_UTILITY_RECONCILIATION_UOM_MISMATCH
        - BUILDING_UTILITY_RECONCILIATION_AREA_MISSING
        - BUILDING_UTILITY_RECONCILIATION_ALREADY_EXISTS
        - BUILDING_ACCESS_DENIED
        - PERMISSION_DENIED
        - VALIDATION_ERROR

    CreateUtilityMeterRequest:
      type: object
      required: [code, name, utilityType, uomId]
      properties:
        code: { type: string, maxLength: 64 }
        name: { type: string, maxLength: 160 }
        utilityType: { $ref: "#/components/schemas/UtilityType" }
        purpose: { $ref: "#/components/schemas/UtilityMeterPurpose" }
        uomId: { $ref: "#/components/schemas/Uuid" }
        spaceId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        functionalLocationId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        serialNumber: { type: string, nullable: true, maxLength: 128 }
        status: { $ref: "#/components/schemas/UtilityMeterStatus" }
    UpdateUtilityMeterRequest:
      type: object
      minProperties: 1
      properties:
        name: { type: string }
        utilityType: { $ref: "#/components/schemas/UtilityType" }
        uomId: { $ref: "#/components/schemas/Uuid" }
        spaceId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        functionalLocationId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        serialNumber: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/UtilityMeterStatus" }
    UtilityMeter:
      type: object
      required: [id, clientId, buildingId, code, name, utilityType, purpose, uomId, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        spaceId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        functionalLocationId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        code: { type: string }
        name: { type: string }
        utilityType: { $ref: "#/components/schemas/UtilityType" }
        purpose: { $ref: "#/components/schemas/UtilityMeterPurpose" }
        uomId: { $ref: "#/components/schemas/Uuid" }
        serialNumber: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/UtilityMeterStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateUtilityMeterHierarchyRequest:
      type: object
      required: [subMeterId]
      properties:
        subMeterId: { $ref: "#/components/schemas/Uuid" }
        effectiveFrom: { type: string, format: date-time }
        effectiveUntil: { type: string, format: date-time, nullable: true }
    UtilityMeterHierarchy:
      type: object
      required: [id, clientId, mainMeterId, subMeterId, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        mainMeterId: { $ref: "#/components/schemas/Uuid" }
        subMeterId: { $ref: "#/components/schemas/Uuid" }
        effectiveFrom: { type: string, format: date-time, nullable: true }
        effectiveUntil: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/UtilityMeterStatus" }

    RecordUtilityReadingRequest:
      type: object
      required: [readingValue, readingAt]
      properties:
        readingValue: { type: number, minimum: 0 }
        readingAt: { type: string, format: date-time }
        uomId: { $ref: "#/components/schemas/Uuid" }
        source: { $ref: "#/components/schemas/UtilityReadingSource" }
        readingType: { $ref: "#/components/schemas/UtilityReadingType" }
        tenantCompanyId: { $ref: "#/components/schemas/Uuid" }
        meterReadingBindingId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        formInstanceId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        notes: { type: string, nullable: true, maxLength: 1024 }
    UtilityMeterReading:
      type: object
      required: [id, clientId, buildingId, meterId, uomId, readingValue, readingAt, source, readingType, recordedByUserId]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        meterId: { $ref: "#/components/schemas/Uuid" }
        uomId: { $ref: "#/components/schemas/Uuid" }
        readingValue: { type: number }
        readingAt: { type: string, format: date-time }
        source: { $ref: "#/components/schemas/UtilityReadingSource" }
        readingType: { $ref: "#/components/schemas/UtilityReadingType" }
        recordedByUserId: { $ref: "#/components/schemas/Uuid" }
        tenantAssignmentId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        tenantCompanyId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        meterReadingBindingId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        formInstanceId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        notes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    SubmitUtilityReadingEvidenceRequest:
      type: object
      required: [evidenceType, fileReference, originalFileName, mimeType, fileSize]
      properties:
        evidenceType: { type: string, enum: [PHOTO, DOCUMENT, SIGNATURE] }
        evidenceRequirementId: { $ref: "#/components/schemas/Uuid" }
        fileReference: { type: string }
        originalFileName: { type: string }
        mimeType: { type: string }
        fileSize: { type: integer, minimum: 1, maximum: 52428800 }
        capturedAt: { type: string, format: date-time }
    UtilityReadingEvidence:
      type: object
      required: [id, clientId, meterReadingId, evidenceType, fileReference, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        meterReadingId: { $ref: "#/components/schemas/Uuid" }
        evidenceRequirementId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        evidenceType: { type: string, enum: [PHOTO, DOCUMENT, SIGNATURE] }
        fileReference: { type: string }
        originalFileName: { type: string }
        mimeType: { type: string }
        fileSize: { type: integer }
        capturedAt: { type: string, format: date-time, nullable: true }
        submittedByUserId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        status: { type: string, enum: [ACTIVE, REMOVED] }
    UtilityEvidenceReadiness:
      type: object
      required: [meterReadingId, ready, missingEvidenceTypes, requirements]
      properties:
        meterReadingId: { $ref: "#/components/schemas/Uuid" }
        ready: { type: boolean }
        missingEvidenceTypes: { type: array, items: { type: string } }
        requirements: { type: array, items: { type: object } }

    BuildingUtilityReconciliationSummary:
      type: object
      required: [reconciliationId, utilityType, periodStart, periodEnd, sourceConsumption, tenantConsumption, commonAreaConsumption, unallocatedConsumption, performanceMetric, performanceValue, applicableAreaSqm, consumptionUomId, performanceUom, calculatedAt]
      properties:
        reconciliationId: { $ref: "#/components/schemas/Uuid" }
        utilityType: { $ref: "#/components/schemas/UtilityBillableType" }
        periodStart: { type: string, format: date-time }
        periodEnd: { type: string, format: date-time }
        sourceConsumption: { type: number }
        tenantConsumption: { type: number }
        commonAreaConsumption: { type: number }
        unallocatedConsumption: { type: number }
        reconciliationPercentage: { type: number, nullable: true }
        performanceMetric: { type: string, enum: [IKE, IKA] }
        performanceValue: { type: number }
        applicableAreaSqm: { type: number }
        consumptionUomId: { $ref: "#/components/schemas/Uuid" }
        performanceUom: { type: string, enum: [kWh/m², m³/m²] }
        calculatedAt: { type: string, format: date-time }
    BuildingUtilityOperationalSummary:
      type: object
      required: [clientId, buildingId, reportingPeriod, reconciliations, meters, exceptions, billingReadiness, asOf]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        reportingPeriod:
          type: object
          required: [start, end]
          properties:
            start: { type: string, format: date-time }
            end: { type: string, format: date-time }
        reconciliations:
          type: array
          items: { $ref: "#/components/schemas/BuildingUtilityReconciliationSummary" }
        meters:
          type: object
          required: [totalActiveMeters, byPurpose, readingDue]
          properties:
            totalActiveMeters: { type: integer }
            byPurpose:
              type: object
              required: [TENANT, BUILDING, COMMON_AREA, ENERGY_SOURCE]
              properties:
                TENANT: { type: integer }
                BUILDING: { type: integer }
                COMMON_AREA: { type: integer }
                ENERGY_SOURCE: { type: integer }
            readingDue:
              type: object
              required: [due, overdue, completed, cancelled]
              properties:
                due: { type: integer }
                overdue: { type: integer }
                completed: { type: integer }
                cancelled: { type: integer }
        exceptions:
          type: object
          required: [total, byType, bySeverity, byStatus]
          properties:
            total: { type: integer }
            byType: { type: object, additionalProperties: { type: integer } }
            bySeverity:
              type: object
              required: [LOW, MEDIUM, HIGH, CRITICAL]
              properties:
                LOW: { type: integer }
                MEDIUM: { type: integer }
                HIGH: { type: integer }
                CRITICAL: { type: integer }
            byStatus:
              type: object
              required: [OPEN, UNDER_REVIEW, RESOLVED, CANCELLED]
              properties:
                OPEN: { type: integer }
                UNDER_REVIEW: { type: integer }
                RESOLVED: { type: integer }
                CANCELLED: { type: integer }
        billingReadiness:
          type: object
          required: [calculationsAwaitingApproval, approvedCalculations, rejectedCalculations, utilityBillsCreated, invoiceReadyUtilityBills]
          properties:
            calculationsAwaitingApproval: { type: integer }
            approvedCalculations: { type: integer }
            rejectedCalculations: { type: integer }
            utilityBillsCreated: { type: integer }
            invoiceReadyUtilityBills: { type: integer }
        asOf: { type: string, format: date-time }
    CreateUtilityExceptionRequest:
      type: object
      required: [exceptionType, severity, summary]
      description: At least one authoritative Utility reference is required.
      properties:
        meterId: { $ref: "#/components/schemas/Uuid" }
        meterReadingId: { $ref: "#/components/schemas/Uuid" }
        readingDueId: { $ref: "#/components/schemas/Uuid" }
        consumptionId: { $ref: "#/components/schemas/Uuid" }
        abnormalConsumptionId: { $ref: "#/components/schemas/Uuid" }
        ocrCandidateId: { $ref: "#/components/schemas/Uuid" }
        reconciliationId: { $ref: "#/components/schemas/Uuid" }
        exceptionType: { $ref: "#/components/schemas/UtilityExceptionType" }
        severity: { $ref: "#/components/schemas/UtilityExceptionSeverity" }
        summary: { type: string, maxLength: 300 }
        details: { type: string, nullable: true, maxLength: 2000 }
    StartUtilityExceptionReviewRequest:
      type: object
      properties:
        reviewNotes: { type: string, nullable: true, maxLength: 2000 }
    ResolveUtilityExceptionRequest:
      type: object
      required: [resolutionNotes]
      properties:
        resolutionNotes: { type: string, maxLength: 2000 }
    CancelUtilityExceptionRequest:
      type: object
      required: [reason]
      properties:
        reason: { type: string, maxLength: 2000 }
    UtilityOperationalException:
      type: object
      required: [id, clientId, buildingId, utilityType, exceptionType, severity, status, summary, detectedAt, detectedByUserId]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        utilityType: { $ref: "#/components/schemas/UtilityType" }
        meterId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        meterReadingId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        readingDueId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        consumptionId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        abnormalConsumptionId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        ocrCandidateId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        reconciliationId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        exceptionType: { $ref: "#/components/schemas/UtilityExceptionType" }
        severity: { $ref: "#/components/schemas/UtilityExceptionSeverity" }
        status: { $ref: "#/components/schemas/UtilityExceptionStatus" }
        summary: { type: string }
        details: { type: string, nullable: true }
        detectedAt: { type: string, format: date-time }
        detectedByUserId: { $ref: "#/components/schemas/Uuid" }
        reviewerUserId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        reviewNotes: { type: string, nullable: true }
        reviewStartedAt: { type: string, format: date-time, nullable: true }
        resolvedByUserId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        resolvedAt: { type: string, format: date-time, nullable: true }
        resolutionNotes: { type: string, nullable: true }
        cancelledByUserId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        cancelledAt: { type: string, format: date-time, nullable: true }
        cancellationReason: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateUtilityReadingDueRequest:
      type: object
      required: [periodStart, periodEnd, dueAt]
      properties:
        periodStart: { type: string, format: date-time }
        periodEnd: { type: string, format: date-time }
        dueAt: { type: string, format: date-time }
        scheduleDefinitionId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        generatedTaskId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
    CompleteUtilityReadingDueRequest:
      type: object
      required: [meterReadingId]
      properties:
        meterReadingId: { $ref: "#/components/schemas/Uuid" }
    CancelUtilityReadingDueRequest:
      type: object
      required: [reason]
      properties:
        reason: { type: string, maxLength: 1000 }
    UtilityReadingDue:
      type: object
      required: [id, clientId, buildingId, meterId, utilityType, periodStart, periodEnd, dueAt, status, createdByUserId, createdAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        meterId: { $ref: "#/components/schemas/Uuid" }
        utilityType: { $ref: "#/components/schemas/UtilityType" }
        periodStart: { type: string, format: date-time }
        periodEnd: { type: string, format: date-time }
        dueAt: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/UtilityReadingDueStatus" }
        scheduleDefinitionId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        generatedTaskId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        meterReadingId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        completedByUserId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        cancelledAt: { type: string, format: date-time, nullable: true }
        cancelledByUserId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        cancellationReason: { type: string, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateUtilityOcrCandidateRequest:
      type: object
      required: [candidateReadingValue]
      properties:
        candidateReadingValue: { type: number, minimum: 0 }
        confidence: { type: number, minimum: 0, maximum: 1, nullable: true }
    AcceptUtilityOcrCandidateRequest:
      type: object
      properties:
        meterReadingId: { $ref: "#/components/schemas/Uuid" }
        readingAt: { type: string, format: date-time }
        readingDueId: { $ref: "#/components/schemas/Uuid" }
        notes: { type: string, nullable: true, maxLength: 1000 }
      description: meterReadingId and readingAt are mutually exclusive.
    RejectUtilityOcrCandidateRequest:
      type: object
      required: [decisionNotes]
      properties:
        decisionNotes: { type: string, maxLength: 1000 }
    UtilityOcrCandidate:
      type: object
      required: [id, clientId, buildingId, meterId, evidenceId, candidateReadingValue, status, createdByUserId, createdAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        meterId: { $ref: "#/components/schemas/Uuid" }
        evidenceId: { $ref: "#/components/schemas/Uuid" }
        candidateReadingValue: { type: number }
        confidence: { type: number, nullable: true, minimum: 0, maximum: 1 }
        status: { $ref: "#/components/schemas/UtilityOcrStatus" }
        acceptedReadingId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        verifiedByUserId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        verifiedAt: { type: string, format: date-time, nullable: true }
        decisionNotes: { type: string, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CalculateUtilityConsumptionRequest:
      type: object
      required: [currentReadingId]
      properties:
        previousReadingId: { $ref: "#/components/schemas/Uuid" }
        currentReadingId: { $ref: "#/components/schemas/Uuid" }
        notes: { type: string, nullable: true }
    UtilityConsumption:
      type: object
      required: [id, clientId, buildingId, meterId, previousReadingId, currentReadingId, uomId, consumptionValue, periodStart, periodEnd]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        meterId: { $ref: "#/components/schemas/Uuid" }
        previousReadingId: { $ref: "#/components/schemas/Uuid" }
        currentReadingId: { $ref: "#/components/schemas/Uuid" }
        uomId: { $ref: "#/components/schemas/Uuid" }
        consumptionValue: { type: number, minimum: 0 }
        periodStart: { type: string, format: date-time }
        periodEnd: { type: string, format: date-time }
        tenantAssignmentId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        tenantCompanyId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        calculatedAt: { type: string, format: date-time }

    CreateUtilityTariffRequest:
      type: object
      required: [utilityType, currency, uomId, ratePerUom, effectiveFrom]
      properties:
        utilityType: { $ref: "#/components/schemas/UtilityBillableType" }
        currency: { type: string, pattern: "^[A-Z]{3}$" }
        uomId: { $ref: "#/components/schemas/Uuid" }
        ratePerUom: { oneOf: [{ type: number, minimum: 0 }, { type: string, pattern: "^(?:0|[1-9][0-9]*)(?:\\.[0-9]{1,8})?$" }] }
        effectiveFrom: { type: string, format: date-time }
        effectiveUntil: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/UtilityTariffStatus" }
    UtilityTariff:
      type: object
      required: [id, clientId, buildingId, utilityType, currency, uomId, ratePerUom, effectiveFrom, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        utilityType: { $ref: "#/components/schemas/UtilityBillableType" }
        currency: { type: string }
        uomId: { $ref: "#/components/schemas/Uuid" }
        ratePerUom: { type: number }
        effectiveFrom: { type: string, format: date-time }
        effectiveUntil: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/UtilityTariffStatus" }

    CalculateUtilityChargeRequest:
      type: object
      properties:
        calculationBasisId: { $ref: "#/components/schemas/Uuid" }
        notes: { type: string, nullable: true }
    UtilityCalculation:
      type: object
      required: [id, clientId, buildingId, meterId, utilityType, consumptionId, consumptionQuantity, uomId, appliedRateValue, calculatedAmount, periodStart, periodEnd, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        meterId: { $ref: "#/components/schemas/Uuid" }
        utilityType: { $ref: "#/components/schemas/UtilityType" }
        consumptionId: { $ref: "#/components/schemas/Uuid" }
        calculationBasisId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        consumptionQuantity: { type: number }
        uomId: { $ref: "#/components/schemas/Uuid" }
        tariffId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        tariffRate: { type: number, nullable: true }
        currency: { type: string, nullable: true }
        appliedRateValue: { type: number }
        calculatedAmount: { type: number }
        periodStart: { type: string, format: date-time }
        periodEnd: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/UtilityCalculationStatus" }
        tenantAssignmentId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        tenantCompanyId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        supersedesCalculationId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        calculatedAt: { type: string, format: date-time }
        finalizedAt: { type: string, format: date-time, nullable: true }

    CreateTenantApprovalRequest:
      type: object
      required: [requestType, requestId, approvalType, approverUserId]
      properties:
        requestType: { type: string, enum: [SERVICE_REQUEST, COMPLAINT, UTILITY_REQUEST, UTILITY_CALCULATION] }
        requestId: { $ref: "#/components/schemas/Uuid" }
        approvalType: { type: string }
        approverUserId: { $ref: "#/components/schemas/Uuid" }
    TenantApprovalDecisionRequest:
      type: object
      properties:
        decisionNotes: { type: string, maxLength: 1000 }
    TenantApprovalActions:
      type: object
      required: [approvalId, state, availableActions]
      properties:
        approvalId: { $ref: "#/components/schemas/Uuid" }
        state: { $ref: "#/components/schemas/TenantApprovalStatus" }
        availableActions: { type: array, items: { type: string, enum: [APPROVE, REJECT] } }
    TenantUtilityApproval:
      type: object
      required: [id, requestType, requestId, tenantCompanyId, buildingId, approvalType, approverUserId, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        requestType: { type: string }
        requestId: { $ref: "#/components/schemas/Uuid" }
        utilityCalculationId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        tenantCompanyId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        utilitySpaceId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        utilityMeterId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        utilityMeterPurpose: { allOf: [{ $ref: "#/components/schemas/UtilityMeterPurpose" }], nullable: true }
        utilityType: { allOf: [{ $ref: "#/components/schemas/UtilityBillableType" }], nullable: true }
        utilityPeriodStart: { type: string, format: date-time, nullable: true }
        utilityPeriodEnd: { type: string, format: date-time, nullable: true }
        utilityConsumptionQuantity: { type: number, nullable: true }
        utilityUomId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        utilityTariffId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        utilityTariffRate: { type: number, nullable: true }
        utilityCalculatedAmount: { type: number, nullable: true }
        utilityCurrency: { type: string, nullable: true }
        approvalType: { type: string }
        approverUserId: { $ref: "#/components/schemas/Uuid" }
        status: { $ref: "#/components/schemas/TenantApprovalStatus" }
        decidedAt: { type: string, format: date-time, nullable: true }
        decisionNotes: { type: string, nullable: true }
    UtilityApprovalContext:
      type: object
      required: [utilityCalculationId, clientId, tenantCompanyId, buildingId, approvable, pendingApprovals, approvals]
      properties:
        utilityCalculationId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        tenantCompanyId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        approvable: { type: boolean }
        pendingApprovals: { type: array, items: { $ref: "#/components/schemas/TenantUtilityApproval" } }
        approvals: { type: array, items: { $ref: "#/components/schemas/TenantUtilityApproval" } }

    GenerateUtilityBillRequest:
      type: object
      required: [calculationId, dueDate]
      properties:
        calculationId: { $ref: "#/components/schemas/Uuid" }
        dueDate: { type: string, format: date }
    UtilityBill:
      type: object
      required: [id, clientId, tenantCompanyId, buildingId, meterId, calculationId, approvalId, spaceId, utilityType, periodStart, periodEnd, consumptionQuantity, uomId, tariffRate, billAmount, currency, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        tenantCompanyId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        spaceId: { $ref: "#/components/schemas/Uuid" }
        meterId: { $ref: "#/components/schemas/Uuid" }
        tenantAssignmentId: { $ref: "#/components/schemas/Uuid" }
        calculationId: { $ref: "#/components/schemas/Uuid" }
        approvalId: { $ref: "#/components/schemas/Uuid" }
        consumptionId: { $ref: "#/components/schemas/Uuid" }
        utilityType: { $ref: "#/components/schemas/UtilityBillableType" }
        periodStart: { type: string, format: date-time }
        periodEnd: { type: string, format: date-time }
        consumptionQuantity: { type: number }
        uomId: { $ref: "#/components/schemas/Uuid" }
        tariffRate: { type: number }
        billAmount: { type: number }
        currency: { type: string }
        dueDate: { type: string, format: date }
        status: { $ref: "#/components/schemas/UtilityBillStatus" }
    UpdateUtilityBillRequest:
      type: object
      minProperties: 1
      properties:
        dueDate: { type: string, format: date }
        status: { $ref: "#/components/schemas/UtilityBillStatus" }
    UtilityInvoiceReady:
      type: object
      required: [tenantCompanyId, billingPeriod, chargeDescription, utilityType, quantity, uomId, unitRate, amount, currency, utilityBillId, sourceCalculationId]
      properties:
        tenantCompanyId: { $ref: "#/components/schemas/Uuid" }
        billingPeriod:
          type: object
          required: [start, end]
          properties:
            start: { type: string, format: date-time }
            end: { type: string, format: date-time }
        chargeDescription: { type: string }
        utilityType: { $ref: "#/components/schemas/UtilityBillableType" }
        quantity: { type: number }
        uomId: { $ref: "#/components/schemas/Uuid" }
        unitRate: { type: number }
        amount: { type: number }
        currency: { type: string }
        utilityBillId: { $ref: "#/components/schemas/Uuid" }
        sourceCalculationId: { $ref: "#/components/schemas/Uuid" }

    CreateUtilityReconciliationRequest:
      type: object
      required: [utilityType, periodStart, periodEnd]
      properties:
        utilityType: { $ref: "#/components/schemas/UtilityBillableType" }
        periodStart: { type: string, format: date-time }
        periodEnd: { type: string, format: date-time }
    UtilityReconciliation:
      type: object
      required: [id, clientId, buildingId, utilityType, periodStart, periodEnd, uomId, sourceConsumptionIds, tenantConsumptionIds, commonAreaConsumptionIds, sourceConsumption, tenantConsumption, commonAreaConsumption, unallocatedConsumption, applicableAreaSqm, performanceMetric, performanceValue, performanceUom, calculatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        utilityType: { $ref: "#/components/schemas/UtilityBillableType" }
        periodStart: { type: string, format: date-time }
        periodEnd: { type: string, format: date-time }
        uomId: { $ref: "#/components/schemas/Uuid" }
        sourceConsumptionIds: { type: array, items: { $ref: "#/components/schemas/Uuid" } }
        tenantConsumptionIds: { type: array, items: { $ref: "#/components/schemas/Uuid" } }
        commonAreaConsumptionIds: { type: array, items: { $ref: "#/components/schemas/Uuid" } }
        sourceConsumption: { type: number }
        tenantConsumption: { type: number }
        commonAreaConsumption: { type: number }
        unallocatedConsumption: { type: number, description: May be negative; totals are never forced to balance. }
        reconciliationPercentage: { type: number, nullable: true }
        applicableAreaSqm: { type: number, exclusiveMinimum: 0 }
        performanceMetric: { type: string, enum: [IKE, IKA] }
        performanceValue: { type: number }
        performanceUom: { type: string, enum: [kWh/m², m³/m²] }
        calculatedAt: { type: string, format: date-time }
        calculatedByUserId: { $ref: "#/components/schemas/Uuid" }

    BuildingUtilityOperationalSummaryEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/BuildingUtilityOperationalSummary" } } }] }
    UtilityExceptionEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityOperationalException" } } }] }
    UtilityExceptionListEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/UtilityOperationalException" } } } }] }
    UtilityReadingDueEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityReadingDue" } } }] }
    UtilityReadingDueListEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/UtilityReadingDue" } } } }] }
    UtilityOcrCandidateEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityOcrCandidate" } } }] }
    UtilityMeterEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityMeter" } } }] }
    UtilityMeterListEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/UtilityMeter" } } } }] }
    UtilityHierarchyEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityMeterHierarchy" } } }] }
    UtilityHierarchyListEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/UtilityMeterHierarchy" } } } }] }
    UtilityReadingEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityMeterReading" } } }] }
    UtilityReadingListEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/UtilityMeterReading" } } } }] }
    UtilityEvidenceEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityReadingEvidence" } } }] }
    UtilityEvidenceListEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/UtilityReadingEvidence" } } } }] }
    UtilityEvidenceReadinessEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityEvidenceReadiness" } } }] }
    UtilityConsumptionEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityConsumption" } } }] }
    UtilityConsumptionListEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/UtilityConsumption" } } } }] }
    UtilityTariffEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityTariff" } } }] }
    UtilityTariffListEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/UtilityTariff" } } } }] }
    UtilityCalculationEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityCalculation" } } }] }
    UtilityCalculationListEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/UtilityCalculation" } } } }] }
    TenantUtilityApprovalEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/TenantUtilityApproval" } } }] }
    TenantUtilityApprovalListEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/TenantUtilityApproval" } } } }] }
    TenantApprovalActionsEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/TenantApprovalActions" } } }] }
    UtilityApprovalContextEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityApprovalContext" } } }] }
    UtilityBillEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityBill" } } }] }
    UtilityBillListEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/UtilityBill" } } } }] }
    UtilityInvoiceReadyEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityInvoiceReady" } } }] }
    UtilityReconciliationEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityReconciliation" } } }] }
    UtilityReconciliationListEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/UtilityReconciliation" } } } }] }
    # ------------------------------------------------------------------
    # Envelope
    # ------------------------------------------------------------------
    SuccessEnvelope:
      type: object
      description: >
        Standard success envelope. `data` holds the payload; `meta` carries
        page/context metadata and is an empty object when not applicable.
      required: [success, data, meta]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: object
          description: Response payload. Concrete shape is per-endpoint.
        meta:
          type: object
          description: Pagination/context metadata (see PaginationMeta).

    ErrorEnvelope:
      type: object
      description: >
        Standard error envelope (BE-25K mobile error contract). Never
        contains stack traces or sensitive details. `code` / `message` /
        `details` are unchanged (Web compatible); `category`, `retryable`,
        `requestId`, `resource` and `conflict` are additive mobile-facing
        fields.
      required: [success, error]
      properties:
        success:
          type: boolean
          const: false
        error:
          type: object
          required: [code, message]
          properties:
            code:
              type: string
              description: Stable machine-readable error code (see src/shared/errors.ts).
            message:
              type: string
              description: Human-readable message.
            details:
              type: array
              description: Optional field-level validation details.
              items:
                $ref: "#/components/schemas/ErrorDetail"
            category:
              type: string
              description: >
                BE-25K stable machine-readable classification:
                VALIDATION / BAD_REQUEST / UNAUTHORIZED / FORBIDDEN /
                NOT_FOUND / CONFLICT / RATE_LIMITED / SERVER. Derived from
                the HTTP status and code.
              enum: [VALIDATION, BAD_REQUEST, UNAUTHORIZED, FORBIDDEN, NOT_FOUND, CONFLICT, RATE_LIMITED, SERVER]
            retryable:
              type: boolean
              description: >
                BE-25K retry flag: true for 5xx and 429 (safe/meaningful to
                retry), false for client errors (correct the request,
                re-authenticate, or reload per conflict guidance).
            requestId:
              type: string
              format: uuid
              description: >
                Server-generated HTTP request correlation UUID. Present on
                error responses after request entry and equal to the
                `X-Request-ID` response header.
            resource:
              type: object
              description: BE-25K resource/context reference where useful.
              required: [type, id]
              properties:
                type:
                  type: string
                  description: e.g. CHECKLIST_EXECUTION, EVIDENCE_SUBMISSION, FINDING.
                id:
                  type: string
                  format: uuid
            conflict:
              type: object
              description: >
                BE-25K conflict metadata where applicable (same shape as the
                BE-25I sync conflict payload): current server state +
                retry/reload guidance. Never a merged result.
              properties:
                current:
                  description: Current authoritative server state/reference.
                guidance:
                  type: object
                  required: [action, reloadEndpoint, message]
                  properties:
                    action:
                      type: string
                      const: reload
                    reloadEndpoint:
                      type: string
                    message:
                      type: string

    ErrorDetail:
      type: object
      description: Field-level validation detail.
      required: [field, message]
      properties:
        field:
          type: string
        message:
          type: string

    # ------------------------------------------------------------------
    # Pagination
    # ------------------------------------------------------------------
    PaginationMeta:
      type: object
      description: >
        Platform pagination metadata emitted in `meta` for paginated list
        endpoints (precedent: GET /auth/audit-events).
      required: [limit, offset, total]
      properties:
        limit:
          type: integer
          description: Effective page size applied (default 50, max 100).
        offset:
          type: integer
          description: Number of items skipped.
        total:
          type: integer
          description: Total number of items matching the filters.

    # ------------------------------------------------------------------
    # Common identifiers / context refs
    # ------------------------------------------------------------------
    Uuid:
      type: string
      format: uuid
      description: Universally unique identifier.

    # ------------------------------------------------------------------
    # CR-BE-BAST-01 — canonical lifecycle and compatibility contracts
    # ------------------------------------------------------------------
    BastStatus:
      type: string
      enum: [DRAFT, SUBMITTED, ACCEPTED, REJECTED]
    BastContextType:
      type: string
      enum: [INTERNAL, TENANT, VENDOR]
    BastRequirement:
      type: string
      enum: [NONE, WORK_ORDER, EACH_VENDOR_WORK]
    BastAcceptanceScopeType:
      type: string
      enum: [WORK_ORDER, VENDOR_WORK]
      nullable: true

    CreateBastDocumentRequest:
      type: object
      required:
        [clientId, buildingId, contextType, title, documentNumber, documentType,
         workOrderId, bastNumber, bastDate]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        contextType: { $ref: "#/components/schemas/BastContextType" }
        sourceType:
          type: string
          enum: [TENANT_COMPANY, VENDOR, INTERNAL]
          nullable: true
        sourceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        title: { type: string, maxLength: 255 }
        description: { type: string, maxLength: 4000, nullable: true }
        fileReference: { type: string, maxLength: 512, nullable: true }
        status:
          type: string
          enum: [DRAFT, ACTIVE, INACTIVE]
        documentNumber: { type: string, maxLength: 100 }
        documentType:
          type: string
          enum: [BAST]
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        vendorWorkId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        workCompletionDocumentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        bastNumber: { type: string, maxLength: 100 }
        bastDate: { type: string, format: date }
        notes: { type: string, maxLength: 4000, nullable: true }

    SubmitBastDocumentRequest:
      type: object
      required: [documentVersionId]
      additionalProperties: false
      properties:
        documentVersionId: { $ref: "#/components/schemas/Uuid" }

    DecideBastDocumentRequest:
      type: object
      required: [decision]
      additionalProperties: false
      properties:
        decision:
          type: string
          enum: [ACCEPT, REJECT]
        notes: { type: string, maxLength: 4000, nullable: true }

    BastSourceDocument:
      type: object
      required:
        [id, clientId, buildingId, documentNumber, documentType, contextType,
         sourceType, sourceId, title, description, fileReference, status,
         expiryDate, archivedAt, archivedByUserId, archiveReason,
         statusBeforeArchive, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        documentNumber: { type: string }
        documentType: { type: string, enum: [BAST] }
        contextType: { $ref: "#/components/schemas/BastContextType" }
        sourceType:
          type: string
          enum: [TENANT_COMPANY, VENDOR, INTERNAL]
          nullable: true
        sourceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        title: { type: string }
        description: { type: string, nullable: true }
        fileReference: { type: string, nullable: true }
        status: { type: string, enum: [DRAFT, ACTIVE, INACTIVE, ARCHIVED] }
        expiryDate: { type: string, format: date, nullable: true }
        archivedAt: { type: string, format: date-time, nullable: true }
        archivedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        archiveReason: { type: string, nullable: true }
        statusBeforeArchive:
          type: string
          enum: [DRAFT, ACTIVE, INACTIVE]
          nullable: true
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    BastDocument:
      type: object
      description: >
        Canonical BE-22 BAST. `id` is the sole BAST identity; acceptance actor
        and timestamp fields are the current lifecycle projection while the
        immutable attempt/Sign-Off/Finding records preserve history.
      required:
        [id, documentId, workOrderId, vendorWorkId, workCompletionDocumentId,
         vendorId, completionReportId, serviceReportId, acceptanceScopeType,
         bastRequirement, clientId, buildingId, contextType, bastNumber,
         bastDate, acceptanceStatus, notes, fileReference, preparedByUserId,
         submittedByUserId, acceptedByUserId, submittedAt, acceptedAt,
         createdAt, updatedAt, document]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        documentId: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        vendorWorkId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        workCompletionDocumentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        vendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        completionReportId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        serviceReportId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        acceptanceScopeType: { $ref: "#/components/schemas/BastAcceptanceScopeType" }
        bastRequirement: { $ref: "#/components/schemas/BastRequirement" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        contextType: { $ref: "#/components/schemas/BastContextType" }
        bastNumber: { type: string }
        bastDate: { type: string, format: date }
        acceptanceStatus: { $ref: "#/components/schemas/BastStatus" }
        notes: { type: string, nullable: true }
        fileReference: { type: string, nullable: true }
        preparedByUserId: { $ref: "#/components/schemas/Uuid" }
        submittedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        acceptedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        submittedAt: { type: string, format: date-time, nullable: true }
        acceptedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        document: { $ref: "#/components/schemas/BastSourceDocument" }

    BastLifecycleCommandResult:
      type: object
      required:
        [bastDocument, submissionAttemptId, documentVersionId,
         acceptanceSignOffId, findingId]
      properties:
        bastDocument: { $ref: "#/components/schemas/BastDocument" }
        submissionAttemptId: { $ref: "#/components/schemas/Uuid" }
        documentVersionId: { $ref: "#/components/schemas/Uuid" }
        acceptanceSignOffId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        findingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true

    BastReconciliationClassification:
      type: string
      enum:
        [CANONICAL_ONLY, LEGACY_ONLY, LINKED_CONSISTENT, DIVERGENT_CONTEXT,
         DIVERGENT_LIFECYCLE, DUPLICATE_NUMBER, DUPLICATE_ACCEPTANCE_SCOPE,
         CONTRADICTORY_SIGN_OFF, MISSING_REFERENCE, MISSING_VERSION,
         CLOSED_BEFORE_ACCEPTED, ORPHAN, AMBIGUOUS_WORK_RELATIONSHIP,
         CROSS_SCOPE_SECURITY_MISMATCH]
    BastReconciliationItem:
      type: object
      required:
        [classification, severity, canonicalBastDocumentId,
         legacyVendorBastBindingId, bastNumber, message]
      properties:
        classification: { $ref: "#/components/schemas/BastReconciliationClassification" }
        severity: { type: string, enum: [INFO, WARNING, ERROR] }
        canonicalBastDocumentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        legacyVendorBastBindingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        bastNumber: { type: string, nullable: true }
        message: { type: string }
    BastReconciliationInventory:
      type: object
      required: [buildingId, generatedAt, readOnly, summary, items]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        generatedAt: { type: string, format: date-time }
        readOnly: { type: boolean, enum: [true] }
        summary:
          type: object
          required: [total, byClassification, bySeverity]
          properties:
            total: { type: integer, minimum: 0 }
            byClassification:
              type: object
              additionalProperties: { type: integer, minimum: 0 }
            bySeverity:
              type: object
              required: [INFO, WARNING, ERROR]
              properties:
                INFO: { type: integer, minimum: 0 }
                WARNING: { type: integer, minimum: 0 }
                ERROR: { type: integer, minimum: 0 }
        items:
          type: array
          items: { $ref: "#/components/schemas/BastReconciliationItem" }

    CreateLegacyVendorBastRequest:
      type: object
      deprecated: true
      required: [vendorWorkId, bastNumber, bastDate]
      properties:
        vendorWorkId: { $ref: "#/components/schemas/Uuid" }
        bastNumber: { type: string, maxLength: 100 }
        bastDate: { type: string, format: date }
        notes: { type: string, nullable: true, maxLength: 4000 }
        fileReference: { type: string, nullable: true, maxLength: 512 }
    LegacyBastDecisionRequest:
      type: object
      properties:
        notes: { type: string, nullable: true, maxLength: 4000 }

    LegacyVendorBastProjection:
      type: object
      description: >
        Compatibility projection only. `bastDocumentId` is populated only for
        a safe context-consistent canonical link; canonical lifecycle and
        Sign-Off values then override labelled legacy fallback state.
      required:
        [id, bastDocumentId, clientId, vendorWorkId, completionReportId,
         serviceReportId, workOrderId, buildingId, bastNumber, bastDate,
         preparedByUserId, submittedByUserId, acceptedByUserId,
         acceptanceStatus, notes, fileReference, submittedAt, acceptedAt,
         createdAt, updatedAt, compatibility, acceptanceSignOff]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        bastDocumentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        clientId: { $ref: "#/components/schemas/Uuid" }
        vendorWorkId: { $ref: "#/components/schemas/Uuid" }
        completionReportId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        serviceReportId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        bastNumber: { type: string }
        bastDate: { type: string, format: date }
        preparedByUserId: { $ref: "#/components/schemas/Uuid" }
        submittedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        acceptedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        acceptanceStatus: { $ref: "#/components/schemas/BastStatus" }
        notes: { type: string, nullable: true }
        fileReference: { type: string, nullable: true }
        submittedAt: { type: string, format: date-time, nullable: true }
        acceptedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        compatibility:
          type: object
          required: [lifecycleAuthority, reconciliationRequired, legacyAcceptanceStatus]
          properties:
            lifecycleAuthority:
              type: string
              enum: [CANONICAL_BAST, LEGACY_ONLY_FALLBACK, UNRESOLVED_CANONICAL_LINK]
            reconciliationRequired: { type: boolean }
            legacyAcceptanceStatus: { $ref: "#/components/schemas/BastStatus" }
        acceptanceSignOff:
          allOf: [{ $ref: "#/components/schemas/LegacyBastAcceptanceSignOff" }]
          nullable: true

    LegacyBastAcceptanceSignOff:
      type: object
      required:
        [id, bastSubmissionAttemptId, documentVersionId, decision,
         signerUserId, notes, signedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        bastSubmissionAttemptId: { $ref: "#/components/schemas/Uuid" }
        documentVersionId: { $ref: "#/components/schemas/Uuid" }
        decision: { type: string, enum: [ACCEPTED, REJECTED] }
        signerUserId: { $ref: "#/components/schemas/Uuid" }
        notes: { type: string, nullable: true }
        signedAt: { type: string, format: date-time }

    WorkOrderCloseResult:
      type: object
      required: [status]
      properties:
        status: { type: string, enum: [CLOSED] }

    # ------------------------------------------------------------------
    # BE-27A — Client Configuration
    # ------------------------------------------------------------------
    ClientConfigurationStatus:
      type: string
      description: >
        BE-27A record availability. This is not the BE-27N/O version/publish
        lifecycle.
      enum: [ACTIVE, INACTIVE]

    ClientConfigurationValue:
      description: >
        JSON-safe scalar, array, or object value (including null). Executable
        behavior and values larger than 65536 serialized bytes are rejected.
      nullable: true

    ClientConfiguration:
      type: object
      required: [id, clientId, key, value, status, createdAt, updatedAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        key:
          type: string
          maxLength: 100
          pattern: "^[A-Z][A-Z0-9_.-]*$"
        value:
          $ref: "#/components/schemas/ClientConfigurationValue"
        status:
          $ref: "#/components/schemas/ClientConfigurationStatus"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateClientConfigurationRequest:
      type: object
      additionalProperties: false
      required: [key, value]
      properties:
        key:
          type: string
          maxLength: 100
          description: Stable key; normalized to uppercase by the backend.
        value:
          $ref: "#/components/schemas/ClientConfigurationValue"
        status:
          $ref: "#/components/schemas/ClientConfigurationStatus"

    UpdateClientConfigurationRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      description: At least one of value or status is required; key and Client are immutable.
      properties:
        value:
          $ref: "#/components/schemas/ClientConfigurationValue"
        status:
          $ref: "#/components/schemas/ClientConfigurationStatus"

    EffectiveClientConfiguration:
      type: object
      required: [clientId, configurations]
      properties:
        clientId:
          $ref: "#/components/schemas/Uuid"
        configurations:
          type: object
          description: ACTIVE Client values keyed by normalized configuration key.
          additionalProperties:
            $ref: "#/components/schemas/ClientConfigurationValue"

    # ------------------------------------------------------------------
    # BE-27B — Building Configuration (reuses BE-27A value/status)
    # ------------------------------------------------------------------
    BuildingConfiguration:
      allOf:
        - $ref: "#/components/schemas/ClientConfiguration"
        - type: object
          required: [buildingId]
          properties:
            buildingId:
              $ref: "#/components/schemas/Uuid"

    CreateBuildingConfigurationRequest:
      allOf:
        - $ref: "#/components/schemas/CreateClientConfigurationRequest"

    UpdateBuildingConfigurationRequest:
      allOf:
        - $ref: "#/components/schemas/UpdateClientConfigurationRequest"

    EffectiveBuildingConfiguration:
      allOf:
        - $ref: "#/components/schemas/EffectiveClientConfiguration"
        - type: object
          required: [buildingId]
          properties:
            buildingId:
              $ref: "#/components/schemas/Uuid"

    # ------------------------------------------------------------------
    # BE-27C — Module Configuration
    # ------------------------------------------------------------------
    ModuleConfiguration:
      type: object
      required:
        [id, scopeType, clientId, buildingId, moduleId, moduleKey, moduleName,
         enabled, createdAt, updatedAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        scopeType:
          type: string
          enum: [CLIENT, BUILDING]
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          type: string
          format: uuid
          nullable: true
        moduleId:
          $ref: "#/components/schemas/Uuid"
        moduleKey:
          type: string
          description: Stable code from the existing Module catalogue.
        moduleName:
          type: string
        enabled:
          type: boolean
          description: Configured enablement intent; not a commercial grant.
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateModuleConfigurationRequest:
      type: object
      additionalProperties: false
      required: [moduleKey, enabled]
      properties:
        moduleKey:
          type: string
          minLength: 2
          maxLength: 64
          pattern: "^[A-Za-z][A-Za-z0-9_]*$"
          description: Existing Module code; normalized to uppercase.
        enabled:
          type: boolean

    UpdateModuleConfigurationRequest:
      type: object
      additionalProperties: false
      required: [enabled]
      properties:
        enabled:
          type: boolean

    EffectiveModuleConfigurationItem:
      type: object
      required:
        [moduleId, moduleKey, moduleName, configuredEnabled, entitled, enabled,
         source]
      properties:
        moduleId:
          $ref: "#/components/schemas/Uuid"
        moduleKey:
          type: string
        moduleName:
          type: string
        configuredEnabled:
          type: boolean
        entitled:
          type: boolean
          description: Result from the existing effective Module Entitlement resolver.
        enabled:
          type: boolean
          description: configuredEnabled AND entitled.
        source:
          type: string
          enum: [CLIENT, BUILDING]

    EffectiveModuleConfiguration:
      type: object
      required: [clientId, buildingId, modules]
      properties:
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          type: string
          format: uuid
          nullable: true
        modules:
          type: array
          items:
            $ref: "#/components/schemas/EffectiveModuleConfigurationItem"

    FeatureEntitlementState:
      type: string
      enum: [ENABLED, DISABLED]
    FeatureEntitlementConfiguration:
      type: object
      required: [id, scopeType, clientId, buildingId, moduleId, moduleKey, moduleName, featureKey, state, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        scopeType: { type: string, enum: [CLIENT, BUILDING] }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        moduleId: { $ref: "#/components/schemas/Uuid" }
        moduleKey: { type: string }
        moduleName: { type: string }
        featureKey: { type: string, maxLength: 100, pattern: "^[A-Z][A-Z0-9_.-]*$" }
        state: { $ref: "#/components/schemas/FeatureEntitlementState" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateFeatureEntitlementRequest:
      type: object
      additionalProperties: false
      required: [moduleKey, featureKey, state]
      properties:
        moduleKey: { type: string, minLength: 2, maxLength: 64 }
        featureKey: { type: string, maxLength: 100 }
        state: { $ref: "#/components/schemas/FeatureEntitlementState" }
    UpdateFeatureEntitlementRequest:
      type: object
      additionalProperties: false
      required: [state]
      properties:
        state: { $ref: "#/components/schemas/FeatureEntitlementState" }
    EffectiveFeatureEntitlementItem:
      type: object
      required: [moduleId, moduleKey, moduleName, featureKey, configuredState, moduleEnabled, state, source]
      properties:
        moduleId: { $ref: "#/components/schemas/Uuid" }
        moduleKey: { type: string }
        moduleName: { type: string }
        featureKey: { type: string }
        configuredState: { $ref: "#/components/schemas/FeatureEntitlementState" }
        moduleEnabled: { type: boolean }
        state: { $ref: "#/components/schemas/FeatureEntitlementState" }
        source: { type: string, enum: [CLIENT, BUILDING] }
    EffectiveFeatureEntitlementConfiguration:
      type: object
      required: [clientId, buildingId, features]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        features:
          type: array
          items: { $ref: "#/components/schemas/EffectiveFeatureEntitlementItem" }

    NavigationItem:
      type: object
      required: [id, scopeType, clientId, buildingId, navigationKey, label, routeReference, parentNavigationKey, displayOrder, moduleId, moduleKey, featureKey, permissionId, permissionCode, enabled]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        scopeType: { type: string, enum: [CLIENT, BUILDING] }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        navigationKey: { type: string, maxLength: 100 }
        label: { type: string, maxLength: 160 }
        routeReference: { type: string, maxLength: 512 }
        parentNavigationKey: { type: string, nullable: true, maxLength: 100 }
        displayOrder: { type: integer, minimum: 0 }
        moduleId: { type: string, format: uuid, nullable: true }
        moduleKey: { type: string, nullable: true }
        featureKey: { type: string, nullable: true }
        permissionId: { type: string, format: uuid, nullable: true }
        permissionCode: { type: string, nullable: true }
        enabled: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateNavigationItemRequest:
      type: object
      additionalProperties: false
      required: [navigationKey, label, routeReference]
      properties:
        navigationKey: { type: string, maxLength: 100 }
        label: { type: string, maxLength: 160 }
        routeReference: { type: string, maxLength: 512 }
        parentNavigationKey: { type: string, nullable: true, maxLength: 100 }
        displayOrder: { type: integer, minimum: 0, default: 0 }
        moduleKey: { type: string, nullable: true }
        featureKey: { type: string, nullable: true }
        permissionCode: { type: string, nullable: true }
        enabled: { type: boolean, default: true }
    UpdateNavigationItemRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        label: { type: string, maxLength: 160 }
        routeReference: { type: string, maxLength: 512 }
        displayOrder: { type: integer, minimum: 0 }
        enabled: { type: boolean }
    EffectiveNavigation:
      type: object
      required: [clientId, buildingId, items]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        items:
          type: array
          items: { $ref: "#/components/schemas/NavigationItem" }

    Workspace:
      type: object
      required: [id, scopeType, clientId, buildingId, workspaceKey, label, description, moduleId, moduleKey, featureKey, navigationKey, permissionId, permissionCode, enabled]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        scopeType: { type: string, enum: [CLIENT, BUILDING] }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        workspaceKey: { type: string, maxLength: 100 }
        label: { type: string, maxLength: 160 }
        description: { type: string, nullable: true, maxLength: 1000 }
        moduleId: { type: string, format: uuid, nullable: true }
        moduleKey: { type: string, nullable: true }
        featureKey: { type: string, nullable: true }
        navigationKey: { type: string, nullable: true }
        permissionId: { type: string, format: uuid, nullable: true }
        permissionCode: { type: string, nullable: true }
        enabled: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateWorkspaceRequest:
      type: object
      additionalProperties: false
      required: [workspaceKey, label]
      properties:
        workspaceKey: { type: string, maxLength: 100 }
        label: { type: string, maxLength: 160 }
        description: { type: string, nullable: true, maxLength: 1000 }
        moduleKey: { type: string, nullable: true }
        featureKey: { type: string, nullable: true }
        navigationKey: { type: string, nullable: true }
        permissionCode: { type: string, nullable: true }
        enabled: { type: boolean, default: true }
    UpdateWorkspaceRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        label: { type: string, maxLength: 160 }
        description: { type: string, nullable: true, maxLength: 1000 }
        enabled: { type: boolean }
    EffectiveWorkspaces:
      type: object
      required: [clientId, buildingId, workspaces]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        workspaces:
          type: array
          items: { $ref: "#/components/schemas/Workspace" }

    Dashboard:
      type: object
      required: [id, scopeType, clientId, buildingId, dashboardKey, label, workspaceKey, metadata, enabled]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        scopeType: { type: string, enum: [CLIENT, BUILDING] }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        dashboardKey: { type: string, maxLength: 100 }
        label: { type: string, maxLength: 160 }
        workspaceKey: { type: string, maxLength: 100 }
        metadata: { type: object, additionalProperties: true }
        enabled: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    DashboardWidget:
      type: object
      required: [id, dashboardId, clientId, buildingId, widgetKey, label, sourceReference, displayOrder, moduleId, moduleKey, featureKey, permissionId, permissionCode, metadata, enabled]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        dashboardId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        widgetKey: { type: string, maxLength: 100 }
        label: { type: string, maxLength: 160 }
        sourceReference: { type: string, description: Approved BE-23/BE-24 read-model reference }
        displayOrder: { type: integer, minimum: 0 }
        moduleId: { type: string, format: uuid, nullable: true }
        moduleKey: { type: string, nullable: true }
        featureKey: { type: string, nullable: true }
        permissionId: { type: string, format: uuid, nullable: true }
        permissionCode: { type: string, nullable: true }
        metadata: { type: object, additionalProperties: true }
        enabled: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateDashboardRequest:
      type: object
      additionalProperties: false
      required: [dashboardKey, label, workspaceKey]
      properties:
        dashboardKey: { type: string }
        label: { type: string }
        workspaceKey: { type: string }
        metadata: { type: object }
        enabled: { type: boolean, default: true }
    UpdateDashboardRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        label: { type: string }
        metadata: { type: object }
        enabled: { type: boolean }
    CreateDashboardWidgetRequest:
      type: object
      additionalProperties: false
      required: [widgetKey, label, sourceReference]
      properties:
        widgetKey: { type: string }
        label: { type: string }
        sourceReference: { type: string }
        displayOrder: { type: integer, minimum: 0, default: 0 }
        moduleKey: { type: string, nullable: true }
        featureKey: { type: string, nullable: true }
        permissionCode: { type: string, nullable: true }
        metadata: { type: object }
        enabled: { type: boolean, default: true }
    UpdateDashboardWidgetRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        label: { type: string }
        displayOrder: { type: integer, minimum: 0 }
        metadata: { type: object }
        enabled: { type: boolean }
    EffectiveDashboard:
      allOf:
        - $ref: "#/components/schemas/Dashboard"
        - type: object
          required: [widgets]
          properties:
            widgets:
              type: array
              items: { $ref: "#/components/schemas/DashboardWidget" }
    EffectiveDashboards:
      type: object
      required: [clientId, buildingId, dashboards]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        dashboards:
          type: array
          items: { $ref: "#/components/schemas/EffectiveDashboard" }

    AdminFormField:
      type: object
      required: [id, formSectionId, code, label, fieldType, required, displayOrder, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        formSectionId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        label: { type: string }
        fieldType: { type: string, enum: [TEXT, TEXTAREA, NUMBER, DATE, DATETIME, BOOLEAN, SELECT] }
        required: { type: boolean }
        displayOrder: { type: integer, minimum: 0 }
        placeholder: { type: string, nullable: true }
        helpText: { type: string, nullable: true }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        uomId: { type: string, format: uuid, nullable: true }
        minimumValue: { type: string, nullable: true }
        maximumValue: { type: string, nullable: true }
        decimalPrecision: { type: integer, nullable: true }
    AdminFormSection:
      type: object
      required: [id, formTemplateId, code, title, displayOrder, status, fields]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        formTemplateId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        title: { type: string }
        description: { type: string, nullable: true }
        displayOrder: { type: integer }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        fields: { type: array, items: { $ref: "#/components/schemas/AdminFormField" } }
    AdminFormDefinition:
      type: object
      required: [template, bindings, sections]
      properties:
        template: { type: object, description: Existing BE-07 PublicFormTemplate }
        bindings: { type: array, items: { type: object } }
        sections: { type: array, items: { $ref: "#/components/schemas/AdminFormSection" } }
    CreateAdminFormTemplateRequest:
      type: object
      required: [sourceFormId, code, name]
      properties:
        sourceFormId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        description: { type: string }
        status: { type: string, enum: [DRAFT, ACTIVE, INACTIVE] }
    UpdateAdminFormTemplateRequest:
      type: object
      properties:
        name: { type: string }
        description: { type: string, nullable: true }
        status: { type: string, enum: [DRAFT, ACTIVE, INACTIVE] }
    CreateAdminFormSectionRequest:
      type: object
      required: [code, title]
      properties:
        code: { type: string }
        title: { type: string }
        description: { type: string, nullable: true }
        displayOrder: { type: integer, minimum: 0 }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
    UpdateAdminFormSectionRequest:
      type: object
      properties:
        title: { type: string }
        description: { type: string, nullable: true }
        displayOrder: { type: integer, minimum: 0 }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
    CreateAdminFormFieldRequest:
      type: object
      required: [code, label, fieldType]
      properties:
        code: { type: string }
        label: { type: string }
        fieldType: { type: string, enum: [TEXT, TEXTAREA, NUMBER, DATE, DATETIME, BOOLEAN, SELECT] }
        required: { type: boolean }
        displayOrder: { type: integer, minimum: 0 }
        placeholder: { type: string, nullable: true }
        helpText: { type: string, nullable: true }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        uomId: { type: string, format: uuid, nullable: true }
        minimumValue: { type: number, nullable: true }
        maximumValue: { type: number, nullable: true }
        decimalPrecision: { type: integer, nullable: true }
    UpdateAdminFormFieldRequest:
      type: object
      minProperties: 1
      properties:
        label: { type: string }
        fieldType: { type: string, enum: [TEXT, TEXTAREA, NUMBER, DATE, DATETIME, BOOLEAN, SELECT] }
        required: { type: boolean }
        displayOrder: { type: integer, minimum: 0 }
        placeholder: { type: string, nullable: true }
        helpText: { type: string, nullable: true }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        uomId: { type: string, format: uuid, nullable: true }
        minimumValue: { type: number, nullable: true }
        maximumValue: { type: number, nullable: true }
        decimalPrecision: { type: integer, nullable: true }

    AdminChecklistTemplate:
      type: object
      required: [id, clientId, code, name, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { type: string, enum: [DRAFT, ACTIVE, INACTIVE] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    ChecklistConfigurationBinding:
      type: object
      required: [id, checklistTemplateId, scopeType, clientId, buildingId, createdAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        scopeType: { type: string, enum: [CLIENT, BUILDING] }
        clientId: { type: string, format: uuid, nullable: true }
        buildingId: { type: string, format: uuid, nullable: true }
        createdAt: { type: string, format: date-time }
    AdminChecklistItem:
      type: object
      description: Existing Checklist Item. Measurement fields are read-only in Checklist Administration.
      required: [id, checklistTemplateId, code, label, itemType, required, displayOrder, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        label: { type: string }
        itemType: { type: string, enum: [CHECK, BOOLEAN, TEXT, NUMBER] }
        required: { type: boolean }
        displayOrder: { type: integer, minimum: 0 }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        uomId: { type: string, format: uuid, nullable: true, readOnly: true }
        minimumValue: { type: string, nullable: true, readOnly: true }
        maximumValue: { type: string, nullable: true, readOnly: true }
        decimalPrecision: { type: integer, nullable: true, readOnly: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    AdminChecklistDefinition:
      type: object
      required: [template, bindings, items]
      properties:
        template: { $ref: "#/components/schemas/AdminChecklistTemplate" }
        bindings: { type: array, items: { $ref: "#/components/schemas/ChecklistConfigurationBinding" } }
        items: { type: array, items: { $ref: "#/components/schemas/AdminChecklistItem" } }
    CreateAdminChecklistTemplateRequest:
      type: object
      additionalProperties: false
      required: [code, name]
      properties:
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { type: string, enum: [DRAFT, ACTIVE, INACTIVE] }
    UpdateAdminChecklistTemplateRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        name: { type: string }
        description: { type: string, nullable: true }
        status: { type: string, enum: [DRAFT, ACTIVE, INACTIVE] }
    CreateAdminChecklistItemRequest:
      type: object
      additionalProperties: false
      required: [code, label, itemType]
      properties:
        code: { type: string }
        label: { type: string }
        itemType: { type: string, enum: [CHECK, BOOLEAN, TEXT, NUMBER] }
        required: { type: boolean }
        displayOrder: { type: integer, minimum: 0 }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
    UpdateAdminChecklistItemRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        label: { type: string }
        itemType: { type: string, enum: [CHECK, BOOLEAN, TEXT, NUMBER] }
        required: { type: boolean }
        displayOrder: { type: integer, minimum: 0 }
        status: { type: string, enum: [ACTIVE, INACTIVE] }

    OperationalSettingKey:
      type: string
      description: Typed, allowlisted operational metadata key.
      enum: [SCHEDULE.DEFAULT_TIMEZONE]
    OperationalSetting:
      type: object
      required: [id, clientId, buildingId, scopeType, key, value, enabled, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        scopeType: { type: string, enum: [CLIENT, BUILDING] }
        key: { $ref: "#/components/schemas/OperationalSettingKey" }
        value:
          type: string
          description: IANA timezone for SCHEDULE.DEFAULT_TIMEZONE.
          example: Asia/Jakarta
        enabled: { type: boolean }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateOperationalSettingRequest:
      type: object
      additionalProperties: false
      required: [key, value]
      properties:
        key: { $ref: "#/components/schemas/OperationalSettingKey" }
        value:
          type: string
          description: Valid IANA timezone.
          example: Asia/Jakarta
        enabled: { type: boolean, default: true }
        status: { type: string, enum: [ACTIVE, INACTIVE], default: ACTIVE }
    UpdateOperationalSettingRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        value:
          type: string
          description: Valid IANA timezone.
          example: Asia/Makassar
        enabled: { type: boolean }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
    EffectiveOperationalSetting:
      type: object
      required: [key, value, enabled, status]
      properties:
        key: { $ref: "#/components/schemas/OperationalSettingKey" }
        value: { type: string, example: Asia/Jakarta }
        enabled: { type: boolean }
        status: { type: string, enum: [ACTIVE] }
    EffectiveOperationalSettings:
      type: object
      required: [clientId, buildingId, settings]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        settings:
          type: array
          items: { $ref: "#/components/schemas/EffectiveOperationalSetting" }

    OrganizationPresentationLabels:
      type: object
      additionalProperties: false
      required: [organization, department, team, position]
      properties:
        organization: { type: string, nullable: true, maxLength: 160 }
        department: { type: string, nullable: true, maxLength: 160 }
        team: { type: string, nullable: true, maxLength: 160 }
        position: { type: string, nullable: true, maxLength: 160 }
    OrganizationPresentationItem:
      type: object
      additionalProperties: false
      required: [entityType, entityId, displayName, displayOrder, visible]
      properties:
        entityType: { type: string, enum: [ORGANIZATION, DEPARTMENT, TEAM, POSITION] }
        entityId: { $ref: "#/components/schemas/Uuid" }
        displayName: { type: string, nullable: true, maxLength: 160 }
        displayOrder: { type: integer, minimum: 0, maximum: 1000000 }
        visible: { type: boolean }
    OrganizationPresentationConfiguration:
      type: object
      required: [id, clientId, buildingId, scopeType, labels, items, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        scopeType: { type: string, enum: [CLIENT, BUILDING] }
        labels: { $ref: "#/components/schemas/OrganizationPresentationLabels" }
        items:
          type: array
          items: { $ref: "#/components/schemas/OrganizationPresentationItem" }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateOrganizationPresentationRequest:
      type: object
      additionalProperties: false
      properties:
        labels:
          type: object
          additionalProperties: false
          properties:
            organization: { type: string, nullable: true, maxLength: 160 }
            department: { type: string, nullable: true, maxLength: 160 }
            team: { type: string, nullable: true, maxLength: 160 }
            position: { type: string, nullable: true, maxLength: 160 }
        items:
          type: array
          maxItems: 1000
          items:
            type: object
            additionalProperties: false
            required: [entityType, entityId]
            properties:
              entityType: { type: string, enum: [ORGANIZATION, DEPARTMENT, TEAM, POSITION] }
              entityId: { $ref: "#/components/schemas/Uuid" }
              displayName: { type: string, nullable: true, maxLength: 160 }
              displayOrder: { type: integer, minimum: 0, maximum: 1000000 }
              visible: { type: boolean, default: true }
        status: { type: string, enum: [ACTIVE, INACTIVE], default: ACTIVE }
    UpdateOrganizationPresentationRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        labels:
          type: object
          additionalProperties: false
          minProperties: 1
          properties:
            organization: { type: string, nullable: true, maxLength: 160 }
            department: { type: string, nullable: true, maxLength: 160 }
            team: { type: string, nullable: true, maxLength: 160 }
            position: { type: string, nullable: true, maxLength: 160 }
        items:
          type: array
          maxItems: 1000
          items: { $ref: "#/components/schemas/OrganizationPresentationItem" }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
    EffectiveOrganizationPresentationItem:
      allOf:
        - $ref: "#/components/schemas/OrganizationPresentationItem"
        - type: object
          required: [code, authoritativeName, effectiveDisplayName, organizationId, departmentId, available]
          properties:
            code: { type: string }
            authoritativeName: { type: string }
            effectiveDisplayName: { type: string }
            organizationId: { $ref: "#/components/schemas/Uuid" }
            departmentId: { type: string, format: uuid, nullable: true }
            available: { type: boolean }
    EffectiveOrganizationPresentation:
      type: object
      required: [clientId, buildingId, labels, items]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        labels:
          type: object
          required: [organization, department, team, position]
          properties:
            organization: { type: string }
            department: { type: string }
            team: { type: string }
            position: { type: string }
        items:
          type: array
          items: { $ref: "#/components/schemas/EffectiveOrganizationPresentationItem" }

    CmsContentType:
      type: string
      enum: [ANNOUNCEMENT, HELP, FAQ, KNOWLEDGE, PORTAL_CONTENT, RELEASE_INFORMATION]
    CmsContentStatus:
      type: string
      enum: [DRAFT, PUBLISHED, INACTIVE]
    CmsContent:
      type: object
      required: [id, clientId, buildingId, scopeType, contentType, slug, title, body, status, createdByUserId, publishedAt, publishedByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        scopeType: { type: string, enum: [CLIENT, BUILDING] }
        contentType: { $ref: "#/components/schemas/CmsContentType" }
        slug: { type: string, maxLength: 120, pattern: "^[a-z0-9]+([._-][a-z0-9]+)*$" }
        title: { type: string, maxLength: 240 }
        body:
          type: string
          maxLength: 100000
          description: Safe plain text/Markdown. Raw HTML, executable script, event handlers, and unsafe URL schemes are rejected.
        status: { $ref: "#/components/schemas/CmsContentStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        publishedAt: { type: string, format: date-time, nullable: true }
        publishedByUserId: { type: string, format: uuid, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateCmsContentRequest:
      type: object
      additionalProperties: false
      required: [contentType, slug, title, body]
      properties:
        contentType: { $ref: "#/components/schemas/CmsContentType" }
        slug: { type: string, maxLength: 120, pattern: "^[a-z0-9]+([._-][a-z0-9]+)*$" }
        title: { type: string, maxLength: 240 }
        body:
          type: string
          maxLength: 100000
          description: Safe plain text/Markdown only; raw HTML/JS is not accepted.
        status: { $ref: "#/components/schemas/CmsContentStatus" }
    UpdateCmsContentRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        title: { type: string, maxLength: 240 }
        body:
          type: string
          maxLength: 100000
          description: Safe plain text/Markdown only; raw HTML/JS is not accepted.
        status: { $ref: "#/components/schemas/CmsContentStatus" }
    EffectiveCmsContent:
      type: object
      required: [clientId, buildingId, content]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        content:
          type: array
          items: { $ref: "#/components/schemas/CmsContent" }

    LoginBranding:
      type: object
      additionalProperties: false
      required: [title, subtitle, showLogo]
      properties:
        title: { type: string, nullable: true, maxLength: 160 }
        subtitle: { type: string, nullable: true, maxLength: 240 }
        showLogo: { type: boolean }
    PortalBranding:
      type: object
      additionalProperties: false
      required: [headerTitle, showLogo]
      properties:
        headerTitle: { type: string, nullable: true, maxLength: 160 }
        showLogo: { type: boolean }
    ReportBranding:
      type: object
      additionalProperties: false
      required: [headerText, footerText, showLogo]
      properties:
        headerText: { type: string, nullable: true, maxLength: 240 }
        footerText: { type: string, nullable: true, maxLength: 500 }
        showLogo: { type: boolean }
    BrandingThemeTokens:
      type: object
      additionalProperties: false
      required: [primaryColor, secondaryColor, accentColor, backgroundColor, surfaceColor, textColor, fontFamily, borderRadius]
      properties:
        primaryColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
        secondaryColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
        accentColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
        backgroundColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
        surfaceColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
        textColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
        fontFamily: { type: string, enum: [SYSTEM, INTER, ROBOTO, OPEN_SANS, LATO] }
        borderRadius: { type: string, enum: [NONE, SMALL, MEDIUM, LARGE] }
    BrandingProfile:
      type: object
      required: [brandName, logoReference, login, portal, report, theme]
      properties:
        brandName: { type: string, maxLength: 160 }
        logoReference:
          type: string
          nullable: true
          maxLength: 512
          description: Opaque backend-managed file/storage reference. The API does not upload, generate, or alter logo bytes.
        login: { $ref: "#/components/schemas/LoginBranding" }
        portal: { $ref: "#/components/schemas/PortalBranding" }
        report: { $ref: "#/components/schemas/ReportBranding" }
        theme: { $ref: "#/components/schemas/BrandingThemeTokens" }
    BrandingConfiguration:
      allOf:
        - $ref: "#/components/schemas/BrandingProfile"
        - type: object
          required: [id, clientId, buildingId, scopeType, status, createdAt, updatedAt]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            clientId: { $ref: "#/components/schemas/Uuid" }
            buildingId: { type: string, format: uuid, nullable: true }
            scopeType: { type: string, enum: [CLIENT, BUILDING] }
            status: { type: string, enum: [ACTIVE, INACTIVE] }
            createdAt: { type: string, format: date-time }
            updatedAt: { type: string, format: date-time }
    CreateBrandingRequest:
      type: object
      additionalProperties: false
      required: [brandName]
      properties:
        brandName: { type: string, maxLength: 160 }
        logoReference: { type: string, nullable: true, maxLength: 512 }
        login:
          type: object
          additionalProperties: false
          properties:
            title: { type: string, nullable: true, maxLength: 160 }
            subtitle: { type: string, nullable: true, maxLength: 240 }
            showLogo: { type: boolean }
        portal:
          type: object
          additionalProperties: false
          properties:
            headerTitle: { type: string, nullable: true, maxLength: 160 }
            showLogo: { type: boolean }
        report:
          type: object
          additionalProperties: false
          properties:
            headerText: { type: string, nullable: true, maxLength: 240 }
            footerText: { type: string, nullable: true, maxLength: 500 }
            showLogo: { type: boolean }
        theme:
          type: object
          additionalProperties: false
          properties:
            primaryColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
            secondaryColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
            accentColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
            backgroundColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
            surfaceColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
            textColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
            fontFamily: { type: string, enum: [SYSTEM, INTER, ROBOTO, OPEN_SANS, LATO] }
            borderRadius: { type: string, enum: [NONE, SMALL, MEDIUM, LARGE] }
        status: { type: string, enum: [ACTIVE, INACTIVE], default: ACTIVE }
    UpdateBrandingRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        brandName: { type: string, maxLength: 160 }
        logoReference: { type: string, nullable: true, maxLength: 512 }
        login:
          type: object
          additionalProperties: false
          minProperties: 1
          properties:
            title: { type: string, nullable: true, maxLength: 160 }
            subtitle: { type: string, nullable: true, maxLength: 240 }
            showLogo: { type: boolean }
        portal:
          type: object
          additionalProperties: false
          minProperties: 1
          properties:
            headerTitle: { type: string, nullable: true, maxLength: 160 }
            showLogo: { type: boolean }
        report:
          type: object
          additionalProperties: false
          minProperties: 1
          properties:
            headerText: { type: string, nullable: true, maxLength: 240 }
            footerText: { type: string, nullable: true, maxLength: 500 }
            showLogo: { type: boolean }
        theme:
          type: object
          additionalProperties: false
          minProperties: 1
          properties:
            primaryColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
            secondaryColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
            accentColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
            backgroundColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
            surfaceColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
            textColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
            fontFamily: { type: string, enum: [SYSTEM, INTER, ROBOTO, OPEN_SANS, LATO] }
            borderRadius: { type: string, enum: [NONE, SMALL, MEDIUM, LARGE] }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
    EffectiveBranding:
      type: object
      required: [clientId, buildingId, branding]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        branding:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/BrandingProfile"

    ConfigurationVersionSourceType:
      type: string
      enum:
        - CLIENT_CONFIGURATION
        - BUILDING_CONFIGURATION
        - MODULE_CONFIGURATION
        - FEATURE_ENTITLEMENT_CONFIGURATION
        - NAVIGATION_ITEM
        - WORKSPACE
        - DASHBOARD
        - DASHBOARD_WIDGET
        - CMS_CONTENT
    ConfigurationLifecycleStatus:
      type: string
      enum: [DRAFT, VALIDATED, PUBLISHED, ACTIVE, SUPERSEDED]
    ConfigurationVersion:
      type: object
      description: Immutable source snapshot with separately controlled lifecycle metadata. Status mirrors the source record; lifecycleStatus is BE-27O state.
      required: [id, sourceType, sourceConfigurationId, clientId, buildingId, versionNumber, status, lifecycleStatus, previousVersionId, snapshot, createdByUserId, createdAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        sourceType: { $ref: "#/components/schemas/ConfigurationVersionSourceType" }
        sourceConfigurationId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        versionNumber: { type: integer, minimum: 1 }
        status: { type: string, maxLength: 64 }
        lifecycleStatus: { $ref: "#/components/schemas/ConfigurationLifecycleStatus" }
        previousVersionId: { type: string, format: uuid, nullable: true }
        snapshot:
          description: JSON-safe immutable source snapshot, capped at 1 MiB.
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
    ConfigurationValidationError:
      type: object
      required: [code, field, message]
      properties:
        code: { type: string }
        field: { type: string }
        message: { type: string }
    ConfigurationValidation:
      type: object
      required: [id, configurationVersionId, valid, errors, validatedByUserId, validatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        configurationVersionId: { $ref: "#/components/schemas/Uuid" }
        valid: { type: boolean }
        errors:
          type: array
          items: { $ref: "#/components/schemas/ConfigurationValidationError" }
        validatedByUserId: { $ref: "#/components/schemas/Uuid" }
        validatedAt: { type: string, format: date-time }
    ConfigurationValidationOutcome:
      type: object
      required: [version, validation]
      properties:
        version: { $ref: "#/components/schemas/ConfigurationVersion" }
        validation: { $ref: "#/components/schemas/ConfigurationValidation" }
    ConfigurationPreviewStatus:
      type: string
      enum: [ACTIVE, REVOKED, EXPIRED]
    ConfigurationPreviewContext:
      type: object
      required: [id, configurationVersionId, clientId, buildingId, createdByUserId, mode, status, expiresAt, revokedAt, revokedByUserId, createdAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        configurationVersionId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        mode: { type: string, enum: [PREVIEW] }
        status: { $ref: "#/components/schemas/ConfigurationPreviewStatus" }
        expiresAt: { type: string, format: date-time }
        revokedAt: { type: string, format: date-time, nullable: true }
        revokedByUserId: { type: string, format: uuid, nullable: true }
        createdAt: { type: string, format: date-time }
    CreateConfigurationPreviewRequest:
      type: object
      additionalProperties: false
      properties:
        expiresInMinutes: { type: integer, minimum: 1, maximum: 120, default: 15 }
    PreviewEffectiveConfiguration:
      type: object
      required: [clientId, buildingId, configurations]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        configurations: { type: object, additionalProperties: true }
    EffectiveConfigurationPreview:
      type: object
      required: [mode, context, version, sourceSnapshot, configuration, navigation, workspaces]
      properties:
        mode: { type: string, enum: [PREVIEW] }
        context: { $ref: "#/components/schemas/ConfigurationPreviewContext" }
        version: { $ref: "#/components/schemas/ConfigurationVersion" }
        sourceSnapshot:
          description: Exact immutable non-active version snapshot under preview.
        configuration:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/PreviewEffectiveConfiguration"
        navigation:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/EffectiveNavigation"
        workspaces:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/EffectiveWorkspaces"

    ConfigurationAuditAction:
      type: string
      enum:
        - CONFIGURATION_DRAFT_CREATED
        - CONFIGURATION_DRAFT_UPDATED
        - CONFIGURATION_VALIDATION_SUCCEEDED
        - CONFIGURATION_VALIDATION_FAILED
        - CONFIGURATION_PUBLISHED
        - CONFIGURATION_ACTIVATED
        - CONFIGURATION_SUPERSEDED
        - CONFIGURATION_PREVIEW_CREATED
        - CONFIGURATION_PREVIEW_ACCESSED
        - CONFIGURATION_PREVIEW_REVOKED
    ConfigurationAuditEvent:
      type: object
      required: [id, configurationId, configurationVersionId, sourceType, action, actorUserId, clientId, buildingId, previousStatus, newStatus, summary, metadata, occurredAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        configurationId: { $ref: "#/components/schemas/Uuid" }
        configurationVersionId: { type: string, format: uuid, nullable: true }
        sourceType: { $ref: "#/components/schemas/ConfigurationVersionSourceType" }
        action: { $ref: "#/components/schemas/ConfigurationAuditAction" }
        actorUserId: { type: string, format: uuid, nullable: true }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        previousStatus: { type: string, nullable: true }
        newStatus: { type: string, nullable: true }
        summary: { type: string, maxLength: 500 }
        metadata:
          type: object
          description: Compact allowlisted identifiers/status metadata only; snapshots, content, credentials, secrets, and tokens are never stored.
        occurredAt: { type: string, format: date-time }

    ClientRef:
      type: object
      required: [id, code, name]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string

    PropertyRef:
      type: object
      required: [id, code, name]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string

    BuildingRef:
      type: object
      required: [id, code, name]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string

    # ------------------------------------------------------------------
    # Health
    # ------------------------------------------------------------------
    HealthStatus:
      type: object
      required: [status, environment, uptimeSeconds, timestamp]
      properties:
        status:
          type: string
          const: ok
        environment:
          type: string
          description: NODE_ENV value (development | test | production).
        uptimeSeconds:
          type: integer
        timestamp:
          type: string
          format: date-time

    DatabaseHealth:
      type: object
      required: [status, database]
      properties:
        status:
          type: string
          const: ok
        database:
          type: string
          const: connected

    # ------------------------------------------------------------------
    # Authentication / session
    # ------------------------------------------------------------------
    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password
          description: Raw password; never returned by any endpoint.

    LoginResponse:
      type: object
      required: [sessionToken, expiresAt, user]
      properties:
        sessionToken:
          type: string
          description: "Opaque session token; send as Authorization: Bearer."
        expiresAt:
          type: string
          format: date-time
          description: Session expiry in ISO 8601 UTC.
        user:
          $ref: "#/components/schemas/PublicUser"

    LogoutResponse:
      type: object
      required: [revoked]
      properties:
        revoked:
          type: boolean
          const: true

    PublicUser:
      type: object
      required: [id, email, displayName, status, createdAt, updatedAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        email:
          type: string
          format: email
        displayName:
          type: string
        status:
          $ref: "#/components/schemas/UserStatus"
        whatsappPhone:
          type: string
          nullable: true
          description: >
            CR-BE-NOTIFY-PROV-01 PART 06 — authoritative WhatsApp E.164
            number (null = no WhatsApp contact). Set only through the
            guarded whatsapp-contact seam (`user.manage`).
        whatsappOptedInAt:
          type: string
          format: date-time
          nullable: true
          description: Instant of the last explicit WhatsApp opt-in.
        whatsappOptedOutAt:
          type: string
          format: date-time
          nullable: true
          description: Instant of the last explicit WhatsApp opt-out.
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    UpdateUserWhatsAppContactRequest:
      type: object
      description: >
        CR-BE-NOTIFY-PROV-01 PART 06 — guarded WhatsApp contact + consent
        update. Both fields are optional (omitted = keep). Consent is always
        explicit: `OPT_IN` requires a phone (existing or supplied in the same
        request); `whatsappPhone: null` clears the number AND both consent
        stamps; clearing the phone and requesting OPT_IN in one call is
        rejected. Numbers are never inferred from any other field.
      properties:
        whatsappPhone:
          type: string
          nullable: true
          description: >
            Strict E.164 WhatsApp number (e.g. +628123456789); null clears
            the contact and consent.
        consent:
          type: string
          enum: [OPT_IN, OPT_OUT]
          description: Explicit consent action; stamps the current instant.

    UserStatus:
      type: string
      enum: [INVITED, ACTIVE, INACTIVE, SUSPENDED]

    # ------------------------------------------------------------------
    # CR-BE-MOB-CONTRACT-01 PART 02 — User / Role / Permission / Building
    # context schemas. Shapes mirror the backend DTOs exactly.
    # ------------------------------------------------------------------
    PublicRole:
      type: object
      required: [id, code, name, description, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        code:
          type: string
        name:
          type: string
        description:
          type: string
          nullable: true
        status:
          $ref: "#/components/schemas/RoleStatus"

    RoleStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    CreateRoleRequest:
      type: object
      required: [code, name]
      properties:
        code:
          type: string
          description: Normalized to uppercase.
        name:
          type: string
        description:
          type: string
        status:
          $ref: "#/components/schemas/RoleStatus"

    AssignRoleRequest:
      type: object
      required: [roleId]
      properties:
        roleId:
          $ref: "#/components/schemas/Uuid"

    PublicPermission:
      type: object
      required: [id, code, name, description, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        code:
          type: string
          description: '<domain>.<verb> permission code (e.g. user.read).'
        name:
          type: string
        description:
          type: string
          nullable: true
        status:
          $ref: "#/components/schemas/PermissionStatus"

    PermissionStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    CreatePermissionRequest:
      type: object
      required: [code, name]
      properties:
        code:
          type: string
          description: Normalized to lowercase.
        name:
          type: string
        description:
          type: string
        status:
          $ref: "#/components/schemas/PermissionStatus"

    AssignPermissionRequest:
      type: object
      required: [permissionId]
      properties:
        permissionId:
          $ref: "#/components/schemas/Uuid"

    CreateUserRequest:
      type: object
      required: [email, displayName]
      properties:
        email:
          type: string
          format: email
          description: Normalized (trimmed + lowercased); unique.
        displayName:
          type: string
        status:
          $ref: "#/components/schemas/UserStatus"

    PublicClient:
      type: object
      required: [id, code, name, legalName, taxId, description, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        code:
          type: string
        name:
          type: string
        legalName:
          type: string
          nullable: true
        taxId:
          type: string
          nullable: true
        description:
          type: string
          nullable: true
        status:
          $ref: "#/components/schemas/ClientStatus"

    ClientStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    CreateClientRequest:
      type: object
      required: [code, name]
      properties:
        code: { type: string }
        name: { type: string }
        legalName: { type: string }
        taxId: { type: string }
        description: { type: string }
        status: { $ref: "#/components/schemas/ClientStatus" }

    UpdateClientRequest:
      type: object
      minProperties: 1
      properties:
        code: { type: string }
        name: { type: string }
        legalName: { type: string, nullable: true }
        taxId: { type: string, nullable: true }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/ClientStatus" }

    PublicProperty:
      type: object
      required:
        [id, clientId, code, name, description, status, addressLine, city,
         province, postalCode, countryCode]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/PropertyStatus" }
        addressLine: { type: string, nullable: true }
        city: { type: string, nullable: true }
        province: { type: string, nullable: true }
        postalCode: { type: string, nullable: true }
        countryCode: { type: string, nullable: true }

    PropertyStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    CreatePropertyRequest:
      type: object
      required: [clientId, code, name]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        description: { type: string }
        status: { $ref: "#/components/schemas/PropertyStatus" }
        addressLine: { type: string }
        city: { type: string }
        province: { type: string }
        postalCode: { type: string }
        countryCode: { type: string }

    UpdatePropertyStatusRequest:
      type: object
      required: [status]
      properties:
        status: { $ref: "#/components/schemas/PropertyStatus" }

    PublicBuilding:
      type: object
      required:
        [id, propertyId, campusId, code, name, description, status, addressLine,
         city, province, postalCode, countryCode, timezone]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        propertyId: { $ref: "#/components/schemas/Uuid" }
        campusId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/BuildingStatus" }
        addressLine: { type: string, nullable: true }
        city: { type: string, nullable: true }
        province: { type: string, nullable: true }
        postalCode: { type: string, nullable: true }
        countryCode: { type: string, nullable: true }
        timezone: { type: string, nullable: true }

    BuildingStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    CreateBuildingRequest:
      type: object
      required: [propertyId, code, name]
      properties:
        propertyId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        description: { type: string }
        status: { $ref: "#/components/schemas/BuildingStatus" }
        addressLine: { type: string }
        city: { type: string }
        province: { type: string }
        postalCode: { type: string }
        countryCode: { type: string }
        timezone:
          type: string
          description: IANA timezone (e.g. Asia/Jakarta).

    UpdateBuildingStatusRequest:
      type: object
      required: [status]
      properties:
        status: { $ref: "#/components/schemas/BuildingStatus" }

    PublicUserBuildingAssignment:
      type: object
      required: [id, userId, buildingId, status, assignedByUserId, assignedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        userId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        status: { $ref: "#/components/schemas/UserBuildingAssignmentStatus" }
        assignedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        assignedAt:
          type: string
          format: date-time

    UserBuildingAssignmentStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    CreateBuildingAssignmentRequest:
      type: object
      required: [buildingId]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }

    # ------------------------------------------------------------------
    # Effective user context
    # ------------------------------------------------------------------
    EffectiveRole:
      type: object
      required: [id, code, name]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string

    EffectiveAccess:
      type: object
      description: >
        Effective ACTIVE roles and the deduplicated effective permission set
        that RBAC (requirePermission) actually enforces — a single source of
        truth.
      required: [roles, permissions]
      properties:
        roles:
          type: array
          items:
            $ref: "#/components/schemas/EffectiveRole"
        permissions:
          type: array
          items:
            type: string

    EffectiveBuildingContext:
      type: object
      required: [id, code, name]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string

    EffectivePropertyContext:
      type: object
      required: [id, code, name, buildings]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string
        buildings:
          type: array
          items:
            $ref: "#/components/schemas/EffectiveBuildingContext"

    EffectiveClientContext:
      type: object
      required: [id, code, name, properties]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string
        properties:
          type: array
          items:
            $ref: "#/components/schemas/EffectivePropertyContext"

    EffectiveEntitlement:
      type: object
      description: Effective module entitlement resolved from Client subscriptions.
      required: [moduleCode, status]
      properties:
        moduleCode:
          type: string
        status:
          type: string
          const: ACTIVE

    EffectiveUserContext:
      type: object
      description: >
        The single authoritative context of the authenticated user (BE-01I /
        BE-02H / BE-25B). `scope` and `context.workforce` are the BE-25B mobile
        additions; all dimensions derive from the same resolvers BE-02G data
        isolation uses — no separate mobile context engine.
      required: [user, access, context, scope, entitlements]
      properties:
        user:
          $ref: "#/components/schemas/PublicUser"
        access:
          $ref: "#/components/schemas/EffectiveAccess"
        context:
          type: object
          required: [clients, workforce]
          properties:
            clients:
              type: array
              description: Reachable commercial hierarchy from explicit building assignments.
              items:
                $ref: "#/components/schemas/EffectiveClientContext"
            workforce:
              type: array
              description: >
                Workforce profile(s) linked to the authenticated user
                (identity only). Empty when the user has no linked profile or
                the profile belongs to an inaccessible Client.
              items:
                $ref: "#/components/schemas/EffectiveWorkforceProfile"
        scope:
          $ref: "#/components/schemas/EffectiveScope"
        entitlements:
          type: array
          items:
            $ref: "#/components/schemas/EffectiveEntitlement"

    EffectiveScope:
      type: object
      description: >
        BE-25B mobile data scope: the flat accessible Building/Client id sets
        derived from the user's explicit Building assignments (the same source
        BE-02G data isolation uses). Never lists scope isolation would deny.
      required: [buildingIds, clientIds]
      properties:
        buildingIds:
          type: array
          description: Accessible Building ids (empty for a zero-scope user).
          items:
            $ref: "#/components/schemas/Uuid"
        clientIds:
          type: array
          description: Clients reachable through the accessible Buildings (empty for a zero-scope user).
          items:
            $ref: "#/components/schemas/Uuid"

    EffectiveWorkforceProfile:
      type: object
      description: >
        BE-25B workforce profile linked to the authenticated user. Identity
        only — Organization / Department / Team / Position dimensions remain
        deferred and are not expanded here.
      required: [id, clientId, employeeCode, fullName, workforceType, status]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        employeeCode:
          type: string
        fullName:
          type: string
        workforceType:
          type: string
          enum: [INTERNAL, OUTSOURCED, CONTRACT, EXTERNAL]
        status:
          type: string
          enum: [ACTIVE, INACTIVE]

    # ------------------------------------------------------------------
    # Current user building assignments (GET /auth/me/buildings)
    # ------------------------------------------------------------------
    UserBuildingContext:
      type: object
      description: One ACTIVE building assignment with its Property/Client refs.
      required: [id, status, building]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
          description: User building assignment id.
        status:
          type: string
          const: ACTIVE
        building:
          $ref: "#/components/schemas/BuildingRef"
        property:
          allOf:
            - $ref: "#/components/schemas/PropertyRef"
          nullable: true
        client:
          allOf:
            - $ref: "#/components/schemas/ClientRef"
          nullable: true

    # ------------------------------------------------------------------
    # BE-23J / BE-24 PART 10 neutral reporting export
    # ------------------------------------------------------------------
    ReportingExportColumn:
      type: object
      required: [key, label, type]
      properties:
        key:
          type: string
        label:
          type: string
        type:
          type: string
          enum: [STRING, NUMBER, PERCENT, DATE, BOOLEAN]

    ReportingExportKpiValue:
      type: object
      description: Value copied from the owning KPI/read model without recalculation.
      required: [key, label, value, type]
      properties:
        key:
          type: string
        label:
          type: string
        value:
          nullable: true
          oneOf:
            - type: string
            - type: number
        type:
          type: string
          enum: [STRING, NUMBER, PERCENT, DATE, BOOLEAN]

    ReportingExportTable:
      type: object
      required: [key, label, columns, rows, rowCount]
      properties:
        key:
          type: string
        label:
          type: string
        columns:
          type: array
          items:
            $ref: "#/components/schemas/ReportingExportColumn"
        rows:
          type: array
          items:
            type: object
            additionalProperties:
              nullable: true
              oneOf:
                - type: string
                - type: number
                - type: boolean
        rowCount:
          type: integer
          minimum: 0

    ReportingExportMetadata:
      type: object
      required:
        [dataset, datasetLabel, buildingId, buildingScope, period, filters, asOf]
      properties:
        dataset:
          $ref: "#/components/schemas/ReportArchiveDataset"
        datasetLabel:
          type: string
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        buildingScope:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/Uuid"
        period:
          type: object
          required: [dateFrom, dateTo]
          properties:
            dateFrom:
              type: string
              nullable: true
            dateTo:
              type: string
              nullable: true
        filters:
          type: object
          additionalProperties:
            nullable: true
            oneOf:
              - type: string
              - type: number
              - type: boolean
        asOf:
          type: string
          format: date-time
          nullable: true

    ReportingExport:
      type: object
      description: >
        Export-ready structured JSON only. No chart metadata, binary file,
        PDF/Excel rendering, BI materialization, or alternate KPI calculation.
      required: [metadata, kpis, tables, generatedAt]
      properties:
        metadata:
          $ref: "#/components/schemas/ReportingExportMetadata"
        kpis:
          type: array
          items:
            $ref: "#/components/schemas/ReportingExportKpiValue"
        tables:
          type: array
          items:
            $ref: "#/components/schemas/ReportingExportTable"
        generatedAt:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # CR-BE-EXP-01 PART 06 — report archive request/completion/download
    # ------------------------------------------------------------------
    ReportArchiveDataset:
      type: string
      enum:
        [SECURITY_PATROL, SECURITY_FINDING_INCIDENT, WORKFORCE,
         VENDOR_TENANT, UTILITY,
         MANAGEMENT_OPERATIONS_COMMAND_CENTER]
      description: Dataset keys currently exposed by the runtime reporting registry.

    ReportArchiveFormat:
      type: string
      enum: [JSON, CSV, XLSX, PDF]
      description: Requested artifact format. Format identity is persisted with the archive.

    ReportArchiveStatus:
      type: string
      enum: [REQUESTED, GENERATING, COMPLETED, FAILED]
      description: Generation lifecycle status; retention is a separate state.

    ReportArchiveRetentionState:
      type: string
      enum: [ACTIVE, RETENTION_DUE, PURGED]
      description: Retention state. Report-archive cleanup remains deferred.

    ReportArchiveFilterObject:
      type: object
      maxProperties: 100
      description: >
        Bounded normalized dataset filters. Runtime validation permits safe JSON
        primitives/objects/arrays within bounded depth and size, rejects
        storage/secret/SQL/HTML fields, and does not accept caller-owned scope
        identity inside this object. Dataset adapters remain authoritative for
        dataset-specific filter semantics.
      additionalProperties: true

    ReportArchiveRequestedScope:
      type: object
      required: [clientId, buildingId, buildingIds]
      properties:
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        buildingIds:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/Uuid"

    ReportArchiveResolvedScope:
      type: object
      required: [clientIds, buildingIds, mode]
      properties:
        clientIds:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/Uuid"
        buildingIds:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/Uuid"
        mode:
          type: string
          enum: [ALL_ACCESSIBLE, CLIENT, SINGLE_BUILDING, MULTI_BUILDING]

    ReportArchivePeriod:
      type: object
      required: [dateFrom, dateTo, timeBasis, dateToMode]
      properties:
        dateFrom:
          type: string
          nullable: true
        dateTo:
          type: string
          nullable: true
        timeBasis:
          type: string
          const: UTC
        dateToMode:
          type: string
          enum: [NONE, INCLUSIVE_DAY, EXCLUSIVE_INSTANT]

    ReportArchiveFilterSnapshot:
      type: object
      required: [dataset, format, requestedScope, resolvedScope, period, filters]
      properties:
        dataset:
          $ref: "#/components/schemas/ReportArchiveDataset"
        format:
          $ref: "#/components/schemas/ReportArchiveFormat"
        requestedScope:
          $ref: "#/components/schemas/ReportArchiveRequestedScope"
        resolvedScope:
          $ref: "#/components/schemas/ReportArchiveResolvedScope"
        period:
          $ref: "#/components/schemas/ReportArchivePeriod"
        filters:
          $ref: "#/components/schemas/ReportArchiveFilterObject"

    CreateReportArchiveRequest:
      type: object
      additionalProperties: false
      required: [clientId, dataset, format]
      properties:
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
          description: Single requested Building; mutually exclusive with buildingIds.
        buildingIds:
          type: array
          uniqueItems: true
          minItems: 1
          items:
            $ref: "#/components/schemas/Uuid"
          description: Explicit requested Building set; mutually exclusive with buildingId.
        dataset:
          $ref: "#/components/schemas/ReportArchiveDataset"
        format:
          $ref: "#/components/schemas/ReportArchiveFormat"
        filters:
          $ref: "#/components/schemas/ReportArchiveFilterObject"

    ReportArchiveArtifact:
      type: object
      required: [available, filename, contentType, fileSize, checksum, checksumAlgorithm]
      properties:
        available:
          type: boolean
          description: True only when a completed, non-purged stored artifact reference exists.
        filename:
          type: string
          nullable: true
          pattern: '^[A-Za-z0-9][A-Za-z0-9._-]{0,200}$'
        contentType:
          type: string
          nullable: true
        fileSize:
          type: integer
          format: int64
          minimum: 0
          nullable: true
        checksum:
          type: string
          nullable: true
          pattern: '^[0-9a-f]{64}$'
        checksumAlgorithm:
          type: string
          nullable: true
          enum: [SHA-256]

    ReportArchiveFailure:
      type: object
      nullable: true
      required: [code, message, failedAt]
      properties:
        code:
          type: string
        message:
          type: string
        failedAt:
          type: string
          format: date-time

    ReportArchiveRetention:
      type: object
      required: [policyId, policyCode, daysSnapshot, appliedAt, retainedUntil, state, hold, holdReason, purgedAt]
      properties:
        policyId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        policyCode:
          type: string
          nullable: true
        daysSnapshot:
          type: integer
          minimum: 1
          nullable: true
        appliedAt:
          type: string
          format: date-time
          nullable: true
        retainedUntil:
          type: string
          format: date-time
          nullable: true
        state:
          $ref: "#/components/schemas/ReportArchiveRetentionState"
        hold:
          type: boolean
        holdReason:
          type: string
          nullable: true
        purgedAt:
          type: string
          format: date-time
          nullable: true

    ReportArchive:
      type: object
      description: >
        One immutable export request/execution and, after completion, its
        stored artifact metadata. Internal storageReference is intentionally
        omitted. Regeneration creates a new archive ID.
      required:
        [id, clientId, buildingId, buildingIds, dataset, format, status,
         filterSnapshot, sourceProvenance, asOf, requestedByUserId, requestedAt,
         generatedAt, artifact, failure, attemptCount, maxAttempts,
         nextAttemptAt, supersedesArchiveId, retention, createdAt, updatedAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        buildingIds:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/Uuid"
        dataset:
          $ref: "#/components/schemas/ReportArchiveDataset"
        format:
          $ref: "#/components/schemas/ReportArchiveFormat"
        status:
          $ref: "#/components/schemas/ReportArchiveStatus"
        filterSnapshot:
          $ref: "#/components/schemas/ReportArchiveFilterSnapshot"
        sourceProvenance:
          type: object
          additionalProperties: true
        asOf:
          type: string
          format: date-time
          nullable: true
        requestedByUserId:
          $ref: "#/components/schemas/Uuid"
        requestedAt:
          type: string
          format: date-time
        generatedAt:
          type: string
          format: date-time
          nullable: true
        artifact:
          $ref: "#/components/schemas/ReportArchiveArtifact"
        failure:
          $ref: "#/components/schemas/ReportArchiveFailure"
        attemptCount:
          type: integer
          minimum: 0
        maxAttempts:
          type: integer
          minimum: 1
        nextAttemptAt:
          type: string
          format: date-time
          nullable: true
        supersedesArchiveId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        retention:
          $ref: "#/components/schemas/ReportArchiveRetention"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    ReportArchivePaginationMeta:
      type: object
      required: [page, pageSize, total, totalPages]
      properties:
        page:
          type: integer
          minimum: 1
        pageSize:
          type: integer
          minimum: 1
          maximum: 200
        total:
          type: integer
          minimum: 0
        totalPages:
          type: integer
          minimum: 0

    ReportArchiveListEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              type: array
              items:
                $ref: "#/components/schemas/ReportArchive"
            meta:
              $ref: "#/components/schemas/ReportArchivePaginationMeta"

    # ------------------------------------------------------------------
    # BE-24 Management & Owner read-model foundation
    # ------------------------------------------------------------------
    ManagementReadScope:
      type: object
      description: >
        Authorized Client/Building scope. buildingIds is the complete set later
        read-model repositories may query; clients is the matching subset of
        the effective-user context hierarchy.
      required: [mode, clientId, clientIds, buildingIds, clients]
      properties:
        mode:
          type: string
          enum: [ALL_ACCESSIBLE, CLIENT, SINGLE_BUILDING, MULTI_BUILDING]
        clientId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        clientIds:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/Uuid"
        buildingIds:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/Uuid"
        clients:
          type: array
          items:
            $ref: "#/components/schemas/EffectiveClientContext"

    ManagementReadPeriod:
      type: object
      description: BE-23-compatible UTC reporting period semantics.
      required: [dateFrom, dateTo, timeBasis, dateToMode]
      properties:
        dateFrom:
          type: string
          nullable: true
          description: Original normalized ISO-8601 lower boundary.
        dateTo:
          type: string
          nullable: true
          description: Original normalized ISO-8601 upper boundary.
        timeBasis:
          type: string
          const: UTC
        dateToMode:
          type: string
          enum: [NONE, INCLUSIVE_DAY, EXCLUSIVE_INSTANT]

    ManagementReadScopeFilters:
      type: object
      description: Normalized shared scope selectors supplied by the caller.
      required: [clientId, buildingId, buildingIds]
      properties:
        clientId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        buildingIds:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/Uuid"

    ManagementReadScopeContext:
      type: object
      required: [scope, period, filters, asOf]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementReadScopeFilters"
        asOf:
          type: string
          format: date-time

    ManagementReadModelContract:
      type: object
      description: >
        Shared base shape for later BE-24 read models. Domain filters extend
        the shared scope filters and data is owned by the specific read model;
        scope/period/asOf always retain this contract.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          type: object
          additionalProperties: true
        asOf:
          type: string
          format: date-time
        data:
          type: object

    ManagementDailyOperationsFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required: [operationalDate, graceMinutes]
          properties:
            operationalDate:
              type: string
              format: date
            graceMinutes:
              type: integer
              minimum: 0
              maximum: 10080

    ManagementDailyOperationsData:
      type: object
      required:
        [scheduled, completed, inProgress, overdue, openFindings,
         criticalOperationalItems]
      properties:
        scheduled:
          type: integer
          minimum: 0
          description: BE-10I scheduled generated-task count for the day.
        completed:
          type: integer
          minimum: 0
          description: BE-10I completed generated-task count for the day.
        inProgress:
          type: integer
          minimum: 0
          description: BE-10I in-progress generated-task count for the day.
        overdue:
          type: integer
          minimum: 0
          description: BE-23G overdue active-assignment count for the day.
        openFindings:
          type: integer
          minimum: 0
          description: BE-10I open Finding count reported in the day.
        criticalOperationalItems:
          type: object
          required: [total, securityIncidents, tenantServiceRequests]
          properties:
            total:
              type: integer
              minimum: 0
            securityIncidents:
              type: integer
              minimum: 0
              description: BE-23F2 Security-relevant CRITICAL Incident count.
            tenantServiceRequests:
              type: integer
              minimum: 0
              description: BE-23H CRITICAL-priority Tenant Service Request count.

    ManagementDailyOperations:
      type: object
      description: BE-24 PART 02A shared-contract Daily Operations projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementDailyOperationsFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementDailyOperationsData"

    ManagementWorkOrderSummaryFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required: [overdueAfterDays]
          properties:
            overdueAfterDays:
              type: integer
              minimum: 0
              maximum: 365
              description: Reporting age threshold; not a BE-08 due date/SLA.

    ManagementWorkOrderPrioritySummary:
      type: object
      required: [low, medium, high, critical]
      properties:
        low:
          type: integer
          minimum: 0
        medium:
          type: integer
          minimum: 0
        high:
          type: integer
          minimum: 0
        critical:
          type: integer
          minimum: 0

    ManagementWorkOrderSummaryData:
      type: object
      required:
        [total, open, inProgress, completed, overdue, verified, closed, priority]
      properties:
        total:
          type: integer
          minimum: 0
        open:
          type: integer
          minimum: 0
          description: Exact BE-08 OPEN status count.
        inProgress:
          type: integer
          minimum: 0
          description: Exact BE-08 IN_PROGRESS status count.
        completed:
          type: integer
          minimum: 0
          description: Exact BE-08 COMPLETED status count.
        overdue:
          type: integer
          minimum: 0
          description: >
            Unfinished Work Orders older than overdueAfterDays by createdAt;
            a reporting measure, not lifecycle state.
        verified:
          type: integer
          minimum: 0
          description: Distinct Work Orders with an APPROVED BE-08I review.
        closed:
          type: integer
          minimum: 0
          description: Exact terminal BE-08 CLOSED status count.
        priority:
          $ref: "#/components/schemas/ManagementWorkOrderPrioritySummary"

    ManagementWorkOrderSummary:
      type: object
      description: BE-24 PART 02B shared-contract Work Order Summary projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementWorkOrderSummaryFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementWorkOrderSummaryData"

    ManagementPendingApprovalItem:
      type: object
      required:
        [approvalId, clientId, buildingId, source, approvalType, resourceType,
         resourceId, resourceReference, submittedAt, currentStatus,
         availableActions]
      properties:
        approvalId:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        source:
          type: string
          enum: [PERMIT, PROCUREMENT, TENANT, DOCUMENT]
        approvalType:
          type: string
        resourceType:
          type: string
        resourceId:
          $ref: "#/components/schemas/Uuid"
        resourceReference:
          type: string
          description: Human reference when exposed by the source, otherwise its stable id.
        submittedAt:
          type: string
          format: date-time
        currentStatus:
          type: string
          const: PENDING
        availableActions:
          type: array
          description: Backend-authoritative source actions; empty when unavailable or disallowed.
          items:
            type: string

    ManagementPendingApprovalData:
      type: object
      required: [pendingCount, items]
      properties:
        pendingCount:
          type: integer
          minimum: 0
        items:
          type: array
          items:
            $ref: "#/components/schemas/ManagementPendingApprovalItem"

    ManagementPendingApproval:
      type: object
      description: BE-24 PART 03A shared-contract Pending Approval projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementReadScopeFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementPendingApprovalData"

    ManagementCriticalFindingsFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required: [overdueAfterDays, criticalSeverityRule]
          properties:
            overdueAfterDays:
              type: integer
              minimum: 0
              maximum: 365
              description: Reporting age threshold; not a BE-09 due date/SLA.
            criticalSeverityRule:
              type: string
              const: HIGHEST_ACTIVE_RANK_PER_CLIENT

    ManagementCriticalFindingResponsibleParty:
      type: object
      required: [type, workforceProfileId, teamId, vendorId]
      properties:
        type:
          type: string
          enum: [WORKFORCE, TEAM, VENDOR, VENDOR_WORKFORCE]
        workforceProfileId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        teamId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        vendorId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true

    ManagementCriticalFindingItem:
      type: object
      required:
        [findingId, clientId, buildingId, findingReference, title, severity,
         source, context, status, responsibleParty, reportedAt, overdue]
      properties:
        findingId:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          $ref: "#/components/schemas/Uuid"
        findingReference:
          type: string
        title:
          type: string
        severity:
          type: object
          required: [id, code, name, rank]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
            code:
              type: string
            name:
              type: string
            rank:
              type: integer
              minimum: 1
        source:
          type: object
          required: [type, id]
          properties:
            type:
              type: string
              nullable: true
            id:
              allOf:
                - $ref: "#/components/schemas/Uuid"
              nullable: true
        context:
          type: object
          required: [type, id]
          properties:
            type:
              type: string
              enum: [ENGINEERING, HOUSEKEEPING, SECURITY, GENERAL]
            id:
              allOf:
                - $ref: "#/components/schemas/Uuid"
              nullable: true
        status:
          type: string
          enum:
            [OPEN, ASSIGNED, IN_PROGRESS, PENDING_REVIEW, REJECTED,
             REWORK_REQUIRED, RESUBMITTED]
        responsibleParty:
          allOf:
            - $ref: "#/components/schemas/ManagementCriticalFindingResponsibleParty"
          nullable: true
        reportedAt:
          type: string
          format: date-time
        overdue:
          type: boolean
          description: Elapsed reporting state, not an authoritative BE-09 status.

    ManagementCriticalFindingsData:
      type: object
      required: [criticalFindingCount, items]
      properties:
        criticalFindingCount:
          type: integer
          minimum: 0
        items:
          type: array
          items:
            $ref: "#/components/schemas/ManagementCriticalFindingItem"

    ManagementCriticalFindings:
      type: object
      description: BE-24 PART 03B shared-contract Critical Findings projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementCriticalFindingsFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementCriticalFindingsData"

    ManagementWorkforceSummaryFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required: [graceMinutes]
          properties:
            graceMinutes:
              type: integer
              minimum: 0
              maximum: 10080

    ManagementWorkforceDepartmentRow:
      type: object
      required:
        [departmentId, departmentCode, departmentName, totalWorkforce,
         activeWorkforce]
      properties:
        departmentId:
          $ref: "#/components/schemas/Uuid"
        departmentCode:
          type: string
        departmentName:
          type: string
        totalWorkforce:
          type: integer
          minimum: 0
        activeWorkforce:
          type: integer
          minimum: 0

    ManagementWorkforceTeamRow:
      allOf:
        - $ref: "#/components/schemas/ManagementWorkforceDepartmentRow"
        - type: object
          required: [teamId, teamCode, teamName]
          properties:
            teamId:
              $ref: "#/components/schemas/Uuid"
            teamCode:
              type: string
            teamName:
              type: string

    ManagementWorkforceManHours:
      type: object
      description: Verbatim BE-23G man-hour projection.
      required:
        [totalHours, measuredAssignments, unmeasuredAssignments,
         averageHoursPerAssignment, averageHoursPerWorkforce]
      properties:
        totalHours:
          type: number
        measuredAssignments:
          type: integer
          minimum: 0
        unmeasuredAssignments:
          type: integer
          minimum: 0
        averageHoursPerAssignment:
          type: number
        averageHoursPerWorkforce:
          type: number

    ManagementWorkforceSummaryData:
      type: object
      required:
        [totalWorkforce, activeWorkforce, assignedWorkforce, byDepartment,
         byTeam, assignments, manHours]
      properties:
        totalWorkforce:
          type: integer
          minimum: 0
          description: BE-23G workforce.total.
        activeWorkforce:
          type: integer
          minimum: 0
          description: BE-23G workforce.active.
        assignedWorkforce:
          type: integer
          minimum: 0
          description: BE-23G workforce.withAssignments.
        byDepartment:
          type: array
          items:
            $ref: "#/components/schemas/ManagementWorkforceDepartmentRow"
        byTeam:
          type: array
          items:
            $ref: "#/components/schemas/ManagementWorkforceTeamRow"
        assignments:
          type: object
          required: [scheduled, completed, overdue]
          properties:
            scheduled:
              type: integer
              minimum: 0
            completed:
              type: integer
              minimum: 0
            overdue:
              type: integer
              minimum: 0
        manHours:
          $ref: "#/components/schemas/ManagementWorkforceManHours"

    ManagementWorkforceSummary:
      type: object
      description: BE-24 PART 04A shared-contract Workforce Summary projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementWorkforceSummaryFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementWorkforceSummaryData"

    ManagementVendorSummaryFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required: [overdueAfterDays]
          properties:
            overdueAfterDays:
              type: integer
              minimum: 0
              maximum: 365

    ManagementVendorPerformanceRow:
      type: object
      description: Verbatim BE-23H per-vendor performance projection.
      required:
        [vendorId, vendorCode, vendorName, vendorStatus, total, completed,
         outstanding, overdue, completionRate, averageCompletionHours]
      properties:
        vendorId:
          $ref: "#/components/schemas/Uuid"
        vendorCode:
          type: string
        vendorName:
          type: string
        vendorStatus:
          type: string
        total:
          type: integer
          minimum: 0
        completed:
          type: integer
          minimum: 0
        outstanding:
          type: integer
          minimum: 0
        overdue:
          type: integer
          minimum: 0
        completionRate:
          type: number
          minimum: 0
          maximum: 100
        averageCompletionHours:
          type: number
          minimum: 0

    ManagementVendorSummaryData:
      type: object
      required:
        [activeVendors, assignedVendorWork, completedVendorWork,
         overdueVendorWork, completionRate, vendorPerformance]
      properties:
        activeVendors:
          type: integer
          minimum: 0
        assignedVendorWork:
          type: integer
          minimum: 0
          description: BE-23H vendorWork.total.
        completedVendorWork:
          type: integer
          minimum: 0
          description: BE-23H vendorWork.completed.
        overdueVendorWork:
          type: integer
          minimum: 0
          description: BE-23H vendorWork.overdue.
        completionRate:
          type: number
          minimum: 0
          maximum: 100
          description: BE-23H vendorWork.completionRate.
        vendorPerformance:
          type: array
          items:
            $ref: "#/components/schemas/ManagementVendorPerformanceRow"

    ManagementVendorSummary:
      type: object
      description: BE-24 PART 04B shared-contract Vendor projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementVendorSummaryFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementVendorSummaryData"

    ManagementTenantServiceSummaryFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required: [overdueAfterDays]
          properties:
            overdueAfterDays:
              type: integer
              minimum: 0
              maximum: 365

    ManagementTenantComplaintSummary:
      type: object
      required: [total, open, escalated, cancelled]
      properties:
        total:
          type: integer
          minimum: 0
        open:
          type: integer
          minimum: 0
        escalated:
          type: integer
          minimum: 0
        cancelled:
          type: integer
          minimum: 0

    ManagementTenantServiceSummaryData:
      type: object
      required:
        [totalRequests, openRequests, inProgressRequests, completedRequests,
         overdueRequests, complaints, completionRate]
      properties:
        totalRequests:
          type: integer
          minimum: 0
          description: BE-23H tenantService.total.
        openRequests:
          type: integer
          minimum: 0
          description: BE-23H tenantService.open.
        inProgressRequests:
          type: integer
          minimum: 0
          description: >
            CONVERTED requests whose linked BE-08 Work Order is IN_PROGRESS
            or ON_HOLD.
        completedRequests:
          type: integer
          minimum: 0
          description: BE-23H tenantService.completed.
        overdueRequests:
          type: integer
          minimum: 0
          description: >
            Outstanding requests older than overdueAfterDays by requestedAt;
            reporting state only.
        complaints:
          $ref: "#/components/schemas/ManagementTenantComplaintSummary"
        completionRate:
          type: number
          minimum: 0
          maximum: 100
          description: BE-23H tenantService.completionRate.

    ManagementTenantServiceSummary:
      type: object
      description: BE-24 PART 04C shared-contract Tenant Service projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementTenantServiceSummaryFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementTenantServiceSummaryData"

    ManagementAssetRegistryComplianceFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required: [expiringWithinDays]
          properties:
            expiringWithinDays:
              type: integer
              minimum: 0
              maximum: 365

    ManagementAssetLocationSummary:
      type: object
      required:
        [buildingId, locationType, functionalLocationId,
         functionalLocationCode, functionalLocationName,
         functionalLocationStatus, assetCount]
      properties:
        buildingId:
          $ref: "#/components/schemas/Uuid"
        locationType:
          type: string
          enum: [BUILDING, FUNCTIONAL_LOCATION]
        functionalLocationId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        functionalLocationCode:
          type: string
          nullable: true
        functionalLocationName:
          type: string
          nullable: true
        functionalLocationStatus:
          type: string
          nullable: true
        assetCount:
          type: integer
          minimum: 0

    ManagementAssetRegistryComplianceData:
      type: object
      required:
        [totalAssets, activeAssets, inactiveAssets, assetStatus, warranties,
         certifications, locations]
      properties:
        totalAssets:
          type: integer
          minimum: 0
        activeAssets:
          type: integer
          minimum: 0
        inactiveAssets:
          type: integer
          minimum: 0
        assetStatus:
          type: object
          required: [ACTIVE, INACTIVE, UNDER_MAINTENANCE, RETIRED]
          properties:
            ACTIVE:
              type: integer
              minimum: 0
            INACTIVE:
              type: integer
              minimum: 0
            UNDER_MAINTENANCE:
              type: integer
              minimum: 0
            RETIRED:
              type: integer
              minimum: 0
        warranties:
          type: object
          required:
            [totalRecords, active, expired, inactive,
             currentlyCoveredAssets, assetsWithoutCurrentCoverage]
          properties:
            totalRecords:
              type: integer
              minimum: 0
            active:
              type: integer
              minimum: 0
            expired:
              type: integer
              minimum: 0
            inactive:
              type: integer
              minimum: 0
            currentlyCoveredAssets:
              type: integer
              minimum: 0
            assetsWithoutCurrentCoverage:
              type: integer
              minimum: 0
        certifications:
          type: object
          required:
            [totalRecords, active, expired, inactive,
             currentlyEffectiveRecords, assetsWithEffectiveCertification,
             assetsWithoutEffectiveCertification, expiredComplianceCount,
             expiringComplianceCount]
          properties:
            totalRecords:
              type: integer
              minimum: 0
            active:
              type: integer
              minimum: 0
            expired:
              type: integer
              minimum: 0
            inactive:
              type: integer
              minimum: 0
            currentlyEffectiveRecords:
              type: integer
              minimum: 0
            assetsWithEffectiveCertification:
              type: integer
              minimum: 0
            assetsWithoutEffectiveCertification:
              type: integer
              minimum: 0
            expiredComplianceCount:
              type: integer
              minimum: 0
            expiringComplianceCount:
              type: integer
              minimum: 0
        locations:
          type: array
          items:
            $ref: "#/components/schemas/ManagementAssetLocationSummary"

    ManagementAssetRegistryCompliance:
      type: object
      description: BE-24 PART 05A shared-contract Asset registry/compliance projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementAssetRegistryComplianceFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementAssetRegistryComplianceData"

    ManagementAssetReliabilityWorkFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required: [graceMinutes]
          properties:
            graceMinutes:
              type: integer
              minimum: 0
              maximum: 10080

    ManagementAssetReliabilityWorkData:
      type: object
      required:
        [breakdowns, failures, activeCorrectiveWorkOrders,
         completedMaintenanceWork, overdueMaintenanceWork,
         assetAvailability, pmCompliance]
      properties:
        breakdowns:
          type: object
          required: [total, open, closed]
          properties:
            total:
              type: integer
              minimum: 0
            open:
              type: integer
              minimum: 0
            closed:
              type: integer
              minimum: 0
        failures:
          type: object
          required: [total, open, inProgress, resolved]
          properties:
            total:
              type: integer
              minimum: 0
            open:
              type: integer
              minimum: 0
            inProgress:
              type: integer
              minimum: 0
            resolved:
              type: integer
              minimum: 0
        activeCorrectiveWorkOrders:
          type: integer
          minimum: 0
          description: Breakdown-linked BE-08 Work Orders in active statuses.
        completedMaintenanceWork:
          type: integer
          minimum: 0
          description: Linked maintenance Work Orders in COMPLETED or CLOSED.
        overdueMaintenanceWork:
          type: integer
          minimum: 0
          description: Overdue tasks on ACTIVE maintenance bindings.
        assetAvailability:
          type: object
          required:
            [availableAssets, unavailableAssets, retiredAssets,
             availabilityRate]
          properties:
            availableAssets:
              type: integer
              minimum: 0
            unavailableAssets:
              type: integer
              minimum: 0
              description: INACTIVE plus UNDER_MAINTENANCE assets.
            retiredAssets:
              type: integer
              minimum: 0
            availabilityRate:
              type: number
              minimum: 0
              maximum: 100
              description: ACTIVE / non-retired assets using BE-23 rounding.
        pmCompliance:
          type: object
          required: [scheduled, completed, overdue, complianceRate]
          properties:
            scheduled:
              type: integer
              minimum: 0
            completed:
              type: integer
              minimum: 0
            overdue:
              type: integer
              minimum: 0
            complianceRate:
              type: number
              minimum: 0
              maximum: 100
              description: Completed/scheduled preventive tasks using BE-23 rounding.

    ManagementAssetReliabilityWork:
      type: object
      description: BE-24 PART 05B shared-contract Asset reliability/work projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementAssetReliabilityWorkFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementAssetReliabilityWorkData"

    ManagementUtilitySummaryFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required: [interval, includeSubMeters]
          properties:
            interval:
              type: string
              enum: [DAY, MONTH, YEAR]
            includeSubMeters:
              type: boolean

    ManagementUtilityConsumption:
      type: object
      required:
        [utilityType, totalConsumption, consumptionCount, meterCount, uomId]
      properties:
        utilityType:
          type: string
          enum: [ELECTRICITY, WATER, GAS]
        totalConsumption:
          type: number
        consumptionCount:
          type: integer
          minimum: 0
        meterCount:
          type: integer
          minimum: 0
        uomId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true

    ManagementUtilityAbnormalSummary:
      type: object
      required: [total, open, resolved, dismissed, byType]
      properties:
        total:
          type: integer
          minimum: 0
        open:
          type: integer
          minimum: 0
        resolved:
          type: integer
          minimum: 0
        dismissed:
          type: integer
          minimum: 0
        byType:
          type: object
          additionalProperties:
            type: integer

    ManagementUtilityVerificationSummary:
      type: object
      description: BE-23I verification of abnormal-consumption readings.
      required: [verified, pending, rejected, reworkRequired, unverified]
      properties:
        verified:
          type: integer
          minimum: 0
        pending:
          type: integer
          minimum: 0
        rejected:
          type: integer
          minimum: 0
        reworkRequired:
          type: integer
          minimum: 0
        unverified:
          type: integer
          minimum: 0

    ManagementTenantUtilityRow:
      type: object
      required:
        [buildingId, tenantCompanyId, tenantName, utilityType,
         totalConsumption, consumptionCount, meterCount, uomId,
         periodStart, periodEnd]
      properties:
        buildingId:
          $ref: "#/components/schemas/Uuid"
        tenantCompanyId:
          $ref: "#/components/schemas/Uuid"
        tenantName:
          type: string
          nullable: true
        utilityType:
          type: string
          enum: [ELECTRICITY, WATER, GAS]
        totalConsumption:
          type: number
        consumptionCount:
          type: integer
          minimum: 0
        meterCount:
          type: integer
          minimum: 0
        uomId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        periodStart:
          type: string
          format: date-time
          nullable: true
        periodEnd:
          type: string
          format: date-time
          nullable: true

    ManagementUtilityTrendPoint:
      type: object
      required: [intervalStart, totalConsumption, consumptionCount, meterCount]
      properties:
        intervalStart:
          type: string
          format: date-time
          nullable: true
        totalConsumption:
          type: number
        consumptionCount:
          type: integer
          minimum: 0
        meterCount:
          type: integer
          minimum: 0

    ManagementUtilitySummaryData:
      type: object
      required:
        [electricity, water, gas, abnormalConsumption,
         verifiedReadingSummary, tenantUtility, periodComparison]
      properties:
        electricity:
          $ref: "#/components/schemas/ManagementUtilityConsumption"
        water:
          $ref: "#/components/schemas/ManagementUtilityConsumption"
        gas:
          $ref: "#/components/schemas/ManagementUtilityConsumption"
        abnormalConsumption:
          $ref: "#/components/schemas/ManagementUtilityAbnormalSummary"
        verifiedReadingSummary:
          $ref: "#/components/schemas/ManagementUtilityVerificationSummary"
        tenantUtility:
          type: array
          items:
            $ref: "#/components/schemas/ManagementTenantUtilityRow"
        periodComparison:
          type: object
          description: Latest two BE-23I trend buckets; no alternate arithmetic.
          required: [interval, previous, current]
          properties:
            interval:
              type: string
              enum: [DAY, MONTH, YEAR]
            previous:
              allOf:
                - $ref: "#/components/schemas/ManagementUtilityTrendPoint"
              nullable: true
            current:
              allOf:
                - $ref: "#/components/schemas/ManagementUtilityTrendPoint"
              nullable: true

    ManagementUtilitySummary:
      type: object
      description: BE-24 PART 06A shared-contract Utility Summary projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementUtilitySummaryFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementUtilitySummaryData"

    ManagementCountAmount:
      type: object
      required: [count, amount]
      properties:
        count:
          type: integer
          minimum: 0
        amount:
          type: number

    ManagementFinancialSummaryBlock:
      type: object
      required:
        [tenantCharges, utilityBills, invoices, payments, receipts,
         vendorServiceCosts, basicExpenses, outstandingBalance,
         incomeVsOperationalCost]
      properties:
        tenantCharges:
          allOf:
            - $ref: "#/components/schemas/ManagementCountAmount"
            - type: object
              required: [cancelledCount]
              properties:
                cancelledCount:
                  type: integer
                  minimum: 0
        utilityBills:
          allOf:
            - $ref: "#/components/schemas/ManagementCountAmount"
            - type: object
              required: [cancelledCount]
              properties:
                cancelledCount:
                  type: integer
                  minimum: 0
        invoices:
          allOf:
            - $ref: "#/components/schemas/ManagementCountAmount"
            - type: object
              required: [draftCount, cancelledCount]
              properties:
                draftCount:
                  type: integer
                  minimum: 0
                cancelledCount:
                  type: integer
                  minimum: 0
        payments:
          type: object
          required:
            [paidAmount, unpaidAmount, overdueAmount, outstandingAmount,
             unpaidCount, partiallyPaidCount, paidCount, overdueCount]
          properties:
            paidAmount:
              type: number
            unpaidAmount:
              type: number
              description: Outstanding amount not yet overdue.
            overdueAmount:
              type: number
            outstandingAmount:
              type: number
            unpaidCount:
              type: integer
              minimum: 0
            partiallyPaidCount:
              type: integer
              minimum: 0
            paidCount:
              type: integer
              minimum: 0
            overdueCount:
              type: integer
              minimum: 0
        receipts:
          type: object
          required: [issued, void]
          properties:
            issued:
              $ref: "#/components/schemas/ManagementCountAmount"
            void:
              $ref: "#/components/schemas/ManagementCountAmount"
        vendorServiceCosts:
          type: object
          required: [finalized, draftCount, cancelledCount]
          properties:
            finalized:
              $ref: "#/components/schemas/ManagementCountAmount"
            draftCount:
              type: integer
              minimum: 0
            cancelledCount:
              type: integer
              minimum: 0
        basicExpenses:
          type: object
          required: [finalized, draftCount, cancelledCount]
          properties:
            finalized:
              $ref: "#/components/schemas/ManagementCountAmount"
            draftCount:
              type: integer
              minimum: 0
            cancelledCount:
              type: integer
              minimum: 0
        outstandingBalance:
          type: object
          required: [invoiceCount, amount]
          properties:
            invoiceCount:
              type: integer
              minimum: 0
            amount:
              type: number
        incomeVsOperationalCost:
          type: object
          required:
            [billedIncome, receivedIncome, operationalCost,
             netBilled, netReceived]
          properties:
            billedIncome:
              type: number
            receivedIncome:
              type: number
            operationalCost:
              type: number
            netBilled:
              type: number
            netReceived:
              type: number

    ManagementBuildingFinancialSummary:
      type: object
      required: [clientId, buildingId, summary]
      properties:
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          $ref: "#/components/schemas/Uuid"
        summary:
          $ref: "#/components/schemas/ManagementFinancialSummaryBlock"

    ManagementClientFinancialSummary:
      type: object
      required: [clientId, buildingIds, summary]
      properties:
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingIds:
          type: array
          items:
            $ref: "#/components/schemas/Uuid"
        summary:
          $ref: "#/components/schemas/ManagementFinancialSummaryBlock"

    ManagementFinancialSummaryData:
      type: object
      description: Client-partitioned monetary summaries; no cross-Client total.
      required: [clientSummaries, buildingSummaries]
      properties:
        clientSummaries:
          type: array
          items:
            $ref: "#/components/schemas/ManagementClientFinancialSummary"
        buildingSummaries:
          type: array
          items:
            $ref: "#/components/schemas/ManagementBuildingFinancialSummary"

    ManagementFinancialSummary:
      type: object
      description: BE-24 PART 06B shared-contract Financial Summary projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementReadScopeFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementFinancialSummaryData"

    # ------------------------------------------------------------------
    # CR-BE-FIN-01 — Operational Finance budget/binding/aggregation contract
    # ------------------------------------------------------------------
    OperationalBudgetStatus:
      type: string
      enum: [DRAFT, ACTIVE, CLOSED, CANCELLED]

    OperationalBudgetCurrency:
      type: string
      enum: [IDR, USD, SGD, MYR, AUD, EUR, GBP, JPY, CNY]

    OperationalBudgetPeriod:
      type: object
      required: [start, end]
      properties:
        start:
          type: string
          format: date
        end:
          type: string
          format: date

    OperationalBudgetOverspendPolicy:
      type: string
      description: >
        STRICT prohibits any commitment beyond available budget.
        ALLOW_WITH_OVERRIDE permits it ONLY through the separate
        operational_budget.override authority plus an explicit recorded reason.
        Overspend is never silently permitted.
      enum: [STRICT, ALLOW_WITH_OVERRIDE]

    OperationalBudget:
      type: object
      required:
        [id, budgetName, clientId, buildingId, budgetPeriod, currency,
         plannedAmount, status, overspendPolicy, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        budgetName: { type: string }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        budgetPeriod: { $ref: "#/components/schemas/OperationalBudgetPeriod" }
        currency: { $ref: "#/components/schemas/OperationalBudgetCurrency" }
        plannedAmount: { type: number, minimum: 0 }
        status: { $ref: "#/components/schemas/OperationalBudgetStatus" }
        overspendPolicy: { $ref: "#/components/schemas/OperationalBudgetOverspendPolicy" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateOperationalBudgetRequest:
      type: object
      required: [budgetPeriod, currency, plannedAmount]
      properties:
        budgetName: { type: string, maxLength: 160 }
        budgetPeriod: { $ref: "#/components/schemas/OperationalBudgetPeriod" }
        currency: { $ref: "#/components/schemas/OperationalBudgetCurrency" }
        plannedAmount: { type: number, minimum: 0 }
        overspendPolicy: { $ref: "#/components/schemas/OperationalBudgetOverspendPolicy" }

    UpdateOperationalBudgetRequest:
      type: object
      minProperties: 1
      description: >
        DRAFT-only. Changing the overspend policy of an ACTIVE budget uses the
        separately authorised POST /operational-budgets/{id}/overspend-policy.
      properties:
        budgetName: { type: string, maxLength: 160 }
        budgetPeriod: { $ref: "#/components/schemas/OperationalBudgetPeriod" }
        currency: { $ref: "#/components/schemas/OperationalBudgetCurrency" }
        plannedAmount: { type: number, minimum: 0 }
        overspendPolicy: { $ref: "#/components/schemas/OperationalBudgetOverspendPolicy" }

    OperationalBudgetCategory:
      type: object
      required: [id, budgetId, code, name, plannedAmount, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        budgetId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        plannedAmount: { type: number, minimum: 0 }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateOperationalBudgetCategoryRequest:
      type: object
      required: [code, name, plannedAmount]
      properties:
        code: { type: string, pattern: '^[A-Z][A-Z0-9_]{0,63}$' }
        name: { type: string, maxLength: 160 }
        plannedAmount: { type: number, minimum: 0 }

    UpdateOperationalBudgetCategoryRequest:
      type: object
      minProperties: 1
      properties:
        name: { type: string, maxLength: 160 }
        plannedAmount: { type: number, minimum: 0 }

    OperationalFinanceSourceType:
      type: string
      enum: [BASIC_EXPENSE, VENDOR_SERVICE_COST, VENDOR_INVOICE, PURCHASE_ORDER, PO_LINE, WORK_ORDER_MATERIAL]

    OperationalFinanceCurrencyStatus:
      type: string
      enum: [MATCHED, MISSING]

    OperationalFinanceBindingStatus:
      type: string
      enum: [ACTIVE, REMOVED]

    OperationalBudgetSourceBinding:
      type: object
      required:
        [id, budgetId, budgetCategoryId, clientId, buildingId, sourceType,
         currencyStatus, status, createdByUserId, removedByUserId, removedAt,
         createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        budgetId: { $ref: "#/components/schemas/Uuid" }
        budgetCategoryId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        sourceType: { $ref: "#/components/schemas/OperationalFinanceSourceType" }
        basicExpenseId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        vendorServiceCostId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        vendorInvoiceId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        purchaseOrderId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        purchaseOrderLineId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        workOrderMaterialUsageId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        currencyStatus: { $ref: "#/components/schemas/OperationalFinanceCurrencyStatus" }
        status: { $ref: "#/components/schemas/OperationalFinanceBindingStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        removedByUserId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        removedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateOperationalBudgetSourceBindingRequest:
      type: object
      description: Exactly one typed source reference is required; source amounts are never accepted.
      required: [budgetCategoryId, sourceType]
      properties:
        budgetCategoryId: { $ref: "#/components/schemas/Uuid" }
        sourceType: { $ref: "#/components/schemas/OperationalFinanceSourceType" }
        basicExpenseId: { $ref: "#/components/schemas/Uuid" }
        vendorServiceCostId: { $ref: "#/components/schemas/Uuid" }
        vendorInvoiceId: { $ref: "#/components/schemas/Uuid" }
        purchaseOrderId: { $ref: "#/components/schemas/Uuid" }
        purchaseOrderLineId: { $ref: "#/components/schemas/Uuid" }
        workOrderMaterialUsageId: { $ref: "#/components/schemas/Uuid" }

    OperationalBudgetAggregationTotals:
      type: object
      required: [plannedAmount, actualAmount, committedAmount, remainingAmount, varianceAmount]
      properties:
        plannedAmount: { type: number }
        actualAmount: { type: number }
        committedAmount: { type: number }
        remainingAmount: { type: number, description: May be negative. }
        varianceAmount: { type: number, description: May be negative. }

    OperationalBudgetAggregationCategory:
      type: object
      required:
        [budgetCategoryId, categoryCode, categoryName, plannedAmount,
         actualAmount, committedAmount, remainingAmount, varianceAmount,
         actualSourceCount, committedSourceCount]
      properties:
        budgetCategoryId: { $ref: "#/components/schemas/Uuid" }
        categoryCode: { type: string }
        categoryName: { type: string }
        plannedAmount: { type: number }
        actualAmount: { type: number }
        committedAmount: { type: number }
        remainingAmount: { type: number, description: May be negative. }
        varianceAmount: { type: number, description: May be negative. }
        actualSourceCount: { type: integer, minimum: 0 }
        committedSourceCount: { type: integer, minimum: 0 }

    OperationalBudgetAggregationExclusion:
      type: object
      required: [bindingId, budgetCategoryId, sourceType, sourceId, reason]
      properties:
        bindingId: { $ref: "#/components/schemas/Uuid" }
        budgetCategoryId: { $ref: "#/components/schemas/Uuid" }
        sourceType: { $ref: "#/components/schemas/OperationalFinanceSourceType" }
        sourceId: { $ref: "#/components/schemas/Uuid" }
        reason: { type: string }

    OperationalBudgetAggregationControls:
      type: object
      required: [failClosed, excludedContributionCount, exclusions]
      properties:
        failClosed: { type: boolean }
        excludedContributionCount: { type: integer, minimum: 0 }
        exclusions:
          type: array
          items: { $ref: "#/components/schemas/OperationalBudgetAggregationExclusion" }

    OperationalBudgetAggregation:
      type: object
      required:
        [budgetId, clientId, budgetName, buildingId, budgetPeriod, currency,
         status, totals, categories, controls, asOf]
      properties:
        budgetId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        budgetName: { type: string }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        budgetPeriod: { $ref: "#/components/schemas/OperationalBudgetPeriod" }
        currency: { $ref: "#/components/schemas/OperationalBudgetCurrency" }
        status: { $ref: "#/components/schemas/OperationalBudgetStatus" }
        totals: { $ref: "#/components/schemas/OperationalBudgetAggregationTotals" }
        categories:
          type: array
          items: { $ref: "#/components/schemas/OperationalBudgetAggregationCategory" }
        controls: { $ref: "#/components/schemas/OperationalBudgetAggregationControls" }
        asOf: { type: string, format: date-time }

    OperationalBudgetEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data: { $ref: "#/components/schemas/OperationalBudget" }

    OperationalBudgetListEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              type: array
              items: { $ref: "#/components/schemas/OperationalBudget" }

    OperationalBudgetCategoryEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data: { $ref: "#/components/schemas/OperationalBudgetCategory" }

    OperationalBudgetCategoryListEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              type: array
              items: { $ref: "#/components/schemas/OperationalBudgetCategory" }

    OperationalBudgetSourceBindingEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data: { $ref: "#/components/schemas/OperationalBudgetSourceBinding" }

    OperationalBudgetSourceBindingListEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              type: array
              items: { $ref: "#/components/schemas/OperationalBudgetSourceBinding" }

    OperationalCommitmentStatus:
      type: string
      description: >
        Derived from the ledger amounts. RELEASED and CANCELLED are terminal;
        a commitment is never cancellable once anything is actualized.
      enum: [COMMITTED, PARTIALLY_ACTUALIZED, ACTUALIZED, RELEASED, CANCELLED]

    OperationalCommitmentOrigin:
      type: string
      description: >
        MANUAL is an explicitly reasoned estimate; PO_LINE and PO_HEADER are
        derived from an ISSUED Purchase Order, the only priced obligation
        authority in the repository.
      enum: [MANUAL, PO_LINE, PO_HEADER]

    OperationalCommitmentEntryType:
      type: string
      enum:
        [CREATE, ADJUST_INCREASE, ADJUST_DECREASE, ACTUALIZE,
         ACTUALIZE_REVERSAL, RELEASE, CANCEL, OVERRIDE]

    OperationalCommitmentOverride:
      type: object
      nullable: true
      required: [reason, byUserId, at]
      properties:
        reason: { type: string }
        byUserId: { $ref: "#/components/schemas/Uuid" }
        at: { type: string, format: date-time }

    OperationalCommitment:
      type: object
      required:
        [id, clientId, buildingId, budgetId, budgetCategoryId, origin, currency,
         committedAmount, actualizedAmount, releasedAmount, openAmount, status,
         title, idempotencyKey, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        budgetId: { $ref: "#/components/schemas/Uuid" }
        budgetCategoryId: { $ref: "#/components/schemas/Uuid" }
        origin: { $ref: "#/components/schemas/OperationalCommitmentOrigin" }
        sourceType:
          type: string
          nullable: true
          enum: [PURCHASE_ORDER, PO_LINE, null]
        purchaseOrderId: { type: string, format: uuid, nullable: true }
        purchaseOrderLineId: { type: string, format: uuid, nullable: true }
        workOrderId: { type: string, format: uuid, nullable: true }
        vendorId: { type: string, format: uuid, nullable: true }
        materialRequestId: { type: string, format: uuid, nullable: true }
        currency: { $ref: "#/components/schemas/OperationalBudgetCurrency" }
        committedAmount: { type: number, minimum: 0 }
        actualizedAmount:
          type: number
          minimum: 0
          description: >
            Actualization moves value from openAmount to actualizedAmount and
            does NOT free budget, so an obligation is never counted twice.
        releasedAmount: { type: number, minimum: 0 }
        openAmount: { type: number, minimum: 0 }
        status: { $ref: "#/components/schemas/OperationalCommitmentStatus" }
        title: { type: string, maxLength: 160 }
        reason: { type: string, nullable: true }
        overspendOverride: { $ref: "#/components/schemas/OperationalCommitmentOverride" }
        idempotencyKey: { type: string }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        closedAt: { type: string, format: date-time, nullable: true }
        closedByUserId: { type: string, format: uuid, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    OperationalCommitmentEntry:
      type: object
      description: >
        Append-only. signedAmount is the effect on the commitment's open amount,
        so the entries of a commitment always sum to its openAmount. Entries are
        never updated or deleted; corrections are new entries.
      required:
        [id, commitmentId, entryType, signedAmount, currency, idempotencyKey,
         actorUserId, occurredAt, createdAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        commitmentId: { $ref: "#/components/schemas/Uuid" }
        entryType: { $ref: "#/components/schemas/OperationalCommitmentEntryType" }
        signedAmount: { type: number }
        currency: { $ref: "#/components/schemas/OperationalBudgetCurrency" }
        sourceBindingId: { type: string, format: uuid, nullable: true }
        idempotencyKey: { type: string }
        reason: { type: string, nullable: true }
        actorUserId: { $ref: "#/components/schemas/Uuid" }
        requestId: { type: string, format: uuid, nullable: true }
        occurredAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }

    OperationalCommitmentDetail:
      allOf:
        - $ref: "#/components/schemas/OperationalCommitment"
        - type: object
          required: [entries]
          properties:
            entries:
              type: array
              items: { $ref: "#/components/schemas/OperationalCommitmentEntry" }

    CreateOperationalCommitmentRequest:
      type: object
      description: >
        Manual/estimated commitment. Derived scope (client, building, budget,
        status and every ledger amount) must not be supplied.
      required: [budgetCategoryId, title, amount, currency, reason, idempotencyKey]
      properties:
        budgetCategoryId: { $ref: "#/components/schemas/Uuid" }
        title: { type: string, maxLength: 160 }
        amount:
          type: number
          exclusiveMinimum: 0
          description: Positive, at most two decimals, in the budget currency.
        currency: { $ref: "#/components/schemas/OperationalBudgetCurrency" }
        reason: { type: string, maxLength: 500 }
        idempotencyKey: { type: string, minLength: 8, maxLength: 120 }
        workOrderId: { type: string, format: uuid, nullable: true }
        vendorId: { type: string, format: uuid, nullable: true }
        materialRequestId: { type: string, format: uuid, nullable: true }
        overspendOverrideReason:
          type: string
          maxLength: 500
          nullable: true
          description: >
            Required to exceed available budget, and only honoured when the
            budget policy is ALLOW_WITH_OVERRIDE and the caller holds
            operational_budget.override.

    CreatePurchaseOrderLineCommitmentRequest:
      type: object
      description: >
        Amount, currency, vendor, Building and Material Request lineage are
        derived from the ISSUED Purchase Order line and must not be supplied.
      required: [purchaseOrderLineId, budgetCategoryId, idempotencyKey]
      properties:
        purchaseOrderLineId: { $ref: "#/components/schemas/Uuid" }
        budgetCategoryId: { $ref: "#/components/schemas/Uuid" }
        idempotencyKey: { type: string, minLength: 8, maxLength: 120 }
        overspendOverrideReason: { type: string, maxLength: 500, nullable: true }

    AdjustOperationalCommitmentRequest:
      type: object
      required: [amount, reason, idempotencyKey]
      properties:
        amount:
          type: number
          description: >
            Non-zero, at most two decimals. Positive increases the obligation,
            negative decreases it; a decrease below the actualized amount is
            rejected.
        reason: { type: string, maxLength: 500 }
        idempotencyKey: { type: string, minLength: 8, maxLength: 120 }
        overspendOverrideReason: { type: string, maxLength: 500, nullable: true }

    CloseOperationalCommitmentRequest:
      type: object
      required: [reason, idempotencyKey]
      properties:
        reason: { type: string, maxLength: 500 }
        idempotencyKey: { type: string, minLength: 8, maxLength: 120 }

    ChangeOperationalBudgetOverspendPolicyRequest:
      type: object
      required: [overspendPolicy, reason]
      properties:
        overspendPolicy: { $ref: "#/components/schemas/OperationalBudgetOverspendPolicy" }
        reason: { type: string, maxLength: 500 }

    OperationalBudgetOverspendPolicyResult:
      type: object
      required: [budgetId, overspendPolicy]
      properties:
        budgetId: { $ref: "#/components/schemas/Uuid" }
        overspendPolicy: { $ref: "#/components/schemas/OperationalBudgetOverspendPolicy" }

    OperationalBudgetVarianceTotals:
      type: object
      description: >
        openCommitment = ledger open + eligible legacy commitment.
        actual = ledger actualized + unallocated actual.
        consumed = (ledger committed - released) + eligible legacy commitment
        + unallocated actual. available = planned - consumed.
        variance = planned - actual. Percentages are null when the plan is zero.
      required:
        [plannedAmount, ledgerCommittedAmount, ledgerOpenAmount,
         ledgerActualizedAmount, ledgerReleasedAmount, legacyCommittedAmount,
         uncommittedMaterialActualAmount, uncommittedInvoiceActualAmount,
         unallocatedActualAmount, openCommitmentAmount, actualAmount,
         consumedAmount, availableAmount, varianceAmount, utilizationPercent,
         committedUtilizationPercent]
      properties:
        plannedAmount: { type: number }
        ledgerCommittedAmount: { type: number }
        ledgerOpenAmount: { type: number }
        ledgerActualizedAmount: { type: number }
        ledgerReleasedAmount: { type: number }
        legacyCommittedAmount: { type: number }
        uncommittedMaterialActualAmount: { type: number }
        uncommittedInvoiceActualAmount: { type: number }
        unallocatedActualAmount: { type: number }
        openCommitmentAmount: { type: number }
        actualAmount: { type: number }
        consumedAmount: { type: number }
        availableAmount: { type: number }
        varianceAmount: { type: number }
        utilizationPercent: { type: number, nullable: true }
        committedUtilizationPercent: { type: number, nullable: true }

    OperationalBudgetVarianceCategory:
      type: object
      required:
        [budgetCategoryId, categoryCode, categoryName, plannedAmount,
         ledgerCommittedAmount, ledgerOpenAmount, ledgerActualizedAmount,
         ledgerReleasedAmount, legacyCommittedAmount, openCommitmentAmount,
         actualAmount, consumedAmount, availableAmount, varianceAmount,
         utilizationPercent, committedUtilizationPercent, commitmentCount]
      properties:
        budgetCategoryId: { $ref: "#/components/schemas/Uuid" }
        categoryCode: { type: string }
        categoryName: { type: string }
        plannedAmount: { type: number }
        ledgerCommittedAmount: { type: number }
        ledgerOpenAmount: { type: number }
        ledgerActualizedAmount: { type: number }
        ledgerReleasedAmount: { type: number }
        legacyCommittedAmount: { type: number }
        openCommitmentAmount: { type: number }
        actualAmount: { type: number }
        consumedAmount: { type: number }
        availableAmount: { type: number }
        varianceAmount: { type: number }
        utilizationPercent: { type: number, nullable: true }
        committedUtilizationPercent: { type: number, nullable: true }
        commitmentCount: { type: integer, minimum: 0 }

    OperationalBudgetVarianceGap:
      type: object
      description: >
        A recorded gap is described, never valued. B-02: vendor_service_costs
        and basic_expenses with an unknown currency are excluded from
        currency-dependent figures. B-03: no approved vendor amount exists
        before a Purchase Order.
      required: [code, reference, message, affectedSourceCount]
      properties:
        code:
          type: string
          enum: [CURRENCYLESS_COST_AUTHORITY, VENDOR_ACTUAL_WITHOUT_COMMITMENT]
        reference: { type: string, enum: [B-02, B-03] }
        message: { type: string }
        affectedSourceCount: { type: integer, minimum: 0 }

    OperationalBudgetVariance:
      type: object
      required:
        [budgetId, clientId, buildingId, budgetName, budgetPeriod, currency,
         status, overspendPolicy, totals, categories, controls, gaps, asOf]
      properties:
        budgetId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        budgetName: { type: string }
        budgetPeriod: { $ref: "#/components/schemas/OperationalBudgetPeriod" }
        currency: { $ref: "#/components/schemas/OperationalBudgetCurrency" }
        status: { $ref: "#/components/schemas/OperationalBudgetStatus" }
        overspendPolicy: { $ref: "#/components/schemas/OperationalBudgetOverspendPolicy" }
        totals: { $ref: "#/components/schemas/OperationalBudgetVarianceTotals" }
        categories:
          type: array
          items: { $ref: "#/components/schemas/OperationalBudgetVarianceCategory" }
        controls:
          type: object
          required:
            [categoryScopeIncludesUnallocatedActual,
             legacyCommitmentSupersededCount, failClosed,
             excludedContributionCount, exclusions]
          properties:
            categoryScopeIncludesUnallocatedActual:
              type: boolean
              description: >
                Always false: an unmatched source carries no cost category and
                none is inferred.
            legacyCommitmentSupersededCount: { type: integer, minimum: 0 }
            failClosed: { type: boolean }
            excludedContributionCount: { type: integer, minimum: 0 }
            exclusions:
              type: array
              items: { $ref: "#/components/schemas/OperationalBudgetAggregationExclusion" }
        gaps:
          type: array
          items: { $ref: "#/components/schemas/OperationalBudgetVarianceGap" }
        asOf: { type: string, format: date-time }

    OperationalBudgetVarianceSummary:
      type: object
      required:
        [budgetId, clientId, buildingId, budgetName, budgetPeriod, currency,
         status, totals, failClosed, gapCount]
      properties:
        budgetId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        budgetName: { type: string }
        budgetPeriod: { $ref: "#/components/schemas/OperationalBudgetPeriod" }
        currency: { $ref: "#/components/schemas/OperationalBudgetCurrency" }
        status: { $ref: "#/components/schemas/OperationalBudgetStatus" }
        totals: { $ref: "#/components/schemas/OperationalBudgetVarianceTotals" }
        failClosed: { type: boolean }
        gapCount: { type: integer, minimum: 0 }

    OperationalBudgetTraceabilityRow:
      type: object
      required: [classification, sourceType, sourceId]
      properties:
        classification:
          type: string
          enum: [COMMITMENT, LEGACY_COMMITMENT, ACTUAL, EXCLUDED]
        sourceType: { type: string }
        sourceId: { $ref: "#/components/schemas/Uuid" }
        commitmentId: { type: string, format: uuid, nullable: true }
        bindingId: { type: string, format: uuid, nullable: true }
        budgetCategoryId: { type: string, format: uuid, nullable: true }
        amount: { type: number, nullable: true }
        openAmount: { type: number, nullable: true }
        actualizedAmount: { type: number, nullable: true }
        currency: { type: string, nullable: true }
        status: { type: string, nullable: true }
        description: { type: string, nullable: true }
        purchaseOrderId: { type: string, format: uuid, nullable: true }
        purchaseOrderLineId: { type: string, format: uuid, nullable: true }
        vendorId: { type: string, format: uuid, nullable: true }
        workOrderId: { type: string, format: uuid, nullable: true }
        materialRequestId: { type: string, format: uuid, nullable: true }
        vendorInvoiceId: { type: string, format: uuid, nullable: true }
        workOrderMaterialUsageId: { type: string, format: uuid, nullable: true }
        exclusionReason: { type: string, nullable: true }

    OperationalBudgetTraceability:
      type: object
      required:
        [budgetId, clientId, buildingId, budgetPeriod, currency, rowCount, rows, asOf]
      properties:
        budgetId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        budgetPeriod: { $ref: "#/components/schemas/OperationalBudgetPeriod" }
        currency: { $ref: "#/components/schemas/OperationalBudgetCurrency" }
        rowCount: { type: integer, minimum: 0 }
        rows:
          type: array
          items: { $ref: "#/components/schemas/OperationalBudgetTraceabilityRow" }
        asOf: { type: string, format: date-time }

    OperationalCommitmentEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data: { $ref: "#/components/schemas/OperationalCommitment" }

    OperationalCommitmentListEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              type: array
              items: { $ref: "#/components/schemas/OperationalCommitment" }

    OperationalCommitmentDetailEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data: { $ref: "#/components/schemas/OperationalCommitmentDetail" }

    OperationalBudgetOverspendPolicyEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data: { $ref: "#/components/schemas/OperationalBudgetOverspendPolicyResult" }

    OperationalBudgetVarianceEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data: { $ref: "#/components/schemas/OperationalBudgetVariance" }

    OperationalBudgetVarianceListEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              type: array
              items: { $ref: "#/components/schemas/OperationalBudgetVarianceSummary" }

    OperationalBudgetTraceabilityEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data: { $ref: "#/components/schemas/OperationalBudgetTraceability" }

    OperationalBudgetAggregationEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data: { $ref: "#/components/schemas/OperationalBudgetAggregation" }

    ManagementOperationalFinanceFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required: [budgetId]
          properties:
            budgetId:
              $ref: "#/components/schemas/Uuid"

    ManagementOperationalBudgetTotals:
      type: object
      required: [plannedAmount, actualAmount, committedAmount, remainingAmount, varianceAmount]
      properties:
        plannedAmount:
          type: number
        actualAmount:
          type: number
        committedAmount:
          type: number
        remainingAmount:
          type: number
          description: Planned amount minus actual and committed amounts; may be negative.
        varianceAmount:
          type: number
          description: Planned amount minus actual amount; may be negative.

    ManagementOperationalFinanceControls:
      type: object
      required: [failClosed, excludedContributionCount, exclusionReasons]
      properties:
        failClosed:
          type: boolean
        excludedContributionCount:
          type: integer
          minimum: 0
        exclusionReasons:
          type: array
          items:
            type: string
            enum:
              - SOURCE_NOT_ELIGIBLE
              - SOURCE_CURRENCY_UNPROVEN
              - SOURCE_CURRENCY_MISMATCH
              - SOURCE_PERIOD_OUTSIDE_BUDGET
              - SOURCE_AMOUNT_UNAVAILABLE
              - SOURCE_LINEAGE_AMBIGUOUS
              - SOURCE_REPRESENTED_BY_DOWNSTREAM_ACTUAL
              - PO_HEADER_SUPERSEDED_BY_PO_LINE
              - PO_COMMITMENT_REPLACED_BY_ACTUAL

    ManagementOperationalBudgetCategory:
      type: object
      required:
        [categoryId, categoryCode, categoryName, plannedAmount,
         actualAmount, committedAmount, remainingAmount, varianceAmount,
         actualSourceCount, committedSourceCount]
      properties:
        categoryId:
          $ref: "#/components/schemas/Uuid"
        categoryCode:
          type: string
        categoryName:
          type: string
        plannedAmount:
          type: number
        actualAmount:
          type: number
        committedAmount:
          type: number
        remainingAmount:
          type: number
          description: Planned amount minus actual and committed amounts; may be negative.
        varianceAmount:
          type: number
          description: Planned amount minus actual amount; may be negative.
        actualSourceCount:
          type: integer
          minimum: 0
        committedSourceCount:
          type: integer
          minimum: 0

    ManagementOperationalBudgetSummaryData:
      type: object
      required:
        [budgetId, clientId, buildingId, budgetName, periodStart, periodEnd,
         currency, status, totals, controls]
      properties:
        budgetId:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          $ref: "#/components/schemas/Uuid"
        budgetName:
          type: string
        periodStart:
          type: string
          format: date
        periodEnd:
          type: string
          format: date
        currency:
          type: string
          enum: [IDR, USD, SGD, MYR, AUD, EUR, GBP, JPY, CNY]
        status:
          type: string
          enum: [DRAFT, ACTIVE, CLOSED, CANCELLED]
        totals:
          $ref: "#/components/schemas/ManagementOperationalBudgetTotals"
        controls:
          $ref: "#/components/schemas/ManagementOperationalFinanceControls"

    ManagementOperationalBudgetCategoriesData:
      type: object
      required:
        [budgetId, clientId, buildingId, budgetName, periodStart, periodEnd,
         currency, status, categories, controls]
      properties:
        budgetId:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          $ref: "#/components/schemas/Uuid"
        budgetName:
          type: string
        periodStart:
          type: string
          format: date
        periodEnd:
          type: string
          format: date
        currency:
          type: string
          enum: [IDR, USD, SGD, MYR, AUD, EUR, GBP, JPY, CNY]
        status:
          type: string
          enum: [DRAFT, ACTIVE, CLOSED, CANCELLED]
        categories:
          type: array
          items:
            $ref: "#/components/schemas/ManagementOperationalBudgetCategory"
        controls:
          $ref: "#/components/schemas/ManagementOperationalFinanceControls"

    ManagementOperationalBudgetSummary:
      type: object
      description: Additive management contract delegated to PART 04 aggregation.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementOperationalFinanceFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementOperationalBudgetSummaryData"

    ManagementOperationalBudgetCategories:
      type: object
      description: Additive management category contract delegated to PART 04 aggregation.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementOperationalFinanceFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementOperationalBudgetCategoriesData"

    ManagementBuildingOperationalFinanceFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required: [buildingId, periodStart, periodEnd]
          properties:
            buildingId:
              $ref: "#/components/schemas/Uuid"
            periodStart:
              type: string
              format: date
            periodEnd:
              type: string
              format: date

    ManagementBuildingOperationalFinanceCategory:
      type: object
      required:
        [categoryId, categoryCode, categoryName, plannedAmount,
         actualAmount, committedAmount, remainingAmount, varianceAmount,
         budgetUtilizationPercent]
      properties:
        categoryId:
          $ref: "#/components/schemas/Uuid"
        categoryCode:
          type: string
        categoryName:
          type: string
        plannedAmount:
          type: number
        actualAmount:
          type: number
        committedAmount:
          type: number
        remainingAmount:
          type: number
          description: Planned amount minus actual and committed amounts; may be negative.
        varianceAmount:
          type: number
          description: Planned amount minus actual amount; may be negative.
        budgetUtilizationPercent:
          type: number
          nullable: true
          description: Actual divided by planned amount multiplied by 100; may exceed 100 percent.

    ManagementBuildingOperationalFinanceData:
      type: object
      required:
        [clientId, buildingId, periodStart, periodEnd, currency,
         plannedAmount, actualAmount, committedAmount, remainingAmount,
         varianceAmount, activeBudgetCount, categoryCount,
         categoriesOverBudget, categoriesWithCommitment, failClosed,
         excludedContributionCount, exclusionReasons, categories]
      properties:
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          $ref: "#/components/schemas/Uuid"
        periodStart:
          type: string
          format: date
        periodEnd:
          type: string
          format: date
        currency:
          type: string
          nullable: true
          enum: [IDR, USD, SGD, MYR, AUD, EUR, GBP, JPY, CNY]
        plannedAmount:
          type: number
        actualAmount:
          type: number
        committedAmount:
          type: number
        remainingAmount:
          type: number
          description: Planned amount minus actual and committed amounts; may be negative.
        varianceAmount:
          type: number
          description: Planned amount minus actual amount; may be negative.
        activeBudgetCount:
          type: integer
          minimum: 0
        categoryCount:
          type: integer
          minimum: 0
        categoriesOverBudget:
          type: integer
          minimum: 0
          description: Categories whose remaining amount is negative.
        categoriesWithCommitment:
          type: integer
          minimum: 0
        failClosed:
          type: boolean
        excludedContributionCount:
          type: integer
          minimum: 0
        exclusionReasons:
          type: array
          items:
            type: string
        categories:
          type: array
          items:
            $ref: "#/components/schemas/ManagementBuildingOperationalFinanceCategory"

    ManagementBuildingOperationalFinance:
      type: object
      description: >
        Read-only Building Operational Finance summary. The data block is
        composed from PART 05 management budget contracts, which delegate
        monetary calculations to PART 04.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementBuildingOperationalFinanceFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementBuildingOperationalFinanceData"

    ManagementOperationalKpiFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required: [graceMinutes]
          properties:
            graceMinutes:
              type: integer
              minimum: 0
              maximum: 10080

    ManagementOperationalKpiData:
      type: object
      required:
        [scheduled, completed, completionRate, overdue, overdueRate,
         workOrders, findings, incidentCount, criticalOperationalItems]
      properties:
        scheduled:
          type: integer
          minimum: 0
          description: BE-23G assignments.scheduled.
        completed:
          type: integer
          minimum: 0
          description: BE-23G assignments.completed.
        completionRate:
          type: number
          minimum: 0
          maximum: 100
          description: BE-23G assignments.completionRate.
        overdue:
          type: integer
          minimum: 0
          description: BE-23G assignments.overdue.
        overdueRate:
          type: number
          minimum: 0
          maximum: 100
          description: Overdue/scheduled using BE-23 percentage rounding.
        workOrders:
          type: object
          required: [open, closed]
          properties:
            open:
              type: integer
              minimum: 0
              description: Exact BE-08 OPEN count.
            closed:
              type: integer
              minimum: 0
              description: Exact BE-08 CLOSED count.
        findings:
          type: object
          required: [open, closed]
          properties:
            open:
              type: integer
              minimum: 0
              description: BE-23F2 outstanding status-set count.
            closed:
              type: integer
              minimum: 0
              description: Exact BE-09 CLOSED count.
        incidentCount:
          type: integer
          minimum: 0
        criticalOperationalItems:
          type: object
          required: [total, securityIncidents, tenantServiceRequests]
          properties:
            total:
              type: integer
              minimum: 0
            securityIncidents:
              type: integer
              minimum: 0
              description: BE-23F2 Security-relevant CRITICAL Incident count.
            tenantServiceRequests:
              type: integer
              minimum: 0
              description: BE-23H CRITICAL-priority Tenant Service Request count.

    ManagementOperationalKpi:
      type: object
      description: BE-24 PART 07 shared-contract Operational KPI projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementOperationalKpiFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementOperationalKpiData"

    ManagementBuildingPerformanceFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required:
            [graceMinutes, overdueAfterDays, expiringWithinDays,
             utilityInterval, includeSubMeters]
          properties:
            graceMinutes:
              type: integer
            overdueAfterDays:
              type: integer
            expiringWithinDays:
              type: integer
            utilityInterval:
              type: string
              enum: [DAY, MONTH, YEAR]
            includeSubMeters:
              type: boolean

    ManagementBuildingPerformanceRow:
      type: object
      required:
        [buildingId, operational, workOrders, findings,
         engineeringAssetHealth, housekeeping, security, utility,
         tenantService]
      properties:
        buildingId:
          $ref: "#/components/schemas/Uuid"
        operational:
          type: object
          required: [completionRate, overdueRate]
          properties:
            completionRate:
              type: number
            overdueRate:
              type: number
        workOrders:
          type: object
          required: [total, open, inProgress, completed, overdue, verified, closed]
          properties:
            total:
              type: integer
            open:
              type: integer
            inProgress:
              type: integer
            completed:
              type: integer
            overdue:
              type: integer
            verified:
              type: integer
            closed:
              type: integer
        findings:
          type: object
          required: [open, closed, critical]
          properties:
            open:
              type: integer
            closed:
              type: integer
            critical:
              type: integer
        engineeringAssetHealth:
          type: object
          required:
            [totalAssets, activeAssets, availabilityRate, breakdowns,
             failures, pmCompliance, expiredComplianceCount,
             expiringComplianceCount]
          properties:
            totalAssets:
              type: integer
            activeAssets:
              type: integer
            availabilityRate:
              type: number
            breakdowns:
              type: object
              additionalProperties:
                type: integer
            failures:
              type: object
              additionalProperties:
                type: integer
            pmCompliance:
              type: object
              additionalProperties:
                type: number
            expiredComplianceCount:
              type: integer
            expiringComplianceCount:
              type: integer
        housekeeping:
          type: object
          required:
            [cleaningTotal, cleaningCompleted, openFindings, reworkRequired,
             qualityAuditAverageScore, activeComplaints]
          properties:
            cleaningTotal:
              type: integer
            cleaningCompleted:
              type: integer
            openFindings:
              type: integer
            reworkRequired:
              type: integer
            qualityAuditAverageScore:
              type: number
              nullable: true
            activeComplaints:
              type: integer
        security:
          type: object
          required:
            [activePosts, patrolsScheduled, patrolsCompleted, openFindings,
             incidentReadiness]
          properties:
            activePosts:
              type: integer
            patrolsScheduled:
              type: integer
            patrolsCompleted:
              type: integer
            openFindings:
              type: integer
            incidentReadiness:
              type: object
              required: [notReady, partial, ready]
              properties:
                notReady:
                  type: integer
                partial:
                  type: integer
                ready:
                  type: integer
        utility:
          type: object
          required:
            [electricityConsumption, waterConsumption, gasConsumption,
             abnormalConsumption, verifiedReadings]
          properties:
            electricityConsumption:
              type: number
            waterConsumption:
              type: number
            gasConsumption:
              type: number
            abnormalConsumption:
              type: integer
            verifiedReadings:
              type: integer
        tenantService:
          type: object
          required:
            [totalRequests, openRequests, inProgressRequests,
             completedRequests, overdueRequests, completionRate, complaints]
          properties:
            totalRequests:
              type: integer
            openRequests:
              type: integer
            inProgressRequests:
              type: integer
            completedRequests:
              type: integer
            overdueRequests:
              type: integer
            completionRate:
              type: number
            complaints:
              $ref: "#/components/schemas/ManagementTenantComplaintSummary"

    ManagementBuildingPerformanceData:
      type: object
      description: Per-Building rows only; Portfolio aggregation is out of scope.
      required: [buildings]
      properties:
        buildings:
          type: array
          items:
            $ref: "#/components/schemas/ManagementBuildingPerformanceRow"

    ManagementBuildingPerformance:
      type: object
      description: BE-24 PART 08A shared-contract Building Performance projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementBuildingPerformanceFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementBuildingPerformanceData"

    ManagementPortfolioOverviewData:
      type: object
      required:
        [buildingCount, operational, workOrders, findings, assetEngineering,
         housekeeping, security, utility, tenantService, financial,
         buildingPerformance]
      properties:
        buildingCount:
          type: integer
          minimum: 0
        operational:
          type: object
          required: [completionRate, overdueRate]
          properties:
            completionRate:
              type: number
            overdueRate:
              type: number
        workOrders:
          type: object
          required: [total, open, inProgress, completed, overdue, verified, closed]
          additionalProperties:
            type: integer
        findings:
          type: object
          required: [open, closed, critical]
          additionalProperties:
            type: integer
        assetEngineering:
          type: object
          required:
            [totalAssets, activeAssets, availabilityRate, breakdowns,
             failures, pmCompliance, expiredComplianceCount,
             expiringComplianceCount]
          properties:
            totalAssets:
              type: integer
            activeAssets:
              type: integer
            availabilityRate:
              type: number
            breakdowns:
              type: object
              additionalProperties:
                type: integer
            failures:
              type: object
              additionalProperties:
                type: integer
            pmCompliance:
              type: object
              additionalProperties:
                type: number
            expiredComplianceCount:
              type: integer
            expiringComplianceCount:
              type: integer
        housekeeping:
          type: object
          required:
            [cleaningTotal, cleaningCompleted, openFindings,
             reworkRequired, activeComplaints]
          additionalProperties:
            type: integer
        security:
          type: object
          required:
            [activePosts, patrolsScheduled, patrolsCompleted,
             openFindings, incidentReadiness]
          properties:
            activePosts:
              type: integer
            patrolsScheduled:
              type: integer
            patrolsCompleted:
              type: integer
            openFindings:
              type: integer
            incidentReadiness:
              type: object
              required: [notReady, partial, ready]
              additionalProperties:
                type: integer
        utility:
          type: object
          required:
            [electricityConsumption, waterConsumption, gasConsumption,
             abnormalConsumption, verifiedReadings]
          additionalProperties:
            type: number
        tenantService:
          type: object
          required:
            [totalRequests, openRequests, inProgressRequests,
             completedRequests, overdueRequests, completionRate, complaints]
          properties:
            totalRequests:
              type: integer
            openRequests:
              type: integer
            inProgressRequests:
              type: integer
            completedRequests:
              type: integer
            overdueRequests:
              type: integer
            completionRate:
              type: number
            complaints:
              $ref: "#/components/schemas/ManagementTenantComplaintSummary"
        financial:
          type: object
          description: Client-partitioned; no cross-Client monetary total.
          required: [clientSummaries]
          properties:
            clientSummaries:
              type: array
              items:
                $ref: "#/components/schemas/ManagementClientFinancialSummary"
        buildingPerformance:
          type: array
          items:
            $ref: "#/components/schemas/ManagementBuildingPerformanceRow"

    ManagementPortfolioOverview:
      type: object
      description: BE-24 PART 08B shared-contract Portfolio Overview projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementBuildingPerformanceFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementPortfolioOverviewData"

    ManagementOperationsCommandCenterFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementBuildingPerformanceFilters"
        - type: object
          required: [operationalDate]
          properties:
            operationalDate:
              type: string
              format: date
              description: UTC day delegated unchanged to PART 02A.

    ManagementOperationsCommandCenterData:
      type: object
      description: >
        Completed BE-24 component data without repeated component scope
        envelopes, alternate calculations, chart metadata, or UI metadata.
      required:
        [dailyOperations, pendingApprovals, criticalFindings,
         workOrderSummary, workforceSummary, vendorSummary,
         tenantServiceSummary, assetEngineeringHealth, utilitySummary,
         financialSummary, operationalKpi, buildingPerformance,
         portfolioContext]
      properties:
        dailyOperations:
          $ref: "#/components/schemas/ManagementDailyOperationsData"
        pendingApprovals:
          $ref: "#/components/schemas/ManagementPendingApprovalData"
        criticalFindings:
          $ref: "#/components/schemas/ManagementCriticalFindingsData"
        workOrderSummary:
          $ref: "#/components/schemas/ManagementWorkOrderSummaryData"
        workforceSummary:
          $ref: "#/components/schemas/ManagementWorkforceSummaryData"
        vendorSummary:
          $ref: "#/components/schemas/ManagementVendorSummaryData"
        tenantServiceSummary:
          $ref: "#/components/schemas/ManagementTenantServiceSummaryData"
        assetEngineeringHealth:
          type: object
          required: [assetRegistryCompliance, assetReliabilityWork]
          properties:
            assetRegistryCompliance:
              $ref: "#/components/schemas/ManagementAssetRegistryComplianceData"
            assetReliabilityWork:
              $ref: "#/components/schemas/ManagementAssetReliabilityWorkData"
        utilitySummary:
          $ref: "#/components/schemas/ManagementUtilitySummaryData"
        financialSummary:
          $ref: "#/components/schemas/ManagementFinancialSummaryData"
        operationalKpi:
          $ref: "#/components/schemas/ManagementOperationalKpiData"
        buildingPerformance:
          type: array
          description: Exact PART 08A rows composed by PART 08B.
          items:
            $ref: "#/components/schemas/ManagementBuildingPerformanceRow"
        portfolioContext:
          type: object
          description: Lightweight values copied from PART 08B.
          required: [buildingCount, operational]
          properties:
            buildingCount:
              type: integer
              minimum: 0
            operational:
              type: object
              required: [completionRate, overdueRate]
              properties:
                completionRate:
                  type: number
                overdueRate:
                  type: number

    ManagementOperationsCommandCenter:
      type: object
      description: BE-24 PART 09 shared-contract Operations Command Center facade.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementOperationsCommandCenterFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementOperationsCommandCenterData"

    # ------------------------------------------------------------------
    # Authentication audit
    # ------------------------------------------------------------------
    # ------------------------------------------------------------------
    # Evidence file (CR-BE-API-01 PART 03)
    # ------------------------------------------------------------------
    FindingEvidenceRequest:
      type: object
      required: [evidenceType, fileReference, originalFileName, mimeType, fileSize]
      properties:
        evidenceType: { type: string, enum: [PHOTO, DOCUMENT, SIGNATURE] }
        evidenceRequirementId: { type: string, format: uuid, nullable: true }
        fileReference: { type: string }
        originalFileName: { type: string }
        mimeType: { type: string }
        fileSize: { type: integer, minimum: 0, maximum: 52428800 }
        capturedAt: { type: string, format: date-time, nullable: true }
    EvidenceResponse:
      allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EvidenceFileMetadata" } } }]
    EvidenceListResponse:
      allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/EvidenceFileMetadata" } } } }]
    EvidenceFileMetadata:
      type: object
      description: >
        An evidence submission is also the file metadata record. After an
        upload, `fileReference` holds the backend-generated storage key and
        `originalFileName` / `mimeType` / `fileSize` reflect the stored file.
      required: [id, evidenceType, fileReference, mimeType, fileSize, status]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        evidenceRequirementId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        executionType:
          type: string
          enum: [FORM_INSTANCE, CHECKLIST_EXECUTION, FINDING, FINDING_REWORK, FINDING_VERIFICATION]
        executionId:
          $ref: "#/components/schemas/Uuid"
        evidenceType:
          type: string
          enum: [PHOTO, DOCUMENT, SIGNATURE]
        fileReference:
          type: string
          description: Backend storage key (evidence/<uuid>) after upload.
        originalFileName:
          type: string
          nullable: true
        mimeType:
          type: string
          nullable: true
        fileSize:
          type: integer
          description: Bytes; capped at 52,428,800 (50 MB).
        capturedAt:
          type: string
          format: date-time
          nullable: true
        submittedByUserId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        status:
          type: string
          enum: [ACTIVE, REMOVED]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        # ------------------------------------------------------------
        # CR-BE-DOC-CONTROL-01 — additive integrity metadata (PART 01/02).
        # ------------------------------------------------------------
        contentSha256:
          type: string
          pattern: "^[0-9a-f]{64}$"
          nullable: true
          description: >
            Server-computed SHA-256 (lowercase hex) of the exact stored
            binary bytes, recorded during the authoritative upload flow.
            Null for legacy/unhashed evidence and metadata-contract
            submissions whose bytes never passed through the server — such
            rows are never assigned a fabricated hash. Callers can never
            submit this value; it is always computed server-side.
        contentHashedAt:
          type: string
          format: date-time
          nullable: true
        hashAlgorithm:
          type: string
          enum: [SHA-256]
          nullable: true
        lastIntegrityStatus:
          type: string
          enum: [VERIFIED, MISMATCH, NOT_HASHED, FILE_UNAVAILABLE]
          nullable: true
          description: Outcome of the most recent integrity verification; null when never verified.
        lastIntegrityCheckedAt:
          type: string
          format: date-time
          nullable: true
        # ------------------------------------------------------------
        # CR-BE-DOC-CONTROL-01 — additive retention snapshot (PART 03/04).
        # The snapshot is immutable: later policy edits never change these
        # values on already-governed evidence.
        # ------------------------------------------------------------
        retentionPolicyId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
          description: Governing retention policy identity; null = ungoverned (never purged).
        retentionPolicyCode:
          type: string
          nullable: true
        retentionDaysSnapshot:
          type: integer
          nullable: true
          description: Retention duration frozen at application time.
        retentionAppliedAt:
          type: string
          format: date-time
          nullable: true
        retainedUntil:
          type: string
          format: date-time
          nullable: true
          description: Anchor (capturedAt ?? createdAt) + retentionDaysSnapshot; null = ungoverned.
        retentionState:
          type: string
          enum: [ACTIVE, RETENTION_DUE, PURGED]
          description: >
            Retention lifecycle state. PURGED is a terminal metadata
            tombstone: the stored binary was disposed by retention, the row
            (hash, size, MIME, original file name, snapshot) survives so
            references never dangle. Purged evidence keeps serving metadata
            reads; file content returns 404, and re-upload, integrity
            verification, and hold requests return 400.
        retentionHold:
          type: boolean
          description: A held row is never purged while the hold is set.
        retentionHoldReason:
          type: string
          nullable: true
        purgedAt:
          type: string
          format: date-time
          nullable: true
          description: Set exactly when retentionState is PURGED.

    EvidenceIntegrityVerification:
      type: object
      description: >
        CR-BE-DOC-CONTROL-01 PART 02 — result of a server-side integrity
        verification: the stored binary is re-read through the storage
        abstraction and its SHA-256 recomputed against the persisted
        upload-time hash. Verification never mutates or deletes evidence;
        MISMATCH / FILE_UNAVAILABLE are auditable facts only.
      required: [evidenceId, outcome, checkedAt]
      properties:
        evidenceId:
          $ref: "#/components/schemas/Uuid"
        outcome:
          type: string
          enum: [VERIFIED, MISMATCH, NOT_HASHED, FILE_UNAVAILABLE]
        algorithm:
          type: string
          enum: [SHA-256]
          nullable: true
          description: Null when the evidence is NOT_HASHED.
        expectedSha256:
          type: string
          nullable: true
          description: Authoritative upload-time hash (null when NOT_HASHED).
        computedSha256:
          type: string
          nullable: true
          description: Hash recomputed from the currently stored bytes (null unless readable).
        checkedAt:
          type: string
          format: date-time

    EvidenceRetentionPolicy:
      type: object
      description: >
        CR-BE-DOC-CONTROL-01 PART 03 — Client-owned, optionally
        Building-scoped evidence retention policy over the existing evidence
        vocabularies. Deterministic application precedence scores
        building +4, executionType +2, evidenceType +1 (highest wins); a
        tied top score applies NO policy and records an
        EVIDENCE_RETENTION_AMBIGUOUS operational event. Governed evidence
        snapshots the applied rule immutably; editing a policy later never
        rewrites governed history.
      required: [id, clientId, code, name, retentionDays, status, effectiveFrom]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
          description: Null = whole Client. Building must belong to the Client.
        code:
          type: string
          pattern: "^[A-Z][A-Z0-9_.-]*$"
          description: Unique per Client.
        name:
          type: string
        evidenceType:
          type: string
          enum: [PHOTO, DOCUMENT, SIGNATURE]
          nullable: true
          description: Null = applies to any evidence type.
        executionType:
          type: string
          enum: [FORM_INSTANCE, CHECKLIST_EXECUTION, WORK_ORDER, VENDOR_WORK, UTILITY_METER_READING, PERMIT, FINDING, FINDING_REWORK, FINDING_VERIFICATION]
          nullable: true
          description: Null = applies to any execution (parent) kind.
        retentionDays:
          type: integer
          minimum: 1
        status:
          type: string
          enum: [ACTIVE, INACTIVE]
        effectiveFrom:
          type: string
          format: date-time
        effectiveTo:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    EvidenceRetentionPolicyCreateRequest:
      type: object
      required: [code, name, retentionDays, effectiveFrom]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string, pattern: "^[A-Z][A-Z0-9_.-]*$", maxLength: 100 }
        name: { type: string, maxLength: 200 }
        evidenceType: { type: string, enum: [PHOTO, DOCUMENT, SIGNATURE] }
        executionType: { type: string, enum: [FORM_INSTANCE, CHECKLIST_EXECUTION, WORK_ORDER, VENDOR_WORK, UTILITY_METER_READING, PERMIT, FINDING, FINDING_REWORK, FINDING_VERIFICATION] }
        retentionDays: { type: integer, minimum: 1 }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time }

    EvidenceRetentionPolicyUpdateRequest:
      type: object
      description: At least one field is required. Applicability fields accept null to widen to "any".
      properties:
        name: { type: string, maxLength: 200 }
        evidenceType: { type: string, enum: [PHOTO, DOCUMENT, SIGNATURE], nullable: true }
        executionType: { type: string, enum: [FORM_INSTANCE, CHECKLIST_EXECUTION, WORK_ORDER, VENDOR_WORK, UTILITY_METER_READING, PERMIT, FINDING, FINDING_REWORK, FINDING_VERIFICATION], nullable: true }
        retentionDays: { type: integer, minimum: 1 }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time, nullable: true }

    AuditEvent:
      type: object
      description: One persisted authentication audit event.
      required:
        [id, eventType, outcome, createdAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        eventType:
          type: string
          description: e.g. LOGIN, LOGOUT, LOGIN_FAILED, INVITATION_ACCEPTED.
        outcome:
          type: string
          enum: [SUCCESS, FAILURE]
        userId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        actorUserId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        sessionId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        requestId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        ipAddress:
          type: string
          nullable: true
        userAgent:
          type: string
          nullable: true
        metadata:
          type: object
          additionalProperties: true
        createdAt:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # BE-25A — Mobile execution surface schemas. Shapes mirror the
    # backend projections (src/modules/**/*.routes.ts `out()` helpers)
    # and must never invent fields.
    # ------------------------------------------------------------------
    MobilePaginationMeta:
      type: object
      description: >
        Pagination metadata emitted in `meta` for the shared page/pageSize
        shape. Mobile list endpoints may opt in under BE-25A; the enterprise
        operational-audit list uses this same shape with bounded pagination by
        default.
      required: [page, pageSize, total, totalPages]
      properties:
        page:
          type: integer
          description: 1-based page number requested.
        pageSize:
          type: integer
          description: Page size applied.
        total:
          type: integer
          description: Total number of items matching the filters.
        totalPages:
          type: integer
          description: Total number of pages (0 when total is 0).
    Task:
      type: object
      description: A generated task (generated_tasks projection).
      required: [id, clientId, occurrenceAt, targetType, targetId, status]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        scheduleDefinitionId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        occurrenceAt:
          type: string
          format: date-time
        targetType:
          type: string
        targetId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        status:
          type: string
          description: Task lifecycle status.
        startedAt:
          type: string
          format: date-time
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
        completedByUserId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        completionNotes:
          type: string
          nullable: true
        generatedAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    TaskAssignment:
      type: object
      description: A task assignment joined with its generated task.
      required: [id, taskId, assigneeType, status]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        taskId:
          $ref: "#/components/schemas/Uuid"
        assigneeType:
          type: string
          enum: [WORKFORCE, TEAM]
        workforceProfileId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        teamId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        assignedByUserId:
          $ref: "#/components/schemas/Uuid"
        assignedAt:
          type: string
          format: date-time
        status:
          type: string
          description: Assignment status (ACTIVE/INACTIVE).
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # CR-BE-MOB-CONTRACT-01 PART 03 — Work Order read schemas.
    # Shapes mirror the BE-08 public DTOs exactly.
    # ------------------------------------------------------------------
    WorkOrderStatus:
      type: string
      enum: [OPEN, ASSIGNED, IN_PROGRESS, ON_HOLD, COMPLETED, CANCELLED, CLOSED]

    WorkOrderPriority:
      type: string
      enum: [LOW, MEDIUM, HIGH, CRITICAL]

    WorkOrder:
      type: object
      description: >
        The authoritative Work Order record (BE-08). `status` is the backend
        lifecycle authority; clients must not infer transitions from it —
        per-caller actions come from `GET /mobile/assignments`. `assetId` /
        `functionalLocationId` are the optional BE-08D bindings (Location-only
        when null; a full resolved context is `GET /work-orders/:id/context`).
      required:
        [id, clientId, buildingId, workOrderNumber, workRequestId, title,
         description, workType, priority, status, bastRequirement,
         createdByUserId, assetId, functionalLocationId, assignedAt, startedAt,
         completedAt, completedByUserId, completionSummary, completionNotes,
         closedAt, cancelledAt, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        workOrderNumber: { type: string }
        workRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        title: { type: string }
        description: { type: string, nullable: true }
        workType: { type: string }
        priority: { $ref: "#/components/schemas/WorkOrderPriority" }
        status: { $ref: "#/components/schemas/WorkOrderStatus" }
        bastRequirement: { $ref: "#/components/schemas/BastRequirement" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        assetId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        functionalLocationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        assignedAt: { type: string, format: date-time, nullable: true }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        completedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        completionSummary: { type: string, nullable: true }
        completionNotes: { type: string, nullable: true }
        closedAt: { type: string, format: date-time, nullable: true }
        cancelledAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    WorkOrderAssigneeType:
      type: string
      enum: [WORKFORCE, TEAM, VENDOR, VENDOR_WORKFORCE]

    WorkOrderAssignmentStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    WorkOrderAssignment:
      type: object
      description: >
        A Work Order assignment (BE-08E). Exactly one ACTIVE assignment exists
        per Work Order; deactivation sets `status: INACTIVE` (never a delete).
        Exactly one of workforceProfileId / teamId / vendorId is populated for
        the assigneeType.
      required:
        [id, workOrderId, assigneeType, workforceProfileId, teamId, vendorId,
         assignedByUserId, assignedAt, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        assigneeType: { $ref: "#/components/schemas/WorkOrderAssigneeType" }
        workforceProfileId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        teamId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        vendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        assignedByUserId: { $ref: "#/components/schemas/Uuid" }
        assignedAt: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/WorkOrderAssignmentStatus" }

    WorkOrderActionType:
      type: string
      enum: [ACKNOWLEDGED, STARTED, ON_HOLD, RESUMED, NOTE_ADDED, CANCELLED]

    WorkOrderAction:
      type: object
      description: >
        An append-only Work Order execution action record (BE-08F). History,
        NOT an available-actions projection.
      required: [id, workOrderId, actionType, actorUserId, notes, occurredAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        actionType: { $ref: "#/components/schemas/WorkOrderActionType" }
        actorUserId: { $ref: "#/components/schemas/Uuid" }
        notes: { type: string, nullable: true }
        occurredAt: { type: string, format: date-time }

    WorkOrderContext:
      type: object
      description: >
        The authoritative resolved Work Order context (BE-08D): bound Asset,
        Functional Location and the projected operational hierarchy (BE-04H).
        Never stored — resolved on read.
      required:
        [workOrderId, clientId, buildingId, asset, functionalLocation,
         operationalContext]
      properties:
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        asset:
          allOf: [{ $ref: "#/components/schemas/WorkOrderContextAsset" }]
          nullable: true
        functionalLocation:
          allOf: [{ $ref: "#/components/schemas/WorkOrderContextFunctionalLocation" }]
          nullable: true
        operationalContext:
          type: object
          description: Projected Client → Property → Building hierarchy (BE-04H).
          nullable: true

    WorkOrderContextAsset:
      type: object
      required: [id, assetCode, assetName, status, functionalLocationId]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        assetCode: { type: string }
        assetName: { type: string }
        status: { type: string }
        functionalLocationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true

    WorkOrderContextFunctionalLocation:
      type: object
      required: [id, code, name, status, spaceId]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        status: { type: string }
        spaceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true

    WorkOrderCompletion:
      type: object
      description: The Work Order completion subset (BE-08H).
      required:
        [workOrderId, status, completedAt, completedByUserId,
         completionSummary, completionNotes]
      properties:
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        status: { type: string }
        completedAt: { type: string, format: date-time, nullable: true }
        completedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        completionSummary: { type: string, nullable: true }
        completionNotes: { type: string, nullable: true }

    # ------------------------------------------------------------------
    # CR-BE-MOB-CONTRACT-01 PART 07 — Work Order execution/evidence/
    # verification/history + Entitlement + Asset sub-resource schemas.
    # ------------------------------------------------------------------
    AssignTaskRequest:
      type: object
      required: [assigneeType]
      properties:
        assigneeType:
          type: string
          enum: [WORKFORCE, TEAM]
        workforceProfileId: { $ref: "#/components/schemas/Uuid" }
        teamId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }

    CreateWorkOrderRequest:
      type: object
      required: [clientId, workOrderNumber, title, workType]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        workOrderNumber: { type: string }
        title: { type: string }
        description: { type: string, nullable: true }
        workType: { type: string }

    UpdateWorkOrderRequest:
      type: object
      minProperties: 1
      properties:
        title: { type: string }
        description: { type: string, nullable: true }
        workType: { type: string }

    UpdateWorkOrderContextRequest:
      type: object
      properties:
        assetId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        functionalLocationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true

    AssignWorkOrderRequest:
      type: object
      required: [assigneeType]
      properties:
        assigneeType: { $ref: "#/components/schemas/WorkOrderAssigneeType" }
        workforceProfileId: { $ref: "#/components/schemas/Uuid" }
        teamId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }

    DeactivateWorkOrderAssignmentRequest:
      type: object
      required: [status]
      properties:
        status: { $ref: "#/components/schemas/WorkOrderAssignmentStatus" }

    WorkOrderActionRequest:
      type: object
      properties:
        notes: { type: string, nullable: true }

    CompleteWorkOrderRequest:
      type: object
      properties:
        completionSummary: { type: string, nullable: true }
        completionNotes: { type: string, nullable: true }

    WorkOrderEvidenceRequirement:
      type: object
      description: A BE-07 evidence requirement bound to a Work Order.
      required:
        [id, clientId, workOrderId, evidenceType, required, minimumCount,
         maximumCount, description, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        evidenceType:
          type: string
          enum: [PHOTO, DOCUMENT, SIGNATURE]
        required: { type: boolean }
        minimumCount: { type: integer }
        maximumCount: { type: integer, nullable: true }
        description: { type: string, nullable: true }
        status: { type: string }

    WorkOrderEvidence:
      type: object
      description: A BE-07 evidence submission bound to a Work Order.
      required:
        [id, clientId, workOrderId, evidenceRequirementId, evidenceType,
         fileReference, originalFileName, mimeType, fileSize, capturedAt,
         submittedByUserId, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        evidenceRequirementId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        evidenceType:
          type: string
          enum: [PHOTO, DOCUMENT, SIGNATURE]
        fileReference: { type: string }
        originalFileName: { type: string }
        mimeType: { type: string }
        fileSize: { type: integer }
        capturedAt: { type: string, format: date-time, nullable: true }
        submittedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        status: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    SubmitWorkOrderEvidenceRequest:
      type: object
      required: [evidenceType, fileReference, originalFileName, mimeType, fileSize]
      properties:
        evidenceType:
          type: string
          enum: [PHOTO, DOCUMENT, SIGNATURE]
        evidenceRequirementId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        fileReference: { type: string }
        originalFileName: { type: string }
        mimeType: { type: string }
        fileSize: { type: integer }
        capturedAt: { type: string, format: date-time, nullable: true }

    WorkOrderVerification:
      type: object
      description: A BE-07 review used as Work Order verification.
      required: [id, clientId, workOrderId, reviewerUserId, decision, notes, reviewedAt, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        reviewerUserId: { $ref: "#/components/schemas/Uuid" }
        decision: { $ref: "#/components/schemas/ReviewDecision" }
        notes: { type: string, nullable: true }
        reviewedAt: { type: string, format: date-time }
        status: { type: string }

    WorkOrderVerificationState:
      type: object
      description: The resolved Work Order verification state.
      required: [workOrderId, status, latestVerification, verifications]
      properties:
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        status: { type: string }
        latestVerification:
          allOf: [{ $ref: "#/components/schemas/WorkOrderVerification" }]
          nullable: true
        verifications:
          type: array
          items: { $ref: "#/components/schemas/WorkOrderVerification" }

    SubmitWorkOrderVerificationRequest:
      type: object
      required: [decision]
      properties:
        decision: { $ref: "#/components/schemas/ReviewDecision" }
        notes: { type: string, nullable: true }

    WorkOrderHistoryEvent:
      type: object
      description: A Work Order history event (BE-07 operational event).
      required:
        [id, workOrderId, clientId, buildingId, eventType, actorUserId,
         summary, metadata, occurredAt, createdAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        eventType: { type: string }
        actorUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        summary: { type: string }
        metadata: { type: object, additionalProperties: true }
        occurredAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }

    EntitlementStatus:
      type: string
      enum: [ACTIVE, SUSPENDED, EXPIRED, REVOKED]

    Entitlement:
      type: object
      description: A module entitlement (BE-02C).
      required: [id, subscriptionId, moduleId, status, startsAt, endsAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        subscriptionId: { $ref: "#/components/schemas/Uuid" }
        moduleId: { $ref: "#/components/schemas/Uuid" }
        status: { $ref: "#/components/schemas/EntitlementStatus" }
        startsAt: { type: string, format: date-time }
        endsAt: { type: string, format: date-time, nullable: true }

    AssetIdentifierType:
      type: string
      enum: [QR, TAG, BARCODE, LEGACY]

    AssetIdentifierStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    AssetIdentifier:
      type: object
      required: [id, assetId, identifierType, identifierValue, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        identifierType: { $ref: "#/components/schemas/AssetIdentifierType" }
        identifierValue: { type: string }
        status: { $ref: "#/components/schemas/AssetIdentifierStatus" }

    AssetHistoryEvent:
      type: object
      description: One asset history event (BE-05I).
      required: [id, assetId, eventType, actorUserId, summary, metadata, createdAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        eventType: { type: string }
        actorUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        summary: { type: string }
        metadata: { type: object, additionalProperties: true }
        createdAt: { type: string, format: date-time }

    AssetWarranty:
      type: object
      description: An asset warranty (BE-05F).
      required:
        [id, assetId, providerName, warrantyNumber, startDate, endDate,
         coverageDescription, status, isCurrentlyCovered]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        providerName: { type: string }
        warrantyNumber: { type: string }
        startDate: { type: string, format: date }
        endDate: { type: string, format: date }
        coverageDescription: { type: string, nullable: true }
        status: { type: string }
        isCurrentlyCovered: { type: boolean }

    AssetCertification:
      type: object
      description: An asset certification (BE-05G).
      required:
        [id, assetId, certificationType, certificateNumber, issuingAuthority,
         issueDate, expiryDate, status, notes, isCurrentlyEffective]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        certificationType: { type: string }
        certificateNumber: { type: string }
        issuingAuthority: { type: string }
        issueDate: { type: string, format: date }
        expiryDate: { type: string, format: date, nullable: true }
        status: { type: string }
        notes: { type: string, nullable: true }
        isCurrentlyEffective: { type: boolean }

    ChecklistExecution:
      type: object
      description: A checklist execution (checklist_executions projection).
      required: [id, clientId, checklistTemplateId, status]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        checklistTemplateId:
          $ref: "#/components/schemas/Uuid"
        status:
          type: string
          enum: [DRAFT, IN_PROGRESS, COMPLETED, CANCELLED]
        startedAt:
          type: string
          format: date-time
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    ChecklistItemResponse:
      type: object
      description: >
        A saved checklist item response (raw joined projection:
        checklist_item_responses × checklist_items).
      required: [id, checklist_execution_id, checklist_item_id, code, item_type]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        checklist_execution_id:
          $ref: "#/components/schemas/Uuid"
        checklist_item_id:
          $ref: "#/components/schemas/Uuid"
        value:
          type: object
          description: Item value (boolean / number / string per item type).
        result:
          type: string
          nullable: true
        notes:
          type: string
          nullable: true
        code:
          type: string
          description: Checklist item code.
        item_type:
          type: string
          description: Checklist item type.
        required:
          type: boolean
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # CR-BE-MOB-CONTRACT-01 PART 04 — Checklist / Measurement / UOM /
    # Evidence Requirement schemas (mirror the BE-07 DTOs).
    # ------------------------------------------------------------------
    ChecklistResponsesRequest:
      type: object
      description: >
        One or more checklist item responses to save. May be a single object
        or an array of objects; each `value` must match the item's `itemType`
        (CHECK/BOOLEAN → boolean, NUMBER → number, TEXT → string).
      required: [itemId, value]
      properties:
        itemId:
          $ref: "#/components/schemas/Uuid"
        value:
          description: boolean / number / string per the item type.
        result:
          type: string
          nullable: true
        notes:
          type: string
          nullable: true

    ChecklistTemplate:
      type: object
      required: [id, clientId, code, name, description, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/ChecklistTemplateStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    ChecklistTemplateStatus:
      type: string
      enum: [DRAFT, ACTIVE, INACTIVE]

    CreateChecklistTemplateRequest:
      type: object
      required: [code, name]
      properties:
        code: { type: string, description: Normalized to uppercase. }
        name: { type: string }
        description: { type: string }
        status: { $ref: "#/components/schemas/ChecklistTemplateStatus" }

    UpdateChecklistTemplateRequest:
      type: object
      minProperties: 1
      properties:
        name: { type: string }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/ChecklistTemplateStatus" }

    ChecklistItem:
      type: object
      required:
        [id, checklistTemplateId, code, label, itemType, required, displayOrder,
         status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        label: { type: string }
        itemType: { $ref: "#/components/schemas/ChecklistItemType" }
        required: { type: boolean }
        displayOrder: { type: integer }
        status: { $ref: "#/components/schemas/ChecklistItemStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    ChecklistItemType:
      type: string
      enum: [CHECK, BOOLEAN, TEXT, NUMBER]

    ChecklistItemStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    CreateChecklistItemRequest:
      type: object
      required: [code, label, itemType, displayOrder]
      properties:
        code: { type: string, description: Normalized to uppercase. }
        label: { type: string }
        itemType: { $ref: "#/components/schemas/ChecklistItemType" }
        required: { type: boolean }
        displayOrder: { type: integer }
        status: { $ref: "#/components/schemas/ChecklistItemStatus" }

    UpdateChecklistItemRequest:
      type: object
      minProperties: 1
      properties:
        label: { type: string }
        required: { type: boolean }
        displayOrder: { type: integer }
        itemType: { $ref: "#/components/schemas/ChecklistItemType" }
        status: { $ref: "#/components/schemas/ChecklistItemStatus" }

    Uom:
      type: object
      required: [id, clientId, code, name, symbol, category, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        symbol: { type: string }
        category: { type: string }
        status: { $ref: "#/components/schemas/UomStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    UomStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    CreateUomRequest:
      type: object
      required: [code, name, symbol, category]
      properties:
        code: { type: string, description: Normalized to uppercase. }
        name: { type: string }
        symbol: { type: string }
        category: { type: string }

    UpdateUomRequest:
      type: object
      minProperties: 1
      properties:
        name: { type: string }
        symbol: { type: string }
        category: { type: string }
        status: { $ref: "#/components/schemas/UomStatus" }

    BindMeasurementRequest:
      type: object
      required: [uomId]
      properties:
        uomId: { $ref: "#/components/schemas/Uuid" }
        minimumValue: { type: number, nullable: true }
        maximumValue: { type: number, nullable: true }
        decimalPrecision: { type: integer, nullable: true }

    MeasurementBoundItem:
      type: object
      description: >
        The definition row (checklist item / form field) after a measurement
        binding, including the bound `uomId` and min/max/decimal precision.
      required: [id, uomId, minimumValue, maximumValue, decimalPrecision]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        uomId: { $ref: "#/components/schemas/Uuid" }
        minimumValue: { type: number, nullable: true }
        maximumValue: { type: number, nullable: true }
        decimalPrecision: { type: integer, nullable: true }

    EvidenceRequirementTargetType:
      type: string
      enum:
        [FORM_TEMPLATE, FORM_VERSION, FORM_SECTION, FORM_FIELD,
         CHECKLIST_TEMPLATE, CHECKLIST_ITEM]

    EvidenceRequirement:
      type: object
      required:
        [id, clientId, targetType, targetId, evidenceType, required,
         minimumCount, maximumCount, description, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        targetType: { $ref: "#/components/schemas/EvidenceRequirementTargetType" }
        targetId: { $ref: "#/components/schemas/Uuid" }
        evidenceType:
          type: string
          enum: [PHOTO, DOCUMENT, SIGNATURE]
        required: { type: boolean }
        minimumCount: { type: integer }
        maximumCount: { type: integer, nullable: true }
        description: { type: string, nullable: true }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateEvidenceRequirementRequest:
      type: object
      required: [targetType, targetId, evidenceType]
      properties:
        targetType: { $ref: "#/components/schemas/EvidenceRequirementTargetType" }
        targetId: { $ref: "#/components/schemas/Uuid" }
        evidenceType:
          type: string
          enum: [PHOTO, DOCUMENT, SIGNATURE]
        required: { type: boolean, description: Defaults to true. }
        minimumCount: { type: integer, description: Defaults to 0. }
        maximumCount: { type: integer, nullable: true }
        description: { type: string, nullable: true }

    UpdateEvidenceRequirementRequest:
      type: object
      minProperties: 1
      properties:
        required: { type: boolean }
        minimumCount: { type: integer }
        maximumCount: { type: integer, nullable: true }
        description: { type: string, nullable: true }
        status: { type: string, enum: [ACTIVE, INACTIVE] }

    SubmitEvidenceRequest:
      type: object
      required: [evidenceType, executionType, executionId, fileReference, originalFileName, mimeType, fileSize]
      properties:
        evidenceType:
          type: string
          enum: [PHOTO, DOCUMENT, SIGNATURE]
        executionType:
          type: string
          enum: [FORM_INSTANCE, CHECKLIST_EXECUTION]
        executionId: { $ref: "#/components/schemas/Uuid" }
        evidenceRequirementId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        fileReference:
          type: string
          description: Backend storage key reference (e.g. evidence/<uuid>).
        originalFileName: { type: string }
        mimeType: { type: string }
        fileSize:
          type: integer
          description: Bytes; ≤ 52,428,800 (50 MB).
        capturedAt:
          type: string
          format: date-time
          nullable: true

    Review:
      type: object
      description: A review of a form instance or checklist execution.
      required: [id, clientId, targetType, targetId, reviewerUserId, status]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        targetType:
          type: string
          enum: [FORM_INSTANCE, CHECKLIST_EXECUTION]
        targetId:
          $ref: "#/components/schemas/Uuid"
        reviewerUserId:
          $ref: "#/components/schemas/Uuid"
        decision:
          type: string
          nullable: true
        notes:
          type: string
          nullable: true
        reviewedAt:
          type: string
          format: date-time
          nullable: true
        status:
          type: string
          description: Review status (OPEN/COMPLETED).
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    OperationalEventSource:
      type: string
      description: Execution source persisted on new operational events; null is retained for historical rows.
      enum: [HTTP, SCHEDULER, SYSTEM]
    OperationalEvent:
      type: object
      description: >
        A scoped append-only operational audit event. `requestId` is the
        server-generated HTTP request UUID or governed background-run UUID;
        historical rows may return null. `source` identifies the execution
        boundary and is null only for historical/standalone rows without a
        governed context. Metadata is scrubbed and bounded; raw payloads,
        credentials, and provider secrets are never exposed.
      required: [id, clientId, eventType, entityType, entityId, requestId, source, occurredAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        eventType:
          type: string
        entityType:
          type: string
        entityId:
          $ref: "#/components/schemas/Uuid"
        actorUserId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        requestId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
          description: Server-generated request or governed background-run correlation UUID; null for historical/standalone rows.
        source:
          allOf:
            - $ref: "#/components/schemas/OperationalEventSource"
          nullable: true
        summary:
          type: string
          nullable: true
        metadata:
          type: object
          additionalProperties: true
          description: Scrubbed, bounded metadata only; secrets and raw payloads are omitted.
        occurredAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
    FindingAvailableActions:
      type: object
      description: >
        Backend-authoritative available actions for a finding. Mobile renders
        only these actions and never infers workflow authority.
      required: [findingId, state, availableActions]
      properties:
        findingId:
          $ref: "#/components/schemas/Uuid"
        state:
          type: string
          description: Current finding workflow state.
        availableActions:
          type: array
          items:
            type: string
          description: >
            Actions the calling user may perform now (ASSIGN, MARK_ASSIGNED,
            START, SUBMIT_FOR_REVIEW, OPEN_REVIEW, APPROVE, REJECT,
            REQUEST_REWORK, RESUBMIT, CLOSE, CANCEL).
    FindingState:
      type: object
      description: The current finding workflow state.
      required: [findingId, state, stateChangedAt]
      properties:
        findingId:
          $ref: "#/components/schemas/Uuid"
        state:
          type: string
        stateChangedAt:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # CR-BE-MOB-CONTRACT-01 PART 05 — Finding / Rework / Verification.
    # Shapes mirror the BE-09 DTOs exactly.
    # ------------------------------------------------------------------
    FindingStatus:
      type: string
      enum:
        [OPEN, ASSIGNED, IN_PROGRESS, PENDING_REVIEW, REJECTED,
         REWORK_REQUIRED, RESUBMITTED, VERIFIED, CLOSED, CANCELLED]

    FindingSourceType:
      type: string
      enum: [FORM_INSTANCE, CHECKLIST_EXECUTION, WORK_ORDER]

    Finding:
      type: object
      description: >
        The authoritative Finding record (BE-09). `status` is the backend
        lifecycle authority; clients must not infer transitions from it — the
        per-caller `availableActions` comes from
        `GET /findings/:id/available-actions`. `sourceType`/`sourceId` bind the
        finding to a checklist execution, form instance, or work order.
      required:
        [id, clientId, buildingId, findingNumber, title, description,
         classificationId, severityId, sourceType, sourceId, status,
         stateChangedAt, reportedByUserId, reportedAt, closedAt, closedByUserId,
         closureNotes, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        findingNumber: { type: string }
        title: { type: string }
        description: { type: string, nullable: true }
        classificationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        severityId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        sourceType:
          allOf: [{ $ref: "#/components/schemas/FindingSourceType" }]
          nullable: true
        sourceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        status: { $ref: "#/components/schemas/FindingStatus" }
        stateChangedAt: { type: string, format: date-time }
        reportedByUserId: { $ref: "#/components/schemas/Uuid" }
        reportedAt: { type: string, format: date-time }
        closedAt: { type: string, format: date-time, nullable: true }
        closedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        closureNotes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateFindingRequest:
      type: object
      required: [clientId, findingNumber, title]
      properties:
        clientId:
          $ref: "#/components/schemas/Uuid"
          description: The finding's Client (must match the Building's Client).
        findingNumber:
          type: string
          description: >-
            Normalized uppercase; 2-64 chars of uppercase letters, digits,
            hyphens, underscores.
        title: { type: string }
        description: { type: string, nullable: true }

    UpdateFindingRequest:
      type: object
      minProperties: 1
      properties:
        title: { type: string }
        description: { type: string, nullable: true }
        classificationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        severityId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true

    TransitionFindingStateRequest:
      type: object
      required: [state]
      properties:
        state: { $ref: "#/components/schemas/FindingStatus" }

    FindingSourceState:
      type: object
      description: >
        The finding's source binding and its resolved source context
        (reference type/id/code/title).
      required: [findingId, sourceType, sourceId, context]
      properties:
        findingId: { $ref: "#/components/schemas/Uuid" }
        sourceType:
          allOf: [{ $ref: "#/components/schemas/FindingSourceType" }]
          nullable: true
        sourceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        context:
          type: object
          nullable: true
          properties:
            sourceType: { $ref: "#/components/schemas/FindingSourceType" }
            sourceId: { $ref: "#/components/schemas/Uuid" }
            clientId: { $ref: "#/components/schemas/Uuid" }
            buildingId:
              allOf: [{ $ref: "#/components/schemas/Uuid" }]
              nullable: true
            status: { type: string }
            referenceType: { type: string }
            referenceId: { $ref: "#/components/schemas/Uuid" }
            referenceCode: { type: string, nullable: true }
            title: { type: string, nullable: true }

    UpdateFindingSourceRequest:
      type: object
      properties:
        sourceType:
          allOf: [{ $ref: "#/components/schemas/FindingSourceType" }]
          nullable: true
        sourceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true

    FindingReworkStatus:
      type: string
      enum: [REQUESTED, RESUBMITTED]

    FindingRework:
      type: object
      description: One finding rework cycle.
      required:
        [id, findingId, reviewId, requestedByUserId, reason, reworkNotes,
         resubmittedByUserId, requestedAt, resubmittedAt, status, createdAt,
         updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        findingId: { $ref: "#/components/schemas/Uuid" }
        reviewId: { $ref: "#/components/schemas/Uuid" }
        requestedByUserId: { $ref: "#/components/schemas/Uuid" }
        reason: { type: string }
        reworkNotes: { type: string, nullable: true }
        resubmittedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        requestedAt: { type: string, format: date-time }
        resubmittedAt: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/FindingReworkStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    FindingReworkContext:
      type: object
      description: The finding's rework context (current cycle + all cycles).
      required: [findingId, findingState, current, cycles]
      properties:
        findingId: { $ref: "#/components/schemas/Uuid" }
        findingState: { type: string }
        current:
          allOf: [{ $ref: "#/components/schemas/FindingRework" }]
          nullable: true
        cycles:
          type: array
          items: { $ref: "#/components/schemas/FindingRework" }

    RequestReworkRequest:
      type: object
      required: [reason]
      properties:
        reason:
          type: string
          description: Required rework/rejection reason (≤ 2000 chars).

    UpdateReworkNotesRequest:
      type: object
      required: [notes]
      properties:
        notes: { type: string, description: Required (≤ 2000 chars). }

    FindingReviewStatus:
      type: string
      enum: [PENDING, COMPLETED]

    ReviewDecision:
      type: string
      enum: [APPROVED, REJECTED, REWORK_REQUIRED]

    FindingReview:
      type: object
      description: A finding review / verification record.
      required:
        [id, clientId, findingId, reviewerUserId, decision, notes, reviewedAt,
         status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        findingId: { $ref: "#/components/schemas/Uuid" }
        reviewerUserId: { $ref: "#/components/schemas/Uuid" }
        decision:
          allOf: [{ $ref: "#/components/schemas/ReviewDecision" }]
          nullable: true
        notes: { type: string, nullable: true }
        reviewedAt: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/FindingReviewStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    FindingVerificationState:
      type: object
      description: The finding's supervisor verification state.
      required: [findingId, state, currentReview, latestVerification, reviews]
      properties:
        findingId: { $ref: "#/components/schemas/Uuid" }
        state: { type: string }
        currentReview:
          allOf: [{ $ref: "#/components/schemas/FindingReview" }]
          nullable: true
        latestVerification:
          allOf: [{ $ref: "#/components/schemas/FindingReview" }]
          nullable: true
        reviews:
          type: array
          items: { $ref: "#/components/schemas/FindingReview" }

    OpenFindingReviewRequest:
      type: object
      properties:
        notes: { type: string, nullable: true }

    SubmitFindingVerificationRequest:
      type: object
      required: [decision]
      properties:
        decision: { $ref: "#/components/schemas/ReviewDecision" }
        notes: { type: string, nullable: true }

    FindingAssigneeType:
      type: string
      enum: [WORKFORCE, TEAM, VENDOR, VENDOR_WORKFORCE]

    FindingAssignmentStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    FindingAssignment:
      type: object
      required:
        [id, findingId, assigneeType, workforceProfileId, teamId, vendorId,
         assignedByUserId, assignedAt, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        findingId: { $ref: "#/components/schemas/Uuid" }
        assigneeType: { $ref: "#/components/schemas/FindingAssigneeType" }
        workforceProfileId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        teamId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        vendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        assignedByUserId: { $ref: "#/components/schemas/Uuid" }
        assignedAt: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/FindingAssignmentStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    AssignFindingRequest:
      type: object
      required: [assigneeType]
      properties:
        assigneeType: { $ref: "#/components/schemas/FindingAssigneeType" }
        workforceProfileId: { $ref: "#/components/schemas/Uuid" }
        teamId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }

    DeactivateFindingAssignmentRequest:
      type: object
      required: [status]
      properties:
        status: { $ref: "#/components/schemas/FindingAssignmentStatus" }

    FindingHistoryEvent:
      type: object
      description: One append-only finding audit/history event.
      required:
        [id, findingId, clientId, buildingId, eventType, actorUserId, summary,
         metadata, occurredAt, createdAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        findingId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        eventType: { type: string }
        actorUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        summary: { type: string }
        metadata: { type: object, additionalProperties: true }
        occurredAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }

    FindingClosureInfo:
      type: object
      description: The finding closure readiness + provenance.
      required:
        [findingId, state, ready, latestVerificationId,
         latestVerificationDecision, reworkPending, closedAt, closedByUserId,
         closureNotes]
      properties:
        findingId: { $ref: "#/components/schemas/Uuid" }
        state: { type: string }
        ready: { type: boolean }
        latestVerificationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        latestVerificationDecision:
          allOf: [{ $ref: "#/components/schemas/ReviewDecision" }]
          nullable: true
        reworkPending: { type: boolean }
        closedAt: { type: string, format: date-time, nullable: true }
        closedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        closureNotes: { type: string, nullable: true }

    CloseFindingRequest:
      type: object
      properties:
        closureNotes: { type: string, nullable: true }

    AssetResolution:
      type: object
      description: Resolved asset identifier (QR-ready lookup).
      required: [identifier, asset]
      properties:
        identifier:
          type: object
          required: [id, identifierType, identifierValue]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
            identifierType:
              type: string
            identifierValue:
              type: string
        asset:
          type: object
          required: [id, buildingId, assetCode, assetName, status]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
            buildingId:
              $ref: "#/components/schemas/Uuid"
            assetCode:
              type: string
            assetName:
              type: string
            status:
              type: string
            functionalLocationId:
              allOf:
                - $ref: "#/components/schemas/Uuid"
              nullable: true

    # ------------------------------------------------------------------
    # CR-BE-MOB-CONTRACT-01 PART 06 — Location hierarchy, Asset/Equipment,
    # and operational context schemas. Shapes mirror the BE-04 / BE-05 DTOs
    # and the BE-04H read projections exactly.
    # ------------------------------------------------------------------
    StructureNode:
      type: object
      description: Minimal authoritative summary of one structure node.
      required: [id, code, name, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        status: { type: string }

    FloorNode:
      allOf:
        - $ref: "#/components/schemas/StructureNode"
        - type: object
          required: [levelNumber]
          properties:
            levelNumber: { type: integer }

    AreaNode:
      allOf:
        - $ref: "#/components/schemas/StructureNode"
        - type: object
          required: [type]
          properties:
            type: { $ref: "#/components/schemas/AreaType" }

    RoomNode:
      allOf:
        - $ref: "#/components/schemas/StructureNode"
        - type: object
          required: [roomType]
          properties:
            roomType:
              allOf: [{ $ref: "#/components/schemas/StructureNode" }]
              nullable: true

    FunctionalLocationNode:
      allOf:
        - $ref: "#/components/schemas/StructureNode"
        - type: object
          required: [spaceId]
          properties:
            spaceId:
              allOf: [{ $ref: "#/components/schemas/Uuid" }]
              nullable: true

    OperationalContext:
      type: object
      description: >
        The authoritative operational location context for one node. Only the
        levels that actually exist are present — missing hierarchy levels are
        never invented. `campus` appears only when the Building is grouped
        under one.
      required: [client, property, building]
      properties:
        client: { $ref: "#/components/schemas/StructureNode" }
        property: { $ref: "#/components/schemas/StructureNode" }
        campus: { $ref: "#/components/schemas/StructureNode" }
        building: { $ref: "#/components/schemas/StructureNode" }
        floor: { $ref: "#/components/schemas/FloorNode" }
        area: { $ref: "#/components/schemas/AreaNode" }
        room: { $ref: "#/components/schemas/RoomNode" }
        roomType: { $ref: "#/components/schemas/StructureNode" }
        space: { $ref: "#/components/schemas/StructureNode" }
        functionalLocation: { $ref: "#/components/schemas/FunctionalLocationNode" }

    SpaceHierarchy:
      allOf:
        - $ref: "#/components/schemas/StructureNode"
        - type: object
          required: [functionalLocations]
          properties:
            functionalLocations:
              type: array
              items: { $ref: "#/components/schemas/FunctionalLocationNode" }

    RoomHierarchy:
      allOf:
        - $ref: "#/components/schemas/RoomNode"
        - type: object
          required: [spaces]
          properties:
            spaces:
              type: array
              items: { $ref: "#/components/schemas/SpaceHierarchy" }

    AreaHierarchy:
      allOf:
        - $ref: "#/components/schemas/AreaNode"
        - type: object
          required: [rooms]
          properties:
            rooms:
              type: array
              items: { $ref: "#/components/schemas/RoomHierarchy" }

    FloorHierarchy:
      allOf:
        - $ref: "#/components/schemas/FloorNode"
        - type: object
          required: [areas]
          properties:
            areas:
              type: array
              items: { $ref: "#/components/schemas/AreaHierarchy" }

    BuildingHierarchy:
      type: object
      description: >
        The full authoritative digital structure of one Building (BE-04H):
        floors → areas → rooms (with Room Type) → spaces (with pinned
        Functional Locations), plus Building-level Functional Locations.
      required: [client, property, building, floors, functionalLocations]
      properties:
        client: { $ref: "#/components/schemas/StructureNode" }
        property: { $ref: "#/components/schemas/StructureNode" }
        campus: { $ref: "#/components/schemas/StructureNode" }
        building: { $ref: "#/components/schemas/StructureNode" }
        floors:
          type: array
          items: { $ref: "#/components/schemas/FloorHierarchy" }
        functionalLocations:
          type: array
          items: { $ref: "#/components/schemas/FunctionalLocationNode" }

    FloorStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    PublicFloor:
      type: object
      required: [id, buildingId, code, name, levelNumber, description, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        levelNumber: { type: integer }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/FloorStatus" }

    AreaType:
      type: string
      enum: [AREA, ZONE]

    AreaStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    PublicArea:
      type: object
      required: [id, floorId, code, name, type, description, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        floorId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        type: { $ref: "#/components/schemas/AreaType" }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/AreaStatus" }

    RoomStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    PublicRoom:
      type: object
      required: [id, areaId, roomTypeId, code, name, description, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        areaId: { $ref: "#/components/schemas/Uuid" }
        roomTypeId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/RoomStatus" }

    SpaceStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    PublicSpace:
      type: object
      required: [id, roomId, code, name, description, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        roomId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/SpaceStatus" }

    FunctionalLocationStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    PublicFunctionalLocation:
      type: object
      required: [id, buildingId, spaceId, code, name, description, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        spaceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/FunctionalLocationStatus" }

    AssetStatus:
      type: string
      enum: [ACTIVE, INACTIVE, UNDER_MAINTENANCE, RETIRED]

    PublicAsset:
      type: object
      required:
        [id, clientId, buildingId, assetCategoryId, assetTypeId,
         functionalLocationId, assetCode, assetName, description, manufacturer,
         model, serialNumber, status, previousStatus, statusChangedAt,
         statusReason]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        assetCategoryId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        assetTypeId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        functionalLocationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        assetCode: { type: string }
        assetName: { type: string }
        description: { type: string, nullable: true }
        manufacturer: { type: string, nullable: true }
        model: { type: string, nullable: true }
        serialNumber: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/AssetStatus" }
        previousStatus:
          allOf: [{ $ref: "#/components/schemas/AssetStatus" }]
          nullable: true
        statusChangedAt: { type: string, format: date-time, nullable: true }
        statusReason: { type: string, nullable: true }

    AssetLocation:
      type: object
      description: >
        The asset plus its authoritative resolved operational location context
        (BE-05C). `location` is the BE-04H projection, never stored.
      required: [asset, location]
      properties:
        asset: { $ref: "#/components/schemas/PublicAsset" }
        location: { $ref: "#/components/schemas/OperationalContext" }

    EquipmentProfileStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    PublicEquipmentProfile:
      type: object
      required:
        [id, assetId, equipmentCode, equipmentName, manufacturer, model,
         serialNumber, specification, capacity, unitOfMeasure,
         installationDate, commissioningDate, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        equipmentCode: { type: string }
        equipmentName: { type: string }
        manufacturer: { type: string, nullable: true }
        model: { type: string, nullable: true }
        serialNumber: { type: string, nullable: true }
        specification: { type: string, nullable: true }
        capacity: { type: number, nullable: true }
        unitOfMeasure: { type: string, nullable: true }
        installationDate: { type: string, format: date-time, nullable: true }
        commissioningDate: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/EquipmentProfileStatus" }

    # ------------------------------------------------------------------
    # BE-25C — Mobile Assignment Contract. Feed composition over the
    # authoritative BE-07 Task / BE-08 Work Order assignment tables; no
    # separate mobile assignment engine.
    # ------------------------------------------------------------------
    MobileAssignment:
      type: object
      description: >
        One mobile assignment feed item (discriminated by `type`). `id` is
        the assignment record id; `status` is the current authoritative work
        status; `availableActions` is backend-authoritative and gated on the
        caller's manage permissions.
      required:
        [id, type, status, assignee, context, schedule, reference, availableActions]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
          description: Assignment record id (task_assignments / work_order_assignments).
        type:
          type: string
          enum: [TASK, WORK_ORDER]
        status:
          type: string
          description: Current authoritative status of the work itself.
        assignee:
          $ref: "#/components/schemas/MobileAssignmentAssignee"
        context:
          $ref: "#/components/schemas/MobileAssignmentContext"
        schedule:
          $ref: "#/components/schemas/MobileAssignmentSchedule"
        reference:
          $ref: "#/components/schemas/MobileAssignmentReference"
        availableActions:
          type: array
          description: >
            Actions the calling user may perform now (TASK: START/COMPLETE/
            CANCEL; WORK_ORDER: ACKNOWLEDGE/START/HOLD/RESUME/ADD_NOTE/CANCEL,
            plus CLOSE when completion, approval, and BAST readiness permit).
            Empty when the caller lacks the matching manage permission or the
            work is terminal.
          items:
            type: string
    MobileAssignmentAssignee:
      type: object
      description: Assignee / user / workforce context of the assignment.
      required:
        [assigneeType, workforceProfileId, teamId, assignedByUserId, assignedAt, assignmentStatus]
      properties:
        assigneeType:
          type: string
          enum: [WORKFORCE, TEAM]
        workforceProfileId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        teamId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        assignedByUserId:
          $ref: "#/components/schemas/Uuid"
        assignedAt:
          type: string
          format: date-time
        assignmentStatus:
          type: string
          const: ACTIVE
    MobileAssignmentContext:
      type: object
      description: Building / Location context of the assigned work.
      required: [clientId, buildingId, buildingCode, buildingName, locations]
      properties:
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        buildingCode:
          type: string
          nullable: true
        buildingName:
          type: string
          nullable: true
        locations:
          type: array
          description: Asset / functional-location references bound to the work.
          items:
            $ref: "#/components/schemas/MobileAssignmentLocation"
    MobileAssignmentLocation:
      type: object
      required: [type, id, code, name]
      properties:
        type:
          type: string
          enum: [ASSET, FUNCTIONAL_LOCATION]
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string
    MobileAssignmentSchedule:
      type: object
      description: >
        Due / schedule context. `occurrenceAt` carries the generated task's
        schedule context; `dueAt` is always null today (no authoritative due
        date exists for Work Orders) and stays reserved for future SLA
        configuration.
      required: [occurrenceAt, dueAt]
      properties:
        occurrenceAt:
          type: string
          format: date-time
          nullable: true
        dueAt:
          type: string
          format: date-time
          nullable: true
    MobileAssignmentReference:
      type: object
      description: >
        Type-specific work reference. TASK items populate the task fields;
        WORK_ORDER items populate the work-order fields (never both).
      properties:
        taskId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        scheduleDefinitionId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        targetType:
          type: string
          nullable: true
        targetId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        generatedAt:
          type: string
          format: date-time
          nullable: true
        workOrderId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        workOrderNumber:
          type: string
          nullable: true
        title:
          type: string
          nullable: true
        description:
          type: string
          nullable: true
        workType:
          type: string
          nullable: true
        priority:
          type: string
          nullable: true
        startedAt:
          type: string
          format: date-time
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
        completedByUserId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        completionSummary:
          type: string
          nullable: true
        completionNotes:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
          nullable: true

    # ------------------------------------------------------------------
    # BE-25D — Mobile Checklist Contract. Read-model composition over the
    # BE-07 Checklist Template / Execution / Measurement / Evidence
    # authorities; no separate mobile checklist engine.
    # ------------------------------------------------------------------
    MobileChecklistExecution:
      type: object
      description: >
        The mobile checklist execution contract. `id` is the checklist
        execution id; `status` is the authoritative execution status;
        `availableActions` is backend-authoritative (START / SAVE_RESPONSES /
        COMPLETE / CANCEL, matching the execution endpoints).
      required:
        [id, clientId, checklist, task, status, items, evidenceRequirements, availableActions]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        checklist:
          $ref: "#/components/schemas/MobileChecklistTemplateReference"
        task:
          allOf:
            - $ref: "#/components/schemas/MobileChecklistTaskReference"
          nullable: true
          description: Task reference when the execution's template is a task target; else null.
        status:
          type: string
          enum: [DRAFT, IN_PROGRESS, COMPLETED, CANCELLED]
        startedAt:
          type: string
          format: date-time
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        items:
          type: array
          description: Active template items (ordered) with response values/status.
          items:
            $ref: "#/components/schemas/MobileChecklistItem"
        evidenceRequirements:
          type: array
          description: Evidence requirements bound to the execution's template (template level).
          items:
            $ref: "#/components/schemas/MobileChecklistEvidenceRequirement"
        availableActions:
          type: array
          items:
            type: string
            enum: [START, SAVE_RESPONSES, COMPLETE, CANCEL]
    MobileChecklistTemplateReference:
      type: object
      required: [id, clientId, code, name, status]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string
        description:
          type: string
          nullable: true
        status:
          type: string
          enum: [DRAFT, ACTIVE, INACTIVE]
    MobileChecklistTaskReference:
      type: object
      description: Task reference of the execution (first generated task targeting the template).
      required: [taskId, occurrenceAt, targetId, taskStatus]
      properties:
        taskId:
          $ref: "#/components/schemas/Uuid"
        scheduleDefinitionId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        occurrenceAt:
          type: string
          format: date-time
        targetId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        taskStatus:
          type: string
    MobileChecklistItem:
      type: object
      description: One checklist item with its current response value/status.
      required:
        [id, code, label, itemType, required, displayOrder, status, itemStatus, measurement, evidenceRequirements]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        label:
          type: string
        itemType:
          type: string
          enum: [CHECK, BOOLEAN, TEXT, NUMBER]
        required:
          type: boolean
        displayOrder:
          type: integer
        status:
          type: string
          enum: [ACTIVE, INACTIVE]
        itemStatus:
          type: string
          enum: [PENDING, ANSWERED]
          description: Response status within this execution.
        value:
          description: Stored response value (boolean / number / string per itemType); null when unanswered.
          nullable: true
        result:
          type: string
          nullable: true
        notes:
          type: string
          nullable: true
        measurement:
          allOf:
            - $ref: "#/components/schemas/MobileChecklistMeasurement"
          nullable: true
          description: Measurement/UOM for NUMBER items; null otherwise.
        evidenceRequirements:
          type: array
          items:
            $ref: "#/components/schemas/MobileChecklistEvidenceRequirement"
    MobileChecklistMeasurement:
      type: object
      required: [uom, minimumValue, maximumValue, decimalPrecision]
      properties:
        uom:
          allOf:
            - $ref: "#/components/schemas/MobileChecklistUom"
          nullable: true
        minimumValue:
          type: number
          nullable: true
        maximumValue:
          type: number
          nullable: true
        decimalPrecision:
          type: integer
          nullable: true
    MobileChecklistUom:
      type: object
      required: [id, code, name, symbol, category]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string
        symbol:
          type: string
        category:
          type: string
    MobileChecklistEvidenceRequirement:
      type: object
      required: [id, evidenceType, required, minimumCount, maximumCount, description]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        evidenceType:
          type: string
          enum: [PHOTO, DOCUMENT, SIGNATURE]
        required:
          type: boolean
        minimumCount:
          type: integer
        maximumCount:
          type: integer
          nullable: true
        description:
          type: string
          nullable: true

    # ------------------------------------------------------------------
    # BE-25E — Mobile Evidence Upload Contract. Composition over the BE-07
    # Evidence authority + CR-BE-API-01 PART 03 storage abstraction; no
    # separate mobile evidence engine, no bytes in PostgreSQL, no internal
    # storage paths exposed.
    # ------------------------------------------------------------------
    MobileEvidence:
      type: object
      description: >
        The mobile evidence contract. `id` is the evidence submission id;
        `uploadStatus` is UPLOADED when the bytes are storage-backed
        (PENDING for metadata-only rows); `fileReference` is deliberately
        NOT exposed — internal storage paths stay server-side (the download
        endpoint addresses files by evidence id).
      required:
        [id, clientId, building, evidenceRequirement, target, evidenceType, file, status, submittedByUserId, createdAt, updatedAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        building:
          allOf:
            - $ref: "#/components/schemas/MobileEvidenceBuilding"
          nullable: true
          description: Building context when resolvable through the target task; else null.
        evidenceRequirement:
          allOf:
            - $ref: "#/components/schemas/MobileEvidenceRequirementReference"
          nullable: true
        target:
          $ref: "#/components/schemas/MobileEvidenceTarget"
        evidenceType:
          type: string
          enum: [PHOTO, DOCUMENT, SIGNATURE]
        file:
          $ref: "#/components/schemas/MobileEvidenceFile"
        status:
          type: string
          enum: [ACTIVE, REMOVED]
        submittedByUserId:
          $ref: "#/components/schemas/Uuid"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    MobileEvidenceBuilding:
      type: object
      required: [id, code, name]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string
    MobileEvidenceRequirementReference:
      type: object
      required: [id, evidenceType, required, minimumCount, maximumCount, description]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        evidenceType:
          type: string
          enum: [PHOTO, DOCUMENT, SIGNATURE]
        required:
          type: boolean
        minimumCount:
          type: integer
        maximumCount:
          type: integer
          nullable: true
        description:
          type: string
          nullable: true
    MobileEvidenceTarget:
      type: object
      description: Target task/checklist/work reference of the evidence.
      required: [executionType, executionId, checklist, form, task]
      properties:
        executionType:
          type: string
          enum: [FORM_INSTANCE, CHECKLIST_EXECUTION]
        executionId:
          $ref: "#/components/schemas/Uuid"
        checklist:
          type: object
          nullable: true
          required: [id, code, name]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
            code:
              type: string
            name:
              type: string
        form:
          type: object
          nullable: true
          required: [id, code, name]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
            code:
              type: string
            name:
              type: string
        task:
          type: object
          nullable: true
          required: [taskId, occurrenceAt, taskStatus, buildingId]
          properties:
            taskId:
              $ref: "#/components/schemas/Uuid"
            occurrenceAt:
              type: string
              format: date-time
            taskStatus:
              type: string
            buildingId:
              allOf:
                - $ref: "#/components/schemas/Uuid"
              nullable: true
    MobileEvidenceFile:
      type: object
      description: File metadata + upload result of the submission.
      required: [originalFileName, mimeType, fileSize, capturedAt, uploadStatus, fileAvailable]
      properties:
        originalFileName:
          type: string
          nullable: true
        mimeType:
          type: string
          nullable: true
        fileSize:
          type: integer
          description: Bytes; capped at 52,428,800 (50 MB).
        capturedAt:
          type: string
          format: date-time
          nullable: true
        uploadStatus:
          type: string
          enum: [UPLOADED, PENDING]
        fileAvailable:
          type: boolean

    # ------------------------------------------------------------------
    # BE-25F — Mobile QR Resolution Contract. Composition over the BE-05H
    # Asset Identifier resolver + BE-02G access authority; no separate QR
    # engine.
    # ------------------------------------------------------------------
    MobileQrResolution:
      type: object
      description: >
        The mobile QR resolution contract. `identifier` is the opaque
        scanned value (with its type); `targetType` discriminates the
        resolved target (ASSET today); `asset` holds the Asset / Equipment
        context when applicable; `available.actions` are read-model hints —
        backend-authoritative workflow actions stay on the resource
        endpoints.
      required: [identifier, targetType, targetId, clientId, location, asset, available]
      properties:
        identifier:
          type: object
          required: [id, identifierType, identifierValue]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
            identifierType:
              type: string
              enum: [QR, TAG, BARCODE, LEGACY]
            identifierValue:
              type: string
        targetType:
          type: string
          enum: [ASSET]
        targetId:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        location:
          $ref: "#/components/schemas/MobileQrLocationContext"
        asset:
          allOf:
            - $ref: "#/components/schemas/MobileQrAssetContext"
          nullable: true
        available:
          $ref: "#/components/schemas/MobileQrAvailableActions"
    MobileQrLocationContext:
      type: object
      required: [building, functionalLocation]
      properties:
        building:
          allOf:
            - $ref: "#/components/schemas/MobileQrBuilding"
          nullable: true
        functionalLocation:
          allOf:
            - $ref: "#/components/schemas/MobileQrFunctionalLocation"
          nullable: true
    MobileQrBuilding:
      type: object
      required: [id, code, name]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string
    MobileQrFunctionalLocation:
      type: object
      required: [id, code, name, status]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string
        description:
          type: string
          nullable: true
        status:
          type: string
    MobileQrAssetContext:
      type: object
      description: Asset / Equipment context of the resolved target.
      required: [id, assetCode, assetName, status, equipment]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        assetCode:
          type: string
        assetName:
          type: string
        description:
          type: string
          nullable: true
        manufacturer:
          type: string
          nullable: true
        model:
          type: string
          nullable: true
        serialNumber:
          type: string
          nullable: true
        status:
          type: string
        equipment:
          type: object
          nullable: true
          required: [id, equipmentCode, equipmentName, status]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
            equipmentCode:
              type: string
            equipmentName:
              type: string
            manufacturer:
              type: string
              nullable: true
            model:
              type: string
              nullable: true
            serialNumber:
              type: string
              nullable: true
            status:
              type: string
    MobileQrAvailableActions:
      type: object
      description: Available mobile action/context hints for the resolved target.
      required: [actions]
      properties:
        actions:
          type: array
          description: >
            Read-model hints only (VIEW_DETAILS, START_FINDING for ACTIVE
            assets). Workflow authority stays on the resource endpoints.
          items:
            type: string

    # ------------------------------------------------------------------
    # BE-25G — Offline Sync Contract. Operations are executed through the
    # same shared services as the REST endpoints; no separate mobile
    # business engine. Idempotency/conflict handling are later parts.
    # ------------------------------------------------------------------
    MobileSyncBatchRequest:
      type: object
      description: >
        A batch of client-generated offline operations (1..100). Envelope
        violations are rejected with 400 VALIDATION_ERROR before any write;
        per-item business failures are returned as per-item FAILED results.
      required: [operations]
      properties:
        operations:
          type: array
          minItems: 1
          maxItems: 100
          items:
            $ref: "#/components/schemas/MobileSyncOperationItem"
    MobileSyncOperationItem:
      type: object
      description: One client-generated sync operation (idempotent in BE-25H).
      required: [operationId, resourceType, resourceId, operation, clientTimestamp, data]
      properties:
        operationId:
          type: string
          description: >
            Client-generated operation identifier (1-128 chars of letters,
            digits, dots, underscores, hyphens). Unique per user: a retry
            with the same operationId replays the stored result instead of
            executing the write again (BE-25H).
        resourceType:
          type: string
          description: >
            The synced resource kind. A kind exists here ONLY when an existing
            backend operation provides a faithful authoritative write target
            (authoritative id + published operation + same RBAC permission +
            same Building scope + explicit `data` contract). Offline records
            with no such target stay local and are deliberately absent — see
            `x-sync-unsupported-resource-types`.
          enum:
            [TASK_EXECUTION, CHECKLIST_RESPONSES, EVIDENCE_SUBMISSION,
             TASK_ASSIGNMENT, PATROL_EXECUTION, PATROL_POINT_VISIT,
             METER_READING]
        resourceId:
          type: string
          format: uuid
          description: >
            Authoritative resource reference — always an id the backend
            already owns, never client-generated: taskId (TASK_EXECUTION,
            TASK_ASSIGNMENT); checklist executionId (CHECKLIST_RESPONSES,
            EVIDENCE_SUBMISSION); patrol execution id, which IS the BE-07
            taskId (PATROL_EXECUTION, PATROL_POINT_VISIT); BE-07 form
            instance id started from a meter-reading binding (METER_READING).
        operation:
          type: string
          description: >
            PART 06 introduced no new verb. Valid combinations:
            TASK_EXECUTION START|COMPLETE|CANCEL; CHECKLIST_RESPONSES SAVE;
            EVIDENCE_SUBMISSION SUBMIT; TASK_ASSIGNMENT UPDATE;
            PATROL_EXECUTION START|COMPLETE; PATROL_POINT_VISIT SUBMIT;
            METER_READING SUBMIT. Any other pairing is rejected with 400
            VALIDATION_ERROR before any write.
          enum: [START, COMPLETE, CANCEL, SAVE, SUBMIT, UPDATE]
        clientTimestamp:
          type: string
          format: date-time
          description: >
            Required on every operation: the client timestamp of when the
            operation happened on-device. It is echoed on the result and is
            never used as an authoritative server time — the server records
            its own `serverTimestamp`.
        data:
          type: object
          description: >
            Type-specific payload: TASK_EXECUTION: { completionNotes? };
            CHECKLIST_RESPONSES: { responses: array|object }; 
            EVIDENCE_SUBMISSION: { evidenceType, executionType, executionId,
            evidenceRequirementId?, fileReference, originalFileName, mimeType,
            fileSize, capturedAt? }; TASK_ASSIGNMENT: { assignmentId,
            status? }; PATROL_EXECUTION: { completionNotes? } on COMPLETE;
            PATROL_POINT_VISIT: { patrolRoutePointId (required, authoritative
            BE-12B point id), notes? }; METER_READING: { value (required,
            finite number), notes? } — the UOM and the effective measurement
            range stay backend-owned and are NOT accepted from the client.
            Any operation may additionally carry `baseVersion` (BE-25I): the
            resource's `updatedAt` the client last saw; a stale baseVersion
            produces SYNC_CONFLICT. Evidence BYTES are never carried here —
            only metadata, uploaded through the evidence file API.
          additionalProperties: true
    MobileSyncBatchResponse:
      type: object
      description: The batch result; every operation gets its own result item.
      required: [batchId, receivedAt, results]
      properties:
        batchId:
          type: string
          format: uuid
          description: Server-generated batch id for correlation.
        receivedAt:
          type: string
          format: date-time
          description: Server timestamp of when the batch was received.
        results:
          type: array
          items:
            $ref: "#/components/schemas/MobileSyncResultItem"
    MobileSyncResultItem:
      type: object
      description: >
        Per-item success/failure result. `status` is the server result/status
        (SUCCESS | FAILED); `result` carries the written public resource on
        success; `error` carries the authoritative error code/message on
        failure; `serverTimestamp` is the server execution timestamp. On a
        duplicate replay, `result`/`error`/`serverTimestamp` are the ORIGINAL
        stored values (BE-25H).
      required: [operationId, clientTimestamp, success, status, result, error, serverTimestamp]
      properties:
        operationId:
          type: string
        clientTimestamp:
          type: string
          format: date-time
        success:
          type: boolean
        status:
          type: string
          enum: [SUCCESS, FAILED]
        result:
          description: The written result (task / evidence metadata / assignment) on success; null on failure.
          nullable: true
        error:
          type: object
          nullable: true
          required: [code, message]
          properties:
            code:
              type: string
              description: Authoritative ERROR_CODES value (e.g. PERMISSION_DENIED, BUILDING_ACCESS_DENIED, BAD_REQUEST, SYNC_CONFLICT).
            message:
              type: string
            conflict:
              type: object
              nullable: true
              description: BE-25I conflict payload (present on SYNC_CONFLICT failures).
              required: [current, guidance]
              properties:
                current:
                  description: Current authoritative server state/reference of the resource.
                guidance:
                  type: object
                  required: [action, reloadEndpoint, message]
                  properties:
                    action:
                      type: string
                      const: reload
                      description: Reload the resource and re-apply (the backend never merges automatically).
                    reloadEndpoint:
                      type: string
                      description: Endpoint to re-fetch the current state (e.g. /tasks/{taskId}).
                    message:
                      type: string
        serverTimestamp:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # BE-25J — Supervisor Verification Contract. Thin composition over the
    # shared BE-07 review authority and the BE-09 finding verification
    # workflow; no separate mobile verification engine.
    # ------------------------------------------------------------------
    MobileVerification:
      type: object
      description: >
        The mobile supervisor verification contract. `verification.state`
        reflects the current authoritative state (PENDING / VERIFIED /
        REJECTED / REWORK_REQUIRED / NOT_REVIEWABLE for review targets
        including WORK_ORDER; the finding status for FINDING).
        `availableActions` is backend-authoritative. A completed
        verification is immutable. WORK_ORDER delegates to the existing
        BE-08I lifecycle (CR-BE-MOB-03 PART 05).
      required:
        [targetType, targetId, clientId, buildingId, resource, verification, reviewer, availableActions, updatedAt]
      properties:
        targetType:
          type: string
          enum: [CHECKLIST_EXECUTION, FORM_INSTANCE, FINDING, WORK_ORDER]
        targetId:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        resource:
          $ref: "#/components/schemas/MobileVerificationResource"
        verification:
          $ref: "#/components/schemas/MobileVerificationState"
        reviewer:
          allOf:
            - $ref: "#/components/schemas/MobileVerificationReviewer"
          nullable: true
        availableActions:
          type: array
          description: >
            SUBMIT_DECISION for review targets when reviewable and not yet
            verified; the BE-09 verification subset (OPEN_REVIEW / APPROVE /
            REJECT / REQUEST_REWORK) for FINDING.
          items:
            type: string
        updatedAt:
          type: string
          format: date-time
          nullable: true
    MobileVerificationResource:
      type: object
      description: Reviewable resource reference (type-specific).
      required: [status, checklist, form, finding, workOrder]
      properties:
        status:
          type: string
        checklist:
          type: object
          nullable: true
          required: [id, code, name]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
            code:
              type: string
            name:
              type: string
        form:
          type: object
          nullable: true
          required: [id, code, name]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
            code:
              type: string
            name:
              type: string
        finding:
          type: object
          nullable: true
          required: [id, findingNumber, title, status, reportedAt]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
            findingNumber:
              type: string
            title:
              type: string
            status:
              type: string
            reportedAt:
              type: string
              format: date-time
        workOrder:
          type: object
          nullable: true
          required: [id, workOrderNumber, title, status]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
              description: Authoritative `work_orders.id`.
            workOrderNumber:
              type: string
            title:
              type: string
            status:
              type: string
    MobileVerificationState:
      type: object
      required: [state, decision, notes, verifiedAt, reviewId, reviewStatus]
      properties:
        state:
          type: string
          description: >
            NOT_REVIEWABLE / PENDING / VERIFIED / REJECTED / REWORK_REQUIRED
            (review targets including WORK_ORDER) or the authoritative
            finding status (FINDING).
        decision:
          type: string
          enum: [APPROVED, REJECTED, REWORK_REQUIRED]
          nullable: true
        notes:
          type: string
          nullable: true
        verifiedAt:
          type: string
          format: date-time
          nullable: true
        reviewId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        reviewStatus:
          type: string
          enum: [PENDING, COMPLETED]
          nullable: true
    MobileVerificationReviewer:
      type: object
      description: Supervisor / reviewer context (user + linked workforce profile).
      required: [userId, displayName, workforceProfileId, fullName, employeeCode]
      properties:
        userId:
          $ref: "#/components/schemas/Uuid"
        displayName:
          type: string
        workforceProfileId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        fullName:
          type: string
          nullable: true
        employeeCode:
          type: string
          nullable: true

    # ------------------------------------------------------------------
    # BE-25L — Push Token Registration. Bound to the authenticated
    # user/device context; refresh/rotation and deactivation supported.
    # No notification delivery engine exists yet (BE-26).
    # ------------------------------------------------------------------
    PushTokenRegistration:
      type: object
      description: >
        A mobile push token registration. `status` is ACTIVE while the token
        is usable; deactivation keeps the row as INACTIVE history, and a
        provider-rejected registration is retained as INVALID history. The
        listing returns every one of the user's registrations regardless of
        status, so all three values are observable. `registeredAt` is the
        first registration time; `lastSeenAt` refreshes on every
        registration/touch of the device.
      required:
        [id, userId, deviceId, pushToken, platform, status, registeredAt, lastSeenAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        userId:
          $ref: "#/components/schemas/Uuid"
        deviceId:
          type: string
        pushToken:
          type: string
        platform:
          type: string
          enum: [ANDROID, IOS]
        appVersion:
          type: string
          nullable: true
        deviceModel:
          type: string
          nullable: true
        deviceOsVersion:
          type: string
          nullable: true
        status:
          type: string
          enum: [ACTIVE, INACTIVE, INVALID]
          description: >-
            ACTIVE — usable registration. INACTIVE — the user or app
            deliberately unregistered the device. INVALID — the push provider
            rejected the registration (for example the token was unregistered
            on the device) and the backend retired it; the row is retained as
            history and is returned by this endpoint, so a client must treat
            INVALID as "re-register this device". The backend never deletes a
            registration row.
        registeredAt:
          type: string
          format: date-time
        lastSeenAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # BE-26A — Notification foundation (in-app inbox). Foundation only:
    # no push/email/WhatsApp/SMS delivery, no create endpoint.
    # ------------------------------------------------------------------
    Notification:
      type: object
      description: >
        A recipient-scoped in-app notification (the in-app delivery record).
        `channel` is always IN_APP in this foundation. `status` is UNREAD
        until `readAt` is stamped by the mark-read transition.
        `sourceEntityType` / `sourceEntityId` / `sourceEventType` reference
        the operational entity and event that produced the notification
        (operational modules remain the source of truth). `templateKey`
        references the BE-26B template that rendered it (null when created
        directly); `deliveredAt` is when the in-app delivery became visible.
        `metadata` is an opaque JSON object.
      required:
        [id, clientId, recipientUserId, type, channel, status, title,
         sourceEntityType, sourceEntityId, createdAt, deliveredAt, readAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        recipientUserId:
          $ref: "#/components/schemas/Uuid"
        type:
          type: string
          description: Notification type (data-driven, uppercase code).
        channel:
          type: string
          enum: [IN_APP]
        status:
          type: string
          enum: [UNREAD, READ]
        title:
          type: string
        body:
          type: string
          nullable: true
        sourceEntityType:
          type: string
          description: Domain entity type of the source (e.g. WORK_ORDER).
        sourceEntityId:
          $ref: "#/components/schemas/Uuid"
        sourceEventType:
          type: string
          nullable: true
          description: Originating operational event type (e.g. WORK_ORDER_ASSIGNED).
        templateKey:
          type: string
          nullable: true
          description: BE-26B template key that rendered this notification.
        metadata:
          type: object
          additionalProperties: true
        createdAt:
          type: string
          format: date-time
        deliveredAt:
          type: string
          format: date-time
          description: When the in-app delivery became visible to the recipient.
        readAt:
          type: string
          format: date-time
          nullable: true
        updatedAt:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # BE-26B — Notification template foundation. Platform configuration;
    # no recipient resolution and no sending.
    # ------------------------------------------------------------------
    NotificationTemplate:
      type: object
      description: >
        A reusable notification template (rendering recipe). `key` is the
        stable unique template identifier; `type` is the notification type
        stamped on rendered notifications. `subject` / `body` are templates
        with `{{variable}}` placeholders; `variables` lists the declared
        variable names. `channel` is IN_APP in this foundation.
      required: [id, key, type, channel, subject, variables, status, createdAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        key:
          type: string
        type:
          type: string
        channel:
          type: string
          enum: [IN_APP]
        subject:
          type: string
        body:
          type: string
          nullable: true
        variables:
          type: array
          items:
            type: string
        status:
          type: string
          enum: [ACTIVE, INACTIVE]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # BE-26K — Notification history read model. Unified chronological view
    # over in-app / email / WhatsApp deliveries; read-only.
    # ------------------------------------------------------------------
    NotificationHistoryItem:
      type: object
      description: >
        A unified delivery/history entry across channels. `channel` is
        IN_APP / EMAIL / WHATSAPP; `status` carries the channel-specific
        value (UNREAD/READ for in-app; SENT/FAILED for email/WhatsApp).
        Timestamps are populated where available: `sentAt`, `deliveredAt`,
        `failedAt`, and `readAt` (in-app only). `failureReason` is a
        sanitized error message (never contains credentials/tokens).
      required:
        [channel, id, clientId, recipientUserId, status, sentAt, deliveredAt,
         failedAt, readAt, createdAt]
      properties:
        channel:
          type: string
          enum: [IN_APP, EMAIL, WHATSAPP]
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        recipientUserId:
          $ref: "#/components/schemas/Uuid"
        type:
          type: string
          nullable: true
          description: Notification type (in-app only).
        templateKey:
          type: string
          nullable: true
          description: BE-26B template key.
        sourceEntityType:
          type: string
          nullable: true
          description: Source/resource domain type (in-app only).
        sourceEntityId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
          description: Source/resource id (in-app only).
        sourceEventType:
          type: string
          nullable: true
          description: Originating event type (in-app only).
        status:
          type: string
          enum: [UNREAD, READ, SENT, FAILED]
        sentAt:
          type: string
          format: date-time
          nullable: true
        deliveredAt:
          type: string
          format: date-time
          nullable: true
        failedAt:
          type: string
          format: date-time
          nullable: true
        readAt:
          type: string
          format: date-time
          nullable: true
        provider:
          type: string
          nullable: true
          description: Delivery provider discriminator (email/WhatsApp only).
        providerReference:
          type: string
          nullable: true
          description: Provider response reference (email/WhatsApp only).
        failureReason:
          type: string
          nullable: true
          description: Sanitized failure reason (email/WhatsApp only).
        deliveryId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
          description: >
            CR-BE-NOTIFY-PROV-01 PART 04 — the outbound delivery ledger row
            this attempt belongs to (null for in-app rows and pre-ledger
            attempts).
        providerFeedbackStatus:
          type: string
          nullable: true
          enum: [DELIVERED, BOUNCED, COMPLAINT, PROVIDER_FAILED]
          description: >
            CR-BE-NOTIFY-PROV-01 PART 07 — post-acceptance provider feedback
            from the linked ledger row (null when no feedback or no ledger
            linkage). Feedback never reopens the send lifecycle.
        feedbackAt:
          type: string
          format: date-time
          nullable: true
          description: When the provider feedback was accepted.
        createdAt:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # BE-26J — Notification secure link foundation. Opaque, recipient-bound,
    # single-purpose reference; no Notification History.
    # ------------------------------------------------------------------
    NotificationSecureLink:
      type: object
      description: >
        A secure notification link: an opaque, recipient-bound, single-purpose
        reference to a target resource + action. The token hash is never
        exposed; the raw token is returned exactly once at creation.
        `status` is ACTIVE until revoked (REVOKED), expired (EXPIRED), or
        exhausted (USED).
      required:
        [id, clientId, recipientUserId, targetEntityType, targetEntityId,
         action, expiresAt, maxUses, usedCount, status, createdAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        recipientUserId:
          $ref: "#/components/schemas/Uuid"
          description: The bound recipient (resolution requires this user).
        targetEntityType:
          type: string
          description: Target resource type.
        targetEntityId:
          $ref: "#/components/schemas/Uuid"
          description: Target resource id.
        action:
          type: string
          description: Target action (uppercase code).
        expiresAt:
          type: string
          format: date-time
        maxUses:
          type: integer
        usedCount:
          type: integer
        status:
          type: string
          enum: [ACTIVE, REVOKED, EXPIRED, USED]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # BE-26I — Notification escalation foundation. Time-deferred escalation
    # trigger; no scheduler engine, no workflow/approval engine, and no
    # business-resource state changes.
    # ------------------------------------------------------------------
    NotificationEscalation:
      type: object
      description: >
        A time-deferred escalation trigger: references the source/resource
        being escalated, the current (pre-escalation) recipient, and the
        escalation recipient rule (BE-26C). At `escalationAt` it renders its
        BE-26B template and triggers notification delivery. `status` is
        PENDING until triggered (TRIGGERED, with `triggeredAt`) or cancelled
        (CANCELLED). `reason` carries escalation notes.
      required:
        [id, key, clientId, sourceEntityType, sourceEntityId,
         escalationRule, templateKey, escalationAt, status, triggeredAt,
         createdAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        key:
          type: string
        clientId:
          $ref: "#/components/schemas/Uuid"
        sourceEntityType:
          type: string
          description: Source/resource domain type being escalated.
        sourceEntityId:
          $ref: "#/components/schemas/Uuid"
          description: Source/resource id being escalated.
        currentRecipientUserId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
          description: The current (pre-escalation) recipient.
        escalationRule:
          $ref: "#/components/schemas/RecipientRule"
        templateKey:
          type: string
          description: BE-26B notification template key.
        escalationAt:
          type: string
          format: date-time
        triggeredAt:
          type: string
          format: date-time
          nullable: true
        status:
          type: string
          enum: [PENDING, TRIGGERED, CANCELLED]
        reason:
          type: string
          nullable: true
          description: Escalation reason/notes.
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # BE-26H — Notification reminder foundation. Time-deferred notification
    # trigger; no scheduler engine and no escalation.
    # ------------------------------------------------------------------
    NotificationReminder:
      type: object
      description: >
        A time-deferred notification trigger: references the source/resource
        being reminded about, the recipients (BE-26C rule), and the BE-26B
        template rendered at dispatch time. `status` is PENDING until
        dispatched (SENT, with `sentAt`) or cancelled (CANCELLED).
      required:
        [id, key, clientId, sourceEntityType, sourceEntityId, recipientRule,
         templateKey, reminderAt, status, sentAt, createdAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        key:
          type: string
        clientId:
          $ref: "#/components/schemas/Uuid"
        sourceEntityType:
          type: string
          description: Source/resource domain type being reminded about.
        sourceEntityId:
          $ref: "#/components/schemas/Uuid"
          description: Source/resource id being reminded about.
        recipientRule:
          $ref: "#/components/schemas/RecipientRule"
        templateKey:
          type: string
          description: BE-26B notification template key.
        variables:
          type: object
          additionalProperties: true
          description: Template variable values snapshot.
        reminderAt:
          type: string
          format: date-time
        status:
          type: string
          enum: [PENDING, SENT, CANCELLED]
        sentAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # BE-26D — Notification event subscription foundation. Declaration
    # only; no delivery and no new domain events.
    # ------------------------------------------------------------------
    NotificationEventSubscription:
      type: object
      description: >
        Maps an existing backend domain event type to a notification trigger:
        a notification template key plus a recipient resolution rule, with an
        optional Client/Building context and an enabled status.
      required:
        [id, key, eventType, templateKey, recipientRule, status, createdAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        key:
          type: string
        eventType:
          type: string
          description: Existing domain event type (e.g. WORK_ORDER_ASSIGNED).
        templateKey:
          type: string
          description: BE-26B notification template key.
        recipientRule:
          $ref: "#/components/schemas/RecipientRule"
        clientId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
          description: Optional Client context (null = any Client).
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
          description: Optional Building context (null = any Building).
        status:
          type: string
          enum: [ACTIVE, INACTIVE]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    RecipientRule:
      type: object
      description: >
        BE-26C recipient resolution rule: one or more recipient specs plus an
        optional Client/Building scope applied to the resolved users.
      required: [specs]
      properties:
        specs:
          type: array
          minItems: 1
          description: Recipient resolution specs (deduplicated at resolution).
          items:
            $ref: "#/components/schemas/RecipientSpec"
        scope:
          $ref: "#/components/schemas/RecipientScope"

    RecipientScope:
      type: object
      description: >
        Client/Building data scope applied to resolved recipients. Only users
        whose OWN accessible Building context satisfies the scope are kept
        (BE-02F/BE-02G).
      properties:
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingIds:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/Uuid"

    RecipientSpec:
      type: object
      description: >
        A single recipient resolution instruction (BE-26C). Exactly one
        variant applies per `kind`.
      oneOf:
        - type: object
          required: [kind, userId]
          properties:
            kind: { type: string, enum: [USER] }
            userId: { $ref: "#/components/schemas/Uuid" }
        - type: object
          required: [kind, roleCode]
          properties:
            kind: { type: string, enum: [ROLE] }
            roleCode: { type: string }
        - type: object
          required: [kind, permissionCode]
          properties:
            kind: { type: string, enum: [PERMISSION] }
            permissionCode: { type: string }
        - type: object
          required: [kind, workforceProfileId]
          properties:
            kind: { type: string, enum: [WORKFORCE] }
            workforceProfileId: { $ref: "#/components/schemas/Uuid" }
        - type: object
          required: [kind, teamId]
          properties:
            kind: { type: string, enum: [TEAM] }
            teamId: { $ref: "#/components/schemas/Uuid" }
        - type: object
          required: [kind]
          properties:
            kind: { type: string, enum: [TENANT_PIC] }
            tenantCompanyId: { $ref: "#/components/schemas/Uuid" }
            tenantPicId: { $ref: "#/components/schemas/Uuid" }
        - type: object
          required: [kind, vendorId]
          properties:
            kind: { type: string, enum: [VENDOR_PIC] }
            vendorId: { $ref: "#/components/schemas/Uuid" }

    # ------------------------------------------------------------------
    # BE-25M — App Version Metadata. Read-only contract; no update
    # delivery and no Flutter UI behavior.
    # ------------------------------------------------------------------
    MobileAppVersionMetadata:
      type: object
      description: >
        Per-platform mobile app version metadata. `updateRequired` is true
        when the caller's version is below `minimumSupportedVersion`;
        `updateAvailable` is true when it is below `currentVersion`.
        `release` carries optional release metadata (version, notes, date).
        Flags default to false when the caller omits `appVersion`.
      required:
        [platform, currentVersion, minimumSupportedVersion, updateRequired, updateAvailable, release]
      properties:
        platform:
          type: string
          enum: [ANDROID, IOS]
        currentVersion:
          type: string
          description: Latest supported version.
        minimumSupportedVersion:
          type: string
          description: Oldest still-supported version.
        updateRequired:
          type: boolean
          description: True when the caller's version is below the minimum supported version.
        updateAvailable:
          type: boolean
          description: True when the caller's version is below the current version.
        release:
          type: object
          nullable: true
          required: [version, notes, date]
          properties:
            version:
              type: string
            notes:
              type: string
              nullable: true
            date:
              type: string
              format: date-time
              nullable: true

    # ------------------------------------------------------------------
    # BE-25N — Mobile Observability. Lightweight diagnostics; correlation
    # via X-Request-ID; device/app metadata in logs only.
    # ------------------------------------------------------------------
    MobileDiagnostics:
      type: object
      description: >
        Basic mobile API health/diagnostic metadata. `requestId` is the
        correlation id of the request (same as the X-Request-ID response
        header); `device` echoes the sanitized mobile context headers when
        the client sent them.
      required: [status, requestId, serverTime, uptimeSeconds]
      properties:
        status:
          type: string
          const: OK
        requestId:
          type: string
        serverTime:
          type: string
          format: date-time
        uptimeSeconds:
          type: integer
        device:
          type: object
          required: []
          properties:
            deviceId:
              type: string
            platform:
              type: string
              enum: [ANDROID, IOS]
            appVersion:
              type: string

    # ------------------------------------------------------------------
    # CR-BE-COM-02 — Vendor Invoice domain schemas (PART 01–05)
    # ------------------------------------------------------------------

    # -------------------------------------------------------------------------
    # CR-BE-R2P-01 PART 07 — upstream chain schemas (pre-existing behaviour).
    # -------------------------------------------------------------------------

    ServiceRequestStatus:
      type: string
      enum: [OPEN, CANCELLED]

    ServiceRequest:
      type: object
      description: >
        BE-17C Service Request line on a Purchase Request. Client and Building
        are derived from the parent Purchase Request.
      required:
        [id, clientId, buildingId, purchaseRequestId, serviceType, title,
         status, requestedByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        purchaseRequestId: { $ref: "#/components/schemas/Uuid" }
        serviceType: { type: string }
        serviceCatalogId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            CR-BE-SVC-01 PART 02 — governed Service Catalog anchor. NULL means
            the request relies on its free-text `serviceType` only (historical
            requests). When set it references an ACTIVE same-Client
            `service_catalog` entry whose code equals `serviceType`.
        title: { type: string }
        description: { type: string, nullable: true }
        requiredDate: { type: string, format: date, nullable: true }
        functionalLocationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        vendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        notes: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/ServiceRequestStatus" }
        requestedByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateServiceRequestRequest:
      type: object
      required: [serviceType, title]
      properties:
        serviceType: { type: string }
        serviceCatalogId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            CR-BE-SVC-01 PART 02 — optional governed Service Catalog anchor
            (same-Client, ACTIVE, code equal to `serviceType`).
        title: { type: string }
        description: { type: string }
        requiredDate: { type: string, format: date, nullable: true }
        functionalLocationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        vendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        notes: { type: string }

    VendorSelectionRequestType:
      type: string
      enum: [PURCHASE_REQUEST, SERVICE_REQUEST]

    VendorSelectionReadinessStatus:
      type: string
      description: Backend-derived verdict; never caller-supplied.
      enum: [READY, NOT_READY, EXPIRED, INELIGIBLE]

    VendorSelection:
      type: object
      description: >
        BE-17E Vendor Selection Readiness — the deterministic verdict on
        whether a Vendor may be selected for a request, with its individual
        checks.
      required:
        [id, clientId, buildingId, requestType, requestId, vendorId, readiness,
         vendorActive, buildingRelationshipOk, capabilityMatch, complianceOk,
         licenseOk, approvalOk, evaluatedAt, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        requestType: { $ref: "#/components/schemas/VendorSelectionRequestType" }
        requestId: { $ref: "#/components/schemas/Uuid" }
        purchaseRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        serviceRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        vendorId: { $ref: "#/components/schemas/Uuid" }
        readiness:
          $ref: "#/components/schemas/VendorSelectionReadinessStatus"
        vendorActive: { type: boolean }
        buildingRelationshipOk: { type: boolean }
        capabilityMatch:
          type: boolean
          description: >
            CR-BE-SVC-01 PART 03 — when BOTH the request and a Vendor capability
            carry a governed `serviceCatalogId`, the match is identity-only (a
            mismatch is NOT eligible even when legacy codes match); otherwise
            normalized-code equality applies.
        complianceOk: { type: boolean }
        licenseOk: { type: boolean }
        approvalOk: { type: boolean }
        serviceCatalogId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            CR-BE-SVC-01 PART 03 — governed demand identity snapshot at
            evaluation time (NULL for un-governed / purchase requests).
        notes: { type: string, nullable: true }
        evaluatedAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateVendorSelectionRequest:
      type: object
      required: [requestType, requestId, vendorId]
      properties:
        requestType: { $ref: "#/components/schemas/VendorSelectionRequestType" }
        requestId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        notes: { type: string }

    POReadinessRequestType:
      type: string
      enum: [PURCHASE_REQUEST, SERVICE_REQUEST]

    POReadinessStatus:
      type: string
      description: >
        THE readiness verdict of the R2P chain. READY means "may commit"; an
        ISSUED Purchase Order means "committed". Downstream domains report this
        verdict verbatim and never re-decide it.
      enum: [READY, NOT_READY, BLOCKED]

    POReadiness:
      type: object
      description: >
        BE-17F Purchase Order Readiness — the sole readiness authority and a
        precondition for committing a Purchase Order.
      required:
        [id, clientId, buildingId, requestType, requestId, vendorId, readiness,
         approvalOk, vendorOk, materialContextOk, serviceContextOk,
         preparedByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        requestType: { $ref: "#/components/schemas/POReadinessRequestType" }
        requestId: { $ref: "#/components/schemas/Uuid" }
        purchaseRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        serviceRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        vendorId: { $ref: "#/components/schemas/Uuid" }
        readiness: { $ref: "#/components/schemas/POReadinessStatus" }
        approvalOk: { type: boolean }
        vendorOk: { type: boolean }
        materialContextOk: { type: boolean }
        serviceContextOk: { type: boolean }
        requiredDate: { type: string, format: date, nullable: true }
        notes: { type: string, nullable: true }
        preparedByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreatePOReadinessRequest:
      type: object
      required: [requestType, requestId, vendorId]
      properties:
        requestType: { $ref: "#/components/schemas/POReadinessRequestType" }
        requestId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        requiredDate: { type: string, format: date, nullable: true }
        notes: { type: string }

    UpdatePOReadinessRequest:
      type: object
      description: >
        Only editable detail; the readiness verdict and checks are
        backend-derived.
      minProperties: 1
      properties:
        requiredDate: { type: string, format: date, nullable: true }
        notes: { type: string, nullable: true }

    # -------------------------------------------------------------------------
    # CR-BE-R2P-01 — Request-to-Pay chain schemas (PART 01–06)
    # -------------------------------------------------------------------------

    PurchaseOrderStatus:
      type: string
      description: >
        Authoritative API lifecycle: DRAFT → ISSUED when issue-readiness passes,
        or DRAFT → CANCELLED before issuance. ISSUED and CANCELLED have no
        outgoing API command and are terminal for this contract.
      enum: [DRAFT, ISSUED, CANCELLED]

    PurchaseOrderAction:
      type: string
      description: >
        Existing Purchase Order command available to the calling user now.
        Tokens are upper-case and map one-to-one to registered command paths.
      enum: [ISSUE, CANCEL]

    PurchaseOrderAvailableActions:
      type: object
      description: >
        Backend-authoritative, caller-specific Purchase Order actions. An empty
        list is authoritative; clients must not infer transitions from state.
      required: [purchaseOrderId, state, availableActions]
      properties:
        purchaseOrderId: { $ref: "#/components/schemas/Uuid" }
        state: { $ref: "#/components/schemas/PurchaseOrderStatus" }
        availableActions:
          type: array
          items: { $ref: "#/components/schemas/PurchaseOrderAction" }

    PurchaseOrderRequestType:
      type: string
      description: >
        The source request behind the commitment. Mirrors the BE-17F PO
        Readiness vocabulary exactly — the PO never widens it.
      enum: [PURCHASE_REQUEST, SERVICE_REQUEST]

    PurchaseOrderCurrency:
      type: string
      description: Identical to the Vendor Invoice currency vocabulary.
      enum: [IDR, USD, SGD, MYR, AUD, EUR, GBP, JPY, CNY]

    PurchaseOrder:
      type: object
      description: >
        The authoritative COMMITMENT to a Vendor for an approved procurement
        request — the R2P link between "may commit" (BE-17F PO Readiness) and
        downstream execution. Carries NO quantity, NO ordered/received ledger,
        NO amount total and NO readiness verdict of its own.
      required:
        [id, clientId, buildingId, poNumber, poDate, vendorId, requestType,
         purchaseRequestId, serviceRequestId, poReadinessId, currency, status,
         vendorReference, requiredDate, notes, createdByUserId, issuedAt,
         issuedByUserId, cancelledAt, cancelledByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Derived from the qualifying PO Readiness.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Derived from the qualifying PO Readiness.
        poNumber:
          type: string
          description: PO identity foundation — unique within a Client.
        poDate: { type: string, format: date }
        vendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Derived from the qualifying PO Readiness.
        requestType: { $ref: "#/components/schemas/PurchaseOrderRequestType" }
        purchaseRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Exactly one of purchaseRequestId / serviceRequestId is set.
        serviceRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        poReadinessId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Required precondition — the BE-17F readiness that qualified this
            commitment. PO Readiness remains the sole readiness authority.
        currency: { $ref: "#/components/schemas/PurchaseOrderCurrency" }
        status: { $ref: "#/components/schemas/PurchaseOrderStatus" }
        vendorReference: { type: string, nullable: true }
        requiredDate: { type: string, format: date, nullable: true }
        notes: { type: string, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        issuedAt:
          type: string
          format: date-time
          nullable: true
          description: >
            CR-BE-R2P-01 PART 03 — set when the commitment was issued. NULL
            while DRAFT and on API-created CANCELLED rows. Historical/imported
            issued-cancelled rows may preserve this provenance even though the
            API exposes no ISSUED cancellation command.
        issuedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        cancelledAt: { type: string, format: date-time, nullable: true }
        cancelledByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreatePurchaseOrderRequest:
      type: object
      description: >
        `poReadinessId` is the ONLY context input — Client, Building, Vendor
        and the request reference are derived from it.
      required: [poReadinessId, poNumber, poDate, currency]
      properties:
        poReadinessId: { $ref: "#/components/schemas/Uuid" }
        poNumber:
          type: string
          minLength: 2
          maxLength: 64
          pattern: "^[A-Za-z0-9][A-Za-z0-9_\\-/]{1,63}$"
          description: >
            Normalized uppercase and unique within the Client.
        poDate: { type: string, format: date }
        currency: { $ref: "#/components/schemas/PurchaseOrderCurrency" }
        vendorReference: { type: string, nullable: true, maxLength: 255 }
        requiredDate: { type: string, format: date, nullable: true }
        notes: { type: string, nullable: true, maxLength: 2000 }

    UpdatePurchaseOrderRequest:
      type: object
      description: >
        DRAFT only. At least one field is required. Identity, commitment
        context and derived scope are immutable and must not be supplied.
      minProperties: 1
      properties:
        poDate: { type: string, format: date }
        currency: { $ref: "#/components/schemas/PurchaseOrderCurrency" }
        vendorReference: { type: string, nullable: true, maxLength: 255 }
        requiredDate: { type: string, format: date, nullable: true }
        notes: { type: string, nullable: true, maxLength: 2000 }

    IssuePurchaseOrderRequest:
      type: object
      description: >
        Optional issuance note only. Status and issuance provenance are
        backend-derived; a caller can neither declare readiness nor stamp
        `issuedAt` / `issuedByUserId`.
      properties:
        notes: { type: string, nullable: true, maxLength: 2000 }

    PurchaseOrderIssueBlocker:
      type: string
      description: >
        A deterministic reason a Purchase Order may not be issued right now.
        Resolved at evaluation time from existing authorities and never
        persisted. `PO_READINESS_NOT_READY` reports the BE-17F verdict — it
        does not re-decide it.
      enum:
        - PO_NOT_DRAFT
        - PO_READINESS_INVALID
        - PO_READINESS_NOT_READY
        - NO_PURCHASE_ORDER_LINES
        - REQUEST_LINKAGE_INVALID
        - REQUEST_LINE_INVALID
        - VENDOR_NOT_COMMITTABLE
        - SCOPE_INCONSISTENT

    PurchaseOrderIssueReadiness:
      type: object
      description: >
        Read-only issue-readiness projection. Evaluating it mutates nothing and
        creates no second readiness authority.
      required:
        [purchaseOrderId, status, issuable, poReadiness, lineCount, blockers,
         availableActions, evaluatedAt]
      properties:
        purchaseOrderId: { $ref: "#/components/schemas/Uuid" }
        status: { $ref: "#/components/schemas/PurchaseOrderStatus" }
        issuable: { type: boolean }
        poReadiness:
          type: string
          nullable: true
          description: >
            The BE-17F verdict reported VERBATIM (e.g. READY / NOT_READY /
            BLOCKED); null when the readiness record is unreadable.
        lineCount: { type: integer, minimum: 0 }
        blockers:
          type: array
          items: { $ref: "#/components/schemas/PurchaseOrderIssueBlocker" }
        availableActions:
          type: array
          description: >
            Contains `ISSUE` only when `issuable` is true and the caller holds
            `purchase_order.manage`; otherwise empty.
          items: { $ref: "#/components/schemas/PurchaseOrderAction" }
        evaluatedAt: { type: string, format: date-time }

    PurchaseOrderLineRequestType:
      type: string
      enum: [MATERIAL_REQUEST, SERVICE_REQUEST]

    PurchaseOrderLine:
      type: object
      description: >
        A commercial commitment over ONE originating MR/SR line. Material
        Request / Service Request remain the single quantity authority: this
        line snapshots the authoritative quantity and adds commercial terms
        only — it is never a competing quantity ledger.
      required:
        [id, purchaseOrderId, clientId, buildingId, lineNumber,
         requestLineType, materialRequestId, serviceRequestId, itemId, uomId,
         sourceServiceId, description, quantitySnapshot, unitPrice, lineAmount,
         notes, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        purchaseOrderId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        lineNumber:
          type: integer
          minimum: 1
          description: Deterministic ordering; unique within the Purchase Order.
        requestLineType:
          $ref: "#/components/schemas/PurchaseOrderLineRequestType"
        materialRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Exactly one of materialRequestId / serviceRequestId is set, in
            agreement with requestLineType.
        serviceRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        itemId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Set for MATERIAL lines only.
        uomId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: UOM snapshot; no conversion authority.
        sourceServiceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            CR-BE-SVC-01 PART 04 — governed SERVICE identity snapshot (NULL for
            MATERIAL_REQUEST and un-governed SERVICE lines).
        description: { type: string }
        quantitySnapshot:
          type: number
          nullable: true
          description: >
            Frozen READ of the request line's approved (or requested) quantity.
            Always NULL for a SERVICE line.
        unitPrice: { type: number }
        lineAmount:
          type: number
          description: >
            Always DERIVED — quantitySnapshot × unitPrice, or the unit price
            itself when there is no quantity. Never caller-supplied.
        notes: { type: string, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    AddPurchaseOrderLineRequest:
      type: object
      description: >
        Quantity, amount, item, UOM and line number are all backend-derived and
        must not be supplied.
      required: [requestLineType, requestLineId, unitPrice]
      properties:
        requestLineType:
          $ref: "#/components/schemas/PurchaseOrderLineRequestType"
        requestLineId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: The originating Material Request / Service Request line.
        unitPrice: { type: number, minimum: 0 }
        description:
          type: string
          maxLength: 500
          description: Defaults to the request line's own description.
        notes: { type: string, nullable: true, maxLength: 2000 }

    UpdatePurchaseOrderLineRequest:
      type: object
      description: >
        Parent PO must be DRAFT. At least one field is required; the line
        amount is always re-derived from the frozen snapshot.
      minProperties: 1
      properties:
        unitPrice: { type: number, minimum: 0 }
        description: { type: string, maxLength: 500 }
        notes: { type: string, nullable: true, maxLength: 2000 }

    RemovePurchaseOrderLineResult:
      type: object
      description: Compact acknowledgement for a successfully deleted PO line.
      required: [removed, purchaseOrderId, lineNumber]
      properties:
        removed:
          type: boolean
          enum: [true]
        purchaseOrderId: { $ref: "#/components/schemas/Uuid" }
        lineNumber: { type: integer, minimum: 1 }

    WorkContractStatus:
      type: string
      description: >
        SPK lifecycle. DRAFT → ACTIVE → COMPLETED, with CANCELLED reachable
        from DRAFT or ACTIVE. COMPLETED is reachable ONLY from ACTIVE, so a
        mandate can never be completed without having been authorized. Both
        COMPLETED and CANCELLED are terminal.
      enum: [DRAFT, ACTIVE, COMPLETED, CANCELLED]

    WorkContractAction:
      type: string
      description: >
        Existing Work Contract command available to the calling user now.
        Tokens are upper-case and map one-to-one to registered command paths.
      enum: [ACTIVATE, COMPLETE, CANCEL]

    WorkContractAvailableActions:
      type: object
      description: >
        Backend-authoritative, caller-specific Work Contract actions. An empty
        list is authoritative; clients must not infer transitions from state.
      required: [workContractId, state, availableActions]
      properties:
        workContractId: { $ref: "#/components/schemas/Uuid" }
        state: { $ref: "#/components/schemas/WorkContractStatus" }
        availableActions:
          type: array
          items: { $ref: "#/components/schemas/WorkContractAction" }

    WorkContract:
      type: object
      description: >
        The SPK (Surat Perintah Kerja / Work Contract) — the EXECUTION MANDATE
        following a committed Purchase Order. Its own entity: it does not
        duplicate the BE-17H Work Order Procurement Binding domain and holds no
        Work Order reference. Carries no quantity, amount ledger or readiness
        verdict.
      required:
        [id, clientId, buildingId, vendorId, purchaseOrderId, spkNumber,
         spkDate, title, scopeDescription, startDate, endDate, notes, status,
         createdByUserId, activatedAt, activatedByUserId, completedAt,
         completedByUserId, cancelledAt, cancelledByUserId, createdAt,
         updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Inherited from the Purchase Order.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Inherited from the Purchase Order.
        vendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Inherited from the Purchase Order.
        purchaseOrderId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: The ISSUED commitment this mandate executes.
        spkNumber:
          type: string
          description: SPK identity foundation — unique within a Client.
        spkDate: { type: string, format: date }
        title: { type: string }
        scopeDescription: { type: string, nullable: true }
        startDate: { type: string, format: date, nullable: true }
        endDate: { type: string, format: date, nullable: true }
        notes: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/WorkContractStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        activatedAt: { type: string, format: date-time, nullable: true }
        activatedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        completedAt: { type: string, format: date-time, nullable: true }
        completedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        cancelledAt: { type: string, format: date-time, nullable: true }
        cancelledByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateWorkContractRequest:
      type: object
      description: >
        `purchaseOrderId` is the ONLY context input — Client, Building and
        Vendor are inherited from that ISSUED Purchase Order.
      required: [purchaseOrderId, spkNumber, spkDate, title]
      properties:
        purchaseOrderId: { $ref: "#/components/schemas/Uuid" }
        spkNumber:
          type: string
          minLength: 2
          maxLength: 64
          pattern: "^[A-Za-z0-9][A-Za-z0-9_\\-/]{1,63}$"
          description: Normalized uppercase and unique within the Client.
        spkDate: { type: string, format: date }
        title: { type: string, maxLength: 255 }
        scopeDescription: { type: string, nullable: true, maxLength: 4000 }
        startDate: { type: string, format: date, nullable: true }
        endDate:
          type: string
          format: date
          nullable: true
          description: Must be the same as or after startDate when both are set.
        notes: { type: string, nullable: true, maxLength: 2000 }

    UpdateWorkContractRequest:
      type: object
      description: >
        DRAFT only. At least one field is required. Identity, the PO reference,
        the inherited scope and all lifecycle provenance are immutable.
      minProperties: 1
      properties:
        spkDate: { type: string, format: date }
        title: { type: string, maxLength: 255 }
        scopeDescription: { type: string, nullable: true, maxLength: 4000 }
        startDate: { type: string, format: date, nullable: true }
        endDate: { type: string, format: date, nullable: true }
        notes: { type: string, nullable: true, maxLength: 2000 }

    WOProcurementStatus:
      type: string
      description: >
        Backend-authoritative: BOUND on create, READY when a READY BE-17F PO
        Readiness exists for the request, RECEIVED when a Receiving is linked.
      enum: [BOUND, READY, RECEIVED]

    WorkOrderProcurementBinding:
      type: object
      description: >
        BE-17H binding of a Work Order to its procurement context, EXTENDED by
        CR-BE-R2P-01 PART 05 with the SPK chain. Exactly one binding exists per
        Work Order; there is no parallel SPK ↔ Work Order binding domain.
      required:
        [id, clientId, buildingId, workOrderId, purchaseRequestId,
         procurementStatus, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        purchaseRequestId: { $ref: "#/components/schemas/Uuid" }
        materialRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        serviceRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        receivingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        workContractId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            PART 05 — the ACTIVE SPK authorizing this Work Order. NULL together
            with purchaseOrderId / vendorId on a request-only binding; set
            together once the chain is bound.
        purchaseOrderId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: PART 05 — derived from the SPK, never caller-supplied.
        vendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: PART 05 — derived from the SPK, never caller-supplied.
        procurementStatus: { $ref: "#/components/schemas/WOProcurementStatus" }
        notes: { type: string, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        workOrder:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            workOrderNumber: { type: string }
            title: { type: string }
            status: { type: string }
        purchaseRequest:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            requestNumber: { type: string }
            title: { type: string }
            status: { type: string }
        materialRequest:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            itemId: { $ref: "#/components/schemas/Uuid" }
            quantity: { type: number }
            status: { type: string }
        serviceRequest:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            serviceType: { type: string }
            title: { type: string }
            status: { type: string }
        receiving:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            receivingType: { type: string }
            status: { type: string }
        workContract:
          type: object
          nullable: true
          description: PART 05 — the bound SPK, when the chain is present.
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            spkNumber: { type: string }
            title: { type: string }
            status: { $ref: "#/components/schemas/WorkContractStatus" }
        purchaseOrder:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            poNumber: { type: string }
            status: { $ref: "#/components/schemas/PurchaseOrderStatus" }

    CreateWorkOrderProcurementBindingRequest:
      type: object
      required: [workOrderId, purchaseRequestId]
      properties:
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        purchaseRequestId: { $ref: "#/components/schemas/Uuid" }
        materialRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        serviceRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        workContractId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            PART 05 — optional ACTIVE SPK to bind. The Purchase Order and
            Vendor are derived from it.
        notes: { type: string, maxLength: 1000 }

    BindWorkContractRequest:
      type: object
      description: >
        The SPK reference is the ONLY input; Purchase Order, Vendor, Client and
        Building are all derived from it.
      required: [workContractId]
      properties:
        workContractId: { $ref: "#/components/schemas/Uuid" }

    LinkReceivingRequest:
      type: object
      required: [receivingId]
      properties:
        receivingId: { $ref: "#/components/schemas/Uuid" }

    VendorInvoiceAction:
      type: string
      description: Existing Vendor Invoice command available to the caller now.
      enum: [FINALIZE, CANCEL, VERIFY, RECORD_PAYMENT]

    VendorInvoiceAvailableActions:
      type: object
      description: >
        Caller-specific action projection. An empty list is authoritative.
      required:
        [vendorInvoiceId, state, verificationStatus, matchingStatus,
         settlementReadiness, availableActions]
      properties:
        vendorInvoiceId: { $ref: "#/components/schemas/Uuid" }
        state: { type: string, enum: [DRAFT, FINALIZED, CANCELLED] }
        verificationStatus: { type: string, enum: [PENDING, VERIFIED, DISCREPANCY] }
        matchingStatus: { $ref: "#/components/schemas/VendorInvoiceMatchingStatus" }
        settlementReadiness:
          $ref: "#/components/schemas/VendorSettlementReadinessStatus"
        availableActions:
          type: array
          items: { $ref: "#/components/schemas/VendorInvoiceAction" }

    VendorInvoiceEligiblePurchaseOrderStatus:
      type: string
      description: Existing PO state eligible for Vendor Invoice linkage.
      enum: [ISSUED]

    VendorInvoiceEligibleWorkContractStatus:
      type: string
      description: Existing SPK states eligible for Vendor Invoice linkage.
      enum: [ACTIVE, COMPLETED]

    VendorInvoice:
      type: object
      description: >
        A Vendor Invoice — the canonical payable invoice from an external Vendor.
        Not a Tenant Invoice, not a vendor-service-cost, not a Purchase Order.
      required:
        [id, clientId, buildingId, vendorId, invoiceNumber, invoiceDate,
         receivedDate, currency, invoiceAmount, status, vendorReference,
         vendorWorkId, workOrderId, completionReportId, serviceReportId,
         bastDocumentId, purchaseOrderId, workContractId, notes,
         createdByUserId, verificationStatus, verifiedByUserId, verifiedAt,
         verificationNotes, discrepancyCodes, paymentStatus, paidAmount,
         outstandingAmount, lastPaymentDate, finalizedAt, finalizedByUserId,
         cancelledAt, cancelledByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        invoiceNumber: { type: string }
        invoiceDate: { type: string, format: date }
        receivedDate: { type: string, format: date }
        currency: { type: string, enum: [IDR, USD, SGD, MYR, AUD, EUR, GBP, JPY, CNY] }
        invoiceAmount: { type: number }
        status: { type: string, enum: [DRAFT, FINALIZED, CANCELLED] }
        vendorReference: { type: string, nullable: true }
        vendorWorkId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        workOrderId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        completionReportId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        serviceReportId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        bastDocumentId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        purchaseOrderId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            The ISSUED Purchase Order being invoiced against. Validated against
            the invoice's Vendor, Client and Building and immutable after
            creation; never a second commercial authority.
        workContractId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            The ACTIVE or COMPLETED SPK under which the work was executed.
            Requires purchaseOrderId, must belong to that Purchase Order, and
            is immutable after invoice creation.
        notes: { type: string, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        verificationStatus: { type: string, enum: [PENDING, VERIFIED, DISCREPANCY] }
        verifiedByUserId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        verifiedAt: { type: string, format: date-time, nullable: true }
        verificationNotes: { type: string, nullable: true }
        discrepancyCodes:
          type: array
          items: { $ref: "#/components/schemas/VendorInvoiceMatchingReason" }
        paymentStatus:
          type: string
          enum: [UNPAID, PARTIALLY_PAID, PAID]
          description: >
            UNPAID before any payment, PARTIALLY_PAID while cumulative
            paidAmount is below invoiceAmount, and PAID only when equal.
        paidAmount:
          type: number
          description: Cumulative payment amount persisted by the backend.
        outstandingAmount:
          type: number
          description: >
            Database-generated invoiceAmount minus paidAmount. Consumers must
            not submit, override, or independently recalculate this authority.
        lastPaymentDate: { type: string, format: date, nullable: true }
        finalizedAt: { type: string, format: date-time, nullable: true }
        finalizedByUserId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        cancelledAt: { type: string, format: date-time, nullable: true }
        cancelledByUserId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateVendorInvoiceRequest:
      type: object
      required: [buildingId, invoiceNumber, invoiceDate, receivedDate, currency, invoiceAmount]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        invoiceNumber:
          type: string
          minLength: 2
          maxLength: 64
          pattern: "^[A-Za-z0-9][A-Za-z0-9_\\-/]{1,63}$"
          description: Normalized uppercase and unique within the Client.
        invoiceDate: { type: string, format: date }
        receivedDate: { type: string, format: date }
        currency: { type: string, enum: [IDR, USD, SGD, MYR, AUD, EUR, GBP, JPY, CNY] }
        invoiceAmount: { type: number, minimum: 0 }
        vendorReference: { type: string, nullable: true, maxLength: 255 }
        vendorWorkId: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        completionReportId: { $ref: "#/components/schemas/Uuid" }
        serviceReportId: { $ref: "#/components/schemas/Uuid" }
        bastDocumentId: { $ref: "#/components/schemas/Uuid" }
        purchaseOrderId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            CR-BE-R2P-01 PART 06 — optional procurement linkage. The Purchase
            Order must be ISSUED (409
            `VENDOR_INVOICE_PURCHASE_ORDER_NOT_ISSUED`) and must belong to the
            invoice's Vendor, Client and Building (400
            `VENDOR_INVOICE_PROCUREMENT_VENDOR_MISMATCH` /
            `VENDOR_INVOICE_PROCUREMENT_SCOPE_MISMATCH`).
        workContractId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Optional SPK linkage. Requires `purchaseOrderId`, must belong to
            that PO (400 `VENDOR_INVOICE_WORK_CONTRACT_PO_MISMATCH`), and must
            be ACTIVE or COMPLETED (409
            `VENDOR_INVOICE_WORK_CONTRACT_NOT_ELIGIBLE`). Client, Building,
            Vendor, PO, and SPK references are authoritative and immutable.
        notes: { type: string, nullable: true, maxLength: 2000 }

    UpdateVendorInvoiceRequest:
      type: object
      description: >
        Existing DRAFT-only PATCH contract. At least one commercial field is
        required. Identity, scope, operational references, purchaseOrderId,
        workContractId, lifecycle, verification, and payment fields are
        immutable and rejected when supplied.
      minProperties: 1
      properties:
        invoiceDate: { type: string, format: date }
        receivedDate: { type: string, format: date }
        currency:
          type: string
          enum: [IDR, USD, SGD, MYR, AUD, EUR, GBP, JPY, CNY]
        invoiceAmount: { type: number, minimum: 0 }
        vendorReference: { type: string, nullable: true, maxLength: 255 }
        notes: { type: string, nullable: true, maxLength: 2000 }

    RecordVendorPaymentRequest:
      type: object
      required: [amount]
      properties:
        amount:
          type: number
          minimum: 0
          exclusiveMinimum: true
          maximum: 10000000000000000
          exclusiveMaximum: true
          multipleOf: 0.01
          description: >
            Positive finite payment amount compatible with NUMERIC(18,2): fewer
            than 10^16 and no more than two decimal places.
        paymentDate:
          type: string
          format: date
          description: >
            Optional YYYY-MM-DD payment date. When supplied it becomes
            lastPaymentDate; when omitted, any existing lastPaymentDate is
            preserved.
        notes:
          type: string
          nullable: true
          maxLength: 2000
          description: >
            Optional payment audit note. Non-empty text is retained in the
            operational payment event; null or blank text records no note.

    VendorInvoiceMatchingStatus:
      type: string
      description: >
        Derived matching outcome, not an invoice lifecycle. MISMATCH takes
        precedence over NOT_READY when both kinds of reason exist.
      enum: [MATCHED, MISMATCH, NOT_READY]

    VendorInvoiceMatchingReason:
      type: string
      enum:
        [VENDOR_MISMATCH, WORK_ORDER_NOT_FOUND, WORK_ORDER_SCOPE_MISMATCH,
         VENDOR_WORK_NOT_FOUND, VENDOR_WORK_VENDOR_MISMATCH,
         PURCHASE_ORDER_NOT_FOUND, PURCHASE_ORDER_NOT_ISSUED,
         PURCHASE_ORDER_SCOPE_MISMATCH, PURCHASE_ORDER_VENDOR_MISMATCH,
         WORK_CONTRACT_NOT_FOUND, WORK_CONTRACT_PO_MISMATCH,
         WORK_CONTRACT_SCOPE_MISMATCH, WORK_CONTRACT_VENDOR_MISMATCH,
         WORK_CONTRACT_NOT_ELIGIBLE, PURCHASE_ORDER_LINES_NOT_FOUND,
         CURRENCY_MISMATCH, INVOICE_PO_AMOUNT_MISMATCH,
         MATERIAL_REQUEST_NOT_FOUND, MATERIAL_RECEIVING_NOT_COMPLETE,
         MATERIAL_RECEIVING_OVER_APPROVED, SERVICE_RECEIVING_NOT_FINALIZED,
         COMPLETION_REPORT_NOT_FOUND, COMPLETION_REPORT_NOT_SUBMITTED,
         SERVICE_REPORT_NOT_FOUND, SERVICE_REPORT_NOT_FINALIZED,
         BAST_NOT_FOUND, BAST_REQUIRED_NOT_AVAILABLE, BAST_NOT_ACCEPTED,
         AMOUNT_INVALID]

    InvoiceMatchingCheck:
      type: object
      required: [check, applicable, status, matched, discrepancy]
      properties:
        check: { type: string }
        applicable:
          type: boolean
          description: False when the optional authoritative reference is absent.
        status: { $ref: "#/components/schemas/VendorInvoiceMatchingStatus" }
        matched:
          type: boolean
          description: Backward-compatible convenience; true iff status is MATCHED.
        discrepancy:
          allOf: [{ $ref: "#/components/schemas/VendorInvoiceMatchingReason" }]
          nullable: true

    InvoiceMatchingResult:
      type: object
      description: >
        One canonical, read-only Vendor Invoice matching result consumed by
        verification. It reuses PO/SPK, PO Lines, MR/SR/Receiving and existing
        completion evidence without persisting a second quantity authority.
      required:
        [invoiceId, status, verificationEligible, vendorMatch,
         purchaseOrderMatch, workContractMatch, amountMatch, receivingMatch,
         workOrderMatch, vendorWorkMatch, completionMatch, serviceMatch,
         bastMatch, allMatched, mismatchCodes, notReadyCodes,
         discrepancyCodes, evaluatedAt]
      properties:
        invoiceId: { $ref: "#/components/schemas/Uuid" }
        status: { $ref: "#/components/schemas/VendorInvoiceMatchingStatus" }
        verificationEligible: { type: boolean }
        vendorMatch: { $ref: "#/components/schemas/InvoiceMatchingCheck" }
        purchaseOrderMatch: { $ref: "#/components/schemas/InvoiceMatchingCheck" }
        workContractMatch: { $ref: "#/components/schemas/InvoiceMatchingCheck" }
        amountMatch: { $ref: "#/components/schemas/InvoiceMatchingCheck" }
        receivingMatch: { $ref: "#/components/schemas/InvoiceMatchingCheck" }
        workOrderMatch: { $ref: "#/components/schemas/InvoiceMatchingCheck" }
        vendorWorkMatch: { $ref: "#/components/schemas/InvoiceMatchingCheck" }
        completionMatch: { $ref: "#/components/schemas/InvoiceMatchingCheck" }
        serviceMatch: { $ref: "#/components/schemas/InvoiceMatchingCheck" }
        bastMatch: { $ref: "#/components/schemas/InvoiceMatchingCheck" }
        allMatched:
          type: boolean
          description: Backward-compatible convenience; true iff status is MATCHED.
        mismatchCodes:
          type: array
          items: { $ref: "#/components/schemas/VendorInvoiceMatchingReason" }
        notReadyCodes:
          type: array
          items: { $ref: "#/components/schemas/VendorInvoiceMatchingReason" }
        discrepancyCodes:
          type: array
          description: Combined mismatchCodes + notReadyCodes.
          items: { $ref: "#/components/schemas/VendorInvoiceMatchingReason" }
        evaluatedAt: { type: string, format: date-time }

    VendorInvoiceVerifyResult:
      type: object
      required: [invoice, matching]
      properties:
        invoice: { $ref: "#/components/schemas/VendorInvoice" }
        matching: { $ref: "#/components/schemas/InvoiceMatchingResult" }

    VendorSettlementReadinessStatus:
      type: string
      description: >
        Derived payment eligibility, not a parallel payment lifecycle. SETTLED
        is derived from the existing PAID state and zero outstanding amount.
      enum: [READY, NOT_READY, SETTLED]

    VendorSettlementReason:
      type: string
      enum:
        [INVOICE_NOT_FINALIZED, INVOICE_CANCELLED, INVOICE_NOT_VERIFIED,
         MATCHING_NOT_READY, MATCHING_MISMATCH, SCOPE_MISMATCH,
         INVALID_INVOICE_AMOUNT, INVALID_CURRENCY, INVALID_PAYMENT_STATE]

    VendorSettlementAction:
      type: string
      description: Existing payment command advertised only when READY.
      enum: [RECORD_PAYMENT]

    VendorSettlementReadinessResult:
      type: object
      description: >
        Canonical read-only payment eligibility over existing invoice,
        matching, consistency and payment authorities. No balance or settlement
        state is persisted by this projection.
      required:
        [invoiceId, readiness, invoiceStatus, verificationStatus,
         matchingStatus, paymentStatus, currency, invoiceAmount, paidAmount,
         outstandingAmount, reasons, blockers, matchingReasons,
         consistencyReasons, availableActions, evaluatedAt]
      properties:
        invoiceId: { $ref: "#/components/schemas/Uuid" }
        readiness: { $ref: "#/components/schemas/VendorSettlementReadinessStatus" }
        invoiceStatus: { type: string, enum: [DRAFT, FINALIZED, CANCELLED] }
        verificationStatus: { type: string, enum: [PENDING, VERIFIED, DISCREPANCY] }
        matchingStatus: { $ref: "#/components/schemas/VendorInvoiceMatchingStatus" }
        paymentStatus: { type: string, enum: [UNPAID, PARTIALLY_PAID, PAID] }
        currency: { type: string, enum: [IDR, USD, SGD, MYR, AUD, EUR, GBP, JPY, CNY] }
        invoiceAmount: { type: number }
        paidAmount: { type: number }
        outstandingAmount: { type: number }
        reasons:
          type: array
          items: { $ref: "#/components/schemas/VendorSettlementReason" }
        blockers:
          type: array
          deprecated: true
          description: Backward-compatible alias of reasons.
          items: { $ref: "#/components/schemas/VendorSettlementReason" }
        matchingReasons:
          type: array
          items: { $ref: "#/components/schemas/VendorInvoiceMatchingReason" }
        consistencyReasons:
          type: array
          items: { type: string }
        availableActions:
          type: array
          items: { $ref: "#/components/schemas/VendorSettlementAction" }
        evaluatedAt: { type: string, format: date-time }

    VendorConsistencyCheck:
      type: object
      required: [check, consistent, reason]
      properties:
        check: { type: string }
        consistent: { type: boolean }
        reason: { type: string, nullable: true }

    VendorConsistencyResult:
      type: object
      description: >
        Vendor consistency cross-check result. Validates consistency across
        the commercial-to-operational chain.
      required: [vendorWorkVendorMatch, workOrderAssignedVendorMatch, procurementVendorMatch, clientIsolationMatch, buildingIsolationMatch, allConsistent, inconsistencyCodes]
      properties:
        vendorWorkVendorMatch: { $ref: "#/components/schemas/VendorConsistencyCheck" }
        workOrderAssignedVendorMatch: { $ref: "#/components/schemas/VendorConsistencyCheck" }
        procurementVendorMatch: { $ref: "#/components/schemas/VendorConsistencyCheck" }
        clientIsolationMatch: { $ref: "#/components/schemas/VendorConsistencyCheck" }
        buildingIsolationMatch: { $ref: "#/components/schemas/VendorConsistencyCheck" }
        allConsistent: { type: boolean }
        inconsistencyCodes:
          type: array
          items:
            type: string
            enum:
              [INVOICE_VENDOR_WORK_MISMATCH, VENDOR_WORK_WORK_ORDER_MISMATCH,
               PROCUREMENT_VENDOR_MISMATCH, CROSS_CLIENT_REFERENCE,
               CROSS_BUILDING_REFERENCE]

    R2PTraceDocumentType:
      type: string
      enum:
        [PURCHASE_REQUEST, MATERIAL_REQUEST, SERVICE_REQUEST,
         PROCUREMENT_APPROVAL, PURCHASE_ORDER, PURCHASE_ORDER_LINE,
         WORK_CONTRACT, RECEIVING, WORK_ORDER, VENDOR_WORK,
         COMPLETION_REPORT, SERVICE_REPORT, BAST, VENDOR_INVOICE,
         INVOICE_MATCHING, INVOICE_VERIFICATION, PAYMENT_STATE,
         SETTLEMENT_READINESS]

    R2PTraceRelationshipType:
      type: string
      enum:
        [SOURCE_OF, APPROVED_BY, COMMITTED_BY_LINE, BELONGS_TO, MANDATES,
         RECEIVED_AS, BOUND_TO, EXECUTED_AS, REPORTED_BY, ACCEPTED_BY,
         INVOICED_BY, PROJECTS]

    R2PTraceDocument:
      type: object
      required: [documentType, documentId, documentNumber, status]
      properties:
        documentType: { $ref: "#/components/schemas/R2PTraceDocumentType" }
        documentId: { $ref: "#/components/schemas/Uuid" }
        documentNumber: { type: string, nullable: true }
        status: { type: string, nullable: true }

    R2PTraceRelationship:
      type: object
      required:
        [relationship, sourceDocumentType, sourceDocumentId,
         targetDocumentType, targetDocumentId]
      properties:
        relationship: { $ref: "#/components/schemas/R2PTraceRelationshipType" }
        sourceDocumentType: { $ref: "#/components/schemas/R2PTraceDocumentType" }
        sourceDocumentId: { $ref: "#/components/schemas/Uuid" }
        targetDocumentType: { $ref: "#/components/schemas/R2PTraceDocumentType" }
        targetDocumentId: { $ref: "#/components/schemas/Uuid" }

    VendorInvoiceTrace:
      type: object
      description: >
        End-to-end traceability references along the vendor commercial chain.
        Only identifiers/links — no large reporting data.
      required:
        [invoiceId, vendorId, clientId, buildingId, vendorWorkId, workOrderId,
         completionReportId, serviceReportId, bastDocumentId,
         purchaseOrderId, workContractId, purchaseOrderStatus,
         purchaseOrderNumber, workContractStatus, workContractSpkNumber,
         workOrderAssignedVendorId, workOrderBastRequirement,
         bastAcceptanceStatus, bastIsLegacyProjection,
         verificationStatus, paymentStatus, invoiceStatus, documents,
         relationships, matching, settlementReadiness, generatedAt]
      properties:
        invoiceId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        vendorWorkId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        workOrderId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        completionReportId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        serviceReportId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        bastDocumentId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        purchaseOrderId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: CR-BE-R2P-01 PART 06 — the linked Purchase Order.
        workContractId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: CR-BE-R2P-01 PART 06 — the linked Work Contract (SPK).
        purchaseOrderStatus:
          allOf: [{ $ref: "#/components/schemas/PurchaseOrderStatus" }]
          nullable: true
          description: Read VERBATIM from the Purchase Order authority.
        purchaseOrderNumber: { type: string, nullable: true }
        workContractStatus:
          allOf: [{ $ref: "#/components/schemas/WorkContractStatus" }]
          nullable: true
          description: Read VERBATIM from the Work Contract authority.
        workContractSpkNumber: { type: string, nullable: true }
        workOrderAssignedVendorId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        workOrderBastRequirement: { type: string, nullable: true }
        bastAcceptanceStatus: { type: string, nullable: true }
        bastIsLegacyProjection: { type: boolean }
        verificationStatus: { type: string, enum: [PENDING, VERIFIED, DISCREPANCY] }
        paymentStatus: { type: string, enum: [UNPAID, PARTIALLY_PAID, PAID] }
        invoiceStatus: { type: string, enum: [DRAFT, FINALIZED, CANCELLED] }
        documents:
          type: array
          items: { $ref: "#/components/schemas/R2PTraceDocument" }
        relationships:
          type: array
          items: { $ref: "#/components/schemas/R2PTraceRelationship" }
        matching: { $ref: "#/components/schemas/InvoiceMatchingResult" }
        settlementReadiness:
          $ref: "#/components/schemas/VendorSettlementReadinessResult"
        generatedAt: { type: string, format: date-time }

    # -------------------------------------------------------------------------
    # CR-BE-MAT-01 — Material fulfilment chain schemas.
    # Traceable stable references: MaterialRequest.purchaseRequestId →
    # Receiving.materialRequestId → Receiving.stockMovementId (STOCK_IN) →
    # StockMovement → StockBalance (derived state) →
    # WorkOrderMaterialUsage.stockMovementId (STOCK_OUT) →
    # WorkOrderMaterialUsage.unitCost/totalCost (operational material cost).
    # -------------------------------------------------------------------------
    MaterialReservationStatus:
      type: string
      description: >
        ACTIVE allocation → RELEASED, CANCELLED, or CONSUMED. Partial issue
        consumption remains ACTIVE with a positive remaining allocation; full
        consumption becomes CONSUMED with zero remaining allocation. Reservation
        state is not a demand or stock authority; it references an existing
        Material Request.
      enum: [ACTIVE, RELEASED, CANCELLED, CONSUMED]
    MaterialReservation:
      type: object
      description: >
        CR-BE-INV-CONTROL-01 PART 01–02 allocation and controlled consumption against an existing approved
        Material Request. `reservedQuantity` is the original allocation;
        `consumedQuantity` and generated `remainingQuantity` track controlled
        issue consumption. Partial consumption remains ACTIVE; full consumption
        becomes CONSUMED. The existing
        `inventory_stock_balances.reserved_quantity` is updated transactionally
        and `quantityOnHand` is never changed by reservation progress.
      required:
        [id, clientId, buildingId, materialRequestId, warehouseId, itemId,
         uomId, reservedQuantity, consumedQuantity, remainingQuantity, status,
         createdByUserId, releasedByUserId, cancelledByUserId, consumedByUserId,
         notes, createdAt, releasedAt, cancelledAt, consumedAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        materialRequestId: { $ref: "#/components/schemas/Uuid" }
        warehouseId: { $ref: "#/components/schemas/Uuid" }
        itemId: { $ref: "#/components/schemas/Uuid" }
        uomId: { type: string, format: uuid, nullable: true }
        reservedQuantity: { type: number, exclusiveMinimum: 0 }
        consumedQuantity: { type: number, minimum: 0 }
        remainingQuantity: { type: number, minimum: 0 }
        status: { $ref: "#/components/schemas/MaterialReservationStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        releasedByUserId: { type: string, format: uuid, nullable: true }
        cancelledByUserId: { type: string, format: uuid, nullable: true }
        consumedByUserId: { type: string, format: uuid, nullable: true }
        notes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        releasedAt: { type: string, format: date-time, nullable: true }
        cancelledAt: { type: string, format: date-time, nullable: true }
        consumedAt: { type: string, format: date-time, nullable: true }
        updatedAt: { type: string, format: date-time }
        materialRequest:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            itemId: { $ref: "#/components/schemas/Uuid" }
            warehouseId: { type: string, format: uuid, nullable: true }
            quantity: { type: number }
            approvedQuantity: { type: number, nullable: true }
            status: { type: string }
        warehouse:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            buildingId: { $ref: "#/components/schemas/Uuid" }
        item:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            itemType: { type: string }
            uomId: { type: string, format: uuid, nullable: true }
    MaterialReservationCreateRequest:
      type: object
      required: [warehouseId, quantity]
      properties:
        warehouseId: { $ref: "#/components/schemas/Uuid" }
        itemId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Optional assertion; the item is derived from the Material Request.
        uomId:
          type: string
          format: uuid
          nullable: true
          description: Optional assertion; must match the Item/MR UOM exactly.
        quantity: { type: number, exclusiveMinimum: 0 }
        notes: { type: string, nullable: true, maxLength: 1000 }
    MaterialRequestStatus:
      type: string
      description: OPEN → APPROVED (PART 02 freeze) | CANCELLED (correction path).
      enum: [OPEN, APPROVED, CANCELLED]
    MaterialRequest:
      type: object
      description: >
        BE-17B Material Request line — the request authority. `quantity` is
        the preserved requested quantity; `approvedQuantity` (PART 02) is the
        authoritative fulfilment cap, defaulting to the requested quantity
        when approval does not explicitly change it, never above it, and NULL
        on historical/unapproved lines (downstream falls back to `quantity`).
        `uomId` is the UOM context captured from the Item Master at creation
        and frozen after approval. Detail reads derive `receivedQuantity` and
        `remainingQuantity`.
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        purchaseRequestId: { $ref: "#/components/schemas/Uuid" }
        itemId: { $ref: "#/components/schemas/Uuid" }
        warehouseId: { type: string, format: uuid, nullable: true }
        quantity: { type: number, description: Requested quantity (preserved). }
        approvedQuantity: { type: number, nullable: true }
        approvedAt: { type: string, format: date-time, nullable: true }
        approvedByUserId: { type: string, format: uuid, nullable: true }
        receivedQuantity:
          type: number
          description: Cumulative valid received quantity (detail reads only).
        remainingQuantity:
          type: number
          description: >
            Derived (approvedQuantity ?? quantity) − receivedQuantity (detail
            reads only). No separate remaining-quantity store exists.
        uomId: { type: string, format: uuid, nullable: true }
        requiredDate: { type: string, format: date-time, nullable: true }
        notes: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/MaterialRequestStatus" }
        requestedByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        purchaseRequest:
          type: object
          nullable: true
          description: Resolved Purchase Request context (detail reads).
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            requestNumber: { type: string }
            title: { type: string }
            status: { type: string }
        item:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            itemType: { type: string }
            uomId: { type: string, format: uuid, nullable: true }
        warehouse:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            buildingId: { $ref: "#/components/schemas/Uuid" }
    MaterialRequestCreateRequest:
      type: object
      required: [itemId, quantity]
      properties:
        itemId: { $ref: "#/components/schemas/Uuid" }
        quantity: { type: number, description: Must be > 0. }
        uomId:
          type: string
          format: uuid
          nullable: true
          description: Optional; must equal the item UOM exactly (no conversion).
        warehouseId: { type: string, format: uuid, nullable: true }
        requiredDate: { type: string, format: date-time, nullable: true }
        notes: { type: string, nullable: true }
    MaterialRequestUpdateRequest:
      type: object
      description: Only OPEN lines are editable (PART 02 freeze).
      properties:
        quantity: { type: number }
        uomId: { type: string, format: uuid, nullable: true }
        warehouseId: { type: string, format: uuid, nullable: true }
        requiredDate: { type: string, format: date-time, nullable: true }
        notes: { type: string, nullable: true }

    ProcurementApprovalRequestType:
      type: string
      enum: [PURCHASE_REQUEST, MATERIAL_REQUEST, SERVICE_REQUEST, RFQ]
    ProcurementApprovalStatus:
      type: string
      enum: [PENDING, APPROVED, REJECTED]
    ProcurementApproval:
      type: object
      description: >
        BE-17D approval binding. Decisions are append-only; an APPROVED
        MATERIAL_REQUEST binding establishes the line's approved quantity
        (CR-BE-MAT-01 PART 02).
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        requestType: { $ref: "#/components/schemas/ProcurementApprovalRequestType" }
        purchaseRequestId: { type: string, format: uuid, nullable: true }
        materialRequestId: { type: string, format: uuid, nullable: true }
        serviceRequestId: { type: string, format: uuid, nullable: true }
        rfqId: { type: string, format: uuid, nullable: true, description: Populated for RFQ award approval targets. }
        recommendationId: { type: string, format: uuid, nullable: true, description: Exact RFQ recommendation for RFQ approval targets. }
        requestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Convenience alias of the populated request reference.
        approvalType: { type: string }
        approverUserId: { $ref: "#/components/schemas/Uuid" }
        status: { $ref: "#/components/schemas/ProcurementApprovalStatus" }
        decidedAt: { type: string, format: date-time, nullable: true }
        decisionNotes: { type: string, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    ProcurementApprovalCreateRequest:
      type: object
      required: [requestType, requestId, approvalType, approverUserId]
      properties:
        requestType: { $ref: "#/components/schemas/ProcurementApprovalRequestType" }
        requestId: { $ref: "#/components/schemas/Uuid" }
        approvalType:
          type: string
          description: Existing data-driven approval type. RFQ awards require `RFQ_AWARD`.
        approverUserId: { $ref: "#/components/schemas/Uuid" }
        recommendationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Required with requestType RFQ; binds approval to the exact recommendation.
    ProcurementApprovalDecisionRequest:
      type: object
      properties:
        decisionNotes: { type: string, nullable: true }
        approvedQuantity:
          type: number
          nullable: true
          description: >
            CR-BE-MAT-01 PART 02 — only when APPROVING a MATERIAL_REQUEST
            binding; > 0 and ≤ the requested line quantity. Omitted →
            approved quantity defaults to the requested quantity.
    ProcurementApprovalAvailableActions:
      type: object
      properties:
        approvalId: { $ref: "#/components/schemas/Uuid" }
        state: { $ref: "#/components/schemas/ProcurementApprovalStatus" }
        availableActions:
          type: array
          items: { type: string, enum: [APPROVE, REJECT] }

    ReceivingType: { type: string, enum: [MATERIAL, SERVICE] }
    ReceivingStatus: { type: string, enum: [RECEIVED, FINALIZED] }
    Receiving:
      type: object
      description: >
        BE-17G Receiving — the receiving authority. `materialRequestId`
        (PART 01) is the stable binding to the fulfilled Material Request
        line; `stockMovementId` is the STOCK_IN ledger movement posted in the
        same transaction; `uomId` is the receipt-time UOM snapshot (PART 04,
        NULL on historical/service rows).
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        requestType: { type: string, enum: [PURCHASE_REQUEST, SERVICE_REQUEST] }
        purchaseRequestId: { type: string, format: uuid, nullable: true }
        serviceRequestId: { type: string, format: uuid, nullable: true }
        materialRequestId: { type: string, format: uuid, nullable: true }
        requestId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        receivingType: { $ref: "#/components/schemas/ReceivingType" }
        itemId: { type: string, format: uuid, nullable: true }
        warehouseId: { type: string, format: uuid, nullable: true }
        quantity: { type: number, nullable: true }
        uomId: { type: string, format: uuid, nullable: true }
        stockMovementId: { type: string, format: uuid, nullable: true }
        receivedByUserId: { $ref: "#/components/schemas/Uuid" }
        receivedAt: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/ReceivingStatus" }
        notes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        vendor:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            vendorCode: { type: string }
            vendorName: { type: string }
            status: { type: string }
        item:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            itemType: { type: string }
        warehouse:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            buildingId: { $ref: "#/components/schemas/Uuid" }
    ReceivingCreateRequest:
      type: object
      required: [requestType, requestId, vendorId, receivingType]
      properties:
        requestType: { type: string, enum: [PURCHASE_REQUEST, SERVICE_REQUEST] }
        requestId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        receivingType: { $ref: "#/components/schemas/ReceivingType" }
        materialRequestId:
          type: string
          format: uuid
          nullable: true
          description: >
            PART 01 — Material Request line being fulfilled (MATERIAL
            receiving on a PURCHASE_REQUEST only). Enables line identity
            enforcement and the cumulative over-receipt guard.
        itemId: { type: string, format: uuid, nullable: true, description: Required for MATERIAL receiving. }
        warehouseId: { type: string, format: uuid, nullable: true, description: Required for MATERIAL receiving. }
        quantity: { type: number, nullable: true, description: Required (> 0) for MATERIAL receiving. }
        uomId:
          type: string
          format: uuid
          nullable: true
          description: >
            PART 04 — optional explicit UOM; must match the resolved MR-line /
            item UOM exactly (`RECEIVING_UOM_INCOMPATIBLE` otherwise).
        notes: { type: string, nullable: true }
    ReceivingUpdateRequest:
      type: object
      properties:
        notes: { type: string, nullable: true }

    StockMovementType: { type: string, enum: [STOCK_IN, STOCK_OUT] }
    StockMovement:
      type: object
      description: >
        BE-16D append-only ledger entry — the stock-ledger authority
        (PART 03). Every balance change has one; `source` carries the
        originating authority (e.g. PROCUREMENT_RECEIVING, STOCK_ADJUSTMENT,
        BALANCE_INITIALIZATION, TRANSFER:{id}, WORK_ORDER:{id}). `uomId` is
        the movement-time UOM snapshot (PART 04, NULL on historical rows).
        `resultingQuantityOnHand`/`resultingAvailableQuantity` snapshot the
        balance after the movement.
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        warehouseId: { $ref: "#/components/schemas/Uuid" }
        itemId: { $ref: "#/components/schemas/Uuid" }
        movementType: { $ref: "#/components/schemas/StockMovementType" }
        quantity: { type: number }
        uomId: { type: string, format: uuid, nullable: true }
        movementDate: { type: string, format: date-time }
        reference: { type: string, nullable: true }
        source: { type: string, nullable: true }
        performedByUserId: { $ref: "#/components/schemas/Uuid" }
        notes: { type: string, nullable: true }
        resultingQuantityOnHand: { type: number }
        resultingAvailableQuantity: { type: number }
        createdAt: { type: string, format: date-time }
        warehouse:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            buildingId: { $ref: "#/components/schemas/Uuid" }
        item:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            itemType: { type: string }
        resultingBalance:
          type: object
          nullable: true
          properties:
            quantityOnHand: { type: number }
            reservedQuantity: { type: number }
            availableQuantity: { type: number }
    StockMovementCreateRequest:
      type: object
      required: [itemId, movementType, quantity]
      properties:
        warehouseId:
          type: string
          format: uuid
          nullable: true
          description: Required on the Client-scoped route; taken from the path otherwise.
        itemId: { $ref: "#/components/schemas/Uuid" }
        movementType: { $ref: "#/components/schemas/StockMovementType" }
        quantity: { type: number, description: Must be > 0. }
        movementDate: { type: string, format: date-time, nullable: true }
        reference: { type: string, nullable: true }
        source: { type: string, nullable: true }
        notes: { type: string, nullable: true }

    StockBalance:
      type: object
      description: >
        BE-16C derived/current stock state per warehouse + item. Mutated ONLY
        through ledger movements (PART 03); `availableQuantity` =
        quantityOnHand − reservedQuantity is DB-generated.
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        warehouseId: { $ref: "#/components/schemas/Uuid" }
        itemId: { $ref: "#/components/schemas/Uuid" }
        quantityOnHand: { type: number }
        reservedQuantity: { type: number }
        availableQuantity: { type: number }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    StockBalanceInitializeRequest:
      type: object
      required: [itemId]
      properties:
        warehouseId:
          type: string
          format: uuid
          nullable: true
          description: Required on the Client-scoped route; taken from the path otherwise.
        itemId: { $ref: "#/components/schemas/Uuid" }
        quantityOnHand:
          type: number
          nullable: true
          description: >
            Default 0. A non-zero seed enters through the ledger as a
            BALANCE_INITIALIZATION STOCK_IN (PART 03).
        reservedQuantity: { type: number, nullable: true, description: Default 0; ≤ quantityOnHand. }

    WorkOrderMaterialUsage:
      type: object
      description: >
        BE-16I Work Order Material Usage — the usage authority (append-only,
        immutable). `stockMovementId` (PART 06) is the stable link to the
        STOCK_OUT ledger movement committed in the same transaction; `uomId`
        (PART 04) and `unitCost`/`totalCost`/`currency`/`costSource`/
        `costReference` (PART 05 operational cost snapshot, `totalCost` =
        quantity × unitCost computed in NUMERIC) never change after issue.
        NULL snapshot fields on historical / cost-less rows.
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        warehouseId: { $ref: "#/components/schemas/Uuid" }
        itemId: { $ref: "#/components/schemas/Uuid" }
        materialRequestId: { type: string, format: uuid, nullable: true }
        reservationId: { type: string, format: uuid, nullable: true }
        quantity: { type: number }
        uomId: { type: string, format: uuid, nullable: true }
        unitCost: { type: number, nullable: true }
        totalCost: { type: number, nullable: true }
        currency:
          type: string
          nullable: true
          pattern: "^[A-Z]{3}$"
          description: >
            Currency snapshot at issue/use time. NULL remains UNKNOWN for
            historical / cost-less rows. Value is governed (ACTIVE Currency
            Master entry + Client-allowed) on new costed usage; historical
            NULL is never inferred, substituted, or converted.
        costSource: { type: string, nullable: true }
        costReference: { type: string, nullable: true }
        stockMovementId: { type: string, format: uuid, nullable: true }
        usedByUserId: { $ref: "#/components/schemas/Uuid" }
        usedAt: { type: string, format: date-time }
        reference: { type: string, nullable: true }
        notes: { type: string, nullable: true }
        resultingQuantityOnHand: { type: number }
        resultingAvailableQuantity: { type: number }
        createdAt: { type: string, format: date-time }
        workOrder:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            workOrderNumber: { type: string }
            title: { type: string }
            status: { type: string }
        warehouse:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            buildingId: { $ref: "#/components/schemas/Uuid" }
        item:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            itemType: { type: string }
        resultingBalance:
          type: object
          nullable: true
          properties:
            quantityOnHand: { type: number }
            reservedQuantity: { type: number }
            availableQuantity: { type: number }
    WorkOrderMaterialUsageCreateRequest:
      type: object
      required: [itemId, warehouseId, quantity]
      properties:
        itemId: { $ref: "#/components/schemas/Uuid" }
        warehouseId: { $ref: "#/components/schemas/Uuid" }
        materialRequestId:
          type: string
          format: uuid
          nullable: true
          description: Optional assertion; authoritative source is resolved from the Work Order procurement binding.
        reservationId:
          type: string
          format: uuid
          nullable: true
          description: Optional ACTIVE reservation allocation to consume.
        quantity: { type: number, description: Must be > 0 and within authorized demand and applicable stock. }
        uomId:
          type: string
          format: uuid
          nullable: true
          description: PART 06 — optional; must equal the item UOM (no conversion).
        unitCost:
          type: number
          nullable: true
          description: PART 05 — optional operational unit cost snapshot (≥ 0).
        currency:
          type: string
          nullable: true
          pattern: "^[A-Z]{3}$"
          description: >
            Required when unitCost is supplied (fail-closed with
            WO_MATERIAL_USAGE_CURRENCY_REQUIRED). Must be an ACTIVE Currency
            Master entry allowed for the resolved Client
            (CURRENCY_INACTIVE_OR_UNKNOWN / CLIENT_CURRENCY_NOT_ALLOWED).
            Optional/null for a non-costed usage. No IDR/default/base inference
            and no FX/conversion.
        costSource: { type: string, nullable: true, description: Defaults to MANUAL when unitCost is set. }
        costReference: { type: string, nullable: true }
        usedAt: { type: string, format: date-time, nullable: true }
        reference:
          type: string
          nullable: true
          description: Duplicate-issue protection — unique per Work Order.
        notes: { type: string, nullable: true }
    WorkOrderMaterialCostSummary:
      type: object
      description: >
        PART 05 — deterministic Work Order → Material Usage → total material
        cost aggregation over immutable snapshots. Material cost only.
      properties:
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        usageCount: { type: integer }
        costedUsageCount: { type: integer }
        totalMaterialCost: { type: number }
        byCurrency:
          type: array
          items:
            type: object
            properties:
              currency: { type: string, nullable: true }
              totalCost: { type: number }

    # ------------------------------------------------------------------
    # CR-BE-MOB-01 PART 01 — Housekeeping operational schemas (BE-11).
    # ------------------------------------------------------------------
    DailyCleaningStatus:
      type: string
      enum: [OPEN, ASSIGNED, IN_PROGRESS, COMPLETED, CANCELLED]
      description: BE-07 generated-task status projected as daily cleaning.
    DailyCleaning:
      type: object
      description: >-
        BE-11C operational view of a BE-07 generated task. `id` equals
        `taskId` and is the authoritative id for start/complete/cancel.
      required:
        [id, taskId, clientId, buildingId, cleaningAreaId, scheduleBindingId,
         scheduleDefinitionId, operationalDate, occurrenceAt, status,
         startedAt, completedAt, completedByUserId, completionNotes,
         cleaningArea, schedule, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        taskId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        scheduleBindingId: { $ref: "#/components/schemas/Uuid" }
        scheduleDefinitionId: { $ref: "#/components/schemas/Uuid" }
        operationalDate: { type: string, format: date }
        occurrenceAt: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/DailyCleaningStatus" }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        completedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        completionNotes: { type: string, nullable: true }
        cleaningArea:
          type: object
          required: [id, code, name, cleaningAreaType, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            cleaningAreaType: { type: string }
            status: { type: string }
            floorId: { type: string, format: uuid, nullable: true }
            areaId: { type: string, format: uuid, nullable: true }
            roomId: { type: string, format: uuid, nullable: true }
            spaceId: { type: string, format: uuid, nullable: true }
            functionalLocationId: { type: string, format: uuid, nullable: true }
        schedule:
          type: object
          required: [id, code, name, targetType, targetId, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            targetType: { type: string }
            targetId: { $ref: "#/components/schemas/Uuid" }
            status: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CleaningAssignmentStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    CleaningAssignment:
      type: object
      required:
        [id, taskId, assigneeType, workforceProfileId, teamId,
         assignedByUserId, assignedAt, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        taskId: { $ref: "#/components/schemas/Uuid" }
        assigneeType: { type: string, enum: [WORKFORCE, TEAM] }
        workforceProfileId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        teamId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        assignedByUserId: { $ref: "#/components/schemas/Uuid" }
        assignedAt: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/CleaningAssignmentStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        assignee:
          type: object
          nullable: true
          properties:
            type: { type: string, enum: [WORKFORCE, TEAM] }
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
    CreateCleaningAssignmentRequest:
      type: object
      required: [assigneeType]
      properties:
        assigneeType: { type: string, enum: [WORKFORCE, TEAM] }
        workforceProfileId:
          type: string
          format: uuid
          description: Required when assigneeType is WORKFORCE.
        teamId:
          type: string
          format: uuid
          description: Required when assigneeType is TEAM.

    HousekeepingBindingStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    ToiletInspectionBinding:
      type: object
      required:
        [id, clientId, buildingId, cleaningAreaId, checklistTemplateId,
         roomId, functionalLocationId, description, status, createdByUserId,
         createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        roomId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        functionalLocationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/HousekeepingBindingStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        cleaningArea:
          type: object
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            cleaningAreaType: { type: string }
            status: { type: string }
        checklistTemplate:
          type: object
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string }
        room:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
        functionalLocation:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string }
    CreateToiletInspectionBindingRequest:
      type: object
      required: [cleaningAreaId, checklistTemplateId]
      properties:
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        roomId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        description: { type: string, maxLength: 512, nullable: true }
        status: { $ref: "#/components/schemas/HousekeepingBindingStatus" }
    UpdateToiletInspectionBindingRequest:
      type: object
      properties:
        roomId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        description: { type: string, maxLength: 512, nullable: true }
        status: { $ref: "#/components/schemas/HousekeepingBindingStatus" }
    ToiletInspectionExecution:
      type: object
      required: [id, checklistTemplateId, toiletInspectionBindingId, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        toiletInspectionBindingId: { $ref: "#/components/schemas/Uuid" }
        status: { type: string }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    ToiletInspectionExecutionContext:
      type: object
      required: [execution, toiletInspectionBinding]
      properties:
        execution: { $ref: "#/components/schemas/ToiletInspectionExecution" }
        toiletInspectionBinding: { $ref: "#/components/schemas/ToiletInspectionBinding" }

    PublicAreaInspectionBinding:
      type: object
      required:
        [id, clientId, buildingId, cleaningAreaId, checklistTemplateId,
         floorId, areaId, roomId, functionalLocationId, description, status,
         createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        floorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        areaId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        roomId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        functionalLocationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/HousekeepingBindingStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        cleaningArea:
          type: object
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            cleaningAreaType: { type: string }
            status: { type: string }
        checklistTemplate:
          type: object
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string }
    CreatePublicAreaInspectionBindingRequest:
      type: object
      required: [cleaningAreaId, checklistTemplateId]
      properties:
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        floorId: { type: string, format: uuid, nullable: true }
        areaId: { type: string, format: uuid, nullable: true }
        roomId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/HousekeepingBindingStatus" }
    UpdatePublicAreaInspectionBindingRequest:
      type: object
      properties:
        floorId: { type: string, format: uuid, nullable: true }
        areaId: { type: string, format: uuid, nullable: true }
        roomId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/HousekeepingBindingStatus" }
    PublicAreaInspectionExecution:
      type: object
      required: [id, checklistTemplateId, publicAreaInspectionBindingId, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        publicAreaInspectionBindingId: { $ref: "#/components/schemas/Uuid" }
        status: { type: string }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    PublicAreaInspectionExecutionContext:
      type: object
      required: [execution, publicAreaInspectionBinding]
      properties:
        execution: { $ref: "#/components/schemas/PublicAreaInspectionExecution" }
        publicAreaInspectionBinding: { $ref: "#/components/schemas/PublicAreaInspectionBinding" }

    SupervisorInspectionTargetType:
      type: string
      enum: [DAILY_CLEANING, TOILET_INSPECTION, PUBLIC_AREA_INSPECTION]
    SupervisorInspectionStatus:
      type: string
      enum: [PENDING, COMPLETED]
    SupervisorInspectionDecision:
      type: string
      enum: [APPROVED, REJECTED, REWORK_REQUIRED]
    SupervisorInspection:
      type: object
      required:
        [id, clientId, buildingId, cleaningAreaId, targetType, targetId,
         reviewId, supervisorUserId, decision, status, notes, inspectedAt,
         createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        targetType: { $ref: "#/components/schemas/SupervisorInspectionTargetType" }
        targetId: { $ref: "#/components/schemas/Uuid" }
        reviewId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        supervisorUserId: { $ref: "#/components/schemas/Uuid" }
        decision:
          allOf: [{ $ref: "#/components/schemas/SupervisorInspectionDecision" }]
          nullable: true
        status: { $ref: "#/components/schemas/SupervisorInspectionStatus" }
        notes: { type: string, nullable: true }
        inspectedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        cleaningArea:
          type: object
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            cleaningAreaType: { type: string }
            status: { type: string }
        targetSummary:
          type: object
          nullable: true
          properties:
            status: { type: string }
            description: { type: string }
    CreateSupervisorInspectionRequest:
      type: object
      required: [targetType, targetId]
      properties:
        targetType: { $ref: "#/components/schemas/SupervisorInspectionTargetType" }
        targetId: { $ref: "#/components/schemas/Uuid" }
        notes: { type: string, maxLength: 1000, nullable: true }
    SubmitSupervisorInspectionDecisionRequest:
      type: object
      required: [decision]
      properties:
        decision: { $ref: "#/components/schemas/SupervisorInspectionDecision" }
        notes: { type: string, maxLength: 1000, nullable: true }

    QualityAuditSourceType:
      type: string
      enum: [DAILY_CLEANING, TOILET_INSPECTION, PUBLIC_AREA_INSPECTION, SUPERVISOR_INSPECTION, CLEANING_AREA]
    QualityAuditStatus:
      type: string
      enum: [DRAFT, COMPLETED]
    QualityAuditResult:
      type: string
      enum: [PASS, FAIL, REWORK_REQUIRED]
    QualityAudit:
      type: object
      required:
        [id, clientId, buildingId, cleaningAreaId, sourceType, sourceId,
         auditorUserId, score, result, status, notes, auditedAt, createdAt,
         updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        sourceType: { $ref: "#/components/schemas/QualityAuditSourceType" }
        sourceId: { $ref: "#/components/schemas/Uuid" }
        auditorUserId: { $ref: "#/components/schemas/Uuid" }
        score: { type: number, nullable: true }
        result:
          allOf: [{ $ref: "#/components/schemas/QualityAuditResult" }]
          nullable: true
        status: { $ref: "#/components/schemas/QualityAuditStatus" }
        notes: { type: string, nullable: true }
        auditedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        cleaningArea:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string }
    CreateQualityAuditRequest:
      type: object
      required: [sourceType, sourceId]
      properties:
        sourceType: { $ref: "#/components/schemas/QualityAuditSourceType" }
        sourceId: { $ref: "#/components/schemas/Uuid" }
        score: { type: number, minimum: 0, maximum: 100, nullable: true }
        result: { $ref: "#/components/schemas/QualityAuditResult" }
        notes: { type: string, maxLength: 1000, nullable: true }
    UpdateQualityAuditRequest:
      type: object
      properties:
        score: { type: number, minimum: 0, maximum: 100, nullable: true }
        result: { $ref: "#/components/schemas/QualityAuditResult" }
        notes: { type: string, maxLength: 1000, nullable: true }
    CompleteQualityAuditRequest:
      type: object
      required: [result]
      properties:
        result: { $ref: "#/components/schemas/QualityAuditResult" }
        score: { type: number, minimum: 0, maximum: 100, nullable: true }
        notes: { type: string, maxLength: 1000, nullable: true }

    HousekeepingFindingSourceType:
      type: string
      enum: [DAILY_CLEANING, TOILET_INSPECTION, PUBLIC_AREA_INSPECTION, SUPERVISOR_INSPECTION]
    HousekeepingFinding:
      type: object
      description: >-
        BE-11H binding. `id` is the HK link id; `findingId` is the BE-09
        Finding used for rework/verification.
      required:
        [id, clientId, buildingId, findingId, cleaningAreaId, sourceType,
         sourceId, floorId, areaId, roomId, functionalLocationId, notes,
         createdAt, updatedAt, finding]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        findingId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        sourceType: { $ref: "#/components/schemas/HousekeepingFindingSourceType" }
        sourceId: { $ref: "#/components/schemas/Uuid" }
        floorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        areaId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        roomId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        functionalLocationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        notes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        finding:
          type: object
          required: [id, findingNumber, title, status, reportedByUserId, reportedAt]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            findingNumber: { type: string }
            title: { type: string }
            description: { type: string, nullable: true }
            status: { type: string }
            state: { type: string }
            reportedByUserId: { $ref: "#/components/schemas/Uuid" }
            reportedAt: { type: string, format: date-time }
        cleaningArea:
          type: object
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            cleaningAreaType: { type: string }
            status: { type: string }
        availableActions:
          type: array
          items: { type: string }
          description: BE-09 backend-authoritative actions for the linked finding.
    CreateHousekeepingFindingRequest:
      type: object
      required: [buildingId, cleaningAreaId, sourceType, sourceId, title]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        sourceType: { $ref: "#/components/schemas/HousekeepingFindingSourceType" }
        sourceId: { $ref: "#/components/schemas/Uuid" }
        title: { type: string, maxLength: 160 }
        description: { type: string, maxLength: 1000, nullable: true }
        findingNumber: { type: string, maxLength: 64 }
        floorId: { type: string, format: uuid, nullable: true }
        areaId: { type: string, format: uuid, nullable: true }
        roomId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        notes: { type: string, maxLength: 1000, nullable: true }

    HousekeepingEvidenceSourceType:
      type: string
      enum: [daily-cleaning, toilet-inspections, public-area-inspections, supervisor-inspections, findings]
    HousekeepingEvidenceRequirement:
      type: object
      required:
        [id, clientId, targetType, targetId, evidenceType, required,
         minimumCount, maximumCount, description, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        targetType: { type: string }
        targetId: { $ref: "#/components/schemas/Uuid" }
        evidenceType: { type: string, enum: [PHOTO, DOCUMENT, SIGNATURE] }
        required: { type: boolean }
        minimumCount: { type: integer }
        maximumCount: { type: integer, nullable: true }
        description: { type: string, nullable: true }
        status: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    HousekeepingEvidenceSubmission:
      type: object
      required:
        [id, clientId, evidenceRequirementId, executionType, executionId,
         evidenceType, fileReference, originalFileName, mimeType, fileSize,
         capturedAt, submittedByUserId, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        evidenceRequirementId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        executionType: { type: string }
        executionId: { $ref: "#/components/schemas/Uuid" }
        evidenceType: { type: string, enum: [PHOTO, DOCUMENT, SIGNATURE] }
        fileReference: { type: string }
        originalFileName: { type: string }
        mimeType: { type: string }
        fileSize: { type: integer }
        capturedAt: { type: string, format: date-time, nullable: true }
        submittedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        status: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    SubmitHousekeepingEvidenceRequest:
      type: object
      required: [evidenceType, fileReference, originalFileName, mimeType, fileSize]
      properties:
        evidenceType: { type: string, enum: [PHOTO, DOCUMENT, SIGNATURE] }
        evidenceRequirementId: { type: string, format: uuid, nullable: true }
        fileReference: { type: string, maxLength: 512 }
        originalFileName: { type: string, maxLength: 256 }
        mimeType: { type: string, maxLength: 128 }
        fileSize: { type: integer, minimum: 0, maximum: 52428800 }
        capturedAt: { type: string, format: date-time }

    # ------------------------------------------------------------------
    # CR-BE-MOB-01 PART 02 — Security operational schemas
    # (BE-12A/B/C/D/E/F/H + BE-21A/B). Shapes mirror the implemented
    # public DTOs; no new field, status, or workflow is introduced.
    # ------------------------------------------------------------------
    SecurityPostStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    SecurityPostType:
      type: string
      enum:
        [GENERAL, LOBBY, GATE, PERIMETER, PARKING, CONTROL_ROOM, PATROL,
         STANDBY, OTHER]
    SecurityPost:
      type: object
      description: >-
        BE-12A Security Post — operational duty/post context anchored to the
        BE-04 structure. `clientId` is derived through Building → Property →
        Client.
      required:
        [id, clientId, buildingId, floorId, areaId, roomId, spaceId,
         functionalLocationId, code, name, description, postType, status,
         createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        floorId: { type: string, format: uuid, nullable: true }
        areaId: { type: string, format: uuid, nullable: true }
        roomId: { type: string, format: uuid, nullable: true }
        spaceId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        postType: { $ref: "#/components/schemas/SecurityPostType" }
        status: { $ref: "#/components/schemas/SecurityPostStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateSecurityPostRequest:
      type: object
      required: [code, name]
      properties:
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        postType: { $ref: "#/components/schemas/SecurityPostType" }
        status: { $ref: "#/components/schemas/SecurityPostStatus" }
        floorId: { type: string, format: uuid, nullable: true }
        areaId: { type: string, format: uuid, nullable: true }
        roomId: { type: string, format: uuid, nullable: true }
        spaceId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
    UpdateSecurityPostRequest:
      type: object
      description: "`code`, `clientId` and `buildingId` are immutable."
      properties:
        name: { type: string }
        description: { type: string, nullable: true }
        postType: { $ref: "#/components/schemas/SecurityPostType" }
        status: { $ref: "#/components/schemas/SecurityPostStatus" }
        floorId: { type: string, format: uuid, nullable: true }
        areaId: { type: string, format: uuid, nullable: true }
        roomId: { type: string, format: uuid, nullable: true }
        spaceId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }

    PatrolRouteStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    PatrolRoute:
      type: object
      description: >-
        BE-12B Patrol Route — an ordered patrol definition for one Building.
        Carries no schedule, task or execution semantics.
      required:
        [id, clientId, buildingId, startSecurityPostId, code, name,
         description, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/PatrolRouteStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreatePatrolRouteRequest:
      type: object
      required: [code, name]
      properties:
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/PatrolRouteStatus" }
        startSecurityPostId: { type: string, format: uuid, nullable: true }
    UpdatePatrolRouteRequest:
      type: object
      properties:
        name: { type: string }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/PatrolRouteStatus" }
        startSecurityPostId: { type: string, format: uuid, nullable: true }

    PatrolRoutePointStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    PatrolRoutePoint:
      type: object
      description: >-
        BE-12B Patrol Route Point — the Security checkpoint. It is a
        sequenced reference to an existing BE-04 location, not a separate
        checkpoint master and not a QR identifier.
      required:
        [id, clientId, buildingId, patrolRouteId, floorId, areaId, roomId,
         spaceId, functionalLocationId, sequence, notes, status, createdAt,
         updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        patrolRouteId: { $ref: "#/components/schemas/Uuid" }
        floorId: { type: string, format: uuid, nullable: true }
        areaId: { type: string, format: uuid, nullable: true }
        roomId: { type: string, format: uuid, nullable: true }
        spaceId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        sequence: { type: integer, minimum: 1 }
        notes: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/PatrolRoutePointStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreatePatrolRoutePointRequest:
      type: object
      required: [sequence]
      properties:
        sequence: { type: integer, minimum: 1 }
        notes: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/PatrolRoutePointStatus" }
        floorId: { type: string, format: uuid, nullable: true }
        areaId: { type: string, format: uuid, nullable: true }
        roomId: { type: string, format: uuid, nullable: true }
        spaceId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
    UpdatePatrolRoutePointRequest:
      type: object
      properties:
        sequence: { type: integer, minimum: 1 }
        notes: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/PatrolRoutePointStatus" }
        floorId: { type: string, format: uuid, nullable: true }
        areaId: { type: string, format: uuid, nullable: true }
        roomId: { type: string, format: uuid, nullable: true }
        spaceId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }

    PatrolScheduleBindingStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    PatrolScheduleBinding:
      type: object
      description: >-
        BE-12C binding between a Patrol Route and a shared BE-07 Schedule
        Definition. Recurrence and task generation stay in BE-07; the
        resolved `schedule` block is read-through context, never a copy.
      required:
        [id, clientId, buildingId, patrolRouteId, scheduleDefinitionId,
         startSecurityPostId, description, status, createdByUserId,
         createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        patrolRouteId: { $ref: "#/components/schemas/Uuid" }
        scheduleDefinitionId: { $ref: "#/components/schemas/Uuid" }
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        description: { type: string, nullable: true }
        status:
          $ref: "#/components/schemas/PatrolScheduleBindingStatus"
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        patrolRoute:
          type: object
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string }
        schedule:
          type: object
          description: Resolved BE-07 Schedule Definition + recurrence.
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            targetType: { type: string }
            targetId: { $ref: "#/components/schemas/Uuid" }
            startAt: { type: string, format: date-time }
            endAt: { type: string, format: date-time, nullable: true }
            timezone: { type: string }
            status: { type: string }
            recurrence:
              type: object
              nullable: true
              properties:
                id: { $ref: "#/components/schemas/Uuid" }
                frequency: { type: string }
                interval: { type: integer }
                daysOfWeek:
                  type: array
                  nullable: true
                  items: { type: integer }
                dayOfMonth: { type: integer, nullable: true }
                startDate: { type: string, format: date }
                endDate: { type: string, format: date, nullable: true }
                status: { type: string }
    CreatePatrolScheduleBindingRequest:
      type: object
      description: >-
        Either bind an existing BE-07 Schedule Definition
        (`scheduleDefinitionId`) or supply the inline schedule fields so the
        BE-07 schedule is created by the shared service. `patrolRouteId` is
        taken from the path.
      properties:
        scheduleDefinitionId: { type: string, format: uuid }
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        description: { type: string, nullable: true }
        status:
          $ref: "#/components/schemas/PatrolScheduleBindingStatus"
        code: { type: string }
        name: { type: string }
        targetType: { type: string }
        targetId: { type: string, format: uuid }
        startAt: { type: string, format: date-time }
        endAt: { type: string, format: date-time, nullable: true }
        timezone: { type: string }
    UpdatePatrolScheduleBindingRequest:
      type: object
      properties:
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        description: { type: string, nullable: true }
        status:
          $ref: "#/components/schemas/PatrolScheduleBindingStatus"

    PatrolExecutionStatus:
      type: string
      enum: [OPEN, ASSIGNED, IN_PROGRESS, COMPLETED, CANCELLED]
      description: >-
        The shared BE-07 generated-task status, re-exported. Security does
        not own a second patrol lifecycle.
    PatrolExecution:
      type: object
      description: >-
        BE-12D operational view of a BE-07 generated task bound to a Patrol
        Route. `id` equals `taskId`; assignment uses the published BE-07
        task-assignment operations on that id.
      required:
        [id, taskId, clientId, buildingId, patrolRouteId, scheduleBindingId,
         scheduleDefinitionId, startSecurityPostId, operationalDate,
         occurrenceAt, status, startedAt, completedAt, completedByUserId,
         completionNotes, patrolRoute, startSecurityPost, schedule,
         pointProgress, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        taskId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        patrolRouteId: { $ref: "#/components/schemas/Uuid" }
        scheduleBindingId: { $ref: "#/components/schemas/Uuid" }
        scheduleDefinitionId: { $ref: "#/components/schemas/Uuid" }
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        operationalDate: { type: string, format: date }
        occurrenceAt: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/PatrolExecutionStatus" }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        completedByUserId: { type: string, format: uuid, nullable: true }
        completionNotes: { type: string, nullable: true }
        patrolRoute:
          type: object
          required: [id, code, name, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string }
        startSecurityPost:
          type: object
          required: [id, code, name]
          properties:
            id: { type: string, format: uuid, nullable: true }
            code: { type: string, nullable: true }
            name: { type: string, nullable: true }
        schedule:
          type: object
          required: [id, code, name, targetType, targetId, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            targetType: { type: string }
            targetId: { $ref: "#/components/schemas/Uuid" }
            status: { type: string }
        pointProgress:
          type: object
          description: >-
            Backend-derived checkpoint progress over the route's ACTIVE
            points. The client must not recompute it.
          required: [totalPoints, visitedPoints, pendingPoints, nextSequence]
          properties:
            totalPoints: { type: integer }
            visitedPoints: { type: integer }
            pendingPoints: { type: integer }
            nextSequence: { type: integer, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    PatrolPointVisit:
      type: object
      description: BE-12D checkpoint confirmation (`patrol_point_visits`).
      required:
        [id, clientId, buildingId, taskId, patrolRouteId, patrolRoutePointId,
         sequence, visitedAt, visitedByUserId, notes, status, createdAt,
         updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        taskId: { $ref: "#/components/schemas/Uuid" }
        patrolRouteId: { $ref: "#/components/schemas/Uuid" }
        patrolRoutePointId: { $ref: "#/components/schemas/Uuid" }
        sequence: { type: integer }
        visitedAt: { type: string, format: date-time }
        visitedByUserId: { $ref: "#/components/schemas/Uuid" }
        notes: { type: string, nullable: true }
        status: { type: string, enum: [VISITED, INACTIVE] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    RecordPatrolPointVisitRequest:
      type: object
      description: >-
        The checkpoint is identified by the `pointId` path parameter; the
        body carries optional field notes only.
      properties:
        notes: { type: string, nullable: true, maxLength: 512 }
    UpdatePatrolPointVisitRequest:
      type: object
      properties:
        notes: { type: string, nullable: true, maxLength: 512 }
    CompletePatrolExecutionRequest:
      type: object
      properties:
        completionNotes: { type: string, nullable: true, maxLength: 512 }

    PatrolChecklistBindingStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    PatrolChecklistBinding:
      type: object
      description: >-
        BE-12E join row between a BE-07 Checklist Template and a Patrol
        Route (+ optional start post) in one Building.
      required:
        [id, clientId, buildingId, patrolRouteId, startSecurityPostId,
         checklistTemplateId, status, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        patrolRouteId: { $ref: "#/components/schemas/Uuid" }
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        status:
          $ref: "#/components/schemas/PatrolChecklistBindingStatus"
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreatePatrolChecklistBindingRequest:
      type: object
      required: [buildingId, patrolRouteId, checklistTemplateId]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        patrolRouteId: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        status:
          $ref: "#/components/schemas/PatrolChecklistBindingStatus"
    UpdatePatrolChecklistBindingRequest:
      type: object
      properties:
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        status:
          $ref: "#/components/schemas/PatrolChecklistBindingStatus"
    PatrolChecklistExecution:
      type: object
      description: >-
        The shared BE-07 checklist execution started from a patrol binding.
        Responses, completion, cancellation, evidence and verification use
        the published BE-07 checklist / evidence / verification operations
        on this `id`.
      required:
        [id, checklistTemplateId, patrolChecklistBindingId, status,
         startedAt, completedAt, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        patrolChecklistBindingId: { $ref: "#/components/schemas/Uuid" }
        status: { type: string }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    PatrolChecklistExecutionContext:
      type: object
      description: >-
        BE-12E projection of the authoritative Building / Template / Route /
        start post for a patrol checklist execution. Derived from the
        binding — never stored a second time.
      required: [execution, building, template, patrolRoute, startSecurityPost]
      properties:
        execution:
          $ref: "#/components/schemas/PatrolChecklistExecution"
        building:
          type: object
          required: [id, code, name]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
        template:
          type: object
          required: [id, code, name, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string }
        patrolRoute:
          type: object
          required: [id, code, name, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string }
        startSecurityPost:
          type: object
          required: [id, code, name]
          properties:
            id: { type: string, format: uuid, nullable: true }
            code: { type: string, nullable: true }
            name: { type: string, nullable: true }

    SecurityDailyActivitySummary:
      type: object
      required:
        [scheduledPatrols, inProgressPatrols, completedPatrols,
         cancelledPatrols, openChecklists, completedChecklists, openFindings]
      properties:
        scheduledPatrols: { type: integer }
        inProgressPatrols: { type: integer }
        completedPatrols: { type: integer }
        cancelledPatrols: { type: integer }
        openChecklists: { type: integer }
        completedChecklists: { type: integer }
        openFindings: { type: integer }
    SecurityDailyActivity:
      type: object
      description: >-
        BE-12F read model composing BE-12A/B/C/D/E + BE-07 + BE-09 for one
        Building and operational date. It stores nothing.
      required:
        [buildingId, operationalDate, shift, securityPost, summary, patrols,
         checklists, findings]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        operationalDate: { type: string, format: date }
        shift:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            startTime: { type: string }
            endTime: { type: string }
            status: { type: string, enum: [ACTIVE, INACTIVE] }
        securityPost:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            postType: { type: string }
            status: { type: string, enum: [ACTIVE, INACTIVE] }
        summary:
          $ref: "#/components/schemas/SecurityDailyActivitySummary"
        patrols:
          type: array
          items:
            type: object
            description: >-
              `id` is the BE-07 generated-task id (the Patrol Execution id).
            properties:
              id: { $ref: "#/components/schemas/Uuid" }
              patrolRouteId: { $ref: "#/components/schemas/Uuid" }
              patrolRoute:
                type: object
                properties:
                  id: { $ref: "#/components/schemas/Uuid" }
                  code: { type: string }
                  name: { type: string }
                  status: { type: string, enum: [ACTIVE, INACTIVE] }
                  startSecurityPostId:
                    { type: string, format: uuid, nullable: true }
              startSecurityPostId:
                { type: string, format: uuid, nullable: true }
              startSecurityPost:
                type: object
                nullable: true
                properties:
                  id: { $ref: "#/components/schemas/Uuid" }
                  code: { type: string }
                  name: { type: string }
                  postType: { type: string }
                  status: { type: string, enum: [ACTIVE, INACTIVE] }
              status:
                $ref: "#/components/schemas/PatrolExecutionStatus"
              operationalDate: { type: string, format: date }
              occurrenceAt: { type: string, format: date-time }
              startedAt: { type: string, format: date-time, nullable: true }
              completedAt: { type: string, format: date-time, nullable: true }
        checklists:
          type: array
          items:
            type: object
            properties:
              id: { $ref: "#/components/schemas/Uuid" }
              checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
              checklistTemplateCode: { type: string }
              checklistTemplateName: { type: string }
              patrolChecklistBindingId: { $ref: "#/components/schemas/Uuid" }
              patrolRouteId: { $ref: "#/components/schemas/Uuid" }
              status:
                type: string
                enum: [DRAFT, IN_PROGRESS, COMPLETED, CANCELLED]
              startedAt: { type: string, format: date-time, nullable: true }
              completedAt: { type: string, format: date-time, nullable: true }
              operationalDate: { type: string, format: date }
              createdAt: { type: string, format: date-time }
        findings:
          type: array
          items:
            type: object
            description: >-
              BE-09 finding projection; `availableActions` is sourced from
              BE-09 and is backend-authoritative.
            properties:
              id: { $ref: "#/components/schemas/Uuid" }
              findingNumber: { type: string }
              title: { type: string }
              status: { $ref: "#/components/schemas/FindingStatus" }
              classificationId:
                { type: string, format: uuid, nullable: true }
              severityId: { type: string, format: uuid, nullable: true }
              createdAt: { type: string, format: date-time }
              reportedByUserId: { $ref: "#/components/schemas/Uuid" }
              availableActions:
                type: array
                items: { type: string }

    SecurityFindingSourceType:
      type: string
      enum:
        [PATROL_EXECUTION, PATROL_CHECKLIST, SECURITY_DAILY_ACTIVITY,
         SHIFT_HANDOVER, SECURITY_POST]
      description: >-
        The Security operational sources. Intentionally separate from BE-09's
        own `FindingSourceType` (WORK_ORDER / CHECKLIST_EXECUTION /
        FORM_INSTANCE).
    SecurityFinding:
      type: object
      description: >-
        BE-12H Security context link for an authoritative BE-09 Finding.
        `id` is the link id; every workflow operation uses `findingId`.
      required:
        [id, clientId, buildingId, findingId, startSecurityPostId,
         patrolRouteId, sourceType, sourceId, createdAt, finding,
         availableActions]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        findingId: { $ref: "#/components/schemas/Uuid" }
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        patrolRouteId: { type: string, format: uuid, nullable: true }
        sourceType:
          $ref: "#/components/schemas/SecurityFindingSourceType"
        sourceId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        finding: { $ref: "#/components/schemas/Finding" }
        availableActions:
          type: array
          items: { type: string }
          description: >-
            BE-09 backend-authoritative actions for the linked finding —
            never re-derived by Security or by the client.
    CreateSecurityFindingRequest:
      type: object
      description: >-
        Creates the BE-09 Finding and the Security link in one call.
        `clientId` and the reporter are derived by the backend.
      required: [buildingId, sourceType, sourceId, title]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        sourceType:
          $ref: "#/components/schemas/SecurityFindingSourceType"
        sourceId: { $ref: "#/components/schemas/Uuid" }
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        patrolRouteId: { type: string, format: uuid, nullable: true }
        title: { type: string, maxLength: 160 }
        description: { type: string, nullable: true }
        classificationId: { type: string, format: uuid }
        severityId: { type: string, format: uuid }
        assigneeType:
          type: string
          enum: [WORKFORCE, TEAM, VENDOR, VENDOR_WORKFORCE]
        workforceProfileId: { type: string, format: uuid }
        teamId: { type: string, format: uuid }
        vendorId: { type: string, format: uuid }

    IncidentType:
      type: string
      enum: [OPERATIONAL, ASSET_FAILURE, FINDING_ESCALATION]
    IncidentSeverity:
      type: string
      enum: [LOW, MEDIUM, HIGH, CRITICAL]
    IncidentPriority:
      type: string
      enum: [LOW, MEDIUM, HIGH, CRITICAL]
    IncidentStatus:
      type: string
      enum: [REPORTED, CANCELLED, CLOSED]
      description: >-
        The BE-21A record lifecycle. `CLOSED` (BE-21K) is the verified
        resolution; `CANCELLED` is withdrawal. Both are terminal and
        mutually exclusive.
    IncidentLocationType:
      type: string
      enum: [FLOOR, AREA, ROOM, SPACE, FUNCTIONAL_LOCATION]
      description: >-
        An OPTIONAL refinement below the Building; omitting it means
        Building level.
    Incident:
      type: object
      description: >-
        BE-21A shared Incident foundation. `clientId` is derived from the
        Building and `reportedByUserId` is the authenticated actor.
      required:
        [id, clientId, buildingId, incidentNumber, incidentType, title,
         description, severity, priority, status, locationType, locationId,
         floorId, areaId, roomId, spaceId, functionalLocationId,
         reportedByUserId, reportedAt, cancelledAt, cancelledByUserId,
         closedAt, closedByUserId, closureNotes, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        incidentNumber: { type: string }
        incidentType: { $ref: "#/components/schemas/IncidentType" }
        title: { type: string }
        description: { type: string, nullable: true }
        severity: { $ref: "#/components/schemas/IncidentSeverity" }
        priority: { $ref: "#/components/schemas/IncidentPriority" }
        status: { $ref: "#/components/schemas/IncidentStatus" }
        locationType:
          allOf: [{ $ref: "#/components/schemas/IncidentLocationType" }]
          nullable: true
        locationId: { type: string, format: uuid, nullable: true }
        floorId: { type: string, format: uuid, nullable: true }
        areaId: { type: string, format: uuid, nullable: true }
        roomId: { type: string, format: uuid, nullable: true }
        spaceId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        reportedByUserId: { $ref: "#/components/schemas/Uuid" }
        reportedAt: { type: string, format: date-time }
        cancelledAt: { type: string, format: date-time, nullable: true }
        cancelledByUserId: { type: string, format: uuid, nullable: true }
        closedAt: { type: string, format: date-time, nullable: true }
        closedByUserId: { type: string, format: uuid, nullable: true }
        closureNotes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateIncidentRequest:
      type: object
      required: [buildingId, incidentNumber, incidentType, title]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        incidentNumber: { type: string }
        incidentType: { $ref: "#/components/schemas/IncidentType" }
        title: { type: string }
        description: { type: string, nullable: true }
        severity: { $ref: "#/components/schemas/IncidentSeverity" }
        priority: { $ref: "#/components/schemas/IncidentPriority" }
        locationType:
          allOf: [{ $ref: "#/components/schemas/IncidentLocationType" }]
          nullable: true
        locationId: { type: string, format: uuid, nullable: true }
        reportedAt: { type: string, format: date-time }
    UpdateIncidentRequest:
      type: object
      description: >-
        Only descriptive metadata is mutable — type, context, number and
        reporter are the identity of the incident.
      properties:
        title: { type: string }
        description: { type: string, nullable: true }
        severity: { $ref: "#/components/schemas/IncidentSeverity" }
        priority: { $ref: "#/components/schemas/IncidentPriority" }

    OperationalIncidentCategory:
      type: string
      enum:
        [UTILITY_FAILURE, ELECTRICAL, PLUMBING, HVAC, LIFT_ESCALATOR,
         FIRE_SAFETY, WATER_LEAK, STRUCTURAL, ENVIRONMENTAL, HOUSEKEEPING,
         SECURITY, SAFETY, ACCESS, OTHER]
    OperationalIncidentStatus:
      type: string
      enum: [OPEN, IN_PROGRESS, RESOLVED]
      description: >-
        BE-21B operational handling progression — distinct from the BE-21A
        record lifecycle. `RESOLVED` means handling finished, NOT verified
        or closed.
    OperationalIncidentAction:
      type: string
      enum: [START_PROGRESS, RESOLVE, REOPEN, UPDATE_DETAILS]
    OperationalIncident:
      type: object
      description: >-
        BE-21B specialization of the BE-21A foundation
        (`incidentType = OPERATIONAL`). `id` is the shared Incident id.
      required:
        [id, operationalIncidentId, clientId, buildingId, incidentNumber,
         incidentType, title, description, severity, priority,
         incidentStatus, operationalStatus, operationalCategory, occurredAt,
         statusChangedAt, notes, locationType, locationId, floorId, areaId,
         roomId, spaceId, functionalLocationId, reportedByUserId, reportedAt,
         createdByUserId, createdAt, updatedAt, availableActions]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        operationalIncidentId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        incidentNumber: { type: string }
        incidentType: { type: string, enum: [OPERATIONAL] }
        title: { type: string }
        description: { type: string, nullable: true }
        severity: { $ref: "#/components/schemas/IncidentSeverity" }
        priority: { $ref: "#/components/schemas/IncidentPriority" }
        incidentStatus: { $ref: "#/components/schemas/IncidentStatus" }
        operationalStatus:
          $ref: "#/components/schemas/OperationalIncidentStatus"
        operationalCategory:
          $ref: "#/components/schemas/OperationalIncidentCategory"
        occurredAt: { type: string, format: date-time }
        statusChangedAt: { type: string, format: date-time }
        notes: { type: string, nullable: true }
        locationType:
          allOf: [{ $ref: "#/components/schemas/IncidentLocationType" }]
          nullable: true
        locationId: { type: string, format: uuid, nullable: true }
        floorId: { type: string, format: uuid, nullable: true }
        areaId: { type: string, format: uuid, nullable: true }
        roomId: { type: string, format: uuid, nullable: true }
        spaceId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        reportedByUserId: { $ref: "#/components/schemas/Uuid" }
        reportedAt: { type: string, format: date-time }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        availableActions:
          type: array
          description: >-
            Backend-resolved per request from state × permission ×
            foundation lifecycle. The client must not recompute them.
          items:
            $ref: "#/components/schemas/OperationalIncidentAction"
    CreateOperationalIncidentRequest:
      type: object
      description: >-
        `incidentType` is absent by design (always OPERATIONAL); `clientId`
        is derived from the Building.
      required: [buildingId, incidentNumber, title, operationalCategory, occurredAt]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        incidentNumber: { type: string }
        title: { type: string }
        description: { type: string, nullable: true }
        severity: { $ref: "#/components/schemas/IncidentSeverity" }
        priority: { $ref: "#/components/schemas/IncidentPriority" }
        locationType:
          allOf: [{ $ref: "#/components/schemas/IncidentLocationType" }]
          nullable: true
        locationId: { type: string, format: uuid, nullable: true }
        operationalCategory:
          $ref: "#/components/schemas/OperationalIncidentCategory"
        occurredAt: { type: string, format: date-time }
        notes: { type: string, nullable: true }
        reportedByUserId: { type: string, format: uuid }
    UpdateOperationalIncidentRequest:
      type: object
      description: >-
        Foundation metadata and operational fields are applied atomically.
        `operationalStatus` must follow the closed BE-21B transition table.
      properties:
        title: { type: string }
        description: { type: string, nullable: true }
        severity: { $ref: "#/components/schemas/IncidentSeverity" }
        priority: { $ref: "#/components/schemas/IncidentPriority" }
        operationalCategory:
          $ref: "#/components/schemas/OperationalIncidentCategory"
        occurredAt: { type: string, format: date-time }
        notes: { type: string, nullable: true }
        operationalStatus:
          $ref: "#/components/schemas/OperationalIncidentStatus"

    # ------------------------------------------------------------------
    # CR-BE-MOB-01 PART 03 — Engineering field / reading schemas
    # (BE-10A/B/C/D/E/F/G/H/J/K). Shapes mirror the implemented public
    # DTOs; no new field, status, or workflow is introduced. Every
    # execution id is the authoritative BE-07 checklist-execution or
    # form-instance id; every work id is the BE-08 Work Order id; every
    # finding id is the BE-09 Finding id.
    # ------------------------------------------------------------------
    EngineeringBindingStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
      description: Shared ACTIVE/INACTIVE status of a BE-10 binding row.
    EngineeringAssetRef:
      type: object
      required: [id, assetCode, assetName, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        assetCode: { type: string }
        assetName: { type: string }
        status: { type: string }
    EngineeringBuildingRef:
      type: object
      required: [id, code, name]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
    EngineeringFunctionalLocationRef:
      type: object
      nullable: true
      required: [id, code, name, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        status: { type: string }

    InspectionBinding:
      type: object
      description: >-
        BE-10B equipment-inspection binding: BE-05 Asset × BE-07 Checklist
        Template (+ optional BE-04 Functional Location). Not an inspection
        engine.
      required:
        [id, clientId, buildingId, assetId, checklistTemplateId,
         functionalLocationId, status, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateInspectionBindingRequest:
      type: object
      required: [checklistTemplateId]
      properties:
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
    UpdateInspectionBindingRequest:
      type: object
      properties:
        functionalLocationId: { type: string, format: uuid, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
    InspectionExecution:
      type: object
      description: >-
        The shared BE-07 checklist execution started from an inspection
        binding. `id` is the authoritative checklist-execution id used by the
        published BE-07 checklist / evidence / verification operations.
      required:
        [id, checklistTemplateId, inspectionBindingId, status, startedAt,
         completedAt, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        inspectionBindingId: { $ref: "#/components/schemas/Uuid" }
        status: { type: string }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    InspectionExecutionContext:
      type: object
      required: [execution, asset, building, functionalLocation]
      properties:
        execution: { $ref: "#/components/schemas/InspectionExecution" }
        asset: { $ref: "#/components/schemas/EngineeringAssetRef" }
        building: { $ref: "#/components/schemas/EngineeringBuildingRef" }
        functionalLocation:
          $ref: "#/components/schemas/EngineeringFunctionalLocationRef"

    MeterReadingBinding:
      type: object
      description: >-
        BE-10C technical meter-reading definition: BE-05 Asset × numeric BE-07
        Form Field × BE-07 UOM (+ optional Functional Location), with an
        optional binding-level tightening of the field's measurement range.
      required:
        [id, clientId, buildingId, assetId, formFieldId, uomId,
         functionalLocationId, minimumValue, maximumValue, status,
         createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        formFieldId: { $ref: "#/components/schemas/Uuid" }
        uomId: { $ref: "#/components/schemas/Uuid" }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        minimumValue: { type: number, nullable: true }
        maximumValue: { type: number, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateMeterReadingBindingRequest:
      type: object
      required: [formFieldId]
      properties:
        formFieldId: { $ref: "#/components/schemas/Uuid" }
        uomId: { type: string, format: uuid }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        minimumValue: { type: number }
        maximumValue: { type: number }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
    UpdateMeterReadingBindingRequest:
      type: object
      properties:
        uomId: { type: string, format: uuid }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        minimumValue: { type: number }
        maximumValue: { type: number }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
    MeterReadingExecution:
      type: object
      description: >-
        The shared BE-07 form instance started from a meter-reading binding.
        `id` is the authoritative form-instance id.
      required:
        [id, formTemplateVersionId, meterReadingBindingId, status, startedAt,
         completedAt, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        formTemplateVersionId: { $ref: "#/components/schemas/Uuid" }
        meterReadingBindingId: { $ref: "#/components/schemas/Uuid" }
        status: { type: string }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    SubmitMeterReadingRequest:
      type: object
      required: [value]
      properties:
        value:
          type: number
          description: >-
            Validated against the effective range (binding range tightened
            over the BE-07 field range).
        notes: { type: string, nullable: true }
    MeterReading:
      type: object
      description: >-
        A submitted technical reading, stored in BE-07's own `form_responses`
        row — not in a second measurement table.
      required:
        [executionId, formInstanceId, versionFieldId, value, uom,
         minimumValue, maximumValue, notes, submittedAt]
      properties:
        executionId: { $ref: "#/components/schemas/Uuid" }
        formInstanceId: { $ref: "#/components/schemas/Uuid" }
        versionFieldId: { $ref: "#/components/schemas/Uuid" }
        value: { type: number }
        uom:
          type: object
          required: [id, code, symbol]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            symbol: { type: string }
        minimumValue: { type: number, nullable: true }
        maximumValue: { type: number, nullable: true }
        notes: { type: string, nullable: true }
        submittedAt: { type: string, format: date-time }
    MeterReadingContext:
      type: object
      required:
        [execution, asset, building, functionalLocation, uom, minimumValue,
         maximumValue, currentValue]
      properties:
        execution: { $ref: "#/components/schemas/MeterReadingExecution" }
        asset: { $ref: "#/components/schemas/EngineeringAssetRef" }
        building: { $ref: "#/components/schemas/EngineeringBuildingRef" }
        functionalLocation:
          $ref: "#/components/schemas/EngineeringFunctionalLocationRef"
        uom:
          type: object
          required: [id, code, name, symbol]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            symbol: { type: string }
        minimumValue: { type: number, nullable: true }
        maximumValue: { type: number, nullable: true }
        currentValue: { type: number, nullable: true }

    LogSheetBinding:
      type: object
      description: >-
        BE-10D equipment log-sheet definition: BE-05 Asset × BE-07 Form
        Template (+ optional pinned Template Version, optional Functional
        Location).
      required:
        [id, clientId, buildingId, assetId, formTemplateId,
         formTemplateVersionId, functionalLocationId, status, createdByUserId,
         createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        formTemplateId: { $ref: "#/components/schemas/Uuid" }
        formTemplateVersionId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateLogSheetBindingRequest:
      type: object
      required: [formTemplateId]
      properties:
        formTemplateId: { $ref: "#/components/schemas/Uuid" }
        formTemplateVersionId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
    UpdateLogSheetBindingRequest:
      type: object
      properties:
        formTemplateVersionId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
    LogSheetExecution:
      type: object
      description: >-
        One log-sheet row = one shared BE-07 form instance. `id` is the
        authoritative form-instance id.
      required:
        [id, formTemplateVersionId, logSheetBindingId, status, startedAt,
         completedAt, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        formTemplateVersionId: { $ref: "#/components/schemas/Uuid" }
        logSheetBindingId: { $ref: "#/components/schemas/Uuid" }
        status: { type: string }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    LogSheetExecutionContext:
      type: object
      required:
        [execution, asset, building, functionalLocation, template, version]
      properties:
        execution: { $ref: "#/components/schemas/LogSheetExecution" }
        asset: { $ref: "#/components/schemas/EngineeringAssetRef" }
        building: { $ref: "#/components/schemas/EngineeringBuildingRef" }
        functionalLocation:
          $ref: "#/components/schemas/EngineeringFunctionalLocationRef"
        template:
          type: object
          required: [id, code, name]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
        version:
          type: object
          required: [id, versionNumber, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            versionNumber: { type: integer }
            status: { type: string }

    EngineeringChecklistBinding:
      type: object
      description: >-
        BE-10E binding: BE-07 Checklist Template × Building (+ optional Asset
        and/or Functional Location target inside that Building).
      required:
        [id, clientId, buildingId, checklistTemplateId, assetId,
         functionalLocationId, status, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        assetId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateEngineeringChecklistBindingRequest:
      type: object
      required: [buildingId, checklistTemplateId]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        assetId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
    UpdateEngineeringChecklistBindingRequest:
      type: object
      properties:
        assetId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
    EngineeringChecklistExecution:
      type: object
      description: >-
        The shared BE-07 checklist execution started from an Engineering
        checklist binding.
      required:
        [id, checklistTemplateId, engineeringChecklistBindingId, status,
         startedAt, completedAt, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        engineeringChecklistBindingId: { $ref: "#/components/schemas/Uuid" }
        status: { type: string }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    EngineeringChecklistExecutionContext:
      type: object
      required: [execution, building, template, asset, functionalLocation]
      properties:
        execution:
          $ref: "#/components/schemas/EngineeringChecklistExecution"
        building: { $ref: "#/components/schemas/EngineeringBuildingRef" }
        template:
          type: object
          required: [id, code, name, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string }
        asset:
          allOf: [{ $ref: "#/components/schemas/EngineeringAssetRef" }]
          nullable: true
        functionalLocation:
          $ref: "#/components/schemas/EngineeringFunctionalLocationRef"

    BreakdownStatus:
      type: string
      enum: [OPEN, CLOSED]
      description: >-
        The BE-10F breakdown record lifecycle. It is deliberately minimal —
        corrective progress lives on the linked BE-08 Work Order.
    BreakdownBinding:
      type: object
      description: >-
        BE-10F breakdown / corrective event on a BE-05 Asset. `corrective`
        projects the current BE-08 status of the linked Work Order and is
        never stored.
      required:
        [id, clientId, buildingId, assetId, functionalLocationId, category,
         description, reportedByUserId, reportedAt, status, createdAt,
         updatedAt, corrective]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        category: { type: string }
        description: { type: string }
        reportedByUserId: { $ref: "#/components/schemas/Uuid" }
        reportedAt: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/BreakdownStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        corrective:
          type: object
          nullable: true
          description: Live projection of the linked BE-08 Work Order.
          required: [workOrderId, workOrderNumber, title, status]
          properties:
            workOrderId: { $ref: "#/components/schemas/Uuid" }
            workOrderNumber: { type: string }
            title: { type: string }
            status: { type: string }
    CreateBreakdownRequest:
      type: object
      required: [category, description]
      properties:
        category: { type: string }
        description: { type: string }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        reportedAt: { type: string, format: date-time }
    CloseBreakdownRequest:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [CLOSED]
          description: CLOSED is the only accepted breakdown transition.
    LinkCorrectiveWorkOrderRequest:
      type: object
      description: >-
        Supply `workOrderId` to link an existing BE-08 Work Order, or `title`
        to have the shared BE-08 service create the corrective Work Order.
      properties:
        workOrderId: { type: string, format: uuid }
        title: { type: string }

    MaintenanceType:
      type: string
      enum: [PREVENTIVE, PREDICTIVE, CONDITION_BASED, CALIBRATION]
      description: >-
        BE-10G planned-maintenance classification. Corrective work is NOT a
        member: it is a BE-10F breakdown plus a BE-08 corrective Work Order.
    MaintenanceBinding:
      type: object
      description: >-
        BE-10G planned-maintenance context for a BE-05 Asset. `schedule`,
        `workOrder` and `tasks` are live projections of the authoritative
        BE-07 / BE-08 records, never stored copies.
      required:
        [id, clientId, buildingId, assetId, functionalLocationId, name,
         maintenanceType, description, status, createdByUserId, createdAt,
         updatedAt, schedule, workOrder, tasks]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        name: { type: string }
        maintenanceType: { $ref: "#/components/schemas/MaintenanceType" }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        schedule:
          type: object
          nullable: true
          description: Linked BE-07 Schedule Definition.
          required: [id, code, name, targetType, targetId, timezone, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            targetType: { type: string }
            targetId: { $ref: "#/components/schemas/Uuid" }
            timezone: { type: string }
            status: { type: string }
        workOrder:
          type: object
          nullable: true
          description: Linked BE-08 Work Order (authoritative status).
          required: [id, workOrderNumber, title, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            workOrderNumber: { type: string }
            title: { type: string }
            status: { type: string }
        tasks:
          type: array
          description: Linked BE-07 generated tasks, newest first.
          items:
            type: object
            required: [id, occurrenceAt, status]
            properties:
              id: { $ref: "#/components/schemas/Uuid" }
              occurrenceAt: { type: string, format: date-time }
              status: { type: string }
    CreateMaintenanceBindingRequest:
      type: object
      required: [name, maintenanceType]
      properties:
        name: { type: string }
        maintenanceType: { $ref: "#/components/schemas/MaintenanceType" }
        description: { type: string }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
    UpdateMaintenanceBindingRequest:
      type: object
      properties:
        name: { type: string }
        maintenanceType: { $ref: "#/components/schemas/MaintenanceType" }
        description: { type: string, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
    LinkMaintenanceScheduleRequest:
      type: object
      description: >-
        Supply `scheduleDefinitionId` to link an existing BE-07 schedule, or
        the creation fields so the shared BE-07 scheduler creates one.
      properties:
        scheduleDefinitionId: { type: string, format: uuid }
        targetType: { type: string }
        targetId: { type: string, format: uuid }
        code: { type: string }
        name: { type: string }
        startAt: { type: string, format: date-time }
        timezone: { type: string }
    LinkMaintenanceTaskRequest:
      type: object
      required: [taskId]
      properties:
        taskId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >-
            The authoritative BE-07 generated task id. Execution stays on the
            published task operations.
    LinkMaintenanceWorkOrderRequest:
      type: object
      description: >-
        Supply `workOrderId` to link an existing BE-08 Work Order, or `title`
        to have the shared BE-08 service create one.
      properties:
        workOrderId: { type: string, format: uuid }
        title: { type: string }

    EngineeringFindingOperationType:
      type: string
      enum:
        [EQUIPMENT_INSPECTION, METER_READING, EQUIPMENT_LOG_SHEET,
         ENGINEERING_CHECKLIST, BREAKDOWN, MAINTENANCE]
      description: >-
        Which Engineering operation produced the finding. Distinct from BE-09's
        own `FindingSourceType`, which records the underlying execution kind.
    EngineeringFinding:
      type: object
      description: >-
        BE-10H Engineering context link for an authoritative BE-09 Finding.
        `id` is the link id; every workflow operation uses `findingId`.
      required:
        [id, clientId, buildingId, findingId, assetId, functionalLocationId,
         operationType, sourceType, sourceId, createdAt, finding,
         availableActions]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        findingId: { $ref: "#/components/schemas/Uuid" }
        assetId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        operationType:
          $ref: "#/components/schemas/EngineeringFindingOperationType"
        sourceType:
          allOf: [{ $ref: "#/components/schemas/FindingSourceType" }]
          nullable: true
        sourceId: { type: string, format: uuid, nullable: true }
        createdAt: { type: string, format: date-time }
        finding: { $ref: "#/components/schemas/Finding" }
        availableActions:
          type: array
          items: { type: string }
          description: >-
            BE-09 backend-authoritative actions for the linked finding —
            never re-derived by Engineering or by the client.
    CreateEngineeringFindingRequest:
      type: object
      description: >-
        Supply `findingId` to link an existing BE-09 Finding, or the create
        fields (`title` …) to have the shared BE-09 service create one.
        `clientId` and the reporter are derived by the backend.
      required: [buildingId, operationType]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        operationType:
          $ref: "#/components/schemas/EngineeringFindingOperationType"
        findingId: { type: string, format: uuid }
        title: { type: string }
        description: { type: string }
        assetId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        sourceType: { $ref: "#/components/schemas/FindingSourceType" }
        sourceId: { type: string, format: uuid }
        classificationId: { type: string, format: uuid }
        severityId: { type: string, format: uuid }
        assigneeType:
          type: string
          enum: [WORKFORCE, TEAM, VENDOR, VENDOR_WORKFORCE]
        workforceProfileId: { type: string, format: uuid }
        teamId: { type: string, format: uuid }
        vendorId: { type: string, format: uuid }

    EngineeringShiftContext:
      type: object
      nullable: true
      description: BE-03 Shift context with the workforce bound to that Shift.
      required:
        [id, code, name, startTime, endTime, status, workforceCount, workforce]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        startTime: { type: string }
        endTime: { type: string }
        status: { type: string }
        workforceCount: { type: integer }
        workforce:
          type: array
          items:
            type: object
            required: [workforceProfileId, employeeCode, fullName]
            properties:
              workforceProfileId: { $ref: "#/components/schemas/Uuid" }
              employeeCode: { type: string }
              fullName: { type: string }
    EngineeringDailyOperationKind:
      type: string
      enum: [TASK, WORK_ORDER, FINDING]
    EngineeringDailyOperationsSummary:
      type: object
      required:
        [scheduled, inProgress, completed, openWorkOrders, openFindings]
      properties:
        scheduled: { type: integer }
        inProgress: { type: integer }
        completed: { type: integer }
        openWorkOrders: { type: integer }
        openFindings: { type: integer }
    EngineeringDailyOperation:
      type: object
      description: >-
        One consolidated daily operation referencing its authoritative source
        record. `id` is the BE-07 task id, BE-08 Work Order id or BE-09 Finding
        id depending on `kind`; `status` is the source domain's own status,
        untranslated.
      required:
        [kind, id, referenceNumber, title, status, occurredAt, completedAt,
         assignee]
      properties:
        kind: { $ref: "#/components/schemas/EngineeringDailyOperationKind" }
        id: { $ref: "#/components/schemas/Uuid" }
        referenceNumber: { type: string, nullable: true }
        title: { type: string }
        status: { type: string }
        occurredAt: { type: string, format: date-time }
        completedAt: { type: string, format: date-time, nullable: true }
        assignee:
          type: object
          nullable: true
          required: [type, workforceProfileId, teamId, vendorId]
          properties:
            type: { type: string }
            workforceProfileId: { type: string, format: uuid, nullable: true }
            teamId: { type: string, format: uuid, nullable: true }
            vendorId: { type: string, format: uuid, nullable: true }
        availableActions:
          type: array
          items: { type: string }
          description: >-
            BE-09 backend-authoritative actions. Present for FINDING rows
            only — Tasks and Work Orders expose no authoritative
            available-actions service in their foundations.
    EngineeringDailyOperations:
      type: object
      description: >-
        BE-10A read model for one Building + operational date (+ optional
        Shift). Composition only — it stores nothing.
      required: [buildingId, operationalDate, shift, summary, operations]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        operationalDate: { type: string, format: date }
        shift: { $ref: "#/components/schemas/EngineeringShiftContext" }
        summary:
          $ref: "#/components/schemas/EngineeringDailyOperationsSummary"
        operations:
          type: array
          items: { $ref: "#/components/schemas/EngineeringDailyOperation" }

    EngineeringOverview:
      type: object
      description: >-
        BE-10K supervisor read model derived from BE-10A operations, BE-09
        finding counts, BE-08 Work Orders and the BE-10J handover dataset.
      required:
        [buildingId, date, shift, summary, operations, findings, workOrders,
         handover]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        date: { type: string, format: date }
        shift: { $ref: "#/components/schemas/EngineeringShiftContext" }
        summary:
          type: object
          required:
            [scheduledOperations, inProgressOperations, completedOperations,
             activeWorkOrders, openFindings, pendingHandoverItems]
          properties:
            scheduledOperations: { type: integer }
            inProgressOperations: { type: integer }
            completedOperations: { type: integer }
            activeWorkOrders: { type: integer }
            openFindings: { type: integer }
            pendingHandoverItems: { type: integer }
        operations:
          $ref: "#/components/schemas/EngineeringDailyOperationsSummary"
        findings:
          type: object
          required: [open, rework, verified, closed]
          properties:
            open: { type: integer }
            rework: { type: integer }
            verified: { type: integer }
            closed: { type: integer }
        workOrders:
          type: object
          required: [active, completedInWindow]
          properties:
            active:
              type: array
              items:
                type: object
                required: [id, workOrderNumber, title, status]
                properties:
                  id: { $ref: "#/components/schemas/Uuid" }
                  workOrderNumber: { type: string }
                  title: { type: string }
                  status: { type: string }
            completedInWindow: { type: integer }
        handover:
          type: object
          required: [drafts, ready, acknowledged, pendingItems]
          properties:
            drafts: { type: integer }
            ready: { type: integer }
            acknowledged: { type: integer }
            pendingItems:
              type: array
              items: { $ref: "#/components/schemas/EngineeringHandoverItem" }

    EngineeringShiftHandoverStatus:
      type: string
      enum: [DRAFT, READY, ACKNOWLEDGED]
    EngineeringHandoverItem:
      type: object
      description: >-
        One unresolved operational item, referenced from its authoritative
        record. `status` is the source record's own status, never translated.
      required: [kind, id, referenceNumber, title, status, assetCode]
      properties:
        kind:
          type: string
          enum:
            [WORK_ORDER, BREAKDOWN, FINDING, INSPECTION, CHECKLIST,
             METER_READING, EQUIPMENT_LOG, MAINTENANCE, TASK]
        id: { $ref: "#/components/schemas/Uuid" }
        referenceNumber: { type: string, nullable: true }
        title: { type: string }
        status: { type: string }
        assetCode: { type: string, nullable: true }
    EngineeringHandoverDataset:
      type: object
      description: >-
        The live Engineering operational dataset for the handover's Building,
        resolved at read time from the authoritative records.
      required:
        [activeWorkOrders, openBreakdowns, openFindings, incompleteInspections,
         incompleteChecklists, incompleteMeterReadings, incompleteLogSheets,
         pendingMaintenance, scheduledTasks]
      properties:
        activeWorkOrders:
          type: array
          items: { $ref: "#/components/schemas/EngineeringHandoverItem" }
        openBreakdowns:
          type: array
          items: { $ref: "#/components/schemas/EngineeringHandoverItem" }
        openFindings:
          type: array
          items: { $ref: "#/components/schemas/EngineeringHandoverItem" }
        incompleteInspections:
          type: array
          items: { $ref: "#/components/schemas/EngineeringHandoverItem" }
        incompleteChecklists:
          type: array
          items: { $ref: "#/components/schemas/EngineeringHandoverItem" }
        incompleteMeterReadings:
          type: array
          items: { $ref: "#/components/schemas/EngineeringHandoverItem" }
        incompleteLogSheets:
          type: array
          items: { $ref: "#/components/schemas/EngineeringHandoverItem" }
        pendingMaintenance:
          type: array
          items: { $ref: "#/components/schemas/EngineeringHandoverItem" }
        scheduledTasks:
          type: array
          items: { $ref: "#/components/schemas/EngineeringHandoverItem" }
    EngineeringShiftHandover:
      type: object
      description: >-
        BE-10J shift handover. Only the summary and lifecycle fields are
        stored; `dataset` is resolved live from the authoritative Engineering
        records at read time.
      required:
        [id, clientId, buildingId, outgoingShift, incomingShift, handoverDate,
         preparedByUserId, acknowledgedByUserId, summary, status, preparedAt,
         acknowledgedAt, createdAt, updatedAt, dataset]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        outgoingShift:
          type: object
          required: [id, code, name, startTime, endTime]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            startTime: { type: string }
            endTime: { type: string }
        incomingShift:
          type: object
          required: [id, code, name, startTime, endTime]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            startTime: { type: string }
            endTime: { type: string }
        handoverDate: { type: string, format: date }
        preparedByUserId: { $ref: "#/components/schemas/Uuid" }
        acknowledgedByUserId: { type: string, format: uuid, nullable: true }
        summary: { type: string, nullable: true }
        status:
          $ref: "#/components/schemas/EngineeringShiftHandoverStatus"
        preparedAt: { type: string, format: date-time }
        acknowledgedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        dataset:
          $ref: "#/components/schemas/EngineeringHandoverDataset"
    CreateEngineeringShiftHandoverRequest:
      type: object
      required: [outgoingShiftId, incomingShiftId, handoverDate]
      properties:
        outgoingShiftId: { $ref: "#/components/schemas/Uuid" }
        incomingShiftId: { $ref: "#/components/schemas/Uuid" }
        handoverDate: { type: string, format: date }
        summary: { type: string }
    UpdateEngineeringShiftHandoverRequest:
      type: object
      properties:
        summary: { type: string }

    # ------------------------------------------------------------------
    # CR-BE-MOB-02 PART 07B — Security Shift Handover contract schemas
    # (BE-12G binding + BE-12M reporting dataset). Shapes mirror the
    # implemented public DTOs; the BE-10J handover lifecycle stays the only
    # authority on handover status.
    # ------------------------------------------------------------------
    SecurityShiftHandoverBindingStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
      description: >-
        The binding's own status. INACTIVE never blocks a re-bind and never
        affects the underlying BE-10J handover lifecycle.
    SecurityShiftHandoverBinding:
      type: object
      description: >-
        BE-12G binding attaching an existing BE-10J shift handover to a
        Security operational context (start Security Post + optional Patrol
        Route) under the same Building. `shiftHandoverId` references the
        authoritative BE-10J handover; `status` here is the binding's own
        ACTIVE/INACTIVE flag, never a duplicate of the handover lifecycle.
      required:
        [id, clientId, buildingId, shiftHandoverId, startSecurityPostId,
         patrolRouteId, status, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        shiftHandoverId: { $ref: "#/components/schemas/Uuid" }
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        patrolRouteId: { type: string, format: uuid, nullable: true }
        status:
          $ref: "#/components/schemas/SecurityShiftHandoverBindingStatus"
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateSecurityShiftHandoverBindingRequest:
      type: object
      required: [shiftHandoverId]
      properties:
        shiftHandoverId: { $ref: "#/components/schemas/Uuid" }
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        patrolRouteId: { type: string, format: uuid, nullable: true }
        status:
          $ref: "#/components/schemas/SecurityShiftHandoverBindingStatus"
    UpdateSecurityShiftHandoverBindingRequest:
      type: object
      description: >-
        Partial update. `shiftHandoverId`, `clientId`, `buildingId` and
        `createdByUserId` are immutable; at least one updatable field is
        required.
      properties:
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        patrolRouteId: { type: string, format: uuid, nullable: true }
        status:
          $ref: "#/components/schemas/SecurityShiftHandoverBindingStatus"
    SecurityShiftHandoverDatasetRow:
      type: object
      description: >-
        One BE-12M reporting row joining the BE-12G binding to the
        authoritative BE-10J handover. `bindingStatus` is the binding's own
        status; `handoverStatus` is the authoritative BE-10J lifecycle status.
      required:
        [bindingId, shiftHandoverId, startSecurityPostId, startSecurityPostCode,
         patrolRouteId, patrolRouteCode, bindingStatus, handoverStatus,
         handoverCreatedAt]
      properties:
        bindingId: { $ref: "#/components/schemas/Uuid" }
        shiftHandoverId: { $ref: "#/components/schemas/Uuid" }
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        startSecurityPostCode: { type: string, nullable: true }
        patrolRouteId: { type: string, format: uuid, nullable: true }
        patrolRouteCode: { type: string, nullable: true }
        bindingStatus: { type: string }
        handoverStatus: { type: string }
        handoverCreatedAt: { type: string, format: date-time }

    # ------------------------------------------------------------------
    # CR-BE-MOB-02 PART 07C — Mobile Current Shift effective-context schemas
    # (BE-25M self-service). Shapes mirror the implemented DTOs; ids are the
    # authoritative BE-03E roster / shift ids, never invented.
    # ------------------------------------------------------------------
    MobileCurrentShift:
      type: object
      description: >-
        One Shift currently in effect for the authenticated user, resolved
        from the BE-03E Shift roster + BE-03C Workforce Profile + BE-02F/G
        Building access. `shiftId` is the authoritative `shifts.id`;
        `assignmentId` is the authoritative `workforce_shift_assignments.id`.
      required:
        [assignmentId, shiftId, workforceProfileId, employeeCode, clientId,
         buildingId, buildingCode, buildingName, code, name, startTime, endTime,
         status, effectiveFrom, effectiveUntil]
      properties:
        assignmentId: { $ref: "#/components/schemas/Uuid" }
        shiftId: { $ref: "#/components/schemas/Uuid" }
        workforceProfileId: { $ref: "#/components/schemas/Uuid" }
        employeeCode: { type: string }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        buildingCode: { type: string }
        buildingName: { type: string }
        code: { type: string }
        name: { type: string }
        startTime:
          type: string
          description: Wall-clock HH:MM:SS in the Building timezone.
        endTime: { type: string }
        status: { type: string }
        effectiveFrom: { type: string, format: date-time, nullable: true }
        effectiveUntil: { type: string, format: date-time, nullable: true }
    MobileCurrentShiftContext:
      type: object
      description: >-
        The effective current-shift context of the authenticated user. An
        empty `shifts` array means the caller is not on shift at `asOf`.
      required: [asOf, shifts]
      properties:
        asOf: { type: string, format: date-time }
        shifts:
          type: array
          items: { $ref: "#/components/schemas/MobileCurrentShift" }

    # ------------------------------------------------------------------
    # CR-BE-MOB-05 PART 01 — Mobile Upcoming Shifts self-service schemas
    # (BE-25M family). The item shape deliberately reuses the authoritative
    # MobileCurrentShift roster-row shape; ids are the authoritative BE-03E
    # roster / shift ids, never invented.
    # ------------------------------------------------------------------
    MobileUpcomingShift:
      allOf:
        - $ref: "#/components/schemas/MobileCurrentShift"
      description: >-
        One current or future shift assignment of the authenticated user.
        Shape is identical to `MobileCurrentShift` (a BE-03E roster row):
        `shiftId` is the authoritative `shifts.id`; `assignmentId` is the
        authoritative `workforce_shift_assignments.id`. Only ACTIVE roster
        assignments of ACTIVE Shifts in accessible Buildings carrying a
        valid IANA timezone are returned; no attendance/clock/schedule state
        is invented.
    MobileUpcomingShiftsContext:
      type: object
      description: >-
        The effective upcoming-shifts schedule of the authenticated user.
        `dateFrom` is the effective inclusive lower bound applied to the
        roster window (defaults to `asOf` when omitted); `dateTo` is the
        effective exclusive upper bound (null when unbounded; a date-only
        `dateTo` is inclusive of that whole UTC day). An empty `shifts`
        array means no matching roster in the queried window.
      required: [asOf, dateFrom, dateTo, shifts]
      properties:
        asOf: { type: string, format: date-time }
        dateFrom: { type: string, format: date-time }
        dateTo: { type: string, format: date-time, nullable: true }
        shifts:
          type: array
          items: { $ref: "#/components/schemas/MobileUpcomingShift" }

    # ------------------------------------------------------------------
    # CR-BE-MOB-05 PART 02 — Workforce Attendance schemas. Shapes mirror
    # the implemented DTOs; ids are authoritative (`attendance_records.id`,
    # `workforce_profiles.id`, `buildings.id`,
    # `workforce_shift_assignments.id`, `shifts.id`), never invented.
    # ------------------------------------------------------------------
    AttendanceStatus:
      type: string
      enum: [CLOCKED_IN, CLOCKED_OUT]
      description: >-
        Lifecycle of an attendance record. CLOCKED_IN is open; CLOCKED_OUT
        is terminal (preserved, never reopened or deleted).
    ClockInRequest:
      type: object
      description: >-
        Clock-in payload: exactly the Building the authenticated profile is
        clocking into. Identity and timestamps are never accepted from the
        client.
      required: [buildingId]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
    AttendanceRecord:
      type: object
      description: >-
        The authoritative workforce attendance record (CR-BE-MOB-05 PART 02).
        `id` is `attendance_records.id`; `workforceProfileId` is
        `workforce_profiles.id` (resolved from the authenticated session);
        `buildingId` is `buildings.id` (BE-02F/G access-asserted at
        clock-in); `workforceShiftAssignmentId` / `shiftId` are set together
        when an applicable ACTIVE roster row was bound (both null otherwise).
        `clockInAt` / `clockOutAt` are backend-set database timestamps —
        never client-supplied.
      required:
        [id, clientId, buildingId, buildingCode, buildingName,
         workforceProfileId, employeeCode, workforceShiftAssignmentId,
         shiftId, shiftCode, shiftName, clockInAt, clockOutAt, status,
         createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        buildingCode: { type: string }
        buildingName: { type: string }
        workforceProfileId: { $ref: "#/components/schemas/Uuid" }
        employeeCode: { type: string }
        workforceShiftAssignmentId:
          type: string
          format: uuid
          nullable: true
        shiftId: { type: string, format: uuid, nullable: true }
        shiftCode: { type: string, nullable: true }
        shiftName: { type: string, nullable: true }
        clockInAt: { type: string, format: date-time }
        clockOutAt: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/AttendanceStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    # ------------------------------------------------------------------
    # CR-BE-MOB-05 PART 03 — Security Operational Logbook schemas. Shapes
    # mirror the implemented DTOs; ids are authoritative
    # (`security_logbook_entries.id`, `workforce_profiles.id`,
    # `buildings.id`, `shift_handovers.id`), never invented.
    # ------------------------------------------------------------------
    SecurityLogbookStatus:
      type: string
      enum: [OPEN, CLOSED]
      description: >-
        Lifecycle of a logbook entry. OPEN entries accept updates; CLOSED is
        terminal (preserved, never reopened or deleted).
    SecurityLogbookCategory:
      type: string
      enum: [GENERAL, INCIDENT, PATROL, HANDOVER, FINDING]
      description: >-
        Fixed classification vocabulary only — the backend branches no
        behavior on a category; no patrol / incident / finding engine is
        implied.
    CreateSecurityLogbookEntryRequest:
      type: object
      description: >-
        Create payload: Building, category, summary, optional detail and an
        optional existing authoritative `shift_handovers.id` in the SAME
        Building. Identity and timestamps are never accepted from the
        client.
      required: [buildingId, category, summary]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        category: { $ref: "#/components/schemas/SecurityLogbookCategory" }
        summary: { type: string, maxLength: 500 }
        detail: { type: string, maxLength: 4000, nullable: true }
        shiftHandoverId: { type: string, format: uuid, nullable: true }
    UpdateSecurityLogbookEntryRequest:
      type: object
      description: >-
        Partial update payload (OPEN entries only). All fields optional;
        `shiftHandoverId: null` clears the binding. Timestamps are never
        accepted from the client.
      properties:
        category: { $ref: "#/components/schemas/SecurityLogbookCategory" }
        summary: { type: string, maxLength: 500 }
        detail: { type: string, maxLength: 4000, nullable: true }
        status: { $ref: "#/components/schemas/SecurityLogbookStatus" }
        shiftHandoverId: { type: string, format: uuid, nullable: true }
    SecurityLogbookEntry:
      type: object
      description: >-
        The authoritative Security logbook entry (CR-BE-MOB-05 PART 03).
        `id` is `security_logbook_entries.id`; `workforceProfileId` is
        `workforce_profiles.id` (resolved from the authenticated session);
        `buildingId` is `buildings.id` (BE-02F/G access-asserted);
        `shiftHandoverId` is `shift_handovers.id` when a valid same-Building
        handover was bound (null otherwise). `recordedAt` is a backend-set
        database timestamp — never client-supplied.
      required:
        [id, clientId, buildingId, buildingCode, buildingName,
         workforceProfileId, employeeCode, shiftHandoverId, category,
         summary, detail, status, recordedAt, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        buildingCode: { type: string }
        buildingName: { type: string }
        workforceProfileId: { $ref: "#/components/schemas/Uuid" }
        employeeCode: { type: string }
        shiftHandoverId: { type: string, format: uuid, nullable: true }
        category: { $ref: "#/components/schemas/SecurityLogbookCategory" }
        summary: { type: string }
        detail: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/SecurityLogbookStatus" }
        recordedAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    # ------------------------------------------------------------------
    # CR-BE-MOB-02 PART 07D — Mobile My Team effective-context schemas
    # (BE-25N self-service). Shapes mirror the implemented DTOs; ids are the
    # authoritative BE-03B/BE-03C hierarchy ids, never invented.
    # ------------------------------------------------------------------
    MobileTeamRef:
      type: object
      description: Minimal authoritative reference to a hierarchy node.
      required: [id, code, name]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
    MobileMyTeam:
      type: object
      description: >-
        The caller's team with its authoritative hierarchy resolved
        (Team → Department → Organization → Client). `id` is `teams.id`.
      required:
        [id, code, name, description, status, department, organization, client]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { type: string }
        department: { $ref: "#/components/schemas/MobileTeamRef" }
        organization: { $ref: "#/components/schemas/MobileTeamRef" }
        client: { $ref: "#/components/schemas/MobileTeamRef" }
    MobileTeamMember:
      type: object
      description: >-
        One ACTIVE workforce profile in the caller's team. `id` is
        `workforce_profiles.id`; `positionId` is `positions.id`; `userId` is
        `users.id` when a User account is linked.
      required:
        [id, employeeCode, fullName, workforceType, status, positionId, userId]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        employeeCode: { type: string }
        fullName: { type: string }
        workforceType: { type: string }
        status: { type: string }
        positionId: { $ref: "#/components/schemas/Uuid" }
        userId: { type: string, format: uuid, nullable: true }
    MobileMyTeamContext:
      type: object
      description: >-
        The effective my-team context of the authenticated user. `team` is
        null when the caller has no linked ACTIVE profile, no team, or a
        profile whose Client is outside the accessible scope; `members` lists
        ACTIVE profiles in that team (the caller included).
      required: [workforceProfile, team, members]
      properties:
        workforceProfile:
          type: object
          nullable: true
          required: [id, employeeCode, fullName]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            employeeCode: { type: string }
            fullName: { type: string }
        team:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/MobileMyTeam"
        members:
          type: array
          items: { $ref: "#/components/schemas/MobileTeamMember" }

    # ------------------------------------------------------------------
    # CR-BE-MOB-01 PART 04 — Work Order material context reference schemas
    # (BE-16A Item master, BE-16B Warehouse master, BE-16H Asset ↔ spare
    # part binding). Shapes mirror the implemented public DTOs. The usage,
    # stock, approved-quantity and verification schemas already exist and
    # are NOT redefined here.
    # ------------------------------------------------------------------
    InventoryItemStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
      description: INACTIVE items are preserved for history, never deleted.
    InventoryItemType:
      type: string
      enum: [SPARE_PART, MATERIAL, CONSUMABLE]
      description: >-
        Classification data only — the backend branches no behaviour on it.
    InventoryItem:
      type: object
      description: >-
        BE-16A Inventory Item master, owned by exactly one Client (`code` is
        unique per Client). `uomId` references the shared BE-07 UOM foundation
        and is the UOM snapshotted onto a Work Order material usage. Carries no
        stock, price or procurement state.
      required:
        [id, clientId, code, name, itemType, category, uomId, description,
         status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        itemType: { $ref: "#/components/schemas/InventoryItemType" }
        category: { type: string, nullable: true }
        uomId: { type: string, format: uuid, nullable: true }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/InventoryItemStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        uom:
          type: object
          nullable: true
          description: Resolved BE-07 UOM context when available.
          required: [id, code, name, symbol]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            symbol: { type: string }

    InventoryWarehouseStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    InventoryWarehouse:
      type: object
      description: >-
        BE-16B Warehouse / store, operated within exactly one Building
        (`code` unique per Building). `clientId` is derived through
        Building → Property → Client and is never accepted from the caller.
        Stock lives on the published stock-balance / stock-movement surface.
      required:
        [id, clientId, buildingId, functionalLocationId, code, name,
         description, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/InventoryWarehouseStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        functionalLocation:
          type: object
          nullable: true
          description: Resolved BE-04 Functional Location when bound.
          required: [id, code, name, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string }

    AssetSparePartStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    AssetSparePart:
      type: object
      description: >-
        BE-16H many-to-many binding between a BE-05 Asset and a SPARE_PART
        Inventory Item. `requiredQuantity` is a planning reference per asset —
        it is NOT an approved quantity (that is `MaterialRequest.approvedQuantity`)
        and NOT an issued quantity (that is `WorkOrderMaterialUsage.quantity`).
      required:
        [id, clientId, buildingId, assetId, itemId, requiredQuantity, status,
         notes, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        itemId: { $ref: "#/components/schemas/Uuid" }
        requiredQuantity: { type: number }
        status: { $ref: "#/components/schemas/AssetSparePartStatus" }
        notes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        asset:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            assetCode: { type: string }
            assetName: { type: string }
            buildingId: { $ref: "#/components/schemas/Uuid" }
            status: { type: string }
        item:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            itemType: { type: string }
            uomId: { type: string, format: uuid, nullable: true }

    # ------------------------------------------------------------------
    # CR-BE-MOB-03 PART 01 — Workforce team workload & status schemas
    # (BE-23G KPI, BE-03I2 reporting, BE-03F reporting lines).
    # Mirrors the implemented DTOs; read-only projections over BE-03/BE-07.
    # ------------------------------------------------------------------
    WorkforceKpiHeadcount:
      type: object
      description: BE-03C headcount, scoped to the accessible Building set.
      required: [total, active, inactive, internal, outsourced, contract, withAssignments]
      properties:
        total: { type: integer, minimum: 0 }
        active: { type: integer, minimum: 0 }
        inactive: { type: integer, minimum: 0 }
        internal: { type: integer, minimum: 0 }
        outsourced: { type: integer, minimum: 0 }
        contract: { type: integer, minimum: 0 }
        withAssignments: { type: integer, minimum: 0, description: Distinct workforce profiles holding an assignment in range. }
    WorkforceKpiAssignments:
      type: object
      description: BE-07 assignment lifecycle counters in range (excluding CANCELLED occurrences from scheduled).
      required: [scheduled, completed, inProgress, open, cancelled, overdue, completionRate, byAssigneeType]
      properties:
        scheduled: { type: integer, minimum: 0 }
        completed: { type: integer, minimum: 0 }
        inProgress: { type: integer, minimum: 0 }
        open: { type: integer, minimum: 0 }
        cancelled: { type: integer, minimum: 0 }
        overdue: { type: integer, minimum: 0, description: Past due and not completed (grace-aware). }
        completionRate: { type: number, description: completed / scheduled as a percentage, 2dp; 0 when scheduled is 0. }
        byAssigneeType:
          type: object
          required: [workforce, team]
          properties:
            workforce: { type: integer, minimum: 0 }
            team: { type: integer, minimum: 0 }
    WorkforceKpiManHours:
      type: object
      description: Derived man-hours over completed assignments (completed_at - started_at); no stored timesheet field exists.
      required: [totalHours, measuredAssignments, unmeasuredAssignments, averageHoursPerAssignment, averageHoursPerWorkforce]
      properties:
        totalHours: { type: number }
        measuredAssignments: { type: integer, minimum: 0 }
        unmeasuredAssignments: { type: integer, minimum: 0, description: Completed with no recorded start — contribute zero hours. }
        averageHoursPerAssignment: { type: number }
        averageHoursPerWorkforce: { type: number }
    WorkforceKpiMember:
      type: object
      description: Per-workforce man-hour and completion breakdown, descending by hours.
      required: [workforceId, employeeCode, fullName, workforceType, status, scheduled, completed, overdue, completionRate, totalHours]
      properties:
        workforceId: { $ref: "#/components/schemas/Uuid" }
        employeeCode: { type: string }
        fullName: { type: string }
        workforceType: { type: string }
        status: { type: string }
        scheduled: { type: integer, minimum: 0 }
        completed: { type: integer, minimum: 0 }
        overdue: { type: integer, minimum: 0 }
        completionRate: { type: number }
        totalHours: { type: number }
    WorkforceKpi:
      type: object
      description: BE-23G workforce KPI projection (read-only).
      required:
        [buildingId, buildingScope, dateFrom, dateTo, graceMinutes, asOf,
         workforce, assignments, manHours, byWorkforce]
      properties:
        buildingId: { type: string, format: uuid, nullable: true, description: Null when the KPI is a multi-building rollup. }
        buildingScope: { type: array, items: { type: string, format: uuid }, description: Every Building actually included in the numbers. }
        dateFrom: { type: string, nullable: true }
        dateTo: { type: string, nullable: true }
        graceMinutes: { type: integer, minimum: 0 }
        asOf: { type: string, format: date-time, description: Evaluation instant used to decide overdue. }
        workforce: { $ref: "#/components/schemas/WorkforceKpiHeadcount" }
        assignments: { $ref: "#/components/schemas/WorkforceKpiAssignments" }
        manHours: { $ref: "#/components/schemas/WorkforceKpiManHours" }
        byWorkforce: { type: array, items: { $ref: "#/components/schemas/WorkforceKpiMember" } }
    CurrentSupervisor:
      type: object
      description: >-
        BE-03F — the Supervisor genuinely in force for a Workforce Profile at
        the current instant (ACTIVE line, ACTIVE supervisor, effective window).
      required:
        [reportingLineId, workforceProfileId, supervisorWorkforceProfileId,
         employeeCode, fullName, effectiveFrom, effectiveUntil]
      properties:
        reportingLineId: { $ref: "#/components/schemas/Uuid" }
        workforceProfileId: { $ref: "#/components/schemas/Uuid" }
        supervisorWorkforceProfileId: { $ref: "#/components/schemas/Uuid" }
        employeeCode: { type: string }
        fullName: { type: string }
        effectiveFrom: { type: string, format: date-time, nullable: true }
        effectiveUntil: { type: string, format: date-time, nullable: true }
    DirectReport:
      type: object
      description: BE-03F — one ACTIVE direct report of a Supervisor (profile fields denormalised).
      required:
        [reportingLineId, workforceProfileId, employeeCode, fullName, status,
         effectiveFrom, effectiveUntil]
      properties:
        reportingLineId: { $ref: "#/components/schemas/Uuid" }
        workforceProfileId: { $ref: "#/components/schemas/Uuid" }
        employeeCode: { type: string }
        fullName: { type: string }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        effectiveFrom: { type: string, format: date-time, nullable: true }
        effectiveUntil: { type: string, format: date-time, nullable: true }
    WorkforceReportingRecord:
      type: object
      description: >-
        BE-03I1 query-side projection of one Workforce Profile (and its
        resolved context) assembled from existing BE-03 source tables. The
        reporting module owns no source-of-truth data and writes nothing.
      required:
        [client, organization, department, team, position, profile, user,
         skills, shifts, supervisor, directReports, buildingAssignments,
         externalAffiliations]
      properties:
        client:
          type: object
          required: [id, code, name, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string, enum: [ACTIVE, INACTIVE] }
        organization:
          type: object
          required: [id, clientId, code, name, description, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            clientId: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            description: { type: string, nullable: true }
            status: { type: string, enum: [ACTIVE, INACTIVE] }
        department:
          type: object
          required: [id, organizationId, clientId, code, name, description, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            organizationId: { $ref: "#/components/schemas/Uuid" }
            clientId: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            description: { type: string, nullable: true }
            status: { type: string, enum: [ACTIVE, INACTIVE] }
        team:
          type: object
          nullable: true
          required: [id, departmentId, organizationId, clientId, code, name, description, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            departmentId: { $ref: "#/components/schemas/Uuid" }
            organizationId: { $ref: "#/components/schemas/Uuid" }
            clientId: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            description: { type: string, nullable: true }
            status: { type: string, enum: [ACTIVE, INACTIVE] }
        position:
          type: object
          required: [id, organizationId, departmentId, clientId, code, name, description, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            organizationId: { $ref: "#/components/schemas/Uuid" }
            departmentId: { type: string, format: uuid, nullable: true }
            clientId: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            description: { type: string, nullable: true }
            status: { type: string, enum: [ACTIVE, INACTIVE] }
        profile:
          type: object
          required: [id, employeeCode, fullName, workforceType, status, userId, createdAt, updatedAt]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            employeeCode: { type: string }
            fullName: { type: string }
            workforceType: { type: string, enum: [INTERNAL, OUTSOURCED, CONTRACT, EXTERNAL] }
            status: { type: string, enum: [ACTIVE, INACTIVE] }
            userId: { type: string, format: uuid, nullable: true }
            createdAt: { type: string, format: date-time }
            updatedAt: { type: string, format: date-time }
        user:
          type: object
          nullable: true
          required: [id, email, displayName, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            email: { type: string }
            displayName: { type: string }
            status: { type: string }
        skills:
          type: array
          items:
            type: object
            required: [id, skillId, code, name, category, status, assignmentStatus, proficiencyLevel, validFrom, validUntil]
            properties:
              id: { $ref: "#/components/schemas/Uuid" }
              skillId: { $ref: "#/components/schemas/Uuid" }
              code: { type: string }
              name: { type: string }
              category: { type: string, enum: [TECHNICAL, OPERATIONAL, SAFETY, SPECIALIST] }
              status: { type: string, enum: [ACTIVE, INACTIVE] }
              assignmentStatus: { type: string, enum: [ACTIVE, INACTIVE] }
              proficiencyLevel: { type: string, enum: [BASIC, INTERMEDIATE, ADVANCED, EXPERT] }
              validFrom: { type: string, format: date-time, nullable: true }
              validUntil: { type: string, format: date-time, nullable: true }
        shifts:
          type: array
          items:
            type: object
            required:
              [id, shiftId, code, name, startTime, endTime, buildingId,
               buildingCode, buildingName, shiftStatus, assignmentStatus,
               effectiveFrom, effectiveUntil]
            properties:
              id: { $ref: "#/components/schemas/Uuid" }
              shiftId: { $ref: "#/components/schemas/Uuid" }
              code: { type: string }
              name: { type: string }
              startTime: { type: string }
              endTime: { type: string }
              buildingId: { $ref: "#/components/schemas/Uuid" }
              buildingCode: { type: string }
              buildingName: { type: string }
              shiftStatus: { type: string, enum: [ACTIVE, INACTIVE] }
              assignmentStatus: { type: string, enum: [ACTIVE, INACTIVE] }
              effectiveFrom: { type: string, format: date-time, nullable: true }
              effectiveUntil: { type: string, format: date-time, nullable: true }
        supervisor:
          type: object
          nullable: true
          required:
            [reportingLineId, workforceProfileId, employeeCode, fullName,
             assignmentStatus, effectiveFrom, effectiveUntil]
          properties:
            reportingLineId: { $ref: "#/components/schemas/Uuid" }
            workforceProfileId: { $ref: "#/components/schemas/Uuid" }
            employeeCode: { type: string }
            fullName: { type: string }
            assignmentStatus: { type: string, enum: [ACTIVE, INACTIVE] }
            effectiveFrom: { type: string, format: date-time, nullable: true }
            effectiveUntil: { type: string, format: date-time, nullable: true }
        directReports:
          type: array
          items:
            type: object
            required:
              [reportingLineId, workforceProfileId, employeeCode, fullName,
               assignmentStatus, effectiveFrom, effectiveUntil]
            properties:
              reportingLineId: { $ref: "#/components/schemas/Uuid" }
              workforceProfileId: { $ref: "#/components/schemas/Uuid" }
              employeeCode: { type: string }
              fullName: { type: string }
              assignmentStatus: { type: string, enum: [ACTIVE, INACTIVE] }
              effectiveFrom: { type: string, format: date-time, nullable: true }
              effectiveUntil: { type: string, format: date-time, nullable: true }
        buildingAssignments:
          type: array
          items:
            type: object
            required:
              [id, buildingId, buildingCode, buildingName, propertyId,
               propertyCode, propertyName, clientId, buildingStatus,
               assignmentStatus, effectiveFrom, effectiveUntil]
            properties:
              id: { $ref: "#/components/schemas/Uuid" }
              buildingId: { $ref: "#/components/schemas/Uuid" }
              buildingCode: { type: string }
              buildingName: { type: string }
              propertyId: { $ref: "#/components/schemas/Uuid" }
              propertyCode: { type: string }
              propertyName: { type: string }
              clientId: { $ref: "#/components/schemas/Uuid" }
              buildingStatus: { type: string, enum: [ACTIVE, INACTIVE] }
              assignmentStatus: { type: string, enum: [ACTIVE, INACTIVE] }
              effectiveFrom: { type: string, format: date-time, nullable: true }
              effectiveUntil: { type: string, format: date-time, nullable: true }
        externalAffiliations:
          type: array
          items:
            type: object
            required:
              [id, externalOrganizationId, externalOrganizationCode,
               externalOrganizationName, externalOrganizationStatus,
               externalPersonnelCode, assignmentStatus, effectiveFrom,
               effectiveUntil]
            properties:
              id: { $ref: "#/components/schemas/Uuid" }
              externalOrganizationId: { $ref: "#/components/schemas/Uuid" }
              externalOrganizationCode: { type: string }
              externalOrganizationName: { type: string }
              externalOrganizationStatus: { type: string }
              externalPersonnelCode: { type: string }
              assignmentStatus: { type: string, enum: [ACTIVE, INACTIVE] }
              effectiveFrom: { type: string, format: date-time, nullable: true }
              effectiveUntil: { type: string, format: date-time, nullable: true }

    # ------------------------------------------------------------------
    # CR-BE-MOB-03 PART 02 — Housekeeping supervisor supporting schemas
    # (BE-11J consumable readiness, BE-16J consumable bindings, BE-11L
    # complaint bindings, BE-11M report datasets). Mirrors the implemented
    # DTOs; ids are the authoritative BE-11/BE-16 rows.
    # ------------------------------------------------------------------
    ConsumableRequirementStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    ReadinessStatus:
      type: string
      enum: [READY, LOW, NOT_READY, UNKNOWN]
      description: >-
        Derived operational readiness of a consumable requirement from
        authoritative BE-16 stock: READY when available >= required, LOW
        when 0 < available < required, NOT_READY when 0 or null,
        UNKNOWN when inactive.
    ConsumableRequirement:
      type: object
      description: BE-11J consumable requirement with latest readiness context.
      required:
        [id, clientId, buildingId, cleaningAreaId, code, name,
         requiredQuantity, unit, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaId: { type: string, format: uuid, nullable: true }
        code: { type: string }
        name: { type: string }
        requiredQuantity: { type: number, minimum: 0 }
        unit: { type: string }
        status: { $ref: "#/components/schemas/ConsumableRequirementStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        cleaningArea:
          type: object
          nullable: true
          required: [id, code, name, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string }
        currentReadiness:
          type: object
          nullable: true
          required: [readinessStatus, availableQuantity, checkedAt]
          properties:
            readinessStatus: { $ref: "#/components/schemas/ReadinessStatus" }
            availableQuantity: { type: number, nullable: true }
            checkedAt: { type: string, format: date-time }
    CreateConsumableRequirementRequest:
      type: object
      required: [buildingId, code, name, unit]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string, maxLength: 160 }
        requiredQuantity: { type: number, minimum: 0, default: 1 }
        unit: { type: string, maxLength: 32 }
        status: { $ref: "#/components/schemas/ConsumableRequirementStatus" }
    UpdateConsumableRequirementRequest:
      type: object
      properties:
        name: { type: string, maxLength: 160 }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        requiredQuantity: { type: number, minimum: 0 }
        unit: { type: string, maxLength: 32 }
        status: { $ref: "#/components/schemas/ConsumableRequirementStatus" }
    RecordConsumableReadinessRequest:
      type: object
      required: [readinessStatus]
      properties:
        readinessStatus: { $ref: "#/components/schemas/ReadinessStatus" }
        availableQuantity: { type: number, minimum: 0, nullable: true, default: null }
        operationalDate: { type: string, description: YYYY-MM-DD operational date. }
        notes: { type: string, nullable: true }
    ConsumableReadiness:
      type: object
      description: BE-11J recorded readiness check.
      required:
        [id, clientId, buildingId, requirementId, operationalDate,
         readinessStatus, availableQuantity, checkedByUserId, checkedAt,
         notes, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        requirementId: { $ref: "#/components/schemas/Uuid" }
        operationalDate: { type: string }
        readinessStatus: { $ref: "#/components/schemas/ReadinessStatus" }
        availableQuantity: { type: number, nullable: true }
        checkedByUserId: { $ref: "#/components/schemas/Uuid" }
        checkedAt: { type: string, format: date-time }
        notes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        requirement:
          type: object
          nullable: true
          required: [id, code, name, requiredQuantity, unit]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            requiredQuantity: { type: number }
            unit: { type: string }
        cleaningArea:
          type: object
          nullable: true
          required: [id, code, name]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
    HkConsumableBindingStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    HkConsumableBinding:
      type: object
      description: >-
        BE-16J binding of a BE-11J requirement to a BE-16A CONSUMABLE item +
        BE-16B warehouse. `readiness` is derived from authoritative stock.
      required:
        [id, clientId, buildingId, cleaningAreaId, consumableRequirementId,
         itemId, warehouseId, requiredQuantity, status, notes, createdAt,
         updatedAt, readiness]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaId: { type: string, format: uuid, nullable: true }
        consumableRequirementId: { $ref: "#/components/schemas/Uuid" }
        itemId: { $ref: "#/components/schemas/Uuid" }
        warehouseId: { $ref: "#/components/schemas/Uuid" }
        requiredQuantity: { type: number, minimum: 0 }
        status: { $ref: "#/components/schemas/HkConsumableBindingStatus" }
        notes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        readiness: { $ref: "#/components/schemas/ReadinessStatus" }
        cleaningArea:
          type: object
          nullable: true
          required: [id, code, name]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
        requirement:
          type: object
          nullable: true
          required: [id, code, name, requiredQuantity, unit, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            requiredQuantity: { type: number }
            unit: { type: string }
            status: { type: string }
        item:
          type: object
          nullable: true
          required: [id, code, name, itemType, uomId]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            itemType: { type: string }
            uomId: { type: string, format: uuid, nullable: true }
        warehouse:
          type: object
          nullable: true
          required: [id, code, name, buildingId]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            buildingId: { $ref: "#/components/schemas/Uuid" }
        currentStock:
          type: object
          nullable: true
          required: [quantityOnHand, reservedQuantity, availableQuantity]
          properties:
            quantityOnHand: { type: number }
            reservedQuantity: { type: number }
            availableQuantity: { type: number }
    CreateHkConsumableBindingRequest:
      type: object
      required: [itemId, warehouseId]
      description: The consumable requirement id comes from the path.
      properties:
        itemId: { $ref: "#/components/schemas/Uuid" }
        warehouseId: { $ref: "#/components/schemas/Uuid" }
        requiredQuantity: { type: number, minimum: 0 }
        status: { $ref: "#/components/schemas/HkConsumableBindingStatus" }
        notes: { type: string, maxLength: 512 }
    UpdateHkConsumableBindingRequest:
      type: object
      properties:
        requiredQuantity: { type: number, minimum: 0 }
        status: { $ref: "#/components/schemas/HkConsumableBindingStatus" }
        notes: { type: string, maxLength: 512, nullable: true }
    HousekeepingComplaintBindingStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    HousekeepingComplaintSourceType:
      type: string
      enum:
        [DAILY_CLEANING, TOILET_INSPECTION, PUBLIC_AREA_INSPECTION,
         SUPERVISOR_INSPECTION, CLEANING_AREA]
    HousekeepingComplaintBinding:
      type: object
      description: >-
        BE-11L binding of a customer complaint / service request to
        Housekeeping operations. Workflow stays on the referenced BE-08 /
        BE-09 / BE-07 authorities.
      required:
        [id, clientId, buildingId, complaintReference, workRequestId,
         cleaningAreaId, housekeepingSourceType, housekeepingSourceId,
         findingId, description, status, createdByUserId, createdAt,
         updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        complaintReference: { type: string }
        workRequestId: { type: string, format: uuid, nullable: true }
        cleaningAreaId: { type: string, format: uuid, nullable: true }
        housekeepingSourceType:
          type: string
          nullable: true
          $ref: "#/components/schemas/HousekeepingComplaintSourceType"
        housekeepingSourceId: { type: string, format: uuid, nullable: true }
        findingId: { type: string, format: uuid, nullable: true }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/HousekeepingComplaintBindingStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        cleaningArea:
          type: object
          nullable: true
          required: [id, code, name, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string }
        finding:
          type: object
          nullable: true
          required: [id, findingNumber, title, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            findingNumber: { type: string }
            title: { type: string }
            status: { type: string }
        workRequest:
          type: object
          nullable: true
          required: [id, requestNumber, title, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            requestNumber: { type: string }
            title: { type: string }
            status: { type: string }
    CreateHousekeepingComplaintBindingRequest:
      type: object
      required: [buildingId, complaintReference]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        complaintReference: { type: string }
        workRequestId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        housekeepingSourceType:
          $ref: "#/components/schemas/HousekeepingComplaintSourceType"
        housekeepingSourceId: { $ref: "#/components/schemas/Uuid" }
        findingId: { $ref: "#/components/schemas/Uuid" }
        description: { type: string }
        status: { $ref: "#/components/schemas/HousekeepingComplaintBindingStatus" }
    UpdateHousekeepingComplaintBindingRequest:
      type: object
      properties:
        description: { type: string, nullable: true }
        findingId: { $ref: "#/components/schemas/Uuid" }
        status: { $ref: "#/components/schemas/HousekeepingComplaintBindingStatus" }
    HousekeepingReportSummary:
      type: object
      description: BE-11M summary projection for one Building.
      required:
        [buildingId, dateRange, cleaningAreasCount, dailyCleaning,
         toiletInspections, publicAreaInspections, supervisorInspections,
         findings, consumables, qualityAudits, complaints]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        dateRange:
          type: object
          required: [dateFrom, dateTo]
          properties:
            dateFrom: { type: string, nullable: true }
            dateTo: { type: string, nullable: true }
        cleaningAreasCount: { type: integer, minimum: 0 }
        dailyCleaning:
          type: object
          required: [total, open, assigned, inProgress, completed, cancelled]
          properties:
            total: { type: integer, minimum: 0 }
            open: { type: integer, minimum: 0 }
            assigned: { type: integer, minimum: 0 }
            inProgress: { type: integer, minimum: 0 }
            completed: { type: integer, minimum: 0 }
            cancelled: { type: integer, minimum: 0 }
        toiletInspections:
          type: object
          required: [bindingsCount, executionsCount, completedCount]
          properties:
            bindingsCount: { type: integer, minimum: 0 }
            executionsCount: { type: integer, minimum: 0 }
            completedCount: { type: integer, minimum: 0 }
        publicAreaInspections:
          type: object
          required: [bindingsCount, executionsCount, completedCount]
          properties:
            bindingsCount: { type: integer, minimum: 0 }
            executionsCount: { type: integer, minimum: 0 }
            completedCount: { type: integer, minimum: 0 }
        supervisorInspections:
          type: object
          required: [total, pending, approved, rejected, reworkRequired]
          properties:
            total: { type: integer, minimum: 0 }
            pending: { type: integer, minimum: 0 }
            approved: { type: integer, minimum: 0 }
            rejected: { type: integer, minimum: 0 }
            reworkRequired: { type: integer, minimum: 0 }
        findings:
          type: object
          required: [total, open, inProgress, reworkRequired, verified, closed]
          properties:
            total: { type: integer, minimum: 0 }
            open: { type: integer, minimum: 0 }
            inProgress: { type: integer, minimum: 0 }
            reworkRequired: { type: integer, minimum: 0 }
            verified: { type: integer, minimum: 0 }
            closed: { type: integer, minimum: 0 }
        consumables:
          type: object
          required: [requirementsCount, readyCount, lowCount, notReadyCount]
          properties:
            requirementsCount: { type: integer, minimum: 0 }
            readyCount: { type: integer, minimum: 0 }
            lowCount: { type: integer, minimum: 0 }
            notReadyCount: { type: integer, minimum: 0 }
        qualityAudits:
          type: object
          required:
            [total, completed, passed, failed, reworkRequired, averageScore]
          properties:
            total: { type: integer, minimum: 0 }
            completed: { type: integer, minimum: 0 }
            passed: { type: integer, minimum: 0 }
            failed: { type: integer, minimum: 0 }
            reworkRequired: { type: integer, minimum: 0 }
            averageScore: { type: number, nullable: true }
        complaints:
          type: object
          required: [totalBindings, activeBindings]
          properties:
            totalBindings: { type: integer, minimum: 0 }
            activeBindings: { type: integer, minimum: 0 }
    HousekeepingCleaningReportRow:
      type: object
      description: BE-11M daily-cleaning dataset row.
      required:
        [taskId, operationalDate, occurrenceAt, status, cleaningAreaId,
         cleaningAreaCode, cleaningAreaName, cleaningAreaType, scheduleCode,
         scheduleName, assigneeType, workforceProfileId, teamId,
         completedByUserId, completedAt]
      properties:
        taskId: { $ref: "#/components/schemas/Uuid", description: BE-07 generated task id. }
        operationalDate: { type: string }
        occurrenceAt: { type: string, format: date-time }
        status: { type: string }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaCode: { type: string }
        cleaningAreaName: { type: string }
        cleaningAreaType: { type: string }
        scheduleCode: { type: string }
        scheduleName: { type: string }
        assigneeType: { type: string, nullable: true }
        workforceProfileId: { type: string, format: uuid, nullable: true }
        teamId: { type: string, format: uuid, nullable: true }
        completedByUserId: { type: string, format: uuid, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
    HousekeepingInspectionReportRow:
      type: object
      description: BE-11M toilet / public-area inspection dataset row.
      required:
        [inspectionType, bindingId, executionId, executionStatus,
         cleaningAreaId, cleaningAreaCode, cleaningAreaName, templateCode,
         templateName, roomCode, functionalLocationCode, startedAt,
         completedAt]
      properties:
        inspectionType: { type: string, enum: [TOILET, PUBLIC_AREA] }
        bindingId: { $ref: "#/components/schemas/Uuid" }
        executionId: { type: string, format: uuid, nullable: true, description: BE-07 checklist execution id. }
        executionStatus: { type: string, nullable: true }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaCode: { type: string }
        cleaningAreaName: { type: string }
        templateCode: { type: string }
        templateName: { type: string }
        roomCode: { type: string, nullable: true }
        functionalLocationCode: { type: string, nullable: true }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
    HousekeepingSupervisorInspectionReportRow:
      type: object
      description: BE-11M supervisor-inspection dataset row.
      required:
        [inspectionId, targetType, targetId, cleaningAreaId, cleaningAreaCode,
         supervisorUserId, decision, status, inspectedAt, notes]
      properties:
        inspectionId: { $ref: "#/components/schemas/Uuid" }
        targetType: { type: string }
        targetId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaCode: { type: string }
        supervisorUserId: { $ref: "#/components/schemas/Uuid" }
        decision: { type: string, nullable: true }
        status: { type: string }
        inspectedAt: { type: string, format: date-time, nullable: true }
        notes: { type: string, nullable: true }
    HousekeepingFindingReportRow:
      type: object
      description: BE-11M housekeeping finding dataset row.
      required:
        [findingId, findingNumber, title, status, sourceType, sourceId,
         cleaningAreaId, cleaningAreaCode, reportedByUserId, reportedAt]
      properties:
        findingId: { $ref: "#/components/schemas/Uuid", description: BE-09 finding id. }
        findingNumber: { type: string }
        title: { type: string }
        status: { type: string }
        sourceType: { type: string }
        sourceId: { type: string, format: uuid }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaCode: { type: string }
        reportedByUserId: { $ref: "#/components/schemas/Uuid" }
        reportedAt: { type: string, format: date-time }
    HousekeepingConsumableReportRow:
      type: object
      description: BE-11M consumable dataset row.
      required:
        [requirementId, requirementCode, requirementName, unit,
         requiredQuantity, cleaningAreaId, cleaningAreaCode, readinessStatus,
         availableQuantity, checkedAt]
      properties:
        requirementId: { $ref: "#/components/schemas/Uuid" }
        requirementCode: { type: string }
        requirementName: { type: string }
        unit: { type: string }
        requiredQuantity: { type: number }
        cleaningAreaId: { type: string, format: uuid, nullable: true }
        cleaningAreaCode: { type: string, nullable: true }
        readinessStatus: { type: string, nullable: true }
        availableQuantity: { type: number, nullable: true }
        checkedAt: { type: string, format: date-time, nullable: true }
    HousekeepingQualityAuditReportRow:
      type: object
      description: BE-11M quality-audit dataset row.
      required:
        [auditId, sourceType, sourceId, cleaningAreaId, cleaningAreaCode,
         auditorUserId, score, result, status, auditedAt]
      properties:
        auditId: { $ref: "#/components/schemas/Uuid" }
        sourceType: { type: string }
        sourceId: { type: string, format: uuid }
        cleaningAreaId: { type: string, format: uuid, nullable: true }
        cleaningAreaCode: { type: string, nullable: true }
        auditorUserId: { $ref: "#/components/schemas/Uuid" }
        score: { type: number, nullable: true }
        result: { type: string, nullable: true }
        status: { type: string }
        auditedAt: { type: string, format: date-time, nullable: true }
    HousekeepingComplaintReportRow:
      type: object
      description: BE-11M complaint-binding dataset row.
      required:
        [bindingId, complaintReference, workRequestId, cleaningAreaId,
         cleaningAreaCode, housekeepingSourceType, housekeepingSourceId,
         findingId, status, createdAt]
      properties:
        bindingId: { $ref: "#/components/schemas/Uuid" }
        complaintReference: { type: string }
        workRequestId: { type: string, format: uuid, nullable: true }
        cleaningAreaId: { type: string, format: uuid, nullable: true }
        cleaningAreaCode: { type: string, nullable: true }
        housekeepingSourceType: { type: string, nullable: true }
        housekeepingSourceId: { type: string, format: uuid, nullable: true }
        findingId: { type: string, format: uuid, nullable: true }
        status: { type: string }
        createdAt: { type: string, format: date-time }

    # ------------------------------------------------------------------
    # CR-BE-MOB-03 PART 03 — Engineering & corrective-action verification
    # schemas (BE-10I report datasets, BE-21J corrective-action
    # verification). Mirrors the implemented DTOs; ids are the authoritative
    # BE-10/BE-21/BE-07 rows.
    # ------------------------------------------------------------------
    EngineeringTechnicalSummary:
      type: object
      description: BE-10I technical summary projection for one Building.
      required:
        [buildingId, dateFrom, dateTo, operations, inspections,
         meterReadings, equipmentLogs, checklists, breakdowns, maintenance,
         findings]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        dateFrom: { type: string, nullable: true }
        dateTo: { type: string, nullable: true }
        operations:
          type: object
          required: [scheduled, inProgress, completed, openWorkOrders, openFindings]
          properties:
            scheduled: { type: integer, minimum: 0 }
            inProgress: { type: integer, minimum: 0 }
            completed: { type: integer, minimum: 0 }
            openWorkOrders: { type: integer, minimum: 0 }
            openFindings: { type: integer, minimum: 0 }
        inspections:
          type: object
          required: [bindingCount, executionCount]
          properties:
            bindingCount: { type: integer, minimum: 0 }
            executionCount: { type: integer, minimum: 0 }
        meterReadings:
          type: object
          required: [bindingCount, readingCount]
          properties:
            bindingCount: { type: integer, minimum: 0 }
            readingCount: { type: integer, minimum: 0 }
        equipmentLogs:
          type: object
          required: [bindingCount, executionCount]
          properties:
            bindingCount: { type: integer, minimum: 0 }
            executionCount: { type: integer, minimum: 0 }
        checklists:
          type: object
          required: [bindingCount, executionCount]
          properties:
            bindingCount: { type: integer, minimum: 0 }
            executionCount: { type: integer, minimum: 0 }
        breakdowns:
          type: object
          required: [open, closed]
          properties:
            open: { type: integer, minimum: 0 }
            closed: { type: integer, minimum: 0 }
        maintenance:
          type: object
          required: [activeBindings, linkedWorkOrders]
          properties:
            activeBindings: { type: integer, minimum: 0 }
            linkedWorkOrders: { type: integer, minimum: 0 }
        findings:
          type: object
          required: [open, verified, closed]
          properties:
            open: { type: integer, minimum: 0 }
            verified: { type: integer, minimum: 0 }
            closed: { type: integer, minimum: 0 }
    EngineeringInspectionReportRow:
      type: object
      description: BE-10I equipment-inspection binding dataset row.
      required:
        [bindingId, assetId, assetCode, assetName, templateId, templateCode,
         functionalLocationCode, status, executionCount, lastExecutionAt]
      properties:
        bindingId: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        assetCode: { type: string }
        assetName: { type: string }
        templateId: { $ref: "#/components/schemas/Uuid" }
        templateCode: { type: string }
        functionalLocationCode: { type: string, nullable: true }
        status: { type: string }
        executionCount: { type: integer, minimum: 0 }
        lastExecutionAt: { type: string, format: date-time, nullable: true }
    EngineeringMeterReadingReportRow:
      type: object
      description: BE-10I meter-reading binding dataset row (BE-07 UOM + range).
      required:
        [bindingId, assetId, assetCode, assetName, fieldCode, uomId, uomCode,
         uomSymbol, minimumValue, maximumValue, status, readingCount,
         lastReadAt]
      properties:
        bindingId: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        assetCode: { type: string }
        assetName: { type: string }
        fieldCode: { type: string, nullable: true }
        uomId: { $ref: "#/components/schemas/Uuid" }
        uomCode: { type: string }
        uomSymbol: { type: string }
        minimumValue: { type: number, nullable: true }
        maximumValue: { type: number, nullable: true }
        status: { type: string }
        readingCount: { type: integer, minimum: 0 }
        lastReadAt: { type: string, format: date-time, nullable: true }
    EngineeringEquipmentLogReportRow:
      type: object
      description: BE-10I equipment log-sheet binding dataset row.
      required:
        [bindingId, assetId, assetCode, assetName, templateId, templateCode,
         status, executionCount, lastExecutionAt]
      properties:
        bindingId: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        assetCode: { type: string }
        assetName: { type: string }
        templateId: { $ref: "#/components/schemas/Uuid" }
        templateCode: { type: string }
        status: { type: string }
        executionCount: { type: integer, minimum: 0 }
        lastExecutionAt: { type: string, format: date-time, nullable: true }
    EngineeringChecklistReportRow:
      type: object
      description: BE-10I engineering-checklist binding dataset row.
      required:
        [bindingId, templateId, templateCode, assetId, assetCode,
         functionalLocationCode, bindingStatus, executionCount,
         lastExecutionAt]
      properties:
        bindingId: { $ref: "#/components/schemas/Uuid" }
        templateId: { $ref: "#/components/schemas/Uuid" }
        templateCode: { type: string }
        assetId: { type: string, format: uuid, nullable: true }
        assetCode: { type: string, nullable: true }
        functionalLocationCode: { type: string, nullable: true }
        bindingStatus: { type: string }
        executionCount: { type: integer, minimum: 0 }
        lastExecutionAt: { type: string, format: date-time, nullable: true }
    EngineeringBreakdownReportRow:
      type: object
      description: BE-10I breakdown dataset row with linked corrective Work Order context.
      required:
        [breakdownId, assetId, assetCode, assetName, category, description,
         reportedByUserId, reportedAt, status, workOrderId, workOrderNumber,
         workOrderStatus]
      properties:
        breakdownId: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        assetCode: { type: string }
        assetName: { type: string }
        category: { type: string }
        description: { type: string }
        reportedByUserId: { $ref: "#/components/schemas/Uuid" }
        reportedAt: { type: string, format: date-time }
        status: { type: string }
        workOrderId: { type: string, format: uuid, nullable: true }
        workOrderNumber: { type: string, nullable: true }
        workOrderStatus: { type: string, nullable: true }
    EngineeringMaintenanceReportRow:
      type: object
      description: BE-10I planned-maintenance binding dataset row.
      required:
        [bindingId, assetId, assetCode, assetName, name, maintenanceType,
         status, scheduleCode, scheduleStatus, workOrderNumber,
         workOrderStatus, taskCount]
      properties:
        bindingId: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        assetCode: { type: string }
        assetName: { type: string }
        name: { type: string }
        maintenanceType: { type: string }
        status: { type: string }
        scheduleCode: { type: string, nullable: true }
        scheduleStatus: { type: string, nullable: true }
        workOrderNumber: { type: string, nullable: true }
        workOrderStatus: { type: string, nullable: true }
        taskCount: { type: integer, minimum: 0 }
    EngineeringFindingReportRow:
      type: object
      description: BE-10I engineering finding dataset row (BE-09 finding via the BE-10H link).
      required:
        [linkId, findingId, findingNumber, title, status, operationType,
         sourceType, sourceId, assetId, assetCode, classificationName,
         severityName, reportedAt]
      properties:
        linkId: { $ref: "#/components/schemas/Uuid", description: BE-10H link id. }
        findingId: { $ref: "#/components/schemas/Uuid", description: BE-09 finding id. }
        findingNumber: { type: string }
        title: { type: string }
        status: { type: string }
        operationType: { type: string }
        sourceType: { type: string, nullable: true }
        sourceId: { type: string, format: uuid, nullable: true }
        assetId: { type: string, format: uuid, nullable: true }
        assetCode: { type: string, nullable: true }
        classificationName: { type: string, nullable: true }
        severityName: { type: string, nullable: true }
        reportedAt: { type: string, format: date-time }
    CorrectiveActionVerificationStatus:
      type: string
      enum: [PENDING, COMPLETED]
    CorrectiveActionVerification:
      type: object
      description: >-
        BE-21J — a shared BE-07 `reviews` row projected for the
        CORRECTIVE_ACTION target type. `decision` reuses the shared
        ReviewDecision vocabulary; the lifecycle outcome (VERIFIED /
        IN_PROGRESS) is derived by the backend, never asserted by the caller.
      required:
        [id, clientId, correctiveActionId, reviewerUserId, decision, notes,
         reviewedAt, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        correctiveActionId: { $ref: "#/components/schemas/Uuid" }
        reviewerUserId: { $ref: "#/components/schemas/Uuid", description: Always the authenticated reviewer. }
        decision: { $ref: "#/components/schemas/ReviewDecision" }
        notes: { type: string, nullable: true }
        reviewedAt: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/CorrectiveActionVerificationStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CorrectiveActionStatus:
      type: string
      description: BE-21G corrective action lifecycle status.
      enum:
        [PROPOSED, APPROVED, IN_PROGRESS, COMPLETED, VERIFIED, REJECTED, CANCELLED]
    CorrectiveActionVerificationContext:
      type: object
      description: >-
        BE-21J backend-authoritative verification context: everything a
        caller needs to decide whether a verification can be opened or
        submitted, computed by the backend. Only COMPLETED actions are
        verifiable.
      required:
        [correctiveActionId, incidentId, clientId, buildingId,
         correctiveActionStatus, verifiable, blockers, currentVerification,
         latestVerification, finalized]
      properties:
        correctiveActionId: { $ref: "#/components/schemas/Uuid" }
        incidentId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        correctiveActionStatus: { $ref: "#/components/schemas/CorrectiveActionStatus" }
        verifiable: { type: boolean }
        blockers:
          type: array
          items: { type: string }
          description: Why the action cannot be verified, when verifiable is false.
        currentVerification:
          type: object
          nullable: true
          allOf:
            - $ref: "#/components/schemas/CorrectiveActionVerification"
          description: The open PENDING verification, if one exists.
        latestVerification:
          type: object
          nullable: true
          allOf:
            - $ref: "#/components/schemas/CorrectiveActionVerification"
          description: The most recent COMPLETED verification — the authoritative result.
        finalized: { type: boolean, description: True once an APPROVED decision has been recorded. }
    OpenCorrectiveActionVerificationRequest:
      type: object
      description: Opening a verification takes an optional note and nothing else.
      properties:
        notes: { type: string, maxLength: 4000, nullable: true }
    SubmitCorrectiveActionVerificationRequest:
      type: object
      description: >-
        The decision payload. `decision` is validated against the shared
        review vocabulary; derived fields (status, reviewer, timestamps) are
        refused.
      required: [decision]
      properties:
        decision: { $ref: "#/components/schemas/ReviewDecision" }
        notes: { type: string, maxLength: 4000, nullable: true }
    SlaDefinition:
      type: object
      description: SLA configuration only; it is not an applied SLA or clock.
      required: [id, clientId, buildingId, code, name, operationalType, workType, priority, responseTargetMinutes, resolutionTargetMinutes, status, effectiveFrom, effectiveTo, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        code: { type: string }
        name: { type: string }
        operationalType: { type: string, enum: [WORK_ORDER] }
        workType: { type: string, nullable: true }
        priority: { type: string, enum: [LOW, MEDIUM, HIGH, CRITICAL], nullable: true }
        responseTargetMinutes: { type: integer, minimum: 1, nullable: true }
        resolutionTargetMinutes: { type: integer, minimum: 1, nullable: true }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateSlaDefinitionRequest:
      type: object
      required: [code, name, operationalType, effectiveFrom]
      anyOf: [{ required: [responseTargetMinutes] }, { required: [resolutionTargetMinutes] }]
      properties:
        code: { type: string }
        name: { type: string }
        operationalType: { type: string, enum: [WORK_ORDER] }
        buildingId: { type: string, format: uuid }
        workType: { type: string }
        priority: { type: string, enum: [LOW, MEDIUM, HIGH, CRITICAL] }
        responseTargetMinutes: { type: integer, minimum: 1 }
        resolutionTargetMinutes: { type: integer, minimum: 1 }
        status: { type: string, enum: [ACTIVE, INACTIVE], default: ACTIVE }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time }
    UpdateSlaDefinitionRequest:
      type: object
      properties:
        name: { type: string }
        workType: { type: string }
        priority: { type: string, enum: [LOW, MEDIUM, HIGH, CRITICAL] }
        responseTargetMinutes: { type: integer, minimum: 1 }
        resolutionTargetMinutes: { type: integer, minimum: 1 }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time }
    SlaEscalationClockType:
      type: string
      enum: [RESPONSE, RESOLUTION, ANY]
      description: Which SLA clock a policy applies to; ANY matches either clock.
    SlaEscalationDerivedTarget:
      type: object
      description: >
        Configuration vocabulary for a recipient target that can only be derived from the
        breached Work Order. Stored and validated here; expansion and resolution are out of
        scope for this contract.
      required: [kind]
      properties:
        kind:
          type: string
          enum: [WORK_ORDER_ASSIGNEE, WORK_ORDER_ASSIGNEE_SUPERVISOR, WORK_ORDER_CREATOR, BUILDING_ROLE, BUILDING_PERMISSION]
        roleCode: { type: string, description: Required when kind is BUILDING_ROLE. }
        permissionCode: { type: string, description: Required when kind is BUILDING_PERMISSION. }
    SlaEscalationRecipientRule:
      type: object
      description: >
        Stored recipient configuration. `specs` reuses the existing recipient rule
        vocabulary; `derived` carries Work-Order-derived targets. At least one static spec
        or one derived target is required. No recipient is resolved by this API.
      properties:
        specs:
          type: array
          items: { type: object }
        derived:
          type: array
          items: { $ref: "#/components/schemas/SlaEscalationDerivedTarget" }
        scope:
          type: object
          properties:
            clientId: { $ref: "#/components/schemas/Uuid" }
            buildingIds: { type: array, items: { $ref: "#/components/schemas/Uuid" } }
    SlaEscalationPolicy:
      type: object
      description: SLA escalation configuration only; it schedules nothing and sends nothing.
      required: [id, clientId, buildingId, code, name, description, operationalType, clockType, workType, priority, status, effectiveFrom, effectiveTo, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        operationalType: { type: string, enum: [WORK_ORDER] }
        clockType: { $ref: "#/components/schemas/SlaEscalationClockType" }
        workType: { type: string, nullable: true }
        priority: { type: string, enum: [LOW, MEDIUM, HIGH, CRITICAL], nullable: true }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateSlaEscalationPolicyRequest:
      type: object
      required: [code, name, operationalType, clockType, effectiveFrom]
      properties:
        code: { type: string }
        name: { type: string }
        description: { type: string }
        buildingId: { type: string, format: uuid }
        operationalType: { type: string, enum: [WORK_ORDER] }
        clockType: { $ref: "#/components/schemas/SlaEscalationClockType" }
        workType: { type: string }
        priority: { type: string, enum: [LOW, MEDIUM, HIGH, CRITICAL] }
        status: { type: string, enum: [ACTIVE, INACTIVE], default: ACTIVE }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time }
    UpdateSlaEscalationPolicyRequest:
      type: object
      properties:
        name: { type: string }
        description: { type: string, nullable: true }
        clockType: { $ref: "#/components/schemas/SlaEscalationClockType" }
        workType: { type: string, nullable: true }
        priority: { type: string, enum: [LOW, MEDIUM, HIGH, CRITICAL], nullable: true }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time }
    SlaEscalationLevel:
      type: object
      description: An ordered escalation step of a policy, expressed as minutes after the breach.
      required: [id, policyId, level, offsetMinutes, templateKey, recipientRule, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        policyId: { $ref: "#/components/schemas/Uuid" }
        level: { type: integer, minimum: 1 }
        offsetMinutes: { type: integer, minimum: 0 }
        templateKey: { type: string }
        recipientRule: { $ref: "#/components/schemas/SlaEscalationRecipientRule" }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateSlaEscalationLevelRequest:
      type: object
      required: [level, templateKey, recipientRule]
      properties:
        level: { type: integer, minimum: 1 }
        offsetMinutes: { type: integer, minimum: 0, default: 0 }
        templateKey: { type: string }
        recipientRule: { $ref: "#/components/schemas/SlaEscalationRecipientRule" }
        status: { type: string, enum: [ACTIVE, INACTIVE], default: ACTIVE }
    UpdateSlaEscalationLevelRequest:
      type: object
      properties:
        level: { type: integer, minimum: 1 }
        offsetMinutes: { type: integer, minimum: 0 }
        templateKey: { type: string }
        recipientRule: { $ref: "#/components/schemas/SlaEscalationRecipientRule" }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
    SlaEscalationAction:
      type: object
      description: >
        One materialized escalation level for one breached SLA clock, with its policy/level
        provenance snapshotted at materialization time. Read-only. Recipient identities are
        deliberately absent: the snapshotted recipient rule is never published, and only the
        aggregate resolved-recipient and created-notification counts are returned. Escalation
        delivery is in-app only, so no provider, transport, or delivery-receipt data exists here.
      required:
        [
          id,
          workOrderId,
          appliedSlaId,
          slaClockId,
          clientId,
          buildingId,
          clockType,
          policyId,
          escalationLevelId,
          level,
          templateKey,
          breachedAt,
          dueAt,
          status,
          triggeredAt,
          cancelledAt,
          cancelReason,
          recipientsResolved,
          notificationsCreated,
          failureReason,
          createdAt,
          updatedAt,
        ]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        appliedSlaId: { $ref: "#/components/schemas/Uuid" }
        slaClockId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        clockType: { $ref: "#/components/schemas/SlaEscalationClockType" }
        policyId: { $ref: "#/components/schemas/Uuid" }
        escalationLevelId: { $ref: "#/components/schemas/Uuid" }
        level: { type: integer, minimum: 1 }
        templateKey: { type: string, description: Snapshotted; later policy edits do not rewrite it. }
        breachedAt: { type: string, format: date-time, description: The persisted breach instant this action descends from. }
        dueAt: { type: string, format: date-time, description: Absolute fire time computed once as breachedAt plus the level offset. }
        status: { type: string, enum: [PENDING, TRIGGERED, CANCELLED, SKIPPED] }
        triggeredAt: { type: string, format: date-time, nullable: true }
        cancelledAt: { type: string, format: date-time, nullable: true }
        cancelReason: { type: string, enum: [CLOCK_SATISFIED, CLOCK_TERMINATED], nullable: true }
        recipientsResolved: { type: integer, minimum: 0, description: Count only; recipient identities are never exposed. }
        notificationsCreated: { type: integer, minimum: 0 }
        failureReason: { type: string, nullable: true, maxLength: 500, description: Operator diagnostic; contains no recipient identities. }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    SlaClock:
      type: object
      description: Neutral PART 02 clock state; no pause, deadline, or breach fields exist.
      required: [id, appliedSlaId, clockType, targetMinutes, startedAt, status, satisfiedAt, terminatedAt, breachedAt, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        appliedSlaId: { $ref: "#/components/schemas/Uuid" }
        clockType: { type: string, enum: [RESPONSE, RESOLUTION] }
        targetMinutes: { type: integer, minimum: 1 }
        startedAt: { type: string, format: date-time }
        status: { type: string, enum: [RUNNING, SATISFIED, TERMINATED] }
        satisfiedAt: { type: string, format: date-time, nullable: true }
        terminatedAt: { type: string, format: date-time, nullable: true }
        breachedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        isPaused: { type: boolean }
        totalPausedMilliseconds: { type: integer, minimum: 0 }
        effectiveElapsedMilliseconds: { type: integer, minimum: 0 }
        pauseIntervals:
          type: array
          items: { $ref: "#/components/schemas/SlaClockPauseInterval" }
    SlaClockPauseInterval:
      type: object
      description: Auditable interval created only by Work Order ON_HOLD and closed by RESUMED or terminal lifecycle.
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        slaClockId: { $ref: "#/components/schemas/Uuid" }
        pausedAt: { type: string, format: date-time }
        resumedAt: { type: string, format: date-time, nullable: true }
        pauseSource: { type: string, enum: [WORK_ORDER_ON_HOLD] }
        pauseActorUserId: { type: string, format: uuid, nullable: true }
        resumeActorUserId: { type: string, format: uuid, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    AppliedSla:
      type: object
      description: Immutable definition snapshot applied prospectively to one Work Order.
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        slaDefinitionId: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        definitionCode: { type: string }
        operationalType: { type: string, enum: [WORK_ORDER] }
        definitionWorkType: { type: string, nullable: true }
        definitionPriority: { type: string, nullable: true }
        workOrderWorkType: { type: string }
        workOrderPriority: { type: string }
        responseTargetMinutes: { type: integer, nullable: true }
        resolutionTargetMinutes: { type: integer, nullable: true }
        definitionEffectiveFrom: { type: string, format: date-time }
        definitionEffectiveTo: { type: string, format: date-time, nullable: true }
        appliedAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }
        clocks: { type: array, items: { $ref: "#/components/schemas/SlaClock" } }

    # -------------------------------------------------------------------
    # CR-BE-INTEG-01 PART 06 — Integration Webhook schemas
    # -------------------------------------------------------------------
    IntegrationWebhookEndpoint:
      type: object
      description: >
        Client-scoped webhook endpoint configuration — the SECRET-FREE
        projection every read/list/update response uses. The signing secret
        is structurally absent from this schema and is disclosed only
        through `IntegrationWebhookEndpointWithSecret` on create/rotate.
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
          description: Optional Building narrowing (null = whole Client).
        name: { type: string }
        url: { type: string, description: HTTPS receiver URL. }
        eventTypes:
          type: array
          items: { type: string }
          description: Subscribed BE-07 event-type codes (uppercase).
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        secretRotatedAt:
          type: string
          format: date-time
          nullable: true
          description: Last rotate-secret time (null = never rotated).
        timeoutMs: { type: integer, minimum: 1000, maximum: 30000 }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    IntegrationWebhookEndpointWithSecret:
      allOf:
        - $ref: "#/components/schemas/IntegrationWebhookEndpoint"
        - type: object
          required: [signingSecret]
          properties:
            signingSecret:
              type: string
              description: >
                ONE-TIME disclosure of the server-generated HMAC signing
                secret (`whsec_` + 32 random bytes, base64url). Returned
                ONLY by create (201) and rotate-secret (200); it can never
                be read back through any other operation. Store it securely
                on the receiver side.
    IntegrationWebhookDelivery:
      type: object
      description: >
        One per-endpoint delivery ledger row (read-only history). Exposes
        delivery state, attempt accounting, timing, the last HTTP response
        status and a sanitized error — never signing secrets, never payload
        bodies. `id` is the receiver-facing idempotency key sent as
        `X-Asentra-Delivery-Id` (stable across retry attempts).
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        outboxEventId: { $ref: "#/components/schemas/Uuid" }
        endpointId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        eventType: { type: string, description: Source BE-07 event type. }
        status:
          type: string
          enum: [PENDING, SENDING, DELIVERED, RETRY_SCHEDULED, FAILED_PERMANENT, EXHAUSTED]
          description: >
            Lifecycle: PENDING → SENDING → DELIVERED (receiver 2xx only) /
            RETRY_SCHEDULED (retryable failure, durable retry window) /
            FAILED_PERMANENT (non-retryable) / EXHAUSTED (attempt budget
            spent). DELIVERED, FAILED_PERMANENT and EXHAUSTED are terminal.
        attemptCount: { type: integer, minimum: 0 }
        maxAttempts: { type: integer, minimum: 1 }
        nextRetryAt:
          type: string
          format: date-time
          nullable: true
          description: Due time of the next retry (RETRY_SCHEDULED only).
        lastAttemptAt: { type: string, format: date-time, nullable: true }
        lastResponseStatus:
          type: integer
          nullable: true
          description: >
            HTTP status of the latest attempt (null for network errors and
            timeouts, where no response was received).
        lastError:
          type: string
          nullable: true
          description: Sanitized failure description (never response bodies).
        deliveredAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    # ------------------------------------------------------------------
    # CR-BE-PRO-02 PART 01–06 — RFQ procurement contract
    # ------------------------------------------------------------------
    RfqSourceMode:
      type: string
      enum: [MATERIAL, SERVICE]
    RfqStatus:
      type: string
      enum: [DRAFT, OPEN, CLOSED, CANCELLED]
    RfqCurrency:
      type: string
      enum: [IDR, USD, SGD, MYR, AUD, EUR, GBP, JPY, CNY]
    Rfq:
      type: object
      description: >
        Client/Building-scoped sourcing record. RFQ, quotation, comparison,
        recommendation, award, and DRAFT PO values do not create a financial
        commitment.
      required: [id, clientId, buildingId, purchaseRequestId, sourceMode, rfqNumber, title, currency, status, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        purchaseRequestId: { $ref: "#/components/schemas/Uuid" }
        sourceMode: { $ref: "#/components/schemas/RfqSourceMode" }
        rfqNumber: { type: string }
        title: { type: string }
        description: { type: string, nullable: true }
        currency: { $ref: "#/components/schemas/RfqCurrency" }
        requiredDate: { type: string, format: date-time, nullable: true }
        responseDeadline: { type: string, format: date-time, nullable: true }
        sourceRequestNumber: { type: string }
        sourceRequestTitle: { type: string }
        sourceRequestStatus: { type: string }
        status: { $ref: "#/components/schemas/RfqStatus" }
        openedAt: { type: string, format: date-time, nullable: true }
        openedByUserId: { $ref: "#/components/schemas/Uuid" }
        closedAt: { type: string, format: date-time, nullable: true }
        closedByUserId: { $ref: "#/components/schemas/Uuid" }
        cancelledAt: { type: string, format: date-time, nullable: true }
        cancelledByUserId: { $ref: "#/components/schemas/Uuid" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateRfqRequest:
      type: object
      required: [purchaseRequestId, sourceMode, rfqNumber, title, currency, idempotencyKey]
      properties:
        purchaseRequestId: { $ref: "#/components/schemas/Uuid" }
        sourceMode: { $ref: "#/components/schemas/RfqSourceMode" }
        rfqNumber: { type: string, maxLength: 64 }
        title: { type: string, maxLength: 200 }
        description: { type: string, nullable: true }
        currency: { $ref: "#/components/schemas/RfqCurrency" }
        requiredDate: { type: string, format: date-time, nullable: true }
        responseDeadline: { type: string, format: date-time, nullable: true }
        idempotencyKey: { type: string, maxLength: 200 }
    UpdateRfqRequest:
      type: object
      minProperties: 1
      properties:
        title: { type: string }
        description: { type: string, nullable: true }
        currency: { $ref: "#/components/schemas/RfqCurrency" }
        requiredDate: { type: string, format: date-time, nullable: true }
        responseDeadline: { type: string, format: date-time, nullable: true }
    RfqAvailableActions:
      type: object
      required: [rfqId, state, availableActions]
      properties:
        rfqId: { $ref: "#/components/schemas/Uuid" }
        state: { $ref: "#/components/schemas/RfqStatus" }
        availableActions:
          type: array
          items: { type: string, enum: [OPEN, CLOSE, CANCEL] }
    RfqLine:
      type: object
      required: [id, rfqId, sourceMode, clientId, buildingId, purchaseRequestId, lineNumber, sourceDescription, sourceServiceId, quantitySnapshot, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        rfqId: { $ref: "#/components/schemas/Uuid" }
        sourceMode: { $ref: "#/components/schemas/RfqSourceMode" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        purchaseRequestId: { $ref: "#/components/schemas/Uuid" }
        lineNumber: { type: integer, minimum: 1 }
        materialRequestId: { $ref: "#/components/schemas/Uuid", nullable: true }
        serviceRequestId: { $ref: "#/components/schemas/Uuid", nullable: true }
        sourceDescription: { type: string }
        sourceItemId: { $ref: "#/components/schemas/Uuid", nullable: true }
        sourceUomId: { $ref: "#/components/schemas/Uuid", nullable: true }
        sourceServiceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            CR-BE-SVC-01 PART 04 — governed SERVICE identity snapshot derived
            from the Service Request (NULL for MATERIAL and un-governed lines;
            never introduces item/UOM/quantity).
        quantitySnapshot: { type: number, nullable: true }
        sourceRequiredDate: { type: string, format: date-time, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateRfqLineRequest:
      type: object
      required: [sourceLineType, sourceLineId]
      properties:
        sourceLineType: { type: string, enum: [MATERIAL_REQUEST, SERVICE_REQUEST] }
        sourceLineId: { $ref: "#/components/schemas/Uuid" }

    RfqInvitationStatus:
      type: string
      enum: [INVITED, ACCEPTED, DECLINED, NO_BID, EXPIRED, REVOKED, QUOTATION_SUBMITTED]
    RfqInvitation:
      type: object
      required: [id, rfqId, vendorId, clientId, buildingId, attemptNumber, status, responseDeadline, tokenExpiresAt, contactSourceType, contactSourceId, contactNameSnapshot, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        rfqId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        attemptNumber: { type: integer, minimum: 1 }
        status: { $ref: "#/components/schemas/RfqInvitationStatus" }
        responseDeadline: { type: string, format: date-time }
        tokenExpiresAt: { type: string, format: date-time }
        tokenConsumedAt: { type: string, format: date-time, nullable: true }
        contactSourceType: { type: string, enum: [VENDOR] }
        contactSourceId: { $ref: "#/components/schemas/Uuid" }
        contactNameSnapshot: { type: string }
        recipientEmailSnapshot: { type: string, format: email, nullable: true }
        viewedAt: { type: string, format: date-time, nullable: true }
        acceptedAt: { type: string, format: date-time, nullable: true }
        declinedAt: { type: string, format: date-time, nullable: true }
        noBidAt: { type: string, format: date-time, nullable: true }
        expiredAt: { type: string, format: date-time, nullable: true }
        revokedAt: { type: string, format: date-time, nullable: true }
        revokedByUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        responseReason: { type: string, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    RfqInvitationIssued:
      allOf:
        - $ref: "#/components/schemas/RfqInvitation"
        - type: object
          properties:
            invitationToken:
              type: string
              nullable: true
              description: One-time disclosure only; never returned on replay or reads.
    CreateRfqInvitationRequest:
      type: object
      required: [vendorId, idempotencyKey]
      properties:
        vendorId: { $ref: "#/components/schemas/Uuid" }
        idempotencyKey: { type: string, maxLength: 200 }
    IdempotencyRequest:
      type: object
      required: [idempotencyKey]
      properties:
        idempotencyKey: { type: string, maxLength: 200 }
    RfqTokenExchangeRequest:
      type: object
      required: [token]
      properties:
        token: { type: string, description: One-time raw invitation token, accepted only in the POST body. }
    RfqResponseReasonRequest:
      type: object
      properties:
        reason: { type: string, nullable: true, maxLength: 2000 }
        responseReason: { type: string, nullable: true, maxLength: 2000 }
    RfqSession:
      type: object
      required: [sessionId, invitationId, rfqId, vendorId]
      properties:
        sessionId: { $ref: "#/components/schemas/Uuid" }
        invitationId: { $ref: "#/components/schemas/Uuid" }
        rfqId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        status: { type: string, enum: [ACTIVE, REVOKED, EXPIRED] }
        sessionToken: { type: string, nullable: true, description: Returned only at exchange. }
    RfqSessionExchange:
      type: object
      required: [sessionToken, session, access]
      properties:
        sessionToken: { type: string }
        session: { $ref: "#/components/schemas/RfqSession" }
        access:
          type: object
          required: [invitationId, rfqId, vendorId]
          properties:
            invitationId: { $ref: "#/components/schemas/Uuid" }
            rfqId: { $ref: "#/components/schemas/Uuid" }
            vendorId: { $ref: "#/components/schemas/Uuid" }
    VendorSafeRfqLine:
      type: object
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        lineNumber: { type: integer }
        sourceMode: { $ref: "#/components/schemas/RfqSourceMode" }
        description: { type: string }
        quantity: { type: number, nullable: true, description: Null for SERVICE lines. }
        requiredDate: { type: string, format: date-time, nullable: true }
    VendorSafeInvitation:
      type: object
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        status: { $ref: "#/components/schemas/RfqInvitationStatus" }
        responseDeadline: { type: string, format: date-time }
        contactName: { type: string }
    VendorSafeRfq:
      type: object
      description: Vendor invitation-scoped RFQ projection; no peers, comparison, award, PO, or commitment data.
      properties:
        rfq:
          type: object
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            rfqNumber: { type: string }
            title: { type: string }
            description: { type: string, nullable: true }
            sourceMode: { $ref: "#/components/schemas/RfqSourceMode" }
            currency: { $ref: "#/components/schemas/RfqCurrency" }
            requiredDate: { type: string, format: date-time, nullable: true }
            responseDeadline: { type: string, format: date-time, nullable: true }
            status: { $ref: "#/components/schemas/RfqStatus" }
            lines: { type: array, items: { $ref: "#/components/schemas/VendorSafeRfqLine" } }
        invitation: { $ref: "#/components/schemas/VendorSafeInvitation" }

    VendorQuotationStatus:
      type: string
      enum: [DRAFT, SUBMITTED, WITHDRAWN]
    VendorQuotationRevisionStatus:
      type: string
      enum: [DRAFT, SUBMITTED, SUPERSEDED]
    TechnicalCompliance:
      type: string
      enum: [COMPLIANT, NON_COMPLIANT, NOT_STATED]
    VendorQuotationLine:
      type: object
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        quotationRevisionId: { $ref: "#/components/schemas/Uuid" }
        quotationId: { $ref: "#/components/schemas/Uuid" }
        rfqId: { $ref: "#/components/schemas/Uuid" }
        rfqLineId: { $ref: "#/components/schemas/Uuid" }
        sourceMode: { $ref: "#/components/schemas/RfqSourceMode" }
        lineNumberSnapshot: { type: integer }
        description: { type: string, nullable: true }
        requiredQuantitySnapshot: { type: number, nullable: true }
        requiredUomId: { $ref: "#/components/schemas/Uuid", nullable: true }
        sourceServiceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            CR-BE-SVC-01 PART 04 — governed SERVICE identity propagated from
            the RFQ line (NULL for MATERIAL and un-governed SERVICE lines; the
            Vendor cannot set it).
        quotedQuantity: { type: number, nullable: true, description: Null for SERVICE lines. }
        unitPrice: { type: number, minimum: 0, format: double }
        lineTotal: { type: number, minimum: 0, format: double }
        technicalCompliance: { $ref: "#/components/schemas/TechnicalCompliance" }
        deviationNotes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    QuotationAttachment:
      type: object
      properties:
        supportingDocumentId: { $ref: "#/components/schemas/Uuid" }
        documentId: { $ref: "#/components/schemas/Uuid" }
        parentType: { type: string, enum: [QUOTATION_REVISION] }
        parentId: { $ref: "#/components/schemas/Uuid" }
        documentNumber: { type: string }
        documentType: { type: string }
        title: { type: string }
        description: { type: string, nullable: true }
        fileReference: { type: string, nullable: true, description: Opaque storage reference, never binary content. }
        status: { type: string }
    VendorQuotationRevision:
      type: object
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        quotationId: { $ref: "#/components/schemas/Uuid" }
        rfqId: { $ref: "#/components/schemas/Uuid" }
        invitationId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        revisionNumber: { type: integer, minimum: 1 }
        status: { $ref: "#/components/schemas/VendorQuotationRevisionStatus" }
        currency: { $ref: "#/components/schemas/RfqCurrency" }
        validUntil: { type: string, format: date, nullable: true }
        leadTimeDays: { type: integer, minimum: 0, nullable: true }
        deliveryTerms: { type: string, nullable: true }
        serviceTerms: { type: string, nullable: true }
        notes: { type: string, nullable: true }
        submittedAt: { type: string, format: date-time, nullable: true }
        supersededAt: { type: string, format: date-time, nullable: true }
        totalAmount: { type: number, minimum: 0 }
        lines: { type: array, items: { $ref: "#/components/schemas/VendorQuotationLine" } }
        attachments: { type: array, items: { $ref: "#/components/schemas/QuotationAttachment" } }
    VendorQuotation:
      type: object
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        rfqId: { $ref: "#/components/schemas/Uuid" }
        invitationId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        quotationNumber: { type: string }
        status: { $ref: "#/components/schemas/VendorQuotationStatus" }
        currentRevision: { allOf: [{ $ref: "#/components/schemas/VendorQuotationRevision" }], nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateVendorQuotationRequest:
      allOf:
        - $ref: "#/components/schemas/CreateVendorQuotationRevisionRequest"
        - type: object
          properties: { quotationNumber: { type: string, maxLength: 128 } }
    CreateVendorQuotationRevisionRequest:
      type: object
      required: [currency, idempotencyKey, lines]
      properties:
        currency: { $ref: "#/components/schemas/RfqCurrency" }
        validUntil: { type: string, format: date, nullable: true }
        leadTimeDays: { type: integer, minimum: 0, nullable: true }
        deliveryTerms: { type: string, nullable: true }
        serviceTerms: { type: string, nullable: true }
        notes: { type: string, nullable: true }
        idempotencyKey: { type: string, maxLength: 200 }
        lines: { type: array, items: { $ref: "#/components/schemas/CreateVendorQuotationLineRequest" } }
    UpdateVendorQuotationRevisionRequest:
      type: object
      properties:
        currency: { $ref: "#/components/schemas/RfqCurrency" }
        validUntil: { type: string, format: date, nullable: true }
        leadTimeDays: { type: integer, minimum: 0, nullable: true }
        deliveryTerms: { type: string, nullable: true }
        serviceTerms: { type: string, nullable: true }
        notes: { type: string, nullable: true }
    CreateVendorQuotationLineRequest:
      type: object
      required: [rfqLineId, unitPrice]
      properties:
        rfqLineId: { $ref: "#/components/schemas/Uuid" }
        quotedQuantity: { type: number, minimum: 0, nullable: true }
        unitPrice: { type: number, minimum: 0 }
        description: { type: string, nullable: true }
        technicalCompliance: { $ref: "#/components/schemas/TechnicalCompliance" }
        deviationNotes: { type: string, nullable: true }
    UpdateVendorQuotationLineRequest:
      type: object
      properties:
        quotedQuantity: { type: number, minimum: 0, nullable: true }
        unitPrice: { type: number, minimum: 0 }
        description: { type: string, nullable: true }
        technicalCompliance: { $ref: "#/components/schemas/TechnicalCompliance" }
        deviationNotes: { type: string, nullable: true }
    CreateQuotationAttachmentRequest:
      type: object
      required: [documentNumber, documentType, title, fileReference]
      properties:
        documentNumber: { type: string, maxLength: 128 }
        documentType: { type: string, maxLength: 128 }
        title: { type: string, maxLength: 500 }
        description: { type: string, nullable: true }
        fileReference: { type: string, maxLength: 512, description: Opaque storage reference only. }

    PriceCatalogCurrency:
      type: string
      description: >
        Exact-match currency vocabulary; no FX conversion exists anywhere in
        the price-authority domain.
      enum: [IDR, USD, SGD, MYR, AUD, EUR, GBP, JPY, CNY]

    PriceCatalogEntryStatus:
      type: string
      description: >
        API lifecycle: DRAFT (mutable, never effective) → ACTIVE (selected by
        window) → INACTIVE (terminal; closed by replacement, correction or
        deactivation). Facts on ACTIVE/INACTIVE rows are never updated.
      enum: [DRAFT, ACTIVE, INACTIVE]

    PriceCatalogEntryKind:
      type: string
      description: >
        Derived state: `VENDOR_CONTRACT` exactly when a Vendor tier key is
        set, otherwise `REFERENCE`. Never caller-editable.
      enum: [REFERENCE, VENDOR_CONTRACT]

    PriceCatalogScopeTier:
      type: string
      description: >
        Provenance tier of a matched authority. Selection precedence is fixed:
        VENDOR_BUILDING → VENDOR → BUILDING → CLIENT_WIDE.
      enum: [VENDOR_BUILDING, VENDOR, BUILDING, CLIENT_WIDE]

    PriceCatalogSourceMode:
      type: string
      description: >
        The price subject discriminator (CR-BE-SVC-01 PART 05). MATERIAL
        prices an Inventory Item per 1 UOM; SERVICE prices a governed
        service_catalog entry per 1 service (no item, no UOM, no quantity).
        The subject shape is enforced by a discriminated-union DB CHECK.
      enum: [MATERIAL, SERVICE]

    ServiceCatalogStatus:
      type: string
      description: >
        CR-BE-SVC-01 PART 01 — reference-master lifecycle. INACTIVE is
        terminal (no reactivation lane in v1); the row is retained for
        history and references stay valid.
      enum: [ACTIVE, INACTIVE]

    ServiceCatalogEntry:
      type: object
      description: >
        CR-BE-SVC-01 PART 01 — governed Service master identity: a
        Client-scoped reference master (the SERVICE analog of
        `inventory_items`). `code` is unique per Client and immutable; it is
        byte-compatible with the legacy free-text `service_type` grammar so
        existing codes are valid catalog codes. Identity only — no
        demand/price/quantity/UOM surface.
      required:
        [id, clientId, code, name, description, category, status,
         createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        code:
          type: string
          description: >
            Stable machine-readable identity, unique per Client, immutable.
            Uppercase `/^[A-Z][A-Z0-9_-]*$/`, 2–64 chars.
        name: { type: string }
        description: { type: string, nullable: true }
        category:
          type: string
          description: Free classification (e.g. ENGINEERING, HOUSEKEEPING).
        status: { $ref: "#/components/schemas/ServiceCatalogStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateServiceCatalogEntryRequest:
      type: object
      description: >
        CR-BE-SVC-01 PART 01. `code` is normalized to uppercase and is
        immutable once created. Created ACTIVE.
      required: [clientId, code, name, category]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        code:
          type: string
          description: Uppercase `/^[A-Z][A-Z0-9_-]*$/`, 2–64 chars.
        name: { type: string }
        category: { type: string }
        description: { type: string, nullable: true, maxLength: 1000 }

    UpdateServiceCatalogEntryRequest:
      type: object
      description: >
        CR-BE-SVC-01 PART 01. `code` and `clientId` are immutable; only
        `name`, `description`, `category` are editable. `status` is not
        editable here — deactivation is the governed lifecycle transition.
      properties:
        name: { type: string }
        description: { type: string, nullable: true, maxLength: 1000 }
        category: { type: string }

    PriceCatalogLookupResolution:
      type: string
      description: >
        Total, typed outcome of the §8 resolver. Non-MATCHED outcomes are
        explicit absence classifications — never fabricated prices. SERVICE
        never emits UOM_INCOMPATIBLE (no UOM).
      enum:
        [MATCHED, NO_REFERENCE_PRICE, UOM_INCOMPATIBLE, CURRENCY_INCOMPATIBLE,
         AMBIGUOUS]

    PriceCatalogEntry:
      type: object
      description: >
        One governed reference price authority: either a MATERIAL Inventory
        Item priced per 1 unit of an explicit UOM, or a SERVICE governed
        service_catalog entry priced per 1 service (no item/UOM/quantity —
        CR-BE-SVC-01 PART 05), inside one scope tier (nullable keys at most
        one of Building and/or Vendor), with a half-open effective window
        `[effectiveFrom, effectiveTo)`. Change is replace/correct — never
        in-place mutation; `replacedByEntryId` links the closing lineage.
      required:
        [id, clientId, buildingId, vendorId, sourceMode, entryKind, itemId,
         uomId, serviceId, currency, unitPrice, effectiveFrom, effectiveTo,
         status, activatedAt, activatedByUserId, deactivatedAt,
         deactivatedByUserId, replacedByEntryId, sourceType, sourceReference,
         notes, approvedByUserId, approvedAt, createdByUserId, createdAt,
         updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Null marks a non-Building tier.
        vendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Null marks a general (non-Vendor) tier.
        sourceMode: { $ref: "#/components/schemas/PriceCatalogSourceMode" }
        entryKind: { $ref: "#/components/schemas/PriceCatalogEntryKind" }
        itemId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: MATERIAL subject (NULL for SERVICE).
        uomId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: MATERIAL UOM (NULL for SERVICE).
        serviceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            SERVICE subject — a governed service_catalog entry (NULL for
            MATERIAL).
        currency: { $ref: "#/components/schemas/PriceCatalogCurrency" }
        unitPrice:
          type: number
          exclusiveMinimum: 0
          description: >
            MATERIAL: price per 1 UOM unit. SERVICE: price per 1 service
            (the service amount; at most two decimals).
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/PriceCatalogEntryStatus" }
        activatedAt: { type: string, format: date-time, nullable: true }
        activatedByUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        deactivatedAt: { type: string, format: date-time, nullable: true }
        deactivatedByUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        replacedByEntryId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Successor link when closed by replace/correct.
        sourceType:
          type: string
          enum: [MANUAL]
          description: v1 provenance is MANUAL only; no backfill/adoption.
        sourceReference: { type: string, nullable: true }
        notes: { type: string, nullable: true }
        approvedByUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        approvedAt: { type: string, format: date-time, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreatePriceCatalogEntryRequest:
      type: object
      description: >
        DRAFT creation facts. `entryKind` is never supplied (derived);
        approved-by attribution is optional human metadata, not an engine.
        `sourceMode` defaults to MATERIAL when omitted. MATERIAL requires
        `itemId` + `uomId`; SERVICE requires `serviceId` (no item/UOM) —
        CR-BE-SVC-01 PART 05.
      required: [clientId, currency, unitPrice, effectiveFrom]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        sourceMode: { $ref: "#/components/schemas/PriceCatalogSourceMode" }
        buildingId: { $ref: "#/components/schemas/Uuid", nullable: true }
        vendorId: { $ref: "#/components/schemas/Uuid", nullable: true }
        itemId: { $ref: "#/components/schemas/Uuid", nullable: true }
        uomId: { $ref: "#/components/schemas/Uuid", nullable: true }
        serviceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        currency: { $ref: "#/components/schemas/PriceCatalogCurrency" }
        unitPrice: { type: number, exclusiveMinimum: 0 }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time, nullable: true }
        sourceReference: { type: string, nullable: true, maxLength: 200 }
        notes: { type: string, nullable: true, maxLength: 1000 }
        approvedByUserId: { $ref: "#/components/schemas/Uuid", nullable: true }

    UpdatePriceCatalogEntryRequest:
      type: object
      description: >
        DRAFT-only field edits; at least one field required. Re-scoping a
        DRAFT re-derives `entryKind` from the final Vendor key.
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid", nullable: true }
        vendorId: { $ref: "#/components/schemas/Uuid", nullable: true }
        itemId: { $ref: "#/components/schemas/Uuid", nullable: true }
        uomId: { $ref: "#/components/schemas/Uuid", nullable: true }
        serviceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        currency: { $ref: "#/components/schemas/PriceCatalogCurrency" }
        unitPrice: { type: number, exclusiveMinimum: 0 }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time, nullable: true }
        sourceReference: { type: string, nullable: true, maxLength: 200 }
        notes: { type: string, nullable: true, maxLength: 1000 }
        approvedByUserId: { $ref: "#/components/schemas/Uuid", nullable: true }

    ReplacePriceCatalogEntryRequest:
      type: object
      description: >
        Successor facts for the managed-change lane. Subject, tier keys, UOM
        and currency are inherited from the predecessor — never supplied.
        `effectiveFrom` must not open before the predecessor's window start
        (that retroactive repair belongs to `/correct`).
      required: [unitPrice, effectiveFrom]
      properties:
        unitPrice: { type: number, exclusiveMinimum: 0 }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time, nullable: true }
        sourceReference: { type: string, nullable: true, maxLength: 200 }
        notes: { type: string, nullable: true, maxLength: 1000 }
        approvedByUserId: { $ref: "#/components/schemas/Uuid", nullable: true }

    CorrectPriceCatalogEntryRequest:
      type: object
      description: >
        Corrective successor facts for the override lane
        (`price_catalog.override`). The `reason` is MANDATORY governance
        evidence: required, non-blank, ≤ 1000 chars; it is recorded in the
        `PRICE_CATALOG_ENTRY_OVERRIDE_CORRECTED` event and joins the
        idempotency fingerprint, so replaying a key with a different reason
        conflicts instead of silently reusing earlier evidence. The successor
        window MAY open before the predecessor's start (retroactive
        correction); subject, scope, UOM and currency remain inherited.
      required: [unitPrice, effectiveFrom, reason]
      properties:
        unitPrice: { type: number, exclusiveMinimum: 0 }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time, nullable: true }
        reason:
          type: string
          minLength: 1
          maxLength: 1000
          description: Mandatory, non-blank governance reason.
        sourceReference: { type: string, nullable: true, maxLength: 200 }
        notes: { type: string, nullable: true, maxLength: 1000 }
        approvedByUserId: { $ref: "#/components/schemas/Uuid", nullable: true }

    PriceCatalogLookupResult:
      type: object
      description: >
        Typed §8 resolver outcome with the full request echo. `scopeTier` and
        `entry` are present only when `resolution` is `MATCHED`. For a SERVICE
        lookup, `itemId`/`uomId` are null and `serviceId` carries the subject
        (CR-BE-SVC-01 PART 05); SERVICE never resolves to UOM_INCOMPATIBLE.
      required:
        [resolution, clientId, buildingId, vendorId, sourceMode, itemId,
         uomId, serviceId, currency, asOf, scopeTier, entry]
      properties:
        resolution: { $ref: "#/components/schemas/PriceCatalogLookupResolution" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid", nullable: true }
        sourceMode: { $ref: "#/components/schemas/PriceCatalogSourceMode" }
        itemId: { $ref: "#/components/schemas/Uuid", nullable: true }
        uomId: { $ref: "#/components/schemas/Uuid", nullable: true }
        serviceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        currency: { $ref: "#/components/schemas/PriceCatalogCurrency" }
        asOf: { type: string, format: date-time }
        scopeTier:
          allOf: [{ $ref: "#/components/schemas/PriceCatalogScopeTier" }]
          nullable: true
        entry:
          allOf: [{ $ref: "#/components/schemas/PriceCatalogEntry" }]
          nullable: true

    PurchaseOrderPriceDeviationResolution:
      type: string
      description: >
        Advisory deviation outcome vocabulary (§8 outcomes + `NOT_REQUESTED`
        for SERVICE lines). Non-MATCHED outcomes carry null facts — explicit,
        never fabricated.
      enum:
        [MATCHED, NO_REFERENCE_PRICE, UOM_INCOMPATIBLE, CURRENCY_INCOMPATIBLE,
         AMBIGUOUS, NOT_REQUESTED]

    PriceDeviationPosition:
      type: string
      description: Coarse advisory comparison of the PO unit price vs reference.
      enum: [ABOVE, BELOW, EQUAL]

    PriceCatalogLinePriceReference:
      type: object
      description: >
        Advisory reference-resolution + variance facts for one PO line. All
        fact fields are null unless `resolution` is `MATCHED`. Computed at
        read time; never persisted; never an issuance gate.
      required:
        [resolution, priceEntryId, scopeTier, scopeVendor, scopeBuilding,
         unitPrice, currency, uomId, effectiveFrom, effectiveTo,
         referenceTotal, unitVariance, totalVariance, variancePercent,
         position]
      properties:
        resolution:
          $ref: "#/components/schemas/PurchaseOrderPriceDeviationResolution"
        priceEntryId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Winning price-authority row when MATCHED.
        scopeTier:
          allOf: [{ $ref: "#/components/schemas/PriceCatalogScopeTier" }]
          nullable: true
        scopeVendor: { type: boolean, nullable: true }
        scopeBuilding: { type: boolean, nullable: true }
        unitPrice:
          type: number
          nullable: true
          description: Reference unit price per 1 reference UOM.
        currency:
          allOf: [{ $ref: "#/components/schemas/PriceCatalogCurrency" }]
          nullable: true
        uomId: { $ref: "#/components/schemas/Uuid", nullable: true }
        effectiveFrom: { type: string, format: date-time, nullable: true }
        effectiveTo: { type: string, format: date-time, nullable: true }
        referenceTotal:
          type: number
          nullable: true
          description: Reference unit price × frozen PO line quantity.
        unitVariance:
          type: number
          nullable: true
          description: PO unit price − reference unit price (2dp).
        totalVariance:
          type: number
          nullable: true
          description: PO line amount − reference total (2dp).
        variancePercent:
          type: number
          nullable: true
          description: (unitVariance / reference unitPrice) × 100 (2dp).
        position:
          allOf: [{ $ref: "#/components/schemas/PriceDeviationPosition" }]
          nullable: true

    PurchaseOrderPriceDeviationLine:
      type: object
      description: >
        One PO line's committed commercial facts (echoed, immutable) plus its
        advisory reference deviation.
      required:
        [purchaseOrderLineId, lineNumber, requestLineType, itemId, uomId,
         quantitySnapshot, unitPrice, lineAmount, reference]
      properties:
        purchaseOrderLineId: { $ref: "#/components/schemas/Uuid" }
        lineNumber: { type: integer }
        requestLineType:
          $ref: "#/components/schemas/PurchaseOrderLineRequestType"
        itemId: { $ref: "#/components/schemas/Uuid", nullable: true }
        uomId: { $ref: "#/components/schemas/Uuid", nullable: true }
        quantitySnapshot:
          type: number
          nullable: true
          description: Frozen request-line quantity; never recomputed.
        unitPrice: { type: number }
        lineAmount: { type: number }
        reference: { $ref: "#/components/schemas/PriceCatalogLinePriceReference" }

    PurchaseOrderPriceDeviation:
      type: object
      description: >
        COMPUTED advisory projection (§13.3): PO line prices vs the current
        reference authority, resolved as-of `asOf` (request time). Warn-only —
        it never gates PO creation/approval/acknowledgement/issuance, never
        rewrites PO facts, never touches RFQ comparison snapshots and is not
        settlement behavior. Available only to holders of BOTH
        `purchase_order.read` and `price_catalog.read`.
      required:
        [purchaseOrderId, poNumber, purchaseOrderStatus, clientId, buildingId,
         vendorId, currency, asOf, advisoryOnly, lines]
      properties:
        purchaseOrderId: { $ref: "#/components/schemas/Uuid" }
        poNumber: { type: string }
        purchaseOrderStatus: { $ref: "#/components/schemas/PurchaseOrderStatus" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        currency: { $ref: "#/components/schemas/PurchaseOrderCurrency" }
        asOf: { type: string, format: date-time }
        advisoryOnly:
          type: boolean
          enum: [true]
          description: Constant posture marker — advisory, never a gate.
        lines:
          type: array
          items: { $ref: "#/components/schemas/PurchaseOrderPriceDeviationLine" }

    RfqComparisonLineReference:
      type: object
      description: >
        CR-BE-PRICE-01 PART 04 advisory reference-price snapshot frozen into a
        comparison line offer at run creation (`reference_*` columns).
        Advisory, non-financial: never a rejection reason, never a winner
        signal. All fact fields are null unless `resolution` is `MATCHED`;
        SERVICE lines freeze as `NOT_REQUESTED`. Immutable once written —
        later catalog replace/correct never recomputes or mutates it.
      properties:
        priceEntryId: { $ref: "#/components/schemas/Uuid", nullable: true }
        unitPrice: { type: number, nullable: true }
        currency: { type: string, nullable: true }
        uomId: { $ref: "#/components/schemas/Uuid", nullable: true }
        scopeVendor: { type: boolean, nullable: true }
        scopeBuilding: { type: boolean, nullable: true }
        effectiveFrom: { type: string, format: date-time, nullable: true }
        resolution:
          type: string
          nullable: true
          enum:
            [MATCHED, NO_REFERENCE_PRICE, UOM_INCOMPATIBLE,
             CURRENCY_INCOMPATIBLE, NOT_REQUESTED, null]
        referenceTotal: { type: number, nullable: true }
        unitVariance:
          type: number
          nullable: true
          description: Quotation unit price − reference unit price (2dp).
        totalVariance: { type: number, nullable: true }
        variancePercent: { type: number, nullable: true }
        position: { type: string, nullable: true, enum: [ABOVE, BELOW, EQUAL, null] }

    RfqComparison:
      type: object
      description: >
        Immutable factual comparison snapshot. Lowest price fields are
        comparison facts only and never constitute a recommendation, winner, or
        award.
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        rfqId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        sourceMode: { $ref: "#/components/schemas/RfqSourceMode" }
        currency: { $ref: "#/components/schemas/RfqCurrency" }
        rfqNumber: { type: string }
        status: { type: string, enum: [SNAPSHOT] }
        snapshotAt: { type: string, format: date-time }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        evidence: { type: array, items: { $ref: "#/components/schemas/RfqComparisonEvidence" } }
        lines: { type: array, items: { $ref: "#/components/schemas/RfqComparisonLine" } }
        commercial: { $ref: "#/components/schemas/RfqComparisonCommercial" }
        evaluations: { type: array, items: { $ref: "#/components/schemas/RfqComparisonEvaluation" } }
    RfqComparisonEvidence:
      type: object
      properties:
        evidenceId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        invitationId: { $ref: "#/components/schemas/Uuid" }
        quotationId: { $ref: "#/components/schemas/Uuid" }
        quotationRevisionId: { $ref: "#/components/schemas/Uuid" }
        quotationNumber: { type: string }
        revisionNumber: { type: integer }
        submittedAt: { type: string, format: date-time }
        currency: { $ref: "#/components/schemas/RfqCurrency" }
        validUntil: { type: string, format: date, nullable: true }
        leadTimeDays: { type: integer, nullable: true }
        deliveryTerms: { type: string, nullable: true }
        serviceTerms: { type: string, nullable: true }
        notes: { type: string, nullable: true }
        totalAmount: { type: number, minimum: 0 }
        lines: { type: array, items: { $ref: "#/components/schemas/RfqComparisonEvidenceLine" } }
        attachments: { type: array, items: { $ref: "#/components/schemas/ComparisonAttachmentReference" } }
    RfqComparisonEvidenceLine:
      type: object
      properties:
        quotationLineId: { $ref: "#/components/schemas/Uuid" }
        rfqLineId: { $ref: "#/components/schemas/Uuid" }
        lineNumber: { type: integer }
        sourceMode: { $ref: "#/components/schemas/RfqSourceMode" }
        description: { type: string }
        offeredDescription: { type: string, nullable: true }
        requiredQuantity: { type: number, nullable: true }
        requiredUomId: { $ref: "#/components/schemas/Uuid", nullable: true }
        quotedQuantity: { type: number, nullable: true }
        unitPrice: { type: number }
        lineTotal: { type: number }
        technicalCompliance: { $ref: "#/components/schemas/TechnicalCompliance" }
        deviationNotes: { type: string, nullable: true }
        lineStatus: { type: string, enum: [QUOTED, MISSING] }
    ComparisonAttachmentReference:
      type: object
      properties:
        supportingDocumentId: { $ref: "#/components/schemas/Uuid" }
        documentId: { $ref: "#/components/schemas/Uuid" }
    RfqComparisonLine:
      type: object
      properties:
        rfqLineId: { $ref: "#/components/schemas/Uuid" }
        lineNumber: { type: integer }
        sourceMode: { $ref: "#/components/schemas/RfqSourceMode" }
        description: { type: string }
        requiredQuantity: { type: number, nullable: true, description: Null is authoritative for SERVICE lines. }
        requiredUomId: { $ref: "#/components/schemas/Uuid", nullable: true }
        offers: { type: array, items: { $ref: "#/components/schemas/RfqComparisonLineOffer" } }
        lowestQuotedUnitPrice: { type: number, nullable: true }
        highestQuotedUnitPrice: { type: number, nullable: true }
        priceDelta: { type: number, nullable: true }
        percentageDelta: { type: number, nullable: true, description: Null when the lowest price is zero or undefined. }
    RfqComparisonLineOffer:
      type: object
      properties:
        evidenceId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        invitationId: { $ref: "#/components/schemas/Uuid" }
        quotationId: { $ref: "#/components/schemas/Uuid" }
        quotationRevisionId: { $ref: "#/components/schemas/Uuid" }
        revisionNumber: { type: integer }
        submittedAt: { type: string, format: date-time }
        quotedQuantity: { type: number, nullable: true }
        unitPrice: { type: number }
        lineTotal: { type: number }
        technicalCompliance: { $ref: "#/components/schemas/TechnicalCompliance" }
        deviationNotes: { type: string, nullable: true }
        reference:
          allOf: [{ $ref: "#/components/schemas/RfqComparisonLineReference" }]
          description: >
            CR-BE-PRICE-01 PART 04 advisory reference snapshot, present ONLY
            when the caller holds both `rfq.read` and `price_catalog.read`
            (conjunctive default) — callers without `price_catalog.read` never
            see this key at all, and RFQ Vendor sessions never receive it.
    RfqComparisonCommercial:
      type: object
      properties:
        lowestTotalQuotations: { type: array, items: { $ref: "#/components/schemas/RfqComparisonTotalFact" } }
        lowestTotalAmount: { type: number, nullable: true }
        highestTotalAmount: { type: number, nullable: true }
        totalPriceSpread: { type: number, nullable: true }
    RfqComparisonTotalFact:
      type: object
      properties:
        evidenceId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        totalAmount: { type: number }
    CreateComparisonRequest:
      type: object
      required: [idempotencyKey]
      properties: { idempotencyKey: { type: string, maxLength: 200 } }
    CreateComparisonEvaluationRequest:
      type: object
      required: [evidenceId]
      properties:
        evidenceId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        commercialObservation: { type: string, nullable: true }
        technicalObservation: { type: string, nullable: true }
        complianceObservation: { type: string, nullable: true }
        evaluatorNote: { type: string, nullable: true }
    UpdateComparisonEvaluationRequest:
      type: object
      properties:
        commercialObservation: { type: string, nullable: true }
        technicalObservation: { type: string, nullable: true }
        complianceObservation: { type: string, nullable: true }
        evaluatorNote: { type: string, nullable: true }
    RfqComparisonEvaluation:
      type: object
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        comparisonRunId: { $ref: "#/components/schemas/Uuid" }
        evidenceId: { $ref: "#/components/schemas/Uuid" }
        rfqId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        quotationRevisionId: { $ref: "#/components/schemas/Uuid" }
        commercialObservation: { type: string, nullable: true }
        technicalObservation: { type: string, nullable: true }
        complianceObservation: { type: string, nullable: true }
        evaluatorNote: { type: string, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        updatedByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    RfqRecommendationOutcome:
      type: string
      enum: [VENDOR, NO_AWARD]
    RfqRecommendationStatus:
      type: string
      enum: [PENDING_APPROVAL, APPROVED, REJECTED, AWARDED]
    RfqRecommendation:
      type: object
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        rfqId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        comparisonRunId: { $ref: "#/components/schemas/Uuid" }
        evidenceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        quotationId: { $ref: "#/components/schemas/Uuid", nullable: true }
        quotationRevisionId: { $ref: "#/components/schemas/Uuid", nullable: true }
        invitationId: { $ref: "#/components/schemas/Uuid", nullable: true }
        vendorId: { $ref: "#/components/schemas/Uuid", nullable: true }
        outcome: { $ref: "#/components/schemas/RfqRecommendationOutcome" }
        status: { $ref: "#/components/schemas/RfqRecommendationStatus" }
        reason: { type: string }
        notes: { type: string, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateRfqRecommendationRequest:
      type: object
      required: [comparisonRunId, reason]
      properties:
        comparisonRunId: { $ref: "#/components/schemas/Uuid" }
        evidenceId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        quotationRevisionId: { $ref: "#/components/schemas/Uuid" }
        outcome: { $ref: "#/components/schemas/RfqRecommendationOutcome" }
        reason: { type: string, minLength: 1, maxLength: 4000 }
        notes: { type: string, nullable: true, maxLength: 4000 }
    RfqAward:
      type: object
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        rfqId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        recommendationId: { $ref: "#/components/schemas/Uuid" }
        approvalId: { $ref: "#/components/schemas/Uuid" }
        comparisonRunId: { $ref: "#/components/schemas/Uuid" }
        evidenceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        quotationId: { $ref: "#/components/schemas/Uuid", nullable: true }
        quotationRevisionId: { $ref: "#/components/schemas/Uuid", nullable: true }
        invitationId: { $ref: "#/components/schemas/Uuid", nullable: true }
        vendorId: { $ref: "#/components/schemas/Uuid", nullable: true }
        outcome: { $ref: "#/components/schemas/RfqRecommendationOutcome" }
        awardReason: { type: string, nullable: true }
        awardedByUserId: { $ref: "#/components/schemas/Uuid" }
        awardedAt: { type: string, format: date-time }
    CreateRfqAwardRequest:
      type: object
      properties:
        awardReason: { type: string, nullable: true, maxLength: 4000 }
    CreateRfqPoConversionRequest:
      type: object
      required: [poReadinessId, poNumber, poDate, idempotencyKey]
      properties:
        poReadinessId: { $ref: "#/components/schemas/Uuid" }
        poNumber: { type: string, maxLength: 128 }
        poDate: { type: string, format: date }
        idempotencyKey: { type: string, maxLength: 200 }
        notes: { type: string, nullable: true, maxLength: 2000 }
    RfqPoLineProvenance:
      type: object
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        conversionId: { $ref: "#/components/schemas/Uuid" }
        purchaseOrderId: { $ref: "#/components/schemas/Uuid" }
        purchaseOrderLineId: { $ref: "#/components/schemas/Uuid" }
        rfqId: { $ref: "#/components/schemas/Uuid" }
        rfqLineId: { $ref: "#/components/schemas/Uuid" }
        quotationLineId: { $ref: "#/components/schemas/Uuid" }
        quotationRevisionId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
    RfqPoConversion:
      type: object
      description: >
        Typed lineage only: RFQ → Award → Vendor → Quotation → Revision →
        existing PO → existing PO Lines. It stores no duplicate amount or
        commitment ledger.
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        awardId: { $ref: "#/components/schemas/Uuid" }
        recommendationId: { $ref: "#/components/schemas/Uuid" }
        rfqId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        comparisonRunId: { $ref: "#/components/schemas/Uuid" }
        evidenceId: { $ref: "#/components/schemas/Uuid" }
        quotationId: { $ref: "#/components/schemas/Uuid" }
        quotationRevisionId: { $ref: "#/components/schemas/Uuid" }
        invitationId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        poReadinessId: { $ref: "#/components/schemas/Uuid" }
        purchaseOrderId: { $ref: "#/components/schemas/Uuid" }
        convertedByUserId: { $ref: "#/components/schemas/Uuid" }
        convertedAt: { type: string, format: date-time }
        purchaseOrder: { $ref: "#/components/schemas/PurchaseOrder" }
        purchaseOrderLines: { type: array, items: { $ref: "#/components/schemas/PurchaseOrderLine" } }
        lineProvenance: { type: array, items: { $ref: "#/components/schemas/RfqPoLineProvenance" } }

# CR-BE-ESG-01 PART 06 — implemented ESG surfaces (no inferred endpoints)
    /esg/metric-definitions:
      get:
        operationId: listEsgMetricDefinitions
        security: [{ bearerAuth: [] }]
        description: Client-scoped ESG definitions. Requires esg.read.
        parameters: [{ name: status, in: query, schema: { type: string, enum: [ACTIVE, INACTIVE] } }]
        responses: { '200': { description: ESG metric definitions } }
      post:
        operationId: createEsgMetricDefinition
        security: [{ bearerAuth: [] }]
        description: Client-scoped definition lifecycle. Requires esg.manage.
        responses: { '201': { description: Created } }
    /esg/metric-definitions/{id}:
      get:
        operationId: getEsgMetricDefinition
        security: [{ bearerAuth: [] }]
        description: Requires esg.read.
        parameters: [{ name: id, in: path, required: true, schema: { type: string, format: uuid } }]
        responses: { '200': { description: Definition } }
      patch:
        operationId: updateEsgMetricDefinition
        security: [{ bearerAuth: [] }]
        description: Requires esg.manage.
        parameters: [{ name: id, in: path, required: true, schema: { type: string, format: uuid } }]
        responses: { '200': { description: Updated } }
    /esg/metric-definitions/{id}/deactivate:
      post:
        operationId: deactivateEsgMetricDefinition
        security: [{ bearerAuth: [] }]
        description: Requires esg.manage; historical records remain preserved.
        parameters: [{ name: id, in: path, required: true, schema: { type: string, format: uuid } }]
        responses: { '200': { description: Deactivated } }
    /esg/waste-records:
      get:
        operationId: listEsgWasteRecords
        security: [{ bearerAuth: [] }]
        description: Building-scoped waste records. Requires esg.read. Quantity is not converted between UOMs.
        responses: { '200': { description: Waste records } }
      post:
        operationId: createEsgWasteRecord
        security: [{ bearerAuth: [] }]
        description: Requires esg.manage.
        responses: { '201': { description: Created } }
    /esg/waste-records/{id}:
      get:
        operationId: getEsgWasteRecord
        security: [{ bearerAuth: [] }]
        description: Requires esg.read.
        parameters: [{ name: id, in: path, required: true, schema: { type: string, format: uuid } }]
        responses: { '200': { description: Waste record } }
    /esg/metric-values:
      get:
        operationId: listEsgMetricValues
        security: [{ bearerAuth: [] }]
        description: Building-scoped periodized values. Requires esg.read. ACTUAL, ESTIMATED, and MISSING remain visible.
        responses: { '200': { description: Metric values } }
      post:
        operationId: createEsgMetricValue
        security: [{ bearerAuth: [] }]
        description: Requires esg.manage.
        responses: { '201': { description: Created } }
    /esg/metric-values/{id}:
      get:
        operationId: getEsgMetricValue
        security: [{ bearerAuth: [] }]
        description: Requires esg.read.
        parameters: [{ name: id, in: path, required: true, schema: { type: string, format: uuid } }]
        responses: { '200': { description: Metric value } }
    /esg/kpi:
      get:
        operationId: getEsgKpi
        security: [{ bearerAuth: [] }]
        description: Building/date scoped read model with provenance. Requires esg.read.
        responses: { '200': { description: ESG KPI read model } }
    /management/esg-summary:
      get:
        operationId: getManagementEsgSummary
        security: [{ bearerAuth: [] }]
        description: Management ESG summary over accessible buildings intersected with requested buildings; includes period provenance. Requires esg.read.
        responses: { '200': { description: Management ESG summary } }


    # ------------------------------------------------------------------
    # CR-BE-FX-01 PART 02 — FX Rate Authority + Client FX Policy
    #
    # CANONICAL CONVENTION (frozen): 1 BASE = RATE x QUOTE.
    # No conversion schema exists yet: conversion is PART 03.
    # ------------------------------------------------------------------
    FxRateStatus:
      type: string
      description: >
        Frozen lifecycle. `PENDING_APPROVAL` -> `ACTIVE` | `REJECTED`;
        `ACTIVE` -> `SUPERSEDED` | `INACTIVE`. `REJECTED`, `SUPERSEDED` and
        `INACTIVE` are terminal.
      enum: [PENDING_APPROVAL, ACTIVE, REJECTED, SUPERSEDED, INACTIVE]
    FxRateType:
      type: string
      description: >
        FX-01 supports exactly one rate type. Selection is by effective date,
        not by rate type. A new type may only be added by a CR proving a
        consumer that effective-date selection cannot serve.
      enum: [REFERENCE]
    FxRateSource:
      type: string
      description: >
        The provider seam. FX-01 ships `MANUAL_TREASURY` only — no external
        provider ingestion exists. A future provider may PROPOSE a rate but may
        never activate one.
      enum: [MANUAL_TREASURY]
    FxRateEventType:
      type: string
      description: The append-only FX rate audit vocabulary.
      enum:
        - FX_RATE_CREATED
        - FX_RATE_APPROVED
        - FX_RATE_REJECTED
        - FX_RATE_SUPERSEDED
        - FX_RATE_DEACTIVATED
    FxRate:
      type: object
      description: >
        One FX rate. Convention: 1 BASE = RATE x QUOTE, so
        `baseCurrencyCode: USD`, `quoteCurrencyCode: IDR`, `rate: "16500"`
        means 1 USD = 16,500 IDR.
      required:
        - id
        - baseCurrencyCode
        - quoteCurrencyCode
        - rateType
        - rate
        - effectiveFrom
        - status
        - source
        - createdByUserId
        - createdAt
        - updatedAt
      properties:
        id: { type: string, format: uuid }
        baseCurrencyCode:
          type: string
          pattern: "^[A-Z]{3}$"
          description: One unit of this currency is the left-hand side of the convention.
        quoteCurrencyCode:
          type: string
          pattern: "^[A-Z]{3}$"
          description: The currency the rate is expressed in. Never equal to the base.
        rateType: { $ref: "#/components/schemas/FxRateType" }
        rate:
          type: string
          description: >
            QUOTE units per ONE unit of BASE, stored as NUMERIC(24,12) and
            returned as a string so a client never sees float drift. Always
            strictly positive.
          example: "16500.000000000000"
        effectiveFrom:
          type: string
          format: date-time
          description: Inclusive start of the effective window.
        effectiveTo:
          type: string
          format: date-time
          nullable: true
          description: Exclusive end of the window; null means still open.
        status: { $ref: "#/components/schemas/FxRateStatus" }
        source: { $ref: "#/components/schemas/FxRateSource" }
        sourceReference:
          type: string
          nullable: true
          description: Treasury rate-sheet or bulletin reference.
        ingestedAt:
          type: string
          format: date-time
          nullable: true
          description: Reserved for future automated ingestion; null for manual entry.
        supersedesRateId:
          type: string
          format: uuid
          nullable: true
          description: The rate this one corrects.
        supersededByRateId:
          type: string
          format: uuid
          nullable: true
          description: The rate that corrected this one.
        createdByUserId:
          type: string
          format: uuid
          description: The maker.
        approvedByUserId:
          type: string
          format: uuid
          nullable: true
          description: The checker. Never equal to `createdByUserId`.
        approvedAt: { type: string, format: date-time, nullable: true }
        rejectedByUserId: { type: string, format: uuid, nullable: true }
        rejectedAt: { type: string, format: date-time, nullable: true }
        rejectedReason: { type: string, nullable: true }
        supersededByUserId: { type: string, format: uuid, nullable: true }
        supersededAt: { type: string, format: date-time, nullable: true }
        deactivatedByUserId: { type: string, format: uuid, nullable: true }
        deactivatedAt: { type: string, format: date-time, nullable: true }
        deactivationReason: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateFxRateRequest:
      type: object
      required: [baseCurrencyCode, quoteCurrencyCode, rate, effectiveFrom, source]
      properties:
        baseCurrencyCode: { type: string, pattern: "^[A-Z]{3}$", example: USD }
        quoteCurrencyCode: { type: string, pattern: "^[A-Z]{3}$", example: IDR }
        rate:
          type: string
          description: >
            Positive plain decimal. QUOTE units per ONE unit of BASE. Exponent
            notation, separators and signed literals are rejected.
          example: "16500"
        effectiveFrom: { type: string, format: date-time }
        effectiveTo:
          type: string
          format: date-time
          nullable: true
          description: Must be strictly later than `effectiveFrom`; null = open.
        source: { $ref: "#/components/schemas/FxRateSource" }
        sourceReference:
          type: string
          maxLength: 200
          nullable: true
    SupersedeFxRateRequest:
      type: object
      description: >
        A correction. The currency pair is inherited from the incumbent and
        cannot be changed, so no currency field exists here.
      required: [effectiveFrom]
      properties:
        rate:
          type: string
          description: >
            Omit for a window-only correction, which reuses the incumbent's
            exact stored decimal.
          example: "16600"
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time, nullable: true }
        sourceReference: { type: string, maxLength: 200, nullable: true }
    FxRateReasonRequest:
      type: object
      description: Optional free-text reason for a rejection or deactivation.
      properties:
        reason:
          type: string
          maxLength: 500
          nullable: true
          description: Blank or absent means no reason given.
    FxRateSupersessionResult:
      type: object
      required: [superseded, successor]
      properties:
        superseded:
          allOf:
            - $ref: "#/components/schemas/FxRate"
          description: The incumbent, now `SUPERSEDED`.
        successor:
          allOf:
            - $ref: "#/components/schemas/FxRate"
          description: The correction, now `ACTIVE`.
    FxRateEvent:
      type: object
      description: >
        One row of the append-only rate audit ledger. Contains no Client
        ownership: an FX rate is platform-global.
      required: [id, fxRateId, eventType, occurredAt, createdAt]
      properties:
        id: { type: string, format: uuid }
        fxRateId: { type: string, format: uuid }
        eventType: { $ref: "#/components/schemas/FxRateEventType" }
        actorUserId: { type: string, format: uuid, nullable: true }
        requestId: { type: string, nullable: true }
        metadata:
          type: object
          description: >
            Provenance copied at the time of the event: pair, rate, convention,
            window, source, status and the lineage/actor fields relevant to the
            transition.
          additionalProperties: true
        occurredAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }
    ClientFxPolicy:
      type: object
      description: >
        Client-scoped FX governance. FAIL CLOSED: `fxEnabled` and
        `inversePermitted` default to false, and an empty `permittedSources`
        permits nothing. No Building override exists.
      required:
        - clientId
        - fxEnabled
        - reportingCurrencyCode
        - permittedSources
        - inversePermitted
        - createdAt
        - updatedAt
      properties:
        clientId: { type: string, format: uuid }
        fxEnabled:
          type: boolean
          description: false means FX is unavailable for this Client.
        reportingCurrencyCode:
          type: string
          pattern: "^[A-Z]{3}$"
          description: Must equal the Client base currency in FX-01.
        permittedSources:
          type: array
          description: Empty array means no rate source may be used.
          items: { $ref: "#/components/schemas/FxRateSource" }
        inversePermitted:
          type: boolean
          description: >
            Whether the governed INVERSE path may be used. Defaults to false.
            The inverse is never stored as a rate and is always recorded as an
            inverse when PART 03 applies it.
        maxStalenessDays:
          type: integer
          nullable: true
          minimum: 1
          description: null means no staleness bound beyond the effective window.
        createdByUserId: { type: string, format: uuid }
        updatedByUserId: { type: string, format: uuid }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    SetClientFxPolicyRequest:
      type: object
      required: [reportingCurrencyCode, permittedSources]
      properties:
        fxEnabled:
          type: boolean
          default: false
          description: Omitted means false — FX fails closed.
        reportingCurrencyCode:
          type: string
          pattern: "^[A-Z]{3}$"
          description: >
            Must be stated explicitly and must equal the Client base currency.
            It is never inferred or injected.
        permittedSources:
          type: array
          items: { $ref: "#/components/schemas/FxRateSource" }
        inversePermitted:
          type: boolean
          default: false
        maxStalenessDays:
          type: integer
          nullable: true
          minimum: 1
    ClientFxPolicyReadModel:
      type: object
      required: [clientId, policy, fxAvailable]
      properties:
        clientId: { type: string, format: uuid }
        policy:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/ClientFxPolicy"
          description: null when the Client has no policy. null means FX is unavailable.
        fxAvailable:
          type: boolean
          description: >
            Resolved availability. true only when a policy exists, is enabled,
            and permits at least one rate source. Absence is never permission.
        baseCurrencyCode:
          type: string
          pattern: "^[A-Z]{3}$"
          nullable: true
          description: The Client base currency, for display only.

    # ------------------------------------------------------------------
    # CR-BE-FIN-RPT-01 — Currency-safe basic financial reporting schemas
    # ------------------------------------------------------------------
    BasicFinancialCurrencyBucket:
      type: object
      required: [count, amount]
      properties:
        count:
          type: integer
          minimum: 0
          description: Count of monetary rows for this currency bucket.
        amount:
          type: number
          description: Exact-currency total amount for this bucket.

    BasicFinancialPaymentBucket:
      type: object
      required:
        [paidAmount, unpaidAmount, overdueAmount, outstandingAmount,
         unpaidCount, partiallyPaidCount, paidCount, overdueCount]
      properties:
        paidAmount:
          type: number
          description: Sum of paid amounts for this exact currency.
        unpaidAmount:
          type: number
          description: Outstanding but not yet overdue for this currency.
        overdueAmount:
          type: number
        outstandingAmount:
          type: number
        unpaidCount:
          type: integer
          minimum: 0
        partiallyPaidCount:
          type: integer
          minimum: 0
        paidCount:
          type: integer
          minimum: 0
        overdueCount:
          type: integer
          minimum: 0

    BasicFinancialByCurrencyEntry:
      type: object
      required:
        [currencyCode, tenantCharges, utilityBills, invoices,
         paidAmount, unpaidAmount, overdueAmount, outstandingAmount,
         receiptsIssued, receiptsVoid, vendorServiceCosts, basicExpenses,
         unrepresentedVendorCosts, billedIncome, receivedIncome,
         operationalCost, netBilled, netReceived]
      properties:
        currencyCode:
          type: string
          pattern: "^[A-Z]{3}$"
          description: Exact currency_code, e.g., IDR, USD.
        tenantCharges:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        utilityBills:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        invoices:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        paidAmount:
          type: number
          description: Paid amount for this exact invoice currency.
        unpaidAmount:
          type: number
        overdueAmount:
          type: number
        outstandingAmount:
          type: number
        receiptsIssued:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        receiptsVoid:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        vendorServiceCosts:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        basicExpenses:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        unrepresentedVendorCosts:
          type: object
          required: [amount]
          properties:
            amount:
              type: number
              description: Vendor costs finalized but not represented as basic expense, for this currency.
        billedIncome:
          type: number
          description: Billed income for this currency (invoices finalized).
        receivedIncome:
          type: number
          description: Received income for this currency (receipts issued).
        operationalCost:
          type: number
          description: Operational cost for this currency (basicExpenses + unrepresentedVendorCosts, exact same-currency addition).
        netBilled:
          type: number
          description: netBilled(C) = billedIncome(C) - operationalCost(C), exact same-currency subtraction.
        netReceived:
          type: number
          description: netReceived(C) = receivedIncome(C) - operationalCost(C).

    BasicFinancialUnknownByTenant:
      type: object
      required: [tenantCompanyId, tenantChargeAmount, utilityBillAmount, invoiceAmount, paidAmount, outstandingAmount]
      properties:
        tenantCompanyId:
          $ref: "#/components/schemas/Uuid"
        tenantChargeAmount:
          type: number
        utilityBillAmount:
          type: number
        invoiceAmount:
          type: number
        paidAmount:
          type: number
        outstandingAmount:
          type: number

    BasicFinancialUnknown:
      type: object
      description: >
        Historical monetary data without a governed currency snapshot. It is
        not IDR, not Client base/default currency, not reporting currency,
        excluded from byCurrency arithmetic and net calculations, never
        inferred. Per-source traceable.
      required:
        [tenantCharges, utilityBills, invoices, paidAmount, outstandingAmount,
         receiptsIssued, receiptsVoid, vendorServiceCosts, basicExpenses,
         unrepresentedVendorCosts, byTenant]
      properties:
        tenantCharges:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        utilityBills:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        invoices:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        paidAmount:
          type: object
          required: [amount, count]
          properties:
            amount: { type: number }
            count: { type: integer, minimum: 0 }
        outstandingAmount:
          type: object
          required: [amount, count]
          properties:
            amount: { type: number }
            count: { type: integer, minimum: 0 }
        receiptsIssued:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        receiptsVoid:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        vendorServiceCosts:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        basicExpenses:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        unrepresentedVendorCosts:
          type: object
          required: [amount]
          properties:
            amount: { type: number }
        byTenant:
          type: array
          items:
            $ref: "#/components/schemas/BasicFinancialUnknownByTenant"

    BasicFinancialMonetarySummary:
      type: object
      required: [byCurrency, unknown, distinctKnownCurrencies, hasUnknown, singleCurrencyCode]
      properties:
        byCurrency:
          type: object
          description: Exact-currency grouped authoritative totals.
          additionalProperties:
            $ref: "#/components/schemas/BasicFinancialByCurrencyEntry"
        unknown:
          $ref: "#/components/schemas/BasicFinancialUnknown"
        distinctKnownCurrencies:
          type: array
          description: Union of all known currencies appearing in any monetary source.
          items:
            type: string
            pattern: "^[A-Z]{3}$"
        hasUnknown:
          type: boolean
          description: True when any monetary source has historical NULL currency.
        singleCurrencyCode:
          type: string
          nullable: true
          pattern: "^[A-Z]{3}$"
          description: >
            Convenience indicator only, not base/default currency. Non-null only
            when exactly one known currency and zero UNKNOWN.

    BasicFinancialSummary:
      type: object
      description: >
        CR-BE-FIN-RPT-01 currency-safe basic financial summary. Authoritative
        totals are in monetarySummary.byCurrency per exact currency. Legacy
        scalar monetary fields are nullable per single-currency convenience
        rule: single known currency + zero UNKNOWN → populated from that
        single currency; multiple currencies OR any UNKNOWN → null and
        monetarySummary.byCurrency is authoritative. No FX conversion in base
        report; FX remains read-side optional additive only. Payment/outstanding
        amounts inherit invoice currency and are grouped by that currency.
        UNKNOWN is historical data without governed snapshot, not IDR, not
        base/default/reporting currency, excluded from byCurrency arithmetic
        and net.
      required:
        [buildingId, periodFrom, periodTo, tenantCompanyId, monetarySummary,
         tenantBilling, invoicePayment, receipts, vendorServiceCosts,
         basicExpenses, outstandingBalance, incomeVsOperationalCost]
      properties:
        buildingId:
          $ref: "#/components/schemas/Uuid"
        periodFrom:
          type: string
          nullable: true
          format: date
        periodTo:
          type: string
          nullable: true
          format: date
        tenantCompanyId:
          type: string
          nullable: true
          format: uuid
        monetarySummary:
          $ref: "#/components/schemas/BasicFinancialMonetarySummary"
        tenantBilling:
          type: object
          required: [tenantCharges, utilityBills, byTenant]
          properties:
            tenantCharges:
              type: object
              required: [count, amount, cancelledCount]
              properties:
                count: { type: integer, minimum: 0 }
                amount:
                  type: number
                  nullable: true
                  description: >
                    Nullable per single-currency convenience rule. Null when
                    multi-currency or UNKNOWN exists; otherwise single currency total.
                cancelledCount: { type: integer, minimum: 0 }
            utilityBills:
              type: object
              required: [count, amount, cancelledCount]
              properties:
                count: { type: integer, minimum: 0 }
                amount:
                  type: number
                  nullable: true
                  description: Nullable per single-currency convenience rule.
                cancelledCount: { type: integer, minimum: 0 }
            byTenant:
              type: array
              items:
                type: object
                required: [tenantCompanyId, tenantChargeAmount, utilityBillAmount, invoiceAmount, paidAmount, outstandingAmount]
                properties:
                  tenantCompanyId: { $ref: "#/components/schemas/Uuid" }
                  tenantChargeAmount:
                    type: number
                    nullable: true
                    description: Nullable when tenant has multi-currency or UNKNOWN.
                  utilityBillAmount:
                    type: number
                    nullable: true
                  invoiceAmount:
                    type: number
                    nullable: true
                  paidAmount:
                    type: number
                    nullable: true
                  outstandingAmount:
                    type: number
                    nullable: true
        invoicePayment:
          type: object
          required: [invoices, payments]
          properties:
            invoices:
              type: object
              required: [count, amount, draftCount, cancelledCount]
              properties:
                count: { type: integer, minimum: 0 }
                amount:
                  type: number
                  nullable: true
                  description: Nullable per single-currency convenience rule.
                draftCount: { type: integer, minimum: 0 }
                cancelledCount: { type: integer, minimum: 0 }
            payments:
              type: object
              required:
                [paidAmount, unpaidAmount, overdueAmount, outstandingAmount,
                 unpaidCount, partiallyPaidCount, paidCount, overdueCount]
              properties:
                paidAmount:
                  type: number
                  nullable: true
                  description: Nullable when multi-currency or UNKNOWN.
                unpaidAmount:
                  type: number
                  nullable: true
                overdueAmount:
                  type: number
                  nullable: true
                outstandingAmount:
                  type: number
                  nullable: true
                unpaidCount: { type: integer, minimum: 0 }
                partiallyPaidCount: { type: integer, minimum: 0 }
                paidCount: { type: integer, minimum: 0 }
                overdueCount: { type: integer, minimum: 0 }
        receipts:
          type: object
          required: [issued, void]
          properties:
            issued:
              type: object
              required: [count, amount]
              properties:
                count: { type: integer, minimum: 0 }
                amount:
                  type: number
                  nullable: true
                  description: Nullable per single-currency convenience rule.
            void:
              type: object
              required: [count, amount]
              properties:
                count: { type: integer, minimum: 0 }
                amount:
                  type: number
                  nullable: true
        vendorServiceCosts:
          type: object
          required: [finalized, draftCount, cancelledCount]
          properties:
            finalized:
              type: object
              required: [count, amount]
              properties:
                count: { type: integer, minimum: 0 }
                amount:
                  type: number
                  nullable: true
                  description: Nullable per single-currency convenience rule.
            draftCount: { type: integer, minimum: 0 }
            cancelledCount: { type: integer, minimum: 0 }
        basicExpenses:
          type: object
          required: [finalized, draftCount, cancelledCount]
          properties:
            finalized:
              type: object
              required: [count, amount]
              properties:
                count: { type: integer, minimum: 0 }
                amount:
                  type: number
                  nullable: true
                  description: Nullable per single-currency convenience rule.
            draftCount: { type: integer, minimum: 0 }
            cancelledCount: { type: integer, minimum: 0 }
        outstandingBalance:
          type: object
          required: [invoiceCount, amount]
          properties:
            invoiceCount: { type: integer, minimum: 0 }
            amount:
              type: number
              nullable: true
              description: Nullable when multi-currency or UNKNOWN.
        incomeVsOperationalCost:
          type: object
          required: [billedIncome, receivedIncome, operationalCost, netBilled, netReceived]
          properties:
            billedIncome:
              type: number
              nullable: true
              description: Nullable per single-currency convenience rule. Null when multi-currency or UNKNOWN.
            receivedIncome:
              type: number
              nullable: true
            operationalCost:
              type: number
              nullable: true
              description: Operational cost for single currency (basicExpenses + unrepresented), null when multi-currency or UNKNOWN.
            netBilled:
              type: number
              nullable: true
              description: netBilled = billedIncome - operationalCost per exact currency, null when multi-currency or UNKNOWN.
            netReceived:
              type: number
              nullable: true
