# Asentra Backend API — OpenAPI Contract
#
# CR-BE-API-01 PART 01 — OpenAPI Contract Foundation.
#
# This document is the AUTHORITATIVE Web/Mobile API contract for the
# Asentra Backend platform (see README "REST API").
#
# Scope note:
#   The contract is maintained incrementally by capability waves. CR-BE-PRO-02
#   PART 01–06 RFQ procurement surfaces are closed here; absence of an
#   unrelated BE-02…BE-23 path does not imply that the runtime route does not
#   exist.
#
# Source of truth: registered routes in src/modules/**/*.routes.ts and
# the DTOs in src/modules/**/*.types.ts. This file must never invent an
# endpoint that is not registered in the backend router.
#
# Server base path: the versioned API is mounted at the configured
# API_PREFIX (default /api/v1). All paths below are relative to it.
#
# Extending this file (incremental rule):
#   1. Add paths only for endpoints already registered in the router.
#   2. Reuse the shared envelope, error and pagination schemas below;
#      introduce new schemas only for genuinely new domain shapes.
#   3. Keep `servers` and `securitySchemes` untouched unless the
#      platform's base path or auth mechanism changes.

openapi: 3.0.3

info:
  title: Asentra Backend API
  version: 0.1.0
  description: |
    Asentra Facility & Housekeeping Management backend — final authority for
    persistence, security authorization, workflow, business rules, data scope
    and available actions. Consumed by asentra-web and asentra-mobile.

    Cross-cutting contract conventions (CR-BE-R2P-CONTRACT-01 PART 01):
    - versioned API prefix: API_PREFIX, default `/api/v1`;
    - every success uses `SuccessEnvelope`; every error uses `ErrorEnvelope`;
    - every HTTP response after request entry carries the server-generated
      `X-Request-ID` response header; error responses also carry the same value
      as `error.requestId`;
    - protected Building-scoped operations require an ACTIVE caller Building
      assignment; inaccessible scope returns 403 `BUILDING_ACCESS_DENIED` and
      list operations are constrained to the caller's accessible Buildings;
    - on PART 01 Purchase Order, Work Contract, procurement-binding, and Vendor
      Invoice paths, `x-required-permission` records the exact permission and
      `x-building-scoped: true` records Building isolation;
    - PART 01 PO/SPK available-actions responses use `{ <resource>Id, state,
      availableActions }`; action tokens are upper-case command names, are
      filtered for the calling user's existing manage permission and current
      business authority, and map only to registered command endpoints.
  license:
    name: Proprietary

servers:
  - url: /api/v1
    description: Versioned API base path (API_PREFIX; default /api/v1)

tags:
  - name: Handyman Requests
    description: >
      CR-HM-BE-01 RUN 3 — Handyman Request HTTP contract: thin exposure of the
      completed Run 1–2 Handyman Request authority (service authority,
      validation, idempotency and audit) through the existing Asentra route
      composition. Four endpoints only — building-scoped create/list plus
      detail and cancel. The building scope comes from the route, the actor
      only from the authenticated session, and clientId / requestNumber /
      status / operationalSurface are server-managed and never accepted from
      the caller. No quotation, inspection, approval, provider/crew,
      scheduling, permit, WorkSession, QC, BAST, invoice, payment, pricing,
      ledger or warranty surface exists here.
  - name: Front Desk
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 01A - J09 Visitor & Front-Desk core
      public contract exposure. Documents the ALREADY REGISTERED BE-13
      runtime routes for the shared Visitor identity master, Expected
      Visitors and Walk-In visits. Contract exposure only - no runtime,
      lifecycle or authorization change.
  - name: Utility
    description: >
      Existing BE-18/BE-19 and CR-BE-UTL-01 Utility contracts. Tenant lineage
      and Building analytics reuse authoritative Meter, Reading, Evidence,
      Consumption, Approval and Bill records; no parallel engine.
  - name: FX
    description: >
      CR-BE-FX-01 PART 02 — FX Rate Authority lifecycle and Client FX Policy
      governance.

      CANONICAL CONVENTION (frozen): **1 BASE = RATE x QUOTE**. `rate` is the
      number of QUOTE units equal to ONE unit of BASE, so `baseCurrencyCode:
      USD`, `quoteCurrencyCode: IDR`, `rate: "16500"` means 1 USD = 16,500 IDR.
      The rate is a ratio, not a monetary amount, and is stored as
      NUMERIC(24,12); it is always returned as a string to avoid float drift.

      LIFECYCLE (truthful, frozen): `PENDING_APPROVAL` -> `ACTIVE` | `REJECTED`;
      `ACTIVE` -> `SUPERSEDED` | `INACTIVE`; `REJECTED`, `SUPERSEDED` and
      `INACTIVE` are terminal and can never return to `ACTIVE`. A rate is created
      `PENDING_APPROVAL` and cannot be used by any consumer until a DIFFERENT
      authorized user approves it (maker-checker). A stored rate value is never
      modified: correction is a new rate plus supersession.

      This surface performs NO conversion. There is no convert/quote endpoint,
      no reporting conversion, no provider ingestion and no FX dashboard in
      FX-01. PART 03 owns the single governed conversion authority.

  - name: Health
    description: Runtime / readiness probes (public).
  - name: Authentication
    description: >
      Login, logout and the opaque session model (CR-BE-MOB-CONTRACT-01
      PART 01). `POST /auth/login` returns a cryptographically random,
      base64url `sessionToken`; only its SHA-256 hash is stored and the raw
      token is never persisted or returned again. Session lifetime is bounded
      by `expiresAt` (SESSION_TTL_MINUTES, default 480). On every protected
      request the backend hashes the presented `Authorization: Bearer <token>`,
      verifies the session is ACTIVE and unexpired, then re-validates the
      user's account state. `POST /auth/logout` revokes the current session
      (the row is marked REVOKED, never deleted). There is NO session-state /
      validate endpoint and NO token refresh/rotation endpoint: session
      validation is implicit in the authentication middleware on every
      protected route, and expiry is handled by re-login — mobile must treat
      any 401 as "re-authenticate". 401 semantics: missing/invalid
      Authorization header → `AUTHENTICATION_REQUIRED`; unknown or non-ACTIVE
      session → `INVALID_SESSION`; expired ACTIVE session → `SESSION_EXPIRED`;
      bad credentials at login → `INVALID_CREDENTIALS` (unknown email and
      wrong password are indistinguishable to avoid account enumeration).
      Repeated login failures are throttled per IP → 429 `AUTH_RATE_LIMITED`.
  - name: Effective Context
    description: Authoritative context of the authenticated user.
  - name: Client Configuration
    description: >
      BE-27A Client-scoped key/value configuration administration and ACTIVE
      effective read. No Building overrides, versioning, preview, or publish
      lifecycle.
  - name: Building Configuration
    description: >
      BE-27B Building-scoped key/value override administration and effective
      Client→Building resolution. No Module configuration, versioning, preview,
      or publish lifecycle.
  - name: Module Configuration
    description: >
      BE-27C Client/Building scoped Module enablement configuration. Effective
      state is intersected with the existing Subscription/License/Entitlement
      authority; configuration never grants commercial availability.
  - name: Feature Entitlement Configuration
    description: >
      BE-27D Client/Building Feature state under an existing Module. Effective
      Feature state cannot exceed BE-27C effective Module availability.
  - name: Navigation Registry
    description: >
      BE-27E server-driven navigation metadata filtered by scope, Module,
      Feature and Permission authority. No frontend menu implementation.
  - name: Workspace Registry
    description: >
      BE-27F server-driven workspace metadata filtered by Module, Feature,
      Navigation and Permission authority. No workspace UI implementation.
  - name: Dashboard Configuration
    description: >
      BE-27G Dashboard/Widget composition referencing approved BE-23/BE-24
      read models. Configuration only; no KPI calculation or chart rendering.
  - name: Form Administration
    description: >
      BE-27H scope-safe administration facade over existing BE-07 Form
      Templates, Sections and Fields. No second Form execution engine.
  - name: Checklist Administration
    description: >
      BE-27I scope-safe administration facade over existing BE-07 Checklist
      Templates and Items. Measurement, evidence, verification, and execution
      remain with their existing backend authorities.
  - name: Operational Settings
    description: >
      BE-27J typed, allowlisted operational metadata over BE-27A/BE-27B.
      Owning domain workflows and validation remain authoritative.
  - name: Organization Presentation
    description: >
      BE-27K Client/Building display labels and metadata referencing the
      authoritative BE-03 Organization hierarchy without changing it.
  - name: CMS Content
    description: >
      BE-27L scoped safe-text/Markdown content. Raw executable HTML/JS and
      Branding/White Label behavior are not supported.
  - name: Branding
    description: >
      BE-27M constrained Client/Building brand metadata and theme tokens using
      opaque storage references. No arbitrary CSS/JS or logo generation.
  - name: Configuration Versioning
    description: >
      BE-27N/O immutable snapshots and controlled Draft/Validate/Publish lifecycle.
  - name: Configuration Preview
    description: >
      BE-27P caller-bound, expiring PREVIEW contexts for validated non-active
      versions. Preview never changes publication or ACTIVE state.
  - name: Configuration Audit
    description: >
      BE-27Q read-only, scope-safe projection of append-only configuration
      events from the existing Operational Event authority.
  - name: BAST Lifecycle
    description: >
      CR-BE-BAST-01 canonical BE-22 BAST identity and lifecycle commands,
      immutable submissions, acceptance Sign-Off, rejection Finding linkage,
      read-only legacy compatibility/reconciliation, and Work Order closure
      readiness. Work Order remains the sole closure authority.
  - name: Vendor Invoice
    description: >
      CR-BE-COM-02 Vendor Invoice lifecycle, verification/matching,
      payment status, settlement readiness, vendor consistency cross-check,
      BAST hard gate, and end-to-end traceability.
  - name: Evidence
    description: >
      Evidence submission metadata, file upload / metadata / download, and
      removal (CR-BE-API-01 PART 03 + CR-BE-MOB-CONTRACT-01 PART 04). The
      evidence chain for mobile is: Checklist/Form execution → Evidence
      Requirement (required rules) → Evidence submission metadata → binary
      file (upload/download). Binary upload IS supported
      (`POST /evidence/:id/file` and the single-call `POST /mobile/evidence`);
      there is no offline byte-queue here (BE-25G sync carries metadata only).
  - name: Checklist & Measurement
    description: >
      Checklist template / item definition, UOM registry, and the
      measurement/UOM binding on numeric checklist items / form fields
      (CR-BE-MOB-CONTRACT-01 PART 04). These are the definitional authorities
      behind the mobile checklist execution read model
      (`GET /mobile/checklist-executions/:id` embeds items + measurement + UOM).
  - name: Evidence Requirements
    description: >
      Evidence requirement definitions (required/optional, type, min/max
      count) bound to checklist templates / items and form templates/fields
      (CR-BE-MOB-CONTRACT-01 PART 04). The required-evidence rules mobile
      enforces come from these records (also embedded in the mobile checklist
      execution read model).
  - name: Management Read Models
    description: Shared Management & Owner read-scope and response contracts (BE-24).
  - name: Operational Finance
    description: Building-scoped Operational Finance budget, binding and read-time aggregation contracts (CR-BE-FIN-01).
  - name: Reporting Export
    description: >
      Governed JSON projections, CSV/XLSX/PDF archive generation, and scoped
      report archive metadata/download contracts. Source KPIs/read models remain
      the calculation authorities; renderers do not recalculate.
  - name: Mobile Execution
    description: >
      Mobile worker execution surface (BE-25A): tasks, checklist executions,
      form instances (MOB-C07 FORM_VERSION-only, `form_instance.execute`,
      exact `generated_task_id` bind, current-shift + assignment recheck;
      evidence stays `evidence.manage`, review stays `review.manage` /
      `review.read`; Form complete/cancel never auto-mutates the generated
      task), evidence, reviews, operational events, finding workflow read
      models and QR resolution. List endpoints support the opt-in mobile
      pagination convention (?page=&pageSize=) described in
      docs/api/mobile-contract.md.
  - name: Work Requests
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 02E2 - J10 Work Request public contract
      exposure (BE-08A). Documents the ALREADY REGISTERED work-request intake
      routes. Contract exposure only - no runtime, lifecycle or authorization
      change.

      SCOPE: this is the BE-08A intake channel that exists BEFORE a formal Work
      Order. It carries no Asset, priority, assignee, execution, evidence,
      approval, SLA or notification concern, and it exposes NO Work Order
      endpoint (BE-08B owns the Work Order domain). `requestType` is a
      data-driven code string - there is no hardcoded Engineering / Housekeeping
      / Security model here.

      LIFECYCLE (truthful, taken from the runtime store, not inferred): a Work
      Request is created `OPEN` - `status` and `requestedAt` are server-assigned
      on create. Only an `OPEN` request can be updated; anything else yields 400
      `WORK_REQUEST_NOT_OPEN`. `OPEN` -> `CANCELLED` happens via
      `POST /work-requests/{id}/cancel`; cancelling an already `CANCELLED`
      request yields 400 `WORK_REQUEST_NOT_OPEN`, and cancelling a `CONVERTED`
      request yields 400 `WORK_REQUEST_TERMINAL_STATE`, because `CONVERTED` is
      terminal for this channel. `CONVERTED` is the BE-08B bridge: it is set by
      the Work Order conversion in the Work Order domain, not by any route here -
      this surface has no conversion, re-open or delete command.

      AUTHORIZATION: every route requires the bearer session plus a
      `work_request.*` permission (`work_request.read` for reads,
      `work_request.manage` for commands) AND BE-02 Building isolation - the
      Building-nested routes enforce it with `requireBuildingAccess('buildingId')`
      and the `/work-requests/{id}` routes resolve the record first (404 when
      unknown) and then require an ACTIVE caller assignment to its Building (403
      `BUILDING_ACCESS_DENIED`). Permission alone is never enough.

  - name: Work Orders
    description: >
      Work Order read surface for mobile operational users
      (CR-BE-MOB-CONTRACT-01 PART 03): the authoritative Work Order record,
      its assignee/personnel, location/asset context, priority, lifecycle
      status, completion subset, and action history. Building isolation
      (BE-02G) is enforced on every route — by-id reads resolve the Work
      Order's Building and require an ACTIVE caller assignment (403
      BUILDING_ACCESS_DENIED otherwise); the list route is Building-nested.
      `GET /work-orders/:id/actions` returns the append-only ACTION HISTORY,
      NOT an available-actions read model: there is no direct
      Work-Order available-actions endpoint — the authoritative per-caller
      `availableActions` for Work Orders is exposed only through the BE-25C
      unified feed `GET /mobile/assignments`.
  - name: Notifications
    description: >
      BE-26A notification foundation: the authenticated user's own in-app
      inbox (record, type, recipient, source/domain reference, channel,
      status, read state). Foundation only — no push/email/WhatsApp/SMS
      delivery and no create endpoint (creation is internal to the backend).
  - name: Notification Templates
    description: >
      BE-26B reusable notification templates (platform configuration): template
      key, notification type, channel, subject/title and message/body
      templates, declared template variables, and active status. Foundation
      only — no recipient resolution and no sending.
  - name: Notification Subscriptions
    description: >
      BE-26D notification event subscriptions (platform configuration): maps
      existing backend domain events to notification triggers — event type,
      notification template reference, recipient resolution rule, enabled
      status, and optional Client/Building context. Foundation only — no
      delivery and no new domain events.
  - name: Notification Reminders
    description: >
      BE-26H notification reminders (platform configuration): a time-deferred
      notification trigger — reminder reference, source/resource reference,
      recipient resolution rule, reminder_at, notification template reference,
      status and sent_at. Foundation only — no scheduler engine and no
      escalation.
  - name: Notification Escalations
    description: >
      BE-26I notification escalations (platform configuration): a time-deferred
      escalation trigger — escalation reference, source/resource reference,
      current recipient, escalation recipient rule, escalation_at/triggered_at,
      notification template reference, status and reason/notes. Foundation
      only — no scheduler engine, no workflow/approval engine, and no
      business-resource state changes.
  - name: Notification Secure Links
    description: >
      BE-26J secure notification links: an opaque, recipient-bound,
      single-purpose reference to a target resource + action. The raw token is
      returned once and never stored (only its hash); expired/revoked/exhausted
      links are rejected. Resolution does not bypass backend authorization or
      workflow rules. Foundation only — no Notification History.
  - name: Notification History
    description: >
      BE-26K notification delivery/history read model: a unified,
      chronological, recipient-scoped view over the in-app, email and
      WhatsApp delivery records. Read-only — no second audit engine, no
      credential/token exposure, and no sensitive payloads.

  - name: Material Requests
    description: >
      CR-BE-MAT-01 — BE-17B Material Request lines on a Purchase Request: the
      REQUEST AUTHORITY of the material fulfilment chain. Carries requested
      quantity, the PART 02 approved quantity (fulfilment cap) and the UOM
      context captured at creation. APPROVED lines are frozen (no silent
      mutation); correction goes through cancellation.
  - name: Material Reservations
    description: >
      CR-BE-INV-CONTROL-01 PART 01 — allocation records against an existing
      APPROVED Material Request. Reservations update the existing stock balance
      reserved quantity but do not create a demand or stock authority.
  - name: Procurement Approvals
    description: >
      BE-17D procurement approval bindings. Approving a MATERIAL_REQUEST
      binding may set an explicit approved quantity (CR-BE-MAT-01 PART 02);
      approval without an explicit quantity defaults approved = requested.
  - name: RFQ Procurement
    description: >
      CR-BE-PRO-02 RFQ, invitation-scoped Vendor responses, immutable
      comparison/evaluation evidence, manual recommendation, approval, award,
      and explicit conversion to an existing DRAFT Purchase Order. Vendor
      external sessions cannot access peer quotations, comparison, award, PO,
      or commitment data.
  - name: Receivings
    description: >
      BE-17G Receiving — the RECEIVING AUTHORITY. Material receivings bind to
      a Material Request line (PART 01), are capped by the approved quantity
      (PART 02, cumulative over-receipt rejected), snapshot the UOM (PART 04)
      and post their STOCK_IN atomically through the inventory movement
      ledger.
  - name: Inventory Stock
    description: >
      BE-16 stock movements and balances. `inventory_stock_movements` is the
      append-only STOCK-LEDGER AUTHORITY (PART 03 — a balance never changes
      without a movement); `inventory_stock_balances` is the derived current
      stock state per warehouse + item.
  - name: Work Order Material Usage
    description: >
      BE-16I Work Order Material Usage — the USAGE AUTHORITY. Controlled
      issue flow (PART 06): validated, atomic STOCK_OUT + usage with stable
      movement linkage, UOM snapshot (PART 04) and operational unit/total
      cost snapshot (PART 05 — no valuation/GL/accounting).

  - name: Service Requests
    description: >
      BE-17C Service Request lines on a Purchase Request — the service-side
      counterpart of the Material Request. Client and Building are derived
      from the parent Purchase Request.
  - name: Vendor Selection Readiness
    description: >
      BE-17E deterministic verdict on whether a Vendor may be selected for a
      Purchase or Service Request, with its individual checks.
  - name: Purchase Order Readiness
    description: >
      BE-17F — THE readiness authority of the R2P chain. READY means "may
      commit"; an ISSUED Purchase Order means "committed". No downstream
      domain re-decides this verdict.
  - name: Purchase Order
    description: >
      CR-BE-R2P-01 PART 01–03 — the COMMITMENT authority of the R2P chain.
      A Purchase Order is committed against a READY BE-17F PO Readiness
      (which remains the sole readiness authority and a precondition only),
      carries commercial lines over MR/SR request lines without creating a
      competing quantity ledger, and is ISSUED to the Vendor through a
      deterministic DRAFT → ISSUED transition.
  - name: Price Catalog
    description: >
      CR-BE-PRICE-01 — the internal reference PRICE AUTHORITY: effective-
      dated, overlap-excluded catalog entries (MATERIAL only) in fixed
      Vendor+Building → Vendor → Building → Client-wide tiers with
      exact-match UOM/currency selection. Change is governed lifecycle
      (activate / replace / deactivate / override-correct with mandatory
      reason under `price_catalog.override`), never in-place mutation.
      Internal-only surface — RFQ Vendor sessions receive nothing from it;
      usage elsewhere is advisory evidence (RFQ comparison snapshots, PO
      price-deviation projection), never a gate.
  - name: Service Catalog
    description: >
      CR-BE-SVC-01 — the governed SERVICE master identity: a Client-scoped
      reference master (the SERVICE analog of `inventory_items`) with an
      immutable code, a category, and ACTIVE/INACTIVE lifecycle. It is the
      governed anchor for Service Requests (PART 02), Vendor capability
      matching (PART 03), the SERVICE procurement lineage (PART 04), and the
      SERVICE price subject (PART 05). Identity only — no demand/price/
      quantity/UOM surface. Created ACTIVE; deactivation is terminal.
  - name: Work Contract (SPK)
    description: >
      CR-BE-R2P-01 PART 04 — the SPK (Surat Perintah Kerja) EXECUTION MANDATE
      raised against an ISSUED Purchase Order, inheriting that PO's Client,
      Building and Vendor. Its own entity — it does not duplicate the BE-17H
      Work Order Procurement Binding domain. Lifecycle
      DRAFT → ACTIVE → COMPLETED / CANCELLED.
  - name: Work Order Procurement Binding
    description: >
      BE-17H binding of a Work Order to its Purchase Request / Material
      Request / Service Request / Receiving, EXTENDED by CR-BE-R2P-01 PART 05
      with the authoritative SPK → PO → Vendor chain. Exactly one binding per
      Work Order; no parallel SPK ↔ Work Order binding domain.
  - name: Users
    description: >
      User identity and account lifecycle (CR-BE-MOB-CONTRACT-01 PART 02).
      The authenticated user's own identity is read via `GET /auth/me`
      (Effective Context tag); these management routes require `user.read` /
      `user.manage` and are the backend-authoritative source for user records
      the mobile app may need to resolve (e.g. assignee display names).
  - name: Roles & Permissions
    description: >
      Roles, permissions and their assignments (CR-BE-MOB-CONTRACT-01 PART 02).
      Default-deny RBAC is enforced by `requirePermission` (permission codes
      are `<domain>.<verb>`). The effective set for the authenticated user is
      the single source of truth exposed in `GET /auth/me` →
      `access.roles` / `access.permissions`; these routes administer the
      underlying assignments and are read-only for mobile context resolution.
  - name: Clients
    description: >
      Client (Company & Client) records (CR-BE-MOB-CONTRACT-01 PART 02).
      The reachable Client → Property → Building hierarchy for the
      authenticated user is exposed authoritatively in `GET /auth/me` →
      `context.clients`; these routes administer the Client registry.
  - name: Properties
    description: >
      Property records under a Client (CR-BE-MOB-CONTRACT-01 PART 02).
      Reachable properties are exposed in `GET /auth/me` →
      `context.clients[].properties`; these routes administer the registry.
  - name: Buildings
    description: >
      Building records under a Property (CR-BE-MOB-CONTRACT-01 PART 02).
      Building isolation (BE-02G) is enforced: by-id reads assert the caller's
      ACTIVE Building assignment (403 BUILDING_ACCESS_DENIED otherwise).
      Reachable buildings are exposed in `GET /auth/me` → `scope.buildingIds`
      and `GET /auth/me/buildings`.
  - name: Building Assignments
    description: >
      User ↔ Building assignment administration and the current user's own
      accessible Building context (CR-BE-MOB-CONTRACT-01 PART 02).
      `GET /auth/me/buildings` is authentication-only (no management
      permission); the `/users/:userId/buildings` routes require
      `building.read` / `building.manage`.
  - name: Findings
    description: >
      Finding, Rework, and Supervisor Verification surface for mobile
      operational users (CR-BE-MOB-CONTRACT-01 PART 05). The Finding record
      (BE-09) with its authoritative `status`, source binding
      (FORM_INSTANCE / CHECKLIST_EXECUTION / WORK_ORDER), location/asset
      context via Building, assignment, rework cycle, supervisor review +
      verification, closure, and audit/history. Building isolation (BE-02G)
      is enforced on every route. Workflow authority is NEVER inferred from
      `status`: the backend-authoritative `availableActions` comes from
      `GET /findings/:id/available-actions`, and supervisor verification from
      `GET/POST /mobile/verification/:targetType/:targetId` (which reuses the
      same BE-09 action authority). Supervisor capability is RBAC-based
      (`finding.read` / `finding.review` / `finding.manage` / `finding.close` /
      `finding.assign`) — there is NO hard-coded mobile role logic.
  - name: Location & Asset Context
    description: >
      QR/tag resolution and the Building → Floor → Area → Room → Space →
      Functional Location → Asset/Equipment operational context for mobile
      (CR-BE-MOB-CONTRACT-01 PART 06). QR/tag resolution is provided by the
      backend (`GET /mobile/qr/resolve/:identifier` and
      `GET /assets/resolve/:identifier`, both over the BE-05H Asset Identifier
      authority — no separate QR engine); physical device GPS/geolocation and
      geofencing are NOT backend responsibilities and are NOT modeled here.
      Location and asset references use canonical backend ids only; Building
      isolation (BE-02G) is enforced on every route (cross-Building access →
      403 BUILDING_ACCESS_DENIED; unknown/inaccessible QR → the same 404 as
      unknown, hiding existence).

  - name: Inventory Master
    description: >-
      CR-BE-MOB-01 PART 04 — existing BE-16A Inventory Item master, BE-16B
      Warehouse master and BE-16H Asset ↔ spare-part binding **reads**: the
      reference data a mobile field picker needs before issuing material
      against a Work Order. It is reference data only and carries no stock,
      price or workflow. The Work Order material chain itself was already
      published and is deliberately not repeated here: issue/usage under
      `Work Order Material Usage` (`recordWorkOrderMaterialUsage`,
      `listWorkOrderMaterialUsages`, `getWorkOrderMaterialUsage`,
      `getWorkOrderMaterialCostSummary`), the authoritative ledger and
      availability under `Inventory Stock` (stock movements STOCK_IN /
      STOCK_OUT and stock balances), the approved-quantity chain under
      `Material Requests` (`quantity`, `approvedQuantity`, `receivedQuantity`,
      `remainingQuantity`) plus `Work Order Procurement Binding`, and
      supervisor verification under `Work Orders`
      (`getWorkOrderVerification` / `submitWorkOrderVerification` /
      `closeWorkOrder…`, over the BE-07 review primitive). Building isolation
      (BE-02G) and default-deny RBAC are enforced on every route. This tag
      invents no material workflow, no inventory engine, no verification
      engine and no mobile-specific API.
  - name: Engineering
    description: >-
      CR-BE-MOB-01 PART 03 — existing BE-10 Engineering field / reading
      contract for mobile. Every BE-10 record is a binding or read model over
      an existing authority, never a second engine: equipment inspection and
      engineering checklist bindings start a shared BE-07 checklist execution;
      meter-reading and log-sheet bindings start a shared BE-07 form instance
      and write into BE-07 `form_responses`; planned maintenance (BE-10G)
      references a BE-07 Schedule, BE-07 generated Tasks and a BE-08 Work
      Order; corrective maintenance is a BE-10F breakdown plus a linked BE-08
      Work Order. PM/CM field execution is therefore the already published
      BE-08 Work Order action set and the BE-07 task operations — this tag
      adds no execution path. Evidence is the published BE-07 evidence API on
      the returned execution id (`executionType` CHECKLIST_EXECUTION or
      FORM_INSTANCE); supervisor review is the published review / mobile
      verification / Work Order verification surface; finding workflow stays
      on BE-09 keyed by `findingId`. Building isolation (BE-02G) and
      default-deny RBAC are enforced on every route. This tag invents no
      diagnosis or test-result domain, no per-reading review workflow, and no
      mobile-specific engineering API.
  - name: Security
    description: >-
      CR-BE-MOB-01 PART 02 — existing BE-12 Security and BE-21A/B Incident
      operational contract for mobile. Security Posts, Patrol Routes and
      Patrol Route Points (the checkpoints) are operational context over the
      BE-04 structure — no second location hierarchy and no checkpoint
      master. A Patrol Execution is the Security view of a BE-07 generated
      task (`id` = `taskId`); start/complete delegate to the shared BE-07
      task-execution engine and patrol assignment is the published BE-07
      task-assignment surface. Checkpoint confirmation is by canonical
      `pointId` — there is no checkpoint QR/identifier authority. Patrol
      checklist bindings only start the shared BE-07 checklist execution;
      responses, completion, evidence and verification stay on the published
      BE-07 operations. Security findings bind a Security source to an
      authoritative BE-09 Finding (workflow keyed by `findingId`), and field
      reporting for a standalone event is the BE-21A/BE-21B Incident chain.
      Building isolation (BE-02G) and default-deny RBAC are enforced on
      every route. This tag invents no patrol engine, no checkpoint domain
      and no "occurrence" domain.
  - name: Housekeeping
    description: >-
      CR-BE-MOB-01 PART 01 — existing BE-11 Housekeeping operational contract
      for mobile. Daily cleaning is a read view of BE-07 generated tasks;
      execution writes remain `startTask` / `completeTask` / `cancelTask`.
      Toilet and public-area inspections bind a Cleaning Area to a BE-07
      checklist template and start a shared checklist execution. Supervisor
      inspection and quality audit provide pass/fail/rework decisions via the
      BE-07 review primitive. Housekeeping findings bind HK sources to a
      BE-09 Finding (rework stays on finding operations). Evidence is a
      BE-11I binding over BE-07 evidence metadata. Building isolation
      (BE-02G) is enforced on every route. This tag does not invent a
      housekeeping engine, reinspection lifecycle, or consumable stock-out.
  - name: Workforce
    description: >-
      CR-BE-MOB-03 PART 01 — existing supervisor team workload & status
      contract (BE-03F reporting lines, BE-03I2 workforce reporting,
      BE-23G workforce KPI). Publish-only: every operation below is an
      existing runtime route, read-only, reusing the BE-03 Workforce /
      BE-07 Task authorities. No new domain, no mobile facade, no
      duplicate endpoint. BE-03F write routes (assign/update a reporting
      line) and the generic BE-03B/C/E administration surfaces are
      deliberately not published here — the mobile supervisor surface is
      read-only. Building isolation (BE-02G) is enforced on the KPI and
      reporting reads; the BE-03F reporting-line reads are profile-resolved
      (404 on unknown profile) and carry no BE-02G gate today — documented
      honestly, not claimed.
  - name: Attendance
    description: >-
      CR-BE-MOB-05 PART 02 — Workforce attendance self-service
      (clock-in / clock-out / current status). The backend-authoritative
      attendance record binds the authenticated Workforce Profile (BE-03C),
      the Building the profile clocked into (BE-02F/G access-asserted), and
      — when an applicable ACTIVE roster row exists — the authoritative
      `workforce_shift_assignments.id` / `shifts.id`. Identity is resolved
      from the authenticated session, never from the client; timestamps are
      set by the backend database clock only. Lifecycle: CLOCKED_IN →
      CLOCKED_OUT, at most one open clock-in per profile. Deliberately no
      payroll, timesheet, overtime, leave, absence, GPS/geofence, biometric,
      QR, offline, or supervisor-administration surface.
  - name: Security Logbook
    description: >-
      CR-BE-MOB-05 PART 03 — Security Operational Logbook. Backend-
      authoritative, Building-scoped Security logbook entries for mobile
      Security operations: create / list / detail / update (OPEN only).
      Each entry binds the authenticated Workforce Profile (BE-03C), the
      Building it belongs to (BE-02F/G access-asserted), and an optional
      existing authoritative `shift_handovers.id` in the SAME Building.
      Identity is resolved from the authenticated session, never from the
      client; `recordedAt` is set by the backend database clock only.
      Lifecycle: OPEN → CLOSED (CLOSED is terminal). Category is a fixed
      classification vocabulary only — no patrol / incident / finding
      engine is implied. Deliberately no patrol, checkpoint, incident,
      visitor, attendance, QR, GPS, push, offline, dashboard, or
      Management Read Model surface.
  - name: Corrective Action Verification
    description: >-
      CR-BE-MOB-03 PART 03 — existing BE-21J corrective-action verification
      contract for the mobile Teknisi supervisor. Publish-only: every
      operation below is an existing runtime route; a verification IS a
      shared BE-07 `reviews` row with target_type CORRECTIVE_ACTION — there
      is no new verification engine, no redesigned workflow, and the
      decision vocabulary (APPROVED / REJECTED / REWORK_REQUIRED) is the
      shared review primitive's. `corrective_action_verification.read`
      sees the context/result/history; only `.manage` may open a
      verification or record a decision. Building isolation (BE-02G) is
      asserted on the corrective action's Building for every operation.
  - name: Integration Webhooks
    description: >
      CR-BE-INTEG-01 outbound integration platform. Client-scoped webhook
      endpoint configuration (optional Building narrowing) plus the read-only
      per-endpoint delivery history over the transactional-outbox pipeline
      (`operational_events` → outbox snapshot → fan-out → HMAC-signed POST
      with bounded retry). SIGNING SECRETS are server-generated (`whsec_` +
      32 random bytes, base64url) and disclosed EXACTLY ONCE — in the create
      (201) and rotate-secret responses; no other read/list/update response
      ever carries a secret, and delivery history exposes state / attempt /
      timing / response metadata only (never payload bodies).

      Outbound request contract (receiver side): every delivery is an HTTPS
      POST whose body is the canonical event envelope
      `{ id, type, occurredAt, clientId, buildingId, entity{type,id},
      summary, metadata }` serialized once at enqueue time (byte-stable
      across attempts and endpoints), with headers
      `Content-Type: application/json`, `User-Agent: Asentra-Webhook/1`,
      `X-Asentra-Timestamp` (Unix seconds at send time),
      `X-Asentra-Delivery-Id` (stable idempotency key across retries),
      `X-Asentra-Event-Id`, `X-Asentra-Event-Type`, and
      `X-Asentra-Signature: v1=<hex>`.

      Receiver verification recipe: (1) read the RAW request body bytes —
      never re-serialize parsed JSON; (2) compute
      `HMAC_SHA256(secret, "<X-Asentra-Timestamp>.<X-Asentra-Delivery-Id>."
      + raw_body)` and hex-encode it; (3) compare against the value after
      `v1=` with a constant-time comparison; (4) reject when
      `|now − X-Asentra-Timestamp|` exceeds 300 seconds (replay protection);
      (5) de-duplicate on `X-Asentra-Delivery-Id` — retries reuse the same
      delivery id and byte-identical body by design (at-least-once
      delivery); (6) respond 2xx to acknowledge. Any non-2xx is a failure:
      408/425/429/5xx and timeouts are retried with exponential backoff
      (5 attempts); other 4xx and 3xx (redirects are never followed) are
      permanent. Per-endpoint ordering is NOT guaranteed — key on
      `occurredAt`/event id, not arrival order.

  - name: Basic Financial Reporting
    description: >
      CR-BE-FIN-RPT-01 — Currency-safe basic financial reporting. BE-19I
      lightweight query/read-model aggregation only; no persisted ledger or
      accounting model. All monetary totals are grouped by exact
      currency_code (currency for utility_bills). No cross-currency
      arithmetic without explicit conversion. UNKNOWN currency (historical
      NULL) is readable, countable, amount-measurable, source-traceable,
      excluded from known-currency totals and net arithmetic, surfaced
      explicitly as reporting gap. Payment/outstanding amounts inherit invoice
      currency and are grouped by that currency. Single known currency + zero
      UNKNOWN may populate legacy scalar convenience fields; otherwise those
      scalars are null and monetarySummary.byCurrency is authoritative. No FX
      conversion in base report; FX remains read-side optional additive only.

  - name: Tenant Service Requests
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 02B - J10 Tenant Service Request
      public contract exposure (BE-14E). Documents the ALREADY REGISTERED
      tenant service-request intake routes and their BE-08 conversion
      bindings. Contract exposure only - no runtime, lifecycle or
      authorization change.

      LIFECYCLE (truthful, taken from the runtime store, not inferred): a
      request is created `OPEN`; `OPEN` -> `CONVERTED` happens when its Work
      Request binding is created (`POST .../work-request`); `OPEN` ->
      `CANCELLED` happens via `POST .../cancel`. Only an `OPEN` request can be
      updated or cancelled - anything else yields 400
      `TENANT_SERVICE_REQUEST_NOT_OPEN`. The Work Order binding
      (`POST .../work-order`) requires an existing Work Request binding and
      does NOT itself change `status`. `CANCELLED` and `CONVERTED` are not
      re-openable: there is no re-open, no approval workflow, no tenant-side
      authorization endpoint and no work-order generation beyond the two
      explicit binding commands.

      AUTHORIZATION: every route requires a `tenant_company.*` permission
      (`tenant_company.read` for reads, `tenant_company.manage` for commands)
      plus BE-02 isolation - Client access on the tenant-company-nested routes
      and Building access on the record. The per-caller command set is
      backend-authoritative and comes from `GET
      /tenant-service-requests/{id}/available-actions`; clients must never
      infer an available action from `status` alone.

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 02B - J10 Tenant Service Requests.
  #
  # Contract exposure of the ALREADY REGISTERED BE-14E runtime routes only.
  # No runtime, lifecycle, authorization or work-order-generation behaviour is
  # added or changed here.
  # ---------------------------------------------------------------------------

  - name: Tenant Complaints
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 02C - J10 Tenant Complaint public
      contract exposure (BE-14F). Documents the ALREADY REGISTERED tenant
      complaint intake routes and their BE-09 Finding / BE-08 Work Order
      bindings. Contract exposure only - no runtime, lifecycle or authorization
      change.

      LIFECYCLE (truthful, taken from the runtime store, not inferred): a
      complaint is created `OPEN`; `OPEN` -> `ESCALATED` happens when its
      Finding binding is created (`POST .../finding`); `OPEN` -> `CANCELLED`
      happens via `POST .../cancel`. Only an `OPEN` complaint can be updated or
      cancelled - anything else yields 400 `TENANT_COMPLAINT_NOT_OPEN`. The Work
      Order binding (`POST .../work-order`) requires an existing Finding binding
      and does NOT itself change `status`. There is no re-open. `ESCALATED`
      means exactly "a Finding is bound" - these routes expose no SLA or
      escalation timer, no assignment workflow, no tenant approval step, no
      notification behaviour and no resolution lifecycle of their own.

      ACTION VOCABULARY: `available-actions` may return the intake commands
      (`UPDATE`, `CANCEL`, `CREATE_FINDING`), `CREATE_WORK_ORDER`, and the
      BE-09 Finding lifecycle tokens of the bound Finding. This route surface
      executes none of the Finding tokens - it has no endpoint for them; they
      are reported so a client can render the bound Finding's state correctly.

      AUTHORIZATION: every route requires a `tenant_company.*` permission
      (`tenant_company.read` for reads, `tenant_company.manage` for commands)
      plus BE-02 isolation - Client access on the tenant-company-nested routes
      and Building access on the record. The per-caller command set is
      backend-authoritative and comes from `GET
      /tenant-complaints/{id}/available-actions`; clients must never infer an
      available action from `status` alone.

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 02C - J10 Tenant Complaints.
  #
  # Contract exposure of the ALREADY REGISTERED BE-14F runtime routes only.
  # No runtime, lifecycle, authorization, escalation or work-order-generation
  # behaviour is added or changed here.
  # ---------------------------------------------------------------------------

  - name: Tenant Utility Requests
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 02D - J10 Tenant Utility Request public
      contract exposure (BE-14G). Documents the ALREADY REGISTERED tenant
      utility-request intake routes and their BE-08 conversion bindings.
      Contract exposure only - no runtime, lifecycle or authorization change.

      SCOPE WARNING FOR CONSUMERS: despite the name, this module is an INTAKE
      and conversion-binding surface only. It performs NO utility calculation,
      NO meter reading, NO billing or invoice behaviour, NO verification or
      reconciliation, NO approval workflow, NO SLA handling and NO notification.
      Those live in the separate BE-18/BE-19 Utility surfaces and are not
      reachable from these routes. A Tenant Utility Request carries a
      free-form `requestDetails` payload plus a data-driven `utilityType` code;
      it holds no meter, tariff, reading, consumption or monetary field.

      LIFECYCLE (truthful, taken from the runtime store, not inferred):
      `OPEN` -> `CONVERTED` happens when the Work Request binding is created
      (`POST .../work-request`); `OPEN` -> `CANCELLED` happens via
      `POST .../cancel`. Only an `OPEN` utility request can be updated or
      cancelled - anything else yields 400
      `TENANT_UTILITY_REQUEST_NOT_OPEN`. The Work Order binding
      (`POST .../work-order`) requires an existing Work Request binding and does
      NOT itself change `status`. There is no re-open.

      AUTHORIZATION: every route requires a `tenant_company.*` permission
      (`tenant_company.read` for reads, `tenant_company.manage` for commands),
      taken from this module's own route registrations. The per-caller command
      set is backend-authoritative and comes from `GET
      /tenant-utility-requests/{id}/available-actions`; clients must never infer
      an available action from `status` alone.

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 02D - J10 Tenant Utility Requests.
  #
  # Contract exposure of the ALREADY REGISTERED BE-14G runtime routes only.
  # No runtime, lifecycle, authorization or work-order-generation behaviour is
  # added or changed here.
  # ---------------------------------------------------------------------------

  - name: Tenant Communications
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 02E1 - J10 Tenant Communication public
      contract exposure (BE-14K). Documents the ALREADY REGISTERED tenant
      communication routes. Contract exposure only - no runtime, lifecycle,
      authorization or delivery change.

      SCOPE WARNING FOR CONSUMERS: this is an operational RECORD surface only.
      It records that a communication exists between the Tenant and an addressed
      recipient. It performs NO actual delivery: there is no WhatsApp, email or
      SMS transport, no provider integration, no retry, no delivery receipt, no
      chat/thread, no message pagination and no attachment field. Nothing leaves
      the platform through these routes.

      LIFECYCLE (truthful, taken from the runtime store, not inferred): `status`
      is server-assigned and is never caller-supplied - the create payload
      carries no `status`, and `status`, `sentAt` and `readAt` are immutable on
      update. The status vocabulary is exactly `DRAFT`, `SENT`, `READ`. Only a
      `DRAFT` communication can be updated or sent - anything else yields 400
      `TENANT_COMMUNICATION_NOT_DRAFT`. `DRAFT` -> `SENT` happens only via
      `POST /tenant-communications/{id}/send` (which records `sentAt`); `SENT`
      -> `READ` happens only via `POST /tenant-communications/{id}/read` (which
      records `readAt`), where a communication that was never sent yields 400
      `TENANT_COMMUNICATION_NOT_SENT` and an already read one yields 409
      `TENANT_COMMUNICATION_ALREADY_READ`. There is no re-open, no recall, no
      delete, no threading and no automatic transition of any kind.

      AUTHORIZATION: every route requires a `tenant_company.*` permission taken
      from this module's own route registrations - `tenant_company.read` for the
      reads and `tenant_company.manage` for the writes - plus BE-02 isolation
      (Client access on the tenant-company-nested routes and on the record).
      Denials in this module are `PERMISSION_DENIED` (permission missing),
      `BUILDING_ACCESS_DENIED` (BE-02 context isolation) and
      `TENANT_COMMUNICATION_UNAUTHORIZED` (the acting User is not authorized for
      that specific communication action). This module exposes NO
      `available-actions` endpoint: clients must not infer a permitted action
      from `status` alone.

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 02E1 - J10 Tenant Communications.
  #
  # Contract exposure of the ALREADY REGISTERED BE-14K runtime routes only.
  # No runtime, lifecycle, authorization, delivery-transport or notification
  # behaviour is added or changed here.
  # ---------------------------------------------------------------------------

  - name: Permits
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 03A - J11 Permit foundation public
      contract exposure (BE-20A). Documents the ALREADY REGISTERED Permit
      foundation routes that form the single shared identity for Vendor and
      Tenant Contractor permits. Contract exposure only - no runtime, lifecycle
      or authorization change.

      SCOPE (BE-20A foundation only): this tag covers the Permit record
      itself. One shared Permit domain for Vendor and Tenant Contractors.
      Carries `permitNumber` (unique per Client), `permitType` (data-driven
      code), `title`, `workDescription`, `applicantReference` and the resolved
      contractor context (`contractorContextType` VENDOR_CONTRACTOR |
      TENANT_CONTRACTOR with `contractorContextId`; resolved to
      `contractorVendorId` + `tenantContractorRelationshipId`). `clientId` and
      `buildingId` are resolved by the BE-20B contractor resolver from that
      context and are never caller-supplied; `status`, `requestedAt`,
      `createdByUserId`, `cancelledAt` and `cancelledByUserId` are
      server-assigned. Later BE-20 workflows (applications, approvals, safety,
      work contexts, validities, workers, equipment, evidence, lifecycle)
      must build on these records rather than add another Permit domain.

      LIFECYCLE (truthful, taken from the runtime store, not inferred): a
      Permit is created `DRAFT`; `DRAFT` -> `CANCELLED` happens via `POST
      /permits/{id}/cancel`. Only a `DRAFT` Permit can be updated (`PATCH
      /permits/{id}`) or cancelled - anything else yields 400
      `PERMIT_UPDATE_NOT_ALLOWED` or `PERMIT_CANCEL_NOT_ALLOWED`. There is no
      approval, execution, submission, validity or re-open on this surface;
      `CONVERTED` and other permit-application lifecycles belong to later
      PARTs and are not exposed here.

      AUTHORIZATION: every route requires the bearer session plus a `permit.*`
      permission (`permit.read` for reads, `permit.manage` for commands) AND
      BE-02 Building isolation - reads resolve the record first (404 when
      unknown) and then require an ACTIVE caller assignment to its Building
      (403 `BUILDING_ACCESS_DENIED`); the list operation is constrained to the
      caller's accessible Buildings. Permission alone is never enough. No
      available-actions endpoint is exposed on this surface.

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03A - J11 Permits.
  #
  # Contract exposure of the ALREADY REGISTERED BE-20A runtime routes only.
  # No runtime, lifecycle, authorization or contractor-context behaviour is
  # added or changed here.
  # ---------------------------------------------------------------------------

  - name: Permit Applications
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 03B - J11 Permit Application public
      contract exposure (BE-20C). Documents the ALREADY REGISTERED Permit
      Application routes that layer a work-request window onto an existing
      BE-20A Permit. Contract exposure only - no runtime, lifecycle or
      authorization change.

      SCOPE (BE-20C only): this tag covers the Permit Application record.
      An Application references one BE-20A Permit (permitId) and carries
      `requestedWorkAt` (future ISO-8601 date-time with timezone) plus
      optional `notes`. `applicantReference`, `contractorContextType`,
      `contractorContextId` and `workDescription` are optional client
      assertions validated against the Permit and never copied into the
      application row; `permitReference`, `permitStatus`, `clientId`,
      `buildingId`, `contractorVendorId`, `tenantContractorRelationshipId`
      and `applicantReference` projections are authoritative from the Permit.
      `status`, `submittedAt`, `submittedByUserId`, `cancelledAt`,
      `cancelledByUserId` and `createdByUserId` are server-assigned. One
      Application per Permit (409 `PERMIT_APPLICATION_ALREADY_EXISTS`);
      `requestedWorkAt` must be a future instant (400
      `PERMIT_APPLICATION_INVALID_WORK_DATE`). Later modules (approvals,
      safety, validity, workers, equipment, evidence, lifecycle) must build
      on these records rather than add another application domain.

      LIFECYCLE (truthful, taken from the runtime store, not inferred): an
      Application is created `DRAFT`; `DRAFT` -> `SUBMITTED` happens via
      `POST /permit-applications/{id}/submit`; `DRAFT` -> `CANCELLED` and
      `SUBMITTED` -> `CANCELLED` happen via `POST
      /permit-applications/{id}/cancel`. Only a `DRAFT` Application can be
      updated (`PATCH /permit-applications/{id}`) or submitted - anything
      else yields 400 `PERMIT_APPLICATION_UPDATE_NOT_ALLOWED` or 400
      `PERMIT_APPLICATION_SUBMIT_NOT_ALLOWED`; only `DRAFT` or `SUBMITTED`
      can be cancelled else 400 `PERMIT_APPLICATION_CANCEL_NOT_ALLOWED`.
      There is no approval, issuance, safety or execution state on this
      surface.

      AUTHORIZATION: every route requires the bearer session plus a `permit.*`
      permission (`permit.read` for reads, `permit.manage` for commands) AND
      BE-02 Building isolation - reads resolve the Application's Permit
      building first (404 when unknown) and then require an ACTIVE caller
      assignment to that Building (403 `BUILDING_ACCESS_DENIED`); the list
      operation is constrained to the caller's accessible Buildings.
      Permission alone is never enough. No available-actions endpoint is
      exposed on this surface.

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03B - J11 Permit Applications.
  #
  # Contract exposure of the ALREADY REGISTERED BE-20C runtime routes only.
  # No runtime, lifecycle, authorization or contractor-context behaviour is
  # added or changed here.
  # ---------------------------------------------------------------------------

  - name: Permit Approvals
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 03C - J11 Permit Approval public
      contract exposure (BE-20F). Documents the ALREADY REGISTERED thin
      Permit approval binding over the shared review primitive and BE-09-style
      authority. Contract exposure only - no runtime, lifecycle or
      authorization change.

      SCOPE (BE-20F only): this tag covers the Permit Approval record. An
      Approval is a thin Permit binding over a shared `reviews` row. It
      references one `SUBMITTED` BE-20C Permit Application (via
      `POST /permit-applications/{applicationId}/approvals`) and owns its own
      `approvalStage`/`approvalType` (data-driven codes, unique per
      Application) and `approverUserId` (must be active, Building-authorized
      and hold `permit.approve`). `reviewId`, `permitId`, `permitReference`,
      `clientId`, `buildingId`, `applicationStatus` and `permitStatus` are
      projections; `reviewStatus` PENDING|COMPLETED and `decision`
      APPROVED|REJECTED|REWORK_REQUIRED live on the review. One stage/type per
      Application (409 `PERMIT_APPROVAL_ALREADY_EXISTS`).

      LIFECYCLE (truthful, taken from the runtime store, not inferred).
      Created `PENDING` (review PENDING, null decision). The caller-specific
      allowed command is resolved by
      `GET /permit-approvals/{id}/available-actions` - clients must not infer
      from `status`. `PENDING` -> `APPROVED` via
      `POST /permit-approvals/{id}/approve` (optional `decisionNotes`); `PENDING`
      -> `REJECTED` via `POST /permit-approvals/{id}/reject` (required
      `decisionNotes`/`notes`); `PENDING` -> `REWORK_REQUIRED` via
      `POST /permit-approvals/{id}/request-rework` (required
      `decisionNotes`/`notes`). An already decided approval yields 409
      `PERMIT_APPROVAL_ALREADY_DECIDED`; deciding as non-assigned approver
      yields 403 `PERMIT_APPROVAL_UNAUTHORIZED_APPROVER` (or 403
      `PERMIT_APPROVAL_ACTION_NOT_ALLOWED` when the action is not offered).
      `PENDING` requires `SUBMITTED` Application and READY safety (400
      `PERMIT_APPROVAL_CONTEXT_INVALID` / 400 `PERMIT_APPROVAL_SAFETY_NOT_READY`
      otherwise).

      AUTHORIZATION: every route requires the bearer session plus a `permit.*`
      permission - `permit.read` for context/pending/available-actions/get,
      `permit.manage` for create, `permit.approve` for the three decision
      commands - AND BE-02 Building isolation (403 `BUILDING_ACCESS_DENIED`);
      approver eligibility additionally requires active user + `permit.approve`
      (400 `PERMIT_APPROVAL_APPROVER_INVALID`). Permission alone is never
      enough. This is not a generic configurable approval engine.

  - name: Permit Equipment
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D1 - J11 Permit Equipment public
      contract exposure (BE-20I). Documents the ALREADY REGISTERED
      permit-bound equipment bindings over the existing Asset / Equipment
      foundations. Contract exposure only - no runtime, lifecycle or
      authorization change.

      SCOPE (BE-20I only): this tag covers the Permit Equipment record. An
      Equipment entry binds one ACTIVE Asset (plus optional Equipment Profile
      anchor, Asset Identifier, Asset Certification and Inspection
      binding/execution pair) to one `SUBMITTED` Permit Application and its
      open Permit validity. `permitId`, `permitReference`, `buildingId`,
      `contractorContextType`/`contractorVendorId`, `application` identity
      and `validity` window are resolved from the Permit/Application; asset
      fields (`assetCode`, `assetName`, `assetStatus`, `equipmentCode`,
      `equipmentName`, `identifierReference`, `certificationReference`,
      `inspectionStatus` etc) are projections from the referenced
      foundations. `validFrom`/`validUntil` default to the Permit validity
      and must be ordered and contained (400
      `PERMIT_EQUIPMENT_INVALID_VALIDITY` otherwise). One ACTIVE entry per
      (`permitApplicationId`, `assetId`) is allowed (409
      `PERMIT_EQUIPMENT_ALREADY_ACTIVE`); backend-resolved fields like
      `equipmentName`, `equipmentType`, `identifierReference`, `status`,
      `deactivatedAt` must not be supplied on add.

      LIFECYCLE (truthful, taken from the runtime store, not inferred):
      created `ACTIVE`; `ACTIVE` -> `INACTIVE` via
      `POST /permit-equipment/{id}/deactivate` - only an `ACTIVE` entry can
      be deactivated (400 `PERMIT_EQUIPMENT_DEACTIVATE_NOT_ALLOWED`
      otherwise). Only an `ACTIVE` entry on an open Permit (`PENDING`|`VALID`
      validity) may be updated (400
      `PERMIT_EQUIPMENT_UPDATE_NOT_ALLOWED` otherwise). `active` resolution
      via `GET /permits/{permitId}/equipment/active` filters to
      `eligibleForActiveWork` and returns `active: false` when validity is
      not `VALID`. This is not an Asset Management, Inventory, Maintenance
      or Inspection engine.

      AUTHORIZATION: every route requires the bearer session plus a
      `permit.*` permission - `permit.read` for get/list/active,
      `permit.manage` for add/update/deactivate - AND BE-02 Building
      isolation (403 `BUILDING_ACCESS_DENIED`); contractor, Building, asset
      and certification/inspection mismatches yield 400
      `PERMIT_EQUIPMENT_*` codes. Permission alone is never enough.

  - name: Permit Evidence
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D2 - J11 Permit Evidence public
      contract exposure (BE-20J). Documents the ALREADY REGISTERED
      permit-bound evidence bindings over the existing BE-07 Evidence
      Requirement / Submission foundations. Contract exposure only - no
      runtime, lifecycle or authorization change.

      SCOPE (BE-20J only): this tag covers two records. A Permit Evidence
      Requirement is a per-Permit, per-`evidenceType` (`PHOTO`|`DOCUMENT`|
      `SIGNATURE`) requirement with `required` flag and `minimumCount`/
      `maximumCount` window (required `minimumCount` >=1, null maximum means
      unbounded, otherwise `maximumCount` >= `minimumCount`). One ACTIVE
      requirement per (`permitId`, `evidenceType`) (409
      `PERMIT_EVIDENCE_REQUIREMENT_ALREADY_EXISTS`). A Permit Evidence
      submission is a file-reference metadata row
      (`fileReference`, `originalFileName`, `mimeType`, `fileSize` 0..52428800,
      optional `capturedAt` ISO with timezone) bound to one Requirement and one
      Permit; `evidenceType` must match the Requirement (400
      `PERMIT_EVIDENCE_TYPE_MISMATCH`). Evidence counts against its Requirement
      and maximum violations yield 400 `PERMIT_EVIDENCE_COUNT_VIOLATION`. All
      `buildingId`/`contractorContextType`/`contractorContextId` assertions are
      validated against the Permit's resolved contractor Building context (400
      `PERMIT_EVIDENCE_CONTEXT_MISMATCH`). This is NOT a generic document
      management, retention, hash, object-storage, OCR or virus-scan surface;
      it stores `fileReference` metadata only - no multipart/binary upload.

      LIFECYCLE (truthful, taken from the runtime store, not inferred):
      Requirements are created `ACTIVE` via
      `POST /permits/{permitId}/evidence-requirements` (requires `permit.manage`
      + ACTIVE Building assignment). Evidence submissions are created `ACTIVE`
      via `POST /permits/{permitId}/evidence` (201) and are removed via
      `PATCH /permit-evidence/{id}` which flips `ACTIVE` -> `REMOVED` (200;
      already `REMOVED` yields 409 `PERMIT_EVIDENCE_ALREADY_REMOVED`). No
      approval, verification or retention workflow is exposed here. Readiness
      is a derived view via `GET /permits/{permitId}/evidence-readiness`
      (`configured`/`ready`/`missingEvidenceTypes`/`requirements` with
      `activeCount`/`satisfied`).

      AUTHORIZATION: every route requires the bearer session plus a
      `permit.*` permission - `permit.read` for list/get/readiness,
      `permit.manage` for create-requirement / submit / remove - AND BE-02
      Building isolation (403 `BUILDING_ACCESS_DENIED`). Permission alone is
      never enough.

  - name: Permit Safety Requirements
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 03E1 - J11 Permit Safety Requirement
      public contract exposure (BE-20E). Documents the ALREADY REGISTERED PTW
      safety prerequisite bindings. Contract exposure only - no runtime,
      lifecycle or authorization change.

      SCOPE (BE-20E only): this tag covers the Permit Safety Requirement
      record. A requirement is a per-Application (via
      `POST /permit-applications/{applicationId}/safety-requirements`) or
      per-Permit (via `POST /permits/{permitId}/safety-requirements`) PTW
      prerequisite bound to a `workType` (controlled work-type code) and
      `requirementType` (data-driven code, unique per Application, 2-64
      uppercased). Required fields: `buildingId` (must match the
      Application/Permit Building), `workType`, `requirementType`,
      `requirementDescription` (1..2000, trimmed). Optional: `required`
      (default `true`), `readinessStatus` (`PENDING`|`READY`|`NOT_READY`|
      `NOT_REQUIRED`, defaults to `PENDING`), `notes` (2000, empty->null),
      `reference` (500), and shared-control bindings `checklistTemplateId`,
      `checklistExecutionId`, `evidenceRequirementId` (all nullable, revalidated
      as shared controls; invalid yields 400
      `PERMIT_SAFETY_SHARED_CONTROL_INVALID`). One requirement per
      (`permitApplicationId`, `requirementType`) is allowed (409
      `PERMIT_SAFETY_REQUIREMENT_ALREADY_EXISTS`). This is NOT a generic HSE,
      HIRADC, JSA, TRA, LOTO, PPE detection, incident or permit-readiness
      engine; satisfying a requirement does not automatically make a permit
      READY unless the derived readiness view explicitly says so.

      LIFECYCLE (truthful, taken from the runtime store, not inferred):
      created via the two POST routes (201). Listed via
      `GET /permit-applications/{applicationId}/safety-requirements`,
      `GET /permits/{permitId}/safety-requirements` and the filtered
      `GET /safety-requirements`. Readiness is derived via
      `GET /permits/{permitId}/safety-readiness` (`ready` boolean,
      `readinessStatus` `NOT_CONFIGURED`|`PENDING`|`READY`|`NOT_READY`,
      `missingRequirementTypes`, `requirements`). Individual readiness is
      updated via `PATCH /safety-requirements/{id}/readiness` (requires
      `readinessStatus`; `CANCELLED` Applications reject with 400
      `PERMIT_SAFETY_UPDATE_NOT_ALLOWED`; incomplete checklist/evidence
      prerequisites reject with 400 `PERMIT_SAFETY_PREREQUISITE_NOT_MET` when
      trying to set `READY`). No HSE workflow beyond that.

      AUTHORIZATION: every route requires the bearer session plus a
      `permit.*` permission - `permit.read` for list/get/readiness,
      `permit.manage` for the two creates and the readiness patch - AND BE-02
      Building isolation (403 `BUILDING_ACCESS_DENIED`). Permission alone is
      never enough.

  - name: Permit Validities
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 03E2 - J11 Permit Validity public
      contract exposure (BE-20G). Documents the ALREADY REGISTERED
      backend-authoritative Permit validity lifecycle. Contract exposure only -
      no runtime, lifecycle or authorization change.

      SCOPE (BE-20G only): this tag covers the Permit Validity record. A
      Validity is a time-bounded authority window for a Permit, bound to
      `permitApplicationId` + `buildingId` (must match the Permit/Application
      Building; 400 `PERMIT_VALIDITY_BUILDING_MISMATCH` otherwise) with
      `validFrom`/`validUntil` (ISO-8601 with timezone, calendar-valid,
      `validUntil` > `validFrom`; 400 `VALIDATION_ERROR` otherwise). Optional
      `notes` (2000, empty->null). Backend-controlled fields `status`
      (`PENDING`|`VALID`|`EXPIRED`|`REVOKED`), `activatedAt`, `expiredAt`,
      `revokedAt`/`revokedByUserId`, `revocationNotes`, `active` (derived)
      must not be supplied on set (400). One `PENDING` or `VALID` per Permit
      at a time (409 `PERMIT_VALIDITY_ALREADY_OPEN`); the Permit/Application
      must be `SUBMITTED` with fully approved context (400
      `PERMIT_VALIDITY_CONTEXT_INVALID` otherwise). Revocation via
      `POST /permit-validities/{id}/revoke` (optional `revocationNotes` alias
      `notes`) flips `PENDING`|`VALID` -> `REVOKED`, otherwise 400
      `PERMIT_VALIDITY_REVOKE_NOT_ALLOWED`. This is not an automatic permit
      activation, expiry scheduler, renewal, approval, execution, work-stop or
      notification/escalation surface.

      LIFECYCLE (truthful, taken from the runtime store, not inferred):
      set via `POST /permits/{permitId}/validity` (201). Current validity
      resolved via `GET /permits/{permitId}/validity` and alias
      `GET /permits/{permitId}/validity/current` (returns
      `{ permitId, permitReference, buildingId, currentValidity, history }`;
      `currentValidity` null when none). List via
      `GET /permit-validities` (filtered, Building-scoped, plain array).
      Revoke via `POST /permit-validities/{id}/revoke` (200). No renewal.

      AUTHORIZATION: every route requires the bearer session plus a
      `permit.*` permission - `permit.read` for get/list/current,
      `permit.manage` for set and revoke - AND BE-02 Building isolation (403
      `BUILDING_ACCESS_DENIED`). Permission alone is never enough.

  - name: Permit Work Contexts
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 03F1 - J11 Permit Work Context public
      contract exposure (BE-20D). Documents the ALREADY REGISTERED
      permit-bound work-context routes over the authoritative BE-04 structure.
      Contract exposure only - no runtime, lifecycle or authorization change.

      SCOPE (BE-20D only): this tag covers the Permit Work Context record.
      A Work Context is the permit-bound assignment of where and what work will
      be performed, over the authoritative BE-04
      Building->Floor->Area->Room->FunctionalLocation hierarchy. It carries
      `locationType`
      (BUILDING|FLOOR|AREA|ROOM|FUNCTIONAL_LOCATION) + `locationId` (the
      concrete BE-04 id) resolved to `buildingLocationId`/`floorId`/`areaId`/
      `roomId`/`functionalLocationId` and validated that the resolved BE-04 row
      is ACTIVE and belongs to the Application's Permit Building (400
      `PERMIT_WORK_LOCATION_INVALID` / `PERMIT_WORK_LOCATION_BUILDING_MISMATCH`
      otherwise), `workType`
      (controlled data code, normalized via work-orders validation) and
      `workDescription` (4000, empty->null), `plannedStartAt`/`plannedEndAt`
      (ISO-8601 with timezone, calendar-valid, both required together for
      assign; future start > now and end > start else 400
      `PERMIT_WORK_PLANNED_PERIOD_INVALID`), plus `accessRestrictionNotes`
      (2000, empty->null) and location projections `locationCode`/
      `locationName`. `permitApplicationId`/`permitId`/`permitReference`/
      `clientId`/`buildingId`/`applicationStatus` are projections from the
      Permit/Application; `id`, `createdByUserId`/`updatedByUserId`/`createdAt`/
      `updatedAt` are server-assigned. This is NOT a work-order execution,
      start/stop/suspend authority, workforce or safety-readiness engine;
      satisfying a work context does not authorize execution.

      LIFECYCLE (truthful, taken from the runtime store, not inferred):
      assigned via `PUT /permit-applications/{id}/work-location` (requires
      locationType+locationId+plannedStartAt+plannedEndAt) and
      `PUT /permit-applications/{id}/work-type` (requires workType) - both
      require `permit.manage` + ACTIVE Building assignment and a DRAFT
      Application (400 `PERMIT_WORK_CONTEXT_UPDATE_NOT_ALLOWED` otherwise).
      The combined context is readable via
      `GET /permit-applications/{id}/work-context` and
      `GET /permits/{permitId}/work-context` (`permit.read` + Building access)
      and is updatable via `PATCH /permit-applications/{id}/work-context`
      (`permit.manage`; at least one field required, locationType+locationId
      must be updated together, immutable fields permitId/permitApplicationId/
      permitReference/clientId/buildingId/applicationStatus/createdByUserId/
      updatedByUserId/createdAt/updatedAt must not be supplied, DRAFT only).
      List via `GET /permit-work-contexts` (`permit.read`, Building-scoped
      filtered by buildingId/locationType/locationId/workType; empty when no
      accessible Building, plain array, not paginated). No execution lifecycle
      is exposed here.

      AUTHORIZATION: every route requires the bearer session plus a `permit.*`
      permission - `permit.read` for the two gets and list, `permit.manage`
      for the two puts and the patch - AND BE-02 Building isolation (403
      `BUILDING_ACCESS_DENIED`). Permission alone is never enough.

  - name: Permit Work Lifecycle
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 03F2 - J11 Permit Work Lifecycle public
      contract exposure (BE-20K). Documents the ALREADY REGISTERED
      permit-specific work lifecycle with BE-09-style authority. Contract
      exposure only - no runtime, lifecycle or authorization change.

      SCOPE (BE-20K only): this tag covers the Permit Work Lifecycle record.
      A Work Lifecycle is the permit-specific work state stored per Permit.
      It carries `status` (READY|IN_PROGRESS|CLOSED|CANCELLED) where
      `CANCELLED` is derived when the BE-20A Permit itself is `CANCELLED`
      (otherwise `READY` when no lifecycle row exists), `startedAt`/
      `startedByUserId`/`startNotes` (2000, empty->null) set on START and
      `closedAt`/`closedByUserId`/`closeNotes` (2000, empty->null) set on
      CLOSE. `permitApplicationId`/`permitId`/`permitReference`/`clientId`/
      `buildingId`/`contractorContextType`/`contractorVendorId` are projections
      from the Permit/Application; `id`/`createdAt`/`updatedAt` are
      server-assigned. Readiness for START is the derived
      `PermitWorkStartReadiness` view (aggregating approvalReady,
      validityStatus, safetyReady, workerListReady, equipmentReady,
      evidenceReady and `blockers`). This is NOT a generic workflow or
      work-order execution engine; it does not infer approval, validity or
      readiness from status and does not expose suspend/resume/approve.

      LIFECYCLE (truthful, taken from the runtime store, not inferred):
      `READY` -> `IN_PROGRESS` via `POST /permits/{permitId}/work-start`
      (201; requires `permit.manage` + ACTIVE Building assignment + both
      `permit.read`+`permit.manage` authority + `ready===true` readiness with
      no blockers; else 400 `PERMIT_WORK_NOT_READY` with blocker details or
      403 `PERMIT_WORK_ACTION_NOT_ALLOWED`; already `IN_PROGRESS`/`CLOSED` yields
      409 `PERMIT_WORK_ALREADY_STARTED`/`ALREADY_CLOSED`). `IN_PROGRESS` ->
      `CLOSED` via `POST /permits/{permitId}/work-close` (200; requires
      `permit.manage` + ACTIVE Building assignment + `IN_PROGRESS` status; else
      400 `PERMIT_WORK_CLOSE_BEFORE_START` when `READY` or 409
      `PERMIT_WORK_ALREADY_CLOSED` when already `CLOSED`, or 403
      `PERMIT_WORK_ACTION_NOT_ALLOWED`). `READY`/`IN_PROGRESS`/`CLOSED` are
      readable via `GET /permits/{permitId}/work-status` (permit.read,
      returns `{ lifecycleId, status, startedAt/By, startNotes, closedAt/By,
      closeNotes, availableActions: [START|CLOSE] filtered by authority }`) and
      readiness via `GET /permits/{permitId}/work-start-readiness` (permit.read,
      returns `PermitWorkStartReadiness` with `workStatus`, `ready`, `blockers`,
      `availableActions`). List via `GET /permit-work` (permit.read,
      Building-scoped, filtered by buildingId/contractorContextType/
      contractorVendorId/status IN_PROGRESS|CLOSED; plain array, not paginated).
      No scheduler or automatic transition.

      AUTHORIZATION: every route requires the bearer session plus a `permit.*`
      permission - `permit.read` for readiness/status/list, `permit.manage`
      for start/close (start/close additionally require both `permit.read` and
      `permit.manage` for the authority check, otherwise 403
      `PERMIT_WORK_ACTION_NOT_ALLOWED`) - AND BE-02 Building isolation (403
      `BUILDING_ACCESS_DENIED`). Permission alone is never enough.

  - name: Permit Workers
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 03G1 - J11 Permit Worker public
      contract exposure (BE-20H). Documents the ALREADY REGISTERED
      permit-bound worker bindings over the existing Workforce masters. Contract
      exposure only - no runtime, lifecycle or authorization change.

      SCOPE (BE-20H only): this tag covers the Permit Worker record. A Worker
      entry binds one Workforce Profile (plus optional Vendor Workforce
      binding) to one `SUBMITTED` Permit Application and its open Permit
      validity. `permitApplicationId`/`permitId`/`permitReference`/`clientId`/
      `buildingId`/`contractorContextType`/`contractorContextId`/
      `contractorVendorId` are projections from the Permit/Application;
      `vendorWorkforceBindingId`/`workforceProfileId`/`workerName`/
      `employeeCode`/`vendorPersonnelCode`/`workforceType`/`workforceStatus`/
      `vendorWorkforceStatus`/`vendorWorkforceEffectiveFrom`/
      `vendorWorkforceEffectiveUntil`/`roleTrade` are resolved from the
      Workforce/Vendor Workforce masters. `validFrom`/`validUntil` are the
      worker window (ISO-8601 with timezone, ordered and contained within
      Permit validity and Workforce effective window; defaults to Permit
      validity when omitted) and `permitValidityStatus`/`eligibleForActiveWork`
      are derived. `notes` (2000, empty->null) plus `workerPersonReference`/
      `identificationReference` aliases are convenience projections. Backend-
      resolved fields like `workerName`, `employeeCode`, `status`,
      `deactivatedAt` must not be supplied on add. One `ACTIVE` entry per
      (`permitId`, `workforceProfileId`) is allowed (409
      `PERMIT_WORKER_ALREADY_ACTIVE`). This is NOT a Workforce/HR master,
      skills, training, attendance or shift surface; adding a worker does not
      imply qualification or work authorization.

      LIFECYCLE (truthful, taken from the runtime store, not inferred):
      created `ACTIVE` via `POST /permits/{permitId}/workers` (201; requires
      `permit.manage` + ACTIVE Building assignment + `permitApplicationId`+
      `buildingId`+`workforceProfileId`+`roleTrade` (data-driven code) and
      optional `vendorWorkforceBindingId`/`validFrom`/`validUntil`/`notes`; fails
      400 `PERMIT_WORKER_CONTEXT_INVALID` / `CONTRACTOR_INVALID` /
      `WORKER_INVALID` / `WORKER_INACTIVE` / `CONTRACTOR_MISMATCH` /
      `INVALID_VALIDITY` otherwise). `ACTIVE` -> `INACTIVE` via
      `POST /permit-workers/{id}/deactivate` (200; only `ACTIVE` can be
      deactivated, else 400 `PERMIT_WORKER_DEACTIVATE_NOT_ALLOWED`). Only an
      `ACTIVE` entry on an open Permit (`PENDING`|`VALID` validity) may be
      updated via `PATCH /permit-workers/{id}` (200; at least one of
      `roleTrade`/`validFrom`/`validUntil`/`notes` required; immutable context
      fields must not be supplied; otherwise 400
      `PERMIT_WORKER_UPDATE_NOT_ALLOWED`). Active resolution via
      `GET /permits/{permitId}/workers/active` returns `active: false` when
      validity is not `VALID` and filters to `eligibleForActiveWork`. This is
      not a readiness or execution gate.

      AUTHORIZATION: every route requires the bearer session plus a `permit.*`
      permission - `permit.read` for get/list/active, `permit.manage` for
      add/update/deactivate - AND BE-02 Building isolation (403
      `BUILDING_ACCESS_DENIED`). Permission alone is never enough.

  - name: Work Permit Readiness
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 03G2 - J11 Work Permit Readiness public
      contract exposure (BE-15D). Documents the ALREADY REGISTERED
      readiness/binding layer for Vendor Work permit requirements. Contract
      exposure only - no runtime, lifecycle or authorization change.

      SCOPE (BE-15D only): this tag covers the Work Permit Readiness record.
      A Readiness entry binds one BE-15B Vendor Work (`vendorWorkId`) to one
      permit requirement type (`permitRequirementType`, uppercased 1..100,
      `NOT_REQUIRED` reserved meaning no permit required) with an optional
      `permitReference` (200, empty->null), `permitStatus`
      (DRAFT|PENDING|ISSUED|SUSPENDED|REJECTED|REVOKED|EXPIRED), `validFrom`/
      `validUntil` (ISO date strings, nullable, `validUntil` before `validFrom`
      yields 400 `PERMIT_READINESS_INVALID_VALIDITY`), `notes` (2000,
      empty->null) and the backend-derived `readinessStatus`
      (READY|NOT_READY|EXPIRED|NOT_REQUIRED - never client-supplied; derived
      from permit requirement type, permit status and validity window). Full
      record carries `id`/`clientId`/`vendorWorkId`/`buildingId`/`workOrderId`/
      `createdByUserId`/`createdAt`/`updatedAt` (server-assigned). One record
      per (`vendorWorkId`, `permitRequirementType`) (409
      `PERMIT_READINESS_ALREADY_EXISTS`). This is NOT a Permit-to-Work engine,
      HSE engine, approval or work-start surface; it does not infer additional
      checks from other J11 modules and being READY does not mean work has
      started, permit was approved or lifecycle changed.

      LIFECYCLE (truthful, taken from the runtime store, not inferred):
      created via `POST /permit-readiness` (201; requires `vendor.manage` +
      Building isolation via Vendor Work's Building; needs `vendorWorkId`+
      `permitRequirementType`; optional `permitReference`, `permitStatus`,
      `validFrom`/`validUntil`, `notes`). Listed via `GET /permit-readiness`
      (`vendor.read`; requires at least one of `vendorWorkId`/`vendorId`/
      `buildingId`; Building filter asserts Building access, Vendor filter
      checks Vendor->Building ACTIVE relationship else 400
      `PERMIT_READINESS_BUILDING_MISMATCH`; plain array, Building-scoped).
      Current readiness via `GET /permit-readiness/current` (`vendor.read`;
      requires `vendorWorkId` query; returns `VendorWorkPermitReadiness` with
      `ready` boolean derived from permits `ready` + `readinessStatus`).
      Single via `GET /permit-readiness/{readinessId}` (`vendor.read`). Updated
      via `PATCH /permit-readiness/{readinessId}` (`vendor.manage`; at least
      one of `permitReference`/`permitStatus`/`validFrom`/`validUntil`/`notes`
      required; `null` clears reference/notes/validity). No delete, no
      automatic transition.

      AUTHORIZATION: every route requires the bearer session plus a `vendor.*`
      permission - `vendor.read` for the three gets/current, `vendor.manage`
      for create/patch - AND BE-02 Building isolation (asserted on the
      readiness's / Vendor Work's Building; 403 `BUILDING_ACCESS_DENIED`;
      Vendor->Building mismatch yields 400
      `PERMIT_READINESS_BUILDING_MISMATCH`). Permission alone is never enough.
  - name: Vendor Assignment
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 04A - J12 Vendor Assignment public
      contract exposure (BE-15A). Documents the ALREADY REGISTERED BE-15A
      runtime routes only. Contract exposure only - no runtime, lifecycle or
      authorization change.

      SCOPE (BE-15A only): a Vendor Assignment binds one existing BE-06A Vendor
      to one existing BE-08B Work Order inside that Work Order's Building. The
      Vendor and the Work Order are referenced, never duplicated or mutated. The
      exposed projection carries `id`, `vendorId`, `workOrderId`, `buildingId`,
      `status`, `notes`, `assignedByUserId` and `assignedAt`; record
      `createdAt`/`updatedAt` are persisted but never exposed. LIFECYCLE
      (truthful, taken from the runtime service, not inferred): `ACTIVE` ->
      `INACTIVE`. Deactivation never deletes the row (history is preserved) and
      re-deactivating an already-INACTIVE assignment is a no-op success.
      Reassignment deactivates the current ACTIVE assignment and creates a fresh
      ACTIVE one, so the PATCH response describes the NEW assignment and
      `previousAssignmentId` is only carried in the operational event, never in
      the HTTP body. At most one ACTIVE assignment may exist per
      (vendor, work order) pair - the partial unique index is the final
      authority for races.

      NOT IN SCOPE: Vendor Master administration, Vendor workforce master,
      Vendor Work execution, BAST acceptance, procurement assignment and any
      generic workflow engine. Assigning a Vendor does not create Vendor Work,
      does not start or complete work, does not approve or accept anything,
      does not link an invoice, and does not change Vendor or Work Order
      status. No automatic transition of any kind is performed by this module.

      AUTHORIZATION: every route requires the bearer session plus `vendor.read`
      (`GET /vendor-assignments`, `GET /vendor-assignments/{assignmentId}`) or
      `vendor.manage` (`POST /vendor-assignments`,
      `PATCH /vendor-assignments/{assignmentId}`), AND BE-02 Building isolation
      resolved from the Work Order's / the assignment's Building
      (403 `BUILDING_ACCESS_DENIED`). Permission alone is never enough, and list
      results are additionally constrained to the caller's accessible Buildings.
  - name: Vendor Work
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 04B1 - J12 Vendor Work public contract
      exposure (BE-15B). Documents the ALREADY REGISTERED BE-15B runtime routes
      only. Contract exposure only - no runtime, lifecycle or authorization
      change.

      SCOPE (BE-15B only): a Vendor Work is the operational execution record of
      one BE-15A Vendor Assignment against a BE-08B Work Order. It references
      the assignment (`vendorAssignmentId`) and copies its `vendorId`,
      `workOrderId` and `buildingId`; no work engine, checklist, evidence or
      document store is created here. The exposed projection carries `id`,
      `vendorAssignmentId`, `vendorId`, `workOrderId`, `buildingId`, `status`,
      `notes`, `startedAt` and `completedAt`; persisted `createdAt`/`updatedAt`
      are not exposed.

      LIFECYCLE (truthful, taken from the explicit runtime transition table - no
      generic workflow engine): `NOT_STARTED` -> `IN_PROGRESS`;
      `IN_PROGRESS` -> `ON_HOLD` | `COMPLETED`; `ON_HOLD` -> `IN_PROGRESS`;
      `COMPLETED` is terminal for BE-15B and nothing may follow it. Any other
      pair - including repeating the current status - is 400
      `VENDOR_WORK_INVALID_TRANSITION`. `startedAt` is set by the backend the
      first time work leaves `NOT_STARTED` and `completedAt` exactly when it
      reaches `COMPLETED`; neither timestamp is client-supplyable. One Vendor
      Work exists per assignment (DB unique constraint), so
      `POST /vendor-assignments/{assignmentId}/work` is idempotent: 201 on
      creation, 200 with the existing record afterwards, never a duplicate and
      never a conflict.

      BOUNDARY: completing a Vendor Work is NOT acceptance. Reaching
      `COMPLETED` does not create or advance a BAST, does not sign off, verify
      or hand over anything, does not create a completion report, does not
      rework or reopen anything, does not touch evidence or history, does not
      link an invoice, and never changes Vendor, Vendor Assignment or Work Order
      status. No automatic transition between Vendor Work and any of those
      surfaces exists in this module, and no such transition is implied here.
      Vendor Assignment administration belongs to the Vendor Assignment tag.

      AUTHORIZATION: every route requires the bearer session plus `vendor.read`
      (list, single) or `vendor.manage` (resolve, status transition), AND BE-02
      Building isolation resolved from the assignment's / the work's Building
      (403 `BUILDING_ACCESS_DENIED`). Permission alone is never enough, and
      list results are additionally constrained to the caller's accessible
      Buildings.
  - name: Vendor Work Evidence
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 04B2 - J12 Vendor Work Evidence public
      contract exposure (BE-15E). Documents the ALREADY REGISTERED BE-15E
      runtime routes only. Contract exposure only - no runtime, lifecycle or
      authorization change.

      SCOPE (BE-15E only): Vendor-work-bound evidence authority over the
      EXISTING BE-07 shared evidence engine - `evidence_requirements` with
      `target_type = 'VENDOR_WORK'` and `evidence_submissions` with
      `execution_type = 'VENDOR_WORK'` and `execution_id = vendor_work_id`. No
      second evidence engine exists: these five operations read the Vendor
      Work's requirements, bind a PHOTO / DOCUMENT / SIGNATURE submission, list
      bound submissions and soft-remove one. Evidence types are exactly `PHOTO`
      | `DOCUMENT` | `SIGNATURE`, and a submission carries file metadata
      (`fileReference`, `originalFileName`, `mimeType`, `fileSize` up to
      52428800 bytes matching BE-07, optional `capturedAt`) - the API never
      accepts or returns file bytes, so no multipart upload, object-storage
      provider, binary transfer, OCR, image compression, virus scanning,
      hashing, integrity verification, retention administration or generic
      document-management contract is exposed by these routes and none is
      implied. Requirement fulfilment is never asserted by a response: a
      listing of submissions is not a verdict that a requirement is satisfied,
      and BE-07 governance attachment happens server-side only.

      BOUNDARY: evidence does not complete, accept, verify, sign off or hand
      over a Vendor Work, does not create a completion report, does not open
      rework and does not touch BAST, invoice or procurement state. Vendor Work
      lifecycle itself belongs to the Vendor Work tag, and the generic BE-07
      Evidence tag is a different surface that this exposure does not modify or
      extend.

      AUTHORIZATION: every route requires the bearer session plus `vendor.read`
      (requirement listing, per-work listing, filtered listing) or
      `vendor.manage` (submit, soft-remove), AND BE-02 Building isolation
      resolved through the Vendor Work's Work Order Building (403
      `BUILDING_ACCESS_DENIED`). Permission alone is never enough.
  - name: Vendor Work History
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 04B2 - J12 Vendor Work History public
      contract exposure (BE-15K). Documents the ALREADY REGISTERED BE-15K
      runtime route only. Contract exposure only - no runtime, lifecycle or
      authorization change.

      SCOPE (BE-15K only): a read-only projection over the shared BE-07
      append-oriented operational-events store, scoped to one Vendor Work and
      its BE-15A assignment and returned in chronological order. Exactly one
      operation exists; no event creation, update, editing, deletion, timeline
      reconstruction, retention or audit-policy surface is registered here and
      none is implied. `eventType` is an open string and `metadata` / `related`
      are opaque server-generated objects, so callers must tolerate unknown
      event types. Events written by the BE-15 domain services - never by this
      API - include `VENDOR_ASSIGNMENT_CREATED` / `_DEACTIVATED` / `_REASSIGNED`,
      `VENDOR_WORK_CREATED`, `VENDOR_WORK_STATUS_CHANGED`,
      `VENDOR_WORK_EVIDENCE_ADDED` and `VENDOR_WORK_EVIDENCE_REMOVED`.

      BOUNDARY: an event is an audit record only. It never means acceptance,
      sign-off, verification, handover or BAST progression, and it carries no
      approval or commercial decision.

      AUTHORIZATION: the bearer session plus `vendor.read`, AND BE-02 Building
      isolation resolved from the Vendor Work's Building (403
      `BUILDING_ACCESS_DENIED`). Permission alone is never enough.
  - name: Vendor Completion Report
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 04C1 - J12 Vendor Completion Reports
      public contract exposure (BE-15F). Documents the ALREADY REGISTERED BE-15F
      runtime routes only. Contract exposure only - no runtime, lifecycle or
      authorization change.

      SCOPE (BE-15F only): a reporting layer over one BE-15B Vendor Work. A
      report records the vendor's completion `summary` / `notes`, and snapshots
      the BE-07 / BE-15E evidence-readiness result
      (`evidenceReady`, `missingEvidenceTypes`). Exactly one report may exist per
      Vendor Work (database unique constraint), so a second create is 409 rather
      than a new draft. The identifiers actually carried by the projection are
      `vendorWorkId`, `workOrderId`, `buildingId` and `clientId`, plus the user
      references `createdByUserId` and `completedByUserId`; there is NO
      `vendorId` and NO `vendorAssignmentId` on the DTO (`vendorId` exists only
      as a list filter), and no evidence, document or BAST identifier is
      embedded.

      LIFECYCLE (truthful, from the runtime store): `DRAFT` -> `SUBMITTED`, and
      `SUBMITTED` is final. Drafting and editing need no evidence - the snapshot
      is recorded as found - while submission re-validates the required evidence
      live and fails 400 `COMPLETION_REPORT_EVIDENCE_INCOMPLETE` (one detail per
      missing type) without mutating anything. On accepted submission the
      backend stamps `completedByUserId` = caller and `completedAt` = now, writes
      `evidenceReady: true` with an empty `missingEvidenceTypes`, and the report
      becomes immutable: no edit, no resubmit, no revert to DRAFT, no delete and
      no re-open are registered on this module, and none is implied. Only
      `summary` and `notes` are ever writable, and only while DRAFT.

      BOUNDARY: a submitted completion report is the vendor's own declaration,
      NOT an acceptance or an approval. It does not set Vendor Work `COMPLETED`,
      does not start, complete or close a Work Order (the BE-08 completion
      lifecycle stays BE-08's authority and is untouched here), does not create,
      submit, accept or approve a BAST, does not produce an acceptance sign-off,
      work-completion document or handover document, does not satisfy or alter
      evidence requirements, does not trigger verification, rework or an invoice,
      and carries no customer acceptance meaning. Nothing in this module
      performs an automatic transition to or from any adjacent J12 surface; the
      only side effect is one operational event readable through Vendor Work
      history.

      AUTHORIZATION: every route requires the bearer session plus `vendor.read`
      (list, single) or `vendor.manage` (create, DRAFT edit, submit), AND BE-02
      Building isolation resolved from the Work Order's / report's Building
      (403 `BUILDING_ACCESS_DENIED`). No BAST, acceptance, approval,
      verification or document permission is used by this module, and none is
      inferred here. Permission alone is never enough.
  - name: Vendor Rework
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 04C2 - J12 Vendor Rework public
      contract exposure (BE-15J). Documents the ALREADY REGISTERED BE-15J
      runtime routes only. Contract exposure only - no runtime, lifecycle or
      authorization change.

      SCOPE (BE-15J only): append-only rework bookkeeping for one BE-15B Vendor
      Work. A rework cycle is always anchored to the Vendor Work's latest BE-15I
      verification whose decision was `REWORK_REQUIRED` (its `reviewId` links to
      that BE-07 review row) and carries `reason`, a mutable `reworkNotes`, the
      requesting actor and, after resubmission, the resubmitting actor and
      timestamp. LIFECYCLE (truthful, from the runtime): `REQUESTED` ->
      `RESUBMITTED`; `RESUBMITTED` is immutable (409 `VENDOR_REWORK_IMMUTABLE`)
      and previous cycles are never overwritten, so the read endpoint returns
      the whole `cycles` list plus the one `current` `REQUESTED` cycle or null.
      At most one current cycle per work and one cycle per anchor review
      (409 `VENDOR_REWORK_ALREADY_OPEN`, 409 `VENDOR_REWORK_REVIEW_CONSUMED`),
      and there is no delete, no cancel, no reopen, no revert and no second
      edit path.

      BOUNDARY: rework is bookkeeping only. Requesting rework does NOT reopen
      the Vendor Work or the Work Order, does not change the Vendor Work
      `status`, does not reset, clear or invalidate the completion report,
      evidence or verification history, and is not a BAST rejection or an
      acceptance rejection. Resubmitting records the vendor's resubmission and
      nothing else: it does not re-verify, does not re-run evidence readiness,
      does not approve anything, does not reverse an invoice and does not create
      a Finding. This module borrows no generic Finding / Rework authority from
      other Asentra modules and grants none.

      AUTHORIZATION: every route requires the bearer session plus `vendor.read`
      (context read) or `vendor.manage` (request rework, edit notes, resubmit),
      AND BE-02 Building isolation asserted twice by the runtime - on the
      Vendor Work's Building in the controller and on the Work Order's Building
      in the service (403 `BUILDING_ACCESS_DENIED`, 400
      `VENDOR_REWORK_BUILDING_MISMATCH`). No BAST, acceptance, approval or
      finding permission is declared by this module and none is inferred.
  - name: Vendor Verification
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 04C2 - J12 Vendor Work Verification
      public contract exposure (BE-15I). Documents the ALREADY REGISTERED
      BE-15I runtime routes only. Contract exposure only - no runtime,
      lifecycle or authorization change.

      SCOPE (BE-15I only): a reviewer decision recorded on a `COMPLETED` BE-15B
      Vendor Work, stored as a row of the shared BE-07 review primitive
      (`target_type = 'VENDOR_WORK'`). Decisions are exactly `APPROVED`,
      `REJECTED` or `REWORK_REQUIRED` - the values of the shared review
      vocabulary, not a Vendor-specific enum - each submission appends a fresh
      review row (history is never overwritten) and only `notes` may accompany
      it. Verification is append-only: there is no edit, no withdrawal, no
      re-decision and no delete, and once an `APPROVED` review exists the work
      cannot be re-verified at all (409
      `VENDOR_VERIFICATION_ALREADY_APPROVED`). The read endpoint also returns
      the completion / service / BAST context of the work; those are read-only
      references that help a reviewer decide, never an authority this module
      writes.

      BOUNDARY - A VERIFICATION RESULT IS NOT ACCEPTANCE: `APPROVED` records a
      verification decision only. It does not accept or create a BAST, is not a
      final acceptance or a customer sign-off, does not close the Work Order,
      does not approve an invoice and does not approve handover.
      `REWORK_REQUIRED` records the decision and nothing more - no rework cycle
      is opened by this module (`POST /vendor-works/{id}/rework` of the Vendor
      Rework surface owns that), and the Vendor Work's `status` is deliberately
      left unchanged, which is why the submit response echoes
      `vendorWorkStatus` as found. `REJECTED` revokes, reverses and invalidates
      nothing. Preconditions are equally narrow: the work must be `COMPLETED`
      and, when a completion report exists, that report must be `SUBMITTED`.

      AUTHORIZATION: the bearer session plus `vendor.read` (state read) or
      `vendor.manage` (submit a decision), AND BE-02 Building isolation on the
      Work Order's Building (403 `BUILDING_ACCESS_DENIED`, 400
      `VENDOR_VERIFICATION_BUILDING_MISMATCH`). No `bast.accept`, no approval
      permission and no `workorder.manage` is declared by this module, and none
      is inferred here. Permission alone is never enough.
  - name: Acceptance Sign-Off
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 04D1 - J12 Acceptance Sign-Offs public
      contract exposure (BE-22E). Documents the ALREADY REGISTERED BE-22E
      runtime routes only. Contract exposure only - no runtime, lifecycle or
      authorization change.

      SCOPE (BE-22E only): an append-only sign-off record anchored to a valid
      BAST or Handover context. A row stores the target link, the `clientId`,
      `buildingId` and `contextType` (`INTERNAL` | `TENANT` | `VENDOR`) copied
      from that target, the `decision` (`ACCEPTED` | `REJECTED`), the single
      `signerUserId`, optional `notes` and `signedAt`. There is deliberately NO
      separate approver, reviewer, acceptedBy / acceptedAt pair, and no
      `status`, stage or signature-image field: the actor of the POST becomes
      the signer, and no body field can name anyone else. `signedAt`, `id` and
      both record timestamps are backend-assigned.
      `bastSubmissionAttemptId` and `documentVersionId` are exposed but always
      null, because no registered route populates them.

      LIFECYCLE (truthful): there is none beyond "recorded". Three routes exist -
      submit, list, read one - and nothing else: no edit, no amend, no revoke, no
      withdrawal, no countersign, no multi-stage approval, no re-open and no
      delete. Multiple sign-offs per target are allowed on purpose
      (no uniqueness constraint) so history is preserved newest-first rather
      than overwritten. Rejections and acceptances are separate rows and never
      mutate each other.

      BOUNDARY: a sign-off row is an assertion made BY this module about a
      document, not the lifecycle of that document. `ACCEPTED` does not accept,
      advance or mutate a BAST, does not close a Work Order, does not complete a
      Vendor Work, is not an invoice or payment approval, is not handover
      completion and is not a customer-satisfaction or generic approval
      workflow - no such authority is registered here and none is inferred.
      BAST decisions specifically are NOT taken through this surface: POST
      rejects any `bastDocumentId` target with 400 `ACCEPTANCE_INVALID_CONTEXT`
      and directs callers to the canonical
      `POST /bast-documents/{id}/decisions` command, which stays the single
      BAST authority. Reads may still return BAST-linked history. The only write
      this module performs is its own sign-off row plus one operational event.

      AUTHORIZATION: the bearer session plus `document.read` (list, single) or
      `document.manage` (submit) - the permissions actually declared by these
      routes - AND Building isolation on the target's / the row's Building
      (403 `BUILDING_ACCESS_DENIED`), with an extra signer check on submission
      (403 `ACCEPTANCE_UNAUTHORIZED_SIGNER`: the acting user must be ACTIVE and
      hold access to that Building). No vendor, BAST, approval or Work Order
      permission is declared or inferred, and permission alone is never enough.

  - name: Work Completion Documents
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 04D2 - J12 Work Completion Documents
      (BE-22B) public contract. It covers exactly the three already registered
      routes - create, list and read one - and adds no behaviour of its own.

      SCOPE: a Work Completion Document is one binding row that attaches ONE
      shared BE-22A Document to a Work Order, optionally narrowed to a single
      Vendor Work. The binding carries only its own identifiers and the
      embedded `document` projection; Work Order and Vendor Work data are
      referenced, never duplicated.

      PRECONDITION: creation succeeds only for a Work Order that already exists
      and already sits in status `COMPLETED`; a supplied Vendor Work must belong
      to that Work Order, share its Building and be `COMPLETED` too. This
      surface reads that work state as an input check and never writes it.

      BOUNDARY: a Work Completion Document is NOT a Vendor Completion Report, a
      BAST, a BAST acceptance, an Acceptance Sign-Off or a Handover Document.
      Creating one does not close a Work Order, complete a Vendor Work, accept
      or verify vendor work, approve an invoice or a payment, or complete a
      handover, and it satisfies or alters none of those adjacent lifecycles.
      It is completion evidence documentation only.

      LIFECYCLE: the only registered actions are "create, then read". There is
      no update, delete, approve, reject, archive, revoke, amend, supersede,
      reopen or countersign route here, no automatic BAST or Handover Document
      creation, and no signature, version-control, retention, integrity, OCR,
      object-storage or upload capability: `fileReference` is an opaque string
      pointer, never a multipart or binary transfer. The underlying document
      starts as `DRAFT` unless the request names another allowed status, and
      `ARCHIVED` cannot be requested because the shared foundation admits it
      only through its own archive action, which is not exposed on this route.

      ACTOR: `createdByUserId` is the authenticated actor recorded by the
      backend at insert. No submitter, executor, completer or signer identity is
      accepted from callers, and none is inferred from Vendor Work, Work Order,
      Vendor Completion Report, verification or acceptance records.

      AUTHORIZATION: the bearer session plus `document.read` (list, single) or
      `document.manage` (create) - the permissions actually declared by these
      routes - AND Building isolation on the binding's / request's Building
      (403 `BUILDING_ACCESS_DENIED`), with Client isolation enforced on the
      shared Document number space and on the filters that name a Client. No
      vendor, `bast.accept`, approval or Work Order permission is declared or
      inferred, and permission alone is never enough.

  - name: Handover Documents
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 04D3 - J12 Handover Documents (BE-22D)
      public contract. It covers exactly the three already registered routes -
      create, list and read one - and adds no behaviour of its own.

      SCOPE: a Handover Document is one binding row that attaches ONE shared
      BE-22A Document to a Work Order and records a handover number, a handover
      date, free-text notes and an optional file pointer, with optional
      references to a Vendor Work, a Work Completion Document and a BAST. The
      four references are validated pointers only: no Work Order, Vendor Work,
      completion-document, BAST or sign-off object is embedded, duplicated or
      mutated, and there is no separate handover engine behind these routes.

      PRECONDITION: creation requires an existing Work Order that already sits
      in status `COMPLETED`. Each optional reference, when supplied, must
      resolve and must belong to that same work context - same Work Order, same
      Building, same Client, and vendor-aligned where both sides carry a Vendor
      Work. A `bastDocumentId` may point at a BAST in any status: this route
      checks identity, never BAST acceptance.

      BOUNDARY: creating a Handover Document is NOT acceptance and NOT handover
      completion. It is not a BAST, a BAST acceptance, an Acceptance Sign-Off, a
      Work Completion Document, a Vendor Completion Report or a Vendor
      Verification, and it does not close a Work Order, complete a Vendor Work,
      approve an invoice or a payment, or record customer satisfaction. Every
      row created here comes back as `handoverStatus: DRAFT` with
      `handedOverByUserId` and `handedOverAt` null, because the create path never
      writes them - a created handover document is documentation, not evidence
      that anyone handed anything over or received anything.

      RELATION TO ACCEPTANCE SIGN-OFF: a BE-22E Acceptance Sign-Off may carry a
      `handoverDocumentId` pointing at these rows, so sign-off history can be read
      by filtering the sign-off list on a handover id. That link does not make a
      Handover Document an acceptance authority: it has no ACCEPTED/REJECTED
      field, no approval state of its own beyond `DRAFT` / `HANDED_OVER`, and
      sign-off semantics are not re-used here as a lifecycle.

      LIFECYCLE: the only registered actions are "create, then read". The module
      exposes no update, delete, hand-over, accept, approve, reject, archive,
      revoke, amend, reopen or countersign operation, so `HANDED_OVER` is not
      reachable through this contract, no automatic BAST or Work Completion
      Document is created, and no automatic Work Order closure happens. The
      shared document starts as `DRAFT` unless the request names another allowed
      status, and `ARCHIVED` cannot be requested at creation. There is no
      signature, version-control, retention, integrity, OCR, object-storage or
      upload mechanism here: `fileReference` is an opaque string pointer, never a
      multipart or binary transfer.

      ACTOR: `preparedByUserId` is the authenticated actor recorded by the
      backend at insert; `handedOverByUserId` and `handedOverAt` are
      backend-assigned columns that no route on this contract populates. There is
      no approver, executor or acceptor field, and no actor or timestamp is
      inferred from the Work Order, Vendor Work, Work Completion Document, BAST,
      verification or acceptance records.

      AUTHORIZATION: the bearer session plus `document.read` (list, single) or
      `document.manage` (create) - the permissions actually declared by these
      routes - AND Building isolation on the binding's / request's Building and
      on every referenced context (403 `BUILDING_ACCESS_DENIED`), with Client
      isolation enforced on the handover-number and document-number spaces and on
      the filters that name a Client. No vendor, `bast.accept`, approval or Work
      Order permission is declared or inferred, and permission alone is never
      enough.

  - name: Documents
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B1 - J19 Documents (BE-22A) public
      contract. It covers exactly the six already registered routes of the
      generic Document foundation - create, list, read one, patch, archive and
      restore - and adds no behaviour of its own.

      SCOPE: a Document is the single shared BE-22A row every journey points at:
      ownership (Client, optional Building), a per-Client `documentNumber`, a
      free-form `documentType` code, one of three `contextType` values with an
      optional resolved source, free-text title and description, an opaque
      `fileReference` string and the lifecycle `status`. The row is the
      authority: no Work Order, Vendor Work, BAST, handover, sign-off, invoice,
      tenant or finding object is embedded, duplicated or mutated here.

      PRECONDITION: creation requires an existing and accessible Client (plus an
      accessible Building that belongs to it when one is named) and a source that
      resolves inside the same Client for `TENANT` / `VENDOR` contexts. Every
      route then re-checks isolation on the stored row: Building isolation when
      the document has a Building, Client isolation when it has none.

      BOUNDARY: this surface owns generic Document state only. It does not own
      Document Versions, Document Expiry, Document Approvals, Supporting
      Documents, Evidence, Evidence Retention or Evidence Integrity, and it is
      not the Work Completion Document, Handover Document, BAST Document or
      Acceptance Sign-Off aggregate - those are separate journeys with their own
      contract parts. There is no upload, download, storage, OCR, checksum,
      hash, size or mime handling: `fileReference` is stored verbatim as an
      opaque pointer. `expiryDate` is not accepted by either write route; the
      expiry of a document is set only by the Document Expiry journey.

      LIFECYCLE: status starts at `DRAFT` unless the request names another
      allowed value, and `ARCHIVED` can never be requested on create or patch.
      Only `POST /documents/{id}/archive` enters `ARCHIVED` - it snapshots the
      current status into `statusBeforeArchive` and records the actor, timestamp
      and optional reason - and only `POST /documents/{id}/restore` leaves it,
      back to that snapshot, clearing all four archive fields. Patching an
      `INACTIVE` or `ARCHIVED` document is refused with 409, as is archiving a
      document that is already archived or that is the canonical document behind
      a BAST binding. Every successful create, patch, archive and restore writes
      one operational event (`DOCUMENT_CREATED` / `DOCUMENT_UPDATED` /
      `DOCUMENT_ARCHIVED` / `DOCUMENT_RESTORED`), and a create additionally seeds
      the foundation's immutable version-1 history row, which no route on this
      contract part exposes as an operation.

      ACTOR: `createdByUserId` and, on archive, `archivedByUserId` are the
      authenticated actor recorded by the backend; `createdAt`, `updatedAt` and
      `archivedAt` are backend timestamps. No approver, uploader, holder,
      custodian or signer identity is accepted from callers or inferred from
      adjacent modules.

      AUTHORIZATION: the bearer session plus `document.read` (list, single read),
      `document.manage` (create, patch) or `document.archive` (archive, restore)
      - the three permissions actually declared by these routes - AND the
      Building / Client isolation above (403 `BUILDING_ACCESS_DENIED`). No
      approval, vendor, tenant, evidence or `bast.accept` permission is declared
      or inferred, and permission alone is never enough.

  - name: Document Versions
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B2 - J19 Document Versions (BE-22G)
      public contract. It covers exactly the five already registered routes of
      the version-history module - append a version, list history, resolve
      latest, read one version by number and read one version by id - and adds no
      behaviour of its own.

      SCOPE: a Document Version is one immutable snapshot row of the six
      versioned fields (`title`, `description`, `fileReference`, `documentType`,
      `status`, `expiryDate`) belonging to an existing generic BE-22A Document,
      numbered inside that document and stamped with its creator. The version
      projection is flat: no `Document` or `DocumentFoundation` object is
      embedded, and no ownership, `documentNumber`, context or source field is
      duplicated.

      PRECONDITION: every route here starts by resolving the parent Document; the
      caller must reach it through its Building when it has one, otherwise
      through its Client (403 `BUILDING_ACCESS_DENIED`). Appending a version is
      additionally refused while the parent document sits in `ARCHIVED` (409
      `DOCUMENT_UPDATE_NOT_ALLOWED`) - the two read routes that can create a
      history baseline do not carry that guard, so history of an archived
      document remains readable and resolvable.

      BOUNDARY: this surface owns Document Version records only. It is not a
      Document editor: the six generic-document fields are not patched through
      these routes (the append action copies a snapshot and the shared
      foundation keeps the parent row aligned with it), and Document update,
      archive and restore stay on the Documents contract part. There is no
      Document Expiry, Document Approval or Supporting Document behaviour here,
      and none for Evidence, Evidence Retention or Evidence Integrity: no
      retention state, hash, checksum, size or mime type appears in this
      projection. `fileReference` is a copied or supplied opaque string pointer -
      no upload, download, storage, OCR or deletion exists on any of these five
      routes. There is no rollback, promotion, supersede, renumber, edit or
      delete route for a version, and no current-version pointer: "latest" is
      computed from the numbering, never stored.

      LIFECYCLE: `versionNumber` is backend-computed as one more than the highest
      existing number for that document (starting at 1 when the document has no
      history row), so numbers ascend by one, are unique per document, and are
      never reused or renumbered; it is not accepted from callers on any route.
      A previously written version row is never overwritten. Creating a version
      with an empty body is legal and stores a duplicate snapshot of the current
      document state under the next number. `GET .../versions` and
      `GET .../versions/latest` write the missing version-1 baseline first when a
      document has no history at all; `GET .../versions/{versionNumber}` and
      `GET /document-versions/{versionId}` never write.

      ACTOR: `createdByUserId` is the authenticated actor recorded at insert for
      an appended version, and the document's own creator for a baseline created
      lazily by the two read routes; `createdAt` and `updatedAt` are backend
      timestamps. Version rows carry no approver, reviewer, uploader or holder
      field, and none is inferred from any adjacent journey.

      AUTHORIZATION: the bearer session plus `document.manage` (append) or
      `document.read` (the four reads) - the two permissions actually declared by
      these routes - AND the parent document's Building / Client isolation above.
      `document.archive` and `document.approve` are not declared or implied, no
      vendor or `bast.accept` permission is inferred, and permission alone is
      never enough.

  - name: Document Expiry
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B3 - J19 Document Expiry (BE-22H)
      public contract. It covers exactly the four already registered routes of
      the expiry module - set and read the expiry of a generic Document, and set
      and read the expiry carried by a Document Version - and adds no behaviour of
      its own. There is no fifth route: expiry is not deleted, extended,
      renewed, cleared by a dedicated verb or scheduled anywhere.

      SCOPE: this surface owns the expiry CONFIGURATION of a document row and of
      the expiry value recorded on a version snapshot - a single nullable date
      value plus the state derived from it at read time. It exposes no storage,
      no file, no retention rule, no integrity or hash field, and no approval or
      sign-off state.

      EXPIRY IS NOT RETENTION, NOT ARCHIVAL AND NOT DELETION: the expiry value
      never removes, purges, locks, hides or archives anything, and no automatic
      transition of any kind is attached to it. There is no scheduler, no job, no
      reminder, no notification and no due-date sweep in this module -
      `expiryState` is computed inside the request from the current clock, so the
      same row can read `EXPIRING` today and `EXPIRED` tomorrow with nothing
      written in between. Evidence Retention policies, evidence retention holds
      and integrity verification are separate authorities on separate routes and
      are not read, mirrored or affected here.

      PRECONDITION: every route resolves its target first - a generic Document row
      for the document pair, a version row for the version pair - and then asserts
      the parent document's isolation (Building access when the document has a
      Building, otherwise Client access). Both write routes additionally refuse a
      parent document that already sits in `ARCHIVED` (409
      `DOCUMENT_UPDATE_NOT_ALLOWED`), because the archive action owns that state;
      the two reads do not carry that guard, so expiry of an archived document
      stays readable.

      LIFECYCLE: setting an expiry is a full replacement of the value (there is no
      increment, no compare-and-set and no conditional write), and clearing is a
      `null` value on the same PUT - not a DELETE, and not an omitted field, which
      is a 400. Both write routes preserve history in the way this module is built:
      setting a document's expiry appends a new immutable version row carrying the
      new value, and setting the expiry of an addressed version never rewrites
      that version row but appends a NEW version row from its snapshot. The version
      write also lands on the parent document row, so the two surfaces are
      synchronised by design - see the individual operations for the exact,
      asymmetric read behaviour that follows from it.

      ACTOR: the new version row stamped by either write records the authenticated
      caller as `createdByUserId`, and the document row's `updatedAt` moves;
      however the expiry response itself carries no actor and no timestamp field -
      it returns only the addressed ids, the effective date and the derived state.
      No expiry approver, reviewer or notifier identity exists on this surface.

      AUTHORIZATION: the bearer session plus `document.manage` (both writes) or
      `document.read` (both reads) - the two permissions actually declared by
      these routes - AND the parent document's Building / Client isolation above.
      `document.archive` and `document.approve` are not declared or implied, no
      retention, evidence or vendor permission is inferred, and permission alone
      is never enough.

  - name: Document Approvals
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B4 - J19 Document Approvals (BE-22I)
      public contract. It covers exactly the five already registered routes of the
      document approval module - submit a document or one of its versions for
      approval, read a document's approval history, read one approval, approve it
      and reject it - and adds no behaviour of its own.

      SCOPE: a Document Approval is one review row addressed by id, bound to a
      single target (a generic BE-22A Document, or one Document Version of it),
      naming exactly one reviewer, carrying free-text notes and an outcome. The
      projection exposes `approvalStatus` (`PENDING`, `APPROVED` or `REJECTED`) and
      the raw `decision` (null until somebody decides); both are derived from the
      same stored row and neither is a document state.

      NOT A GENERIC APPROVAL ENGINE: this surface is document-specific. It is not
      the configurable approval / workflow authority planned under
      CR-BE-APPROVAL-ADMIN-01 and it exposes none of that vocabulary - there is no
      approval definition or configuration route, no stage, step, sequence,
      routing, delegation, escalation, SLA or multi-level chain, no parallel or
      quorum reviewer, no form schema, no `available_actions` payload and no
      cross-module approval register. It is also not the BAST decision authority,
      not an Acceptance Sign-Off, not a Vendor Verification or Work Order
      verification, and not an evidence, retention or expiry capability. Only the
      document's own row can be attached to it, and only through the two targets
      named above.

      PRECONDITION: submission requires the target document to exist (a missing
      document is 400 `DOCUMENT_APPROVAL_CONTEXT_INVALID`, deliberately not 404),
      not to be `ARCHIVED` (409 `DOCUMENT_UPDATE_NOT_ALLOWED`), and - when
      `versionId` is supplied - that version must resolve and must belong to that
      document, otherwise 400 again. The submitter must reach the document's
      Building (or its Client, when it has none), and the named approver must be an
      ACTIVE user who reaches the same Building or Client and holds
      `document.approve` or `document.manage`; a rejected approver is 403
      `DOCUMENT_APPROVAL_UNAUTHORIZED` at submission time, not later. Deciding
      requires the caller to BE the named reviewer and to still satisfy that same
      approver test at decision time.

      LIFECYCLE: the whole state machine is `PENDING -> APPROVED` or
      `PENDING -> REJECTED`, and both transitions are single-shot. A row that is no
      longer pending, or that already carries a decision, cannot be decided again:
      the update is issued with a `status = 'PENDING'` predicate, so a concurrent
      second decision simply fails to match and both paths answer 409
      `DOCUMENT_APPROVAL_ALREADY_DECIDED` - no decision is ever overwritten and no
      answer is a silent duplicate success. There is no revoke, withdraw, reopen,
      supersede, countersign, delegate, escalate, request-rework, transfer,
      reminder or bulk-decision route, and no automatic or scheduled approval:
      nothing ever decides a pending review by itself. A decided row is terminal. A
      new submission after a final decision is allowed and is a NEW approval row -
      history is preserved rather than replaced - and only one `PENDING` row per
      target and reviewer may exist at a time (409
      `DOCUMENT_APPROVAL_ALREADY_PENDING`). Submitting neither changes the
      document's `status` nor its expiry, version or archive state; approving does
      not publish, activate, accept, complete or lock anything outside this row.

      ACTOR: `reviewerUserId` is the designated decision-maker, named by the
      submitter and validated by the backend; `createdByUserId` is the submitter,
      echoed on the submission response only - the review row persists no submitter
      column, so every later read answers null; `reviewedAt` is the backend
      decision timestamp, null while pending. There is no approver alias, delegate,
      proxy, signer or actor-inferred-from-creator field, and the submitter is
      never allowed to decide their own submission by right - only the named
      reviewer can.

      AUTHORIZATION: the bearer session plus `document.manage` (submit),
      `document.read` (both reads) or `document.approve` (approve, reject) - the
      three permissions actually declared by these routes - AND the target
      document's Building / Client isolation, plus the reviewer identity check and
      the approver-authority re-check above. `document.archive`, generic approval
      engine permissions, vendor, `bast.accept` and Work Order permissions are
      neither declared nor inferred, and permission alone is never enough.


  - name: Supporting Documents
    description: >
      CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B5 - J19 Supporting Documents (BE-22F)
      public contract. It covers exactly the three already registered routes of the
      supporting-document module - bind a newly created document to one business
      target, list those bindings and read one - and adds no behaviour of its own.

      SCOPE: a Supporting Document is a pairing of two rows. The first is an
      ordinary generic BE-22A Document, created here through the shared document
      foundation and returned embedded under `document` as exactly that projection;
      the second is this module's own binding row, which carries `parentType` plus
      `parentId`, copies `clientId`, `buildingId` and `contextType`, and stores the
      submitter and its own timestamps. The `id` of every response is the BINDING
      row id - not the document id - and `documentId` is the document that binding
      was created with.

      NOT THE GENERIC DOCUMENT, NOT EVIDENCE, NOT STORAGE: this is not the Documents
      surface of this contract - it owns no patch, no archive call, no status
      transition and no document list of its own - and a Supporting Document is not
      a Document Version, an expiry record or an approval. It is not an evidence
      package, attachment collection, chain of custody, retention schedule, legal
      hold or integrity record: `fileReference` is a required opaque string and no
      route uploads, downloads, stores, retrieves, hashes, scans, OCRs, versions or
      expires anything. There is no bucket, object key, size, mime type, checksum,
      page count or document body anywhere in this contract.

      TARGETS ARE A CLOSED SET: `parentType` accepts exactly the nine values the
      runtime enumerates and nothing else - `WORK_COMPLETION`, `BAST`, `HANDOVER`,
      `SIGN_OFF`, `TENANT_COMPANY`, `VENDOR`, `DOCUMENT`, `RFQ` and
      `QUOTATION_REVISION` - each resolved by `parentId` against that target's own
      row. There is therefore no polymorphic "attach to any entity" model, no
      free-form target type, no `targetType` string, no array or multi-parent form,
      and no reverse read path: a target's own routes never list its supporting
      documents, and no response here returns the parent object itself.

      BINDING SEMANTICS: creating one inserts a NEW Document - it never adopts,
      re-binds or wraps an existing one, because the request has no `documentId` -
      and then records the binding. No registered route re-points, replaces,
      unbinds, restores or deletes a binding, and this module issues no update
      statement on the row, so a binding is write-once from this contract's point of
      view. A target may accumulate any number of supporting documents: neither the
      write path nor the read path of this module looks for an existing binding for
      (`parentType`, `parentId`) and nothing supersedes an older row. The parent is
      only ever read - for its client, building, context and archived state - so
      binding leaves the parent row and its own status, decision, expiry or workflow
      untouched and grants no authority over it:
      naming a `SIGN_OFF` parent neither accepts, rejects nor completes a sign-off,
      and naming a `VENDOR`, `RFQ` or `QUOTATION_REVISION` parent creates no vendor,
      procurement or BAST state of its own.

      PRECONDITIONS: the target row must exist (404 `SUPPORTING_DOCUMENT_PARENT_NOT_FOUND`);
      where that target is itself backed by a generic document, that document must
      not be `ARCHIVED` (409 `DOCUMENT_UPDATE_NOT_ALLOWED`); the declared Client
      must equal the target's Client; a supplied Building must exist, belong to a
      Property of that Client and match the target's Building, while a request that
      names none INHERITS the target's Building - and the actor must then be able to
      reach that one too; the declared `contextType` must equal the target's own
      context for the five targets the runtime compares (`WORK_COMPLETION`, `BAST`,
      `HANDOVER`, `SIGN_OFF`, `DOCUMENT`); a `TENANT` or `VENDOR` context must name
      an existing source row under the same Client; and a `documentNumber` already
      used in that Client is refused (409 `DOCUMENT_NUMBER_ALREADY_EXISTS`). All of
      these run before anything is written, and if the binding insert then fails the
      document created moments earlier is removed again by this module's own
      compensating write, so a failed bind leaves no orphan document behind.

      ACTOR: `createdByUserId` is the authenticated caller, persisted on the binding
      row and readable back on every route here. There is no updated-by, approver,
      reviewer, signer, recipient, owner, assigned-to or delegated-to field, no
      decision or acknowledgement state, and no actor is ever inferred from the
      parent target or from the underlying document.

      AUTHORIZATION: the bearer session plus `document.manage` (create) or
      `document.read` (both reads) - the two permissions these routes declare - AND
      the Building / Client isolation of the row. `document.archive`,
      `document.approve`, vendor, `bast.accept` and Work Order permissions are
      neither declared nor inferred, and permission alone is never enough.
  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03C - J11 Permit Approvals.
  #
  # Contract exposure of the ALREADY REGISTERED BE-20F runtime routes only.
  # No runtime, lifecycle or generic approval-engine behaviour is added or
  # changed here.
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D1 - J11 Permit Equipment.
  #
  # Contract exposure of the ALREADY REGISTERED BE-20I runtime routes only.
  # No runtime, lifecycle or generic equipment behaviour is added or
  # changed here.
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D2 - J11 Permit Evidence.
  #
  # Contract exposure of the ALREADY REGISTERED BE-20J runtime routes only.
  # No runtime, lifecycle or generic evidence behaviour is added or
  # changed here.
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03E1 - J11 Permit Safety Requirements.
  #
  # Contract exposure of the ALREADY REGISTERED BE-20E runtime routes only.
  # No runtime, lifecycle or generic HSE behaviour is added or
  # changed here.
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03E2 - J11 Permit Validities.
  #
  # Contract exposure of the ALREADY REGISTERED BE-20G runtime routes only.
  # No runtime, lifecycle or generic validity-renewal behaviour is added or
  # changed here.
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03F1 - J11 Permit Work Contexts.
  #
  # Contract exposure of the ALREADY REGISTERED BE-20D runtime routes only.
  # No runtime, lifecycle or generic work-execution behaviour is added or
  # changed here.
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03F2 - J11 Permit Work Lifecycle.
  #
  # Contract exposure of the ALREADY REGISTERED BE-20K runtime routes only.
  # No runtime, lifecycle or generic work-execution behaviour is added or
  # changed here.
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03G1 - J11 Permit Workers.
  #
  # Contract exposure of the ALREADY REGISTERED BE-20H runtime routes only.
  # No runtime, lifecycle or generic HR behaviour is added or
  # changed here.
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03G2 - J11 Work Permit Readiness.
  #
  # Contract exposure of the ALREADY REGISTERED BE-15D runtime routes only.
  # No runtime, lifecycle or generic readiness behaviour is added or
  # changed here.
  # ---------------------------------------------------------------------------

# No global security requirement: public endpoints (health, login,
# invitation acceptance) opt out explicitly. All other documented
# endpoints require the bearer session token.
security: []

paths:
  /health:
    get:
      operationId: getHealth
      summary: Process liveness probe
      tags: [Health]
      responses:
        "200":
          description: Process is alive.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/HealthStatus"

  /health/database:
    get:
      operationId: getDatabaseHealth
      summary: Database readiness probe (SELECT 1)
      tags: [Health]
      responses:
        "200":
          description: Database connection verified.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/DatabaseHealth"
        "503":
          $ref: "#/components/responses/DatabaseUnavailable"

  /auth/login:
    post:
      operationId: login
      summary: Authenticate with email and password
      description: >
        Public. Returns an opaque session token plus the user. The token is
        sent on subsequent requests as `Authorization: Bearer <token>`; only
        its SHA-256 hash is stored server-side and the raw token is never
        returned again. Session lifetime is bounded by `expiresAt`
        (SESSION_TTL_MINUTES, default 480). The password policy is NOT
        enforced at login (policy applies only at set/change time). Unknown
        email, wrong password and non-login-allowed account states all return
        the same 401 `INVALID_CREDENTIALS` to avoid account enumeration.
      tags: [Authentication]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Authenticated successfully.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/LoginResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/InvalidCredentials"
        "429":
          $ref: "#/components/responses/RateLimited"

  /auth/me:
    get:
      operationId: getEffectiveUserContext
      summary: Effective user context
      description: >
        The single authoritative context of the authenticated user: identity,
        effective roles and permissions, reachable Client/Property/Building
        hierarchy, the flat accessible data scope, linked workforce identity
        and effective module entitlements. Web and Mobile must treat this as
        the source of truth for what the user can do and reach. This is also
        the authenticated-user / session-validation probe: it requires only a
        valid bearer session (no permission) and returns 401 with
        `AUTHENTICATION_REQUIRED` / `INVALID_SESSION` / `SESSION_EXPIRED` when
        the session is missing, invalid, or expired. There is no separate
        session-state endpoint.


        Mobile context contract (authoritative semantics, read-only):

        - `user` — safe identity (`PublicUser`); never a credential.

        - `access.roles` — every ACTIVE role currently assigned to the user.
          Multiple roles are normal; they are the user's effective roles, not
          a selectable/active role. `access.permissions` is the deduplicated
          union of all ACTIVE roles' ACTIVE permission codes (sorted string
          codes, identical to what `requirePermission` enforces) — a flat
          authority list, never mobile-only permission booleans.

        - `context.clients` — the reachable Client → Property → Building
          hierarchy derived ONLY from the user's explicit ACTIVE Building
          assignments (the BE-02F resolver, the same source BE-02G data
          isolation and `scope` use). No sibling/Client-wide expansion.

        - `context.workforce` — the Workforce Profile(s) linked to the user,
          identity only (`workforceProfileId` = `id`); empty when the user has
          no linked profile or its Client is outside the accessible scope.

        - `scope.buildingIds` / `scope.clientIds` — the flat accessible id
          sets for filtering/offline boundaries. They are backend-computed
          from the same authoritative source; a client-sent building/client
          id never confers access.

        - `entitlements` — effective module entitlements (`status` always
          `ACTIVE`); empty when no module is effectively entitled.

        Empty-state: a zero-scope user still gets 200 with empty
        `context.clients`, empty `context.workforce`, empty
        `scope.buildingIds` / `scope.clientIds` and empty `entitlements` —
        well-formed empty arrays, never an error.

        There is intentionally NO `availableActions` on this context: actions
        stay resource/lifecycle-scoped (work, finding, patrol, verification,
        permit, etc. endpoints). The backend does NOT persist a mobile
        "active building" / building session and there is no context-id
        header: the backend owns the accessible Building set and validates
        every scoped request. A mobile app may locally choose a building for
        presentation/navigation, but that choice grants no authority.

      tags: [Effective Context]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Effective context resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EffectiveUserContext"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /auth/logout:
    post:
      operationId: logout
      summary: Revoke the current session
      description: >
        Revokes the calling session (the row is marked REVOKED, never
        deleted); the token becomes unusable immediately. A re-used or
        already-revoked token yields 401. There is no token refresh endpoint:
        to continue working the client re-authenticates via `POST /auth/login`.
      tags: [Authentication]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Session revoked.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/LogoutResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /auth/audit-events:
    get:
      operationId: listAuthAuditEvents
      summary: List authentication audit events
      description: >
        Paginated authentication audit trail. Requires the
        `auth.audit.read` permission. `meta.limit`, `meta.offset` and
        `meta.total` follow the platform pagination contract.
      tags: [Authentication]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/OffsetParam"
      responses:
        "200":
          description: Audit events page.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/AuditEvent"
                      meta:
                        $ref: "#/components/schemas/PaginationMeta"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /auth/me/buildings:
    get:
      operationId: getMyBuildings
      summary: Current user building assignments
      description: >
        The authenticated user's own ACTIVE Building assignments with their
        reachable hierarchy (Building → Property → Client). Authentication
        only; no management permission is required (this is the caller's own
        context, like `GET /auth/me`). Each item is one ACTIVE assignment:
        `building` is always present; `property` and `client` are the
        resolved hierarchy and are `null` when the Building does not resolve
        to that level. This is the same BE-02F resolver BE-02G data isolation
        uses, so it never returns a Building the caller cannot access — a
        zero-assignment user gets `200` with an empty array, never an error.
        It returns the accessible SET only: it does not create or persist an
        "active building" selection (the backend owns building scope; mobile
        may locally select one for navigation, which confers no authority).
      tags: [Effective Context]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Accessible building contexts.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/UserBuildingContext"
        "401":
          $ref: "#/components/responses/Unauthorized"

  # --------------------------------------------------------------------
  # CR-BE-MOB-CONTRACT-01 PART 02 — User, Role, Permission & Building
  # Context. Existing backend-authoritative management routes, published
  # for the mobile context-resolution chain
  # User → Role → Permission → Client → Property → Building.
  # --------------------------------------------------------------------
  /users:
    post:
      operationId: createUser
      summary: Create a user
      description: >
        Requires `user.manage`. Creates a user with a normalized email
        (trimmed + lowercased); status defaults to ACTIVE.
      tags: [Users]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateUserRequest" }
      responses:
        "201":
          description: User created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicUser" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /users/{userId}:
    get:
      operationId: getUser
      summary: Get a user by id
      description: Requires `user.read`. Returns the public user identity.
      tags: [Users]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
      responses:
        "200":
          description: User record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicUser" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /users/{userId}/deactivate:
    post:
      operationId: deactivateUser
      summary: Deactivate a user
      description: Requires `user.manage`. Revokes active sessions for the user.
      tags: [Users]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
      responses:
        "200":
          description: User deactivated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicUser" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /users/{userId}/suspend:
    post:
      operationId: suspendUser
      summary: Suspend a user
      description: Requires `user.manage`. Revokes active sessions for the user.
      tags: [Users]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
      responses:
        "200":
          description: User suspended.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicUser" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /users/{userId}/reactivate:
    post:
      operationId: reactivateUser
      summary: Reactivate a user
      description: Requires `user.manage`.
      tags: [Users]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
      responses:
        "200":
          description: User reactivated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicUser" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /users/{userId}/whatsapp-contact:
    patch:
      operationId: updateUserWhatsAppContact
      summary: Update a user's WhatsApp contact and consent
      description: >
        CR-BE-NOTIFY-PROV-01 PART 06. Requires `user.manage`. Sets the
        authoritative E.164 WhatsApp number and/or stamps explicit consent
        (OPT_IN / OPT_OUT). OPT_IN requires a phone; clearing the phone
        clears consent; one account per number (409
        USER_WHATSAPP_PHONE_ALREADY_EXISTS). Consent gates outbound WhatsApp
        intents: only ACTIVE users with active consent are messaged.
      tags: [Users]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              { $ref: "#/components/schemas/UpdateUserWhatsAppContactRequest" }
      responses:
        "200":
          description: Contact/consent updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicUser" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /roles:
    post:
      operationId: createRole
      summary: Create a role
      description: Requires `role.manage`. Code is normalized to uppercase.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateRoleRequest" }
      responses:
        "201":
          description: Role created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicRole" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listRoles
      summary: List roles
      description: Requires `role.read`.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Role list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicRole" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /roles/{roleId}:
    get:
      operationId: getRole
      summary: Get a role by id
      description: Requires `role.read`.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/RoleIdPath"
      responses:
        "200":
          description: Role record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicRole" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /users/{userId}/roles:
    post:
      operationId: assignRoleToUser
      summary: Assign an ACTIVE role to a user
      description: >
        Requires `role.manage`. Returns the assigned role. Rejects unknown
        users, unknown/inactive roles, and duplicate active assignments.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AssignRoleRequest" }
      responses:
        "201":
          description: Role assigned.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicRole" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listUserRoles
      summary: List the ACTIVE roles assigned to a user
      description: >
        Requires `role.read`. For the authenticated user's own effective
        roles, prefer `GET /auth/me` → `access.roles`.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
      responses:
        "200":
          description: User roles.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicRole" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /permissions:
    post:
      operationId: createPermission
      summary: Create a permission
      description: Requires `permission.manage`. Code is normalized to lowercase.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePermissionRequest" }
      responses:
        "201":
          description: Permission created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicPermission" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listPermissions
      summary: List permissions
      description: Requires `permission.read`.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Permission list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicPermission" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /permissions/{permissionId}:
    get:
      operationId: getPermission
      summary: Get a permission by id
      description: Requires `permission.read`.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermissionIdPath"
      responses:
        "200":
          description: Permission record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicPermission" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /roles/{roleId}/permissions:
    post:
      operationId: assignPermissionToRole
      summary: Assign an ACTIVE permission to an ACTIVE role
      description: >
        Requires `permission.manage`. Returns the assigned permission.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/RoleIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AssignPermissionRequest" }
      responses:
        "201":
          description: Permission assigned.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicPermission" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listRolePermissions
      summary: List the ACTIVE permissions assigned to a role
      description: Requires `permission.read`.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/RoleIdPath"
      responses:
        "200":
          description: Role permissions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicPermission" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /clients:
    post:
      operationId: createClient
      summary: Create a Client
      description: Requires `client.manage`.
      tags: [Clients]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateClientRequest" }
      responses:
        "201":
          description: Client created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicClient" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listClients
      summary: List Clients
      description: >
        Requires `client.read`. For the authenticated user's reachable
        hierarchy prefer `GET /auth/me` → `context.clients`.
      tags: [Clients]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Client list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicClient" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /clients/{clientId}:
    get:
      operationId: getClient
      summary: Get a Client by id
      description: Requires `client.read`.
      tags: [Clients]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      responses:
        "200":
          description: Client record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicClient" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateClient
      summary: Update a Client
      description: Requires `client.manage`.
      tags: [Clients]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateClientRequest" }
      responses:
        "200":
          description: Client updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicClient" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /properties:
    post:
      operationId: createProperty
      summary: Create a Property
      description: Requires `property.manage`.
      tags: [Properties]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePropertyRequest" }
      responses:
        "201":
          description: Property created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicProperty" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listProperties
      summary: List Properties
      description: Requires `property.read`. Optionally filtered by `clientId`.
      tags: [Properties]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdQuery"
      responses:
        "200":
          description: Property list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicProperty" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /properties/{propertyId}:
    get:
      operationId: getProperty
      summary: Get a Property by id
      description: Requires `property.read`.
      tags: [Properties]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PropertyIdPath"
      responses:
        "200":
          description: Property record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicProperty" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /properties/{propertyId}/status:
    patch:
      operationId: updatePropertyStatus
      summary: Update a Property status
      description: Requires `property.manage`.
      tags: [Properties]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PropertyIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdatePropertyStatusRequest" }
      responses:
        "200":
          description: Property updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicProperty" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /clients/{clientId}/properties:
    get:
      operationId: listClientProperties
      summary: List Properties under a Client
      description: Requires `property.read`.
      tags: [Properties]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      responses:
        "200":
          description: Property list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicProperty" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /buildings:
    post:
      operationId: createBuilding
      summary: Create a Building
      description: Requires `building.manage`.
      tags: [Buildings]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateBuildingRequest" }
      responses:
        "201":
          description: Building created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicBuilding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildings
      summary: List Buildings
      description: >
        Requires `building.read`. Optionally filtered by `propertyId`. For the
        authenticated user's accessible set prefer `GET /auth/me` →
        `scope.buildingIds` and `GET /auth/me/buildings`.
      tags: [Buildings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PropertyIdQuery"
      responses:
        "200":
          description: Building list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicBuilding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /buildings/{buildingId}:
    get:
      operationId: getBuilding
      summary: Get a Building by id
      description: >
        Requires `building.read` AND an ACTIVE Building assignment for the
        caller (BE-02G). Inaccessible Building → 403 BUILDING_ACCESS_DENIED.
      tags: [Buildings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Building record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicBuilding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/status:
    patch:
      operationId: updateBuildingStatus
      summary: Update a Building status
      description: Requires `building.manage`.
      tags: [Buildings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateBuildingStatusRequest" }
      responses:
        "200":
          description: Building updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicBuilding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /properties/{propertyId}/buildings:
    get:
      operationId: listPropertyBuildings
      summary: List Buildings under a Property
      description: Requires `building.read`.
      tags: [Buildings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PropertyIdPath"
      responses:
        "200":
          description: Building list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicBuilding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /users/{userId}/buildings:
    post:
      operationId: assignBuildingToUser
      summary: Assign a Building to a user
      description: >
        Requires `building.manage`. Creates an ACTIVE User ↔ Building
        assignment (the BE-02G source for `GET /auth/me/buildings`).
      tags: [Building Assignments]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateBuildingAssignmentRequest" }
      responses:
        "201":
          description: Assignment created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicUserBuildingAssignment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listUserBuildings
      summary: List a user's Building assignments
      description: >
        Requires `building.read`. For the authenticated user's own accessible
        set prefer `GET /auth/me/buildings` (authentication only).
      tags: [Building Assignments]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
      responses:
        "200":
          description: Assignment list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicUserBuildingAssignment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /users/{userId}/buildings/{buildingId}:
    delete:
      operationId: deactivateBuildingAssignment
      summary: Deactivate a user's Building assignment
      description: Requires `building.manage`.
      tags: [Building Assignments]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Assignment deactivated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicUserBuildingAssignment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # --------------------------------------------------------------------
  # BE-27A — Client Configuration. Client scope only; no Building
  # override, configuration version, preview, or publish lifecycle.
  # --------------------------------------------------------------------
  /clients/{clientId}/configurations:
    post:
      operationId: createClientConfiguration
      summary: Create a Client configuration record
      description: >
        Creates one stable key/value record for an accessible Client. Keys are
        normalized to uppercase and unique per Client. Requires
        `client_configuration.manage`; permission alone does not bypass the
        caller's BE-02G Client scope.
      tags: [Client Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateClientConfigurationRequest"
      responses:
        "201":
          description: Client configuration created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ClientConfiguration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
    get:
      operationId: listClientConfigurations
      summary: List Client configuration records
      description: >
        Lists configuration administration records for one accessible Client,
        ordered by key. Requires `client_configuration.read` and Client scope.
      tags: [Client Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/ClientConfigurationStatus"
      responses:
        "200":
          description: Client configuration records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/ClientConfiguration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /clients/{clientId}/configurations/effective:
    get:
      operationId: getEffectiveClientConfiguration
      summary: Resolve effective Client configuration
      description: >
        Returns only ACTIVE key/value records for one accessible Client as an
        authoritative key/value object. BE-27A has no Building override,
        entitlement projection, version, preview, or publish behavior.
        Requires `client_configuration.read` and Client scope.
      tags: [Client Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      responses:
        "200":
          description: Effective Client configuration.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EffectiveClientConfiguration"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /client-configurations/{clientConfigurationId}:
    get:
      operationId: getClientConfiguration
      summary: Get a Client configuration record
      description: >
        Reads one administration record after checking its owning Client
        against the caller's scope. Requires `client_configuration.read`.
      tags: [Client Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ClientConfigurationIdPath"
      responses:
        "200":
          description: Client configuration record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ClientConfiguration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateClientConfiguration
      summary: Update a Client configuration value or status
      description: >
        Updates only `value` and/or record availability status. Client and key
        are immutable. Requires `client_configuration.manage` and Client scope.
      tags: [Client Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ClientConfigurationIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateClientConfigurationRequest"
      responses:
        "200":
          description: Client configuration updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ClientConfiguration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # --------------------------------------------------------------------
  # BE-27B — Building Configuration. ACTIVE Building values override the
  # effective BE-27A Client values for the same normalized key.
  # --------------------------------------------------------------------
  /buildings/{buildingId}/configurations:
    post:
      operationId: createBuildingConfiguration
      summary: Create a Building configuration record
      description: >
        Creates one key/value record for an accessible Building. Client context
        is derived through Building → Property → Client and is never accepted
        from the request body. Requires `building_configuration.manage` and
        explicit BE-02G Building scope.
      tags: [Building Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateBuildingConfigurationRequest"
      responses:
        "201":
          description: Building configuration created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/BuildingConfiguration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
    get:
      operationId: listBuildingConfigurations
      summary: List Building configuration records
      description: >
        Lists administration records for one explicitly accessible Building,
        ordered by key. Requires `building_configuration.read`.
      tags: [Building Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/ClientConfigurationStatus"
      responses:
        "200":
          description: Building configuration records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/BuildingConfiguration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /buildings/{buildingId}/configurations/effective:
    get:
      operationId: getEffectiveBuildingConfiguration
      summary: Resolve effective Building configuration
      description: >
        Resolves ACTIVE BE-27A Client values and overlays ACTIVE values for the
        selected Building; the Building value wins on key collision. Inactive
        Building records do not suppress Client fallback. Requires
        `building_configuration.read` and explicit Building scope.
      tags: [Building Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Effective Client→Building configuration.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EffectiveBuildingConfiguration"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /building-configurations/{buildingConfigurationId}:
    get:
      operationId: getBuildingConfiguration
      summary: Get a Building configuration record
      description: >
        Reads one administration record after checking its owning Building
        against the caller's explicit scope. Requires
        `building_configuration.read`.
      tags: [Building Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingConfigurationIdPath"
      responses:
        "200":
          description: Building configuration record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/BuildingConfiguration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateBuildingConfiguration
      summary: Update a Building configuration value or status
      description: >
        Updates only value and/or record availability status. Building, derived
        Client, and key are immutable. Requires `building_configuration.manage`
        and explicit Building scope.
      tags: [Building Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingConfigurationIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateBuildingConfigurationRequest"
      responses:
        "200":
          description: Building configuration updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/BuildingConfiguration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # --------------------------------------------------------------------
  # BE-27C — Client / Building scoped Module Configuration.
  # --------------------------------------------------------------------
  /clients/{clientId}/module-configurations:
    post:
      operationId: createClientModuleConfiguration
      summary: Configure a Module for a Client
      description: >
        Binds an existing ACTIVE Module key to an accessible Client with an
        enabled/disabled intent. Requires `module_configuration.manage`.
        This does not create a Module or Entitlement.
      tags: [Module Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateModuleConfigurationRequest"
      responses:
        "201":
          description: Client Module configuration created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ModuleConfiguration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
    get:
      operationId: listClientModuleConfigurations
      summary: List Client Module configuration records
      description: Requires `module_configuration.read` and accessible Client scope.
      tags: [Module Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      responses:
        "200":
          description: Client Module configuration records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/ModuleConfiguration"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /clients/{clientId}/module-configurations/effective:
    get:
      operationId: getEffectiveClientModuleConfiguration
      summary: Resolve effective Client Module configuration
      description: >
        Intersects configured Client enablement with the existing effective
        Subscription/License/Module Entitlement resolver. Unconfigured Modules
        are not implicitly enabled. Requires `module_configuration.read`.
      tags: [Module Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      responses:
        "200":
          description: Effective Client Module configuration.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EffectiveModuleConfiguration"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /buildings/{buildingId}/module-configurations:
    post:
      operationId: createBuildingModuleConfiguration
      summary: Configure a Module for a Building
      description: >
        Binds an existing ACTIVE Module key to an explicitly accessible
        Building. Client context is derived through Building → Property →
        Client. Requires `module_configuration.manage`.
      tags: [Module Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateModuleConfigurationRequest"
      responses:
        "201":
          description: Building Module configuration created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ModuleConfiguration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
    get:
      operationId: listBuildingModuleConfigurations
      summary: List Building Module configuration records
      description: Requires `module_configuration.read` and explicit Building scope.
      tags: [Module Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Building Module configuration records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/ModuleConfiguration"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /buildings/{buildingId}/module-configurations/effective:
    get:
      operationId: getEffectiveBuildingModuleConfiguration
      summary: Resolve effective Building Module configuration
      description: >
        Applies Building Module intent over Client Module intent for the same
        Module, then intersects the result with existing effective commercial
        Entitlements. Requires `module_configuration.read` and explicit
        Building scope.
      tags: [Module Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Effective Client→Building Module configuration.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EffectiveModuleConfiguration"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /module-configurations/{moduleConfigurationId}:
    get:
      operationId: getModuleConfiguration
      summary: Get a Module configuration record
      description: >
        Requires `module_configuration.read` and the record's Client or
        Building scope.
      tags: [Module Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ModuleConfigurationIdPath"
      responses:
        "200":
          description: Module configuration record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ModuleConfiguration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateModuleConfiguration
      summary: Enable or disable a Module configuration
      description: >
        Updates configured intent only. Scope and Module reference are
        immutable. Requires `module_configuration.manage` and record scope.
      tags: [Module Configuration]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ModuleConfigurationIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateModuleConfigurationRequest"
      responses:
        "200":
          description: Module configuration updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ModuleConfiguration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # BE-27D — Feature Entitlement Configuration
  /clients/{clientId}/feature-entitlements:
    post:
      operationId: createClientFeatureEntitlement
      summary: Configure a Feature for a Client
      description: Requires `feature_entitlement_configuration.manage`; references an existing ACTIVE Module.
      tags: [Feature Entitlement Configuration]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateFeatureEntitlementRequest" }
      responses:
        "201":
          description: Client Feature configuration created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FeatureEntitlementConfiguration" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listClientFeatureEntitlements
      summary: List Client Feature configurations
      tags: [Feature Entitlement Configuration]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      responses:
        "200":
          description: Client Feature configuration records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/FeatureEntitlementConfiguration" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /clients/{clientId}/feature-entitlements/effective:
    get:
      operationId: getEffectiveClientFeatureEntitlements
      summary: Resolve effective Client Feature entitlements
      description: Feature ENABLED requires its BE-27C effective Module to be enabled.
      tags: [Feature Entitlement Configuration]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      responses:
        "200":
          description: Effective Client Feature entitlements.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/EffectiveFeatureEntitlementConfiguration" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /buildings/{buildingId}/feature-entitlements:
    post:
      operationId: createBuildingFeatureEntitlement
      summary: Configure a Feature for a Building
      description: Client context is derived from the explicitly accessible Building.
      tags: [Feature Entitlement Configuration]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateFeatureEntitlementRequest" }
      responses:
        "201":
          description: Building Feature configuration created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FeatureEntitlementConfiguration" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildingFeatureEntitlements
      summary: List Building Feature configurations
      tags: [Feature Entitlement Configuration]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Building Feature configuration records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/FeatureEntitlementConfiguration" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /buildings/{buildingId}/feature-entitlements/effective:
    get:
      operationId: getEffectiveBuildingFeatureEntitlements
      summary: Resolve effective Building Feature entitlements
      description: Building state overrides Client state, then BE-27C Module availability is enforced.
      tags: [Feature Entitlement Configuration]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Effective Building Feature entitlements.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/EffectiveFeatureEntitlementConfiguration" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /feature-entitlements/{featureEntitlementConfigurationId}:
    get:
      operationId: getFeatureEntitlementConfiguration
      summary: Get a Feature entitlement configuration
      tags: [Feature Entitlement Configuration]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/FeatureEntitlementConfigurationIdPath"
      responses:
        "200":
          description: Feature entitlement configuration record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FeatureEntitlementConfiguration" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateFeatureEntitlementConfiguration
      summary: Update configured Feature state
      tags: [Feature Entitlement Configuration]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/FeatureEntitlementConfigurationIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateFeatureEntitlementRequest" }
      responses:
        "200":
          description: Feature entitlement configuration updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FeatureEntitlementConfiguration" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # BE-27E — Navigation Registry
  /clients/{clientId}/navigation-items:
    post:
      operationId: createClientNavigationItem
      summary: Create Client navigation metadata
      tags: [Navigation Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CreateNavigationItemRequest" } } }
      responses:
        "201": { description: Navigation item created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/NavigationItem" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listClientNavigationItems
      summary: List Client navigation metadata
      tags: [Navigation Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Navigation items, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/NavigationItem" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /clients/{clientId}/navigation/effective:
    get:
      operationId: getEffectiveClientNavigation
      summary: Resolve effective Client navigation
      description: Filters enabled items by effective Module, Feature, Permission and parent visibility.
      tags: [Navigation Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Effective navigation, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveNavigation" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /buildings/{buildingId}/navigation-items:
    post:
      operationId: createBuildingNavigationItem
      summary: Create Building navigation metadata
      tags: [Navigation Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CreateNavigationItemRequest" } } }
      responses:
        "201": { description: Navigation item created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/NavigationItem" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildingNavigationItems
      summary: List Building navigation metadata
      tags: [Navigation Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Navigation items, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/NavigationItem" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /buildings/{buildingId}/navigation/effective:
    get:
      operationId: getEffectiveBuildingNavigation
      summary: Resolve effective Building navigation
      description: Building keys override Client keys before authority filtering.
      tags: [Navigation Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Effective navigation, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveNavigation" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /navigation-items/{navigationItemId}:
    get:
      operationId: getNavigationItem
      summary: Get navigation metadata
      tags: [Navigation Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/NavigationItemIdPath" }]
      responses:
        "200": { description: Navigation item, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/NavigationItem" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateNavigationItem
      summary: Update navigation presentation metadata
      tags: [Navigation Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/NavigationItemIdPath" }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/UpdateNavigationItemRequest" } } }
      responses:
        "200": { description: Navigation item updated, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/NavigationItem" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # BE-27F — Workspace Registry
  /clients/{clientId}/workspaces:
    post:
      operationId: createClientWorkspace
      summary: Create Client workspace metadata
      tags: [Workspace Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateWorkspaceRequest" } } } }
      responses:
        "201": { description: Workspace created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/Workspace" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listClientWorkspaces
      summary: List Client workspaces
      tags: [Workspace Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Workspaces, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/Workspace" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /clients/{clientId}/workspaces/effective:
    get:
      operationId: getEffectiveClientWorkspaces
      summary: Resolve effective Client workspaces
      tags: [Workspace Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Effective workspaces, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveWorkspaces" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /buildings/{buildingId}/workspaces:
    post:
      operationId: createBuildingWorkspace
      summary: Create Building workspace metadata
      tags: [Workspace Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateWorkspaceRequest" } } } }
      responses:
        "201": { description: Workspace created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/Workspace" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildingWorkspaces
      summary: List Building workspaces
      tags: [Workspace Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Workspaces, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/Workspace" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /buildings/{buildingId}/workspaces/effective:
    get:
      operationId: getEffectiveBuildingWorkspaces
      summary: Resolve effective Building workspaces
      tags: [Workspace Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Effective workspaces, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveWorkspaces" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /workspaces/{workspaceId}:
    get:
      operationId: getWorkspace
      summary: Get workspace metadata
      tags: [Workspace Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkspaceIdPath" }]
      responses:
        "200": { description: Workspace, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/Workspace" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateWorkspace
      summary: Update workspace metadata
      tags: [Workspace Registry]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/WorkspaceIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateWorkspaceRequest" } } } }
      responses:
        "200": { description: Workspace updated, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/Workspace" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # BE-27G — Dashboard / Widget Configuration
  /clients/{clientId}/dashboards:
    post:
      operationId: createClientDashboard
      summary: Create Client Dashboard configuration
      tags: [Dashboard Configuration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateDashboardRequest" } } } }
      responses:
        "201": { description: Dashboard created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/Dashboard" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listClientDashboards
      summary: List Client Dashboards
      tags: [Dashboard Configuration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Dashboards, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/Dashboard" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /clients/{clientId}/dashboards/effective:
    get:
      operationId: getEffectiveClientDashboards
      summary: Resolve effective Client Dashboard/Widget composition
      tags: [Dashboard Configuration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Effective Dashboards, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveDashboards" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /buildings/{buildingId}/dashboards:
    post:
      operationId: createBuildingDashboard
      summary: Create Building Dashboard configuration
      tags: [Dashboard Configuration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateDashboardRequest" } } } }
      responses:
        "201": { description: Dashboard created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/Dashboard" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildingDashboards
      summary: List Building Dashboards
      tags: [Dashboard Configuration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Dashboards, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/Dashboard" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /buildings/{buildingId}/dashboards/effective:
    get:
      operationId: getEffectiveBuildingDashboards
      summary: Resolve effective Building Dashboard/Widget composition
      tags: [Dashboard Configuration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Effective Dashboards, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveDashboards" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /dashboards/{dashboardId}:
    get:
      operationId: getDashboard
      summary: Get Dashboard configuration
      tags: [Dashboard Configuration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/DashboardIdPath" }]
      responses:
        "200": { description: Dashboard, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/Dashboard" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateDashboard
      summary: Update Dashboard metadata
      tags: [Dashboard Configuration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/DashboardIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateDashboardRequest" } } } }
      responses:
        "200": { description: Dashboard updated, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/Dashboard" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /dashboards/{dashboardId}/widgets:
    post:
      operationId: createDashboardWidget
      summary: Add Widget composition metadata
      description: sourceReference must be an approved existing BE-23/BE-24 read model; no calculation is executed.
      tags: [Dashboard Configuration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/DashboardIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateDashboardWidgetRequest" } } } }
      responses:
        "201": { description: Widget created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/DashboardWidget" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listDashboardWidgets
      summary: List Dashboard Widgets
      tags: [Dashboard Configuration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/DashboardIdPath" }]
      responses:
        "200": { description: Widgets, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/DashboardWidget" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /dashboard-widgets/{dashboardWidgetId}:
    get:
      operationId: getDashboardWidget
      summary: Get Dashboard Widget metadata
      tags: [Dashboard Configuration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/DashboardWidgetIdPath" }]
      responses:
        "200": { description: Widget, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/DashboardWidget" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateDashboardWidget
      summary: Update Dashboard Widget metadata/order/state
      tags: [Dashboard Configuration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/DashboardWidgetIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateDashboardWidgetRequest" } } } }
      responses:
        "200": { description: Widget updated, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/DashboardWidget" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # BE-27H — Dynamic Form Administration over BE-07
  /clients/{clientId}/form-administration/templates:
    post:
      operationId: createClientAdminForm
      summary: Create Client-bound Form Template definition
      tags: [Form Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateAdminFormTemplateRequest" } } } }
      responses:
        "201": { description: Form definition created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/AdminFormDefinition" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listClientAdminForms
      summary: List Client-bound Form definitions
      tags: [Form Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Form definitions, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/AdminFormDefinition" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /buildings/{buildingId}/form-administration/templates:
    post:
      operationId: createBuildingAdminForm
      summary: Create Building-bound Form Template definition
      tags: [Form Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateAdminFormTemplateRequest" } } } }
      responses:
        "201": { description: Form definition created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/AdminFormDefinition" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    get:
      operationId: listBuildingAdminForms
      summary: List Building-bound Form definitions
      tags: [Form Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Form definitions, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/AdminFormDefinition" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /clients/{clientId}/form-administration/templates/{formTemplateId}/binding:
    post:
      operationId: bindClientAdminForm
      summary: Bind existing Form Template to Client configuration
      tags: [Form Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }, { $ref: "#/components/parameters/FormTemplateIdPath" }]
      responses:
        "201": { description: Binding created }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /buildings/{buildingId}/form-administration/templates/{formTemplateId}/binding:
    post:
      operationId: bindBuildingAdminForm
      summary: Bind existing Form Template to Building configuration
      tags: [Form Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }, { $ref: "#/components/parameters/FormTemplateIdPath" }]
      responses:
        "201": { description: Binding created }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /form-administration/templates/{formTemplateId}:
    get:
      operationId: getAdminFormDefinition
      summary: Get Form definition with bindings, Sections and Fields
      tags: [Form Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/FormTemplateIdPath" }]
      responses:
        "200": { description: Form definition, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/AdminFormDefinition" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateAdminFormTemplate
      summary: Update Form metadata/status through existing Form Template service
      tags: [Form Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/FormTemplateIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateAdminFormTemplateRequest" } } } }
      responses:
        "200": { description: Updated Form definition }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /form-administration/templates/{formTemplateId}/sections:
    post:
      operationId: createAdminFormSection
      summary: Add existing-model Form Section
      tags: [Form Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/FormTemplateIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateAdminFormSectionRequest" } } } }
      responses:
        "201": { description: Section created }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /form-administration/sections/{formSectionId}:
    patch:
      operationId: updateAdminFormSection
      summary: Update existing-model Form Section
      tags: [Form Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/FormSectionIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateAdminFormSectionRequest" } } } }
      responses:
        "200": { description: Section updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /form-administration/sections/{formSectionId}/fields:
    post:
      operationId: createAdminFormField
      summary: Add existing-model Form Field configuration
      tags: [Form Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/FormSectionIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateAdminFormFieldRequest" } } } }
      responses:
        "201": { description: Field created }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /form-administration/fields/{formFieldId}:
    patch:
      operationId: updateAdminFormField
      summary: Update existing-model Form Field configuration
      tags: [Form Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/FormFieldIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateAdminFormFieldRequest" } } } }
      responses:
        "200": { description: Field updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  # BE-27I — Checklist Administration over the existing BE-07 Checklist engine
  /clients/{clientId}/checklist-administration/templates:
    post:
      operationId: createClientAdminChecklist
      summary: Create a Client-bound Checklist definition
      tags: [Checklist Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateAdminChecklistTemplateRequest" } } } }
      responses:
        "201": { description: Checklist definition created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/AdminChecklistDefinition" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listClientAdminChecklists
      summary: List Client-bound Checklist definitions
      tags: [Checklist Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Checklist definitions, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/AdminChecklistDefinition" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /buildings/{buildingId}/checklist-administration/templates:
    post:
      operationId: createBuildingAdminChecklist
      summary: Create a Building-bound Checklist definition
      tags: [Checklist Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateAdminChecklistTemplateRequest" } } } }
      responses:
        "201": { description: Checklist definition created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/AdminChecklistDefinition" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildingAdminChecklists
      summary: List Building-bound Checklist definitions
      tags: [Checklist Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Checklist definitions, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/AdminChecklistDefinition" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /clients/{clientId}/checklist-administration/templates/{checklistTemplateId}/binding:
    post:
      operationId: bindClientAdminChecklist
      summary: Bind an existing Checklist Template to Client configuration
      tags: [Checklist Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }, { $ref: "#/components/parameters/ChecklistTemplateIdPath" }]
      responses:
        "201": { description: Binding created }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /buildings/{buildingId}/checklist-administration/templates/{checklistTemplateId}/binding:
    post:
      operationId: bindBuildingAdminChecklist
      summary: Bind an existing Checklist Template to Building configuration
      tags: [Checklist Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }, { $ref: "#/components/parameters/ChecklistTemplateIdPath" }]
      responses:
        "201": { description: Binding created }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /checklist-administration/templates/{checklistTemplateId}:
    get:
      operationId: getAdminChecklistDefinition
      summary: Get Checklist definition with bindings and Items
      tags: [Checklist Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ChecklistTemplateIdPath" }]
      responses:
        "200": { description: Checklist definition, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/AdminChecklistDefinition" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateAdminChecklistTemplate
      summary: Update existing Checklist metadata or status
      tags: [Checklist Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ChecklistTemplateIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateAdminChecklistTemplateRequest" } } } }
      responses:
        "200": { description: Checklist definition updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /checklist-administration/templates/{checklistTemplateId}/items:
    post:
      operationId: createAdminChecklistItem
      summary: Add an Item to the existing Checklist definition
      description: Measurement, evidence, and verification configuration remain authoritative in their existing APIs.
      tags: [Checklist Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ChecklistTemplateIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateAdminChecklistItemRequest" } } } }
      responses:
        "201": { description: Checklist Item created }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /checklist-administration/items/{checklistItemId}:
    patch:
      operationId: updateAdminChecklistItem
      summary: Update core existing Checklist Item configuration
      description: Measurement metadata is read-only here; evidence and verification remain in their existing APIs.
      tags: [Checklist Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ChecklistItemIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateAdminChecklistItemRequest" } } } }
      responses:
        "200": { description: Checklist Item updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /checklist-administration/item-options/{checklistItemOptionId}:
    patch:
      operationId: updateAdminChecklistItemOption
      summary: Update label, display order, or status of a Checklist Item Option
      description: |
        Option code is immutable and never accepted in the payload. Options are
        soft-deactivated via status INACTIVE; no DELETE endpoint exists. Scope
        follows the parent Checklist Template access rules.
      tags: [Checklist Administration]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ChecklistItemOptionIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateAdminChecklistItemOptionRequest" } } } }
      responses:
        "200": { description: Checklist Item Option updated, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/AdminChecklistItemOption" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # BE-27J — typed Operational Settings facade over BE-27A/BE-27B
  /clients/{clientId}/operational-settings:
    post:
      operationId: createClientOperationalSetting
      summary: Create an allowlisted Client Operational Setting
      description: Requires `client_configuration.manage`. Settings are metadata and never bypass owning-domain validation.
      tags: [Operational Settings]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateOperationalSettingRequest" } } } }
      responses:
        "201": { description: Operational Setting created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/OperationalSetting" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listClientOperationalSettings
      summary: List Client Operational Settings
      tags: [Operational Settings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
        - $ref: "#/components/parameters/ConfigurationStatusQuery"
      responses:
        "200": { description: Operational Settings, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/OperationalSetting" } } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /clients/{clientId}/operational-settings/effective:
    get:
      operationId: getEffectiveClientOperationalSettings
      summary: Get effective ACTIVE Client Operational Settings
      tags: [Operational Settings]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Effective Operational Settings, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveOperationalSettings" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /client-operational-settings/{operationalSettingId}:
    get:
      operationId: getClientOperationalSetting
      summary: Get a Client Operational Setting
      tags: [Operational Settings]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/OperationalSettingIdPath" }]
      responses:
        "200": { description: Operational Setting, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/OperationalSetting" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateClientOperationalSetting
      summary: Update value, enabled state, or status
      tags: [Operational Settings]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/OperationalSettingIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateOperationalSettingRequest" } } } }
      responses:
        "200": { description: Operational Setting updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /buildings/{buildingId}/operational-settings:
    post:
      operationId: createBuildingOperationalSetting
      summary: Create an allowlisted Building Operational Setting
      description: Requires `building_configuration.manage`; Building ownership is derived through Property to Client.
      tags: [Operational Settings]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateOperationalSettingRequest" } } } }
      responses:
        "201": { description: Operational Setting created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/OperationalSetting" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildingOperationalSettings
      summary: List Building Operational Settings
      tags: [Operational Settings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - $ref: "#/components/parameters/ConfigurationStatusQuery"
      responses:
        "200": { description: Operational Settings, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/OperationalSetting" } } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /buildings/{buildingId}/operational-settings/effective:
    get:
      operationId: getEffectiveBuildingOperationalSettings
      summary: Get effective Client to Building Operational Settings
      description: ACTIVE Building records replace ACTIVE Client records by key; enabled false remains an explicit effective disable.
      tags: [Operational Settings]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Effective Operational Settings, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveOperationalSettings" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /building-operational-settings/{operationalSettingId}:
    get:
      operationId: getBuildingOperationalSetting
      summary: Get a Building Operational Setting
      tags: [Operational Settings]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/OperationalSettingIdPath" }]
      responses:
        "200": { description: Operational Setting, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/OperationalSetting" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateBuildingOperationalSetting
      summary: Update Building value, enabled state, or status
      tags: [Operational Settings]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/OperationalSettingIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateOperationalSettingRequest" } } } }
      responses:
        "200": { description: Operational Setting updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # BE-27K — presentation metadata over authoritative BE-03 entities
  /clients/{clientId}/organization-presentation:
    post:
      operationId: createClientOrganizationPresentation
      summary: Create Client Organization Presentation Configuration
      description: Stores display-only labels and entity references. Requires `client_configuration.manage`.
      tags: [Organization Presentation]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateOrganizationPresentationRequest" } } } }
      responses:
        "201": { description: Presentation Configuration created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/OrganizationPresentationConfiguration" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: getClientOrganizationPresentation
      summary: Get Client Organization Presentation Configuration
      tags: [Organization Presentation]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Presentation Configuration, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/OrganizationPresentationConfiguration" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /clients/{clientId}/organization-presentation/effective:
    get:
      operationId: getEffectiveClientOrganizationPresentation
      summary: Get effective Client Organization Presentation
      description: Resolves label/display-name fallbacks from authoritative BE-03 entities; inactive hierarchy entries cannot be made visible.
      tags: [Organization Presentation]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Effective Presentation, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveOrganizationPresentation" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /client-organization-presentation/{organizationPresentationId}:
    get:
      operationId: getClientOrganizationPresentationById
      summary: Get Client Presentation Configuration by id
      tags: [Organization Presentation]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/OrganizationPresentationIdPath" }]
      responses:
        "200": { description: Presentation Configuration, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/OrganizationPresentationConfiguration" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateClientOrganizationPresentation
      summary: Update Client Presentation metadata/status
      tags: [Organization Presentation]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/OrganizationPresentationIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateOrganizationPresentationRequest" } } } }
      responses:
        "200": { description: Presentation Configuration updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /buildings/{buildingId}/organization-presentation:
    post:
      operationId: createBuildingOrganizationPresentation
      summary: Create Building Organization Presentation Configuration
      description: Building ownership is derived through Property to Client. Requires `building_configuration.manage`.
      tags: [Organization Presentation]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateOrganizationPresentationRequest" } } } }
      responses:
        "201": { description: Presentation Configuration created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/OrganizationPresentationConfiguration" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: getBuildingOrganizationPresentation
      summary: Get Building Organization Presentation Configuration
      tags: [Organization Presentation]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Presentation Configuration, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/OrganizationPresentationConfiguration" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /buildings/{buildingId}/organization-presentation/effective:
    get:
      operationId: getEffectiveBuildingOrganizationPresentation
      summary: Get effective Client to Building Organization Presentation
      description: An ACTIVE Building artifact replaces the ACTIVE Client artifact as a whole; hierarchy authority remains BE-03.
      tags: [Organization Presentation]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Effective Presentation, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveOrganizationPresentation" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /building-organization-presentation/{organizationPresentationId}:
    get:
      operationId: getBuildingOrganizationPresentationById
      summary: Get Building Presentation Configuration by id
      tags: [Organization Presentation]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/OrganizationPresentationIdPath" }]
      responses:
        "200": { description: Presentation Configuration, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/OrganizationPresentationConfiguration" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateBuildingOrganizationPresentation
      summary: Update Building Presentation metadata/status
      tags: [Organization Presentation]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/OrganizationPresentationIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateOrganizationPresentationRequest" } } } }
      responses:
        "200": { description: Presentation Configuration updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # BE-27L — scoped safe CMS content foundation
  /clients/{clientId}/cms-content:
    post:
      operationId: createClientCmsContent
      summary: Create Client CMS Content
      description: Accepts safe plain text/Markdown only. Requires `client_configuration.manage`.
      tags: [CMS Content]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateCmsContentRequest" } } } }
      responses:
        "201": { description: CMS Content created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/CmsContent" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listClientCmsContent
      summary: List Client CMS Content administration records
      tags: [CMS Content]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
        - $ref: "#/components/parameters/CmsContentTypeQuery"
        - $ref: "#/components/parameters/CmsContentStatusQuery"
      responses:
        "200": { description: CMS Content list, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/CmsContent" } } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /clients/{clientId}/cms-content/effective:
    get:
      operationId: getEffectiveClientCmsContent
      summary: Get PUBLISHED effective Client CMS Content
      tags: [CMS Content]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
        - $ref: "#/components/parameters/CmsContentTypeQuery"
      responses:
        "200": { description: Effective CMS Content, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveCmsContent" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /client-cms-content/{cmsContentId}:
    get:
      operationId: getClientCmsContent
      summary: Get Client CMS Content by id
      tags: [CMS Content]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/CmsContentIdPath" }]
      responses:
        "200": { description: CMS Content, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/CmsContent" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateClientCmsContent
      summary: Update or publish Client CMS Content
      tags: [CMS Content]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/CmsContentIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateCmsContentRequest" } } } }
      responses:
        "200": { description: CMS Content updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /buildings/{buildingId}/cms-content:
    post:
      operationId: createBuildingCmsContent
      summary: Create Building CMS Content
      description: Building ownership is derived through Property to Client. Requires `building_configuration.manage`.
      tags: [CMS Content]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateCmsContentRequest" } } } }
      responses:
        "201": { description: CMS Content created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/CmsContent" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildingCmsContent
      summary: List Building CMS Content administration records
      tags: [CMS Content]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - $ref: "#/components/parameters/CmsContentTypeQuery"
        - $ref: "#/components/parameters/CmsContentStatusQuery"
      responses:
        "200": { description: CMS Content list, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/CmsContent" } } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /buildings/{buildingId}/cms-content/effective:
    get:
      operationId: getEffectiveBuildingCmsContent
      summary: Get effective Client to Building CMS Content
      description: PUBLISHED Building content replaces PUBLISHED Client content with the same contentType and slug.
      tags: [CMS Content]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - $ref: "#/components/parameters/CmsContentTypeQuery"
      responses:
        "200": { description: Effective CMS Content, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveCmsContent" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /building-cms-content/{cmsContentId}:
    get:
      operationId: getBuildingCmsContent
      summary: Get Building CMS Content by id
      tags: [CMS Content]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/CmsContentIdPath" }]
      responses:
        "200": { description: CMS Content, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/CmsContent" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateBuildingCmsContent
      summary: Update or publish Building CMS Content
      tags: [CMS Content]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/CmsContentIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateCmsContentRequest" } } } }
      responses:
        "200": { description: CMS Content updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # BE-27M — constrained Branding & White Label configuration
  /clients/{clientId}/branding:
    post:
      operationId: createClientBranding
      summary: Create Client Branding Configuration
      description: Uses constrained metadata/theme tokens and an opaque logo storage reference. Requires `client_configuration.manage`.
      tags: [Branding]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateBrandingRequest" } } } }
      responses:
        "201": { description: Branding Configuration created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/BrandingConfiguration" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: getClientBranding
      summary: Get Client Branding Configuration
      tags: [Branding]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Branding Configuration, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/BrandingConfiguration" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /clients/{clientId}/branding/effective:
    get:
      operationId: getEffectiveClientBranding
      summary: Get effective ACTIVE Client Branding
      tags: [Branding]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ClientIdPath" }]
      responses:
        "200": { description: Effective Branding, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveBranding" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /client-branding/{brandingConfigurationId}:
    get:
      operationId: getClientBrandingById
      summary: Get Client Branding Configuration by id
      tags: [Branding]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BrandingConfigurationIdPath" }]
      responses:
        "200": { description: Branding Configuration, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/BrandingConfiguration" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateClientBranding
      summary: Update Client Branding metadata/status
      tags: [Branding]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BrandingConfigurationIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateBrandingRequest" } } } }
      responses:
        "200": { description: Branding Configuration updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /buildings/{buildingId}/branding:
    post:
      operationId: createBuildingBranding
      summary: Create Building Branding Configuration
      description: Building ownership is derived through Property to Client. Requires `building_configuration.manage`.
      tags: [Branding]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateBrandingRequest" } } } }
      responses:
        "201": { description: Branding Configuration created, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/BrandingConfiguration" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: getBuildingBranding
      summary: Get Building Branding Configuration
      tags: [Branding]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Branding Configuration, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/BrandingConfiguration" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /buildings/{buildingId}/branding/effective:
    get:
      operationId: getEffectiveBuildingBranding
      summary: Get effective Client to Building Branding
      description: An ACTIVE Building profile replaces the ACTIVE Client profile as a whole.
      tags: [Branding]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BuildingIdPath" }]
      responses:
        "200": { description: Effective Branding, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveBranding" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /building-branding/{brandingConfigurationId}:
    get:
      operationId: getBuildingBrandingById
      summary: Get Building Branding Configuration by id
      tags: [Branding]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BrandingConfigurationIdPath" }]
      responses:
        "200": { description: Branding Configuration, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/BrandingConfiguration" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateBuildingBranding
      summary: Update Building Branding metadata/status
      tags: [Branding]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/BrandingConfigurationIdPath" }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateBrandingRequest" } } } }
      responses:
        "200": { description: Branding Configuration updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # BE-27N — immutable version history; no lifecycle mutation endpoints
  /configuration-sources/{sourceType}/{sourceConfigurationId}/versions:
    get:
      operationId: listConfigurationVersions
      summary: List immutable versions for a configuration source
      description: Requires the existing source-domain read permission and Client/Building Data Scope.
      tags: [Configuration Versioning]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ConfigurationVersionSourceTypePath"
        - $ref: "#/components/parameters/SourceConfigurationIdPath"
      responses:
        "200": { description: Configuration version history, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/ConfigurationVersion" } } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /configuration-versions/{configurationVersionId}:
    get:
      operationId: getConfigurationVersion
      summary: Get one immutable configuration version
      tags: [Configuration Versioning]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ConfigurationVersionIdPath" }]
      responses:
        "200": { description: Configuration version, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/ConfigurationVersion" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /configuration-versions/{configurationVersionId}/validations:
    get:
      operationId: listConfigurationValidations
      summary: List append-only validation results
      tags: [Configuration Versioning]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ConfigurationVersionIdPath" }]
      responses:
        "200": { description: Validation results, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/ConfigurationValidation" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /configuration-versions/{configurationVersionId}/draft:
    post:
      operationId: createConfigurationDraft
      summary: Clone an immutable version into a new DRAFT
      description: Configuration changes continue through existing source-domain APIs, which save new DRAFT snapshots.
      tags: [Configuration Versioning]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ConfigurationVersionIdPath" }]
      responses:
        "201": { description: New DRAFT version, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/ConfigurationVersion" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /configuration-versions/{configurationVersionId}/validate:
    post:
      operationId: validateConfigurationVersion
      summary: Validate a DRAFT against its authoritative source and scope
      tags: [Configuration Versioning]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ConfigurationVersionIdPath" }]
      responses:
        "200": { description: Validation outcome; failed validation leaves lifecycleStatus DRAFT, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/ConfigurationValidationOutcome" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
  /configuration-versions/{configurationVersionId}/publish:
    post:
      operationId: publishConfigurationVersion
      summary: Publish a successfully VALIDATED version
      tags: [Configuration Versioning]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ConfigurationVersionIdPath" }]
      responses:
        "200": { description: PUBLISHED version }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /configuration-versions/{configurationVersionId}/activate:
    post:
      operationId: activateConfigurationVersion
      summary: Activate PUBLISHED version and supersede prior ACTIVE version
      tags: [Configuration Versioning]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ConfigurationVersionIdPath" }]
      responses:
        "200": { description: ACTIVE version }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  # BE-27P — safe non-active configuration preview
  /configuration-versions/{configurationVersionId}/preview-contexts:
    post:
      operationId: createConfigurationPreview
      summary: Create a caller-bound preview for a VALIDATED/PUBLISHED version
      tags: [Configuration Preview]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ConfigurationVersionIdPath" }]
      requestBody: { required: false, content: { application/json: { schema: { $ref: "#/components/schemas/CreateConfigurationPreviewRequest" } } } }
      responses:
        "201": { description: Preview context, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/ConfigurationPreviewContext" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /configuration-preview-contexts/{configurationPreviewId}:
    get:
      operationId: getConfigurationPreview
      summary: Get PREVIEW status and metadata
      tags: [Configuration Preview]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ConfigurationPreviewIdPath" }]
      responses:
        "200": { description: Preview metadata, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/ConfigurationPreviewContext" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /configuration-preview-contexts/{configurationPreviewId}/effective:
    get:
      operationId: getEffectiveConfigurationPreview
      summary: Resolve effective PREVIEW configuration/navigation/workspace
      description: Clearly isolated from ACTIVE context; this endpoint has no publication side effects.
      tags: [Configuration Preview]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ConfigurationPreviewIdPath" }]
      responses:
        "200": { description: Effective PREVIEW projection, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EffectiveConfigurationPreview" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "410": { description: Preview context expired or revoked }
  /configuration-preview-contexts/{configurationPreviewId}/revoke:
    post:
      operationId: revokeConfigurationPreview
      summary: Revoke a caller-owned preview context
      tags: [Configuration Preview]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ConfigurationPreviewIdPath" }]
      responses:
        "200": { description: REVOKED preview context }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # BE-27Q — read-only projection over append-only operational_events
  /configuration-audit:
    get:
      operationId: listConfigurationAudit
      summary: List scoped configuration audit/history events
      description: Requires `operational_event.read`; no update/delete endpoints exist.
      tags: [Configuration Audit]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: configurationId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: configurationVersionId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: sourceType
          in: query
          schema: { $ref: "#/components/schemas/ConfigurationVersionSourceType" }
        - name: action
          in: query
          schema: { $ref: "#/components/schemas/ConfigurationAuditAction" }
        - name: buildingId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: from
          in: query
          schema: { type: string, format: date-time }
        - name: to
          in: query
          schema: { type: string, format: date-time }
      responses:
        "200": { description: Configuration audit events, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/ConfigurationAuditEvent" } } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /configuration-audit/{configurationAuditEventId}:
    get:
      operationId: getConfigurationAudit
      summary: Get one scoped configuration audit event
      tags: [Configuration Audit]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: "#/components/parameters/ConfigurationAuditEventIdPath" }]
      responses:
        "200": { description: Configuration audit event, content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/ConfigurationAuditEvent" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /reports/export:
    get:
      operationId: getReportingExport
      summary: Export an authoritative reporting dataset as structured JSON
      description: >
        Neutral KPI/table projection over an owning BE-23 KPI or the BE-24
        Operations Command Center. Values are copied without alternate
        calculations. The endpoint never renders charts or generates PDF/Excel
        files. Requires `reporting_export.read`; the Management dataset also
        requires `management_read_model.read`.
      tags: [Reporting Export]
      security:
        - bearerAuth: []
      parameters:
        - name: dataset
          in: query
          required: true
          schema:
            $ref: "#/components/schemas/ReportArchiveDataset"
        - name: clientId
          in: query
          required: false
          description: BE-24 Management dataset Client narrowing.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Accessible single Building; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: BE-24 comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: date
          in: query
          required: false
          description: BE-24 Daily Operations UTC date.
          schema:
            type: string
            format: date
        - name: dateFrom
          in: query
          required: false
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          schema:
            type: string
        - name: graceMinutes
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 10080
        - name: overdueAfterDays
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 365
        - name: expiringWithinDays
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 365
        - name: interval
          in: query
          required: false
          schema:
            type: string
            enum: [DAY, MONTH, YEAR]
        - name: includeSubMeters
          in: query
          required: false
          schema:
            type: boolean
      responses:
        "200":
          description: Export-ready structured JSON dataset resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ReportingExport"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /reporting/exports:
    post:
      operationId: createReportExport
      x-required-permission: report_export.generate
      x-building-scoped: true
      summary: Create and synchronously generate a governed report archive
      description: >
        Creates one governed export request, resolves the authenticated
        caller's Client/Building scope, builds one canonical reporting
        snapshot, renders exactly the requested format, and returns the
        archive metadata. The request is idempotent when `Idempotency-Key` is
        supplied: the same key and canonical request fingerprint return the
        same archive execution; reusing the key with different request data
        returns 409. A deliberate regeneration is a new request/archive ID.
        No storage reference or public storage URL is returned.
      tags: [Reporting Export]
      security:
        - bearerAuth: []
      parameters:
        - name: Idempotency-Key
          in: header
          required: false
          description: >
            Optional client request key. It is hashed server-side and is not
            returned. Same key plus the same canonical request returns the
            existing execution; same key plus different request data conflicts.
          schema:
            type: string
            maxLength: 200
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateReportArchiveRequest"
      responses:
        "201":
          description: >
            The durable archive request. PART 05 normally returns COMPLETED
            for a successful synchronous generation or FAILED with safe
            failure metadata when generation did not succeed.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ReportArchive"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /reporting/exports/{id}:
    get:
      operationId: getReportExport
      x-required-permission: report_archive.read
      x-building-scoped: true
      summary: Get one scoped report export archive status
      description: >
        Returns the same durable archive resource used by the export request.
        The record is visible only when the caller has current access to its
        Client and complete recorded Building scope. Inaccessible or unknown
        archive IDs use the same scoped not-found boundary.
      tags: [Reporting Export]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Scoped report archive status and provenance.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ReportArchive"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /reporting/archives:
    get:
      operationId: listReportArchives
      x-required-permission: report_archive.read
      x-building-scoped: true
      summary: List accessible report archives
      description: >
        Lists only archives whose Client is reachable and whose complete
        recorded Building scope remains within the caller's current accessible
        Building set. Results are newest first and always paginated. A
        clientId/buildingId filter only narrows the scoped result; it never
        widens access.
      tags: [Reporting Export]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          description: Matches single- or multi-Building archives containing this Building.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dataset
          in: query
          schema: { $ref: "#/components/schemas/ReportArchiveDataset" }
        - name: format
          in: query
          schema: { $ref: "#/components/schemas/ReportArchiveFormat" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/ReportArchiveStatus" }
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: pageSize
          in: query
          schema: { type: integer, minimum: 1, maximum: 200, default: 50 }
      responses:
        "200":
          description: Scoped paginated report archive list.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportArchiveListEnvelope"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /reporting/archives/{id}:
    get:
      operationId: getReportArchive
      x-required-permission: report_archive.read
      x-building-scoped: true
      summary: Get one scoped report archive
      description: >
        Returns immutable request, source/as-of provenance, generation status,
        safe artifact metadata, retention state, and safe failure metadata.
        Internal storage references and public storage URLs are omitted.
      tags: [Reporting Export]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Scoped report archive.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ReportArchive"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /reporting/archives/{id}/download:
    get:
      operationId: downloadReportArchive
      x-required-permission: report_archive.read
      x-building-scoped: true
      summary: Download one completed report archive
      description: >
        Serves the already-stored completed artifact after the same current
        Client/Building authorization and stored size/SHA-256 verification as
        archive read. The operation never regenerates the report. The response
        uses `Content-Disposition: attachment` with a backend-generated safe
        filename (`filename*` UTF-8 encoding) and never exposes an internal
        storage key, permanent URL, or anonymous download URL. REQUESTED,
        GENERATING, FAILED, and PURGED archives are not downloadable.
      tags: [Reporting Export]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: >
            Stored report bytes. The concrete media type is the archive's
            requested format: JSON, CSV, XLSX, or PDF.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
            Content-Disposition:
              required: true
              description: attachment with the stored backend-generated safe filename.
              schema: { type: string }
            Content-Length:
              required: true
              description: Byte length verified against archive metadata.
              schema: { type: integer, format: int64 }
          content:
            application/json:
              schema: { type: string, format: binary }
            text/csv:
              schema: { type: string, format: binary }
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet:
              schema: { type: string, format: binary }
            application/pdf:
              schema: { type: string, format: binary }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "500":
          description: Stored artifact failed integrity verification.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"

  /management/read-scope:
    get:
      operationId: getManagementReadScope
      summary: Resolve Management & Owner read scope
      description: >
        Resolves the authenticated caller's Client and single/multi-Building
        read scope from the same hierarchy as the effective-user context.
        Selectors only narrow explicit Building assignments. Requires
        `management_read_model.read`. This foundation endpoint returns no
        dashboard, summary, or KPI data.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          description: Optional Client narrowing within accessible Buildings.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: >
            Explicit accessible Building set. Accepts comma-separated values or
            repeated query parameters; mutually exclusive with buildingId.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          description: BE-23-compatible ISO-8601 lower reporting boundary (UTC).
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          description: >
            BE-23-compatible ISO-8601 upper boundary. A date-only value includes
            the whole UTC day; a datetime is an exclusive upper instant.
          schema:
            type: string
      responses:
        "200":
          description: Authorized read scope and reporting period resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementReadScopeContext"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/daily-operations:
    get:
      operationId: getManagementDailyOperations
      summary: Management / Owner daily operations read model
      description: >
        Read-only daily aggregate for the authorized Client/Building scope.
        Scheduled/completed/in-progress/open-finding values reuse BE-10I;
        overdue reuses BE-23G; critical source counters reuse BE-23F2 and
        BE-23H. Requires `management_read_model.read`. This is not the
        Operations Command Center.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: date
          in: query
          required: false
          description: UTC operational date; defaults to today (UTC).
          schema:
            type: string
            format: date
        - name: graceMinutes
          in: query
          required: false
          description: BE-23G overdue tolerance; defaults to 0.
          schema:
            type: integer
            minimum: 0
            maximum: 10080
            default: 0
      responses:
        "200":
          description: Daily Operations read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementDailyOperations"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/work-order-summary:
    get:
      operationId: getManagementWorkOrderSummary
      summary: Management / Owner Work Order Summary read model
      description: >
        Read-only BE-08 Work Order aggregate for the authorized Client/Building
        scope. Status and priority counters use exact authoritative values;
        verification uses APPROVED BE-08I reviews. Because BE-08 has no due
        date or SLA, overdue is an explicit elapsed-created-age reporting
        threshold compatible with BE-23H, not a Work Order lifecycle state.
        Requires `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          description: BE-23-compatible ISO-8601 lower created-at boundary (UTC).
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          description: BE-23-compatible inclusive date or exclusive datetime boundary.
          schema:
            type: string
        - name: overdueAfterDays
          in: query
          required: false
          description: >
            Elapsed created-age threshold for unfinished Work Orders. Reporting
            filter only; not a persisted due date or SLA. Defaults to 7.
          schema:
            type: integer
            minimum: 0
            maximum: 365
            default: 7
      responses:
        "200":
          description: Work Order Summary read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementWorkOrderSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/pending-approvals:
    get:
      operationId: getManagementPendingApprovals
      summary: Management / Owner Pending Approval read model
      description: >
        Read-only composition of existing Permit, Procurement, Tenant, and
        Document approval foundations. The endpoint does not create or decide
        approvals. Available actions are delegated to source authorities where
        those read models exist. Requires `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          description: BE-23-compatible ISO-8601 lower submitted-at boundary (UTC).
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          description: BE-23-compatible inclusive date or exclusive datetime boundary.
          schema:
            type: string
      responses:
        "200":
          description: Pending Approval read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementPendingApproval"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/critical-findings:
    get:
      operationId: getManagementCriticalFindings
      summary: Management / Owner Critical Findings read model
      description: >
        Read-only BE-09 Finding projection for the authorized Client/Building
        scope. A Client's critical tier is its highest ACTIVE configured
        finding-severity rank; no global severity code or threshold is
        invented. Outstanding statuses reuse BE-23F2. Because BE-09 has no due
        date/SLA, overdue is an explicit elapsed-reported-age reporting state.
        Requires `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          description: BE-23-compatible ISO-8601 lower reported-at boundary (UTC).
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          description: BE-23-compatible inclusive date or exclusive datetime boundary.
          schema:
            type: string
        - name: overdueAfterDays
          in: query
          required: false
          description: >
            Elapsed reported-age threshold for outstanding Findings. Reporting
            filter only; not a persisted due date or SLA. Defaults to 7.
          schema:
            type: integer
            minimum: 0
            maximum: 365
            default: 7
      responses:
        "200":
          description: Critical Findings read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementCriticalFindings"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/workforce-summary:
    get:
      operationId: getManagementWorkforceSummary
      summary: Management / Owner Workforce Summary read model
      description: >
        Read-only workforce aggregate for the authorized Client/Building scope.
        Headcount, assignment, overdue, and man-hour values are projected by
        BE-23G's authoritative KPI helpers. Team/Department breakdowns use the
        same ACTIVE BE-03 Workforce-Building binding. Requires
        `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          description: BE-23-compatible ISO-8601 assignment-period lower boundary (UTC).
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          description: BE-23-compatible inclusive date or exclusive datetime boundary.
          schema:
            type: string
        - name: graceMinutes
          in: query
          required: false
          description: BE-23G overdue tolerance; defaults to 0.
          schema:
            type: integer
            minimum: 0
            maximum: 10080
            default: 0
      responses:
        "200":
          description: Workforce Summary read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementWorkforceSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/vendor-summary:
    get:
      operationId: getManagementVendorSummary
      summary: Management / Owner Vendor Summary and Performance read model
      description: >
        Read-only Vendor aggregate for the authorized Client/Building scope.
        Assigned/completed/overdue work, completion rate, and per-vendor
        performance are projected by BE-23H. Active Vendors use the
        authoritative ACTIVE Vendor-Building relationship. Requires
        `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          description: BE-23-compatible ISO-8601 assignment-period lower boundary (UTC).
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          description: BE-23-compatible inclusive date or exclusive datetime boundary.
          schema:
            type: string
        - name: overdueAfterDays
          in: query
          required: false
          description: BE-23H elapsed assignment-age threshold; defaults to 7.
          schema:
            type: integer
            minimum: 0
            maximum: 365
            default: 7
      responses:
        "200":
          description: Vendor Summary and Performance read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementVendorSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/tenant-service-summary:
    get:
      operationId: getManagementTenantServiceSummary
      summary: Management / Owner Tenant Service Summary read model
      description: >
        Read-only Tenant Service and Complaint aggregate for the authorized
        Client/Building scope. Total/open/completed/completion-rate values are
        projected by BE-23H. In-progress fulfilment uses authoritative BE-08
        Work Order status; complaint counts use exact BE-14 states. Requires
        `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          description: BE-23-compatible ISO-8601 request-period lower boundary (UTC).
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          description: BE-23-compatible inclusive date or exclusive datetime boundary.
          schema:
            type: string
        - name: overdueAfterDays
          in: query
          required: false
          description: >
            Elapsed requested-age threshold for outstanding requests. Reporting
            filter only; not a persisted due date or SLA. Defaults to 7.
          schema:
            type: integer
            minimum: 0
            maximum: 365
            default: 7
      responses:
        "200":
          description: Tenant Service Summary read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementTenantServiceSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/asset-registry-compliance:
    get:
      operationId: getManagementAssetRegistryCompliance
      summary: Management / Owner Asset Registry and Compliance read model
      description: >
        Read-only BE-05 registry, warranty, certification, compliance, and
        location snapshot for the authorized Client/Building scope. Current
        coverage/effectiveness delegates to existing BE-05 helpers. No
        maintenance or reliability KPI is included. Requires
        `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: expiringWithinDays
          in: query
          required: false
          description: Forward compliance-expiry window; defaults to 30 days.
          schema:
            type: integer
            minimum: 0
            maximum: 365
            default: 30
      responses:
        "200":
          description: Asset Registry and Compliance read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementAssetRegistryCompliance"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/asset-reliability-work:
    get:
      operationId: getManagementAssetReliabilityWork
      summary: Management / Owner Asset Reliability and Work read model
      description: >
        Read-only BE-05/07/08/10/21 reliability and maintenance projection for
        the authorized Client/Building scope. Work Order and task statuses stay
        authoritative; percentage rounding and overdue grace reuse BE-23.
        PART 05A warranty/certification compliance is not repeated. Requires
        `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          description: BE-23-compatible ISO-8601 operational-period lower boundary (UTC).
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          description: BE-23-compatible inclusive date or exclusive datetime boundary.
          schema:
            type: string
        - name: graceMinutes
          in: query
          required: false
          description: BE-23 overdue tolerance for maintenance tasks; defaults to 0.
          schema:
            type: integer
            minimum: 0
            maximum: 10080
            default: 0
      responses:
        "200":
          description: Asset Reliability and Work read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementAssetReliabilityWork"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/utility-summary:
    get:
      operationId: getManagementUtilitySummary
      summary: Management / Owner Utility Summary read model
      description: >
        Read-only Utility projection for the authorized Client/Building scope.
        Electricity, water, gas, abnormality, verification and period values
        are returned by BE-23I, which delegates consumption calculations to
        BE-18M. Tenant rows are direct BE-18M grouped results. No billing,
        tariff, or cost logic is included. Requires `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          description: BE-23-compatible ISO-8601 consumption-period lower boundary (UTC).
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          description: BE-23-compatible inclusive date or datetime boundary.
          schema:
            type: string
        - name: interval
          in: query
          required: false
          description: BE-23I comparison/trend interval; defaults to MONTH.
          schema:
            type: string
            enum: [DAY, MONTH, YEAR]
            default: MONTH
        - name: includeSubMeters
          in: query
          required: false
          description: >
            Includes Sub Meters when true. Defaults to false to preserve
            BE-18M double-counting protection.
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: Utility Summary read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementUtilitySummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/financial-summary:
    get:
      operationId: getManagementFinancialSummary
      summary: Management / Owner Financial Summary read model
      description: >
        Read-only composition of authoritative BE-19I Building summaries.
        Multi-Building monetary totals are partitioned by Client because BE-19
        stores no currency dimension; values are never summed across Clients.
        No ledger, tax, accounting, payroll, or billing mutation is introduced.
        Requires `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          description: BE-23-compatible lower financial calendar boundary (UTC date).
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          description: BE-23-compatible upper financial calendar boundary (UTC date).
          schema:
            type: string
      responses:
        "200":
          description: Financial Summary read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementFinancialSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  # ------------------------------------------------------------------
  # CR-BE-FIN-RPT-01 — Currency-safe basic financial reporting
  # ------------------------------------------------------------------
  /buildings/{buildingId}/financial-summary:
    get:
      operationId: getBasicFinancialSummary
      summary: Currency-safe basic financial summary per Building
      description: >
        CR-BE-FIN-RPT-01 PART 05 — Currency-safe basic financial reporting.
        BE-19I lightweight query/read-model aggregation only; no persisted
        ledger or accounting model. All monetary totals are grouped by exact
        currency_code (currency for utility_bills). No cross-currency
        arithmetic without explicit conversion. UNKNOWN currency (historical
        NULL) is readable, countable, amount-measurable, source-traceable,
        excluded from known-currency totals and net arithmetic, surfaced
        explicitly as reporting gap. Payment/outstanding amounts inherit
        invoice currency and are grouped by that currency. Single known
        currency + zero UNKNOWN may populate legacy scalar convenience fields;
        otherwise those scalars are null and monetarySummary.byCurrency is
        authoritative. No FX conversion in base report; FX remains read-side
        optional additive only. Requires `basic_financial_reporting.read` and
        an ACTIVE Building assignment.

        UNKNOWN definition: historical monetary data without a governed
        currency snapshot. It is not IDR, not Client base/default currency,
        not reporting currency, excluded from byCurrency arithmetic and net
        calculations, never inferred.

        Legacy scalar compatibility: single known currency + zero UNKNOWN →
        legacy monetary scalar fields may contain values from that single
        currency; multiple known currencies OR any UNKNOWN → unsafe legacy
        monetary scalar fields are null. Counts remain scalar.
      tags: [Basic Financial Reporting]
      security:
        - bearerAuth: []
      parameters:
        - name: buildingId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: periodFrom
          in: query
          required: false
          description: YYYY-MM-DD inclusive lower bound for business dates.
          schema:
            type: string
            format: date
        - name: periodTo
          in: query
          required: false
          description: YYYY-MM-DD inclusive upper bound for business dates.
          schema:
            type: string
            format: date
        - name: tenantCompanyId
          in: query
          required: false
          description: Optional Tenant Company filter (UUID).
          schema:
            $ref: "#/components/schemas/Uuid"
      x-required-permission: basic_financial_reporting.read
      x-building-scoped: true
      responses:
        "200":
          description: Currency-safe financial summary resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/BasicFinancialSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  # ------------------------------------------------------------------
  # CR-BE-FIN-01 — Operational Finance budget, binding and aggregation API
  # ------------------------------------------------------------------
  /buildings/{buildingId}/operational-budgets:
    post:
      operationId: createOperationalBudget
      summary: Create an Operational Budget
      description: >
        Creates a Building-scoped DRAFT Operational Budget. Client ownership is
        derived from Building → Property → Client. Currency is explicit and no
        source transaction is created or bound by this operation.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: buildingId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateOperationalBudgetRequest" }
      responses:
        "201": { $ref: "#/components/responses/OperationalBudgetSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildingOperationalBudgets
      summary: List Operational Budgets for a Building
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: buildingId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/OperationalBudgetStatus" }
        - name: periodFrom
          in: query
          schema: { type: string, format: date }
        - name: periodTo
          in: query
          schema: { type: string, format: date }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetListSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /operational-budgets:
    get:
      operationId: listOperationalBudgets
      summary: List Accessible Operational Budgets
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: buildingId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/OperationalBudgetStatus" }
        - name: periodFrom
          in: query
          schema: { type: string, format: date }
        - name: periodTo
          in: query
          schema: { type: string, format: date }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetListSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /operational-budgets/{id}:
    get:
      operationId: getOperationalBudget
      summary: Get an Operational Budget
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateOperationalBudget
      summary: Update a Draft Operational Budget
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateOperationalBudgetRequest" }
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "404": { $ref: "#/components/responses/NotFound" }

  /operational-budgets/{id}/activate:
    post:
      operationId: activateOperationalBudget
      summary: Activate an Operational Budget
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /operational-budgets/{id}/close:
    post:
      operationId: closeOperationalBudget
      summary: Close an Operational Budget
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /operational-budgets/{id}/cancel:
    post:
      operationId: cancelOperationalBudget
      summary: Cancel an Operational Budget
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /operational-budgets/{budgetId}/categories:
    post:
      operationId: createOperationalBudgetCategory
      summary: Create an Operational Budget Category
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateOperationalBudgetCategoryRequest" }
      responses:
        "201": { $ref: "#/components/responses/OperationalBudgetCategorySuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listOperationalBudgetCategories
      summary: List Operational Budget Categories
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetCategoryListSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /operational-budget-categories/{id}:
    get:
      operationId: getOperationalBudgetCategory
      summary: Get an Operational Budget Category
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetCategorySuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateOperationalBudgetCategory
      summary: Update a Draft Operational Budget Category
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateOperationalBudgetCategoryRequest" }
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetCategorySuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      operationId: deleteOperationalBudgetCategory
      summary: Delete a Draft Operational Budget Category
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      responses:
        "204": { description: Category deleted. }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /operational-budgets/{budgetId}/source-bindings:
    post:
      operationId: createOperationalBudgetSourceBinding
      summary: Bind an Authoritative Operational Cost Source
      description: >
        Creates typed lineage only. Exactly one supported source reference is
        accepted. No source amount is stored or aggregated by this operation.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateOperationalBudgetSourceBindingRequest" }
      responses:
        "201": { $ref: "#/components/responses/OperationalBudgetSourceBindingSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listOperationalBudgetSourceBindings
      summary: List Operational Budget Source Bindings
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetSourceBindingListSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /operational-budget-source-bindings/{id}:
    get:
      operationId: getOperationalBudgetSourceBinding
      summary: Get an Operational Budget Source Binding
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetSourceBindingSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /operational-budget-source-bindings/{id}/remove:
    post:
      operationId: removeOperationalBudgetSourceBinding
      summary: Remove an Operational Budget Source Binding
      description: Removes the active binding while retaining lineage history.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetSourceBindingSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /operational-budgets/{budgetId}/aggregation:
    get:
      operationId: getOperationalBudgetAggregation
      summary: Get Read-Time Operational Budget Aggregation
      description: >
        Read-time aggregation over active typed source bindings. Amounts are
        read from authoritative source modules and are never persisted here.
        Ambiguous and currency-incompatible contributions fail closed.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetAggregationSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /operational-budgets/{budgetId}/summary:
    get:
      operationId: getOperationalBudgetAggregationSummary
      summary: Get Operational Budget Aggregation Summary
      description: Alias of the read-time PART 04 aggregation contract.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetAggregationSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # ── CR-BE-COMM-VAR-01 — Operational commitment ledger & variance ──────────
  /operational-budgets/{id}/overspend-policy:
    post:
      operationId: changeOperationalBudgetOverspendPolicy
      summary: Change Operational Budget Overspend Policy
      description: >
        Narrow, separately authorised overspend-policy transition for a DRAFT or
        ACTIVE budget. It requires BOTH operational_budget.manage and
        operational_budget.override, plus a mandatory reason, and is audited
        through the existing OPERATIONAL_BUDGET_OVERSPEND_POLICY_CHANGED event.
        The generic budget PATCH remains DRAFT-only. Administering a budget must
        never imply the authority to exceed approved spending.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.override
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ChangeOperationalBudgetOverspendPolicyRequest" }
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetOverspendPolicySuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /operational-budgets/{budgetId}/commitments:
    post:
      operationId: createOperationalCommitment
      summary: Create Manual Operational Commitment
      description: >
        Creates a manual/estimated commitment against an ACTIVE budget. The
        decision runs in one transaction opened with a budget-row lock, and
        available budget is re-read inside that lock, so two concurrent
        approvals can never consume the same remainder. Overspend is rejected
        (409 OPERATIONAL_BUDGET_OVERSPEND_REJECTED) unless the budget policy is
        ALLOW_WITH_OVERRIDE, the caller holds operational_budget.override, and
        an explicit overspendOverrideReason is supplied. Creation is idempotent
        per (budget, idempotencyKey).
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateOperationalCommitmentRequest" }
      responses:
        "201": { $ref: "#/components/responses/OperationalCommitmentSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listOperationalCommitments
      summary: List Operational Commitments
      description: Commitment ledger headers for one budget.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: budgetCategoryId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/OperationalCommitmentStatus" }
        - name: origin
          in: query
          required: false
          schema: { $ref: "#/components/schemas/OperationalCommitmentOrigin" }
        - name: workOrderId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalCommitmentListSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /operational-budgets/{budgetId}/commitments/from-purchase-order-line:
    post:
      operationId: createPurchaseOrderLineCommitment
      summary: Create Commitment From An Issued Purchase Order Line
      description: >
        Raises the commitment for one priced, ISSUED Purchase Order line — the
        only automatic commitment authority in the repository. Amount, currency,
        Client, Building, vendor and Material Request lineage are DERIVED from
        the line and must not be supplied. The caller provides only the explicit
        cost category and an idempotency key. A Purchase Order already
        represented by a CR-BE-FIN-01 source binding, or already carrying a live
        commitment, is refused so one obligation is never counted twice.
        Approved Material Requests, reservations and receivings carry no price
        and never create a commitment.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePurchaseOrderLineCommitmentRequest" }
      responses:
        "201": { $ref: "#/components/responses/OperationalCommitmentSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /operational-commitments/{id}:
    get:
      operationId: getOperationalCommitment
      summary: Get Operational Commitment With Its Ledger Entries
      description: >
        The commitment header plus its append-only ledger entries. signedAmount
        is the effect on the open amount, so the entries always sum to
        openAmount.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalCommitmentDetailSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /operational-commitments/{id}/adjust:
    post:
      operationId: adjustOperationalCommitment
      summary: Adjust An Open Operational Commitment
      description: >
        Appends a signed adjustment entry: a positive amount increases the
        obligation (subject to the same overspend control as creation), a
        negative amount decreases it. A decrease below the already-actualized
        amount is rejected. Amounts are never edited in place.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AdjustOperationalCommitmentRequest" }
      responses:
        "200": { $ref: "#/components/responses/OperationalCommitmentSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /operational-commitments/{id}/release:
    post:
      operationId: releaseOperationalCommitment
      summary: Release The Remainder Of An Operational Commitment
      description: >
        Releases exactly the remaining open amount and closes the commitment.
        This is the correct path for a partially actualized obligation that will
        not complete.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CloseOperationalCommitmentRequest" }
      responses:
        "200": { $ref: "#/components/responses/OperationalCommitmentSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /operational-commitments/{id}/cancel:
    post:
      operationId: cancelOperationalCommitment
      summary: Cancel An Operational Commitment
      description: >
        Withdraws an obligation that never became actual. Rejected once anything
        has been actualized (409) — release the remainder instead.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CloseOperationalCommitmentRequest" }
      responses:
        "200": { $ref: "#/components/responses/OperationalCommitmentSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /operational-budgets/{budgetId}/variance:
    get:
      operationId: getOperationalBudgetVariance
      summary: Get Operational Budget Variance
      description: >
        Derived at read time; nothing is persisted or cached. Ledger and legacy
        contributions are reported separately as well as combined. A legacy
        source binding contributes only while no ledger commitment represents
        the same Purchase Order. Unallocated actual (costed material usages and
        verified vendor invoices with no commitment) is a BUDGET-scope figure
        only, because an unmatched source carries no cost category. Currency and
        period mismatches fail closed and are listed in controls.exclusions.
        Recorded gaps (B-02, B-03) are described in gaps and never valued.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetVarianceSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /operational-budget-variance:
    get:
      operationId: listOperationalBudgetVariance
      summary: List Operational Budget Variance By Building And Period
      description: >
        Portfolio breakdown: one variance summary per accessible budget. Building
        and period are the budget's own dimensions, so no new aggregation axis is
        introduced.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/OperationalBudgetStatus" }
        - name: periodFrom
          in: query
          required: false
          schema: { type: string, format: date }
        - name: periodTo
          in: query
          required: false
          schema: { type: string, format: date }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetVarianceListSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /buildings/{buildingId}/operational-budget-variance:
    get:
      operationId: listBuildingOperationalBudgetVariance
      summary: List Operational Budget Variance For One Building
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: buildingId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/OperationalBudgetStatus" }
        - name: periodFrom
          in: query
          required: false
          schema: { type: string, format: date }
        - name: periodTo
          in: query
          required: false
          schema: { type: string, format: date }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetVarianceListSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /operational-budgets/{budgetId}/traceability:
    get:
      operationId: getOperationalBudgetTraceability
      summary: Get Operational Budget Source Traceability
      description: >
        Flat source-transaction rows behind the variance figures, each with its
        classification, commitment/binding identity and lineage keys (purchase
        order, purchase order line, vendor, work order, material request, vendor
        invoice, material usage) and an exclusion reason when it does not count.
      tags: [Operational Finance]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      x-required-permission: operational_budget.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/OperationalBudgetTraceabilitySuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /management/buildings/{buildingId}/operational-finance-summary:
    get:
      operationId: getManagementBuildingOperationalFinanceSummary
      summary: Building Operational Finance Management Summary
      description: >
        Read-only Building-level Operational Finance summary for an explicit
        period. Budget amounts and category amounts are delegated to the
        existing CR-BE-FIN-01 PART 04 aggregation through the PART 05
        management contracts. No source transaction payloads, accounting,
        currency conversion or fiscal-year logic is introduced.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: buildingId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: periodStart
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: periodEnd
          in: query
          required: true
          schema:
            type: string
            format: date
      x-required-permission: management_read_model.read
      x-building-scoped: true
      responses:
        "200":
          description: Building Operational Finance summary resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementBuildingOperationalFinance"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"

  /management/operational-finance/budgets/{budgetId}/summary:
    get:
      operationId: getManagementOperationalBudgetSummary
      summary: Management Operational Budget Summary
      description: >
        Read-only Operational Finance budget summary delegated to CR-BE-FIN-01
        PART 04. Returns one Building budget with planned, actual, committed,
        remaining and variance amounts. Ambiguous, unresolved or currency-
        incompatible source contributions remain excluded by the PART 04
        fail-closed rules. This is additive and does not modify the semantics
        of `/management/financial-summary`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      x-required-permission: management_read_model.read
      x-building-scoped: true
      responses:
        "200":
          description: Operational Budget summary resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementOperationalBudgetSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /management/operational-finance/budgets/{budgetId}/categories:
    get:
      operationId: getManagementOperationalBudgetCategories
      summary: Management Operational Budget Category Breakdown
      description: >
        Read-only category breakdown delegated to CR-BE-FIN-01 PART 04. Each
        category preserves the authoritative planned, actual, committed,
        remaining and variance values. No source transaction payloads are
        duplicated in this response.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: budgetId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      x-required-permission: management_read_model.read
      x-building-scoped: true
      responses:
        "200":
          description: Operational Budget category breakdown resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementOperationalBudgetCategories"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /management/operational-kpi:
    get:
      operationId: getManagementOperationalKpi
      summary: Management / Owner Operational KPI read model
      description: >
        Read-only operational KPI for the authorized Client/Building scope.
        Assignment counts and completion rate reuse BE-23G; critical source
        counters reuse BE-23F2/BE-23H. Work Order, Finding and Incident values
        read authoritative source statuses. No source workflow is duplicated.
        Requires `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          description: BE-23-compatible ISO-8601 operational-period lower boundary (UTC).
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          description: BE-23-compatible inclusive date or exclusive datetime boundary.
          schema:
            type: string
        - name: graceMinutes
          in: query
          required: false
          description: BE-23G overdue tolerance; defaults to 0.
          schema:
            type: integer
            minimum: 0
            maximum: 10080
            default: 0
      responses:
        "200":
          description: Operational KPI read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementOperationalKpi"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/building-performance:
    get:
      operationId: getManagementBuildingPerformance
      summary: Management / Owner Building Performance read model
      description: >
        Lightweight per-Building composition of existing BE-24 summaries and
        BE-23/domain reports. No score, weighting, ranking, domain formula, or
        Portfolio aggregation is introduced. Requires
        `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Single Building selector; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          schema:
            type: string
        - name: graceMinutes
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 10080
            default: 0
        - name: overdueAfterDays
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 365
            default: 7
        - name: expiringWithinDays
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 365
            default: 30
        - name: interval
          in: query
          required: false
          schema:
            type: string
            enum: [DAY, MONTH, YEAR]
            default: MONTH
        - name: includeSubMeters
          in: query
          required: false
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: Per-Building Performance rows resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementBuildingPerformance"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/portfolio-overview:
    get:
      operationId: getManagementPortfolioOverview
      summary: Management / Owner Portfolio Overview read model
      description: >
        Multi-Building composition of exact scope-wide BE-24 summaries plus
        PART 08A per-Building rows. Rates are copied from scope-wide KPI owners,
        not averaged. Monetary summaries remain partitioned by Client. No
        composite score, ranking, chart, or UI metadata is introduced.
        Requires `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Accessible single Building; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: dateFrom
          in: query
          required: false
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          schema:
            type: string
        - name: graceMinutes
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 10080
            default: 0
        - name: overdueAfterDays
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 365
            default: 7
        - name: expiringWithinDays
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 365
            default: 30
        - name: interval
          in: query
          required: false
          schema:
            type: string
            enum: [DAY, MONTH, YEAR]
            default: MONTH
        - name: includeSubMeters
          in: query
          required: false
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: Portfolio Overview resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementPortfolioOverview"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /management/operations-command-center:
    get:
      operationId: getManagementOperationsCommandCenter
      summary: Management / Owner Operations Command Center read model
      description: >
        Thin facade over completed BE-24 read models for an accessible single-
        or multi-Building scope. Component values are copied without direct
        domain queries, alternate KPI formulas, cross-Client monetary totals,
        chart metadata, or UI metadata. Requires `management_read_model.read`.
      tags: [Management Read Models]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          description: Accessible single Building; mutually exclusive with buildingIds.
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingIds
          in: query
          required: false
          description: Comma-separated or repeated accessible Building ids.
          schema:
            type: array
            uniqueItems: true
            items:
              $ref: "#/components/schemas/Uuid"
          style: form
          explode: false
        - name: date
          in: query
          required: false
          description: UTC operational day delegated to Daily Operations.
          schema:
            type: string
            format: date
        - name: dateFrom
          in: query
          required: false
          schema:
            type: string
        - name: dateTo
          in: query
          required: false
          schema:
            type: string
        - name: graceMinutes
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 10080
            default: 0
        - name: overdueAfterDays
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 365
            default: 7
        - name: expiringWithinDays
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            maximum: 365
            default: 30
        - name: interval
          in: query
          required: false
          schema:
            type: string
            enum: [DAY, MONTH, YEAR]
            default: MONTH
        - name: includeSubMeters
          in: query
          required: false
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: Operations Command Center read model resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ManagementOperationsCommandCenter"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /evidence/{evidenceId}/file:
    post:
      operationId: uploadEvidenceFile
      summary: Upload an evidence file
      description: >
        Attaches the file bytes of an existing evidence submission
        (multipart field `file`). The submission row is the file metadata
        record: `fileReference` becomes the backend-generated storage key and
        `originalFileName`, `mimeType`, `fileSize` are updated. Requires
        `evidence.manage`. File access is bound to the evidence record's
        Client scope (BE-02G).
        CR-BE-DOC-CONTROL-01: the server computes and persists the SHA-256
        of the exact stored bytes atomically with this upload
        (`contentSha256` / `contentHashedAt` / `hashAlgorithm`); callers can
        never supply a trusted hash. A PURGED retention tombstone is
        terminal and rejects re-upload with 400.
      tags: [Evidence]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/EvidenceIdPathParam"
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                  description: Max 50 MB; MIME type must match the evidence type.
      responses:
        "201":
          description: File uploaded and metadata updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EvidenceFileMetadata"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    get:
      operationId: getEvidenceFileMetadata
      summary: Resolve evidence file metadata
      description: >
        Returns the metadata of an evidence submission (the evidence file
        metadata record). Requires `evidence.read`. Access is bound to
        the evidence record's Client scope.
      tags: [Evidence]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/EvidenceIdPathParam"
      responses:
        "200":
          description: Evidence file metadata.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EvidenceFileMetadata"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /evidence/{evidenceId}/file/content:
    get:
      operationId: downloadEvidenceFile
      summary: Download an evidence file
      description: >
        Streams the stored evidence file bytes (`Content-Type` and
        `Content-Disposition` reflect the stored metadata). Requires
        `evidence.read`. Access is bound to the evidence record's Client
        scope; internal storage paths are never exposed.
        CR-BE-DOC-CONTROL-01: for a PURGED retention tombstone the binary
        was disposed — this endpoint returns 404 while the metadata record
        remains readable.
      tags: [Evidence]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/EvidenceIdPathParam"
      responses:
        "200":
          description: File bytes.
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # ------------------------------------------------------------------
  # CR-BE-DOC-CONTROL-01 — Evidence integrity verification + retention
  # hold + retention policy administration.
  # ------------------------------------------------------------------
  /evidence/{evidenceId}/integrity-verification:
    post:
      operationId: verifyEvidenceIntegrity
      summary: Verify evidence integrity
      description: >
        Server-side integrity verification: re-reads the stored binary
        through the storage abstraction, recomputes its SHA-256, and
        compares it against the authoritative hash persisted at upload time.
        Persists `lastIntegrityStatus` / `lastIntegrityCheckedAt` and
        records an operational event (`EVIDENCE_INTEGRITY_VERIFIED` on
        success, `EVIDENCE_INTEGRITY_FAILED` otherwise). Outcomes: VERIFIED,
        MISMATCH (stored bytes changed outside Asentra), NOT_HASHED
        (legacy/unhashed evidence — never backfilled), FILE_UNAVAILABLE
        (stored object unreadable). Verification NEVER mutates or deletes
        evidence. The request carries no body — a caller-supplied hash is
        never accepted. Requires `evidence.manage`; access is bound to the
        evidence record's Client/Building scope. A PURGED retention
        tombstone is not verifiable (400).
      tags: [Evidence]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/EvidenceIdPathParam"
      responses:
        "200":
          description: Verification outcome (persisted and audited).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EvidenceIntegrityVerification"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /evidence/{evidenceId}/retention-hold:
    post:
      operationId: setEvidenceRetentionHold
      summary: Set a retention hold on evidence
      description: >
        Places a minimal legal/operational hold on the evidence row. A held
        row is never purged by retention execution (the hold blocks only the
        final purge — due marking still happens). Audited as
        EVIDENCE_RETENTION_HOLD_SET. Requires `evidence.manage`; PURGED
        tombstones and already-held rows are rejected with 400.
      tags: [Evidence]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/EvidenceIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason:
                  type: string
                  maxLength: 500
      responses:
        "200":
          description: Evidence with the hold set.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EvidenceResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      operationId: clearEvidenceRetentionHold
      summary: Clear a retention hold on evidence
      description: >
        Clears the retention hold; the next retention execution pass may
        purge the row if it is due. Audited as
        EVIDENCE_RETENTION_HOLD_CLEARED. Requires `evidence.manage`; a row
        that is not on hold is rejected with 400.
      tags: [Evidence]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/EvidenceIdPathParam"
      responses:
        "200":
          description: Evidence with the hold cleared.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EvidenceResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /clients/{clientId}/evidence-retention-policies:
    post:
      operationId: createEvidenceRetentionPolicy
      summary: Create an evidence retention policy
      description: >
        Creates a Client-scoped (optionally Building-scoped) evidence
        retention policy. Administration reuses the existing
        `client_configuration.manage` permission (SLA-definition precedent);
        the Building must belong to the Client. Duplicate code per Client →
        409. Activation is prospective only: existing evidence is never
        swept (deferred legacy adoption is a separate future CR).
      tags: [Configuration]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EvidenceRetentionPolicyCreateRequest"
      responses:
        "201":
          description: Created retention policy.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EvidenceRetentionPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
    get:
      operationId: listEvidenceRetentionPolicies
      summary: List evidence retention policies
      description: >
        Lists the Client's evidence retention policies (optional
        `buildingId` / `status` filters). Requires
        `client_configuration.read` and Client access.
      tags: [Configuration]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [ACTIVE, INACTIVE]
      responses:
        "200":
          description: Retention policies of the Client.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/EvidenceRetentionPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /evidence-retention-policies/{id}:
    get:
      operationId: getEvidenceRetentionPolicy
      summary: Get an evidence retention policy
      description: Requires `client_configuration.read` and access to the policy's Client/Building scope.
      tags: [Configuration]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The retention policy.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EvidenceRetentionPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateEvidenceRetentionPolicy
      summary: Update an evidence retention policy
      description: >
        Updates policy fields (name, applicability, retentionDays, status,
        effective window). Requires `client_configuration.manage`. Edits are
        prospective only: evidence already governed keeps its immutable
        snapshot (`retentionDaysSnapshot` / `retainedUntil` never change).
      tags: [Configuration]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EvidenceRetentionPolicyUpdateRequest"
      responses:
        "200":
          description: The updated retention policy.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EvidenceRetentionPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # ------------------------------------------------------------------
  # BE-25A — Mobile execution surface (task / checklist / evidence /
  # review / operational event / finding workflow / QR resolution).
  # List endpoints support the opt-in mobile pagination convention
  # (?page=&pageSize=) → meta: MobilePaginationMeta.
  # ------------------------------------------------------------------
  /tasks:
    get:
      operationId: listTasks
      summary: List generated tasks
      description: >
        Lists generated tasks within the caller's accessible Buildings
        (BE-02G). Optional `clientId` narrows within the accessible set.
        Pagination is opt-in via `page`/`pageSize`; without them the full
        scoped list is returned (Web compatibility). Requires `task.read`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          description: Optional Client filter (must be within the accessible scope).
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/PageSizeParam"
      responses:
        "200":
          description: Scoped task list (full list or requested page).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/Task"
                      meta:
                        $ref: "#/components/schemas/MobilePaginationMeta"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /tasks/{taskId}:
    get:
      operationId: getTask
      summary: Get a generated task
      description: Requires `task.read` and Building-scope access to the task.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TaskIdPathParam"
      responses:
        "200":
          description: The task.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/Task"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /tasks/{taskId}/start:
    post:
      operationId: startTask
      summary: Start an assigned task
      description: >
        Transitions an OPEN/ASSIGNED task to IN_PROGRESS. The active WORKFORCE
        assignee must be the calling user. Requires `task.manage`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TaskIdPathParam"
      responses:
        "200":
          description: The updated task.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/Task"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /tasks/{taskId}/complete:
    post:
      operationId: completeTask
      summary: Complete an in-progress task
      description: >
        Transitions an IN_PROGRESS task to COMPLETED. Requires `task.manage`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TaskIdPathParam"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                completionNotes:
                  type: string
                  nullable: true
      responses:
        "200":
          description: The completed task.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/Task"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /tasks/{taskId}/cancel:
    post:
      operationId: cancelTask
      summary: Cancel a task
      description: Requires `task.manage`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TaskIdPathParam"
      responses:
        "200":
          description: The cancelled task.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/Task"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /workforce/{workforceId}/tasks:
    get:
      operationId: listWorkforceTasks
      summary: List active task assignments for a workforce profile
      description: >
        Active task assignments (WORKFORCE) with their generated tasks, scoped
        to the caller's accessible Buildings. Pagination is opt-in.
        Requires `task.read`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkforceIdPathParam"
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/PageSizeParam"
      responses:
        "200":
          description: Scoped assignment list (full list or requested page).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/TaskAssignment"
                      meta:
                        $ref: "#/components/schemas/MobilePaginationMeta"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /teams/{teamId}/tasks:
    get:
      operationId: listTeamTasks
      summary: List active task assignments for a team
      description: >
        Active task assignments (TEAM) with their generated tasks, scoped to
        the caller's accessible Buildings. Pagination is opt-in.
        Requires `task.read`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TeamIdPathParam"
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/PageSizeParam"
      responses:
        "200":
          description: Scoped assignment list (full list or requested page).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/TaskAssignment"
                      meta:
                        $ref: "#/components/schemas/MobilePaginationMeta"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /tasks/{taskId}/assignments:
    get:
      operationId: listTaskAssignments
      summary: List assignments for a task
      description: >
        The assigned personnel for a generated task (BE-07 assignment
        authority). Scoped to the task's accessible Building (BE-02G): a task
        outside the caller's scope yields 403 BUILDING_ACCESS_DENIED.
        Requires `task.read`. NOTE: this read returns the assignment records;
        the authoritative per-caller `availableActions` for the task is
        exposed through `GET /mobile/assignments`, not here.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TaskIdPathParam"
      responses:
        "200":
          description: Task assignment records (ACTIVE and historical).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/TaskAssignment"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      operationId: assignTask
      summary: Assign a task
      description: >
        MUTATION — assigns a task to a WORKFORCE / TEAM assignee. Requires
        `task.manage` and Building-scope access to the task.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TaskIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AssignTaskRequest" }
      responses:
        "201":
          description: The created task assignment.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TaskAssignment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # --------------------------------------------------------------------
  # CR-BE-MOB-CONTRACT-01 PART 03 — Work Order mobile read surface.
  # Read-only projection of the BE-08 authorities; no lifecycle changes.
  # --------------------------------------------------------------------
  /buildings/{buildingId}/work-orders:
    get:
      operationId: listBuildingWorkOrders
      summary: List Work Orders for a Building
      description: >
        Work Orders within one explicitly accessible Building (BE-02G:
        `requireBuildingAccess`). Requires `work_order.read`. Optional
        filters: `status`, `workType`, `workRequestId`.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/WorkOrderStatus"
        - name: workType
          in: query
          required: false
          schema:
            type: string
        - name: workRequestId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: Work Order list for the accessible Building.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/WorkOrder"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: createWorkOrder
      summary: Create a Work Order for a Building
      description: >
        MUTATION — creates a Work Order in the accessible Building. Requires
        `work_order.manage` and an ACTIVE Building assignment
        (`requireBuildingAccess`). `createdByUserId` is derived from the
        session.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateWorkOrderRequest" }
      responses:
        "201":
          description: The created Work Order.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrder" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /work-orders/team:
    get:
      operationId: listTeamWorkOrders
      x-required-permission: work_order.read
      x-building-scoped: true
      summary: List Work Orders assigned to the caller's team
      description: >-
        CR-BE-MOB-03 PART 04 (TMW-04) — self-service team-scoped Work Order
        read for supervisors. The caller's team is derived from the
        authenticated session: the linked, ACTIVE BE-03C Workforce Profile's
        `team_id` (the same authority `getMobileMyTeam` uses) — never from a
        caller-supplied id. A Work Order is included when its ACTIVE BE-08E
        assignment targets the team itself (`assignee_type = 'TEAM'`) or an
        ACTIVE member of the team (`assignee_type = 'WORKFORCE'` whose
        profile is in the team). Results are constrained in SQL to the
        caller's BE-02G accessible Building set, so no cross-Building /
        cross-Client Work Order can be returned even though no building
        parameter is supplied. Requires `work_order.read` (existing RBAC).
        Read-only: Work Order ids, statuses and lifecycle are the existing
        authoritative BE-08 ones; nothing is written and no mobile facade is
        created. Optional `status` / `workType` / `workRequestId` filters
        narrow the list (same vocabulary as `listBuildingWorkOrders`). A
        caller with no linked ACTIVE profile, no team, or no accessible
        Building gets an empty list.
      tags: [Work Orders]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/WorkOrderStatus" }
        - name: workType
          in: query
          required: false
          schema: { type: string }
          description: Data-driven work type code (uppercase).
        - name: workRequestId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: The team-scoped Work Order list (empty when the caller has no team).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/WorkOrder"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /work-orders/{workOrderId}:
    get:
      operationId: getWorkOrder
      summary: Get a Work Order by id
      description: >
        The authoritative Work Order record. Requires `work_order.read` AND an
        ACTIVE Building assignment for the Work Order's Building (BE-02G) —
        permission alone is never enough; inaccessible → 403
        BUILDING_ACCESS_DENIED. `status` is authoritative; clients must not
        infer transitions from it (per-caller actions come from
        `GET /mobile/assignments`).
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      responses:
        "200":
          description: The Work Order.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/WorkOrder"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateWorkOrder
      summary: Update Work Order metadata
      description: >
        MUTATION — updates title/description/workType. Requires
        `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateWorkOrderRequest" }
      responses:
        "200":
          description: The updated Work Order.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrder" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/assignments:
    get:
      operationId: listWorkOrderAssignments
      summary: List assignments for a Work Order
      description: >
        The assigned personnel/vendor records for a Work Order (BE-08E).
        Requires `work_order.read` and Building access to the Work Order.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      responses:
        "200":
          description: Work Order assignment records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/WorkOrderAssignment"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      operationId: assignWorkOrder
      summary: Assign a Work Order
      description: >
        MUTATION — assigns the Work Order to a WORKFORCE / TEAM / VENDOR /
        VENDOR_WORKFORCE assignee. Requires `work_order.manage` and Building
        access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AssignWorkOrderRequest" }
      responses:
        "201":
          description: The created assignment.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderAssignment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/assignments/current:
    get:
      operationId: getCurrentWorkOrderAssignment
      summary: Get the current (ACTIVE) Work Order assignment
      description: >
        The single ACTIVE assignment for the Work Order, or `null` when none.
        Requires `work_order.read` and Building access to the Work Order.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      responses:
        "200":
          description: The current assignment (or null).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        allOf:
                          - $ref: "#/components/schemas/WorkOrderAssignment"
                        nullable: true
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /work-orders/{workOrderId}/actions:
    get:
      operationId: listWorkOrderActions
      summary: List Work Order action history
      description: >
        The append-only execution action HISTORY (BE-08F): acknowledge, start,
        hold, resume, note, cancel. This is NOT an available-actions read
        model — clients must not derive valid next actions from this list; the
        authoritative per-caller `availableActions` is in
        `GET /mobile/assignments`. Requires `work_order.read` and Building
        access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      responses:
        "200":
          description: Work Order action history.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/WorkOrderAction"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /work-orders/{workOrderId}/context:
    get:
      operationId: getWorkOrderContext
      summary: Get the Work Order location / asset / operational context
      description: >
        The authoritative resolved context (BE-08D): the bound Asset,
        Functional Location, and the projected operational hierarchy
        (Client → Property → Building → …). Projected through the BE-04H
        resolver, never stored. Requires `work_order.read` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      responses:
        "200":
          description: The resolved Work Order context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/WorkOrderContext"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateWorkOrderContext
      summary: Bind/clear Work Order asset/location context
      description: >
        MUTATION — binds or clears the Work Order's Asset / Functional
        Location (BE-08D). Requires `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateWorkOrderContextRequest" }
      responses:
        "200":
          description: The updated Work Order context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/completion:
    get:
      operationId: getWorkOrderCompletion
      summary: Get the Work Order completion subset
      description: >
        The completion subset of a Work Order (BE-08H): status plus completion
        provenance/summary/notes. Requires `work_order.read` and Building
        access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      responses:
        "200":
          description: The Work Order completion subset.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/WorkOrderCompletion"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # --------------------------------------------------------------------
  # CR-BE-MOB-CONTRACT-01 PART 07 — Work Order execution actions,
  # evidence, verification, history, and management mutations (BE-08),
  # Task assignment mutations (BE-07), Entitlement (BE-02C), and Asset
  # sub-resource reads (BE-05). All already implemented; published for
  # completeness.
  # --------------------------------------------------------------------
  /tasks/{taskId}/assignments/{assignmentId}:
    patch:
      operationId: updateTaskAssignment
      summary: Update a task assignment status
      description: >
        MUTATION — updates a task assignment status (ACTIVE/INACTIVE).
        Requires `task.manage` and Building-scope access to the task.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/TaskIdPathParam"
        - name: assignmentId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status:
                  type: string
                  enum: [ACTIVE, INACTIVE]
      responses:
        "200":
          description: The updated task assignment.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TaskAssignment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/priority:
    patch:
      operationId: updateWorkOrderPriority
      summary: Update Work Order priority
      description: MUTATION — requires `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [priority]
              properties:
                priority: { $ref: "#/components/schemas/WorkOrderPriority" }
      responses:
        "200":
          description: The updated Work Order.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrder" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/status:
    patch:
      operationId: updateWorkOrderStatus
      summary: Transition the Work Order status
      description: >
        MUTATION — applies a lifecycle transition via the BE-08C transition
        engine (`WORK_ORDER_TRANSITIONS`). Invalid transitions are rejected
        deterministically. Requires `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status: { $ref: "#/components/schemas/WorkOrderStatus" }
      responses:
        "200":
          description: The updated Work Order.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrder" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/bast-requirement:
    patch:
      operationId: updateWorkOrderBastRequirement
      summary: Update the Work Order BAST requirement policy
      description: MUTATION — requires `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [bastRequirement]
              properties:
                bastRequirement: { $ref: "#/components/schemas/BastRequirement" }
      responses:
        "200":
          description: The updated Work Order.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrder" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/assignments/{assignmentId}:
    patch:
      operationId: updateWorkOrderAssignment
      summary: Deactivate or reassign a Work Order assignment
      description: Requires `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
        - name: assignmentId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - { $ref: "#/components/schemas/AssignWorkOrderRequest" }
                - $ref: "#/components/schemas/DeactivateWorkOrderAssignmentRequest"
      responses:
        "200":
          description: The updated assignment.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderAssignment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/acknowledge:
    post:
      operationId: acknowledgeWorkOrder
      summary: Acknowledge the Work Order
      description: >
        MUTATION — records an ACKNOWLEDGED execution action (BE-08F).
        Requires `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/WorkOrderActionRequest" }
      responses:
        "201":
          description: The recorded action.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderAction" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/start:
    post:
      operationId: startWorkOrder
      summary: Start the Work Order
      description: >
        MUTATION — records a STARTED execution action. Requires
        `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/WorkOrderActionRequest" }
      responses:
        "201":
          description: The recorded action.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderAction" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/hold:
    post:
      operationId: holdWorkOrder
      summary: Hold the Work Order
      description: >
        MUTATION — records an ON_HOLD execution action. Requires
        `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/WorkOrderActionRequest" }
      responses:
        "201":
          description: The recorded action.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderAction" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/resume:
    post:
      operationId: resumeWorkOrder
      summary: Resume the Work Order
      description: >
        MUTATION — records a RESUMED execution action. Requires
        `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/WorkOrderActionRequest" }
      responses:
        "201":
          description: The recorded action.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderAction" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/notes:
    post:
      operationId: addWorkOrderNote
      summary: Add a note to the Work Order
      description: >
        MUTATION — records a NOTE_ADDED execution action (requires `notes`).
        Requires `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/WorkOrderActionRequest" }
      responses:
        "201":
          description: The recorded action.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderAction" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/cancel:
    post:
      operationId: cancelWorkOrder
      summary: Cancel the Work Order
      description: >
        MUTATION — records a CANCELLED execution action. Requires
        `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/WorkOrderActionRequest" }
      responses:
        "201":
          description: The recorded action.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderAction" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/complete:
    post:
      operationId: completeWorkOrder
      summary: Complete the Work Order
      description: >
        MUTATION — completes the Work Order (BE-08H), with optional
        `completionSummary` / `completionNotes`. Requires `work_order.manage`
        and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CompleteWorkOrderRequest" }
      responses:
        "200":
          description: The completed Work Order.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrder" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/evidence-requirements:
    get:
      operationId: listWorkOrderEvidenceRequirements
      summary: List evidence requirements for a Work Order
      description: >
        The BE-07 evidence requirements bound to a Work Order (required/type/
        min/max). Requires `work_order.read` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      responses:
        "200":
          description: Work Order evidence requirements.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/WorkOrderEvidenceRequirement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/evidence:
    get:
      operationId: listWorkOrderEvidence
      summary: List evidence for a Work Order
      description: >
        The BE-07 evidence submissions bound to a Work Order. Requires
        `work_order.read` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      responses:
        "200":
          description: Work Order evidence submissions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/WorkOrderEvidence" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: submitWorkOrderEvidence
      summary: Submit evidence for a Work Order
      description: >
        MUTATION — creates a BE-07 evidence submission bound to the Work
        Order (execution_type WORK_ORDER). Requires `work_order.manage` and
        Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitWorkOrderEvidenceRequest" }
      responses:
        "201":
          description: The created evidence submission.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderEvidence" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/evidence/{evidenceId}:
    patch:
      operationId: removeWorkOrderEvidence
      summary: Remove Work Order evidence
      description: >
        MUTATION — marks the evidence submission REMOVED. Requires
        `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
        - name: evidenceId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: The removed evidence submission.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderEvidence" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/verification:
    get:
      operationId: getWorkOrderVerification
      summary: Get the Work Order verification state
      description: >
        The Work Order verification state (BE-08I): latest verification +
        all verification records. Requires `work_order.read` and Building
        access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      responses:
        "200":
          description: The verification state.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderVerificationState" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: submitWorkOrderVerification
      summary: Submit a Work Order verification decision
      description: >
        MUTATION — verifies a COMPLETED Work Order with
        APPROVED / REJECTED / REWORK_REQUIRED (BE-08I, reusing the BE-07
        review primitive). Requires `work_order.manage` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitWorkOrderVerificationRequest" }
      responses:
        "201":
          description: The verification result.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderVerificationState" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/history:
    get:
      operationId: getWorkOrderHistory
      summary: Get the Work Order audit/history
      description: >
        The append-only Work Order history (BE-08J, reusing BE-07 operational
        events). Requires `work_order.read` and Building access.
      tags: [Work Orders]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      responses:
        "200":
          description: Work Order history events.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/WorkOrderHistoryEvent" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # Entitlement (BE-02C) — module entitlements are resolved for the
  # authenticated user via /auth/me → entitlements; these are the
  # administration reads.
  /entitlements:
    get:
      operationId: listEntitlements
      summary: List module entitlements
      description: Requires `entitlement.read`.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Entitlement list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Entitlement" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /entitlements/{entitlementId}:
    get:
      operationId: getEntitlement
      summary: Get an entitlement by id
      description: Requires `entitlement.read`.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/EntitlementIdPath"
      responses:
        "200":
          description: Entitlement record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Entitlement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /entitlements/{entitlementId}/status:
    patch:
      operationId: updateEntitlementStatus
      summary: Update an entitlement status
      description: Requires `entitlement.manage`.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/EntitlementIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status: { $ref: "#/components/schemas/EntitlementStatus" }
      responses:
        "200":
          description: The updated entitlement.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Entitlement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /subscriptions/{subscriptionId}/entitlements:
    get:
      operationId: listSubscriptionEntitlements
      summary: List entitlements for a subscription
      description: Requires `entitlement.read`.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/SubscriptionIdPath"
      responses:
        "200":
          description: Subscription entitlements.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Entitlement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: createEntitlement
      summary: Create an entitlement for a subscription
      description: Requires `entitlement.manage`.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/SubscriptionIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [moduleId]
              properties:
                moduleId: { $ref: "#/components/schemas/Uuid" }
                startsAt: { type: string, format: date-time }
                endsAt: { type: string, format: date-time, nullable: true }
                status: { $ref: "#/components/schemas/EntitlementStatus" }
      responses:
        "201":
          description: The created entitlement.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Entitlement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /subscriptions/{subscriptionId}/entitlements/effective:
    get:
      operationId: resolveEffectiveEntitlements
      summary: Resolve effective entitlements for a subscription
      description: >
        The BE-02C effective entitlements (the same resolver /auth/me uses).
        Requires `entitlement.read`.
      tags: [Roles & Permissions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/SubscriptionIdPath"
      responses:
        "200":
          description: Effective entitlements.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Entitlement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # Asset sub-resource reads (BE-05).
  /assets/{assetId}/status:
    get:
      operationId: getAssetStatus
      summary: Get the asset lifecycle status
      description: Requires `asset.read` and Building access.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: The asset status.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicAsset" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /assets/{assetId}/identifiers:
    get:
      operationId: listAssetIdentifiers
      summary: List asset identifiers (QR/tag/barcode)
      description: Requires `asset_identifier.read` and Building access.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: Asset identifier list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/AssetIdentifier" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /assets/{assetId}/history:
    get:
      operationId: getAssetHistory
      summary: Get the asset history
      description: Requires `asset_history.read` and Building access.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: Asset history events.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/AssetHistoryEvent" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /assets/{assetId}/warranties:
    get:
      operationId: listAssetWarranties
      summary: List asset warranties
      description: Requires `asset_warranty.read` and Building access.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: Asset warranty list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/AssetWarranty" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /assets/{assetId}/certifications:
    get:
      operationId: listAssetCertifications
      summary: List asset certifications
      description: Requires `asset_certification.read` and Building access.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: Asset certification list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/AssetCertification" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /operational-events/{eventId}:
    get:
      operationId: getOperationalEvent
      x-required-permission: operational_event.read
      x-building-scoped: true
      summary: Get one operational audit event
      description: >
        Returns one event only when its Client/Building is inside the caller's
        current accessible scope. An inaccessible event is indistinguishable
        from an unknown event at this read boundary. Requires
        `operational_event.read`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - name: eventId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: The scoped operational audit event.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/OperationalEvent" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /checklist-executions:
    get:
      operationId: listChecklistExecutions
      summary: List checklist executions
      description: >
        Checklist executions within the caller's accessible Clients. Pagination
        is opt-in via `page`/`pageSize`. Requires `checklist.read`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/PageSizeParam"
      responses:
        "200":
          description: Scoped execution list (full list or requested page).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/ChecklistExecution"
                      meta:
                        $ref: "#/components/schemas/MobilePaginationMeta"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /checklist-executions/{executionId}:
    get:
      operationId: getChecklistExecution
      summary: Get a checklist execution
      description: Requires `checklist.read` and Client-scope access.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ExecutionIdPathParam"
      responses:
        "200":
          description: The checklist execution.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ChecklistExecution"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /checklist-executions/{executionId}/start:
    post:
      operationId: startChecklistExecution
      summary: Start a draft checklist execution
      description: Requires `checklist.manage`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ExecutionIdPathParam"
      responses:
        "200":
          description: The started execution.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ChecklistExecution"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /checklist-executions/{executionId}/responses:
    get:
      operationId: getChecklistResponses
      summary: Get saved checklist item responses
      description: Requires `checklist.read` and Client-scope access.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ExecutionIdPathParam"
      responses:
        "200":
          description: Saved responses for the execution.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/ChecklistItemResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      operationId: saveChecklistResponses
      summary: Save checklist item responses
      description: >
        MUTATION — saves one or more item responses (single object or array of
        `{ itemId, value?, isNa?, naNotes?, result?, notes? }`). Requires
        `checklist.manage`. Backend-authoritative validation: the execution
        must be in-scope and non-terminal, each item must belong to the
        execution's template, and ordinary (non-N/A) `value` must match the
        item's `itemType` (CHECK/BOOLEAN → boolean, NUMBER → number, TEXT/SELECT
        → string). SELECT values are validated against the exact item's ACTIVE
        option code atomically in the write statement. An entry may set
        `isNa: true` to record an explicit N/A state (item policy must allow
        it; value/result must be null; naNotes may be required per item
        policy). Returns `data: {}` on success.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ExecutionIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ChecklistResponsesRequest" }
      responses:
        "200":
          description: Responses saved (empty payload).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: object
                        additionalProperties: false
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /checklist-executions/{executionId}/complete:
    post:
      operationId: completeChecklistExecution
      summary: Complete an in-progress checklist execution
      description: Requires `checklist.manage`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ExecutionIdPathParam"
      responses:
        "200":
          description: The completed execution.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ChecklistExecution"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /checklist-executions/{executionId}/cancel:
    post:
      operationId: cancelChecklistExecution
      summary: Cancel a non-terminal checklist execution
      description: Requires `checklist.manage`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ExecutionIdPathParam"
      responses:
        "200":
          description: The cancelled execution.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/ChecklistExecution"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # --------------------------------------------------------------------
  # CR-BE-MOB-CONTRACT-01 PART 04 — Checklist definition, UOM,
  # measurement binding, and evidence requirement authorities.
  # --------------------------------------------------------------------
  /checklist-templates/{templateId}/executions:
    post:
      operationId: createChecklistExecution
      summary: Create a checklist execution from a template
      description: >
        MUTATION — creates a DRAFT checklist execution for an ACTIVE template.
        Requires `checklist.manage` and Client-scope access to the template.
        This is the entry point of the mobile checklist chain
        (Task/Work Order → Checklist execution).
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ChecklistTemplateIdPath"
      responses:
        "201":
          description: Checklist execution created (DRAFT).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ChecklistExecution" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /clients/{clientId}/checklist-templates:
    get:
      operationId: listChecklistTemplates
      summary: List checklist templates for a Client
      description: Requires `checklist.read` and accessible Client scope.
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      responses:
        "200":
          description: Checklist template list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/ChecklistTemplate" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createChecklistTemplate
      summary: Create a checklist template
      description: Requires `checklist.manage` and accessible Client scope.
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateChecklistTemplateRequest" }
      responses:
        "201":
          description: Checklist template created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ChecklistTemplate" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /checklist-templates/{checklistTemplateId}:
    get:
      operationId: getChecklistTemplate
      summary: Get a checklist template
      description: Requires `checklist.read` and Client-scope access.
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ChecklistTemplateIdPath"
      responses:
        "200":
          description: Checklist template.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ChecklistTemplate" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateChecklistTemplate
      summary: Update checklist template metadata/status
      description: Requires `checklist.manage` and Client-scope access.
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ChecklistTemplateIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateChecklistTemplateRequest" }
      responses:
        "200":
          description: Checklist template updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ChecklistTemplate" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /checklist-templates/{checklistTemplateId}/items:
    get:
      operationId: listChecklistItems
      summary: List items of a checklist template
      description: >
        The ordered checklist items of a template. Requires `checklist.read`.
        Item `required` and `itemType` are the backend-authoritative response
        and validation rules (mirrored in the mobile checklist execution read
        model).
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ChecklistTemplateIdPath"
      responses:
        "200":
          description: Checklist items (ordered).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/ChecklistItem" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: createChecklistItem
      summary: Add an item to a checklist template
      description: Requires `checklist.manage`.
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ChecklistTemplateIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateChecklistItemRequest" }
      responses:
        "201":
          description: Checklist item created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ChecklistItem" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /checklist-items/{checklistItemId}:
    patch:
      operationId: updateChecklistItem
      summary: Update a checklist item
      description: Requires `checklist.manage`.
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ChecklistItemIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateChecklistItemRequest" }
      responses:
        "200":
          description: Checklist item updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ChecklistItem" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /clients/{clientId}/uoms:
    get:
      operationId: listUoms
      summary: List UOMs for a Client
      description: >
        The Client's units of measure (measurement/UOM authority). Requires
        `uom.read` and accessible Client scope.
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      responses:
        "200":
          description: UOM list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Uom" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createUom
      summary: Create a UOM
      description: Requires `uom.manage` and accessible Client scope.
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateUomRequest" }
      responses:
        "201":
          description: UOM created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Uom" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /uoms/{uomId}:
    get:
      operationId: getUom
      summary: Get a UOM by id
      description: Requires `uom.read` and Client-scope access.
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/UomIdPath"
      responses:
        "200":
          description: UOM record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Uom" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateUom
      summary: Update a UOM
      description: Requires `uom.manage` and Client-scope access.
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/UomIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateUomRequest" }
      responses:
        "200":
          description: UOM updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Uom" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /checklist-items/{checklistItemId}/measurement:
    patch:
      operationId: bindChecklistItemMeasurement
      summary: Bind measurement/UOM to a checklist item
      description: >
        MUTATION — binds a UOM plus min/max/decimal precision to a numeric
        checklist item (measurement authority). Requires `uom.manage`. The
        item must be NUMBER-typed, the UOM ACTIVE and in the item's Client
        scope, and `minimumValue ≤ maximumValue`. Mirrored in the mobile
        checklist execution read model's `measurement` object.
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ChecklistItemIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BindMeasurementRequest" }
      responses:
        "200":
          description: Measurement bound to the checklist item.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/MeasurementBoundItem" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /form-fields/{formFieldId}/measurement:
    patch:
      operationId: bindFormFieldMeasurement
      summary: Bind measurement/UOM to a form field
      description: >
        MUTATION — binds a UOM plus min/max/decimal precision to a numeric
        form field (the form counterpart of the checklist measurement
        authority). Requires `uom.manage`. Same rules as the checklist item
        binding.
      tags: [Checklist & Measurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/FormFieldIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BindMeasurementRequest" }
      responses:
        "200":
          description: Measurement bound to the form field.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/MeasurementBoundItem" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /evidence-requirements:
    get:
      operationId: listEvidenceRequirements
      summary: List evidence requirements
      description: >
        Evidence requirement definitions within the caller's accessible
        Clients. Optional `targetType` / `targetId` / `clientId` filters.
        Requires `evidence.read`. These are the required-evidence rules
        mobile enforces (also embedded in the mobile checklist execution
        read model).
      tags: [Evidence Requirements]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: targetType
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/EvidenceRequirementTargetType"
        - name: targetId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: clientId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Evidence requirement list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/EvidenceRequirement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createEvidenceRequirement
      summary: Create an evidence requirement
      description: >
        Requires `evidence.manage`. Binds a required/optional evidence rule
        (type PHOTO/DOCUMENT/SIGNATURE, min/max count) to a checklist
        template/item or form template/version/section/field.
      tags: [Evidence Requirements]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateEvidenceRequirementRequest" }
      responses:
        "201":
          description: Evidence requirement created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/EvidenceRequirement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /evidence-requirements/{evidenceRequirementId}:
    get:
      operationId: getEvidenceRequirement
      summary: Get an evidence requirement
      description: Requires `evidence.read` and Client-scope access.
      tags: [Evidence Requirements]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/EvidenceRequirementIdPath"
      responses:
        "200":
          description: Evidence requirement record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/EvidenceRequirement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateEvidenceRequirement
      summary: Update an evidence requirement
      description: Requires `evidence.manage` and Client-scope access.
      tags: [Evidence Requirements]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/EvidenceRequirementIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateEvidenceRequirementRequest" }
      responses:
        "200":
          description: Evidence requirement updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/EvidenceRequirement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # CR-BE-EVD-01 PART 05 — Finding/Rework/Verification Evidence
  /findings/{findingId}/evidence:
    parameters:
      - $ref: "#/components/parameters/FindingIdPathParam"
    get:
      operationId: listFindingEvidence
      summary: List Finding Evidence
      description: Requires `evidence.read`; scope is derived from the Finding Building and Client.
      tags: [Findings]
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Finding Evidence list, content: { application/json: { schema: { $ref: "#/components/schemas/EvidenceListResponse" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: createFindingEvidence
      summary: Create Finding Evidence metadata
      description: Creates `FINDING` Evidence with executionId equal to findingId. Requires `evidence.manage`; bytes use the shared file endpoint.
      tags: [Findings]
      security: [{ bearerAuth: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/FindingEvidenceRequest" } } } }
      responses:
        "201": { description: Created, content: { application/json: { schema: { $ref: "#/components/schemas/EvidenceResponse" } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /findings/{findingId}/rework/{reworkId}/evidence:
    parameters:
      - $ref: "#/components/parameters/FindingIdPathParam"
      - name: reworkId
        in: path
        required: true
        schema: { $ref: "#/components/schemas/Uuid" }
    get: { operationId: listFindingReworkEvidence, summary: List Rework Evidence, tags: [Findings], security: [{ bearerAuth: [] }], responses: { "200": { description: Evidence list, content: { application/json: { schema: { $ref: "#/components/schemas/EvidenceListResponse" } } } }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } } }
    post: { operationId: createFindingReworkEvidence, summary: Create Rework Evidence metadata, description: Creates `FINDING_REWORK` Evidence. Requires `evidence.manage`; the cycle must be REQUESTED., tags: [Findings], security: [{ bearerAuth: [] }], requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/FindingEvidenceRequest" } } } }, responses: { "201": { description: Created, content: { application/json: { schema: { $ref: "#/components/schemas/EvidenceResponse" } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } } }
  /findings/{findingId}/verification/{verificationId}/evidence:
    parameters:
      - $ref: "#/components/parameters/FindingIdPathParam"
      - name: verificationId
        in: path
        required: true
        schema: { $ref: "#/components/schemas/Uuid" }
    get: { operationId: listFindingVerificationEvidence, summary: List Verification Evidence, tags: [Findings], security: [{ bearerAuth: [] }], responses: { "200": { description: Evidence list, content: { application/json: { schema: { $ref: "#/components/schemas/EvidenceListResponse" } } } }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } } }
    post: { operationId: createFindingVerificationEvidence, summary: Create Verification Evidence metadata, description: Creates `FINDING_VERIFICATION` Evidence for a PENDING Finding review. Requires `finding.review`., tags: [Findings], security: [{ bearerAuth: [] }], requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/FindingEvidenceRequest" } } } }, responses: { "201": { description: Created, content: { application/json: { schema: { $ref: "#/components/schemas/EvidenceResponse" } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } } }

  /evidence:
    post:
      operationId: submitEvidence
      summary: Submit evidence metadata
      description: >-
        MUTATION — creates an evidence submission metadata record (BE-07).
        Permission is asserted exactly per `executionType` (MOB-C06 PART
        01A): FORM_INSTANCE / CHECKLIST_EXECUTION / FINDING / FINDING_REWORK
        → `evidence.manage`; FINDING_VERIFICATION → `finding.review` (the
        same authority as the dedicated Finding verification evidence route
        and the mobile upload; `finding.review` alone is sufficient).
        `form_instance.execute` does not grant evidence upload.

        MOB-C07 PART 05 — a task-bound FORM_INSTANCE (`generated_task_id`
        NOT NULL) derives Building from the exact generated task (FORM_VERSION
        pin + Client consistency). Unbound generic/admin instances stay
        Client-scoped. Current shift is not required.


        Body carries the evidence type, the owning execution (FORM_INSTANCE /
        CHECKLIST_EXECUTION / FINDING / FINDING_REWORK / FINDING_VERIFICATION)
        and the file metadata (`fileReference` + `originalFileName` /
        `mimeType` / `fileSize`); optional `evidenceRequirementId` is
        validated against the ACTIVE requirement (type + Client match).
        Terminal executions are rejected. The actual bytes are attached
        afterwards via `POST /evidence/:id/file` (or the single-call
        `POST /mobile/evidence`).
      tags: [Evidence]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitEvidenceRequest" }
      responses:
        "201":
          description: Evidence submission metadata created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/EvidenceFileMetadata" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    get:
      operationId: listEvidence
      summary: List evidence submissions
      description: >
        Evidence submissions within the caller's accessible Clients. Optional
        `executionId` / `requirementId` filters. Pagination is opt-in via
        `page`/`pageSize`. Requires `evidence.read`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - name: executionId
          in: query
          description: Filter by owning execution (form instance / checklist execution).
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: requirementId
          in: query
          description: Filter by evidence requirement.
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/PageSizeParam"
      responses:
        "200":
          description: Scoped evidence list (full list or requested page).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/EvidenceFileMetadata"
                      meta:
                        $ref: "#/components/schemas/MobilePaginationMeta"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /evidence/{evidenceId}:
    get:
      operationId: getEvidence
      summary: Get an evidence submission (metadata record)
      description: Requires `evidence.read` and Client-scope access.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/EvidenceIdPathParam"
      responses:
        "200":
          description: The evidence submission metadata.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EvidenceFileMetadata"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: removeEvidence
      summary: Remove an evidence submission
      description: >
        MUTATION — sets the evidence submission `status` to REMOVED (never a
        delete). Requires `evidence.manage` and Client-scope access to the
        submission's Client.
      tags: [Evidence]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/EvidenceIdPathParam"
      responses:
        "200":
          description: Evidence submission removed.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EvidenceFileMetadata"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /reviews:
    get:
      operationId: listReviews
      summary: List reviews
      description: >
        Reviews within the caller's accessible Clients. Optional `targetType` /
        `targetId` filters. Pagination is opt-in via `page`/`pageSize`.
        Requires `review.read`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - name: targetType
          in: query
          description: Filter by review target type.
          required: false
          schema:
            type: string
            enum: [FORM_INSTANCE, CHECKLIST_EXECUTION]
        - name: targetId
          in: query
          description: Filter by review target id.
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/PageSizeParam"
      responses:
        "200":
          description: Scoped review list (full list or requested page).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/Review"
                      meta:
                        $ref: "#/components/schemas/MobilePaginationMeta"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /operational-events:
    get:
      operationId: listOperationalEvents
      x-required-permission: operational_event.read
      x-building-scoped: true
      summary: Search operational audit events
      description: >
        Bounded, newest-first operational audit search over the append-only
        `operational_events` authority. Results are always paginated with a
        default page size of 50 and a maximum of 200. The caller's accessible
        Client/Building scope is applied in SQL; an explicit out-of-scope
        Client or Building filter is denied. Ordinary reads do not create
        audit events. Requires `operational_event.read`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - name: clientId
          in: query
          description: Optional accessible Client filter.
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: buildingId
          in: query
          description: Optional accessible Building filter.
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: actorUserId
          in: query
          description: Filter by the event actor User.
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: eventType
          in: query
          description: Filter by event type.
          required: false
          schema:
            type: string
            maxLength: 128
        - name: entityType
          in: query
          description: Filter by entity type.
          required: false
          schema:
            type: string
            maxLength: 128
        - name: entityId
          in: query
          description: Filter by entity id.
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: requestId
          in: query
          description: Filter by the server request or background-run UUID.
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: source
          in: query
          description: Filter by execution source; null historical sources are not selected by this filter.
          required: false
          schema:
            $ref: "#/components/schemas/OperationalEventSource"
        - name: from
          in: query
          description: Inclusive lower bound for occurredAt (ISO-8601).
          required: false
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          description: Inclusive upper bound for occurredAt (ISO-8601).
          required: false
          schema:
            type: string
            format: date-time
        - name: page
          in: query
          description: 1-based page number; defaults to 1. Audit search is always bounded.
          required: false
          schema:
            type: integer
            minimum: 1
            default: 1
        - name: pageSize
          in: query
          description: Page size; defaults to 50 and is capped at 200.
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
      responses:
        "200":
          description: Scoped operational audit events, newest first.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/OperationalEvent"
                      meta:
                        $ref: "#/components/schemas/MobilePaginationMeta"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /findings/{findingId}/available-actions:
    get:
      operationId: getFindingAvailableActions
      summary: Get backend-authoritative available actions for a finding
      description: >
        Returns the actions the calling user may perform on the finding in its
        current state. Mobile renders only these actions — it never infers
        workflow authority. Requires `finding.read`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: Backend-authoritative available actions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/FindingAvailableActions"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /findings/{findingId}/state:
    get:
      operationId: getFindingState
      summary: Get the current finding workflow state
      description: Requires `finding.read`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: The finding state.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/FindingState"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: transitionFindingState
      summary: Transition the finding state
      description: >
        MUTATION — transitions the finding to a new `state`. The transition is
        validated by the BE-09 transition authority
        (`assertFindingTransitionAllowed`): an invalid/unauthorized transition
        yields 403 `FINDING_ACTION_NOT_ALLOWED` (deterministic). Requires the
        action authority (not a bare `finding.manage`) — the same authority
        `available-actions` reports. No lifecycle states or transitions are
        invented here.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/TransitionFindingStateRequest" }
      responses:
        "200":
          description: The finding after the transition.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Finding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # --------------------------------------------------------------------
  # CR-BE-MOB-CONTRACT-01 PART 05 — Finding, Rework & Verification.
  # Read-only projections + the existing BE-09 mutations; no invented
  # transitions (workflow authority is the action/transition authority).
  # --------------------------------------------------------------------
  /buildings/{buildingId}/findings:
    get:
      operationId: listFindings
      summary: List findings for a Building
      description: >
        Findings within one explicitly accessible Building (BE-02G:
        `requireBuildingAccess`). Requires `finding.read`. Optional `status`
        filter.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/FindingStatus"
      responses:
        "200":
          description: Finding list for the accessible Building.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/Finding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createFinding
      summary: Create a finding
      description: >
        MUTATION — creates a finding in the accessible Building. Requires
        `finding.manage` and an ACTIVE Building assignment
        (`requireBuildingAccess`). `reportedByUserId` is derived from the
        session; status starts OPEN.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateFindingRequest" }
      responses:
        "201":
          description: Finding created (OPEN).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Finding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /findings/{findingId}:
    get:
      operationId: getFinding
      summary: Get a finding by id
      description: >
        The authoritative Finding record. Requires `finding.read` AND an ACTIVE
        Building assignment for the finding's Building (BE-02G). `status` is
        authoritative; clients must not infer transitions — use
        `GET /findings/:id/available-actions`.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: The finding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Finding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateFinding
      summary: Update finding metadata
      description: >
        MUTATION — updates title/description/classification/severity. Requires
        `finding.manage` and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateFindingRequest" }
      responses:
        "200":
          description: The updated finding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Finding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/source:
    get:
      operationId: getFindingSource
      summary: Get the finding source context
      description: >
        The finding's source binding (FORM_INSTANCE / CHECKLIST_EXECUTION /
        WORK_ORDER) and its resolved source context (reference type, id,
        code, title). Requires `finding.read` and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: The finding source context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FindingSourceState" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateFindingSource
      summary: Bind/clear the finding source
      description: >
        MUTATION — binds or clears the finding's source (sourceType/sourceId).
        Requires `finding.manage` and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateFindingSourceRequest" }
      responses:
        "200":
          description: The updated finding source.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FindingSourceState" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/cancel:
    post:
      operationId: cancelFinding
      summary: Cancel a finding
      description: >
        MUTATION — cancels a non-terminal finding. Requires `finding.manage`
        and Building access. Terminal states are rejected.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: The cancelled finding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Finding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/rework:
    get:
      operationId: getFindingRework
      summary: Get the finding rework context
      description: >
        The finding's rework context: current cycle + all rework cycles
        (REQUESTED / RESUBMITTED). Requires `finding.read` and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: The rework context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FindingReworkContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: requestFindingRework
      summary: Request rework on a finding
      description: >
        MUTATION — reviewer requests rework with a required `reason`. Requires
        `finding.review` and Building access (the BE-09 rework authority),
        plus proof the caller is the current reviewer. Executors holding only
        `finding.execute` cannot request rework. Does not invent transitions —
        it delegates to the existing rework workflow.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RequestReworkRequest" }
      responses:
        "201":
          description: Rework requested.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FindingReworkContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateFindingReworkNotes
      summary: Update rework notes
      description: >
        MUTATION — executor updates the current rework cycle's notes. Requires
        `finding.execute` and Building access, plus proof the caller is the
        current ACTIVE finding assignee (the action authority enforces it).
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateReworkNotesRequest" }
      responses:
        "200":
          description: The updated rework context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FindingReworkContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/reject:
    post:
      operationId: rejectFinding
      summary: Reject a finding (supervisor)
      description: >
        MUTATION — rejects a finding under review with a required `reason`.
        Requires `finding.review` and Building access. This is the existing
        BE-09 rejection authority; no transition is invented.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RequestReworkRequest" }
      responses:
        "200":
          description: The finding after rejection.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Finding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/resubmit:
    post:
      operationId: resubmitFinding
      summary: Resubmit a reworked finding
      description: >
        MUTATION — executor resubmits a finding after rework. Requires
        `finding.execute` and Building access, plus proof the caller is the
        current ACTIVE finding assignee (the action authority enforces it).
        It performs the existing RESUBMIT lifecycle transition.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: The resubmitted finding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Finding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/reviews:
    get:
      operationId: listFindingReviews
      summary: List finding reviews
      description: >
        The finding's supervisor reviews. Requires `finding.read` and Building
        access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: Finding reviews.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/FindingReview" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: openFindingReview
      summary: Open a finding review (supervisor)
      description: >
        MUTATION — opens a review on the finding. Requires `finding.review`
        and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/OpenFindingReviewRequest" }
      responses:
        "201":
          description: Review opened.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FindingReview" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/reviews/current:
    get:
      operationId: getCurrentFindingReview
      summary: Get the current finding review
      description: Requires `finding.read` and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: The current review (or null).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        allOf:
                          - $ref: "#/components/schemas/FindingReview"
                        nullable: true
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/verification:
    get:
      operationId: getFindingVerification
      summary: Get the finding verification state
      description: >
        The finding's verification state: current review, latest verification
        decision, and all reviews. Requires `finding.read` and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: The verification state.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FindingVerificationState" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: submitFindingVerification
      summary: Submit a finding verification decision (supervisor)
      description: >
        MUTATION — submits a supervisor verification decision
        (APPROVED / REJECTED / REWORK_REQUIRED) via the BE-09 verification
        workflow. Requires `finding.review` AND the BE-09 action authority
        (the same authority `available-actions` reports). A completed
        verification is immutable.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitFindingVerificationRequest" }
      responses:
        "200":
          description: The verification state after the decision.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FindingVerificationState" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/assignments:
    get:
      operationId: listFindingAssignments
      summary: List finding assignments
      description: >
        The finding's assignee records (WORKFORCE / TEAM / VENDOR /
        VENDOR_WORKFORCE). Requires `finding.read` and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: Finding assignments.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/FindingAssignment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: assignFinding
      summary: Assign a finding
      description: >
        MUTATION — assigns the finding to a workforce/team/vendor assignee.
        Requires `finding.assign` and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AssignFindingRequest" }
      responses:
        "201":
          description: Assignment created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FindingAssignment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/assignments/current:
    get:
      operationId: getCurrentFindingAssignment
      summary: Get the current finding assignment
      description: Requires `finding.read` and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: The current assignment (or null).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        allOf:
                          - $ref: "#/components/schemas/FindingAssignment"
                        nullable: true
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/assignments/{assignmentId}:
    patch:
      operationId: updateFindingAssignment
      summary: Update a finding assignment
      description: >
        MUTATION — deactivates or reassigns a finding assignment. Requires
        `finding.assign` and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
        - name: assignmentId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - { $ref: "#/components/schemas/AssignFindingRequest" }
                - $ref: "#/components/schemas/DeactivateFindingAssignmentRequest"
      responses:
        "200":
          description: The updated assignment.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FindingAssignment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/history:
    get:
      operationId: getFindingHistory
      summary: Get the finding audit/history
      description: >
        The append-only finding history (audit events). Requires `finding.read`
        and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: Finding history events.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/FindingHistoryEvent" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/closure:
    get:
      operationId: getFindingClosure
      summary: Get the finding closure readiness
      description: >
        The finding's closure info: state, readiness, latest verification
        decision, pending rework, and closure provenance. Requires
        `finding.read` and Building access.
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      responses:
        "200":
          description: Finding closure info.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FindingClosureInfo" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /findings/{findingId}/close:
    post:
      operationId: closeFinding
      summary: Close a verified finding
      description: >
        MUTATION — closes a VERIFIED finding (optional `closureNotes`).
        Requires `finding.close` and Building access (BE-09 closure authority).
      tags: [Findings]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FindingIdPathParam"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CloseFindingRequest" }
      responses:
        "200":
          description: The closed finding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Finding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /assets/resolve/{identifier}:
    get:
      operationId: resolveAssetByIdentifier
      summary: Resolve an asset identifier (QR-ready)
      description: >
        Resolves a printed/label identifier (QR) to its asset. The asset's
        Building must be within the caller's accessible scope.
        Requires `asset_identifier.read`.

        CR-BE-MOB-01 PART 05 — this is the single identifier authority behind
        `resolveMobileQr`; the payload is the same opaque BE-05H
        `identifier_value`. The registry is asset-scoped, so no other
        operational context (location, functional location, patrol checkpoint,
        Work Order) can be resolved here. Unchanged by PART 05.
      x-required-permission: asset_identifier.read
      x-building-scoped: true
      x-qr-supported-target-types: [ASSET]
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/IdentifierPathParam"
      responses:
        "200":
          description: The resolved identifier and asset.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/AssetResolution"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /mobile/app-version/{platform}:
    get:
      operationId: getAppVersionMetadata
      summary: Get mobile app version metadata
      description: >
        BE-25M app version metadata contract (public, no authentication — the
        app can check for a required update before login). Returns the
        platform's current supported version, minimum supported version,
        update-required / update-available flags, and release metadata where
        applicable. The optional `appVersion` query param (the caller's
        running version) is used to compute the flags; a client that omits it
        receives the metadata with both flags false. Read-only — no app
        distribution/update delivery and no Flutter UI behavior.
      tags: [Mobile Execution]
      parameters:
        - name: platform
          in: path
          description: Target platform.
          required: true
          schema:
            type: string
            enum: [ANDROID, IOS]
        - name: appVersion
          in: query
          description: The caller's running app version (e.g. 1.2.3). Used to compute the update flags.
          required: false
          schema:
            type: string
      responses:
        "200":
          description: The app version metadata contract.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileAppVersionMetadata"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
  /mobile/diagnostics:
    get:
      operationId: getMobileDiagnostics
      summary: Get mobile API diagnostics metadata
      description: >
        BE-25N basic mobile API health/diagnostic metadata (authenticated):
        correlation id (requestId — the same value as the X-Request-ID
        response header), server time, process uptime, and the sanitized
        mobile device/app context echoed from the request headers
        (X-Device-Id / X-Platform / X-App-Version) when present. Lightweight
        by design — no monitoring platform, no sensitive data.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Mobile API diagnostics metadata.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileDiagnostics"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /mobile/push-tokens:
    post:
      operationId: registerPushToken
      summary: Register (or rotate) a mobile push token
      description: >
        BE-25L push token registration. Registers the authenticated user's
        device push token. Re-registering the same `deviceId` ROTATES the
        token on the existing ACTIVE registration; a token re-registered
        from another device of the same user replaces the old device's
        registration. Returns the ACTIVE registration with registered_at /
        last_seen_at / status.

        CR-BE-MOB-01 PART 07 / CR-BE-PUSH-01 PART 05 — registration is NOT
        delivery, and provider acceptance is NOT delivery. Storing a token
        creates no notification and sends nothing: a successful registration
        must never be presented to the user as "push enabled". A push is only
        ever produced by the shared outbound delivery engine from an existing
        notification, never by this endpoint. Even when a push is sent, an
        accepted attempt means the provider QUEUED it — the backend cannot
        observe whether the device received, displayed or read it, so no
        acceptance may be reported as "delivered". BE-26 delivers IN_APP (the
        authoritative mobile path, `listNotifications` /
        `markNotificationRead`), EMAIL and WHATSAPP; PUSH delivery now exists
        server-side but is deliberately absent from the history read model —
        see `x-push-delivery-lifecycle`. The registration is self-scoped: it
        is bound to the authenticated `userId` and requires no permission
        code, and a user can only ever see or deactivate their own device
        registrations. Registrations the provider rejected are returned with
        status INVALID and should be re-registered.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      x-recipient-scoped: true
      x-push-delivery-lifecycle:
        - stage: 1
          name: TOKEN_REGISTRATION
          status: EXISTING
          authority: BE-25L mobile_push_tokens
          operationIds: [registerPushToken, listPushTokens, deactivatePushToken]
          notes: >-
            Authoritative per user + device, with rotation and deactivation.
            Tokens are stored only; nothing reads them to send anything.
        - stage: 2
          name: NOTIFICATION_CREATION
          status: EXISTING
          authority: BE-26A record + BE-26D subscription + BE-26B template + BE-26C recipient resolution
          operationIds: [listNotifications, getNotification, markNotificationRead]
          notes: >-
            A domain event resolves matching subscriptions, renders the
            template and creates notification records for authoritative
            recipient user ids. The notification record channel enum is
            IN_APP only — creating a notification is not a push.
        - stage: 3
          name: DELIVERY_ATTEMPT
          status: EXISTING
          authority: BE-26E in-app, BE-26F email, BE-26G WhatsApp, BE-26K history, CR-BE-PUSH-01 push attempt evidence
          operationIds: [listNotificationHistory, getNotificationHistoryItem]
          implementedChannels: [IN_APP, EMAIL, WHATSAPP, PUSH]
          missingChannels: []
          notes: >-
            Every implemented channel persists an attempt row with status,
            sent/failed timestamps, provider and failure reason. IN_APP, EMAIL
            and WHATSAPP are surfaced by the BE-26K history read model. PUSH
            attempts are recorded per device as internal, append-only evidence
            (CR-BE-PUSH-01) and are deliberately NOT exposed through the
            history read model or any other endpoint — recorded and auditable
            server-side, but not client-readable.
        - stage: 4
          name: PROVIDER_DELIVERY
          status: EXISTING
          authority: CR-BE-PUSH-01 governed provider adapter (email and WhatsApp have their own adapters)
          operationIds: []
          notes: >-
            A governed, provider-neutral push adapter now exists behind a
            single internal port, invoked only by the shared outbound delivery
            engine over the user's ACTIVE registrations. It is not reachable
            from any endpoint: `operationIds` is empty because no route sends,
            tests, resends or reports a push. Provider acceptance means the
            provider QUEUED the message — it is NOT proof the device received,
            displayed or read it, and neither the backend nor a client may
            present it as such. Mobile continues to rely on the IN_APP inbox
            as the authoritative record, and must never simulate a push
            locally and report it as delivered.
      x-push-delivery-failure-behavior: >-
        For the history-backed channels a failed attempt is persisted as a
        FAILED row carrying `failedAt`, `provider` and `failureReason`, and is
        readable through the history model — failures are recorded, never
        silently dropped, and never retried into a fake success. A failed PUSH
        attempt is likewise recorded per device as internal append-only
        evidence with a sanitized error, and is retried by the SHARED outbound
        retry engine under the same budget as the other channels; it is not
        client-readable. When the provider reports that a registration is no
        longer valid, that device's registration is retired to INVALID and is
        visible as such through `listPushTokens`. A push that the provider
        accepted but the device never displayed remains unobservable to the
        backend: acceptance is not delivery confirmation.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [deviceId, pushToken, platform]
              properties:
                deviceId:
                  type: string
                  maxLength: 128
                  description: Stable client-generated device identifier.
                pushToken:
                  type: string
                  maxLength: 512
                  description: Provider push token (FCM/APNs style opaque token).
                platform:
                  type: string
                  enum: [ANDROID, IOS]
                appVersion:
                  type: string
                  maxLength: 64
                  nullable: true
                deviceModel:
                  type: string
                  maxLength: 128
                  nullable: true
                deviceOsVersion:
                  type: string
                  maxLength: 64
                  nullable: true
      responses:
        "201":
          description: The ACTIVE push token registration.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PushTokenRegistration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    get:
      operationId: listPushTokens
      summary: List the authenticated user's push token registrations
      description: Returns the user's own registrations (newest first). Scoped to the authenticated user only.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: The user's push token registrations.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/PushTokenRegistration"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /mobile/push-tokens/{tokenId}:
    delete:
      operationId: deactivatePushToken
      summary: Deactivate / unregister a push token
      description: >
        Deactivates (unregisters) the authenticated user's push token
        registration. Only the token's owner can deactivate it. The row is
        kept as INACTIVE history.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - name: tokenId
          in: path
          description: Push token registration id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The deactivated registration.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PushTokenRegistration"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /notification-templates:
    post:
      operationId: createNotificationTemplate
      summary: Create a notification template
      description: >
        BE-26B notification template foundation. Creates a reusable rendering
        recipe (platform configuration). Requires `notification_template.manage`.
        Every `{{variable}}` used in subject/body must be declared in
        `variables`. The template key is unique and immutable after creation.
      tags: [Notification Templates]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [key, type, channel, subject]
              properties:
                key:
                  type: string
                  maxLength: 64
                  description: Stable, unique template key (uppercase code).
                type:
                  type: string
                  maxLength: 64
                  description: Notification type stamped on rendered notifications.
                channel:
                  type: string
                  enum: [IN_APP]
                subject:
                  type: string
                  maxLength: 500
                  description: Subject/title template with {{variable}} placeholders.
                body:
                  type: string
                  maxLength: 4000
                  nullable: true
                  description: Message/body template with {{variable}} placeholders.
                variables:
                  type: array
                  maxItems: 50
                  description: Declared template variable names.
                  items:
                    type: string
                    pattern: "^[A-Za-z][A-Za-z0-9_]*$"
      responses:
        "201":
          description: The created notification template.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationTemplate"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
    get:
      operationId: listNotificationTemplates
      summary: List notification templates
      description: >
        Lists notification templates (platform configuration), ordered by key.
        Optional `status` filter (ACTIVE / INACTIVE). Requires
        `notification_template.read`.
      tags: [Notification Templates]
      security:
        - bearerAuth: []
      parameters:
        - name: status
          in: query
          description: Optional status filter.
          required: false
          schema:
            type: string
            enum: [ACTIVE, INACTIVE]
      responses:
        "200":
          description: The notification templates.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/NotificationTemplate"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /notification-templates/{notificationTemplateId}:
    get:
      operationId: getNotificationTemplate
      summary: Get a notification template
      description: >
        Returns a single notification template by id. Requires
        `notification_template.read`.
      tags: [Notification Templates]
      security:
        - bearerAuth: []
      parameters:
        - name: notificationTemplateId
          in: path
          description: Notification template id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The notification template.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationTemplate"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateNotificationTemplate
      summary: Update or deactivate a notification template
      description: >
        Updates a notification template's type/channel/subject/body/variables
        or sets `status` (ACTIVE / INACTIVE). The template key is immutable.
        Requires `notification_template.manage`.
      tags: [Notification Templates]
      security:
        - bearerAuth: []
      parameters:
        - name: notificationTemplateId
          in: path
          description: Notification template id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                type:
                  type: string
                  maxLength: 64
                channel:
                  type: string
                  enum: [IN_APP]
                subject:
                  type: string
                  maxLength: 500
                body:
                  type: string
                  maxLength: 4000
                  nullable: true
                variables:
                  type: array
                  maxItems: 50
                  items:
                    type: string
                    pattern: "^[A-Za-z][A-Za-z0-9_]*$"
                status:
                  type: string
                  enum: [ACTIVE, INACTIVE]
      responses:
        "200":
          description: The updated notification template.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationTemplate"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /notification-subscriptions:
    post:
      operationId: createNotificationSubscription
      summary: Create a notification event subscription
      description: >
        BE-26D notification event subscription. Maps an existing backend
        domain event type to a notification trigger: a BE-26B template plus a
        BE-26C recipient rule, with optional Client/Building context. The
        referenced template must exist; the subscription key is unique and
        immutable after creation. Requires `notification_subscription.manage`.
      tags: [Notification Subscriptions]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [key, eventType, templateKey, recipientRule]
              properties:
                key:
                  type: string
                  maxLength: 128
                  description: Stable, unique subscription key (uppercase code).
                eventType:
                  type: string
                  maxLength: 128
                  description: Existing domain event type (e.g. WORK_ORDER_ASSIGNED).
                templateKey:
                  type: string
                  maxLength: 64
                  description: BE-26B notification template key.
                recipientRule:
                  $ref: "#/components/schemas/RecipientRule"
                clientId:
                  allOf:
                    - $ref: "#/components/schemas/Uuid"
                  nullable: true
                  description: Optional Client context (null = any Client).
                buildingId:
                  allOf:
                    - $ref: "#/components/schemas/Uuid"
                  nullable: true
                  description: Optional Building context (null = any Building).
                status:
                  type: string
                  enum: [ACTIVE, INACTIVE]
      responses:
        "201":
          description: The created notification event subscription.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationEventSubscription"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
    get:
      operationId: listNotificationSubscriptions
      summary: List notification event subscriptions
      description: >
        Lists notification event subscriptions (platform configuration),
        ordered by key. Optional `status` filter (ACTIVE / INACTIVE).
        Requires `notification_subscription.read`.
      tags: [Notification Subscriptions]
      security:
        - bearerAuth: []
      parameters:
        - name: status
          in: query
          description: Optional status filter.
          required: false
          schema:
            type: string
            enum: [ACTIVE, INACTIVE]
      responses:
        "200":
          description: The notification event subscriptions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/NotificationEventSubscription"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /notification-subscriptions/{notificationSubscriptionId}:
    get:
      operationId: getNotificationSubscription
      summary: Get a notification event subscription
      description: >
        Returns a single notification event subscription by id. Requires
        `notification_subscription.read`.
      tags: [Notification Subscriptions]
      security:
        - bearerAuth: []
      parameters:
        - name: notificationSubscriptionId
          in: path
          description: Notification event subscription id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The notification event subscription.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationEventSubscription"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateNotificationSubscription
      summary: Update or deactivate a notification event subscription
      description: >
        Updates a notification event subscription's event type, template,
        recipient rule, Client/Building context, or `status` (ACTIVE /
        INACTIVE). The subscription key is immutable. Requires
        `notification_subscription.manage`.
      tags: [Notification Subscriptions]
      security:
        - bearerAuth: []
      parameters:
        - name: notificationSubscriptionId
          in: path
          description: Notification event subscription id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                eventType:
                  type: string
                  maxLength: 128
                templateKey:
                  type: string
                  maxLength: 64
                recipientRule:
                  $ref: "#/components/schemas/RecipientRule"
                clientId:
                  allOf:
                    - $ref: "#/components/schemas/Uuid"
                  nullable: true
                buildingId:
                  allOf:
                    - $ref: "#/components/schemas/Uuid"
                  nullable: true
                status:
                  type: string
                  enum: [ACTIVE, INACTIVE]
      responses:
        "200":
          description: The updated notification event subscription.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationEventSubscription"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /notification-reminders:
    post:
      operationId: createNotificationReminder
      summary: Create a notification reminder
      description: >
        BE-26H notification reminder. A time-deferred notification trigger:
        at `reminderAt` the reminder renders its template, resolves its
        recipients, and triggers in-app notification delivery. The referenced
        template must exist; the reminder key is unique and immutable after
        creation. Requires `notification_reminder.manage`.
      tags: [Notification Reminders]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                [key, clientId, sourceEntityType, sourceEntityId,
                 recipientRule, templateKey, reminderAt]
              properties:
                key:
                  type: string
                  maxLength: 128
                  description: Stable, unique reminder key (uppercase code).
                clientId:
                  $ref: "#/components/schemas/Uuid"
                  description: Client context (Client isolation).
                sourceEntityType:
                  type: string
                  maxLength: 128
                  description: Source/resource domain type being reminded about.
                sourceEntityId:
                  $ref: "#/components/schemas/Uuid"
                  description: Source/resource id being reminded about.
                recipientRule:
                  $ref: "#/components/schemas/RecipientRule"
                templateKey:
                  type: string
                  maxLength: 64
                  description: BE-26B notification template key.
                reminderAt:
                  type: string
                  format: date-time
                  description: When the reminder becomes due.
                variables:
                  type: object
                  additionalProperties: true
                  description: Template variable values snapshot.
      responses:
        "201":
          description: The created notification reminder.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationReminder"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
    get:
      operationId: listNotificationReminders
      summary: List notification reminders
      description: >
        Lists notification reminders (platform configuration), ordered by
        `reminderAt`. Optional `status` filter (PENDING / SENT / CANCELLED).
        Requires `notification_reminder.read`.
      tags: [Notification Reminders]
      security:
        - bearerAuth: []
      parameters:
        - name: status
          in: query
          description: Optional status filter.
          required: false
          schema:
            type: string
            enum: [PENDING, SENT, CANCELLED]
      responses:
        "200":
          description: The notification reminders.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/NotificationReminder"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /notification-reminders/{notificationReminderId}:
    get:
      operationId: getNotificationReminder
      summary: Get a notification reminder
      description: Requires `notification_reminder.read`.
      tags: [Notification Reminders]
      security:
        - bearerAuth: []
      parameters:
        - name: notificationReminderId
          in: path
          description: Notification reminder id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The notification reminder.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationReminder"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateNotificationReminder
      summary: Update a notification reminder
      description: >
        Reschedules/updates a PENDING reminder's `reminderAt`, template,
        recipient rule, or variables. The reminder key is immutable. Requires
        `notification_reminder.manage`.
      tags: [Notification Reminders]
      security:
        - bearerAuth: []
      parameters:
        - name: notificationReminderId
          in: path
          description: Notification reminder id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                reminderAt:
                  type: string
                  format: date-time
                templateKey:
                  type: string
                  maxLength: 64
                recipientRule:
                  $ref: "#/components/schemas/RecipientRule"
                variables:
                  type: object
                  additionalProperties: true
      responses:
        "200":
          description: The updated notification reminder.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationReminder"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /notification-reminders/{notificationReminderId}/cancel:
    post:
      operationId: cancelNotificationReminder
      summary: Cancel a notification reminder
      description: >
        Cancels a PENDING reminder (terminal: PENDING → CANCELLED). Requires
        `notification_reminder.manage`.
      tags: [Notification Reminders]
      security:
        - bearerAuth: []
      parameters:
        - name: notificationReminderId
          in: path
          description: Notification reminder id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The cancelled notification reminder.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationReminder"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /notification-escalations:
    post:
      operationId: createNotificationEscalation
      summary: Create a notification escalation
      description: >
        BE-26I notification escalation. A time-deferred escalation trigger:
        when the current recipient has not resolved the source resource by
        `escalationAt`, the escalation renders its template and triggers
        notification delivery to the escalation recipients. The referenced
        template must exist; the escalation key is unique and immutable after
        creation. Requires `notification_escalation.manage`.
      tags: [Notification Escalations]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                [key, clientId, sourceEntityType, sourceEntityId,
                 escalationRule, templateKey, escalationAt]
              properties:
                key:
                  type: string
                  maxLength: 128
                  description: Stable, unique escalation key (uppercase code).
                clientId:
                  $ref: "#/components/schemas/Uuid"
                  description: Client context (Client isolation).
                sourceEntityType:
                  type: string
                  maxLength: 128
                  description: Source/resource domain type being escalated.
                sourceEntityId:
                  $ref: "#/components/schemas/Uuid"
                  description: Source/resource id being escalated.
                currentRecipientUserId:
                  allOf:
                    - $ref: "#/components/schemas/Uuid"
                  nullable: true
                  description: The current (pre-escalation) recipient.
                escalationRule:
                  $ref: "#/components/schemas/RecipientRule"
                  description: The escalation recipient resolution rule.
                templateKey:
                  type: string
                  maxLength: 64
                  description: BE-26B notification template key.
                escalationAt:
                  type: string
                  format: date-time
                  description: When the escalation becomes due.
                reason:
                  type: string
                  maxLength: 2000
                  nullable: true
                  description: Escalation reason/notes.
      responses:
        "201":
          description: The created notification escalation.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationEscalation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
    get:
      operationId: listNotificationEscalations
      summary: List notification escalations
      description: >
        Lists notification escalations (platform configuration), ordered by
        `escalationAt`. Optional `status` filter (PENDING / TRIGGERED /
        CANCELLED). Requires `notification_escalation.read`.
      tags: [Notification Escalations]
      security:
        - bearerAuth: []
      parameters:
        - name: status
          in: query
          description: Optional status filter.
          required: false
          schema:
            type: string
            enum: [PENDING, TRIGGERED, CANCELLED]
      responses:
        "200":
          description: The notification escalations.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/NotificationEscalation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /notification-escalations/{notificationEscalationId}:
    get:
      operationId: getNotificationEscalation
      summary: Get a notification escalation
      description: Requires `notification_escalation.read`.
      tags: [Notification Escalations]
      security:
        - bearerAuth: []
      parameters:
        - name: notificationEscalationId
          in: path
          description: Notification escalation id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The notification escalation.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationEscalation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateNotificationEscalation
      summary: Update a notification escalation
      description: >
        Reschedules/updates a PENDING escalation's `escalationAt`,
        currentRecipientUserId, escalation rule, template, or reason. The
        escalation key is immutable. Requires `notification_escalation.manage`.
      tags: [Notification Escalations]
      security:
        - bearerAuth: []
      parameters:
        - name: notificationEscalationId
          in: path
          description: Notification escalation id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                escalationAt:
                  type: string
                  format: date-time
                currentRecipientUserId:
                  allOf:
                    - $ref: "#/components/schemas/Uuid"
                  nullable: true
                escalationRule:
                  $ref: "#/components/schemas/RecipientRule"
                templateKey:
                  type: string
                  maxLength: 64
                reason:
                  type: string
                  maxLength: 2000
                  nullable: true
      responses:
        "200":
          description: The updated notification escalation.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationEscalation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /notification-escalations/{notificationEscalationId}/cancel:
    post:
      operationId: cancelNotificationEscalation
      summary: Cancel a notification escalation
      description: >
        Cancels a PENDING escalation (terminal: PENDING → CANCELLED). Requires
        `notification_escalation.manage`.
      tags: [Notification Escalations]
      security:
        - bearerAuth: []
      parameters:
        - name: notificationEscalationId
          in: path
          description: Notification escalation id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The cancelled notification escalation.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationEscalation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /notification-links:
    post:
      operationId: createSecureLink
      summary: Create a secure notification link
      description: >
        BE-26J secure link creation. Generates an opaque, recipient-bound,
        single-purpose token for a target resource + action. The raw token is
        returned exactly once (in `data.token`) and never stored — only its
        SHA-256 hash is persisted. Requires `notification_secure_link.manage`.
      tags: [Notification Secure Links]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                [clientId, recipientUserId, targetEntityType, targetEntityId,
                 action, expiresAt]
              properties:
                clientId:
                  $ref: "#/components/schemas/Uuid"
                  description: Client context (Client isolation).
                recipientUserId:
                  $ref: "#/components/schemas/Uuid"
                  description: The bound recipient (must be an active user).
                targetEntityType:
                  type: string
                  maxLength: 64
                  description: Target resource type (uppercase code).
                targetEntityId:
                  $ref: "#/components/schemas/Uuid"
                  description: Target resource id.
                action:
                  type: string
                  maxLength: 64
                  description: Target action (uppercase code).
                expiresAt:
                  type: string
                  format: date-time
                  description: Link expiry (future timestamp).
                maxUses:
                  type: integer
                  minimum: 1
                  default: 1
                  description: One-time (1) or limited-use (N) behavior.
      responses:
        "201":
          description: The created secure link (raw token in `data.token`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        allOf:
                          - $ref: "#/components/schemas/NotificationSecureLink"
                          - type: object
                            required: [token]
                            properties:
                              token:
                                type: string
                                description: The raw token (returned exactly once).
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /notification-links/resolve:
    post:
      operationId: resolveSecureLink
      summary: Resolve and consume a secure notification link
      description: >
        Validates the token against the authenticated user (recipient binding),
        expiry, revocation, and usage, then consumes a use and returns the
        target resource + action. Expired / revoked / exhausted links are
        rejected. Resolution does NOT perform the action — the client uses the
        normal authorized endpoints afterwards.
      tags: [Notification Secure Links]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [token]
              properties:
                token:
                  type: string
                  description: The opaque secure link token.
      responses:
        "200":
          description: The validated target resource + action.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationSecureLink"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /notification-links/{secureLinkId}:
    get:
      operationId: getSecureLink
      summary: Get a secure link status
      description: >
        Returns a secure link's validation/revocation status (non-consuming).
        Requires `notification_secure_link.read`.
      tags: [Notification Secure Links]
      security:
        - bearerAuth: []
      parameters:
        - name: secureLinkId
          in: path
          description: Secure link record id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The secure link status.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationSecureLink"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /notification-links/{secureLinkId}/revoke:
    post:
      operationId: revokeSecureLink
      summary: Revoke a secure link
      description: >
        Revokes an ACTIVE secure link (ACTIVE → REVOKED). Revoked links are
        rejected at resolution. Requires `notification_secure_link.manage`.
      tags: [Notification Secure Links]
      security:
        - bearerAuth: []
      parameters:
        - name: secureLinkId
          in: path
          description: Secure link record id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The revoked secure link.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationSecureLink"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /notification-history:
    get:
      operationId: listNotificationHistory
      summary: List the authenticated user's notification history
      description: >
        BE-26K notification history read model: a unified, chronological view
        of the recipient's own delivery records across IN_APP / EMAIL /
        WHATSAPP channels, newest first. Optional `channel` and `status`
        filters; pagination is opt-in via `page`/`pageSize`. Read-only —
        no second audit engine, no credential/token exposure.

        CR-BE-MOB-01 PART 07 — the channel enum is exhaustive: it lists every
        channel that can produce a delivery attempt record. PUSH is absent
        because no push delivery attempt exists in the backend, so the absence
        of a PUSH row is not a delivery failure — it means the capability is
        not implemented. Rows are recipient-scoped to the authenticated user.
      tags: [Notification History]
      security:
        - bearerAuth: []
      x-recipient-scoped: true
      x-delivery-channels-implemented: [IN_APP, EMAIL, WHATSAPP]
      x-delivery-channels-missing: [PUSH]
      parameters:
        - name: channel
          in: query
          description: Optional channel filter.
          required: false
          schema:
            type: string
            enum: [IN_APP, EMAIL, WHATSAPP]
        - name: status
          in: query
          description: Optional status filter (channel-specific values).
          required: false
          schema:
            type: string
            enum: [UNREAD, READ, SENT, FAILED]
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/PageSizeParam"
      responses:
        "200":
          description: The recipient's notification history (full list or page).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/NotificationHistoryItem"
                      meta:
                        $ref: "#/components/schemas/MobilePaginationMeta"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /notification-history/{channel}/{historyId}:
    get:
      operationId: getNotificationHistoryItem
      summary: Get one notification history item
      description: >
        Returns a single history item owned by the authenticated user, scoped
        by channel. Missing items (or items belonging to another user) return
        404 NOT_FOUND — no cross-user data is reachable.
      tags: [Notification History]
      security:
        - bearerAuth: []
      parameters:
        - name: channel
          in: path
          description: Delivery channel (IN_APP / EMAIL / WHATSAPP).
          required: true
          schema:
            type: string
            enum: [IN_APP, EMAIL, WHATSAPP]
        - name: historyId
          in: path
          description: The underlying delivery record id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The notification history item.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/NotificationHistoryItem"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /notifications:
    get:
      operationId: listNotifications
      summary: List the authenticated user's notifications
      description: >
        BE-26A notification foundation: the recipient's own in-app inbox,
        newest first. Self-scoped to the authenticated user only. Optional
        `status` filter (UNREAD / READ). Pagination is opt-in via
        `page`/`pageSize`; without them the full inbox is returned.
      tags: [Notifications]
      security:
        - bearerAuth: []
      parameters:
        - name: status
          in: query
          description: Optional read-state filter.
          required: false
          schema:
            type: string
            enum: [UNREAD, READ]
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/PageSizeParam"
      responses:
        "200":
          description: The recipient's notifications (full inbox or requested page).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/Notification"
                      meta:
                        $ref: "#/components/schemas/MobilePaginationMeta"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /notifications/{notificationId}:
    get:
      operationId: getNotification
      summary: Get the authenticated user's notification
      description: >
        Returns a single notification owned by the authenticated user.
        Missing notifications (or notifications belonging to another user)
        return 404 NOT_FOUND — no cross-user data is reachable.
      tags: [Notifications]
      security:
        - bearerAuth: []
      parameters:
        - name: notificationId
          in: path
          description: Notification id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The notification record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/Notification"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /notifications/{notificationId}/read:
    patch:
      operationId: markNotificationRead
      summary: Mark the authenticated user's notification as read
      description: >
        Sets the notification to READ and stamps `readAt`. Idempotent — the
        original `readAt` is preserved on repeat calls. Only the recipient
        can mark their own notification read.
      tags: [Notifications]
      security:
        - bearerAuth: []
      parameters:
        - name: notificationId
          in: path
          description: Notification id.
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The updated (READ) notification record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/Notification"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /mobile/verification/{targetType}/{targetId}:
    get:
      operationId: getMobileVerification
      summary: Get the mobile supervisor verification contract
      description: >
        BE-25J mobile supervisor verification read model: reviewable resource
        reference, supervisor/reviewer context, current verification state,
        verification decision, notes, verified_at, and backend-authoritative
        available actions. Target types: CHECKLIST_EXECUTION / FORM_INSTANCE
        (shared BE-07 review authority), FINDING (BE-09 finding
        verification), and WORK_ORDER (BE-08I verification lifecycle —
        COMPLETED-only, already-APPROVED immutable, REWORK_REQUIRED moves the
        Work Order back to IN_PROGRESS; CR-BE-MOB-03 PART 05). Requires the
        target-type read permission (review.read / finding.read /
        work_order.read) and the BE-02G scope of the target.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/VerificationTargetTypeParam"
        - $ref: "#/components/parameters/VerificationTargetIdParam"
      responses:
        "200":
          description: The mobile supervisor verification contract.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileVerification"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      operationId: submitMobileVerification
      summary: Submit a supervisor verification decision from mobile
      description: >
        BE-25J mobile supervisor verification submission. Accepts
        `{ decision: APPROVED | REJECTED | REWORK_REQUIRED, notes? }` and
        executes through the SAME services as the Web endpoints
        (review.manage for CHECKLIST_EXECUTION / FORM_INSTANCE, finding.review
        + the BE-09 action authority for FINDING, work_order.manage for
        WORK_ORDER — delegating to the BE-08I verification service so the
        mobile and Web surfaces share one lifecycle). A completed
        verification is immutable and is never silently overwritten. Returns
        the refreshed verification contract.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/VerificationTargetTypeParam"
        - $ref: "#/components/parameters/VerificationTargetIdParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [decision]
              properties:
                decision:
                  type: string
                  enum: [APPROVED, REJECTED, REWORK_REQUIRED]
                notes:
                  type: string
                  maxLength: 1000
      responses:
        "201":
          description: The refreshed verification contract after the decision.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileVerification"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /mobile/sync:
    post:
      operationId: processSyncBatch
      summary: Process a mobile offline sync batch
      description: >
        BE-25G offline sync contract. A lightweight batch of
        client-generated operations; every operation is executed through the
        SAME shared services as the REST endpoints — validation, per-item
        RBAC, data scope, and workflow rules are never bypassed and no
        separate mobile business engine exists. Each operation returns a
        per-item result with server status, server timestamp, and the written
        result or a failure code.

        Supported resources (already-implemented mobile operational
        resources only): TASK_EXECUTION (START/COMPLETE/CANCEL),
        CHECKLIST_RESPONSES (SAVE), EVIDENCE_SUBMISSION (SUBMIT metadata
        reference), TASK_ASSIGNMENT (UPDATE status).

        BE-25H idempotency: every operation is keyed by (user, operationId).
        A duplicate retry of the same operationId (in the same or a later
        batch) replays the ORIGINAL stored result — success or failure —
        without executing the write again; the stored result/reference is
        returned as the safe replay response. Concurrent duplicate batches
        are guarded by the unique key.

        BE-25I conflict handling: when an operation carries
        `data.baseVersion` (the resource's `updatedAt` the client last saw)
        and it is older than the CURRENT server version, the item returns a
        SYNC_CONFLICT failure with the current server state/reference and
        retry/reload guidance (`error.conflict`); the write is NOT executed
        and newer server data is never silently overwritten. Operations
        without `baseVersion` behave exactly as before.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      x-required-permission: per-resourceType (see x-sync-supported-resource-types)
      x-building-scoped: true
      x-sync-supported-resource-types:
        - resourceType: TASK_EXECUTION
          operations: [START, COMPLETE, CANCEL]
          operationIds: [startTask, completeTask, cancelTask]
          permission: task.manage
        - resourceType: CHECKLIST_RESPONSES
          operations: [SAVE]
          operationIds: [saveChecklistResponses]
          permission: checklist.manage
        - resourceType: EVIDENCE_SUBMISSION
          operations: [SUBMIT]
          operationIds: [submitEvidence]
          permission: evidence.manage
          permissionByExecutionType: >-
            MOB-C06 PART 01A — the exact permission is derived per operation
            from data.executionType (never from a client-supplied value):
            FINDING_VERIFICATION → finding.review; FINDING / FINDING_REWORK
            / existing kinds → evidence.manage (the default above).
        - resourceType: TASK_ASSIGNMENT
          operations: [UPDATE]
          operationIds: [updateTaskAssignment]
          permission: task.manage
        - resourceType: PATROL_EXECUTION
          operations: [START, COMPLETE]
          operationIds: [startPatrolExecution, completePatrolExecution]
          permission: patrol_execution.manage
        - resourceType: PATROL_POINT_VISIT
          operations: [SUBMIT]
          operationIds: [recordPatrolPointVisit]
          permission: patrol_execution.manage
        - resourceType: METER_READING
          operations: [SUBMIT]
          operationIds: [submitMeterReading]
          permission: meter_reading_binding.manage
      x-sync-unsupported-resource-types:
        - resourceType: WORK_ORDER_ACTION
          status: MISSING
          reason: >-
            The BE-08 action set is acknowledge / start / hold / resume / note
            / complete / cancel, but the sync envelope has no verb for
            acknowledge, hold, resume or note. Adding a kind that could carry
            only part of the action set would advertise a half-contract, and
            adding verbs is a separate decision.
          useInstead: [acknowledgeWorkOrder, startWorkOrder, completeWorkOrder]
        - resourceType: WORK_ORDER_MATERIAL_USAGE
          status: MISSING
          reason: >-
            `recordWorkOrderMaterialUsage` is a stock-ledger write (STOCK_OUT
            + balance + cost snapshot). Queued offline issues would be applied
            against availability that may no longer exist, and the contract
            has no reservation concept. This needs an explicit product
            decision, not a dispatcher entry.
          useInstead: [recordWorkOrderMaterialUsage]
        - resourceType: INCIDENT
          status: MISSING
          reason: >-
            Incident and Operational Incident creation requires a
            client-supplied `incidentNumber` that must be unique per Client;
            offline devices cannot allocate one safely, and the sync envelope
            keys every operation to an EXISTING authoritative `resourceId`.
          useInstead: [createIncident, createOperationalIncident]
        - resourceType: SECURITY_FINDING
          status: MISSING
          reason: >-
            Same create-shaped gap as INCIDENT — `createSecurityFinding`
            creates a new BE-09 Finding, so there is no prior authoritative
            resourceId to key the operation to.
          useInstead: [createSecurityFinding, createEngineeringFinding]
        - resourceType: SUPERVISOR_DECISION
          status: MISSING
          reason: >-
            Supervisor verification is an online reviewer act on a completed
            execution; queueing a stale approve/reject offline would let a
            decision be applied to state the reviewer never saw. Deliberately
            excluded.
          useInstead:
            [submitSupervisorInspectionDecision, submitMobileVerification,
             submitWorkOrderVerification]
        - resourceType: EVIDENCE_BYTES
          status: NOT_REQUIRED
          reason: >-
            The backend must not hold an offline byte queue. EVIDENCE_SUBMISSION
            syncs metadata only; bytes go to the evidence file API.
          useInstead: [uploadMobileEvidence, uploadEvidenceFile]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MobileSyncBatchRequest"
      responses:
        "200":
          description: The batch result with per-item results.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileSyncBatchResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /mobile/qr/resolve/{identifier}:
    get:
      operationId: resolveMobileQr
      summary: Resolve a QR/identifier from mobile
      description: >
        BE-25F mobile QR resolution contract. Resolves an opaque identifier
        (QR label, tag, barcode) through the existing BE-05H Asset Identifier
        authority and returns the discriminated target with Building /
        Functional-Location context, Asset / Equipment context where
        applicable, and available mobile action hints. Requires
        `asset_identifier.read`.

        Access is restricted to the BE-02G accessible set: an identifier whose
        Asset's Building is not accessible yields the SAME 404 as an unknown
        value (existence is hidden). No separate QR engine; the existing
        resolve endpoint remains unchanged.

        CR-BE-MOB-01 PART 05 — frozen QR payload convention. The scanned
        payload is the OPAQUE `identifier_value` of a row in the BE-05H
        `asset_identifiers` registry (`identifierType` QR / TAG / BARCODE /
        LEGACY). It is deliberately meaningless: no Client, Building, URL or
        security data is encoded, so a label read by anyone leaks nothing. It
        is NOT a canonical UUID and NOT a structured deep link. The identifier
        registry is asset-scoped — identifiers are registered only through
        `POST /assets/{assetId}/identifiers` — therefore ASSET is the only
        target type this operation can ever return today
        (`x-qr-supported-target-types`).

        Scanning is never anonymous: the caller must present a valid session
        and hold `asset_identifier.read`, and the result is filtered by the
        caller's accessible Buildings. The backend records no GPS coordinate
        and performs no geofence check — scan position is not an input to,
        or an output of, this contract.

        Operational contexts that are NOT scan-resolvable are listed in
        `x-qr-target-type-boundary`. For each of them the client must use the
        authoritative canonical-id read named there; the backend must not be
        asked to guess, and such a target must never be mapped onto ASSET.
      x-required-permission: asset_identifier.read
      x-building-scoped: true
      x-qr-supported-target-types: [ASSET]
      x-qr-target-type-boundary:
        - targetType: LOCATION
          status: MISSING
          reason: >-
            BE-04 Floor / Area / Room / Space rows have a `code` that is
            unique per parent, but there is no location identifier registry
            and no resolve-by-identifier route. Nothing exists to resolve a
            scanned label to a location.
          authority: BE-04 Structure
          canonicalIdReads:
            [getBuildingHierarchy, listBuildingFloors, getFloor, getArea,
             getRoom, getSpace]
        - targetType: FUNCTIONAL_LOCATION
          status: MISSING
          reason: >-
            Same as LOCATION — BE-04 Functional Locations carry no identifier
            registry. The context projection is already published.
          authority: BE-04 Functional Locations
          canonicalIdReads:
            [getFunctionalLocation, getFunctionalLocationContext,
             listBuildingFunctionalLocations]
        - targetType: CHECKPOINT
          status: MISSING
          reason: >-
            BE-12B patrol route points are identified by canonical UUID plus
            sequence and location FKs; they carry no identifier value.
            Checkpoint confirmation is therefore by canonical `pointId`
            (`recordPatrolPointVisit`), not by scan.
          authority: BE-12B Patrol Route Points + BE-12D Patrol Execution
          canonicalIdReads:
            [listPatrolRoutePoints, getPatrolExecution, listPatrolPointVisits]
        - targetType: WORK_ORDER
          status: MISSING
          reason: >-
            BE-08 Work Orders have a human-readable `workOrderNumber` but no
            identifier registry and no resolve-by-number route. Mobile reaches
            a Work Order through its assignment feed or canonical id.
          authority: BE-08 Work Order
          canonicalIdReads:
            [listMobileAssignments, getWorkOrder, listBuildingWorkOrders]
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/IdentifierPathParam"
      responses:
        "200":
          description: The mobile QR resolution contract.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileQrResolution"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # --------------------------------------------------------------------
  # CR-BE-MOB-CONTRACT-01 PART 06 — Location hierarchy & operational
  # context. Read-only projections of the BE-04 / BE-05 authorities.
  # --------------------------------------------------------------------
  /buildings/{buildingId}/hierarchy:
    get:
      operationId: getBuildingHierarchy
      summary: Get the Building digital structure
      description: >
        The complete authoritative digital structure of one Building (BE-04H):
        floors → areas → rooms (with Room Type) → spaces (with pinned
        Functional Locations), plus Building-level Functional Locations.
        Requires `building.read` and an ACTIVE Building assignment
        (`requireBuildingAccess`). Read-only projection — never stored.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: The Building digital structure.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/BuildingHierarchy" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /functional-locations/{functionalLocationId}/context:
    get:
      operationId: getFunctionalLocationContext
      summary: Get the operational context of a Functional Location
      description: >
        The authoritative operational location context (BE-04H) behind one
        Functional Location — location hierarchy only, never Asset/Equipment
        records. Requires `functional_location.read` AND an ACTIVE Building
        assignment for the Functional Location's Building (403
        BUILDING_ACCESS_DENIED otherwise). Only levels that exist are present.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FunctionalLocationIdPath"
      responses:
        "200":
          description: The functional location operational context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/OperationalContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/floors:
    get:
      operationId: listBuildingFloors
      summary: List floors for a Building
      description: Requires `floor.read` and an ACTIVE Building assignment.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Floor list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicFloor" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /floors/{floorId}:
    get:
      operationId: getFloor
      summary: Get a floor by id
      description: Requires `floor.read` and Building access (resolved from the floor).
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FloorIdPath"
      responses:
        "200":
          description: Floor record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicFloor" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /floors/{floorId}/areas:
    get:
      operationId: listFloorAreas
      summary: List areas for a floor
      description: Requires `area.read` and Building access (resolved from the floor).
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FloorIdPath"
      responses:
        "200":
          description: Area list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicArea" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /areas/{areaId}:
    get:
      operationId: getArea
      summary: Get an area by id
      description: Requires `area.read` and Building access (resolved from the area).
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/AreaIdPath"
      responses:
        "200":
          description: Area record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicArea" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /areas/{areaId}/rooms:
    get:
      operationId: listAreaRooms
      summary: List rooms for an area
      description: Requires `room.read` and Building access (resolved from the area).
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/AreaIdPath"
      responses:
        "200":
          description: Room list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicRoom" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /rooms/{roomId}:
    get:
      operationId: getRoom
      summary: Get a room by id
      description: Requires `room.read` and Building access (resolved from the room).
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/RoomIdPath"
      responses:
        "200":
          description: Room record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicRoom" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /rooms/{roomId}/spaces:
    get:
      operationId: listRoomSpaces
      summary: List spaces for a room
      description: Requires `space.read` and Building access (resolved from the room).
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/RoomIdPath"
      responses:
        "200":
          description: Space list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicSpace" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /spaces/{spaceId}:
    get:
      operationId: getSpace
      summary: Get a space by id
      description: Requires `space.read` and Building access (resolved from the space).
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/SpaceIdPath"
      responses:
        "200":
          description: Space record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicSpace" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/functional-locations:
    get:
      operationId: listBuildingFunctionalLocations
      summary: List Functional Locations for a Building
      description: Requires `functional_location.read` and an ACTIVE Building assignment.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Functional Location list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicFunctionalLocation" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /functional-locations/{functionalLocationId}:
    get:
      operationId: getFunctionalLocation
      summary: Get a Functional Location by id
      description: Requires `functional_location.read` and Building access.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/FunctionalLocationIdPath"
      responses:
        "200":
          description: Functional Location record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicFunctionalLocation" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/assets:
    get:
      operationId: listBuildingAssets
      summary: List assets for a Building
      description: >
        Requires `asset.read` and an ACTIVE Building assignment. Optional
        `status` filter.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/AssetStatus" }
      responses:
        "200":
          description: Asset list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicAsset" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /assets/{assetId}:
    get:
      operationId: getAsset
      summary: Get an asset by id
      description: >
        The authoritative Asset record (BE-05A). Requires `asset.read` and an
        ACTIVE Building assignment for the asset's Building (403
        BUILDING_ACCESS_DENIED otherwise).
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: Asset record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicAsset" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /assets/{assetId}/location:
    get:
      operationId: getAssetLocation
      summary: Get the asset location / operational context
      description: >
        The asset plus its authoritative resolved operational location context
        (BE-05C). Requires `asset.read` and Building access.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: The asset and its location context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/AssetLocation" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /assets/{assetId}/equipment-profile:
    get:
      operationId: getEquipmentProfile
      summary: Get the asset's equipment profile
      description: >
        The Equipment Profile bound to an asset (BE-05D), or null when none.
        Requires `equipment_profile.read` and Building access.
      tags: [Location & Asset Context]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: The equipment profile (or null).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        allOf:
                          - $ref: "#/components/schemas/PublicEquipmentProfile"
                        nullable: true
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /mobile/evidence:
    post:
      operationId: uploadMobileEvidence
      summary: Upload evidence from mobile (single call)
      description: >-
        BE-25E mobile evidence upload contract. One multipart request that
        validates the evidence requirement/type/context, stores the file bytes
        through the CR-BE-API-01 PART 03 storage abstraction (never in
        PostgreSQL) and creates the evidence submission in a single step.


        MOB-C06 PART 01 — the upload also accepts the Finding evidence
        parents. The parent is resolved ONLY from `executionType` +
        `executionId` through the shared evidence execution loader (the same
        authority as the generic Finding evidence endpoints); Building/Client
        scope is always server-derived and no building/client/finding
        reference is accepted from the request.


        Permission is asserted exactly per `executionType` after the multipart
        body is parsed:

        - FORM_INSTANCE / CHECKLIST_EXECUTION / FINDING / FINDING_REWORK →
        `evidence.manage`
        - FINDING_VERIFICATION → `finding.review` (a reviewer is not forced
        to hold `evidence.manage`; `evidence.manage` alone does not grant
        verification evidence)


        Parent-state gates mirror the generic Finding evidence endpoints:
        a rework cycle (`FINDING_REWORK`, `executionId` = rework cycle id)
        only receives evidence while REQUESTED; a verification review
        (`FINDING_VERIFICATION`, `executionId` = review id) only receives
        evidence while PENDING; terminal parents are rejected.


        Multipart fields: `file` (max 50 MB), `evidenceType`
        (PHOTO/DOCUMENT/SIGNATURE), `executionType`
        (FORM_INSTANCE/CHECKLIST_EXECUTION/FINDING/FINDING_REWORK/FINDING_VERIFICATION),
        `executionId`, optional `evidenceRequirementId`, `capturedAt`
        (ISO-8601), `originalFileName`. Internal storage paths are never
        exposed.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file, evidenceType, executionType, executionId]
              properties:
                file:
                  type: string
                  format: binary
                  description: Max 50 MB; MIME type must match the evidence type.
                evidenceType:
                  type: string
                  enum: [PHOTO, DOCUMENT, SIGNATURE]
                executionType:
                  type: string
                  enum: [FORM_INSTANCE, CHECKLIST_EXECUTION, FINDING, FINDING_REWORK, FINDING_VERIFICATION]
                executionId:
                  type: string
                  format: uuid
                  description: >-
                    The authoritative parent id: form instance id
                    (FORM_INSTANCE), checklist execution id
                    (CHECKLIST_EXECUTION), Finding id (FINDING), rework cycle
                    id (FINDING_REWORK), or verification review id
                    (FINDING_VERIFICATION).
                evidenceRequirementId:
                  type: string
                  format: uuid
                capturedAt:
                  type: string
                  format: date-time
                originalFileName:
                  type: string
      responses:
        "201":
          description: The created mobile evidence contract.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileEvidence"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /mobile/evidence/{evidenceId}:
    get:
      operationId: getMobileEvidence
      summary: Get mobile evidence reference
      description: >
        BE-25E mobile evidence reference read model for an existing evidence
        submission: requirement reference, target task/checklist/work
        reference, file metadata, evidence type, captured_at, upload status,
        and Client/Building context. Requires `evidence.read`; access is bound
        to the evidence record's Client scope (BE-02G).
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/EvidenceIdPathParam"
      responses:
        "200":
          description: The mobile evidence contract.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileEvidence"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /mobile/checklist-executions/{executionId}:
    get:
      operationId: getMobileChecklistExecution
      summary: Get a checklist execution for mobile (checklist mobile contract)
      description: >
        BE-25D mobile checklist execution read model. Returns the execution
        with its checklist/task reference, checklist items (values/status),
        measurement/UOM where applicable, evidence requirements, execution
        status, and backend-authoritative available actions. Requires
        `checklist.read`; the execution's Client must be inside the caller's
        accessible scope (BE-02G). Composition over the BE-07 authorities
        only — no separate mobile checklist engine. Evidence submission is
        the BE-25E contract.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ExecutionIdPathParam"
      responses:
        "200":
          description: The mobile checklist execution contract.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileChecklistExecution"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # --------------------------------------------------------------------------
  # MOB-C04 PART 02 — Online mobile checklist execution lock.
  #
  # Mobile executes a checklist ONLINE through these commands (offline execution
  # stays on the BE-25G sync contract). Each command reuses the SAME shared
  # checklist-execution service as the generic REST execution endpoints — the
  # lifecycle and scope rules are never re-implemented — and is additionally
  # gated by the authoritative current-shift Building authority: the execution
  # may be mutated only by a user currently on shift at the Building its work
  # belongs to. Server-derives Building/shift authority; no client-provided
  # buildingId/shiftId/securityPostId/role/permission/activeShift/
  # workforceProfileId is authority. Accessible-but-off-shift or no-current
  # shift → 403 CHECKLIST_EXECUTION_NOT_IN_CURRENT_SHIFT. Inaccessible
  # execution → existing 403 BUILDING_ACCESS_DENIED. Requires `checklist.manage`.
  # --------------------------------------------------------------------------
  /mobile/checklist-executions/{executionId}/start:
    post:
      operationId: startMobileChecklistExecution
      summary: Start a checklist execution for mobile (shift-locked)
      description: >-
        MOB-C04 PART 02 / PART 02A mobile online execution: DRAFT →
        IN_PROGRESS. Delegates to the shared checklist-execution service after
        the authoritative work + current-shift Building authority gate: the
        execution must be authoritatively bound to a generated task
        (`generated_task_id`); that task's Building must be a Building the
        caller is currently on shift at; and the task must be assigned to the
        caller (existing task-assignment authority). Unbound / standalone
        executions and off-shift / unassigned work are rejected (403
        CHECKLIST_EXECUTION_NOT_IN_CURRENT_SHIFT). Requires `checklist.manage`.
        The Building is derived server-side from `generated_task_id` — never
        from a template-first task lookup or client input.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ExecutionIdPathParam"
      responses:
        "200":
          description: Updated execution row.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileChecklistExecutionUpdate"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /mobile/checklist-executions/{executionId}/responses:
    put:
      operationId: saveMobileChecklistResponses
      summary: Save checklist responses for mobile (shift-locked)
      description: >-
        MOB-C04 PART 02 / PART 02A mobile online execution: saves checklist
        item responses through the shared checklist-execution service after the
        authoritative work + current-shift Building authority gate (must be
        bound to a generated task, on-shift at that task's Building, and
        assigned to the caller; unbound/standalone or off-shift/unassigned work
        is rejected). Requires `checklist.manage`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ExecutionIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: "#/components/schemas/ChecklistResponsesRequest"
      responses:
        "200":
          description: Responses saved (empty payload, matching the generic endpoint).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /mobile/checklist-executions/{executionId}/complete:
    post:
      operationId: completeMobileChecklistExecution
      summary: Complete a checklist execution for mobile (shift-locked)
      description: >-
        MOB-C04 PART 02 / PART 02A mobile online execution: completes the
        execution (all required items answered) through the shared
        checklist-execution service after the authoritative work + current-shift
        Building authority gate (must be bound to a generated task, on-shift at
        that task's Building, and assigned to the caller; unbound/standalone or
        off-shift/unassigned work is rejected). Requires `checklist.manage`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ExecutionIdPathParam"
      responses:
        "200":
          description: Updated execution row.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileChecklistExecutionUpdate"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /mobile/checklist-executions/{executionId}/cancel:
    post:
      operationId: cancelMobileChecklistExecution
      summary: Cancel a checklist execution for mobile (shift-locked)
      description: >-
        MOB-C04 PART 02 / PART 02A mobile online execution: cancels the
        execution through the shared checklist-execution service after the
        authoritative work + current-shift Building authority gate (must be
        bound to a generated task, on-shift at that task's Building, and
        assigned to the caller; unbound/standalone or off-shift/unassigned work
        is rejected). Requires `checklist.manage`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ExecutionIdPathParam"
      responses:
        "200":
          description: Updated execution row.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileChecklistExecutionUpdate"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /mobile/checklist-executions/{executionId}/finding:
    post:
      operationId: createMobileChecklistFinding
      summary: Report a Finding from an authoritative checklist execution
      description: >-
        MOB-C05 PART 03 — thin mobile command for a field worker to report a
        Finding from an authoritative bound checklist execution. Authority is
        derived entirely server-side and NEVER from the client: the execution
        must be bound to an exact generated task
        (`checklist_execution.generated_task_id → generated_tasks` →
        Building/Client, the PART 01 authoritative source resolver), that
        Building must be inside the caller's BE-02G accessible scope, the task
        must be executable by the caller via the existing ACTIVE task-assignment
        authority, and the caller must currently be on shift in that exact
        Building (the C04 authority gate). Unbound/standalone executions, a
        bound task with no Building, cross-Client bindings, inaccessible
        Buildings, unassigned workers, and off-shift workers all fail closed
        (403). The Finding number is generated server-side (`FND_<8 uppercase
        hex>`) and is never accepted from the client; the Finding is created
        and its `CHECKLIST_EXECUTION` source bound atomically in one
        transaction. The client may send only `title` (required) and optional
        `description`; any authoritative-context field (clientId, buildingId,
        findingNumber, reportedByUserId, generatedTaskId, checklistTemplateId,
        workforceProfileId, shift, sourceType/sourceId, status, assignee,
        reviewer) is rejected. QR resolution and direct TASK / WORK_ORDER /
        standalone / manual Finding creation are NOT supported by this
        endpoint. A checklist execution may legitimately produce more than one
        Finding, so repeated calls create new Findings (no source-uniqueness
        idempotency is imposed). Requires `checklist.manage` (the existing
        execution authority) — NOT `finding.manage`; server-side authority is
        the real gate.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ExecutionIdPathParam"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateMobileChecklistFindingRequest"
      responses:
        "201":
          description: The created Finding acknowledgement.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileChecklistFindingCreated"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
  /mobile/tasks/{taskId}/checklist-execution:
    post:
      operationId: openMobileChecklistExecution
      summary: Open (get-or-create) the checklist execution for an assigned task
      description: >-
        MOB-C04 PART 02B — authoritative task → checklist-execution entry point.
        Opens (get-or-create) the bound checklist execution for a generated task
        (`taskId` = the mobile-assignment feed `reference.taskId` for a TASK
        item). Authority is derived server-side from the authenticated session +
        the generated task: the task must exist, its `target_type` must be
        CHECKLIST_TEMPLATE with an ACTIVE template, the task Building must be in
        the caller's BE-02G accessible scope, the task must be executable by the
        caller via the existing WORKFORCE / TEAM assignment authority, and the
        caller must currently be on shift in the task Building. It is
        idempotent (get-or-create): a partial UNIQUE index on
        `generated_task_id` guarantees at most one bound execution per task.
        Client-provided templateId/buildingId/shiftId/securityPostId/
        workforceProfileId are never authority. Only establishes the binding;
        lifecycle runs via the shared service + the shift-locked mobile
        commands. Requires `checklist.manage`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - name: taskId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The bound checklist execution reference (created or reused).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileChecklistOpenReference"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /mobile/tasks/{taskId}/form-instance:
    post:
      operationId: openMobileFormInstance
      summary: Open (get-or-create) the form instance for an assigned task
      description: >-
        MOB-C07 PART 02 — authoritative task → form-instance entry point.
        Opens (get-or-create) the bound Form Instance for a generated task
        (`taskId` = the mobile-assignment feed `reference.taskId` for a TASK
        item). Authority is derived server-side from the authenticated session +
        the generated task: the task must exist, its `target_type` must be
        FORM_VERSION with an exact PUBLISHED version whose parent template is
        ACTIVE and whose parent Client matches `task.client_id`, the task
        Building must be in the caller's BE-02G accessible scope, the task must
        be executable by the caller via the existing WORKFORCE / TEAM
        assignment authority, the caller must currently be on shift in the
        task Building, and the task status must be OPEN, ASSIGNED, or
        IN_PROGRESS. Client-provided clientId/buildingId/generatedTaskId/
        formTemplateVersionId are never authority. FORM_TEMPLATE tasks are
        rejected (no latest-PUBLISHED resolve). It is idempotent (get-or-create):
        a partial UNIQUE index on `generated_task_id` guarantees at most one
        bound Form Instance per task. Creation only establishes the binding as
        DRAFT; it does not start the instance or mutate the task. Requires
        `form_instance.execute`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      x-required-permission: form_instance.execute
      parameters:
        - name: taskId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The bound Form Instance reference (created or reused).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileFormInstanceOpenReference"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /mobile/form-instances/{instanceId}/start:
    post:
      operationId: startMobileFormInstance
      summary: Start a bound form instance for mobile
      description: >-
        MOB-C07 PART 03 — DRAFT → IN_PROGRESS for an already task-bound Form
        Instance. Re-checks bound-instance authority on every call: the instance
        must exist and have `generated_task_id` NOT NULL (unbound generic
        instances are denied and stay on the generic routes); the exact
        generated task must be FORM_VERSION; the instance version must equal
        `task.target_id`; that version must be PUBLISHED with an ACTIVE parent
        template whose Client matches `task.client_id` and `instance.client_id`;
        the task Building must be in the caller's BE-02G scope; the caller must
        be assigned (C04 WORKFORCE/TEAM) and currently on shift in that
        Building; and the task must be OPEN, ASSIGNED, or IN_PROGRESS.
        Client-provided clientId/buildingId/taskId/versionId/workforce/shift
        are never authority. Lifecycle is PART 01A `startFormInstance` (DRAFT
        only; IN_PROGRESS/COMPLETED/CANCELLED keep the shared rejections).
        Does not start, complete, or otherwise mutate the generated task.
        Requires `form_instance.execute`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      x-required-permission: form_instance.execute
      parameters:
        - name: instanceId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The started Form Instance reference.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileFormInstanceOpenReference"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /mobile/form-instances/{instanceId}/responses:
    put:
      operationId: saveMobileFormInstanceResponses
      summary: Save responses on a bound form instance for mobile
      description: >-
        MOB-C07 PART 03 — save field responses on an already task-bound Form
        Instance. Re-checks the same bound-instance authority as start (bound
        generated task, FORM_VERSION, published version, ACTIVE parent, client
        match, BE-02G Building, C04 assignment, current shift, non-terminal
        task). Unbound instances are denied. Payload semantics match the
        generic PUT `/form-instances/{id}/responses` (single object or array
        of `{ fieldId, value }`). Validation (pinned-version fields, types,
        occurrence unique index, terminal guard) is PART 01A
        `saveFormResponses`. Returns the same empty payload. Does not mutate
        the generated task. Requires `form_instance.execute`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      x-required-permission: form_instance.execute
      parameters:
        - name: instanceId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FormInstanceResponsesWrite"
      responses:
        "200":
          description: Responses saved (empty payload, matching the generic endpoint).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: object
                        additionalProperties: false
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /mobile/form-instances/{instanceId}/complete:
    post:
      operationId: completeMobileFormInstance
      summary: Complete a bound form instance for mobile
      description: >-
        MOB-C07 PART 04 — complete an already task-bound Form Instance.
        Re-checks bound-instance authority on every call: the instance must
        exist and have `generated_task_id` NOT NULL (unbound generic instances
        are denied and stay on the generic routes); the exact generated task
        must be FORM_VERSION; the instance version must equal `task.target_id`;
        that version must be PUBLISHED with an ACTIVE parent template whose
        Client matches `task.client_id` and `instance.client_id`; the task
        Building must be in the caller's BE-02G scope; the caller must be
        assigned (C04 WORKFORCE/TEAM) and currently on shift in that Building;
        and the task must be OPEN, ASSIGNED, or IN_PROGRESS. Client-provided
        clientId/buildingId/taskId/versionId/workforce/shift are never
        authority. Lifecycle is PART 01A `finishFormInstance` (required-field
        completeness, DRAFT complete when required fields are present, terminal
        guards, `completed_at`). Completing the Form Instance does NOT start,
        complete, cancel, or otherwise mutate the generated task. Requires
        `form_instance.execute`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      x-required-permission: form_instance.execute
      parameters:
        - name: instanceId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The completed Form Instance reference.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileFormInstanceOpenReference"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /mobile/form-instances/{instanceId}/cancel:
    post:
      operationId: cancelMobileFormInstance
      summary: Cancel a bound form instance for mobile
      description: >-
        MOB-C07 PART 04 — cancel an already task-bound Form Instance.
        Re-checks the same bound-instance authority as complete. Lifecycle is
        PART 01A `finishFormInstance` (terminal guards; `completed_at` remains
        unset on cancel). Cancelling the Form Instance does NOT cancel or
        otherwise mutate the generated task. The cancelled instance remains
        bound to the same `generated_task_id`; it is not deleted, unbound, or
        replaced. Requires `form_instance.execute`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      x-required-permission: form_instance.execute
      parameters:
        - name: instanceId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The cancelled Form Instance reference.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileFormInstanceOpenReference"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /mobile/assignments:
    get:
      operationId: listMobileAssignments
      summary: List my assignments (mobile field-user feed)
      description: >
        BE-25C unified mobile assignment feed. Returns the authenticated
        user's active Task (BE-07) and Work Order (BE-08) assignments — direct
        WORKFORCE assignments of the user's linked Workforce Profile and TEAM
        assignments of the user's Team — restricted to the accessible
        Client/Building scope (BE-02G). Requires `task.read` and
        `work_order.read`.

        `availableActions` is backend-authoritative (the same authorities the
        execution endpoints enforce) and gated on the caller's
        `task.manage` / `work_order.manage` permissions. Work orders expose
        execution actions (ACKNOWLEDGE, START, HOLD, RESUME, ADD_NOTE,
        CANCEL) plus CLOSE only when every closure rule is satisfied;
        `schedule.dueAt` is always null (no authoritative due date
        exists yet). Pagination is opt-in via `page`/`pageSize` (BE-25A
        convention).

        MOB-C04 PART 01: each item also carries a nullable `shift` context when
        the work's Building is the Building the worker is currently on shift
        at, resolved by reusing the `/mobile/current-shift` authority (BE-03E
        roster + BE-02F/G scope + Building-timezone wall-clock) — no
        client-computed "active shift" flag.

        MOB-C04 PART 01A — projection selector. Without `shift` the feed is the
        BE-25C default: ALL accessible active assignments (on-shift or not).
        With `shift=current` it returns the now-live current-shift projection:
        only assignments whose Building is a Building the worker is currently
        on shift at (per the authoritative `/mobile/current-shift` authority),
        i.e. the currently-executable field work for the worker's authoritative
        current shift. When the worker has no current shift at the instant of
        the request, `shift=current` returns `200` with an empty `data` array —
        it never falls back to the all-accessible surface. BE-02G scope
        isolation is the isolation floor in both modes; `shift=current` never
        expands or re-selects authority and `buildingId`/`shiftId`/
        `securityPostId` supplied by the client are not authority.
        `securityPost` remains context only and is never a filter. `current` is
        the only supported `shift` projection value; any other value is a
        `400 VALIDATION_ERROR`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/PageParam"
        - $ref: "#/components/parameters/PageSizeParam"
        - name: shift
          in: query
          description: >
            MOB-C04 PART 01A projection selector. Omit for the default BE-25C
            all-accessible feed, or set `current` for the now-live current-shift
            projection (empty data array when the worker is not on shift).
          required: false
          schema:
            type: string
            enum: [current]
      responses:
        "200":
          description: >
            Scoped mobile assignment feed (full list or requested page). Default
            mode = all accessible active assignments. `shift=current` = the
            current-shift projection (200 with an empty data array when the
            worker has no current shift).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/MobileAssignment"
                      meta:
                        $ref: "#/components/schemas/MobilePaginationMeta"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  # --------------------------------------------------------------------
  # CR-BE-MOB-02 PART 07C — Mobile Current Shift effective-context contract.
  #
  # A small self-service effective-context endpoint. The backend derives the
  # authenticated user's current shift(s) from the linked BE-03C Workforce
  # Profile, the BE-03E Shift roster and the BE-02F/G Building access
  # authority — never from caller-supplied identity and never the generic
  # Shift CRUD surface. Authentication-only (same posture as GET /auth/me).
  # --------------------------------------------------------------------
  /mobile/current-shift:
    get:
      operationId: getMobileCurrentShift
      summary: Resolve the authenticated user's effective current shift
      description: >-
        BE-25M self-service effective-context read. Derives the caller's
        current shift(s) from the linked BE-03C Workforce Profile, the BE-03E
        Shift roster (`workforce_shift_assignments`) and the BE-02F/G
        accessible-Building authority — never from caller-supplied identity
        and never from the generic Shift CRUD surface. A Shift is current
        only when: its roster assignment is ACTIVE and its optional
        `effectiveFrom`/`effectiveUntil` window contains the current instant;
        the Shift itself is ACTIVE and in an accessible Building that carries
        an IANA `timezone`; and the current time-of-day in that timezone falls
        inside the Shift's `startTime`/`endTime` window (overnight-aware).
        A Building without a timezone cannot affirm "current", so its shifts
        are excluded rather than guessed. Requires only a valid bearer session
        (self-service, like `GET /auth/me`); an empty `shifts` array means the
        caller is not on shift at the derivation instant. Authoritative ids:
        `shiftId` = `shifts.id`, `assignmentId` =
        `workforce_shift_assignments.id`, `workforceProfileId` =
        `workforce_profiles.id`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Effective current-shift context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileCurrentShiftContext"
        "401":
          $ref: "#/components/responses/Unauthorized"

  # --------------------------------------------------------------------
  # CR-BE-MOB-05 PART 01 — Mobile Upcoming Shifts self-service contract.
  #
  # A read-only, caller-scoped schedule of the authenticated user's current
  # and future shift assignments, derived from the same authorities as
  # `GET /mobile/current-shift` (BE-25M family): the linked BE-03C Workforce
  # Profile, the BE-03E Shift roster (`workforce_shift_assignments`), the
  # BE-03E `shifts` and `buildings` records, and the BE-02F/G accessible
  # Building authority — never from caller-supplied identity and never the
  # generic Shift CRUD surface. No attendance, clock, roster, handover, or
  # schedule state is invented: only ACTIVE roster assignments of ACTIVE
  # Shifts in accessible Buildings whose effective window overlaps the
  # queried window are returned. Authentication-only self-service (same
  # posture as GET /auth/me and GET /mobile/current-shift).
  # --------------------------------------------------------------------
  /mobile/upcoming-shifts:
    get:
      operationId: getMobileUpcomingShifts
      summary: Resolve the authenticated user's current and future shift assignments
      description: >-
        CR-BE-MOB-05 PART 01 self-service read. Derives the caller's
        current/future shift schedule from the linked BE-03C Workforce
        Profile, the BE-03E Shift roster (`workforce_shift_assignments`) and
        the BE-02F/G accessible-Building authority — never from
        caller-supplied identity and never from the generic Shift CRUD
        surface. A roster assignment is included only when it is ACTIVE, its
        Shift is ACTIVE, its Building is in the caller's accessible set and
        carries a valid IANA `timezone`, and its optional
        `effectiveFrom`/`effectiveUntil` window overlaps the queried window
        (default: from the current instant, unbounded into the future).
        A Building without a valid timezone cannot be exposed safely, so its
        shifts are excluded rather than guessed. The daily wall-clock window
        is NOT evaluated here — `GET /mobile/current-shift` remains the
        single authority for "what is live right now"; this endpoint lists
        the caller's rostered current/future assignments. Optional
        `dateFrom` / `dateTo` bound the roster effective window (ISO-8601
        date or datetime; a date-only `dateTo` is inclusive of that whole
        UTC day; `dateFrom` must not exceed `dateTo`; range capped at 366
        days). Requires only a valid bearer session (self-service, like
        `GET /auth/me` and `GET /mobile/current-shift`); an empty `shifts`
        array means no matching roster. Authoritative ids: `shiftId` =
        `shifts.id`, `assignmentId` = `workforce_shift_assignments.id`,
        `workforceProfileId` = `workforce_profiles.id`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      parameters:
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
          description: >-
            Optional inclusive lower bound on the roster effective window
            (ISO-8601 date YYYY-MM-DD treated as a UTC day, or datetime).
            Defaults to the current instant.
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
          description: >-
            Optional upper bound on the roster effective window (ISO-8601
            date YYYY-MM-DD inclusive of that whole UTC day, or datetime).
            Omitted = unbounded.
      responses:
        "200":
          description: Effective upcoming-shifts schedule context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileUpcomingShiftsContext"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  # --------------------------------------------------------------------
  # CR-BE-MOB-02 PART 07D — Mobile My Team effective-context contract.
  #
  # A small self-service effective-context endpoint. The backend derives the
  # authenticated user's own team and its ACTIVE members from the linked
  # BE-03C Workforce Profile (`team_id`) and the BE-03B Team hierarchy
  # (Team → Department → Organization → Client) — never from caller-supplied
  # workforceProfileId / teamId and never the generic Team CRUD surface.
  # Authentication-only (same posture as GET /auth/me).
  # --------------------------------------------------------------------
  /mobile/my-team:
    get:
      operationId: getMobileMyTeam
      summary: Resolve the authenticated user's team context and members
      description: >-
        BE-25N self-service effective-context read. Derives the caller's own
        team (the team of their linked BE-03C Workforce Profile) and its ACTIVE
        members from the session identity — never from caller-supplied
        `workforceProfileId` or `teamId` and never from the generic Team CRUD
        surface. The team hierarchy (Team → Department → Organization →
        Client) is exposed only when the profile's Client is inside the
        caller's accessible scope (BE-02F/G). `team` is null when the caller
        has no linked ACTIVE profile, no team, or a profile whose Client is
        outside the accessible scope; `members` lists ACTIVE profiles in that
        team (the caller included). Requires only a valid bearer session
        (self-service, like `GET /auth/me` and `GET /mobile/current-shift`).
        Authoritative ids: team = `teams.id`, members = `workforce_profiles.id`
        (+ `positions.id`), hierarchy refs = `departments.id` /
        `organizations.id` / `clients.id`.
      tags: [Mobile Execution]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Effective my-team context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MobileMyTeamContext"
        "401":
          $ref: "#/components/responses/Unauthorized"

  # --------------------------------------------------------------------
  # CR-BE-MOB-05 PART 02 — Workforce Attendance self-service contract.
  #
  # Backend-authoritative clock-in / clock-out for the authenticated
  # workforce profile. Identity is resolved from the authenticated session
  # (linked BE-03C Workforce Profile) and never accepted from the client;
  # timestamps are set by the backend database clock (NOW()) and never
  # accepted from the client. Clock-in asserts BE-02F/G Building access and
  # binds the applicable ACTIVE BE-03E roster row when one exists (optional
  # "when available" binding — never invented, never derived from Current
  # Shift / task / handover / Team). Lifecycle: CLOCKED_IN → CLOCKED_OUT;
  # at most one open clock-in per profile (409 on duplicate); clock-out
  # without an open record → 409. Self-service gated by the dedicated
  # `attendance.manage` (clock-in/out) and `attendance.read` (current)
  # codes — no attendance administration surface exists.
  # --------------------------------------------------------------------
  /attendance/clock-in:
    post:
      operationId: clockInAttendance
      summary: Clock the authenticated workforce profile into a Building
      description: >-
        CR-BE-MOB-05 PART 02. Opens a CLOCKED_IN attendance record for the
        authenticated Workforce Profile (resolved from the session, never
        from the request) at the supplied `buildingId`. The Building is
        access-asserted (BE-02F/G, 403 when inaccessible), must be ACTIVE
        and must resolve to the profile's own Client. When the profile holds
        an applicable ACTIVE roster assignment at the Building (effective
        window containing now, wall-clock-current preferred among several),
        the record binds the authoritative `workforce_shift_assignment_id`
        and `shiftId`; otherwise the binding is null — attendance is never
        derived from Current Shift, tasks, handovers, Team, or local state.
        `clockInAt` is the backend database timestamp. Duplicate open
        clock-in → 409 `ATTENDANCE_ACTIVE_ALREADY_EXISTS`. Requires
        `attendance.manage`.
      tags: [Attendance]
      security:
        - bearerAuth: []
      x-required-permission: attendance.manage
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ClockInRequest" }
      responses:
        "201":
          description: The authoritative attendance record (CLOCKED_IN).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/AttendanceRecord" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /attendance/clock-out:
    post:
      operationId: clockOutAttendance
      summary: Clock the authenticated workforce profile out
      description: >-
        CR-BE-MOB-05 PART 02. Closes the authenticated profile's open
        CLOCKED_IN attendance record: `clockOutAt` is the backend database
        timestamp and the status becomes CLOCKED_OUT (terminal, preserved).
        The record is resolved from the session — no payload and no
        caller-supplied identity or timestamp. Clock-out without an open
        record → 409 `ATTENDANCE_NOT_ACTIVE`. Requires `attendance.manage`.
      tags: [Attendance]
      security:
        - bearerAuth: []
      x-required-permission: attendance.manage
      responses:
        "200":
          description: The authoritative attendance record (CLOCKED_OUT).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/AttendanceRecord" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "409": { $ref: "#/components/responses/Conflict" }

  /attendance/current:
    get:
      operationId: getCurrentAttendance
      summary: Resolve the authenticated profile's open attendance record
      description: >-
        CR-BE-MOB-05 PART 02. Returns the authenticated profile's open
        CLOCKED_IN attendance record, or null when the profile has no linked
        ACTIVE Workforce Profile or no open record. Self-service: only the
        caller's own record is ever returned. Requires `attendance.read`.
        Authoritative ids: `id` = `attendance_records.id`,
        `workforceProfileId` = `workforce_profiles.id`, `buildingId` =
        `buildings.id`, `workforceShiftAssignmentId` =
        `workforce_shift_assignments.id`, `shiftId` = `shifts.id`.
      tags: [Attendance]
      security:
        - bearerAuth: []
      x-required-permission: attendance.read
      responses:
        "200":
          description: The open attendance record, or null.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        nullable: true
                        allOf:
                          - $ref: "#/components/schemas/AttendanceRecord"
        "401": { $ref: "#/components/responses/Unauthorized" }

  # --------------------------------------------------------------------
  # CR-BE-MOB-05 PART 03 — Security Operational Logbook contract.
  #
  # Backend-authoritative, Building-scoped Security logbook entries. Each
  # entry binds the authenticated Workforce Profile (resolved from the
  # session, never from the request), the Building it belongs to (BE-02F/G
  # access-asserted on every operation — no cross-Building reads or writes),
  # and an optional existing authoritative `shift_handovers.id` in the SAME
  # Building (unknown → 404; cross-Building → 400). `recordedAt` is set by
  # the backend database clock only. Lifecycle OPEN → CLOSED: updates are
  # allowed only while OPEN; CLOSED is terminal (400
  # SECURITY_LOGBOOK_ENTRY_IMMUTABLE). Category is a fixed classification
  # vocabulary; the backend branches no behavior on it.
  # --------------------------------------------------------------------
  /buildings/{buildingId}/security/logbook:
    post:
      operationId: createSecurityLogbookEntry
      summary: Create a Security logbook entry at a Building
      description: >-
        CR-BE-MOB-05 PART 03. Records a Building-scoped Security logbook
        entry for the authenticated Workforce Profile (session-resolved,
        never caller-supplied) at the supplied Building (BE-02F/G access-
        asserted; 403 outside scope). The optional `shiftHandoverId` must
        reference an existing authoritative `shift_handovers.id` in the SAME
        Building — unknown → 404 `SHIFT_HANDOVER_NOT_FOUND`, cross-Building →
        400 `SECURITY_LOGBOOK_HANDOVER_BUILDING_MISMATCH`. Outgoing/incoming
        Shift IDs are never invented. `recordedAt` is the backend database
        timestamp. Status is OPEN on creation. Requires
        `security_logbook.manage`.
      tags: [Security Logbook]
      security:
        - bearerAuth: []
      x-required-permission: security_logbook.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSecurityLogbookEntryRequest"
      responses:
        "201":
          description: The authoritative logbook entry (OPEN).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SecurityLogbookEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    get:
      operationId: listSecurityLogbookEntries
      summary: List Security logbook entries of a Building
      description: >-
        CR-BE-MOB-05 PART 03. Lists the Building's logbook entries, newest
        first, constrained to the caller's accessible Buildings (BE-02F/G;
        Building-nested route with `requireBuildingAccess`). Optional
        `status` / `category` filters and a `dateFrom` / `dateTo` window on
        `recordedAt` (ISO-8601 date or datetime; a date-only `dateTo` is
        inclusive of that whole UTC day; range capped at 366 days). Requires
        `security_logbook.read`.
      tags: [Security Logbook]
      security:
        - bearerAuth: []
      x-required-permission: security_logbook.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/SecurityLogbookStatus" }
        - name: category
          in: query
          required: false
          schema: { $ref: "#/components/schemas/SecurityLogbookCategory" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
          description: Inclusive lower bound on recordedAt (ISO-8601 date or datetime).
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
          description: >-
            Upper bound on recordedAt (ISO-8601 date inclusive of that whole
            UTC day, or datetime).
      responses:
        "200":
          description: The Building's logbook entries.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/SecurityLogbookEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /security/logbook/{id}:
    get:
      operationId: getSecurityLogbookEntry
      summary: Resolve a Security logbook entry
      description: >-
        CR-BE-MOB-05 PART 03. Returns one authoritative logbook entry; the
        caller must have BE-02F/G access to the entry's Building (an unknown
        id → 404; an entry in an inaccessible Building → 403
        `BUILDING_ACCESS_DENIED` — never a cross-Building leak). Requires
        `security_logbook.read`. Authoritative id: `id` =
        `security_logbook_entries.id`.
      tags: [Security Logbook]
      security:
        - bearerAuth: []
      x-required-permission: security_logbook.read
      x-building-scoped: true
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: The authoritative logbook entry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SecurityLogbookEntry" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateSecurityLogbookEntry
      summary: Update a Security logbook entry while OPEN
      description: >-
        CR-BE-MOB-05 PART 03. Updates category / summary / detail / status
        / optional handover binding of an OPEN entry. CLOSED entries are
        terminal → 400 `SECURITY_LOGBOOK_ENTRY_IMMUTABLE`. The entry's
        Building is access-asserted (BE-02F/G): an unknown id → 404; an
        entry in an inaccessible Building → 403 `BUILDING_ACCESS_DENIED`. A
        supplied `shiftHandoverId` must reference an existing authoritative
        `shift_handovers.id` in the SAME Building (unknown → 404;
        cross-Building → 400). Timestamps are never accepted from the
        client. Requires `security_logbook.manage`.
      tags: [Security Logbook]
      security:
        - bearerAuth: []
      x-required-permission: security_logbook.manage
      x-building-scoped: true
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSecurityLogbookEntryRequest"
      responses:
        "200":
          description: The updated authoritative logbook entry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SecurityLogbookEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # --------------------------------------------------------------------
  # CR-BE-BAST-01 — canonical BE-22 lifecycle, compatibility and closure
  # --------------------------------------------------------------------
  /bast-documents:
    post:
      operationId: createCanonicalBastDocument
      summary: Create a canonical BAST and its shared Document/Version
      description: >
        Creates the sole authoritative BAST identity (`bast_documents.id`).
        `documentType` must be BAST. Work Order remains closure authority.
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateBastDocumentRequest" }
      responses:
        "201":
          description: Canonical BAST created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/BastDocument" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listCanonicalBastDocuments
      summary: List canonical BAST documents in accessible scope
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: workOrderId, in: query, schema: { $ref: "#/components/schemas/Uuid" } }
        - { name: vendorWorkId, in: query, schema: { $ref: "#/components/schemas/Uuid" } }
        - { name: workCompletionDocumentId, in: query, schema: { $ref: "#/components/schemas/Uuid" } }
        - { name: buildingId, in: query, schema: { $ref: "#/components/schemas/Uuid" } }
        - { name: clientId, in: query, schema: { $ref: "#/components/schemas/Uuid" } }
        - { name: contextType, in: query, schema: { $ref: "#/components/schemas/BastContextType" } }
        - { name: acceptanceStatus, in: query, schema: { $ref: "#/components/schemas/BastStatus" } }
        - { name: bastNumber, in: query, schema: { type: string } }
      responses:
        "200":
          description: Scope-filtered canonical BAST list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/BastDocument" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /bast-documents/reconciliation:
    get:
      operationId: getBastReconciliationInventory
      summary: Read non-destructive canonical/legacy BAST reconciliation inventory
      description: >
        Returns read-only classifications and safe identifiers. It never links,
        renumbers, changes lifecycle state, deletes legacy data, or resolves a
        quarantine.
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Read-only reconciliation inventory.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/BastReconciliationInventory" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /bast-documents/{id}:
    get:
      operationId: getCanonicalBastDocument
      summary: Get one canonical BAST by authoritative identity
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BastDocumentIdPath"
      responses:
        "200":
          description: Canonical BAST.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/BastDocument" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /bast-documents/{id}/submit:
    post:
      operationId: submitCanonicalBastDocument
      summary: Create the initial immutable BAST submission attempt
      description: >
        DRAFT → SUBMITTED. Pins the selected immutable Document Version,
        Completion/Service Report references, latest completed approved Work
        Order/Vendor Work reviews, readiness snapshot, and current evidence.
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BastDocumentIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitBastDocumentRequest" }
      responses:
        "200":
          description: Initial submission attempt created atomically.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/BastLifecycleCommandResult" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /bast-documents/{id}/resubmit:
    post:
      operationId: resubmitCanonicalBastDocument
      summary: Submit a corrected BAST as a new immutable attempt
      description: >
        REJECTED → SUBMITTED. Requires the rejection-linked Finding to be
        VERIFIED or CLOSED, a newer immutable Document Version, and current
        readiness/evidence. Historical attempts, Sign-Offs and Finding links
        are preserved; current acceptance projection fields are cleared.
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BastDocumentIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitBastDocumentRequest" }
      responses:
        "200":
          description: Corrected immutable submission attempt created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/BastLifecycleCommandResult" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /bast-documents/{id}/decisions:
    post:
      operationId: decideCanonicalBastDocument
      summary: Accept or reject the current BAST submission attempt
      description: >
        Requires `bast.accept`. SUBMITTED → ACCEPTED creates one immutable
        acceptance Sign-Off. SUBMITTED → REJECTED creates one rejection
        Sign-Off and one linked Finding. The current attempt and Document
        Version are always used; clients cannot supply competing audit links.
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BastDocumentIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DecideBastDocumentRequest" }
      responses:
        "200":
          description: Atomic acceptance or rejection decision.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/BastLifecycleCommandResult" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /vendor-basts:
    post:
      deprecated: true
      operationId: createLegacyVendorBast
      summary: Deprecated legacy BAST create (always restricted)
      description: >
        Compatibility contract only. Returns BAST_LEGACY_WRITE_RESTRICTED and
        directs callers to POST /bast-documents; it cannot create a competing
        lifecycle authority.
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateLegacyVendorBastRequest" }
      responses:
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      deprecated: true
      operationId: listLegacyVendorBasts
      summary: List scope-safe legacy BAST compatibility projections
      description: >
        Safely linked rows project canonical `bast_documents.id`, lifecycle and
        Sign-Off state. Legacy-only/divergent rows remain labelled fallbacks.
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: vendorWorkId, in: query, schema: { $ref: "#/components/schemas/Uuid" } }
        - { name: vendorId, in: query, schema: { $ref: "#/components/schemas/Uuid" } }
        - { name: buildingId, in: query, schema: { $ref: "#/components/schemas/Uuid" } }
      responses:
        "200":
          description: Legacy compatibility projections.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/LegacyVendorBastProjection" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /vendor-basts/{bastId}:
    get:
      deprecated: true
      operationId: getLegacyVendorBast
      summary: Get one legacy BAST compatibility projection
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/LegacyVendorBastIdPath"
      responses:
        "200":
          description: Legacy compatibility projection.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/LegacyVendorBastProjection" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /vendor-basts/{bastId}/submit:
    post:
      deprecated: true
      operationId: submitLegacyVendorBastProjection
      summary: Delegate a safe linked legacy submit/resubmit to canonical BE-22
      description: >
        Never mutates legacy lifecycle columns. Safe context-consistent links
        delegate to the canonical submit or resubmit command; all other writes
        are restricted pending reconciliation.
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/LegacyVendorBastIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitBastDocumentRequest" }
      responses:
        "200":
          description: Updated compatibility projection from canonical state.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/LegacyVendorBastProjection" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /vendor-basts/{bastId}/accept:
    post:
      deprecated: true
      operationId: acceptLegacyVendorBastProjection
      summary: Delegate a safe linked legacy acceptance to canonical BE-22
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/LegacyVendorBastIdPath"
      requestBody:
        content:
          application/json:
            schema: { $ref: "#/components/schemas/LegacyBastDecisionRequest" }
      responses:
        "200":
          description: Updated compatibility projection from canonical state.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/LegacyVendorBastProjection" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /vendor-basts/{bastId}/reject:
    post:
      deprecated: true
      operationId: rejectLegacyVendorBastProjection
      summary: Delegate a safe linked legacy rejection to canonical BE-22
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/LegacyVendorBastIdPath"
      requestBody:
        content:
          application/json:
            schema: { $ref: "#/components/schemas/LegacyBastDecisionRequest" }
      responses:
        "200":
          description: Updated compatibility projection from canonical state.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/LegacyVendorBastProjection" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /work-orders/{id}/close:
    post:
      operationId: closeWorkOrderAfterCanonicalBastReadiness
      summary: Close a completed and approved Work Order
      description: >
        Work Order is the sole closure authority. Existing completion/latest
        approval rules are enforced before canonical BAST cardinality
        (`NONE`, `WORK_ORDER`, `EACH_VENDOR_WORK`), accepted traceability,
        Finding/Rework and reconciliation-quarantine readiness. Failure is
        non-mutating and legacy BAST state never independently satisfies closure.
      tags: [BAST Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Work Order transitioned to CLOSED.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderCloseResult" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  # ==================================================================
  # CR-BE-COM-02 — Vendor Invoice (PART 01–05)
  # ==================================================================

  /vendors/{vendorId}/invoices:
    post:
      operationId: createVendorInvoice
      x-required-permission: vendor_invoice.manage
      x-building-scoped: true
      summary: Create a Vendor Invoice
      description: >
        Creates a DRAFT Vendor Invoice for an external Vendor. The Vendor must
        be ACTIVE and hold an ACTIVE relationship to the invoice's Building.
        `purchaseOrderId` is optional; when supplied it must reference an
        existing ISSUED PO with the same Client, Building, and Vendor.
        `workContractId` is optional but requires `purchaseOrderId`; it must
        reference that PO's ACTIVE or COMPLETED SPK with the same scope and
        Vendor. PO/SPK IDs are persisted as immutable authoritative references.
        Existing operational references must share the invoice Vendor, Client,
        Building, and their actual Work Order/Vendor Work/report/BAST chain.
        This creation gate does not persist matching or quantity truth.
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: vendorId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateVendorInvoiceRequest" }
      responses:
        "201":
          description: Vendor Invoice created as DRAFT.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorInvoice" }
        "400":
          description: >
            Invalid request/linkage (`VALIDATION_ERROR`), unknown PO
            (`VENDOR_INVOICE_PURCHASE_ORDER_INVALID`), unknown SPK
            (`VENDOR_INVOICE_WORK_CONTRACT_INVALID`), SPK/PO mismatch
            (`VENDOR_INVOICE_WORK_CONTRACT_PO_MISMATCH`), Vendor mismatch
            (`VENDOR_INVOICE_PROCUREMENT_VENDOR_MISMATCH`), Building/Client
            mismatch (`VENDOR_INVOICE_PROCUREMENT_SCOPE_MISMATCH`), invalid
            existing operational context (`VENDOR_INVOICE_CONTEXT_INVALID`),
            inactive Vendor (`VENDOR_INACTIVE`), or invalid Vendor/Building
            relationship (`VENDOR_INVOICE_VENDOR_INVALID`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Route Vendor not found (`VENDOR_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            Duplicate invoice number within the Client
            (`VENDOR_INVOICE_NUMBER_ALREADY_EXISTS`), PO state is not ISSUED
            (`VENDOR_INVOICE_PURCHASE_ORDER_NOT_ISSUED`), or SPK state is not
            ACTIVE/COMPLETED
            (`VENDOR_INVOICE_WORK_CONTRACT_NOT_ELIGIBLE`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /vendor-invoices:
    get:
      operationId: listVendorInvoices
      x-required-permission: vendor_invoice.read
      x-building-scoped: true
      summary: List Vendor Invoices
      description: >
        Lists Vendor Invoices scoped to the caller's accessible Buildings,
        optionally filtered by vendor, building, work order, status, or date range.
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: vendorId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: workOrderId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: purchaseOrderId
          in: query
          description: CR-BE-R2P-01 PART 06 — filter by the linked Purchase Order.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: workContractId
          in: query
          description: CR-BE-R2P-01 PART 06 — filter by the linked SPK.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          schema: { type: string, enum: [DRAFT, FINALIZED, CANCELLED] }
        - name: invoiceDateFrom
          in: query
          schema: { type: string, format: date }
        - name: invoiceDateTo
          in: query
          schema: { type: string, format: date }
      responses:
        "200":
          description: List of Vendor Invoices.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/VendorInvoice" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /vendor-invoices/{id}:
    get:
      operationId: getVendorInvoice
      x-required-permission: vendor_invoice.read
      x-building-scoped: true
      summary: Get a Vendor Invoice
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Vendor Invoice.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorInvoice" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateVendorInvoice
      x-required-permission: vendor_invoice.manage
      x-building-scoped: true
      summary: Update a DRAFT Vendor Invoice
      description: >
        Requires `vendor_invoice.manage` and access to the invoice Building.
        Only DRAFT commercial fields may change. Invoice identity, Vendor,
        Client/Building, operational references, `purchaseOrderId`, and
        `workContractId` are immutable. FINALIZED and CANCELLED invoices reject
        updates without changing payment, verification, or linkage authority.
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateVendorInvoiceRequest" }
      responses:
        "200":
          description: DRAFT Vendor Invoice updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorInvoice" }
        "400":
          description: >
            Invalid/immutable input (`VALIDATION_ERROR`), a FINALIZED invoice
            (`VENDOR_INVOICE_FINALIZED_PROTECTED`), or a non-DRAFT invoice
            (`VENDOR_INVOICE_NOT_DRAFT`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Vendor Invoice not found (`VENDOR_INVOICE_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /vendor-invoices/{id}/available-actions:
    get:
      operationId: getVendorInvoiceAvailableActions
      x-required-permission: vendor_invoice.read
      x-building-scoped: true
      summary: Resolve caller-specific Vendor Invoice actions
      description: >
        Requires invoice Building access. Uses existing invoice lifecycle,
        canonical matching, verification, settlement readiness and the caller's
        `vendor_invoice.manage` permission. Every returned token maps to an
        existing command. Read-only callers and terminal invoices receive an
        empty list; frontend must not infer additional actions from status.
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Caller-specific authoritative actions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/VendorInvoiceAvailableActions"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Vendor Invoice not found (`VENDOR_INVOICE_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /vendor-invoices/{id}/finalize:
    post:
      operationId: finalizeVendorInvoice
      x-required-permission: vendor_invoice.manage
      x-building-scoped: true
      summary: Finalize a draft Vendor Invoice
      description: >
        Transitions a DRAFT Vendor Invoice to FINALIZED. Finalized invoices are
        immutable and eligible for verification.
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Vendor Invoice finalized.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorInvoice" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /vendor-invoices/{id}/cancel:
    post:
      operationId: cancelVendorInvoice
      x-required-permission: vendor_invoice.manage
      x-building-scoped: true
      summary: Cancel a Vendor Invoice
      description: >
        Transitions a DRAFT or FINALIZED Vendor Invoice to CANCELLED (terminal).
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Vendor Invoice cancelled.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorInvoice" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /vendor-invoices/{id}/verify:
    post:
      operationId: verifyVendorInvoice
      x-required-permission: vendor_invoice.manage
      x-building-scoped: true
      summary: Verify a finalized Vendor Invoice
      description: >
        Consumes the exact canonical result returned by GET /matching.
        MATCHED promotes the FINALIZED invoice to VERIFIED. MISMATCH records
        DISCREPANCY with deterministic reasons. NOT_READY rejects with 409
        `VENDOR_INVOICE_MATCHING_NOT_READY` and does not mutate verification
        state. A still-MATCHED VERIFIED invoice is idempotent.
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                notes: { type: string, nullable: true, maxLength: 2000 }
      responses:
        "200":
          description: Verification result.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorInvoiceVerifyResult" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: >
            Canonical matching status is NOT_READY
            (`VENDOR_INVOICE_MATCHING_NOT_READY`); verification state is not
            mutated and reasons are returned in `error.details`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /vendor-invoices/{id}/matching:
    get:
      operationId: getVendorInvoiceMatching
      x-required-permission: vendor_invoice.read
      x-building-scoped: true
      summary: Get matching evaluation (read-only diagnostic)
      description: >
        Returns the canonical MATCHED / MISMATCH / NOT_READY result without
        mutation. For linked invoices it compares immutable PO/SPK context,
        invoice amount/currency to committed PO Lines, authoritative MR
        received quantities or finalized service Receiving, and existing
        Work Order/Vendor Work/report/BAST evidence with its actual scope and
        relationships. Unlinked legacy invoices retain the prior operational-
        evidence path with procurement checks not applicable.
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Matching result.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/InvoiceMatchingResult" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /vendor-invoices/{id}/payment:
    post:
      operationId: recordVendorPayment
      x-required-permission: vendor_invoice.manage
      x-building-scoped: true
      summary: Record a payment against a Vendor Invoice
      description: >
        Records a positive, finite payment only when the canonical settlement
        readiness authority is READY. This enforces FINALIZED + VERIFIED,
        canonical MATCHED, valid scope/amount/currency and coherent existing
        payment state. The amount must fit NUMERIC(18,2), have at most two
        decimal places, and keep cumulative paidAmount at or below invoiceAmount. A cumulative amount
        below invoiceAmount produces PARTIALLY_PAID; an exact full payment
        produces PAID; overpayment is rejected with 422
        VENDOR_INVOICE_OVERPAYMENT. PostgreSQL derives outstandingAmount from
        invoiceAmount minus the cumulative paidAmount. A supplied paymentDate
        becomes lastPaymentDate; when omitted, an existing lastPaymentDate is
        preserved. Optional notes are retained in the payment audit event.
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RecordVendorPaymentRequest" }
      responses:
        "200":
          description: Payment recorded.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorInvoice" }
        "400":
          description: >
            Invalid amount/date/notes (`VALIDATION_ERROR`), invoice not VERIFIED
            (`VENDOR_INVOICE_NOT_VERIFIED_FOR_PAYMENT`), or payment prohibited by
            its lifecycle (`VENDOR_INVOICE_PAYMENT_NOT_ALLOWED`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              examples:
                invalidAmount:
                  value:
                    success: false
                    error:
                      code: VALIDATION_ERROR
                      message: Request validation failed.
                notVerified:
                  value:
                    success: false
                    error:
                      code: VENDOR_INVOICE_NOT_VERIFIED_FOR_PAYMENT
                      message: Only a verified Vendor Invoice can receive payments.
                paymentNotAllowed:
                  value:
                    success: false
                    error:
                      code: VENDOR_INVOICE_PAYMENT_NOT_ALLOWED
                      message: Payments cannot be recorded against this invoice.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            Missing `vendor_invoice.manage` permission (`PERMISSION_DENIED`) or
            no access to the invoice Building (`BUILDING_ACCESS_DENIED`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: Vendor Invoice not found (`VENDOR_INVOICE_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            Canonical settlement readiness is NOT_READY
            (`VENDOR_INVOICE_PAYMENT_NOT_READY`). Existing lifecycle-specific
            errors remain 400; fully paid duplicate attempts remain 422.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "422":
          description: >
            The payment would make cumulative paidAmount exceed invoiceAmount
            (`VENDOR_INVOICE_OVERPAYMENT`). No payment state is committed.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              examples:
                overpayment:
                  value:
                    success: false
                    error:
                      code: VENDOR_INVOICE_OVERPAYMENT
                      message: Payment would exceed the outstanding amount on this invoice.

  /vendor-invoices/{id}/settlement-readiness:
    get:
      operationId: getVendorInvoiceSettlementReadiness
      x-required-permission: vendor_invoice.read
      x-building-scoped: true
      summary: Evaluate settlement readiness
      description: >
        Returns canonical READY / NOT_READY / SETTLED payment eligibility.
        READY requires FINALIZED + VERIFIED, canonical MATCHED, valid scope,
        amount/currency and a coherent unpaid/partial payment state. SETTLED is
        derived from existing PAID + zero outstanding state. NOT_READY includes
        deterministic reasons. The projection is read-only and advertises only
        `RECORD_PAYMENT`, the existing payment command, when READY.
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Settlement readiness result.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorSettlementReadinessResult" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /vendor-invoices/{id}/consistency:
    get:
      operationId: getVendorInvoiceConsistency
      x-required-permission: vendor_invoice.read
      x-building-scoped: true
      summary: Evaluate vendor consistency cross-check
      description: >
        Evaluates vendor consistency across the commercial-to-operational chain.
        Checks invoice vendor vs vendor work vendor, vendor work vendor vs work
        order assigned vendor, procurement vendor match, and client/building
        isolation. Pure read-only diagnostic — does not mutate any records.
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Vendor consistency result.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorConsistencyResult" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /vendor-invoices/{id}/trace:
    get:
      operationId: getVendorInvoiceTrace
      x-required-permission: vendor_invoice.read
      x-building-scoped: true
      summary: Canonical Request-to-Pay trace from a Vendor Invoice
      description: >
        Returns one read-only graph derived from existing foreign keys and
        authoritative projections. Documents expose type/id/number/status;
        relationships expose source/target ids. Optional PO/SPK, material or
        service Receiving, Work/Vendor Work, report and BAST paths appear only
        when real records exist. Matching, verification, payment and settlement
        readiness are current projections, never persisted graph state. Requires
        invoice Building access; no cross-Building records are returned.
      tags: [Vendor Invoice]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Trace result.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorInvoiceTrace" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Vendor Invoice not found (`VENDOR_INVOICE_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-R2P-01 PART 07 — upstream chain links.
  #
  # Documented here so the authoritative chain is traversable end to end from
  # the contract alone. These endpoints predate CR-BE-R2P-01; their behaviour
  # is unchanged and is described exactly as implemented.
  # ---------------------------------------------------------------------------

  /purchase-requests/{purchaseRequestId}/service-requests:
    post:
      operationId: createServiceRequest
      summary: Create a Service Request line on a Purchase Request
      description: >
        Requires `service_request.manage` and access to the Purchase Request's
        Building. Client and Building are derived from the parent Purchase
        Request, which must be OPEN. The optional Vendor must belong to the
        same Client and the optional Functional Location to the same Building.
      tags: [Service Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: purchaseRequestId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateServiceRequestRequest" }
      responses:
        "201":
          description: Service Request created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ServiceRequest" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    get:
      operationId: listServiceRequestsByPurchaseRequest
      summary: List the Service Request lines of a Purchase Request
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02A (BE-17C). Requires
        `service_request.read`. The route carries a Purchase Request parameter
        but no Building parameter, so BE-02 Building isolation is enforced in
        the handler after the parent Purchase Request's Building is resolved:
        a caller without an ACTIVE assignment to that Building receives
        `BUILDING_ACCESS_DENIED`. Optional `status` and `serviceType` filters
        are parsed by the shared Service Request filter validator; an invalid
        value is rejected with `VALIDATION_ERROR`. `data` is a plain array —
        this listing is not paginated. No approval, vendor selection, PO
        readiness or operational Work Order execution is exposed here.
      tags: [Service Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: purchaseRequestId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ServiceRequestStatus" }
        - name: serviceType
          in: query
          required: false
          description: >
            Data-driven service type code. Trimmed and uppercased before
            matching; must start with a letter and contain only uppercase
            letters, digits, hyphens and underscores (2-64 characters).
          schema: { type: string, minLength: 2, maxLength: 64 }
      responses:
        "200":
          description: Service Request lines of the Purchase Request.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/ServiceRequest" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/service-requests:
    get:
      operationId: listServiceRequestsByBuilding
      summary: List the Service Requests of a Building
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02A (BE-17C). Requires
        `service_request.read`. BE-02 Building isolation is enforced at the
        route level by `requireBuildingAccess('buildingId')`: a caller without
        an ACTIVE assignment to the Building receives `BUILDING_ACCESS_DENIED`.
        Optional `status`, `purchaseRequestId` and `serviceType` filters are
        parsed by the shared Service Request filter validator; an invalid value
        is rejected with `VALIDATION_ERROR`. `data` is a plain array — this
        listing is not paginated.
      tags: [Service Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ServiceRequestStatus" }
        - name: purchaseRequestId
          in: query
          required: false
          description: Restrict the listing to one Purchase Request of the Building.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: serviceType
          in: query
          required: false
          description: >
            Data-driven service type code. Trimmed and uppercased before
            matching; must start with a letter and contain only uppercase
            letters, digits, hyphens and underscores (2-64 characters).
          schema: { type: string, minLength: 2, maxLength: 64 }
      responses:
        "200":
          description: Service Requests of the Building.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/ServiceRequest" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /service-requests/{id}:
    get:
      operationId: getServiceRequest
      summary: Get a Service Request
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02A (BE-17C). Requires
        `service_request.read`. The route carries no Building parameter, so
        BE-02 Building isolation is enforced in the handler after the record's
        Building is resolved: a caller without an ACTIVE assignment to that
        Building receives `BUILDING_ACCESS_DENIED`.
      tags: [Service Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          description: Service Request id (BE-17C).
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Service Request.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ServiceRequest" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Service Request not found (`SERVICE_REQUEST_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    patch:
      operationId: updateServiceRequest
      summary: Update an OPEN Service Request
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02A (BE-17C). Requires
        `service_request.manage` and access to the record's Building. Only OPEN
        Service Requests are mutable at intake; a CANCELLED request is rejected
        with `SERVICE_REQUEST_NOT_OPEN`. Every body field is optional and only
        the supplied fields change; `null` clears a nullable field.
        `clientId`, `buildingId`, `purchaseRequestId`, `status` and
        `requestedByUserId` are never caller-supplied — cancellation uses
        `POST /service-requests/{id}/cancel`, and there is no reopen. A
        supplied `serviceCatalogId` must resolve, belong to the same Client,
        be ACTIVE and have a code equal to `serviceType`; the free-text
        `serviceType` is never rewritten by the anchor. An optional Functional
        Location must belong to the request's Building and an optional Vendor
        to the request's Client.
      tags: [Service Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          description: Service Request id (BE-17C).
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateServiceRequestRequest" }
      responses:
        "200":
          description: Updated Service Request.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ServiceRequest" }
        "400":
          description: >
            Validation failed, or `SERVICE_REQUEST_NOT_OPEN` (already
            cancelled), `SERVICE_REQUEST_LOCATION_BUILDING_MISMATCH`,
            `SERVICE_REQUEST_VENDOR_CLIENT_MISMATCH`,
            `SERVICE_REQUEST_CATALOG_CLIENT_MISMATCH`,
            `SERVICE_REQUEST_CATALOG_INACTIVE`,
            `SERVICE_REQUEST_CATALOG_CODE_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: >
            `SERVICE_REQUEST_NOT_FOUND`, or `SERVICE_REQUEST_CATALOG_NOT_FOUND`
            when the supplied Service Catalog anchor does not resolve.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /service-requests/{id}/cancel:
    post:
      operationId: cancelServiceRequest
      summary: Cancel an OPEN Service Request
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02A (BE-17C). Requires
        `service_request.manage` and access to the record's Building.
        Cancellation is the `OPEN` -> `CANCELLED` lifecycle transition, not a
        delete: the record is preserved. `CANCELLED` is terminal — there is no
        reopen — and an already-cancelled request is rejected with
        `SERVICE_REQUEST_NOT_OPEN`. No request body.
      tags: [Service Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          description: Service Request id (BE-17C).
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Cancelled Service Request (`status` = `CANCELLED`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ServiceRequest" }
        "400":
          description: "`SERVICE_REQUEST_NOT_OPEN` — already cancelled."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Service Request not found (`SERVICE_REQUEST_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /vendor-selections:
    post:
      operationId: createVendorSelection
      summary: Evaluate Vendor Selection Readiness for a request
      description: >
        Requires `vendor_selection.manage`. BE-17E — evaluates whether a Vendor
        may be selected for a Purchase or Service Request and persists the
        deterministic verdict with its individual checks. Client and Building
        are derived from the referenced request. Backend-authoritative: the
        caller supplies no readiness value.
      tags: [Vendor Selection Readiness]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateVendorSelectionRequest" }
      responses:
        "201":
          description: Vendor Selection Readiness evaluated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorSelection" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /vendor-selections/{id}:
    get:
      operationId: getVendorSelection
      summary: Get a Vendor Selection Readiness record
      description: Requires `vendor_selection.read`.
      tags: [Vendor Selection Readiness]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Vendor Selection Readiness.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorSelection" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /po-readiness:
    post:
      operationId: createPOReadiness
      summary: Evaluate Purchase Order Readiness for a request
      description: >
        Requires `po_readiness.manage`. BE-17F — THE readiness authority of the
        R2P chain. Evaluates whether a request may be committed to a Purchase
        Order and persists the deterministic verdict (READY / NOT_READY /
        BLOCKED) with its individual checks. READY means "may commit"; an
        ISSUED Purchase Order means "committed".


        Client, Building and the request reference are derived from the
        referenced request. Backend-authoritative: the caller supplies no
        readiness value, and no downstream domain re-decides this verdict.
      tags: [Purchase Order Readiness]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePOReadinessRequest" }
      responses:
        "201":
          description: PO Readiness evaluated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/POReadiness" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /po-readiness/{id}:
    get:
      operationId: getPOReadiness
      summary: Get a Purchase Order Readiness record
      description: >
        Requires `po_readiness.read`. This is the record a Purchase Order
        references through `poReadinessId`.
      tags: [Purchase Order Readiness]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: PO Readiness.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/POReadiness" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updatePOReadiness
      summary: Update the editable detail of a PO Readiness record
      description: >
        Requires `po_readiness.manage`. Only `requiredDate` and `notes` may
        change — the readiness verdict and its checks are backend-derived and
        never caller-supplied.
      tags: [Purchase Order Readiness]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdatePOReadinessRequest" }
      responses:
        "200":
          description: PO Readiness updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/POReadiness" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # ---------------------------------------------------------------------------
  # CR-BE-R2P-01 — Request-to-Procurement / Request-to-Pay chain (PART 01–06)
  #
  # Authoritative chain:
  #   Request → Selected Vendor → PO Readiness (BE-17F, "may commit")
  #   → ISSUED Purchase Order (committed) → ACTIVE Work Contract / SPK
  #   → Work Order → BAST → Vendor Invoice → Matching / Verification
  #   → Settlement Readiness → Payment State
  #
  # Context rule: PO/SPK/Work Order binding scope is DERIVED from its
  # authoritative parent (PO Readiness, PO, or SPK) and cannot be overridden.
  # Vendor Invoice Vendor comes from the route and its Building from the body;
  # both are validated against every supplied PO/SPK/operational reference.
  # BE-02G Building isolation is enforced after context resolution, so an actor
  # without access to the resolved Building receives 403 BUILDING_ACCESS_DENIED.
  # ---------------------------------------------------------------------------

  /price-catalog/lookup:
    get:
      operationId: lookupPriceCatalogEntry
      x-required-permission: price_catalog.read
      x-building-scoped: true
      summary: Resolve the applicable reference price (deterministic §8 probe)
      description: >
        Requires `price_catalog.read` plus access to the anchor Building.
        Internal-only probe — RFQ Vendor sessions never receive this surface.


        Answers one governed question: for a subject (an item + required UOM
        for MATERIAL, or a governed service for SERVICE), a currency, a
        Building and an optional Vendor as-of `asOf`, which ACTIVE price
        authority applies. `sourceMode` defaults to MATERIAL; SERVICE uses
        `serviceId` and has no UOM/quantity. Exact match only (no FX, no UOM
        conversion); precedence is fixed: Vendor+Building → Vendor → Building
        → Client-wide. Typed outcomes only: `MATCHED` carries the full
        authority snapshot; `NO_REFERENCE_PRICE`, `UOM_INCOMPATIBLE`
        (MATERIAL only) and `CURRENCY_INCOMPATIBLE` are explicit absence
        classifications, never fabricated prices. The defensive `AMBIGUOUS`
        trip fails closed as 409 `PRICE_CATALOG_LOOKUP_AMBIGUOUS` (and is
        audited), never a 200 price.
      tags: [Price Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          description: Scope anchor; derives the governed Client.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: sourceMode
          in: query
          description: >
            Defaults to MATERIAL. SERVICE resolves by `serviceId` with no
            UOM/quantity (CR-BE-SVC-01 PART 05).
          schema: { $ref: "#/components/schemas/PriceCatalogSourceMode" }
        - name: itemId
          in: query
          description: MATERIAL subject (required when sourceMode is MATERIAL).
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: uomId
          in: query
          description: >
            Exact-match UOM (required when sourceMode is MATERIAL); no
            conversion exists or is inferred.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: serviceId
          in: query
          description: SERVICE subject (required when sourceMode is SERVICE).
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: currency
          in: query
          required: true
          schema: { $ref: "#/components/schemas/PriceCatalogCurrency" }
        - name: asOf
          in: query
          required: true
          description: ISO-8601 selection instant (half-open window `[)`).
          schema: { type: string, format: date-time }
        - name: vendorId
          in: query
          description: Optional vendor context; enables the vendor tiers.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Typed resolution outcome (never a bare null).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PriceCatalogLookupResult" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /price-catalog/entries:
    post:
      operationId: createPriceCatalogEntry
      x-required-permission: price_catalog.manage
      x-building-scoped: true
      summary: Create a DRAFT price-catalog entry (idempotent)
      description: >
        Requires `price_catalog.manage` plus access to the entry's scope.
        Creates a DRAFT (no effectiveness; never selected until activation).
        MATERIAL-only authority: one Inventory Item priced per 1 unit of an
        explicit UOM in one supported currency. Scope tiers are expressed via
        nullable keys: Client-wide / Building / Vendor / Vendor+Building;
        `entryKind` is derived (`VENDOR_CONTRACT` exactly when `vendorId` is
        set). Approved-by attribution is optional and must reference an
        ACTIVE user. `unitPrice` must be > 0 with at most two decimals;
        `effectiveTo` must be after `effectiveFrom` when given.
      tags: [Price Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/IdempotencyKeyHeader"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePriceCatalogEntryRequest" }
      responses:
        "201":
          description: DRAFT entry created (or idempotent replay).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PriceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listPriceCatalogEntries
      x-required-permission: price_catalog.read
      x-building-scoped: true
      summary: List price-catalog entries (scope-filtered, no existence oracle)
      description: >
        Requires `price_catalog.read`. Scoped to the caller's accessible
        Clients/Buildings; filters outside that reach yield an empty page,
        never an existence oracle.
      tags: [Price Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: clientId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: uomId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: currency
          in: query
          schema: { $ref: "#/components/schemas/PriceCatalogCurrency" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/PriceCatalogEntryStatus" }
      responses:
        "200":
          description: Entries visible to the caller.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PriceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /price-catalog/entries/{id}:
    get:
      operationId: getPriceCatalogEntry
      x-required-permission: price_catalog.read
      x-building-scoped: true
      summary: Read one price-catalog entry (any lifecycle state)
      description: >
        Requires `price_catalog.read` plus access to the entry's scope.
        Historical rows stay queryable: replaced/deactivated entries remain
        byte-intact evidence with their lifecycle closure fields.
      tags: [Price Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PriceCatalogEntryIdPath"
      responses:
        "200":
          description: The entry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PriceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updatePriceCatalogEntry
      x-required-permission: price_catalog.manage
      x-building-scoped: true
      summary: Edit a DRAFT entry (DRAFT only — never ACTIVE/INACTIVE facts)
      description: >
        Requires `price_catalog.manage`. Edits DRAFT fields only — 409
        `PRICE_CATALOG_NOT_DRAFT` otherwise. Re-validates every changed
        reference, scope and window; `entryKind` is re-derived from the final
        Vendor key. ACTIVE and INACTIVE rows are never updated in place.
      tags: [Price Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PriceCatalogEntryIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdatePriceCatalogEntryRequest" }
      responses:
        "200":
          description: Updated DRAFT entry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PriceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /price-catalog/entries/{id}/activate:
    post:
      operationId: activatePriceCatalogEntry
      x-required-permission: price_catalog.manage
      x-building-scoped: true
      summary: Activate a DRAFT entry
      description: >
        Requires `price_catalog.manage`. DRAFT → ACTIVE with its
        `effectiveFrom`/`effectiveTo` window; the per-tier exclusion
        constraint rejects overlaps at activation instant (409
        `PRICE_CATALOG_WINDOW_OVERLAP`). Future-dated windows are supported —
        selection as-of `T` simply ignores them until `T` arrives; nothing is
        flipped by a scheduler.
      tags: [Price Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PriceCatalogEntryIdPath"
      responses:
        "200":
          description: Activated entry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PriceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /price-catalog/entries/{id}/replace:
    post:
      operationId: replacePriceCatalogEntry
      x-required-permission: price_catalog.manage
      x-building-scoped: true
      summary: Atomically close an ACTIVE entry and activate its successor
      description: >
        Requires `price_catalog.manage`. The governed change command: the
        ACTIVE predecessor is closed (INACTIVE, actor/time +
        `replaced_by_entry_id`) and a successor is activated in the same
        transaction. The successor inherits subject, tier keys, UOM and
        currency — only price facts and window change, so a replacement can
        never silently re-scope an authority. The successor
        `effectiveFrom` must not open earlier than the predecessor's window
        start (400 `PRICE_CATALOG_EFFECTIVE_WINDOW_INVALID`) — writing into
        the already-entered past is reserved for the `/correct` override
        lane. One successor per predecessor; overlapping an adjacent ACTIVE
        same-tier window fails 409 `PRICE_CATALOG_WINDOW_OVERLAP`.
      tags: [Price Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PriceCatalogEntryIdPath"
        - $ref: "#/components/parameters/IdempotencyKeyHeader"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ReplacePriceCatalogEntryRequest" }
      responses:
        "201":
          description: Successor created and activated (or idempotent replay).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PriceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /price-catalog/entries/{id}/correct:
    post:
      operationId: correctPriceCatalogEntry
      x-required-permission: price_catalog.override
      x-building-scoped: true
      summary: Governed retroactive correction of an ACTIVE window (override)
      description: >
        Requires `price_catalog.override` — the exceptional authority, never
        implied by `price_catalog.read` or `price_catalog.manage` and
        unassigned by default (even the platform admin role must be granted it
        explicitly). Plus access to the entry's scope (BE-02G) and the
        mandatory `Idempotency-Key`.


        The retroactive-correction lane for an already-entered ACTIVE window.
        The predecessor is closed exactly like a replacement (lifecycle
        linkage only; its price/window facts are never mutated and stay
        queryable), and a NEW ACTIVE successor is created inheriting the
        subject, scope, UOM and currency. Unlike `/replace`, the successor
        window MAY open before the predecessor's start — that exceptional
        capability is precisely why this lane requires the override
        permission. The corrected authority resolves only under its own new
        effective window; existing RFQ comparison snapshots referencing the
        predecessor stay byte-stable historical evidence.


        Governance evidence is mandatory: `reason` is required, non-blank and
        length-checked (≤ 1000 chars) — 400 `VALIDATION_ERROR` when missing
        or blank. Every accepted correction emits exactly one
        `PRICE_CATALOG_ENTRY_OVERRIDE_CORRECTED` operational event carrying
        the reason, the permission code used and both windows. The reason
        joins the idempotency fingerprint: a replayed key with altered
        evidence conflicts (409 `PRICE_CATALOG_IDEMPOTENCY_CONFLICT`).


        Lifecycle guards: target must be ACTIVE (409 `PRICE_CATALOG_NOT_ACTIVE`
        for DRAFT/INACTIVE/already-replaced); an inverted window is 400
        `PRICE_CATALOG_EFFECTIVE_WINDOW_INVALID`; overlapping an adjacent
        ACTIVE same-tier window stays structurally fail-closed (409
        `PRICE_CATALOG_WINDOW_OVERLAP`).
      tags: [Price Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PriceCatalogEntryIdPath"
        - $ref: "#/components/parameters/IdempotencyKeyHeader"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CorrectPriceCatalogEntryRequest" }
      responses:
        "201":
          description: Corrective successor created and activated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PriceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /price-catalog/entries/{id}/deactivate:
    post:
      operationId: deactivatePriceCatalogEntry
      x-required-permission: price_catalog.manage
      x-building-scoped: true
      summary: Deactivate an ACTIVE entry (terminal close, no successor)
      description: >
        Requires `price_catalog.manage`. ACTIVE → INACTIVE, actor/time
        recorded; the row and its window remain as history and historical
        snapshots referencing the entry stay valid.
      tags: [Price Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PriceCatalogEntryIdPath"
      responses:
        "200":
          description: Deactivated entry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PriceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /service-catalog/entries:
    post:
      operationId: createServiceCatalogEntry
      x-required-permission: service_catalog.manage
      x-building-scoped: true
      summary: Create a Service Catalog entry (governed Service identity)
      description: >
        CR-BE-SVC-01 PART 01. Requires `service_catalog.manage` plus access to
        the entry's Client (reached through a Building assignment). Creates a
        governed Service master entry as ACTIVE: a Client-scoped `code`
        (unique per Client, immutable), a `category`, and a `name`. Audit:
        `SERVICE_CATALOG_ENTRY_CREATED` (transaction-atomic).
      tags: [Service Catalog]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateServiceCatalogEntryRequest" }
      responses:
        "201":
          description: Created entry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ServiceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listServiceCatalogEntries
      x-required-permission: service_catalog.read
      x-building-scoped: true
      summary: List Service Catalog entries (Client-scoped, no existence oracle)
      description: >
        CR-BE-SVC-01 PART 01. Requires `service_catalog.read`. Lists entries
        bound to the caller's accessible Client set; an out-of-reach
        `clientId` filter returns an empty page (no existence oracle).
        Optional filters: `clientId`, `status`, `category`, `search`.
      tags: [Service Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: clientId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/ServiceCatalogStatus" }
        - name: category
          in: query
          schema: { type: string }
        - name: search
          in: query
          schema: { type: string }
      responses:
        "200":
          description: Scoped entry list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/ServiceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /service-catalog/entries/{id}:
    get:
      operationId: getServiceCatalogEntry
      x-required-permission: service_catalog.read
      x-building-scoped: true
      summary: Read one Service Catalog entry
      description: >
        CR-BE-SVC-01 PART 01. Requires `service_catalog.read` plus Client
        access (the catalog is Client-scoped — a caller reaches a Client
        through their Building assignments).
      tags: [Service Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ServiceCatalogEntryIdPath"
      responses:
        "200":
          description: The entry (any status).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ServiceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateServiceCatalogEntry
      x-required-permission: service_catalog.manage
      x-building-scoped: true
      summary: Edit a Service Catalog entry (non-identity fields only)
      description: >
        CR-BE-SVC-01 PART 01. Requires `service_catalog.manage`. Edits
        `name`, `description`, `category` only — `code` and `clientId` are
        immutable. Audit: `SERVICE_CATALOG_ENTRY_UPDATED`.
      tags: [Service Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ServiceCatalogEntryIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateServiceCatalogEntryRequest" }
      responses:
        "200":
          description: Updated entry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ServiceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /service-catalog/entries/{id}/deactivate:
    post:
      operationId: deactivateServiceCatalogEntry
      x-required-permission: service_catalog.manage
      x-building-scoped: true
      summary: Deactivate a Service Catalog entry (terminal; no reactivation lane)
      description: >
        CR-BE-SVC-01 PART 01. Requires `service_catalog.manage`. ACTIVE →
        INACTIVE (terminal); the row is retained for history and existing
        references stay valid (governed snapshots/anchors are preserved).
        Audit: `SERVICE_CATALOG_ENTRY_DEACTIVATED`.
      tags: [Service Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ServiceCatalogEntryIdPath"
      responses:
        "200":
          description: Deactivated entry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ServiceCatalogEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /purchase-orders:
    post:
      operationId: createPurchaseOrder
      x-required-permission: purchase_order.manage
      x-building-scoped: true
      summary: Commit a Purchase Order against a READY PO Readiness
      description: >
        Requires `purchase_order.manage`. Creates the PO as DRAFT.


        `poReadinessId` is the ONLY context input: Client, Building, Vendor and
        the request reference are derived from that BE-17F readiness record,
        which must be READY (409 `PURCHASE_ORDER_READINESS_NOT_READY`
        otherwise). PO Readiness remains the sole readiness authority — this
        domain evaluates no readiness of its own. Supplying `clientId`,
        `buildingId`, `vendorId`, `requestType`, `purchaseRequestId`,
        `serviceRequestId` or `status` is rejected as caller-supplied context.


        At most one live (non-CANCELLED) PO may exist per readiness record; the
        PO number is unique within a Client.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePurchaseOrderRequest" }
      responses:
        "201":
          description: Purchase Order committed as DRAFT.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PurchaseOrder" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listPurchaseOrders
      x-required-permission: purchase_order.read
      x-building-scoped: true
      summary: List Purchase Orders
      description: >
        Requires `purchase_order.read`. Scoped to the caller's accessible
        Buildings — rows outside that scope are never returned.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: vendorId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: purchaseRequestId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: serviceRequestId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/PurchaseOrderStatus" }
        - name: poDateFrom
          in: query
          schema: { type: string, format: date }
        - name: poDateTo
          in: query
          schema: { type: string, format: date }
      responses:
        "200":
          description: List of Purchase Orders.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PurchaseOrder" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /purchase-orders/{id}:
    get:
      operationId: getPurchaseOrder
      x-required-permission: purchase_order.read
      x-building-scoped: true
      summary: Get a Purchase Order
      description: Requires `purchase_order.read` and access to the PO's Building.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseOrderIdPath"
      responses:
        "200":
          description: Purchase Order.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PurchaseOrder" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updatePurchaseOrder
      x-required-permission: purchase_order.manage
      x-building-scoped: true
      summary: Update a DRAFT Purchase Order
      description: >
        Requires `purchase_order.manage`. DRAFT only — a non-DRAFT PO yields
        400 `PURCHASE_ORDER_NOT_DRAFT`.


        Only commercial detail may change. Identity (`poNumber`), commitment
        context (`poReadinessId`, `vendorId`, request references), derived scope
        (`clientId`, `buildingId`), `status` and the issuance/cancellation
        provenance columns are immutable; supplying any of them yields
        400 VALIDATION_ERROR. RFQ-derived DRAFT POs are additionally guarded
        with 409 `PURCHASE_ORDER_RFQ_DERIVED_IMMUTABLE`; commercial changes
        must return to the RFQ/award process.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseOrderIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdatePurchaseOrderRequest" }
      responses:
        "200":
          description: Purchase Order updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PurchaseOrder" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /purchase-orders/{id}/available-actions:
    get:
      operationId: getPurchaseOrderAvailableActions
      x-required-permission: purchase_order.read
      x-building-scoped: true
      summary: Resolve the caller's available Purchase Order actions
      description: >
        Requires `purchase_order.read` and access to the PO's Building. Uses
        the established `{ purchaseOrderId, state, availableActions }` shape.
        Actions are upper-case command tokens and are returned only when the
        caller also holds `purchase_order.manage` and the existing command
        preconditions pass. `ISSUE` reuses issue-readiness; `CANCEL` mirrors
        the current DRAFT-only cancel command. No lifecycle rule is added.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseOrderIdPath"
      responses:
        "200":
          description: Caller-specific available actions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PurchaseOrderAvailableActions"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /purchase-orders/{id}/cancel:
    post:
      operationId: cancelPurchaseOrder
      x-required-permission: purchase_order.manage
      x-building-scoped: true
      summary: Cancel a DRAFT Purchase Order
      description: >
        Requires `purchase_order.manage`. DRAFT → CANCELLED only; any other
        source state yields 400 `PURCHASE_ORDER_CANCEL_NOT_ALLOWED`. RFQ-derived
        DRAFT POs cannot be cancelled through this generic route and yield 409
        `PURCHASE_ORDER_RFQ_DERIVED_IMMUTABLE`; commercial changes must return
        to the RFQ/award process. Non-RFQ DRAFT cancellation retains its normal
        behavior.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseOrderIdPath"
      responses:
        "200":
          description: Purchase Order cancelled.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PurchaseOrder" }
        "400":
          description: >
            The Purchase Order is not DRAFT
            (`PURCHASE_ORDER_CANCEL_NOT_ALLOWED`) or request validation failed.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /purchase-orders/{id}/issue-readiness:
    get:
      operationId: getPurchaseOrderIssueReadiness
      x-required-permission: purchase_order.read
      x-building-scoped: true
      summary: Evaluate whether a Purchase Order may be issued
      description: >
        Requires `purchase_order.read`. Read-only projection — evaluating it
        mutates nothing.


        `poReadiness` reports the BE-17F verdict VERBATIM; this endpoint is not
        a second readiness authority. `availableActions` contains `ISSUE` only
        when `issuable` is true and the caller holds `purchase_order.manage`;
        a read-only caller receives an empty action list. Blockers are
        deterministic facts resolved from existing authorities and are never
        persisted on the PO.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseOrderIdPath"
      responses:
        "200":
          description: Issue-readiness projection.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PurchaseOrderIssueReadiness"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /purchase-orders/{id}/issue:
    post:
      operationId: issuePurchaseOrder
      x-required-permission: purchase_order.manage
      x-building-scoped: true
      summary: Issue a Purchase Order (DRAFT → ISSUED)
      description: >
        Requires `purchase_order.manage`. Commits the order to the Vendor and
        stamps `issuedAt` / `issuedByUserId`.


        Preconditions, all deterministic: the PO is DRAFT; its BE-17F readiness
        is READY; it has at least one line; request linkage, vendor and
        Client/Building scope are consistent. A non-DRAFT PO yields 409
        `PURCHASE_ORDER_NOT_ISSUABLE_STATE` (this is also how repeated issuance
        is rejected); an empty PO yields 409 `PURCHASE_ORDER_NO_LINES`; any
        other blocker set yields 409 `PURCHASE_ORDER_NOT_ISSUABLE` with the
        blocker codes in `error.details[].message`.


        Issuance is non-mutating on failure and freezes the PO's lines, since
        every line mutation requires a DRAFT parent.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseOrderIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/IssuePurchaseOrderRequest" }
      responses:
        "200":
          description: Purchase Order issued.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PurchaseOrder" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /purchase-orders/{id}/price-deviation:
    get:
      operationId: getPurchaseOrderPriceDeviation
      x-required-permission: purchase_order.read + price_catalog.read
      x-building-scoped: true
      summary: Advisory reference-price deviation projection for a PO (§13.3)
      description: >
        Conjunctive permission: requires BOTH `purchase_order.read` and
        `price_catalog.read`, plus access to the PO's Building — the whole
        payload is price-authority data, so a caller lacking either code
        receives 403, never a degraded variant. This is the only PO surface
        carrying reference-price fields; PO and PO-line reads never grow
        them, so no masked field can leak to a `purchase_order.read`-only
        caller.


        Advisory, COMPUTED, read-only, warn-only (§13.3/§13.4): for each line
        it resolves the CURRENT applicable reference price through the frozen
        §8 semantics (as-of = request time; precedence Vendor+Building →
        Vendor → Building → Client-wide with the PO's own Vendor as tier
        context; exact UOM/currency match) and reports the PO line's committed
        price against it: resolution outcome, matched authority id + price
        facts + tier provenance + effective window, PO unit price, unit/total
        variance (PO − reference, 2dp), variance percent and ABOVE/BELOW/EQUAL
        position. Explicit non-fabricated outcomes — `NO_REFERENCE_PRICE`,
        `UOM_INCOMPATIBLE`, `CURRENCY_INCOMPATIBLE`, `NOT_REQUESTED` (SERVICE
        lines) and the fail-closed `AMBIGUOUS` (audited) — carry null facts.
        Nothing is persisted: PO facts are immutable, catalog
        replace/correct afterwards rewrites nothing, and this advisory
        projection never gates creation, approval, acknowledgement or
        issuance, never selects winners and never settles.
      tags: [Purchase Order, Price Catalog]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseOrderIdPath"
      responses:
        "200":
          description: Advisory deviation projection.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PurchaseOrderPriceDeviation" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /purchase-orders/{id}/lines:
    post:
      operationId: addPurchaseOrderLine
      x-required-permission: purchase_order.manage
      x-building-scoped: true
      summary: Commit a Purchase Order line against an MR/SR line
      description: >
        Requires `purchase_order.manage`. The parent PO must be DRAFT (400
        `PURCHASE_ORDER_LINE_NOT_DRAFT` otherwise).


        Material Request / Service Request remain the SINGLE quantity
        authority: `quantitySnapshot` is a frozen READ of the originating
        line's approved (or requested) quantity, and `lineAmount` is derived
        from it. A caller may never supply a quantity, an amount, `itemId`,
        `uomId` or `lineNumber` — doing so yields 400 VALIDATION_ERROR. The PO
        creates no ordered/received/remaining ledger.


        A MATERIAL line requires an item and a positive quantity; a SERVICE
        line carries neither, and its unit price IS the line amount. Each
        request line may be committed at most once per PO. RFQ-derived DRAFT
        POs reject generic line additions with 409
        `PURCHASE_ORDER_RFQ_DERIVED_IMMUTABLE`; only the RFQ conversion path
        creates their lines.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseOrderIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AddPurchaseOrderLineRequest" }
      responses:
        "201":
          description: Purchase Order line committed.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PurchaseOrderLine" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listPurchaseOrderLines
      x-required-permission: purchase_order.read
      x-building-scoped: true
      summary: List the lines of a Purchase Order
      description: >
        Requires `purchase_order.read`. Ordered deterministically by
        `lineNumber`.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseOrderIdPath"
      responses:
        "200":
          description: Purchase Order lines.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/PurchaseOrderLine"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /purchase-order-lines/{lineId}:
    get:
      operationId: getPurchaseOrderLine
      x-required-permission: purchase_order.read
      x-building-scoped: true
      summary: Get a Purchase Order line
      description: Requires `purchase_order.read` and access to the line's Building.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseOrderLineIdPath"
      responses:
        "200":
          description: Purchase Order line.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PurchaseOrderLine" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updatePurchaseOrderLine
      x-required-permission: purchase_order.manage
      x-building-scoped: true
      summary: Update a Purchase Order line
      description: >
        Requires `purchase_order.manage`. The parent PO must be DRAFT.


        Only commercial detail changes: `lineAmount` is always RE-DERIVED from
        the frozen quantity snapshot, never accepted from the caller, so the
        quantity authority cannot be bypassed through the amount. RFQ-derived
        DRAFT lines are immutable here and return 409
        `PURCHASE_ORDER_RFQ_DERIVED_IMMUTABLE`; changes require a new RFQ award.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseOrderLineIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePurchaseOrderLineRequest"
      responses:
        "200":
          description: Purchase Order line updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PurchaseOrderLine" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      operationId: removePurchaseOrderLine
      x-required-permission: purchase_order.manage
      x-building-scoped: true
      summary: Remove a Purchase Order line
      description: >
        Requires `purchase_order.manage` and access to the line's Building. The
        parent PO must be DRAFT. The line is deleted, its originating request
        line becomes eligible for a future live commitment, and the response is
        a compact removal acknowledgement. RFQ-derived DRAFT lines cannot be
        removed here and return 409 `PURCHASE_ORDER_RFQ_DERIVED_IMMUTABLE`.
        The surviving removal audit is the operational event; the deleted line
        is not returned.
      tags: [Purchase Order]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseOrderLineIdPath"
      responses:
        "200":
          description: Purchase Order line removed.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/RemovePurchaseOrderLineResult"
        "400":
          description: >
            The parent Purchase Order is not DRAFT
            (`PURCHASE_ORDER_LINE_NOT_DRAFT`) or the line id is invalid
            (`VALIDATION_ERROR`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Purchase Order Line not found (`PURCHASE_ORDER_LINE_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /work-contracts:
    post:
      operationId: createWorkContract
      x-required-permission: work_contract.manage
      x-building-scoped: true
      summary: Raise a Work Contract (SPK) against an ISSUED Purchase Order
      description: >
        Requires `work_contract.manage`. Creates the SPK as DRAFT.


        `purchaseOrderId` is the ONLY context input: Client, Building and
        Vendor are INHERITED from that Purchase Order and can never be supplied
        or overridden (400 VALIDATION_ERROR). The PO must be ISSUED — a DRAFT
        commitment is not yet a mandate to execute and a CANCELLED one never
        will be (409 `WORK_CONTRACT_PURCHASE_ORDER_NOT_ISSUED`).


        At most one live (DRAFT or ACTIVE) SPK may occupy a Purchase Order (409
        `WORK_CONTRACT_ALREADY_EXISTS_FOR_PO`); completing or cancelling
        releases the PO for a follow-up mandate. The SPK number is unique
        within a Client.
      tags: [Work Contract (SPK)]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateWorkContractRequest" }
      responses:
        "201":
          description: Work Contract created as DRAFT.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkContract" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listWorkContracts
      x-required-permission: work_contract.read
      x-building-scoped: true
      summary: List Work Contracts (SPK)
      description: >
        Requires `work_contract.read`. Scoped to the caller's accessible
        Buildings.
      tags: [Work Contract (SPK)]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: purchaseOrderId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/WorkContractStatus" }
        - name: spkDateFrom
          in: query
          schema: { type: string, format: date }
        - name: spkDateTo
          in: query
          schema: { type: string, format: date }
      responses:
        "200":
          description: List of Work Contracts.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/WorkContract" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /work-contracts/{id}:
    get:
      operationId: getWorkContract
      x-required-permission: work_contract.read
      x-building-scoped: true
      summary: Get a Work Contract (SPK)
      description: Requires `work_contract.read` and access to the SPK's Building.
      tags: [Work Contract (SPK)]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkContractIdPath"
      responses:
        "200":
          description: Work Contract.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkContract" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateWorkContract
      x-required-permission: work_contract.manage
      x-building-scoped: true
      summary: Update a DRAFT Work Contract (SPK)
      description: >
        Requires `work_contract.manage`. DRAFT only — once ACTIVE the mandate
        is in force and immutable (400 `WORK_CONTRACT_NOT_DRAFT`).


        Identity (`spkNumber`), the commitment reference (`purchaseOrderId`),
        the inherited scope (`clientId`, `buildingId`, `vendorId`) and all
        lifecycle provenance are immutable.
      tags: [Work Contract (SPK)]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkContractIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateWorkContractRequest" }
      responses:
        "200":
          description: Work Contract updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkContract" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-contracts/{id}/available-actions:
    get:
      operationId: getWorkContractAvailableActions
      x-required-permission: work_contract.read
      x-building-scoped: true
      summary: Resolve the caller's available Work Contract actions
      description: >
        Requires `work_contract.read` and access to the SPK's Building. Uses
        the established `{ workContractId, state, availableActions }` shape.
        Actions are upper-case command tokens and are returned only when the
        caller also holds `work_contract.manage` and the existing transition
        preconditions pass. DRAFT can expose `ACTIVATE` and `CANCEL`; ACTIVE
        can expose `COMPLETE` and `CANCEL`; terminal states expose none.
        Vendor usability is rechecked before advertising `ACTIVATE`.
      tags: [Work Contract (SPK)]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkContractIdPath"
      responses:
        "200":
          description: Caller-specific available actions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/WorkContractAvailableActions"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-contracts/{id}/activate:
    post:
      operationId: activateWorkContract
      x-required-permission: work_contract.manage
      x-building-scoped: true
      summary: Activate a Work Contract (DRAFT → ACTIVE)
      description: >
        Requires `work_contract.manage`. Puts the mandate in force, after which
        it becomes immutable.


        The inherited Vendor is re-validated at this moment (it must still be
        ACTIVE, belong to the Client and hold an ACTIVE relationship to the
        Building) — 400 `WORK_CONTRACT_VENDOR_INVALID` otherwise. An invalid or
        repeated transition yields 409 `WORK_CONTRACT_TRANSITION_INVALID`.
      tags: [Work Contract (SPK)]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkContractIdPath"
      responses:
        "200":
          description: Work Contract activated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkContract" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /work-contracts/{id}/complete:
    post:
      operationId: completeWorkContract
      x-required-permission: work_contract.manage
      x-building-scoped: true
      summary: Complete a Work Contract (ACTIVE → COMPLETED)
      description: >
        Requires `work_contract.manage`. Terminal state, reachable ONLY from
        ACTIVE — a DRAFT SPK cannot be completed without having been authorized
        (409 `WORK_CONTRACT_TRANSITION_INVALID`). Completion releases the
        Purchase Order for a follow-up mandate and preserves the activation
        stamp.


        Unlike activation, this wind-down transition does NOT re-validate the
        Vendor, so a mandate can never be stranded by a vendor deactivation.
      tags: [Work Contract (SPK)]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkContractIdPath"
      responses:
        "200":
          description: Work Contract completed.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkContract" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /work-contracts/{id}/cancel:
    post:
      operationId: cancelWorkContract
      x-required-permission: work_contract.manage
      x-building-scoped: true
      summary: Cancel a Work Contract (DRAFT | ACTIVE → CANCELLED)
      description: >
        Requires `work_contract.manage`. Terminal state, reachable from DRAFT
        or ACTIVE; a COMPLETED or already CANCELLED SPK yields 409
        `WORK_CONTRACT_TRANSITION_INVALID`. Cancelling releases the Purchase
        Order and preserves whatever lifecycle stamps the SPK had reached.
      tags: [Work Contract (SPK)]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkContractIdPath"
      responses:
        "200":
          description: Work Contract cancelled.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkContract" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /work-order-procurement-bindings:
    post:
      operationId: createWorkOrderProcurementBinding
      x-required-permission: wo_procurement.manage
      x-building-scoped: true
      summary: Bind a Work Order to its procurement context
      description: >
        Requires `wo_procurement.manage`. Binds a Work Order to its Purchase
        Request and, optionally, to a specific Material or Service Request.
        The Work Order and every referenced record must share one Client and
        Building (400 `WO_PROCUREMENT_BUILDING_MISMATCH`).


        Exactly ONE procurement binding may exist per Work Order (409
        `WO_PROCUREMENT_ALREADY_BOUND`).


        CR-BE-R2P-01 PART 05 — an optional `workContractId` binds the ACTIVE
        SPK chain in the same call. When supplied, the Purchase Order and
        Vendor are DERIVED from that SPK; supplying `purchaseOrderId`,
        `vendorId`, `clientId` or `buildingId` is rejected as an override
        attempt. The same chain rules as
        `POST /work-order-procurement-bindings/{id}/bind-work-contract` apply.
      tags: [Work Order Procurement Binding]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWorkOrderProcurementBindingRequest"
      responses:
        "201":
          description: Binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/WorkOrderProcurementBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /work-order-procurement-bindings/{id}:
    get:
      operationId: getWorkOrderProcurementBinding
      x-required-permission: wo_procurement.read
      x-building-scoped: true
      summary: Get a Work Order procurement binding
      description: >
        Requires `wo_procurement.read`. Returns the binding with its resolved
        Work Order, Purchase Request, Material/Service Request, Receiving and —
        when the chain is bound — Work Contract and Purchase Order summaries.
      tags: [Work Order Procurement Binding]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkOrderProcurementBindingIdPath"
      responses:
        "200":
          description: Binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/WorkOrderProcurementBinding"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-order-procurement-bindings/{id}/resolve-readiness:
    post:
      operationId: resolveWorkOrderProcurementReadiness
      x-required-permission: wo_procurement.manage
      x-building-scoped: true
      summary: Resolve the procurement readiness of a binding
      description: >
        Requires `wo_procurement.manage`. Backend-authoritative: a READY BE-17F
        PO Readiness for the binding's request promotes `procurementStatus`
        from BOUND to READY. This reads the readiness authority — it does not
        re-evaluate readiness.
      tags: [Work Order Procurement Binding]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkOrderProcurementBindingIdPath"
      responses:
        "200":
          description: Readiness resolved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/WorkOrderProcurementBinding"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-order-procurement-bindings/{id}/link-receiving:
    post:
      operationId: linkReceivingToWorkOrderProcurementBinding
      x-required-permission: wo_procurement.manage
      x-building-scoped: true
      summary: Link a Receiving to a binding
      description: >
        Requires `wo_procurement.manage`. The Receiving must belong to the same
        Client and Building and to the binding's own request (400
        `WO_PROCUREMENT_RECEIVING_MISMATCH`). Linking promotes
        `procurementStatus` to RECEIVED. Receiving quantity, inventory and
        over-receipt rules are owned by BE-17G and are not affected.
      tags: [Work Order Procurement Binding]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkOrderProcurementBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/LinkReceivingRequest" }
      responses:
        "200":
          description: Receiving linked.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/WorkOrderProcurementBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-order-procurement-bindings/{id}/bind-work-contract:
    post:
      operationId: bindWorkContractToProcurementBinding
      x-required-permission: wo_procurement.manage
      x-building-scoped: true
      summary: Bind the ACTIVE SPK chain to a Work Order's binding
      description: >
        Requires `wo_procurement.manage`. Completes the authoritative execution
        chain — Request → Vendor → ISSUED PO → ACTIVE SPK → Work Order — by
        EXTENDING the existing binding row. No parallel SPK ↔ Work Order
        binding domain exists, and the one-binding-per-Work-Order guarantee is
        unchanged.


        `workContractId` is the ONLY input: the Purchase Order and Vendor are
        DERIVED from the SPK. Supplying `purchaseOrderId`, `vendorId`,
        `clientId` or `buildingId` yields 400 VALIDATION_ERROR.


        Enforced: the SPK must be ACTIVE (409
        `WO_PROCUREMENT_WORK_CONTRACT_NOT_ACTIVE` for DRAFT / COMPLETED /
        CANCELLED); its Purchase Order must still be ISSUED (409
        `WO_PROCUREMENT_PURCHASE_ORDER_NOT_ISSUED`); the Work Order's
        Client/Building must match the SPK (400
        `WO_PROCUREMENT_BUILDING_MISMATCH`); and the Vendor holding the ACTIVE
        BE-15A assignment on that Work Order must be the SPK's Vendor (400
        `WO_PROCUREMENT_VENDOR_MISMATCH`, also returned when no vendor is
        assigned). An already-chained binding is never silently overwritten
        (409 `WO_PROCUREMENT_WORK_CONTRACT_ALREADY_BOUND`).
      tags: [Work Order Procurement Binding]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkOrderProcurementBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BindWorkContractRequest" }
      responses:
        "200":
          description: SPK chain bound to the Work Order's procurement binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/WorkOrderProcurementBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /work-orders/{workOrderId}/procurement-bindings:
    get:
      operationId: listWorkOrderProcurementBindings
      x-required-permission: wo_procurement.read
      x-building-scoped: true
      summary: List the procurement context bound to a Work Order
      description: >
        Requires `wo_procurement.read` and access to the Work Order's Building.
        At most one binding exists per Work Order.
      tags: [Work Order Procurement Binding]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: workOrderId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Bindings for the Work Order.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/WorkOrderProcurementBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  # ---------------------------------------------------------------------------
  # CR-BE-MAT-01 — Material fulfilment chain
  # Material Request → MR Line → Receiving → STOCK_IN → Movement/Balance →
  # STOCK_OUT → Work Order Material Usage → operational material cost.
  # ---------------------------------------------------------------------------

  /purchase-requests/{purchaseRequestId}/material-requests:
    post:
      operationId: createMaterialRequest
      summary: Create a Material Request line on a Purchase Request
      description: >
        Requires `material_request.manage` and access to the Purchase
        Request's Building. `clientId`/`buildingId` derive from the Purchase
        Request; `uomId` is captured from the Item Master (a supplied `uomId`
        must match the item UOM exactly — no conversion).
      tags: [Material Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseRequestIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MaterialRequestCreateRequest" }
      responses:
        "201":
          description: Material Request line created (status OPEN).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/MaterialRequest" }
        "400":
          description: >
            Validation failed, or a deterministic domain rejection:
            `MATERIAL_REQUEST_INVALID_QUANTITY`,
            `MATERIAL_REQUEST_PURCHASE_REQUEST_NOT_OPEN`,
            `MATERIAL_REQUEST_ITEM_CLIENT_MISMATCH`,
            `MATERIAL_REQUEST_WAREHOUSE_CLIENT_MISMATCH`,
            `MATERIAL_REQUEST_WAREHOUSE_BUILDING_MISMATCH`,
            `MATERIAL_REQUEST_ITEM_UOM_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    get:
      operationId: listMaterialRequestsByPurchaseRequest
      summary: List Material Request lines of a Purchase Request
      description: Requires `material_request.read` and Building access.
      tags: [Material Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseRequestIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/MaterialRequestStatus" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Material Request lines.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/MaterialRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/material-requests:
    get:
      operationId: listMaterialRequestsByBuilding
      summary: List Material Requests of a Building
      description: >
        Requires `material_request.read`; Building access enforced
        (Client/Building isolation). Optional `status` filter.
      tags: [Material Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/MaterialRequestStatus" }
      responses:
        "200":
          description: Material Request lines.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/MaterialRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /items/{itemId}/material-requests:
    get:
      operationId: listMaterialRequestsByItem
      summary: List Material Requests referencing an inventory item
      description: Requires `material_request.read`. Optional `status` filter.
      tags: [Material Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/InventoryItemIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/MaterialRequestStatus" }
      responses:
        "200":
          description: Material Request lines.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/MaterialRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /material-requests/{id}:
    get:
      operationId: getMaterialRequest
      summary: Get a Material Request line (with fulfilment quantities)
      description: >
        Requires `material_request.read` and Building access. The detail read
        additionally derives `receivedQuantity` (cumulative valid receipts)
        and `remainingQuantity` = (`approvedQuantity` ?? `quantity`) −
        `receivedQuantity`. No separate remaining-quantity store exists.
      tags: [Material Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/MaterialRequestIdPath"
      responses:
        "200":
          description: Material Request line.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/MaterialRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateMaterialRequest
      summary: Update an OPEN Material Request line
      description: >
        Requires `material_request.manage` and Building access. Only OPEN
        lines are editable — the PART 02 freeze rejects updates to APPROVED
        or CANCELLED lines with `MATERIAL_REQUEST_NOT_OPEN` (item identity,
        requested/approved quantity, UOM and warehouse scope are
        fulfilment-critical and cannot be silently changed after approval).
      tags: [Material Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/MaterialRequestIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MaterialRequestUpdateRequest" }
      responses:
        "200":
          description: Updated Material Request line.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/MaterialRequest" }
        "400":
          description: >
            Validation failed, or `MATERIAL_REQUEST_NOT_OPEN` (frozen line),
            `MATERIAL_REQUEST_INVALID_QUANTITY`,
            `MATERIAL_REQUEST_ITEM_UOM_MISMATCH`,
            `MATERIAL_REQUEST_WAREHOUSE_CLIENT_MISMATCH`,
            `MATERIAL_REQUEST_WAREHOUSE_BUILDING_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              examples:
                frozen:
                  value:
                    success: false
                    error:
                      code: MATERIAL_REQUEST_NOT_OPEN
                      message: Material Request is not OPEN.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /material-requests/{id}/cancel:
    post:
      operationId: cancelMaterialRequest
      summary: Cancel a Material Request line (OPEN or APPROVED)
      description: >
        Requires `material_request.manage` and Building access. Cancellation
        is the correction path after approval (PART 02); an already-cancelled
        line is rejected with `MATERIAL_REQUEST_NOT_OPEN`. Not a delete —
        history is preserved.
      tags: [Material Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/MaterialRequestIdPath"
      responses:
        "200":
          description: Cancelled Material Request line.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/MaterialRequest" }
        "400":
          description: "`MATERIAL_REQUEST_NOT_OPEN` — already cancelled."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "`MATERIAL_REQUEST_ACTIVE_RESERVATION` — cancellation is blocked while an ACTIVE reservation remains."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /material-requests/{materialRequestId}/reservations:
    post:
      operationId: createMaterialReservation
      summary: Reserve stock for an approved Material Request
      description: >
        Requires `inventory_stock.manage` and access to the Material Request's
        Building. The Material Request must be APPROVED. The server derives the
        item, Client, Building and UOM from the existing authorities; the
        selected warehouse must be in scope and have sufficient available stock.
        Demand is calculated under a Material Request lock as
        `approvedQuantity ?? quantity - cumulativeIssued - activeReserved`.
        The reservation and existing balance reserved-quantity update commit
        atomically. No stock movement is created because on-hand does not
        change.
      tags: [Material Reservations]
      security: [{ bearerAuth: [] }]
      x-required-permission: inventory_stock.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MaterialReservationSourceIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MaterialReservationCreateRequest" }
      responses:
        "201":
          description: Active reservation created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/MaterialReservation" }
        "400":
          description: >
            Validation failed (`INVENTORY_MATERIAL_RESERVATION_INVALID_QUANTITY`),
            or one of `INVENTORY_MATERIAL_RESERVATION_CLIENT_MISMATCH`,
            `INVENTORY_MATERIAL_RESERVATION_BUILDING_MISMATCH`,
            `INVENTORY_MATERIAL_RESERVATION_ITEM_MISMATCH`,
            `INVENTORY_MATERIAL_RESERVATION_WAREHOUSE_MISMATCH`, or
            `INVENTORY_MATERIAL_RESERVATION_UOM_INCOMPATIBLE` applies.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: >
            `INVENTORY_MATERIAL_RESERVATION_DEMAND_NOT_APPROVED`,
            `INVENTORY_MATERIAL_RESERVATION_DEMAND_EXCEEDED`, or
            `INVENTORY_MATERIAL_RESERVATION_INSUFFICIENT_STOCK`. Nothing is
            committed.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listMaterialReservations
      summary: List reservations of a Material Request
      description: >
        Requires `inventory_stock.read` and Building access. Results are scoped
        to the Material Request and may be filtered by reservation `status`.
      tags: [Material Reservations]
      security: [{ bearerAuth: [] }]
      x-required-permission: inventory_stock.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MaterialReservationSourceIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/MaterialReservationStatus" }
      responses:
        "200":
          description: Material reservations.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/MaterialReservation" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /material-reservations/{id}:
    get:
      operationId: getMaterialReservation
      summary: Get a material reservation
      description: >
        Requires `inventory_stock.read` and Building access.
        `INVENTORY_MATERIAL_RESERVATION_NOT_FOUND` is returned when the
        reservation does not exist.
      tags: [Material Reservations]
      security: [{ bearerAuth: [] }]
      x-required-permission: inventory_stock.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MaterialReservationIdPath"
      responses:
        "200":
          description: Material reservation.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/MaterialReservation" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /material-reservations/{id}/release:
    post:
      operationId: releaseMaterialReservation
      summary: Release an active material reservation
      description: >
        Requires `inventory_stock.manage` and Building access. Only ACTIVE
        reservations can be released. The existing stock balance's reserved
        quantity is reduced exactly once; on-hand is unchanged.
      tags: [Material Reservations]
      security: [{ bearerAuth: [] }]
      x-required-permission: inventory_stock.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MaterialReservationIdPath"
      responses:
        "200":
          description: Reservation released.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/MaterialReservation" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "`INVENTORY_MATERIAL_RESERVATION_NOT_ACTIVE` — reservation is already released, cancelled, or consumed."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /material-reservations/{id}/cancel:
    post:
      operationId: cancelMaterialReservation
      summary: Cancel an active material reservation
      description: >
        Requires `inventory_stock.manage` and Building access. Only ACTIVE
        reservations can be cancelled. The existing stock balance's reserved
        quantity is reduced exactly once; on-hand is unchanged.
      tags: [Material Reservations]
      security: [{ bearerAuth: [] }]
      x-required-permission: inventory_stock.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MaterialReservationIdPath"
      responses:
        "200":
          description: Reservation cancelled.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/MaterialReservation" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "`INVENTORY_MATERIAL_RESERVATION_NOT_ACTIVE` — reservation is already released, cancelled, or consumed."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /procurement-approvals:
    post:
      operationId: createProcurementApproval
      summary: Create a procurement approval binding
      description: >
        Requires `procurement_approval.manage` and Building access. Binds one
        PENDING approval decision to a PURCHASE_REQUEST, MATERIAL_REQUEST or
        SERVICE_REQUEST that is still OPEN. For RFQ awards, the target is a
        CLOSED RFQ and the request must include `requestType: RFQ`,
        `approvalType: RFQ_AWARD`, and the exact `recommendationId`. The
        approver must hold `procurement_approval.manage` in the target
        Building.
      tags: [Procurement Approvals]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ProcurementApprovalCreateRequest" }
      responses:
        "201":
          description: PENDING approval binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ProcurementApproval" }
        "400":
          description: >
            Validation failed, or `PROCUREMENT_APPROVAL_REQUEST_INVALID`,
            `PROCUREMENT_APPROVAL_APPROVER_INVALID`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409":
          description: "`PROCUREMENT_APPROVAL_ALREADY_PENDING` — duplicate pending binding."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /procurement-approvals/pending:
    get:
      operationId: listPendingProcurementApprovals
      summary: List pending procurement approvals in the accessible scope
      description: >
        Requires `procurement_approval.read`. Results are limited to the
        caller's accessible Buildings. Filters: `buildingId`,
        `approverUserId`, `requestType`, `approvalType`.
      tags: [Procurement Approvals]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: approverUserId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: requestType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ProcurementApprovalRequestType" }
        - name: approvalType
          in: query
          required: false
          schema: { type: string }
      responses:
        "200":
          description: Pending approval bindings.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/ProcurementApproval" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /procurement-approvals/{id}:
    get:
      operationId: getProcurementApproval
      summary: Get a procurement approval binding
      description: Requires `procurement_approval.read` and Building access.
      tags: [Procurement Approvals]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ProcurementApprovalIdPath"
      responses:
        "200":
          description: Approval binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ProcurementApproval" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /procurement-approvals/{id}/available-actions:
    get:
      operationId: getProcurementApprovalAvailableActions
      summary: Resolve the caller's available approval actions
      description: >
        Requires `procurement_approval.read` and Building access. Only the
        assigned approver of a PENDING binding whose target request is still
        OPEN receives `APPROVE` / `REJECT`.
      tags: [Procurement Approvals]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ProcurementApprovalIdPath"
      responses:
        "200":
          description: Available actions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ProcurementApprovalAvailableActions" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /procurement-approvals/{id}/approve:
    post:
      operationId: approveProcurementApproval
      summary: Approve (optionally with an explicit approved quantity)
      description: >
        Requires `procurement_approval.manage`; only the assigned approver may
        decide. CR-BE-MAT-01 PART 02 — the decision and the Material Request
        promotion commit atomically: approving a MATERIAL_REQUEST binding sets
        the line's authoritative `approvedQuantity` (explicit value, or
        default approved = requested) and freezes the line; approving a
        PURCHASE_REQUEST binding defaults approved = requested on every
        still-OPEN line (already APPROVED lines keep their approved quantity).
        `approvedQuantity` is only applicable when APPROVING a
        MATERIAL_REQUEST binding, must be > 0 and must not exceed the
        requested line quantity.
      tags: [Procurement Approvals]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ProcurementApprovalIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ProcurementApprovalDecisionRequest" }
      responses:
        "200":
          description: Approval decided (APPROVED).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ProcurementApproval" }
        "400":
          description: >
            Validation failed, or a deterministic approved-quantity rejection:
            `MATERIAL_REQUEST_APPROVED_QUANTITY_INVALID`,
            `MATERIAL_REQUEST_APPROVED_QUANTITY_EXCEEDS_REQUESTED`,
            `PROCUREMENT_APPROVAL_APPROVED_QUANTITY_NOT_APPLICABLE`. The
            binding stays PENDING and no Material Request is mutated.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              examples:
                exceedsRequested:
                  value:
                    success: false
                    error:
                      code: MATERIAL_REQUEST_APPROVED_QUANTITY_EXCEEDS_REQUESTED
                      message: Approved quantity must not exceed the requested Material Request quantity.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            Missing permission, `PROCUREMENT_APPROVAL_UNAUTHORIZED_APPROVER`,
            `PROCUREMENT_APPROVAL_ACTION_NOT_ALLOWED`, or Building access
            denied.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "`PROCUREMENT_APPROVAL_ALREADY_DECIDED` — decisions are append-only."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /procurement-approvals/{id}/reject:
    post:
      operationId: rejectProcurementApproval
      summary: Reject a pending procurement approval
      description: >
        Requires `procurement_approval.manage`; only the assigned approver may
        decide. Rejection never mutates the target request;
        `approvedQuantity` is not applicable
        (`PROCUREMENT_APPROVAL_APPROVED_QUANTITY_NOT_APPLICABLE`).
      tags: [Procurement Approvals]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ProcurementApprovalIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ProcurementApprovalDecisionRequest" }
      responses:
        "200":
          description: Approval decided (REJECTED).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ProcurementApproval" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "`PROCUREMENT_APPROVAL_ALREADY_DECIDED`."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /receivings:
    post:
      operationId: createReceiving
      summary: Record a receiving (material posts STOCK_IN through the ledger)
      description: >
        Requires `receiving.manage` and access to the request's Building. The
        request + vendor must have a READY purchase-order readiness. MATERIAL
        receiving validates item/warehouse Client-Building scope and, when
        `materialRequestId` is supplied (CR-BE-MAT-01 PART 01), binds the
        receiving to that Material Request line: same Purchase Request,
        Client/Building scope, item identity and target warehouse are
        enforced, the UOM snapshot is taken from the line (PART 04, no
        conversion), and the cumulative over-receipt guard caps
        received + new ≤ (approvedQuantity ?? quantity) under a line lock
        (PART 02). Line guard, STOCK_IN movement and receiving row commit in
        ONE transaction — a rejected receiving mutates nothing. SERVICE
        receiving records acceptance without stock movement.
      tags: [Receivings]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ReceivingCreateRequest" }
      responses:
        "201":
          description: Receiving recorded (status RECEIVED).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Receiving" }
        "400":
          description: >
            Validation failed, or a deterministic domain rejection:
            `RECEIVING_REQUEST_INVALID`, `RECEIVING_VENDOR_INVALID`,
            `RECEIVING_READINESS_INVALID`, `RECEIVING_INVALID_QUANTITY`,
            `RECEIVING_ITEM_CLIENT_MISMATCH`,
            `RECEIVING_WAREHOUSE_CLIENT_MISMATCH`,
            `RECEIVING_WAREHOUSE_BUILDING_MISMATCH`,
            `RECEIVING_MATERIAL_REQUEST_INVALID` (unknown/cancelled line or
            different Purchase Request),
            `RECEIVING_MATERIAL_REQUEST_ITEM_MISMATCH`,
            `RECEIVING_MATERIAL_REQUEST_SCOPE_MISMATCH` (cross Client/Building
            or different target warehouse), `RECEIVING_UOM_INCOMPATIBLE`
            (PART 04 — differing unit without a conversion authority).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              examples:
                uomIncompatible:
                  value:
                    success: false
                    error:
                      code: RECEIVING_UOM_INCOMPATIBLE
                      message: The receiving UOM is incompatible with the Material Request line / item UOM and no conversion authority exists.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409":
          description: >
            `RECEIVING_OVER_RECEIPT` — cumulative received quantity would
            exceed the Material Request line's approved (or requested)
            quantity. No STOCK_IN, balance change or receiving row is
            created.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              examples:
                overReceipt:
                  value:
                    success: false
                    error:
                      code: RECEIVING_OVER_RECEIPT
                      message: Cumulative received quantity would exceed the Material Request line quantity.

  /receivings/{id}:
    get:
      operationId: getReceiving
      summary: Get a receiving record
      description: Requires `receiving.read` and Building access.
      tags: [Receivings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ReceivingIdPath"
      responses:
        "200":
          description: Receiving record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Receiving" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateReceiving
      summary: Update notes on a RECEIVED record
      description: >
        Requires `receiving.manage` and Building access. Only `notes` is
        mutable; a FINALIZED record is immutable
        (`RECEIVING_ALREADY_FINALIZED`).
      tags: [Receivings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ReceivingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ReceivingUpdateRequest" }
      responses:
        "200":
          description: Updated receiving record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Receiving" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "`RECEIVING_ALREADY_FINALIZED`."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /receivings/{id}/finalize:
    post:
      operationId: finalizeReceiving
      summary: Finalize a receiving (RECEIVED → FINALIZED)
      description: >
        Requires `receiving.manage` and Building access. Idempotency guard: an
        already-FINALIZED record cannot be finalized again
        (`RECEIVING_ALREADY_FINALIZED`). FINALIZED receipts still count toward
        the PART 01 cumulative over-receipt guard.
      tags: [Receivings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ReceivingIdPath"
      responses:
        "200":
          description: Finalized receiving record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Receiving" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "`RECEIVING_ALREADY_FINALIZED`."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /buildings/{buildingId}/receivings:
    get:
      operationId: listReceivingsByBuilding
      summary: List receivings of a Building
      description: >
        Requires `receiving.read` and Building access. Filters: `status`,
        `receivingType`.
      tags: [Receivings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReceivingStatus" }
        - name: receivingType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReceivingType" }
      responses:
        "200":
          description: Receiving records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Receiving" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /purchase-requests/{purchaseRequestId}/receivings:
    get:
      operationId: listReceivingsByPurchaseRequest
      summary: List receivings of a Purchase Request
      description: >
        Requires `receiving.read` and access to the request's Building.
        Filters: `status`, `receivingType`.
      tags: [Receivings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PurchaseRequestIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReceivingStatus" }
        - name: receivingType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReceivingType" }
      responses:
        "200":
          description: Receiving records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Receiving" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /service-requests/{serviceRequestId}/receivings:
    get:
      operationId: listReceivingsByServiceRequest
      summary: List receivings of a Service Request
      description: >
        Requires `receiving.read` and access to the request's Building.
        Filters: `status`, `receivingType`.
      tags: [Receivings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ServiceRequestIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReceivingStatus" }
        - name: receivingType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReceivingType" }
      responses:
        "200":
          description: Receiving records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Receiving" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /vendors/{vendorId}/receivings:
    get:
      operationId: listReceivingsByVendor
      summary: List receivings of a Vendor within the accessible scope
      description: >
        Requires `receiving.read`. Results are limited to the caller's
        accessible Buildings. Filters: `status`, `receivingType`.
      tags: [Receivings]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VendorIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReceivingStatus" }
        - name: receivingType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReceivingType" }
      responses:
        "200":
          description: Receiving records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Receiving" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /warehouses/{warehouseId}/stock-movements:
    post:
      operationId: postWarehouseStockMovement
      summary: Post a STOCK_IN / STOCK_OUT movement (ledger authority)
      description: >
        Requires `inventory_stock.manage` and access to the warehouse's
        Building. The movement and its balance update commit atomically under
        a `FOR UPDATE` lock (CR-BE-MAT-01 PART 03 — a balance never changes
        without a movement; a failed movement changes nothing). The item UOM
        is snapshotted on the movement (PART 04). STOCK_OUT requires
        sufficient available stock; negative resulting stock is impossible.
      tags: [Inventory Stock]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WarehouseIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StockMovementCreateRequest" }
      responses:
        "201":
          description: Movement posted; balance updated atomically.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/StockMovement" }
        "400":
          description: >
            Validation failed, or `INVENTORY_STOCK_MOVEMENT_INVALID_QUANTITY`,
            `INVENTORY_STOCK_MOVEMENT_CLIENT_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: >
            `INVENTORY_STOCK_MOVEMENT_INSUFFICIENT_STOCK` — STOCK_OUT above
            available stock, or `INVENTORY_STOCK_MOVEMENT_WORK_ORDER_BYPASS`
            when the exact Work Order material source convention is supplied.
            Nothing is committed.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listWarehouseStockMovements
      summary: List movements of a warehouse
      description: >
        Requires `inventory_stock.read` and Building access. Filters:
        `itemId`, `movementType`, `dateFrom`, `dateTo`, `reference`.
      tags: [Inventory Stock]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WarehouseIdPath"
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: movementType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/StockMovementType" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string, format: date-time }
        - name: dateTo
          in: query
          required: false
          schema: { type: string, format: date-time }
        - name: reference
          in: query
          required: false
          schema: { type: string }
      responses:
        "200":
          description: Append-only movement ledger entries (newest first).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/StockMovement" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /clients/{clientId}/stock-movements:
    post:
      operationId: postClientStockMovement
      summary: Post a movement (warehouse supplied in the body)
      description: >
        Same contract as the warehouse-scoped POST; `warehouseId` is required
        in the body. Requires `inventory_stock.manage` and access to the
        warehouse's Building.
      tags: [Inventory Stock]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StockMovementCreateRequest" }
      responses:
        "201":
          description: Movement posted; balance updated atomically.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/StockMovement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "`INVENTORY_STOCK_MOVEMENT_INSUFFICIENT_STOCK`."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listClientStockMovements
      summary: List movements in a Client scope
      description: >
        Requires `inventory_stock.read` and Client access. Filters:
        `buildingId`, `warehouseId`, `itemId`, `movementType`, `dateFrom`,
        `dateTo`, `reference`.
      tags: [Inventory Stock]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: movementType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/StockMovementType" }
      responses:
        "200":
          description: Movement ledger entries.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/StockMovement" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /buildings/{buildingId}/stock-movements:
    get:
      operationId: listBuildingStockMovements
      summary: List movements of a Building
      description: >
        Requires `inventory_stock.read` and Building access. Filters:
        `warehouseId`, `itemId`, `movementType`, `dateFrom`, `dateTo`.
      tags: [Inventory Stock]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: movementType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/StockMovementType" }
      responses:
        "200":
          description: Movement ledger entries.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/StockMovement" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /stock-movements/{id}:
    get:
      operationId: getStockMovement
      summary: Get a single ledger movement
      description: >
        Requires `inventory_stock.read` and Building access. Movements are
        immutable; historical rows without a UOM snapshot return
        `uomId: null` (fall back to the item's current UOM).
      tags: [Inventory Stock]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/StockMovementIdPath"
      responses:
        "200":
          description: Movement ledger entry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/StockMovement" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /warehouses/{warehouseId}/stock-balances:
    post:
      operationId: initializeWarehouseStockBalance
      summary: Initialize a stock balance (non-zero seeds enter the ledger)
      description: >
        Requires `inventory_stock.manage` and Building access. One balance per
        warehouse + item. Zero-quantity initialization stays movement-free;
        a NON-ZERO initialization writes a `BALANCE_INITIALIZATION` STOCK_IN
        movement atomically with the balance (CR-BE-MAT-01 PART 03 — no
        silent seed outside the ledger; an unattributable non-zero seed is
        rejected with `INVENTORY_STOCK_BALANCE_INIT_ACTOR_REQUIRED`).
      tags: [Inventory Stock]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WarehouseIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StockBalanceInitializeRequest" }
      responses:
        "201":
          description: Balance initialized.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/StockBalance" }
        "400":
          description: >
            Validation failed, or `INVENTORY_STOCK_BALANCE_NEGATIVE_QUANTITY`,
            `INVENTORY_STOCK_BALANCE_RESERVED_EXCEEDS_ON_HAND`,
            `INVENTORY_STOCK_BALANCE_CLIENT_MISMATCH`,
            `INVENTORY_STOCK_BALANCE_INIT_ACTOR_REQUIRED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "`INVENTORY_STOCK_BALANCE_ALREADY_EXISTS` — one balance per warehouse + item."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listWarehouseStockBalances
      summary: List balances of a warehouse
      description: >
        Requires `inventory_stock.read` and Building access. Filter: `itemId`.
        Balances are the derived current stock state — there is no direct
        balance mutation endpoint; stock changes only through movements.
      tags: [Inventory Stock]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WarehouseIdPath"
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Stock balances.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/StockBalance" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /clients/{clientId}/stock-balances:
    post:
      operationId: initializeClientStockBalance
      summary: Initialize a stock balance (warehouse supplied in the body)
      description: >
        Same contract as the warehouse-scoped POST; `warehouseId` is required
        in the body.
      tags: [Inventory Stock]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StockBalanceInitializeRequest" }
      responses:
        "201":
          description: Balance initialized.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/StockBalance" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: "`INVENTORY_STOCK_BALANCE_ALREADY_EXISTS`."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listClientStockBalances
      summary: List balances in a Client scope
      description: >
        Requires `inventory_stock.read` and Client access. Filters:
        `buildingId`, `warehouseId`, `itemId`.
      tags: [Inventory Stock]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Stock balances.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/StockBalance" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /buildings/{buildingId}/stock-balances:
    get:
      operationId: listBuildingStockBalances
      summary: List balances of a Building
      description: >
        Requires `inventory_stock.read` and Building access. Filters:
        `warehouseId`, `itemId`, `clientId`.
      tags: [Inventory Stock]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Stock balances.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/StockBalance" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /stock-balances/{id}:
    get:
      operationId: getStockBalance
      summary: Get a single stock balance
      description: Requires `inventory_stock.read` and Building access.
      tags: [Inventory Stock]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/StockBalanceIdPath"
      responses:
        "200":
          description: Stock balance.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/StockBalance" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/material-usages:
    post:
      operationId: recordWorkOrderMaterialUsage
      summary: Issue material to a Work Order (controlled issue flow)
      description: >
        Requires `inventory_stock.manage` and access to the Work Order's
        Building. CR-BE-INV-CONTROL-01 PART 02 — the Work Order must have an
        existing procurement binding to an APPROVED Material Request whose item,
        UOM, Client, and Building match the issue. The issue quantity is capped
        by `approved_quantity ?? quantity - cumulative controlled issues`;
        receiving quantity is not used as issued quantity. An optional
        `reservationId` consumes only that ACTIVE reservation allocation.
        Unreserved issues use existing available stock. STOCK_OUT, balance,
        usage, and reservation progress commit or fail TOGETHER. Existing UOM,
        cost, movement linkage, and duplicate-reference protections remain.
      tags: [Work Order Material Usage]
      security: [{ bearerAuth: [] }]
      x-required-permission: inventory_stock.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/WorkOrderMaterialUsageCreateRequest" }
      responses:
        "201":
          description: Material issued; STOCK_OUT, balance and usage committed atomically.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderMaterialUsage" }
        "400":
          description: >
            Validation failed, or a deterministic domain rejection:
            `INVENTORY_WO_MATERIAL_USAGE_INVALID_QUANTITY`,
            `WO_MATERIAL_USAGE_INVALID_UNIT_COST`,
            `WO_MATERIAL_USAGE_UOM_INCOMPATIBLE`,
            `INVENTORY_WO_MATERIAL_USAGE_CLIENT_MISMATCH`,
            `INVENTORY_WO_MATERIAL_USAGE_BUILDING_MISMATCH`,
            `INVENTORY_WO_MATERIAL_USAGE_MATERIAL_REQUEST_INVALID`, or
            `INVENTORY_WO_MATERIAL_USAGE_RESERVATION_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              examples:
                uomIncompatible:
                  value:
                    success: false
                    error:
                      code: WO_MATERIAL_USAGE_UOM_INCOMPATIBLE
                      message: The supplied UOM is incompatible with the item UOM and no conversion authority exists.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: >
            `WORK_ORDER_NOT_FOUND`, `INVENTORY_ITEM_NOT_FOUND`,
            `INVENTORY_WAREHOUSE_NOT_FOUND`, `MATERIAL_REQUEST_NOT_FOUND`, or
            `INVENTORY_MATERIAL_RESERVATION_NOT_FOUND`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `INVENTORY_WO_MATERIAL_USAGE_INSUFFICIENT_STOCK` (issue above
            available or physical stock — negative resulting stock is
            impossible), `INVENTORY_WO_MATERIAL_USAGE_DEMAND_EXCEEDED`,
            `INVENTORY_WO_MATERIAL_USAGE_MATERIAL_REQUEST_REQUIRED`,
            `INVENTORY_WO_MATERIAL_USAGE_MATERIAL_REQUEST_NOT_APPROVED`,
            `INVENTORY_MATERIAL_RESERVATION_NOT_ACTIVE`, or
            `INVENTORY_MATERIAL_RESERVATION_ALLOCATION_EXCEEDED`. The existing
            `WO_MATERIAL_USAGE_WORK_ORDER_STATE_INVALID` and
            `WO_MATERIAL_USAGE_DUPLICATE_REFERENCE` protections remain. Nothing
            is committed.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
              examples:
                duplicateIssue:
                  value:
                    success: false
                    error:
                      code: WO_MATERIAL_USAGE_DUPLICATE_REFERENCE
                      message: A material usage with this reference already exists for the work order (duplicate issue protection).
    get:
      operationId: listWorkOrderMaterialUsages
      summary: List material usages of a Work Order
      description: >
        Requires `inventory_stock.read` and Building access. Filters:
        `warehouseId`, `itemId`, `materialRequestId`, `reservationId`,
        `dateFrom`, `dateTo`.
      tags: [Work Order Material Usage]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: materialRequestId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
          description: Filter by the controlled Material Request demand source.
        - name: reservationId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
          description: Filter by the reservation allocation consumed by the usage.
        - name: dateFrom
          in: query
          required: false
          schema: { type: string, format: date-time }
        - name: dateTo
          in: query
          required: false
          schema: { type: string, format: date-time }
      responses:
        "200":
          description: Material usage records (append-only).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/WorkOrderMaterialUsage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /work-orders/{workOrderId}/material-cost-summary:
    get:
      operationId: getWorkOrderMaterialCostSummary
      summary: Deterministic Work Order material-cost aggregation
      description: >
        Requires `inventory_stock.read` and access to the Work Order's
        Building. CR-BE-MAT-01 PART 05 — aggregates the immutable operational
        cost snapshots: Work Order → Material Usage → total material cost
        (NUMERIC sum, with a per-currency breakdown). Material cost only — no
        labour/service cost, no valuation/GL. Pure read: never mutates stock.
      tags: [Work Order Material Usage]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkOrderIdPath"
      responses:
        "200":
          description: Material cost summary.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderMaterialCostSummary" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/work-order-material-usages:
    get:
      operationId: listBuildingWorkOrderMaterialUsages
      summary: List Work Order material usages of a Building
      description: >
        Requires `inventory_stock.read` and Building access. Filters:
        `workOrderId`, `warehouseId`, `itemId`, `materialRequestId`,
        `reservationId`, `dateFrom`, `dateTo`.
      tags: [Work Order Material Usage]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: workOrderId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: materialRequestId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
          description: Filter by the controlled Material Request demand source.
        - name: reservationId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
          description: Filter by the reservation allocation consumed by the usage.
      responses:
        "200":
          description: Material usage records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/WorkOrderMaterialUsage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /clients/{clientId}/work-order-material-usages:
    get:
      operationId: listClientWorkOrderMaterialUsages
      summary: List Work Order material usages in a Client scope
      description: >
        Requires `inventory_stock.read` and Client access. Filters:
        `buildingId`, `workOrderId`, `warehouseId`, `itemId`,
        `materialRequestId`, `reservationId`, `dateFrom`, `dateTo`.
      tags: [Work Order Material Usage]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: workOrderId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: materialRequestId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
          description: Filter by the controlled Material Request demand source.
        - name: reservationId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
          description: Filter by the reservation allocation consumed by the usage.
      responses:
        "200":
          description: Material usage records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/WorkOrderMaterialUsage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /work-order-material-usages/{id}:
    get:
      operationId: getWorkOrderMaterialUsage
      summary: Get a single Work Order material usage
      description: >
        Requires `inventory_stock.read` and Building access. Usage records
        are immutable; UOM and cost snapshots never change after issue.
        Historical rows without snapshots return null fields (fall back to
        the item's current UOM for legacy reads).
      tags: [Work Order Material Usage]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkOrderMaterialUsageIdPath"
      responses:
        "200":
          description: Material usage record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkOrderMaterialUsage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # --------------------------------------------------------------------
  # CR-BE-MOB-01 PART 01 — Housekeeping operational contract.
  # Existing BE-11 routes published for mobile. Execution writes stay on
  # BE-07 task/checklist endpoints; rework stays on BE-09 findings.
  # --------------------------------------------------------------------
  /buildings/{buildingId}/housekeeping/daily-cleaning:
    get:
      operationId: listBuildingDailyCleaning
      x-required-permission: daily_cleaning.read
      x-building-scoped: true
      summary: List daily cleaning tasks for a Building
      description: >-
        BE-11C read view of BE-07 generated tasks bound to a Cleaning Area.
        Requires `daily_cleaning.read` and BE-02G Building access.
        Execution writes are `startTask` / `completeTask` / `cancelTask`
        using `taskId` (same as `id`). Mobile relevance: cleaning work list.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: date
          in: query
          required: false
          schema: { type: string, format: date }
          description: Operational date YYYY-MM-DD.
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/DailyCleaningStatus" }
      responses:
        "200":
          description: Daily cleaning list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/DailyCleaning" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/daily-cleaning/{id}:
    get:
      operationId: getDailyCleaning
      x-required-permission: daily_cleaning.read
      x-building-scoped: true
      summary: Get a daily cleaning task by id
      description: >-
        Authoritative daily-cleaning read. `id` is the BE-07 generated task
        id (`taskId`). Requires `daily_cleaning.read` and Building access
        on the task's Building.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DailyCleaningIdPath"
      responses:
        "200":
          description: Daily cleaning record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/DailyCleaning" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/cleaning-areas/{id}/daily-cleaning:
    get:
      operationId: listCleaningAreaDailyCleaning
      x-required-permission: daily_cleaning.read
      x-building-scoped: true
      summary: List daily cleaning tasks for a Cleaning Area
      description: >-
        Requires `daily_cleaning.read` and Building access on the area's
        Building. Path `{id}` is the Cleaning Area id.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/CleaningAreaIdPath"
        - name: date
          in: query
          required: false
          schema: { type: string, format: date }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/DailyCleaningStatus" }
      responses:
        "200":
          description: Daily cleaning list for the area.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/DailyCleaning" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/daily-cleaning/{id}/assignments:
    get:
      operationId: listDailyCleaningAssignments
      x-required-permission: cleaning_assignment.read
      x-building-scoped: true
      summary: List assignments for a daily cleaning task
      description: >-
        BE-11D reuse of BE-07 task assignment. Requires
        `cleaning_assignment.read` and Building access. Path `{id}` is the
        daily-cleaning / task id.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DailyCleaningIdPath"
      responses:
        "200":
          description: Assignment list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/CleaningAssignment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: assignDailyCleaning
      x-required-permission: cleaning_assignment.manage
      x-building-scoped: true
      summary: Assign a daily cleaning task
      description: >-
        MUTATION — binds WORKFORCE or TEAM via BE-07 task assignment.
        Requires `cleaning_assignment.manage` and Building access.
        Terminal tasks (COMPLETED/CANCELLED) are rejected. Replaces any
        ACTIVE assignment.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DailyCleaningIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateCleaningAssignmentRequest" }
      responses:
        "201":
          description: Assignment created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/CleaningAssignment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /workforce/{workforceId}/housekeeping/daily-cleaning:
    get:
      operationId: listWorkforceDailyCleaning
      x-required-permission: cleaning_assignment.read
      x-building-scoped: true
      summary: List daily cleaning assigned to a workforce profile
      description: >-
        Requires `cleaning_assignment.read`. Optional date/status filters.
        Mobile relevance: my-cleaning feed keyed to workforceProfileId
        from `GET /auth/me`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkforceIdPathParam"
        - name: date
          in: query
          required: false
          schema: { type: string, format: date }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/DailyCleaningStatus" }
      responses:
        "200":
          description: Assigned daily cleaning list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/DailyCleaning" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /teams/{teamId}/housekeeping/daily-cleaning:
    get:
      operationId: listTeamDailyCleaning
      x-required-permission: cleaning_assignment.read
      x-building-scoped: true
      summary: List daily cleaning assigned to a Team
      description: Requires `cleaning_assignment.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TeamIdPathParam"
        - name: date
          in: query
          required: false
          schema: { type: string, format: date }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/DailyCleaningStatus" }
      responses:
        "200":
          description: Team daily cleaning list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/DailyCleaning" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/toilet-inspection-bindings:
    get:
      operationId: listToiletInspectionBindings
      x-required-permission: toilet_inspection.read
      x-building-scoped: true
      summary: List toilet inspection bindings
      description: >-
        BE-11E binding of a Cleaning Area to a BE-07 checklist template.
        Requires `toilet_inspection.read`. Pass `buildingId` for scoped
        mobile lists (Building access asserted).
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/HousekeepingBindingStatus" }
      responses:
        "200":
          description: Binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/ToiletInspectionBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createToiletInspectionBinding
      x-required-permission: toilet_inspection.manage
      x-building-scoped: true
      summary: Create a toilet inspection binding
      description: >-
        Requires `toilet_inspection.manage` and Building access on the
        cleaning area. Does not execute an inspection.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateToiletInspectionBindingRequest" }
      responses:
        "201":
          description: Binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ToiletInspectionBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/toilet-inspection-bindings/{id}:
    get:
      operationId: getToiletInspectionBinding
      x-required-permission: toilet_inspection.read
      x-building-scoped: true
      summary: Get a toilet inspection binding
      description: Requires `toilet_inspection.read` and Building access.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ToiletInspectionBindingIdPath"
      responses:
        "200":
          description: Binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ToiletInspectionBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateToiletInspectionBinding
      x-required-permission: toilet_inspection.manage
      x-building-scoped: true
      summary: Update a toilet inspection binding
      description: Requires `toilet_inspection.manage` and Building access.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ToiletInspectionBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateToiletInspectionBindingRequest" }
      responses:
        "200":
          description: Binding updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ToiletInspectionBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/toilet-inspection-bindings/{id}/start:
    post:
      operationId: startToiletInspectionExecution
      x-required-permission: toilet_inspection.manage
      x-building-scoped: true
      summary: Start a toilet inspection checklist execution
      description: >-
        Creates a BE-07 checklist execution for the binding. Complete/cancel
        and responses stay on published checklist endpoints using the
        returned execution `id`. Requires `toilet_inspection.manage`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ToiletInspectionBindingIdPath"
      responses:
        "201":
          description: Execution started.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ToiletInspectionExecutionContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/toilet-inspection-executions/{id}:
    get:
      operationId: getToiletInspectionExecution
      x-required-permission: toilet_inspection.read
      x-building-scoped: true
      summary: Get toilet inspection execution context
      description: >-
        Path `{id}` is the BE-07 checklist execution id. Requires
        `toilet_inspection.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ToiletInspectionExecutionIdPath"
      responses:
        "200":
          description: Execution plus binding context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ToiletInspectionExecutionContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/public-area-inspection-bindings:
    get:
      operationId: listPublicAreaInspectionBindings
      x-required-permission: public_area_inspection.read
      x-building-scoped: true
      summary: List public-area inspection bindings
      description: Requires `public_area_inspection.read`. Pass `buildingId` for scoped mobile lists.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/HousekeepingBindingStatus" }
      responses:
        "200":
          description: Binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PublicAreaInspectionBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createPublicAreaInspectionBinding
      x-required-permission: public_area_inspection.manage
      x-building-scoped: true
      summary: Create a public-area inspection binding
      description: Requires `public_area_inspection.manage` and Building access.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePublicAreaInspectionBindingRequest" }
      responses:
        "201":
          description: Binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicAreaInspectionBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/public-area-inspection-bindings/{id}:
    get:
      operationId: getPublicAreaInspectionBinding
      x-required-permission: public_area_inspection.read
      x-building-scoped: true
      summary: Get a public-area inspection binding
      description: Requires `public_area_inspection.read` and Building access.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PublicAreaInspectionBindingIdPath"
      responses:
        "200":
          description: Binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicAreaInspectionBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updatePublicAreaInspectionBinding
      x-required-permission: public_area_inspection.manage
      x-building-scoped: true
      summary: Update a public-area inspection binding
      description: Requires `public_area_inspection.manage` and Building access.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PublicAreaInspectionBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdatePublicAreaInspectionBindingRequest" }
      responses:
        "200":
          description: Binding updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicAreaInspectionBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/public-area-inspection-bindings/{id}/start:
    post:
      operationId: startPublicAreaInspectionExecution
      x-required-permission: public_area_inspection.manage
      x-building-scoped: true
      summary: Start a public-area inspection checklist execution
      description: >-
        Creates a BE-07 checklist execution. Complete/responses stay on
        published checklist endpoints. Requires `public_area_inspection.manage`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PublicAreaInspectionBindingIdPath"
      responses:
        "201":
          description: Execution started.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicAreaInspectionExecutionContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/public-area-inspection-executions/{id}:
    get:
      operationId: getPublicAreaInspectionExecution
      x-required-permission: public_area_inspection.read
      x-building-scoped: true
      summary: Get public-area inspection execution context
      description: Path `{id}` is the BE-07 checklist execution id. Requires `public_area_inspection.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PublicAreaInspectionExecutionIdPath"
      responses:
        "200":
          description: Execution plus binding context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PublicAreaInspectionExecutionContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/supervisor-inspections:
    get:
      operationId: listSupervisorInspections
      x-required-permission: supervisor_inspection.read
      x-building-scoped: true
      summary: List supervisor inspections
      description: >-
        BE-11G verification records over daily cleaning / toilet / public-area
        executions, using the BE-07 review primitive. Requires
        `supervisor_inspection.read`. Pass `buildingId` for scoped lists.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: targetType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/SupervisorInspectionTargetType" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/SupervisorInspectionStatus" }
      responses:
        "200":
          description: Supervisor inspection list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/SupervisorInspection" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createSupervisorInspection
      x-required-permission: supervisor_inspection.manage
      x-building-scoped: true
      summary: Open a supervisor inspection
      description: >-
        Requires `supervisor_inspection.manage`. `supervisorUserId` is the
        authenticated user. Decision is submitted separately.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateSupervisorInspectionRequest" }
      responses:
        "201":
          description: Inspection opened.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SupervisorInspection" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/supervisor-inspections/{id}:
    get:
      operationId: getSupervisorInspection
      x-required-permission: supervisor_inspection.read
      x-building-scoped: true
      summary: Get a supervisor inspection
      description: Requires `supervisor_inspection.read` and Building access.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/SupervisorInspectionIdPath"
      responses:
        "200":
          description: Supervisor inspection.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SupervisorInspection" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/supervisor-inspections/{id}/decision:
    post:
      operationId: submitSupervisorInspectionDecision
      x-required-permission: supervisor_inspection.manage
      x-building-scoped: true
      summary: Submit a supervisor inspection decision
      description: >-
        MUTATION — APPROVED / REJECTED / REWORK_REQUIRED via BE-07 review.
        Requires `supervisor_inspection.manage`. There is no reinspection
        engine; FAIL/REWORK is followed by a new checklist start plus BE-09
        finding rework when a finding exists.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/SupervisorInspectionIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitSupervisorInspectionDecisionRequest" }
      responses:
        "200":
          description: Decision recorded.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SupervisorInspection" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/quality-audits:
    get:
      operationId: listQualityAudits
      x-required-permission: quality_audit.read
      x-building-scoped: true
      summary: List quality audits
      description: >-
        BE-11K PASS / FAIL / REWORK_REQUIRED scoring layer. Requires
        `quality_audit.read`. Pass `buildingId` for scoped mobile lists.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: sourceType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/QualityAuditSourceType" }
        - name: result
          in: query
          required: false
          schema: { $ref: "#/components/schemas/QualityAuditResult" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/QualityAuditStatus" }
      responses:
        "200":
          description: Quality audit list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/QualityAudit" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createQualityAudit
      x-required-permission: quality_audit.manage
      x-building-scoped: true
      summary: Create a quality audit
      description: Requires `quality_audit.manage`. `auditorUserId` is the authenticated user.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateQualityAuditRequest" }
      responses:
        "201":
          description: Audit created (DRAFT).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/QualityAudit" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/quality-audits/{id}:
    get:
      operationId: getQualityAudit
      x-required-permission: quality_audit.read
      x-building-scoped: true
      summary: Get a quality audit
      description: Requires `quality_audit.read` and Building access.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/QualityAuditIdPath"
      responses:
        "200":
          description: Quality audit.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/QualityAudit" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateQualityAudit
      x-required-permission: quality_audit.manage
      x-building-scoped: true
      summary: Update a draft quality audit
      description: Requires `quality_audit.manage`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/QualityAuditIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateQualityAuditRequest" }
      responses:
        "200":
          description: Audit updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/QualityAudit" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/quality-audits/{id}/complete:
    post:
      operationId: completeQualityAudit
      x-required-permission: quality_audit.manage
      x-building-scoped: true
      summary: Complete a quality audit
      description: >-
        Records PASS / FAIL / REWORK_REQUIRED. Requires `quality_audit.manage`.
        Not a reinspection engine.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/QualityAuditIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CompleteQualityAuditRequest" }
      responses:
        "200":
          description: Audit completed.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/QualityAudit" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/findings:
    get:
      operationId: listHousekeepingFindings
      x-required-permission: housekeeping_finding.read
      x-building-scoped: true
      summary: List housekeeping finding bindings
      description: >-
        BE-11H links HK sources to a BE-09 Finding. Lifecycle/rework stay on
        published finding operations using `findingId`. Requires
        `housekeeping_finding.read`. Pass `buildingId` for scoped lists.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: sourceType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/HousekeepingFindingSourceType" }
        - name: status
          in: query
          required: false
          schema: { type: string }
          description: Finding status filter (BE-09 status).
      responses:
        "200":
          description: Housekeeping finding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HousekeepingFinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createHousekeepingFinding
      x-required-permission: housekeeping_finding.manage
      x-building-scoped: true
      summary: Create a housekeeping finding (BE-09 Finding + HK link)
      description: >-
        Requires `housekeeping_finding.manage` and Building access.
        Creates an authoritative BE-09 Finding and the HK source binding.
        Rework uses `requestFindingRework` / `resubmitFinding` on `findingId`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateHousekeepingFindingRequest" }
      responses:
        "201":
          description: Housekeeping finding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HousekeepingFinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/findings/{id}:
    get:
      operationId: getHousekeepingFinding
      x-required-permission: housekeeping_finding.read
      x-building-scoped: true
      summary: Get a housekeeping finding binding
      description: >-
        Path `{id}` is the HK link id, not the BE-09 finding id. The payload
        includes `findingId` and `availableActions` from BE-09. Requires
        `housekeeping_finding.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HousekeepingFindingIdPath"
      responses:
        "200":
          description: Housekeeping finding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HousekeepingFinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/{sourceType}/{sourceId}/evidence-requirements:
    get:
      operationId: listHousekeepingEvidenceRequirements
      x-required-permission: housekeeping_evidence.read
      x-building-scoped: true
      summary: List evidence requirements for a housekeeping source
      description: >-
        BE-11I binding over BE-07 evidence requirements. Requires
        `housekeeping_evidence.read` and Building access on the source.
        `sourceType` is a kebab path segment (daily-cleaning, toilet-inspections,
        public-area-inspections, supervisor-inspections, findings).
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HousekeepingEvidenceSourceTypePath"
        - $ref: "#/components/parameters/HousekeepingEvidenceSourceIdPath"
      responses:
        "200":
          description: Requirement list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HousekeepingEvidenceRequirement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/{sourceType}/{sourceId}/evidence:
    get:
      operationId: listHousekeepingEvidence
      x-required-permission: housekeeping_evidence.read
      x-building-scoped: true
      summary: List evidence submissions for a housekeeping source
      description: Requires `housekeeping_evidence.read` and Building access.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HousekeepingEvidenceSourceTypePath"
        - $ref: "#/components/parameters/HousekeepingEvidenceSourceIdPath"
      responses:
        "200":
          description: Submission list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HousekeepingEvidenceSubmission" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: submitHousekeepingEvidence
      x-required-permission: housekeeping_evidence.manage
      x-building-scoped: true
      summary: Submit evidence metadata for a housekeeping source
      description: >-
        BE-07 evidence metadata (bytes stay on `uploadEvidenceFile` /
        `uploadMobileEvidence`). Requires `housekeeping_evidence.manage`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HousekeepingEvidenceSourceTypePath"
        - $ref: "#/components/parameters/HousekeepingEvidenceSourceIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitHousekeepingEvidenceRequest" }
      responses:
        "201":
          description: Evidence submitted.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HousekeepingEvidenceSubmission" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # --------------------------------------------------------------------
  # CR-BE-MOB-03 PART 01 — Workforce team workload & status contract.
  #
  # Existing read-only routes published for the mobile supervisor:
  #   BE-23G  GET /workforce/reports/kpi          — team workload KPI
  #   BE-03I2 GET /workforce/reporting[/{id}]     — workforce reporting reads
  #   BE-03F  GET /workforce/{id}/supervisor      — current supervisor
  #   BE-03F  GET /workforce/{id}/direct-reports  — direct reports
  # Nothing new is introduced; the KPI and reporting reads enforce BE-02G
  # Building scope, the BE-03F reads are profile-resolved (404 on unknown
  # profile) and are documented without a building-scope claim.
  # --------------------------------------------------------------------

  /workforce/reports/kpi:
    get:
      operationId: getWorkforceKpi
      summary: Workforce team workload KPI for a Building or accessible scope
      description: >-
        BE-23G read-only KPI projection over the BE-03 Workforce records and
        the BE-07 Task assignment store. `buildingId` is access-asserted when
        supplied; omitted, the KPI rolls up across exactly the Buildings the
        caller can reach (BE-02G), so cross-Building / cross-Client leakage is
        impossible. Optional `workforceId` / `teamId` / `workforceType`
        narrow the numbers; `dateFrom` / `dateTo` bound the assignment window
        (UTC day convention); `graceMinutes` is the overdue tolerance.
        Requires `workforce_kpi.read`. Read-only — no ETL, no mutation of
        BE-03/BE-07 state.
      tags: [Workforce]
      security: [{ bearerAuth: [] }]
      x-required-permission: workforce_kpi.read
      x-building-scoped: true
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
          description: Narrow to one Building; access-asserted (403 outside scope).
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
          description: BE-03C workforce profile narrowing.
        - name: teamId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
          description: BE-03B team narrowing.
        - name: workforceType
          in: query
          required: false
          schema:
            type: string
            enum: [INTERNAL, OUTSOURCED, CONTRACT]
          description: BE-03C workforce_type narrowing.
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
          description: ISO date (YYYY-MM-DD) or datetime; day windows are UTC, matching BE-07.
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
          description: ISO date (YYYY-MM-DD) or datetime; must not precede dateFrom and range is capped at 366 days.
        - name: graceMinutes
          in: query
          required: false
          schema: { type: integer, minimum: 0, maximum: 10080 }
          description: Minutes of tolerance before a past-due assignment counts as overdue (default 0).
      responses:
        "200":
          description: The workforce KPI projection.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/WorkforceKpi"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound", description: Unknown `buildingId`. }

  /workforce/reporting:
    get:
      operationId: listWorkforceReporting
      summary: List workforce reporting records in the caller's accessible scope
      description: >-
        BE-03I2 read-only reporting query over the BE-03I1 read model. Every
        returned record is connected to at least one Building in the caller's
        accessible set (BE-02G `requireAccessibleBuilding`), so
        organization/department/team filters can never widen access beyond
        Building isolation. Optional `organizationId` / `departmentId` /
        `teamId` / `positionId` / `buildingId` / `shiftId` /
        `externalOrganizationId` / `workforceType` / `status` /
        `includeInactive` filters narrow the page. Requires
        `workforce.read`.
      tags: [Workforce]
      security: [{ bearerAuth: [] }]
      x-required-permission: workforce.read
      x-building-scoped: true
      parameters:
        - name: organizationId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: departmentId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: teamId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: positionId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: shiftId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: externalOrganizationId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: workforceType
          in: query
          required: false
          schema:
            type: string
            enum: [INTERNAL, OUTSOURCED, CONTRACT, EXTERNAL]
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [ACTIVE, INACTIVE]
          description: Do not combine with includeInactive=true.
        - name: includeInactive
          in: query
          required: false
          schema: { type: boolean }
        - name: page
          in: query
          required: false
          schema: { type: integer, minimum: 1, default: 1 }
        - name: limit
          in: query
          required: false
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
      responses:
        "200":
          description: Paginated workforce reporting records.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/WorkforceReportingRecord"
                      meta:
                        type: object
                        required: [page, limit, total]
                        properties:
                          page: { type: integer }
                          limit: { type: integer }
                          total: { type: integer }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /workforce/reporting/{id}:
    get:
      operationId: getWorkforceReporting
      summary: Get one workforce reporting record by Workforce Profile id
      description: >-
        BE-03I2 read of a single Workforce Reporting record by its
        authoritative `workforce_profiles.id`. The record must be connected to
        at least one Building in the caller's accessible set (BE-02G); an
        unknown id or a profile outside the accessible scope is a 404, never a
        cross-Client leak. Requires `workforce.read`.
      tags: [Workforce]
      security: [{ bearerAuth: [] }]
      x-required-permission: workforce.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/WorkforceReportingIdPath"
      responses:
        "200":
          description: The workforce reporting record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/WorkforceReportingRecord"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /workforce/{workforceId}/supervisor:
    get:
      operationId: getWorkforceCurrentSupervisor
      summary: Resolve the current Supervisor of a Workforce Profile
      description: >-
        BE-03F read. Resolves the reporting line genuinely in force for the
        profile at the current instant (ACTIVE line, ACTIVE supervisor,
        effective window). The profile id is resolved through the
        Organization → Client chain, so an unknown profile is a controlled
        404. The reporting line is structurally same-Client (the BE-03F write
        path enforces the cross-Client rule), but this read path carries no
        BE-02G accessible-scope gate today — it is documented as-is, not
        claimed as building-scoped. Requires `workforce.read`.
      tags: [Workforce]
      security: [{ bearerAuth: [] }]
      x-required-permission: workforce.read
      parameters:
        - $ref: "#/components/parameters/WorkforceIdPathParam"
      responses:
        "200":
          description: The current supervisor of the profile.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/CurrentSupervisor"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /workforce/{supervisorId}/direct-reports:
    get:
      operationId: listWorkforceDirectReports
      summary: List the Workforce Profiles reporting to a Supervisor
      description: >-
        BE-03F read. Lists the ACTIVE direct reports of the Supervisor
        (ACTIVE line, ACTIVE subordinate, effective window), ordered by
        employee code. The supervisor id is resolved through the Organization
        → Client chain, so an unknown supervisor is a controlled 404. The
        reporting lines are structurally same-Client (the BE-03F write path
        enforces the cross-Client rule), but this read path carries no BE-02G
        accessible-scope gate today — it is documented as-is, not claimed as
        building-scoped. Requires `workforce.read`.
      tags: [Workforce]
      security: [{ bearerAuth: [] }]
      x-required-permission: workforce.read
      parameters:
        - $ref: "#/components/parameters/SupervisorIdPathParam"
      responses:
        "200":
          description: The direct reports of the supervisor.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/DirectReport"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # --------------------------------------------------------------------
  # CR-BE-MOB-03 PART 02 — Housekeeping supervisor supporting contract.
  #
  # Existing Housekeeping supervisor-supporting routes published for the
  # mobile Cleaning Service supervisor:
  #   BE-11J  consumable requirements + readiness checks
  #   BE-16J  housekeeping consumable bindings (item + warehouse + stock)
  #   BE-11L  housekeeping complaint bindings
  #   BE-11M  housekeeping operational / report datasets
  # Publish-only: every operation below is an existing runtime route.
  # Nothing new is introduced, no housekeeping workflow is redesigned, no
  # stock-usage engine and no reinspection lifecycle is invented (HK-04/06
  # stay NOT_REQUIRED). Building isolation (BE-02G) is asserted per request
  # on every write and single-record read; list endpoints assert the
  # supplied buildingId/clientId (pass buildingId for scoped supervisor
  # lists — the same convention as the CR-BE-MOB-01 Housekeeping tag).
  # --------------------------------------------------------------------

  /housekeeping/consumable-requirements:
    get:
      operationId: listConsumableRequirements
      x-required-permission: consumable_readiness.read
      x-building-scoped: true
      summary: List housekeeping consumable requirements
      description: >-
        BE-11J read. Consumable requirements with their latest readiness
        check and resolved Cleaning Area. Optional `buildingId` /
        `cleaningAreaId` / `status` filters narrow the list; when
        `buildingId` is supplied it is access-asserted (BE-02G) — pass
        `buildingId` for scoped supervisor lists. Requires
        `consumable_readiness.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ConsumableRequirementStatus" }
      responses:
        "200":
          description: Consumable requirement list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/ConsumableRequirement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createConsumableRequirement
      x-required-permission: consumable_readiness.manage
      x-building-scoped: true
      summary: Create a housekeeping consumable requirement
      description: >-
        BE-11J write. Defines the consumable requirement (code / name /
        required quantity / unit) for a Building and optional Cleaning
        Area. `buildingId` is access-asserted (BE-02G). Requires
        `consumable_readiness.manage`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateConsumableRequirementRequest" }
      responses:
        "201":
          description: Consumable requirement created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ConsumableRequirement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /housekeeping/consumable-requirements/{id}:
    get:
      operationId: getConsumableRequirement
      x-required-permission: consumable_readiness.read
      x-building-scoped: true
      summary: Get a housekeeping consumable requirement by id
      description: >-
        BE-11J read. The requirement is loaded then Building access is
        asserted on its `buildingId` (BE-02G); an unknown id or a
        requirement outside the caller's accessible scope is a 404/403.
        Requires `consumable_readiness.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ConsumableRequirementIdPath"
      responses:
        "200":
          description: The consumable requirement.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ConsumableRequirement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateConsumableRequirement
      x-required-permission: consumable_readiness.manage
      x-building-scoped: true
      summary: Update a housekeeping consumable requirement
      description: >-
        BE-11J write. Partial update of `name` / `cleaningAreaId` /
        `requiredQuantity` / `unit` / `status`; Building access asserted on
        the existing requirement (BE-02G). Requires
        `consumable_readiness.manage`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ConsumableRequirementIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateConsumableRequirementRequest" }
      responses:
        "200":
          description: The updated consumable requirement.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ConsumableRequirement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /housekeeping/consumable-requirements/{id}/readiness:
    post:
      operationId: recordConsumableReadiness
      x-required-permission: consumable_readiness.manage
      x-building-scoped: true
      summary: Record a housekeeping consumable readiness check
      description: >-
        BE-11J write. Records the daily operational readiness check
        (READY / LOW / NOT_READY / UNKNOWN + available quantity + notes) for
        a requirement; `checkedByUserId` is the authenticated session, never
        the caller. Building access asserted on the requirement (BE-02G).
        Requires `consumable_readiness.manage`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ConsumableRequirementIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RecordConsumableReadinessRequest" }
      responses:
        "201":
          description: The readiness check recorded.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ConsumableReadiness" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/consumable-readiness:
    get:
      operationId: listConsumableReadiness
      x-required-permission: consumable_readiness.read
      x-building-scoped: true
      summary: List housekeeping consumable readiness checks
      description: >-
        BE-11J read. Readiness checks with resolved requirement and Cleaning
        Area. Optional `buildingId` / `cleaningAreaId` / `readinessStatus` /
        `date` filters narrow the list; when `buildingId` is supplied it is
        access-asserted (BE-02G) — pass `buildingId` for scoped supervisor
        lists. Requires `consumable_readiness.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: readinessStatus
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReadinessStatus" }
        - name: date
          in: query
          required: false
          schema: { type: string }
          description: Operational date (YYYY-MM-DD) to narrow the checks.
      responses:
        "200":
          description: Readiness check list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/ConsumableReadiness" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/consumable-requirements/{requirementId}/bindings:
    get:
      operationId: listConsumableRequirementBindings
      x-required-permission: consumable_readiness.read
      x-building-scoped: true
      summary: List housekeeping consumable bindings of a requirement
      description: >-
        BE-16J read. Consumable bindings of one BE-11J requirement, each
        with the BE-16A item, BE-16B warehouse and the derived readiness
        (READY / LOW / NOT_READY / UNKNOWN from authoritative stock).
        Building access is asserted on the requirement (BE-02G). Requires
        `consumable_readiness.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HkConsumableRequirementIdPath"
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/HkConsumableBindingStatus" }
        - name: readiness
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReadinessStatus" }
      responses:
        "200":
          description: Binding list for the requirement.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HkConsumableBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: createConsumableRequirementBinding
      x-required-permission: consumable_readiness.manage
      x-building-scoped: true
      summary: Bind a consumable item and warehouse to a requirement
      description: >-
        BE-16J write. Binds a BE-16A CONSUMABLE item + BE-16B warehouse to a
        BE-11J requirement. Cross-Client and requirement/warehouse Building
        mismatches are rejected; Building access asserted on the requirement
        (BE-02G). Requires `consumable_readiness.manage`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HkConsumableRequirementIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateHkConsumableBindingRequest" }
      responses:
        "201":
          description: Binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HkConsumableBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /housekeeping/consumable-bindings:
    get:
      operationId: listHousekeepingConsumableBindings
      x-required-permission: consumable_readiness.read
      x-building-scoped: true
      summary: List housekeeping consumable bindings with optional filters
      description: >-
        BE-16J read. Consumable bindings across the accessible scope.
        Optional `clientId` / `buildingId` / `cleaningAreaId` /
        `consumableRequirementId` / `itemId` / `warehouseId` / `status` /
        `readiness` filters narrow the list; `buildingId` and `clientId`
        are access-asserted (BE-02G) — pass `buildingId` for scoped
        supervisor lists. Requires `consumable_readiness.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: clientId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: consumableRequirementId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/HkConsumableBindingStatus" }
        - name: readiness
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReadinessStatus" }
      responses:
        "200":
          description: Binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HkConsumableBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/consumable-bindings/{id}:
    get:
      operationId: getHousekeepingConsumableBinding
      x-required-permission: consumable_readiness.read
      x-building-scoped: true
      summary: Get a housekeeping consumable binding by id
      description: >-
        BE-16J read. The binding is loaded then Building access is asserted
        on its `buildingId` (BE-02G); unknown id or out-of-scope binding is
        a 404/403. Includes the derived readiness and current stock context.
        Requires `consumable_readiness.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HkConsumableBindingIdPath"
      responses:
        "200":
          description: The binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HkConsumableBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateHousekeepingConsumableBinding
      x-required-permission: consumable_readiness.manage
      x-building-scoped: true
      summary: Update a housekeeping consumable binding
      description: >-
        BE-16J write. Partial update of `requiredQuantity` / `status` /
        `notes`; Building access asserted on the existing binding (BE-02G).
        Requires `consumable_readiness.manage`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HkConsumableBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateHkConsumableBindingRequest" }
      responses:
        "200":
          description: The updated binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HkConsumableBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/housekeeping-consumable-bindings:
    get:
      operationId: listBuildingHousekeepingConsumableBindings
      x-required-permission: consumable_readiness.read
      x-building-scoped: true
      summary: List housekeeping consumable bindings in a Building
      description: >-
        BE-16J read, Building-nested. `requireBuildingAccess` is enforced on
        the path (BE-02G). Optional `cleaningAreaId` / `consumableRequirementId`
        / `itemId` / `warehouseId` / `status` / `readiness` filters narrow
        the list. Requires `consumable_readiness.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: consumableRequirementId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/HkConsumableBindingStatus" }
        - name: readiness
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReadinessStatus" }
      responses:
        "200":
          description: Binding list for the Building.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HkConsumableBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /cleaning-areas/{cleaningAreaId}/housekeeping-consumable-bindings:
    get:
      operationId: listCleaningAreaHousekeepingConsumableBindings
      x-required-permission: consumable_readiness.read
      x-building-scoped: true
      summary: List housekeeping consumable bindings of a Cleaning Area
      description: >-
        BE-16J read, Cleaning-Area-nested. Optional `buildingId` /
        `consumableRequirementId` / `itemId` / `warehouseId` / `status` /
        `readiness` filters; `buildingId` is access-asserted when supplied
        (BE-02G) — pass it for scoped supervisor lists. Requires
        `consumable_readiness.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HkBindingCleaningAreaIdPath"
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: consumableRequirementId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/HkConsumableBindingStatus" }
        - name: readiness
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReadinessStatus" }
      responses:
        "200":
          description: Binding list for the Cleaning Area.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HkConsumableBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /clients/{clientId}/housekeeping-consumable-bindings:
    get:
      operationId: listClientHousekeepingConsumableBindings
      x-required-permission: consumable_readiness.read
      x-building-scoped: true
      summary: List housekeeping consumable bindings of a Client
      description: >-
        BE-16J read, Client-nested. Client access is asserted (BE-02F);
        optional `buildingId` / `cleaningAreaId` / `consumableRequirementId`
        / `itemId` / `warehouseId` / `status` / `readiness` filters.
        Requires `consumable_readiness.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: consumableRequirementId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/HkConsumableBindingStatus" }
        - name: readiness
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReadinessStatus" }
      responses:
        "200":
          description: Binding list for the Client.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HkConsumableBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /warehouses/{warehouseId}/housekeeping-consumable-bindings:
    get:
      operationId: listWarehouseHousekeepingConsumableBindings
      x-required-permission: consumable_readiness.read
      x-building-scoped: true
      summary: List housekeeping consumable bindings of a Warehouse
      description: >-
        BE-16J read, Warehouse-nested. Building access is asserted on the
        warehouse's Building (BE-02G); optional `buildingId` /
        `cleaningAreaId` / `consumableRequirementId` / `itemId` / `status` /
        `readiness` filters. Requires `consumable_readiness.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WarehouseIdPath"
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: consumableRequirementId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/HkConsumableBindingStatus" }
        - name: readiness
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReadinessStatus" }
      responses:
        "200":
          description: Binding list for the Warehouse.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HkConsumableBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /inventory-items/{itemId}/housekeeping-consumable-bindings:
    get:
      operationId: listItemHousekeepingConsumableBindings
      x-required-permission: consumable_readiness.read
      x-building-scoped: true
      summary: List housekeeping consumable bindings of an Inventory Item
      description: >-
        BE-16J read, Item-nested. Client access is asserted on the item
        (BE-02F); optional `buildingId` / `cleaningAreaId` /
        `consumableRequirementId` / `warehouseId` / `status` / `readiness`
        filters. Requires `consumable_readiness.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/InventoryItemIdPath"
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: consumableRequirementId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: warehouseId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/HkConsumableBindingStatus" }
        - name: readiness
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReadinessStatus" }
      responses:
        "200":
          description: Binding list for the Inventory Item.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HkConsumableBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/complaint-bindings:
    get:
      operationId: listHousekeepingComplaintBindings
      x-required-permission: housekeeping_complaint.read
      x-building-scoped: true
      summary: List housekeeping complaint bindings
      description: >-
        BE-11L read. Complaint / service-request bindings to Housekeeping
        operations. Optional `buildingId` / `cleaningAreaId` / `findingId` /
        `workRequestId` / `complaintReference` / `status` filters; when
        `buildingId` is supplied it is access-asserted (BE-02G) — pass
        `buildingId` for scoped supervisor lists. Requires
        `housekeeping_complaint.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: findingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: workRequestId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: complaintReference
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/HousekeepingComplaintBindingStatus" }
      responses:
        "200":
          description: Complaint binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HousekeepingComplaintBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createHousekeepingComplaintBinding
      x-required-permission: housekeeping_complaint.manage
      x-building-scoped: true
      summary: Create a housekeeping complaint binding
      description: >-
        BE-11L write. Binds a customer complaint / service request to
        Housekeeping operations (source pair must be both-provided or both
        omitted). `buildingId` is access-asserted (BE-02G); `createdByUserId`
        is the authenticated session. Requires `housekeeping_complaint.manage`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateHousekeepingComplaintBindingRequest" }
      responses:
        "201":
          description: Complaint binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HousekeepingComplaintBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /housekeeping/complaint-bindings/{id}:
    get:
      operationId: getHousekeepingComplaintBinding
      x-required-permission: housekeeping_complaint.read
      x-building-scoped: true
      summary: Get a housekeeping complaint binding by id
      description: >-
        BE-11L read. The binding is loaded then Building access is asserted
        on its `buildingId` (BE-02G); unknown id or out-of-scope binding is
        a 404/403. Requires `housekeeping_complaint.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HousekeepingComplaintBindingIdPath"
      responses:
        "200":
          description: The complaint binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HousekeepingComplaintBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateHousekeepingComplaintBinding
      x-required-permission: housekeeping_complaint.manage
      x-building-scoped: true
      summary: Update a housekeeping complaint binding
      description: >-
        BE-11L write. Partial update of `description` / `findingId` /
        `status`; Building access asserted on the existing binding (BE-02G).
        Requires `housekeeping_complaint.manage`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HousekeepingComplaintBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateHousekeepingComplaintBindingRequest" }
      responses:
        "200":
          description: The updated complaint binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HousekeepingComplaintBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /housekeeping/reports/summary:
    get:
      operationId: getHousekeepingReportSummary
      x-required-permission: housekeeping_report.read
      x-building-scoped: true
      summary: Housekeeping summary report for a Building
      description: >-
        BE-11M read. One summary projection (daily cleaning, inspections,
        supervisor inspections, findings, consumables, quality audits,
        complaints) for a Building. `buildingId` is required and
        access-asserted (BE-02G); optional `cleaningAreaId` / `dateFrom` /
        `dateTo` narrow the window. Requires `housekeeping_report.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
          description: YYYY-MM-DD window start.
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
          description: YYYY-MM-DD window end; must not precede dateFrom.
      responses:
        "200":
          description: The summary report.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HousekeepingReportSummary" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/reports/cleaning:
    get:
      operationId: getHousekeepingReportCleaning
      x-required-permission: housekeeping_report.read
      x-building-scoped: true
      summary: Housekeeping cleaning dataset for a Building
      description: >-
        BE-11M read. Daily-cleaning report rows (task, Cleaning Area,
        schedule, assignee, completion) for a Building. `buildingId` is
        required and access-asserted (BE-02G); optional `cleaningAreaId` /
        `dateFrom` / `dateTo` / `status` / `workforceId` / `teamId` narrow
        the dataset. Requires `housekeeping_report.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: teamId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Cleaning dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HousekeepingCleaningReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/reports/inspections:
    get:
      operationId: getHousekeepingReportInspections
      x-required-permission: housekeeping_report.read
      x-building-scoped: true
      summary: Housekeeping inspection dataset for a Building
      description: >-
        BE-11M read. Toilet + public-area inspection report rows for a
        Building. `buildingId` is required and access-asserted (BE-02G);
        optional `cleaningAreaId` / `dateFrom` / `dateTo` / `status` /
        `workforceId` / `teamId` narrow the dataset. Requires
        `housekeeping_report.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: teamId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Inspection dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HousekeepingInspectionReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/reports/supervisor-inspections:
    get:
      operationId: getHousekeepingReportSupervisorInspections
      x-required-permission: housekeeping_report.read
      x-building-scoped: true
      summary: Housekeeping supervisor-inspection dataset for a Building
      description: >-
        BE-11M read. Supervisor inspection report rows (decision, status,
        notes) for a Building. `buildingId` is required and access-asserted
        (BE-02G); optional `cleaningAreaId` / `dateFrom` / `dateTo` /
        `status` / `workforceId` / `teamId` narrow the dataset. Requires
        `housekeeping_report.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: teamId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Supervisor-inspection dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HousekeepingSupervisorInspectionReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/reports/findings:
    get:
      operationId: getHousekeepingReportFindings
      x-required-permission: housekeeping_report.read
      x-building-scoped: true
      summary: Housekeeping finding dataset for a Building
      description: >-
        BE-11M read. Housekeeping finding report rows (BE-09 `findingId`,
        source, Cleaning Area) for a Building. `buildingId` is required and
        access-asserted (BE-02G); optional `cleaningAreaId` / `dateFrom` /
        `dateTo` / `status` / `workforceId` / `teamId` narrow the dataset.
        Requires `housekeeping_report.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: teamId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Finding dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HousekeepingFindingReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/reports/consumables:
    get:
      operationId: getHousekeepingReportConsumables
      x-required-permission: housekeeping_report.read
      x-building-scoped: true
      summary: Housekeeping consumable dataset for a Building
      description: >-
        BE-11M read. Consumable report rows (requirement, readiness,
        available quantity) for a Building. `buildingId` is required and
        access-asserted (BE-02G); optional `cleaningAreaId` / `dateFrom` /
        `dateTo` / `status` / `workforceId` / `teamId` narrow the dataset.
        Requires `housekeeping_report.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: teamId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Consumable dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HousekeepingConsumableReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/reports/quality-audits:
    get:
      operationId: getHousekeepingReportQualityAudits
      x-required-permission: housekeeping_report.read
      x-building-scoped: true
      summary: Housekeeping quality-audit dataset for a Building
      description: >-
        BE-11M read. Quality-audit report rows (score, result, status) for a
        Building. `buildingId` is required and access-asserted (BE-02G);
        optional `cleaningAreaId` / `dateFrom` / `dateTo` / `status` /
        `workforceId` / `teamId` narrow the dataset. Requires
        `housekeeping_report.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: teamId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Quality-audit dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HousekeepingQualityAuditReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /housekeeping/reports/complaints:
    get:
      operationId: getHousekeepingReportComplaints
      x-required-permission: housekeeping_report.read
      x-building-scoped: true
      summary: Housekeeping complaint dataset for a Building
      description: >-
        BE-11M read. Complaint-binding report rows for a Building.
        `buildingId` is required and access-asserted (BE-02G); optional
        `cleaningAreaId` / `dateFrom` / `dateTo` / `status` / `workforceId`
        / `teamId` narrow the dataset. Requires `housekeeping_report.read`.
      tags: [Housekeeping]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: cleaningAreaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: teamId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Complaint dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HousekeepingComplaintReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  # --------------------------------------------------------------------
  # CR-BE-MOB-03 PART 03 — Engineering & corrective-action verification
  # contract.
  #
  # Existing engineering-supervisor supporting routes published for the
  # mobile Teknisi supervisor:
  #   BE-10I  GET /engineering/reports/*          — engineering report datasets
  #   BE-21J  /corrective-actions/{id}/verification* — corrective-action
  #           verification (a shared BE-07 `reviews` row with target_type
  #           CORRECTIVE_ACTION; no new verification engine)
  # Publish-only: every operation below is an existing runtime route.
  # Nothing new is introduced, no verification workflow is redesigned, the
  # finding/rework lifecycle is untouched, and /mobile/verification is not
  # extended. Building isolation (BE-02G) is asserted per request on every
  # operation (buildingId required + access-asserted on the report reads;
  # assertBuildingAccess on the corrective action's Building for the
  # verification surface).
  # --------------------------------------------------------------------

  /engineering/reports/technical-summary:
    get:
      operationId: getEngineeringReportTechnicalSummary
      x-required-permission: engineering_report.read
      x-building-scoped: true
      summary: Engineering technical summary for a Building
      description: >-
        BE-10I read. One summary projection (operations, inspections, meter
        readings, equipment logs, checklists, breakdowns, maintenance,
        findings) for a Building. `buildingId` is required and
        access-asserted (BE-02G); optional `assetId` / `dateFrom` / `dateTo`
        / `workforceId` / `vendorId` narrow the window. Requires
        `engineering_report.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: assetId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
          description: ISO date/datetime; day windows are UTC (BE-07 convention), range capped at 366 days.
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
          description: ISO date/datetime; must not precede dateFrom.
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: The engineering technical summary.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/EngineeringTechnicalSummary" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/reports/inspections:
    get:
      operationId: getEngineeringReportInspections
      x-required-permission: engineering_report.read
      x-building-scoped: true
      summary: Engineering inspection dataset for a Building
      description: >-
        BE-10I read. Equipment-inspection binding report rows for a Building.
        `buildingId` is required and access-asserted (BE-02G); optional
        `assetId` / `dateFrom` / `dateTo` / `status` (ACTIVE | INACTIVE) /
        `workforceId` / `vendorId` narrow the dataset. Requires
        `engineering_report.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: assetId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string, enum: [ACTIVE, INACTIVE] }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Inspection dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/EngineeringInspectionReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/reports/meter-readings:
    get:
      operationId: getEngineeringReportMeterReadings
      x-required-permission: engineering_report.read
      x-building-scoped: true
      summary: Engineering meter-reading dataset for a Building
      description: >-
        BE-10I read. Meter-reading binding report rows (BE-10C, with the
        BE-07 UOM and effective min/max range) for a Building. `buildingId`
        is required and access-asserted (BE-02G); optional `assetId` /
        `dateFrom` / `dateTo` / `status` (ACTIVE | INACTIVE) / `uomId` /
        `workforceId` / `vendorId` narrow the dataset. Requires
        `engineering_report.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: assetId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string, enum: [ACTIVE, INACTIVE] }
        - name: uomId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Meter-reading dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/EngineeringMeterReadingReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/reports/equipment-logs:
    get:
      operationId: getEngineeringReportEquipmentLogs
      x-required-permission: engineering_report.read
      x-building-scoped: true
      summary: Engineering equipment-log dataset for a Building
      description: >-
        BE-10I read. Equipment log-sheet binding report rows for a Building.
        `buildingId` is required and access-asserted (BE-02G); optional
        `assetId` / `dateFrom` / `dateTo` / `status` (DRAFT | IN_PROGRESS |
        COMPLETED | CANCELLED) / `workforceId` / `vendorId` narrow the
        dataset. Requires `engineering_report.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: assetId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string, enum: [DRAFT, IN_PROGRESS, COMPLETED, CANCELLED] }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Equipment-log dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/EngineeringEquipmentLogReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/reports/checklists:
    get:
      operationId: getEngineeringReportChecklists
      x-required-permission: engineering_report.read
      x-building-scoped: true
      summary: Engineering checklist dataset for a Building
      description: >-
        BE-10I read. Engineering-checklist binding report rows for a
        Building. `buildingId` is required and access-asserted (BE-02G);
        optional `assetId` / `dateFrom` / `dateTo` / `status` (DRAFT |
        IN_PROGRESS | COMPLETED | CANCELLED) / `workforceId` / `vendorId`
        narrow the dataset. Requires `engineering_report.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: assetId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string, enum: [DRAFT, IN_PROGRESS, COMPLETED, CANCELLED] }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Checklist dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/EngineeringChecklistReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/reports/breakdowns:
    get:
      operationId: getEngineeringReportBreakdowns
      x-required-permission: engineering_report.read
      x-building-scoped: true
      summary: Engineering breakdown dataset for a Building
      description: >-
        BE-10I read. Breakdown report rows (with linked corrective Work
        Order context when present) for a Building. `buildingId` is required
        and access-asserted (BE-02G); optional `assetId` / `dateFrom` /
        `dateTo` / `status` (OPEN | CLOSED) / `workforceId` / `vendorId`
        narrow the dataset. Requires `engineering_report.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: assetId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string, enum: [OPEN, CLOSED] }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Breakdown dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/EngineeringBreakdownReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/reports/maintenance:
    get:
      operationId: getEngineeringReportMaintenance
      x-required-permission: engineering_report.read
      x-building-scoped: true
      summary: Engineering maintenance dataset for a Building
      description: >-
        BE-10I read. Planned-maintenance binding report rows (with linked
        schedule / Work Order context) for a Building. `buildingId` is
        required and access-asserted (BE-02G); optional `assetId` /
        `dateFrom` / `dateTo` / `status` (ACTIVE | INACTIVE) / `workforceId`
        / `vendorId` narrow the dataset. Requires `engineering_report.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: assetId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema: { type: string, enum: [ACTIVE, INACTIVE] }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Maintenance dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/EngineeringMaintenanceReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/reports/findings:
    get:
      operationId: getEngineeringReportFindings
      x-required-permission: engineering_report.read
      x-building-scoped: true
      summary: Engineering finding dataset for a Building
      description: >-
        BE-10I read. Engineering finding report rows (BE-09 `findingId` via
        the BE-10H link, with classification / severity context) for a
        Building. `buildingId` is required and access-asserted (BE-02G);
        optional `assetId` / `dateFrom` / `dateTo` / `status` (BE-09 finding
        statuses) / `sourceType` / `workforceId` / `vendorId` narrow the
        dataset. Requires `engineering_report.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: assetId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: dateFrom
          in: query
          required: false
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          schema: { type: string }
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum:
              [OPEN, ASSIGNED, IN_PROGRESS, PENDING_REVIEW, REJECTED,
               REWORK_REQUIRED, RESUBMITTED, VERIFIED, CLOSED, CANCELLED]
        - name: sourceType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/FindingSourceType" }
        - name: workforceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Finding dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/EngineeringFindingReportRow" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /corrective-action-verifications:
    get:
      operationId: listCorrectiveActionVerifications
      x-required-permission: corrective_action_verification.read
      x-building-scoped: true
      summary: List corrective-action verifications
      description: >-
        BE-21J read. Verification records over corrective actions (each a
        shared BE-07 `reviews` row with target_type CORRECTIVE_ACTION).
        Optional `correctiveActionId` / `incidentId` / `buildingId` /
        `status` (PENDING | COMPLETED) / `decision` (APPROVED | REJECTED |
        REWORK_REQUIRED) filters narrow the list; `buildingId` and
        `incidentId` are access-asserted (BE-02G), and the result is always
        constrained to the caller's accessible Buildings. Requires
        `corrective_action_verification.read`.
      tags: [Corrective Action Verification]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: correctiveActionId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: incidentId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/CorrectiveActionVerificationStatus" }
        - name: decision
          in: query
          required: false
          schema: { $ref: "#/components/schemas/ReviewDecision" }
      responses:
        "200":
          description: Corrective-action verification list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/CorrectiveActionVerification" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /corrective-actions/{id}/verification:
    get:
      operationId: getCorrectiveActionVerificationContext
      x-required-permission: corrective_action_verification.read
      x-building-scoped: true
      summary: Get the corrective-action verification context
      description: >-
        BE-21J read. The backend-authoritative verification context for one
        Corrective Action: its lifecycle status, whether it is verifiable
        (only COMPLETED is), the blockers when it is not, the open (PENDING)
        verification, the latest COMPLETED verification (the authoritative
        result), and whether it is finalized (an APPROVED decision has been
        recorded). Building access is asserted on the action's Building
        (BE-02G). Requires `corrective_action_verification.read`.
      tags: [Corrective Action Verification]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/CorrectiveActionIdPath"
      responses:
        "200":
          description: The verification context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/CorrectiveActionVerificationContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: openCorrectiveActionVerification
      x-required-permission: corrective_action_verification.manage
      x-building-scoped: true
      summary: Open a corrective-action verification
      description: >-
        BE-21J write. Claims the verification for the authenticated reviewer
        (the reviewer is always the session user, never a caller-supplied
        id). Only a COMPLETED Corrective Action is verifiable; one open
        PENDING verification per action (409). Building access is asserted
        on the action's Building (BE-02G). Requires
        `corrective_action_verification.manage`.
      tags: [Corrective Action Verification]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/CorrectiveActionIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/OpenCorrectiveActionVerificationRequest" }
      responses:
        "201":
          description: The opened verification.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/CorrectiveActionVerification" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /corrective-actions/{id}/verification/decision:
    post:
      operationId: submitCorrectiveActionVerification
      x-required-permission: corrective_action_verification.manage
      x-building-scoped: true
      summary: Submit a corrective-action verification decision
      description: >-
        BE-21J write. Records the reviewer decision (APPROVED | REJECTED |
        REWORK_REQUIRED) against the open verification. The decision is
        validated against the SHARED review vocabulary and its lifecycle
        outcome is derived by the backend (APPROVED → VERIFIED,
        REWORK_REQUIRED → IN_PROGRESS, REJECTED → status unchanged); the
        caller can never assert the resulting status. The reviewer is the
        authenticated session user. Building access is asserted on the
        action's Building (BE-02G). Requires
        `corrective_action_verification.manage`.
      tags: [Corrective Action Verification]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/CorrectiveActionIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitCorrectiveActionVerificationRequest" }
      responses:
        "200":
          description: The completed verification.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/CorrectiveActionVerification" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /corrective-actions/{id}/verification/latest:
    get:
      operationId: getLatestCorrectiveActionVerification
      x-required-permission: corrective_action_verification.read
      x-building-scoped: true
      summary: Get the latest completed corrective-action verification
      description: >-
        BE-21J read. The most recent COMPLETED verification (the
        authoritative result) of a Corrective Action, or null when no
        decision has been recorded yet. Building access is asserted on the
        action's Building (BE-02G). Requires
        `corrective_action_verification.read`.
      tags: [Corrective Action Verification]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/CorrectiveActionIdPath"
      responses:
        "200":
          description: The latest verification (null when none recorded).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: object
                        nullable: true
                        allOf:
                          - $ref: "#/components/schemas/CorrectiveActionVerification"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /corrective-actions/{id}/verification/history:
    get:
      operationId: listCorrectiveActionVerificationHistory
      x-required-permission: corrective_action_verification.read
      x-building-scoped: true
      summary: List corrective-action verification history
      description: >-
        BE-21J read. Full verification history of a Corrective Action,
        oldest first — every attempt is preserved, none replaced. Building
        access is asserted on the action's Building (BE-02G). Requires
        `corrective_action_verification.read`.
      tags: [Corrective Action Verification]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/CorrectiveActionIdPath"
      responses:
        "200":
          description: Verification history (oldest first).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/CorrectiveActionVerification" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # --------------------------------------------------------------------
  # CR-BE-MOB-01 PART 02 — Security operational contract.
  #
  # Existing BE-12 (Security) and BE-21A/B (Incident) routes published for
  # mobile. Nothing new is introduced: patrol execution start/complete
  # delegate to the BE-07 task engine, patrol checklist execution stays on
  # BE-07 checklist endpoints, and finding workflow stays on BE-09.
  # --------------------------------------------------------------------

  /buildings/{buildingId}/security-posts:
    post:
      operationId: createSecurityPost
      summary: Create a Security Post in a Building
      description: >-
        BE-12A. A Security Post is Security operational context anchored to
        the BE-04 building structure (Floor / Area / Room / Space /
        Functional Location references only — never a second hierarchy).
        Requires `security_post.manage` and BE-02G Building access.
        `clientId` is derived through Building → Property → Client and is
        never accepted from the caller.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_post.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateSecurityPostRequest" }
      responses:
        "201":
          description: Security Post created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SecurityPost" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildingSecurityPosts
      summary: List Security Posts in a Building
      description: >-
        BE-12A read. Requires `security_post.read` and BE-02G Building
        access. Mobile relevance: duty-post picker and patrol start-post
        context.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_post.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/SecurityPostStatus" }
        - name: postType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/SecurityPostType" }
        - name: floorId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: areaId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: roomId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: spaceId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: functionalLocationId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Security Post list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/SecurityPost" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /security/posts/{id}:
    get:
      operationId: getSecurityPost
      summary: Get a Security Post by id
      description: >-
        BE-12A read. Requires `security_post.read`; the service walks
        Security Post → Building and asserts BE-02G access, so an
        inaccessible post is rejected with 403 `BUILDING_ACCESS_DENIED`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_post.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/SecurityPostIdPath"
      responses:
        "200":
          description: Security Post.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SecurityPost" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateSecurityPost
      summary: Update a Security Post
      description: >-
        BE-12A. Requires `security_post.manage` and Building access.
        `code`, `clientId` and `buildingId` are immutable.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_post.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/SecurityPostIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateSecurityPostRequest" }
      responses:
        "200":
          description: Updated Security Post.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SecurityPost" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/security/patrol-routes:
    post:
      operationId: createPatrolRoute
      summary: Create a Patrol Route in a Building
      description: >-
        BE-12B. A Patrol Route is the ordered patrol definition for one
        Building, optionally starting at a BE-12A Security Post in the same
        Building. It carries no schedule, task, or execution semantics.
        Requires `patrol_route.manage` and BE-02G Building access.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_route.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePatrolRouteRequest" }
      responses:
        "201":
          description: Patrol Route created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PatrolRoute" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildingPatrolRoutes
      summary: List Patrol Routes in a Building
      description: >-
        BE-12B read. Requires `patrol_route.read` and BE-02G Building
        access. Mobile relevance: route picker and checkpoint context.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_route.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Patrol Route list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PatrolRoute" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /security/patrol-routes/{id}:
    get:
      operationId: getPatrolRoute
      summary: Get a Patrol Route by id
      description: >-
        BE-12B read. Requires `patrol_route.read`; Building access is
        asserted through Patrol Route → Building.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_route.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolRouteIdPath"
      responses:
        "200":
          description: Patrol Route.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PatrolRoute" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updatePatrolRoute
      summary: Update a Patrol Route
      description: >-
        BE-12B. Requires `patrol_route.manage`. `code`, `clientId` and
        `buildingId` are immutable; a start post must belong to the same
        Building (`PATROL_ROUTE_START_POST_MISMATCH`).
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_route.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolRouteIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdatePatrolRouteRequest" }
      responses:
        "200":
          description: Updated Patrol Route.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PatrolRoute" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /security/patrol-routes/{id}/points:
    post:
      operationId: addPatrolRoutePoint
      summary: Add a checkpoint (patrol route point) to a Patrol Route
      description: >-
        BE-12B. A patrol route point IS the Security checkpoint: an ordered
        (`sequence`) reference to an existing BE-04 location (Floor / Area /
        Room / Space / Functional Location). There is no separate checkpoint
        master and no checkpoint identifier/QR authority — scan-to-confirm
        remains unavailable (see the QR boundary in CR-BE-MOB-01 PART 05).
        Requires `patrol_route.manage`; duplicate sequences are rejected with
        `PATROL_ROUTE_POINT_DUPLICATE_SEQUENCE`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_route.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolRouteIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePatrolRoutePointRequest"
      responses:
        "201":
          description: Checkpoint added.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PatrolRoutePoint" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listPatrolRoutePoints
      summary: List the checkpoints of a Patrol Route
      description: >-
        BE-12B read, ordered by `sequence`. Requires `patrol_route.read`.
        Mobile relevance: the checkpoint list a guard must confirm during a
        patrol execution.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_route.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolRouteIdPath"
      responses:
        "200":
          description: Checkpoint list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/PatrolRoutePoint"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /security/patrol-route-points/{id}:
    patch:
      operationId: updatePatrolRoutePoint
      summary: Update a checkpoint (patrol route point)
      description: >-
        BE-12B. Requires `patrol_route.manage`. The point stays inside its
        route's Building (`PATROL_ROUTE_POINT_LOCATION_MISMATCH`).
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_route.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolRoutePointIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePatrolRoutePointRequest"
      responses:
        "200":
          description: Updated checkpoint.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PatrolRoutePoint" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /security/patrol-routes/{id}/schedule-bindings:
    post:
      operationId: createPatrolScheduleBinding
      summary: Bind a Patrol Route to a BE-07 Schedule Definition
      description: >-
        BE-12C. The binding is the patrol *assignment of work over time*:
        it links a Patrol Route to a shared BE-07 Schedule Definition, which
        generates the tasks that become Patrol Executions. Recurrence,
        occurrence preview and task generation stay in BE-07 — this module
        never re-implements them. Assignment of a generated patrol to a
        workforce profile or team is the published BE-07 task assignment
        surface (`POST /tasks/{taskId}/assignments`), where `taskId` is the
        Patrol Execution id. Requires `patrol_schedule.manage`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_schedule.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolRouteIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePatrolScheduleBindingRequest"
      responses:
        "201":
          description: Patrol Schedule Binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PatrolScheduleBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listPatrolRouteScheduleBindings
      summary: List the Schedule Bindings of a Patrol Route
      description: >-
        BE-12C read. Requires `patrol_schedule.read`. Each row resolves the
        BE-07 schedule and recurrence context without copying it.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_schedule.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolRouteIdPath"
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/PatrolScheduleBindingStatus"
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: patrolRouteId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: scheduleDefinitionId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Patrol Schedule Binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/PatrolScheduleBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /security/patrol-schedule-bindings/{id}:
    get:
      operationId: getPatrolScheduleBinding
      summary: Get a Patrol Schedule Binding by id
      description: >-
        BE-12C read. Requires `patrol_schedule.read`; Building access is
        asserted through the binding's Building.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_schedule.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolScheduleBindingIdPath"
      responses:
        "200":
          description: Patrol Schedule Binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PatrolScheduleBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updatePatrolScheduleBinding
      summary: Update a Patrol Schedule Binding
      description: >-
        BE-12C. Requires `patrol_schedule.manage`. Only the Security-side
        binding fields are mutable; the BE-07 Schedule Definition is edited
        through BE-07.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_schedule.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolScheduleBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePatrolScheduleBindingRequest"
      responses:
        "200":
          description: Updated Patrol Schedule Binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PatrolScheduleBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/security/patrol-executions:
    get:
      operationId: listBuildingPatrolExecutions
      summary: List Patrol Executions for a Building
      description: >-
        BE-12D read. A Patrol Execution is the Security view of an
        authoritative BE-07 generated task bound to a Patrol Route through a
        BE-12C Schedule Binding; `id` equals the BE-07 `taskId` and `status`
        is the shared task status (no second lifecycle). Requires
        `patrol_execution.read` and BE-02G Building access. Mobile
        relevance: the guard's patrol work list.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_execution.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: date
          in: query
          required: false
          schema: { type: string, format: date }
          description: Operational date YYYY-MM-DD.
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/PatrolExecutionStatus" }
        - name: patrolRouteId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Patrol Execution list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/PatrolExecution"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /security/patrol-executions/{id}:
    get:
      operationId: getPatrolExecution
      summary: Get a Patrol Execution by id
      description: >-
        BE-12D read. `id` is the BE-07 task id. `pointProgress` is derived
        from `patrol_point_visits` versus the route's ACTIVE checkpoints and
        is backend-authoritative. Requires `patrol_execution.read`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_execution.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolExecutionIdPath"
      responses:
        "200":
          description: Patrol Execution.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PatrolExecution" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /security/patrol-executions/{id}/start:
    post:
      operationId: startPatrolExecution
      summary: Start a Patrol Execution
      description: >-
        BE-12D. Delegates to the shared BE-07 task-execution engine, which
        owns the transition (`OPEN|ASSIGNED → IN_PROGRESS`) and rejects an
        illegal one. Prefer this over the generic `startTask` on a patrol so
        the Security route/binding checks run. Requires
        `patrol_execution.manage`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_execution.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolExecutionIdPath"
      responses:
        "200":
          description: Started Patrol Execution.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PatrolExecution" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /security/patrol-executions/{id}/points/{pointId}/visit:
    post:
      operationId: recordPatrolPointVisit
      summary: Confirm a checkpoint during a Patrol Execution
      description: >-
        BE-12D. Records a `patrol_point_visits` row (`VISITED`) for the
        checkpoint `pointId` of the execution's route. This is the
        checkpoint-confirmation authority; confirmation is by canonical
        `pointId`, NOT by QR scan (no checkpoint identifier authority
        exists). Requires `patrol_execution.manage`; the execution must be
        IN_PROGRESS and the point must belong to the execution's route.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_execution.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolExecutionIdPath"
        - $ref: "#/components/parameters/PatrolPointIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordPatrolPointVisitRequest"
      responses:
        "201":
          description: Checkpoint visit recorded.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PatrolPointVisit" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /security/patrol-executions/{id}/points:
    get:
      operationId: listPatrolPointVisits
      summary: List checkpoint visits of a Patrol Execution
      description: >-
        BE-12D read of `patrol_point_visits` for the execution, ordered by
        checkpoint `sequence`. Requires `patrol_execution.read`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_execution.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolExecutionIdPath"
      responses:
        "200":
          description: Checkpoint visit list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/PatrolPointVisit"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /security/patrol-executions/{id}/complete:
    post:
      operationId: completePatrolExecution
      summary: Complete a Patrol Execution
      description: >-
        BE-12D. Applies the Security checkpoint-progress rules and then
        delegates the status transition to the shared BE-07 task-execution
        engine. Mobile must call this rather than the generic `completeTask`
        on a patrol, otherwise the patrol-specific checks are bypassed.
        Requires `patrol_execution.manage`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_execution.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolExecutionIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CompletePatrolExecutionRequest"
      responses:
        "200":
          description: Completed Patrol Execution.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PatrolExecution" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /security/patrol-point-visits/{id}:
    patch:
      operationId: updatePatrolPointVisit
      summary: Update the notes of a checkpoint visit
      description: >-
        BE-12D. Only `notes` are mutable — the visit time and actor are
        history. Requires `patrol_execution.manage`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_execution.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolPointVisitIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePatrolPointVisitRequest"
      responses:
        "200":
          description: Updated checkpoint visit.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PatrolPointVisit" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /security/patrol-checklist-bindings:
    post:
      operationId: createPatrolChecklistBinding
      summary: Bind a BE-07 Checklist Template to a Patrol Route
      description: >-
        BE-12E. The checklist linkage for Security: a join row associating a
        Checklist Template with a Patrol Route (+ optional start post) in one
        Building. Checklist execution itself stays BE-07 — responses,
        completion, evidence and verification use the published checklist
        and evidence operations. Requires
        `patrol_checklist_binding.manage`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_checklist_binding.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePatrolChecklistBindingRequest"
      responses:
        "201":
          description: Patrol Checklist Binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PatrolChecklistBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listPatrolChecklistBindings
      summary: List Patrol Checklist Bindings
      description: >-
        BE-12E read, constrained to the caller's accessible Buildings.
        Requires `patrol_checklist_binding.read`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_checklist_binding.read
      x-building-scoped: true
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: patrolRouteId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: checklistTemplateId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/PatrolChecklistBindingStatus"
      responses:
        "200":
          description: Patrol Checklist Binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/PatrolChecklistBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /security/patrol-checklist-bindings/{id}:
    get:
      operationId: getPatrolChecklistBinding
      summary: Get a Patrol Checklist Binding by id
      description: >-
        BE-12E read. Requires `patrol_checklist_binding.read` and Building
        access on the binding's Building.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_checklist_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolChecklistBindingIdPath"
      responses:
        "200":
          description: Patrol Checklist Binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PatrolChecklistBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updatePatrolChecklistBinding
      summary: Update a Patrol Checklist Binding
      description: >-
        BE-12E. Only the Security binding fields are mutable; the Checklist
        Template is edited through BE-07. Requires
        `patrol_checklist_binding.manage`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_checklist_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolChecklistBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePatrolChecklistBindingRequest"
      responses:
        "200":
          description: Updated Patrol Checklist Binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PatrolChecklistBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /security/patrol-checklist-bindings/{id}/start:
    post:
      operationId: startPatrolChecklistExecution
      summary: Start the BE-07 checklist execution for a patrol binding
      description: >-
        BE-12E. Creates the shared BE-07 checklist execution row for the
        binding and returns it. No request body. Everything after this —
        saving responses, completing or cancelling, attaching evidence,
        supervisor verification — uses the published BE-07 checklist,
        evidence and verification operations on the returned execution
        `id`. Requires `patrol_checklist_binding.manage`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_checklist_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolChecklistBindingIdPath"
      responses:
        "201":
          description: Shared BE-07 checklist execution started.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PatrolChecklistExecution"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /security/patrol-checklist-executions/{id}:
    get:
      operationId: getPatrolChecklistExecutionContext
      summary: Resolve the Security context of a patrol checklist execution
      description: >-
        BE-12E read. Projects the authoritative Building / Template / Patrol
        Route / start post for a BE-07 checklist execution that was started
        from a patrol binding. The execution state itself stays with BE-07.
        Requires `patrol_checklist_binding.read`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: patrol_checklist_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/PatrolChecklistExecutionIdPath"
      responses:
        "200":
          description: Patrol checklist execution context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/PatrolChecklistExecutionContext"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/security/daily-activity:
    get:
      operationId: getSecurityDailyActivity
      summary: Security daily activity read model for a Building
      description: >-
        BE-12F. A per-Building, per-operational-date composition of existing
        authoritative records (BE-12A/B/C/D/E + BE-07 + BE-09). It creates no
        state: it is the Security field-reporting *read* surface. Each open
        finding carries BE-09's backend-authoritative `availableActions`.
        Requires `security_daily_activity.read` and BE-02G Building access.
        `date` defaults to the current operational date when omitted.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_daily_activity.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: date
          in: query
          required: false
          schema: { type: string, format: date }
          description: Operational date YYYY-MM-DD.
        - name: shiftId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: securityPostId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Security daily activity.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/SecurityDailyActivity"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /security/findings:
    post:
      operationId: createSecurityFinding
      summary: Report a Security finding (creates the linked BE-09 Finding)
      description: >-
        BE-12H. The Security field-reporting write for an issue observed on
        a Security source (`PATROL_EXECUTION`, `PATROL_CHECKLIST`,
        `SECURITY_DAILY_ACTIVITY`, `SHIFT_HANDOVER`, `SECURITY_POST`). It
        creates the authoritative BE-09 Finding and records the Security
        context link — it does not fork Finding workflow. Classification,
        severity, assignment, state transitions, review, rework, closure and
        `availableActions` all remain on the published BE-09 finding
        operations, keyed by `findingId` (NOT by this link's `id`).
        Requires `security_finding.manage`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_finding.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSecurityFindingRequest"
      responses:
        "201":
          description: Security finding link + authoritative BE-09 Finding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SecurityFinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listSecurityFindings
      summary: List Security findings
      description: >-
        BE-12H read, constrained to the caller's accessible Buildings. Each
        row embeds the authoritative BE-09 Finding and its per-caller
        `availableActions`. Requires `security_finding.read`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_finding.read
      x-building-scoped: true
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: startSecurityPostId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: patrolRouteId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: sourceType
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/SecurityFindingSourceType"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/FindingStatus" }
      responses:
        "200":
          description: Security finding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/SecurityFinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /security/findings/{id}:
    get:
      operationId: getSecurityFinding
      summary: Get a Security finding link by id
      description: >-
        BE-12H read. `{id}` is the Security link id; use the embedded
        `findingId` for every BE-09 workflow operation. Requires
        `security_finding.read`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_finding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/SecurityFindingIdPath"
      responses:
        "200":
          description: Security finding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SecurityFinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /incidents:
    post:
      operationId: createIncident
      summary: Report an Incident (shared BE-21A foundation)
      description: >-
        BE-21A. ONE Incident foundation serves Operational Incident,
        Asset Failure and Finding Escalation, distinguished only by
        `incidentType`. For a standalone Security event, prefer
        `createOperationalIncident` with `operationalCategory: SECURITY`,
        which pins `incidentType` to OPERATIONAL and adds the operational
        handling status. `clientId` is derived from the Building and
        `reportedByUserId` is the authenticated actor — neither is accepted
        from the caller. Requires `incident.manage` and Building access.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: incident.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateIncidentRequest" }
      responses:
        "201":
          description: Incident reported.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Incident" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listIncidents
      summary: List Incidents
      description: >-
        BE-21A read, constrained to the caller's accessible Buildings.
        Requires `incident.read`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: incident.read
      x-building-scoped: true
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: incidentType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/IncidentType" }
        - name: severity
          in: query
          required: false
          schema: { $ref: "#/components/schemas/IncidentSeverity" }
        - name: priority
          in: query
          required: false
          schema: { $ref: "#/components/schemas/IncidentPriority" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/IncidentStatus" }
      responses:
        "200":
          description: Incident list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Incident" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /incidents/{id}:
    get:
      operationId: getIncident
      summary: Get an Incident by id
      description: BE-21A read. Requires `incident.read` and Building access.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: incident.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/IncidentIdPath"
      responses:
        "200":
          description: Incident.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Incident" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateIncident
      summary: Update descriptive Incident metadata
      description: >-
        BE-21A. Only `title`, `description`, `severity` and `priority` are
        mutable; type, context, number and reporter are the identity of the
        record. Requires `incident.manage`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: incident.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/IncidentIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateIncidentRequest" }
      responses:
        "200":
          description: Updated Incident.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Incident" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /incidents/{id}/cancel:
    post:
      operationId: cancelIncident
      summary: Cancel (withdraw) an Incident
      description: >-
        BE-21A. `CANCELLED` is terminal and mutually exclusive with the
        BE-21K `CLOSED` end state: a withdrawn incident was never resolved.
        Requires `incident.manage`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: incident.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/IncidentIdPath"
      responses:
        "200":
          description: Cancelled Incident.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Incident" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /operational-incidents:
    post:
      operationId: createOperationalIncident
      summary: Report an Operational Incident (Security field report)
      description: >-
        BE-21B. The authoritative field-report write for a standalone
        operational event; use `operationalCategory: SECURITY` (or `SAFETY`
        / `ACCESS`) for Security. It is a specialization of the BE-21A
        foundation with `incidentType` pinned to OPERATIONAL — identity,
        Building/Client context, location, severity, priority and reporter
        stay in BE-21A and are never stored twice. There is no separate
        "occurrence" domain: a patrol/checklist issue becomes a Security
        finding (`createSecurityFinding`), a standalone event becomes an
        Operational Incident. Requires `operational_incident.manage`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: operational_incident.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOperationalIncidentRequest"
      responses:
        "201":
          description: Operational Incident reported.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/OperationalIncident"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listOperationalIncidents
      summary: List Operational Incidents
      description: >-
        BE-21B read, constrained to the caller's accessible Buildings.
        Requires `operational_incident.read`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: operational_incident.read
      x-building-scoped: true
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: locationType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/IncidentLocationType" }
        - name: locationId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: operationalCategory
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/OperationalIncidentCategory"
        - name: severity
          in: query
          required: false
          schema: { $ref: "#/components/schemas/IncidentSeverity" }
        - name: priority
          in: query
          required: false
          schema: { $ref: "#/components/schemas/IncidentPriority" }
        - name: operationalStatus
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/OperationalIncidentStatus"
        - name: incidentStatus
          in: query
          required: false
          schema: { $ref: "#/components/schemas/IncidentStatus" }
        - name: occurredFrom
          in: query
          required: false
          schema: { type: string, format: date-time }
        - name: occurredTo
          in: query
          required: false
          schema: { type: string, format: date-time }
      responses:
        "200":
          description: Operational Incident list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/OperationalIncident"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /operational-incidents/{id}:
    get:
      operationId: getOperationalIncident
      summary: Get an Operational Incident by id
      description: >-
        BE-21B read. `id` is the shared BE-21A Incident id;
        `operationalIncidentId` is the internal specialization id.
        `availableActions` is backend-resolved per request from state ×
        permission × foundation lifecycle and must never be recomputed by
        the client. Requires `operational_incident.read`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: operational_incident.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/OperationalIncidentIdPath"
      responses:
        "200":
          description: Operational Incident.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/OperationalIncident"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateOperationalIncident
      summary: Update an Operational Incident (incl. handling status)
      description: >-
        BE-21B. Foundation metadata and operational fields are accepted on
        one request and applied atomically. `operationalStatus` follows the
        closed transition table (OPEN → IN_PROGRESS|RESOLVED, IN_PROGRESS →
        RESOLVED|OPEN, RESOLVED → IN_PROGRESS); an illegal transition is
        rejected. `RESOLVED` means operational handling finished — NOT
        verified or closed. Requires `operational_incident.manage`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: operational_incident.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/OperationalIncidentIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateOperationalIncidentRequest"
      responses:
        "200":
          description: Updated Operational Incident.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/OperationalIncident"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  # --------------------------------------------------------------------
  # CR-BE-MOB-01 PART 03 — Engineering field / reading contract.
  #
  # Existing BE-10 routes published for mobile. Nothing new is introduced:
  # every binding is a join row over an existing authority. Executions are
  # BE-07 checklist executions / form instances, PM & corrective execution
  # is the published BE-08 Work Order action set, evidence is the published
  # BE-07 evidence API on the returned execution id, and finding workflow
  # stays on BE-09 keyed by findingId.
  # --------------------------------------------------------------------

  /assets/{assetId}/inspection-bindings:
    post:
      operationId: createInspectionBinding
      summary: Bind a BE-07 Checklist Template to an Asset (equipment inspection)
      description: >-
        BE-10B. The equipment-inspection binding is a join row between a BE-05
        Asset and a BE-07 Checklist Template, optionally refined by a BE-04
        Functional Location. It is not an inspection engine and duplicates no
        Asset / Checklist / Evidence / Verification data. `clientId` and
        `buildingId` are derived from the Asset. Requires
        `inspection_binding.manage` and BE-02G Building access on the Asset's
        Building.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: inspection_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateInspectionBindingRequest"
      responses:
        "201":
          description: Inspection binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/InspectionBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listAssetInspectionBindings
      summary: List the equipment-inspection bindings of an Asset
      description: >-
        BE-10B read. Requires `inspection_binding.read` and Building access on
        the Asset's Building. Mobile relevance: which inspection checklists
        apply to the scanned/selected equipment.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: inspection_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: Inspection binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/InspectionBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/engineering/inspection-bindings:
    get:
      operationId: listBuildingInspectionBindings
      summary: List equipment-inspection bindings in a Building
      description: >-
        BE-10B read. Requires `inspection_binding.read` and BE-02G Building
        access.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: inspection_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Inspection binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/InspectionBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/inspection-bindings/{id}:
    get:
      operationId: getInspectionBinding
      summary: Get an equipment-inspection binding by id
      description: >-
        BE-10B read. Requires `inspection_binding.read`; Building access is
        asserted through the binding's Building.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: inspection_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/InspectionBindingIdPath"
      responses:
        "200":
          description: Inspection binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/InspectionBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateInspectionBinding
      summary: Update an equipment-inspection binding
      description: >-
        BE-10B. Only the Functional Location refinement and the binding status
        are mutable; the Asset and Checklist Template are the identity of the
        binding. Requires `inspection_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: inspection_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/InspectionBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateInspectionBindingRequest"
      responses:
        "200":
          description: Updated inspection binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/InspectionBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /engineering/inspection-bindings/{id}/start:
    post:
      operationId: startInspectionExecution
      summary: Start the BE-07 checklist execution for an inspection binding
      description: >-
        BE-10B. Creates the shared BE-07 checklist execution for the binding
        and returns it. No request body. Everything after this — saving
        responses, completing or cancelling, attaching evidence and supervisor
        verification — uses the published BE-07 checklist, evidence
        (`executionType: CHECKLIST_EXECUTION`) and verification operations on
        the returned execution `id`. Requires `inspection_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: inspection_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/InspectionBindingIdPath"
      responses:
        "201":
          description: Shared BE-07 checklist execution started.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/InspectionExecution"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /engineering/inspection-executions/{id}:
    get:
      operationId: getInspectionExecutionContext
      summary: Resolve the Engineering context of an inspection execution
      description: >-
        BE-10B read. Projects the authoritative Asset / Building / Functional
        Location for a BE-07 checklist execution started from an inspection
        binding. The execution state itself stays with BE-07. Requires
        `inspection_binding.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: inspection_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/InspectionExecutionIdPath"
      responses:
        "200":
          description: Inspection execution context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/InspectionExecutionContext"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /assets/{assetId}/meter-reading-bindings:
    post:
      operationId: createMeterReadingBinding
      summary: Bind a numeric BE-07 Form Field + UOM to an Asset (meter reading)
      description: >-
        BE-10C. The technical meter-reading definition for a piece of
        equipment: a join row between a BE-05 Asset, a numeric BE-07 Form
        Field, a BE-07 UOM and an optional BE-04 Functional Location, with an
        optional binding-level tightening of the field's measurement range.
        Readings are written into BE-07's own `form_responses` through a shared
        Form Instance — there is no second measurement store. NOTE: this is the
        *engineering/technical* meter authority; BE-18 utility meters are a
        separate billing-oriented authority and are not published here.
        Requires `meter_reading_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: meter_reading_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateMeterReadingBindingRequest"
      responses:
        "201":
          description: Meter reading binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MeterReadingBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listAssetMeterReadingBindings
      summary: List the meter-reading bindings of an Asset
      description: >-
        BE-10C read. Requires `meter_reading_binding.read` and Building access
        on the Asset's Building. Mobile relevance: which readings a technician
        must take on this equipment, with the authoritative UOM and range.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: meter_reading_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: Meter reading binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/MeterReadingBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/engineering/meter-reading-bindings:
    get:
      operationId: listBuildingMeterReadingBindings
      summary: List meter-reading bindings in a Building
      description: >-
        BE-10C read. Requires `meter_reading_binding.read` and BE-02G Building
        access.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: meter_reading_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Meter reading binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/MeterReadingBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/meter-reading-bindings/{id}:
    get:
      operationId: getMeterReadingBinding
      summary: Get a meter-reading binding by id
      description: >-
        BE-10C read. Requires `meter_reading_binding.read`; Building access is
        asserted through the binding's Building.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: meter_reading_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MeterReadingBindingIdPath"
      responses:
        "200":
          description: Meter reading binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MeterReadingBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateMeterReadingBinding
      summary: Update a meter-reading binding
      description: >-
        BE-10C. UOM, location refinement, measurement range and status are
        mutable; the Asset and Form Field are the identity of the binding. The
        range must stay inside the BE-07 field's configured range. Requires
        `meter_reading_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: meter_reading_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MeterReadingBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateMeterReadingBindingRequest"
      responses:
        "200":
          description: Updated meter reading binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MeterReadingBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /engineering/meter-reading-bindings/{id}/start:
    post:
      operationId: startMeterReadingExecution
      summary: Start the BE-07 form instance for a meter-reading binding
      description: >-
        BE-10C. Creates the shared BE-07 form instance that will carry the
        reading. No request body. Requires `meter_reading_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: meter_reading_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MeterReadingBindingIdPath"
      responses:
        "201":
          description: Shared BE-07 form instance started.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MeterReadingExecution"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /engineering/meter-reading-executions/{id}/reading:
    put:
      operationId: submitMeterReading
      summary: Submit the technical reading value for a reading execution
      description: >-
        BE-10C. Idempotent upsert of the reading into BE-07's `form_responses`
        row for the execution's numeric field. The value is validated against
        the effective measurement range (binding range tightened over the BE-07
        field range) and stored with the binding's UOM. This is the only
        reading write — there is no second measurement engine and no
        per-reading review workflow (review a completed reading through the
        published BE-07 review / mobile verification operations on the form
        instance). Requires `meter_reading_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: meter_reading_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MeterReadingExecutionIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubmitMeterReadingRequest"
      responses:
        "200":
          description: Submitted reading.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/MeterReading" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /engineering/meter-reading-executions/{id}:
    get:
      operationId: getMeterReadingContext
      summary: Resolve the context and current value of a reading execution
      description: >-
        BE-10C read. Projects the authoritative Asset / Building / Functional
        Location / UOM, the effective measurement range and the current stored
        value. Derived from the binding and the BE-07 stores. Requires
        `meter_reading_binding.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: meter_reading_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MeterReadingExecutionIdPath"
      responses:
        "200":
          description: Meter reading context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MeterReadingContext"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /assets/{assetId}/log-sheet-bindings:
    post:
      operationId: createLogSheetBinding
      summary: Bind a BE-07 Form Template to an Asset (equipment log sheet)
      description: >-
        BE-10D. The equipment log-sheet definition: a join row between a BE-05
        Asset, a BE-07 Form Template, an optional published Template Version
        snapshot and an optional BE-04 Functional Location. Log-sheet rows are
        BE-07 Form Instances; responses live in `form_responses`. Requires
        `log_sheet_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: log_sheet_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateLogSheetBindingRequest"
      responses:
        "201":
          description: Log sheet binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/LogSheetBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listAssetLogSheetBindings
      summary: List the log-sheet bindings of an Asset
      description: >-
        BE-10D read. Requires `log_sheet_binding.read` and Building access on
        the Asset's Building.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: log_sheet_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: Log sheet binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/LogSheetBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/engineering/log-sheet-bindings:
    get:
      operationId: listBuildingLogSheetBindings
      summary: List log-sheet bindings in a Building
      description: >-
        BE-10D read. Requires `log_sheet_binding.read` and BE-02G Building
        access.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: log_sheet_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Log sheet binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/LogSheetBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/log-sheet-bindings/{id}:
    get:
      operationId: getLogSheetBinding
      summary: Get a log-sheet binding by id
      description: >-
        BE-10D read. Requires `log_sheet_binding.read`; Building access is
        asserted through the binding's Building.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: log_sheet_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/LogSheetBindingIdPath"
      responses:
        "200":
          description: Log sheet binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/LogSheetBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateLogSheetBinding
      summary: Update a log-sheet binding
      description: >-
        BE-10D. Template version pin, location refinement and status are
        mutable; the Asset and Form Template are the identity of the binding.
        Requires `log_sheet_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: log_sheet_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/LogSheetBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateLogSheetBindingRequest"
      responses:
        "200":
          description: Updated log sheet binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/LogSheetBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /engineering/log-sheet-bindings/{id}/start:
    post:
      operationId: startLogSheetExecution
      summary: Start a BE-07 form instance for a log-sheet binding
      description: >-
        BE-10D. Creates one shared BE-07 form instance (a log-sheet row). No
        request body. Responses are written through the published BE-07 form
        operations. Requires `log_sheet_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: log_sheet_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/LogSheetBindingIdPath"
      responses:
        "201":
          description: Shared BE-07 form instance started.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/LogSheetExecution" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /engineering/log-sheet-bindings/{id}/executions:
    get:
      operationId: listLogSheetExecutions
      summary: List the log-sheet rows (form instances) of a binding
      description: >-
        BE-10D read, newest first. Requires `log_sheet_binding.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: log_sheet_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/LogSheetBindingIdPath"
      responses:
        "200":
          description: Log sheet execution list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/LogSheetExecution"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /engineering/log-sheet-executions/{id}:
    get:
      operationId: getLogSheetExecutionContext
      summary: Resolve the Engineering context of a log-sheet row
      description: >-
        BE-10D read. Projects the authoritative Asset / Building / Functional
        Location plus the BE-07 template and version the row is tied to.
        Requires `log_sheet_binding.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: log_sheet_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/LogSheetExecutionIdPath"
      responses:
        "200":
          description: Log sheet execution context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/LogSheetExecutionContext"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /engineering/checklist-bindings:
    post:
      operationId: createEngineeringChecklistBinding
      summary: Bind a BE-07 Checklist Template to an Engineering context
      description: >-
        BE-10E. A join row between a BE-07 Checklist Template and a Building
        plus an optional BE-05 Asset and/or BE-04 Functional Location target
        inside that Building. Executions stay BE-07 checklist executions.
        Requires `engineering_checklist_binding.manage` and Building access.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: engineering_checklist_binding.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateEngineeringChecklistBindingRequest"
      responses:
        "201":
          description: Engineering checklist binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringChecklistBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listEngineeringChecklistBindings
      summary: List Engineering checklist bindings
      description: >-
        BE-10E read, constrained to the caller's accessible Buildings.
        Requires `engineering_checklist_binding.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: engineering_checklist_binding.read
      x-building-scoped: true
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: assetId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Engineering checklist binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/EngineeringChecklistBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/checklist-bindings/{id}:
    get:
      operationId: getEngineeringChecklistBinding
      summary: Get an Engineering checklist binding by id
      description: >-
        BE-10E read. Requires `engineering_checklist_binding.read` and Building
        access on the binding's Building.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: engineering_checklist_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/EngineeringChecklistBindingIdPath"
      responses:
        "200":
          description: Engineering checklist binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringChecklistBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateEngineeringChecklistBinding
      summary: Update an Engineering checklist binding
      description: >-
        BE-10E. Asset / Functional Location targets and status are mutable; the
        Building and Checklist Template are the identity of the binding.
        Requires `engineering_checklist_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: engineering_checklist_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/EngineeringChecklistBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateEngineeringChecklistBindingRequest"
      responses:
        "200":
          description: Updated Engineering checklist binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringChecklistBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /engineering/checklist-bindings/{id}/start:
    post:
      operationId: startEngineeringChecklistExecution
      summary: Start the BE-07 checklist execution for an Engineering binding
      description: >-
        BE-10E. Creates the shared BE-07 checklist execution for the binding
        and returns it. No request body. Responses, completion, evidence and
        verification stay on the published BE-07 operations. Requires
        `engineering_checklist_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: engineering_checklist_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/EngineeringChecklistBindingIdPath"
      responses:
        "201":
          description: Shared BE-07 checklist execution started.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringChecklistExecution"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /engineering/checklist-executions/{id}:
    get:
      operationId: getEngineeringChecklistExecutionContext
      summary: Resolve the Engineering context of a checklist execution
      description: >-
        BE-10E read. Projects the authoritative Building / Template plus the
        bound Asset / Functional Location targets. Requires
        `engineering_checklist_binding.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: engineering_checklist_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/EngineeringChecklistExecutionIdPath"
      responses:
        "200":
          description: Engineering checklist execution context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringChecklistExecutionContext"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /assets/{assetId}/breakdowns:
    post:
      operationId: createBreakdown
      summary: Report an equipment breakdown on an Asset
      description: >-
        BE-10F. Records the breakdown/corrective event against a BE-05 Asset.
        It is the entry point of corrective maintenance; the corrective work
        itself is a BE-08 Work Order linked through
        `linkBreakdownCorrectiveWorkOrder`, and that Work Order's lifecycle,
        assignment and verification remain BE-08's. Requires
        `breakdown.manage` and Building access on the Asset's Building.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: breakdown.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateBreakdownRequest" }
      responses:
        "201":
          description: Breakdown recorded.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/BreakdownBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listAssetBreakdowns
      summary: List the breakdowns of an Asset
      description: >-
        BE-10F read. Each row projects the current BE-08 status of its linked
        corrective Work Order (never stored). Requires `breakdown.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: breakdown.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: Breakdown list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/BreakdownBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/engineering/breakdowns:
    get:
      operationId: listBuildingBreakdowns
      summary: List breakdowns in a Building
      description: >-
        BE-10F read. Requires `breakdown.read` and BE-02G Building access.
        Mobile relevance: the corrective-maintenance backlog for the site.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: breakdown.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Breakdown list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/BreakdownBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/breakdowns/{id}:
    get:
      operationId: getBreakdown
      summary: Get a breakdown by id
      description: >-
        BE-10F read. Requires `breakdown.read`; Building access is asserted
        through the breakdown's Building.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: breakdown.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BreakdownIdPath"
      responses:
        "200":
          description: Breakdown.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/BreakdownBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: closeBreakdown
      summary: Close a breakdown
      description: >-
        BE-10F. `status: CLOSED` is the only accepted transition — the
        breakdown record has no other lifecycle, and closing it does not close
        the corrective Work Order (that stays a BE-08 decision). Requires
        `breakdown.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: breakdown.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BreakdownIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CloseBreakdownRequest" }
      responses:
        "200":
          description: Closed breakdown.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/BreakdownBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /engineering/breakdowns/{id}/work-order:
    post:
      operationId: linkBreakdownCorrectiveWorkOrder
      summary: Link or create the corrective Work Order for a breakdown
      description: >-
        BE-10F. Either links an existing BE-08 Work Order (`workOrderId`) or
        asks the shared BE-08 service to create one (`title`). The corrective
        execution itself — acknowledge / start / hold / resume / note /
        complete, assignment, evidence and verification — is the already
        published BE-08 Work Order surface. This endpoint only records the
        link. Requires `breakdown.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: breakdown.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BreakdownIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LinkCorrectiveWorkOrderRequest"
      responses:
        "201":
          description: Breakdown with its linked corrective Work Order.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/BreakdownBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /assets/{assetId}/maintenance-bindings:
    post:
      operationId: createMaintenanceBinding
      summary: Create a planned-maintenance binding for an Asset
      description: >-
        BE-10G. The PM context for a BE-05 Asset (`maintenanceType`:
        PREVENTIVE / PREDICTIVE / CONDITION_BASED / CALIBRATION). The binding
        does not execute work: it references a shared BE-07 Schedule
        Definition, the BE-07 generated Tasks and an optional BE-08 Work
        Order. Scheduling, task generation, checklist, evidence, assignment,
        finding and verification all remain owned by BE-07 / BE-08 / BE-09.
        Requires `maintenance_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: maintenance_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateMaintenanceBindingRequest"
      responses:
        "201":
          description: Maintenance binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MaintenanceBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listAssetMaintenanceBindings
      summary: List the maintenance bindings of an Asset
      description: >-
        BE-10G read. Each row projects the linked BE-07 schedule, the newest
        BE-07 generated tasks and the BE-08 Work Order status (never stored).
        Requires `maintenance_binding.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: maintenance_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
      responses:
        "200":
          description: Maintenance binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/MaintenanceBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/engineering/maintenance-bindings:
    get:
      operationId: listBuildingMaintenanceBindings
      summary: List maintenance bindings in a Building
      description: >-
        BE-10G read. Requires `maintenance_binding.read` and BE-02G Building
        access. Mobile relevance: PM context for the site's equipment.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: maintenance_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200":
          description: Maintenance binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/MaintenanceBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/maintenance-bindings/{id}:
    get:
      operationId: getMaintenanceBinding
      summary: Get a maintenance binding by id
      description: >-
        BE-10G read. Requires `maintenance_binding.read`; Building access is
        asserted through the binding's Building.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: maintenance_binding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MaintenanceBindingIdPath"
      responses:
        "200":
          description: Maintenance binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MaintenanceBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateMaintenanceBinding
      summary: Update a maintenance binding
      description: >-
        BE-10G. Descriptive fields, maintenance type, location refinement and
        status are mutable; the Asset is the identity of the binding.
        Requires `maintenance_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: maintenance_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MaintenanceBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateMaintenanceBindingRequest"
      responses:
        "200":
          description: Updated maintenance binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MaintenanceBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /engineering/maintenance-bindings/{id}/schedule:
    post:
      operationId: linkMaintenanceSchedule
      summary: Link (or create) the BE-07 Schedule of a maintenance binding
      description: >-
        BE-10G. Either links an existing `scheduleDefinitionId` or asks the
        shared BE-07 scheduler to create one. Recurrence and task generation
        are BE-07's — this endpoint never re-implements them. Requires
        `maintenance_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: maintenance_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MaintenanceBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LinkMaintenanceScheduleRequest"
      responses:
        "200":
          description: Maintenance binding with its linked schedule.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MaintenanceBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /engineering/maintenance-bindings/{id}/task:
    post:
      operationId: linkMaintenanceTask
      summary: Link a BE-07 generated Task to a maintenance binding
      description: >-
        BE-10G. Records that an existing BE-07 generated task belongs to this
        PM binding. Execution of that task is the published BE-07 task surface
        (`startTask` / `completeTask` / `cancelTask`, assignment via
        `assignTask`); the id in this request is the authoritative `taskId`.
        Requires `maintenance_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: maintenance_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MaintenanceBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LinkMaintenanceTaskRequest"
      responses:
        "200":
          description: Maintenance binding with its linked tasks.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MaintenanceBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /engineering/maintenance-bindings/{id}/work-order:
    post:
      operationId: linkMaintenanceWorkOrder
      summary: Link (or create) the BE-08 Work Order of a maintenance binding
      description: >-
        BE-10G. Either links an existing `workOrderId` or asks the shared BE-08
        service to create one. PM field execution is then the published BE-08
        action set on that Work Order — this binding adds no second execution
        path. Requires `maintenance_binding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: maintenance_binding.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/MaintenanceBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LinkMaintenanceWorkOrderRequest"
      responses:
        "200":
          description: Maintenance binding with its linked Work Order.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/MaintenanceBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /engineering/findings:
    post:
      operationId: createEngineeringFinding
      summary: Report an Engineering finding (or link an existing BE-09 Finding)
      description: >-
        BE-10H. Records the Engineering context (Asset / Functional Location /
        `operationType` / source execution) for an authoritative BE-09 Finding.
        Supply `findingId` to link an existing Finding, or the create fields to
        have the shared BE-09 service create one. Classification, severity,
        assignment, state transitions, review, rework, closure and
        `availableActions` all remain on the published BE-09 operations, keyed
        by `findingId` (NOT by this link's `id`). Requires
        `engineering_finding.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: engineering_finding.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateEngineeringFindingRequest"
      responses:
        "201":
          description: Engineering finding link + authoritative BE-09 Finding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringFinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listEngineeringFindings
      summary: List Engineering findings
      description: >-
        BE-10H read, constrained to the caller's accessible Buildings. Each row
        embeds the authoritative BE-09 Finding and its per-caller
        `availableActions`. Requires `engineering_finding.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: engineering_finding.read
      x-building-scoped: true
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: assetId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: sourceType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/FindingSourceType" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/FindingStatus" }
      responses:
        "200":
          description: Engineering finding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/EngineeringFinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/findings/{id}:
    get:
      operationId: getEngineeringFinding
      summary: Get an Engineering finding link by id
      description: >-
        BE-10H read. `{id}` is the Engineering link id; use the embedded
        `findingId` for every BE-09 workflow operation. Requires
        `engineering_finding.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: engineering_finding.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/EngineeringFindingIdPath"
      responses:
        "200":
          description: Engineering finding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringFinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/engineering/daily-operations:
    get:
      operationId: getEngineeringDailyOperations
      summary: Daily Engineering operations read model for a Building
      description: >-
        BE-10A. A consolidated per-Building, per-operational-date (+ optional
        Shift) view of BE-07 generated Tasks, BE-08 Work Orders and BE-09
        Findings. Every row references its authoritative source id and carries
        the source domain's own status, untranslated; `availableActions` is
        present for FINDING rows only, because Tasks and Work Orders expose no
        authoritative available-actions service. Nothing is copied into a new
        master table. `date` is REQUIRED. Requires `engineering.read` and
        BE-02G Building access.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: engineering.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: date
          in: query
          required: true
          schema: { type: string, format: date }
          description: Operational date YYYY-MM-DD (required).
        - name: shiftId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Daily Engineering operations.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringDailyOperations"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/engineering/overview:
    get:
      operationId: getEngineeringOverview
      summary: Engineering supervisor overview for a Building
      description: >-
        BE-10K. A concise supervisor read model composed from the BE-10A daily
        operations, BE-09 finding counts, BE-08 active Work Orders and the
        BE-10J handover dataset. Derived at read time — no analytics engine and
        no duplicated operational data. `date` defaults to today. Requires
        `engineering_overview.read` and BE-02G Building access.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: engineering_overview.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: date
          in: query
          required: false
          schema: { type: string, format: date }
        - name: shiftId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Engineering overview.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringOverview"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/engineering/shift-handovers:
    post:
      operationId: createEngineeringShiftHandover
      summary: Create an Engineering shift handover
      description: >-
        BE-10J. Links an outgoing and an incoming BE-03 Shift for one Building
        and date, with a DRAFT → READY → ACKNOWLEDGED lifecycle. The
        operational `dataset` is resolved live from the authoritative
        Engineering records at read time and is never copied into the handover
        row. Requires `shift_handover.manage` and BE-02G Building access.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: shift_handover.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateEngineeringShiftHandoverRequest"
      responses:
        "201":
          description: Shift handover created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringShiftHandover"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listEngineeringShiftHandovers
      summary: List Engineering shift handovers for a Building
      description: >-
        BE-10J read, optionally filtered by handover date. Requires
        `shift_handover.read` and BE-02G Building access.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: shift_handover.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: date
          in: query
          required: false
          schema: { type: string, format: date }
      responses:
        "200":
          description: Shift handover list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/EngineeringShiftHandover"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /engineering/shift-handovers/{id}:
    get:
      operationId: getEngineeringShiftHandover
      summary: Get an Engineering shift handover by id
      description: >-
        BE-10J read, including the live authoritative handover dataset.
        Requires `shift_handover.read`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: shift_handover.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/EngineeringShiftHandoverIdPath"
      responses:
        "200":
          description: Shift handover.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringShiftHandover"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateEngineeringShiftHandover
      summary: Update the summary of a shift handover
      description: >-
        BE-10J. Only the free-text `summary` is mutable, and only while the
        handover is still DRAFT. Requires `shift_handover.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: shift_handover.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/EngineeringShiftHandoverIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateEngineeringShiftHandoverRequest"
      responses:
        "200":
          description: Updated shift handover.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringShiftHandover"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /engineering/shift-handovers/{id}/ready:
    post:
      operationId: markEngineeringShiftHandoverReady
      summary: Mark a shift handover READY
      description: >-
        BE-10J lifecycle transition DRAFT → READY. Requires
        `shift_handover.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: shift_handover.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/EngineeringShiftHandoverIdPath"
      responses:
        "200":
          description: Shift handover marked READY.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringShiftHandover"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /engineering/shift-handovers/{id}/acknowledge:
    post:
      operationId: acknowledgeEngineeringShiftHandover
      summary: Acknowledge a shift handover
      description: >-
        BE-10J lifecycle transition READY → ACKNOWLEDGED, recording the
        acknowledging user. This is the supervisor-facing acceptance of the
        outgoing shift's open work. Requires `shift_handover.manage`.
      tags: [Engineering]
      security: [{ bearerAuth: [] }]
      x-required-permission: shift_handover.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/EngineeringShiftHandoverIdPath"
      responses:
        "200":
          description: Shift handover acknowledged.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/EngineeringShiftHandover"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  # --------------------------------------------------------------------
  # CR-BE-MOB-02 PART 07B — Security Shift Handover contract publication.
  #
  # Existing BE-12G binding + BE-12M reporting dataset **routes** published
  # for mobile. The Security binding attaches an existing BE-10J
  # shift_handovers row to a Security operational context (a BE-12A start
  # Security Post + optional BE-12B Patrol Route) under the same Building;
  # the BE-10J DRAFT → READY → ACKNOWLEDGED lifecycle stays the only
  # authority on the handover, and the binding's ACTIVE/INACTIVE status is
  # independent of it. No runtime, table, or duplicate mobile endpoint is
  # introduced — these are documentation-only publications of registered
  # handlers.
  # --------------------------------------------------------------------

  /buildings/{buildingId}/security/shift-handovers:
    post:
      operationId: createSecurityShiftHandoverBinding
      summary: Create a Security shift handover binding in a Building
      description: >-
        BE-12G. Links an existing BE-10J `shift_handovers` row to a Security
        operational context — a BE-12A start Security Post and an optional
        BE-12B Patrol Route — under the same Building. The BE-10J lifecycle
        (DRAFT → READY → ACKNOWLEDGED) remains authoritative on the handover;
        the binding's own ACTIVE/INACTIVE status is independent of it and
        never affects the handover. Requires `security_shift_handover.manage`
        and BE-02G Building access. `clientId` is derived through Building →
        Property → Client and is never accepted from the caller. One ACTIVE
        binding per handover (409).
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_shift_handover.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSecurityShiftHandoverBindingRequest"
      responses:
        "201":
          description: Security shift handover binding created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/SecurityShiftHandoverBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildingSecurityShiftHandoverBindings
      summary: List Security shift handover bindings in a Building
      description: >-
        BE-12G read, scoped to one Building. Optional `shiftHandoverId`,
        `startSecurityPostId`, `patrolRouteId` and `status` filters narrow the
        result. Requires `security_shift_handover.read` and BE-02G Building
        access.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_shift_handover.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: shiftHandoverId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: startSecurityPostId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: patrolRouteId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/SecurityShiftHandoverBindingStatus"
      responses:
        "200":
          description: Security shift handover binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/SecurityShiftHandoverBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /security/shift-handovers/{id}:
    get:
      operationId: getSecurityShiftHandoverBinding
      summary: Get a Security shift handover binding by id
      description: >-
        BE-12G read. `{id}` is the Security binding id (never the BE-10J
        handover id — that stays `shiftHandoverId`). Requires
        `security_shift_handover.read`; the service walks binding → Building
        and asserts BE-02G access, so an inaccessible binding is rejected with
        403 `BUILDING_ACCESS_DENIED`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_shift_handover.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/SecurityShiftHandoverBindingIdPath"
      responses:
        "200":
          description: Security shift handover binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/SecurityShiftHandoverBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateSecurityShiftHandoverBinding
      summary: Update a Security shift handover binding
      description: >-
        BE-12G. Mutable fields are `startSecurityPostId`, `patrolRouteId` and
        `status`; `shiftHandoverId`, `clientId`, `buildingId` and
        `createdByUserId` are immutable. Deactivation is `status: INACTIVE`
        (the row is retained, never deleted). Reactivating to ACTIVE is
        rejected while another ACTIVE binding exists for the same handover.
        Requires `security_shift_handover.manage` and BE-02G Building access.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_shift_handover.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/SecurityShiftHandoverBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSecurityShiftHandoverBindingRequest"
      responses:
        "200":
          description: Updated Security shift handover binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/SecurityShiftHandoverBinding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /security/reports/shift-handovers:
    get:
      operationId: listSecurityShiftHandoverDataset
      summary: Security shift handover reporting dataset
      description: >-
        BE-12M read-only reporting dataset over the authoritative BE-12G
        `security_shift_handover_bindings` joined to the BE-10J
        `shift_handovers` (with start Security Post / Patrol Route codes).
        `bindingStatus` is the binding's own ACTIVE/INACTIVE state;
        `handoverStatus` is the authoritative BE-10J lifecycle status. Scoped
        to the caller's accessible Buildings; `buildingId` narrows to one
        Building and, when omitted, the dataset rolls up across every
        accessible Building. Requires `security_report.read`.
      tags: [Security]
      security: [{ bearerAuth: [] }]
      x-required-permission: security_report.read
      x-building-scoped: true
      parameters:
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
          description: Narrow to one Building (optional; rolls up across accessible Buildings when omitted).
        - name: securityPostId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: patrolRouteId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          description: Filter on the authoritative BE-10J handover status.
          schema:
            type: string
            enum: [DRAFT, READY, ACKNOWLEDGED, CANCELLED]
        - name: dateFrom
          in: query
          required: false
          description: Inclusive lower bound on the handover creation time (ISO-8601 date or datetime, UTC).
          schema: { type: string }
        - name: dateTo
          in: query
          required: false
          description: Exclusive upper bound on the handover creation time (ISO-8601 date or datetime, UTC).
          schema: { type: string }
      responses:
        "200":
          description: Security shift handover reporting dataset rows.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/SecurityShiftHandoverDatasetRow"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # --------------------------------------------------------------------
  # CR-BE-MOB-01 PART 04 — Work Order material context reference data.
  #
  # Existing BE-16A / BE-16B / BE-16H **read** routes published for mobile.
  # The Work Order material *write* chain is already published elsewhere and
  # is NOT repeated here: `recordWorkOrderMaterialUsage`,
  # `listWorkOrderMaterialUsages`, `getWorkOrderMaterialCostSummary`
  # (Work Order Material Usage), the stock ledger (Inventory Stock), the
  # approved-quantity chain (Material Requests, Work Order Procurement
  # Binding) and Work Order verification (Work Orders). This block only adds
  # the item / warehouse / spare-part reference reads a field picker needs.
  # --------------------------------------------------------------------

  /clients/{clientId}/inventory-items:
    get:
      operationId: listClientInventoryItems
      summary: List the Inventory Item master of a Client
      description: >-
        BE-16A read. The authoritative material / spare-part catalogue owned by
        one Client; `itemType` is data (SPARE_PART / MATERIAL / CONSUMABLE) and
        drives no behaviour. `uomId` reuses the BE-07 UOM foundation and is the
        UOM snapshotted onto a Work Order material usage. Mobile relevance: the
        item picker behind `recordWorkOrderMaterialUsage`. Requires
        `inventory_item.read`; results are constrained to the caller's
        accessible Clients. This route carries no stock — availability is the
        published stock-balance surface.
      tags: [Inventory Master]
      security: [{ bearerAuth: [] }]
      x-required-permission: inventory_item.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/InventoryItemStatus" }
        - name: itemType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/InventoryItemType" }
        - name: category
          in: query
          required: false
          schema: { type: string }
        - name: search
          in: query
          required: false
          schema: { type: string }
          description: Free-text match over item code and name.
        - name: uomId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Inventory item list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/InventoryItem" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /inventory-items/{itemId}:
    get:
      operationId: getInventoryItem
      summary: Get an Inventory Item by id
      description: >-
        BE-16A read, including the resolved BE-07 UOM context when present.
        `itemId` is the authoritative id embedded in material usages, material
        requests, stock balances and stock movements. Requires
        `inventory_item.read`.
      tags: [Inventory Master]
      security: [{ bearerAuth: [] }]
      x-required-permission: inventory_item.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/InventoryItemIdPath"
      responses:
        "200":
          description: Inventory item.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/InventoryItem" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/warehouses:
    get:
      operationId: listBuildingWarehouses
      summary: List the Warehouses / stores of a Building
      description: >-
        BE-16B read. A Warehouse is operated within exactly one Building
        (Client → Property → Building → Warehouse) and may bind to a BE-04
        Functional Location. Mobile relevance: the issuing-warehouse picker
        behind `recordWorkOrderMaterialUsage` and the scope of the published
        stock-balance reads. Requires `inventory_warehouse.read` and BE-02G
        Building access.
      tags: [Inventory Master]
      security: [{ bearerAuth: [] }]
      x-required-permission: inventory_warehouse.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/InventoryWarehouseStatus" }
        - name: search
          in: query
          required: false
          schema: { type: string }
          description: Free-text match over warehouse code and name.
        - name: functionalLocationId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Warehouse list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/InventoryWarehouse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /clients/{clientId}/warehouses:
    get:
      operationId: listClientWarehouses
      summary: List the Warehouses / stores of a Client
      description: >-
        BE-16B read across the Client's Buildings, constrained to the caller's
        accessible scope. Requires `inventory_warehouse.read`.
      tags: [Inventory Master]
      security: [{ bearerAuth: [] }]
      x-required-permission: inventory_warehouse.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/InventoryWarehouseStatus" }
        - name: search
          in: query
          required: false
          schema: { type: string }
      responses:
        "200":
          description: Warehouse list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/InventoryWarehouse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /warehouses/{warehouseId}:
    get:
      operationId: getWarehouse
      summary: Get a Warehouse / store by id
      description: >-
        BE-16B read, including the resolved Functional Location when bound.
        `warehouseId` is the authoritative id embedded in material usages and
        stock records. Requires `inventory_warehouse.read` and Building access
        on the warehouse's Building.
      tags: [Inventory Master]
      security: [{ bearerAuth: [] }]
      x-required-permission: inventory_warehouse.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/WarehouseIdPath"
      responses:
        "200":
          description: Warehouse.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/InventoryWarehouse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /assets/{assetId}/spare-parts:
    get:
      operationId: listAssetSpareParts
      summary: List the spare-part Items bound to an Asset
      description: >-
        BE-16H read. The many-to-many binding between a BE-05 Asset and
        SPARE_PART Inventory Items, with the `requiredQuantity` planned per
        asset. Mobile relevance: the expected parts for corrective / planned
        work on this equipment, before issuing them through
        `recordWorkOrderMaterialUsage`. `requiredQuantity` is a planning
        reference only — it is neither an approved nor an issued quantity.
        Requires `asset.read` and Building access on the Asset's Building.
      tags: [Inventory Master]
      security: [{ bearerAuth: [] }]
      x-required-permission: asset.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/AssetIdPath"
        - name: itemId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/AssetSparePartStatus" }
        - name: clientId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Asset spare-part binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/AssetSparePart" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /inventory-items/{itemId}/asset-bindings:
    get:
      operationId: listInventoryItemAssetBindings
      summary: List the Assets an Inventory Item is a spare part for
      description: >-
        BE-16H reverse read of the same binding. Requires
        `inventory_item.read`.
      tags: [Inventory Master]
      security: [{ bearerAuth: [] }]
      x-required-permission: inventory_item.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/InventoryItemIdPath"
        - name: assetId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/AssetSparePartStatus" }
        - name: clientId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Asset spare-part binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/AssetSparePart" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /asset-spare-parts/{id}:
    get:
      operationId: getAssetSparePart
      summary: Get an Asset ↔ spare-part Item binding by id
      description: >-
        BE-16H read, including the resolved Asset and Item context. Requires
        `asset.read`.
      tags: [Inventory Master]
      security: [{ bearerAuth: [] }]
      x-required-permission: asset.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/AssetSparePartIdPath"
      responses:
        "200":
          description: Asset spare-part binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/AssetSparePart" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # CR-BE-UTL-01 PART 13 — existing Utility domain contract.
  /buildings/{buildingId}/utility-meters:
    parameters:
      - $ref: "#/components/parameters/BuildingIdPath"
    get:
      operationId: listBuildingUtilityMeters
      summary: List Utility meters in an accessible Building
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/UtilityMeterListSuccess" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createUtilityMeter
      summary: Create a Utility meter with governed purpose and UOM
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateUtilityMeterRequest" }
      responses:
        "201": { $ref: "#/components/responses/UtilityMeterSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/meters/{id}:
    parameters:
      - $ref: "#/components/parameters/UtilityResourceIdPath"
    get:
      operationId: getUtilityMeter
      summary: Get a Utility meter
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/UtilityMeterSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateUtilityMeter
      summary: Update mutable Utility meter master fields
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateUtilityMeterRequest" }
      responses:
        "200": { $ref: "#/components/responses/UtilityMeterSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /utility/meters/{id}/sub-meters:
    parameters:
      - $ref: "#/components/parameters/UtilityResourceIdPath"
    get:
      operationId: listUtilitySubMeters
      summary: List active/history-filtered sub-meter relationships
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/UtilityHierarchyListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: bindUtilitySubMeter
      summary: Bind an existing meter as a sub-meter
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateUtilityMeterHierarchyRequest" }
      responses:
        "201": { $ref: "#/components/responses/UtilityHierarchySuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/meters/{id}/readings:
    parameters:
      - $ref: "#/components/parameters/UtilityResourceIdPath"
    get:
      operationId: listUtilityMeterReadings
      summary: List append-only readings for a Utility meter
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/UtilityReadingListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: recordUtilityMeterReading
      summary: Record an ACTUAL or ESTIMATED Utility meter reading
      description: >
        Optionally links the BE-10C reading execution through
        meterReadingBindingId/formInstanceId. Readings are append-only; no
        update/delete lifecycle exists. PHOTO OCR candidate extraction is not
        currently an API capability and must not be inferred from evidence.
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RecordUtilityReadingRequest" }
      responses:
        "201": { $ref: "#/components/responses/UtilityReadingSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/meter-readings/{id}:
    get:
      operationId: getUtilityMeterReading
      summary: Get one Utility meter reading and lineage references
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityReadingSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /utility/meter-readings/{id}/evidence:
    parameters:
      - $ref: "#/components/parameters/UtilityResourceIdPath"
    get:
      operationId: listUtilityMeterReadingEvidence
      summary: List PHOTO/DOCUMENT/SIGNATURE evidence for a reading
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/UtilityEvidenceListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: submitUtilityMeterReadingEvidence
      summary: Submit a storage reference through the shared Evidence engine
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitUtilityReadingEvidenceRequest" }
      responses:
        "201": { $ref: "#/components/responses/UtilityEvidenceSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /utility/meter-readings/{id}/evidence-validation:
    get:
      operationId: validateUtilityMeterReadingEvidence
      summary: Resolve shared Evidence readiness for a reading
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityEvidenceReadinessSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /utility/meters/{id}/consumptions:
    parameters:
      - $ref: "#/components/parameters/UtilityResourceIdPath"
    get:
      operationId: listUtilityMeterConsumptions
      summary: List authoritative consumption deltas for a meter
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/UtilityConsumptionListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: calculateUtilityMeterConsumption
      summary: Derive consumption from two authoritative readings
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CalculateUtilityConsumptionRequest" }
      responses:
        "201": { $ref: "#/components/responses/UtilityConsumptionSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/meter-consumptions/{id}:
    get:
      operationId: getUtilityMeterConsumption
      summary: Get one consumption with reading lineage
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityConsumptionSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/utility-tariffs:
    parameters:
      - $ref: "#/components/parameters/BuildingIdPath"
    get:
      operationId: listBuildingUtilityTariffs
      summary: List effective-dated Electricity/Water tariffs
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/UtilityTariffListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createBuildingUtilityTariff
      summary: Configure a Building tariff
      description: >
        The explicit `currency` is required and validated as an ACTIVE Currency
        Master entry allowed for the resolved Building Client
        (CURRENCY_INACTIVE_OR_UNKNOWN / CLIENT_CURRENCY_NOT_ALLOWED). No
        IDR/base/default inference or currency conversion. The tariff currency
        is the immutable snapshot that propagates exactly to calculation and
        bill (tariff → calculation → bill).
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateUtilityTariffRequest" }
      responses:
        "201": { $ref: "#/components/responses/UtilityTariffSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/meters/{id}/reading-dues:
    parameters:
      - $ref: "#/components/parameters/UtilityResourceIdPath"
    get:
      operationId: listMeterUtilityReadingDues
      summary: List expected reading periods for one Utility Meter
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/UtilityReadingDueListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createUtilityReadingDue
      summary: Create an expected reading period, optionally linked to shared scheduler records
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateUtilityReadingDueRequest" }
      responses:
        "201": { $ref: "#/components/responses/UtilityReadingDueSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /buildings/{buildingId}/utility-reading-dues:
    get:
      operationId: listBuildingUtilityReadingDues
      summary: List Reading Due records in one Building
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityReadingDueListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /clients/{clientId}/utility-reading-dues:
    get:
      operationId: listClientUtilityReadingDues
      summary: List Reading Due records restricted to accessible Client Buildings
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityReadingDueListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /utility/reading-dues/{id}:
    get:
      operationId: getUtilityReadingDue
      summary: Get Reading Due status and Meter Reading linkage
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityReadingDueSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /utility/reading-dues/{id}/complete:
    post:
      operationId: completeUtilityReadingDue
      summary: Link a Due/Overdue expectation to an authoritative Meter Reading
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CompleteUtilityReadingDueRequest" }
      responses:
        "200": { $ref: "#/components/responses/UtilityReadingDueSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/reading-dues/{id}/cancel:
    post:
      operationId: cancelUtilityReadingDue
      summary: Cancel a Due/Overdue reading expectation with reason
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CancelUtilityReadingDueRequest" }
      responses:
        "200": { $ref: "#/components/responses/UtilityReadingDueSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/meter-reading-evidence/{evidenceId}/ocr-candidates:
    post:
      operationId: createUtilityOcrCandidate
      summary: Stage one OCR reading candidate from ACTIVE PHOTO evidence
      description: The PHOTO remains authoritative evidence; this never changes a Meter Reading.
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      parameters:
        - name: evidenceId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateUtilityOcrCandidateRequest" }
      responses:
        "201": { $ref: "#/components/responses/UtilityOcrCandidateSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/ocr-candidates/{id}:
    get:
      operationId: getUtilityOcrCandidate
      summary: Get OCR candidate and immutable human decision
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityOcrCandidateSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /utility/ocr-candidates/{id}/accept:
    post:
      operationId: acceptUtilityOcrCandidate
      summary: Human-accept OCR and link/create an authoritative validated reading
      description: >
        With meterReadingId, links an existing equal-value reading. With
        readingAt, calls the existing Meter Reading workflow using the
        candidate value. With neither, verifies against the PHOTO's reading.
        Optional readingDueId completes that expectation through its normal gate.
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      requestBody:
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AcceptUtilityOcrCandidateRequest" }
      responses:
        "200": { $ref: "#/components/responses/UtilityOcrCandidateSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/ocr-candidates/{id}/reject:
    post:
      operationId: rejectUtilityOcrCandidate
      summary: Human-reject OCR with a preserved reason
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RejectUtilityOcrCandidateRequest" }
      responses:
        "200": { $ref: "#/components/responses/UtilityOcrCandidateSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /buildings/{buildingId}/utility-summary:
    get:
      operationId: getBuildingUtilityOperationalSummary
      summary: Read the governed Building Utility operational summary
      description: >
        Read-only exact-period projection over persisted reconciliation,
        Meter/Reading Due, Utility exception, approval/calculation and Utility
        Bill records. It does not recalculate or mutate historical snapshots.
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: management_read_model.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: periodStart
          in: query
          required: true
          schema: { type: string, format: date-time }
        - name: periodEnd
          in: query
          required: true
          schema: { type: string, format: date-time }
      responses:
        "200": { $ref: "#/components/responses/BuildingUtilityOperationalSummarySuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /utility/exceptions:
    get:
      operationId: listUtilityOperationalExceptions
      summary: List Utility exceptions within accessible Buildings
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      parameters:
        - name: buildingId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: utilityType
          in: query
          schema: { $ref: "#/components/schemas/UtilityType" }
        - name: exceptionType
          in: query
          schema: { $ref: "#/components/schemas/UtilityExceptionType" }
        - name: severity
          in: query
          schema: { $ref: "#/components/schemas/UtilityExceptionSeverity" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/UtilityExceptionStatus" }
        - name: reconciliationId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200": { $ref: "#/components/responses/UtilityExceptionListSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createUtilityOperationalException
      summary: Open an operational exception against authoritative Utility references
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateUtilityExceptionRequest" }
      responses:
        "201": { $ref: "#/components/responses/UtilityExceptionSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/exceptions/{id}:
    get:
      operationId: getUtilityOperationalException
      summary: Get one auditable Utility operational exception
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityExceptionSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /utility/exceptions/{id}/start-review:
    post:
      operationId: startUtilityOperationalExceptionReview
      summary: Transition an OPEN Utility exception to UNDER_REVIEW
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      requestBody:
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StartUtilityExceptionReviewRequest" }
      responses:
        "200": { $ref: "#/components/responses/UtilityExceptionSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/exceptions/{id}/resolve:
    post:
      operationId: resolveUtilityOperationalException
      summary: Resolve an UNDER_REVIEW Utility exception with notes
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ResolveUtilityExceptionRequest" }
      responses:
        "200": { $ref: "#/components/responses/UtilityExceptionSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/exceptions/{id}/cancel:
    post:
      operationId: cancelUtilityOperationalException
      summary: Cancel an OPEN or UNDER_REVIEW Utility exception with reason
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CancelUtilityExceptionRequest" }
      responses:
        "200": { $ref: "#/components/responses/UtilityExceptionSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/consumptions/{id}/calculations:
    parameters:
      - $ref: "#/components/parameters/UtilityResourceIdPath"
    get:
      operationId: listUtilityConsumptionCalculations
      summary: List calculation history for one consumption
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/UtilityCalculationListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: calculateUtilityCharge
      summary: Resolve tariff and calculate an immutable Utility charge snapshot
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      requestBody:
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CalculateUtilityChargeRequest" }
      responses:
        "201": { $ref: "#/components/responses/UtilityCalculationSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/calculations/{id}:
    get:
      operationId: getUtilityCalculation
      summary: Get a tariff calculation and immutable snapshot lineage
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityCalculationSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /utility/calculations/{id}/finalize:
    post:
      operationId: finalizeUtilityCalculation
      summary: Freeze a DRAFT Utility calculation
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityCalculationSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /tenant-approvals/pending:
    get:
      operationId: listPendingTenantUtilityApprovals
      summary: List Building-scoped pending Tenant approvals
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: tenant_company.read
      x-building-scoped: true
      x-tenant-pic-self-service: false
      responses:
        "200": { $ref: "#/components/responses/TenantUtilityApprovalListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /tenant-approvals/{id}:
    get:
      operationId: getTenantUtilityApproval
      summary: Get a Tenant approval and immutable Utility snapshot when applicable
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: tenant_company.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/TenantUtilityApprovalSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /tenant-approvals/{id}/available-actions:
    get:
      operationId: getTenantUtilityApprovalAvailableActions
      summary: Resolve APPROVE/REJECT actions for the assigned approver
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: tenant_company.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/TenantApprovalActionsSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /tenant-approvals:
    post:
      operationId: createTenantUtilityApproval
      summary: Create an approval binding, including Utility Calculation targets
      description: >
        For requestType UTILITY_CALCULATION, snapshots the finalized TENANT
        meter charge. This endpoint also serves existing non-Utility Tenant
        request types and is not a Utility-specific approval engine.
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: tenant_company.manage
      x-building-scoped: true
      x-tenant-pic-self-service: false
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateTenantApprovalRequest" }
      responses:
        "201": { $ref: "#/components/responses/TenantUtilityApprovalSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /tenant-approvals/{id}/approve:
    post:
      operationId: approveTenantUtilityApproval
      summary: Approve the assigned Tenant approval
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: tenant_company.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      requestBody:
        content:
          application/json:
            schema: { $ref: "#/components/schemas/TenantApprovalDecisionRequest" }
      responses:
        "200": { $ref: "#/components/responses/TenantUtilityApprovalSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /tenant-approvals/{id}/reject:
    post:
      operationId: rejectTenantUtilityApproval
      summary: Reject the assigned Tenant approval with required reason
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: tenant_company.manage
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/TenantApprovalDecisionRequest" }
      responses:
        "200": { $ref: "#/components/responses/TenantUtilityApprovalSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/calculations/{id}/tenant-approvals:
    get:
      operationId: getUtilityCalculationTenantApprovals
      summary: Get approval context/history for one finalized Tenant calculation
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: tenant_company.read
      x-building-scoped: true
      x-tenant-pic-self-service: false
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityApprovalContextSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /tenant-companies/{tenantCompanyId}/utility-bills:
    post:
      operationId: generateApprovedUtilityBill
      summary: Create a bill when no approval is pending and the latest terminal decision is APPROVED
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_bill.manage
      x-building-scoped: true
      x-tenant-context: historical-calculation-and-approval-snapshot
      x-tenant-pic-self-service: false
      parameters:
        - $ref: "#/components/parameters/TenantCompanyIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/GenerateUtilityBillRequest" }
      responses:
        "201": { $ref: "#/components/responses/UtilityBillSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility-bills:
    get:
      operationId: listUtilityBills
      summary: List Utility Bills within accessible Buildings
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_bill.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/UtilityBillListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /utility-bills/{id}:
    get:
      operationId: getUtilityBill
      summary: Get an immutable-snapshot Utility Bill
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_bill.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityBillSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

    patch:
      operationId: updateUtilityBill
      summary: Update due date or transition DRAFT/ISSUED/CANCELLED lifecycle
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_bill.manage
      x-building-scoped: true
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateUtilityBillRequest" }
      responses:
        "200": { $ref: "#/components/responses/UtilityBillSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility-bills/{id}/invoice-ready:
    get:
      operationId: getUtilityBillInvoiceReady
      summary: Project a non-invoice, invoice-ready Utility charge line
      description: No invoice numbering, tax, payment, AR, receipt, or accounting side effect.
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_bill.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityInvoiceReadySuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /buildings/{buildingId}/utility-reconciliations:
    parameters:
      - $ref: "#/components/parameters/BuildingIdPath"
    get:
      operationId: listBuildingUtilityReconciliations
      summary: List immutable Building reconciliation and IKE/IKA snapshots
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      responses:
        "200": { $ref: "#/components/responses/UtilityReconciliationListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createBuildingUtilityReconciliation
      summary: Calculate Building Electricity/Water reconciliation and IKE/IKA
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.manage
      x-building-scoped: true
      x-tenant-billing-side-effects: false
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateUtilityReconciliationRequest" }
      responses:
        "201": { $ref: "#/components/responses/UtilityReconciliationSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /utility/reconciliations/{id}:
    get:
      operationId: getBuildingUtilityReconciliation
      summary: Get a Building reconciliation and IKE/IKA snapshot
      tags: [Utility]
      security: [{ bearerAuth: [] }]
      x-required-permission: utility_meter.read
      x-building-scoped: true
      parameters:
        - $ref: "#/components/parameters/UtilityResourceIdPath"
      responses:
        "200": { $ref: "#/components/responses/UtilityReconciliationSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /clients/{clientId}/sla-definitions:
    parameters:
      - { name: clientId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }
    post:
      summary: Create an SLA definition
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CreateSlaDefinitionRequest" } } }
      responses:
        "201": { description: Created }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    get:
      summary: List SLA definitions in a client scope
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: buildingId, in: query, schema: { $ref: "#/components/schemas/Uuid" } }
        - { name: status, in: query, schema: { type: string, enum: [ACTIVE, INACTIVE] } }
        - { name: operationalType, in: query, schema: { type: string, enum: [WORK_ORDER] } }
        - { name: priority, in: query, schema: { type: string, enum: [LOW, MEDIUM, HIGH, CRITICAL] } }
      responses:
        "200": { description: Definitions listed }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /work-orders/{id}/sla:
    parameters:
      - { name: id, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }
    get:
      summary: Get the immutable applied SLA and clocks for a Work Order
      description: Returns null when the prospective Work Order has no applicable SLA definition.
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Applied SLA returned, or null when none applies }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /work-orders/{id}/sla/escalations:
    parameters:
      - { name: id, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }
    get:
      summary: List the SLA escalation history of a Work Order
      description: >
        Read-only history of the escalation levels materialized from this Work Order's SLA
        breaches, ordered by clock type then level. Returns an empty array when the Work Order
        never breached, matched no escalation policy, or has no SLA. Recipient identities are
        never exposed — only the resolved recipient count and the created in-app notification
        count. There is no manual trigger, retry, or cancel endpoint: execution belongs solely
        to the due-job dispatcher.
      security: [{ bearerAuth: [] }]
      x-required-permission: work_order.read
      x-building-scoped: true
      responses:
        "200":
          description: Escalation history returned (possibly empty)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/SlaEscalationAction" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /sla-definitions/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }
    get:
      summary: Get an SLA definition
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Definition returned }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update or enable/disable an SLA definition
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/UpdateSlaDefinitionRequest" } } }
      responses:
        "200": { description: Definition updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /clients/{clientId}/sla-escalation-policies:
    parameters:
      - { name: clientId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }
    post:
      summary: Create an SLA escalation policy
      description: Configuration only — creating a policy schedules and sends nothing.
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CreateSlaEscalationPolicyRequest" } } }
      responses:
        "201": { description: Created }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      summary: List SLA escalation policies in a client scope
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: buildingId, in: query, schema: { $ref: "#/components/schemas/Uuid" } }
        - { name: status, in: query, schema: { type: string, enum: [ACTIVE, INACTIVE] } }
        - { name: clockType, in: query, schema: { type: string, enum: [RESPONSE, RESOLUTION, ANY] } }
        - { name: priority, in: query, schema: { type: string, enum: [LOW, MEDIUM, HIGH, CRITICAL] } }
      responses:
        "200": { description: Policies listed }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /sla-escalation-policies/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }
    get:
      summary: Get an SLA escalation policy
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Policy returned }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update or enable/disable an SLA escalation policy
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/UpdateSlaEscalationPolicyRequest" } } }
      responses:
        "200": { description: Policy updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /sla-escalation-policies/{id}/levels:
    parameters:
      - { name: id, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }
    post:
      summary: Add an ordered escalation level to a policy
      description: >
        Stores the level configuration only. The recipient rule is validated for shape and
        vocabulary; it is never resolved, and no notification is created by this endpoint.
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CreateSlaEscalationLevelRequest" } } }
      responses:
        "201": { description: Created }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      summary: List the escalation levels of a policy in level order
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Levels listed }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /sla-escalation-levels/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }
    get:
      summary: Get an SLA escalation level
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Level returned }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update or enable/disable an SLA escalation level
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/UpdateSlaEscalationLevelRequest" } } }
      responses:
        "200": { description: Level updated }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  # ---------------------------------------------------------------------
  # CR-BE-INTEG-01 PART 06 — Integration Webhooks (endpoint configuration
  # + delivery history). See the "Integration Webhooks" tag for the
  # outbound signature headers and the receiver verification recipe.
  # ---------------------------------------------------------------------
  /integration/webhook-endpoints:
    post:
      operationId: createIntegrationWebhookEndpoint
      summary: Create a webhook endpoint (secret disclosed once)
      description: >
        Creates a Client-scoped webhook endpoint (optional Building
        narrowing). The server generates the `whsec_` signing secret and
        returns it in THIS response ONLY — it can never be read back; use
        rotate-secret to replace a lost secret. `url` must be HTTPS without
        embedded credentials and must not target localhost/loopback/
        private/link-local/metadata addresses (SSRF guard; redirects are
        never followed at delivery time). `eventTypes` are uppercase
        BE-07 event-type codes; `INTEGRATION_*` and `NOTIFICATION_OUTBOUND_*`
        families are rejected (recursion guard). Activation is prospective:
        only events recorded AFTER the endpoint exists can ever be
        delivered to it — there is no historical replay or backfill.
        Requires `integration_webhook.manage` plus BE-02G access to the
        Client.
      tags: [Integration Webhooks]
      security:
        - bearerAuth: []
      x-required-permission: integration_webhook.manage
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [clientId, name, url, eventTypes]
              properties:
                clientId:
                  $ref: "#/components/schemas/Uuid"
                buildingId:
                  allOf:
                    - $ref: "#/components/schemas/Uuid"
                  nullable: true
                  description: >
                    Optional Building narrowing (must belong to the Client).
                    Null = all events of the Client.
                name:
                  type: string
                  maxLength: 200
                url:
                  type: string
                  maxLength: 2000
                  description: HTTPS receiver URL (SSRF-validated).
                eventTypes:
                  type: array
                  minItems: 1
                  maxItems: 100
                  items:
                    type: string
                    maxLength: 128
                  description: Subscribed BE-07 event-type codes (uppercased).
                status:
                  type: string
                  enum: [ACTIVE, INACTIVE]
                  default: ACTIVE
                timeoutMs:
                  type: integer
                  minimum: 1000
                  maximum: 30000
                  default: 10000
                  description: Per-delivery receiver timeout.
      responses:
        "201":
          description: >
            The created endpoint INCLUDING the one-time `signingSecret`
            disclosure (never returned again by any endpoint).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/IntegrationWebhookEndpointWithSecret"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    get:
      operationId: listIntegrationWebhookEndpoints
      summary: List webhook endpoints (secret-free)
      description: >
        Lists webhook endpoints of the caller's accessible Clients (BE-02G
        intersection — an explicit `clientId` outside the caller's scope is
        rejected with 403). Responses NEVER carry signing secrets. Requires
        `integration_webhook.read`.
      tags: [Integration Webhooks]
      security:
        - bearerAuth: []
      x-required-permission: integration_webhook.read
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [ACTIVE, INACTIVE]
        - name: eventType
          in: query
          required: false
          schema:
            type: string
            maxLength: 128
          description: Only endpoints subscribed to this event type.
      responses:
        "200":
          description: The accessible webhook endpoints (secret-free).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/IntegrationWebhookEndpoint"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /integration/webhook-endpoints/{integrationWebhookEndpointId}:
    get:
      operationId: getIntegrationWebhookEndpoint
      summary: Get a webhook endpoint (secret-free)
      description: >
        Returns one webhook endpoint by id — WITHOUT the signing secret.
        Requires `integration_webhook.read` plus BE-02G access to the
        endpoint's Client.
      tags: [Integration Webhooks]
      security:
        - bearerAuth: []
      x-required-permission: integration_webhook.read
      parameters:
        - name: integrationWebhookEndpointId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The webhook endpoint (secret-free).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/IntegrationWebhookEndpoint"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateIntegrationWebhookEndpoint
      summary: Update webhook endpoint configuration
      description: >
        Updates `name`, `url`, `eventTypes`, `timeoutMs` and/or `status`
        (ACTIVE/INACTIVE — deactivation is the retirement path; there is NO
        delete operation, so delivery history always keeps a valid endpoint
        reference). `clientId`/`buildingId` are immutable; secret material
        in the body is rejected (use rotate-secret). Config edits apply to
        FUTURE behavior only — already-snapshotted payloads and existing
        delivery records are never rewritten. Requires
        `integration_webhook.manage` plus BE-02G access.
      tags: [Integration Webhooks]
      security:
        - bearerAuth: []
      x-required-permission: integration_webhook.manage
      parameters:
        - name: integrationWebhookEndpointId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              minProperties: 1
              properties:
                name:
                  type: string
                  maxLength: 200
                url:
                  type: string
                  maxLength: 2000
                eventTypes:
                  type: array
                  minItems: 1
                  maxItems: 100
                  items:
                    type: string
                    maxLength: 128
                status:
                  type: string
                  enum: [ACTIVE, INACTIVE]
                timeoutMs:
                  type: integer
                  minimum: 1000
                  maximum: 30000
      responses:
        "200":
          description: The updated endpoint (secret-free).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/IntegrationWebhookEndpoint"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /integration/webhook-endpoints/{integrationWebhookEndpointId}/rotate-secret:
    post:
      operationId: rotateIntegrationWebhookEndpointSecret
      summary: Rotate the signing secret (new secret disclosed once)
      description: >
        Replaces the endpoint's signing secret with a newly generated
        `whsec_` value and returns it in THIS response ONLY. Single-active-
        secret model: the previous secret stops signing immediately (no
        overlap window) — update the receiver first, then rotate. Rotation
        is audited (`INTEGRATION_ENDPOINT_SECRET_ROTATED`) without secret
        material. Requires `integration_webhook.manage` plus BE-02G access.
      tags: [Integration Webhooks]
      security:
        - bearerAuth: []
      x-required-permission: integration_webhook.manage
      parameters:
        - name: integrationWebhookEndpointId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: >
            The endpoint INCLUDING the one-time `signingSecret` disclosure
            of the NEW secret.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/IntegrationWebhookEndpointWithSecret"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /integration/webhook-deliveries:
    get:
      operationId: listIntegrationWebhookDeliveries
      summary: List webhook delivery history
      description: >
        Read-only per-endpoint delivery history over the caller's accessible
        Clients (BE-02G intersection — an explicit `clientId` outside the
        caller's scope is rejected with 403), newest first. Each row exposes
        delivery state, attempt accounting, timing, and the last HTTP
        response status plus a sanitized error — NEVER signing secrets and
        NEVER payload bodies. Optional shared pagination (`page`,
        `pageSize`); optional filters below. There is no manual retry and no
        manual event-injection operation. Requires
        `integration_webhook.read`.
      tags: [Integration Webhooks]
      security:
        - bearerAuth: []
      x-required-permission: integration_webhook.read
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: endpointId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/Uuid"
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [PENDING, SENDING, DELIVERED, RETRY_SCHEDULED, FAILED_PERMANENT, EXHAUSTED]
        - name: eventType
          in: query
          required: false
          schema:
            type: string
            maxLength: 128
        - name: from
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Lower bound on the delivery's created timestamp.
        - name: to
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Upper bound on the delivery's created timestamp.
        - name: page
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
        - name: pageSize
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
      responses:
        "200":
          description: The accessible webhook deliveries (newest first).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/IntegrationWebhookDelivery"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /integration/webhook-deliveries/{integrationWebhookDeliveryId}:
    get:
      operationId: getIntegrationWebhookDelivery
      summary: Get one webhook delivery
      description: >
        Returns one delivery ledger row by id (state / attempt / timing /
        response metadata only — never secrets, never payload bodies).
        Requires `integration_webhook.read` plus BE-02G access to the
        delivery's Client.
      tags: [Integration Webhooks]
      security:
        - bearerAuth: []
      x-required-permission: integration_webhook.read
      parameters:
        - name: integrationWebhookDeliveryId
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/Uuid"
      responses:
        "200":
          description: The webhook delivery.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/IntegrationWebhookDelivery"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  # ---------------------------------------------------------------------------
  # CR-BE-PRO-02 — RFQ → Vendor response → comparison → award → PO conversion
  # ---------------------------------------------------------------------------
  /rfqs:
    post:
      operationId: createRfq
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Create a DRAFT RFQ
      description: >
        Internal RFQ foundation. The typed Material/Service demand lineage and
        Client/Building scope are derived from the existing Purchase Request.
        RFQ creation has no quotation, award, PO, budget, or commitment effect.
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateRfqRequest" }
      responses:
        "201":
          description: DRAFT RFQ created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties: { data: { $ref: "#/components/schemas/Rfq" } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listRfqs
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: List RFQs in accessible Buildings
      description: RFQs are Client/Building isolated and never expose inaccessible rows.
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: RFQ list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { type: array, items: { $ref: "#/components/schemas/Rfq" } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /rfqs/{rfqId}:
    parameters:
      - name: rfqId
        in: path
        required: true
        schema: { $ref: "#/components/schemas/Uuid" }
    get:
      operationId: getRfq
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: Get an RFQ
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: RFQ.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties: { data: { $ref: "#/components/schemas/Rfq" } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateRfq
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Update a DRAFT RFQ
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateRfqRequest" }
      responses:
        "200":
          description: Updated RFQ.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties: { data: { $ref: "#/components/schemas/Rfq" } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /rfqs/{rfqId}/available-actions:
    get:
      operationId: getRfqAvailableActions
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: Resolve RFQ lifecycle actions
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Caller-specific RFQ actions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties: { data: { $ref: "#/components/schemas/RfqAvailableActions" } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /rfqs/{rfqId}/open:
    post:
      operationId: openRfq
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Open an RFQ
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: RFQ opened., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/Rfq" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /rfqs/{rfqId}/close:
    post:
      operationId: closeRfq
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Close an RFQ before evaluation/recommendation
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: RFQ closed., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/Rfq" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
  /rfqs/{rfqId}/cancel:
    post:
      operationId: cancelRfq
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Cancel an RFQ
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: RFQ cancelled., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/Rfq" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /rfqs/{rfqId}/lines:
    post:
      operationId: addRfqLine
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Add a typed demand line to a DRAFT RFQ
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json: { schema: { $ref: "#/components/schemas/CreateRfqLineRequest" } }
      responses:
        "201":
          description: RFQ line added.
          content:
            application/json:
              schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqLine" } } }] }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listRfqLines
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: List RFQ demand lines
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: RFQ lines.
          content:
            application/json:
              schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/RfqLine" } } } }] }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /rfq-lines/{lineId}:
    get:
      operationId: getRfqLine
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: Get an RFQ demand line
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: lineId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200": { description: RFQ line., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqLine" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /rfqs/{rfqId}/invitations:
    post:
      operationId: createRfqVendorInvitation
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Invite a Vendor to an RFQ
      description: >
        Internal invitation management. The returned one-time invitation token
        is disclosed only at issuance. It creates no quotation, PO, budget, or
        commitment.
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CreateRfqInvitationRequest" } } }
      responses:
        "201": { description: Invitation created., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqInvitationIssued" } } }] } } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listRfqVendorInvitations
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: List RFQ Vendor Invitations
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Invitations., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/RfqInvitation" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /rfq-vendor-invitations:
    get:
      operationId: listAccessibleRfqVendorInvitations
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: List accessible RFQ Vendor Invitations
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Invitations., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/RfqInvitation" } } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /rfq-vendor-invitations/{invitationId}:
    get:
      operationId: getRfqVendorInvitation
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: Get an RFQ Vendor Invitation
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: invitationId
          in: path
          required: true
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200": { description: Invitation., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqInvitation" } } }] } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
  /rfq-vendor-invitations/{invitationId}/resend:
    post:
      operationId: resendRfqVendorInvitation
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Resend an RFQ Vendor Invitation
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: invitationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/IdempotencyRequest" } } } }
      responses: { "200": { description: Invitation resent., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqInvitationIssued" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "409": { $ref: "#/components/responses/Conflict" } }
  /rfq-vendor-invitations/{invitationId}/revoke:
    post:
      operationId: revokeRfqVendorInvitation
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Revoke an RFQ Vendor Invitation
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: invitationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Invitation revoked., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqInvitation" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "409": { $ref: "#/components/responses/Conflict" } }

  /vendor-rfq-access/exchange:
    post:
      operationId: exchangeRfqVendorInvitationToken
      summary: Exchange a one-time RFQ invitation token
      description: >
        Public issuance boundary. The returned bearer is a dedicated,
        invitation-scoped Vendor RFQ session, not an internal User session and
        not an RBAC grant. Raw invitation/session tokens are never persisted.
      tags: [RFQ Procurement]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/RfqTokenExchangeRequest" } } } }
      responses: { "200": { description: External RFQ session issued., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqSessionExchange" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "409": { $ref: "#/components/responses/Conflict" } }
  /vendor-rfq-access/me:
    get:
      operationId: getVendorRfqMe
      summary: Get the current invitation-scoped Vendor RFQ session
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      responses: { "200": { description: Scoped Vendor session., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqSession" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "404": { $ref: "#/components/responses/NotFound" } }
  /vendor-rfq-access/rfqs/{rfqId}:
    get:
      operationId: getVendorSafeRfq
      summary: Get only the RFQ content permitted to this Vendor invitation
      description: No peer quotation, comparison, recommendation, approval, award, PO, or commitment data is exposed.
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: rfqId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Vendor-safe RFQ., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/VendorSafeRfq" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "404": { $ref: "#/components/responses/NotFound" } }
  /vendor-rfq-access/invitations/{invitationId}:
    get:
      operationId: getVendorSafeInvitation
      summary: Get the current Vendor's own RFQ invitation
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: invitationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Own invitation., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqInvitation" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "404": { $ref: "#/components/responses/NotFound" } }
  /vendor-rfq-access/invitations/{invitationId}/accept:
    post:
      operationId: acceptVendorRfqInvitation
      summary: Accept own RFQ invitation
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: invitationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Invitation accepted., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqInvitation" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "404": { $ref: "#/components/responses/NotFound" }, "409": { $ref: "#/components/responses/Conflict" } }
  /vendor-rfq-access/invitations/{invitationId}/decline:
    post:
      operationId: declineVendorRfqInvitation
      summary: Decline own RFQ invitation
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: invitationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: false, content: { application/json: { schema: { $ref: "#/components/schemas/RfqResponseReasonRequest" } } } }
      responses: { "200": { description: Invitation declined., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqInvitation" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "404": { $ref: "#/components/responses/NotFound" }, "409": { $ref: "#/components/responses/Conflict" } }
  /vendor-rfq-access/invitations/{invitationId}/no-bid:
    post:
      operationId: noBidVendorRfqInvitation
      summary: Record a Vendor no-bid response
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: invitationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: false, content: { application/json: { schema: { $ref: "#/components/schemas/RfqResponseReasonRequest" } } } }
      responses: { "200": { description: No-bid recorded., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqInvitation" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "404": { $ref: "#/components/responses/NotFound" }, "409": { $ref: "#/components/responses/Conflict" } }

  /vendor-rfq-access/invitations/{invitationId}/quotations:
    post:
      operationId: createVendorQuotation
      summary: Create the current Vendor's quotation draft
      description: Vendor session only; the quotation belongs to the invitation and is not visible to peer Vendors.
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: invitationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateVendorQuotationRequest" } } } }
      responses: { "201": { description: Quotation draft., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/VendorQuotation" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "404": { $ref: "#/components/responses/NotFound" }, "409": { $ref: "#/components/responses/Conflict" } }
  /vendor-rfq-access/quotations/current:
    get:
      operationId: getVendorCurrentQuotation
      summary: Get the current Vendor's own quotation
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      responses: { "200": { description: Own quotation., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/VendorQuotation" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" } }
  /vendor-rfq-access/quotations/{quotationId}:
    get:
      operationId: getVendorQuotation
      summary: Get the current Vendor's own quotation
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: quotationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Own quotation., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/VendorQuotation" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "404": { $ref: "#/components/responses/NotFound" } }
  /vendor-rfq-access/quotations/{quotationId}/revisions:
    get:
      operationId: listVendorQuotationRevisions
      summary: List the current Vendor's quotation revisions
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: quotationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Own revisions., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/VendorQuotationRevision" } } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "404": { $ref: "#/components/responses/NotFound" } }
    post:
      operationId: createVendorQuotationRevision
      summary: Create a new Vendor quotation revision
      description: Submitted revisions are immutable; new commercial terms require a new sequential revision.
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: quotationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateVendorQuotationRevisionRequest" } } } }
      responses: { "201": { description: Draft revision., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/VendorQuotationRevision" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "409": { $ref: "#/components/responses/Conflict" } }
  /vendor-rfq-access/quotation-revisions/{revisionId}:
    patch:
      operationId: updateVendorQuotationRevision
      summary: Update the current Vendor's DRAFT revision
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: revisionId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateVendorQuotationRevisionRequest" } } } }
      responses: { "200": { description: Draft revision., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/VendorQuotationRevision" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "409": { $ref: "#/components/responses/Conflict" } }
  /vendor-rfq-access/quotation-revisions/{revisionId}/lines:
    post:
      operationId: addVendorQuotationLine
      summary: Add an RFQ-mapped line to a Vendor DRAFT revision
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: revisionId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateVendorQuotationLineRequest" } } } }
      responses: { "201": { description: Quotation line., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/VendorQuotationLine" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "409": { $ref: "#/components/responses/Conflict" } }
  /vendor-rfq-access/quotation-lines/{lineId}:
    patch:
      operationId: updateVendorQuotationLine
      summary: Update a Vendor quotation DRAFT line
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: lineId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateVendorQuotationLineRequest" } } } }
      responses: { "200": { description: Quotation line., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/VendorQuotationLine" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "409": { $ref: "#/components/responses/Conflict" } }
  /vendor-rfq-access/quotation-revisions/{revisionId}/submit:
    post:
      operationId: submitVendorQuotationRevision
      summary: Submit and freeze a Vendor quotation revision
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: revisionId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Submitted immutable revision., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/VendorQuotationRevision" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "409": { $ref: "#/components/responses/Conflict" } }
  /vendor-rfq-access/quotation-revisions/{revisionId}/attachments:
    post:
      operationId: addVendorQuotationAttachment
      summary: Link an attachment to a Vendor DRAFT quotation revision
      description: Uses shared Documents, Versions and Supporting Documents; fileReference remains opaque and no binary content is stored in PostgreSQL.
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: revisionId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateQuotationAttachmentRequest" } } } }
      responses: { "201": { description: Attachment linked., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/QuotationAttachment" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "409": { $ref: "#/components/responses/Conflict" } }
    get:
      operationId: listVendorQuotationAttachments
      summary: List attachments on a Vendor quotation revision
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: revisionId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Attachment references., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/QuotationAttachment" } } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "404": { $ref: "#/components/responses/NotFound" } }
  /vendor-rfq-access/quotation-attachments/{attachmentId}:
    get:
      operationId: getVendorQuotationAttachment
      summary: Get the current Vendor's own attachment reference
      tags: [RFQ Procurement]
      security: [{ vendorRfqSession: [] }]
      parameters: [{ name: attachmentId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Attachment reference., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/QuotationAttachment" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "404": { $ref: "#/components/responses/NotFound" } }

  /rfqs/{rfqId}/quotations:
    get:
      operationId: listRfqQuotations
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: List internal quotations invited to an RFQ
      description: Internal RFQ read only. All Vendor quotations are hidden from Vendor external sessions.
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: rfqId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Internal quotations., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/VendorQuotation" } } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" } }
  /vendor-quotations/{quotationId}:
    get:
      operationId: getInternalQuotation
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: Get an internal Vendor quotation
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: quotationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Quotation., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/VendorQuotation" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }
  /vendor-quotations/{quotationId}/revisions:
    get:
      operationId: listInternalQuotationRevisions
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: List immutable quotation revisions internally
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: quotationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Revisions., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/VendorQuotationRevision" } } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }
  /quotation-revisions/{revisionId}/attachments:
    get:
      operationId: listInternalQuotationAttachments
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: List internal quotation attachment references
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: revisionId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Attachment references., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/QuotationAttachment" } } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }

  /rfqs/{rfqId}/comparisons:
    post:
      operationId: createRfqComparison
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Create an immutable RFQ comparison snapshot
      description: >
        Internal only. The snapshot uses exact SUBMITTED quotation revisions,
        never drafts or mutable draft state. It creates no recommendation,
        award, PO, budget reservation, or commitment.
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: rfqId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateComparisonRequest" } } } }
      responses: { "201": { description: Comparison snapshot., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqComparison" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "409": { $ref: "#/components/responses/Conflict" } }
    get:
      operationId: listRfqComparisons
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: List immutable RFQ comparison snapshots
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: rfqId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Comparisons., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/RfqComparison" } } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" } }
  /rfq-comparisons/{comparisonId}:
    get:
      operationId: getRfqComparison
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: Get an immutable RFQ comparison
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: comparisonId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Comparison., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqComparison" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }
  /rfq-comparisons/{comparisonId}/evaluations:
    get:
      operationId: listRfqComparisonEvaluations
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: List human comparison evaluations
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: comparisonId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Evaluations., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/RfqComparisonEvaluation" } } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" } }
    post:
      operationId: createRfqComparisonEvaluation
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Record a human comparison evaluation
      description: Observations only; no weighted score or automatic winner is calculated.
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: comparisonId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateComparisonEvaluationRequest" } } } }
      responses: { "201": { description: Evaluation., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqComparisonEvaluation" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "409": { $ref: "#/components/responses/Conflict" } }
  /rfq-comparison-evaluations/{evaluationId}:
    get:
      operationId: getRfqComparisonEvaluation
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: Get a comparison evaluation
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: evaluationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Evaluation., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqComparisonEvaluation" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }
    patch:
      operationId: updateRfqComparisonEvaluation
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Update human evaluation observations
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: evaluationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/UpdateComparisonEvaluationRequest" } } } }
      responses: { "200": { description: Evaluation updated., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqComparisonEvaluation" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }

  /rfqs/{rfqId}/recommendations:
    post:
      operationId: createRfqRecommendation
      x-required-permission: rfq.manage
      x-building-scoped: true
      summary: Create a human RFQ recommendation
      description: >
        Requires a CLOSED RFQ and a current immutable comparison. This is an
        accountable human proposal, not automatic scoring or winner selection.
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: rfqId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateRfqRecommendationRequest" } } } }
      responses: { "201": { description: Recommendation pending approval., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqRecommendation" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "409": { $ref: "#/components/responses/Conflict" } }
    get:
      operationId: getRfqRecommendationForRfq
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: Get the RFQ recommendation
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: rfqId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Recommendation or null., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { allOf: [{ $ref: "#/components/schemas/RfqRecommendation" }], nullable: true } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" } }
  /rfq-recommendations/{recommendationId}:
    get:
      operationId: getRfqRecommendation
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: Get an RFQ recommendation
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: recommendationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Recommendation., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqRecommendation" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }
  /rfq-recommendations/{recommendationId}/award:
    post:
      operationId: awardRfqRecommendation
      x-required-permission: rfq.award
      x-building-scoped: true
      summary: Finalize an approved RFQ award
      description: >
        Requires an APPROVED `RFQ_AWARD` procurement approval. `rfq.award` is a
        sensitive permission and is not granted by default. Award creates no
        PO or commitment; PO conversion is a separate explicit command.
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: recommendationId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: false, content: { application/json: { schema: { $ref: "#/components/schemas/CreateRfqAwardRequest" } } } }
      responses: { "201": { description: Immutable award., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqAward" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "409": { $ref: "#/components/responses/Conflict" } }
  /rfq-awards/{awardId}:
    get:
      operationId: getRfqAward
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: Get an immutable RFQ award
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: awardId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Award., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqAward" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }
  /rfq-awards/{awardId}/convert-to-po:
    post:
      operationId: createRfqPoConversion
      x-required-permission: rfq.award
      x-building-scoped: true
      summary: Convert a finalized RFQ award into an existing DRAFT Purchase Order
      description: >
        Requires an exact finalized Vendor award and a matching READY existing
        PO Readiness. Vendor, currency, and PO Lines are derived from the
        immutable awarded quotation revision. Creates no commitment; PO
        issuance remains explicit and the ISSUED PO Line remains the only
        automatic priced commitment source.
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: awardId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/CreateRfqPoConversionRequest" } } } }
      responses: { "201": { description: DRAFT PO conversion and provenance., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqPoConversion" } } }] } } } }, "400": { $ref: "#/components/responses/BadRequest" }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "409": { $ref: "#/components/responses/Conflict" } }
  /rfq-po-conversions/{conversionId}:
    get:
      operationId: getRfqPoConversion
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: Get RFQ-to-PO conversion provenance
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: conversionId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Conversion provenance., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqPoConversion" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }
  /purchase-orders/{purchaseOrderId}/rfq-provenance:
    get:
      operationId: getRfqPoProvenance
      x-required-permission: rfq.read
      x-building-scoped: true
      summary: Get RFQ provenance for a Purchase Order
      description: >
        Returns typed RFQ → Award → Vendor → Quotation → Revision → PO → PO
        Line references. Vendor external sessions cannot access this projection.
      tags: [RFQ Procurement]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: purchaseOrderId, in: path, required: true, schema: { $ref: "#/components/schemas/Uuid" } }]
      responses: { "200": { description: Provenance., content: { application/json: { schema: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/RfqPoConversion" } } }] } } } }, "401": { $ref: "#/components/responses/Unauthorized" }, "403": { $ref: "#/components/responses/Forbidden" }, "404": { $ref: "#/components/responses/NotFound" } }

  # ------------------------------------------------------------------
  # CR-BE-FX-01 PART 02 — FX Rate Authority + Client FX Policy
  #
  # CANONICAL CONVENTION (frozen): 1 BASE = RATE x QUOTE.
  # This surface performs NO conversion of any kind.
  # ------------------------------------------------------------------
  /fx-rates:
    get:
      operationId: listFxRates
      summary: List FX rates
      description: >
        Platform-global rate authority. Rates contain no Client data, so no
        Client scoping applies; `fx_rate.read` is the whole authorization
        boundary. Filters are optional and an unrecognized value is ignored
        rather than guessed.
      tags: [FX]
      security: [{ bearerAuth: [] }]
      x-required-permission: fx_rate.read
      parameters:
        - name: baseCurrencyCode
          in: query
          schema: { type: string, pattern: "^[A-Z]{3}$" }
        - name: quoteCurrencyCode
          in: query
          schema: { type: string, pattern: "^[A-Z]{3}$" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/FxRateStatus" }
        - name: source
          in: query
          schema: { $ref: "#/components/schemas/FxRateSource" }
        - name: effectiveAt
          in: query
          description: Only rates whose closed-open window covers this instant.
          schema: { type: string, format: date-time }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 500, default: 100 }
        - name: offset
          in: query
          schema: { type: integer, minimum: 0, default: 0 }
      responses:
        "200": { $ref: "#/components/responses/FxRateListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createFxRate
      summary: Propose an FX rate (PENDING_APPROVAL)
      description: >
        Creates a rate in `PENDING_APPROVAL`. It is NOT usable by any consumer
        until a different authorized user approves it. Both currency legs must
        be ACTIVE in the Currency Master. `rate` is the number of QUOTE units
        per ONE unit of BASE (1 BASE = RATE x QUOTE) and must be a positive
        plain decimal — exponent notation is rejected. The effective window is
        closed-open `[effectiveFrom, effectiveTo)`; a future `effectiveFrom` is
        accepted as scheduling. Only `MANUAL_TREASURY` exists in FX-01: there is
        no provider ingestion.
      tags: [FX]
      security: [{ bearerAuth: [] }]
      x-required-permission: fx_rate.manage
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateFxRateRequest" }
      responses:
        "201": { $ref: "#/components/responses/FxRateSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /fx-rates/{rateId}:
    parameters:
      - $ref: "#/components/parameters/FxRateIdPath"
    get:
      operationId: getFxRate
      summary: Read one FX rate
      tags: [FX]
      security: [{ bearerAuth: [] }]
      x-required-permission: fx_rate.read
      responses:
        "200": { $ref: "#/components/responses/FxRateSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /fx-rates/{rateId}/events:
    parameters:
      - $ref: "#/components/parameters/FxRateIdPath"
    get:
      operationId: listFxRateEvents
      summary: Read the append-only audit trail of one FX rate
      description: >
        The five governed lifecycle events. Rows are append-only: the database
        rejects both UPDATE and DELETE, so this trail cannot be rewritten.
      tags: [FX]
      security: [{ bearerAuth: [] }]
      x-required-permission: fx_rate.read
      responses:
        "200": { $ref: "#/components/responses/FxRateEventListSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /fx-rates/{rateId}/approve:
    parameters:
      - $ref: "#/components/parameters/FxRateIdPath"
    post:
      operationId: approveFxRate
      summary: Activate a proposed FX rate (checker)
      description: >
        `PENDING_APPROVAL` -> `ACTIVE`. MAKER-CHECKER: the approver must not be
        the maker, otherwise 409 `FX_RATE_SELF_APPROVAL`.

        ACTIVE WINDOW: at most one ACTIVE rate may cover any instant for a
        (base, quote, rateType). If activation would overlap an existing ACTIVE
        rate the request fails closed with 409 `FX_RATE_ACTIVE_WINDOW_CONFLICT`.
        The incumbent window is never shortened, the incumbent is never silently
        superseded, and no latest-rate fallback is applied.
      tags: [FX]
      security: [{ bearerAuth: [] }]
      x-required-permission: fx_rate.approve
      responses:
        "200": { $ref: "#/components/responses/FxRateSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /fx-rates/{rateId}/reject:
    parameters:
      - $ref: "#/components/parameters/FxRateIdPath"
    post:
      operationId: rejectFxRate
      summary: Reject a proposed FX rate (terminal)
      description: >
        `PENDING_APPROVAL` -> `REJECTED`. `REJECTED` is terminal: a rejected
        rate can never be approved or revived. The actor is recorded and an
        `FX_RATE_REJECTED` audit row is written in the same transaction.
      tags: [FX]
      security: [{ bearerAuth: [] }]
      x-required-permission: fx_rate.approve
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FxRateReasonRequest" }
      responses:
        "200": { $ref: "#/components/responses/FxRateSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /fx-rates/{rateId}/supersede:
    parameters:
      - $ref: "#/components/parameters/FxRateIdPath"
    post:
      operationId: supersedeFxRate
      summary: Correct an ACTIVE FX rate by supersession
      description: >
        The ONLY sanctioned correction path. A stored rate value is never
        modified. One transaction creates an ACTIVE successor and moves the
        incumbent to `SUPERSEDED`, linking both directions exactly
        (`successor.supersedesRateId` = incumbent id,
        `incumbent.supersededByRateId` = successor id). If the successor cannot
        be inserted the whole transaction rolls back and the incumbent stays
        ACTIVE.

        The currency pair is inherited and cannot be changed. Omitting `rate`
        performs a window-only correction that reuses the incumbent's exact
        stored decimal. The successor is authored by the incumbent's maker and
        approved by the superseding actor, so the existing maker-checker
        invariant requires the two to differ (409 `FX_RATE_SELF_APPROVAL`).
      tags: [FX]
      security: [{ bearerAuth: [] }]
      x-required-permission: fx_rate.approve
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SupersedeFxRateRequest" }
      responses:
        "200": { $ref: "#/components/responses/FxRateSupersessionSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /fx-rates/{rateId}/deactivate:
    parameters:
      - $ref: "#/components/parameters/FxRateIdPath"
    post:
      operationId: deactivateFxRate
      summary: Withdraw an ACTIVE FX rate (terminal)
      description: >
        `ACTIVE` -> `INACTIVE`, with no replacement. `INACTIVE` is terminal.
        The actor is recorded and an `FX_RATE_DEACTIVATED` audit row is written
        in the same transaction.
      tags: [FX]
      security: [{ bearerAuth: [] }]
      x-required-permission: fx_rate.approve
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FxRateReasonRequest" }
      responses:
        "200": { $ref: "#/components/responses/FxRateSuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /clients/{clientId}/fx-policy:
    parameters:
      - $ref: "#/components/parameters/FxClientIdPath"
    get:
      operationId: getClientFxPolicy
      summary: Read a Client FX policy
      description: >
        FAIL-CLOSED READ MODEL. `policy` is `null` when the Client has no
        policy, and `fxAvailable` is then `false` — absence is never treated as
        permission. `fxAvailable` is also `false` when the policy is disabled or
        permits no rate source. `baseCurrencyCode` is returned for display only
        and is never injected into a write. Requires Client reachability; an
        unreachable Client returns 403.
      tags: [FX]
      security: [{ bearerAuth: [] }]
      x-required-permission: client_fx_policy.read
      responses:
        "200": { $ref: "#/components/responses/ClientFxPolicyReadSuccess" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      operationId: setClientFxPolicy
      summary: Set or replace a Client FX policy
      description: >
        FAIL-CLOSED WRITE. `fxEnabled` and `inversePermitted` default to `false`
        when omitted; an empty `permittedSources` permits nothing.

        NO SILENT INJECTION: `reportingCurrencyCode` must be stated explicitly
        and must equal the Client's `base_currency_code`
        (400 `FX_POLICY_REPORTING_CURRENCY_NOT_BASE` otherwise). It is never
        copied from the base or the default transaction currency — FX-01 does
        not add a second reporting axis. There is no Building override.

        Writes exactly one Client-scoped audit event
        (`CLIENT_FX_POLICY_SET` on first write, `CLIENT_FX_POLICY_CHANGED`
        afterwards) through the existing operational-events architecture.
      tags: [FX]
      security: [{ bearerAuth: [] }]
      x-required-permission: client_fx_policy.manage
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SetClientFxPolicyRequest" }
      responses:
        "200": { $ref: "#/components/responses/ClientFxPolicySuccess" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }



  /clients/{clientId}/visitors:
    post:
      operationId: createVisitor
      x-required-permission: visitor.manage
      summary: Register a shared Visitor identity for a Client
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01A (BE-13A). Requires
        `visitor.manage` plus access to `{clientId}`. The Visitor is the
        single shared Client-scoped identity master of the whole BE-13 Front
        Desk domain - Invitation, Expected Visitor, Walk-In, Contractor and
        Delivery/Courier flows all reference it. Created `ACTIVE` unless an
        explicit `status` is supplied. `identityNumber` requires a concrete
        `identityType` (400 `VISITOR_IDENTITY_NUMBER_REQUIRES_TYPE`
        otherwise). Duplicate identity document (identityType +
        identityNumber) within the Client is rejected 409
        `VISITOR_IDENTITY_ALREADY_EXISTS` - reuse the existing identity.
        Data minimization: only operationally necessary identity data is
        stored; no photo binaries, birthdates or addresses.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateVisitorRequest" }
      responses:
        "201":
          description: Created Visitor identity.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Visitor" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listVisitors
      x-required-permission: visitor.read
      summary: List Visitor identities of a Client
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01A (BE-13A). Requires
        `visitor.read` plus access to `{clientId}`. Returns Visitor
        identities of that Client. Optional filters: `search`
        (case-insensitive substring over full name), `identityType`,
        `identityNumber`, `phone`, `email`, `status`.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ClientIdPath"
        - name: search
          in: query
          schema: { type: string, maxLength: 256 }
        - name: identityType
          in: query
          schema: { $ref: "#/components/schemas/VisitorIdentityType" }
        - name: identityNumber
          in: query
          schema: { type: string, maxLength: 128 }
        - name: phone
          in: query
          schema: { type: string, maxLength: 32 }
        - name: email
          in: query
          schema: { type: string, maxLength: 254 }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/VisitorStatus" }
      responses:
        "200":
          description: Visitor identity list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Visitor" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /visitors/{id}:
    get:
      operationId: getVisitor
      x-required-permission: visitor.read
      summary: Read one Visitor identity
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01A (BE-13A). Requires
        `visitor.read` plus access to the Visitor's Client. There is no
        delete endpoint: identities are deactivated (`INACTIVE`) or blocked
        (`BLOCKED`), never destroyed, because later Visit records must keep
        a valid identity reference.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VisitorIdPath"
      responses:
        "200":
          description: The Visitor identity (any status).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Visitor" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateVisitor
      x-required-permission: visitor.manage
      summary: Edit a Visitor identity (status via INACTIVE/BLOCKED)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01A (BE-13A). Requires
        `visitor.manage`. At least one updatable field is required; empty
        bodies are rejected 400. Setting an `identityNumber` requires a
        concrete `identityType` (400 otherwise); changing the identity
        document onto an existing duplicate is rejected 409
        `VISITOR_IDENTITY_ALREADY_EXISTS`. `clientId` and id are immutable.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VisitorIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateVisitorRequest" }
      responses:
        "200":
          description: Updated Visitor identity.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Visitor" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /expected-visitors:
    post:
      operationId: createExpectedVisitor
      x-required-permission: expected_visitor.manage
      summary: Create a front-desk Expected Visitor record
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01A (BE-13C). Requires
        `expected_visitor.manage` plus Client/Building scope access.
        References the shared BE-13A visitor identity - never duplicates it.
        Two creation modes: standalone (`buildingId`, `visitorId`,
        `expectedArrivalAt`, `purpose` and a resolvable Host required - 400
        otherwise) or from a PENDING BE-13B invitation via
        `visitorInvitationId`, where Building/Visitor/Host/window/purpose
        default from the invitation and explicitly supplied values must stay
        consistent with it (same Building, same Visitor; 400
        `EXPECTED_VISITOR_INVITATION_MISMATCH` otherwise; cancelled
        invitation 400 `EXPECTED_VISITOR_INVITATION_CANCELLED`). At most one
        non-CANCELLED Expected Visitor exists per invitation - 409
        `EXPECTED_VISITOR_INVITATION_ALREADY_USED`. Lifecycle: EXPECTED ->
        CANCELLED (terminal).
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateExpectedVisitorRequest" }
      responses:
        "201":
          description: Created Expected Visitor record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ExpectedVisitor" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listExpectedVisitors
      x-required-permission: expected_visitor.read
      summary: List Expected Visitors (scoped)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01A (BE-13C). Requires
        `expected_visitor.read`. Lists Expected Visitor records inside the
        caller's accessible Client/Building scope. Optional filters:
        `buildingId`, `visitorId`, `visitorInvitationId`, `hostUserId`,
        `status`, `expectedFrom` / `expectedTo` (inclusive ISO bounds on the
        expected arrival).
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: visitorId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: visitorInvitationId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: hostUserId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/ExpectedVisitorStatus" }
        - name: expectedFrom
          in: query
          schema: { type: string, format: date-time }
        - name: expectedTo
          in: query
          schema: { type: string, format: date-time }
      responses:
        "200":
          description: Expected Visitor list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/ExpectedVisitor" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /expected-visitors/{id}:
    get:
      operationId: getExpectedVisitor
      x-required-permission: expected_visitor.read
      summary: Read one Expected Visitor record
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01A (BE-13C). Requires
        `expected_visitor.read` plus scope access to the record's
        Client/Building.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ExpectedVisitorIdPath"
      responses:
        "200":
          description: The Expected Visitor record (any status).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ExpectedVisitor" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateExpectedVisitor
      x-required-permission: expected_visitor.manage
      summary: Edit a front-desk Expectation
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01A (BE-13C). Requires
        `expected_visitor.manage`. Edits Host references, arrival/departure
        window, `purpose` and `notes` only; at least one field is required.
        A CANCELLED record can no longer be modified (409
        `EXPECTED_VISITOR_ALREADY_CANCELLED`). Building, Visitor and
        invitation linkage are immutable.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ExpectedVisitorIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateExpectedVisitorRequest" }
      responses:
        "200":
          description: Updated Expected Visitor record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ExpectedVisitor" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /expected-visitors/{id}/cancel:
    post:
      operationId: cancelExpectedVisitor
      x-required-permission: expected_visitor.manage
      summary: Cancel an Expected Visitor (EXPECTED -> CANCELLED, terminal)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01A (BE-13C). Requires
        `expected_visitor.manage`. EXPECTED -> CANCELLED. CANCELLED is
        terminal - repeated cancellation or any later modification is
        rejected 409 `EXPECTED_VISITOR_ALREADY_CANCELLED`. No request body.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ExpectedVisitorIdPath"
      responses:
        "200":
          description: Cancelled Expected Visitor record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ExpectedVisitor" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /walk-in-visits:
    post:
      operationId: createWalkInVisit
      x-required-permission: walk_in_visit.manage
      summary: Register a Walk-In visit (front-desk guest book entry)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01A (BE-13D). Requires
        `walk_in_visit.manage` plus Client/Building scope access. A Walk-In
        is the guest-book entry for a visitor arriving WITHOUT a prior
        invitation. Exactly ONE identity reference is required - `visitorId`
        to reuse an existing matched identity or `newVisitor` to register a
        new identity inline through the BE-13A visitor service (400
        `WALK_IN_VISIT_VISITOR_REFERENCE_REQUIRED` otherwise); this module
        never writes the visitor master directly. Host context is optional.
        `arrivedAt` defaults to the current time and must not be in the
        future (400 `WALK_IN_VISIT_ARRIVAL_IN_FUTURE`). One open
        (REGISTERED) entry per (building, visitor) - 409
        `WALK_IN_VISIT_ALREADY_REGISTERED`; cancelled entries never block
        re-registration. Lifecycle: REGISTERED -> CANCELLED (terminal).
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateWalkInVisitRequest" }
      responses:
        "201":
          description: Registered Walk-In visit.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WalkInVisit" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listWalkInVisits
      x-required-permission: walk_in_visit.read
      summary: List Walk-In visits (scoped guest book)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01A (BE-13D). Requires
        `walk_in_visit.read`. Lists Walk-In visit entries inside the
        caller's accessible Client/Building scope. Optional filters:
        `buildingId`, `visitorId`, `hostUserId`, `status`, `arrivedFrom` /
        `arrivedTo` (inclusive ISO bounds on the arrival).
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: visitorId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: hostUserId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/WalkInVisitStatus" }
        - name: arrivedFrom
          in: query
          schema: { type: string, format: date-time }
        - name: arrivedTo
          in: query
          schema: { type: string, format: date-time }
      responses:
        "200":
          description: Walk-In visit list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/WalkInVisit" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /walk-in-visits/{id}:
    get:
      operationId: getWalkInVisit
      x-required-permission: walk_in_visit.read
      summary: Read one Walk-In visit entry
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01A (BE-13D). Requires
        `walk_in_visit.read` plus scope access to the entry's
        Client/Building.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WalkInVisitIdPath"
      responses:
        "200":
          description: The Walk-In visit entry (any status).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WalkInVisit" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateWalkInVisit
      x-required-permission: walk_in_visit.manage
      summary: Edit a Walk-In visit entry
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01A (BE-13D). Requires
        `walk_in_visit.manage`. Edits Host references, `purpose`,
        `arrivedAt` and `frontDeskNotes` only; at least one field is
        required. A CANCELLED entry can no longer be modified (409
        `WALK_IN_VISIT_ALREADY_CANCELLED`). Building and Visitor linkage
        are immutable.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WalkInVisitIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateWalkInVisitRequest" }
      responses:
        "200":
          description: Updated Walk-In visit entry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WalkInVisit" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /walk-in-visits/{id}/cancel:
    post:
      operationId: cancelWalkInVisit
      x-required-permission: walk_in_visit.manage
      summary: Cancel a Walk-In visit (REGISTERED -> CANCELLED, terminal)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01A (BE-13D). Requires
        `walk_in_visit.manage`. REGISTERED -> CANCELLED. CANCELLED is
        terminal - repeated cancellation or any later modification is
        rejected 409 `WALK_IN_VISIT_ALREADY_CANCELLED`. No request body.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WalkInVisitIdPath"
      responses:
        "200":
          description: Cancelled Walk-In visit entry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WalkInVisit" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }


  /visit-check-ins:
    post:
      operationId: checkInVisit
      x-required-permission: visit_check_in.manage
      summary: Check in a visit (backend-authoritative visit lifecycle open)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01B1 (BE-13G). Requires
        `visit_check_in.manage` plus Client/Building scope access. Opens
        the ONE controlled visit lifecycle over an existing BE-13 visit
        context: exactly ONE of `expectedVisitorId` / `walkInVisitId` is
        required (400 `VISIT_CHECK_IN_VISIT_REFERENCE_REQUIRED` otherwise);
        Client, Building and visitor identity all derive from the visit -
        the visitor identity is never duplicated. A CANCELLED visit cannot
        check in (400 `VISIT_CHECK_IN_VISIT_CANCELLED`); the visitor
        identity must be ACTIVE (400 otherwise). Host-confirmation gate:
        when a BE-13F confirmation exists for the visit it must be
        CONFIRMED - PENDING blocks 409 `VISIT_CHECK_IN_CONFIRMATION_PENDING`,
        REJECTED blocks 409 `VISIT_CHECK_IN_CONFIRMATION_REJECTED`; visits
        without a confirmation record may check in. At most one active
        check-in per visit - repeat check-in is rejected 409
        `VISIT_CHECK_IN_ALREADY_CHECKED_IN`. `checkedInAt` defaults to now
        and must not be in the future (400 `VISIT_CHECK_IN_TIME_IN_FUTURE`).
        Lifecycle: CHECKED_IN -> CHECKED_OUT | CANCELLED (both terminal).
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateVisitCheckInRequest" }
      responses:
        "201":
          description: Opened Check-In record (status CHECKED_IN).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VisitCheckIn" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listVisitCheckIns
      x-required-permission: visit_check_in.read
      summary: List Check-In records (scoped visit history)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01B1 (BE-13G/H). Requires
        `visit_check_in.read`. Lists Check-In records inside the caller's
        accessible Client/Building scope. Optional filters: `buildingId`,
        `visitorId`, `expectedVisitorId`, `walkInVisitId`, `status`,
        `checkedInFrom` / `checkedInTo` (inclusive ISO bounds on
        `checkedInAt`) and `checkedOutFrom` / `checkedOutTo` (inclusive ISO
        bounds on `checkedOutAt`).
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: visitorId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: expectedVisitorId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: walkInVisitId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/VisitCheckInStatus" }
        - name: checkedInFrom
          in: query
          schema: { type: string, format: date-time }
        - name: checkedInTo
          in: query
          schema: { type: string, format: date-time }
        - name: checkedOutFrom
          in: query
          schema: { type: string, format: date-time }
        - name: checkedOutTo
          in: query
          schema: { type: string, format: date-time }
      responses:
        "200":
          description: Check-In record list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/VisitCheckIn" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /visit-check-ins/{id}:
    get:
      operationId: getVisitCheckIn
      x-required-permission: visit_check_in.read
      summary: Read one Check-In record
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01B1 (BE-13G/H). Requires
        `visit_check_in.read` plus scope access to the record's
        Client/Building. Check-Out preserves the original check-in history
        on the same row - `checkedIn*` fields stay untouched.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VisitCheckInIdPath"
      responses:
        "200":
          description: The Check-In record (any status).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VisitCheckIn" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /visit-check-ins/{id}/check-out:
    post:
      operationId: checkOutVisit
      x-required-permission: visit_check_in.manage
      summary: Check out an active visit (CHECKED_IN -> CHECKED_OUT, terminal)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01B1 (BE-13H). Requires
        `visit_check_in.manage`. Closes the active visit while preserving
        the check-in history. A non-active record is rejected 409
        (`VISIT_CHECK_OUT_NOT_ACTIVE` for CANCELLED,
        `VISIT_CHECK_OUT_ALREADY_CHECKED_OUT` when already checked out).
        An ACTIVE BE-13I Visitor Pass must be returned or cancelled first -
        visit closure refuses to leave a pass unresolved (409
        `VISITOR_PASS_ACTIVE_AT_VISIT_CLOSURE`). `checkedOutAt` defaults to
        now, must not be in the future (400
        `VISIT_CHECK_OUT_TIME_IN_FUTURE`) and must not precede the original
        check-in (400 `VISIT_CHECK_OUT_BEFORE_CHECK_IN`). CHECKED_OUT is
        terminal - a completed visit never re-activates.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VisitCheckInIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CheckOutVisitRequest" }
      responses:
        "200":
          description: Closed Check-In record (status CHECKED_OUT).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VisitCheckIn" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /visit-check-ins/{id}/cancel:
    post:
      operationId: cancelVisitCheckIn
      x-required-permission: visit_check_in.manage
      summary: Cancel a Check-In (CHECKED_IN -> CANCELLED; mistake reversal only)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01B1 (BE-13G). Requires
        `visit_check_in.manage`. Cancels an active Check-In (CHECKED_IN ->
        CANCELLED) - a mistake reversal only, not a visit closure.
        CANCELLED is terminal: a cancelled check-in cannot be cancelled
        again (409 `VISIT_CHECK_IN_ALREADY_CANCELLED`) and cannot be
        checked out (409 `VISIT_CHECK_OUT_NOT_ACTIVE`); cancelling frees
        the visit for a fresh check-in. No request body.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VisitCheckInIdPath"
      responses:
        "200":
          description: Cancelled Check-In record (status CANCELLED).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VisitCheckIn" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }


  /visitor-passes:
    post:
      operationId: issueVisitorPass
      x-required-permission: visitor_pass.manage
      summary: Issue a Visitor Pass for an active visit
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01B2 (BE-13I). Requires
        `visitor_pass.manage` plus Client/Building scope access. A pass is
        an administrative visitor credential bound to ONE BE-13G/H visit
        lifecycle (`visitCheckInId`): the referenced check-in must exist
        (404 `VISITOR_PASS_VISIT_NOT_FOUND`), its Building must equal the
        request `buildingId` (400 `VISITOR_PASS_BUILDING_MISMATCH`) and it
        must still be active - CHECKED_IN (409
        `VISITOR_PASS_VISIT_NOT_ACTIVE` otherwise). `passCode` is
        normalized to trimmed uppercase, must be 1-64 letters / digits /
        hyphens / underscores and is unique per Building (409
        `VISITOR_PASS_CODE_ALREADY_EXISTS`). At most one ACTIVE pass per
        visit (409 `VISITOR_PASS_ACTIVE_ALREADY_EXISTS`). `issuedAt`
        defaults to now. Physical access-control hardware and integrations
        are outside this contract. Lifecycle: ACTIVE -> RETURNED |
        CANCELLED (both terminal).
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/IssueVisitorPassRequest" }
      responses:
        "201":
          description: Issued Visitor Pass (status ACTIVE).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VisitorPass" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listVisitorPasses
      x-required-permission: visitor_pass.read
      summary: List Visitor Passes (scoped)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01B2 (BE-13I). Requires
        `visitor_pass.read`. Lists Visitor Passes inside the caller's
        accessible Client/Building scope. Optional filters: `buildingId`,
        `status`, `visitCheckInId`.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/VisitorPassStatus" }
        - name: visitCheckInId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Visitor Pass list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/VisitorPass" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /visitor-passes/{id}:
    get:
      operationId: getVisitorPass
      x-required-permission: visitor_pass.read
      summary: Read one Visitor Pass
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01B2 (BE-13I). Requires
        `visitor_pass.read` plus scope access to the pass's Client/Building.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VisitorPassIdPath"
      responses:
        "200":
          description: The Visitor Pass (any status).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VisitorPass" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /visitor-passes/{id}/return:
    post:
      operationId: returnVisitorPass
      x-required-permission: visitor_pass.manage
      summary: Return a Visitor Pass (ACTIVE -> RETURNED, terminal)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01B2 (BE-13I). Requires
        `visitor_pass.manage`. Closes an ACTIVE pass as RETURNED; a
        non-ACTIVE pass is rejected 409 `VISITOR_PASS_NOT_ACTIVE`.
        `returnedAt` defaults to now and must be a valid time (400
        `VISITOR_PASS_RETURN_TIME_INVALID`). BE-13H normally requires the
        ACTIVE pass of a visit to be returned or cancelled before that
        visit may be closed (see check-out). Optional request body.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VisitorPassIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ReturnVisitorPassRequest" }
      responses:
        "200":
          description: Returned Visitor Pass (status RETURNED).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VisitorPass" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /visitor-passes/{id}/cancel:
    post:
      operationId: cancelVisitorPass
      x-required-permission: visitor_pass.manage
      summary: Cancel a Visitor Pass (ACTIVE -> CANCELLED, terminal)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01B2 (BE-13I). Requires
        `visitor_pass.manage`. Cancels an ACTIVE pass; a non-ACTIVE pass is
        rejected 409 `VISITOR_PASS_NOT_ACTIVE`. No request body.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VisitorPassIdPath"
      responses:
        "200":
          description: Cancelled Visitor Pass (status CANCELLED).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VisitorPass" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }


  /visitors/{visitorId}/photos:
    post:
      operationId: attachVisitorPhoto
      x-required-permission: visitor_photo.manage
      summary: Attach a Visitor Photo / identity document reference
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01B3 (BE-13E). Requires
        `visitor_photo.manage` plus Client scope access. Attaches a safe
        file REFERENCE plus metadata to the shared BE-13A visitor
        identity, following the BE-07 evidence convention - this endpoint
        accepts metadata/reference JSON only and stores NO image binary in
        PostgreSQL (`fileSize` limit 50 MB mirrors the BE-07 evidence
        limit). `requestOcr: true` marks the photo queued for external OCR
        processing at attach time (`ocrStatus` = PENDING, optional
        `ocrProvider`); otherwise `ocrStatus` stays NOT_REQUESTED. There is
        no custom OCR/AI engine - only metadata from an external processor
        is recorded. Normalization of extracted identity values is never
        applied silently: staged OCR output requires an explicit review.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VisitorIdNestedPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateVisitorPhotoRequest" }
      responses:
        "201":
          description: Attached photo readiness record (status ACTIVE).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VisitorPhoto" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    get:
      operationId: listVisitorPhotos
      x-required-permission: visitor_photo.read
      summary: List photo readiness records of a Visitor
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01B3 (BE-13E). Requires
        `visitor_photo.read` plus Client scope access. Optional filters:
        `photoType`, `ocrStatus`, `reviewStatus`, `status`.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VisitorIdNestedPath"
        - name: photoType
          in: query
          schema: { $ref: "#/components/schemas/VisitorPhotoType" }
        - name: ocrStatus
          in: query
          schema: { $ref: "#/components/schemas/VisitorPhotoOcrStatus" }
        - name: reviewStatus
          in: query
          schema: { $ref: "#/components/schemas/VisitorPhotoReviewStatus" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/VisitorPhotoStatus" }
      responses:
        "200":
          description: Photo readiness list of the Visitor.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/VisitorPhoto" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /visitor-photos/{id}:
    get:
      operationId: getVisitorPhoto
      x-required-permission: visitor_photo.read
      summary: Read one photo readiness record
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01B3 (BE-13E). Requires
        `visitor_photo.read` plus Client scope access.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VisitorPhotoIdPath"
      responses:
        "200":
          description: The photo readiness record (any status).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VisitorPhoto" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /visitor-photos/{id}/ocr-result:
    post:
      operationId: recordVisitorPhotoOcrResult
      x-required-permission: visitor_photo.manage
      summary: Record external OCR result metadata on a photo
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01B3 (BE-13E). Requires
        `visitor_photo.manage`. Records external OCR processor metadata on
        the readiness row: PROCESSED carries the extracted identity fields,
        FAILED carries the error text; `ocrProcessedAt` defaults to now for
        PROCESSED / FAILED. OCR must have been requested - a photo with
        `ocrStatus` NOT_REQUESTED is rejected 409
        `VISITOR_PHOTO_OCR_NOT_REQUESTED`. Extracted fields are STAGED on
        the photo row only and never silently touch the authoritative
        BE-13A visitor identity.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VisitorPhotoIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RecordVisitorPhotoOcrResultRequest" }
      responses:
        "200":
          description: Photo readiness record after OCR metadata update.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VisitorPhoto" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /visitor-photos/{id}/review:
    post:
      operationId: reviewVisitorPhoto
      x-required-permission: visitor_photo.manage
      summary: Review staged OCR output (APPLY or REJECT)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01B3 (BE-13E). Requires
        `visitor_photo.manage`. Explicit review decision over staged OCR
        output: `decision: APPLY` routes the staged identity fields through
        the BE-13A visitor service (optionally narrowed by `applyFullName`
        / `applyIdentity`; defaults to all non-null staged fields) - requires
        OCR PROCESSED (409 `VISITOR_PHOTO_OCR_NOT_PROCESSED` otherwise) and
        at least one staged value (400 `VISITOR_PHOTO_NOTHING_TO_APPLY`);
        `decision: REJECT` discards the staged output. An already reviewed
        record is rejected 409 `VISITOR_PHOTO_ALREADY_REVIEWED`.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VisitorPhotoIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ReviewVisitorPhotoRequest" }
      responses:
        "200":
          description: Reviewed photo readiness record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VisitorPhoto" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /visitor-photos/{id}/remove:
    post:
      operationId: removeVisitorPhoto
      x-required-permission: visitor_photo.manage
      summary: Remove a photo readiness record (ACTIVE -> REMOVED)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01B3 (BE-13E). Requires
        `visitor_photo.manage`. Soft-removes the readiness record
        (ACTIVE -> REMOVED); the row and its history are retained. An
        already REMOVED record is rejected 409 `VISITOR_PHOTO_REMOVED`. No
        request body.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VisitorPhotoIdPath"
      responses:
        "200":
          description: Removed photo readiness record (status REMOVED).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VisitorPhoto" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }


  /front-desk-logs:
    get:
      operationId: listFrontDeskLogs
      x-required-permission: front_desk_log.read
      summary: List Front Desk activity log entries (read-only projection)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C1 (BE-13L). Requires
        `front_desk_log.read`. Chronological READ-ONLY projection over the
        authoritative BE-13 activities - events are projected at read time;
        no Visitor or Visit data is copied into another persistence engine
        and no write operations exist on this surface. Optional filters:
        `buildingId`, `visitorId`, `activityType`, `occurredFrom` /
        `occurredTo` (inclusive ISO timestamp bounds).
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: visitorId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: activityType
          in: query
          schema: { $ref: "#/components/schemas/FrontDeskActivityType" }
        - name: occurredFrom
          in: query
          schema: { type: string, format: date-time }
        - name: occurredTo
          in: query
          schema: { type: string, format: date-time }
      responses:
        "200":
          description: Front Desk activity entry list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/FrontDeskLog" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /front-desk-logs/{id}:
    get:
      operationId: getFrontDeskLog
      x-required-permission: front_desk_log.read
      summary: Read one Front Desk activity log entry (read-only)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C1 (BE-13L). Requires
        `front_desk_log.read`. Reads one projected activity entry - this
        read-model is read-only by design; no reporting/BI or editable
        authority exists.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/FrontDeskLogIdPath"
      responses:
        "200":
          description: The Front Desk activity entry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/FrontDeskLog" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /visitor-invitations:
    post:
      operationId: createVisitorInvitation
      x-required-permission: visitor_invitation.manage
      summary: Create a Visitor Invitation (visit-planning record)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C1 (BE-13B). Requires
        `visitor_invitation.manage` plus Client/Building scope access. A
        Visitor Invitation is a visit-planning record: a known BE-13A
        Visitor identity is invited to a Building for an expected time
        window - it never duplicates visitor personal data and is distinct
        from the BE-01 auth `invitations` module (user-account onboarding).
        At least one host reference (`hostUserId`, `hostWorkforceId` or
        `hostName`) is required (400 `VISITOR_INVITATION_HOST_REQUIRED`);
        a BLOCKED or INACTIVE visitor identity and a departure not after
        the arrival are rejected 400. This surface carries no
        notification, delivery or token semantics. Lifecycle: PENDING ->
        CANCELLED (terminal; never re-activated).
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateVisitorInvitationRequest" }
      responses:
        "201":
          description: Created Visitor Invitation (status PENDING).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VisitorInvitation" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    get:
      operationId: listVisitorInvitations
      x-required-permission: visitor_invitation.read
      summary: List Visitor Invitations (scoped)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C1 (BE-13B). Requires
        `visitor_invitation.read`. Lists invitations inside the caller's
        accessible Client/Building scope. Optional filters: `buildingId`,
        `visitorId`, `hostUserId`, `status`, `expectedFrom` /
        `expectedTo` (inclusive ISO bounds on the expected arrival).
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: visitorId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: hostUserId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/VisitorInvitationStatus" }
        - name: expectedFrom
          in: query
          schema: { type: string, format: date-time }
        - name: expectedTo
          in: query
          schema: { type: string, format: date-time }
      responses:
        "200":
          description: Visitor Invitation list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/VisitorInvitation" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /visitor-invitations/{id}:
    get:
      operationId: getVisitorInvitation
      x-required-permission: visitor_invitation.read
      summary: Read one Visitor Invitation
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C1 (BE-13B). Requires
        `visitor_invitation.read` plus scope access to the invitation's
        Client/Building.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VisitorInvitationIdPath"
      responses:
        "200":
          description: The Visitor Invitation (any status).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VisitorInvitation" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateVisitorInvitation
      x-required-permission: visitor_invitation.manage
      summary: Edit a Visitor Invitation (PENDING only)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C1 (BE-13B). Requires
        `visitor_invitation.manage`. Edits host references, expected
        window, `purpose` and `notes`; at least one field is required.
        A CANCELLED invitation is terminal - never re-activated (409
        `VISITOR_INVITATION_ALREADY_CANCELLED`). Building, Visitor and
        Client linkage are immutable.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VisitorInvitationIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateVisitorInvitationRequest" }
      responses:
        "200":
          description: Updated Visitor Invitation.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VisitorInvitation" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /visitor-invitations/{id}/cancel:
    post:
      operationId: cancelVisitorInvitation
      x-required-permission: visitor_invitation.manage
      summary: Cancel a Visitor Invitation (PENDING -> CANCELLED, terminal)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C1 (BE-13B). Requires
        `visitor_invitation.manage`. PENDING -> CANCELLED. CANCELLED is
        terminal: repeated cancellation or any later modification is
        rejected 409 `VISITOR_INVITATION_ALREADY_CANCELLED`. No request
        body.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VisitorInvitationIdPath"
      responses:
        "200":
          description: Cancelled Visitor Invitation (status CANCELLED).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VisitorInvitation" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }


  /contractor-visitors:
    post:
      operationId: createContractorVisitor
      x-required-permission: contractor_visitor.manage
      summary: Register contractor metadata on an existing visit
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C2 (BE-13J). Requires
        `contractor_visitor.manage` plus Client/Building scope access.
        Contractor-specific metadata bound to exactly ONE existing BE-13
        visit - `expectedVisitorId` XOR `walkInVisitId` is required (400
        `CONTRACTOR_VISITOR_VISIT_REFERENCE_REQUIRED` otherwise); Visitor
        identity and operational entry/exit stay owned by the BE-13A
        Visitor, Check-In/Check-Out and Visitor Pass modules. Building and
        visitor derive from the visit (409
        `CONTRACTOR_VISITOR_ALREADY_EXISTS` for a second contractor record
        on the same visit; 400 for cancelled visit / non-active identity /
        missing host / mismatched or inactive host / functional location
        references). Optional `buildingId` / `visitorId` inputs are
        consistency assertions only. Lifecycle: REGISTERED -> CANCELLED
        (terminal).
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateContractorVisitorRequest" }
      responses:
        "201":
          description: Registered Contractor Visitor record (status REGISTERED).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ContractorVisitor" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listContractorVisitors
      x-required-permission: contractor_visitor.read
      summary: List Contractor Visitor records (scoped)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C2 (BE-13J). Requires
        `contractor_visitor.read`. Lists records inside the caller's
        accessible Client/Building scope. Optional filters: `buildingId`,
        `visitorId`, `expectedVisitorId`, `walkInVisitId`, `status`,
        `search` (case-insensitive over company, purpose, PIC,
        work-location and notes).
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: visitorId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: expectedVisitorId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: walkInVisitId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/ContractorVisitorStatus" }
        - name: search
          in: query
          schema: { type: string, maxLength: 255 }
      responses:
        "200":
          description: Contractor Visitor record list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/ContractorVisitor" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /contractor-visitors/{id}:
    get:
      operationId: getContractorVisitor
      x-required-permission: contractor_visitor.read
      summary: Read one Contractor Visitor record
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C2 (BE-13J). Requires
        `contractor_visitor.read` plus scope access to the record's
        Client/Building.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ContractorVisitorIdPath"
      responses:
        "200":
          description: The Contractor Visitor record (any status).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ContractorVisitor" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateContractorVisitor
      x-required-permission: contractor_visitor.manage
      summary: Edit contractor metadata or cancel the record
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C2 (BE-13J). Requires
        `contractor_visitor.manage`. Edits contractor company / purpose /
        responsible-host references / functional-location / work-location /
        notes; `status: CANCELLED` performs the lifecycle cancellation
        (REGISTERED -> CANCELLED, terminal). At least one field is
        required. An already CANCELLED record is rejected 409
        `CONTRACTOR_VISITOR_ALREADY_CANCELLED`. Visit / Building / visitor
        linkage are immutable; visit-termination states are guarded (409
        `CONTRACTOR_VISITOR_ACTIVE_VISIT` /
        `CONTRACTOR_VISITOR_CANCELLED_CHECK_IN`).
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ContractorVisitorIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateContractorVisitorRequest" }
      responses:
        "200":
          description: Updated Contractor Visitor record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/ContractorVisitor" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /host-confirmations:
    post:
      operationId: createHostConfirmation
      x-required-permission: host_confirmation.manage
      summary: Request a Host confirmation over a visit
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C2 (BE-13F). Requires
        `host_confirmation.manage` plus Client/Building scope access.
        Backend-authoritative host confirmation over an existing BE-13
        visit context - `expectedVisitorId` XOR `walkInVisitId` is
        required (400 `HOST_CONFIRMATION_VISIT_REFERENCE_REQUIRED`
        otherwise) and one confirmation exists per visit (409
        `HOST_CONFIRMATION_ALREADY_EXISTS`). Host references default from
        the visit record and may be overridden with validation; at least
        one host reference must resolve (400
        `HOST_CONFIRMATION_HOST_REQUIRED`). A cancelled visit cannot be
        confirmed (400). No notification delivery, approval workflow,
        escalation or timeout semantics exist on this contract.
        Lifecycle: PENDING -> CONFIRMED | REJECTED (single, terminal
        decision).
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateHostConfirmationRequest" }
      responses:
        "201":
          description: Created Host Confirmation (status PENDING).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HostConfirmation" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listHostConfirmations
      x-required-permission: host_confirmation.read
      summary: List Host Confirmations (scoped)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C2 (BE-13F). Requires
        `host_confirmation.read`. Optional filters: `buildingId`,
        `expectedVisitorId`, `walkInVisitId`, `hostUserId`, `status`.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: expectedVisitorId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: walkInVisitId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: hostUserId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/HostConfirmationStatus" }
      responses:
        "200":
          description: Host Confirmation list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HostConfirmation" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /host-confirmations/{id}:
    get:
      operationId: getHostConfirmation
      x-required-permission: host_confirmation.read
      summary: Read one Host Confirmation
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C2 (BE-13F). Requires
        `host_confirmation.read` plus scope access.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HostConfirmationIdPath"
      responses:
        "200":
          description: The Host Confirmation (any status).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HostConfirmation" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /host-confirmations/{id}/confirm:
    post:
      operationId: confirmHostConfirmation
      x-required-permission: host_confirmation.manage
      summary: Confirm a Host Confirmation (PENDING -> CONFIRMED, terminal)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C2 (BE-13F). Requires
        `host_confirmation.manage`. Records the host confirmation;
        CONFIRMED is terminal - an already decided record is rejected 409
        `HOST_CONFIRMATION_ALREADY_DECIDED`. A CONFIRMED visit may proceed
        to BE-13G Check-In. Optional request body (`notes`).
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HostConfirmationIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ConfirmHostConfirmationRequest" }
      responses:
        "200":
          description: Confirmed Host Confirmation (status CONFIRMED).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HostConfirmation" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /host-confirmations/{id}/reject:
    post:
      operationId: rejectHostConfirmation
      x-required-permission: host_confirmation.manage
      summary: Reject a Host Confirmation (PENDING -> REJECTED, terminal)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C2 (BE-13F). Requires
        `host_confirmation.manage`. Records the host rejection with a
        mandatory `rejectionReason`; REJECTED is terminal - a REJECTED
        visit can never proceed as confirmed, and an already decided record
        is rejected 409 `HOST_CONFIRMATION_ALREADY_DECIDED`.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HostConfirmationIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RejectHostConfirmationRequest" }
      responses:
        "200":
          description: Rejected Host Confirmation (status REJECTED).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HostConfirmation" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }


  /delivery-couriers:
    post:
      operationId: createDeliveryCourier
      x-required-permission: delivery_courier.manage
      summary: Register a Delivery / Courier arrival
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C3 (BE-13K). Requires
        `delivery_courier.manage` plus Client/Building scope access.
        Delivery metadata may optionally bind to the shared Visitor
        identity (`visitorId`) and to ONE existing visit
        (`expectedVisitorId` XOR `walkInVisitId`); without a visit
        reference `buildingId` is required (400
        `DELIVERY_COURIER_CONTEXT_REQUIRED`). Building derives from the
        visit when bound. Courier identity (company and/or name) and at
        least one resolvable recipient reference are required (400
        `DELIVERY_COURIER_COURIER_REQUIRED` /
        `DELIVERY_COURIER_RECIPIENT_REQUIRED`); cancelled visits,
        client/building mismatches and inactive identities or recipients
        are rejected 400. One ARRIVED record per visit binding (409
        `DELIVERY_COURIER_ALREADY_EXISTS`). `arrivedAt` defaults to now
        and must not be in the future (400
        `DELIVERY_COURIER_ARRIVAL_IN_FUTURE`). Check-In / Check-Out stay
        owned by the BE-13 visit lifecycle. No parcel tracking, external
        courier-integration, proof-of-delivery, notification or COD
        semantics exist here. Lifecycle: ARRIVED -> RECEIVED | REJECTED |
        CANCELLED (terminals).
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateDeliveryCourierRequest" }
      responses:
        "201":
          description: Registered Delivery / Courier record (status ARRIVED).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/DeliveryCourier" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listDeliveryCouriers
      x-required-permission: delivery_courier.read
      summary: List Delivery / Courier records (scoped)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C3 (BE-13K). Requires
        `delivery_courier.read`. Optional filters: `buildingId`,
        `visitorId`, `expectedVisitorId`, `walkInVisitId`, `deliveryType`,
        `status`, `search`.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: visitorId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: expectedVisitorId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: walkInVisitId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: deliveryType
          in: query
          schema: { $ref: "#/components/schemas/DeliveryCourierType" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/DeliveryCourierStatus" }
        - name: search
          in: query
          schema: { type: string, maxLength: 255 }
      responses:
        "200":
          description: Delivery / Courier record list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/DeliveryCourier" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /delivery-couriers/{id}:
    get:
      operationId: getDeliveryCourier
      x-required-permission: delivery_courier.read
      summary: Read one Delivery / Courier record
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C3 (BE-13K). Requires
        `delivery_courier.read` plus scope access to the record's
        Client/Building.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DeliveryCourierIdPath"
      responses:
        "200":
          description: The Delivery / Courier record (any status).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/DeliveryCourier" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /delivery-couriers/{id}/status:
    patch:
      operationId: updateDeliveryCourierStatus
      x-required-permission: delivery_courier.manage
      summary: Resolve a Delivery / Courier (ARRIVED -> RECEIVED | REJECTED | CANCELLED)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C3 (BE-13K). Requires
        `delivery_courier.manage`. Only ARRIVED records can transition -
        anything else is rejected 409 `DELIVERY_COURIER_INVALID_TRANSITION`.
        REJECTED / CANCELLED additionally require that no ACTIVE Check-In
        exists for the bound visit (409 `DELIVERY_COURIER_ACTIVE_VISIT`).
        `referenceNumber` / `notes` may be updated together with the
        transition.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DeliveryCourierIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateDeliveryCourierStatusRequest" }
      responses:
        "200":
          description: Resolved Delivery / Courier record.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/DeliveryCourier" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /security/visitor-bindings:
    post:
      operationId: createSecurityVisitorBinding
      x-required-permission: security_visitor_binding.manage
      summary: Create a Security-side binding for an external visit reference
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C3 (BE-12J). Requires
        `security_visitor_binding.manage` plus Client/Building scope
        access. Records the Security operational context for an OPAQUE,
        future-authoritative `externalVisitReference` (free-form
        caller-supplied string a future Visitor module can later match -
        it is intentionally NOT a foreign key). The binding stores client
        + building, an optional Security Post anchor, an optional Security
        Workforce anchor, the external reference, free-form context and its
        own status only - NO visitor personal data is stored. Optional
        anchors must belong to the same Building and be ACTIVE (400).
        One ACTIVE binding per (building, externalVisitReference) - 409
        `SECURITY_VISITOR_BINDING_ALREADY_EXISTS`; INACTIVE bindings do
        not block a re-bind. Status is independent of any future Visitor
        lifecycle. No patrol-assignment, guard-shift, incident or
        escort-workflow semantics exist in this module.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateSecurityVisitorBindingRequest" }
      responses:
        "201":
          description: Created Security Visitor Binding (status ACTIVE unless INACTIVE was requested).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SecurityVisitorBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listSecurityVisitorBindings
      x-required-permission: security_visitor_binding.read
      summary: List Security Visitor Bindings (scoped)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C3 (BE-12J). Requires
        `security_visitor_binding.read`. Optional filters: `buildingId`,
        `securityPostId`, `securityWorkforceId`, `externalVisitReference`,
        `status`.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: securityPostId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: securityWorkforceId
          in: query
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: externalVisitReference
          in: query
          schema: { type: string, maxLength: 256 }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/SecurityVisitorBindingStatus" }
      responses:
        "200":
          description: Security Visitor Binding list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/SecurityVisitorBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /security/visitor-bindings/{id}:
    get:
      operationId: getSecurityVisitorBinding
      x-required-permission: security_visitor_binding.read
      summary: Read one Security Visitor Binding
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C3 (BE-12J). Requires
        `security_visitor_binding.read` plus scope access to the binding's
        Client/Building.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/SecurityVisitorBindingIdPath"
      responses:
        "200":
          description: The Security Visitor Binding (any status).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SecurityVisitorBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateSecurityVisitorBinding
      x-required-permission: security_visitor_binding.manage
      summary: Edit a Security Visitor Binding (anchors, context, status)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C3 (BE-12J). Requires
        `security_visitor_binding.manage`. Edits the Security Post /
        Workforce anchors, `externalVisitReference`, `securityContext` and
        `status` (ACTIVE <-> INACTIVE deactivation lane; at least one
        field required). Optional anchors keep the same-Building / ACTIVE
        constraints (400); an `externalVisitReference` change that would
        collide with another ACTIVE binding is rejected 409. Client /
        Building are immutable.
      tags: [Front Desk]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/SecurityVisitorBindingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateSecurityVisitorBindingRequest" }
      responses:
        "200":
          description: Updated Security Visitor Binding.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SecurityVisitorBinding" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /tenant-companies/{tenantCompanyId}/service-requests:
    post:
      operationId: createTenantServiceRequest
      x-required-permission: tenant_company.manage
      x-building-scoped: true
      summary: Create a Tenant Service Request for a Tenant Company
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02B (BE-14E). Requires
        `tenant_company.manage`, Client access to the Tenant Company's Client
        and Building access to the supplied `buildingId` (403
        `BUILDING_ACCESS_DENIED` otherwise). `clientId` is derived from the
        Tenant Company and `tenantCompanyId` from the path - neither is
        caller-supplied. Preconditions enforced by the runtime: an ACTIVE
        tenant-building context effective now and an ACTIVE Tenant Company (400
        `TENANT_SERVICE_REQUEST_CONTEXT_INVALID`); the requester must be an
        ACTIVE PIC of that same Tenant Company (400
        `TENANT_SERVICE_REQUEST_REQUESTER_INVALID`); a supplied `spaceId` must
        have an ACTIVE relationship to the tenant in that Building (400
        `TENANT_SERVICE_REQUEST_SPACE_MISMATCH`). `requestNumber` is unique per
        Client (409 `TENANT_SERVICE_REQUEST_NUMBER_ALREADY_EXISTS`). The new
        record is `OPEN`; no approval step exists.
      tags: [Tenant Service Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantCompanyIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateTenantServiceRequestRequest" }
      responses:
        "201":
          description: Tenant Service Request created (`status` = `OPEN`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TenantServiceRequest" }
        "400":
          description: >
            Request validation failed, or
            `TENANT_SERVICE_REQUEST_CONTEXT_INVALID`,
            `TENANT_SERVICE_REQUEST_REQUESTER_INVALID`,
            `TENANT_SERVICE_REQUEST_SPACE_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Tenant Company not found (`TENANT_COMPANY_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `TENANT_SERVICE_REQUEST_NUMBER_ALREADY_EXISTS` - the `requestNumber`
            is already used by another service request of the same Client.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listTenantServiceRequests
      x-required-permission: tenant_company.read
      x-building-scoped: true
      summary: List the Tenant Service Requests of a Tenant Company
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02B (BE-14E). Requires
        `tenant_company.read` and Client access to the Tenant Company's Client
        (403 `BUILDING_ACCESS_DENIED` otherwise). The result is always narrowed
        to the Buildings the caller can access, so a caller sees only their own
        scope - the listing is not a full-company dump. Supplying the
        `buildingId` filter additionally asserts Building access on that
        Building. `tenantCompanyId` is taken from the path and is authoritative
        over any `tenantCompanyId` query value. Optional `status`,
        `requestType` and `buildingId` filters; an invalid value is rejected
        with `VALIDATION_ERROR`. `data` is a plain array - not paginated.
      tags: [Tenant Service Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantCompanyIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/TenantServiceRequestStatus" }
        - name: requestType
          in: query
          required: false
          description: >
            Data-driven request type code. Trimmed and uppercased before
            matching; must start with a letter and contain only uppercase
            letters, digits, hyphens and underscores (2-64 characters).
          schema: { type: string, minLength: 2, maxLength: 64 }
        - name: buildingId
          in: query
          required: false
          description: >
            Narrow the listing to one Building. Building access on this Building
            is asserted when supplied.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Tenant Service Requests of the Tenant Company.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/TenantServiceRequest" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Tenant Company not found (`TENANT_COMPANY_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /buildings/{buildingId}/tenant-service-requests:
    get:
      operationId: listBuildingTenantServiceRequests
      x-required-permission: tenant_company.read
      x-building-scoped: true
      summary: List the Tenant Service Requests of a Building
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02B (BE-14E). Requires
        `tenant_company.read`; BE-02 Building isolation is asserted on
        `buildingId` (403 `BUILDING_ACCESS_DENIED` otherwise). `buildingId` is
        taken from the path and is authoritative over any `buildingId` query
        value. Optional `status`, `requestType` and `tenantCompanyId` filters;
        an invalid value is rejected with `VALIDATION_ERROR`. `data` is a plain
        array - not paginated.
      tags: [Tenant Service Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/TenantServiceRequestStatus" }
        - name: requestType
          in: query
          required: false
          description: >
            Data-driven request type code. Trimmed and uppercased before
            matching; must start with a letter and contain only uppercase
            letters, digits, hyphens and underscores (2-64 characters).
          schema: { type: string, minLength: 2, maxLength: 64 }
        - name: tenantCompanyId
          in: query
          required: false
          description: Narrow the listing to one Tenant Company of the Building.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Tenant Service Requests of the Building.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/TenantServiceRequest" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /tenant-service-requests/{id}:
    get:
      operationId: getTenantServiceRequest
      x-required-permission: tenant_company.read
      x-building-scoped: true
      summary: Get a Tenant Service Request
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02B (BE-14E). Requires
        `tenant_company.read`. The route carries no Building parameter, so BE-02
        Building isolation is asserted on the record's own Building after it is
        loaded (403 `BUILDING_ACCESS_DENIED` otherwise).
      tags: [Tenant Service Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantServiceRequestIdPath"
      responses:
        "200":
          description: Tenant Service Request.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TenantServiceRequest" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: >
            Tenant Service Request not found
            (`TENANT_SERVICE_REQUEST_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    patch:
      operationId: updateTenantServiceRequest
      x-required-permission: tenant_company.manage
      x-building-scoped: true
      summary: Update an OPEN Tenant Service Request
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02B (BE-14E). Requires
        `tenant_company.manage`, Building access to the record's Building, and
        the backend-resolved `UPDATE` action (403
        `TENANT_SERVICE_REQUEST_ACTION_NOT_ALLOWED` when the caller may not).
        Only an `OPEN` request is editable - `CANCELLED` and `CONVERTED` yield
        400 `TENANT_SERVICE_REQUEST_NOT_OPEN`. Only `requestType`, `title`,
        `description` and `priority` are mutable; supplying any of `clientId`,
        `tenantCompanyId`, `tenantPicId`, `buildingId`, `spaceId`,
        `requestNumber`, `status`, `requestedAt`, `workRequestId` or
        `workOrderId` is rejected 400 as an immutable field. `description` may
        be set to `null` to clear it. No approval workflow and no status
        transition are performed by this route.
      tags: [Tenant Service Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantServiceRequestIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateTenantServiceRequestRequest" }
      responses:
        "200":
          description: Updated Tenant Service Request.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TenantServiceRequest" }
        "400":
          description: >
            Request validation failed, an immutable field was supplied, or
            `TENANT_SERVICE_REQUEST_NOT_OPEN` (the request is `CANCELLED` or
            `CONVERTED`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED`, `BUILDING_ACCESS_DENIED`, or
            `TENANT_SERVICE_REQUEST_ACTION_NOT_ALLOWED` (`UPDATE` is not
            available to this caller).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Tenant Service Request not found
            (`TENANT_SERVICE_REQUEST_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /tenant-service-requests/{id}/available-actions:
    get:
      operationId: getTenantServiceRequestAvailableActions
      x-required-permission: tenant_company.read
      x-building-scoped: true
      summary: Resolve the caller's available Tenant Service Request actions
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02B (BE-14E). Requires
        `tenant_company.read` and Building access to the record's Building.
        Uses the established `{ serviceRequestId, state, availableActions }`
        shape. `availableActions` is backend-authoritative and caller-specific:
        it is the subset of `UPDATE`, `CANCEL`, `CREATE_WORK_REQUEST` and
        `CREATE_WORK_ORDER` this caller may actually run now. An empty list is
        authoritative - clients must not infer an action from `state`. This
        route performs no transition.
      tags: [Tenant Service Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantServiceRequestIdPath"
      responses:
        "200":
          description: Caller-specific available actions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/TenantServiceRequestAvailableActions"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: >
            Tenant Service Request not found
            (`TENANT_SERVICE_REQUEST_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /tenant-service-requests/{id}/work-request:
    post:
      operationId: createTenantServiceRequestWorkRequest
      x-required-permission: tenant_company.manage
      x-building-scoped: true
      summary: Bind a new Work Request to a Tenant Service Request (OPEN -> CONVERTED)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02B (BE-14E). Requires
        `tenant_company.manage`, Building access to the record's Building, and
        the backend-resolved `CREATE_WORK_REQUEST` action (403
        `TENANT_SERVICE_REQUEST_ACTION_NOT_ALLOWED` otherwise). This is the
        BE-08 conversion binding: it creates the Work Request from the request's
        own `requestNumber`, `title`, `description` and `requestType`, stores
        the resulting id in `workRequestId` and moves `status` from `OPEN` to
        `CONVERTED`. No request body - every field is derived server-side, so
        the caller supplies no Work Request data. One binding per request: an
        already-bound request yields 409
        `TENANT_SERVICE_REQUEST_WORK_REQUEST_EXISTS`. `data` is the updated
        Tenant Service Request, not the Work Request.
      tags: [Tenant Service Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantServiceRequestIdPath"
      responses:
        "201":
          description: >
            Work Request bound; the returned Tenant Service Request carries
            `workRequestId` and `status` = `CONVERTED`.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TenantServiceRequest" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED`, `BUILDING_ACCESS_DENIED`, or
            `TENANT_SERVICE_REQUEST_ACTION_NOT_ALLOWED` (`CREATE_WORK_REQUEST`
            is not available to this caller).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Tenant Service Request not found
            (`TENANT_SERVICE_REQUEST_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `TENANT_SERVICE_REQUEST_WORK_REQUEST_EXISTS` - the request is
            already linked to a Work Request.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /tenant-service-requests/{id}/work-order:
    post:
      operationId: createTenantServiceRequestWorkOrder
      x-required-permission: tenant_company.manage
      x-building-scoped: true
      summary: Bind a new Work Order to a converted Tenant Service Request
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02B (BE-14E). Requires
        `tenant_company.manage`, Building access to the record's Building, and
        the backend-resolved `CREATE_WORK_ORDER` action (403
        `TENANT_SERVICE_REQUEST_ACTION_NOT_ALLOWED` otherwise). PRECONDITION:
        a Work Request must already be bound - a request without `workRequestId`
        is rejected 403 `TENANT_SERVICE_REQUEST_ACTION_NOT_ALLOWED`, so the
        Work Order is always created from the bound Work Request and never
        directly from the intake record. The created Work Order inherits the
        request's `title`, `description` and `requestType` (as `workType`) and
        is aligned to the request's `priority`; the caller supplies only
        `workOrderNumber`. The resulting id is stored in `workOrderId`; this
        binding does NOT change `status`. One binding per request: an
        already-bound request yields 409
        `TENANT_SERVICE_REQUEST_WORK_ORDER_EXISTS`. `data` is the updated Tenant
        Service Request, not the Work Order.
      tags: [Tenant Service Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantServiceRequestIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateTenantServiceRequestWorkOrderRequest" }
      responses:
        "201":
          description: >
            Work Order bound; the returned Tenant Service Request carries
            `workOrderId`.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TenantServiceRequest" }
        "400":
          description: >
            Request validation failed - `workOrderNumber` is required and must
            be a valid Work Order number.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED`, `BUILDING_ACCESS_DENIED`, or
            `TENANT_SERVICE_REQUEST_ACTION_NOT_ALLOWED` - the latter also covers
            the missing Work Request precondition.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Tenant Service Request not found
            (`TENANT_SERVICE_REQUEST_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `TENANT_SERVICE_REQUEST_WORK_ORDER_EXISTS` - the request is already
            linked to a Work Order.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /tenant-service-requests/{id}/cancel:
    post:
      operationId: cancelTenantServiceRequest
      x-required-permission: tenant_company.manage
      x-building-scoped: true
      summary: Cancel an OPEN Tenant Service Request (OPEN -> CANCELLED)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02B (BE-14E). Requires
        `tenant_company.manage`, Building access to the record's Building, and
        the backend-resolved `CANCEL` action (403
        `TENANT_SERVICE_REQUEST_ACTION_NOT_ALLOWED` otherwise). Only an `OPEN`
        request can be cancelled; a `CANCELLED` or `CONVERTED` request yields
        400 `TENANT_SERVICE_REQUEST_NOT_OPEN`. Cancellation is the
        `OPEN` -> `CANCELLED` transition, not a delete - the record is
        preserved and there is no re-open. No request body.
      tags: [Tenant Service Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantServiceRequestIdPath"
      responses:
        "200":
          description: Cancelled Tenant Service Request (`status` = `CANCELLED`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TenantServiceRequest" }
        "400":
          description: >
            `TENANT_SERVICE_REQUEST_NOT_OPEN` - the request is already
            `CANCELLED` or `CONVERTED`, or the id failed validation.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED`, `BUILDING_ACCESS_DENIED`, or
            `TENANT_SERVICE_REQUEST_ACTION_NOT_ALLOWED` (`CANCEL` is not
            available to this caller).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Tenant Service Request not found
            (`TENANT_SERVICE_REQUEST_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 02C - J10 Tenant Complaints (BE-14F).
  #
  # Contract exposure of the ALREADY REGISTERED runtime routes only. No
  # runtime, lifecycle, authorization, escalation or work-order-generation
  # behaviour is added or changed here.
  # ---------------------------------------------------------------------------

  /tenant-companies/{tenantCompanyId}/complaints:
    post:
      operationId: createTenantComplaint
      x-required-permission: tenant_company.manage
      x-building-scoped: true
      summary: Create a Tenant Complaint for a Tenant Company
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02C (BE-14F). Requires
        `tenant_company.manage`, Client access to the Tenant Company's Client
        and Building access to the supplied `buildingId` (403
        `BUILDING_ACCESS_DENIED` otherwise). `clientId` is derived from the
        Tenant Company and `tenantCompanyId` from the path - neither is
        caller-supplied. Preconditions enforced by the runtime: an ACTIVE
        tenant-building context effective now and an ACTIVE Tenant Company (400
        `TENANT_COMPLAINT_CONTEXT_INVALID`); the complainant must be an ACTIVE
        PIC of that same Tenant Company (400
        `TENANT_COMPLAINT_COMPLAINANT_INVALID`); a supplied `spaceId` must have
        an ACTIVE relationship to the tenant in that Building, effective now
        (400 `TENANT_COMPLAINT_SPACE_MISMATCH`). `complaintNumber` is unique per
        Client (409 `TENANT_COMPLAINT_NUMBER_ALREADY_EXISTS`). The new record is
        `OPEN`; no approval step and no notification are exposed here.
      tags: [Tenant Complaints]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantCompanyIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateTenantComplaintRequest" }
      responses:
        "201":
          description: Tenant Complaint created (`status` = `OPEN`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TenantComplaint" }
        "400":
          description: >
            Request validation failed, or `TENANT_COMPLAINT_CONTEXT_INVALID`,
            `TENANT_COMPLAINT_COMPLAINANT_INVALID`,
            `TENANT_COMPLAINT_SPACE_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Tenant Company not found (`TENANT_COMPANY_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `TENANT_COMPLAINT_NUMBER_ALREADY_EXISTS` - the `complaintNumber` is
            already used by another complaint of the same Client.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listTenantComplaints
      x-required-permission: tenant_company.read
      x-building-scoped: true
      summary: List the Tenant Complaints of a Tenant Company
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02C (BE-14F). Requires
        `tenant_company.read` and Client access to the Tenant Company's Client
        (403 `BUILDING_ACCESS_DENIED` otherwise). The result is always narrowed
        to the Buildings the caller can access, so a caller sees only their own
        scope - the listing is not a full-company dump. Supplying the
        `buildingId` filter additionally asserts Building access on that
        Building. `tenantCompanyId` is taken from the path and is authoritative
        over any `tenantCompanyId` query value. Optional `status`,
        `complaintType` and `buildingId` filters; an invalid value is rejected
        with `VALIDATION_ERROR`. `data` is a plain array - not paginated.
      tags: [Tenant Complaints]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantCompanyIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/TenantComplaintStatus" }
        - name: complaintType
          in: query
          required: false
          description: >
            Data-driven complaint type code. Trimmed and uppercased before
            matching; must start with a letter and contain only uppercase
            letters, digits, hyphens and underscores (2-64 characters).
          schema: { type: string, minLength: 2, maxLength: 64 }
        - name: buildingId
          in: query
          required: false
          description: >
            Narrow the listing to one Building. Building access on this Building
            is asserted when supplied.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Tenant Complaints of the Tenant Company.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/TenantComplaint" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Tenant Company not found (`TENANT_COMPANY_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /buildings/{buildingId}/tenant-complaints:
    get:
      operationId: listBuildingTenantComplaints
      x-required-permission: tenant_company.read
      x-building-scoped: true
      summary: List the Tenant Complaints of a Building
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02C (BE-14F). Requires
        `tenant_company.read`; BE-02 Building isolation is asserted on
        `buildingId` (403 `BUILDING_ACCESS_DENIED` otherwise). `buildingId` is
        taken from the path and is authoritative over any `buildingId` query
        value. Optional `status`, `complaintType` and `tenantCompanyId` filters;
        an invalid value is rejected with `VALIDATION_ERROR`. `data` is a plain
        array - not paginated.
      tags: [Tenant Complaints]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/TenantComplaintStatus" }
        - name: complaintType
          in: query
          required: false
          description: >
            Data-driven complaint type code. Trimmed and uppercased before
            matching; must start with a letter and contain only uppercase
            letters, digits, hyphens and underscores (2-64 characters).
          schema: { type: string, minLength: 2, maxLength: 64 }
        - name: tenantCompanyId
          in: query
          required: false
          description: Narrow the listing to one Tenant Company of the Building.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Tenant Complaints of the Building.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/TenantComplaint" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /tenant-complaints/{id}:
    get:
      operationId: getTenantComplaint
      x-required-permission: tenant_company.read
      x-building-scoped: true
      summary: Get a Tenant Complaint
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02C (BE-14F). Requires
        `tenant_company.read`. The route carries no Building parameter, so BE-02
        Building isolation is asserted on the record's own Building after it is
        loaded (403 `BUILDING_ACCESS_DENIED` otherwise).
      tags: [Tenant Complaints]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantComplaintIdPath"
      responses:
        "200":
          description: Tenant Complaint.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TenantComplaint" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Tenant Complaint not found (`TENANT_COMPLAINT_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    patch:
      operationId: updateTenantComplaint
      x-required-permission: tenant_company.manage
      x-building-scoped: true
      summary: Update an OPEN Tenant Complaint
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02C (BE-14F). Requires
        `tenant_company.manage`, Building access to the record's Building, and
        the backend-resolved `UPDATE` action (403
        `TENANT_COMPLAINT_ACTION_NOT_ALLOWED` when the caller may not). Only an
        `OPEN` complaint is editable - `CANCELLED` and `ESCALATED` yield 400
        `TENANT_COMPLAINT_NOT_OPEN`. Only `complaintType`, `title`,
        `description` and `severity` are mutable; supplying any of `clientId`,
        `tenantCompanyId`, `tenantPicId`, `buildingId`, `spaceId`,
        `complaintNumber`, `status`, `reportedAt`, `findingId` or `workOrderId`
        is rejected 400 as an immutable field. `description` may be set to
        `null` to clear it. This route performs no escalation, no assignment and
        no status transition.
      tags: [Tenant Complaints]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantComplaintIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateTenantComplaintRequest" }
      responses:
        "200":
          description: Updated Tenant Complaint.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TenantComplaint" }
        "400":
          description: >
            Request validation failed, an immutable field was supplied, or
            `TENANT_COMPLAINT_NOT_OPEN` (the complaint is `CANCELLED` or
            `ESCALATED`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED`, `BUILDING_ACCESS_DENIED`, or
            `TENANT_COMPLAINT_ACTION_NOT_ALLOWED` (`UPDATE` is not available to
            this caller).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: Tenant Complaint not found (`TENANT_COMPLAINT_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /tenant-complaints/{id}/available-actions:
    get:
      operationId: getTenantComplaintAvailableActions
      x-required-permission: tenant_company.read
      x-building-scoped: true
      summary: Resolve the caller's available Tenant Complaint actions
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02C (BE-14F). Requires
        `tenant_company.read` and Building access to the record's Building.
        Uses the established `{ complaintId, state, findingId, availableActions
        }` shape. `availableActions` is backend-authoritative and
        caller-specific: the subset of the complaint action vocabulary this
        caller may actually run now. An empty list is authoritative - clients
        must not infer an action from `state`. `findingId` is echoed so a client
        can resolve the bound Finding without a second call. This route performs
        no transition and executes none of the returned actions; the Finding
        lifecycle tokens it may report are run through the BE-09 Finding
        surface, not here.
      tags: [Tenant Complaints]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantComplaintIdPath"
      responses:
        "200":
          description: Caller-specific available actions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/TenantComplaintAvailableActions"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Tenant Complaint not found (`TENANT_COMPLAINT_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /tenant-complaints/{id}/finding:
    post:
      operationId: createTenantComplaintFinding
      x-required-permission: tenant_company.manage
      x-building-scoped: true
      summary: Bind a new Finding to a Tenant Complaint (OPEN -> ESCALATED)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02C (BE-14F). Requires
        `tenant_company.manage`, Building access to the record's Building, and
        the backend-resolved `CREATE_FINDING` action (403
        `TENANT_COMPLAINT_ACTION_NOT_ALLOWED` otherwise). This is the BE-09
        escalation binding: it creates the Finding from the complaint's own
        `complaintNumber` (as the finding number), `title` and `description`,
        stores the resulting id in `findingId` and moves `status` from `OPEN` to
        `ESCALATED`. No request body - every field is derived server-side, so
        the caller supplies no Finding data. One binding per complaint: an
        already-bound complaint yields 409 `TENANT_COMPLAINT_FINDING_EXISTS`.
        `data` is the updated Tenant Complaint, not the Finding.
      tags: [Tenant Complaints]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantComplaintIdPath"
      responses:
        "201":
          description: >
            Finding bound; the returned Tenant Complaint carries `findingId` and
            `status` = `ESCALATED`.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TenantComplaint" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED`, `BUILDING_ACCESS_DENIED`, or
            `TENANT_COMPLAINT_ACTION_NOT_ALLOWED` (`CREATE_FINDING` is not
            available to this caller).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: Tenant Complaint not found (`TENANT_COMPLAINT_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `TENANT_COMPLAINT_FINDING_EXISTS` - the complaint is already linked
            to a Finding.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /tenant-complaints/{id}/work-order:
    post:
      operationId: createTenantComplaintWorkOrder
      x-required-permission: tenant_company.manage
      x-building-scoped: true
      summary: Bind a new Work Order to an escalated Tenant Complaint
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02C (BE-14F). Requires
        `tenant_company.manage`, Building access to the record's Building, and
        the backend-resolved `CREATE_WORK_ORDER` action (403
        `TENANT_COMPLAINT_ACTION_NOT_ALLOWED` otherwise). PRECONDITION: a
        Finding must already be bound - a complaint without `findingId` is
        rejected 403 `TENANT_COMPLAINT_ACTION_NOT_ALLOWED`, so a Work Order is
        never created directly from an un-escalated intake record. The created
        Work Order takes the complaint's `title`, `description` and
        `complaintType` (as `workType`) and is aligned to the complaint's
        `severity`; the caller supplies only `workOrderNumber`. The resulting id
        is stored in `workOrderId`; this binding does NOT change `status`. One
        binding per complaint: an already-bound complaint yields 409
        `TENANT_COMPLAINT_WORK_ORDER_EXISTS`. `data` is the updated Tenant
        Complaint, not the Work Order.
      tags: [Tenant Complaints]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantComplaintIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateTenantComplaintWorkOrderRequest" }
      responses:
        "201":
          description: >
            Work Order bound; the returned Tenant Complaint carries `workOrderId`
            and an unchanged `status`.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TenantComplaint" }
        "400":
          description: >
            Request validation failed - `workOrderNumber` is required and must be
            a valid Work Order number.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED`, `BUILDING_ACCESS_DENIED`, or
            `TENANT_COMPLAINT_ACTION_NOT_ALLOWED` - the latter also covers the
            missing Finding precondition.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: Tenant Complaint not found (`TENANT_COMPLAINT_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `TENANT_COMPLAINT_WORK_ORDER_EXISTS` - the complaint is already
            linked to a Work Order.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /tenant-complaints/{id}/cancel:
    post:
      operationId: cancelTenantComplaint
      x-required-permission: tenant_company.manage
      x-building-scoped: true
      summary: Cancel an OPEN Tenant Complaint (OPEN -> CANCELLED)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02C (BE-14F). Requires
        `tenant_company.manage`, Building access to the record's Building, and
        the backend-resolved `CANCEL` action (403
        `TENANT_COMPLAINT_ACTION_NOT_ALLOWED` otherwise). Only an `OPEN`
        complaint can be cancelled; a `CANCELLED` or `ESCALATED` complaint
        yields 400 `TENANT_COMPLAINT_NOT_OPEN`. Cancellation is the
        `OPEN` -> `CANCELLED` transition, not a delete - the record is preserved
        and there is no re-open. No request body.
      tags: [Tenant Complaints]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantComplaintIdPath"
      responses:
        "200":
          description: Cancelled Tenant Complaint (`status` = `CANCELLED`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TenantComplaint" }
        "400":
          description: >
            `TENANT_COMPLAINT_NOT_OPEN` - the complaint is already `CANCELLED`
            or `ESCALATED`, or the id failed validation.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED`, `BUILDING_ACCESS_DENIED`, or
            `TENANT_COMPLAINT_ACTION_NOT_ALLOWED` (`CANCEL` is not available to
            this caller).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: Tenant Complaint not found (`TENANT_COMPLAINT_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 02D - J10 Tenant Utility Requests
  # (BE-14G).
  #
  # Contract exposure of the ALREADY REGISTERED runtime routes only. No
  # runtime, lifecycle, authorization or work-order-generation behaviour is
  # added or changed here. This surface performs no utility calculation, meter
  # reading, billing, verification, reconciliation, approval, SLA or
  # notification behaviour.
  # ---------------------------------------------------------------------------

  /tenant-companies/{tenantCompanyId}/utility-requests:
    post:
      operationId: createTenantUtilityRequest
      x-required-permission: tenant_company.manage
      summary: Create a Tenant Utility Request for a Tenant Company
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02D (BE-14G). Requires
        `tenant_company.manage`. `clientId` is derived from the Tenant Company
        and `tenantCompanyId` from the path - neither is caller-supplied.
        Preconditions enforced by the runtime, each with its own error: an
        ACTIVE tenant building context is required (400
        `TENANT_UTILITY_REQUEST_CONTEXT_INVALID`); the requester must be an
        ACTIVE PIC of the Tenant Company (400
        `TENANT_UTILITY_REQUEST_REQUESTER_INVALID`); the `spaceId` is REQUIRED
        and must have an ACTIVE relationship to the tenant in this Building (400
        `TENANT_UTILITY_REQUEST_SPACE_INVALID`). `requestNumber` is unique per
        Client (409 `TENANT_UTILITY_REQUEST_NUMBER_ALREADY_EXISTS`). This is an
        intake record only: it carries no meter, tariff, reading, consumption or
        monetary field and triggers no utility calculation, billing or
        notification.
      tags: [Tenant Utility Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantCompanyIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateTenantUtilityRequestRequest" }
      responses:
        "201":
          description: Tenant Utility Request created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TenantUtilityRequest" }
        "400":
          description: >
            Request validation failed, or
            `TENANT_UTILITY_REQUEST_CONTEXT_INVALID`,
            `TENANT_UTILITY_REQUEST_REQUESTER_INVALID`,
            `TENANT_UTILITY_REQUEST_SPACE_INVALID`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: "`PERMISSION_DENIED` - `tenant_company.manage` is missing."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `TENANT_UTILITY_REQUEST_NUMBER_ALREADY_EXISTS` - the
            `requestNumber` is already used by another utility request of the
            same Client.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listTenantUtilityRequests
      x-required-permission: tenant_company.read
      summary: List the Tenant Utility Requests of a Tenant Company
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02D (BE-14G). Requires
        `tenant_company.read`. The listing is scoped by the path
        `tenantCompanyId`, which is authoritative: a `tenantCompanyId` query
        value cannot re-scope the request. Optional `status`, `utilityType` and
        `buildingId` filters narrow the result further; an invalid value is
        rejected with `VALIDATION_ERROR`. `data` is a plain array - not
        paginated.
      tags: [Tenant Utility Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantCompanyIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/TenantUtilityRequestStatus" }
        - name: utilityType
          in: query
          required: false
          description: >
            Data-driven utility type code. Trimmed and uppercased before
            matching; must start with a letter and contain only uppercase
            letters, digits, hyphens and underscores (2-64 characters).
          schema: { type: string, minLength: 2, maxLength: 64 }
        - name: buildingId
          in: query
          required: false
          description: Narrow the listing to one Building.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Tenant Utility Requests of the Tenant Company.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/TenantUtilityRequest" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: "`PERMISSION_DENIED` - `tenant_company.read` is missing."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /buildings/{buildingId}/tenant-utility-requests:
    get:
      operationId: listBuildingTenantUtilityRequests
      x-required-permission: tenant_company.read
      summary: List the Tenant Utility Requests of a Building
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02D (BE-14G). Requires
        `tenant_company.read`. The listing is scoped by the path `buildingId`,
        which is authoritative: a `buildingId` query value cannot re-scope the
        request. Optional `status`, `utilityType` and `tenantCompanyId` filters
        narrow the result further; an invalid value is rejected with
        `VALIDATION_ERROR`. `data` is a plain array - not paginated.
      tags: [Tenant Utility Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/TenantUtilityRequestStatus" }
        - name: utilityType
          in: query
          required: false
          description: >
            Data-driven utility type code. Trimmed and uppercased before
            matching; must start with a letter and contain only uppercase
            letters, digits, hyphens and underscores (2-64 characters).
          schema: { type: string, minLength: 2, maxLength: 64 }
        - name: tenantCompanyId
          in: query
          required: false
          description: Narrow the listing to one Tenant Company of the Building.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Tenant Utility Requests of the Building.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/TenantUtilityRequest" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: "`PERMISSION_DENIED` - `tenant_company.read` is missing."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /tenant-utility-requests/{id}:
    get:
      operationId: getTenantUtilityRequest
      x-required-permission: tenant_company.read
      summary: Get a Tenant Utility Request
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02D (BE-14G). Requires
        `tenant_company.read`.
      tags: [Tenant Utility Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantUtilityRequestIdPath"
      responses:
        "200":
          description: Tenant Utility Request.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TenantUtilityRequest" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: "`PERMISSION_DENIED` - `tenant_company.read` is missing."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Tenant Utility Request not found
            (`TENANT_UTILITY_REQUEST_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    patch:
      operationId: updateTenantUtilityRequest
      x-required-permission: tenant_company.manage
      summary: Update an OPEN Tenant Utility Request
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02D (BE-14G). Requires
        `tenant_company.manage` and the backend-resolved `UPDATE` action (403
        `TENANT_UTILITY_REQUEST_ACTION_NOT_ALLOWED` when the caller may not).
        Only an `OPEN` utility request can be updated - `CANCELLED` and
        `CONVERTED` yield 400 `TENANT_UTILITY_REQUEST_NOT_OPEN`. Only
        `utilityType`, `requestDetails` and `notes` are mutable; supplying any
        of `clientId`, `tenantCompanyId`, `tenantPicId`, `buildingId`,
        `spaceId`, `requestNumber`, `requestedAt`, `status`, `workRequestId` or
        `workOrderId` is rejected 400 as an immutable field. `notes` may be set
        to `null` to clear it. This route performs no status transition.
      tags: [Tenant Utility Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantUtilityRequestIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateTenantUtilityRequestRequest" }
      responses:
        "200":
          description: Updated Tenant Utility Request.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TenantUtilityRequest" }
        "400":
          description: >
            Request validation failed, an immutable field was supplied, or
            `TENANT_UTILITY_REQUEST_NOT_OPEN` (the request is `CANCELLED` or
            `CONVERTED`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED`, or `TENANT_UTILITY_REQUEST_ACTION_NOT_ALLOWED`
            (`UPDATE` is not available to this caller).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Tenant Utility Request not found
            (`TENANT_UTILITY_REQUEST_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /tenant-utility-requests/{id}/available-actions:
    get:
      operationId: getTenantUtilityRequestAvailableActions
      x-required-permission: tenant_company.read
      summary: Resolve the caller's available Tenant Utility Request actions
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02D (BE-14G). Requires
        `tenant_company.read`. Uses the established `{ utilityRequestId, state,
        availableActions }` shape. `availableActions` is backend-authoritative
        and caller-specific: the subset of `UPDATE`, `CANCEL`,
        `CREATE_WORK_REQUEST` and `CREATE_WORK_ORDER` this caller may actually
        run now. An empty list is authoritative - clients must not infer an
        action from `state`. This route performs no transition.
      tags: [Tenant Utility Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantUtilityRequestIdPath"
      responses:
        "200":
          description: Caller-specific available actions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        $ref: "#/components/schemas/TenantUtilityRequestAvailableActions"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: "`PERMISSION_DENIED` - `tenant_company.read` is missing."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Tenant Utility Request not found
            (`TENANT_UTILITY_REQUEST_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /tenant-utility-requests/{id}/work-request:
    post:
      operationId: createTenantUtilityRequestWorkRequest
      x-required-permission: tenant_company.manage
      summary: Bind a new Work Request to a Tenant Utility Request (OPEN -> CONVERTED)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02D (BE-14G). Requires
        `tenant_company.manage` and the backend-resolved `CREATE_WORK_REQUEST`
        action (403 `TENANT_UTILITY_REQUEST_ACTION_NOT_ALLOWED` otherwise).
        This is the BE-08 conversion binding: it creates the Work Request
        server-side from the utility request's own `requestNumber` and
        `utilityType` (as the Work Request `requestType`) with a server-derived
        title and description, stores the resulting id in `workRequestId` and
        moves `status` from `OPEN` to `CONVERTED`. No request body - the caller
        supplies no Work Request data. One binding per request: an already-bound
        request yields 409 `TENANT_UTILITY_REQUEST_WORK_REQUEST_EXISTS`. `data`
        is the updated Tenant Utility Request, not the Work Request. No utility
        calculation, meter reading or billing behaviour is triggered.
      tags: [Tenant Utility Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantUtilityRequestIdPath"
      responses:
        "201":
          description: >
            Work Request bound; the returned Tenant Utility Request carries
            `workRequestId` and `status` = `CONVERTED`.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TenantUtilityRequest" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED`, or `TENANT_UTILITY_REQUEST_ACTION_NOT_ALLOWED`
            (`CREATE_WORK_REQUEST` is not available to this caller).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Tenant Utility Request not found
            (`TENANT_UTILITY_REQUEST_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `TENANT_UTILITY_REQUEST_WORK_REQUEST_EXISTS` - the request is
            already linked to a Work Request.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /tenant-utility-requests/{id}/work-order:
    post:
      operationId: createTenantUtilityRequestWorkOrder
      x-required-permission: tenant_company.manage
      summary: Bind a new Work Order to a converted Tenant Utility Request
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02D (BE-14G). Requires
        `tenant_company.manage` and the backend-resolved `CREATE_WORK_ORDER`
        action (403 `TENANT_UTILITY_REQUEST_ACTION_NOT_ALLOWED` otherwise).
        PRECONDITION: a Work Request must already be bound - a request without
        `workRequestId` is rejected 403
        `TENANT_UTILITY_REQUEST_ACTION_NOT_ALLOWED`, so the Work Order is always
        created from the bound Work Request and never directly from the intake
        record. The Work Order takes a server-derived title and description and
        the utility request's `utilityType` as its `workType`; the caller
        supplies only `workOrderNumber`. The resulting id is stored in
        `workOrderId`; this binding does NOT change `status`. One binding per
        request: an already-bound request yields 409
        `TENANT_UTILITY_REQUEST_WORK_ORDER_EXISTS`. `data` is the updated Tenant
        Utility Request, not the Work Order.
      tags: [Tenant Utility Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantUtilityRequestIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateTenantUtilityRequestWorkOrderRequest" }
      responses:
        "201":
          description: >
            Work Order bound; the returned Tenant Utility Request carries
            `workOrderId` and an unchanged `status`.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TenantUtilityRequest" }
        "400":
          description: >
            Request validation failed - `workOrderNumber` is required and must be
            a valid Work Order number.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED`, or `TENANT_UTILITY_REQUEST_ACTION_NOT_ALLOWED` -
            the latter also covers the missing Work Request precondition.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Tenant Utility Request not found
            (`TENANT_UTILITY_REQUEST_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `TENANT_UTILITY_REQUEST_WORK_ORDER_EXISTS` - the request is already
            linked to a Work Order.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /tenant-utility-requests/{id}/cancel:
    post:
      operationId: cancelTenantUtilityRequest
      x-required-permission: tenant_company.manage
      summary: Cancel an OPEN Tenant Utility Request (OPEN -> CANCELLED)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02D (BE-14G). Requires
        `tenant_company.manage` and the backend-resolved `CANCEL` action (403
        `TENANT_UTILITY_REQUEST_ACTION_NOT_ALLOWED` otherwise). Only an `OPEN`
        utility request can be cancelled; a `CANCELLED` or `CONVERTED` request
        yields 400 `TENANT_UTILITY_REQUEST_NOT_OPEN`. Cancellation is the
        `OPEN` -> `CANCELLED` transition, not a delete - the record is preserved
        and there is no re-open. No request body.
      tags: [Tenant Utility Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantUtilityRequestIdPath"
      responses:
        "200":
          description: Cancelled Tenant Utility Request (`status` = `CANCELLED`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TenantUtilityRequest" }
        "400":
          description: >
            `TENANT_UTILITY_REQUEST_NOT_OPEN` - the request is already
            `CANCELLED` or `CONVERTED`, or the id failed validation.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED`, or `TENANT_UTILITY_REQUEST_ACTION_NOT_ALLOWED`
            (`CANCEL` is not available to this caller).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Tenant Utility Request not found
            (`TENANT_UTILITY_REQUEST_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 02E1 - J10 Tenant Communications.
  #
  # Contract exposure of the ALREADY REGISTERED BE-14K runtime routes only.
  # No runtime, lifecycle, authorization, delivery-transport or notification
  # behaviour is added or changed here.
  # ---------------------------------------------------------------------------

  /tenant-companies/{tenantCompanyId}/communications:
    post:
      operationId: createTenantCommunication
      x-required-permission: tenant_company.manage
      summary: Create a Tenant Communication record
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02E1 (BE-14K). Requires
        `tenant_company.manage`. `clientId` is derived from the Tenant Company
        and `senderUserId` from the authenticated session - neither is
        caller-supplied - and `status` is server-assigned, so the payload
        carries no `status`, `sentAt` or `readAt`. At least one recipient is
        required: `recipientTenantPicId` or `recipientUserId`, else 400
        `VALIDATION_ERROR`. An optional `buildingId` must be an ACTIVE Building
        context of the Tenant, else 400 `TENANT_COMMUNICATION_CONTEXT_INVALID`;
        a recipient that does not belong to the Tenant and the requested context
        yields 400 `TENANT_COMMUNICATION_RECIPIENT_INVALID`; `relatedType` and
        `relatedId` must be supplied together and must reference a record of the
        same Tenant/Building context, else 400
        `TENANT_COMMUNICATION_RELATED_INVALID`. This route creates a RECORD
        only: it delivers nothing and triggers no transport, notification or
        automatic lifecycle transition.
      tags: [Tenant Communications]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantCompanyIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateTenantCommunicationRequest" }
      responses:
        "201":
          description: Tenant Communication created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TenantCommunication" }
        "400":
          description: >
            `VALIDATION_ERROR` (body or path id), or
            `TENANT_COMMUNICATION_RECIPIENT_INVALID`,
            `TENANT_COMMUNICATION_RELATED_INVALID`, or
            `TENANT_COMMUNICATION_CONTEXT_INVALID`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `tenant_company.manage` is missing - or the
            Tenant Company lies outside the caller's BE-02 context
            (`BUILDING_ACCESS_DENIED`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listTenantCommunications
      x-required-permission: tenant_company.read
      summary: List the Tenant Communications of a Tenant Company
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02E1 (BE-14K). Requires
        `tenant_company.read`. The listing is scoped by the path
        `tenantCompanyId`, which is authoritative: a `tenantCompanyId` query
        value cannot re-scope the request, and `tenantCompanyId` is not an
        accepted filter. The optional `recipientTenantPicId`, `recipientUserId`,
        `buildingId`, `communicationType`, `relatedType`, `relatedId` and
        `status` filters narrow the result further; `relatedId` additionally
        requires `relatedType`. An invalid filter value is rejected with 400
        `VALIDATION_ERROR`. `data` is a plain array - not paginated.
      tags: [Tenant Communications]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantCompanyIdPath"
        - name: recipientTenantPicId
          in: query
          required: false
          description: Narrow the listing to one recipient Tenant PIC.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: recipientUserId
          in: query
          required: false
          description: Narrow the listing to one recipient internal User.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          description: Narrow the listing to one Building.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: communicationType
          in: query
          required: false
          description: >
            Data-driven communication type code, normalized and validated with
            the same data-driven code rules the module uses on intake.
          schema: { type: string }
        - name: relatedType
          in: query
          required: false
          schema: { $ref: "#/components/schemas/TenantCommunicationRelatedType" }
        - name: relatedId
          in: query
          required: false
          description: >
            Related record id. Requires `relatedType`; the runtime rejects
            `relatedId` without `relatedType` with 400 `VALIDATION_ERROR`.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/TenantCommunicationStatus" }
      responses:
        "200":
          description: Tenant Communications of the Tenant Company.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/TenantCommunication" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `tenant_company.read` is missing - or the
            Tenant Company lies outside the caller's BE-02 context
            (`BUILDING_ACCESS_DENIED`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /tenant-communications/{id}:
    get:
      operationId: getTenantCommunication
      x-required-permission: tenant_company.read
      summary: Get a Tenant Communication
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02E1 (BE-14K). Requires
        `tenant_company.read`. Reading the record does not change its `status`
        and does not record a read receipt - only
        `POST /tenant-communications/{id}/read` does.
      tags: [Tenant Communications]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantCommunicationIdPath"
      responses:
        "200":
          description: Tenant Communication.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TenantCommunication" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED`, `TENANT_COMMUNICATION_UNAUTHORIZED` (the acting
            User is not authorized for this communication action), or
            `BUILDING_ACCESS_DENIED` (BE-02 context isolation).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Tenant Communication not found (`TENANT_COMMUNICATION_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    patch:
      operationId: updateTenantCommunication
      x-required-permission: tenant_company.manage
      summary: Update a DRAFT Tenant Communication
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02E1 (BE-14K). Requires
        `tenant_company.manage`. Only a `DRAFT` communication can be updated -
        anything else yields 400 `TENANT_COMMUNICATION_NOT_DRAFT`. Only
        `communicationType`, `subject` and `messageBody` are mutable and all
        three are optional; only the supplied fields change. Supplying
        `clientId`, `tenantCompanyId`, `buildingId`, `senderUserId`,
        `recipientTenantPicId`, `recipientUserId`, `relatedType`, `relatedId`,
        `status`, `sentAt` or `readAt` is rejected with 400 `VALIDATION_ERROR` as
        an immutable field. The route never changes `status` and sends nothing.
      tags: [Tenant Communications]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantCommunicationIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateTenantCommunicationRequest" }
      responses:
        "200":
          description: Updated Tenant Communication (`status` unchanged).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TenantCommunication" }
        "400":
          description: >
            `VALIDATION_ERROR` (id or payload, including an immutable field), or
            `TENANT_COMMUNICATION_NOT_DRAFT` - the communication is `SENT` or
            `READ`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED`, `TENANT_COMMUNICATION_UNAUTHORIZED` (the acting
            User is not authorized for this communication action), or
            `BUILDING_ACCESS_DENIED` (BE-02 context isolation).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Tenant Communication not found (`TENANT_COMMUNICATION_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /tenant-communications/{id}/send:
    post:
      operationId: sendTenantCommunication
      x-required-permission: tenant_company.manage
      summary: Mark a DRAFT Tenant Communication as SENT
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02E1 (BE-14K). Requires
        `tenant_company.manage`. This command is the `DRAFT` -> `SENT`
        transition of the record and records `sentAt`; it delivers nothing.
        Only a `DRAFT` communication can be sent - anything else yields 400
        `TENANT_COMMUNICATION_NOT_DRAFT`. Idempotent re-send is NOT supported:
        sending an already `SENT` or `READ` communication fails. No request
        body.
      tags: [Tenant Communications]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantCommunicationIdPath"
      responses:
        "200":
          description: Sent Tenant Communication (`status` = `SENT`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TenantCommunication" }
        "400":
          description: >
            `TENANT_COMMUNICATION_NOT_DRAFT` - the communication is already
            `SENT` or `READ` - or the id failed validation
            (`VALIDATION_ERROR`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED`, `TENANT_COMMUNICATION_UNAUTHORIZED` (the acting
            User is not authorized for this communication action), or
            `BUILDING_ACCESS_DENIED` (BE-02 context isolation).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Tenant Communication not found (`TENANT_COMMUNICATION_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /tenant-communications/{id}/read:
    post:
      operationId: readTenantCommunication
      x-required-permission: tenant_company.read
      summary: Mark a SENT Tenant Communication as READ
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02E1 (BE-14K). Requires
        `tenant_company.read`. This command is the `SENT` -> `READ` transition
        of the record and records `readAt`. Only a sent communication can be
        marked read - a communication that was never sent yields 400
        `TENANT_COMMUNICATION_NOT_SENT`, and an already read one yields 409
        `TENANT_COMMUNICATION_ALREADY_READ`. No request body.
      tags: [Tenant Communications]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/TenantCommunicationIdPath"
      responses:
        "200":
          description: Read Tenant Communication (`status` = `READ`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/TenantCommunication" }
        "400":
          description: >
            `TENANT_COMMUNICATION_NOT_SENT` - the communication is still
            `DRAFT` - or the id failed validation (`VALIDATION_ERROR`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED`, `TENANT_COMMUNICATION_UNAUTHORIZED` (the acting
            User is not authorized for this communication action), or
            `BUILDING_ACCESS_DENIED` (BE-02 context isolation).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Tenant Communication not found (`TENANT_COMMUNICATION_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `TENANT_COMMUNICATION_ALREADY_READ` - the communication is already
            `READ`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 02E2 - J10 Work Requests.
  #
  # Contract exposure of the ALREADY REGISTERED BE-08A runtime routes only.
  # No runtime, lifecycle, authorization, conversion or work-order behaviour is
  # added or changed here.
  # ---------------------------------------------------------------------------

  /buildings/{buildingId}/work-requests:
    post:
      operationId: createWorkRequest
      x-required-permission: work_request.manage
      summary: Create a Work Request in a Building
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02E2 (BE-08A). Requires
        `work_request.manage` and an ACTIVE caller Building assignment for the
        path `buildingId` (`requireBuildingAccess('buildingId')`, else 403
        `BUILDING_ACCESS_DENIED`). `buildingId` comes from the path and
        `requestedByUserId` from the authenticated session, so the requester can
        never be spoofed. `clientId` IS caller-supplied and must resolve, through
        the Building's Property, to the same Client, else 400
        `WORK_REQUEST_BUILDING_CLIENT_MISMATCH`. `requestNumber` is unique per
        Client (409 `WORK_REQUEST_NUMBER_ALREADY_EXISTS`). `requestNumber` and
        `requestType` are trimmed and uppercased on intake. The request is
        created `OPEN` with server-assigned `status` and `requestedAt`; it
        carries no Asset, priority, assignee, evidence or execution field and
        triggers no Work Order, approval, SLA or notification behaviour.
      tags: [Work Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateWorkRequestRequest" }
      responses:
        "201":
          description: Work Request created (`status` = `OPEN`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkRequest" }
        "400":
          description: >
            `VALIDATION_ERROR` (body or path id), or
            `WORK_REQUEST_BUILDING_CLIENT_MISMATCH` - the Building does not
            resolve to the supplied `clientId`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `work_request.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `WORK_REQUEST_NUMBER_ALREADY_EXISTS` - the `requestNumber` is
            already used by another Work Request of the same Client.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listBuildingWorkRequests
      x-required-permission: work_request.read
      summary: List the Work Requests of a Building
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02E2 (BE-08A). Requires
        `work_request.read` and an ACTIVE caller Building assignment for the path
        `buildingId` (else 403 `BUILDING_ACCESS_DENIED`). The listing is scoped
        by the path Building, which is authoritative. The optional `status` and
        `requestType` filters narrow the result; an invalid value is rejected
        with 400 `VALIDATION_ERROR`. `data` is a plain array - not paginated.
      tags: [Work Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/BuildingIdPath"
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/WorkRequestStatus" }
        - name: requestType
          in: query
          required: false
          description: >
            Data-driven request type code. Trimmed and uppercased before
            matching; must start with a letter and contain only uppercase
            letters, digits, hyphens and underscores (2-64 characters).
          schema: { type: string, minLength: 2, maxLength: 64 }
      responses:
        "200":
          description: Work Requests of the Building.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/WorkRequest" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `work_request.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /work-requests/{id}:
    get:
      operationId: getWorkRequest
      x-required-permission: work_request.read
      summary: Get a Work Request
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02E2 (BE-08A). Requires
        `work_request.read`. This path carries no `buildingId`, so BE-02 Building
        isolation is enforced after the record is loaded: an unknown id yields
        404 `WORK_REQUEST_NOT_FOUND` first, then the caller must hold an ACTIVE
        assignment to the record's Building, else 403 `BUILDING_ACCESS_DENIED`.
      tags: [Work Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkRequestIdPath"
      responses:
        "200":
          description: Work Request.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkRequest" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `work_request.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Work Request's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Work Request not found (`WORK_REQUEST_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    patch:
      operationId: updateWorkRequest
      x-required-permission: work_request.manage
      summary: Update an OPEN Work Request
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02E2 (BE-08A). Requires
        `work_request.manage` and an ACTIVE caller Building assignment to the
        record's Building (else 403 `BUILDING_ACCESS_DENIED`). Only an `OPEN`
        request can be updated - `CANCELLED` and `CONVERTED` yield 400
        `WORK_REQUEST_NOT_OPEN`. Only `title`, `description` and `requestType`
        are mutable and all three are optional; only the supplied fields change
        and `description: null` clears it. `requestNumber`, `clientId`,
        `buildingId`, `requestedByUserId`, `requestedAt` and `status` are not
        part of this DTO - `status` is never changed by this route, and any other
        field present in the payload is ignored by the update parser.
      tags: [Work Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkRequestIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateWorkRequestRequest" }
      responses:
        "200":
          description: Updated Work Request (`status` unchanged).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkRequest" }
        "400":
          description: >
            `VALIDATION_ERROR` (id or payload), or `WORK_REQUEST_NOT_OPEN` - the
            request is `CANCELLED` or `CONVERTED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `work_request.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Work Request's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Work Request not found (`WORK_REQUEST_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /work-requests/{id}/cancel:
    post:
      operationId: cancelWorkRequest
      x-required-permission: work_request.manage
      summary: Cancel an OPEN Work Request (OPEN -> CANCELLED)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02E2 (BE-08A). Requires
        `work_request.manage` and an ACTIVE caller Building assignment to the
        record's Building (else 403 `BUILDING_ACCESS_DENIED`). This command is
        the `OPEN` -> `CANCELLED` transition, not a delete: the record is
        preserved and there is no re-open. Cancelling an already `CANCELLED`
        request yields 400 `WORK_REQUEST_NOT_OPEN`; cancelling a `CONVERTED`
        request yields 400 `WORK_REQUEST_TERMINAL_STATE`, because `CONVERTED` is
        terminal for the request channel. No request body.
      tags: [Work Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkRequestIdPath"
      responses:
        "200":
          description: Cancelled Work Request (`status` = `CANCELLED`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkRequest" }
        "400":
          description: >
            `WORK_REQUEST_NOT_OPEN` - the request is already `CANCELLED` - or
            `WORK_REQUEST_TERMINAL_STATE` - the request is `CONVERTED` - or the
            id failed validation (`VALIDATION_ERROR`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `work_request.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Work Request's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Work Request not found (`WORK_REQUEST_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03A - J11 Permits (BE-20A).
  #
  # Contract exposure of the ALREADY REGISTERED BE-20A runtime routes only.
  # No runtime, lifecycle, authorization or contractor-context behaviour is
  # added or changed here.
  # ---------------------------------------------------------------------------

  /permits:
    post:
      operationId: createPermit
      x-required-permission: permit.manage
      summary: Create a Permit (BE-20A foundation)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03A (BE-20A). Requires
        `permit.manage` and an ACTIVE caller Building assignment resolved
        through the contractor context. `buildingId` is the Building where
        the work will be performed and must match the resolved contractor's
        Building; `contractorContextType` + `contractorContextId` are
        resolved by the BE-20B contractor resolver (Vendor or Tenant
        Contractor) - mismatched, inactive or inaccessible contexts yield
        400 `PERMIT_CONTRACTOR_INVALID` or 400 `PERMIT_CONTEXT_MISMATCH`.
        `permitNumber` is unique per Client (409
        `PERMIT_NUMBER_ALREADY_EXISTS`); `permitNumber` and `permitType`
        are uppercased and trimmed. `clientId`, `status`, `requestedAt`,
        `createdByUserId`, `cancelledAt`, `cancelledByUserId`, `createdAt`
        and `updatedAt` are server-controlled and must not be supplied (400
        `VALIDATION_ERROR`). The record is created `DRAFT` with
        server-assigned `requestedAt`. No approval, validity, worker,
        equipment, evidence or lifecycle behaviour is performed by this route.
      tags: [Permits]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePermitRequest" }
      responses:
        "201":
          description: Permit created (`status` = `DRAFT`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Permit" }
        "400":
          description: >
            `VALIDATION_ERROR` (body or Building/contractor validation),
            `PERMIT_CONTRACTOR_INVALID` - contractor context invalid/inactive,
            or `PERMIT_CONTEXT_MISMATCH` - Permit/Building/Client/contractor
            contexts do not match.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the resolved Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `PERMIT_NUMBER_ALREADY_EXISTS` - the `permitNumber` is already used
            by another Permit of the same Client.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listPermits
      x-required-permission: permit.read
      summary: List Permits (Building-scoped)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03A (BE-20A). Requires
        `permit.read`. The result is constrained to the Buildings the caller
        can access (empty when the caller has no accessible Building). An
        explicit `buildingId` filter additionally asserts Building access on
        that Building. Optional filters narrow the result; an invalid value
        is rejected with 400 `VALIDATION_ERROR`. `data` is a plain array -
        not paginated. Filters referencing other BE-20 domains (work context,
        validity) are read-only existence checks and do not mutate state.
      tags: [Permits]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: false
          description: Narrow the listing to one Building. Building access is asserted when supplied.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: contractorVendorId
          in: query
          required: false
          description: Filter by the resolved Vendor id.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: contractorId
          in: query
          required: false
          description: Alias for `contractorVendorId`. When both are supplied they must match.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: contractorContextId
          in: query
          required: false
          description: Filter by the source-specific context id (Vendor id or Tenant Contractor relationship id).
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/PermitStatus" }
        - name: permitType
          in: query
          required: false
          description: >
            Data-driven permit type code. Trimmed and uppercased; must match
            `^[A-Z][A-Z0-9_-]{0,99}$`.
          schema: { type: string, minLength: 1, maxLength: 100 }
        - name: locationType
          in: query
          required: false
          description: >
            Work-context location type filter (BE-20D). Must be one of
            BUILDING, FLOOR, AREA, ROOM, FUNCTIONAL_LOCATION.
          schema: { $ref: "#/components/schemas/PermitWorkLocationType" }
        - name: locationId
          in: query
          required: false
          description: Work-context location id (matched against building/floor/area/room/functionalLocation).
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: workType
          in: query
          required: false
          description: >
            Data-driven work type code. Trimmed and uppercased; must match
            `^[A-Z][A-Z0-9_-]*$` (2-64 characters).
          schema: { type: string, minLength: 2, maxLength: 64 }
        - name: validityStatus
          in: query
          required: false
          description: >
            Current-validity status filter (BE-20G). Must be one of PENDING,
            VALID, EXPIRED, REVOKED.
          schema: { $ref: "#/components/schemas/PermitValidityStatus" }
        - name: validAt
          in: query
          required: false
          description: >
            Validity instant filter. Must be an ISO-8601 date-time with a
            timezone (e.g. 2025-01-01T00:00:00Z).
          schema: { type: string, format: date-time }
      responses:
        "200":
          description: Permits matching the filters within the caller's accessible Buildings.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/Permit" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the filtered Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permits/{id}:
    get:
      operationId: getPermit
      x-required-permission: permit.read
      summary: Get a Permit
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03A (BE-20A). Requires
        `permit.read`. The path carries no `buildingId`, so BE-02 Building
        isolation is enforced after the record is loaded: an unknown id yields
        404 `PERMIT_NOT_FOUND` first, then the caller must hold an ACTIVE
        assignment to the record's Building, else 403 `BUILDING_ACCESS_DENIED`.
      tags: [Permits]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitIdPath"
      responses:
        "200":
          description: Permit.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Permit" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Permit's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit not found (`PERMIT_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    patch:
      operationId: updatePermit
      x-required-permission: permit.manage
      summary: Update a DRAFT Permit
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03A (BE-20A). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the
        record's Building (else 403 `BUILDING_ACCESS_DENIED`). Only a `DRAFT`
        Permit can be updated - `CANCELLED` and any later terminal state yield
        400 `PERMIT_UPDATE_NOT_ALLOWED`. Only `permitType`, `title`,
        `workDescription` and `applicantReference` are mutable; only the
        supplied fields change and `applicantReference: null` clears it. At
        least one mutable field is required. `permitNumber`, `clientId`,
        `buildingId`, contractor context fields, `status`, `requestedAt`,
        `createdByUserId`, `cancelledAt`, `cancelledByUserId`, `createdAt`
        and `updatedAt` are immutable - supplying them yields 400
        `VALIDATION_ERROR`.
      tags: [Permits]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdatePermitRequest" }
      responses:
        "200":
          description: Updated Permit (`status` unchanged).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Permit" }
        "400":
          description: >
            `VALIDATION_ERROR` (id or payload) or `PERMIT_UPDATE_NOT_ALLOWED` -
            the Permit is not `DRAFT` (or an immutable field was supplied).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Permit's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit not found (`PERMIT_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permits/{id}/cancel:
    post:
      operationId: cancelPermit
      x-required-permission: permit.manage
      summary: Cancel a DRAFT Permit (DRAFT -> CANCELLED)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03A (BE-20A). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the
        record's Building (else 403 `BUILDING_ACCESS_DENIED`). This command is
        the `DRAFT` -> `CANCELLED` transition, not a delete: the record is
        preserved and there is no re-open. Only a `DRAFT` Permit can be
        cancelled - `CANCELLED` and any later terminal state yield 400
        `PERMIT_CANCEL_NOT_ALLOWED`. No request body.
      tags: [Permits]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitIdPath"
      responses:
        "200":
          description: Cancelled Permit (`status` = `CANCELLED`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/Permit" }
        "400":
          description: >
            `PERMIT_CANCEL_NOT_ALLOWED` - the Permit is not `DRAFT` - or the id
            failed validation (`VALIDATION_ERROR`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Permit's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit not found (`PERMIT_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03B - J11 Permit Applications (BE-20C).
  #
  # Contract exposure of the ALREADY REGISTERED BE-20C runtime routes only.
  # No runtime, lifecycle, authorization or contractor-context behaviour is
  # added or changed here.
  # ---------------------------------------------------------------------------

  /permit-applications:
    post:
      operationId: createPermitApplication
      x-required-permission: permit.manage
      summary: Create a Permit Application (BE-20C)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03B (BE-20C). Requires
        `permit.manage` and an ACTIVE caller Building assignment resolved
        through the referenced Permit. `permitId` must reference an existing
        BE-20A Permit whose Building the caller can access - the Application's
        `clientId`, `buildingId`, `permitReference`, `permitStatus`,
        `contractorVendorId` and `tenantContractorRelationshipId` are
        authoritative projections of that Permit and are never caller-supplied
        as `clientId`/`buildingId`. `requestedWorkAt` must be a future
        ISO-8601 date-time with timezone (400
        `PERMIT_APPLICATION_INVALID_WORK_DATE` otherwise); optional
        `applicantReference`, `contractorContextType`,
        `contractorContextId` and `workDescription` are assertions validated
        against the Permit (mismatch yields 400
        `PERMIT_APPLICATION_CONTRACTOR_INVALID`). Server-controlled fields
        `clientId`, `buildingId`, `permitReference`, `permitStatus`,
        `status`, `submittedAt`, `submittedByUserId`, `cancelledAt`,
        `cancelledByUserId`, `createdByUserId`, `createdAt`, `updatedAt`
        must not be supplied (400 `VALIDATION_ERROR`). One Application per
        Permit (409 `PERMIT_APPLICATION_ALREADY_EXISTS`). No approval, safety
        or validity behaviour is performed by this route.
      tags: [Permit Applications]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePermitApplicationRequest" }
      responses:
        "201":
          description: Permit Application created (`status` = `DRAFT`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitApplication" }
        "400":
          description: >
            `VALIDATION_ERROR` (body or timestamp), or
            `PERMIT_APPLICATION_PERMIT_INVALID` - the Permit does not exist or
            cannot receive an Application, `PERMIT_APPLICATION_CONTRACTOR_INVALID`
            - contractor assertion mismatch/ineligible, or
            `PERMIT_APPLICATION_INVALID_WORK_DATE` - `requestedWorkAt` is not a
            future instant.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Permit's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `PERMIT_APPLICATION_ALREADY_EXISTS` - the Permit already has an
            Application.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listPermitApplications
      x-required-permission: permit.read
      summary: List Permit Applications (Building-scoped)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03B (BE-20C). Requires
        `permit.read`. The result is constrained to the Buildings the caller
        can access (empty when the caller has no accessible Building). An
        explicit `buildingId` filter additionally asserts Building access on
        that Building. Optional filters narrow the result; an invalid value
        is rejected with 400 `VALIDATION_ERROR`. `data` is a plain array -
        not paginated. `requestedWorkFrom`/`requestedWorkTo` may be supplied
        as `dateFrom`/`dateTo` aliases but must match when both are sent.
      tags: [Permit Applications]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: false
          description: Narrow the listing to one Building. Building access is asserted when supplied.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: contractorVendorId
          in: query
          required: false
          description: Filter by the resolved Vendor id.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: contractorId
          in: query
          required: false
          description: Alias for `contractorVendorId`. When both are supplied they must match.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: contractorContextId
          in: query
          required: false
          description: Filter by the source-specific context id (Vendor id or Tenant Contractor relationship id).
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          schema: { $ref: "#/components/schemas/PermitApplicationStatus" }
        - name: requestedWorkFrom
          in: query
          required: false
          description: >
            Inclusive lower bound for `requestedWorkAt`. Must be ISO-8601 with
            timezone. Alias `dateFrom` is accepted but must equal the primary.
          schema: { type: string, format: date-time }
        - name: dateFrom
          in: query
          required: false
          description: Alias for `requestedWorkFrom`. When both are supplied they must be identical.
          schema: { type: string, format: date-time }
        - name: requestedWorkTo
          in: query
          required: false
          description: >
            Inclusive upper bound for `requestedWorkAt`. Must be ISO-8601 with
            timezone. Must not be before `requestedWorkFrom`. Alias `dateTo`
            is accepted but must equal the primary.
          schema: { type: string, format: date-time }
        - name: dateTo
          in: query
          required: false
          description: Alias for `requestedWorkTo`. When both are supplied they must be identical.
          schema: { type: string, format: date-time }
        - name: requestedWorkDate
          in: query
          required: false
          description: >
            Calendar-date filter `YYYY-MM-DD` for `requestedWorkAt`. Alias
            `date` is accepted.
          schema: { type: string, pattern: "^\\d{4}-\\d{2}-\\d{2}$", example: "2026-09-15" }
        - name: date
          in: query
          required: false
          description: Alias for `requestedWorkDate`. When both are supplied they must describe the same date.
          schema: { type: string, pattern: "^\\d{4}-\\d{2}-\\d{2}$" }
      responses:
        "200":
          description: Permit Applications matching the filters within the caller's accessible Buildings.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PermitApplication" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the filtered Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-applications/{id}:
    get:
      operationId: getPermitApplication
      x-required-permission: permit.read
      summary: Get a Permit Application
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03B (BE-20C). Requires
        `permit.read`. The path carries no `buildingId`, so BE-02 Building
        isolation is enforced after the Application's Permit is loaded: an
        unknown id yields 404 `PERMIT_APPLICATION_NOT_FOUND` first, then the
        caller must hold an ACTIVE assignment to the Application's Building,
        else 403 `BUILDING_ACCESS_DENIED`.
      tags: [Permit Applications]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitApplicationIdPath"
      responses:
        "200":
          description: Permit Application.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitApplication" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Application's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Application not found (`PERMIT_APPLICATION_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    patch:
      operationId: updatePermitApplication
      x-required-permission: permit.manage
      summary: Update a DRAFT Permit Application
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03B (BE-20C). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the
        Application's Building (else 403 `BUILDING_ACCESS_DENIED`). Only a
        `DRAFT` Application can be updated - `SUBMITTED` or `CANCELLED` yield
        400 `PERMIT_APPLICATION_UPDATE_NOT_ALLOWED`. Only `requestedWorkAt`
        and `notes` are mutable; only the supplied fields change and
        `notes: null` clears it. At least one mutable field is required.
        `permitId`, `permitReference`, `clientId`, `buildingId`,
        `applicantReference`, contractor context fields, `workDescription`,
        `status`, `submittedAt`, `submittedByUserId`, `cancelledAt`,
        `cancelledByUserId`, `createdByUserId`, `createdAt` and `updatedAt`
        are inherited/controlled and must not be supplied (400
        `VALIDATION_ERROR`).
      tags: [Permit Applications]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitApplicationIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdatePermitApplicationRequest" }
      responses:
        "200":
          description: Updated Permit Application (`status` unchanged).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitApplication" }
        "400":
          description: >
            `VALIDATION_ERROR` (id or payload) or
            `PERMIT_APPLICATION_UPDATE_NOT_ALLOWED` - the Application is not
            `DRAFT` (or an inherited field was supplied).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Application's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Application not found (`PERMIT_APPLICATION_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-applications/{id}/submit:
    post:
      operationId: submitPermitApplication
      x-required-permission: permit.manage
      summary: Submit a DRAFT Permit Application (DRAFT -> SUBMITTED)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03B (BE-20C). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the
        Application's Building (else 403 `BUILDING_ACCESS_DENIED`). This
        command is the `DRAFT` -> `SUBMITTED` transition: the record is
        preserved and `submittedAt`/`submittedByUserId` are recorded. Only a
        `DRAFT` Application with a future `requestedWorkAt` can be submitted -
        `SUBMITTED` or `CANCELLED` yield 400
        `PERMIT_APPLICATION_SUBMIT_NOT_ALLOWED`. No request body.
      tags: [Permit Applications]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitApplicationIdPath"
      responses:
        "200":
          description: Submitted Permit Application (`status` = `SUBMITTED`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitApplication" }
        "400":
          description: >
            `PERMIT_APPLICATION_SUBMIT_NOT_ALLOWED` - the Application is not a
            valid `DRAFT` (or `requestedWorkAt` is not future) - or the id
            failed validation (`VALIDATION_ERROR`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Application's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Application not found (`PERMIT_APPLICATION_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-applications/{id}/cancel:
    post:
      operationId: cancelPermitApplication
      x-required-permission: permit.manage
      summary: Cancel a Permit Application (DRAFT|SUBMITTED -> CANCELLED)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03B (BE-20C). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the
        Application's Building (else 403 `BUILDING_ACCESS_DENIED`). This
        command is the `DRAFT`|`SUBMITTED` -> `CANCELLED` transition, not a
        delete: the record is preserved and there is no re-open. `CANCELLED`
        or an already terminal state yields 400
        `PERMIT_APPLICATION_CANCEL_NOT_ALLOWED`. No request body.
      tags: [Permit Applications]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitApplicationIdPath"
      responses:
        "200":
          description: Cancelled Permit Application (`status` = `CANCELLED`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitApplication" }
        "400":
          description: >
            `PERMIT_APPLICATION_CANCEL_NOT_ALLOWED` - the Application is
            already `CANCELLED` or not cancellable - or the id failed
            validation (`VALIDATION_ERROR`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Application's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Application not found (`PERMIT_APPLICATION_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03C - J11 Permit Approvals (BE-20F).
  #
  # Contract exposure of the ALREADY REGISTERED BE-20F runtime routes only.
  # No runtime, lifecycle or generic approval-engine behaviour is added or
  # changed here.
  # ---------------------------------------------------------------------------

  /permit-applications/{applicationId}/approvals:
    post:
      operationId: createPermitApproval
      x-required-permission: permit.manage
      summary: Create a Permit Approval (bind approver to SUBMITTED application)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03C (BE-20F). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the
        Application's Building. The Application must be `SUBMITTED` and its
        safety readiness must be READY - otherwise 400
        `PERMIT_APPROVAL_CONTEXT_INVALID` or 400
        `PERMIT_APPROVAL_SAFETY_NOT_READY`. The caller-provided
        `approvalStage`/`approvalType` (data-driven codes, trimmed and
        uppercased) must be unique per Application (409
        `PERMIT_APPROVAL_ALREADY_EXISTS`). `approverUserId` must be an active
        User who already holds `permit.approve` and has an ACTIVE Building
        assignment to the Application's Building (400
        `PERMIT_APPROVAL_APPROVER_INVALID` otherwise). Optional `notes` is
        stored with the binding. This creates the thin review row
        (`PENDING`).
      tags: [Permit Approvals]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: applicationId
          in: path
          required: true
          description: Authoritative `permit_applications.id` that must be `SUBMITTED`.
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePermitApprovalRequest" }
      responses:
        "201":
          description: Permit Approval created (`approvalStatus` = `PENDING`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitApproval" }
        "400":
          description: >
            `VALIDATION_ERROR` (path or body), or
            `PERMIT_APPROVAL_CONTEXT_INVALID` - Application not SUBMITTED,
            `PERMIT_APPROVAL_APPROVER_INVALID` - approver ineligible, or
            `PERMIT_APPROVAL_SAFETY_NOT_READY` - safety not READY.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Application's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Application not found (`PERMIT_APPLICATION_NOT_FOUND` or
            `PERMIT_APPROVAL_CONTEXT_INVALID` when the Application id is unknown).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `PERMIT_APPROVAL_ALREADY_EXISTS` - the `approvalStage`/`approvalType`
            is already bound to the Application.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-applications/{applicationId}/approval-context:
    get:
      operationId: getPermitApprovalContext
      x-required-permission: permit.read
      summary: Get Permit approval context (safety readiness + approvals)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03C (BE-20F). Requires
        `permit.read` and an ACTIVE caller Building assignment to the
        Application's Building (else 403 `BUILDING_ACCESS_DENIED`). Returns
        the Application's `permitId`/`buildingId`, its current `applicationStatus`,
        the computed `safetyReadiness` (from BE-20E, READY vs NOT_READY) and
        the full list of `approvals` bound to the Application. An unknown
        `applicationId` yields 404 `PERMIT_APPLICATION_NOT_FOUND`.
      tags: [Permit Approvals]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: applicationId
          in: path
          required: true
          description: Authoritative `permit_applications.id`.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Permit approval context with safety readiness and approvals.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitApprovalContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Application's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Application not found (`PERMIT_APPLICATION_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-approvals/pending:
    get:
      operationId: listPendingPermitApprovals
      x-required-permission: permit.read
      summary: List pending Permit Approvals (Building-scoped)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03C (BE-20F). Requires
        `permit.read`. The result is constrained to the Buildings the caller
        can access (empty when the caller has no accessible Building). An
        explicit `buildingId` filter additionally asserts Building access on
        that Building. Optional `approverUserId`, `approvalStage` and
        `approvalType` filters narrow the result; an invalid code is rejected
        with 400 `VALIDATION_ERROR`. Only `PENDING` approvals are returned.
        No pagination - `data` is a plain array.
      tags: [Permit Approvals]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: false
          description: Narrow the listing to one Building. Building access is asserted when supplied.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: approverUserId
          in: query
          required: false
          description: Filter by the assigned approver.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: approvalStage
          in: query
          required: false
          description: >
            Data-driven stage code. Trimmed and uppercased; must match the
            valid request-type pattern.
          schema: { type: string, minLength: 2, maxLength: 64 }
        - name: approvalType
          in: query
          required: false
          description: >
            Data-driven type code. Trimmed and uppercased; must match the
            valid request-type pattern.
          schema: { type: string, minLength: 2, maxLength: 64 }
      responses:
        "200":
          description: Pending Permit Approvals within the caller's accessible Buildings.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PermitApproval" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the filtered Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-approvals/{id}/available-actions:
    get:
      operationId: getPermitApprovalAvailableActions
      x-required-permission: permit.read
      summary: Get available actions for a Permit Approval
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03C (BE-20F). Requires
        `permit.read` and an ACTIVE caller Building assignment to the
        Approval's Building (else 403 `BUILDING_ACCESS_DENIED`). Returns
        `{ approvalId, approvalStatus, applicationStatus,
        safetyReadinessStatus, availableActions }` where `availableActions`
        is the backend-authoritative, caller-specific subset of
        `[APPROVE, REJECT, REQUEST_REWORK]` the approver may actually run now.
        Only an assigned, authorized `PENDING` approval whose safety is READY
        may offer actions; an empty list is authoritative and means nothing is
        allowed now. An unknown `id` yields 404 `PERMIT_APPROVAL_NOT_FOUND`.
      tags: [Permit Approvals]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitApprovalIdPath"
      responses:
        "200":
          description: Available actions for the approval.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitApprovalAvailableActions" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Approval's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Approval not found (`PERMIT_APPROVAL_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-approvals/{id}/approve:
    post:
      operationId: approvePermitApproval
      x-required-permission: permit.approve
      summary: Approve a Permit Approval (PENDING -> APPROVED)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03C (BE-20F). Requires
        `permit.approve` and that the caller is the assigned approver with an
        ACTIVE Building assignment to the Approval's Building. The approval
        must be `PENDING` and its action `APPROVE` must be currently offered
        (determined via available-actions) - otherwise 403
        `PERMIT_APPROVAL_ACTION_NOT_ALLOWED`. An already decided approval
        yields 409 `PERMIT_APPROVAL_ALREADY_DECIDED`; deciding as
        non-assigned approver yields 403
        `PERMIT_APPROVAL_UNAUTHORIZED_APPROVER`. Optional `decisionNotes`
        (`notes` alias accepted) is stored with the review decision; empty is
        treated as null. This performs the `PENDING` -> `APPROVED` review
        transition.
      tags: [Permit Approvals]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitApprovalIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PermitApprovalDecisionRequest" }
      responses:
        "200":
          description: Approved Permit Approval (`approvalStatus` = `APPROVED`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitApproval" }
        "400":
          description: >
            `VALIDATION_ERROR` (body) or the review id failed validation.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.approve` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Approval's Building - or `PERMIT_APPROVAL_UNAUTHORIZED_APPROVER`
            / `PERMIT_APPROVAL_ACTION_NOT_ALLOWED` - not the assigned approver
            or action not offered.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Approval not found (`PERMIT_APPROVAL_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `PERMIT_APPROVAL_ALREADY_DECIDED` - the approval has already been
            decided.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-approvals/{id}/reject:
    post:
      operationId: rejectPermitApproval
      x-required-permission: permit.approve
      summary: Reject a Permit Approval (PENDING -> REJECTED)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03C (BE-20F). Requires
        `permit.approve` and that the caller is the assigned approver with an
        ACTIVE Building assignment to the Approval's Building. The approval
        must be `PENDING` and `REJECT` must be offered - otherwise 403
        `PERMIT_APPROVAL_ACTION_NOT_ALLOWED`. `decisionNotes` (alias `notes`)
        is required (400 `VALIDATION_ERROR` when absent). An already decided
        approval yields 409 `PERMIT_APPROVAL_ALREADY_DECIDED`.
      tags: [Permit Approvals]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitApprovalIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PermitApprovalDecisionRequest" }
      responses:
        "200":
          description: Rejected Permit Approval (`approvalStatus` = `REJECTED`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitApproval" }
        "400":
          description: >
            `VALIDATION_ERROR` - `decisionNotes` is required - or the id failed
            validation.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` / `BUILDING_ACCESS_DENIED` /
            `PERMIT_APPROVAL_UNAUTHORIZED_APPROVER` /
            `PERMIT_APPROVAL_ACTION_NOT_ALLOWED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Approval not found (`PERMIT_APPROVAL_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `PERMIT_APPROVAL_ALREADY_DECIDED` - the approval has already been
            decided.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-approvals/{id}/request-rework:
    post:
      operationId: requestPermitApprovalRework
      x-required-permission: permit.approve
      summary: Request rework for a Permit Approval (PENDING -> REWORK_REQUIRED)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03C (BE-20F). Requires
        `permit.approve` and that the caller is the assigned approver with an
        ACTIVE Building assignment to the Approval's Building. The approval
        must be `PENDING` and `REQUEST_REWORK` must be offered - otherwise 403
        `PERMIT_APPROVAL_ACTION_NOT_ALLOWED`. `decisionNotes` (alias `notes`)
        is required. An already decided approval yields 409
        `PERMIT_APPROVAL_ALREADY_DECIDED`.
      tags: [Permit Approvals]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitApprovalIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PermitApprovalDecisionRequest" }
      responses:
        "200":
          description: Rework-requested Permit Approval (`approvalStatus` = `REWORK_REQUIRED`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitApproval" }
        "400":
          description: >
            `VALIDATION_ERROR` - `decisionNotes` is required - or the id failed
            validation.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` / `BUILDING_ACCESS_DENIED` /
            `PERMIT_APPROVAL_UNAUTHORIZED_APPROVER` /
            `PERMIT_APPROVAL_ACTION_NOT_ALLOWED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Approval not found (`PERMIT_APPROVAL_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `PERMIT_APPROVAL_ALREADY_DECIDED` - the approval has already been
            decided.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-approvals/{id}:
    get:
      operationId: getPermitApproval
      x-required-permission: permit.read
      summary: Get a Permit Approval
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03C (BE-20F). Requires
        `permit.read` and an ACTIVE caller Building assignment to the
        Approval's Building (else 403 `BUILDING_ACCESS_DENIED`). An unknown
        `id` yields 404 `PERMIT_APPROVAL_NOT_FOUND`.
      tags: [Permit Approvals]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitApprovalIdPath"
      responses:
        "200":
          description: Permit Approval.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitApproval" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Approval's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Approval not found (`PERMIT_APPROVAL_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D1 - J11 Permit Equipment (BE-20I).
  #
  # Contract exposure of the ALREADY REGISTERED BE-20I runtime routes only.
  # No runtime, lifecycle or generic equipment behaviour is added or
  # changed here.
  # ---------------------------------------------------------------------------

  /permits/{permitId}/equipment:
    post:
      operationId: addPermitEquipment
      x-required-permission: permit.manage
      summary: Add Permit Equipment (bind Asset to Permit)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D1 (BE-20I). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the
        Permit's Building (else 403 `BUILDING_ACCESS_DENIED`). The Permit
        must exist and have a `SUBMITTED` Application and an open Permit
        validity (`PENDING` or `VALID`) - otherwise 400
        `PERMIT_EQUIPMENT_CONTEXT_INVALID`. The request `buildingId` and
        `contractorContextType`/`contractorContextId` must match the Permit's
        resolved contractor Building context (400
        `PERMIT_EQUIPMENT_BUILDING_MISMATCH` /
        `PERMIT_EQUIPMENT_CONTRACTOR_MISMATCH` otherwise). The referenced
        `assetId` (alias `equipmentAssetReference` accepted) must be an
        `ACTIVE` Asset in the same Building; its Equipment Profile if present
        must be `ACTIVE` (400 `PERMIT_EQUIPMENT_INVALID` / 400
        `PERMIT_EQUIPMENT_INACTIVE` otherwise). Optional
        `assetIdentifierId`/`assetCertificationId`/`inspectionBindingId`+`inspectionExecutionId`
        are validated against the Asset and its Building, validity window and
        active status. `inspectionBindingId` and `inspectionExecutionId` must be
        supplied together (400 `VALIDATION_ERROR` otherwise) and the execution
        must be `COMPLETED` with an `ACTIVE` binding (400
        `PERMIT_EQUIPMENT_INSPECTION_INVALID`). `assetCertificationId` must be
        `ACTIVE` and cover the equipment validity period (400
        `PERMIT_EQUIPMENT_CERTIFICATION_INVALID`). `validFrom`/`validUntil` are
        ISO-8601 date-times with timezone; `validUntil` must be after
        `validFrom` and the window must be contained within the Permit validity
        (400 `PERMIT_EQUIPMENT_INVALID_VALIDITY` otherwise; defaults to the
        Permit validity when omitted). Backend-resolved fields
        `equipmentName`, `equipmentType`, `identifierReference`, `status`,
        `deactivatedAt` etc must not be supplied (400 `VALIDATION_ERROR`).
        One ACTIVE entry per (`permitApplicationId`, `assetId`) (409
        `PERMIT_EQUIPMENT_ALREADY_ACTIVE`). Created `ACTIVE`.
      tags: [Permit Equipment]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: path
          required: true
          description: Authoritative `permits.id` that owns the equipment binding.
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AddPermitEquipmentRequest" }
      responses:
        "201":
          description: Permit Equipment created (`status` = `ACTIVE`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitEquipment" }
        "400":
          description: >
            `VALIDATION_ERROR` (body/path), or
            `PERMIT_EQUIPMENT_CONTEXT_INVALID` - Permit/Application/validity
            not open, `PERMIT_EQUIPMENT_BUILDING_MISMATCH` - Building mismatch,
            `PERMIT_EQUIPMENT_CONTRACTOR_MISMATCH`, `PERMIT_EQUIPMENT_INVALID` -
            Asset/Profile invalid, `PERMIT_EQUIPMENT_INACTIVE` - Asset/Profile
            inactive, `PERMIT_EQUIPMENT_CERTIFICATION_INVALID`,
            `PERMIT_EQUIPMENT_INSPECTION_INVALID`, or
            `PERMIT_EQUIPMENT_INVALID_VALIDITY` - window not ordered/contained.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Permit's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit not found (surfaced as 400 `PERMIT_EQUIPMENT_CONTEXT_INVALID`
            when the Permit/Application is unknown in this context).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `PERMIT_EQUIPMENT_ALREADY_ACTIVE` - the Asset already has an ACTIVE
            entry on this Permit Application.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listPermitEquipmentForPermit
      x-required-permission: permit.read
      summary: List Permit Equipment for a Permit
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D1 (BE-20I). Requires
        `permit.read` and an ACTIVE caller Building assignment to the Permit's
        Building (else 403 `BUILDING_ACCESS_DENIED`). Lists all Permit Equipment
        bound to the Permit (both `ACTIVE` and `INACTIVE`). An unknown
        `permitId` yields 400 `PERMIT_EQUIPMENT_CONTEXT_INVALID`. No pagination
        - `data` is a plain array. Building isolation is enforced after the
        Permit is loaded.
      tags: [Permit Equipment]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: path
          required: true
          description: Authoritative `permits.id`.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Permit Equipment bound to the Permit.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PermitEquipment" }
        "400":
          description: >
            `VALIDATION_ERROR` - path validation - or
            `PERMIT_EQUIPMENT_CONTEXT_INVALID` - Permit not found / no open
            context.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Permit's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permits/{permitId}/equipment/active:
    get:
      operationId: resolveActivePermitEquipment
      x-required-permission: permit.read
      summary: Resolve active Permit Equipment (eligible for work)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D1 (BE-20I). Requires
        `permit.read` and an ACTIVE caller Building assignment to the Permit's
        Building (else 403 `BUILDING_ACCESS_DENIED`). Returns the
        Permit-scoped active equipment view
        `{ permitId, permitReference, buildingId, contractorContextType,
        contractorVendorId, validityStatus, active, equipmentCount, equipment }`.
        `validityStatus` is the current Permit validity (`PENDING`|`VALID`|
        `EXPIRED`|`REVOKED` or null) and `active` is true only when `VALID`.
        When `active` is false `equipment` is empty; when `VALID` only entries
        with `eligibleForActiveWork` true are returned. An unknown `permitId`
        yields 400 `PERMIT_EQUIPMENT_CONTEXT_INVALID`.
      tags: [Permit Equipment]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: path
          required: true
          description: Authoritative `permits.id`.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Active Permit Equipment view for the Permit.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitActiveEquipmentList" }
        "400":
          description: >
            `VALIDATION_ERROR` - path validation - or
            `PERMIT_EQUIPMENT_CONTEXT_INVALID` - Permit not found.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Permit's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-equipment:
    get:
      operationId: listPermitEquipment
      x-required-permission: permit.read
      summary: List Permit Equipment (Building-scoped)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D1 (BE-20I). Requires
        `permit.read`. The result is constrained to the Buildings the caller
        can access (empty when the caller has no accessible Building). An
        explicit `buildingId` filter additionally asserts Building access on
        that Building. Optional filters `permitId`, `permitApplicationId`,
        `contractorContextType` (`TENANT_CONTRACTOR`|`VENDOR_CONTRACTOR`,
        alias `sourceType`), `contractorContextId`, `contractorVendorId`
        (alias `contractorId`), `assetId` and `status` (`ACTIVE`|`INACTIVE`)
        narrow the result; an invalid value is rejected with 400
        `VALIDATION_ERROR`. `data` is a plain array - not paginated.
      tags: [Permit Equipment]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: query
          required: false
          description: Filter by Permit.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: permitApplicationId
          in: query
          required: false
          description: Filter by Permit Application.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          description: Narrow the listing to one Building. Building access is asserted when supplied.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: contractorContextType
          in: query
          required: false
          description: Filter by contractor context type (`TENANT_CONTRACTOR` or `VENDOR_CONTRACTOR`; alias `sourceType` accepted).
          schema: { $ref: "#/components/schemas/PermitContractorContextType" }
        - name: contractorContextId
          in: query
          required: false
          description: Filter by source-specific contractor context id.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: contractorVendorId
          in: query
          required: false
          description: Filter by resolved Vendor id (alias `contractorId` accepted).
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: assetId
          in: query
          required: false
          description: Filter by Asset.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          description: Filter by Permit Equipment status.
          schema: { $ref: "#/components/schemas/PermitEquipmentStatus" }
      responses:
        "200":
          description: Permit Equipment within the caller's accessible Buildings.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PermitEquipment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the filtered Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-equipment/{id}/deactivate:
    post:
      operationId: deactivatePermitEquipment
      x-required-permission: permit.manage
      summary: Deactivate Permit Equipment (ACTIVE -> INACTIVE)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D1 (BE-20I). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the
        Equipment's Building (else 403 `BUILDING_ACCESS_DENIED`). Only an
        `ACTIVE` entry can be deactivated - an `INACTIVE` entry yields 400
        `PERMIT_EQUIPMENT_DEACTIVATE_NOT_ALLOWED`. No request body.
      tags: [Permit Equipment]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitEquipmentIdPath"
      responses:
        "200":
          description: Deactivated Permit Equipment (`status` = `INACTIVE`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitEquipment" }
        "400":
          description: >
            `VALIDATION_ERROR` - path validation - or
            `PERMIT_EQUIPMENT_DEACTIVATE_NOT_ALLOWED` - not ACTIVE.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Equipment's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Equipment not found (`PERMIT_EQUIPMENT_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-equipment/{id}:
    get:
      operationId: getPermitEquipment
      x-required-permission: permit.read
      summary: Get Permit Equipment
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D1 (BE-20I). Requires
        `permit.read` and an ACTIVE caller Building assignment to the
        Equipment's Building (else 403 `BUILDING_ACCESS_DENIED`). An unknown
        `id` yields 404 `PERMIT_EQUIPMENT_NOT_FOUND`.
      tags: [Permit Equipment]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitEquipmentIdPath"
      responses:
        "200":
          description: Permit Equipment.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitEquipment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Equipment's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Equipment not found (`PERMIT_EQUIPMENT_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    patch:
      operationId: updatePermitEquipment
      x-required-permission: permit.manage
      summary: Update Permit Equipment (ACTIVE on open Permit only)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D1 (BE-20I). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the
        Equipment's Building (else 403 `BUILDING_ACCESS_DENIED`). Only an
        `ACTIVE` entry on an open Permit (`PENDING`|`VALID` validity) may be
        updated (400 `PERMIT_EQUIPMENT_UPDATE_NOT_ALLOWED` otherwise). Updatable
        fields are `assetIdentifierId` (nullable, null clears), `assetCertificationId`
        (nullable, null clears), `inspectionBindingId` (nullable, null clears),
        `inspectionExecutionId` (nullable, null clears), `validFrom`,
        `validUntil` (ISO-8601 date-time with timezone, ordered and contained)
        and `notes` (nullable, 2000 chars; `null` clears). At least one field
        is required (400 `VALIDATION_ERROR` otherwise). Immutable context fields
        (`permitId`, `permitApplicationId`, `buildingId`, `contractorContextType`,
        `contractorContextId`, `contractorVendorId`, `assetId`,
        `equipmentProfileId`, `status`, `deactivatedAt`) must not be supplied (400
        `VALIDATION_ERROR`). Certification and inspection references are revalidated
        as on add. An unknown `id` yields 404 `PERMIT_EQUIPMENT_NOT_FOUND`.
      tags: [Permit Equipment]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitEquipmentIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdatePermitEquipmentRequest" }
      responses:
        "200":
          description: Updated Permit Equipment.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitEquipment" }
        "400":
          description: >
            `VALIDATION_ERROR` (body/path), or
            `PERMIT_EQUIPMENT_UPDATE_NOT_ALLOWED` - not ACTIVE or not on open
            Permit, `PERMIT_EQUIPMENT_CERTIFICATION_INVALID`,
            `PERMIT_EQUIPMENT_INSPECTION_INVALID`, or
            `PERMIT_EQUIPMENT_INVALID_VALIDITY` - window not ordered/contained.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Equipment's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Equipment not found (`PERMIT_EQUIPMENT_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D2 - J11 Permit Evidence (BE-20J).
  #
  # Contract exposure of the ALREADY REGISTERED BE-20J runtime routes only.
  # No runtime, lifecycle or generic evidence behaviour is added or
  # changed here.
  # ---------------------------------------------------------------------------

  /permits/{permitId}/evidence-requirements:
    post:
      operationId: createPermitEvidenceRequirement
      x-required-permission: permit.manage
      summary: Create Permit Evidence Requirement
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D2 (BE-20J). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the
        Permit's Building (else 403 `BUILDING_ACCESS_DENIED`). Creates one
        per-Permit, per-`evidenceType` requirement. The request `buildingId` and
        `contractorContextType`/`contractorContextId` must match the Permit's
        resolved contractor Building context (400
        `PERMIT_EVIDENCE_CONTEXT_MISMATCH` otherwise). `evidenceType` must be
        `PHOTO`|`DOCUMENT`|`SIGNATURE` (400 `VALIDATION_ERROR` otherwise);
        `required` defaults to `true`, `minimumCount` defaults to `1` when
        `required` and `0` otherwise; `maximumCount` is `null` when omitted
        (unbounded) otherwise must be >= `minimumCount` (400 `VALIDATION_ERROR`).
        `description` is optional (2000 chars, empty -> null). One ACTIVE
        requirement per (`permitId`, `evidenceType`) (409
        `PERMIT_EVIDENCE_REQUIREMENT_ALREADY_EXISTS`). An unknown `permitId`
        yields 400 `PERMIT_EVIDENCE_PERMIT_INVALID`.
      tags: [Permit Evidence]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: path
          required: true
          description: Authoritative `permits.id`.
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePermitEvidenceRequirementRequest" }
      responses:
        "201":
          description: Permit Evidence Requirement created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitEvidenceRequirement" }
        "400":
          description: >
            `VALIDATION_ERROR` (body/path), or
            `PERMIT_EVIDENCE_PERMIT_INVALID` - Permit not found, or
            `PERMIT_EVIDENCE_CONTEXT_MISMATCH` - contractor/Building mismatch.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Permit's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `PERMIT_EVIDENCE_REQUIREMENT_ALREADY_EXISTS` - an ACTIVE requirement
            of this `evidenceType` already exists for the Permit.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listPermitEvidenceRequirements
      x-required-permission: permit.read
      summary: List Permit Evidence Requirements for a Permit
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D2 (BE-20J). Requires
        `permit.read` and an ACTIVE caller Building assignment to the Permit's
        Building (else 403 `BUILDING_ACCESS_DENIED`). Returns the ACTIVE
        evidence requirements bound to the Permit. An unknown `permitId` yields
        400 `PERMIT_EVIDENCE_PERMIT_INVALID`. No pagination - `data` is a plain
        array.
      tags: [Permit Evidence]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: path
          required: true
          description: Authoritative `permits.id`.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Permit Evidence Requirements for the Permit.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PermitEvidenceRequirement" }
        "400":
          description: >
            `VALIDATION_ERROR` - path validation - or
            `PERMIT_EVIDENCE_PERMIT_INVALID` - Permit not found.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Permit's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permits/{permitId}/evidence:
    post:
      operationId: submitPermitEvidence
      x-required-permission: permit.manage
      summary: Submit Permit Evidence (file reference metadata)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D2 (BE-20J). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the
        Permit's Building (else 403 `BUILDING_ACCESS_DENIED`). Submits one
        evidence metadata row. The request `buildingId` and
        `contractorContextType`/`contractorContextId` must match the Permit's
        resolved context (400 `PERMIT_EVIDENCE_CONTEXT_MISMATCH`). The
        `evidenceRequirementId` must belong to the Permit (400
        `PERMIT_EVIDENCE_REQUIREMENT_INVALID` otherwise) and `evidenceType`
        must match the Requirement (400 `PERMIT_EVIDENCE_TYPE_MISMATCH`).
        `fileReference` is a storage reference string (512 chars, required),
        `originalFileName` (255), `mimeType` (255) and `fileSize`
        (0..52428800 integer) describe the referenced file; no multipart/binary
        upload is performed by this route. Optional `capturedAt` is an
        ISO-8601 date-time with timezone. Count is checked against the
        Requirement's `maximumCount` (400 `PERMIT_EVIDENCE_COUNT_VIOLATION`
        when exceeded). An unknown `permitId` yields 400
        `PERMIT_EVIDENCE_PERMIT_INVALID`. Created `ACTIVE`.
      tags: [Permit Evidence]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: path
          required: true
          description: Authoritative `permits.id`.
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitPermitEvidenceRequest" }
      responses:
        "201":
          description: Permit Evidence submitted (`status` = `ACTIVE`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitEvidence" }
        "400":
          description: >
            `VALIDATION_ERROR` (body/path), or
            `PERMIT_EVIDENCE_PERMIT_INVALID`, `PERMIT_EVIDENCE_CONTEXT_MISMATCH`,
            `PERMIT_EVIDENCE_REQUIREMENT_INVALID`,
            `PERMIT_EVIDENCE_TYPE_MISMATCH`, or
            `PERMIT_EVIDENCE_COUNT_VIOLATION`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Permit's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listPermitEvidence
      x-required-permission: permit.read
      summary: List Permit Evidence for a Permit
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D2 (BE-20J). Requires
        `permit.read` and an ACTIVE caller Building assignment to the Permit's
        Building (else 403 `BUILDING_ACCESS_DENIED`). Returns the evidence
        submissions bound to the Permit (both `ACTIVE` and `REMOVED` are
        returned unless filtered by the global `GET /permit-evidence`
        endpoint). An unknown `permitId` yields 400
        `PERMIT_EVIDENCE_PERMIT_INVALID`. No pagination - `data` is a plain
        array.
      tags: [Permit Evidence]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: path
          required: true
          description: Authoritative `permits.id`.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Permit Evidence bound to the Permit.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PermitEvidence" }
        "400":
          description: >
            `VALIDATION_ERROR` - path validation - or
            `PERMIT_EVIDENCE_PERMIT_INVALID` - Permit not found.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Permit's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permits/{permitId}/evidence-readiness:
    get:
      operationId: getPermitEvidenceReadiness
      x-required-permission: permit.read
      summary: Get Permit Evidence readiness
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D2 (BE-20J). Requires
        `permit.read` and an ACTIVE caller Building assignment to the Permit's
        Building (else 403 `BUILDING_ACCESS_DENIED`). Returns the derived
        readiness view
        `{ permitId, permitReference, buildingId, contractorContextType,
        contractorVendorId, configured, ready, missingEvidenceTypes,
        requirements: [{ evidenceRequirementId, evidenceType, required,
        minimumCount, maximumCount, activeCount, satisfied }] }`.
        `configured` is false when no requirements exist; `ready` is true only
        when every `required` requirement has `activeCount` >= `minimumCount`.
        An unknown `permitId` yields 400 `PERMIT_EVIDENCE_PERMIT_INVALID`.
      tags: [Permit Evidence]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: path
          required: true
          description: Authoritative `permits.id`.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Permit Evidence readiness view.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitEvidenceReadiness" }
        "400":
          description: >
            `VALIDATION_ERROR` - path validation - or
            `PERMIT_EVIDENCE_PERMIT_INVALID` - Permit not found.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Permit's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-evidence:
    get:
      operationId: listPermitEvidenceByFilters
      x-required-permission: permit.read
      summary: List Permit Evidence (Building-scoped, filtered)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D2 (BE-20J). Requires
        `permit.read`. The result is constrained to the Buildings the caller
        can access (empty when the caller has no accessible Building). An
        explicit `buildingId` filter additionally asserts Building access on
        that Building. Optional filters `permitId`, `contractorVendorId`
        (alias `contractorId`), `evidenceType` (`PHOTO`|`DOCUMENT`|`SIGNATURE`)
        and `status` (`ACTIVE`|`REMOVED`) narrow the result; an invalid value
        is rejected with 400 `VALIDATION_ERROR`. `data` is a plain array - not
        paginated.
      tags: [Permit Evidence]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: query
          required: false
          description: Filter by Permit.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          description: Narrow the listing to one Building. Building access is asserted when supplied.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: contractorVendorId
          in: query
          required: false
          description: Filter by resolved Vendor id (alias `contractorId` accepted).
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: evidenceType
          in: query
          required: false
          description: Filter by evidence type.
          schema: { $ref: "#/components/schemas/PermitEvidenceType" }
        - name: status
          in: query
          required: false
          description: Filter by evidence status.
          schema: { $ref: "#/components/schemas/PermitEvidenceStatus" }
      responses:
        "200":
          description: Permit Evidence within the caller's accessible Buildings.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PermitEvidence" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the filtered Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-evidence/{id}:
    get:
      operationId: getPermitEvidence
      x-required-permission: permit.read
      summary: Get Permit Evidence
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D2 (BE-20J). Requires
        `permit.read` and an ACTIVE caller Building assignment to the
        Evidence's Building (else 403 `BUILDING_ACCESS_DENIED`). An unknown
        `id` yields 404 `PERMIT_EVIDENCE_NOT_FOUND`.
      tags: [Permit Evidence]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitEvidenceIdPath"
      responses:
        "200":
          description: Permit Evidence.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitEvidence" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Evidence's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Evidence not found (`PERMIT_EVIDENCE_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    patch:
      operationId: removePermitEvidence
      x-required-permission: permit.manage
      summary: Remove Permit Evidence (ACTIVE -> REMOVED)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D2 (BE-20J). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the
        Evidence's Building (else 403 `BUILDING_ACCESS_DENIED`). Flips an
        `ACTIVE` evidence to `REMOVED`; an already `REMOVED` entry yields 409
        `PERMIT_EVIDENCE_ALREADY_REMOVED`. No request body (the patch is a
        state transition only). An unknown `id` yields 404
        `PERMIT_EVIDENCE_NOT_FOUND`.
      tags: [Permit Evidence]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitEvidenceIdPath"
      responses:
        "200":
          description: Removed Permit Evidence (`status` = `REMOVED`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitEvidence" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Evidence's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Evidence not found (`PERMIT_EVIDENCE_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `PERMIT_EVIDENCE_ALREADY_REMOVED` - the evidence is already
            `REMOVED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03E1 - J11 Permit Safety Requirements (BE-20E).
  #
  # Contract exposure of the ALREADY REGISTERED BE-20E runtime routes only.
  # No runtime, lifecycle or generic HSE behaviour is added or
  # changed here.
  # ---------------------------------------------------------------------------

  /permit-applications/{applicationId}/safety-requirements:
    post:
      operationId: createApplicationSafetyRequirement
      x-required-permission: permit.manage
      summary: Create Permit Safety Requirement for Application
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03E1 (BE-20E). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the
        Application's Building (else 403 `BUILDING_ACCESS_DENIED`). Creates
        one PTW prerequisite bound to the Application. Required body:
        `buildingId` (must match the Application/Permit Building, 400
        `PERMIT_SAFETY_CONTEXT_MISMATCH` otherwise), `workType` (controlled
        work-type code, uppercased, 400 `VALIDATION_ERROR` otherwise),
        `requirementType` (data-driven code 2-64, uppercased, must match
        `^[A-Z][A-Z0-9_-]{1,63}$`, 400 otherwise), `requirementDescription`
        (1..2000, trimmed). Optional: `required` (boolean, default `true`),
        `readinessStatus` (`PENDING`|`READY`|`NOT_READY`|`NOT_REQUIRED`,
        default `PENDING`), `notes` (2000, empty->null), `reference` (500),
        `checklistTemplateId` / `checklistExecutionId` / `evidenceRequirementId`
        (nullable Uuid, revalidated as shared controls; 400
        `PERMIT_SAFETY_SHARED_CONTROL_INVALID` when invalid for this Permit).
        One requirement per (`permitApplicationId`, `requirementType`) (409
        `PERMIT_SAFETY_REQUIREMENT_ALREADY_EXISTS`). An unknown
        `applicationId` yields 400 `PERMIT_SAFETY_PERMIT_INVALID`.
        `CANCELLED` Applications reject with 400
        `PERMIT_SAFETY_UPDATE_NOT_ALLOWED`. Created `PENDING` by default.
      tags: [Permit Safety Requirements]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: applicationId
          in: path
          required: true
          description: Authoritative `permit_applications.id`.
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePermitSafetyRequirementRequest" }
      responses:
        "201":
          description: Permit Safety Requirement created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitSafetyRequirement" }
        "400":
          description: >
            `VALIDATION_ERROR` (body/path), or
            `PERMIT_SAFETY_PERMIT_INVALID` - Application/Permit invalid, or
            `PERMIT_SAFETY_CONTEXT_MISMATCH` - Building/workType mismatch, or
            `PERMIT_SAFETY_SHARED_CONTROL_INVALID` - shared control invalid,
            or `PERMIT_SAFETY_UPDATE_NOT_ALLOWED` - CANCELLED Application.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Application's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `PERMIT_SAFETY_REQUIREMENT_ALREADY_EXISTS` - this
            `requirementType` already exists for the Application.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listApplicationSafetyRequirements
      x-required-permission: permit.read
      summary: List Permit Safety Requirements for Application
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03E1 (BE-20E). Requires
        `permit.read` and an ACTIVE caller Building assignment to the
        Application's Building (else 403 `BUILDING_ACCESS_DENIED`). Returns
        the safety requirements bound to the Application/Application's Permit.
        An unknown `applicationId` yields 400
        `PERMIT_SAFETY_PERMIT_INVALID`. No pagination - `data` is a plain
        array.
      tags: [Permit Safety Requirements]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: applicationId
          in: path
          required: true
          description: Authoritative `permit_applications.id`.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Permit Safety Requirements for the Application.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PermitSafetyRequirement" }
        "400":
          description: >
            `VALIDATION_ERROR` - path validation - or
            `PERMIT_SAFETY_PERMIT_INVALID` - Application/Permit not found.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Application's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permits/{permitId}/safety-requirements:
    post:
      operationId: createPermitSafetyRequirement
      x-required-permission: permit.manage
      summary: Create Permit Safety Requirement for Permit
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03E1 (BE-20E). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the Permit's
        Building (else 403 `BUILDING_ACCESS_DENIED`). Convenience alias for
        `POST /permit-applications/{applicationId}/safety-requirements` that
        resolves the Application from the Permit. The body contract is identical
        to `createApplicationSafetyRequirement` (requires `buildingId`,
        `workType`, `requirementType`, `requirementDescription`; same optionals
        and error codes). An unknown `permitId` (or Permit without Application)
        yields 400 `PERMIT_SAFETY_PERMIT_INVALID`. `CANCELLED` Applications
        reject with 400 `PERMIT_SAFETY_UPDATE_NOT_ALLOWED`.
      tags: [Permit Safety Requirements]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: path
          required: true
          description: Authoritative `permits.id`.
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreatePermitSafetyRequirementRequest" }
      responses:
        "201":
          description: Permit Safety Requirement created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitSafetyRequirement" }
        "400":
          description: >
            `VALIDATION_ERROR`, or `PERMIT_SAFETY_PERMIT_INVALID`,
            `PERMIT_SAFETY_CONTEXT_MISMATCH`,
            `PERMIT_SAFETY_SHARED_CONTROL_INVALID`,
            `PERMIT_SAFETY_UPDATE_NOT_ALLOWED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Permit's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `PERMIT_SAFETY_REQUIREMENT_ALREADY_EXISTS` - this
            `requirementType` already exists for the Application.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listPermitSafetyRequirements
      x-required-permission: permit.read
      summary: List Permit Safety Requirements for Permit
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03E1 (BE-20E). Requires
        `permit.read` and an ACTIVE caller Building assignment to the Permit's
        Building (else 403 `BUILDING_ACCESS_DENIED`). Returns the safety
        requirements for the Permit's Application. An unknown `permitId` yields
        400 `PERMIT_SAFETY_PERMIT_INVALID`. No pagination.
      tags: [Permit Safety Requirements]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: path
          required: true
          description: Authoritative `permits.id`.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Permit Safety Requirements for the Permit.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PermitSafetyRequirement" }
        "400":
          description: >
            `VALIDATION_ERROR` - path validation - or
            `PERMIT_SAFETY_PERMIT_INVALID` - Permit/Application not found.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Permit's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permits/{permitId}/safety-readiness:
    get:
      operationId: resolvePermitSafetyReadiness
      x-required-permission: permit.read
      summary: Get Permit Safety readiness
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03E1 (BE-20E). Requires
        `permit.read` and an ACTIVE caller Building assignment to the Permit's
        Building (else 403 `BUILDING_ACCESS_DENIED`). Returns the derived
        readiness view
        `{ permitId, permitApplicationId, permitReference, buildingId,
        workType, ready, readinessStatus, requiredCount, readyCount,
        missingRequirementTypes, requirements }`. `readinessStatus` is
        `NOT_CONFIGURED` when no requirements exist, otherwise `PENDING`|
        `READY`|`NOT_READY` aggregated from requirements; `ready` is true only
        when every `required` requirement is `READY`. An unknown `permitId`
        yields 400 `PERMIT_SAFETY_PERMIT_INVALID`.
      tags: [Permit Safety Requirements]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: path
          required: true
          description: Authoritative `permits.id`.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Permit Safety readiness view.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitSafetyReadiness" }
        "400":
          description: >
            `VALIDATION_ERROR` - path validation - or
            `PERMIT_SAFETY_PERMIT_INVALID` - Permit/Application not found.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Permit's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /safety-requirements:
    get:
      operationId: listSafetyRequirements
      x-required-permission: permit.read
      summary: List Permit Safety Requirements (Building-scoped, filtered)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03E1 (BE-20E). Requires
        `permit.read`. The result is constrained to the Buildings the caller
        can access (empty when the caller has no accessible Building). An
        explicit `buildingId` filter additionally asserts Building access on
        that Building. Optional filters `permitId`, `permitApplicationId`,
        `workType` (controlled work-type code) and `readinessStatus`/`status`
        (`PENDING`|`READY`|`NOT_READY`|`NOT_REQUIRED`) narrow the result; an
        invalid value is rejected with 400 `VALIDATION_ERROR`. `data` is a
        plain array - not paginated.
      tags: [Permit Safety Requirements]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: query
          required: false
          description: Filter by Permit.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: permitApplicationId
          in: query
          required: false
          description: Filter by Permit Application.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          description: Narrow the listing to one Building. Building access is asserted when supplied.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: workType
          in: query
          required: false
          description: Filter by controlled work-type code.
          schema: { type: string, minLength: 2, maxLength: 64 }
        - name: readinessStatus
          in: query
          required: false
          description: Filter by readiness status (alias `status` accepted).
          schema: { $ref: "#/components/schemas/PermitSafetyReadinessStatus" }
        - name: status
          in: query
          required: false
          description: Alias for `readinessStatus`.
          schema: { $ref: "#/components/schemas/PermitSafetyReadinessStatus" }
      responses:
        "200":
          description: Permit Safety Requirements within the caller's accessible Buildings.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PermitSafetyRequirement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the filtered Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /safety-requirements/{id}/readiness:
    patch:
      operationId: updatePermitSafetyReadiness
      x-required-permission: permit.manage
      summary: Update Permit Safety Requirement readiness
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03E1 (BE-20E). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the
        Requirement's Building (else 403 `BUILDING_ACCESS_DENIED`). Updates the
        readiness state of one requirement. Required `readinessStatus`
        (`PENDING`|`READY`|`NOT_READY`|`NOT_REQUIRED`); optional `notes`
        (2000, empty->null), `reference` (500), `checklistTemplateId`,
        `checklistExecutionId`, `evidenceRequirementId` (nullable Uuid, null
        clears). Trying to set `READY` when checklist/evidence prerequisites are
        incomplete yields 400 `PERMIT_SAFETY_PREREQUISITE_NOT_MET`. Updating a
        requirement on a `CANCELLED` Application yields 400
        `PERMIT_SAFETY_UPDATE_NOT_ALLOWED`; shared-control invalid yields 400
        `PERMIT_SAFETY_SHARED_CONTROL_INVALID`. Immutable fields (`permitId`,
        `permitApplicationId`, `buildingId`, `workType`, `requirementType`,
        `requirementDescription`, `required`) must not be supplied (400
        `VALIDATION_ERROR`). An unknown `id` yields 404
        `PERMIT_SAFETY_REQUIREMENT_NOT_FOUND`.
      tags: [Permit Safety Requirements]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitSafetyRequirementIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdatePermitSafetyReadinessRequest" }
      responses:
        "200":
          description: Updated Permit Safety Requirement.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitSafetyRequirement" }
        "400":
          description: >
            `VALIDATION_ERROR` (body/path), or
            `PERMIT_SAFETY_UPDATE_NOT_ALLOWED` - CANCELLED Application, or
            `PERMIT_SAFETY_SHARED_CONTROL_INVALID`, or
            `PERMIT_SAFETY_PREREQUISITE_NOT_MET` - prerequisites not complete
            for `READY`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Requirement's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Safety Requirement not found
            (`PERMIT_SAFETY_REQUIREMENT_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /safety-requirements/{id}:
    get:
      operationId: getPermitSafetyRequirement
      x-required-permission: permit.read
      summary: Get Permit Safety Requirement
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03E1 (BE-20E). Requires
        `permit.read` and an ACTIVE caller Building assignment to the
        Requirement's Building (else 403 `BUILDING_ACCESS_DENIED`). An unknown
        `id` yields 404 `PERMIT_SAFETY_REQUIREMENT_NOT_FOUND`.
      tags: [Permit Safety Requirements]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitSafetyRequirementIdPath"
      responses:
        "200":
          description: Permit Safety Requirement.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitSafetyRequirement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Requirement's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Safety Requirement not found
            (`PERMIT_SAFETY_REQUIREMENT_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03E2 - J11 Permit Validities (BE-20G).
  #
  # Contract exposure of the ALREADY REGISTERED BE-20G runtime routes only.
  # No runtime, lifecycle or generic validity-renewal behaviour is added or
  # changed here.
  # ---------------------------------------------------------------------------

  /permits/{permitId}/validity:
    post:
      operationId: setPermitValidity
      x-required-permission: permit.manage
      summary: Set Permit Validity window
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03E2 (BE-20G). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the
        Permit's Building (else 403 `BUILDING_ACCESS_DENIED`). Sets the
        time-bounded validity window for the Permit. Required body:
        `permitApplicationId` (must be the Permit's `SUBMITTED` Application),
        `buildingId` (must match the Permit Building, 400
        `PERMIT_VALIDITY_BUILDING_MISMATCH` otherwise), `validFrom` and
        `validUntil` (ISO-8601 with timezone, calendar-valid,
        `validUntil` > `validFrom`; 400 `VALIDATION_ERROR` or 400
        `PERMIT_VALIDITY_INVALID_RANGE` otherwise). Optional `notes` (2000,
        empty->null). Backend-controlled fields `status`, `activatedAt`,
        `expiredAt`, `revokedAt`, `revokedByUserId`, `createdByUserId`,
        `createdAt`, `updatedAt` must not be supplied (400). The Application
        must be `SUBMITTED` with fully approved context (400
        `PERMIT_VALIDITY_CONTEXT_INVALID` otherwise) and the Permit must not
        already have a `PENDING` or `VALID` window (409
        `PERMIT_VALIDITY_ALREADY_OPEN`). Created `PENDING` or `VALID`
        depending on window vs now. An unknown `permitId` yields 400
        `PERMIT_VALIDITY_CONTEXT_INVALID`.
      tags: [Permit Validities]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: path
          required: true
          description: Authoritative `permits.id`.
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SetPermitValidityRequest" }
      responses:
        "201":
          description: Permit Validity created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitValidity" }
        "400":
          description: >
            `VALIDATION_ERROR` (body/path), or
            `PERMIT_VALIDITY_CONTEXT_INVALID` - Permit/Application invalid,
            `PERMIT_VALIDITY_BUILDING_MISMATCH` - Building mismatch, or
            `PERMIT_VALIDITY_INVALID_RANGE` - window not ordered.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Permit's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `PERMIT_VALIDITY_ALREADY_OPEN` - Permit already has a `PENDING` or
            `VALID` validity.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: resolveCurrentPermitValidity
      x-required-permission: permit.read
      summary: Resolve current Permit Validity state
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03E2 (BE-20G). Requires
        `permit.read` and an ACTIVE caller Building assignment to the Permit's
        Building (else 403 `BUILDING_ACCESS_DENIED`). Returns the derived
        state `{ permitId, permitReference, buildingId, currentValidity,
        history }` where `currentValidity` is the current
        `PENDING`/`VALID`/`EXPIRED`/`REVOKED` window or null when none, and
        `history` is the full validity history for the Permit (plain array).
        An unknown `permitId` yields 404? The runtime surfaces as 400
        `PERMIT_VALIDITY_CONTEXT_INVALID` when the Permit is unknown in this
        context, else 200 with null `currentValidity`. No pagination.
      tags: [Permit Validities]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: path
          required: true
          description: Authoritative `permits.id`.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Current Permit Validity state.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitValidityState" }
        "400":
          description: >
            `VALIDATION_ERROR` - path validation - or
            `PERMIT_VALIDITY_CONTEXT_INVALID` - Permit not found in this
            context.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Permit's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permits/{permitId}/validity/current:
    get:
      operationId: resolveCurrentPermitValidityCurrent
      x-required-permission: permit.read
      summary: Resolve current Permit Validity state (alias)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03E2 (BE-20G). Alias for
        `GET /permits/{permitId}/validity`. Requires `permit.read` and an
        ACTIVE caller Building assignment to the Permit's Building (else 403
        `BUILDING_ACCESS_DENIED`). Returns the same
        `{ permitId, permitReference, buildingId, currentValidity, history }`
        state. See `GET /permits/{permitId}/validity` for error semantics.
      tags: [Permit Validities]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: path
          required: true
          description: Authoritative `permits.id`.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Current Permit Validity state (alias).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitValidityState" }
        "400":
          description: >
            `VALIDATION_ERROR` - path validation - or
            `PERMIT_VALIDITY_CONTEXT_INVALID` - Permit not found.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Permit's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-validities:
    get:
      operationId: listPermitValidities
      x-required-permission: permit.read
      summary: List Permit Validities (Building-scoped, filtered)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03E2 (BE-20G). Requires
        `permit.read`. The result is constrained to the Buildings the caller
        can access (empty when the caller has no accessible Building). An
        explicit `buildingId` filter additionally asserts Building access on
        that Building. Optional filters `permitId`, `status`/`validityStatus`
        (`PENDING`|`VALID`|`EXPIRED`|`REVOKED`), `validFrom`, `validUntil` and
        `validAt` (all ISO-8601 with timezone, calendar-valid) narrow the
        result; `validAt` is an instant contained within the validity window.
        An invalid value is rejected with 400 `VALIDATION_ERROR`. `data` is a
        plain array - not paginated.
      tags: [Permit Validities]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: query
          required: false
          description: Filter by Permit.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          description: Narrow the listing to one Building. Building access is asserted when supplied.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          description: Filter by validity status (alias `validityStatus` accepted).
          schema: { $ref: "#/components/schemas/PermitValidityStatus" }
        - name: validityStatus
          in: query
          required: false
          description: Alias for `status`.
          schema: { $ref: "#/components/schemas/PermitValidityStatus" }
        - name: validFrom
          in: query
          required: false
          description: Filter validities with `validFrom` >= this instant (ISO-8601 with timezone).
          schema: { type: string, format: date-time }
        - name: validUntil
          in: query
          required: false
          description: Filter validities with `validUntil` <= this instant.
          schema: { type: string, format: date-time }
        - name: validAt
          in: query
          required: false
          description: Filter validities where `validFrom` <= `validAt` <= `validUntil`.
          schema: { type: string, format: date-time }
      responses:
        "200":
          description: Permit Validities within the caller's accessible Buildings.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PermitValidity" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the filtered Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-validities/{id}/revoke:
    post:
      operationId: revokePermitValidity
      x-required-permission: permit.manage
      summary: Revoke Permit Validity (PENDING|VALID -> REVOKED)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03E2 (BE-20G). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the
        Validity's Building (else 403 `BUILDING_ACCESS_DENIED`). Revokes one
        validity window. Only `PENDING` or `VALID` can be revoked (400
        `PERMIT_VALIDITY_REVOKE_NOT_ALLOWED` otherwise). Optional
        `revocationNotes` (alias `notes`, 2000 chars, empty->null) is stored
        as `revocationNotes`. No request body is also accepted (empty revoke).
        An unknown `id` yields 404 `PERMIT_VALIDITY_NOT_FOUND`.
      tags: [Permit Validities]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitValidityIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RevokePermitValidityRequest" }
      responses:
        "200":
          description: Revoked Permit Validity (`status` = `REVOKED`).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitValidity" }
        "400":
          description: >
            `VALIDATION_ERROR` (body/path), or
            `PERMIT_VALIDITY_REVOKE_NOT_ALLOWED` - not `PENDING`/`VALID`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Validity's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Validity not found (`PERMIT_VALIDITY_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-validities/{id}:
    get:
      operationId: getPermitValidity
      x-required-permission: permit.read
      summary: Get Permit Validity
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03E2 (BE-20G). Requires
        `permit.read` and an ACTIVE caller Building assignment to the
        Validity's Building (else 403 `BUILDING_ACCESS_DENIED`). An unknown
        `id` yields 404 `PERMIT_VALIDITY_NOT_FOUND`.
      tags: [Permit Validities]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitValidityIdPath"
      responses:
        "200":
          description: Permit Validity.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitValidity" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Validity's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Validity not found (`PERMIT_VALIDITY_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03F1 - J11 Permit Work Contexts (BE-20D).
  #
  # Contract exposure of the ALREADY REGISTERED BE-20D runtime routes only.
  # No runtime, lifecycle or generic work-execution behaviour is added or
  # changed here.
  # ---------------------------------------------------------------------------

  /permit-applications/{id}/work-location:
    put:
      operationId: assignPermitWorkLocation
      x-required-permission: permit.manage
      summary: Assign Permit Work Location (DRAFT only)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03F1 (BE-20D). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the
        Application's Permit Building (else 403 `BUILDING_ACCESS_DENIED`).
        Assigns the work location over the authoritative BE-04 hierarchy.
        Required body: `locationType` (BUILDING|FLOOR|AREA|ROOM|
        FUNCTIONAL_LOCATION), `locationId` (BE-04 id), `plannedStartAt` and
        `plannedEndAt` (ISO-8601 with timezone, calendar-valid, future
        `plannedStartAt` > now and `plannedEndAt` > `plannedStartAt`, else
        400 `PERMIT_WORK_PLANNED_PERIOD_INVALID`). Optional
        `accessRestrictionNotes` (2000, empty->null) and `workDescription`
        (4000, empty->null). The resolved BE-04 row must be ACTIVE and belong
        to the Application's Permit Building (400
        `PERMIT_WORK_LOCATION_INVALID` / `PERMIT_WORK_LOCATION_BUILDING_MISMATCH`
        otherwise). The Application must be `DRAFT` (400
        `PERMIT_WORK_CONTEXT_UPDATE_NOT_ALLOWED` otherwise). An unknown `id`
        yields 404 `PERMIT_APPLICATION_NOT_FOUND`.
      tags: [Permit Work Contexts]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitApplicationIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AssignPermitWorkLocationRequest" }
      responses:
        "200":
          description: Assigned Permit Work Context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitWorkContext" }
        "400":
          description: >
            `VALIDATION_ERROR` (body/path), or
            `PERMIT_WORK_LOCATION_INVALID` / `PERMIT_WORK_LOCATION_BUILDING_MISMATCH` /
            `PERMIT_WORK_PLANNED_PERIOD_INVALID` /
            `PERMIT_WORK_CONTEXT_UPDATE_NOT_ALLOWED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Application's Permit Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Application not found (`PERMIT_APPLICATION_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-applications/{id}/work-type:
    put:
      operationId: assignPermitWorkType
      x-required-permission: permit.manage
      summary: Assign Permit Work Type (DRAFT only)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03F1 (BE-20D). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the
        Application's Permit Building (else 403 `BUILDING_ACCESS_DENIED`).
        Assigns the controlled `workType` code. Required body: `workType`
        (normalized controlled data code, e.g. uppercased, must be a valid
        controlled code, else 400 `PERMIT_WORK_TYPE_INVALID` / `VALIDATION_ERROR`);
        optional `workDescription` (4000, empty->null). The Application must be
        `DRAFT` (400 `PERMIT_WORK_CONTEXT_UPDATE_NOT_ALLOWED` otherwise). An
        unknown `id` yields 404 `PERMIT_APPLICATION_NOT_FOUND`.
      tags: [Permit Work Contexts]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitApplicationIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AssignPermitWorkTypeRequest" }
      responses:
        "200":
          description: Assigned Permit Work Context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitWorkContext" }
        "400":
          description: >
            `VALIDATION_ERROR` / `PERMIT_WORK_TYPE_INVALID` /
            `PERMIT_WORK_CONTEXT_UPDATE_NOT_ALLOWED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Application's Permit Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Application not found (`PERMIT_APPLICATION_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-applications/{id}/work-context:
    get:
      operationId: getApplicationWorkContext
      x-required-permission: permit.read
      summary: Get Permit Work Context by Application
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03F1 (BE-20D). Requires
        `permit.read` and an ACTIVE caller Building assignment to the
        Application's Permit Building (else 403 `BUILDING_ACCESS_DENIED`).
        Returns the combined work-context view for the Application (location +
        work type + planned period). A missing context returns 404
        `PERMIT_WORK_CONTEXT_NOT_FOUND`. An unknown `id` yields 404
        `PERMIT_APPLICATION_NOT_FOUND`.
      tags: [Permit Work Contexts]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitApplicationIdPath"
      responses:
        "200":
          description: Permit Work Context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitWorkContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Application's Permit Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Work Context not found (`PERMIT_WORK_CONTEXT_NOT_FOUND`) or
            Permit Application not found (`PERMIT_APPLICATION_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    patch:
      operationId: updatePermitWorkContext
      x-required-permission: permit.manage
      summary: Update Permit Work Context (DRAFT only, partial)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03F1 (BE-20D). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the
        Application's Permit Building (else 403 `BUILDING_ACCESS_DENIED`).
        Partial update of the combined work context. Optional fields:
        `locationType`+`locationId` (must be supplied together, else 400
        `VALIDATION_ERROR`; same `locationType` enum and ACTIVE + Building
        checks as assign), `workType` (controlled code),
        `workDescription` (4000, empty->null, `null` clears), `plannedStartAt`/
        `plannedEndAt` (ISO-8601 with timezone, calendar-valid, when one is
        supplied both must produce a valid future period with
        end > start else 400 `PERMIT_WORK_PLANNED_PERIOD_INVALID`),
        `accessRestrictionNotes` (2000, empty->null, `null` clears). At least
        one field is required. Immutable fields `permitId`,
        `permitApplicationId`, `permitReference`, `clientId`, `buildingId`,
        `applicationStatus`, `createdByUserId`, `updatedByUserId`, `createdAt`,
        `updatedAt` must not be supplied (400). The Application must be
        `DRAFT` (400 `PERMIT_WORK_CONTEXT_UPDATE_NOT_ALLOWED` otherwise). An
        unknown `id` yields 404 `PERMIT_APPLICATION_NOT_FOUND`.
      tags: [Permit Work Contexts]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitApplicationIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdatePermitWorkContextRequest" }
      responses:
        "200":
          description: Updated Permit Work Context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitWorkContext" }
        "400":
          description: >
            `VALIDATION_ERROR` / `PERMIT_WORK_LOCATION_INVALID` /
            `PERMIT_WORK_LOCATION_BUILDING_MISMATCH` / `PERMIT_WORK_TYPE_INVALID` /
            `PERMIT_WORK_PLANNED_PERIOD_INVALID` /
            `PERMIT_WORK_CONTEXT_UPDATE_NOT_ALLOWED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Application's Permit Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Application not found (`PERMIT_APPLICATION_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permits/{permitId}/work-context:
    get:
      operationId: getPermitWorkContext
      x-required-permission: permit.read
      summary: Get Permit Work Context by Permit
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03F1 (BE-20D). Requires
        `permit.read` and an ACTIVE caller Building assignment to the Permit's
        Building (else 403 `BUILDING_ACCESS_DENIED`). Returns the work-context
        resolved via the Permit's Application (or the stored context). A
        missing context returns 404 `PERMIT_WORK_CONTEXT_NOT_FOUND`. An
        unknown `permitId` yields 404 `PERMIT_NOT_FOUND` (surfaced as
        `PERMIT_WORK_CONTEXT_NOT_FOUND` when no context exists).
      tags: [Permit Work Contexts]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: path
          required: true
          description: Authoritative `permits.id`.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Permit Work Context.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitWorkContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Permit's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Work Context not found (`PERMIT_WORK_CONTEXT_NOT_FOUND`) or
            Permit not found.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-work-contexts:
    get:
      operationId: listPermitWorkContexts
      x-required-permission: permit.read
      summary: List Permit Work Contexts (Building-scoped, filtered)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03F1 (BE-20D). Requires
        `permit.read`. The result is constrained to the Buildings the caller
        can access (empty when the caller has no accessible Building). An
        explicit `buildingId` filter additionally asserts Building access on
        that Building. Optional filters `locationType`
        (BUILDING|FLOOR|AREA|ROOM|FUNCTIONAL_LOCATION), `locationId` and
        `workType` (controlled code) narrow the result. An invalid value is
        rejected with 400 `VALIDATION_ERROR`. `data` is a plain array - not
        paginated. This is not a work-execution feed.
      tags: [Permit Work Contexts]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: false
          description: Narrow the listing to one Building. Building access is asserted when supplied.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: locationType
          in: query
          required: false
          description: Filter by location type.
          schema: { $ref: "#/components/schemas/PermitWorkLocationType" }
        - name: locationId
          in: query
          required: false
          description: Filter by concrete BE-04 location id.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: workType
          in: query
          required: false
          description: Filter by controlled work-type code.
          schema: { type: string }
      responses:
        "200":
          description: Permit Work Contexts within the caller's accessible Buildings.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PermitWorkContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the filtered Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03F2 - J11 Permit Work Lifecycle (BE-20K).
  #
  # Contract exposure of the ALREADY REGISTERED BE-20K runtime routes only.
  # No runtime, lifecycle or generic work-execution behaviour is added or
  # changed here. BE-09-style authority: READY -> IN_PROGRESS -> CLOSED,
  # CANCELLED derived from Permit.
  # ---------------------------------------------------------------------------

  /permits/{permitId}/work-start-readiness:
    get:
      operationId: getPermitWorkStartReadiness
      x-required-permission: permit.read
      summary: Get Permit Work Start Readiness
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03F2 (BE-20K). Requires
        `permit.read` and an ACTIVE caller Building assignment to the Permit's
        Building (else 403 `BUILDING_ACCESS_DENIED`). Returns the derived
        readiness view `PermitWorkStartReadiness` for the Permit: `permitId`,
        `permitApplicationId`, `permitReference`, `buildingId`,
        `contractorContextType`/`contractorVendorId`, `workStatus`
        (READY|IN_PROGRESS|CLOSED|CANCELLED - CANCELLED derived when the BE-20A
        Permit itself is CANCELLED), `ready` boolean (true only when every
        prerequisite is ready), `approvalReady`, `validityStatus`
        (PENDING|VALID|EXPIRED|REVOKED or null), `safetyReady`/
        `safetyReadinessStatus`, `workerListReady`/`activeWorkerCount`/
        `configuredWorkerCount`, `equipmentRequired`/`equipmentReady`/
        `activeEquipmentCount`/`configuredEquipmentCount`, `evidenceConfigured`/
        `evidenceReady`, `blockers` string array and `availableActions`
        ([START] when `READY`+`ready`+authorized else []). When `workStatus` is
        not `READY` `ready` is false and `blockers` contains
        `WORK_ALREADY_TRANSITIONED`. An unknown `permitId` yields 400
        `PERMIT_WORK_CONTEXT_INVALID`.
      tags: [Permit Work Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: path
          required: true
          description: Authoritative `permits.id`.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Permit Work Start Readiness.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitWorkStartReadiness" }
        "400":
          description: >
            `VALIDATION_ERROR` - path validation - or
            `PERMIT_WORK_CONTEXT_INVALID` - Permit not found or has no
            Application context.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Permit's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permits/{permitId}/work-start:
    post:
      operationId: startPermitWork
      x-required-permission: permit.manage
      summary: Start Permit Work (READY -> IN_PROGRESS)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03F2 (BE-20K). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the Permit's
        Building (else 403 `BUILDING_ACCESS_DENIED`). BE-09-style authority:
        allowed only when `workStatus` is `READY` and `ready===true` (no
        blockers) and the caller holds both `permit.read` and `permit.manage`
        (otherwise 403 `PERMIT_WORK_ACTION_NOT_ALLOWED`). Prerequisites are the
        readiness prerequisites (approvalReady, VALID validity, safety READY,
        worker list ready, equipment ready when required, evidence ready);
        otherwise 400 `PERMIT_WORK_NOT_READY` with `details: [{blocker}]` listing
        blockers, or 400 `PERMIT_WORK_CONTEXT_INVALID` when the Permit has no
        Application context. Already `IN_PROGRESS`/`CLOSED` yields 409
        `PERMIT_WORK_ALREADY_STARTED` / `PERMIT_WORK_ALREADY_CLOSED`. Optional
        body `notes` (2000, trimmed, empty->null) is stored as `startNotes`.
        An unknown `permitId` yields 400 `PERMIT_WORK_CONTEXT_INVALID`.
      tags: [Permit Work Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: path
          required: true
          description: Authoritative `permits.id`.
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PermitWorkNotesRequest" }
      responses:
        "201":
          description: Permit Work started (`status` IN_PROGRESS).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitWorkLifecycle" }
        "400":
          description: >
            `VALIDATION_ERROR` / `PERMIT_WORK_CONTEXT_INVALID` /
            `PERMIT_WORK_NOT_READY` (with blockers) /
            `PERMIT_WORK_CLOSE_BEFORE_START` not applicable here.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `PERMIT_WORK_ACTION_NOT_ALLOWED` - not `READY`, not `ready`, or
            caller lacks `permit.read`+`permit.manage` - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `PERMIT_WORK_ALREADY_STARTED` - already `IN_PROGRESS` - or
            `PERMIT_WORK_ALREADY_CLOSED` - already `CLOSED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permits/{permitId}/work-status:
    get:
      operationId: getPermitWorkStatus
      x-required-permission: permit.read
      summary: Get Permit Work Status
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03F2 (BE-20K). Requires
        `permit.read` and an ACTIVE caller Building assignment to the Permit's
        Building (else 403 `BUILDING_ACCESS_DENIED`). Returns the work-status
        view `{ lifecycleId, permitId, permitApplicationId, permitReference,
        buildingId, contractorContextType, contractorVendorId, status
        (READY|IN_PROGRESS|CLOSED|CANCELLED), startedAt/startedByUserId/
        startNotes, closedAt/closedByUserId/closeNotes, availableActions
        ([START] when `READY`+ready, [CLOSE] when `IN_PROGRESS`, [] otherwise)
        }` where `status` is `CANCELLED` when the BE-20A Permit is
        `CANCELLED`, otherwise the stored lifecycle status or `READY` when no
        row exists. No separate available-actions endpoint is exposed. An
        unknown `permitId` yields 400 `PERMIT_WORK_CONTEXT_INVALID`.
      tags: [Permit Work Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: path
          required: true
          description: Authoritative `permits.id`.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Permit Work Status.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitWorkStatusView" }
        "400":
          description: >
            `VALIDATION_ERROR` - path validation - or
            `PERMIT_WORK_CONTEXT_INVALID` - Permit not found / no context.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permits/{permitId}/work-close:
    post:
      operationId: closePermitWork
      x-required-permission: permit.manage
      summary: Close Permit Work (IN_PROGRESS -> CLOSED)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03F2 (BE-20K). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the Permit's
        Building (else 403 `BUILDING_ACCESS_DENIED`). BE-09-style authority:
        allowed only when `workStatus` is `IN_PROGRESS` and the caller holds both
        `permit.read` and `permit.manage` (otherwise 403
        `PERMIT_WORK_ACTION_NOT_ALLOWED`). When `workStatus` is `READY` yields
        400 `PERMIT_WORK_CLOSE_BEFORE_START`; when already `CLOSED` yields 409
        `PERMIT_WORK_ALREADY_CLOSED`. Optional body `notes` (2000, trimmed,
        empty->null) is stored as `closeNotes`. An unknown `permitId` yields
        400 `PERMIT_WORK_CONTEXT_INVALID`.
      tags: [Permit Work Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: path
          required: true
          description: Authoritative `permits.id`.
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PermitWorkNotesRequest" }
      responses:
        "200":
          description: Permit Work closed (`status` CLOSED).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitWorkLifecycle" }
        "400":
          description: >
            `VALIDATION_ERROR` / `PERMIT_WORK_CONTEXT_INVALID` /
            `PERMIT_WORK_CLOSE_BEFORE_START` - cannot close before start.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `PERMIT_WORK_ACTION_NOT_ALLOWED` - not `IN_PROGRESS` or lacks
            `permit.read`+`permit.manage` - or `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `PERMIT_WORK_ALREADY_CLOSED` - already `CLOSED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-work:
    get:
      operationId: listPermitWork
      x-required-permission: permit.read
      summary: List Permit Work (Building-scoped, filtered)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03F2 (BE-20K). Requires
        `permit.read`. The result is constrained to the Buildings the caller
        can access (empty when the caller has no accessible Building). An
        explicit `buildingId` filter additionally asserts Building access on
        that Building. Optional filters `contractorContextType`
        (VENDOR_CONTRACTOR|TENANT_CONTRACTOR), `contractorVendorId`
        (alias `contractorId`) and `status` (IN_PROGRESS|CLOSED only) narrow
        the result. `status` `READY`/`CANCELLED` are not listable via this
        endpoint (they are visible via `GET /permits/{permitId}/work-status`).
        An invalid value is rejected with 400 `VALIDATION_ERROR`. `data` is a
        plain array - not paginated.
      tags: [Permit Work Lifecycle]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: buildingId
          in: query
          required: false
          description: Narrow the listing to one Building. Building access is asserted when supplied.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: contractorContextType
          in: query
          required: false
          description: Filter by contractor context type.
          schema: { $ref: "#/components/schemas/PermitContractorContextType" }
        - name: contractorVendorId
          in: query
          required: false
          description: Filter by contractor Vendor id (alias `contractorId` accepted).
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: contractorId
          in: query
          required: false
          description: Alias for `contractorVendorId`.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          description: Filter by work status (IN_PROGRESS or CLOSED only).
          schema: { $ref: "#/components/schemas/PermitWorkStatus" }
      responses:
        "200":
          description: Permit Work within the caller's accessible Buildings.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PermitWorkLifecycle" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the filtered Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03G1 - J11 Permit Workers (BE-20H).
  #
  # Contract exposure of the ALREADY REGISTERED BE-20H runtime routes only.
  # No runtime, lifecycle or generic HR behaviour is added or
  # changed here. Workers are bindings, not qualification gates.
  # ---------------------------------------------------------------------------

  /permits/{permitId}/workers:
    post:
      operationId: addPermitWorker
      x-required-permission: permit.manage
      summary: Add Permit Worker (ACTIVE binding)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03G1 (BE-20H). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the Permit's
        Building (else 403 `BUILDING_ACCESS_DENIED`). Binds one Workforce
        Profile (+ optional Vendor Workforce binding) to one `SUBMITTED`
        Permit Application and its open Permit validity. Required body:
        `permitApplicationId` (must be Permit Application, `SUBMITTED`), `buildingId`
        (must match Permit Building), `workforceProfileId` (alias
        `workerPersonReference` accepted) must resolve to an existing Workforce
        profile, `roleTrade` (data-driven code, normalized, must be valid).
        Optional `vendorWorkforceBindingId`, `validFrom`/`validUntil`
        (ISO-8601 with timezone, ordered, contained within Permit validity and
        Workforce effective window; defaults to Permit validity when omitted)
        and `notes` (2000, empty->null). Backend-resolved fields `workerName`,
        `employeeCode`, `vendorPersonnelCode`, `identificationReference`,
        `status`, `deactivatedAt` etc must not be supplied (400). The Worker
        must be ACTIVE/ effective and belong to the Permit Contractor (400
        `PERMIT_WORKER_INVALID`/`INACTIVE`/`CONTRACTOR_MISMATCH`); contractor
        context must be valid (400 `PERMIT_WORKER_CONTRACTOR_INVALID`); validity
        must be ordered/contained (400 `PERMIT_WORKER_INVALID_VALIDITY`);
        one `ACTIVE` per (`permitId`, `workforceProfileId`) allowed (409
        `PERMIT_WORKER_ALREADY_ACTIVE`); otherwise 400
        `PERMIT_WORKER_CONTEXT_INVALID` when Application/Building/validity does
        not match. An unknown `permitId` yields 400
        `PERMIT_WORKER_CONTEXT_INVALID`.
      tags: [Permit Workers]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: path
          required: true
          description: Authoritative `permits.id`.
          schema: { $ref: "#/components/schemas/Uuid" }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AddPermitWorkerRequest" }
      responses:
        "201":
          description: Permit Worker created (`status` ACTIVE).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitWorker" }
        "400":
          description: >
            `VALIDATION_ERROR` / `PERMIT_WORKER_CONTEXT_INVALID` /
            `PERMIT_WORKER_CONTRACTOR_INVALID` / `PERMIT_WORKER_INVALID` /
            `PERMIT_WORKER_INACTIVE` / `PERMIT_WORKER_CONTRACTOR_MISMATCH` /
            `PERMIT_WORKER_INVALID_VALIDITY`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Permit's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `PERMIT_WORKER_ALREADY_ACTIVE` - Worker already has an ACTIVE entry
            on this Permit.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listPermitWorkersForPermit
      x-required-permission: permit.read
      summary: List Permit Workers for Permit
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03G1 (BE-20H). Requires
        `permit.read` and an ACTIVE caller Building assignment to the Permit's
        Building (else 403 `BUILDING_ACCESS_DENIED`). Lists all Permit Worker
        entries (ACTIVE+INACTIVE, plain array) bound to one Permit. An unknown
        `permitId` yields 404 `PERMIT_WORKER_NOT_FOUND` or empty when context
        invalid (runtime maps to 400 `PERMIT_WORKER_CONTEXT_INVALID` for
        unknown Permit; expose as 404 when not found). No pagination.
      tags: [Permit Workers]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: path
          required: true
          description: Authoritative `permits.id`.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Permit Workers bound to the Permit.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PermitWorker" }
        "400":
          description: >
            `VALIDATION_ERROR` - path validation - or
            `PERMIT_WORKER_CONTEXT_INVALID` - Permit/Application invalid.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit not found (`PERMIT_WORKER_NOT_FOUND` / `PERMIT_WORKER_CONTEXT_INVALID`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permits/{permitId}/workers/active:
    get:
      operationId: resolveActivePermitWorkers
      x-required-permission: permit.read
      summary: Resolve active Permit Workers
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03G1 (BE-20H). Requires
        `permit.read` and an ACTIVE caller Building assignment to the Permit's
        Building (else 403 `BUILDING_ACCESS_DENIED`). Returns the derived
        active-worker view `{ permitId, permitReference, buildingId,
        contractorContextType, contractorVendorId, validityStatus
        (PENDING|VALID|EXPIRED|REVOKED or null), active (true only when
        `validityStatus` is `VALID`), workerCount, workers }` where `workers`
        are `eligibleForActiveWork` entries (filtered when Permit validity is
        not `VALID` `active` is false and `workers` empty). An unknown
        `permitId` yields 400 `PERMIT_WORKER_CONTEXT_INVALID`.
      tags: [Permit Workers]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: path
          required: true
          description: Authoritative `permits.id`.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Active Permit Workers view.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitActiveWorkerList" }
        "400":
          description: >
            `VALIDATION_ERROR` - path validation - or
            `PERMIT_WORKER_CONTEXT_INVALID` - Permit/Application invalid.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-workers:
    get:
      operationId: listPermitWorkers
      x-required-permission: permit.read
      summary: List Permit Workers (Building-scoped, filtered)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03G1 (BE-20H). Requires
        `permit.read`. The result is constrained to the Buildings the caller
        can access (empty when no accessible Building). An explicit `buildingId`
        filter additionally asserts Building access on that Building. Optional
        filters `permitId`, `permitApplicationId`, `workforceProfileId`,
        `contractorContextType` (VENDOR_CONTRACTOR|TENANT_CONTRACTOR, alias
        `sourceType`), `contractorContextId`, `contractorVendorId` (alias
        `contractorId`) and `status` (ACTIVE|INACTIVE) narrow the result. An
        invalid value is rejected with 400 `VALIDATION_ERROR`. `data` is a plain
        array - not paginated. This is not a Workforce master search.
      tags: [Permit Workers]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: permitId
          in: query
          required: false
          description: Filter by Permit.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: permitApplicationId
          in: query
          required: false
          description: Filter by Permit Application.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          description: Narrow the listing to one Building. Building access is asserted when supplied.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: contractorContextType
          in: query
          required: false
          description: Filter by contractor context type (alias `sourceType`).
          schema: { $ref: "#/components/schemas/PermitContractorContextType" }
        - name: sourceType
          in: query
          required: false
          description: Alias for `contractorContextType`.
          schema: { $ref: "#/components/schemas/PermitContractorContextType" }
        - name: contractorContextId
          in: query
          required: false
          description: Filter by contractor context id.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: contractorVendorId
          in: query
          required: false
          description: Filter by contractor Vendor id (alias `contractorId`).
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: contractorId
          in: query
          required: false
          description: Alias for `contractorVendorId`.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: workforceProfileId
          in: query
          required: false
          description: Filter by Workforce Profile.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          description: Filter by worker status (ACTIVE|INACTIVE).
          schema: { $ref: "#/components/schemas/PermitWorkerStatus" }
      responses:
        "200":
          description: Permit Workers within the caller's accessible Buildings.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/PermitWorker" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the filtered Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-workers/{id}:
    get:
      operationId: getPermitWorker
      x-required-permission: permit.read
      summary: Get Permit Worker
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03G1 (BE-20H). Requires
        `permit.read` and an ACTIVE caller Building assignment to the Worker's
        Building (else 403 `BUILDING_ACCESS_DENIED`). An unknown `id` yields
        404 `PERMIT_WORKER_NOT_FOUND`.
      tags: [Permit Workers]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitWorkerIdPath"
      responses:
        "200":
          description: Permit Worker.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitWorker" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.read` is missing - or
            `BUILDING_ACCESS_DENIED` - the caller holds no ACTIVE assignment to
            the Worker's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Worker not found (`PERMIT_WORKER_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    patch:
      operationId: updatePermitWorker
      x-required-permission: permit.manage
      summary: Update Permit Worker (ACTIVE on open Permit only)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03G1 (BE-20H). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the Worker's
        Building (else 403 `BUILDING_ACCESS_DENIED`). Updates an `ACTIVE`
        Worker entry on an open Permit (`PENDING`|`VALID` validity); otherwise
        400 `PERMIT_WORKER_UPDATE_NOT_ALLOWED`. Updatable fields: `roleTrade`
        (data-driven code), `validFrom`/`validUntil` (ISO-8601 with timezone,
        ordered and contained) and `notes` (2000, empty->null, `null` clears);
        at least one is required. Immutable context fields (`permitId`,
        `permitApplicationId`, `buildingId`, `contractorContextType` etc,
        `workforceProfileId`, `vendorWorkforceBindingId`, `status`,
        `deactivatedAt`) must not be supplied (400). An unknown `id` yields 404
        `PERMIT_WORKER_NOT_FOUND`.
      tags: [Permit Workers]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitWorkerIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdatePermitWorkerRequest" }
      responses:
        "200":
          description: Updated Permit Worker (`status` still ACTIVE).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitWorker" }
        "400":
          description: >
            `VALIDATION_ERROR` / `PERMIT_WORKER_INVALID_VALIDITY` /
            `PERMIT_WORKER_UPDATE_NOT_ALLOWED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Worker not found (`PERMIT_WORKER_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-workers/{id}/deactivate:
    post:
      operationId: deactivatePermitWorker
      x-required-permission: permit.manage
      summary: Deactivate Permit Worker (ACTIVE -> INACTIVE)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03G1 (BE-20H). Requires
        `permit.manage` and an ACTIVE caller Building assignment to the Worker's
        Building (else 403 `BUILDING_ACCESS_DENIED`). Deactivates one Worker
        entry. Only `ACTIVE` can be deactivated (400
        `PERMIT_WORKER_DEACTIVATE_NOT_ALLOWED` otherwise). An unknown `id`
        yields 404 `PERMIT_WORKER_NOT_FOUND`.
      tags: [Permit Workers]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/PermitWorkerIdPath"
      responses:
        "200":
          description: Deactivated Permit Worker (`status` INACTIVE).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/PermitWorker" }
        "400":
          description: >
            `VALIDATION_ERROR` - path validation - or
            `PERMIT_WORKER_DEACTIVATE_NOT_ALLOWED` - not `ACTIVE`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `permit.manage` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Permit Worker not found (`PERMIT_WORKER_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03G2 - J11 Work Permit Readiness (BE-15D).
  #
  # Contract exposure of the ALREADY REGISTERED BE-15D runtime routes only.
  # No runtime, lifecycle or generic readiness behaviour is added or
  # changed here. Read-only projection + binding create/update only.
  # ---------------------------------------------------------------------------

  /permit-readiness:
    post:
      operationId: createWorkPermitReadiness
      x-required-permission: vendor.manage
      summary: Create Work Permit Readiness
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03G2 (BE-15D). Requires
        `vendor.manage` and Building isolation via the Vendor Work's Building
        (else 400 `PERMIT_READINESS_BUILDING_MISMATCH` / 403
        `BUILDING_ACCESS_DENIED`). Creates a readiness/binding record for one
        Vendor Work (`vendorWorkId`) and one `permitRequirementType`
        (uppercased 1..100, `NOT_REQUIRED` reserved). Required body:
        `vendorWorkId` (Vendor Work id), `permitRequirementType`. Optional
        `permitReference` (200, empty->null), `permitStatus`
        (DRAFT|PENDING|ISSUED|SUSPENDED|REJECTED|REVOKED|EXPIRED), `validFrom`/
        `validUntil` (ISO date strings, nullable, `validUntil` before
        `validFrom` yields 400 `PERMIT_READINESS_INVALID_VALIDITY` when both
        set) and `notes` (2000, empty->null). `readinessStatus`
        (READY|NOT_READY|EXPIRED|NOT_REQUIRED) is backend-derived and never
        client-supplied. One per (`vendorWorkId`, `permitRequirementType`)
        (409 `PERMIT_READINESS_ALREADY_EXISTS`). `clientId`/`buildingId`/
        `workOrderId`/`readinessStatus`/`createdByUserId`/`createdAt`/
        `updatedAt` are server-assigned.
      tags: [Work Permit Readiness]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateWorkPermitReadinessRequest" }
      responses:
        "201":
          description: Work Permit Readiness created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkPermitReadiness" }
        "400":
          description: >
            `VALIDATION_ERROR` / `PERMIT_READINESS_BUILDING_MISMATCH` /
            `PERMIT_READINESS_INVALID_VALIDITY`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.manage` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `PERMIT_READINESS_ALREADY_EXISTS` - readiness already exists for
            this vendor work and requirement type.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listWorkPermitReadiness
      x-required-permission: vendor.read
      summary: List Work Permit Readiness (filtered)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03G2 (BE-15D). Requires
        `vendor.read`. Lists readiness/binding records filtered by at least
        one of `vendorWorkId`, `vendorId` or `buildingId` (400
        `VALIDATION_ERROR` when none supplied). `vendorWorkId` filters to one
        Vendor Work, `vendorId` to one Vendor's works, `buildingId` additionally
        asserts Building access (403 `BUILDING_ACCESS_DENIED` when not accessible
        and 400 `PERMIT_READINESS_BUILDING_MISMATCH` when the Vendor holds no
        ACTIVE relationship to that Building). `data` is a plain array filtered
        to the caller's accessible Buildings - not paginated. No automatic
        permit evaluation is performed beyond `readinessStatus` derivation.
      tags: [Work Permit Readiness]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: vendorWorkId
          in: query
          required: false
          description: Filter by Vendor Work id. At least one of vendorWorkId/vendorId/buildingId required.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          required: false
          description: Filter by Vendor id.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          description: Filter by Building id. Building access is asserted when supplied.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Work Permit Readiness list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/WorkPermitReadiness" }
        "400":
          description: >
            `VALIDATION_ERROR` - at least one filter required - or
            `PERMIT_READINESS_BUILDING_MISMATCH` - Vendor/Building mismatch.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.read` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-readiness/current:
    get:
      operationId: resolveVendorWorkPermitReadiness
      x-required-permission: vendor.read
      summary: Resolve current Work Permit Readiness for Vendor Work
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03G2 (BE-15D). Requires
        `vendor.read` and Building isolation via the Vendor Work's Building
        (else 403 `BUILDING_ACCESS_DENIED`). Resolves the authoritative,
        time-aware current readiness for one Vendor Work: `vendorWorkId`
        (required query), returning `VendorWorkPermitReadiness` with `ready`
        boolean (true when every `readinessStatus` is READY/NOT_REQUIRED and no
        EXPIRED), `readinessStatus` aggregate (READY|NOT_READY|EXPIRED|
        NOT_REQUIRED), `buildingId` and `permits` array of
        `ResolvedPermitReadiness` (time-aware). An unknown `vendorWorkId`
        yields 404 `PERMIT_READINESS_NOT_FOUND` via Vendor Work lookup or 400
        `VALIDATION_ERROR`.
      tags: [Work Permit Readiness]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: vendorWorkId
          in: query
          required: true
          description: Required Vendor Work id.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Current Vendor Work Permit Readiness.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorWorkPermitReadiness" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.read` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Vendor Work not found or readiness not found
            (`PERMIT_READINESS_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /permit-readiness/{readinessId}:
    get:
      operationId: getWorkPermitReadiness
      x-required-permission: vendor.read
      summary: Get Work Permit Readiness
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03G2 (BE-15D). Requires
        `vendor.read` and Building isolation via the readiness's Building
        (else 403 `BUILDING_ACCESS_DENIED`). An unknown `readinessId`
        yields 404 `PERMIT_READINESS_NOT_FOUND`.
      tags: [Work Permit Readiness]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkPermitReadinessIdPath"
      responses:
        "200":
          description: Work Permit Readiness.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkPermitReadiness" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.read` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Work permit readiness not found (`PERMIT_READINESS_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    patch:
      operationId: updateWorkPermitReadiness
      x-required-permission: vendor.manage
      summary: Update Work Permit Readiness
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 03G2 (BE-15D). Requires
        `vendor.manage` and Building isolation via the readiness's Building
        (else 403 `BUILDING_ACCESS_DENIED`). Partial update of a readiness
        record. Optional fields: `permitReference` (200, empty->null, `null`
        clears), `permitStatus`
        (DRAFT|PENDING|ISSUED|SUSPENDED|REJECTED|REVOKED|EXPIRED),
        `validFrom`/`validUntil` (ISO date strings, nullable, `null` clears,
        `validUntil` before `validFrom` yields 400
        `PERMIT_READINESS_INVALID_VALIDITY`), `notes` (2000, empty->null,
        `null` clears). At least one is required. `permitRequirementType` and
        identity fields (`vendorWorkId`/`buildingId`/`clientId`/`workOrderId`)
        are immutable. `readinessStatus` remains backend-derived. An unknown
        `readinessId` yields 404 `PERMIT_READINESS_NOT_FOUND`.
      tags: [Work Permit Readiness]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkPermitReadinessIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateWorkPermitReadinessRequest" }
      responses:
        "200":
          description: Updated Work Permit Readiness.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkPermitReadiness" }
        "400":
          description: >
            `VALIDATION_ERROR` / `PERMIT_READINESS_INVALID_VALIDITY` - at least
            one field required.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.manage` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Work permit readiness not found (`PERMIT_READINESS_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 04A - J12 Vendor Assignments (BE-15A).
  #
  # Contract exposure of the ALREADY REGISTERED BE-15A runtime routes only.
  # No runtime, lifecycle or authorization behaviour is added or changed here.
  # Vendor Work execution and every later J12 module remain undocumented.
  # ---------------------------------------------------------------------------

  /vendor-assignments:
    post:
      operationId: assignVendorToWorkOrder
      x-required-permission: vendor.manage
      summary: Assign a Vendor to a Work Order
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04A (BE-15A). Creates an ACTIVE
        Vendor Assignment binding `vendorId` to `workOrderId` inside the Work
        Order's Building. Requires `vendor.manage` plus Building isolation via
        the Work Order's Building (403 `BUILDING_ACCESS_DENIED`; an unknown
        `workOrderId` resolves to 404 before access is asserted). Required
        body: `vendorId` and `workOrderId` (both UUIDs); optional `notes`
        (trimmed, max 2000, empty/null -> null). Unknown body fields are
        ignored. Business guards, all 400 unless stated: the Work Order must be
        in an assignable lifecycle state (`VENDOR_ASSIGNMENT_WORK_ORDER_INVALID_STATE`),
        the Vendor must belong to the same Client as the Work Order
        (`VENDOR_ASSIGNMENT_CLIENT_MISMATCH`, deliberately 400 rather than 404
        so no other Client's existence is observable) and the Vendor must hold
        an ACTIVE relationship to the Work Order's Building
        (`VENDOR_ASSIGNMENT_BUILDING_MISMATCH`). A duplicate ACTIVE pair yields
        409 `VENDOR_ASSIGNMENT_ALREADY_ACTIVE` - deactivate or reassign the
        existing assignment first. `id`, `buildingId`, `status` (starts
        `ACTIVE`), `assignedByUserId` (the authenticated caller, never
        client-supplied) and `assignedAt` are server-assigned. Creating an
        assignment never creates a Vendor Work and never changes Vendor or Work
        Order status.
      tags: [Vendor Assignment]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateVendorAssignmentRequest" }
      responses:
        "201":
          description: Vendor assignment created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorAssignment" }
        "400":
          description: >
            `VALIDATION_ERROR` (missing/invalid `vendorId` or `workOrderId`,
            `notes` over 2000) or `VENDOR_ASSIGNMENT_WORK_ORDER_INVALID_STATE`
            / `VENDOR_ASSIGNMENT_CLIENT_MISMATCH` /
            `VENDOR_ASSIGNMENT_BUILDING_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.manage` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: The referenced Work Order does not exist.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            This vendor already has an active assignment for the work order
            (`VENDOR_ASSIGNMENT_ALREADY_ACTIVE`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listVendorAssignments
      x-required-permission: vendor.read
      summary: List Vendor Assignments (filtered)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04A (BE-15A). Requires `vendor.read`.
        Lists Vendor Assignments filtered by at least one of `vendorId`,
        `workOrderId` or `buildingId` (400 `VALIDATION_ERROR` when none is
        supplied; each value must be a UUID). `buildingId` additionally asserts
        Building access (403 `BUILDING_ACCESS_DENIED` when the caller holds no
        ACTIVE assignment to it). `data` is a plain array - including INACTIVE
        history rows, never paginated - constrained to the caller's accessible
        Buildings, and an empty array is returned when the caller reaches no
        Building. No filter means no result is ever leaked: the filter
        requirement is a hard validation rule, not a default "all".
      tags: [Vendor Assignment]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: vendorId
          in: query
          required: false
          description: Filter by Vendor id. At least one of vendorId/workOrderId/buildingId is required.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: workOrderId
          in: query
          required: false
          description: Filter by Work Order id.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          description: Filter by Building id. Building access is asserted when supplied.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Vendor assignment list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/VendorAssignment" }
        "400":
          description: >
            `VALIDATION_ERROR` - at least one filter is required and every
            supplied filter must be a valid UUID.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.read` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /vendor-assignments/{assignmentId}:
    get:
      operationId: getVendorAssignment
      x-required-permission: vendor.read
      summary: Get one Vendor Assignment
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04A (BE-15A). Requires `vendor.read`.
        Resolves the assignment first, then asserts the caller's ACTIVE
        assignment to that assignment's Building (403
        `BUILDING_ACCESS_DENIED`). `assignmentId` must be a valid UUID
        (400 `VALIDATION_ERROR` otherwise); an unknown id yields 404
        `VENDOR_ASSIGNMENT_NOT_FOUND`. INACTIVE rows remain readable - they are
        history, not deleted records.
      tags: [Vendor Assignment]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VendorAssignmentIdPath"
      responses:
        "200":
          description: Vendor assignment.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorAssignment" }
        "400":
          description: >
            `VALIDATION_ERROR` - `assignmentId` must be a valid UUID.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.read` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Vendor assignment not found (`VENDOR_ASSIGNMENT_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    patch:
      operationId: updateVendorAssignment
      x-required-permission: vendor.manage
      summary: Deactivate or reassign a Vendor Assignment
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04A (BE-15A). Requires
        `vendor.manage`. The assignment is resolved first and its Building is
        used for Building isolation (403 `BUILDING_ACCESS_DENIED`, 404
        `VENDOR_ASSIGNMENT_NOT_FOUND` for an unknown id). Exactly one of two
        body forms is accepted, each mutating once and returning 200 `data` of
        the affected `VendorAssignment`: (1) DEACTIVATE - `{ "status":
        "INACTIVE" }`; `INACTIVE` is the only accepted value (any other status,
        including `ACTIVE`, is 400 `VALIDATION_ERROR`). The row is marked
        INACTIVE and never deleted; re-deactivating an already-INACTIVE
        assignment is a no-op success returning that same row. (2) REASSIGN - a
        vendor/work-order payload (`vendorId` and `workOrderId` required,
        `notes` optional) with no `status` key; it deactivates the current
        assignment and creates a fresh ACTIVE one, so the returned `data` is the
        NEW assignment, not the path one. Reassignment requires the target to be
        ACTIVE (400 `VENDOR_ASSIGNMENT_NOT_ACTIVE`) and applies every create
        guard: Work Order assignable state, Client match, Vendor->Building
        relationship (all 400) and a single ACTIVE pair (409
        `VENDOR_ASSIGNMENT_ALREADY_ACTIVE`). There is no delete, no restore, no
        reactivation of an INACTIVE row and no Vendor Work or acceptance
        side-effect.
      tags: [Vendor Assignment]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VendorAssignmentIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateVendorAssignmentRequest" }
      responses:
        "200":
          description: >
            Deactivated assignment, or the newly created assignment when
            reassigned.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorAssignment" }
        "400":
          description: >
            `VALIDATION_ERROR` - invalid `assignmentId`/body, `status` other
            than `INACTIVE`, or a reassign payload missing `vendorId`/
            `workOrderId` - or `VENDOR_ASSIGNMENT_NOT_ACTIVE` (only ACTIVE may
            be reassigned) - or `VENDOR_ASSIGNMENT_WORK_ORDER_INVALID_STATE` /
            `VENDOR_ASSIGNMENT_CLIENT_MISMATCH` /
            `VENDOR_ASSIGNMENT_BUILDING_MISMATCH` on the reassign target.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.manage` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Vendor assignment not found (`VENDOR_ASSIGNMENT_NOT_FOUND`), or the
            reassign target Work Order does not exist.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            This vendor already has an active assignment for the work order
            (`VENDOR_ASSIGNMENT_ALREADY_ACTIVE`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 04B1 - J12 Vendor Work (BE-15B).
  #
  # Contract exposure of the ALREADY REGISTERED BE-15B runtime routes only.
  # No runtime, lifecycle or authorization behaviour is added or changed here.
  # Vendor Work Evidence / History, completion reports, rework, verification,
  # BAST and acceptance surfaces stay out of scope in this PART.
  # ---------------------------------------------------------------------------

  /vendor-assignments/{assignmentId}/work:
    post:
      operationId: resolveVendorWorkForAssignment
      x-required-permission: vendor.manage
      summary: Resolve the Vendor Work of a Vendor Assignment
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04B1 (BE-15B). Resolves - creates on
        the first call, returns as-is afterwards - the single Vendor Work
        execution record of one BE-15A Vendor Assignment. Requires
        `vendor.manage`; the assignment is loaded first (unknown id 404
        `VENDOR_ASSIGNMENT_NOT_FOUND`) and its Building is used for Building
        isolation (403 `BUILDING_ACCESS_DENIED`) before the body is parsed. The
        request body is OPTIONAL and may be omitted entirely; only `notes` is
        accepted. Because one Vendor Work per assignment is enforced by a
        database unique constraint, this operation is idempotent: 201 when the
        record is created (`status` starts at `NOT_STARTED`,
        `startedAt`/`completedAt` null) and 200 carrying the pre-existing
        record on every later call, so `notes` on a repeat call is not applied
        and no 409 is ever raised (a create race resolves to the same 200
        existing record). `vendorId`, `workOrderId` and `buildingId` are copied
        from the assignment and are never client-supplyable. Before creating,
        the runtime re-validates the underlying masters so a stale assignment
        cannot start work: the Work Order must still exist (404
        `WORK_ORDER_NOT_FOUND`) and still be in an assignable state (400
        `VENDOR_WORK_WORK_ORDER_INVALID_STATE`) and still sit in the
        assignment's Building, and the Vendor must still exist (404
        `VENDOR_NOT_FOUND`), be ACTIVE (400 `VENDOR_INACTIVE`) and hold an
        ACTIVE relationship to that Building; the remaining relationship or
        Building failures report 400 `VENDOR_WORK_BUILDING_MISMATCH` and a
        non-ACTIVE assignment reports 400 `VENDOR_WORK_ASSIGNMENT_INACTIVE`.
        Resolving work starts nothing, completes nothing, creates no evidence,
        checklist or document, and does not accept or approve anything.
      tags: [Vendor Work]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VendorAssignmentIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ResolveVendorWorkRequest" }
      responses:
        "200":
          description: >
            Existing Vendor Work returned unchanged (idempotent repeat or
            create race).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorWork" }
        "201":
          description: Vendor Work created for this assignment.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorWork" }
        "400":
          description: >
            `VALIDATION_ERROR` - invalid `assignmentId`, non-object body or
            `notes` over 2000 - or `VENDOR_WORK_ASSIGNMENT_INACTIVE` /
            `VENDOR_WORK_WORK_ORDER_INVALID_STATE` /
            `VENDOR_WORK_BUILDING_MISMATCH` / `VENDOR_INACTIVE`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.manage` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Vendor assignment not found (`VENDOR_ASSIGNMENT_NOT_FOUND`), or the
            referenced Work Order / Vendor no longer exists
            (`WORK_ORDER_NOT_FOUND` / `VENDOR_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /vendor-works:
    get:
      operationId: listVendorWorks
      x-required-permission: vendor.read
      summary: List Vendor Works (filtered)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04B1 (BE-15B). Requires
        `vendor.read`. Lists Vendor Works filtered by at least one of
        `vendorId`, `buildingId` or `status` (400 `VALIDATION_ERROR` when none
        is supplied; each id must be a valid UUID and `status` must be one of
        `NOT_STARTED`/`IN_PROGRESS`/`ON_HOLD`/`COMPLETED`). `buildingId`
        additionally asserts Building access (403 `BUILDING_ACCESS_DENIED` when
        the caller holds no ACTIVE assignment to it), and when `vendorId` and
        `buildingId` are combined the Vendor must hold an ACTIVE relationship to
        that Building (400 `VENDOR_WORK_BUILDING_MISMATCH`). `data` is a plain
        array - never paginated - constrained to the caller's accessible
        Buildings, and an empty array is returned when the caller reaches no
        Building. This is a read of execution state only: it exposes no BAST,
        acceptance, verification or financial projection.
      tags: [Vendor Work]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: vendorId
          in: query
          required: false
          description: Filter by Vendor id. At least one of vendorId/buildingId/status is required.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          description: Filter by Building id. Building access is asserted when supplied.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          description: Filter by Vendor Work lifecycle status.
          schema: { $ref: "#/components/schemas/VendorWorkStatus" }
      responses:
        "200":
          description: Vendor work list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/VendorWork" }
        "400":
          description: >
            `VALIDATION_ERROR` - at least one filter required, an invalid UUID
            or an unknown `status` - or `VENDOR_WORK_BUILDING_MISMATCH` - the
            vendorId/buildingId pair holds no ACTIVE relationship.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.read` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /vendor-works/{workId}:
    get:
      operationId: getVendorWork
      x-required-permission: vendor.read
      summary: Get one Vendor Work
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04B1 (BE-15B). Requires
        `vendor.read`. Resolves the Vendor Work first (404
        `VENDOR_WORK_NOT_FOUND` for an unknown id, 400 `VALIDATION_ERROR` for a
        non-UUID `workId`), then asserts the caller's ACTIVE assignment to that
        work's Building (403 `BUILDING_ACCESS_DENIED`). The response carries the
        execution record and its current `status` only - a `COMPLETED` work is
        not evidence of acceptance.
      tags: [Vendor Work]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VendorWorkIdPath"
      responses:
        "200":
          description: Vendor work.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorWork" }
        "400":
          description: >
            `VALIDATION_ERROR` - `workId` must be a valid UUID.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.read` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Vendor work not found (`VENDOR_WORK_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /vendor-works/{workId}/status:
    patch:
      operationId: transitionVendorWorkStatus
      x-required-permission: vendor.manage
      summary: Transition Vendor Work status (start / hold / complete)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04B1 (BE-15B). Requires
        `vendor.manage`. The Vendor Work is resolved first (404
        `VENDOR_WORK_NOT_FOUND`, 400 `VALIDATION_ERROR` for a non-UUID `workId`)
        and its Building is used for Building isolation (403
        `BUILDING_ACCESS_DENIED`) before the transition is evaluated. `status`
        is required and `notes` is optional; the explicit backend transition
        table, mirrored in `x-vendor-work-transitions`, is the sole authority -
        there is no generic workflow engine and no client-side override. A pair
        outside that table, including a repeat of the current status or any move
        out of `COMPLETED`, is rejected with 400
        `VENDOR_WORK_INVALID_TRANSITION` and mutates nothing. `startedAt` is set
        by the backend the first time the work leaves `NOT_STARTED` and
        `completedAt` when it reaches `COMPLETED`; neither timestamp is accepted
        in the body. A non-empty trimmed `notes` replaces the stored notes,
        while `null` or an empty string is accepted and ignored (it does not
        clear notes). No other field is mutable, there is no delete and no
        re-open in this module. Reaching `COMPLETED` records execution
        completion only: it does not create or advance a BAST, does not accept,
        verify or sign off anything, does not generate a completion report or an
        invoice, does not trigger rework, and does not change Vendor Assignment
        or Work Order status.
      tags: [Vendor Work]
      security: [{ bearerAuth: [] }]
      x-vendor-work-transitions:
        NOT_STARTED: [IN_PROGRESS]
        IN_PROGRESS: [ON_HOLD, COMPLETED]
        ON_HOLD: [IN_PROGRESS]
        COMPLETED: []
      parameters:
        - $ref: "#/components/parameters/VendorWorkIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateVendorWorkStatusRequest" }
      responses:
        "200":
          description: Updated Vendor Work after the accepted transition.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorWork" }
        "400":
          description: >
            `VALIDATION_ERROR` - missing or unknown `status`, invalid `workId`
            or `notes` over 2000 - or `VENDOR_WORK_INVALID_TRANSITION` - the
            pair is not allowed by the backend transition table.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.manage` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Vendor work not found (`VENDOR_WORK_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 04B2 - J12 Vendor Work Evidence (BE-15E)
  # + Vendor Work History (BE-15K).
  #
  # Contract exposure of the ALREADY REGISTERED BE-15E / BE-15K runtime routes
  # only. No runtime, lifecycle or authorization behaviour is added or changed
  # here. These routes bind and read the shared BE-07 evidence engine and the
  # BE-07 operational-events store for Vendor Work; no generic evidence,
  # document-management, retention, integrity, storage or approval surface is
  # introduced, and completion reports / rework / verification / BAST /
  # acceptance stay out of scope in this PART.
  # ---------------------------------------------------------------------------

  /vendor-works/{id}/evidence-requirements:
    get:
      operationId: listVendorWorkEvidenceRequirements
      x-required-permission: vendor.read
      summary: List the evidence requirements bound to a Vendor Work
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04B2 (BE-15E). Requires
        `vendor.read`; no request body and no query parameters. The Vendor Work
        is resolved first (unknown id 404 `VENDOR_WORK_NOT_FOUND`), then its
        BE-08 Work Order (404 `WORK_ORDER_NOT_FOUND`) as the authority for the
        Building; a Work Order whose Building differs from the work's yields 400
        `VENDOR_WORK_EVIDENCE_BUILDING_MISMATCH`, and the resolved Building is
        then used for Building isolation (403 `BUILDING_ACCESS_DENIED`).
        `data` is the plain, non-paginated array of the ACTIVE BE-07
        `evidence_requirements` bound to this Vendor Work
        (`target_type = 'VENDOR_WORK'`) - possibly empty when the work carries no
        requirement. This is a read of the shared engine through the Vendor Work
        scope only: no requirement is created, updated or satisfied here, and
        requirement fulfilment is never asserted by this response.
      tags: [Vendor Work Evidence]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VendorWorkPathId"
      responses:
        "200":
          description: Vendor work evidence requirements.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          { $ref: "#/components/schemas/VendorWorkEvidenceRequirement" }
        "400":
          description: >
            `VALIDATION_ERROR` - `id` must be a valid UUID (validation detail
            field `vendorWorkId`) - or `VENDOR_WORK_EVIDENCE_BUILDING_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.read` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Vendor work not found (`VENDOR_WORK_NOT_FOUND`) or its Work Order no
            longer exists (`WORK_ORDER_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /vendor-works/{id}/evidence:
    post:
      operationId: submitVendorWorkEvidence
      x-required-permission: vendor.manage
      summary: Submit evidence against a Vendor Work
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04B2 (BE-15E). Requires
        `vendor.manage`; 201 with the created `VendorWorkEvidence` bound to the
        Vendor Work through the shared BE-07 engine (`execution_type =
        'VENDOR_WORK'`). The payload is JSON metadata only - this route
        registers NO multipart, binary or object-storage upload, receives no
        file bytes, and performs no OCR, compression, virus scanning, hashing or
        integrity governance. The runtime enforces these guards in this pinned
        order: an unknown Vendor Work is 404 `VENDOR_WORK_NOT_FOUND`, a Work
        Order that no longer exists is 404 `WORK_ORDER_NOT_FOUND`, a Vendor Work
        in `COMPLETED` is 400 `VENDOR_WORK_EVIDENCE_INVALID_STATE` (terminal
        work cannot receive evidence), an `evidenceRequirementId` unknown to
        this work, one whose `evidenceType` differs from the submitted one, or
        a `mimeType` outside the type's allowed set is 400
        `VENDOR_WORK_EVIDENCE_REQUIREMENT_MISMATCH`, and a requirement whose
        `maximumCount` is already reached is 400
        `VENDOR_WORK_EVIDENCE_COUNT_VIOLATION`. Allowed MIME per type exactly as
        the runtime table: PHOTO = image/jpeg, image/png, image/webp; DOCUMENT =
        application/pdf, image/jpeg, image/png; SIGNATURE = image/png,
        image/svg+xml. Building isolation is asserted on the work's resolved
        Building (403 `BUILDING_ACCESS_DENIED`) after that Work Order / work
        resolution and before the state, requirement and count checks. `id`,
        `clientId`, `vendorWorkId`, `submittedByUserId` (from the session),
        `status`, `createdAt` and `updatedAt` are server-assigned. Submitting evidence does not complete, accept, verify,
        sign off or hand over the work, does not create a completion report, and
        never satisfies a requirement flag on the Vendor Work.
      tags: [Vendor Work Evidence]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VendorWorkPathId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubmitVendorWorkEvidenceRequest" }
      responses:
        "201":
          description: Evidence submission bound to the Vendor Work.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorWorkEvidence" }
        "400":
          description: >
            `VALIDATION_ERROR` - invalid `id` or body
            (`evidenceType`/`fileReference`/`originalFileName`/`mimeType`/
            `fileSize` required, `fileSize` an integer 0..52428800) - or
            `VENDOR_WORK_EVIDENCE_INVALID_STATE` /
            `VENDOR_WORK_EVIDENCE_REQUIREMENT_MISMATCH` /
            `VENDOR_WORK_EVIDENCE_COUNT_VIOLATION` /
            `VENDOR_WORK_EVIDENCE_BUILDING_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.manage` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Vendor work not found (`VENDOR_WORK_NOT_FOUND`) or its Work Order no
            longer exists (`WORK_ORDER_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listVendorWorkEvidence
      x-required-permission: vendor.read
      summary: List evidence submitted for a Vendor Work
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04B2 (BE-15E). Requires
        `vendor.read`; no request body and no query parameters. Same Vendor
        Work / Work Order resolution and Building isolation as the requirement
        listing (404 `VENDOR_WORK_NOT_FOUND` / `WORK_ORDER_NOT_FOUND`, 400
        `VENDOR_WORK_EVIDENCE_BUILDING_MISMATCH`, 403
        `BUILDING_ACCESS_DENIED`). `data` is the plain, non-paginated array of
        the ACTIVE submissions bound to this Vendor Work; soft-removed
        (`REMOVED`) rows are not included. Removal semantics are exposed only on
        `PATCH /vendor-work-evidence/{evidenceId}`.
      tags: [Vendor Work Evidence]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VendorWorkPathId"
      responses:
        "200":
          description: Vendor work evidence submissions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/VendorWorkEvidence" }
        "400":
          description: >
            `VALIDATION_ERROR` - `id` must be a valid UUID (validation detail
            field `vendorWorkId`) - or `VENDOR_WORK_EVIDENCE_BUILDING_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.read` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Vendor work not found (`VENDOR_WORK_NOT_FOUND`) or its Work Order no
            longer exists (`WORK_ORDER_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /vendor-works/{id}/history:
    get:
      operationId: getVendorWorkHistory
      x-required-permission: vendor.read
      summary: Read the history of a Vendor Work
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04B2 (BE-15K). Requires
        `vendor.read`; no request body. `data` is the plain, non-paginated
        array of the Vendor Work's history events in chronological order, read
        from the shared BE-07 append-oriented operational-events store and
        scoped to the work plus its BE-15A assignment. The Vendor Work is
        resolved first (404 `VENDOR_WORK_NOT_FOUND`; a non-UUID `id` is 400
        `VALIDATION_ERROR` with validation detail field `workId`, the BE-15B
        parser being reused here) and its Building is then used for Building
        isolation (403 `BUILDING_ACCESS_DENIED`), so an inaccessible work is
        indistinguishable from an unknown one only after that order. `eventType`
        filters to one exact event type (trimmed, at most 64 characters, empty
        treated as absent) and `from` / `to` bound the range (any value
        `Date.parse` accepts, normalised to ISO-8601, empty treated as absent,
        applied by the shared store as the range bounds);
        `from` after `to` is 400 `BAD_REQUEST` with message "History filter
        range is invalid.". HISTORY IS READ-ONLY: no event creation, update,
        editing, deletion, timeline reconstruction or retention surface is
        registered on this module and none is implied - events are written by
        the BE-15 domain services themselves, and `metadata` / `related` are
        opaque server-generated objects. `eventType` is an open string: callers
        must tolerate values outside those recorded by the surfaces documented
        today, and a `VENDOR_WORK_STATUS_CHANGED` or evidence event is an audit
        record only - it never means acceptance, sign-off, verification,
        handover or BAST progression, and it exposes no approval decision.
      tags: [Vendor Work History]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VendorWorkPathId"
        - name: eventType
          in: query
          required: false
          description: Exact event type to filter by (trimmed, max 64; empty is treated as absent).
          schema: { type: string, maxLength: 64 }
        - name: from
          in: query
          required: false
          description: Lower bound of the event range; any date or ISO timestamp accepted by Date.parse, normalised to ISO-8601 before the store is queried.
          schema: { type: string, format: date-time }
        - name: to
          in: query
          required: false
          description: Upper bound of the event range; any date or ISO timestamp accepted by Date.parse, normalised to ISO-8601 before the store is queried. `from` after `to` yields 400 BAD_REQUEST.
          schema: { type: string, format: date-time }
      responses:
        "200":
          description: Vendor work history events in chronological order.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/VendorWorkHistoryEvent" }
        "400":
          description: >
            `VALIDATION_ERROR` - non-UUID `id`, `eventType` over 64 characters
            or an unparseable `from`/`to` - or `BAD_REQUEST` - `from` is after
            `to`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.read` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Vendor work not found (`VENDOR_WORK_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /vendor-work-evidence:
    get:
      operationId: listVendorWorkEvidenceByFilters
      x-required-permission: vendor.read
      summary: List Vendor Work evidence (filtered)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04B2 (BE-15E). Requires
        `vendor.read`; no request body. Lists evidence submissions filtered by
        at least one of `vendorWorkId`, `vendorId` or `buildingId` (400
        `VALIDATION_ERROR` when none is supplied and every supplied value must
        be a valid UUID). `vendorWorkId` additionally asserts Building access on
        that work's Building (403 `BUILDING_ACCESS_DENIED`, 404
        `VENDOR_WORK_NOT_FOUND` for an unknown work) and pins the Building
        filter to that work's Building. `buildingId` asserts Building access on
        its own, and combining `vendorId` with a Building requires the Vendor to
        hold an ACTIVE relationship to it (400
        `VENDOR_WORK_EVIDENCE_BUILDING_MISMATCH`). `data` is a plain,
        non-paginated array constrained to the caller's accessible Buildings,
        and an empty array is returned when the caller reaches no Building. No
        evidence is created, removed or evaluated for fulfilment here, and the
        response exposes no retention, integrity or approval state.
      tags: [Vendor Work Evidence]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: vendorWorkId
          in: query
          required: false
          description: Filter by Vendor Work id. At least one of vendorWorkId/vendorId/buildingId is required.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          required: false
          description: Filter by Vendor id.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          description: Filter by Building id. Building access is asserted when supplied.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Vendor work evidence submissions.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/VendorWorkEvidence" }
        "400":
          description: >
            `VALIDATION_ERROR` - at least one filter required and each must be a
            valid UUID - or `VENDOR_WORK_EVIDENCE_BUILDING_MISMATCH` - the
            vendorId/buildingId pair holds no ACTIVE relationship.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.read` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Vendor work not found (`VENDOR_WORK_NOT_FOUND`) for the
            `vendorWorkId` filter.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /vendor-work-evidence/{evidenceId}:
    patch:
      operationId: removeVendorWorkEvidence
      x-required-permission: vendor.manage
      summary: Soft-remove a Vendor Work evidence submission
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04B2 (BE-15E). Requires
        `vendor.manage`. This PATCH consumes NO request body - there is no
        field to set and no removal-reason, comment, replacement or
        re-attachment contract. It applies the BE-07 remove rule, which
        soft-deactivates the row by setting its `status` to `REMOVED`; the
        submission, its stored file reference and its history are never deleted,
        so the removed row keeps existing for audit while disappearing from the
        per-work ACTIVE listing. Unknown `evidenceId` (or an `evidenceId` that
        resolves to no Vendor-Work-bound submission) is 404
        `VENDOR_WORK_EVIDENCE_NOT_FOUND`, a non-UUID one is 400
        `VALIDATION_ERROR`, and the owning Vendor Work's Building is used for
        Building isolation (403 `BUILDING_ACCESS_DENIED`, with the same Work
        Order resolution and 400 `VENDOR_WORK_EVIDENCE_BUILDING_MISMATCH` / 404
        `VENDOR_WORK_NOT_FOUND` / `WORK_ORDER_NOT_FOUND` guards as the other
        BE-15E routes). The response is the removed `VendorWorkEvidence`
        projection. Removal does not remove a requirement, does not unlock or
        re-open a Vendor Work, does not reject or rework anything, and does not
        touch BAST, acceptance, verification, handover or invoicing.
      tags: [Vendor Work Evidence]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VendorWorkEvidenceIdPath"
      responses:
        "200":
          description: Evidence submission soft-removed (status REMOVED).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorWorkEvidence" }
        "400":
          description: >
            `VALIDATION_ERROR` - `evidenceId` must be a valid UUID - or
            `VENDOR_WORK_EVIDENCE_BUILDING_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.manage` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Vendor work evidence not found (`VENDOR_WORK_EVIDENCE_NOT_FOUND`),
            or the owning Vendor Work / Work Order no longer exists
            (`VENDOR_WORK_NOT_FOUND` / `WORK_ORDER_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 04C1 - J12 Vendor Completion Reports
  # (BE-15F).
  #
  # Contract exposure of the ALREADY REGISTERED BE-15F runtime routes only.
  # No runtime, lifecycle or authorization behaviour is added or changed here.
  # Vendor Rework, Vendor Verification, acceptance sign-off, work-completion
  # documents, handover and BAST surfaces stay out of scope in this PART.
  # ---------------------------------------------------------------------------

  /vendor-completion-reports:
    post:
      operationId: createCompletionReport
      x-required-permission: vendor.manage
      summary: Draft a Vendor Completion Report for a Vendor Work
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04C1 (BE-15F). Creates the single
        completion report of one BE-15B Vendor Work in `DRAFT` and returns it
        with 201. Requires `vendor.manage`. Preconditions run in this order: the
        Vendor Work must exist (404 `VENDOR_WORK_NOT_FOUND`), its BE-08 Work
        Order must exist (404 `WORK_ORDER_NOT_FOUND`, the authority for the
        Client and Building) and the Work Order's Building must equal the work's
        (400 `COMPLETION_REPORT_BUILDING_MISMATCH`); only then is the caller's
        BE-02 Building access asserted (403 `BUILDING_ACCESS_DENIED`). One
        report per Vendor Work is enforced (409
        `COMPLETION_REPORT_ALREADY_EXISTS`, including the database unique
        constraint `vendor_completion_reports_work_unique` as the final
        authority for races) - correct a DRAFT with PATCH instead of creating a
        second one. `vendorWorkId` is the only linkage the body may carry;
        `workOrderId`, `buildingId` and `clientId` are derived from the Work
        Order and `createdByUserId` from the session. Evidence is NOT required
        to draft: the BE-07 readiness snapshot is computed and stored as-is, so
        a report can be created with `evidenceReady: false` and a populated
        `missingEvidenceTypes`, and it is never a verdict that work, acceptance
        or closure has happened. `completedByUserId` and `completedAt` are null
        until submission.
      tags: [Vendor Completion Report]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateVendorCompletionReportRequest"
      responses:
        "201":
          description: Completion report created as DRAFT.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        { $ref: "#/components/schemas/VendorCompletionReport" }
        "400":
          description: >
            `VALIDATION_ERROR` - missing/invalid `vendorWorkId`, non-object body
            or `summary`/`notes` over 4000 - or
            `COMPLETION_REPORT_BUILDING_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.manage` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Vendor work not found (`VENDOR_WORK_NOT_FOUND`) or its Work Order no
            longer exists (`WORK_ORDER_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            A completion report already exists for this vendor work
            (`COMPLETION_REPORT_ALREADY_EXISTS`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listCompletionReports
      x-required-permission: vendor.read
      summary: List Vendor Completion Reports (filtered)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04C1 (BE-15F). Requires
        `vendor.read`; no request body. Reports are filtered by at least one of
        `vendorWorkId`, `vendorId` or `buildingId` (400 `VALIDATION_ERROR` with
        validation detail field `filter` when none is supplied, and every
        supplied value must be a valid UUID). `vendorWorkId` resolves that
        Vendor Work first (404 `VENDOR_WORK_NOT_FOUND`), asserts access to its
        Building (403 `BUILDING_ACCESS_DENIED`) and pins the Building filter to
        it; `buildingId` on its own asserts access to that Building; combining
        `vendorId` with a Building requires the Vendor to hold an ACTIVE
        relationship to it (400 `COMPLETION_REPORT_BUILDING_MISMATCH`). `data`
        is a plain, non-paginated array constrained to the caller's accessible
        Buildings, and an empty array is returned when the caller reaches no
        Building. There is no status, ordering, search, pagination or
        include-archived parameter on this route, and listing never mutates the
        readiness snapshot - only create and a DRAFT edit recompute it, while
        submission writes the final ready state.
      tags: [Vendor Completion Report]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: vendorWorkId
          in: query
          required: false
          description: Filter by Vendor Work id. At least one of vendorWorkId/vendorId/buildingId is required.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorId
          in: query
          required: false
          description: Filter by Vendor id. Valid only as a filter - a report DTO never carries vendorId.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          description: Filter by Building id. Building access is asserted when supplied.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Vendor completion reports.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          { $ref: "#/components/schemas/VendorCompletionReport" }
        "400":
          description: >
            `VALIDATION_ERROR` - at least one filter required and each must be a
            valid UUID - or `COMPLETION_REPORT_BUILDING_MISMATCH` - the
            vendorId/buildingId pair holds no ACTIVE relationship.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.read` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Vendor work not found (`VENDOR_WORK_NOT_FOUND`) for the
            `vendorWorkId` filter.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /vendor-completion-reports/{reportId}:
    get:
      operationId: getCompletionReport
      x-required-permission: vendor.read
      summary: Get one Vendor Completion Report
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04C1 (BE-15F). Requires
        `vendor.read`; no request body and no query parameters. The report is
        loaded first, so an unknown id is 404 `COMPLETION_REPORT_NOT_FOUND`
        (a non-UUID `reportId` is 400 `VALIDATION_ERROR` with validation detail
        field `reportId`), and the report's own `buildingId` is then used for
        Building isolation (403 `BUILDING_ACCESS_DENIED`). Both DRAFT and
        SUBMITTED reports are returned unchanged - reading never recomputes the
        evidence snapshot and never advances the report.
      tags: [Vendor Completion Report]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VendorCompletionReportIdPath"
      responses:
        "200":
          description: Vendor completion report.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        { $ref: "#/components/schemas/VendorCompletionReport" }
        "400":
          description: >
            `VALIDATION_ERROR` - `reportId` must be a valid UUID.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.read` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Vendor completion report not found
            (`COMPLETION_REPORT_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    patch:
      operationId: updateCompletionReport
      x-required-permission: vendor.manage
      summary: Edit a DRAFT Vendor Completion Report
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04C1 (BE-15F). Requires
        `vendor.manage`. Mutates `summary` and `notes` of a `DRAFT` report only:
        at least one of the two must be present (400 `VALIDATION_ERROR` with
        validation detail field `body`), an omitted key keeps its current value,
        and `null` or an empty/whitespace string clears it to null (each at most
        4000 characters, trimmed). Guard order: an unknown id is 404
        `COMPLETION_REPORT_NOT_FOUND`; a report already `SUBMITTED` is 409
        `COMPLETION_REPORT_ALREADY_SUBMITTED`; only then is Building isolation
        asserted (403 `BUILDING_ACCESS_DENIED`). Every
        successful edit also recomputes and stores the BE-07 evidence-readiness
        snapshot, so `evidenceReady` / `missingEvidenceTypes` in the response are
        live, while a rejected edit mutates nothing. No other field is writable:
        `completionStatus` cannot be set, changed or forced to `SUBMITTED`
        through this route (submission is
        `POST /vendor-completion-reports/{reportId}/submit`), the linkage
        (`vendorWorkId`, `workOrderId`, `buildingId`, `clientId`),
        `createdByUserId` and both timestamps are immutable, there is no delete,
        no clear-then-recreate and no re-open of a submitted report. Editing or
        completing a report never changes Vendor Work status, Work Order status
        or any acceptance-bearing state.
      tags: [Vendor Completion Report]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VendorCompletionReportIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateVendorCompletionReportRequest"
      responses:
        "200":
          description: Updated DRAFT completion report.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        { $ref: "#/components/schemas/VendorCompletionReport" }
        "400":
          description: >
            `VALIDATION_ERROR` - invalid `reportId`, neither `summary` nor
            `notes` supplied, or a value over 4000 characters / not a string.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.manage` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Vendor completion report not found
            (`COMPLETION_REPORT_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            This completion report is already submitted
            (`COMPLETION_REPORT_ALREADY_SUBMITTED`) and is immutable.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /vendor-completion-reports/{reportId}/submit:
    post:
      operationId: submitCompletionReport
      x-required-permission: vendor.manage
      summary: Finalize a Vendor Completion Report (DRAFT to SUBMITTED)
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04C1 (BE-15F). The only action of this
        module's lifecycle: it finalizes a `DRAFT` report to `SUBMITTED`,
        stamping `completedByUserId` with the authenticated caller and
        `completedAt` with the submission time, and writing the readiness
        snapshot as `evidenceReady: true` with an empty `missingEvidenceTypes`.
        Requires `vendor.manage` and it consumes NO request body - there is no
        comment, reason, approver, decision or attachment field to send, and the
        caller cannot self-declare readiness. Guard order is exact: unknown id
        404 `COMPLETION_REPORT_NOT_FOUND`; already `SUBMITTED` 409
        `COMPLETION_REPORT_ALREADY_SUBMITTED` (a report can be submitted once
        and never reverted, edited or resubmitted); Building isolation on the
        report's Building 403 `BUILDING_ACCESS_DENIED`; authoritative
        re-validation of the Vendor Work and Work Order context (404
        `VENDOR_WORK_NOT_FOUND`, 404 `WORK_ORDER_NOT_FOUND`, 400
        `COMPLETION_REPORT_BUILDING_MISMATCH`); and the BE-07 required-evidence
        check, which rejects with 400 `COMPLETION_REPORT_EVIDENCE_INCOMPLETE`
        carrying one `details` entry per missing type - field `evidence`,
        message "Missing required evidence: <TYPE>." - and mutates nothing.
        A lost submit race also resolves to 409.
        SUBMISSION IS A REPORTING ACT, NOT ACCEPTANCE: it does not set Vendor
        Work `COMPLETED`, does not start, complete or close a Work Order, does
        not create, submit or accept a BAST, does not produce an acceptance
        sign-off, work-completion document or handover document, does not
        trigger verification or rework, and does not touch invoices,
        procurement or commercial approval. It writes one operational event
        (`VENDOR_COMPLETION_REPORT_SUBMITTED`) that
        `GET /vendor-works/{id}/history` can later read back.
      tags: [Vendor Completion Report]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VendorCompletionReportIdPath"
      responses:
        "200":
          description: Submission accepted; the report is now SUBMITTED.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        { $ref: "#/components/schemas/VendorCompletionReport" }
        "400":
          description: >
            `VALIDATION_ERROR` - `reportId` must be a valid UUID - or
            `COMPLETION_REPORT_EVIDENCE_INCOMPLETE` - required BE-07 evidence is
            below a requirement's minimum count - or
            `COMPLETION_REPORT_BUILDING_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.manage` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Vendor completion report not found
            (`COMPLETION_REPORT_NOT_FOUND`), Vendor work no longer exists
            (`VENDOR_WORK_NOT_FOUND`) or its Work Order no longer exists
            (`WORK_ORDER_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            This completion report is already submitted
            (`COMPLETION_REPORT_ALREADY_SUBMITTED`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 04C2 - J12 Vendor Rework (BE-15J) and
  # Vendor Work Verification (BE-15I).
  #
  # Contract exposure of the ALREADY REGISTERED BE-15J / BE-15I runtime routes
  # only. No runtime, lifecycle or authorization behaviour is added or changed
  # here. Acceptance sign-off, work-completion documents, handover documents and
  # every BAST surface stay out of scope in this PART, and no generic Finding /
  # Rework or generic verification behaviour is borrowed from other modules.
  # ---------------------------------------------------------------------------

  /vendor-works/{id}/rework:
    post:
      operationId: requestVendorRework
      x-required-permission: vendor.manage
      summary: Request rework on a Vendor Work
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04C2 (BE-15J). Opens one append-only
        rework cycle for the Vendor Work and returns it with 201 as
        `{ rework }`, in `REQUESTED` state. Requires
        `vendor.manage`; the caller is authorized through the Vendor Work first
        (non-UUID `id` 400 `VALIDATION_ERROR` with validation detail field
        `workId`; unknown work 404 `VENDOR_WORK_NOT_FOUND`; 403
        `BUILDING_ACCESS_DENIED` on the work's Building), after which the
        service re-resolves the authoritative context from the Work Order (404
        `WORK_ORDER_NOT_FOUND`, 400 `VENDOR_REWORK_BUILDING_MISMATCH`) and
        asserts Building access on it. Two business preconditions are enforced
        in order: the LATEST verification of the work must exist and be a
        `REWORK_REQUIRED` decision (400 `VENDOR_REWORK_INVALID_VERIFICATION`) -
        rework cannot be invented without a decision behind it - and no current
        `REQUESTED` cycle may already exist (409 `VENDOR_REWORK_ALREADY_OPEN`,
        also raised when the `vendor_current_rework_unique` index wins a race).
        The body accepts exactly one field, `reason` (required, non-empty after
        trimming, at most 2000 characters); `reviewId`, `vendorWorkId`, both
        actors and all timestamps are server-assigned, and the new cycle is
        anchored to the review id of that latest `REWORK_REQUIRED` verification.
        If that review has already produced a cycle the result is 409
        `VENDOR_REWORK_REVIEW_CONSUMED`: a fresh verification decision is
        required first. Requesting rework changes nothing else - it does not
        reopen the Vendor Work or the Work Order, does not reset or delete the
        completion report, evidence or history, does not reject a BAST, and
        touches no acceptance, invoice or finding surface.
      tags: [Vendor Rework]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VendorWorkPathId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RequestVendorReworkRequest"
      responses:
        "201":
          description: Rework cycle opened in REQUESTED state.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: object
                        required: [rework]
                        properties:
                          rework: { $ref: "#/components/schemas/VendorRework" }
        "400":
          description: >
            `VALIDATION_ERROR` - invalid `id`, non-object body, missing or blank
            `reason`, `reason` over 2000 - or `VENDOR_REWORK_INVALID_VERIFICATION`
            - no `REWORK_REQUIRED` decision is the latest verification - or
            `VENDOR_REWORK_BUILDING_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.manage` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Vendor work not found (`VENDOR_WORK_NOT_FOUND`) or its Work Order no
            longer exists (`WORK_ORDER_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `VENDOR_REWORK_ALREADY_OPEN` - a current `REQUESTED` cycle exists -
            or `VENDOR_REWORK_REVIEW_CONSUMED` - that review already produced a
            cycle and a new verification decision is required.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: getVendorReworkContext
      x-required-permission: vendor.read
      summary: Read the rework context of a Vendor Work
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04C2 (BE-15J). Requires `vendor.read`;
        no request body and no query parameters. Returns `VendorReworkContext`:
        the work's current `vendorWorkStatus`, the resolved `buildingId`, the
        single `current` cycle (the most recent one still in `REQUESTED`, or
        null) and the full append-preserving `cycles` history in the order the
        store returns them. Reading never mutates: no cycle is created,
        advanced, re-ordered or closed by this call, and an empty `cycles` array
        simply means the work has never been reworked. Guard order is the shared
        one: parse `id` (400 `VALIDATION_ERROR`, detail field `workId`), resolve
        the work (404 `VENDOR_WORK_NOT_FOUND`), Building access (403
        `BUILDING_ACCESS_DENIED`), then the authoritative Work Order context
        (404 `WORK_ORDER_NOT_FOUND`, 400 `VENDOR_REWORK_BUILDING_MISMATCH`).
        The context exposes rework bookkeeping only - it carries no acceptance,
        sign-off, BAST, invoice or closure state.
      tags: [Vendor Rework]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VendorWorkPathId"
      responses:
        "200":
          description: Rework context of the Vendor Work.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorReworkContext" }
        "400":
          description: >
            `VALIDATION_ERROR` - `id` must be a valid UUID - or
            `VENDOR_REWORK_BUILDING_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.read` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Vendor work not found (`VENDOR_WORK_NOT_FOUND`) or its Work Order no
            longer exists (`WORK_ORDER_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    patch:
      operationId: updateVendorReworkNotes
      x-required-permission: vendor.manage
      summary: Update the notes of the current rework cycle
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04C2 (BE-15J). Requires
        `vendor.manage`. Replaces the `reworkNotes` of the single CURRENT
        (REQUESTED) cycle and returns that updated cycle (200, data =
        `VendorRework`). The body is exactly `{ "notes": <string> }` and the
        value is required, non-empty after trimming and at most 2000 characters;
        it cannot be cleared, and no other field is writable - `reason`,
        `reviewId`, `vendorWorkId`, both actors, `status` and all timestamps are
        immutable here. Failure semantics are exact: a work with no current
        `REQUESTED` cycle is 404 `VENDOR_REWORK_NOT_FOUND` (a `RESUBMITTED`
        cycle is not "current" and can never be edited again), and a cycle that
        closed between the lookup and the guarded write is 409
        `VENDOR_REWORK_IMMUTABLE`. Editing notes does not resubmit anything,
        does not reopen or complete a Vendor Work, and does not alter the
        verification decision, the completion report, evidence or any BAST or
        acceptance state.
      tags: [Vendor Rework]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VendorWorkPathId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VendorReworkNotesRequest"
      responses:
        "200":
          description: Current rework cycle with updated notes.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/VendorRework" }
        "400":
          description: >
            `VALIDATION_ERROR` - invalid `id`, non-object body, missing or blank
            `notes`, `notes` over 2000 - or `VENDOR_REWORK_BUILDING_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.manage` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `VENDOR_REWORK_NOT_FOUND` - this vendor work has no current rework
            cycle - or `VENDOR_WORK_NOT_FOUND` / `WORK_ORDER_NOT_FOUND`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `VENDOR_REWORK_IMMUTABLE` - the cycle is no longer a current
            REQUESTED cycle.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /vendor-works/{id}/resubmit:
    post:
      operationId: resubmitVendorWork
      x-required-permission: vendor.manage
      summary: Resubmit a Vendor Work after rework
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04C2 (BE-15J). Requires
        `vendor.manage`. Closes the CURRENT rework cycle by marking it
        `RESUBMITTED` with `resubmittedByUserId` set to the authenticated caller
        and `resubmittedAt` set by the backend, replacing the cycle's
        `reworkNotes` with the required `notes`, and returns the closed cycle as
        `{ rework }` with 200. The body is exactly `{ "notes": <string> }` - required,
        non-empty after trimming, at most 2000 characters - and nothing else is
        accepted: no status, no timestamp, no evidence reference and no decision
        can be sent. A work with no current `REQUESTED` cycle is 404
        `VENDOR_REWORK_NOT_FOUND`; a cycle that was already closed concurrently
        is 409 `VENDOR_REWORK_IMMUTABLE`; the shared work / Work Order /
        Building guards are the same as the other rework routes (400
        `VALIDATION_ERROR`, 400 `VENDOR_REWORK_BUILDING_MISMATCH`, 403
        `BUILDING_ACCESS_DENIED`, 404 `VENDOR_WORK_NOT_FOUND` /
        `WORK_ORDER_NOT_FOUND`). Resubmission is bookkeeping only: previous
        cycles are preserved untouched, the Vendor Work's `status` is NOT
        reopened or changed, completion history and evidence are left as they
        are, and a new `REWORK_REQUIRED` verification (BE-15I) is required before
        another cycle can be opened. This operation performs no BAST,
        acceptance, sign-off, handover, invoice or Work Order closure action,
        and no automatic re-verification is triggered.
      tags: [Vendor Rework]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VendorWorkPathId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VendorReworkNotesRequest"
      responses:
        "200":
          description: Rework cycle closed as RESUBMITTED.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: object
                        required: [rework]
                        properties:
                          rework: { $ref: "#/components/schemas/VendorRework" }
        "400":
          description: >
            `VALIDATION_ERROR` - invalid `id`, non-object body, missing or blank
            `notes`, `notes` over 2000 - or `VENDOR_REWORK_BUILDING_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.manage` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `VENDOR_REWORK_NOT_FOUND` - no current rework cycle - or
            `VENDOR_WORK_NOT_FOUND` / `WORK_ORDER_NOT_FOUND`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `VENDOR_REWORK_IMMUTABLE` - the cycle is already RESUBMITTED and can
            never be modified again.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /vendor-works/{id}/verification:
    get:
      operationId: getVendorVerificationState
      x-required-permission: vendor.read
      summary: Read the verification state of a Vendor Work
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04C2 (BE-15I). Requires
        `vendor.read`; no request body and no query parameters. Returns
        `VendorVerificationState`: the shared BE-07 review rows for this Vendor
        Work (`verifications`, oldest first as stored) with `latestVerification`
        being the most recent one or null, plus the resolved completion / service
        / BAST context the reviewer needs. Guard order is exact: parse `id` (400
        `VALIDATION_ERROR` with validation detail field `vendorWorkId`), resolve
        the Vendor Work (404 `VENDOR_WORK_NOT_FOUND`), its Work Order (404
        `WORK_ORDER_NOT_FOUND`, authoritative for Client and Building), the
        Building equality check (400 `VENDOR_VERIFICATION_BUILDING_MISMATCH`) and
        only then the caller's Building access (403 `BUILDING_ACCESS_DENIED`).
        The context sub-objects are read-only references, and `bast` /
        `completionReport` / `serviceReport` are null when the corresponding
        record does not exist. This is a read: it never re-decides, re-opens or
        appends a verification, and the presence of an `acceptanceStatus` value
        here is display context only - this route neither reads nor writes any
        acceptance authority.
      tags: [Vendor Verification]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VendorWorkPathId"
      responses:
        "200":
          description: Verification state of the Vendor Work.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        { $ref: "#/components/schemas/VendorVerificationState" }
        "400":
          description: >
            `VALIDATION_ERROR` - `id` must be a valid UUID - or
            `VENDOR_VERIFICATION_BUILDING_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.read` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Vendor work not found (`VENDOR_WORK_NOT_FOUND`) or its Work Order no
            longer exists (`WORK_ORDER_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    post:
      operationId: submitVendorVerification
      x-required-permission: vendor.manage
      summary: Submit a verification decision for a completed Vendor Work
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04C2 (BE-15I). Requires
        `vendor.manage`. Appends one fresh BE-07 review row
        (`target_type = 'VENDOR_WORK'`) and returns 201 as
        `{ verification, vendorWorkStatus }`; a completed verification is never
        overwritten. The body accepts `decision` (required, one of `APPROVED`,
        `REJECTED`, `REWORK_REQUIRED`) and `notes` (optional, trimmed, at most
        4000 characters; empty or null is stored as null); `clientId`,
        `vendorWorkId`, `reviewerUserId` (from the session), `reviewedAt`,
        `status` and `id` are server-assigned and cannot be supplied. Guards,
        in this order: Vendor Work exists (404 `VENDOR_WORK_NOT_FOUND`), Work
        Order exists and its Building matches the work's (404
        `WORK_ORDER_NOT_FOUND`, 400 `VENDOR_VERIFICATION_BUILDING_MISMATCH`),
        caller holds Building access (403 `BUILDING_ACCESS_DENIED`), the Vendor
        Work is `COMPLETED` (400 `VENDOR_VERIFICATION_INVALID_STATE` - only
        completed work can be verified), a present Completion Report is
        `SUBMITTED` (400 `VENDOR_VERIFICATION_COMPLETION_NOT_SUBMITTED`; a
        missing report is not blocked by this rule), and the work has no earlier
        `APPROVED` review (409 `VENDOR_VERIFICATION_ALREADY_APPROVED` -
        approval is final and cannot be re-verified). A verification result is
        NOT acceptance and NOT closure: `APPROVED` does not accept a BAST, does
        not sign off, does not close the Work Order, does not approve an invoice
        and does not approve handover; `REWORK_REQUIRED` only records the
        decision - no rework cycle is created by this route,
        `POST /vendor-works/{id}/rework` owns that - and the Vendor Work's
        `status` is deliberately left unchanged, which is why the response
        echoes `vendorWorkStatus` unchanged. Rejection likewise revokes nothing
        and reverses nothing.
      tags: [Vendor Verification]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/VendorWorkPathId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubmitVendorVerificationRequest"
      responses:
        "201":
          description: Verification decision recorded.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: object
                        required: [verification, vendorWorkStatus]
                        properties:
                          verification:
                            { $ref: "#/components/schemas/VendorVerification" }
                          vendorWorkStatus:
                            type: string
                            description: >
                              The Vendor Work status as found - verification
                              never changes it.
        "400":
          description: >
            `VALIDATION_ERROR` - invalid `id`, non-object body, missing or
            unknown `decision`, `notes` over 4000 or not a string - or
            `VENDOR_VERIFICATION_INVALID_STATE` - the work is not `COMPLETED` -
            or `VENDOR_VERIFICATION_COMPLETION_NOT_SUBMITTED` - or
            `VENDOR_VERIFICATION_BUILDING_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `vendor.manage` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Vendor work not found (`VENDOR_WORK_NOT_FOUND`) or its Work Order no
            longer exists (`WORK_ORDER_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `VENDOR_VERIFICATION_ALREADY_APPROVED` - the work already carries an
            `APPROVED` review and cannot be re-verified.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 04D1 - J12 Acceptance Sign-Offs (BE-22E).
  #
  # Contract exposure of the ALREADY REGISTERED BE-22E runtime routes only.
  # No runtime, lifecycle or authorization behaviour is added or changed here.
  # Work-completion documents and handover documents stay out of scope in this
  # PART, the pre-existing BAST / bast-documents contracts are not modified, and
  # no generic approval or sign-off engine is introduced.
  # ---------------------------------------------------------------------------

  /acceptance-sign-offs:
    post:
      operationId: submitAcceptanceSignOff
      x-required-permission: document.manage
      summary: Record an acceptance sign-off on a handover document
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04D1 (BE-22E). Appends one
        Acceptance Sign-Off row and returns it with 201. Requires
        `document.manage`; the session is checked before the body is parsed, so
        an unauthenticated caller gets 401 rather than a validation error. The
        body must carry EXACTLY ONE of `bastDocumentId` / `handoverDocumentId`
        (400 `VALIDATION_ERROR` naming field `bastDocumentId` when both or
        neither is present; an explicit null counts as absent) plus the required
        `decision`. BAST targets are not accepted by this route: any
        `bastDocumentId` is rejected first with 400
        `ACCEPTANCE_INVALID_CONTEXT` and the message "BAST decisions must use
        the canonical /bast-documents/:id/decisions command", so the pre-existing
        canonical BAST decision command stays the single BAST authority and this
        module neither reads nor writes BAST lifecycle state. For a handover
        target the sign-off then copies `clientId`, `buildingId` and
        `contextType` from the resolved Handover document (404
        `ACCEPTANCE_DOCUMENT_NOT_FOUND` when the target or its underlying
        document row is unknown; 409 `DOCUMENT_UPDATE_NOT_ALLOWED` when that
        document is `ARCHIVED`), asserts the caller's Building access (403
        `BUILDING_ACCESS_DENIED`) and re-validates the signer - which is always
        the authenticated actor, because the body has no signer field - as an
        ACTIVE user with access to that Building (403
        `ACCEPTANCE_UNAUTHORIZED_SIGNER`). `id`, `signedAt` and both record
        timestamps are backend-assigned, `notes` is optional, and
        `bastSubmissionAttemptId` / `documentVersionId` are always null because
        no registered route populates them. One target may be signed off more
        than once - history is append-preserving and no route edits, replaces,
        revokes or deletes a sign-off. Recording a sign-off is only that: it does
        not accept or mutate a BAST, does not close a Work Order, does not
        complete a Vendor Work, does not approve an invoice or a payment, does
        not complete a handover and triggers no generic approval workflow; the
        only side effect is one operational event
        (`ACCEPTANCE_SIGNED_ACCEPTED` / `ACCEPTANCE_SIGNED_REJECTED`).
      tags: [Acceptance Sign-Off]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubmitAcceptanceSignOffRequest"
      responses:
        "201":
          description: Sign-off recorded.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/AcceptanceSignOff" }
        "400":
          description: >
            `VALIDATION_ERROR` - non-object body, missing or unknown `decision`,
            a non-UUID or missing target, both targets supplied, or `notes` over
            4000 - or `ACCEPTANCE_INVALID_CONTEXT` - a `bastDocumentId` target is
            rejected in favour of the canonical BAST decision command.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.manage` is missing -
            `BUILDING_ACCESS_DENIED` for the target's Building - or
            `ACCEPTANCE_UNAUTHORIZED_SIGNER` when the acting user is not an
            ACTIVE user with access to that Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Target Handover document or its underlying document row not found
            (`ACCEPTANCE_DOCUMENT_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `DOCUMENT_UPDATE_NOT_ALLOWED` - the target document is `ARCHIVED`
            and can no longer carry a sign-off.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listAcceptanceSignOffs
      x-required-permission: document.read
      summary: List acceptance sign-offs
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04D1 (BE-22E). Requires
        `document.read`; no request body. Unlike the other J12 list routes this
        one has NO at-least-one-filter rule: every filter is optional and an
        empty query is valid, returning the caller's whole visible sign-off
        history. `data` is a plain, non-paginated array ordered by `signedAt`
        then `createdAt`, both descending (newest first). Rows are restricted to
        those in a Building the caller can access, or - for the rows that carry
        no Building - to an accessible Client, and an empty array is returned
        when the caller reaches neither. Target filters are validated before
        scoping: `bastDocumentId` / `handoverDocumentId` must resolve (404
        `ACCEPTANCE_DOCUMENT_NOT_FOUND`) and their Building must be accessible
        (403 `BUILDING_ACCESS_DENIED`), `buildingId` asserts access to that
        Building, and `clientId` asserts it is one of the caller's accessible
        Clients (403 `BUILDING_ACCESS_DENIED`). Reading is never a decision:
        no sign-off is created, revised, revoked or re-verified, and BAST-linked
        rows remain readable here even though BAST sign-offs cannot be created
        through this route.
      tags: [Acceptance Sign-Off]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: bastDocumentId
          in: query
          required: false
          description: Filter by canonical BAST document id. Read-only filter - this route cannot create BAST sign-offs.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: handoverDocumentId
          in: query
          required: false
          description: Filter by Handover document id.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          description: Filter by Building id. Access to that Building is asserted when supplied.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: clientId
          in: query
          required: false
          description: Filter by Client id. The Client must be one of the caller's accessible Clients.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: contextType
          in: query
          required: false
          description: "Filter by sign-off context. Case-insensitive: trimmed and upper-cased before validation."
          schema:
            type: string
            enum: [INTERNAL, TENANT, VENDOR]
        - name: decision
          in: query
          required: false
          description: "Filter by recorded decision. Case-insensitive: trimmed and upper-cased before validation."
          schema:
            type: string
            enum: [ACCEPTED, REJECTED]
        - name: signerUserId
          in: query
          required: false
          description: Filter by the user whose signature was recorded.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Acceptance sign-offs.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/AcceptanceSignOff" }
        "400":
          description: >
            `VALIDATION_ERROR` - a filter that is not a valid UUID, or a
            `contextType` / `decision` outside the accepted values.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.read` is missing - or
            `BUILDING_ACCESS_DENIED` for the requested Building or Client scope.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `ACCEPTANCE_DOCUMENT_NOT_FOUND` - the `bastDocumentId` or
            `handoverDocumentId` filter does not resolve.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /acceptance-sign-offs/{id}:
    get:
      operationId: getAcceptanceSignOff
      x-required-permission: document.read
      summary: Get one acceptance sign-off
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04D1 (BE-22E). Requires
        `document.read`; no request body and no query parameters. Guard order is
        exact and observable: a non-UUID `id` is 400 `VALIDATION_ERROR`
        (validation detail field `id`, value trimmed and lower-cased), an unknown
        id is 404 `ACCEPTANCE_SIGN_OFF_NOT_FOUND` - checked before access, so
        existence is reported ahead of authorization - and only then is the
        sign-off's own `buildingId` used for Building isolation (403
        `BUILDING_ACCESS_DENIED`). The response is the stored sign-off exactly
        as recorded: a decision cannot be changed, revoked or re-signed through
        this route, and the row is the audit artifact rather than the live state
        of any BAST, Handover, Work Order or Vendor Work surface.
      tags: [Acceptance Sign-Off]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/AcceptanceSignOffIdPath"
      responses:
        "200":
          description: Acceptance sign-off.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/AcceptanceSignOff" }
        "400":
          description: >
            `VALIDATION_ERROR` - `id` must be a valid UUID.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.read` is missing - or
            `BUILDING_ACCESS_DENIED`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            Acceptance sign-off not found (`ACCEPTANCE_SIGN_OFF_NOT_FOUND`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 04D2 - J12 Work Completion Documents
  # (BE-22B).
  #
  # Contract exposure of the ALREADY REGISTERED BE-22B runtime routes only.
  # No runtime, lifecycle or authorization behaviour is added or changed here.
  # ---------------------------------------------------------------------------
  /work-completion-documents:
    post:
      operationId: createWorkCompletionDocument
      x-required-permission: document.manage
      summary: Attach a shared Document to a completed work context
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04D2 (BE-22B). Creates one shared
        BE-22A Document and the Work Completion Document binding that attaches
        it to a Work Order, returning the binding with the created document
        embedded and status 201. Requires `document.manage`; the session is
        checked before the body is parsed, so an unauthenticated caller gets 401
        rather than a validation error. `id`, `createdByUserId` and both record
        timestamps are backend-assigned and are not accepted from callers.

        The checks run in this runtime order, any of which can fail the request:
        the Client must exist (404 `CLIENT_NOT_FOUND`), the Building must exist
        (404 `BUILDING_NOT_FOUND`) and its Property must belong to that Client
        (400 `WORK_COMPLETION_BUILDING_MISMATCH`), and the actor must have access
        to that Building (403 `BUILDING_ACCESS_DENIED`). The work context is
        validated next so an unusable reference is reported first: the Work Order
        must resolve (404 `WORK_COMPLETION_WORK_NOT_FOUND`) and already be
        `COMPLETED` (400 `WORK_COMPLETION_WORK_NOT_COMPLETED`), its Client must
        equal `clientId` (400 `WORK_COMPLETION_CONTEXT_MISMATCH`, "Document
        client does not match work client.") and its Building must equal
        `buildingId` (400 `WORK_COMPLETION_BUILDING_MISMATCH`). A supplied
        `vendorWorkId` must resolve (404 `WORK_COMPLETION_WORK_NOT_FOUND`),
        belong to that Work Order (400 `WORK_COMPLETION_CONTEXT_MISMATCH`),
        share its Building (400 `WORK_COMPLETION_BUILDING_MISMATCH`) and be
        `COMPLETED` as well (400 `WORK_COMPLETION_WORK_NOT_COMPLETED`); in a
        `VENDOR` context its Vendor must also equal `sourceId`. The declared
        source is then resolved against the shared-document rules (404
        `DOCUMENT_SOURCE_NOT_FOUND`, 400 `DOCUMENT_SOURCE_CLIENT_MISMATCH`, 400
        `WORK_COMPLETION_CONTEXT_MISMATCH`) and `documentNumber` must still be
        free inside that Client (409 `DOCUMENT_NUMBER_ALREADY_EXISTS`).

        A completed work context is an input, never an output: nothing is written
        to the Work Order or the Vendor Work, no BAST, Acceptance Sign-Off,
        Handover Document, Vendor Completion Report, invoice or payment record is
        created or mutated, and no acceptance, verification or closure is implied
        by the response. The only side effects are the document row, the binding
        row and one operational event (`WORK_COMPLETION_DOCUMENT_CREATED`,
        summary "Work completion document <documentNumber> bound to work
        <workOrderId>"). If the binding insert collides with the document-id
        unique constraint, the just-created document row is rolled back and 409
        `WORK_COMPLETION_ALREADY_EXISTS` is returned - so that guard is per
        underlying Document, and a Work Order is not limited to one completion
        document by it.
      tags: [Work Completion Documents]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWorkCompletionDocumentRequest"
      responses:
        "201":
          description: Work completion document created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkCompletionDocument" }
        "400":
          description: >
            `VALIDATION_ERROR` - non-object body, a missing or malformed field
            (each detail names its field), a `documentType` outside
            `[A-Z][A-Z0-9_-]*` of 2-64 characters after upper-casing, a
            `documentNumber` / `title` over 64 / 255 characters, `description`
            over 2000 or `fileReference` over 512 characters, a
            `contextType` / `sourceType` / `status` outside its enum, only one of
            `sourceType` / `sourceId` supplied, a `sourceType` that does not pair
            with `contextType`, or a null `status` - or a precondition failure
            already reported as `WORK_COMPLETION_WORK_NOT_COMPLETED`,
            `WORK_COMPLETION_CONTEXT_MISMATCH` or
            `WORK_COMPLETION_BUILDING_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.manage` is missing - or
            `BUILDING_ACCESS_DENIED` for `buildingId`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `CLIENT_NOT_FOUND`, `BUILDING_NOT_FOUND`,
            `WORK_COMPLETION_WORK_NOT_FOUND` for an unknown Work Order or Vendor
            Work, or `DOCUMENT_SOURCE_NOT_FOUND` for an unresolved TENANT or
            VENDOR source.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `DOCUMENT_NUMBER_ALREADY_EXISTS` for a `documentNumber` already used
            in that Client, `WORK_COMPLETION_ALREADY_EXISTS` when the binding
            collides with the document-id unique constraint (the created document
            is rolled back), or `DOCUMENT_UPDATE_NOT_ALLOWED` when the request
            asks for the `ARCHIVED` status, which only the shared foundation's
            own archive action may set and which is not exposed on this route.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listWorkCompletionDocuments
      x-required-permission: document.read
      summary: List work completion documents
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04D2 (BE-22B). Requires
        `document.read`; no request body and no at-least-one-filter rule - all
        seven filters below are optional and an empty query returns the whole
        visible set. `data` is a plain, non-paginated array ordered by the
        binding's `createdAt` descending (newest first). Rows are restricted to
        those in a Building the caller can access, or - for bindings that carry
        no Building - to an accessible Client, and an empty array is returned
        when the caller reaches neither; scope is enforced by that SQL predicate
        alone, without a per-row access check, and an inaccessible binding simply
        does not appear rather than answering 403. Filters are validated before
        scoping: the UUID ones must be valid UUIDs, `contextType` and `status`
        are upper-cased then enum-checked, `documentType` is upper-cased and
        pattern-checked (400 `VALIDATION_ERROR`, detail "documentType is
        invalid."), `buildingId` / `clientId` must be accessible (403
        `BUILDING_ACCESS_DENIED`), and `workOrderId` / `vendorWorkId` must
        resolve (404 `WORK_COMPLETION_WORK_NOT_FOUND`) before their Building is
        access-checked. `documentType` and `status` filter on the embedded
        shared document, not on the binding. Reading is never a lifecycle
        action: no document is created, revised, approved, archived or bound,
        and Work Order / Vendor Work status is not touched.
      tags: [Work Completion Documents]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: workOrderId
          in: query
          required: false
          description: Filter by Work Order id; must resolve and be in an accessible Building.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorWorkId
          in: query
          required: false
          description: Filter by Vendor Work id; must resolve and be in an accessible Building.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          description: Filter by Building id. Access to that Building is asserted when supplied.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: clientId
          in: query
          required: false
          description: Filter by Client id. It must be one of the caller's accessible Clients.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: contextType
          in: query
          required: false
          description: Filter by the binding's document context (case-insensitive on input).
          schema: { type: string, enum: [INTERNAL, TENANT, VENDOR] }
        - name: documentType
          in: query
          required: false
          description: >
            Filter by the shared document's type. Upper-cased and matched
            exactly; same 2-64 character `[A-Z][A-Z0-9_-]*` shape as the body.
          schema: { type: string, maxLength: 64, pattern: "^[A-Z][A-Z0-9_-]{1,63}$" }
        - name: status
          in: query
          required: false
          description: >
            Filter by the shared document's status (case-insensitive on input).
            A filter only - this route cannot change a status.
          schema: { type: string, enum: [DRAFT, ACTIVE, INACTIVE, ARCHIVED] }
      responses:
        "200":
          description: Work completion documents visible to the caller (possibly empty).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/WorkCompletionDocument" }
        "400":
          description: >
            `VALIDATION_ERROR` - a malformed UUID filter, an empty or invalid
            `documentType`, or an unknown `contextType` / `status`; each detail
            names its field. Unknown query parameters are ignored.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.read` is missing - or
            `BUILDING_ACCESS_DENIED` for the filtered Building, for the Building
            of the filtered Work Order / Vendor Work, or because the filtered
            Client is not accessible.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `WORK_COMPLETION_WORK_NOT_FOUND` - the `workOrderId` or
            `vendorWorkId` filter does not resolve.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
  /work-completion-documents/{id}:
    get:
      operationId: getWorkCompletionDocument
      x-required-permission: document.read
      summary: Read one work completion document
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04D2 (BE-22B). Requires
        `document.read`. Returns the binding row with its shared document
        projection embedded and status 200. The id is trimmed, lower-cased and
        must be a valid UUID, otherwise 400 `VALIDATION_ERROR` with detail
        "Work completion document id must be a valid UUID.". Existence is
        checked before access: an unknown id is 404
        `WORK_COMPLETION_DOCUMENT_NOT_FOUND` while a known but inaccessible one
        is 403 `BUILDING_ACCESS_DENIED` for the binding's Building; a binding
        whose underlying document row no longer exists is 404 as well. No
        Work Order, Vendor Work, BAST, Acceptance Sign-Off, Handover Document
        or Vendor Completion Report object is embedded or resolved, and reading
        records nothing and changes nothing.
      tags: [Work Completion Documents]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/WorkCompletionDocumentIdPath"
      responses:
        "200":
          description: The requested work completion document.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/WorkCompletionDocument" }
        "400":
          description: "`VALIDATION_ERROR` - the path id is not a valid UUID."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.read` is missing - or
            `BUILDING_ACCESS_DENIED` for the binding's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `WORK_COMPLETION_DOCUMENT_NOT_FOUND` - no such binding, or its
            underlying shared document row is gone.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 04D3 - J12 Handover Documents (BE-22D).
  #
  # Contract exposure of the ALREADY REGISTERED BE-22D runtime routes only.
  # No runtime, lifecycle or authorization behaviour is added or changed here.
  # ---------------------------------------------------------------------------
  /handover-documents:
    post:
      operationId: createHandoverDocument
      x-required-permission: document.manage
      summary: Attach a shared Document to a completed work context as a handover record
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04D3 (BE-22D). Creates one shared
        BE-22A Document plus the Handover Document binding that attaches it to a
        Work Order, returning the binding with the created document embedded and
        status 201. Requires `document.manage`; the session is checked before the
        body is parsed, so an unauthenticated caller gets 401 rather than a
        validation error. `id`, `preparedByUserId` and the record timestamps are
        backend-assigned; `handoverStatus`, `handedOverByUserId` and
        `handedOverAt` are not accepted from callers and are never written by
        this route, so a created row reads back as `DRAFT` with both handover
        actor fields null.

        The checks run in this runtime order, any of which can fail the request:
        the Client must exist (404 `CLIENT_NOT_FOUND`), the Building must exist
        (404 `BUILDING_NOT_FOUND`) and its Property must belong to that Client
        (400 `HANDOVER_BUILDING_MISMATCH`), and the actor must have access to
        that Building (403 `BUILDING_ACCESS_DENIED`). The work context is then
        validated so an unusable reference is reported first: the Work Order
        must resolve (404 `HANDOVER_WORK_NOT_FOUND`) and already be `COMPLETED`
        (400 `HANDOVER_WORK_NOT_COMPLETED`, "Work is not completed and cannot
        have a handover."), its Client must equal `clientId` (400
        `HANDOVER_CONTEXT_MISMATCH`, "Handover client does not match work
        client.") and its Building must equal `buildingId` (400
        `HANDOVER_BUILDING_MISMATCH`). A supplied `vendorWorkId` must resolve
        (404 `HANDOVER_WORK_NOT_FOUND`), belong to that Work Order, match its
        Building and already be `COMPLETED` (400 `HANDOVER_CONTEXT_MISMATCH` /
        `HANDOVER_BUILDING_MISMATCH` / `HANDOVER_WORK_NOT_COMPLETED`), and in a
        `VENDOR` context with a `sourceId` its Vendor must be that same vendor.
        A supplied `workCompletionDocumentId` must resolve (404
        `HANDOVER_WORK_NOT_FOUND`), belong to the Work Order and share its
        Client, Building and - where both sides name one - Vendor Work (400
        `HANDOVER_CONTEXT_MISMATCH` / `HANDOVER_BUILDING_MISMATCH`). A supplied
        `bastDocumentId` must resolve (404 `HANDOVER_BAST_NOT_FOUND`), belong to
        the Work Order (400 `HANDOVER_BAST_MISMATCH`), share its Building (400
        `HANDOVER_BUILDING_MISMATCH`) and Client (400
        `HANDOVER_CONTEXT_MISMATCH`) and align with `vendorWorkId` when both are
        present; the BAST's own status is never checked here, so this route
        neither requires nor grants BAST acceptance. Declared-source rules then
        apply (400 `HANDOVER_CONTEXT_MISMATCH`, 404 `DOCUMENT_SOURCE_NOT_FOUND`,
        400 `DOCUMENT_SOURCE_CLIENT_MISMATCH`), `handoverNumber` must be unused
        inside the Client (409 `HANDOVER_NUMBER_ALREADY_EXISTS`), a Vendor Work
        that already has a handover is refused (409 `HANDOVER_ALREADY_EXISTS`)
        and `documentNumber` must still be free inside that Client (409
        `DOCUMENT_NUMBER_ALREADY_EXISTS`).

        Nothing outside this module is written: the Work Order, Vendor Work,
        Work Completion Document and BAST rows keep their own values, no
        Acceptance Sign-Off, Vendor Completion Report, invoice or payment record
        is created or mutated, and no completion, acceptance, verification or
        closure is implied by the response. The only side effects are the
        document row, the handover row and one operational event
        (`HANDOVER_DOCUMENT_CREATED`, summary "Handover <handoverNumber> created
        for work <workOrderId>"). If the insert fails on a unique constraint,
        the just-created document row is rolled back and the violation is
        reported as 409 `HANDOVER_NUMBER_ALREADY_EXISTS` or 409
        `HANDOVER_ALREADY_EXISTS`. Uniqueness on this table is exactly threefold:
        `handover_number` per Client, one handover per shared Document
        (`document_id` unique) and one handover per Vendor Work (a partial unique
        index, so it bites only when a `vendorWorkId` is named). Multiple handover
        rows for one Work Order are therefore allowed whenever they do not repeat
        a Client number, a document or a Vendor Work.
      tags: [Handover Documents]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateHandoverDocumentRequest"
      responses:
        "201":
          description: Handover document created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HandoverDocument" }
        "400":
          description: >
            `VALIDATION_ERROR` - non-object body, a missing or malformed field
            (each detail names its field), a `handoverNumber` over 100
            characters, a `handoverDate` that is not a valid `YYYY-MM-DD` date,
            a `documentNumber` / `title` over 64 / 255 characters,
            `description` / `notes` / `fileReference` over 2000 / 4000 / 512
            characters, a `documentType` outside `[A-Z][A-Z0-9_-]*` of 2-64
            characters after upper-casing, a `contextType` / `sourceType` /
            `status` outside its enum (null `status` is not accepted), only one
            of `sourceType` / `sourceId` supplied, or a `sourceType` that does
            not pair with `contextType` - or a context failure already reported
            as `HANDOVER_WORK_NOT_COMPLETED`, `HANDOVER_CONTEXT_MISMATCH` or
            `HANDOVER_BUILDING_MISMATCH`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.manage` is missing - or
            `BUILDING_ACCESS_DENIED` for `buildingId`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `CLIENT_NOT_FOUND`, `BUILDING_NOT_FOUND`,
            `HANDOVER_WORK_NOT_FOUND` for an unknown Work Order, Vendor Work or
            Work Completion Document, `HANDOVER_BAST_NOT_FOUND` for an unknown
            BAST, or `DOCUMENT_SOURCE_NOT_FOUND` for an unresolved TENANT or
            VENDOR source.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `HANDOVER_NUMBER_ALREADY_EXISTS` for a `handoverNumber` already used
            in that Client, `HANDOVER_ALREADY_EXISTS` when that Vendor Work
            already has a handover document, `DOCUMENT_NUMBER_ALREADY_EXISTS` for
            a `documentNumber` already used in that Client, or
            `DOCUMENT_UPDATE_NOT_ALLOWED` when the request asks for the
            `ARCHIVED` document status, which only the shared foundation's own
            archive action may set and which is not exposed on this route.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listHandoverDocuments
      x-required-permission: document.read
      summary: List handover documents
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04D3 (BE-22D). Requires
        `document.read`; no request body and no at-least-one-filter rule - all
        ten filters below are optional and an empty query returns the whole
        visible set. `data` is a plain, non-paginated array ordered by the
        binding's `createdAt` descending (newest first). Rows are restricted to
        those in a Building the caller can access, or - for bindings that carry
        no Building - to an accessible Client, and an empty array is returned
        when the caller reaches neither; that SQL predicate is the only scope
        check, so an inaccessible row never appears rather than answering 403.
        Filters are validated before scoping: the UUID ones must be valid UUIDs,
        `contextType` / `handoverStatus` / `status` are upper-cased then
        enum-checked, `handoverNumber` must be non-empty and at most 100
        characters and is matched exactly as trimmed (no case folding),
        `buildingId` / `clientId` must be accessible (403
        `BUILDING_ACCESS_DENIED`), and `workOrderId` / `vendorWorkId` /
        `workCompletionDocumentId` must resolve (404 `HANDOVER_WORK_NOT_FOUND`)
        while `bastDocumentId` must resolve (404 `HANDOVER_BAST_NOT_FOUND`), each
        followed by an access check on the resolved row's Building. `status`
        filters on the embedded shared document, not on the handover row;
        `handoverStatus` filters on the handover row. Reading is never a
        lifecycle action: no document is created or revised, no handover is
        marked complete, and no referenced module's state is touched.
      tags: [Handover Documents]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: workOrderId
          in: query
          required: false
          description: Filter by Work Order id; must resolve and be in an accessible Building.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: vendorWorkId
          in: query
          required: false
          description: Filter by Vendor Work id; must resolve and be in an accessible Building.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: workCompletionDocumentId
          in: query
          required: false
          description: >
            Filter by the referenced Work Completion Document (BE-22B) id; must
            resolve and be in an accessible Building. A pointer filter only - it
            neither reads nor changes that document's state.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: bastDocumentId
          in: query
          required: false
          description: >
            Filter by the referenced BAST document id; must resolve and be in an
            accessible Building. Reading a handover never accepts or mutates the
            BAST.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          description: Filter by Building id. Access to that Building is asserted when supplied.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: clientId
          in: query
          required: false
          description: Filter by Client id. It must be one of the caller's accessible Clients.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: contextType
          in: query
          required: false
          description: Filter by the binding's document context (case-insensitive on input).
          schema: { $ref: "#/components/schemas/DocumentContextType" }
        - name: handoverStatus
          in: query
          required: false
          description: >
            Filter by the handover row's own status (case-insensitive on input).
            A filter only - no route on this contract transitions a row into
            `HANDED_OVER`.
          schema: { $ref: "#/components/schemas/HandoverStatus" }
        - name: handoverNumber
          in: query
          required: false
          description: Exact match on the handover number, trimmed, at most 100 characters.
          schema: { type: string, maxLength: 100 }
        - name: status
          in: query
          required: false
          description: >
            Filter by the shared document's status (case-insensitive on input).
            A filter only - this route cannot change a status.
          schema: { $ref: "#/components/schemas/DocumentStatus" }
      responses:
        "200":
          description: Handover documents visible to the caller (possibly empty).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/HandoverDocument" }
        "400":
          description: >
            `VALIDATION_ERROR` - a malformed UUID filter, an unknown
            `contextType` / `handoverStatus` / `status`, or an empty or over-long
            `handoverNumber`; each detail names its field. Unknown query
            parameters are ignored, and filters that parse to an empty string
            are dropped instead of narrowing the result.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.read` is missing - or
            `BUILDING_ACCESS_DENIED` for the filtered Building, for the Building
            of the filtered Work Order / Vendor Work / Work Completion Document /
            BAST, or because the filtered Client is not accessible.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `HANDOVER_WORK_NOT_FOUND` - the `workOrderId`, `vendorWorkId` or
            `workCompletionDocumentId` filter does not resolve - or
            `HANDOVER_BAST_NOT_FOUND` for an unresolvable `bastDocumentId`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
  /handover-documents/{id}:
    get:
      operationId: getHandoverDocument
      x-required-permission: document.read
      summary: Read one handover document
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 04D3 (BE-22D). Requires
        `document.read`. Returns the handover row with its shared document
        projection embedded and status 200. The id is trimmed, lower-cased and
        must be a valid UUID, otherwise 400 `VALIDATION_ERROR` with detail
        "Handover document id must be a valid UUID.". Existence is checked
        before access: an unknown id is 404 `HANDOVER_DOCUMENT_NOT_FOUND` while a
        known but inaccessible one is 403 `BUILDING_ACCESS_DENIED` for the
        binding's Building; a binding whose underlying document row no longer
        exists is 404 as well. The referenced Work Order, Vendor Work, Work
        Completion Document and BAST are not resolved or embedded, so their
        current state cannot be read through this response, and reading records
        nothing and changes nothing - it never marks a handover as completed.
      tags: [Handover Documents]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/HandoverDocumentIdPath"
      responses:
        "200":
          description: The requested handover document.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/HandoverDocument" }
        "400":
          description: "`VALIDATION_ERROR` - the path id is not a valid UUID."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.read` is missing - or
            `BUILDING_ACCESS_DENIED` for the binding's Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `HANDOVER_DOCUMENT_NOT_FOUND` - no such handover row, or its
            underlying shared document row is gone.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B1 - J19 Documents (BE-22A).
  #
  # Contract exposure of the ALREADY REGISTERED BE-22A Document foundation
  # runtime routes only. No runtime, lifecycle, storage or authorization
  # behaviour is added or changed here.
  # ---------------------------------------------------------------------------

  /documents:
    post:
      operationId: createDocument
      x-required-permission: document.manage
      summary: Create a shared Document
      description: >
        MUTATION - CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B1 (BE-22A). Creates one
        generic Document row and returns it with status 201. Requires
        `document.manage`; the bearer session is asserted before the body is
        parsed, so an unauthenticated caller gets 401 rather than a validation
        error. `id`, `createdByUserId`, `archivedAt`, `statusBeforeArchive`,
        `createdAt` and `updatedAt` are backend-assigned and are not accepted
        from callers.

        The checks run in this runtime order, any of which can fail the request
        (body validation happens before all of them, so a malformed body is 400
        even when a lifecycle guard would also fire): a requested `ARCHIVED`
        status is refused first (409
        `DOCUMENT_UPDATE_NOT_ALLOWED`, because only the dedicated archive action
        may enter that state), then the Client must exist (404
        `CLIENT_NOT_FOUND`); with a `buildingId` the Building must exist (404
        `BUILDING_NOT_FOUND`) and its Property must belong to that Client (400
        `DOCUMENT_BUILDING_MISMATCH`) and be accessible to the actor, while
        without one the actor must at least be able to act for that Client; the
        context/source pair is then resolved against the foundation rules (400
        `DOCUMENT_CONTEXT_INVALID`, 404 `DOCUMENT_SOURCE_NOT_FOUND`, 400
        `DOCUMENT_SOURCE_CLIENT_MISMATCH`) and `documentNumber` must still be free
        inside that Client (409 `DOCUMENT_NUMBER_ALREADY_EXISTS`).

        The only side effects are the document row, one `DOCUMENT_CREATED`
        operational event on entity type `DOCUMENT`, and the foundation's
        immutable version-1 history row written in the same request - a
        `document_versions` record that no route on this contract part exposes as
        an operation, lists or reads. Nothing is uploaded: `fileReference` is
        stored exactly as supplied, and no expiry, approval, evidence or
        retention state is created here. Creating a document never creates,
        links or mutates a Work Order, Vendor Work, BAST, Handover Document,
        Acceptance Sign-Off, tenant or vendor record.
      tags: [Documents]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateDocumentRequest"
      responses:
        "201":
          description: Document created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/DocumentFoundation" }
        "400":
          description: >
            `VALIDATION_ERROR` - the body is not a JSON object, a required field
            is missing or blank, a UUID field is malformed, `documentNumber` /
            `title` exceed 64 / 255 characters, `description` exceeds 2000 or
            `fileReference` exceeds 512, `documentType` is outside 2-64 characters
            of `[A-Z][A-Z0-9_-]*` after upper-casing, `contextType` / `sourceType`
            / `status` is outside its enum (a null `status` included), only one of
            `sourceType` / `sourceId` is supplied, `sourceType` does not pair with
            `contextType`, or a `TENANT` / `VENDOR` context is declared without
            its source - each detail names its field. Also 400
            `DOCUMENT_BUILDING_MISMATCH` when the Building's Property belongs to
            another Client, `DOCUMENT_CONTEXT_INVALID` for an unresolvable or
            mismatched source, and `DOCUMENT_SOURCE_CLIENT_MISMATCH` when the
            Tenant Company or Vendor sits under another Client.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.manage` is missing - or
            `BUILDING_ACCESS_DENIED` because the request's Building is not
            accessible to the actor, or, with no Building, because the Client is
            not one of the actor's accessible Clients.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `CLIENT_NOT_FOUND` for an unknown Client, `BUILDING_NOT_FOUND` for an
            unknown Building, or `DOCUMENT_SOURCE_NOT_FOUND` for a
            `TENANT_COMPANY` / `VENDOR` source id that does not resolve.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `DOCUMENT_NUMBER_ALREADY_EXISTS` - `documentNumber` is already used
            inside that Client; it is checked before insert and additionally
            guarded by the `documents_client_number_unique` constraint, so a
            concurrent duplicate is reported the same way - or
            `DOCUMENT_UPDATE_NOT_ALLOWED` when the request asks for the `ARCHIVED`
            status.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listDocuments
      x-required-permission: document.read
      summary: List documents visible to the caller
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B1 (BE-22A). Requires
        `document.read`; no request body and no at-least-one-filter rule - all
        five filters below are optional and an empty query returns the whole
        visible set. `data` is a plain, NON-PAGINATED array ordered by
        `createdAt` descending and then `documentNumber` ascending; there is no
        `page` / `pageSize` handling on this route, so `meta` is an empty object.

        Rows are restricted by a SQL scope clause to documents in a Building the
        caller can access, or - for documents that carry no Building - to an
        accessible Client; when the caller reaches neither, an empty array is
        returned and an out-of-scope document simply does not appear rather than
        answering 403. Filters are validated first (400 `VALIDATION_ERROR`, each
        detail naming its field; unknown query parameters are ignored), then a
        filtered Building or Client is access-checked (403
        `BUILDING_ACCESS_DENIED`). Archived documents are NOT hidden here:
        `status` is only a filter, so `ARCHIVED` rows are returned unless they are
        filtered out, and reading never changes a status, an archive state or any
        adjacent module.
      tags: [Documents]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: documentType
          in: query
          required: false
          description: >
            Filter by type code. Upper-cased and matched exactly against the
            stored value; 2-64 characters of `[A-Z][A-Z0-9_-]*`, otherwise 400
            with detail "documentType is invalid.".
          schema: { type: string, maxLength: 64, pattern: "^[A-Z][A-Z0-9_-]{1,63}$" }
        - name: contextType
          in: query
          required: false
          description: Filter by document context (case-insensitive on input).
          schema: { $ref: "#/components/schemas/DocumentContextType" }
        - name: buildingId
          in: query
          required: false
          description: >
            Filter by Building. Must be a valid UUID and must be accessible to
            the caller. Matches only documents that carry that Building.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          description: >
            Filter by document status (case-insensitive on input). A filter only
            - this route cannot change a status, archive or restore anything.
          schema: { $ref: "#/components/schemas/DocumentStatus" }
        - name: clientId
          in: query
          required: false
          description: >
            Filter by owner Client. Must be a valid UUID and one of the caller's
            accessible Clients; it matches documents by Client ownership
            independently of the Building scope clause.
          schema: { $ref: "#/components/schemas/Uuid" }
      responses:
        "200":
          description: Documents visible to the caller (possibly empty).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/DocumentFoundation" }
        "400":
          description: >
            `VALIDATION_ERROR` - a malformed UUID in `buildingId` / `clientId`, an
            empty or invalid `documentType`, or an unknown `contextType` /
            `status`; each detail names its field.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.read` is missing - or
            `BUILDING_ACCESS_DENIED` for the filtered Building or for a filtered
            Client the caller cannot act for.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /documents/{id}:
    get:
      operationId: getDocument
      x-required-permission: document.read
      summary: Read one document
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B1 (BE-22A). Requires
        `document.read` and returns the stored document exactly as the foundation
        projects it. The row is resolved before isolation is checked, so an
        unknown id is 404 `DOCUMENT_NOT_FOUND` while a known but inaccessible one
        is 403 `BUILDING_ACCESS_DENIED` (Building isolation when the document
        carries a Building, otherwise Client isolation). Any status is readable,
        including `ARCHIVED`: archiving hides nothing and revokes nothing here.
        The read writes nothing - no view event, no lifecycle movement, no
        version, expiry, approval, evidence or retention state is touched.
      tags: [Documents]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DocumentIdPath"
      responses:
        "200":
          description: The requested document.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/DocumentFoundation" }
        "400":
          description: >
            `VALIDATION_ERROR` - the path id is not a valid UUID.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.read` is missing - or
            `BUILDING_ACCESS_DENIED` for the document's Building, or for its
            Client when it has no Building.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `DOCUMENT_NOT_FOUND` - no document row with that id.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    patch:
      operationId: updateDocument
      x-required-permission: document.manage
      summary: Patch the mutable fields of a document
      description: >
        MUTATION - CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B1 (BE-22A). Requires
        `document.manage`. PATCH semantics only: every field is optional, only the
        supplied keys are written, and an omitted key is never cleared or
        defaulted. An explicit `null` clears `description`, `fileReference` or
        `buildingId`. A body that supplies no mutable field at all is legal and is
        a true no-op - no UPDATE is issued, so `updatedAt` does not move -
        although one `DOCUMENT_UPDATED` operational event is still recorded.
        Unknown properties are ignored.

        Identity and context are immutable on this route: `clientId`,
        `documentNumber`, `contextType`, `sourceType` and `sourceId` may not
        appear in the body at all, even unchanged, and each is reported with its
        own "… is immutable and cannot be updated." detail. `expiryDate` is not a
        mutable field here either - expiry is written only by the Document Expiry
        journey - and `status: ARCHIVED` is refused because only the archive
        action may enter that state.

        Guards run in this order after the row resolves (404 `DOCUMENT_NOT_FOUND`)
        and the caller's isolation is asserted (403 `BUILDING_ACCESS_DENIED`): a
        document that is the canonical document behind a BAST binding may not
        change its `documentType` or its Building through this generic endpoint
        (409 `DOCUMENT_UPDATE_NOT_ALLOWED`); a document in `INACTIVE` or
        `ARCHIVED` status may not be patched at all (409
        `DOCUMENT_UPDATE_NOT_ALLOWED`); and re-pointing `buildingId` requires that
        Building to exist (404 `BUILDING_NOT_FOUND`), to belong to the document's
        Client (400 `DOCUMENT_BUILDING_MISMATCH`) and to be accessible to the
        caller (403 `BUILDING_ACCESS_DENIED`). A successful patch returns the
        re-read projection with `updatedAt` advanced; it approves nothing,
        versions nothing, uploads nothing and never touches an adjacent journey.
      tags: [Documents]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DocumentIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateDocumentRequest"
      responses:
        "200":
          description: The patched document.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/DocumentFoundation" }
        "400":
          description: >
            `VALIDATION_ERROR` - body is not a JSON object; an immutable field was
            supplied; `documentType` is outside 2-64 characters of
            `[A-Z][A-Z0-9_-]*` after upper-casing; `title` is blank or over 255;
            `description` exceeds 2000 or `fileReference` exceeds 512 and is
            neither a string nor null; `status` is outside its enum or null; or
            `buildingId` is not a valid UUID - each detail names its field. Also
            400 `DOCUMENT_BUILDING_MISMATCH` when the new Building belongs to
            another Client.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.manage` is missing - or
            `BUILDING_ACCESS_DENIED` for the document's own Building (or Client,
            when it has none) or for the Building being attached.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `DOCUMENT_NOT_FOUND` - no document row with that id - or
            `BUILDING_NOT_FOUND` for a `buildingId` that does not resolve.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `DOCUMENT_UPDATE_NOT_ALLOWED` - the document is `INACTIVE` or
            `ARCHIVED`, the patch asks for the `ARCHIVED` status, or the row is
            the canonical document of a BAST binding and the patch changes its
            `documentType` or Building. There is no force flag and no bypass.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /documents/{id}/archive:
    post:
      operationId: archiveDocument
      x-required-permission: document.archive
      summary: Archive a document
      description: >
        MUTATION - CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B1 (BE-22A). Requires
        `document.archive` and is the ONLY route that can put a generic document
        into `ARCHIVED`. The optional body carries `reason` alone (see the
        request schema); an absent or null body archives without a reason.

        The runtime copies the current status into `statusBeforeArchive`, sets
        `status` to `ARCHIVED` and stamps `archivedAt` (server clock),
        `archivedByUserId` (the authenticated actor) and `archiveReason`, all in
        one guarded write whose predicate already excludes an archived row, and
        then records one `DOCUMENT_ARCHIVED` operational event carrying the
        previous status, the reason and the archive timestamp. No file is deleted,
        no storage object is touched, no retention or purge is executed and no
        version, approval or evidence state changes.

        Guards: unknown id 404 `DOCUMENT_NOT_FOUND` before isolation, 403
        `BUILDING_ACCESS_DENIED` for an inaccessible Building or Client, 409
        `DOCUMENT_ALREADY_ARCHIVED` when the document is already `ARCHIVED`, and
        409 `DOCUMENT_UPDATE_NOT_ALLOWED` when the row is the canonical document
        behind a BAST binding - a linked source document cannot be archived
        through the generic endpoint. `archivedAt`, `archivedByUserId`,
        `archiveReason` and `statusBeforeArchive` in the response are the values
        just written; they are not accepted from callers.
      tags: [Documents]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DocumentIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ArchiveDocumentRequest"
      responses:
        "200":
          description: The archived document, with its archive metadata.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/DocumentFoundation" }
        "400":
          description: >
            `VALIDATION_ERROR` - the path id is not a valid UUID, or the body is
            not an object, contains a key other than `reason`, or carries a
            `reason` that is not a non-empty string of at most 1000 characters.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.archive` is missing - or
            `BUILDING_ACCESS_DENIED` for the document's Building (or Client, when
            it has none).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `DOCUMENT_NOT_FOUND` - no document row with that id.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `DOCUMENT_ALREADY_ARCHIVED` - the document is already archived (this
            is not an idempotent success) - or `DOCUMENT_UPDATE_NOT_ALLOWED` when
            the row is the canonical document of a BAST binding.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /documents/{id}/restore:
    post:
      operationId: restoreDocument
      x-required-permission: document.archive
      summary: Restore an archived document
      description: >
        MUTATION - CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B1 (BE-22A). Requires
        `document.archive` and is the only route that leaves `ARCHIVED`. There is
        no request body and no target-status parameter: the runtime restores the
        snapshot `statusBeforeArchive` (one of `DRAFT`, `ACTIVE`, `INACTIVE`) and
        clears `archivedAt`, `archivedByUserId`, `archiveReason` and
        `statusBeforeArchive` back to null in one guarded write whose predicate
        already requires an archived row, then records one `DOCUMENT_RESTORED`
        operational event carrying the restored status and the archive metadata
        that was cleared. No file is restored, no version is created, no approval
        or expiry state is replayed and nothing is written to an adjacent journey.

        Guards: unknown id 404 `DOCUMENT_NOT_FOUND` before isolation and 403
        `BUILDING_ACCESS_DENIED` for an inaccessible Building or Client; 409
        `DOCUMENT_NOT_ARCHIVED` when the document is not currently `ARCHIVED`; and
        409 `DOCUMENT_RESTORE_NOT_ALLOWED` when no restorable status snapshot
        exists. Unlike archive, restore carries no BAST-canonical guard, and a
        concurrent restore that has already run is reported as
        `DOCUMENT_RESTORE_NOT_ALLOWED` rather than as a duplicate success.
      tags: [Documents]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DocumentIdPath"
      responses:
        "200":
          description: The restored document, with its archive metadata cleared.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/DocumentFoundation" }
        "400":
          description: >
            `VALIDATION_ERROR` - the path id is not a valid UUID.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.archive` is missing - or
            `BUILDING_ACCESS_DENIED` for the document's Building (or Client, when
            it has none).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `DOCUMENT_NOT_FOUND` - no document row with that id.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `DOCUMENT_NOT_ARCHIVED` - the document is not in `ARCHIVED` status -
            or `DOCUMENT_RESTORE_NOT_ALLOWED` - there is no
            `DRAFT` / `ACTIVE` / `INACTIVE` snapshot to restore to.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B2 - J19 Document Versions (BE-22G).
  #
  # Contract exposure of the ALREADY REGISTERED BE-22G runtime routes only.
  # No runtime, lifecycle, storage or version-history behaviour is added or
  # changed here.
  # ---------------------------------------------------------------------------

  /documents/{documentId}/versions:
    post:
      operationId: createDocumentVersion
      x-required-permission: document.manage
      summary: Append an immutable version snapshot to a document
      description: >
        MUTATION - CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B2 (BE-22G). Appends one
        immutable snapshot row to the history of an existing generic BE-22A
        Document and returns it with status 201. Requires `document.manage`; the
        bearer session is asserted before the path id and body are parsed.

        The checks run in this runtime order, any of which can fail the request:
        the parent Document row is resolved first (404 `DOCUMENT_NOT_FOUND`,
        message "Document not found for versioning."), and a parent already in
        `ARCHIVED` is refused before anything is read or written (409
        `DOCUMENT_UPDATE_NOT_ALLOWED`, because the archive action owns that state
        and an archived document's history is closed). Isolation is asserted next
        on the parent row: Building access when the document has a Building,
        Client access when it has none (403 `BUILDING_ACCESS_DENIED`). The number
        is then computed as one more than `COALESCE(MAX(version_number), 0)` for
        that document - no client-supplied number is consulted - and the insert
        runs against the `document_versions_document_version_unique` constraint
        (409 `DOCUMENT_VERSION_INVALID_SEQUENCE` when two concurrent appends race
        for the same next number; a collision is reported, never silently
        renumbered or retried).

        Snapshot semantics: each of the five accepted fields is either an
        override or an inheritance - a key that is absent takes the document's
        current value and a supplied key stores the supplied value. Only
        `description` and `fileReference` accept an explicit `null` as an
        override to null; on `documentType` and `status` a null is a 400
        `VALIDATION_ERROR` ("documentType must be a non-empty string." / "status
        must be a string."), so on those two only omission inherits. An empty or
        absent body is legal and stores a full duplicate of the current document
        state under the next number. Nothing is ever rewritten in place: earlier
        version rows keep their values forever, and no rollback, promotion,
        supersede, renumber or delete route exists.

        Side effects are exactly three: the new version row; one alignment write
        that copies the same six snapshot values (including the unchanged,
        inherited `expiryDate`) onto the parent document row together with
        `updated_at = NOW()`, so the parent and its latest version agree; and one
        `DOCUMENT_VERSION_CREATED` operational event on entity type
        `DOCUMENT_VERSION` (summary "Document <documentNumber> version N
        created"). That parent alignment is a direct write, not the Documents
        patch route: it bypasses those guards, it can only ever write the six
        versioned fields, it never touches `clientId`, `documentNumber`,
        `contextType`, `sourceType` or `sourceId`, it sets and clears no archive
        field, and it emits no `DOCUMENT_UPDATED` operational event of its own - a
        caller watching document-level events sees only the version event. There is no
        file transfer, no storage, hash, integrity, retention or expiry change, no
        approval or sign-off state, and no mutation of a Work Completion Document,
        Handover Document, BAST, Evidence or any other journey.
      tags: [Document Versions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DocumentVersionDocumentIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateDocumentVersionRequest"
      responses:
        "201":
          description: Version appended and returned as stored.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/DocumentVersion" }
        "400":
          description: >
            `VALIDATION_ERROR` - the path `documentId` is not a valid UUID (detail
            "Document id must be a valid UUID."), the body is not a JSON object,
            `title` / `description` / `fileReference` is neither a string nor
            null or exceeds 255 / 2000 / 512 characters after trimming,
            `documentType` is null, blank or not 2-64 characters of
            `[A-Z][A-Z0-9_-]*` after upper-casing, or `status` is null, blank or
            outside `DRAFT` / `ACTIVE` / `INACTIVE` - each detail names its field.
            A blank `title` is the one case the parser does not report here: it is
            normalised to null instead (see the request schema). Unknown body
            properties are ignored, not rejected.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.manage` is missing - or
            `BUILDING_ACCESS_DENIED` for the parent document's Building, or for
            its Client when it has none. The parent must exist before access is
            checked, so an unknown `documentId` is 404 rather than 403.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `DOCUMENT_NOT_FOUND` - no generic Document row with that id. There is
            no separate "version not found" answer here: this route only appends.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `DOCUMENT_UPDATE_NOT_ALLOWED` - the parent document is `ARCHIVED`, so
            no further version may be appended - or `DOCUMENT_VERSION_INVALID_SEQUENCE`
            - the computed `(documentId, versionNumber)` collided with an existing
            version row. Neither has a force flag or a bypass, and neither is
            retried by the runtime.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listDocumentVersions
      x-required-permission: document.read
      summary: List a document's version history
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B2 (BE-22G). Requires
        `document.read`. `data` is a plain, NON-PAGINATED array of the document's
        version rows ordered by `version_number` ASCENDING - oldest first, so the
        last element is the latest - and `meta` is an empty object. The route
        accepts NO query parameter at all: there is no filter, no page control
        and no `status` narrowing, and any query string is ignored.

        Guards: the path `documentId` is trimmed and lower-cased and must be a
        valid UUID (400), the parent document must exist (404
        `DOCUMENT_NOT_FOUND`) and its Building or Client must be reachable by the
        caller (403 `BUILDING_ACCESS_DENIED`); the version rows themselves carry
        no isolation clause, because scoping is entirely inherited from the parent.
        A `DOCUMENT_VERSION_NOT_FOUND` answer is not possible here - an empty
        history is not an error.

        One narrow exception makes this a non-pure read: when the document has no
        version row at all, the runtime first inserts an implicit `versionNumber` 1
        snapshot of the document's current six versioned fields - attributed to
        the document's own `createdByUserId`, not to the caller - and returns that
        single row; the insert carries no conflict handling on this path.
        Documents created through `POST /documents` already have that baseline, so
        this only happens for a document whose history is genuinely missing.
        Archived documents are listed normally: unlike the append route, this read
        carries no archived-status guard.
      tags: [Document Versions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DocumentVersionDocumentIdPath"
      responses:
        "200":
          description: >
            Version history, oldest first (a single implicit version-1 row when
            the history had to be created on this request).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/DocumentVersion" }
        "400":
          description: >
            `VALIDATION_ERROR` - the path `documentId` is not a valid UUID.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.read` is missing - or
            `BUILDING_ACCESS_DENIED` for the parent document's Building or Client.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `DOCUMENT_NOT_FOUND` - no generic Document row with that id.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /documents/{documentId}/versions/latest:
    get:
      operationId: resolveLatestDocumentVersion
      x-required-permission: document.read
      summary: Resolve a document's latest version
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B2 (BE-22G). Requires
        `document.read`. "Latest" is computed, not stored: the runtime selects the
        document's row with the HIGHEST `version_number`
        (`ORDER BY version_number DESC LIMIT 1`). It is therefore not the newest
        `createdAt`, not the row whose `status` is `ACTIVE`, not a
        current-version pointer and not a promotion result - there is no pointer
        column and no promote route to move one. The response is one
        `DocumentVersion` with status 200, not an envelope carrying the parent
        document.

        Guards are the same as on the list route: valid UUID (400), existing
        parent (404 `DOCUMENT_NOT_FOUND`) and the parent's Building or Client
        reachable by the caller (403 `BUILDING_ACCESS_DENIED`). Like the list
        route - and unlike the two other reads - this route WRITES when the
        document has no version row at all: it first inserts the implicit
        `versionNumber` 1 snapshot of the current document state, attributed to the
        document's creator, and returns it. `latest` never answers 404
        `DOCUMENT_VERSION_NOT_FOUND` for a document that exists and is reachable.
        Because `latest` is registered as its own path before
        `/documents/{documentId}/versions/{versionNumber}`, the segment is matched
        literally and can never be requested as, or confused with, a version
        number. Archived parents are resolved normally - there is no archived-status
        guard on this read.
      tags: [Document Versions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DocumentVersionDocumentIdPath"
      responses:
        "200":
          description: >
            The highest-numbered version (or the implicit version-1 baseline
            created on this request when the document had no history).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/DocumentVersion" }
        "400":
          description: >
            `VALIDATION_ERROR` - the path `documentId` is not a valid UUID.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.read` is missing - or
            `BUILDING_ACCESS_DENIED` for the parent document's Building or Client.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `DOCUMENT_NOT_FOUND` - no generic Document row with that id.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /documents/{documentId}/versions/{versionNumber}:
    get:
      operationId: getDocumentVersionByNumber
      x-required-permission: document.read
      summary: Read one version by its number
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B2 (BE-22G). Requires
        `document.read`. Returns the single version row addressed by
        `(documentId, versionNumber)` - an exact-match lookup, never a
        nearest-below or fallback resolution, and never the parent document's
        current row when the number is missing.

        The number is validated in the handler before any query: the raw path
        segment is coerced with `Number()` and must be an integer greater than or
        equal to 1, so `0`, `-1`, `1.5`, `latest`, an empty or a non-numeric
        segment is 400 `VALIDATION_ERROR` with detail "versionNumber must be a
        positive integer.", while a whitespace-padded or `N.0` spelling coerces to
        the same integer as `N`. `1` is not special: it is simply the baseline
        number, and this route does NOT create it lazily the way the list and
        latest routes do - if version 1 is genuinely absent the answer is 404.

        Ordering of the remaining checks is exact: the version row is looked up
        first, so a number that does not exist under that document is 404
        `DOCUMENT_VERSION_NOT_FOUND` even when the caller could not reach the
        parent at all; only after a row is found is the parent document resolved
        and its isolation asserted (404 `DOCUMENT_NOT_FOUND` when the parent row is
        gone, 403 `BUILDING_ACCESS_DENIED` when it is out of scope). Reading
        nothing else: no version is created, updated or renumbered, no parent row
        is touched, and any stored status - including `ARCHIVED` on a lazily
        created baseline - is returned as stored.
      tags: [Document Versions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DocumentVersionDocumentIdPath"
        - $ref: "#/components/parameters/DocumentVersionNumberPath"
      responses:
        "200":
          description: The requested version row.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/DocumentVersion" }
        "400":
          description: >
            `VALIDATION_ERROR` - the path `documentId` is not a valid UUID, or
            `versionNumber` is not an integer greater than or equal to 1.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.read` is missing - or
            `BUILDING_ACCESS_DENIED` for the parent document's Building or Client.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `DOCUMENT_VERSION_NOT_FOUND` - no version row with that number under
            that document - or `DOCUMENT_NOT_FOUND` when the row exists but its
            parent document no longer does.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /document-versions/{versionId}:
    get:
      operationId: getDocumentVersion
      x-required-permission: document.read
      summary: Read one version by its id
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B2 (BE-22G). Requires `document.read`
        and returns the single version row whose own id is `versionId` - the
        addressable identity of a version, independent of its number. The id is
        trimmed, lower-cased and must be a valid UUID (400 `VALIDATION_ERROR`,
        detail "Version id must be a valid UUID.").

        The version row is resolved before any scoping, so an unknown id is 404
        `DOCUMENT_VERSION_NOT_FOUND`; only once a row is found does the runtime
        resolve its parent document and assert that parent's isolation, which
        yields 404 `DOCUMENT_NOT_FOUND` when the parent row is gone and 403
        `BUILDING_ACCESS_DENIED` when the caller cannot reach the parent's
        Building (or Client, when it has none). A version is therefore never
        readable by id alone without its parent's context, and this route cannot
        be used to enumerate versions: there is no list-by-id, no search, no
        filter and no pagination - only the document-scoped list route returns a
        set. Nothing is written, no version is created, promoted, renumbered or
        deleted, and no adjacent journey is read or touched.
      tags: [Document Versions]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DocumentVersionIdPath"
      responses:
        "200":
          description: The requested version row.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/DocumentVersion" }
        "400":
          description: >
            `VALIDATION_ERROR` - the path `versionId` is not a valid UUID.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.read` is missing - or
            `BUILDING_ACCESS_DENIED` for the parent document's Building or Client.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `DOCUMENT_VERSION_NOT_FOUND` - no version row with that id - or
            `DOCUMENT_NOT_FOUND` when the version exists but its parent document
            row no longer does.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B3 - J19 Document Expiry (BE-22H).
  #
  # Contract exposure of the ALREADY REGISTERED BE-22H runtime routes only.
  # No runtime, lifecycle, scheduler, retention or authorization behaviour is
  # added or changed here.
  # ---------------------------------------------------------------------------

  /documents/{documentId}/expiry:
    put:
      operationId: setDocumentExpiry
      x-required-permission: document.manage
      summary: Set or clear a document's expiry date
      description: >
        MUTATION - CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B3 (BE-22H). Replaces the
        expiry value of one generic BE-22A Document and returns the resulting
        expiry projection with status 200 - it is a full replacement, never an
        increment or a compare-and-set, and it is not a create (there is no 201)
        and not a delete (there is no DELETE route: `expiryDate: null` on this
        same PUT is the only way to clear). Requires `document.manage`; the bearer
        session is asserted before the path id and the body are parsed.

        The checks run in this runtime order, any of which can fail the request:
        the Document row is resolved first (404 `DOCUMENT_NOT_FOUND`, message
        "Document not found."), an `ARCHIVED` parent is refused next (409
        `DOCUMENT_UPDATE_NOT_ALLOWED` - an archived document's expiry cannot be
        moved), and only then is isolation asserted on the parent row: Building
        access when the document has a Building, otherwise Client access (403
        `BUILDING_ACCESS_DENIED`). Unlike the generic Documents patch route, this
        write carries no canonical-BAST identity guard.

        Two writes follow, issued sequentially and without a surrounding
        transaction: the document row's `expiry_date` is set to the accepted value
        together with `updated_at = NOW()`, and history is preserved by appending
        a new immutable Document Version row numbered one above the current maximum
        for that document, copying the document's title, description,
        fileReference, documentType and status and carrying the new expiry value.
        That insert has no conflict mapping on this path - a concurrent append that
        races the same number is therefore not translated into
        `DOCUMENT_VERSION_INVALID_SEQUENCE` here. Finally one
        `DOCUMENT_EXPIRY_UPDATED` operational event is recorded on entity type
        `DOCUMENT_EXPIRY` (entity id = the document id, summary "Document
        <documentNumber> expiry updated").

        The response is computed from the value this request submitted, not re-read:
        `expiryDate` is the accepted instant serialised through `toISOString()` and
        `expiryState` is derived from it against the clock of this request, so a
        far-future date answers `ACTIVE`, a date within 30 days answers `EXPIRING`
        and a past date answers `EXPIRED`. Nothing is scheduled, notified,
        archived, retained or deleted as a consequence - and no version row is
        ever rewritten.
      tags: [Document Expiry]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DocumentExpiryDocumentIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetDocumentExpiryRequest"
      responses:
        "200":
          description: >
            The expiry as it now stands, derived at this instant. The response
            carries no `versionId` key on this route, although the append it
            performed is visible through the Document Versions contract.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/DocumentExpiry" }
        "400":
          description: >
            `VALIDATION_ERROR` - the path `documentId` is not a valid UUID (detail
            "Document id must be a valid UUID."), the body is not a JSON object,
            `expiryDate` is absent (detail "expiryDate is required (use null to
            clear)."), or `expiryDate` is neither null nor a parsable date string
            (details "expiryDate must be an ISO-8601 date string or null." and
            "expiryDate must be a valid date."). Unknown body properties are
            ignored, not rejected.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.manage` is missing - or
            `BUILDING_ACCESS_DENIED` for the document's Building, or for its
            Client when it has none. The row is resolved before scoping, so an
            unknown id is 404 rather than 403.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `DOCUMENT_NOT_FOUND` - no generic Document row with that id.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `DOCUMENT_UPDATE_NOT_ALLOWED` - the document is `ARCHIVED`, so its
            expiry can no longer be moved. There is no force flag, and un-archiving
            is a Documents-route action that is not part of this contract.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: getDocumentExpiry
      x-required-permission: document.read
      summary: Read a document's expiry and derived state
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B3 (BE-22H). Requires
        `document.read`. Returns the stored expiry value of the document row
        itself - whatever wrote it, whether the generic Documents surface, this
        route or a version-scoped expiry write - with `expiryState` derived in this
        request from the stored date against the current clock: null answers
        `ACTIVE`, a past instant answers `EXPIRED`, a value at most 30 days ahead
        answers `EXPIRING` and anything further ahead answers `ACTIVE`. The state
        is never stored, cached, queued or pushed, and re-reading later is the only
        way it changes.

        `expiryDate` comes from the stored row and is serialised through
        `toISOString()`, so it appears as a full RFC3339 timestamp rather than a
        bare date. The response carries no `versionId`, no actor and no timestamp
        field, and it is not a list: there is no history here, no pagination and
        no query parameter of any kind (any query string is ignored).

        Guards: valid UUID path id (400), then the document row (404
        `DOCUMENT_NOT_FOUND`), then the parent's isolation (403
        `BUILDING_ACCESS_DENIED`). An `ARCHIVED` document is answered normally: this
        read carries no status guard, and reading writes nothing - no event, no
        version row, no expiry change, no state transition and no follow-up action.
      tags: [Document Expiry]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DocumentExpiryDocumentIdPath"
      responses:
        "200":
          description: >
            The document's current expiry value and the state derived from it right
            now (null date plus `ACTIVE` when no expiry is set).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/DocumentExpiry" }
        "400":
          description: >
            `VALIDATION_ERROR` - the path `documentId` is not a valid UUID.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.read` is missing - or
            `BUILDING_ACCESS_DENIED` for the document's Building or Client.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `DOCUMENT_NOT_FOUND` - no generic Document row with that id.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /document-versions/{versionId}/expiry:
    put:
      operationId: setDocumentVersionExpiry
      x-required-permission: document.manage
      summary: Record an expiry on a version by appending a new version
      description: >
        MUTATION - CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B3 (BE-22H). Requires
        `document.manage`. This route never edits the addressed version row: a
        version is immutable history. What it does is append a NEW version row for
        the same document that copies the addressed version's title, description,
        fileReference, documentType and status, carries the supplied expiry value
        (or the cleared null), takes the next number above the document's current
        maximum and records the authenticated caller as `createdByUserId`.

        It additionally writes the same expiry value onto the parent document row
        (`expiry_date` plus `updated_at = NOW()`), so a version-scoped expiry write
        is visible through `GET /documents/{documentId}/expiry` as well. That is a
        deliberate synchronisation of the live document with its newest snapshot,
        not a rollback, promotion or re-pointing of "latest": latest stays a
        computed highest number on the Document Versions contract.

        Guards run in this order: the version row is resolved first (404
        `DOCUMENT_VERSION_NOT_FOUND`), then its parent document (404
        `DOCUMENT_NOT_FOUND`, message "Document not found for versioning."), then
        an `ARCHIVED` parent is refused (409 `DOCUMENT_UPDATE_NOT_ALLOWED`), and
        only then is isolation asserted on the parent (403
        `BUILDING_ACCESS_DENIED`). No guard is applied to the addressed version's
        own status - an archived snapshot may still be used as the source of a new
        one.

        Side effects are the appended version row, the parent document update and
        one `DOCUMENT_VERSION_EXPIRY_UPDATED` operational event on entity type
        `DOCUMENT_VERSION_EXPIRY` whose entity id is the NEW version row (metadata
        carries the document id, the addressed `sourceVersionId`, the new
        `versionId` and the effective date). The version insert is a plain append
        with no conflict mapping on this path. Nothing is deleted, archived,
        retained or purged, no file is touched, and no notification, reminder or
        scheduled job is created from the new date.

        The response reports the NEW version row's identity in `versionId` - not
        the addressed one - alongside the submitted expiry value and the state
        derived from it at this instant, with status 200.
      tags: [Document Expiry]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DocumentExpiryVersionIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetDocumentExpiryRequest"
      responses:
        "200":
          description: >
            The expiry as applied, together with the id of the newly appended
            version row that now carries it.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/DocumentExpiry" }
        "400":
          description: >
            `VALIDATION_ERROR` - the path `versionId` is not a valid UUID (detail
            "Version id must be a valid UUID."), the body is not a JSON object,
            `expiryDate` is absent (detail "expiryDate is required (use null to
            clear)."), or `expiryDate` is neither null nor a parsable date string
            (details "expiryDate must be an ISO-8601 date string or null." and
            "expiryDate must be a valid date."). Unknown body properties are
            ignored.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.manage` is missing - or
            `BUILDING_ACCESS_DENIED` for the parent document's Building, or for its
            Client when it has none. Access is always evaluated on the parent,
            never on the version row itself.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `DOCUMENT_VERSION_NOT_FOUND` - no version row with that id - or
            `DOCUMENT_NOT_FOUND` when the version exists but its parent document
            row no longer does.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `DOCUMENT_UPDATE_NOT_ALLOWED` - the parent document is `ARCHIVED`, so
            no further expiry snapshot may be appended to it. No force flag exists
            and un-archiving is not part of this contract.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: getDocumentVersionExpiry
      x-required-permission: document.read
      summary: Read the expiry carried by one version
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B3 (BE-22H). Requires `document.read`.
        Returns the expiry stored on the ADDRESSED version row and the state
        derived from it at this instant - not the parent document's value, not the
        newest version's value and not a "current" pointer. That is the asymmetry
        to read carefully: after
        `PUT /document-versions/{versionId}/expiry` the addressed row is unchanged,
        so GET on that same `versionId` still answers with its original expiry,
        while the value that was just written is carried by the newly appended
        version row (whose id the PUT returned) and by the parent document row
        reachable through `GET /documents/{documentId}/expiry`. Reading history
        therefore means reading the specific version rows, and this route performs
        no lazy baseline creation and no write of any kind.

        Guards: valid UUID path id (400), the version row (404
        `DOCUMENT_VERSION_NOT_FOUND`), its parent document (404
        `DOCUMENT_NOT_FOUND`), then the parent's Building / Client isolation (403
        `BUILDING_ACCESS_DENIED`); an `ARCHIVED` parent is readable. The projection
        carries `versionId` here (the addressed row), the parent's `documentId`,
        `clientId` and `buildingId`, and no actor or timestamp field; there is no
        query parameter, no filter and no pagination.
      tags: [Document Expiry]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DocumentExpiryVersionIdPath"
      responses:
        "200":
          description: >
            The addressed version's own expiry value and the state derived from it
            right now (null date plus `ACTIVE` when that snapshot carried none).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/DocumentExpiry" }
        "400":
          description: >
            `VALIDATION_ERROR` - the path `versionId` is not a valid UUID.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.read` is missing - or
            `BUILDING_ACCESS_DENIED` for the parent document's Building or Client.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `DOCUMENT_VERSION_NOT_FOUND` - no version row with that id - or
            `DOCUMENT_NOT_FOUND` when the version exists but its parent document
            row no longer does.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B4 - J19 Document Approvals (BE-22I).
  #
  # Contract exposure of the ALREADY REGISTERED BE-22I runtime routes only.
  # No runtime, lifecycle, workflow-engine or authorization behaviour is added
  # or changed here.
  # ---------------------------------------------------------------------------

  /documents/{documentId}/approvals:
    post:
      operationId: submitDocumentForApproval
      x-required-permission: document.manage
      summary: Submit a document or one version for approval
      description: >
        MUTATION - CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B4 (BE-22I). Creates ONE
        pending review row for the addressed document (or for one of its versions
        when `versionId` is supplied) and returns it with status 201. Requires
        `document.manage`; the bearer session is asserted before the path id and
        the body are parsed. `id`, `clientId`, `buildingId`, `contextType`,
        `approvalStatus`, `decision`, `reviewedAt` and both timestamps are
        backend-owned and are not accepted from callers - only `approverUserId`,
        `versionId` and `notes` are read from the body.

        Checks run in this runtime order, any of which can fail the request: the
        target document row is resolved first and a missing one is 400
        `DOCUMENT_APPROVAL_CONTEXT_INVALID` - this module reports an unusable
        target as an invalid context, never as 404; an `ARCHIVED` document is
        refused with 409 `DOCUMENT_UPDATE_NOT_ALLOWED`; a supplied `versionId`
        must resolve through the Document Version store AND carry the same
        document, otherwise 400 `DOCUMENT_APPROVAL_CONTEXT_INVALID` (a version of
        another document is not silently re-pointed); the submitter's isolation is
        then asserted on the document - Building when it has one, otherwise Client
        (403 `BUILDING_ACCESS_DENIED`); the named `approverUserId` must be an
        ACTIVE user in the same Building or Client context who resolves to
        `document.approve` or `document.manage` (403
        `DOCUMENT_APPROVAL_UNAUTHORIZED`, checked at submission so an unusable
        approver is never stored); and no `PENDING` review may already exist for
        the same target and the same reviewer (409
        `DOCUMENT_APPROVAL_ALREADY_PENDING`).

        A prior final decision does NOT block a new submission by design: an
        already-approved document may be submitted again and the new review becomes
        an additional row, because history is preserved instead of overwritten. The
        row is written with `status = 'PENDING'`, no decision and no reviewed
        timestamp, and one `DOCUMENT_APPROVAL_SUBMITTED` operational event is
        recorded on entity type `DOCUMENT_APPROVAL` (summary "Document
        <documentNumber> submitted for approval"). Because the review store keeps
        no submitter column, the `createdByUserId` in this one response is the
        in-memory echo of the caller: every subsequent read of the same row
        reports it as null. Nothing else changes - the document's `status`, expiry,
        versions and archive fields are untouched, no version row is created, and
        no workflow stage, routing, delegation, reminder or task is generated.
      tags: [Document Approvals]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DocumentApprovalDocumentIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubmitDocumentApprovalRequest"
      responses:
        "201":
          description: >
            The pending approval as the module holds it for this response -
            `approvalStatus` `PENDING`, `decision` null, `reviewedAt` null.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/DocumentApproval" }
        "400":
          description: >
            `VALIDATION_ERROR` - the path `documentId` is not a valid UUID (detail
            "Document id must be a valid UUID."), the body is not a JSON object,
            `approverUserId` is missing ("approverUserId is required.") or
            malformed, `versionId` is not a valid UUID, or `notes` is neither a
            string nor null or exceeds 4000 characters - each detail names its
            field - or `DOCUMENT_APPROVAL_CONTEXT_INVALID` ("Document approval
            context is invalid.") when the target document does not exist or the
            named `versionId` does not resolve to a version of that document.
            Unknown body properties are ignored, not rejected.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.manage` is missing - or
            `BUILDING_ACCESS_DENIED` because the submitter cannot reach the
            document's Building or Client - or `DOCUMENT_APPROVAL_UNAUTHORIZED`
            ("Approver is not authorized.") because `approverUserId` names an
            unknown or inactive user, one outside the document's Building / Client
            context, or one without `document.approve` or `document.manage`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `DOCUMENT_APPROVAL_ALREADY_PENDING` - a pending review for the same
            target and the same reviewer already exists - or
            `DOCUMENT_UPDATE_NOT_ALLOWED` - the target document is `ARCHIVED`.
            Neither is bypassed, and a duplicate submission never reuses or
            resets an existing pending row.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listDocumentApprovalHistory
      x-required-permission: document.read
      summary: List a document's approval history
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B4 (BE-22I). Requires
        `document.read`. `data` is a plain, NON-PAGINATED array of every approval
        row whose target is this document OR any version of this document, ordered
        by the review's own `createdAt` ASCENDING (oldest first), and `meta` is an
        empty object. Version-targeted reviews are therefore included here and are
        distinguishable by their non-null `versionId`; the parent `documentId` is
        resolved back through the version so every element of the list reports the
        addressed document.

        The route accepts NO query parameter: the module's filter type is not read
        by this handler, so status, reviewer and building cannot be narrowed,
        paged or sorted by the caller, and any query string is ignored. There is
        no cross-document, cross-client or "my pending approvals" queue on this
        surface - the document id is mandatory and is the only selector.

        Guards: valid UUID path id (400), then the document row - a missing one is
        400 `DOCUMENT_APPROVAL_CONTEXT_INVALID`, not 404 - then the document's
        Building / Client isolation (403 `BUILDING_ACCESS_DENIED`). An `ARCHIVED`
        document's history stays readable, and an empty array is a normal answer
        (no history is not an error). Reading writes nothing: no reminder, no
        re-notification, no status recomputation and no automatic decision.
      tags: [Document Approvals]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DocumentApprovalDocumentIdPath"
      responses:
        "200":
          description: >
            Approval history for the document and its versions, oldest first
            (possibly empty).
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/DocumentApproval" }
        "400":
          description: >
            `VALIDATION_ERROR` - the path `documentId` is not a valid UUID - or
            `DOCUMENT_APPROVAL_CONTEXT_INVALID` when that document row no longer
            exists.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.read` is missing - or
            `BUILDING_ACCESS_DENIED` for the document's Building or Client.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /document-approvals/{id}:
    get:
      operationId: getDocumentApprovalStatus
      x-required-permission: document.read
      summary: Read one approval by its id
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B4 (BE-22I). Requires
        `document.read` and returns the single approval row whose review id is
        `id`. The id is the approval's own identity - it is neither the document
        id nor a version id, and no `documentNumber` or reviewer lookup is
        accepted here.

        The row is resolved first (404 `DOCUMENT_APPROVAL_NOT_FOUND` for an unknown
        id) and isolation is asserted afterwards on the row's own Building (or the
        Client, when the target document has none), so a known but unreachable
        approval is 403 `BUILDING_ACCESS_DENIED` rather than 404. For a
        version-targeted approval the document, its Building, its Client and its
        `contextType` are resolved through the version row, which is also why an
        approval can still answer while the parent has moved on - and why
        `contextType` falls back to `INTERNAL` when it cannot be resolved.

        `approvalStatus` is derived from the stored pair at read time: `PENDING`
        while the review is open, otherwise the recorded decision
        (`APPROVED` / `REJECTED`). `createdByUserId` reads back as null on this
        route - the submitter is not persisted on the review row - and no
        `availableActions`, stage, routing, delegation or workflow field exists on
        this contract. Reading never decides, reopens, expires or re-queues
        anything, and no query parameter or pagination applies.
      tags: [Document Approvals]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DocumentApprovalIdPath"
      responses:
        "200":
          description: The requested approval row.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/DocumentApproval" }
        "400":
          description: >
            `VALIDATION_ERROR` - the path `id` is not a valid UUID.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.read` is missing - or
            `BUILDING_ACCESS_DENIED` for the target document's Building or Client.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `DOCUMENT_APPROVAL_NOT_FOUND` - no approval row with that review id.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /document-approvals/{id}/approve:
    post:
      operationId: approveDocumentApproval
      x-required-permission: document.approve
      summary: Approve a pending document approval
      description: >
        MUTATION - CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B4 (BE-22I). Records
        `decision` APPROVED on the addressed pending approval and returns the
        decided row with status 200 (not 201: nothing is created). The only
        accepted body field is `notes`; the caller's identity, the decision value,
        `reviewedAt` and `updatedAt` are all backend-assigned.

        Guards run in this exact order: the review row (404
        `DOCUMENT_APPROVAL_NOT_FOUND`), then its target document - a document row
        that has disappeared is 400 `DOCUMENT_APPROVAL_CONTEXT_INVALID`, not 404 -
        then an `ARCHIVED` target document (409 `DOCUMENT_UPDATE_NOT_ALLOWED`, so
        no decision can be recorded against an archived document), then the
        caller's isolation (403 `BUILDING_ACCESS_DENIED`), then the reviewer
        identity check: the caller MUST be the row's own `reviewerUserId`, and an
        unrelated - even privileged - caller is 403
        `DOCUMENT_APPROVAL_UNAUTHORIZED` (409 for an already-decided row is only
        reached after it). The row must still be pending and undecided, otherwise
        409 `DOCUMENT_APPROVAL_ALREADY_DECIDED`; and the approver authority is
        re-evaluated at this moment, so an approver whose account has become
        inactive or who no longer reaches the context or no longer holds
        `document.approve` or `document.manage` is refused with 403.

        The write is conditional on `status = 'PENDING'`: it sets the decision,
        moves the row to completed, stamps `reviewed_at` and `updated_at` to the
        server clock and replaces `notes` only when the request supplies a non-null
        value (`COALESCE`), so omitting notes - or sending null - KEEPS the notes
        the submitter left and notes can never be cleared by a decision. A
        concurrent second decision matches no row and is answered 409
        `DOCUMENT_APPROVAL_ALREADY_DECIDED` as well; there is no overwrite, no
        retry and no idempotent replay. The response is the re-read row, so a
        reader sees the stored outcome rather than an echo.

        Approving changes nothing outside this row: the Document's `status` is not
        mutated - an approved document stays in whatever lifecycle state it was in
        (`DRAFT`, `ACTIVE` or `INACTIVE`) - no version is created, promoted or
        marked current, no expiry or archive field is touched, no BAST, Acceptance
        Sign-Off, Work Order verification or invoice is affected, and no workflow
        stage advances because there is no stage to advance. One
        `DOCUMENT_APPROVAL_APPROVED` operational event is recorded on entity type
        `DOCUMENT_APPROVAL` (summary "Document approval APPROVED").
      tags: [Document Approvals]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DocumentApprovalIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DocumentApprovalDecisionRequest"
      responses:
        "200":
          description: >
            The decided approval as stored - `approvalStatus` `APPROVED`,
            `decision` `APPROVED`, `reviewedAt` set.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/DocumentApproval" }
        "400":
          description: >
            `VALIDATION_ERROR` - the path `id` is not a valid UUID (detail
            "Approval id must be a valid UUID.") or `notes` is neither a string
            nor null, or exceeds 4000 characters - or
            `DOCUMENT_APPROVAL_CONTEXT_INVALID` when the target document row is
            gone.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.approve` is missing - or
            `BUILDING_ACCESS_DENIED` for the target document's Building or Client
            - or `DOCUMENT_APPROVAL_UNAUTHORIZED` when the caller is not the named
            reviewer, or when the named reviewer is no longer an ACTIVE user in
            context holding `document.approve` or `document.manage`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `DOCUMENT_APPROVAL_NOT_FOUND` - no approval row with that review id,
            including the race where the row is deleted between the guard and the
            re-read.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `DOCUMENT_APPROVAL_ALREADY_DECIDED` - the approval is no longer
            pending or already carries a decision (a second attempt on the same row
            is always this conflict, never a silent re-decision or a duplicate
            success) - or `DOCUMENT_UPDATE_NOT_ALLOWED` when the target document is
            `ARCHIVED`. There is no revoke, reopen or overwrite path.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /document-approvals/{id}/reject:
    post:
      operationId: rejectDocumentApproval
      x-required-permission: document.approve
      summary: Reject a pending document approval
      description: >
        MUTATION - CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B4 (BE-22I). The exact
        mirror of the approve route with the opposite outcome: `decision` REJECTED
        on the addressed pending approval, status 200, `notes` the only accepted
        body field. Every guard, its order, the reviewer-identity requirement, the
        live approver-authority re-check, the conditional `status = 'PENDING'`
        write, the `COALESCE` notes behaviour (an omitted or null `notes` keeps the
        submitter's notes; a decision can never clear them) and the 409 answers on
        an already-decided row are identical to that route, as is the
        `DOCUMENT_APPROVAL_ALREADY_DECIDED` answer for a concurrent double decision.

        A rejected approval is terminal in the same way an approved one is: the
        row cannot be re-decided, reopened, withdrawn or overwritten, and there is
        no request-changes or rework action attached to it. Rejecting sends the
        document nowhere: it does not change the document's `status`, does not
        archive, expire or delete it, does not create or amend a version, does not
        unlock editing beyond what the Documents contract already allows and does
        not notify, task or schedule anything - `notes` are stored on the row only.
        One `DOCUMENT_APPROVAL_REJECTED` operational event is recorded on entity
        type `DOCUMENT_APPROVAL` (summary "Document approval REJECTED"), and the
        response is the re-read row.
      tags: [Document Approvals]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/DocumentApprovalIdPath"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DocumentApprovalDecisionRequest"
      responses:
        "200":
          description: >
            The decided approval as stored - `approvalStatus` `REJECTED`,
            `decision` `REJECTED`, `reviewedAt` set.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/DocumentApproval" }
        "400":
          description: >
            `VALIDATION_ERROR` - the path `id` is not a valid UUID or `notes` is
            neither a string nor null, or exceeds 4000 characters - or
            `DOCUMENT_APPROVAL_CONTEXT_INVALID` when the target document row is
            gone.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.approve` is missing - or
            `BUILDING_ACCESS_DENIED` for the target document's Building or Client
            - or `DOCUMENT_APPROVAL_UNAUTHORIZED` when the caller is not the named
            reviewer or that reviewer no longer qualifies.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `DOCUMENT_APPROVAL_NOT_FOUND` - no approval row with that review id.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `DOCUMENT_APPROVAL_ALREADY_DECIDED` - pending-only transition guard
            failed, including a lost race against another decision - or
            `DOCUMENT_UPDATE_NOT_ALLOWED` when the target document is `ARCHIVED`.
            No status change, no reopen and no replacement of the decision.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }


  # ---------------------------------------------------------------------------
  # CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B5 - J19 Supporting Documents (BE-22F).
  #
  # Contract exposure of the ALREADY REGISTERED BE-22F runtime routes only.
  # No runtime, binding, lifecycle or storage behaviour is added or changed here.
  # ---------------------------------------------------------------------------

  /supporting-documents:
    post:
      operationId: createSupportingDocument
      x-required-permission: document.manage
      summary: Create a document and bind it to one business target
      description: >
        MUTATION - CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B5 (BE-22F). This is a
        two-row write behind one route: it creates a NEW generic BE-22A Document
        through the shared document foundation and then inserts this module's
        binding row for (`parentType`, `parentId`), returning the binding with the
        created document embedded, with status 201. Requires `document.manage`; the
        bearer session is asserted before the body is parsed. `id`, `documentId`,
        `createdAt` and `updatedAt` are backend-assigned and are not fields of the
        request, and there is no `documentId` to point at: an existing document can
        never be adopted, re-bound or wrapped by this route.

        Every check runs before anything is written, in this runtime order: the
        target row is resolved by `parentId` in the store that owns the chosen
        `parentType` and a miss is 404 `SUPPORTING_DOCUMENT_PARENT_NOT_FOUND`; when
        that target is itself backed by a generic document - a Work Completion, BAST
        or Handover Document, an Acceptance Sign-Off (through its BAST or Handover
        document, when it has one), or a `DOCUMENT` target - an `ARCHIVED` one is
        refused with 409 `DOCUMENT_UPDATE_NOT_ALLOWED`, so no supporting document
        can be attached to a frozen parent; the declared `clientId` must exist (404
        `CLIENT_NOT_FOUND`); a supplied `buildingId` must exist (404
        `BUILDING_NOT_FOUND`) and its Property must sit under that same Client (400
        `SUPPORTING_DOCUMENT_BUILDING_MISMATCH`) and be reachable by the actor (403
        `BUILDING_ACCESS_DENIED`), while with no Building the actor must instead
        reach the Client (403 `BUILDING_ACCESS_DENIED` as well - a Client-level
        failure is reported with that same code, not a Client-specific one).

        The binding is then aligned to the target: the target's Client must equal the
        declared one (400 `SUPPORTING_DOCUMENT_CONTEXT_MISMATCH` with detail
        "Supporting document client does not match parent."), a supplied Building
        must equal the target's (400 `SUPPORTING_DOCUMENT_BUILDING_MISMATCH`), and a
        request that names no Building silently INHERITS the target's - after which
        the actor's Building access is asserted again on the inherited value, so an
        inherited Building the caller cannot reach is still 403. For the five targets
        that expose a context and are compared - `WORK_COMPLETION`, `BAST`,
        `HANDOVER`, `SIGN_OFF` and `DOCUMENT` - the declared `contextType` must equal
        the target's own (400 `SUPPORTING_DOCUMENT_CONTEXT_MISMATCH`, "Supporting
        document context does not match parent."); `TENANT_COMPANY`, `VENDOR`, `RFQ`
        and `QUOTATION_REVISION` targets are not compared. The document's own source
        is then validated - a `TENANT` or `VENDOR` context must name an existing
        Tenant Company or Vendor row (404 `DOCUMENT_SOURCE_NOT_FOUND`) sitting under
        the same Client (400 `DOCUMENT_SOURCE_CLIENT_MISMATCH`), and an `INTERNAL`
        context may not name another source kind - and a `documentNumber` already
        used by that Client is refused with 409 `DOCUMENT_NUMBER_ALREADY_EXISTS`.

        Only then is the Document inserted, with the resolved Building, the supplied
        metadata, `description` defaulting to null and `status` defaulting to
        `DRAFT`; the binding row is written next, storing the caller as
        `createdByUserId` and copying the Client, Building and context onto itself.
        If that second write - or the audit write - fails, the module deletes the
        document it has just created as a compensation before re-throwing, so a
        failed bind leaves no orphan document and no partially bound one. Success
        records one `SUPPORTING_DOCUMENT_CREATED` operational event on entity type
        `SUPPORTING_DOCUMENT` (summary "Supporting document <documentNumber> bound
        to <parentType> <parentId>", metadata carrying both row ids, the target pair
        and the `fileReference`). Nothing outside these two rows changes: no parent
        row is written, no Document Version is appended, no expiry, approval,
        evidence or notification is created, and no file is touched.
      tags: [Supporting Documents]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSupportingDocumentRequest"
      responses:
        "201":
          description: >
            The binding row with the document it just created embedded under
            `document`; `createdAt` and `updatedAt` come from the binding row's own
            insert, so both carry the same instant.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SupportingDocument" }
        "400":
          description: >
            `VALIDATION_ERROR` - the body is not a JSON object, or a required field
            is missing or blank (`clientId`, `contextType`, `title`, `fileReference`,
            `documentNumber`, `documentType`, `parentType`, `parentId`), or a UUID
            field is malformed, or `title` / `fileReference` / `documentNumber` /
            `description` exceed 255 / 512 / 64 / 2000 characters, or `documentType`
            is outside 2-64 characters of `[A-Z][A-Z0-9_-]*` after upper-casing, or
            `contextType` / `sourceType` / `status` / `parentType` is outside its
            enum (a null `status` included), or only one of `sourceType` / `sourceId`
            is supplied, or `sourceType` does not pair with `contextType` (`TENANT`
            requires `TENANT_COMPANY`, `VENDOR` requires `VENDOR`, `INTERNAL` allows
            only `INTERNAL` or none) - each detail names its field - or
            `SUPPORTING_DOCUMENT_CONTEXT_MISMATCH` for the parent's Client or context
            disagreeing, for a `TENANT` / `VENDOR` context declared without its
            source, and for the `fileReference` re-check on this route, or
            `SUPPORTING_DOCUMENT_BUILDING_MISMATCH` when the Building's Property
            belongs to another Client or differs from the target's, or
            `DOCUMENT_SOURCE_CLIENT_MISMATCH` when the Tenant Company or Vendor sits
            under another Client. Unknown body properties are ignored, not rejected.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.manage` is missing - or
            `BUILDING_ACCESS_DENIED` because the supplied Building, the Building
            inherited from the target, or - with no Building at all - the declared
            Client is not reachable by the actor. Reaching the target's own context
            is never a substitute for reaching this row's.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `SUPPORTING_DOCUMENT_PARENT_NOT_FOUND` - no row for that (`parentType`,
            `parentId`) pair in the store that owns the target - or
            `CLIENT_NOT_FOUND` / `BUILDING_NOT_FOUND` for a declared Client or
            Building that does not exist, or `DOCUMENT_SOURCE_NOT_FOUND` when the
            `TENANT` / `VENDOR` source row is missing. A missing parent is never
            reported as a validation detail, and no parent row is created on demand.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "409":
          description: >
            `DOCUMENT_UPDATE_NOT_ALLOWED` - the parent's underlying document is
            `ARCHIVED` - or `DOCUMENT_NUMBER_ALREADY_EXISTS` - `documentNumber` is
            already taken by another document of the same Client. Neither is
            bypassed, and no number is auto-assigned, suffixed or reused here.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
    get:
      operationId: listSupportingDocuments
      x-required-permission: document.read
      summary: List supporting document bindings
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B5 (BE-22F). Requires `document.read`.
        `data` is a plain, NON-PAGINATED array of binding rows, ordered by the
        binding's own `created_at` DESCENDING (newest first), and `meta` is an empty
        object. There is no limit, page, offset, cursor, sort or search parameter on
        this route, and no total count is reported, so a caller reads the whole
        visible set at once.

        Six optional query filters narrow the set, all on the binding row itself:
        `parentType`, `parentId`, `clientId`, `buildingId`, `contextType` and
        `documentType` (the last one matching the bound Document's own type code
        through a join, never a column of the binding row). `parentType` and
        `parentId` must be supplied together, and together they are also validated
        as a real target: an unresolvable pair is 404
        `SUPPORTING_DOCUMENT_PARENT_NOT_FOUND`, and the actor must reach that
        target's Building or Client before the query runs. `buildingId` and
        `clientId` are likewise asserted for reachability (403), not silently
        filtered to nothing. Any other query key is ignored, and a repeated key is
        not merged - each filter expects one string value.

        Isolation is a predicate rather than a per-row check: only bindings whose
        Building is one of the actor's accessible Buildings - or which have no
        Building and whose Client is one of the actor's accessible Clients - can
        appear, so an actor with no accessible context at all receives an empty array
        (200, not 403), and no `clientId` or `buildingId` filter can widen the set
        beyond it. There is no archived, status, expiry or parent-state filter, no
        "only pending" or "my documents" view, and no cross-target aggregate:
        bindings for several parents may interleave freely. A binding whose bound
        Document row has since disappeared is skipped by the module while it builds
        the response, so this list can be shorter than the number of stored binding
        rows, and reading writes nothing.
      tags: [Supporting Documents]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: parentType
          in: query
          required: false
          description: >
            Keep only bindings of this parent kind. Trimmed and upper-cased, and a
            value outside the nine is 400 `VALIDATION_ERROR` with a detail naming the
            accepted set. It must accompany `parentId`; supplying only one of the two
            is a 400 on `parentType`. It selects no target table by itself and grants
            no authority over the target's own module.
          schema:
            $ref: "#/components/schemas/SupportingParentType"
        - name: parentId
          in: query
          required: false
          description: >
            Keep only bindings of this one target row - a UUID, trimmed and
            lower-cased, and never a target number, code or name. Paired with
            `parentType`, it is also resolved as a real target before the query (404
            when it does not exist) and its Building or Client must be reachable by
            the actor (403), so an id of another tenant's row is not an empty answer
            but a refusal. Alone it is a 400.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: clientId
          in: query
          required: false
          description: >
            Restrict to one Client scope; the actor must be able to reach that Client
            or the request is 403 `BUILDING_ACCESS_DENIED`, and a malformed value is
            400. It narrows the accessible set, never widens it.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: buildingId
          in: query
          required: false
          description: >
            Restrict to one Building (same rules: 400 on a malformed value, 403 when
            it is not accessible). Bindings stored without a Building are never
            matched by this filter, because there is no implicit Building to fall
            back to.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: contextType
          in: query
          required: false
          description: >
            Restrict to the shared document context recorded on the binding row -
            trimmed and upper-cased, outside the enum is 400. It is the value copied
            at creation, not a re-read of the parent's current context.
          schema:
            $ref: "#/components/schemas/DocumentContextType"
        - name: documentType
          in: query
          required: false
          description: >
            Restrict to the type code of the BOUND Document, matched through a join on
            `documents.document_type` after trimming and upper-casing; an empty value
            is 400 `documentType must be a non-empty string.` and a shape outside
            2-64 characters of `[A-Z][A-Z0-9_-]*` is 400 `documentType is invalid.`.
            It filters only - it never creates a type, and no type list is returned.
          schema:
            type: string
      responses:
        "200":
          description: >
            Visible bindings, newest first (possibly empty), each with its bound
            document embedded.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items: { $ref: "#/components/schemas/SupportingDocument" }
        "400":
          description: >
            `VALIDATION_ERROR` - `parentType` or `contextType` outside its enum, a
            malformed `parentId` / `clientId` / `buildingId` UUID, `documentType`
            empty or malformed, or only one of `parentType` / `parentId` supplied.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.read` is missing - or
            `BUILDING_ACCESS_DENIED` for a `buildingId`, `clientId` or `parentId`
            filter the actor cannot reach.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `SUPPORTING_DOCUMENT_PARENT_NOT_FOUND` - a `parentType` / `parentId`
            filter naming a target row that does not exist. An empty result for a
            valid filter is not 404.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /supporting-documents/{id}:
    get:
      operationId: getSupportingDocument
      x-required-permission: document.read
      summary: Read one supporting document binding
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B5 (BE-22F). Requires `document.read`
        and returns the single binding row whose own id is `id`, with the bound
        Document re-read and embedded under `document`. The id is the binding's
        identity - neither the document id nor the parent id - and neither a
        `documentNumber` nor a target pair is accepted on this path.

        The row is resolved first (404), then isolation is asserted on the row's own
        Building - or on its Client when it has none - so a known but unreachable
        binding is 403 `BUILDING_ACCESS_DENIED` rather than 404, and the bound
        Document is loaded afterwards for embedding: a binding whose Document row has
        disappeared is reported with the SAME code, 404
        `SUPPORTING_DOCUMENT_NOT_FOUND`, not as a document-level 404, so one answer
        covers both halves of the pair.

        No state is required and no guard beyond that: there is no archived,
        `ARCHIVED`-parent, status or expiry check on read, and the parent target is
        deliberately not resolved at all - so `parentType` and `parentId` keep
        reporting the stored pair even when the target row has since been deleted,
        and this route never fails because of something the parent did. Reading
        writes nothing: no event, no view counter, no re-validation of the
        `fileReference` and no refresh of the copied Client, Building or context.
        There is no query parameter, no projection switch and no way to ask for the
        target to be expanded: `parentType` and `parentId` are identifiers only, and
        the only embedded object is the bound Document.
      tags: [Supporting Documents]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/SupportingDocumentIdPath"
      responses:
        "200":
          description: The requested binding with its bound document embedded.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { $ref: "#/components/schemas/SupportingDocument" }
        "400":
          description: >
            `VALIDATION_ERROR` - the path `id` is not a valid UUID (detail
            "Supporting document id must be a valid UUID.").
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            `PERMISSION_DENIED` - `document.read` is missing - or
            `BUILDING_ACCESS_DENIED` for the binding's Building, or its Client when
            the binding has none.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }
        "404":
          description: >
            `SUPPORTING_DOCUMENT_NOT_FOUND` - no binding row with that id, or a
            binding whose bound Document row no longer exists. The target
            (`parentType` / `parentId`) is not checked here, so a deleted parent is
            not 404 on this route.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorEnvelope" }

  /buildings/{buildingId}/handyman-requests:
    parameters:
      - name: buildingId
        in: path
        required: true
        description: Authoritative Building scope of the request.
        schema: { $ref: "#/components/schemas/Uuid" }
    post:
      operationId: createHandymanRequest
      x-required-permission: handyman_request.create
      x-building-scoped: true
      summary: Submit a Handyman Request for a Building
      description: >
        CR-HM-BE-01 RUN 3 — thin HTTP exposure of the Run 2 Handyman Request
        authority. The Building scope comes from the path, the actor only from
        the authenticated session (`createdByUserId`), the Client identity is
        derived from the Building, and `requestNumber`, `status` and
        `operationalSurface` are server-managed: none of them is accepted in
        the body (400 with a field detail). The body accepts ONLY the allowed
        intake fields of `CreateHandymanRequest` — any other field is
        rejected. An optional `Idempotency-Key` header is forwarded to the
        existing service: the same key with the same payload returns the
        original request; the same key with a different payload is 409
        `HANDYMAN_REQUEST_IDEMPOTENCY_CONFLICT`. Creation records a
        `HANDYMAN_REQUEST_CREATED` operational event with no customer PII.
        There is no lifecycle effect beyond `SUBMITTED` — no quotation,
        inspection, approval, provider/crew, scheduling, permit, WorkSession,
        QC, BAST, invoice, payment, pricing, ledger or warranty surface.
      tags: [Handyman Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: Idempotency-Key
          in: header
          required: false
          description: >
            Optional client request key (at most 255 characters). A replayed
            key with an identical payload returns the original handyman
            request; a replayed key with a different payload fails 409. It is
            never accepted in the JSON body.
          schema:
            type: string
            maxLength: 255
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateHandymanRequest" }
      responses:
        "201":
          description: >
            Handyman request submitted (or the original request returned for
            an `Idempotency-Key` replay). The internal idempotency
            fingerprint is never exposed.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties: { data: { $ref: "#/components/schemas/HandymanRequest" } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
    get:
      operationId: listBuildingHandymanRequests
      x-required-permission: handyman_request.read
      x-building-scoped: true
      summary: List Handyman Requests in a Building
      description: >
        Building-scoped list. The path `buildingId` is the authoritative
        scope: a `buildingId` query parameter can never widen it, and the
        caller must hold an ACTIVE Building assignment (403
        `BUILDING_ACCESS_DENIED` otherwise). Supports the existing
        limit/offset pagination convention and the already-supported filters.
      tags: [Handyman Requests]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: spaceId
          in: query
          required: false
          description: Filter by Space.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: status
          in: query
          required: false
          description: Filter by status.
          schema: { type: string, enum: [SUBMITTED, CANCELLED] }
        - name: inboundChannel
          in: query
          required: false
          description: Filter by inbound channel.
          schema: { type: string, enum: [WHATSAPP, PHONE, WALK_IN, OTHER] }
        - name: createdByUserId
          in: query
          required: false
          description: Filter by submitting user.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: tenantCompanyId
          in: query
          required: false
          description: Filter by Tenant Company.
          schema: { $ref: "#/components/schemas/Uuid" }
        - name: search
          in: query
          required: false
          description: Free-text search over request number, title and customer name.
          schema: { type: string }
        - name: limit
          in: query
          required: false
          description: Page size (1..100).
          schema: { type: integer, minimum: 1, maximum: 100 }
        - name: offset
          in: query
          required: false
          description: Page offset (non-negative integer).
          schema: { type: integer, minimum: 0 }
      responses:
        "200":
          description: Handyman requests of the Building.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties:
                      data: { type: array, items: { $ref: "#/components/schemas/HandymanRequest" } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /handyman-requests/{handymanRequestId}:
    parameters:
      - name: handymanRequestId
        in: path
        required: true
        description: Authoritative `handyman_requests.id`.
        schema: { $ref: "#/components/schemas/Uuid" }
    get:
      operationId: getHandymanRequest
      x-required-permission: handyman_request.read
      x-building-scoped: true
      summary: Get a Handyman Request
      description: >
        Returns one Handyman Request. Building access enforcement is preserved:
        the record is resolved first, then the caller's assignment to the
        record's Building is asserted — an inaccessible Building is 403
        `BUILDING_ACCESS_DENIED`, an unknown id is 404
        `HANDYMAN_REQUEST_NOT_FOUND`.
      tags: [Handyman Requests]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Handyman request.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties: { data: { $ref: "#/components/schemas/HandymanRequest" } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /handyman-requests/{handymanRequestId}/cancel:
    post:
      operationId: cancelHandymanRequest
      x-required-permission: handyman_request.manage
      x-building-scoped: true
      summary: Cancel a submitted Handyman Request
      description: >
        Calls the existing cancellation service only. A `SUBMITTED` request is
        cancelled (a `HANDYMAN_REQUEST_CANCELLED` operational event is
        recorded); cancelling an already-cancelled request is 409
        `HANDYMAN_REQUEST_ALREADY_CANCELLED`. There is deliberately no generic
        status-update surface on this path.
      tags: [Handyman Requests]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Cancelled handyman request.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/SuccessEnvelope"
                  - type: object
                    properties: { data: { $ref: "#/components/schemas/HandymanRequest" } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: opaque session token
      description: >
        Opaque internal User session token returned by `POST /auth/login` and
        carried as `Authorization: Bearer <token>`.
    vendorRfqSession:
      type: http
      scheme: bearer
      bearerFormat: invitation-scoped RFQ session token
      description: >
        Dedicated external Vendor RFQ session issued by
        `POST /vendor-rfq-access/exchange`. This is not an internal User
        session, does not grant RBAC permissions, and is limited to one Vendor,
        invitation and RFQ.

  headers:
    RequestId:
      description: >-
        Server-generated UUID identifying this HTTP request. The value is
        authoritative for this response and is never taken from a caller's
        `X-Request-ID` header.
      required: true
      schema:
        type: string
        format: uuid

  parameters:
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C3 - J09 Delivery / Security Binding identifiers.
    DeliveryCourierIdPath:
      name: id
      in: path
      description: Authoritative `delivery_couriers.id` (BE-13K row).
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    SecurityVisitorBindingIdPath:
      name: id
      in: path
      description: Authoritative `security_visitor_bindings.id` (BE-12J row).
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C2 - J09 Contractor Visitor / Host Confirmation identifiers.
    ContractorVisitorIdPath:
      name: id
      in: path
      description: Authoritative `contractor_visitors.id` (BE-13J visit metadata row).
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    HostConfirmationIdPath:
      name: id
      in: path
      description: Authoritative `host_confirmations.id`.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C1 - J09 Front Desk Log / Visitor Invitation identifiers.
    FrontDeskLogIdPath:
      name: id
      in: path
      description: Authoritative `front_desk_log` read-model row id (BE-13L projection).
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    VisitorInvitationIdPath:
      name: id
      in: path
      description: Authoritative `visitor_invitations.id`.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 01B3 - J09 Visitor Photo identifiers.
    VisitorIdNestedPath:
      name: visitorId
      in: path
      description: Authoritative `visitors.id` (shared BE-13A identity).
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    VisitorPhotoIdPath:
      name: id
      in: path
      description: Authoritative `visitor_photos.id` (BE-13E readiness row).
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 01B2 - J09 Visitor Pass identifier.
    VisitorPassIdPath:
      name: id
      in: path
      description: Authoritative `visitor_passes.id`.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 01B1 - J09 Visit Check-In identifier.
    VisitCheckInIdPath:
      name: id
      in: path
      description: Authoritative `visit_check_ins.id` (BE-13G/H check-in row).
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 01A - J09 Visitor Core identifiers.
    VisitorIdPath:
      name: id
      in: path
      description: Authoritative `visitors.id` (shared BE-13A identity).
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    ExpectedVisitorIdPath:
      name: id
      in: path
      description: Authoritative `expected_visitors.id`.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    WalkInVisitIdPath:
      name: id
      in: path
      description: Authoritative `walk_in_visits.id`.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    # CR-BE-MOB-CONTRACT-01 PART 02 — identity & Building context identifiers.
    UserIdPath:
      name: userId
      in: path
      description: User id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    RoleIdPath:
      name: roleId
      in: path
      description: Role id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    PermissionIdPath:
      name: permissionId
      in: path
      description: Permission id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    PropertyIdPath:
      name: propertyId
      in: path
      description: Property id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    ClientIdQuery:
      name: clientId
      in: query
      description: Optional Client filter.
      required: false
      schema: { $ref: "#/components/schemas/Uuid" }
    PropertyIdQuery:
      name: propertyId
      in: query
      description: Optional Property filter.
      required: false
      schema: { $ref: "#/components/schemas/Uuid" }
    # CR-BE-R2P-01 — Request-to-Pay chain identifiers.
    PurchaseOrderIdPath:
      name: id
      in: path
      description: Authoritative `purchase_orders.id`.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    PurchaseOrderLineIdPath:
      name: lineId
      in: path
      description: Authoritative `purchase_order_lines.id`.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    PriceCatalogEntryIdPath:
      name: id
      in: path
      description: Authoritative `price_catalog_entries.id`.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    ServiceCatalogEntryIdPath:
      name: id
      in: path
      description: Authoritative `service_catalog.id`.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    IdempotencyKeyHeader:
      name: Idempotency-Key
      in: header
      description: >
        Required idempotency key (≤ 200 chars). A replayed key with an
        identical payload returns the original result; a replayed key with a
        different payload fails 409 `PRICE_CATALOG_IDEMPOTENCY_CONFLICT`.
      required: true
      schema:
        type: string
        maxLength: 200
    WorkContractIdPath:
      name: id
      in: path
      description: Authoritative `work_contracts.id` (SPK).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    WorkOrderProcurementBindingIdPath:
      name: id
      in: path
      description: Authoritative `work_order_procurement_bindings.id`.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    BastDocumentIdPath:
      name: id
      in: path
      description: Authoritative canonical `bast_documents.id`.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    LegacyVendorBastIdPath:
      name: bastId
      in: path
      description: Legacy `vendor_bast_bindings.id` compatibility identifier.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    ClientIdPath:
      name: clientId
      in: path
      description: Client id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    ClientConfigurationIdPath:
      name: clientConfigurationId
      in: path
      description: Client configuration record id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    BuildingIdPath:
      name: buildingId
      in: path
      description: Building id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 02E2 - J10 Work Request identifier.
    WorkRequestIdPath:
      name: id
      in: path
      description: Authoritative Work Request record id (BE-08A).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03A - J11 Permit identifier.
    PermitIdPath:
      name: id
      in: path
      description: Authoritative `permits.id` (BE-20A foundation row).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03B - J11 Permit Application identifier.
    PermitApplicationIdPath:
      name: id
      in: path
      description: Authoritative `permit_applications.id` (BE-20C row).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03C - J11 Permit Approval identifier.
    PermitApprovalIdPath:
      name: id
      in: path
      description: Authoritative `permit_approvals.id` (BE-20F review binding row, `reviews.id` projection).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D1 - J11 Permit Equipment identifier.
    PermitEquipmentIdPath:
      name: id
      in: path
      description: Authoritative `permit_equipment.id` (BE-20I binding row).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D2 - J11 Permit Evidence identifier.
    PermitEvidenceIdPath:
      name: id
      in: path
      description: Authoritative `permit_evidence.id` (BE-20J evidence submission row).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03E1 - J11 Permit Safety Requirement identifier.
    PermitSafetyRequirementIdPath:
      name: id
      in: path
      description: Authoritative `permit_safety_requirements.id` (BE-20E prerequisite row).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03E2 - J11 Permit Validity identifier.
    PermitValidityIdPath:
      name: id
      in: path
      description: Authoritative `permit_validities.id` (BE-20G validity window row).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03G1 - J11 Permit Worker identifier.
    PermitWorkerIdPath:
      name: id
      in: path
      description: Authoritative `permit_workers.id` (BE-20H worker binding row).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03G2 - J11 Work Permit Readiness identifier.
    WorkPermitReadinessIdPath:
      name: readinessId
      in: path
      description: Authoritative `work_permit_readiness.id` (BE-15D binding row).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    UtilityResourceIdPath:
      name: id
      in: path
      description: Authoritative Utility resource id for the path.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    TenantCompanyIdPath:
      name: tenantCompanyId
      in: path
      description: Authoritative Tenant Company id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    FloorIdPath:
      name: floorId
      in: path
      description: Floor id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    AreaIdPath:
      name: areaId
      in: path
      description: Area id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    RoomIdPath:
      name: roomId
      in: path
      description: Room id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    SpaceIdPath:
      name: spaceId
      in: path
      description: Space id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    FunctionalLocationIdPath:
      name: functionalLocationId
      in: path
      description: Functional Location id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    AssetIdPath:
      name: assetId
      in: path
      description: Asset id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    EntitlementIdPath:
      name: entitlementId
      in: path
      description: Module entitlement id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    SubscriptionIdPath:
      name: subscriptionId
      in: path
      description: Subscription id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    BuildingConfigurationIdPath:
      name: buildingConfigurationId
      in: path
      description: Building configuration record id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    ModuleConfigurationIdPath:
      name: moduleConfigurationId
      in: path
      description: Module configuration record id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    FeatureEntitlementConfigurationIdPath:
      name: featureEntitlementConfigurationId
      in: path
      description: Feature entitlement configuration record id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    NavigationItemIdPath:
      name: navigationItemId
      in: path
      description: Navigation item id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    WorkspaceIdPath:
      name: workspaceId
      in: path
      description: Workspace id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    DashboardIdPath:
      name: dashboardId
      in: path
      description: Dashboard configuration id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    DashboardWidgetIdPath:
      name: dashboardWidgetId
      in: path
      description: Dashboard Widget id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    FormTemplateIdPath:
      name: formTemplateId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    FormSectionIdPath:
      name: formSectionId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    FormFieldIdPath:
      name: formFieldId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    ChecklistTemplateIdPath:
      name: checklistTemplateId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    ChecklistItemIdPath:
      name: checklistItemId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    ChecklistItemOptionIdPath:
      name: checklistItemOptionId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    UomIdPath:
      name: uomId
      in: path
      description: Unit of measure id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    EvidenceRequirementIdPath:
      name: evidenceRequirementId
      in: path
      description: Evidence requirement id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    OperationalSettingIdPath:
      name: operationalSettingId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    OrganizationPresentationIdPath:
      name: organizationPresentationId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    CmsContentIdPath:
      name: cmsContentId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    BrandingConfigurationIdPath:
      name: brandingConfigurationId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    ConfigurationVersionSourceTypePath:
      name: sourceType
      in: path
      required: true
      schema: { $ref: "#/components/schemas/ConfigurationVersionSourceType" }
    SourceConfigurationIdPath:
      name: sourceConfigurationId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    ConfigurationVersionIdPath:
      name: configurationVersionId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    ConfigurationPreviewIdPath:
      name: configurationPreviewId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    ConfigurationAuditEventIdPath:
      name: configurationAuditEventId
      in: path
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    CmsContentTypeQuery:
      name: contentType
      in: query
      required: false
      schema: { $ref: "#/components/schemas/CmsContentType" }
    CmsContentStatusQuery:
      name: status
      in: query
      required: false
      schema: { $ref: "#/components/schemas/CmsContentStatus" }
    ConfigurationStatusQuery:
      name: status
      in: query
      required: false
      schema: { type: string, enum: [ACTIVE, INACTIVE] }
    LimitParam:
      name: limit
      in: query
      description: Maximum number of items to return (default 50, max 100).
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 50
    OffsetParam:
      name: offset
      in: query
      description: Number of items to skip before the page.
      required: false
      schema:
        type: integer
        minimum: 0
        default: 0
    VerificationTargetTypeParam:
      name: targetType
      in: path
      description: >-
        Reviewable resource type (CHECKLIST_EXECUTION, FORM_INSTANCE,
        FINDING, WORK_ORDER). WORK_ORDER (CR-BE-MOB-03 PART 05) delegates to
        the existing BE-08I verification lifecycle — COMPLETED-only,
        already-APPROVED immutable, REWORK_REQUIRED moves the Work Order
        back to IN_PROGRESS.
      required: true
      schema:
        type: string
        enum: [CHECKLIST_EXECUTION, FORM_INSTANCE, FINDING, WORK_ORDER]
    VerificationTargetIdParam:
      name: targetId
      in: path
      description: Id of the reviewable resource (checklist execution / form instance / finding / work order).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    PageParam:
      name: page
      in: query
      description: >
        1-based page number (mobile opt-in pagination convention, BE-25A).
        Ignored when absent; without pagination parameters the full scoped
        list is returned.
      required: false
      schema:
        type: integer
        minimum: 1
        default: 1
    PageSizeParam:
      name: pageSize
      in: query
      description: >
        Page size (mobile opt-in pagination convention, BE-25A; default 50,
        max 200).
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 200
        default: 50
    TaskIdPathParam:
      name: taskId
      in: path
      description: Generated task id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    ExecutionIdPathParam:
      name: executionId
      in: path
      description: Checklist execution id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    WorkforceIdPathParam:
      name: workforceId
      in: path
      description: Workforce profile id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    SupervisorIdPathParam:
      name: supervisorId
      in: path
      description: Supervisor Workforce Profile id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    WorkforceReportingIdPath:
      name: id
      in: path
      description: Workforce Profile id of the reporting record.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    TeamIdPathParam:
      name: teamId
      in: path
      description: Team id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    DailyCleaningIdPath:
      name: id
      in: path
      required: true
      description: Daily cleaning id — the BE-07 generated task id.
      schema: { $ref: "#/components/schemas/Uuid" }
    CleaningAreaIdPath:
      name: id
      in: path
      required: true
      description: Cleaning Area id.
      schema: { $ref: "#/components/schemas/Uuid" }
    HkBindingCleaningAreaIdPath:
      name: cleaningAreaId
      in: path
      required: true
      description: Cleaning Area id (BE-11A).
      schema: { $ref: "#/components/schemas/Uuid" }
    ConsumableRequirementIdPath:
      name: id
      in: path
      required: true
      description: Consumable requirement id (BE-11J).
      schema: { $ref: "#/components/schemas/Uuid" }
    HkConsumableRequirementIdPath:
      name: requirementId
      in: path
      required: true
      description: Consumable requirement id (BE-11J) of the binding.
      schema: { $ref: "#/components/schemas/Uuid" }
    HkConsumableBindingIdPath:
      name: id
      in: path
      required: true
      description: Housekeeping consumable binding id (BE-16J).
      schema: { $ref: "#/components/schemas/Uuid" }
    HousekeepingComplaintBindingIdPath:
      name: id
      in: path
      required: true
      description: Housekeeping complaint binding id (BE-11L).
      schema: { $ref: "#/components/schemas/Uuid" }
    CorrectiveActionIdPath:
      name: id
      in: path
      required: true
      description: Corrective Action id (BE-21G).
      schema: { $ref: "#/components/schemas/Uuid" }
    ToiletInspectionBindingIdPath:
      name: id
      in: path
      required: true
      description: Toilet inspection binding id.
      schema: { $ref: "#/components/schemas/Uuid" }
    ToiletInspectionExecutionIdPath:
      name: id
      in: path
      required: true
      description: BE-07 checklist execution id started from a toilet binding.
      schema: { $ref: "#/components/schemas/Uuid" }
    PublicAreaInspectionBindingIdPath:
      name: id
      in: path
      required: true
      description: Public-area inspection binding id.
      schema: { $ref: "#/components/schemas/Uuid" }
    PublicAreaInspectionExecutionIdPath:
      name: id
      in: path
      required: true
      description: BE-07 checklist execution id started from a public-area binding.
      schema: { $ref: "#/components/schemas/Uuid" }
    SupervisorInspectionIdPath:
      name: id
      in: path
      required: true
      description: Supervisor inspection id.
      schema: { $ref: "#/components/schemas/Uuid" }
    QualityAuditIdPath:
      name: id
      in: path
      required: true
      description: Quality audit id.
      schema: { $ref: "#/components/schemas/Uuid" }
    HousekeepingFindingIdPath:
      name: id
      in: path
      required: true
      description: Housekeeping finding link id (not the BE-09 finding id).
      schema: { $ref: "#/components/schemas/Uuid" }
    HousekeepingEvidenceSourceTypePath:
      name: sourceType
      in: path
      required: true
      schema: { $ref: "#/components/schemas/HousekeepingEvidenceSourceType" }
    HousekeepingEvidenceSourceIdPath:
      name: sourceId
      in: path
      required: true
      description: Source record id matching sourceType.
      schema: { $ref: "#/components/schemas/Uuid" }
    SecurityPostIdPath:
      name: id
      in: path
      required: true
      description: Security Post id (BE-12A).
      schema: { $ref: "#/components/schemas/Uuid" }
    PatrolRouteIdPath:
      name: id
      in: path
      required: true
      description: Patrol Route id (BE-12B).
      schema: { $ref: "#/components/schemas/Uuid" }
    PatrolRoutePointIdPath:
      name: id
      in: path
      required: true
      description: Patrol Route Point (checkpoint) id (BE-12B).
      schema: { $ref: "#/components/schemas/Uuid" }
    PatrolScheduleBindingIdPath:
      name: id
      in: path
      required: true
      description: Patrol Schedule Binding id (BE-12C).
      schema: { $ref: "#/components/schemas/Uuid" }
    PatrolExecutionIdPath:
      name: id
      in: path
      required: true
      description: Patrol Execution id — the BE-07 generated task id.
      schema: { $ref: "#/components/schemas/Uuid" }
    PatrolPointIdPath:
      name: pointId
      in: path
      required: true
      description: Patrol Route Point (checkpoint) id being confirmed.
      schema: { $ref: "#/components/schemas/Uuid" }
    PatrolPointVisitIdPath:
      name: id
      in: path
      required: true
      description: Patrol Point Visit id (BE-12D).
      schema: { $ref: "#/components/schemas/Uuid" }
    PatrolChecklistBindingIdPath:
      name: id
      in: path
      required: true
      description: Patrol Checklist Binding id (BE-12E).
      schema: { $ref: "#/components/schemas/Uuid" }
    PatrolChecklistExecutionIdPath:
      name: id
      in: path
      required: true
      description: BE-07 checklist execution id started from a patrol binding.
      schema: { $ref: "#/components/schemas/Uuid" }
    SecurityFindingIdPath:
      name: id
      in: path
      required: true
      description: Security finding link id (not the BE-09 finding id).
      schema: { $ref: "#/components/schemas/Uuid" }
    IncidentIdPath:
      name: id
      in: path
      required: true
      description: Incident id (BE-21A foundation).
      schema: { $ref: "#/components/schemas/Uuid" }
    OperationalIncidentIdPath:
      name: id
      in: path
      required: true
      description: Operational Incident id — the shared BE-21A Incident id.
      schema: { $ref: "#/components/schemas/Uuid" }
    InspectionBindingIdPath:
      name: id
      in: path
      required: true
      description: Equipment inspection binding id (BE-10B).
      schema: { $ref: "#/components/schemas/Uuid" }
    InspectionExecutionIdPath:
      name: id
      in: path
      required: true
      description: BE-07 checklist execution id started from an inspection binding.
      schema: { $ref: "#/components/schemas/Uuid" }
    MeterReadingBindingIdPath:
      name: id
      in: path
      required: true
      description: Meter reading binding id (BE-10C).
      schema: { $ref: "#/components/schemas/Uuid" }
    MeterReadingExecutionIdPath:
      name: id
      in: path
      required: true
      description: BE-07 form instance id started from a meter reading binding.
      schema: { $ref: "#/components/schemas/Uuid" }
    LogSheetBindingIdPath:
      name: id
      in: path
      required: true
      description: Equipment log sheet binding id (BE-10D).
      schema: { $ref: "#/components/schemas/Uuid" }
    LogSheetExecutionIdPath:
      name: id
      in: path
      required: true
      description: BE-07 form instance id for one log sheet row.
      schema: { $ref: "#/components/schemas/Uuid" }
    EngineeringChecklistBindingIdPath:
      name: id
      in: path
      required: true
      description: Engineering checklist binding id (BE-10E).
      schema: { $ref: "#/components/schemas/Uuid" }
    EngineeringChecklistExecutionIdPath:
      name: id
      in: path
      required: true
      description: BE-07 checklist execution id started from an engineering binding.
      schema: { $ref: "#/components/schemas/Uuid" }
    BreakdownIdPath:
      name: id
      in: path
      required: true
      description: Breakdown / corrective event id (BE-10F).
      schema: { $ref: "#/components/schemas/Uuid" }
    MaintenanceBindingIdPath:
      name: id
      in: path
      required: true
      description: Planned maintenance binding id (BE-10G).
      schema: { $ref: "#/components/schemas/Uuid" }
    EngineeringFindingIdPath:
      name: id
      in: path
      required: true
      description: Engineering finding link id (not the BE-09 finding id).
      schema: { $ref: "#/components/schemas/Uuid" }
    EngineeringShiftHandoverIdPath:
      name: id
      in: path
      required: true
      description: Engineering shift handover id (BE-10J).
      schema: { $ref: "#/components/schemas/Uuid" }
    SecurityShiftHandoverBindingIdPath:
      name: id
      in: path
      required: true
      description: Security shift handover binding id (BE-12G).
      schema: { $ref: "#/components/schemas/Uuid" }
    AssetSparePartIdPath:
      name: id
      in: path
      required: true
      description: Asset ↔ spare-part Item binding id (BE-16H).
      schema: { $ref: "#/components/schemas/Uuid" }
    FindingIdPathParam:
      name: findingId
      in: path
      description: Finding id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    IdentifierPathParam:
      name: identifier
      in: path
      description: Asset identifier value (printed label / QR payload).
      required: true
      schema:
        type: string
    EvidenceIdPathParam:
      name: evidenceId
      in: path
      description: Evidence submission (file metadata record) id.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"

    PurchaseRequestIdPath:
      name: purchaseRequestId
      in: path
      description: Purchase Request id (BE-17A).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    ServiceRequestIdPath:
      name: serviceRequestId
      in: path
      description: Service Request id (BE-17C).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    MaterialRequestIdPath:
      name: id
      in: path
      description: Material Request line id (BE-17B).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    MaterialReservationSourceIdPath:
      name: materialRequestId
      in: path
      description: Existing Material Request line id used as reservation demand source.
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    MaterialReservationIdPath:
      name: id
      in: path
      description: Material reservation id (CR-BE-INV-CONTROL-01 PART 01).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    ProcurementApprovalIdPath:
      name: id
      in: path
      description: Procurement approval binding id (BE-17D).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    ReceivingIdPath:
      name: id
      in: path
      description: Receiving record id (BE-17G).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    InventoryItemIdPath:
      name: itemId
      in: path
      description: Inventory item id (BE-16A material / spare part identity).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    WarehouseIdPath:
      name: warehouseId
      in: path
      description: Inventory warehouse / store id (BE-16B).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    StockMovementIdPath:
      name: id
      in: path
      description: Inventory stock movement id (BE-16D ledger entry).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    StockBalanceIdPath:
      name: id
      in: path
      description: Inventory stock balance id (BE-16C).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    WorkOrderIdPath:
      name: workOrderId
      in: path
      description: Work Order id (BE-08).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    WorkOrderMaterialUsageIdPath:
      name: id
      in: path
      description: Work Order material usage id (BE-16I).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"
    VendorIdPath:
      name: vendorId
      in: path
      description: Vendor id (BE-13).
      required: true
      schema:
        $ref: "#/components/schemas/Uuid"

    FxRateIdPath:
      name: rateId
      in: path
      required: true
      description: FX rate id.
      schema: { type: string, format: uuid }
    FxClientIdPath:
      name: clientId
      in: path
      required: true
      description: Client id whose FX policy is addressed.
      schema: { type: string, format: uuid }
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 02B - J10 Tenant Service Request identifier.
    TenantServiceRequestIdPath:
      name: id
      in: path
      description: Authoritative `tenant_service_requests.id` (BE-14E).
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 02C - J10 Tenant Complaint identifier.
    TenantComplaintIdPath:
      name: id
      in: path
      description: Authoritative `tenant_complaints.id` (BE-14F).
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 02D - J10 Tenant Utility Request identifier.
    TenantUtilityRequestIdPath:
      name: id
      in: path
      description: Authoritative `tenant_utility_requests.id` (BE-14G).
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 02E1 - J10 Tenant Communication identifier.
    TenantCommunicationIdPath:
      name: id
      in: path
      description: Authoritative Tenant Communication record id (BE-14K).
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 04A - J12 Vendor Assignment identifier.
    VendorAssignmentIdPath:
      name: assignmentId
      in: path
      description: >
        Authoritative Vendor Assignment id (BE-15A). Must be a valid UUID -
        otherwise 400 `VALIDATION_ERROR`; an unknown id yields 404
        `VENDOR_ASSIGNMENT_NOT_FOUND`.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 04B1 - J12 Vendor Work identifier.
    VendorWorkIdPath:
      name: workId
      in: path
      description: >
        Authoritative Vendor Work id (BE-15B). Must be a valid UUID - otherwise
        400 `VALIDATION_ERROR`; an unknown id yields 404
        `VENDOR_WORK_NOT_FOUND`.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 04B2 - J12 Vendor Work sub-resource
    # identifier. The BE-15E / BE-15K routers register the Vendor Work id as a
    # bare `:id` segment (unlike the BE-15B `/vendor-works/{workId}` routes), so
    # this component is deliberately separate from VendorWorkIdPath.
    VendorWorkPathId:
      name: id
      in: path
      description: >
        Authoritative Vendor Work id for the `/vendor-works/{id}/...`
        sub-resource routes (BE-15E evidence, BE-15K history). Must be a valid
        UUID - otherwise 400 `VALIDATION_ERROR`; an unknown id yields 404
        `VENDOR_WORK_NOT_FOUND`.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 04B2 - J12 Vendor Work evidence
    # submission identifier. Kept separate from the generic BE-07
    # EvidenceIdPathParam because this id addresses the Vendor-Work-bound BE-15E
    # projection with its own not-found code.
    VendorWorkEvidenceIdPath:
      name: evidenceId
      in: path
      description: >
        Vendor Work evidence submission id (BE-15E projection of a BE-07
        `evidence_submissions` row). Must be a valid UUID - otherwise 400
        `VALIDATION_ERROR`; an unknown id yields 404
        `VENDOR_WORK_EVIDENCE_NOT_FOUND`.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 04C1 - J12 Vendor Completion Report
    # identifier.
    VendorCompletionReportIdPath:
      name: reportId
      in: path
      description: >
        Authoritative Vendor Completion Report id (BE-15F). Must be a valid
        UUID - otherwise 400 `VALIDATION_ERROR`; an unknown id yields 404
        `COMPLETION_REPORT_NOT_FOUND`.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 04D1 - J12 Acceptance Sign-Off
    # identifier.
    AcceptanceSignOffIdPath:
      name: id
      in: path
      description: >
        Acceptance sign-off id (BE-22E). Trimmed and lower-cased, and must be a
        valid UUID - otherwise 400 `VALIDATION_ERROR`; the existence check runs
        before Building isolation, so an unknown id is 404
        `ACCEPTANCE_SIGN_OFF_NOT_FOUND` while a known but inaccessible one is 403
        `BUILDING_ACCESS_DENIED`.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 04D2 - J12 Work Completion Document
    # identifier.
    WorkCompletionDocumentIdPath:
      name: id
      in: path
      description: >
        Work completion document binding id (BE-22B). Trimmed and lower-cased,
        and it must be a valid UUID - otherwise 400 `VALIDATION_ERROR` with
        detail "Work completion document id must be a valid UUID.". The
        existence check runs before Building isolation, so an unknown id is 404
        `WORK_COMPLETION_DOCUMENT_NOT_FOUND` while a known but inaccessible one
        is 403 `BUILDING_ACCESS_DENIED`. This is the binding id, not the shared
        document id: `documentId` is not a path key on this route.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 04D3 - J12 Handover Document identifier.
    HandoverDocumentIdPath:
      name: id
      in: path
      description: >
        Handover document binding id (BE-22D). Trimmed and lower-cased, and it
        must be a valid UUID - otherwise 400 `VALIDATION_ERROR` with detail
        "Handover document id must be a valid UUID.". The existence check runs
        before Building isolation, so an unknown id is 404
        `HANDOVER_DOCUMENT_NOT_FOUND` while a known but inaccessible one is 403
        `BUILDING_ACCESS_DENIED`. This is the handover row id, not the shared
        document id: the repository can look a row up by `documentId`, but no
        registered route exposes that lookup, and neither `handoverNumber` nor
        `workOrderId` is accepted as a path key here.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B1 - J19 generic Document identifier.
    DocumentIdPath:
      name: id
      in: path
      description: >
        Shared BE-22A Document row id. It is trimmed and lower-cased and must be
        a valid UUID, otherwise 400 `VALIDATION_ERROR` with detail "Document id
        must be a valid UUID.". The existence check runs before isolation, so an
        unknown id is 404 `DOCUMENT_NOT_FOUND` while a known but inaccessible one
        is 403 `BUILDING_ACCESS_DENIED`. This is the generic document id, not a
        Work Completion, Handover, BAST or Supporting Document binding id, and
        neither `documentNumber` nor a version number is accepted as a path key
        on any route of this contract part.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B2 - J19 version-history identifiers.
    DocumentVersionDocumentIdPath:
      name: documentId
      in: path
      description: >
        Parent generic Document id (BE-22A row) whose version history is
        addressed. Trimmed, lower-cased and must be a valid UUID, otherwise 400
        `VALIDATION_ERROR` with detail "Document id must be a valid UUID."; the
        404 answer for an unknown value is `DOCUMENT_NOT_FOUND`, never
        `DOCUMENT_VERSION_NOT_FOUND`. This is the document row id - not a Work
        Completion, Handover, BAST or Supporting Document binding id, and not a
        `versionId`.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    DocumentVersionIdPath:
      name: versionId
      in: path
      description: >
        Document Version row id (`document_versions.id`). Trimmed, lower-cased and
        must be a valid UUID, otherwise 400 `VALIDATION_ERROR` with detail
        "Version id must be a valid UUID.". The version row is resolved before the
        parent's isolation, so an unknown id is 404 `DOCUMENT_VERSION_NOT_FOUND`
        while a known but unreachable one is 403 `BUILDING_ACCESS_DENIED`. It is
        a row identity only - it is not a version number, and it never addresses
        the parent document.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    DocumentVersionNumberPath:
      name: versionNumber
      in: path
      description: >
        Version number inside the parent document's own numbering. The raw segment
        is coerced with `Number()` and must satisfy `Number.isInteger` and be
        greater than or equal to 1, otherwise 400 `VALIDATION_ERROR` with detail
        "versionNumber must be a positive integer." - so `0`, `-1`, `1.5` and a
        non-numeric segment are rejected, while padded or `N.0` spellings resolve
        to the same integer as `N`. The lookup is exact: an absent number is 404
        `DOCUMENT_VERSION_NOT_FOUND` and is never answered with the latest version
        or with the parent document's current state. `latest` is a separate
        registered path, matched before this parameter, so it can never arrive
        here as a number.
      required: true
      schema: { type: integer, minimum: 1 }
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B3 - J19 expiry targets.
    DocumentExpiryDocumentIdPath:
      name: documentId
      in: path
      description: >
        Generic Document row id (BE-22A) whose live expiry is read or replaced.
        Trimmed, lower-cased and required to be a valid UUID, otherwise 400
        `VALIDATION_ERROR` with detail "Document id must be a valid UUID."; an
        unknown value is 404 `DOCUMENT_NOT_FOUND`. This is the live document row,
        not a version id, and not a Work Completion, Handover, BAST or Supporting
        Document binding id.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    DocumentExpiryVersionIdPath:
      name: versionId
      in: path
      description: >
        Document Version row id (`document_versions.id`) used as the read target of
        the version GET and as the source snapshot of the version PUT. Trimmed,
        lower-cased and required to be a valid UUID, otherwise 400
        `VALIDATION_ERROR` with detail "Version id must be a valid UUID."; the row
        is resolved before its parent, so an unknown value is 404
        `DOCUMENT_VERSION_NOT_FOUND` and a version whose parent row is gone is 404
        `DOCUMENT_NOT_FOUND`. It is a row identity only - a version NUMBER is never
        accepted on these two paths, and on the PUT the id in the path is the
        addressed snapshot, not the row the write appends.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    DocumentApprovalDocumentIdPath:
      name: documentId
      in: path
      description: >
        Target Document id, whose generic BE-22A `documents` row scopes the
        approval collection - `POST` opens one pending review against it, `GET`
        lists every review targeting it or any of its versions, oldest first. It
        must be a UUID and is parsed by `parseDocumentIdParam`, which trims it and
        lowercases it before the UUID test, so the row is looked up in canonical
        form; the response `documentId` is therefore the canonical id even when the
        request spelled it differently. An unknown id is not reported as 404 here:
        this module answers 400 `DOCUMENT_APPROVAL_CONTEXT_INVALID`, because an
        unusable approval target is a context error rather than a missing
        collection. On the submit route the document additionally supplies the
        Client, Building and `contextType` recorded on the review row, and its
        `ARCHIVED` state alone blocks new submissions; it also bounds the `GET`
        list, which cannot be narrowed further. It is not accepted in a body - the
        route path is the only source of it - and it is never re-derived from a
        supplied `versionId`.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    DocumentApprovalIdPath:
      name: id
      in: path
      description: >
        Approval row id - the identity of the review record itself (its `id` in
        the response), NOT the document or version it targets. Both `/approve` and
        `/reject` address a pending review with it, and the same id addresses the
        single-row read. `parseDocumentApprovalIdParam` trims and lowercases it and
        requires a UUID, failing otherwise with 400 `VALIDATION_ERROR` (detail
        "Approval id must be a valid UUID."). The route permission is enforced
        before it is parsed, and the row it selects is then guarded by isolation,
        reviewer identity and pending state; an unknown id is 404
        `DOCUMENT_APPROVAL_NOT_FOUND` on all three routes. A caller cannot address
        an approval by document number, reviewer or `available_actions` hint, and
        the id is not accepted in a body.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
    SupportingDocumentIdPath:
      name: id
      in: path
      description: >
        Supporting document binding id - the identity of the pairing row itself (its
        `id` in the response), NOT the bound document id and NOT the parent target
        id. It is the only selector of the single-row read: this surface has no
        lookup by `documentId`, by the (`parentType`, `parentId`) pair or by the
        bound document's `documentNumber`, and the module's own lookup by document id
        is not exposed by any registered route. `parseSupportingDocumentIdParam`
        trims and lower-cases it and requires a UUID, failing otherwise with 400
        `VALIDATION_ERROR` (detail "Supporting document id must be a valid UUID."),
        so a differently spelled value still addresses the same row. It is resolved
        after the route permission and before isolation; an unknown value is 404
        `SUPPORTING_DOCUMENT_NOT_FOUND`, as is a known binding whose bound Document
        row is gone.
      required: true
      schema: { $ref: "#/components/schemas/Uuid" }
  responses:
    BuildingUtilityOperationalSummarySuccess:
      description: Building Utility operational summary response.
      content: { application/json: { schema: { $ref: "#/components/schemas/BuildingUtilityOperationalSummaryEnvelope" } } }
    UtilityExceptionSuccess:
      description: Utility operational exception response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityExceptionEnvelope" } } }
    UtilityExceptionListSuccess:
      description: Utility operational exception list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityExceptionListEnvelope" } } }
    UtilityReadingDueSuccess:
      description: Utility Reading Due response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityReadingDueEnvelope" } } }
    UtilityReadingDueListSuccess:
      description: Utility Reading Due list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityReadingDueListEnvelope" } } }
    UtilityOcrCandidateSuccess:
      description: Utility OCR candidate response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityOcrCandidateEnvelope" } } }
    UtilityMeterSuccess:
      description: Utility Meter response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityMeterEnvelope" } } }
    UtilityMeterListSuccess:
      description: Utility Meter list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityMeterListEnvelope" } } }
    UtilityHierarchySuccess:
      description: Utility Meter hierarchy response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityHierarchyEnvelope" } } }
    UtilityHierarchyListSuccess:
      description: Utility Meter hierarchy list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityHierarchyListEnvelope" } } }
    UtilityReadingSuccess:
      description: Utility Meter Reading response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityReadingEnvelope" } } }
    UtilityReadingListSuccess:
      description: Utility Meter Reading list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityReadingListEnvelope" } } }
    UtilityEvidenceSuccess:
      description: Utility Reading Evidence response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityEvidenceEnvelope" } } }
    UtilityEvidenceListSuccess:
      description: Utility Reading Evidence list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityEvidenceListEnvelope" } } }
    UtilityEvidenceReadinessSuccess:
      description: Utility Reading Evidence readiness response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityEvidenceReadinessEnvelope" } } }
    UtilityConsumptionSuccess:
      description: Utility Consumption response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityConsumptionEnvelope" } } }
    UtilityConsumptionListSuccess:
      description: Utility Consumption list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityConsumptionListEnvelope" } } }
    UtilityTariffSuccess:
      description: Utility Tariff response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityTariffEnvelope" } } }
    UtilityTariffListSuccess:
      description: Utility Tariff list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityTariffListEnvelope" } } }
    UtilityCalculationSuccess:
      description: Utility Calculation response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityCalculationEnvelope" } } }
    UtilityCalculationListSuccess:
      description: Utility Calculation list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityCalculationListEnvelope" } } }
    TenantUtilityApprovalSuccess:
      description: Tenant Approval response.
      content: { application/json: { schema: { $ref: "#/components/schemas/TenantUtilityApprovalEnvelope" } } }
    TenantUtilityApprovalListSuccess:
      description: Tenant Approval list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/TenantUtilityApprovalListEnvelope" } } }
    TenantApprovalActionsSuccess:
      description: Tenant Approval available-actions response.
      content: { application/json: { schema: { $ref: "#/components/schemas/TenantApprovalActionsEnvelope" } } }
    UtilityApprovalContextSuccess:
      description: Utility Calculation approval-context response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityApprovalContextEnvelope" } } }
    UtilityBillSuccess:
      description: Utility Bill response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityBillEnvelope" } } }
    UtilityBillListSuccess:
      description: Utility Bill list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityBillListEnvelope" } } }
    UtilityInvoiceReadySuccess:
      description: Invoice-ready Utility projection response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityInvoiceReadyEnvelope" } } }
    UtilityReconciliationSuccess:
      description: Building Utility reconciliation response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityReconciliationEnvelope" } } }
    UtilityReconciliationListSuccess:
      description: Building Utility reconciliation list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/UtilityReconciliationListEnvelope" } } }
    OperationalBudgetSuccess:
      description: Operational Budget response.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalBudgetEnvelope" } } }
    OperationalBudgetListSuccess:
      description: Operational Budget list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalBudgetListEnvelope" } } }
    OperationalBudgetCategorySuccess:
      description: Operational Budget Category response.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalBudgetCategoryEnvelope" } } }
    OperationalBudgetCategoryListSuccess:
      description: Operational Budget Category list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalBudgetCategoryListEnvelope" } } }
    OperationalBudgetSourceBindingSuccess:
      description: Operational Budget source binding response.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalBudgetSourceBindingEnvelope" } } }
    OperationalBudgetSourceBindingListSuccess:
      description: Operational Budget source binding list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalBudgetSourceBindingListEnvelope" } } }
    OperationalBudgetOverspendPolicySuccess:
      description: Operational Budget overspend policy transition response.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalBudgetOverspendPolicyEnvelope" } } }
    OperationalCommitmentSuccess:
      description: Operational Commitment response.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalCommitmentEnvelope" } } }
    OperationalCommitmentListSuccess:
      description: Operational Commitment list response.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalCommitmentListEnvelope" } } }
    OperationalCommitmentDetailSuccess:
      description: Operational Commitment with its append-only ledger entries.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalCommitmentDetailEnvelope" } } }
    OperationalBudgetVarianceSuccess:
      description: Derived Operational Budget variance response.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalBudgetVarianceEnvelope" } } }
    OperationalBudgetVarianceListSuccess:
      description: Derived Operational Budget variance summaries.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalBudgetVarianceListEnvelope" } } }
    OperationalBudgetTraceabilitySuccess:
      description: Operational Budget source traceability response.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalBudgetTraceabilityEnvelope" } } }
    OperationalBudgetAggregationSuccess:
      description: Read-time Operational Budget aggregation response.
      content: { application/json: { schema: { $ref: "#/components/schemas/OperationalBudgetAggregationEnvelope" } } }
    BadRequest:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: Request validation failed.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
          examples:
            validation:
              value:
                success: false
                error:
                  code: VALIDATION_ERROR
                  message: Request validation failed.
                  details:
                    - field: email
                      message: Email is required and must be a valid email address.
    Unauthorized:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: >
        Missing, invalid or expired session (protected routes). Login failures
        use `InvalidCredentials` (401 `INVALID_CREDENTIALS`) instead. Mobile
        must treat any 401 as "re-authenticate".
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
          examples:
            missingSession:
              value:
                success: false
                error:
                  code: AUTHENTICATION_REQUIRED
                  message: Authentication is required.
            invalidSession:
              value:
                success: false
                error:
                  code: INVALID_SESSION
                  message: The session is not valid.
            sessionExpired:
              value:
                success: false
                error:
                  code: SESSION_EXPIRED
                  message: The session has expired.
    InvalidCredentials:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: >
        Login failed: the email is unknown, the password is wrong, or the
        account is not in a login-allowed state. Deliberately generic so the
        three cases are indistinguishable (no account enumeration).
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
          examples:
            invalidCredentials:
              value:
                success: false
                error:
                  code: INVALID_CREDENTIALS
                  message: Invalid email or password.
    Forbidden:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: >
        Authenticated but the required permission is missing, or the caller
        lacks an ACTIVE assignment to the operation's resolved Building.
        Building-scoped resources do not widen access through a Client,
        Property, Vendor, or sibling Building relationship.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
          examples:
            permissionDenied:
              value:
                success: false
                error:
                  code: PERMISSION_DENIED
                  message: You do not have permission to perform this action.
            buildingAccessDenied:
              value:
                success: false
                error:
                  code: BUILDING_ACCESS_DENIED
                  message: Access to the requested building context is denied.
    NotFound:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: The requested resource does not exist.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
          examples:
            notFound:
              value:
                success: false
                error:
                  code: NOT_FOUND
                  message: Resource not found.
    Conflict:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: The request conflicts with existing state (e.g. a duplicate key).
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    UnprocessableEntity:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: The request is well-formed but semantically invalid (e.g. overpayment, business rule violation).
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
    RateLimited:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: Too many attempts (login throttle).
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
          examples:
            rateLimited:
              value:
                success: false
                error:
                  code: AUTH_RATE_LIMITED
                  message: Too many login attempts. Please try again later.
    InternalServerError:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: Unhandled server error. Stack traces are never returned.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
          examples:
            internal:
              value:
                success: false
                error:
                  code: INTERNAL_SERVER_ERROR
                  message: An unexpected server error occurred.
    DatabaseUnavailable:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: Database connection is unavailable.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorEnvelope"
          examples:
            database:
              value:
                success: false
                error:
                  code: DATABASE_UNAVAILABLE
                  message: Database connection is unavailable.

    # ------------------------------------------------------------------
    # CR-BE-FX-01 PART 02 — FX Rate Authority + Client FX Policy
    # ------------------------------------------------------------------
    FxRateSuccess:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: One FX rate.
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/SuccessEnvelope"
              - type: object
                properties:
                  data:
                    $ref: "#/components/schemas/FxRate"
    FxRateListSuccess:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: FX rates matching the filters.
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/SuccessEnvelope"
              - type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FxRate"
    FxRateEventListSuccess:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: >
        The append-only audit trail of one FX rate, oldest first. Rows can never
        be updated or deleted.
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/SuccessEnvelope"
              - type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FxRateEvent"
    FxRateSupersessionSuccess:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: >
        The superseded incumbent and its ACTIVE successor, linked exactly in
        both directions (`supersededByRateId` / `supersedesRateId`).
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/SuccessEnvelope"
              - type: object
                properties:
                  data:
                    $ref: "#/components/schemas/FxRateSupersessionResult"
    ClientFxPolicyReadSuccess:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: >
        The Client FX policy read model. `policy` is `null` when no policy
        exists, and `fxAvailable` is `false` in that case — absence of a policy
        is never treated as permission.
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/SuccessEnvelope"
              - type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ClientFxPolicyReadModel"
    ClientFxPolicySuccess:
      headers:
        X-Request-ID:
          $ref: "#/components/headers/RequestId"
      description: The stored Client FX policy.
      content:
        application/json:
          schema:
            allOf:
              - $ref: "#/components/schemas/SuccessEnvelope"
              - type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ClientFxPolicy"

  schemas:
    # ------------------------------------------------------------------
    # CR-BE-UTL-01 PART 13 — Utility domain
    # ------------------------------------------------------------------
    UtilityType:
      type: string
      enum: [ELECTRICITY, WATER, GAS]
    UtilityBillableType:
      type: string
      enum: [ELECTRICITY, WATER]
    UtilityMeterPurpose:
      type: string
      enum: [TENANT, BUILDING, COMMON_AREA, ENERGY_SOURCE]
    UtilityMeterStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    UtilityReadingSource:
      type: string
      enum: [MANUAL, ENGINEERING, IMPORT, SYSTEM]
    UtilityReadingType:
      type: string
      enum: [ACTUAL, ESTIMATED]
    UtilityExceptionType:
      type: string
      enum: [ABNORMAL_CONSUMPTION, MISSING_OR_LATE_READING, OCR_MANUAL_FOLLOW_UP, RECONCILIATION_VARIANCE, UNALLOCATED_CONSUMPTION, OTHER]
    UtilityExceptionSeverity:
      type: string
      enum: [LOW, MEDIUM, HIGH, CRITICAL]
    UtilityExceptionStatus:
      type: string
      enum: [OPEN, UNDER_REVIEW, RESOLVED, CANCELLED]
    UtilityReadingDueStatus:
      type: string
      enum: [DUE, COMPLETED, OVERDUE, CANCELLED]
    UtilityOcrStatus:
      type: string
      enum: [PENDING_REVIEW, ACCEPTED, REJECTED]
    UtilityCalculationStatus:
      type: string
      enum: [DRAFT, FINALIZED, SUPERSEDED]
    TenantApprovalStatus:
      type: string
      enum: [PENDING, APPROVED, REJECTED]
    UtilityBillStatus:
      type: string
      enum: [DRAFT, ISSUED, CANCELLED]
    UtilityTariffStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    UtilityErrorCode:
      type: string
      description: Stable Utility errors; all are returned in ErrorEnvelope.error.code.
      enum:
        - UTILITY_METER_NOT_FOUND
        - UTILITY_METER_READING_NOT_FOUND
        - UTILITY_METER_READING_ALREADY_EXISTS
        - UTILITY_METER_READING_IMMUTABLE
        - UTILITY_METER_CONSUMPTION_READING_INVALID
        - UTILITY_METER_CONSUMPTION_PERIOD_INVALID
        - UTILITY_METER_CONSUMPTION_UOM_MISMATCH
        - UTILITY_METER_CONSUMPTION_TENANT_MISMATCH
        - UTILITY_EXCEPTION_NOT_FOUND
        - UTILITY_EXCEPTION_REFERENCE_INVALID
        - UTILITY_EXCEPTION_ALREADY_OPEN
        - UTILITY_EXCEPTION_TRANSITION_INVALID
        - UTILITY_READING_DUE_NOT_FOUND
        - UTILITY_READING_DUE_ALREADY_EXISTS
        - UTILITY_READING_DUE_SCHEDULE_INVALID
        - UTILITY_READING_DUE_TRANSITION_INVALID
        - UTILITY_READING_DUE_READING_MISMATCH
        - UTILITY_OCR_CANDIDATE_NOT_FOUND
        - UTILITY_OCR_EVIDENCE_INVALID
        - UTILITY_OCR_CANDIDATE_ALREADY_EXISTS
        - UTILITY_OCR_DECISION_FINAL
        - UTILITY_OCR_READING_MISMATCH
        - UTILITY_TARIFF_NOT_FOUND
        - UTILITY_TARIFF_INVALID
        - UTILITY_TARIFF_PERIOD_OVERLAP
        - TENANT_APPROVAL_CONTEXT_MISMATCH
        - TENANT_APPROVAL_ALREADY_DECIDED
        - UTILITY_BILL_APPROVAL_REQUIRED
        - UTILITY_BILL_APPROVAL_REJECTED
        - UTILITY_BILL_ALREADY_EXISTS
        - BUILDING_UTILITY_RECONCILIATION_NO_SOURCE
        - BUILDING_UTILITY_RECONCILIATION_UOM_MISMATCH
        - BUILDING_UTILITY_RECONCILIATION_AREA_MISSING
        - BUILDING_UTILITY_RECONCILIATION_ALREADY_EXISTS
        - BUILDING_ACCESS_DENIED
        - PERMISSION_DENIED
        - VALIDATION_ERROR

    CreateUtilityMeterRequest:
      type: object
      required: [code, name, utilityType, uomId]
      properties:
        code: { type: string, maxLength: 64 }
        name: { type: string, maxLength: 160 }
        utilityType: { $ref: "#/components/schemas/UtilityType" }
        purpose: { $ref: "#/components/schemas/UtilityMeterPurpose" }
        uomId: { $ref: "#/components/schemas/Uuid" }
        spaceId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        functionalLocationId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        serialNumber: { type: string, nullable: true, maxLength: 128 }
        status: { $ref: "#/components/schemas/UtilityMeterStatus" }
    UpdateUtilityMeterRequest:
      type: object
      minProperties: 1
      properties:
        name: { type: string }
        utilityType: { $ref: "#/components/schemas/UtilityType" }
        uomId: { $ref: "#/components/schemas/Uuid" }
        spaceId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        functionalLocationId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        serialNumber: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/UtilityMeterStatus" }
    UtilityMeter:
      type: object
      required: [id, clientId, buildingId, code, name, utilityType, purpose, uomId, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        spaceId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        functionalLocationId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        code: { type: string }
        name: { type: string }
        utilityType: { $ref: "#/components/schemas/UtilityType" }
        purpose: { $ref: "#/components/schemas/UtilityMeterPurpose" }
        uomId: { $ref: "#/components/schemas/Uuid" }
        serialNumber: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/UtilityMeterStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateUtilityMeterHierarchyRequest:
      type: object
      required: [subMeterId]
      properties:
        subMeterId: { $ref: "#/components/schemas/Uuid" }
        effectiveFrom: { type: string, format: date-time }
        effectiveUntil: { type: string, format: date-time, nullable: true }
    UtilityMeterHierarchy:
      type: object
      required: [id, clientId, mainMeterId, subMeterId, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        mainMeterId: { $ref: "#/components/schemas/Uuid" }
        subMeterId: { $ref: "#/components/schemas/Uuid" }
        effectiveFrom: { type: string, format: date-time, nullable: true }
        effectiveUntil: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/UtilityMeterStatus" }

    RecordUtilityReadingRequest:
      type: object
      required: [readingValue, readingAt]
      properties:
        readingValue: { type: number, minimum: 0 }
        readingAt: { type: string, format: date-time }
        uomId: { $ref: "#/components/schemas/Uuid" }
        source: { $ref: "#/components/schemas/UtilityReadingSource" }
        readingType: { $ref: "#/components/schemas/UtilityReadingType" }
        tenantCompanyId: { $ref: "#/components/schemas/Uuid" }
        meterReadingBindingId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        formInstanceId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        notes: { type: string, nullable: true, maxLength: 1024 }
    UtilityMeterReading:
      type: object
      required: [id, clientId, buildingId, meterId, uomId, readingValue, readingAt, source, readingType, recordedByUserId]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        meterId: { $ref: "#/components/schemas/Uuid" }
        uomId: { $ref: "#/components/schemas/Uuid" }
        readingValue: { type: number }
        readingAt: { type: string, format: date-time }
        source: { $ref: "#/components/schemas/UtilityReadingSource" }
        readingType: { $ref: "#/components/schemas/UtilityReadingType" }
        recordedByUserId: { $ref: "#/components/schemas/Uuid" }
        tenantAssignmentId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        tenantCompanyId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        meterReadingBindingId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        formInstanceId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        notes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    SubmitUtilityReadingEvidenceRequest:
      type: object
      required: [evidenceType, fileReference, originalFileName, mimeType, fileSize]
      properties:
        evidenceType: { type: string, enum: [PHOTO, DOCUMENT, SIGNATURE] }
        evidenceRequirementId: { $ref: "#/components/schemas/Uuid" }
        fileReference: { type: string }
        originalFileName: { type: string }
        mimeType: { type: string }
        fileSize: { type: integer, minimum: 1, maximum: 52428800 }
        capturedAt: { type: string, format: date-time }
    UtilityReadingEvidence:
      type: object
      required: [id, clientId, meterReadingId, evidenceType, fileReference, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        meterReadingId: { $ref: "#/components/schemas/Uuid" }
        evidenceRequirementId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        evidenceType: { type: string, enum: [PHOTO, DOCUMENT, SIGNATURE] }
        fileReference: { type: string }
        originalFileName: { type: string }
        mimeType: { type: string }
        fileSize: { type: integer }
        capturedAt: { type: string, format: date-time, nullable: true }
        submittedByUserId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        status: { type: string, enum: [ACTIVE, REMOVED] }
    UtilityEvidenceReadiness:
      type: object
      required: [meterReadingId, ready, missingEvidenceTypes, requirements]
      properties:
        meterReadingId: { $ref: "#/components/schemas/Uuid" }
        ready: { type: boolean }
        missingEvidenceTypes: { type: array, items: { type: string } }
        requirements: { type: array, items: { type: object } }

    BuildingUtilityReconciliationSummary:
      type: object
      required: [reconciliationId, utilityType, periodStart, periodEnd, sourceConsumption, tenantConsumption, commonAreaConsumption, unallocatedConsumption, performanceMetric, performanceValue, applicableAreaSqm, consumptionUomId, performanceUom, calculatedAt]
      properties:
        reconciliationId: { $ref: "#/components/schemas/Uuid" }
        utilityType: { $ref: "#/components/schemas/UtilityBillableType" }
        periodStart: { type: string, format: date-time }
        periodEnd: { type: string, format: date-time }
        sourceConsumption: { type: number }
        tenantConsumption: { type: number }
        commonAreaConsumption: { type: number }
        unallocatedConsumption: { type: number }
        reconciliationPercentage: { type: number, nullable: true }
        performanceMetric: { type: string, enum: [IKE, IKA] }
        performanceValue: { type: number }
        applicableAreaSqm: { type: number }
        consumptionUomId: { $ref: "#/components/schemas/Uuid" }
        performanceUom: { type: string, enum: [kWh/m², m³/m²] }
        calculatedAt: { type: string, format: date-time }
    BuildingUtilityOperationalSummary:
      type: object
      required: [clientId, buildingId, reportingPeriod, reconciliations, meters, exceptions, billingReadiness, asOf]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        reportingPeriod:
          type: object
          required: [start, end]
          properties:
            start: { type: string, format: date-time }
            end: { type: string, format: date-time }
        reconciliations:
          type: array
          items: { $ref: "#/components/schemas/BuildingUtilityReconciliationSummary" }
        meters:
          type: object
          required: [totalActiveMeters, byPurpose, readingDue]
          properties:
            totalActiveMeters: { type: integer }
            byPurpose:
              type: object
              required: [TENANT, BUILDING, COMMON_AREA, ENERGY_SOURCE]
              properties:
                TENANT: { type: integer }
                BUILDING: { type: integer }
                COMMON_AREA: { type: integer }
                ENERGY_SOURCE: { type: integer }
            readingDue:
              type: object
              required: [due, overdue, completed, cancelled]
              properties:
                due: { type: integer }
                overdue: { type: integer }
                completed: { type: integer }
                cancelled: { type: integer }
        exceptions:
          type: object
          required: [total, byType, bySeverity, byStatus]
          properties:
            total: { type: integer }
            byType: { type: object, additionalProperties: { type: integer } }
            bySeverity:
              type: object
              required: [LOW, MEDIUM, HIGH, CRITICAL]
              properties:
                LOW: { type: integer }
                MEDIUM: { type: integer }
                HIGH: { type: integer }
                CRITICAL: { type: integer }
            byStatus:
              type: object
              required: [OPEN, UNDER_REVIEW, RESOLVED, CANCELLED]
              properties:
                OPEN: { type: integer }
                UNDER_REVIEW: { type: integer }
                RESOLVED: { type: integer }
                CANCELLED: { type: integer }
        billingReadiness:
          type: object
          required: [calculationsAwaitingApproval, approvedCalculations, rejectedCalculations, utilityBillsCreated, invoiceReadyUtilityBills]
          properties:
            calculationsAwaitingApproval: { type: integer }
            approvedCalculations: { type: integer }
            rejectedCalculations: { type: integer }
            utilityBillsCreated: { type: integer }
            invoiceReadyUtilityBills: { type: integer }
        asOf: { type: string, format: date-time }
    CreateUtilityExceptionRequest:
      type: object
      required: [exceptionType, severity, summary]
      description: At least one authoritative Utility reference is required.
      properties:
        meterId: { $ref: "#/components/schemas/Uuid" }
        meterReadingId: { $ref: "#/components/schemas/Uuid" }
        readingDueId: { $ref: "#/components/schemas/Uuid" }
        consumptionId: { $ref: "#/components/schemas/Uuid" }
        abnormalConsumptionId: { $ref: "#/components/schemas/Uuid" }
        ocrCandidateId: { $ref: "#/components/schemas/Uuid" }
        reconciliationId: { $ref: "#/components/schemas/Uuid" }
        exceptionType: { $ref: "#/components/schemas/UtilityExceptionType" }
        severity: { $ref: "#/components/schemas/UtilityExceptionSeverity" }
        summary: { type: string, maxLength: 300 }
        details: { type: string, nullable: true, maxLength: 2000 }
    StartUtilityExceptionReviewRequest:
      type: object
      properties:
        reviewNotes: { type: string, nullable: true, maxLength: 2000 }
    ResolveUtilityExceptionRequest:
      type: object
      required: [resolutionNotes]
      properties:
        resolutionNotes: { type: string, maxLength: 2000 }
    CancelUtilityExceptionRequest:
      type: object
      required: [reason]
      properties:
        reason: { type: string, maxLength: 2000 }
    UtilityOperationalException:
      type: object
      required: [id, clientId, buildingId, utilityType, exceptionType, severity, status, summary, detectedAt, detectedByUserId]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        utilityType: { $ref: "#/components/schemas/UtilityType" }
        meterId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        meterReadingId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        readingDueId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        consumptionId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        abnormalConsumptionId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        ocrCandidateId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        reconciliationId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        exceptionType: { $ref: "#/components/schemas/UtilityExceptionType" }
        severity: { $ref: "#/components/schemas/UtilityExceptionSeverity" }
        status: { $ref: "#/components/schemas/UtilityExceptionStatus" }
        summary: { type: string }
        details: { type: string, nullable: true }
        detectedAt: { type: string, format: date-time }
        detectedByUserId: { $ref: "#/components/schemas/Uuid" }
        reviewerUserId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        reviewNotes: { type: string, nullable: true }
        reviewStartedAt: { type: string, format: date-time, nullable: true }
        resolvedByUserId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        resolvedAt: { type: string, format: date-time, nullable: true }
        resolutionNotes: { type: string, nullable: true }
        cancelledByUserId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        cancelledAt: { type: string, format: date-time, nullable: true }
        cancellationReason: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateUtilityReadingDueRequest:
      type: object
      required: [periodStart, periodEnd, dueAt]
      properties:
        periodStart: { type: string, format: date-time }
        periodEnd: { type: string, format: date-time }
        dueAt: { type: string, format: date-time }
        scheduleDefinitionId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        generatedTaskId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
    CompleteUtilityReadingDueRequest:
      type: object
      required: [meterReadingId]
      properties:
        meterReadingId: { $ref: "#/components/schemas/Uuid" }
    CancelUtilityReadingDueRequest:
      type: object
      required: [reason]
      properties:
        reason: { type: string, maxLength: 1000 }
    UtilityReadingDue:
      type: object
      required: [id, clientId, buildingId, meterId, utilityType, periodStart, periodEnd, dueAt, status, createdByUserId, createdAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        meterId: { $ref: "#/components/schemas/Uuid" }
        utilityType: { $ref: "#/components/schemas/UtilityType" }
        periodStart: { type: string, format: date-time }
        periodEnd: { type: string, format: date-time }
        dueAt: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/UtilityReadingDueStatus" }
        scheduleDefinitionId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        generatedTaskId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        meterReadingId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        completedByUserId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        cancelledAt: { type: string, format: date-time, nullable: true }
        cancelledByUserId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        cancellationReason: { type: string, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateUtilityOcrCandidateRequest:
      type: object
      required: [candidateReadingValue]
      properties:
        candidateReadingValue: { type: number, minimum: 0 }
        confidence: { type: number, minimum: 0, maximum: 1, nullable: true }
    AcceptUtilityOcrCandidateRequest:
      type: object
      properties:
        meterReadingId: { $ref: "#/components/schemas/Uuid" }
        readingAt: { type: string, format: date-time }
        readingDueId: { $ref: "#/components/schemas/Uuid" }
        notes: { type: string, nullable: true, maxLength: 1000 }
      description: meterReadingId and readingAt are mutually exclusive.
    RejectUtilityOcrCandidateRequest:
      type: object
      required: [decisionNotes]
      properties:
        decisionNotes: { type: string, maxLength: 1000 }
    UtilityOcrCandidate:
      type: object
      required: [id, clientId, buildingId, meterId, evidenceId, candidateReadingValue, status, createdByUserId, createdAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        meterId: { $ref: "#/components/schemas/Uuid" }
        evidenceId: { $ref: "#/components/schemas/Uuid" }
        candidateReadingValue: { type: number }
        confidence: { type: number, nullable: true, minimum: 0, maximum: 1 }
        status: { $ref: "#/components/schemas/UtilityOcrStatus" }
        acceptedReadingId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        verifiedByUserId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        verifiedAt: { type: string, format: date-time, nullable: true }
        decisionNotes: { type: string, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CalculateUtilityConsumptionRequest:
      type: object
      required: [currentReadingId]
      properties:
        previousReadingId: { $ref: "#/components/schemas/Uuid" }
        currentReadingId: { $ref: "#/components/schemas/Uuid" }
        notes: { type: string, nullable: true }
    UtilityConsumption:
      type: object
      required: [id, clientId, buildingId, meterId, previousReadingId, currentReadingId, uomId, consumptionValue, periodStart, periodEnd]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        meterId: { $ref: "#/components/schemas/Uuid" }
        previousReadingId: { $ref: "#/components/schemas/Uuid" }
        currentReadingId: { $ref: "#/components/schemas/Uuid" }
        uomId: { $ref: "#/components/schemas/Uuid" }
        consumptionValue: { type: number, minimum: 0 }
        periodStart: { type: string, format: date-time }
        periodEnd: { type: string, format: date-time }
        tenantAssignmentId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        tenantCompanyId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        calculatedAt: { type: string, format: date-time }

    CreateUtilityTariffRequest:
      type: object
      required: [utilityType, currency, uomId, ratePerUom, effectiveFrom]
      properties:
        utilityType: { $ref: "#/components/schemas/UtilityBillableType" }
        currency: { type: string, pattern: "^[A-Z]{3}$" }
        uomId: { $ref: "#/components/schemas/Uuid" }
        ratePerUom: { oneOf: [{ type: number, minimum: 0 }, { type: string, pattern: "^(?:0|[1-9][0-9]*)(?:\\.[0-9]{1,8})?$" }] }
        effectiveFrom: { type: string, format: date-time }
        effectiveUntil: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/UtilityTariffStatus" }
    UtilityTariff:
      type: object
      required: [id, clientId, buildingId, utilityType, currency, uomId, ratePerUom, effectiveFrom, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        utilityType: { $ref: "#/components/schemas/UtilityBillableType" }
        currency: { type: string }
        uomId: { $ref: "#/components/schemas/Uuid" }
        ratePerUom: { type: number }
        effectiveFrom: { type: string, format: date-time }
        effectiveUntil: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/UtilityTariffStatus" }

    CalculateUtilityChargeRequest:
      type: object
      properties:
        calculationBasisId: { $ref: "#/components/schemas/Uuid" }
        notes: { type: string, nullable: true }
    UtilityCalculation:
      type: object
      required: [id, clientId, buildingId, meterId, utilityType, consumptionId, consumptionQuantity, uomId, appliedRateValue, calculatedAmount, periodStart, periodEnd, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        meterId: { $ref: "#/components/schemas/Uuid" }
        utilityType: { $ref: "#/components/schemas/UtilityType" }
        consumptionId: { $ref: "#/components/schemas/Uuid" }
        calculationBasisId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        consumptionQuantity: { type: number }
        uomId: { $ref: "#/components/schemas/Uuid" }
        tariffId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        tariffRate: { type: number, nullable: true }
        currency: { type: string, nullable: true }
        appliedRateValue: { type: number }
        calculatedAmount: { type: number }
        periodStart: { type: string, format: date-time }
        periodEnd: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/UtilityCalculationStatus" }
        tenantAssignmentId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        tenantCompanyId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        supersedesCalculationId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        calculatedAt: { type: string, format: date-time }
        finalizedAt: { type: string, format: date-time, nullable: true }

    CreateTenantApprovalRequest:
      type: object
      required: [requestType, requestId, approvalType, approverUserId]
      properties:
        requestType: { type: string, enum: [SERVICE_REQUEST, COMPLAINT, UTILITY_REQUEST, UTILITY_CALCULATION] }
        requestId: { $ref: "#/components/schemas/Uuid" }
        approvalType: { type: string }
        approverUserId: { $ref: "#/components/schemas/Uuid" }
    TenantApprovalDecisionRequest:
      type: object
      properties:
        decisionNotes: { type: string, maxLength: 1000 }
    TenantApprovalActions:
      type: object
      required: [approvalId, state, availableActions]
      properties:
        approvalId: { $ref: "#/components/schemas/Uuid" }
        state: { $ref: "#/components/schemas/TenantApprovalStatus" }
        availableActions: { type: array, items: { type: string, enum: [APPROVE, REJECT] } }
    TenantUtilityApproval:
      type: object
      required: [id, requestType, requestId, tenantCompanyId, buildingId, approvalType, approverUserId, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        requestType: { type: string }
        requestId: { $ref: "#/components/schemas/Uuid" }
        utilityCalculationId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        tenantCompanyId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        utilitySpaceId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        utilityMeterId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        utilityMeterPurpose: { allOf: [{ $ref: "#/components/schemas/UtilityMeterPurpose" }], nullable: true }
        utilityType: { allOf: [{ $ref: "#/components/schemas/UtilityBillableType" }], nullable: true }
        utilityPeriodStart: { type: string, format: date-time, nullable: true }
        utilityPeriodEnd: { type: string, format: date-time, nullable: true }
        utilityConsumptionQuantity: { type: number, nullable: true }
        utilityUomId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        utilityTariffId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        utilityTariffRate: { type: number, nullable: true }
        utilityCalculatedAmount: { type: number, nullable: true }
        utilityCurrency: { type: string, nullable: true }
        approvalType: { type: string }
        approverUserId: { $ref: "#/components/schemas/Uuid" }
        status: { $ref: "#/components/schemas/TenantApprovalStatus" }
        decidedAt: { type: string, format: date-time, nullable: true }
        decisionNotes: { type: string, nullable: true }
    UtilityApprovalContext:
      type: object
      required: [utilityCalculationId, clientId, tenantCompanyId, buildingId, approvable, pendingApprovals, approvals]
      properties:
        utilityCalculationId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        tenantCompanyId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        approvable: { type: boolean }
        pendingApprovals: { type: array, items: { $ref: "#/components/schemas/TenantUtilityApproval" } }
        approvals: { type: array, items: { $ref: "#/components/schemas/TenantUtilityApproval" } }

    GenerateUtilityBillRequest:
      type: object
      required: [calculationId, dueDate]
      properties:
        calculationId: { $ref: "#/components/schemas/Uuid" }
        dueDate: { type: string, format: date }
    UtilityBill:
      type: object
      required: [id, clientId, tenantCompanyId, buildingId, meterId, calculationId, approvalId, spaceId, utilityType, periodStart, periodEnd, consumptionQuantity, uomId, tariffRate, billAmount, currency, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        tenantCompanyId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        spaceId: { $ref: "#/components/schemas/Uuid" }
        meterId: { $ref: "#/components/schemas/Uuid" }
        tenantAssignmentId: { $ref: "#/components/schemas/Uuid" }
        calculationId: { $ref: "#/components/schemas/Uuid" }
        approvalId: { $ref: "#/components/schemas/Uuid" }
        consumptionId: { $ref: "#/components/schemas/Uuid" }
        utilityType: { $ref: "#/components/schemas/UtilityBillableType" }
        periodStart: { type: string, format: date-time }
        periodEnd: { type: string, format: date-time }
        consumptionQuantity: { type: number }
        uomId: { $ref: "#/components/schemas/Uuid" }
        tariffRate: { type: number }
        billAmount: { type: number }
        currency: { type: string }
        dueDate: { type: string, format: date }
        status: { $ref: "#/components/schemas/UtilityBillStatus" }
    UpdateUtilityBillRequest:
      type: object
      minProperties: 1
      properties:
        dueDate: { type: string, format: date }
        status: { $ref: "#/components/schemas/UtilityBillStatus" }
    UtilityInvoiceReady:
      type: object
      required: [tenantCompanyId, billingPeriod, chargeDescription, utilityType, quantity, uomId, unitRate, amount, currency, utilityBillId, sourceCalculationId]
      properties:
        tenantCompanyId: { $ref: "#/components/schemas/Uuid" }
        billingPeriod:
          type: object
          required: [start, end]
          properties:
            start: { type: string, format: date-time }
            end: { type: string, format: date-time }
        chargeDescription: { type: string }
        utilityType: { $ref: "#/components/schemas/UtilityBillableType" }
        quantity: { type: number }
        uomId: { $ref: "#/components/schemas/Uuid" }
        unitRate: { type: number }
        amount: { type: number }
        currency: { type: string }
        utilityBillId: { $ref: "#/components/schemas/Uuid" }
        sourceCalculationId: { $ref: "#/components/schemas/Uuid" }

    CreateUtilityReconciliationRequest:
      type: object
      required: [utilityType, periodStart, periodEnd]
      properties:
        utilityType: { $ref: "#/components/schemas/UtilityBillableType" }
        periodStart: { type: string, format: date-time }
        periodEnd: { type: string, format: date-time }
    UtilityReconciliation:
      type: object
      required: [id, clientId, buildingId, utilityType, periodStart, periodEnd, uomId, sourceConsumptionIds, tenantConsumptionIds, commonAreaConsumptionIds, sourceConsumption, tenantConsumption, commonAreaConsumption, unallocatedConsumption, applicableAreaSqm, performanceMetric, performanceValue, performanceUom, calculatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        utilityType: { $ref: "#/components/schemas/UtilityBillableType" }
        periodStart: { type: string, format: date-time }
        periodEnd: { type: string, format: date-time }
        uomId: { $ref: "#/components/schemas/Uuid" }
        sourceConsumptionIds: { type: array, items: { $ref: "#/components/schemas/Uuid" } }
        tenantConsumptionIds: { type: array, items: { $ref: "#/components/schemas/Uuid" } }
        commonAreaConsumptionIds: { type: array, items: { $ref: "#/components/schemas/Uuid" } }
        sourceConsumption: { type: number }
        tenantConsumption: { type: number }
        commonAreaConsumption: { type: number }
        unallocatedConsumption: { type: number, description: May be negative; totals are never forced to balance. }
        reconciliationPercentage: { type: number, nullable: true }
        applicableAreaSqm: { type: number, exclusiveMinimum: 0 }
        performanceMetric: { type: string, enum: [IKE, IKA] }
        performanceValue: { type: number }
        performanceUom: { type: string, enum: [kWh/m², m³/m²] }
        calculatedAt: { type: string, format: date-time }
        calculatedByUserId: { $ref: "#/components/schemas/Uuid" }

    BuildingUtilityOperationalSummaryEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/BuildingUtilityOperationalSummary" } } }] }
    UtilityExceptionEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityOperationalException" } } }] }
    UtilityExceptionListEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/UtilityOperationalException" } } } }] }
    UtilityReadingDueEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityReadingDue" } } }] }
    UtilityReadingDueListEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/UtilityReadingDue" } } } }] }
    UtilityOcrCandidateEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityOcrCandidate" } } }] }
    UtilityMeterEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityMeter" } } }] }
    UtilityMeterListEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/UtilityMeter" } } } }] }
    UtilityHierarchyEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityMeterHierarchy" } } }] }
    UtilityHierarchyListEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/UtilityMeterHierarchy" } } } }] }
    UtilityReadingEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityMeterReading" } } }] }
    UtilityReadingListEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/UtilityMeterReading" } } } }] }
    UtilityEvidenceEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityReadingEvidence" } } }] }
    UtilityEvidenceListEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/UtilityReadingEvidence" } } } }] }
    UtilityEvidenceReadinessEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityEvidenceReadiness" } } }] }
    UtilityConsumptionEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityConsumption" } } }] }
    UtilityConsumptionListEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/UtilityConsumption" } } } }] }
    UtilityTariffEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityTariff" } } }] }
    UtilityTariffListEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/UtilityTariff" } } } }] }
    UtilityCalculationEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityCalculation" } } }] }
    UtilityCalculationListEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/UtilityCalculation" } } } }] }
    TenantUtilityApprovalEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/TenantUtilityApproval" } } }] }
    TenantUtilityApprovalListEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/TenantUtilityApproval" } } } }] }
    TenantApprovalActionsEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/TenantApprovalActions" } } }] }
    UtilityApprovalContextEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityApprovalContext" } } }] }
    UtilityBillEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityBill" } } }] }
    UtilityBillListEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/UtilityBill" } } } }] }
    UtilityInvoiceReadyEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityInvoiceReady" } } }] }
    UtilityReconciliationEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/UtilityReconciliation" } } }] }
    UtilityReconciliationListEnvelope: { allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/UtilityReconciliation" } } } }] }
    # ------------------------------------------------------------------
    # Envelope
    # ------------------------------------------------------------------
    SuccessEnvelope:
      type: object
      description: >
        Standard success envelope. `data` holds the payload; `meta` carries
        page/context metadata and is an empty object when not applicable.
      required: [success, data, meta]
      properties:
        success:
          type: boolean
          const: true
        data:
          type: object
          description: Response payload. Concrete shape is per-endpoint.
        meta:
          type: object
          description: Pagination/context metadata (see PaginationMeta).

    ErrorEnvelope:
      type: object
      description: >
        Standard error envelope (BE-25K mobile error contract). Never
        contains stack traces or sensitive details. `code` / `message` /
        `details` are unchanged (Web compatible); `category`, `retryable`,
        `requestId`, `resource` and `conflict` are additive mobile-facing
        fields.
      required: [success, error]
      properties:
        success:
          type: boolean
          const: false
        error:
          type: object
          required: [code, message]
          properties:
            code:
              type: string
              description: Stable machine-readable error code (see src/shared/errors.ts).
            message:
              type: string
              description: Human-readable message.
            details:
              type: array
              description: Optional field-level validation details.
              items:
                $ref: "#/components/schemas/ErrorDetail"
            category:
              type: string
              description: >
                BE-25K stable machine-readable classification:
                VALIDATION / BAD_REQUEST / UNAUTHORIZED / FORBIDDEN /
                NOT_FOUND / CONFLICT / RATE_LIMITED / SERVER. Derived from
                the HTTP status and code.
              enum: [VALIDATION, BAD_REQUEST, UNAUTHORIZED, FORBIDDEN, NOT_FOUND, CONFLICT, RATE_LIMITED, SERVER]
            retryable:
              type: boolean
              description: >
                BE-25K retry flag: true for 5xx and 429 (safe/meaningful to
                retry), false for client errors (correct the request,
                re-authenticate, or reload per conflict guidance).
            requestId:
              type: string
              format: uuid
              description: >
                Server-generated HTTP request correlation UUID. Present on
                error responses after request entry and equal to the
                `X-Request-ID` response header.
            resource:
              type: object
              description: BE-25K resource/context reference where useful.
              required: [type, id]
              properties:
                type:
                  type: string
                  description: e.g. CHECKLIST_EXECUTION, EVIDENCE_SUBMISSION, FINDING.
                id:
                  type: string
                  format: uuid
            conflict:
              type: object
              description: >
                BE-25K conflict metadata where applicable (same shape as the
                BE-25I sync conflict payload): current server state +
                retry/reload guidance. Never a merged result.
              properties:
                current:
                  description: Current authoritative server state/reference.
                guidance:
                  type: object
                  required: [action, reloadEndpoint, message]
                  properties:
                    action:
                      type: string
                      const: reload
                    reloadEndpoint:
                      type: string
                    message:
                      type: string

    ErrorDetail:
      type: object
      description: Field-level validation detail.
      required: [field, message]
      properties:
        field:
          type: string
        message:
          type: string

    # ------------------------------------------------------------------
    # Pagination
    # ------------------------------------------------------------------
    PaginationMeta:
      type: object
      description: >
        Platform pagination metadata emitted in `meta` for paginated list
        endpoints (precedent: GET /auth/audit-events).
      required: [limit, offset, total]
      properties:
        limit:
          type: integer
          description: Effective page size applied (default 50, max 100).
        offset:
          type: integer
          description: Number of items skipped.
        total:
          type: integer
          description: Total number of items matching the filters.

    # ------------------------------------------------------------------
    # Common identifiers / context refs
    # ------------------------------------------------------------------
    Uuid:
      type: string
      format: uuid
      description: Universally unique identifier.

    # ------------------------------------------------------------------
    # CR-BE-BAST-01 — canonical lifecycle and compatibility contracts
    # ------------------------------------------------------------------
    BastStatus:
      type: string
      enum: [DRAFT, SUBMITTED, ACCEPTED, REJECTED]
    BastContextType:
      type: string
      enum: [INTERNAL, TENANT, VENDOR]
    BastRequirement:
      type: string
      enum: [NONE, WORK_ORDER, EACH_VENDOR_WORK]
    BastAcceptanceScopeType:
      type: string
      enum: [WORK_ORDER, VENDOR_WORK]
      nullable: true

    CreateBastDocumentRequest:
      type: object
      required:
        [clientId, buildingId, contextType, title, documentNumber, documentType,
         workOrderId, bastNumber, bastDate]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        contextType: { $ref: "#/components/schemas/BastContextType" }
        sourceType:
          type: string
          enum: [TENANT_COMPANY, VENDOR, INTERNAL]
          nullable: true
        sourceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        title: { type: string, maxLength: 255 }
        description: { type: string, maxLength: 4000, nullable: true }
        fileReference: { type: string, maxLength: 512, nullable: true }
        status:
          type: string
          enum: [DRAFT, ACTIVE, INACTIVE]
        documentNumber: { type: string, maxLength: 100 }
        documentType:
          type: string
          enum: [BAST]
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        vendorWorkId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        workCompletionDocumentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        bastNumber: { type: string, maxLength: 100 }
        bastDate: { type: string, format: date }
        notes: { type: string, maxLength: 4000, nullable: true }

    SubmitBastDocumentRequest:
      type: object
      required: [documentVersionId]
      additionalProperties: false
      properties:
        documentVersionId: { $ref: "#/components/schemas/Uuid" }

    DecideBastDocumentRequest:
      type: object
      required: [decision]
      additionalProperties: false
      properties:
        decision:
          type: string
          enum: [ACCEPT, REJECT]
        notes: { type: string, maxLength: 4000, nullable: true }

    BastSourceDocument:
      type: object
      required:
        [id, clientId, buildingId, documentNumber, documentType, contextType,
         sourceType, sourceId, title, description, fileReference, status,
         expiryDate, archivedAt, archivedByUserId, archiveReason,
         statusBeforeArchive, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        documentNumber: { type: string }
        documentType: { type: string, enum: [BAST] }
        contextType: { $ref: "#/components/schemas/BastContextType" }
        sourceType:
          type: string
          enum: [TENANT_COMPANY, VENDOR, INTERNAL]
          nullable: true
        sourceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        title: { type: string }
        description: { type: string, nullable: true }
        fileReference: { type: string, nullable: true }
        status: { type: string, enum: [DRAFT, ACTIVE, INACTIVE, ARCHIVED] }
        expiryDate: { type: string, format: date, nullable: true }
        archivedAt: { type: string, format: date-time, nullable: true }
        archivedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        archiveReason: { type: string, nullable: true }
        statusBeforeArchive:
          type: string
          enum: [DRAFT, ACTIVE, INACTIVE]
          nullable: true
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    BastDocument:
      type: object
      description: >
        Canonical BE-22 BAST. `id` is the sole BAST identity; acceptance actor
        and timestamp fields are the current lifecycle projection while the
        immutable attempt/Sign-Off/Finding records preserve history.
      required:
        [id, documentId, workOrderId, vendorWorkId, workCompletionDocumentId,
         vendorId, completionReportId, serviceReportId, acceptanceScopeType,
         bastRequirement, clientId, buildingId, contextType, bastNumber,
         bastDate, acceptanceStatus, notes, fileReference, preparedByUserId,
         submittedByUserId, acceptedByUserId, submittedAt, acceptedAt,
         createdAt, updatedAt, document]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        documentId: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        vendorWorkId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        workCompletionDocumentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        vendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        completionReportId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        serviceReportId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        acceptanceScopeType: { $ref: "#/components/schemas/BastAcceptanceScopeType" }
        bastRequirement: { $ref: "#/components/schemas/BastRequirement" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        contextType: { $ref: "#/components/schemas/BastContextType" }
        bastNumber: { type: string }
        bastDate: { type: string, format: date }
        acceptanceStatus: { $ref: "#/components/schemas/BastStatus" }
        notes: { type: string, nullable: true }
        fileReference: { type: string, nullable: true }
        preparedByUserId: { $ref: "#/components/schemas/Uuid" }
        submittedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        acceptedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        submittedAt: { type: string, format: date-time, nullable: true }
        acceptedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        document: { $ref: "#/components/schemas/BastSourceDocument" }

    BastLifecycleCommandResult:
      type: object
      required:
        [bastDocument, submissionAttemptId, documentVersionId,
         acceptanceSignOffId, findingId]
      properties:
        bastDocument: { $ref: "#/components/schemas/BastDocument" }
        submissionAttemptId: { $ref: "#/components/schemas/Uuid" }
        documentVersionId: { $ref: "#/components/schemas/Uuid" }
        acceptanceSignOffId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        findingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true

    BastReconciliationClassification:
      type: string
      enum:
        [CANONICAL_ONLY, LEGACY_ONLY, LINKED_CONSISTENT, DIVERGENT_CONTEXT,
         DIVERGENT_LIFECYCLE, DUPLICATE_NUMBER, DUPLICATE_ACCEPTANCE_SCOPE,
         CONTRADICTORY_SIGN_OFF, MISSING_REFERENCE, MISSING_VERSION,
         CLOSED_BEFORE_ACCEPTED, ORPHAN, AMBIGUOUS_WORK_RELATIONSHIP,
         CROSS_SCOPE_SECURITY_MISMATCH]
    BastReconciliationItem:
      type: object
      required:
        [classification, severity, canonicalBastDocumentId,
         legacyVendorBastBindingId, bastNumber, message]
      properties:
        classification: { $ref: "#/components/schemas/BastReconciliationClassification" }
        severity: { type: string, enum: [INFO, WARNING, ERROR] }
        canonicalBastDocumentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        legacyVendorBastBindingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        bastNumber: { type: string, nullable: true }
        message: { type: string }
    BastReconciliationInventory:
      type: object
      required: [buildingId, generatedAt, readOnly, summary, items]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        generatedAt: { type: string, format: date-time }
        readOnly: { type: boolean, enum: [true] }
        summary:
          type: object
          required: [total, byClassification, bySeverity]
          properties:
            total: { type: integer, minimum: 0 }
            byClassification:
              type: object
              additionalProperties: { type: integer, minimum: 0 }
            bySeverity:
              type: object
              required: [INFO, WARNING, ERROR]
              properties:
                INFO: { type: integer, minimum: 0 }
                WARNING: { type: integer, minimum: 0 }
                ERROR: { type: integer, minimum: 0 }
        items:
          type: array
          items: { $ref: "#/components/schemas/BastReconciliationItem" }

    CreateLegacyVendorBastRequest:
      type: object
      deprecated: true
      required: [vendorWorkId, bastNumber, bastDate]
      properties:
        vendorWorkId: { $ref: "#/components/schemas/Uuid" }
        bastNumber: { type: string, maxLength: 100 }
        bastDate: { type: string, format: date }
        notes: { type: string, nullable: true, maxLength: 4000 }
        fileReference: { type: string, nullable: true, maxLength: 512 }
    LegacyBastDecisionRequest:
      type: object
      properties:
        notes: { type: string, nullable: true, maxLength: 4000 }

    LegacyVendorBastProjection:
      type: object
      description: >
        Compatibility projection only. `bastDocumentId` is populated only for
        a safe context-consistent canonical link; canonical lifecycle and
        Sign-Off values then override labelled legacy fallback state.
      required:
        [id, bastDocumentId, clientId, vendorWorkId, completionReportId,
         serviceReportId, workOrderId, buildingId, bastNumber, bastDate,
         preparedByUserId, submittedByUserId, acceptedByUserId,
         acceptanceStatus, notes, fileReference, submittedAt, acceptedAt,
         createdAt, updatedAt, compatibility, acceptanceSignOff]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        bastDocumentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        clientId: { $ref: "#/components/schemas/Uuid" }
        vendorWorkId: { $ref: "#/components/schemas/Uuid" }
        completionReportId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        serviceReportId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        bastNumber: { type: string }
        bastDate: { type: string, format: date }
        preparedByUserId: { $ref: "#/components/schemas/Uuid" }
        submittedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        acceptedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        acceptanceStatus: { $ref: "#/components/schemas/BastStatus" }
        notes: { type: string, nullable: true }
        fileReference: { type: string, nullable: true }
        submittedAt: { type: string, format: date-time, nullable: true }
        acceptedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        compatibility:
          type: object
          required: [lifecycleAuthority, reconciliationRequired, legacyAcceptanceStatus]
          properties:
            lifecycleAuthority:
              type: string
              enum: [CANONICAL_BAST, LEGACY_ONLY_FALLBACK, UNRESOLVED_CANONICAL_LINK]
            reconciliationRequired: { type: boolean }
            legacyAcceptanceStatus: { $ref: "#/components/schemas/BastStatus" }
        acceptanceSignOff:
          allOf: [{ $ref: "#/components/schemas/LegacyBastAcceptanceSignOff" }]
          nullable: true

    LegacyBastAcceptanceSignOff:
      type: object
      required:
        [id, bastSubmissionAttemptId, documentVersionId, decision,
         signerUserId, notes, signedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        bastSubmissionAttemptId: { $ref: "#/components/schemas/Uuid" }
        documentVersionId: { $ref: "#/components/schemas/Uuid" }
        decision: { type: string, enum: [ACCEPTED, REJECTED] }
        signerUserId: { $ref: "#/components/schemas/Uuid" }
        notes: { type: string, nullable: true }
        signedAt: { type: string, format: date-time }

    WorkOrderCloseResult:
      type: object
      required: [status]
      properties:
        status: { type: string, enum: [CLOSED] }

    # ------------------------------------------------------------------
    # BE-27A — Client Configuration
    # ------------------------------------------------------------------
    ClientConfigurationStatus:
      type: string
      description: >
        BE-27A record availability. This is not the BE-27N/O version/publish
        lifecycle.
      enum: [ACTIVE, INACTIVE]

    ClientConfigurationValue:
      description: >
        JSON-safe scalar, array, or object value (including null). Executable
        behavior and values larger than 65536 serialized bytes are rejected.
      nullable: true

    ClientConfiguration:
      type: object
      required: [id, clientId, key, value, status, createdAt, updatedAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        key:
          type: string
          maxLength: 100
          pattern: "^[A-Z][A-Z0-9_.-]*$"
        value:
          $ref: "#/components/schemas/ClientConfigurationValue"
        status:
          $ref: "#/components/schemas/ClientConfigurationStatus"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateClientConfigurationRequest:
      type: object
      additionalProperties: false
      required: [key, value]
      properties:
        key:
          type: string
          maxLength: 100
          description: Stable key; normalized to uppercase by the backend.
        value:
          $ref: "#/components/schemas/ClientConfigurationValue"
        status:
          $ref: "#/components/schemas/ClientConfigurationStatus"

    UpdateClientConfigurationRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      description: At least one of value or status is required; key and Client are immutable.
      properties:
        value:
          $ref: "#/components/schemas/ClientConfigurationValue"
        status:
          $ref: "#/components/schemas/ClientConfigurationStatus"

    EffectiveClientConfiguration:
      type: object
      required: [clientId, configurations]
      properties:
        clientId:
          $ref: "#/components/schemas/Uuid"
        configurations:
          type: object
          description: ACTIVE Client values keyed by normalized configuration key.
          additionalProperties:
            $ref: "#/components/schemas/ClientConfigurationValue"

    # ------------------------------------------------------------------
    # BE-27B — Building Configuration (reuses BE-27A value/status)
    # ------------------------------------------------------------------
    BuildingConfiguration:
      allOf:
        - $ref: "#/components/schemas/ClientConfiguration"
        - type: object
          required: [buildingId]
          properties:
            buildingId:
              $ref: "#/components/schemas/Uuid"

    CreateBuildingConfigurationRequest:
      allOf:
        - $ref: "#/components/schemas/CreateClientConfigurationRequest"

    UpdateBuildingConfigurationRequest:
      allOf:
        - $ref: "#/components/schemas/UpdateClientConfigurationRequest"

    EffectiveBuildingConfiguration:
      allOf:
        - $ref: "#/components/schemas/EffectiveClientConfiguration"
        - type: object
          required: [buildingId]
          properties:
            buildingId:
              $ref: "#/components/schemas/Uuid"

    # ------------------------------------------------------------------
    # BE-27C — Module Configuration
    # ------------------------------------------------------------------
    ModuleConfiguration:
      type: object
      required:
        [id, scopeType, clientId, buildingId, moduleId, moduleKey, moduleName,
         enabled, createdAt, updatedAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        scopeType:
          type: string
          enum: [CLIENT, BUILDING]
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          type: string
          format: uuid
          nullable: true
        moduleId:
          $ref: "#/components/schemas/Uuid"
        moduleKey:
          type: string
          description: Stable code from the existing Module catalogue.
        moduleName:
          type: string
        enabled:
          type: boolean
          description: Configured enablement intent; not a commercial grant.
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateModuleConfigurationRequest:
      type: object
      additionalProperties: false
      required: [moduleKey, enabled]
      properties:
        moduleKey:
          type: string
          minLength: 2
          maxLength: 64
          pattern: "^[A-Za-z][A-Za-z0-9_]*$"
          description: Existing Module code; normalized to uppercase.
        enabled:
          type: boolean

    UpdateModuleConfigurationRequest:
      type: object
      additionalProperties: false
      required: [enabled]
      properties:
        enabled:
          type: boolean

    EffectiveModuleConfigurationItem:
      type: object
      required:
        [moduleId, moduleKey, moduleName, configuredEnabled, entitled, enabled,
         source]
      properties:
        moduleId:
          $ref: "#/components/schemas/Uuid"
        moduleKey:
          type: string
        moduleName:
          type: string
        configuredEnabled:
          type: boolean
        entitled:
          type: boolean
          description: Result from the existing effective Module Entitlement resolver.
        enabled:
          type: boolean
          description: configuredEnabled AND entitled.
        source:
          type: string
          enum: [CLIENT, BUILDING]

    EffectiveModuleConfiguration:
      type: object
      required: [clientId, buildingId, modules]
      properties:
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          type: string
          format: uuid
          nullable: true
        modules:
          type: array
          items:
            $ref: "#/components/schemas/EffectiveModuleConfigurationItem"

    FeatureEntitlementState:
      type: string
      enum: [ENABLED, DISABLED]
    FeatureEntitlementConfiguration:
      type: object
      required: [id, scopeType, clientId, buildingId, moduleId, moduleKey, moduleName, featureKey, state, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        scopeType: { type: string, enum: [CLIENT, BUILDING] }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        moduleId: { $ref: "#/components/schemas/Uuid" }
        moduleKey: { type: string }
        moduleName: { type: string }
        featureKey: { type: string, maxLength: 100, pattern: "^[A-Z][A-Z0-9_.-]*$" }
        state: { $ref: "#/components/schemas/FeatureEntitlementState" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateFeatureEntitlementRequest:
      type: object
      additionalProperties: false
      required: [moduleKey, featureKey, state]
      properties:
        moduleKey: { type: string, minLength: 2, maxLength: 64 }
        featureKey: { type: string, maxLength: 100 }
        state: { $ref: "#/components/schemas/FeatureEntitlementState" }
    UpdateFeatureEntitlementRequest:
      type: object
      additionalProperties: false
      required: [state]
      properties:
        state: { $ref: "#/components/schemas/FeatureEntitlementState" }
    EffectiveFeatureEntitlementItem:
      type: object
      required: [moduleId, moduleKey, moduleName, featureKey, configuredState, moduleEnabled, state, source]
      properties:
        moduleId: { $ref: "#/components/schemas/Uuid" }
        moduleKey: { type: string }
        moduleName: { type: string }
        featureKey: { type: string }
        configuredState: { $ref: "#/components/schemas/FeatureEntitlementState" }
        moduleEnabled: { type: boolean }
        state: { $ref: "#/components/schemas/FeatureEntitlementState" }
        source: { type: string, enum: [CLIENT, BUILDING] }
    EffectiveFeatureEntitlementConfiguration:
      type: object
      required: [clientId, buildingId, features]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        features:
          type: array
          items: { $ref: "#/components/schemas/EffectiveFeatureEntitlementItem" }

    NavigationItem:
      type: object
      required: [id, scopeType, clientId, buildingId, navigationKey, label, routeReference, parentNavigationKey, displayOrder, moduleId, moduleKey, featureKey, permissionId, permissionCode, enabled]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        scopeType: { type: string, enum: [CLIENT, BUILDING] }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        navigationKey: { type: string, maxLength: 100 }
        label: { type: string, maxLength: 160 }
        routeReference: { type: string, maxLength: 512 }
        parentNavigationKey: { type: string, nullable: true, maxLength: 100 }
        displayOrder: { type: integer, minimum: 0 }
        moduleId: { type: string, format: uuid, nullable: true }
        moduleKey: { type: string, nullable: true }
        featureKey: { type: string, nullable: true }
        permissionId: { type: string, format: uuid, nullable: true }
        permissionCode: { type: string, nullable: true }
        enabled: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateNavigationItemRequest:
      type: object
      additionalProperties: false
      required: [navigationKey, label, routeReference]
      properties:
        navigationKey: { type: string, maxLength: 100 }
        label: { type: string, maxLength: 160 }
        routeReference: { type: string, maxLength: 512 }
        parentNavigationKey: { type: string, nullable: true, maxLength: 100 }
        displayOrder: { type: integer, minimum: 0, default: 0 }
        moduleKey: { type: string, nullable: true }
        featureKey: { type: string, nullable: true }
        permissionCode: { type: string, nullable: true }
        enabled: { type: boolean, default: true }
    UpdateNavigationItemRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        label: { type: string, maxLength: 160 }
        routeReference: { type: string, maxLength: 512 }
        displayOrder: { type: integer, minimum: 0 }
        enabled: { type: boolean }
    EffectiveNavigation:
      type: object
      required: [clientId, buildingId, items]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        items:
          type: array
          items: { $ref: "#/components/schemas/NavigationItem" }

    Workspace:
      type: object
      required: [id, scopeType, clientId, buildingId, workspaceKey, label, description, moduleId, moduleKey, featureKey, navigationKey, permissionId, permissionCode, enabled]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        scopeType: { type: string, enum: [CLIENT, BUILDING] }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        workspaceKey: { type: string, maxLength: 100 }
        label: { type: string, maxLength: 160 }
        description: { type: string, nullable: true, maxLength: 1000 }
        moduleId: { type: string, format: uuid, nullable: true }
        moduleKey: { type: string, nullable: true }
        featureKey: { type: string, nullable: true }
        navigationKey: { type: string, nullable: true }
        permissionId: { type: string, format: uuid, nullable: true }
        permissionCode: { type: string, nullable: true }
        enabled: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateWorkspaceRequest:
      type: object
      additionalProperties: false
      required: [workspaceKey, label]
      properties:
        workspaceKey: { type: string, maxLength: 100 }
        label: { type: string, maxLength: 160 }
        description: { type: string, nullable: true, maxLength: 1000 }
        moduleKey: { type: string, nullable: true }
        featureKey: { type: string, nullable: true }
        navigationKey: { type: string, nullable: true }
        permissionCode: { type: string, nullable: true }
        enabled: { type: boolean, default: true }
    UpdateWorkspaceRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        label: { type: string, maxLength: 160 }
        description: { type: string, nullable: true, maxLength: 1000 }
        enabled: { type: boolean }
    EffectiveWorkspaces:
      type: object
      required: [clientId, buildingId, workspaces]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        workspaces:
          type: array
          items: { $ref: "#/components/schemas/Workspace" }

    Dashboard:
      type: object
      required: [id, scopeType, clientId, buildingId, dashboardKey, label, workspaceKey, metadata, enabled]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        scopeType: { type: string, enum: [CLIENT, BUILDING] }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        dashboardKey: { type: string, maxLength: 100 }
        label: { type: string, maxLength: 160 }
        workspaceKey: { type: string, maxLength: 100 }
        metadata: { type: object, additionalProperties: true }
        enabled: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    DashboardWidget:
      type: object
      required: [id, dashboardId, clientId, buildingId, widgetKey, label, sourceReference, displayOrder, moduleId, moduleKey, featureKey, permissionId, permissionCode, metadata, enabled]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        dashboardId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        widgetKey: { type: string, maxLength: 100 }
        label: { type: string, maxLength: 160 }
        sourceReference: { type: string, description: Approved BE-23/BE-24 read-model reference }
        displayOrder: { type: integer, minimum: 0 }
        moduleId: { type: string, format: uuid, nullable: true }
        moduleKey: { type: string, nullable: true }
        featureKey: { type: string, nullable: true }
        permissionId: { type: string, format: uuid, nullable: true }
        permissionCode: { type: string, nullable: true }
        metadata: { type: object, additionalProperties: true }
        enabled: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateDashboardRequest:
      type: object
      additionalProperties: false
      required: [dashboardKey, label, workspaceKey]
      properties:
        dashboardKey: { type: string }
        label: { type: string }
        workspaceKey: { type: string }
        metadata: { type: object }
        enabled: { type: boolean, default: true }
    UpdateDashboardRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        label: { type: string }
        metadata: { type: object }
        enabled: { type: boolean }
    CreateDashboardWidgetRequest:
      type: object
      additionalProperties: false
      required: [widgetKey, label, sourceReference]
      properties:
        widgetKey: { type: string }
        label: { type: string }
        sourceReference: { type: string }
        displayOrder: { type: integer, minimum: 0, default: 0 }
        moduleKey: { type: string, nullable: true }
        featureKey: { type: string, nullable: true }
        permissionCode: { type: string, nullable: true }
        metadata: { type: object }
        enabled: { type: boolean, default: true }
    UpdateDashboardWidgetRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        label: { type: string }
        displayOrder: { type: integer, minimum: 0 }
        metadata: { type: object }
        enabled: { type: boolean }
    EffectiveDashboard:
      allOf:
        - $ref: "#/components/schemas/Dashboard"
        - type: object
          required: [widgets]
          properties:
            widgets:
              type: array
              items: { $ref: "#/components/schemas/DashboardWidget" }
    EffectiveDashboards:
      type: object
      required: [clientId, buildingId, dashboards]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        dashboards:
          type: array
          items: { $ref: "#/components/schemas/EffectiveDashboard" }

    AdminFormField:
      type: object
      required: [id, formSectionId, code, label, fieldType, required, displayOrder, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        formSectionId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        label: { type: string }
        fieldType: { type: string, enum: [TEXT, TEXTAREA, NUMBER, DATE, DATETIME, BOOLEAN, SELECT] }
        required: { type: boolean }
        displayOrder: { type: integer, minimum: 0 }
        placeholder: { type: string, nullable: true }
        helpText: { type: string, nullable: true }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        uomId: { type: string, format: uuid, nullable: true }
        minimumValue: { type: string, nullable: true }
        maximumValue: { type: string, nullable: true }
        decimalPrecision: { type: integer, nullable: true }
    AdminFormSection:
      type: object
      required: [id, formTemplateId, code, title, displayOrder, status, fields]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        formTemplateId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        title: { type: string }
        description: { type: string, nullable: true }
        displayOrder: { type: integer }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        fields: { type: array, items: { $ref: "#/components/schemas/AdminFormField" } }
    AdminFormDefinition:
      type: object
      required: [template, bindings, sections]
      properties:
        template: { type: object, description: Existing BE-07 PublicFormTemplate }
        bindings: { type: array, items: { type: object } }
        sections: { type: array, items: { $ref: "#/components/schemas/AdminFormSection" } }
    CreateAdminFormTemplateRequest:
      type: object
      required: [sourceFormId, code, name]
      properties:
        sourceFormId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        description: { type: string }
        status: { type: string, enum: [DRAFT, ACTIVE, INACTIVE] }
    UpdateAdminFormTemplateRequest:
      type: object
      properties:
        name: { type: string }
        description: { type: string, nullable: true }
        status: { type: string, enum: [DRAFT, ACTIVE, INACTIVE] }
    CreateAdminFormSectionRequest:
      type: object
      required: [code, title]
      properties:
        code: { type: string }
        title: { type: string }
        description: { type: string, nullable: true }
        displayOrder: { type: integer, minimum: 0 }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
    UpdateAdminFormSectionRequest:
      type: object
      properties:
        title: { type: string }
        description: { type: string, nullable: true }
        displayOrder: { type: integer, minimum: 0 }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
    CreateAdminFormFieldRequest:
      type: object
      required: [code, label, fieldType]
      properties:
        code: { type: string }
        label: { type: string }
        fieldType: { type: string, enum: [TEXT, TEXTAREA, NUMBER, DATE, DATETIME, BOOLEAN, SELECT] }
        required: { type: boolean }
        displayOrder: { type: integer, minimum: 0 }
        placeholder: { type: string, nullable: true }
        helpText: { type: string, nullable: true }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        uomId: { type: string, format: uuid, nullable: true }
        minimumValue: { type: number, nullable: true }
        maximumValue: { type: number, nullable: true }
        decimalPrecision: { type: integer, nullable: true }
    UpdateAdminFormFieldRequest:
      type: object
      minProperties: 1
      properties:
        label: { type: string }
        fieldType: { type: string, enum: [TEXT, TEXTAREA, NUMBER, DATE, DATETIME, BOOLEAN, SELECT] }
        required: { type: boolean }
        displayOrder: { type: integer, minimum: 0 }
        placeholder: { type: string, nullable: true }
        helpText: { type: string, nullable: true }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        uomId: { type: string, format: uuid, nullable: true }
        minimumValue: { type: number, nullable: true }
        maximumValue: { type: number, nullable: true }
        decimalPrecision: { type: integer, nullable: true }

    AdminChecklistTemplate:
      type: object
      required: [id, clientId, code, name, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { type: string, enum: [DRAFT, ACTIVE, INACTIVE] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    ChecklistConfigurationBinding:
      type: object
      required: [id, checklistTemplateId, scopeType, clientId, buildingId, createdAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        scopeType: { type: string, enum: [CLIENT, BUILDING] }
        clientId: { type: string, format: uuid, nullable: true }
        buildingId: { type: string, format: uuid, nullable: true }
        createdAt: { type: string, format: date-time }
    AdminChecklistItem:
      type: object
      description: Existing Checklist Item. Measurement fields are read-only in Checklist Administration.
      required: [id, checklistTemplateId, code, label, itemType, required, displayOrder, status, isNaAllowed, naRequiresNote, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        label: { type: string }
        itemType: { type: string, enum: [CHECK, BOOLEAN, TEXT, NUMBER, SELECT] }
        required: { type: boolean }
        displayOrder: { type: integer, minimum: 0 }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        uomId: { type: string, format: uuid, nullable: true, readOnly: true }
        minimumValue: { type: string, nullable: true, readOnly: true }
        maximumValue: { type: string, nullable: true, readOnly: true }
        decimalPrecision: { type: integer, nullable: true, readOnly: true }
        isNaAllowed: { type: boolean, description: When true, executions may mark this item explicitly N/A. Fail-closed default false. }
        naRequiresNote: { type: boolean, description: When true (and isNaAllowed=true), an N/A execution requires a non-empty naNote. }
        options: { type: array, items: { $ref: "#/components/schemas/AdminChecklistItemOption" } }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    AdminChecklistItemOption:
      type: object
      required: [id, checklistItemId, code, label, displayOrder, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        checklistItemId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        label: { type: string }
        displayOrder: { type: integer, minimum: 0 }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateAdminChecklistItemOption:
      type: object
      additionalProperties: false
      required: [code, label, displayOrder]
      properties:
        code: { type: string }
        label: { type: string }
        displayOrder: { type: integer, minimum: 0 }
    UpdateAdminChecklistItemOptionRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        label: { type: string }
        displayOrder: { type: integer, minimum: 0 }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
    AdminChecklistDefinition:
      type: object
      required: [template, bindings, items]
      properties:
        template: { $ref: "#/components/schemas/AdminChecklistTemplate" }
        bindings: { type: array, items: { $ref: "#/components/schemas/ChecklistConfigurationBinding" } }
        items: { type: array, items: { $ref: "#/components/schemas/AdminChecklistItem" } }
    CreateAdminChecklistTemplateRequest:
      type: object
      additionalProperties: false
      required: [code, name]
      properties:
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { type: string, enum: [DRAFT, ACTIVE, INACTIVE] }
    UpdateAdminChecklistTemplateRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        name: { type: string }
        description: { type: string, nullable: true }
        status: { type: string, enum: [DRAFT, ACTIVE, INACTIVE] }
    CreateAdminChecklistItemRequest:
      type: object
      additionalProperties: false
      required: [code, label, itemType]
      properties:
        code: { type: string }
        label: { type: string }
        itemType: { type: string, enum: [CHECK, BOOLEAN, TEXT, NUMBER, SELECT] }
        required: { type: boolean }
        displayOrder: { type: integer, minimum: 0 }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        isNaAllowed: { type: boolean, default: false }
        naRequiresNote: { type: boolean, default: false, description: Only valid when isNaAllowed=true. }
        options: { type: array, items: { $ref: "#/components/schemas/CreateAdminChecklistItemOption" } }
    UpdateAdminChecklistItemRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        label: { type: string }
        itemType: { type: string, enum: [CHECK, BOOLEAN, TEXT, NUMBER, SELECT] }
        required: { type: boolean }
        displayOrder: { type: integer, minimum: 0 }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        isNaAllowed: { type: boolean }
        naRequiresNote: { type: boolean, description: Effective state after patch must satisfy naRequiresNote=true ⇒ isNaAllowed=true. }

    OperationalSettingKey:
      type: string
      description: Typed, allowlisted operational metadata key.
      enum: [SCHEDULE.DEFAULT_TIMEZONE]
    OperationalSetting:
      type: object
      required: [id, clientId, buildingId, scopeType, key, value, enabled, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        scopeType: { type: string, enum: [CLIENT, BUILDING] }
        key: { $ref: "#/components/schemas/OperationalSettingKey" }
        value:
          type: string
          description: IANA timezone for SCHEDULE.DEFAULT_TIMEZONE.
          example: Asia/Jakarta
        enabled: { type: boolean }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateOperationalSettingRequest:
      type: object
      additionalProperties: false
      required: [key, value]
      properties:
        key: { $ref: "#/components/schemas/OperationalSettingKey" }
        value:
          type: string
          description: Valid IANA timezone.
          example: Asia/Jakarta
        enabled: { type: boolean, default: true }
        status: { type: string, enum: [ACTIVE, INACTIVE], default: ACTIVE }
    UpdateOperationalSettingRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        value:
          type: string
          description: Valid IANA timezone.
          example: Asia/Makassar
        enabled: { type: boolean }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
    EffectiveOperationalSetting:
      type: object
      required: [key, value, enabled, status]
      properties:
        key: { $ref: "#/components/schemas/OperationalSettingKey" }
        value: { type: string, example: Asia/Jakarta }
        enabled: { type: boolean }
        status: { type: string, enum: [ACTIVE] }
    EffectiveOperationalSettings:
      type: object
      required: [clientId, buildingId, settings]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        settings:
          type: array
          items: { $ref: "#/components/schemas/EffectiveOperationalSetting" }

    OrganizationPresentationLabels:
      type: object
      additionalProperties: false
      required: [organization, department, team, position]
      properties:
        organization: { type: string, nullable: true, maxLength: 160 }
        department: { type: string, nullable: true, maxLength: 160 }
        team: { type: string, nullable: true, maxLength: 160 }
        position: { type: string, nullable: true, maxLength: 160 }
    OrganizationPresentationItem:
      type: object
      additionalProperties: false
      required: [entityType, entityId, displayName, displayOrder, visible]
      properties:
        entityType: { type: string, enum: [ORGANIZATION, DEPARTMENT, TEAM, POSITION] }
        entityId: { $ref: "#/components/schemas/Uuid" }
        displayName: { type: string, nullable: true, maxLength: 160 }
        displayOrder: { type: integer, minimum: 0, maximum: 1000000 }
        visible: { type: boolean }
    OrganizationPresentationConfiguration:
      type: object
      required: [id, clientId, buildingId, scopeType, labels, items, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        scopeType: { type: string, enum: [CLIENT, BUILDING] }
        labels: { $ref: "#/components/schemas/OrganizationPresentationLabels" }
        items:
          type: array
          items: { $ref: "#/components/schemas/OrganizationPresentationItem" }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateOrganizationPresentationRequest:
      type: object
      additionalProperties: false
      properties:
        labels:
          type: object
          additionalProperties: false
          properties:
            organization: { type: string, nullable: true, maxLength: 160 }
            department: { type: string, nullable: true, maxLength: 160 }
            team: { type: string, nullable: true, maxLength: 160 }
            position: { type: string, nullable: true, maxLength: 160 }
        items:
          type: array
          maxItems: 1000
          items:
            type: object
            additionalProperties: false
            required: [entityType, entityId]
            properties:
              entityType: { type: string, enum: [ORGANIZATION, DEPARTMENT, TEAM, POSITION] }
              entityId: { $ref: "#/components/schemas/Uuid" }
              displayName: { type: string, nullable: true, maxLength: 160 }
              displayOrder: { type: integer, minimum: 0, maximum: 1000000 }
              visible: { type: boolean, default: true }
        status: { type: string, enum: [ACTIVE, INACTIVE], default: ACTIVE }
    UpdateOrganizationPresentationRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        labels:
          type: object
          additionalProperties: false
          minProperties: 1
          properties:
            organization: { type: string, nullable: true, maxLength: 160 }
            department: { type: string, nullable: true, maxLength: 160 }
            team: { type: string, nullable: true, maxLength: 160 }
            position: { type: string, nullable: true, maxLength: 160 }
        items:
          type: array
          maxItems: 1000
          items: { $ref: "#/components/schemas/OrganizationPresentationItem" }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
    EffectiveOrganizationPresentationItem:
      allOf:
        - $ref: "#/components/schemas/OrganizationPresentationItem"
        - type: object
          required: [code, authoritativeName, effectiveDisplayName, organizationId, departmentId, available]
          properties:
            code: { type: string }
            authoritativeName: { type: string }
            effectiveDisplayName: { type: string }
            organizationId: { $ref: "#/components/schemas/Uuid" }
            departmentId: { type: string, format: uuid, nullable: true }
            available: { type: boolean }
    EffectiveOrganizationPresentation:
      type: object
      required: [clientId, buildingId, labels, items]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        labels:
          type: object
          required: [organization, department, team, position]
          properties:
            organization: { type: string }
            department: { type: string }
            team: { type: string }
            position: { type: string }
        items:
          type: array
          items: { $ref: "#/components/schemas/EffectiveOrganizationPresentationItem" }

    CmsContentType:
      type: string
      enum: [ANNOUNCEMENT, HELP, FAQ, KNOWLEDGE, PORTAL_CONTENT, RELEASE_INFORMATION]
    CmsContentStatus:
      type: string
      enum: [DRAFT, PUBLISHED, INACTIVE]
    CmsContent:
      type: object
      required: [id, clientId, buildingId, scopeType, contentType, slug, title, body, status, createdByUserId, publishedAt, publishedByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        scopeType: { type: string, enum: [CLIENT, BUILDING] }
        contentType: { $ref: "#/components/schemas/CmsContentType" }
        slug: { type: string, maxLength: 120, pattern: "^[a-z0-9]+([._-][a-z0-9]+)*$" }
        title: { type: string, maxLength: 240 }
        body:
          type: string
          maxLength: 100000
          description: Safe plain text/Markdown. Raw HTML, executable script, event handlers, and unsafe URL schemes are rejected.
        status: { $ref: "#/components/schemas/CmsContentStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        publishedAt: { type: string, format: date-time, nullable: true }
        publishedByUserId: { type: string, format: uuid, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateCmsContentRequest:
      type: object
      additionalProperties: false
      required: [contentType, slug, title, body]
      properties:
        contentType: { $ref: "#/components/schemas/CmsContentType" }
        slug: { type: string, maxLength: 120, pattern: "^[a-z0-9]+([._-][a-z0-9]+)*$" }
        title: { type: string, maxLength: 240 }
        body:
          type: string
          maxLength: 100000
          description: Safe plain text/Markdown only; raw HTML/JS is not accepted.
        status: { $ref: "#/components/schemas/CmsContentStatus" }
    UpdateCmsContentRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        title: { type: string, maxLength: 240 }
        body:
          type: string
          maxLength: 100000
          description: Safe plain text/Markdown only; raw HTML/JS is not accepted.
        status: { $ref: "#/components/schemas/CmsContentStatus" }
    EffectiveCmsContent:
      type: object
      required: [clientId, buildingId, content]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        content:
          type: array
          items: { $ref: "#/components/schemas/CmsContent" }

    LoginBranding:
      type: object
      additionalProperties: false
      required: [title, subtitle, showLogo]
      properties:
        title: { type: string, nullable: true, maxLength: 160 }
        subtitle: { type: string, nullable: true, maxLength: 240 }
        showLogo: { type: boolean }
    PortalBranding:
      type: object
      additionalProperties: false
      required: [headerTitle, showLogo]
      properties:
        headerTitle: { type: string, nullable: true, maxLength: 160 }
        showLogo: { type: boolean }
    ReportBranding:
      type: object
      additionalProperties: false
      required: [headerText, footerText, showLogo]
      properties:
        headerText: { type: string, nullable: true, maxLength: 240 }
        footerText: { type: string, nullable: true, maxLength: 500 }
        showLogo: { type: boolean }
    BrandingThemeTokens:
      type: object
      additionalProperties: false
      required: [primaryColor, secondaryColor, accentColor, backgroundColor, surfaceColor, textColor, fontFamily, borderRadius]
      properties:
        primaryColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
        secondaryColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
        accentColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
        backgroundColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
        surfaceColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
        textColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
        fontFamily: { type: string, enum: [SYSTEM, INTER, ROBOTO, OPEN_SANS, LATO] }
        borderRadius: { type: string, enum: [NONE, SMALL, MEDIUM, LARGE] }
    BrandingProfile:
      type: object
      required: [brandName, logoReference, login, portal, report, theme]
      properties:
        brandName: { type: string, maxLength: 160 }
        logoReference:
          type: string
          nullable: true
          maxLength: 512
          description: Opaque backend-managed file/storage reference. The API does not upload, generate, or alter logo bytes.
        login: { $ref: "#/components/schemas/LoginBranding" }
        portal: { $ref: "#/components/schemas/PortalBranding" }
        report: { $ref: "#/components/schemas/ReportBranding" }
        theme: { $ref: "#/components/schemas/BrandingThemeTokens" }
    BrandingConfiguration:
      allOf:
        - $ref: "#/components/schemas/BrandingProfile"
        - type: object
          required: [id, clientId, buildingId, scopeType, status, createdAt, updatedAt]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            clientId: { $ref: "#/components/schemas/Uuid" }
            buildingId: { type: string, format: uuid, nullable: true }
            scopeType: { type: string, enum: [CLIENT, BUILDING] }
            status: { type: string, enum: [ACTIVE, INACTIVE] }
            createdAt: { type: string, format: date-time }
            updatedAt: { type: string, format: date-time }
    CreateBrandingRequest:
      type: object
      additionalProperties: false
      required: [brandName]
      properties:
        brandName: { type: string, maxLength: 160 }
        logoReference: { type: string, nullable: true, maxLength: 512 }
        login:
          type: object
          additionalProperties: false
          properties:
            title: { type: string, nullable: true, maxLength: 160 }
            subtitle: { type: string, nullable: true, maxLength: 240 }
            showLogo: { type: boolean }
        portal:
          type: object
          additionalProperties: false
          properties:
            headerTitle: { type: string, nullable: true, maxLength: 160 }
            showLogo: { type: boolean }
        report:
          type: object
          additionalProperties: false
          properties:
            headerText: { type: string, nullable: true, maxLength: 240 }
            footerText: { type: string, nullable: true, maxLength: 500 }
            showLogo: { type: boolean }
        theme:
          type: object
          additionalProperties: false
          properties:
            primaryColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
            secondaryColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
            accentColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
            backgroundColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
            surfaceColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
            textColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
            fontFamily: { type: string, enum: [SYSTEM, INTER, ROBOTO, OPEN_SANS, LATO] }
            borderRadius: { type: string, enum: [NONE, SMALL, MEDIUM, LARGE] }
        status: { type: string, enum: [ACTIVE, INACTIVE], default: ACTIVE }
    UpdateBrandingRequest:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        brandName: { type: string, maxLength: 160 }
        logoReference: { type: string, nullable: true, maxLength: 512 }
        login:
          type: object
          additionalProperties: false
          minProperties: 1
          properties:
            title: { type: string, nullable: true, maxLength: 160 }
            subtitle: { type: string, nullable: true, maxLength: 240 }
            showLogo: { type: boolean }
        portal:
          type: object
          additionalProperties: false
          minProperties: 1
          properties:
            headerTitle: { type: string, nullable: true, maxLength: 160 }
            showLogo: { type: boolean }
        report:
          type: object
          additionalProperties: false
          minProperties: 1
          properties:
            headerText: { type: string, nullable: true, maxLength: 240 }
            footerText: { type: string, nullable: true, maxLength: 500 }
            showLogo: { type: boolean }
        theme:
          type: object
          additionalProperties: false
          minProperties: 1
          properties:
            primaryColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
            secondaryColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
            accentColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
            backgroundColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
            surfaceColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
            textColor: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
            fontFamily: { type: string, enum: [SYSTEM, INTER, ROBOTO, OPEN_SANS, LATO] }
            borderRadius: { type: string, enum: [NONE, SMALL, MEDIUM, LARGE] }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
    EffectiveBranding:
      type: object
      required: [clientId, buildingId, branding]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        branding:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/BrandingProfile"

    ConfigurationVersionSourceType:
      type: string
      enum:
        - CLIENT_CONFIGURATION
        - BUILDING_CONFIGURATION
        - MODULE_CONFIGURATION
        - FEATURE_ENTITLEMENT_CONFIGURATION
        - NAVIGATION_ITEM
        - WORKSPACE
        - DASHBOARD
        - DASHBOARD_WIDGET
        - CMS_CONTENT
    ConfigurationLifecycleStatus:
      type: string
      enum: [DRAFT, VALIDATED, PUBLISHED, ACTIVE, SUPERSEDED]
    ConfigurationVersion:
      type: object
      description: Immutable source snapshot with separately controlled lifecycle metadata. Status mirrors the source record; lifecycleStatus is BE-27O state.
      required: [id, sourceType, sourceConfigurationId, clientId, buildingId, versionNumber, status, lifecycleStatus, previousVersionId, snapshot, createdByUserId, createdAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        sourceType: { $ref: "#/components/schemas/ConfigurationVersionSourceType" }
        sourceConfigurationId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        versionNumber: { type: integer, minimum: 1 }
        status: { type: string, maxLength: 64 }
        lifecycleStatus: { $ref: "#/components/schemas/ConfigurationLifecycleStatus" }
        previousVersionId: { type: string, format: uuid, nullable: true }
        snapshot:
          description: JSON-safe immutable source snapshot, capped at 1 MiB.
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
    ConfigurationValidationError:
      type: object
      required: [code, field, message]
      properties:
        code: { type: string }
        field: { type: string }
        message: { type: string }
    ConfigurationValidation:
      type: object
      required: [id, configurationVersionId, valid, errors, validatedByUserId, validatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        configurationVersionId: { $ref: "#/components/schemas/Uuid" }
        valid: { type: boolean }
        errors:
          type: array
          items: { $ref: "#/components/schemas/ConfigurationValidationError" }
        validatedByUserId: { $ref: "#/components/schemas/Uuid" }
        validatedAt: { type: string, format: date-time }
    ConfigurationValidationOutcome:
      type: object
      required: [version, validation]
      properties:
        version: { $ref: "#/components/schemas/ConfigurationVersion" }
        validation: { $ref: "#/components/schemas/ConfigurationValidation" }
    ConfigurationPreviewStatus:
      type: string
      enum: [ACTIVE, REVOKED, EXPIRED]
    ConfigurationPreviewContext:
      type: object
      required: [id, configurationVersionId, clientId, buildingId, createdByUserId, mode, status, expiresAt, revokedAt, revokedByUserId, createdAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        configurationVersionId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        mode: { type: string, enum: [PREVIEW] }
        status: { $ref: "#/components/schemas/ConfigurationPreviewStatus" }
        expiresAt: { type: string, format: date-time }
        revokedAt: { type: string, format: date-time, nullable: true }
        revokedByUserId: { type: string, format: uuid, nullable: true }
        createdAt: { type: string, format: date-time }
    CreateConfigurationPreviewRequest:
      type: object
      additionalProperties: false
      properties:
        expiresInMinutes: { type: integer, minimum: 1, maximum: 120, default: 15 }
    PreviewEffectiveConfiguration:
      type: object
      required: [clientId, buildingId, configurations]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        configurations: { type: object, additionalProperties: true }
    EffectiveConfigurationPreview:
      type: object
      required: [mode, context, version, sourceSnapshot, configuration, navigation, workspaces]
      properties:
        mode: { type: string, enum: [PREVIEW] }
        context: { $ref: "#/components/schemas/ConfigurationPreviewContext" }
        version: { $ref: "#/components/schemas/ConfigurationVersion" }
        sourceSnapshot:
          description: Exact immutable non-active version snapshot under preview.
        configuration:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/PreviewEffectiveConfiguration"
        navigation:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/EffectiveNavigation"
        workspaces:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/EffectiveWorkspaces"

    ConfigurationAuditAction:
      type: string
      enum:
        - CONFIGURATION_DRAFT_CREATED
        - CONFIGURATION_DRAFT_UPDATED
        - CONFIGURATION_VALIDATION_SUCCEEDED
        - CONFIGURATION_VALIDATION_FAILED
        - CONFIGURATION_PUBLISHED
        - CONFIGURATION_ACTIVATED
        - CONFIGURATION_SUPERSEDED
        - CONFIGURATION_PREVIEW_CREATED
        - CONFIGURATION_PREVIEW_ACCESSED
        - CONFIGURATION_PREVIEW_REVOKED
    ConfigurationAuditEvent:
      type: object
      required: [id, configurationId, configurationVersionId, sourceType, action, actorUserId, clientId, buildingId, previousStatus, newStatus, summary, metadata, occurredAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        configurationId: { $ref: "#/components/schemas/Uuid" }
        configurationVersionId: { type: string, format: uuid, nullable: true }
        sourceType: { $ref: "#/components/schemas/ConfigurationVersionSourceType" }
        action: { $ref: "#/components/schemas/ConfigurationAuditAction" }
        actorUserId: { type: string, format: uuid, nullable: true }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        previousStatus: { type: string, nullable: true }
        newStatus: { type: string, nullable: true }
        summary: { type: string, maxLength: 500 }
        metadata:
          type: object
          description: Compact allowlisted identifiers/status metadata only; snapshots, content, credentials, secrets, and tokens are never stored.
        occurredAt: { type: string, format: date-time }

    ClientRef:
      type: object
      required: [id, code, name]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string

    PropertyRef:
      type: object
      required: [id, code, name]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string

    BuildingRef:
      type: object
      required: [id, code, name]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string

    # ------------------------------------------------------------------
    # Health
    # ------------------------------------------------------------------
    HealthStatus:
      type: object
      required: [status, environment, uptimeSeconds, timestamp]
      properties:
        status:
          type: string
          const: ok
        environment:
          type: string
          description: NODE_ENV value (development | test | production).
        uptimeSeconds:
          type: integer
        timestamp:
          type: string
          format: date-time

    DatabaseHealth:
      type: object
      required: [status, database]
      properties:
        status:
          type: string
          const: ok
        database:
          type: string
          const: connected

    # ------------------------------------------------------------------
    # Authentication / session
    # ------------------------------------------------------------------
    LoginRequest:
      type: object
      description: >-
        Email + password login identifier. The email is the sole login
        identifier: it is trimmed and lowercased (normalized) before lookup;
        an unknown email, a wrong password and a non-login-allowed account
        state all return the same 401 `INVALID_CREDENTIALS` (no account
        enumeration). The password is the raw secret for this one request;
        the password policy is enforced only at set/change time, never at
        login. No refresh token or refresh endpoint exists — when the session
        expires the client re-authenticates with this request.
      required: [email, password]
      properties:
        email:
          type: string
          format: email
          description: >-
            Login identifier. Trimmed and lowercased server-side before the
            credential lookup.
        password:
          type: string
          format: password
          description: Raw password; verified against the stored hash and never returned by any endpoint.

    LoginResponse:
      type: object
      required: [sessionToken, expiresAt, user]
      properties:
        sessionToken:
          type: string
          description: >-
            Opaque, revocable bearer session token (high-entropy random; only
            its SHA-256 hash is stored server-side). Send it as
            `Authorization: Bearer <sessionToken>`. It is the single session
            credential — there is no separate access token and no refresh
            token; on expiry the client logs in again. Mobile adapters may map
            this value to their own `accessToken` field; the backend contract
            name is `sessionToken`.
        expiresAt:
          type: string
          format: date-time
          description: >-
            Session expiry instant, ISO 8601 UTC (`date-time`). The TTL is
            backend-controlled (`SESSION_TTL_MINUTES`); the session is invalid
            after this instant and there is no sliding refresh/rotation.
        user:
          $ref: "#/components/schemas/PublicUser"

    LogoutResponse:
      type: object
      required: [revoked]
      properties:
        revoked:
          type: boolean
          const: true

    PublicUser:
      type: object
      required: [id, email, displayName, status, createdAt, updatedAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        email:
          type: string
          format: email
        displayName:
          type: string
        status:
          $ref: "#/components/schemas/UserStatus"
        whatsappPhone:
          type: string
          nullable: true
          description: >
            CR-BE-NOTIFY-PROV-01 PART 06 — authoritative WhatsApp E.164
            number (null = no WhatsApp contact). Set only through the
            guarded whatsapp-contact seam (`user.manage`).
        whatsappOptedInAt:
          type: string
          format: date-time
          nullable: true
          description: Instant of the last explicit WhatsApp opt-in.
        whatsappOptedOutAt:
          type: string
          format: date-time
          nullable: true
          description: Instant of the last explicit WhatsApp opt-out.
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    UpdateUserWhatsAppContactRequest:
      type: object
      description: >
        CR-BE-NOTIFY-PROV-01 PART 06 — guarded WhatsApp contact + consent
        update. Both fields are optional (omitted = keep). Consent is always
        explicit: `OPT_IN` requires a phone (existing or supplied in the same
        request); `whatsappPhone: null` clears the number AND both consent
        stamps; clearing the phone and requesting OPT_IN in one call is
        rejected. Numbers are never inferred from any other field.
      properties:
        whatsappPhone:
          type: string
          nullable: true
          description: >
            Strict E.164 WhatsApp number (e.g. +628123456789); null clears
            the contact and consent.
        consent:
          type: string
          enum: [OPT_IN, OPT_OUT]
          description: Explicit consent action; stamps the current instant.

    UserStatus:
      type: string
      enum: [INVITED, ACTIVE, INACTIVE, SUSPENDED]

    # ------------------------------------------------------------------
    # CR-BE-MOB-CONTRACT-01 PART 02 — User / Role / Permission / Building
    # context schemas. Shapes mirror the backend DTOs exactly.
    # ------------------------------------------------------------------
    PublicRole:
      type: object
      required: [id, code, name, description, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        code:
          type: string
        name:
          type: string
        description:
          type: string
          nullable: true
        status:
          $ref: "#/components/schemas/RoleStatus"

    RoleStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    CreateRoleRequest:
      type: object
      required: [code, name]
      properties:
        code:
          type: string
          description: Normalized to uppercase.
        name:
          type: string
        description:
          type: string
        status:
          $ref: "#/components/schemas/RoleStatus"

    AssignRoleRequest:
      type: object
      required: [roleId]
      properties:
        roleId:
          $ref: "#/components/schemas/Uuid"

    PublicPermission:
      type: object
      required: [id, code, name, description, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        code:
          type: string
          description: '<domain>.<verb> permission code (e.g. user.read).'
        name:
          type: string
        description:
          type: string
          nullable: true
        status:
          $ref: "#/components/schemas/PermissionStatus"

    PermissionStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    CreatePermissionRequest:
      type: object
      required: [code, name]
      properties:
        code:
          type: string
          description: Normalized to lowercase.
        name:
          type: string
        description:
          type: string
        status:
          $ref: "#/components/schemas/PermissionStatus"

    AssignPermissionRequest:
      type: object
      required: [permissionId]
      properties:
        permissionId:
          $ref: "#/components/schemas/Uuid"

    CreateUserRequest:
      type: object
      required: [email, displayName]
      properties:
        email:
          type: string
          format: email
          description: Normalized (trimmed + lowercased); unique.
        displayName:
          type: string
        status:
          $ref: "#/components/schemas/UserStatus"

    PublicClient:
      type: object
      required: [id, code, name, legalName, taxId, description, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        code:
          type: string
        name:
          type: string
        legalName:
          type: string
          nullable: true
        taxId:
          type: string
          nullable: true
        description:
          type: string
          nullable: true
        status:
          $ref: "#/components/schemas/ClientStatus"

    ClientStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    CreateClientRequest:
      type: object
      required: [code, name]
      properties:
        code: { type: string }
        name: { type: string }
        legalName: { type: string }
        taxId: { type: string }
        description: { type: string }
        status: { $ref: "#/components/schemas/ClientStatus" }

    UpdateClientRequest:
      type: object
      minProperties: 1
      properties:
        code: { type: string }
        name: { type: string }
        legalName: { type: string, nullable: true }
        taxId: { type: string, nullable: true }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/ClientStatus" }

    PublicProperty:
      type: object
      required:
        [id, clientId, code, name, description, status, addressLine, city,
         province, postalCode, countryCode]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/PropertyStatus" }
        addressLine: { type: string, nullable: true }
        city: { type: string, nullable: true }
        province: { type: string, nullable: true }
        postalCode: { type: string, nullable: true }
        countryCode: { type: string, nullable: true }

    PropertyStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    CreatePropertyRequest:
      type: object
      required: [clientId, code, name]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        description: { type: string }
        status: { $ref: "#/components/schemas/PropertyStatus" }
        addressLine: { type: string }
        city: { type: string }
        province: { type: string }
        postalCode: { type: string }
        countryCode: { type: string }

    UpdatePropertyStatusRequest:
      type: object
      required: [status]
      properties:
        status: { $ref: "#/components/schemas/PropertyStatus" }

    PublicBuilding:
      type: object
      required:
        [id, propertyId, campusId, code, name, description, status, addressLine,
         city, province, postalCode, countryCode, timezone]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        propertyId: { $ref: "#/components/schemas/Uuid" }
        campusId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/BuildingStatus" }
        addressLine: { type: string, nullable: true }
        city: { type: string, nullable: true }
        province: { type: string, nullable: true }
        postalCode: { type: string, nullable: true }
        countryCode: { type: string, nullable: true }
        timezone: { type: string, nullable: true }

    BuildingStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    CreateBuildingRequest:
      type: object
      required: [propertyId, code, name]
      properties:
        propertyId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        description: { type: string }
        status: { $ref: "#/components/schemas/BuildingStatus" }
        addressLine: { type: string }
        city: { type: string }
        province: { type: string }
        postalCode: { type: string }
        countryCode: { type: string }
        timezone:
          type: string
          description: IANA timezone (e.g. Asia/Jakarta).

    UpdateBuildingStatusRequest:
      type: object
      required: [status]
      properties:
        status: { $ref: "#/components/schemas/BuildingStatus" }

    PublicUserBuildingAssignment:
      type: object
      required: [id, userId, buildingId, status, assignedByUserId, assignedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        userId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        status: { $ref: "#/components/schemas/UserBuildingAssignmentStatus" }
        assignedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        assignedAt:
          type: string
          format: date-time

    UserBuildingAssignmentStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    CreateBuildingAssignmentRequest:
      type: object
      required: [buildingId]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }

    # ------------------------------------------------------------------
    # Effective user context
    # ------------------------------------------------------------------
    EffectiveRole:
      type: object
      description: >-
        One ACTIVE role assigned to the user. A user may hold several roles
        simultaneously; they are the effective role SET, not a single
        selectable/active role — nothing here lets a mobile client choose or
        override a role.
      required: [id, code, name]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
          description: Stable role code (e.g. `PLATFORM_ADMIN`).
        name:
          type: string

    EffectiveAccess:
      type: object
      description: >
        Effective ACTIVE roles and the deduplicated effective permission set
        that RBAC (`requirePermission`) actually enforces — a single source
        of truth. `roles` is an array (multiple roles allowed); `permissions`
        is the sorted, deduplicated union of all ACTIVE roles' ACTIVE
        permission codes, presented as flat string codes — never mobile-only
        permission booleans, and never filtered by a mobile-selected role.
        Both arrays are empty (never omitted) for a user with no assignment.
      required: [roles, permissions]
      properties:
        roles:
          type: array
          items:
            $ref: "#/components/schemas/EffectiveRole"
        permissions:
          type: array
          description: Effective permission codes (e.g. `building.read`).
          items:
            type: string

    EffectiveBuildingContext:
      type: object
      required: [id, code, name]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string

    EffectivePropertyContext:
      type: object
      required: [id, code, name, buildings]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string
        buildings:
          type: array
          items:
            $ref: "#/components/schemas/EffectiveBuildingContext"

    EffectiveClientContext:
      type: object
      required: [id, code, name, properties]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string
        properties:
          type: array
          items:
            $ref: "#/components/schemas/EffectivePropertyContext"

    EffectiveEntitlement:
      type: object
      description: Effective module entitlement resolved from Client subscriptions.
      required: [moduleCode, status]
      properties:
        moduleCode:
          type: string
        status:
          type: string
          const: ACTIVE

    EffectiveUserContext:
      type: object
      description: >
        The single authoritative context of the authenticated user (BE-01I /
        BE-02H / BE-25B). `scope` and `context.workforce` are the BE-25B mobile
        additions; all dimensions derive from the same resolvers BE-02G data
        isolation uses — no separate mobile context engine.
      required: [user, access, context, scope, entitlements]
      properties:
        user:
          $ref: "#/components/schemas/PublicUser"
        access:
          $ref: "#/components/schemas/EffectiveAccess"
        context:
          type: object
          required: [clients, workforce]
          properties:
            clients:
              type: array
              description: Reachable commercial hierarchy from explicit building assignments.
              items:
                $ref: "#/components/schemas/EffectiveClientContext"
            workforce:
              type: array
              description: >
                Workforce profile(s) linked to the authenticated user
                (identity only). Empty when the user has no linked profile or
                the profile belongs to an inaccessible Client.
              items:
                $ref: "#/components/schemas/EffectiveWorkforceProfile"
        scope:
          $ref: "#/components/schemas/EffectiveScope"
        entitlements:
          type: array
          items:
            $ref: "#/components/schemas/EffectiveEntitlement"

    EffectiveScope:
      type: object
      description: >
        BE-25B mobile data scope: the flat accessible Building/Client id sets
        derived from the user's explicit Building assignments (the same source
        BE-02G data isolation uses). Never lists scope isolation would deny.
      required: [buildingIds, clientIds]
      properties:
        buildingIds:
          type: array
          description: Accessible Building ids (empty for a zero-scope user).
          items:
            $ref: "#/components/schemas/Uuid"
        clientIds:
          type: array
          description: Clients reachable through the accessible Buildings (empty for a zero-scope user).
          items:
            $ref: "#/components/schemas/Uuid"

    EffectiveWorkforceProfile:
      type: object
      description: >
        BE-25B workforce profile linked to the authenticated user. Identity
        only — Organization / Department / Team / Position dimensions remain
        deferred and are not expanded here.
      required: [id, clientId, employeeCode, fullName, workforceType, status]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        employeeCode:
          type: string
        fullName:
          type: string
        workforceType:
          type: string
          enum: [INTERNAL, OUTSOURCED, CONTRACT, EXTERNAL]
        status:
          type: string
          enum: [ACTIVE, INACTIVE]

    # ------------------------------------------------------------------
    # Current user building assignments (GET /auth/me/buildings)
    # ------------------------------------------------------------------
    UserBuildingContext:
      type: object
      description: One ACTIVE building assignment with its Property/Client refs.
      required: [id, status, building]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
          description: User building assignment id.
        status:
          type: string
          const: ACTIVE
        building:
          $ref: "#/components/schemas/BuildingRef"
        property:
          allOf:
            - $ref: "#/components/schemas/PropertyRef"
          nullable: true
        client:
          allOf:
            - $ref: "#/components/schemas/ClientRef"
          nullable: true

    # ------------------------------------------------------------------
    # BE-23J / BE-24 PART 10 neutral reporting export
    # ------------------------------------------------------------------
    ReportingExportColumn:
      type: object
      required: [key, label, type]
      properties:
        key:
          type: string
        label:
          type: string
        type:
          type: string
          enum: [STRING, NUMBER, PERCENT, DATE, BOOLEAN]

    ReportingExportKpiValue:
      type: object
      description: Value copied from the owning KPI/read model without recalculation.
      required: [key, label, value, type]
      properties:
        key:
          type: string
        label:
          type: string
        value:
          nullable: true
          oneOf:
            - type: string
            - type: number
        type:
          type: string
          enum: [STRING, NUMBER, PERCENT, DATE, BOOLEAN]

    ReportingExportTable:
      type: object
      required: [key, label, columns, rows, rowCount]
      properties:
        key:
          type: string
        label:
          type: string
        columns:
          type: array
          items:
            $ref: "#/components/schemas/ReportingExportColumn"
        rows:
          type: array
          items:
            type: object
            additionalProperties:
              nullable: true
              oneOf:
                - type: string
                - type: number
                - type: boolean
        rowCount:
          type: integer
          minimum: 0

    ReportingExportMetadata:
      type: object
      required:
        [dataset, datasetLabel, buildingId, buildingScope, period, filters, asOf]
      properties:
        dataset:
          $ref: "#/components/schemas/ReportArchiveDataset"
        datasetLabel:
          type: string
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        buildingScope:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/Uuid"
        period:
          type: object
          required: [dateFrom, dateTo]
          properties:
            dateFrom:
              type: string
              nullable: true
            dateTo:
              type: string
              nullable: true
        filters:
          type: object
          additionalProperties:
            nullable: true
            oneOf:
              - type: string
              - type: number
              - type: boolean
        asOf:
          type: string
          format: date-time
          nullable: true

    ReportingExport:
      type: object
      description: >
        Export-ready structured JSON only. No chart metadata, binary file,
        PDF/Excel rendering, BI materialization, or alternate KPI calculation.
      required: [metadata, kpis, tables, generatedAt]
      properties:
        metadata:
          $ref: "#/components/schemas/ReportingExportMetadata"
        kpis:
          type: array
          items:
            $ref: "#/components/schemas/ReportingExportKpiValue"
        tables:
          type: array
          items:
            $ref: "#/components/schemas/ReportingExportTable"
        generatedAt:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # CR-BE-EXP-01 PART 06 — report archive request/completion/download
    # ------------------------------------------------------------------
    ReportArchiveDataset:
      type: string
      enum:
        [SECURITY_PATROL, SECURITY_FINDING_INCIDENT, WORKFORCE,
         VENDOR_TENANT, UTILITY,
         MANAGEMENT_OPERATIONS_COMMAND_CENTER,
         VENDOR_SERVICE_REGISTER, FINDING_REGISTER,
         WORK_ORDER_REGISTER, CHECKLIST_EXECUTION_SUMMARY,
         OPERATIONAL_DETAIL, CORRECTIVE_ACTION,
         OPERATIONAL_DETAIL_HISTORY, WORK_ORDER_SLA,
         SCHEDULED_OPERATION_LINEAGE, PERMIT_TO_WORK,
         SECURITY_OPERATIONAL_DETAIL,
         INCIDENT_REGISTER,
         RECEIVING_REGISTER,
         PURCHASE_ORDER_REGISTER,
         VENDOR_INVOICE_REGISTER,
         STOCK_MOVEMENT_REGISTER,
         OPERATIONAL_BUDGET_VARIANCE,
         ESG_METRIC_TREND,
         ESG_WASTE_REGISTER,
         UTILITY_CONSUMPTION_TREND,
         PORTFOLIO_OPERATIONAL_COMPARISON]
      description: >
        Dataset keys currently exposed by the runtime reporting registry.
        OPERATIONAL_DETAIL exposes the operationalDetail primary table and
        the operationalDetailAttribution presentation table. For CSV,
        omitted tableKey uses operationalDetail; an explicit tableKey may
        select either governed table.
        CORRECTIVE_ACTION (R10) is a pure Reporting adapter over the
        authoritative BE-21G corrective-action read and requires
        corrective_action.read. It exposes exactly one table,
        correctiveAction, carrying one row per authoritative corrective
        action with its resolved Incident context. dueState and isOverdue
        are copied verbatim from the owning domain's derived deadline
        projection and are never recomputed by Reporting; a COMPLETED
        action is therefore never relabelled OVERDUE. The verification and
        responsibility child histories are separate grains and are not
        joined into this export. Because the response holds a single table,
        an omitted tableKey selects it and no dataset-specific CSV default
        exists. Supported filters are exactly those the authoritative read
        accepts: incidentId, buildingId, actionType, status,
        incidentStatus, incidentType, overdue, hasDueDate, dueBefore and
        dueAfter. Omitting buildingId rolls the export up across the
        Buildings the caller can access.
        OPERATIONAL_DETAIL_HISTORY (R10) exposes the existing R08 child
        history grains and requires checklist.read, the same authority
        OPERATIONAL_DETAIL uses. Two parameters are REQUIRED. `history`
        selects exactly one child grain; the implemented values are
        EVIDENCE, FINDING_REWORK and REVIEW, which is the whole frozen
        vocabulary, so no declared value routes to a missing
        implementation and any value outside it is rejected with a
        validation error. `engine` is
        required with no default and accepts only CHECKLIST_EXECUTION or
        FORM_INSTANCE; BOTH, ALL and an omitted engine are rejected. For
        history=EVIDENCE the response holds exactly one table,
        operationalDetailEvidence, with one row per authoritative evidence
        submission and no aggregation or deduplication. Fields are a
        verbatim re-presentation of the R08 evidence row: metadata only,
        never a storage key, path, bucket, token, signed URL or file
        bytes. capturedAt is a nullable device timestamp and not an actor;
        submittedByUserId means submitted by only and is never an
        executor. Integrity and retention facts are copied unchanged and
        never re-derived, and status and retentionState remain independent
        stored dimensions, so withdrawn and disposed evidence stay visible
        as lineage. Its filters are exactly those the R08 evidence child
        accepts: buildingId, executionId, templateId, dateFrom, dateTo,
        status and retentionState. For history=FINDING_REWORK the response
        holds exactly one table, operationalDetailFindingRework, with one
        row per finding rework cycle and never one row per finding,
        execution or review, so multiple cycles for one finding are never
        collapsed, aggregated or deduplicated by findingId. Its fields are
        a verbatim re-presentation of the R08 finding-rework row. status
        is exactly REQUESTED or RESUBMITTED copied unchanged, and
        RESUBMITTED means only that the finding was resubmitted from that
        cycle; it never means accepted, approved, verified, closed or
        completed, and no derived lifecycle classification exists.
        requestedByUserId and resubmittedByUserId are the only two
        persisted cycle actors and neither is an executor, performed-by or
        actual executor. triggerReviewId is the review that caused the
        cycle and is never its re-verification, acceptance or latest
        review. reworkNotes is mutable and is overwritten at resubmission,
        so the pre-resubmission value is unavailable and is never
        reconstructed or labelled original notes; reason is the separate
        stable instruction persisted when the cycle was requested.
        parentBuildingId and findingBuildingId are both preserved as
        distinct facts and are never collapsed, coalesced, compared or
        used as a fallback for one another. requestedAt and resubmittedAt
        are the authoritative lifecycle timestamps, while updatedAt moves
        on notes edits and on resubmission, so it is not a lineage
        timestamp and never orders lifecycle semantics. Its filters are
        exactly those the R08 finding-rework child accepts: buildingId,
        executionId, templateId, dateFrom, dateTo, findingId,
        reworkCycleId and status; executionId, findingId, reworkCycleId
        and templateId narrow only and can never bypass authorized
        building scope, and clientId remains a row fact rather than a
        caller filter. For history=REVIEW the response holds exactly one
        table, operationalDetailReviewHistory, with one row per persisted
        execution review. This is FULL review history and is never
        narrowed: it is not the latest review, not the latest completed
        review, not one row per execution and not one row per decision, so
        several reviews of one execution all appear and nothing is
        aggregated or deduplicated. Its fields are a verbatim
        re-presentation of the R08 review-history row. status is exactly
        PENDING or COMPLETED copied unchanged, and BOTH are included:
        PENDING rows are part of this history and are never filtered out,
        so the response is never completed-only. decision reuses the
        shared reviews authority APPROVED, REJECTED and REWORK_REQUIRED
        and may be null, which is meaningful on every PENDING row; a null
        decision is never coerced to PENDING, UNDECIDED, NONE, UNKNOWN or
        N/A and never normalized to a pass/fail or approval outcome.
        reviewerUserId is the user stored when the review was OPENED and is
        never rewritten. The user who submitted a decision is NOT persisted
        anywhere, so this history exposes no decision actor and must never
        be read as one: reviewerUserId is a reviewer identity only, not
        decided-by, approved-by, rejected-by or verified-by, and because
        PENDING rows are included this is review history rather than a
        verification audit trail. notes is mutable and the completion path
        may overwrite the prior value, which is not preserved anywhere and
        is therefore unavailable; the current value is exposed verbatim and
        is never reconstructed or labelled original, initial, rejection or
        decision notes. reviewedAt is the persisted review-completion time
        and is null until completion, while createdAt is the only immutable
        timestamp and updatedAt moves on every completion write, so
        updatedAt is a record fact that is never used as the review
        completion time, as a decision time or for ordering. reviews stores
        no building_id, so parentBuildingId, resolved for the parent
        execution, is the only building fact and no review-level building
        scope exists. Only execution review targets are reachable: the
        child binds the review target type to the engine, so the other
        shared review targets such as FINDING, WORK_ORDER, VENDOR_WORK,
        PERMIT_APPLICATION, DOCUMENT and CORRECTIVE_ACTION remain
        structurally unreachable and are not widened here. Its filters are
        exactly those the R08 review-history child accepts: buildingId,
        executionId, templateId, dateFrom, dateTo, reviewId, status and
        decision; executionId, reviewId and templateId narrow only and can
        never bypass authorized building scope, and clientId remains a row
        fact rather than a caller filter. For every grain caller limit and
        offset are not export filters and are stripped; paging is internal,
        with the upper date boundary frozen once per export. Because each
        response holds a single table, an omitted tableKey selects it and
        no dataset-specific CSV default exists.
        WORK_ORDER_SLA (R10) exposes the completed work-order-sla-register
        read model and requires work_order.read, the existing governed Work
        Order read permission; no new permission is introduced. Parsing,
        validation, fail-closed Building-scope resolution and the row read all
        stay with that register, so Reporting holds no SLA, clock, pause or
        escalation query of its own. Its grain is exactly one row per SLA
        clock, NOT one row per Work Order: a single Work Order legitimately
        produces both a RESPONSE and a RESOLUTION row, repeated workOrderId
        values are expected, and the two clocks are never collapsed,
        aggregated or reduced to a current or primary clock. The response
        holds exactly one table, workOrderSla, whose 34 fields are a verbatim
        re-presentation of the register row with no recomputation, no renamed
        semantics and no inferred value. buildingId is both an access
        assertion and a narrowing filter, while workOrderId narrows only and
        can never create or widen Building authority; no clientId scope
        override is accepted. Its filters are exactly the register's twelve:
        buildingId, workOrderId, clockType, clockStatus, breached, paused,
        escalationStatus, definitionCode, workType, priority, dateFrom and
        dateTo. dateFrom and dateTo form a UTC half-open window over the
        applied SLA appliedAt and are never reinterpreted as a breachedAt,
        startedAt, satisfiedAt or escalation period; there is no default
        reporting period. breachedAt is the persisted breach truth and NULL
        means never breached — breach is never recomputed from startedAt,
        targetMinutes or the current time, and no approaching-breach rule or
        threshold exists. effectiveElapsedMilliseconds is a CURRENT-ONLY
        read-time value measured at the envelope asOf instant, not a
        historical or final elapsed snapshot, and it is never a breach,
        overdue or SLA violation duration. isPaused, pauseCount and
        totalPausedMilliseconds are separate persisted pause facts for that
        clock; the pause deduction applied to elapsed is RESOLUTION-only in
        the owning SLA authority and Reporting neither applies nor reverses
        nor normalizes it. escalationCount with latestEscalationLevel,
        latestEscalationStatus and latestEscalationTriggeredAt are bounded
        aggregate and latest facts only — latestEscalationStatus is the
        status of the latest escalation action and never a current SLA
        status, no full escalation history is exposed, and no recipient
        identity is returned. No SLA KPI is calculated: there is no breach
        rate, compliance percentage, average elapsed, average pause,
        escalation rate or near-breach count.
        SCHEDULED_OPERATION_LINEAGE (R10) exposes the completed
        scheduled-operation-lineage read model and requires task.read, the
        existing permission that already governs generated-task reads; no new
        permission is introduced and schedule.read is not used, because it
        governs the schedule-definition read model rather than the generated
        tasks returned here. Parsing, validation, fail-closed Building-scope
        resolution and the row read all stay with that module, so Reporting
        holds no generated-task, schedule, recurrence, assignment or
        execution-binding query of its own. Its grain is exactly one row per
        generated task, identified by generatedTaskId: that IS the task
        identity and there is no separate taskId field, because the schema has
        no task entity apart from generated_tasks. It is NOT one row per
        schedule and NOT one row per execution — a single schedule definition
        legitimately contributes many rows, one per generated occurrence, and
        rows are never collapsed, grouped or deduplicated by schedule, status,
        assignee type or binding. The dataset is Building-scoped only:
        generated tasks with no Building are excluded and are never re-admitted
        through a Client scope, and no clientId scope override is accepted. The
        response holds exactly one table, scheduledOperationLineage, whose 24
        fields are a verbatim re-presentation of the read-model row with no
        recomputation and no renamed semantics. Its filters are exactly the
        module's eleven: buildingId, scheduleDefinitionId, generatedTaskId,
        status, checklistExecutionId, formInstanceId, assigneeType,
        assignedWorkforceProfileId, assignedTeamId, dateFrom and dateTo.
        dateFrom and dateTo form a UTC half-open window over the generated task
        occurrenceAt and are never reinterpreted as createdAt, generatedAt,
        completedAt, an assignment time or a recurrence start or end; there is
        no default reporting period. checklistExecutionId and formInstanceId
        are two separate 0..1 bindings and are never merged into a generic
        execution identifier, nor is an execution engine inferred from whichever
        is present; only the bound identifiers are returned, never execution
        status, timestamps, responses or actors, so no execution history is
        exposed. Every assignment field describes who the task is assigned to
        and never who executed it: there is no executor, executed-by,
        performed-by or completed-by attribution, and assigned workforce and
        team names are live current reference names rather than historical
        snapshots. Recurrence is persisted 0..1 total lineage per schedule
        definition, not a recurrence history: there is no recurrence count,
        version or latest-recurrence selector, and an inactive recurrence is
        reported as the factual lineage value it is rather than filtered out.
        No missed, overdue, late, on-time, grace or schedule-compliance value is
        derived, and status remains the persisted generated-task status rather
        than a timeliness outcome. No KPI is calculated: there is no scheduled,
        completed, missed or overdue count and no completion, on-time,
        assignment or execution rate.
        PERMIT_TO_WORK (R10) exposes the completed permit-to-work-register
        read model and requires permit.read, the existing permission that
        already governs permit reads; no new permission is introduced and
        neither permit.manage nor permit.approve is used, because those are
        write and decision authorities rather than a read authority.
        Parsing, validation, fail-closed Building-scope resolution and the
        row read all stay with that module, so Reporting holds no permit,
        application, approval-binding, validity or work-lifecycle query of
        its own. It is ONE dataset with TWO required presentation views,
        not two datasets and no alias: view is REQUIRED, accepts only
        LIFECYCLE or APPROVAL, has no default, and is never inferred from
        which filters are present. A filter belonging to the other view is
        rejected rather than silently ignored. Each view returns exactly
        one table and never both, so an omitted tableKey selects that
        single table and no dataset-specific CSV default exists.
        For view=LIFECYCLE the response holds exactly one table,
        permitToWorkLifecycle, whose 19 fields are a verbatim
        re-presentation of the read-model row with no recomputation and no
        renamed semantics. Its grain is exactly one row per permit
        application, identified by permitApplicationId, and it is never one
        row per permit, per approval or per validity. Its filters are
        exactly the module's seven: buildingId, permitApplicationId,
        permitId, applicationStatus, permitStatus, dateFrom and dateTo.
        dateFrom and dateTo form a UTC half-open window over the requested
        work instant requestedWorkAt and are never reinterpreted as
        submittedAt, cancelledAt, createdAt, a validity bound or an
        approval time; there is no default reporting period. approvalCount
        and pendingApprovalCount are aggregate counts only and are never a
        current, next, remaining or required approval selector; this view
        exposes no approval id, stage, type, status or assigned approver,
        because those belong to the APPROVAL grain.
        For view=APPROVAL the response holds exactly one table,
        permitToWorkApproval, whose 19 fields are a verbatim
        re-presentation of the read-model row with no recomputation and no
        renamed semantics. Its grain is exactly one row per approval
        binding, identified by approvalId, and it is a true one-to-many
        child of the application: a single permit legitimately contributes
        many approval rows, which are never collapsed, grouped or
        deduplicated by application, stage, type, status or approver. The
        two views are never merged into a universal permit row. Its filters
        are exactly the module's thirteen: buildingId, permitApplicationId,
        permitId, applicationStatus, permitStatus, approvalStage,
        approvalType, approvalStatus, reviewStatus, decision,
        assignedApproverUserId, dateFrom and dateTo. dateFrom and dateTo
        form a UTC half-open window over the approval binding createdAt and
        are never reinterpreted as reviewedAt, an application timestamp or
        a validity bound. There is no current, latest, next or active
        approval and no stage precedence: approvalStage and approvalType
        are free-form persisted values with no ordering or workflow
        position implied, and the published field order is not a sequence.
        approvalStatus is the owning domain's single derivation copied
        verbatim and never recomputed by Reporting, while reviewStatus and
        decision remain separate published inputs rather than a second
        interpretation of them. applicationStatus, permitStatus and
        approvalStatus stay three independent dimensions and are never
        merged into one combined status. notes is the binding's current
        mutable note and is never a note history. assignedApproverUserId
        records assignment only and createdByUserId records binding
        creation only; neither is a decision actor, and no decision actor
        is published, so there is no approved-by, rejected-by or
        decided-by field. Both views are Building-scoped only and no
        clientId scope override is accepted. No KPI is calculated for
        either view: there is no application, submitted, cancelled, valid,
        approval, pending, approved, rejected or rework count and no
        completion, approval or pending rate.
        SECURITY_OPERATIONAL_DETAIL (R10) exposes the Security operational
        detail grains and requires security_report.read, the existing
        permission that already gates every security report dataset
        endpoint; no new permission is introduced and no generic
        security.read, task.read, reporting.read or admin fallback is
        substituted. It is ONE dataset behind a REQUIRED `source`
        discriminator, not one dataset per source: there is no
        SECURITY_PATROL detail dataset, no PATROL_DETAIL, no
        PATROL_OPERATIONAL_DETAIL and no alias. `source` has no default and
        is never inferred from the filters present; an omitted, empty,
        repeated or unknown source is a validation error. The declared
        vocabulary is PATROL, SHIFT_HANDOVER, SECURITY_FINDING and
        INCIDENT_READINESS, and all four are implemented, so no declared
        value is currently rejected as unimplemented. Declaring a value is
        still not availability: the fail-closed guard is deliberately
        retained, so any value staged into this vocabulary later is rejected
        as not yet implemented rather than routed to another source or
        answered with an empty result set. Exactly one source is served per
        request, so a response never holds more than one of
        securityOperationalPatrol, securityOperationalShiftHandover,
        securityOperationalFinding and
        securityOperationalIncidentReadiness, and no placeholder table
        exists for an unimplemented source.
        For source=PATROL the response holds exactly one table,
        securityOperationalPatrol, whose 13 fields are a verbatim
        re-presentation of the authoritative BE-12M patrol dataset row with
        no recomputation and no renamed semantics. No second table, no
        placeholder table for a later source, no patrol summary and no
        patrol KPI table is returned. Because the response holds a single
        table, an omitted tableKey selects it, and no dataset-specific CSV
        default exists for SECURITY_OPERATIONAL_DETAIL: later sources will
        add their own source-dependent table keys, so a dataset-wide default
        would be wrong. OPERATIONAL_DETAIL remains the only dataset with a
        CSV default.
        `buildingId` is REQUIRED for source=PATROL and its absence is a
        validation error. The export never rolls up across every accessible
        Building and never widens the scope: the authorized Building scope
        is exactly the requested buildingId, which the owning read has
        already existence- and access-checked. Patrol rows carry no
        buildingId column, so the scope is reported from the authorized
        filter rather than derived from the returned rows. dateFrom and
        dateTo keep the owning read's own period semantics over the patrol
        task occurrenceAt, including its exclusive next-day upper bound for
        a date-only dateTo; Reporting creates no second interpretation and
        has no default reporting period.
        The grain is exactly one row per governed patrol dataset row, and
        the row identity is the pair taskId and patrolScheduleBindingId.
        taskId is the owning read's own field name and is never renamed,
        aliased or reinterpreted; there is no generatedTaskId field in this
        dataset. patrolScheduleBindingId identifies the ACTIVE patrol
        schedule binding the task was joined through. The patrol query joins
        a generated task to its bindings on the schedule definition alone,
        while the ACTIVE binding uniqueness constraint covers the patrol
        route together with the schedule definition, so several ACTIVE
        bindings for one schedule definition across different patrol routes
        are valid and one taskId legitimately appears on more than one row.
        That multiplicity is authoritative: repeated taskId is valid, rows
        are never collapsed, grouped, deduplicated or ranked by taskId,
        schedule definition or patrol route, and no primary, current, first
        or latest binding is selected. Neither identity field is derived
        from the other and no composite string identity is synthesized.
        Supported filters are exactly those the owning patrol read applies:
        buildingId, securityPostId, patrolRouteId, status, dateFrom and
        dateTo. No missed, overdue, dueBefore, graceMinutes,
        patrolScheduleBindingId or generatedTaskId filter exists, and the
        binding identity is exposed as a row fact rather than as a new
        filter authority.
        No timeliness value is materialized on these rows. The Security KPI
        derives overdue from a status test combined with an occurrence
        instant compared against an asOf minus graceMinutes, and missed from
        that plus a NULL started instant; none of that arithmetic is
        reproduced in Reporting, and no missed, isMissed, overdue,
        isOverdue, dueBefore, graceMinutes, onTime, late or
        complianceStatus field exists. Rows are never classified against a
        clock, and the envelope asOf is export metadata only. status remains
        the persisted generated-task status rather than a timeliness
        outcome, and occurrenceAt, startedAt and completedAt are the
        persisted instants they are. completedByUserId is the persisted
        completion actor as published, copied rather than inferred.
        Consequently this patrol list is NOT an exact contributor list for
        the MISSED or OVERDUE patrol KPI and is never presented as one: it
        is the governed patrol record list for the selected period and
        Building. The patrol dataset filters a security post with a COALESCE
        of the binding's and the route's start post, whereas the KPI domain
        matches either of them; that known divergence is owned by the
        security domain and is deliberately not reconciled here, so
        security-post filtered patrol records must not be read as KPI
        contributor parity. No KPI is calculated in this Reporting dataset:
        there is no patrol count, missed count, overdue count, completion
        rate or compliance rate, and the existing Security KPI remains
        authoritative elsewhere.
        For source=SHIFT_HANDOVER the response holds exactly one table,
        securityOperationalShiftHandover, whose 9 fields are a verbatim
        re-presentation of the authoritative BE-12M shift-handover
        dataset row with no recomputation and no renamed semantics:
        bindingId, shiftHandoverId, startSecurityPostId,
        startSecurityPostCode, patrolRouteId, patrolRouteCode,
        bindingStatus, handoverStatus and handoverCreatedAt, in that
        authoritative order. Every one is a renderer-safe scalar, so
        nothing is flattened, joined or serialized. No second table, no
        summary table and no handover KPI table is returned.
        `buildingId` is REQUIRED for source=SHIFT_HANDOVER exactly as it
        is for source=PATROL, and its absence is a validation error. The
        export never rolls up across every accessible Building and never
        widens the scope: the authorized Building scope is exactly the
        requested buildingId, which the owning read has already
        existence- and access-checked. The scope is reported from the
        authorized filter rather than derived from the returned rows.
        The grain is exactly one row per governed shift-handover dataset
        row, and the row identity is bindingId, the owning read's own
        binding identifier, copied verbatim. No synthetic handover
        identifier is minted and no composite identifier string is
        synthesized. shiftHandoverId is a related fact rather than the
        row identity: one shift handover may be bound more than once, so
        a repeated shiftHandoverId legitimately appears on several rows
        and is preserved. Rows are never collapsed, grouped,
        deduplicated or ranked, and no latest, current, most recent,
        active or effective handover is selected; there is no LIMIT 1,
        no maximum-timestamp pick, no DISTINCT ON, no ROW_NUMBER and no
        sort-and-first election anywhere in this path.
        Supported filters are exactly those the owning shift-handover
        read applies: buildingId, securityPostId, patrolRouteId, status,
        dateFrom and dateTo. status is validated against the security
        module's own published handover status vocabulary and matches
        the handover's persisted status; an unknown status is a
        validation error rather than an empty result set. No latestOnly,
        currentOnly, handoverState, acknowledged, unacknowledged,
        fromShift or toShift filter exists, and no generic page, limit,
        offset, sort or search parameter is accepted. This read matches
        a security post by direct equality on the binding's own start
        post, whereas source=PATROL uses a COALESCE of the binding's and
        the route's start post; that difference is owned by the security
        domain and is deliberately not reconciled here, so the two
        sources do not filter security posts identically.
        dateFrom and dateTo keep the owning read's own period semantics,
        which bound the shift handover's persisted created instant with
        a half-open range and order rows by that instant descending,
        then by the binding's created instant descending. Reporting
        substitutes no other date column, invents no shift start or
        shift end period, creates no new date mode and has no default
        reporting period.
        Both persisted statuses are projected verbatim and separately:
        bindingStatus is the binding's own status and handoverStatus is
        the handover's status. Neither is derived from the other,
        merged, prioritized or relabeled, and no completed, accepted,
        acknowledged, overdue, missed, pending or late value is inferred
        from handoverCreatedAt or from any other field. The
        authoritative row publishes no actor field of any kind, so none
        is projected and none is inferred: no creator, author,
        submittedBy, handoverBy, receiver, acknowledger, workforce or
        executor identity is attached to a handover row, and
        acknowledgement is represented only by the persisted
        handoverStatus value.
        No KPI is calculated for this source: there is no handover
        count, handover completion rate, pending handover count, late
        handover figure or shift compliance metric, and kpis is empty.
        The permission is the same existing security_report.read that
        gates every source of this dataset and every security report
        dataset endpoint. SECURITY_OPERATIONAL_DETAIL still has no
        dataset-specific CSV default because its table key is source-
        dependent; OPERATIONAL_DETAIL remains the only dataset with a
        CSV default.
        For source=SECURITY_FINDING the response holds exactly one
        table, securityOperationalFinding, whose 11 fields are a
        verbatim re-presentation of the authoritative BE-12M security-
        finding dataset row with no recomputation and no renamed
        semantics: linkId, findingId, findingNumber, findingTitle,
        findingStatus, linkStatus, sourcePostId, sourcePostCode,
        sourceRouteId, sourceRouteCode and reportedAt, in that
        authoritative order. Every one is a renderer-safe scalar, so
        nothing is flattened, joined or serialized. No second table, no
        summary table and no finding KPI table is returned.
        `buildingId` is REQUIRED for source=SECURITY_FINDING exactly as
        it is for source=PATROL and source=SHIFT_HANDOVER, and its
        absence is a validation error. The export never rolls up across
        every accessible Building and never widens the scope: the
        authorized Building scope is exactly the requested buildingId,
        which the owning read has already existence- and access-checked.
        The scope is reported from the authorized filter rather than
        derived from the returned rows.
        The grain is exactly one row per governed security-finding
        dataset row, and the row identity is linkId, the owning read's
        own finding-link identifier, copied verbatim. No synthetic
        identity is minted: there is no securityFindingKey,
        compositeFindingId, findingReference or sourceFindingId, because
        the authoritative public row has no such field. findingId is a
        related fact rather than the row identity: one finding may be
        linked to more than one post or route, so a repeated findingId
        legitimately appears on several rows and is preserved. Rows are
        never collapsed, grouped, deduplicated or ranked, and no latest,
        current, most recent, active or effective finding is selected;
        there is no LIMIT 1, no maximum-timestamp pick, no DISTINCT ON,
        no ROW_NUMBER and no sort-and-first election anywhere in this
        path.
        These rows are the Security Reports finding-link dataset only.
        They are NOT the R08 Finding child grain and NOT the
        FINDING_REGISTER dataset, and Reporting performs no join to
        either, merges nothing across those grains and enriches no row.
        No relation is inferred from a shared finding identifier, title,
        timestamp, security post, patrol route, Building or user, and no
        second finding authority, finding repository, finding status
        vocabulary or finding lifecycle is created here.
        Supported filters are exactly those the owning security-finding
        read applies: buildingId, status, securityPostId, patrolRouteId,
        dateFrom and dateTo. status is validated against the security
        module's own published finding status vocabulary and matches the
        FINDING's persisted status; an unknown status is a validation
        error rather than an empty result set. No openOnly,
        unresolvedOnly, criticalOnly, overdue, ageDays, SLA, assignee,
        latestOnly or currentOnly filter exists, and no generic page,
        limit, offset, sort or search parameter is accepted. This read
        matches a security post by direct equality on the link's own
        start post, which is the same form source=SHIFT_HANDOVER uses
        and not the COALESCE form source=PATROL uses; that difference is
        owned by the security domain and is deliberately not reconciled
        here, so the sources do not filter security posts identically.
        dateFrom and dateTo keep the owning read's own period semantics,
        which bound the FINDING's persisted reported instant with a
        half-open range and order rows by that instant descending, then
        by the finding link's created instant descending. Reporting
        substitutes no other date column, creates no second date
        interpretation, re-bounds on no createdAt, resolvedAt or
        updatedAt, and has no default reporting period.
        Two statuses are published and both are projected verbatim and
        separately. findingStatus is the finding's own persisted status.
        linkStatus is NOT a persisted link lifecycle column: the owning
        read publishes it as a constant ACTIVE literal, and it is copied
        exactly as published. Neither column is derived from the other,
        merged, prioritized or relabeled, and no open, closed, resolved,
        overdue, aging, breached or escalated value is inferred from
        reportedAt, from the other status, or from any other field.
        Reporting declares no finding severity or status vocabulary of
        its own.
        The authoritative row publishes no actor field of any kind, so
        none is projected and none is inferred: there is no createdBy,
        reportedBy, assignedTo, verifiedBy or resolvedBy column, nothing
        is relabeled as an executor, performedBy or completedBy, and no
        identity is inferred from a related record. reportedAt is a
        persisted instant, not an actor.
        No SLA, overdue or age value is materialized on these rows:
        there is no SLA, overdue, age, timeOpen, breach, responseTime or
        resolutionTime field, no row is classified against a clock, and
        no current-time arithmetic is performed. The envelope asOf is
        export metadata only and is never compared against reportedAt.
        No KPI is calculated for this source: there is no finding count,
        open count, critical count, closure rate, average age or overdue
        count, and kpis is empty, so the existing Security reporting and
        KPI authority remains separate and unchanged. The permission is
        the same existing security_report.read that gates every source
        of this dataset and every security report dataset endpoint,
        including the findings endpoint. SECURITY_OPERATIONAL_DETAIL
        still has no dataset-specific CSV default because its table key
        is source-dependent; OPERATIONAL_DETAIL remains the only dataset
        with a CSV default.
        For source=INCIDENT_READINESS the response holds exactly one
        table, securityOperationalIncidentReadiness, whose 11 fields are
        a verbatim re-presentation of the authoritative BE-12M incident-
        readiness dataset row with no recomputation and no renamed
        semantics: bindingId, buildingId, securityPostId,
        securityPostCode, category, status, teamId, teamName,
        primaryWorkforceId, primaryWorkforceName and updatedAt, in that
        authoritative order. Every one is a renderer-safe scalar, so
        nothing is flattened, joined or serialized. No second table, no
        summary table and no readiness KPI table is returned.
        `buildingId` is REQUIRED for source=INCIDENT_READINESS exactly
        as it is for the other three sources, and its absence is a
        validation error. The export never rolls up across every
        accessible Building and never widens the scope: the authorized
        Building scope is exactly the requested buildingId, which the
        owning read has already existence- and access-checked. These
        rows do carry a buildingId column, but the scope is still
        reported from the authorization rather than derived from the
        returned rows, so Reporting never re-derives or widens an access
        decision.
        The grain is exactly one row per governed incident-readiness
        dataset row, and the row identity is bindingId, the owning
        read's own identifier for that readiness record, copied
        verbatim. No synthetic identity is minted: there is no
        incidentReadinessKey, compositeIncidentId, currentIncident,
        latestIncident or primaryIncident, because the authoritative
        public row has no such field. securityPostId, teamId and
        primaryWorkforceId are related facts rather than the row
        identity; each is nullable and each may legitimately repeat
        across several readiness records, so repeated values are
        preserved. Rows are never collapsed, grouped, deduplicated or
        ranked, and no latest, current, most recent, active, effective
        or primary record is selected; there is no LIMIT 1, no maximum-
        timestamp pick, no DISTINCT ON, no ROW_NUMBER and no sort-and-
        first election anywhere in this path.
        Supported filters are exactly those the owning incident-
        readiness read applies: buildingId, securityPostId, teamId,
        workforceId, status and category. teamId matches the record's
        RESPONSIBLE team and workforceId matches its RESPONSIBLE
        workforce. status is validated against the security module's own
        published incident-readiness status vocabulary, whose values are
        NOT_READY, PARTIAL and READY, and an unknown status is a
        validation error rather than an empty result set. No openOnly,
        unresolvedOnly, criticalOnly, readyOnly, notReadyOnly, overdue,
        ageDays, SLA, latestOnly or currentOnly filter exists, and no
        generic page, limit, offset, sort or search parameter is
        accepted.
        This source differs from the other three in one material way:
        its owning read applies NO period bound at all. The service
        resolves only a Building scope and issues no date predicate, so
        dateFrom and dateTo are accepted and normalized by the shared
        parser but do NOT restrict the returned rows, and the echoed
        period describes what the caller requested rather than a bound
        that was enforced. This read also does not apply patrolRouteId.
        Reporting invents no period: updatedAt is the record's persisted
        last-update instant and is never used as a bound, and no
        createdAt, reportedAt, resolvedAt or shift instant is
        substituted. The owning read's own ordering, by that persisted
        last-update instant descending and then by the record's created
        instant descending, is preserved exactly as returned, with no
        post-query sort.
        status is the record's own persisted readiness status, copied
        verbatim, and category is likewise a persisted source value
        copied as-is. Readiness is never inferred, recomputed or
        escalated here: no ready, not-ready, partial, compliant,
        overdue, breached, stale or aging value is derived from
        updatedAt, from category, or from the responsible team or
        workforce, and Reporting declares no readiness or category
        vocabulary of its own.
        The source publishes RESPONSIBILITY, not execution. teamId and
        teamName come from the record's responsible team and that team's
        name, and primaryWorkforceId and primaryWorkforceName come from
        its responsible workforce and that workforce profile's full
        name. All four are copied verbatim under the authoritative field
        names and are never relabeled as an executor, performedBy,
        completedBy, assignee, responder or actor, because being
        responsible for a readiness record is not evidence of having
        performed anything. No actor is inferred for records that have
        none, no identity is inferred from a related record, and
        updatedAt is a persisted instant rather than an actor.
        No SLA, overdue or age value is materialized on these rows:
        there is no SLA, overdue, age, timeOpen, breach, responseTime,
        resolutionTime or readiness-percentage field, no row is
        classified against a clock, and no current-time arithmetic is
        performed. The envelope asOf is export metadata only and is
        never compared against updatedAt. No KPI is calculated for this
        source: there is no readiness percentage, incident count, open
        count, not-ready count, response KPI, overdue KPI or SLA KPI,
        and kpis is empty, so the existing Security reporting and KPI
        authority remains separate and unchanged. The permission is the
        same existing security_report.read that gates every source of
        this dataset and every security report dataset endpoint,
        including the incident-readiness endpoint.
        SECURITY_OPERATIONAL_DETAIL still has no dataset-specific CSV
        default because its table key is source-dependent;
        OPERATIONAL_DETAIL remains the only dataset with a CSV default.

        INCIDENT_REGISTER (R10) is a pure Reporting adapter over the
        existing BE-21A shared Incident foundation list read, the same
        governed read behind GET /incidents. BE-21A is the ONE Incident
        foundation that already serves Operational Incident (BE-21B),
        Asset Failure / Defect (BE-21C) and Finding Escalation (BE-21D),
        distinguished only by the source's own incidentType
        discriminator, so this dataset adds no incident lifecycle, no
        incident repository, no incident status authority, no KPI
        authority, no universal incident model and no duplicate Incident
        model. It is not the BE-21B composite operational-incident view,
        which layers a lifecycle transition gate and permission-derived
        availableActions over the same foundation records, nor the
        incident-closure read, nor the security finding and incident KPI
        authority; all three remain separate and unchanged.
        The response holds exactly one table, incidentRegister, with no
        secondary child table. Its 26 fields are a verbatim re-
        presentation of the authoritative public incident row with no
        recomputation and no renamed semantics: id, clientId,
        buildingId, incidentNumber, incidentType, title, description,
        severity, priority, status, locationType, floorId, areaId,
        roomId, spaceId, functionalLocationId, reportedByUserId,
        reportedAt, cancelledAt, cancelledByUserId, closedAt,
        closedByUserId, closureNotes, createdAt, updatedAt and
        locationId, in that authoritative order. Every one is a
        renderer-safe scalar, so nothing is flattened, joined or
        serialized. No KPI is calculated for this dataset: there is no
        incident count, open count, critical count, closure rate,
        average age or SLA KPI, and kpis is empty.
        The grain is exactly one row per authoritative public incident
        row, that is, per one foundation record. The owning list read
        returns its rows through a single shared column-alias list with
        no join fan-out and no child expansion, and the cancellation and
        closure facts are columns on the record rather than child rows,
        so nothing is expanded, collapsed or merged. The row identity is
        id, the foundation record's own primary key, copied verbatim.
        incidentNumber is the Client-unique business number and is
        preserved as an ordinary fact: it is never the row identity and
        never part of a composite key. No incidentKey,
        incidentCompositeId, latestIncidentId or currentIncidentId is
        minted, and no latest, current, most recent, active or primary
        incident is selected; there is no LIMIT 1, no ROW_NUMBER, no
        maximum-timestamp pick and no sort-and-first election anywhere
        in this path, and rows are never collapsed, grouped,
        deduplicated or ranked. All three incident kinds appear in this
        one register exactly as the foundation holds them: the register
        is not split by incidentType and no per-type table is added.
        Supported filters are exactly the five the owning read owns:
        buildingId, incidentType, severity, priority and status. They
        are validated by the owning domain's own parser against the
        foundation's published vocabularies, whose values are
        OPERATIONAL, ASSET_FAILURE and FINDING_ESCALATION for
        incidentType, LOW, MEDIUM, HIGH and CRITICAL for severity and
        for priority, and REPORTED, CANCELLED and CLOSED for status;
        buildingId must be a valid UUID, and an unknown filter value is
        a validation error rather than an empty result set. No
        criticalOnly, openOnly, unresolvedOnly, overdue, ageDays, SLA,
        latestOnly or currentOnly filter exists, and no generic page,
        limit, offset, sort or search parameter is accepted.
        Building and Client scope stays entirely with the owning
        service. buildingId is an OPTIONAL narrowing filter, exactly as
        the source contract supports it: when supplied, the owning
        service asserts Building access for the actor before reading,
        and the read always scopes its query in SQL to the actor's
        accessible Buildings, returning zero rows without querying when
        that scope is empty. Reporting therefore neither narrows the
        contract by requiring a Building nor widens it: a multi-Building
        rollup is the source's own supported behaviour, in which case
        the envelope buildingId is null. Because the owning read
        publishes rows and not a scope list, the envelope buildingScope
        is the distinct set of Buildings actually represented in those
        authoritative rows, every one of which was already inside the
        actor's accessible scope, so client and Building isolation is
        never bypassed.
        This register has no period authority. The owning filter
        contract has no date window and the generated SQL carries no
        date predicate, so the rows are not period-bounded and the
        envelope dateFrom and dateTo are null. The five published
        instants, reportedAt, cancelledAt, closedAt, createdAt and
        updatedAt, are persisted facts and are never used as bounds, and
        none is substituted for another. The owning read's own ordering,
        by the reported instant descending and then by the record id
        descending, is preserved exactly as returned, with no post-query
        sort.
        status is the foundation record's own persisted status, copied
        verbatim from the source vocabulary. Closure is never inferred:
        closedAt, closedByUserId and closureNotes are separate persisted
        facts, so a record carrying a closedAt while its status says
        otherwise is copied exactly as the source published it. No
        closure, readiness, overdue, SLA, age or staleness value is
        derived from any timestamp, and status is never recomputed from
        severity, priority or incidentType. No SLA, overdue or age value
        is materialized on these rows: there is no SLA, overdue, age,
        timeOpen, breach, responseTime, resolutionTime or days-to-close
        field, no row is classified against a clock, and no current-time
        arithmetic is performed. The envelope asOf is export metadata
        only, captured immediately before the read, and is never
        compared against any incident instant.
        The source publishes persisted actor references and nothing
        else: reportedByUserId is who reported the incident,
        cancelledByUserId is who cancelled it and closedByUserId is who
        closed it. They keep those exact meanings and are never
        relabeled as an executor, completer, assignee, owner, reviewer
        or responder, because reporting, cancelling or closing is not
        evidence of any other role, and no actor is inferred for a
        record that has none. Reporting resolves no user, team or
        workforce name for them, because the owning read publishes no
        name and enriching from an unrelated module would invent a fact
        the source does not own.
        locationId is the owning module's own published derivation of
        the single BE-04 reference implied by locationType, and it is
        copied verbatim rather than re-derived by Reporting; all five
        location references and locationType stay published side by
        side, so no location is collapsed and no hierarchy is inferred.
        clientId is likewise the source's own derived tenancy fact,
        resolved through Building, Property and Client, and is never
        accepted from the caller nor re-derived here. INCIDENT_REGISTER
        has no dataset-specific CSV default, so OPERATIONAL_DETAIL
        remains the only dataset with one.
        RECEIVING_REGISTER (R11) is a pure Reporting adapter over the
        internal receiving-register read contract, itself a bounded read
        model over the authoritative BE-17G receivings table. The dataset
        adds no receiving lifecycle, no receiving repository, no receiving
        status authority, no KPI authority and no second receiving read
        model, and no receivings table is queried from Reporting.
        The response holds exactly one table, receivingRegister, with no
        secondary child, summary, metadata or drill table. Its 18 fields
        are a verbatim re-presentation of the authoritative receiving row
        with no recomputation and no renamed semantics: receivingId,
        clientId, buildingId, requestType, purchaseRequestId,
        serviceRequestId, materialRequestId, vendorId, receivingType,
        itemId, warehouseId, quantity, uomId, stockMovementId,
        receivedByUserId, receivedAt, status and notes, in that
        authoritative order. Every one is a renderer-safe scalar, so
        nothing is flattened, joined or serialized. No KPI is calculated
        for this dataset: there is no received total, quantity total,
        completion percentage, material value, receiving value, purchase
        order matching percentage or cycle time, and kpis is empty.
        The grain is exactly one row per persisted receivings record. The
        owning read selects a single table with zero joins, so nothing is
        expanded, collapsed, deduplicated or ranked, and the row identity
        is receivingId, the record's own primary key, copied verbatim. No
        latest, current or primary receiving is elected and no LIMIT 1,
        ROW_NUMBER or sort-and-first pick exists in this path.
        The register is request-anchored: the schema persists no
        receiving to purchase order or purchase order line reference, so
        none is exposed or inferred, neither from vendor, readiness,
        item, quantity, material request, service request, timestamp nor
        from the current live purchase order. The persisted request
        lineage (purchaseRequestId, serviceRequestId and the optional
        MATERIAL-line materialRequestId) is the only procurement context
        published.
        The dataset carries no monetary authority: no price, amount,
        cost, value, currency or valuation field exists on the receiving
        row, and quantity is a row-level fact that is never summed across
        items or UOMs, never converted and never valued. uomId keeps the
        source's receipt-time snapshot semantics and stockMovementId is
        the receiving-associated inventory STOCK_IN movement reference,
        copied verbatim without expanding the movement grain.
        Supported filters are exactly the nine the owning parser owns:
        buildingId, receivingType, status, vendorId, purchaseRequestId,
        serviceRequestId, materialRequestId, dateFrom and dateTo. Status
        values are RECEIVED and FINALIZED and receiving type values are
        MATERIAL and SERVICE, validated by the receivings authority's own
        validators; UUID filters must be valid UUIDs, dates must be valid
        ISO-8601 values, dateFrom must not exceed dateTo and the window
        is bounded to 366 days. An unknown filter value is a validation
        error rather than an empty result set. No purchaseOrderId,
        purchaseOrderLineId, workOrderId, invoiceId, latestOnly,
        currentOnly, pagination or free-text search parameter is accepted.
        Building and Client scope stays entirely with the owning service:
        an explicit buildingId is existence-checked and access-asserted
        for the actor, an omitted one rolls the register up across
        exactly the Buildings the caller can reach, and an empty
        authorized scope returns a well-formed empty register rather than
        an error, so client and Building isolation is never bypassed and
        never re-derived in Reporting.
        The period authority is received_at, applied by the owning read
        as a half-open window (dateFrom inclusive, dateTo exclusive, with
        a date-only dateTo covering that whole UTC day); created_at and
        updated_at are never period substitutes and are not published.
        receivedByUserId is the persisted receiving actor (Received By)
        and is never relabeled as an executor, performer, approver,
        verifier or requester. RECEIVING_REGISTER has no dataset-specific
        CSV default, so OPERATIONAL_DETAIL remains the only dataset with
        one.
        PURCHASE_ORDER_REGISTER (R11) is ONE dataset with TWO source-owned
        grains behind a REQUIRED view discriminator, HEADER or LINE,
        following the SECURITY_OPERATIONAL_DETAIL and PERMIT_TO_WORK
        precedent. The view parameter is mandatory and has no default: a
        missing, empty, repeated or unknown view is a validation error,
        there is no implicit HEADER, and the view is never inferred from
        which filters happen to be present. Each response holds exactly ONE
        selected table, purchaseOrderHeader for view=HEADER or
        purchaseOrderLine for view=LINE, never both, never an empty second
        table and never a flattened header-times-line fan-out. Both views
        are pure adapters: HEADER over the existing purchase-orders public
        list read and LINE over the internal purchase-order-line-register
        set-based read (one bounded query across the whole authorized
        scope, built precisely because the per-PO public line read would
        force a prohibited N+1 loop). No purchase-order SQL, no repository
        access, no second read model and no header duplication exists in
        Reporting, and the register is not a procurement mega-read: RFQ
        provenance, price deviation, purchase-order readiness, SPK or
        work-contract linkage, receiving, vendor invoices and operational
        commitments stay separate drill authorities and are never fetched
        or joined here.
        view=HEADER emits one row per purchase_orders record with 22
        verbatim fields: purchaseOrderId (the source record's own id under
        its explicit register name, the only rename), clientId, buildingId,
        vendorId, purchaseRequestId, serviceRequestId (exactly one non-null
        per the schema check), poReadinessId (the row's own persisted
        precondition reference as a bare id), requestType, poNumber,
        currency, status, poDate, requiredDate, vendorReference, notes,
        createdByUserId, issuedByUserId, issuedAt, cancelledByUserId,
        cancelledAt, createdAt and updatedAt. The header carries NO total
        amount: the schema persists none, and no totalAmount, ordered,
        received or remaining quantity, line count, RFQ status, deviation,
        SPK state or invoice state is exposed. currency is context only.
        The native status vocabulary is DRAFT, ISSUED and CANCELLED and the
        native request types are PURCHASE_REQUEST and SERVICE_REQUEST, both
        copied verbatim and never recomputed from the issuance or
        cancellation provenance.
        view=LINE emits one row per purchase_order_lines record with 25
        verbatim fields: purchaseOrderLineId (the line's own id, explicitly
        named), the parent context purchaseOrderId, clientId, buildingId,
        vendorId, purchaseRequestId, poNumber, poDate, currency and
        purchaseOrderStatus, and the line facts lineNumber,
        requestLineType, materialRequestId, serviceRequestId, itemId,
        uomId, sourceServiceId, description, quantitySnapshot, unitPrice,
        lineAmount, notes, createdByUserId, createdAt and updatedAt. The
        parent context comes from a one-to-one structural join inside the
        owning read and is never expanded or collapsed. requestLineType is
        the authoritative persisted discriminator, MATERIAL_REQUEST or
        SERVICE_REQUEST, never inferred from the nullable lineage fields.
        purchaseOrderStatus is the parent PO's native status under an
        explicit semantic name; a line owns no independent lifecycle, so no
        lineStatus exists. The line's own serviceRequestId and
        materialRequestId lineage is preserved from the line authority and
        never overwritten with parent-header semantics. quantitySnapshot is
        the frozen commit-time copy of the request line's quantity:
        historical data only, never relabeled as ordered, approved,
        current, received or remaining quantity, and NULL is preserved for
        SERVICE lines.
        Money discipline: unitPrice and lineAmount are persisted historical
        transactional commitments denominated in the parent PO's currency,
        the one authoritative currency per PO. Reporting never sums line
        amounts into a PO total, never recomputes lineAmount from quantity
        times unit price, never applies FX or tax, never looks up or
        compares against the current price catalog and never calculates a
        deviation. kpis is empty for both views: no committed value, PO
        count, line count, quantity total, issuance rate or average price
        is calculated.
        Supported query parameters: view (required) plus buildingId,
        vendorId, purchaseRequestId, serviceRequestId, status, dateFrom and
        dateTo for both views, and additionally materialRequestId and itemId
        for view=LINE only. Supplying a LINE-only filter with view=HEADER,
        or the header-native poDateFrom or poDateTo names with view=LINE,
        is a validation error rather than a silently ignored key. UUID
        filters must be valid UUIDs and status must be a native PO status,
        validated by the owning purchase-orders authority's own parser.
        Both views share ONE date-filter meaning over the PO's own po_date
        business period, inclusive calendar dates on both ends: dateFrom
        and dateTo are YYYY-MM-DD values applied as po_date greater than or
        equal to dateFrom and less than or equal to dateTo. For view=HEADER
        the adapter maps dateFrom and dateTo onto the owning parser's
        native poDateFrom and poDateTo names as a pure transport mapping
        with no semantic reinterpretation and no second date parser; the
        LINE view additionally bounds the window to 366 days, the
        reporting-safety bound its owning read enforces. created_at,
        updated_at, issued_at and cancelled_at are never period
        substitutes.
        Scope stays entirely with the owning authorities: an explicit
        buildingId is existence-checked and access-asserted for the actor,
        an omitted one rolls the view up across exactly the Buildings the
        caller can reach, and an empty authorized scope yields a
        well-formed empty register rather than an error or a database
        round trip. The authenticated userId is passed to the owning read
        in both views and Reporting never expands, re-resolves or widens
        scope. The dataset is gated by the existing purchase_order.read
        permission; price_catalog.read is deliberately NOT required because
        no price-authority field is part of this dataset.
        PURCHASE_ORDER_REGISTER has no dataset-specific CSV default: with
        two possible tables the generic export requires the actually
        selected response table, so OPERATIONAL_DETAIL remains the only
        dataset with one.
        VENDOR_INVOICE_REGISTER (R11) is a pure Reporting adapter over the
        existing vendor-invoices public list read: the row-level commercial
        anchor for vendor invoicing, with exactly one table,
        vendorInvoiceRegister, and no secondary child, summary, metadata or
        drill table. The grain is one row per persisted vendor_invoices
        record; the owning read is a single scoped query with zero joins,
        so nothing is expanded, collapsed, deduplicated or merged, and the
        row identity is vendorInvoiceId, the source record's own id under
        its explicit register name (the only rename). No latest, current or
        primary invoice is elected and no LIMIT 1, ROW_NUMBER or
        sort-and-first pick exists in this path. The dataset is NOT a
        payment-event ledger, NOT an accounting or tax surface, NOT a
        budget-variance calculation and NOT a vendor operational lifecycle
        report: VENDOR_SERVICE_REGISTER remains authoritative for vendor
        execution and lifecycle.
        Its 35 fields are a verbatim re-presentation of the authoritative
        public invoice row: vendorInvoiceId, clientId, buildingId,
        vendorId, invoiceNumber, invoiceDate, receivedDate, currency,
        invoiceAmount, status, vendorReference, vendorWorkId, workOrderId,
        completionReportId, serviceReportId, bastDocumentId,
        purchaseOrderId, workContractId, notes, createdByUserId,
        verificationStatus, verifiedByUserId, verifiedAt,
        verificationNotes, discrepancyCodes, paymentStatus, paidAmount,
        outstandingAmount, lastPaymentDate, finalizedAt,
        finalizedByUserId, cancelledAt, cancelledByUserId, createdAt and
        updatedAt, in that authoritative order.
        The three native status dimensions stay separate and verbatim and
        are never collapsed into one generic status, normalized or
        extended: status is the invoice lifecycle (DRAFT, FINALIZED,
        CANCELLED), verificationStatus is the verification dimension
        (PENDING, VERIFIED, DISCREPANCY) and paymentStatus is the payment
        dimension (UNPAID, PARTIALLY_PAID, PAID). No OPEN, CLOSED,
        SETTLED, APPROVED, PAYABLE or PAYMENT_COMPLETE vocabulary is
        invented.
        Payment facts are CURRENT STATE ONLY: paidAmount,
        outstandingAmount, lastPaymentDate and paymentStatus are the
        persisted cumulative facts on the invoice row. The dataset
        materializes no payment event, receipt, transaction, instrument or
        bank-transfer row, never expands vendor_invoice_history, and never
        infers individual payments from changes in paidAmount; the absence
        of discrete vendor payment documents is a known non-blocking
        limitation of this track.
        Money discipline: invoiceAmount, paidAmount and outstandingAmount
        are source-generated numeric facts and currency is the invoice's
        own persisted ISO-4217 currency, all copied verbatim with zero
        arithmetic. Reporting never sums, never subtracts, never
        recomputes the outstanding amount (the persisted source value is
        echoed as-is), never totals across invoices or currencies, and
        applies no FX, tax, VAT, price-catalog lookup or accounting
        classification. kpis is empty: no invoice count, invoice amount,
        paid total, outstanding total, discrepancy rate, verification
        rate, payment rate, aging or overdue value is calculated, and even
        conceptually-safe per-currency aggregates are intentionally
        deferred.
        discrepancyCodes is the source-owned deterministic code array on
        the invoice grain, serialized comma-joined into one cell per the
        house convention for array facts: the persisted codes and their
        order survive verbatim, are never fanned out to multiple rows,
        never recalculated, and never interpreted into a new status. No
        invoice matching logic runs from Reporting.
        Lineage discipline: vendorWorkId, workOrderId,
        completionReportId, serviceReportId, bastDocumentId,
        purchaseOrderId and workContractId are the invoice row's own
        persisted references, projected as bare drill handles. No vendor
        work, work order, completion report, service report, BAST,
        purchase order or SPK record is fetched, joined or enriched, and
        no RFQ, receiving, budget, commitment or price-catalog surface is
        consulted: the register is not a procurement mega-read.
        Actor semantics are preserved exactly: createdByUserId created the
        invoice, finalizedByUserId finalized it, cancelledByUserId
        cancelled it and verifiedByUserId is the verification actor; none
        is relabeled as a payer, payment actor, approver, executor or
        vendor PIC.
        Supported filters are exactly the eight the owning parser owns,
        under stable Reporting transport names: buildingId, vendorId,
        workOrderId, purchaseOrderId, workContractId, status, dateFrom and
        dateTo. UUID filters must be valid UUIDs, status must be a native
        lifecycle status, dates must be strict YYYY-MM-DD calendar values
        and dateTo must not precede dateFrom, all validated by the owning
        authority's own parser; supplying the source-native
        invoiceDateFrom or invoiceDateTo names is a validation error
        rather than a silently ignored key. verificationStatus,
        paymentStatus, invoiceNumber, discrepancyCode, amount bounds,
        search, latestOnly, currentOnly and pagination are NOT part of the
        source filter contract and are not exposed.
        The period authority is invoice_date, applied by the owning read
        as an INCLUSIVE calendar-date window (dateFrom and dateTo both
        included); the adapter maps dateFrom and dateTo onto the parser's
        native invoiceDateFrom and invoiceDateTo names as a pure transport
        mapping with no second date parser. created_at, received_date,
        finalized_at, verified_at and last_payment_date are never period
        substitutes.
        Scope stays entirely with the owning service: an explicit
        buildingId is access-asserted for the actor, an omitted one rolls
        the register up across exactly the Buildings the caller can reach,
        and an empty authorized scope returns a well-formed empty register
        without a database round trip, so client and Building isolation is
        never bypassed and never re-derived in Reporting. Rows are ordered
        by the owning read's own deterministic invoice_date DESC,
        created_at DESC ordering. The dataset is gated by the existing
        vendor_invoice.read permission. VENDOR_INVOICE_REGISTER has no
        dataset-specific CSV default: with exactly one table, the generic
        sole-table selection is sufficient, so OPERATIONAL_DETAIL remains
        the only dataset with one.
        STOCK_MOVEMENT_REGISTER (R11) is a pure Reporting adapter over the
        governed internal stock-movement-register read built on the
        authoritative append-only inventory_stock_movements ledger: exactly
        one table, stockMovementRegister, and no secondary balance, item,
        warehouse, summary, metadata or drill table. The grain is one row
        per persisted movement record; the owning read is a single scoped
        query with zero joins, so nothing is expanded, collapsed,
        deduplicated or merged, and the row identity is stockMovementId,
        the movement record's own id under its explicit register name (the
        only rename). No latest, current or primary movement is elected and
        no LIMIT 1, ROW_NUMBER or sort-and-first pick exists in this path.
        movementType keeps the native STOCK_IN / STOCK_OUT direction
        vocabulary verbatim: it is not a lifecycle status, it is never
        normalized, and no generic status is exposed. quantity and the
        movement-time uomId snapshot are per-row facts, never summed across
        items or UOMs, never converted and never netted.
        resultingQuantityOnHand and resultingAvailableQuantity are
        HISTORICAL post-movement snapshots copied verbatim: they are never
        presented as current balances — the stock-balance domain remains
        the sole current-balance authority — and no reserved quantity or
        other balance arithmetic is derived. The dataset carries ZERO
        monetary authority: no cost, price, amount, currency, inventory
        value, COGS or valuation field exists, and none is inferred from
        purchase orders, vendor invoices, price catalog, receivings or
        work-order material usage. The generic persisted reference and
        source texts are bare on-row facts, never resolved into a
        receiving, work order, purchase order, material request,
        reservation, transfer or adjustment link, and no reverse-domain
        lookup is performed. performedByUserId is the movement actor
        ("Performed By") only, with no actor, warehouse, item or UOM name
        resolution. Period authority is movement_date under the source's
        own strict YYYY-MM-DD validation, normalized to a half-open UTC
        window that fully includes the dateTo calendar day; created_at is
        never a period substitute.
        Scope stays entirely with the owning service: an explicit
        buildingId is existence-checked and access-asserted for the actor,
        an omitted one rolls the register up across exactly the Buildings
        the caller can reach, and an empty authorized scope returns a
        well-formed empty register without a database round trip, so
        client and Building isolation is never bypassed and never
        re-derived in Reporting. Rows are ordered by the owning read's own
        deterministic movement_date DESC, created_at DESC, id ordering.
        The dataset is gated by the existing inventory_stock.read
        permission. STOCK_MOVEMENT_REGISTER has no dataset-specific CSV
        default: with exactly one table, the generic sole-table selection
        is sufficient, so OPERATIONAL_DETAIL remains the only dataset with
        one.
        OPERATIONAL_BUDGET_VARIANCE (R11) is a pure Reporting adapter over
        the existing operational-finance variance read authority: one
        dataset with two source-owned grains behind a REQUIRED view
        discriminator, BUDGET or CATEGORY, and exactly one table per
        response. There is no default view and no inference from the
        filters present; a missing, empty or unknown view is a validation
        error. BUDGET presents operationalBudgetVariance, one row per
        budget-level variance summary from the governed list read:
        identity budgetId, the budget's own control period
        (periodStart/periodEnd, never transaction timestamps), its single
        authoritative currency, its native status, the source-computed
        totals (plannedAmount, ledgerCommittedAmount, ledgerOpenAmount,
        ledgerActualizedAmount, ledgerReleasedAmount,
        legacyCommittedAmount, uncommittedMaterialActualAmount,
        uncommittedInvoiceActualAmount, unallocatedActualAmount,
        openCommitmentAmount, actualAmount, consumedAmount,
        availableAmount, varianceAmount, utilizationPercent and
        committedUtilizationPercent) plus the source-owned failClosed
        control and gapCount, all copied verbatim. CATEGORY presents
        operationalBudgetVarianceCategory, one row per persisted budget
        category from the single-budget variance detail read addressed by
        a REQUIRED budgetId: identity budgetId plus budgetCategoryId, the
        parent budget context (client, building, currency, control period,
        native status and overspendPolicy) and the source-computed
        per-category figures including commitmentCount. The two grains are
        never merged, the budget row is never duplicated per category,
        category arrays are never embedded into budget rows and budget
        totals never appear on category rows. The BUDGET view accepts only
        the source-owned filters buildingId, status and the strict
        YYYY-MM-DD periodFrom/periodTo budget-period overlap window; each
        view fails closed on the other view's exclusive filters. Reporting
        performs ZERO financial arithmetic: no planned-minus-actual,
        available, variance, utilization, open-commitment, actualization
        or release figure is computed, rounded or re-derived; every amount
        and percentage is the owning authority's own read-time value and a
        null percentage stays null. Each row remains tied to exactly one
        budget currency: no cross-currency sum, no FX conversion, no
        reporting-currency selection and no consolidated amount KPI
        exists. Gap and exclusion codes stay source-owned diagnostics
        described at their own grain and are never valued, reinterpreted,
        suppressed or zero-filled. Scope stays entirely with the owning
        service: an explicit buildingId is access-asserted, an omitted one
        rolls up across exactly the Buildings the caller can reach, and
        the category detail read existence-checks the budget and asserts
        Building access before returning anything. The dataset is gated by
        the existing operational_budget.read permission; the budget
        traceability read remains a separate drill authority and is not
        part of this dataset. OPERATIONAL_BUDGET_VARIANCE has no
        dataset-specific CSV default: with two possible tables the generic
        export requires the actually selected response table, so
        OPERATIONAL_DETAIL remains the only dataset with one.
        ESG_METRIC_TREND (R12) is a pure Reporting adapter over the governed
        ESG metric-value source read (CR-BE-ESG-01 readEsgMetricValues) and is
        gated by the existing esg.read permission. Its grain stays exactly one
        row per persisted ESG metric-value fact: Reporting introduces no trend
        calculation, carbon conversion, target variance, interpolation,
        synthetic period or Utility-to-ESG inference, and period containment,
        actor Building intersection, metric filtering and every native ESG
        value remain the owning read's own. Filters are exactly those the
        owning read accepts; no Reporting-side filter is added.
        ESG_WASTE_REGISTER (R12) is a pure Reporting adapter over the bounded
        actor-scoped ESG waste read (CR-BE-ESG-01 readEsgWasteRecords) and is
        gated by the existing esg.read permission. Its grain stays exactly one
        row per persisted waste fact: quantity, UOM, period date and native
        status remain source facts, with no carbon, conversion, aggregation or
        Utility-to-ESG inference. Filters are exactly those the owning read
        accepts; no Reporting-side filter is added.
        UTILITY_CONSUMPTION_TREND (R12) projects bounded utility consumption
        buckets from persisted utility_meter_consumptions facts (BE-18G
        readUtilityConsumptionTrend) and is gated by the existing
        utility_kpi.read permission. Period bucketing, Building access,
        utility type and UOM grouping are the source's own; Reporting performs
        no reading delta, conversion, cost, carbon, hierarchy or missing-period
        logic. Filters are exactly those the owning read accepts; no
        Reporting-side filter is added.
        PORTFOLIO_OPERATIONAL_COMPARISON (R12) reports absolute source-row
        counts only, read from the existing governed Work Order, Finding,
        Incident, Checklist/Form Execution and Scheduled Operation reads, and
        is gated by the existing work_order.read permission. No normalization,
        lifecycle reconstruction, financial or sustainability comparison,
        score, rank or denominator is introduced, and no count is derived from
        another count. Filters are exactly those the owning reads accept; no
        Reporting-side filter is added.

    ReportArchiveFormat:
      type: string
      enum: [JSON, CSV, XLSX, PDF]
      description: Requested artifact format. Format identity is persisted with the archive.

    ReportArchiveStatus:
      type: string
      enum: [REQUESTED, GENERATING, COMPLETED, FAILED]
      description: Generation lifecycle status; retention is a separate state.

    ReportArchiveRetentionState:
      type: string
      enum: [ACTIVE, RETENTION_DUE, PURGED]
      description: Retention state. Report-archive cleanup remains deferred.

    ReportArchiveFilterObject:
      type: object
      maxProperties: 100
      description: >
        Bounded normalized dataset filters. Runtime validation permits safe JSON
        primitives/objects/arrays within bounded depth and size, rejects
        storage/secret/SQL/HTML fields, and does not accept caller-owned scope
        identity inside this object. Dataset adapters remain authoritative for
        dataset-specific filter semantics.
      additionalProperties: true

    ReportArchiveRequestedScope:
      type: object
      required: [clientId, buildingId, buildingIds]
      properties:
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        buildingIds:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/Uuid"

    ReportArchiveResolvedScope:
      type: object
      required: [clientIds, buildingIds, mode]
      properties:
        clientIds:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/Uuid"
        buildingIds:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/Uuid"
        mode:
          type: string
          enum: [ALL_ACCESSIBLE, CLIENT, SINGLE_BUILDING, MULTI_BUILDING]

    ReportArchivePeriod:
      type: object
      required: [dateFrom, dateTo, timeBasis, dateToMode]
      properties:
        dateFrom:
          type: string
          nullable: true
        dateTo:
          type: string
          nullable: true
        timeBasis:
          type: string
          const: UTC
        dateToMode:
          type: string
          enum: [NONE, INCLUSIVE_DAY, EXCLUSIVE_INSTANT]

    ReportArchiveFilterSnapshot:
      type: object
      required: [dataset, format, requestedScope, resolvedScope, period, filters]
      properties:
        dataset:
          $ref: "#/components/schemas/ReportArchiveDataset"
        format:
          $ref: "#/components/schemas/ReportArchiveFormat"
        requestedScope:
          $ref: "#/components/schemas/ReportArchiveRequestedScope"
        resolvedScope:
          $ref: "#/components/schemas/ReportArchiveResolvedScope"
        period:
          $ref: "#/components/schemas/ReportArchivePeriod"
        filters:
          $ref: "#/components/schemas/ReportArchiveFilterObject"

    CreateReportArchiveRequest:
      type: object
      additionalProperties: false
      required: [clientId, dataset, format]
      properties:
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
          description: Single requested Building; mutually exclusive with buildingIds.
        buildingIds:
          type: array
          uniqueItems: true
          minItems: 1
          items:
            $ref: "#/components/schemas/Uuid"
          description: Explicit requested Building set; mutually exclusive with buildingId.
        dataset:
          $ref: "#/components/schemas/ReportArchiveDataset"
        format:
          $ref: "#/components/schemas/ReportArchiveFormat"
        filters:
          $ref: "#/components/schemas/ReportArchiveFilterObject"

    ReportArchiveArtifact:
      type: object
      required: [available, filename, contentType, fileSize, checksum, checksumAlgorithm]
      properties:
        available:
          type: boolean
          description: True only when a completed, non-purged stored artifact reference exists.
        filename:
          type: string
          nullable: true
          pattern: '^[A-Za-z0-9][A-Za-z0-9._-]{0,200}$'
        contentType:
          type: string
          nullable: true
        fileSize:
          type: integer
          format: int64
          minimum: 0
          nullable: true
        checksum:
          type: string
          nullable: true
          pattern: '^[0-9a-f]{64}$'
        checksumAlgorithm:
          type: string
          nullable: true
          enum: [SHA-256]

    ReportArchiveFailure:
      type: object
      nullable: true
      required: [code, message, failedAt]
      properties:
        code:
          type: string
        message:
          type: string
        failedAt:
          type: string
          format: date-time

    ReportArchiveRetention:
      type: object
      required: [policyId, policyCode, daysSnapshot, appliedAt, retainedUntil, state, hold, holdReason, purgedAt]
      properties:
        policyId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        policyCode:
          type: string
          nullable: true
        daysSnapshot:
          type: integer
          minimum: 1
          nullable: true
        appliedAt:
          type: string
          format: date-time
          nullable: true
        retainedUntil:
          type: string
          format: date-time
          nullable: true
        state:
          $ref: "#/components/schemas/ReportArchiveRetentionState"
        hold:
          type: boolean
        holdReason:
          type: string
          nullable: true
        purgedAt:
          type: string
          format: date-time
          nullable: true

    ReportArchive:
      type: object
      description: >
        One immutable export request/execution and, after completion, its
        stored artifact metadata. Internal storageReference is intentionally
        omitted. Regeneration creates a new archive ID.
      required:
        [id, clientId, buildingId, buildingIds, dataset, format, status,
         filterSnapshot, sourceProvenance, asOf, requestedByUserId, requestedAt,
         generatedAt, artifact, failure, attemptCount, maxAttempts,
         nextAttemptAt, supersedesArchiveId, retention, createdAt, updatedAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        buildingIds:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/Uuid"
        dataset:
          $ref: "#/components/schemas/ReportArchiveDataset"
        format:
          $ref: "#/components/schemas/ReportArchiveFormat"
        status:
          $ref: "#/components/schemas/ReportArchiveStatus"
        filterSnapshot:
          $ref: "#/components/schemas/ReportArchiveFilterSnapshot"
        sourceProvenance:
          type: object
          additionalProperties: true
        asOf:
          type: string
          format: date-time
          nullable: true
        requestedByUserId:
          $ref: "#/components/schemas/Uuid"
        requestedAt:
          type: string
          format: date-time
        generatedAt:
          type: string
          format: date-time
          nullable: true
        artifact:
          $ref: "#/components/schemas/ReportArchiveArtifact"
        failure:
          $ref: "#/components/schemas/ReportArchiveFailure"
        attemptCount:
          type: integer
          minimum: 0
        maxAttempts:
          type: integer
          minimum: 1
        nextAttemptAt:
          type: string
          format: date-time
          nullable: true
        supersedesArchiveId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        retention:
          $ref: "#/components/schemas/ReportArchiveRetention"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    ReportArchivePaginationMeta:
      type: object
      required: [page, pageSize, total, totalPages]
      properties:
        page:
          type: integer
          minimum: 1
        pageSize:
          type: integer
          minimum: 1
          maximum: 200
        total:
          type: integer
          minimum: 0
        totalPages:
          type: integer
          minimum: 0

    ReportArchiveListEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              type: array
              items:
                $ref: "#/components/schemas/ReportArchive"
            meta:
              $ref: "#/components/schemas/ReportArchivePaginationMeta"

    # ------------------------------------------------------------------
    # BE-24 Management & Owner read-model foundation
    # ------------------------------------------------------------------
    ManagementReadScope:
      type: object
      description: >
        Authorized Client/Building scope. buildingIds is the complete set later
        read-model repositories may query; clients is the matching subset of
        the effective-user context hierarchy.
      required: [mode, clientId, clientIds, buildingIds, clients]
      properties:
        mode:
          type: string
          enum: [ALL_ACCESSIBLE, CLIENT, SINGLE_BUILDING, MULTI_BUILDING]
        clientId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        clientIds:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/Uuid"
        buildingIds:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/Uuid"
        clients:
          type: array
          items:
            $ref: "#/components/schemas/EffectiveClientContext"

    ManagementReadPeriod:
      type: object
      description: BE-23-compatible UTC reporting period semantics.
      required: [dateFrom, dateTo, timeBasis, dateToMode]
      properties:
        dateFrom:
          type: string
          nullable: true
          description: Original normalized ISO-8601 lower boundary.
        dateTo:
          type: string
          nullable: true
          description: Original normalized ISO-8601 upper boundary.
        timeBasis:
          type: string
          const: UTC
        dateToMode:
          type: string
          enum: [NONE, INCLUSIVE_DAY, EXCLUSIVE_INSTANT]

    ManagementReadScopeFilters:
      type: object
      description: Normalized shared scope selectors supplied by the caller.
      required: [clientId, buildingId, buildingIds]
      properties:
        clientId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        buildingIds:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/Uuid"

    ManagementReadScopeContext:
      type: object
      required: [scope, period, filters, asOf]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementReadScopeFilters"
        asOf:
          type: string
          format: date-time

    ManagementReadModelContract:
      type: object
      description: >
        Shared base shape for later BE-24 read models. Domain filters extend
        the shared scope filters and data is owned by the specific read model;
        scope/period/asOf always retain this contract.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          type: object
          additionalProperties: true
        asOf:
          type: string
          format: date-time
        data:
          type: object

    ManagementDailyOperationsFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required: [operationalDate, graceMinutes]
          properties:
            operationalDate:
              type: string
              format: date
            graceMinutes:
              type: integer
              minimum: 0
              maximum: 10080

    ManagementDailyOperationsData:
      type: object
      required:
        [scheduled, completed, inProgress, overdue, openFindings,
         criticalOperationalItems]
      properties:
        scheduled:
          type: integer
          minimum: 0
          description: BE-10I scheduled generated-task count for the day.
        completed:
          type: integer
          minimum: 0
          description: BE-10I completed generated-task count for the day.
        inProgress:
          type: integer
          minimum: 0
          description: BE-10I in-progress generated-task count for the day.
        overdue:
          type: integer
          minimum: 0
          description: BE-23G overdue active-assignment count for the day.
        openFindings:
          type: integer
          minimum: 0
          description: BE-10I open Finding count reported in the day.
        criticalOperationalItems:
          type: object
          required: [total, securityIncidents, tenantServiceRequests]
          properties:
            total:
              type: integer
              minimum: 0
            securityIncidents:
              type: integer
              minimum: 0
              description: BE-23F2 Security-relevant CRITICAL Incident count.
            tenantServiceRequests:
              type: integer
              minimum: 0
              description: BE-23H CRITICAL-priority Tenant Service Request count.

    ManagementDailyOperations:
      type: object
      description: BE-24 PART 02A shared-contract Daily Operations projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementDailyOperationsFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementDailyOperationsData"

    ManagementWorkOrderSummaryFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required: [overdueAfterDays]
          properties:
            overdueAfterDays:
              type: integer
              minimum: 0
              maximum: 365
              description: Reporting age threshold; not a BE-08 due date/SLA.

    ManagementWorkOrderPrioritySummary:
      type: object
      required: [low, medium, high, critical]
      properties:
        low:
          type: integer
          minimum: 0
        medium:
          type: integer
          minimum: 0
        high:
          type: integer
          minimum: 0
        critical:
          type: integer
          minimum: 0

    ManagementWorkOrderSummaryData:
      type: object
      required:
        [total, open, inProgress, completed, overdue, verified, closed, priority]
      properties:
        total:
          type: integer
          minimum: 0
        open:
          type: integer
          minimum: 0
          description: Exact BE-08 OPEN status count.
        inProgress:
          type: integer
          minimum: 0
          description: Exact BE-08 IN_PROGRESS status count.
        completed:
          type: integer
          minimum: 0
          description: Exact BE-08 COMPLETED status count.
        overdue:
          type: integer
          minimum: 0
          description: >
            Unfinished Work Orders older than overdueAfterDays by createdAt;
            a reporting measure, not lifecycle state.
        verified:
          type: integer
          minimum: 0
          description: Distinct Work Orders with an APPROVED BE-08I review.
        closed:
          type: integer
          minimum: 0
          description: Exact terminal BE-08 CLOSED status count.
        priority:
          $ref: "#/components/schemas/ManagementWorkOrderPrioritySummary"

    ManagementWorkOrderSummary:
      type: object
      description: BE-24 PART 02B shared-contract Work Order Summary projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementWorkOrderSummaryFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementWorkOrderSummaryData"

    ManagementPendingApprovalItem:
      type: object
      required:
        [approvalId, clientId, buildingId, source, approvalType, resourceType,
         resourceId, resourceReference, submittedAt, currentStatus,
         availableActions]
      properties:
        approvalId:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        source:
          type: string
          enum: [PERMIT, PROCUREMENT, TENANT, DOCUMENT]
        approvalType:
          type: string
        resourceType:
          type: string
        resourceId:
          $ref: "#/components/schemas/Uuid"
        resourceReference:
          type: string
          description: Human reference when exposed by the source, otherwise its stable id.
        submittedAt:
          type: string
          format: date-time
        currentStatus:
          type: string
          const: PENDING
        availableActions:
          type: array
          description: Backend-authoritative source actions; empty when unavailable or disallowed.
          items:
            type: string

    ManagementPendingApprovalData:
      type: object
      required: [pendingCount, items]
      properties:
        pendingCount:
          type: integer
          minimum: 0
        items:
          type: array
          items:
            $ref: "#/components/schemas/ManagementPendingApprovalItem"

    ManagementPendingApproval:
      type: object
      description: BE-24 PART 03A shared-contract Pending Approval projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementReadScopeFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementPendingApprovalData"

    ManagementCriticalFindingsFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required: [overdueAfterDays, criticalSeverityRule]
          properties:
            overdueAfterDays:
              type: integer
              minimum: 0
              maximum: 365
              description: Reporting age threshold; not a BE-09 due date/SLA.
            criticalSeverityRule:
              type: string
              const: HIGHEST_ACTIVE_RANK_PER_CLIENT

    ManagementCriticalFindingResponsibleParty:
      type: object
      required: [type, workforceProfileId, teamId, vendorId]
      properties:
        type:
          type: string
          enum: [WORKFORCE, TEAM, VENDOR, VENDOR_WORKFORCE]
        workforceProfileId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        teamId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        vendorId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true

    ManagementCriticalFindingItem:
      type: object
      required:
        [findingId, clientId, buildingId, findingReference, title, severity,
         source, context, status, responsibleParty, reportedAt, overdue]
      properties:
        findingId:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          $ref: "#/components/schemas/Uuid"
        findingReference:
          type: string
        title:
          type: string
        severity:
          type: object
          required: [id, code, name, rank]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
            code:
              type: string
            name:
              type: string
            rank:
              type: integer
              minimum: 1
        source:
          type: object
          required: [type, id]
          properties:
            type:
              type: string
              nullable: true
            id:
              allOf:
                - $ref: "#/components/schemas/Uuid"
              nullable: true
        context:
          type: object
          required: [type, id]
          properties:
            type:
              type: string
              enum: [ENGINEERING, HOUSEKEEPING, SECURITY, GENERAL]
            id:
              allOf:
                - $ref: "#/components/schemas/Uuid"
              nullable: true
        status:
          type: string
          enum:
            [OPEN, ASSIGNED, IN_PROGRESS, PENDING_REVIEW, REJECTED,
             REWORK_REQUIRED, RESUBMITTED]
        responsibleParty:
          allOf:
            - $ref: "#/components/schemas/ManagementCriticalFindingResponsibleParty"
          nullable: true
        reportedAt:
          type: string
          format: date-time
        overdue:
          type: boolean
          description: Elapsed reporting state, not an authoritative BE-09 status.

    ManagementCriticalFindingsData:
      type: object
      required: [criticalFindingCount, items]
      properties:
        criticalFindingCount:
          type: integer
          minimum: 0
        items:
          type: array
          items:
            $ref: "#/components/schemas/ManagementCriticalFindingItem"

    ManagementCriticalFindings:
      type: object
      description: BE-24 PART 03B shared-contract Critical Findings projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementCriticalFindingsFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementCriticalFindingsData"

    ManagementWorkforceSummaryFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required: [graceMinutes]
          properties:
            graceMinutes:
              type: integer
              minimum: 0
              maximum: 10080

    ManagementWorkforceDepartmentRow:
      type: object
      required:
        [departmentId, departmentCode, departmentName, totalWorkforce,
         activeWorkforce]
      properties:
        departmentId:
          $ref: "#/components/schemas/Uuid"
        departmentCode:
          type: string
        departmentName:
          type: string
        totalWorkforce:
          type: integer
          minimum: 0
        activeWorkforce:
          type: integer
          minimum: 0

    ManagementWorkforceTeamRow:
      allOf:
        - $ref: "#/components/schemas/ManagementWorkforceDepartmentRow"
        - type: object
          required: [teamId, teamCode, teamName]
          properties:
            teamId:
              $ref: "#/components/schemas/Uuid"
            teamCode:
              type: string
            teamName:
              type: string

    ManagementWorkforceManHours:
      type: object
      description: Verbatim BE-23G man-hour projection.
      required:
        [totalHours, measuredAssignments, unmeasuredAssignments,
         averageHoursPerAssignment, averageHoursPerWorkforce]
      properties:
        totalHours:
          type: number
        measuredAssignments:
          type: integer
          minimum: 0
        unmeasuredAssignments:
          type: integer
          minimum: 0
        averageHoursPerAssignment:
          type: number
        averageHoursPerWorkforce:
          type: number

    ManagementWorkforceSummaryData:
      type: object
      required:
        [totalWorkforce, activeWorkforce, assignedWorkforce, byDepartment,
         byTeam, assignments, manHours]
      properties:
        totalWorkforce:
          type: integer
          minimum: 0
          description: BE-23G workforce.total.
        activeWorkforce:
          type: integer
          minimum: 0
          description: BE-23G workforce.active.
        assignedWorkforce:
          type: integer
          minimum: 0
          description: BE-23G workforce.withAssignments.
        byDepartment:
          type: array
          items:
            $ref: "#/components/schemas/ManagementWorkforceDepartmentRow"
        byTeam:
          type: array
          items:
            $ref: "#/components/schemas/ManagementWorkforceTeamRow"
        assignments:
          type: object
          required: [scheduled, completed, overdue]
          properties:
            scheduled:
              type: integer
              minimum: 0
            completed:
              type: integer
              minimum: 0
            overdue:
              type: integer
              minimum: 0
        manHours:
          $ref: "#/components/schemas/ManagementWorkforceManHours"

    ManagementWorkforceSummary:
      type: object
      description: BE-24 PART 04A shared-contract Workforce Summary projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementWorkforceSummaryFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementWorkforceSummaryData"

    ManagementVendorSummaryFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required: [overdueAfterDays]
          properties:
            overdueAfterDays:
              type: integer
              minimum: 0
              maximum: 365

    ManagementVendorPerformanceRow:
      type: object
      description: Verbatim BE-23H per-vendor performance projection.
      required:
        [vendorId, vendorCode, vendorName, vendorStatus, total, completed,
         outstanding, overdue, completionRate, averageCompletionHours]
      properties:
        vendorId:
          $ref: "#/components/schemas/Uuid"
        vendorCode:
          type: string
        vendorName:
          type: string
        vendorStatus:
          type: string
        total:
          type: integer
          minimum: 0
        completed:
          type: integer
          minimum: 0
        outstanding:
          type: integer
          minimum: 0
        overdue:
          type: integer
          minimum: 0
        completionRate:
          type: number
          minimum: 0
          maximum: 100
        averageCompletionHours:
          type: number
          minimum: 0

    ManagementVendorSummaryData:
      type: object
      required:
        [activeVendors, assignedVendorWork, completedVendorWork,
         overdueVendorWork, completionRate, vendorPerformance]
      properties:
        activeVendors:
          type: integer
          minimum: 0
        assignedVendorWork:
          type: integer
          minimum: 0
          description: BE-23H vendorWork.total.
        completedVendorWork:
          type: integer
          minimum: 0
          description: BE-23H vendorWork.completed.
        overdueVendorWork:
          type: integer
          minimum: 0
          description: BE-23H vendorWork.overdue.
        completionRate:
          type: number
          minimum: 0
          maximum: 100
          description: BE-23H vendorWork.completionRate.
        vendorPerformance:
          type: array
          items:
            $ref: "#/components/schemas/ManagementVendorPerformanceRow"

    ManagementVendorSummary:
      type: object
      description: BE-24 PART 04B shared-contract Vendor projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementVendorSummaryFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementVendorSummaryData"

    ManagementTenantServiceSummaryFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required: [overdueAfterDays]
          properties:
            overdueAfterDays:
              type: integer
              minimum: 0
              maximum: 365

    ManagementTenantComplaintSummary:
      type: object
      required: [total, open, escalated, cancelled]
      properties:
        total:
          type: integer
          minimum: 0
        open:
          type: integer
          minimum: 0
        escalated:
          type: integer
          minimum: 0
        cancelled:
          type: integer
          minimum: 0

    ManagementTenantServiceSummaryData:
      type: object
      required:
        [totalRequests, openRequests, inProgressRequests, completedRequests,
         overdueRequests, complaints, completionRate]
      properties:
        totalRequests:
          type: integer
          minimum: 0
          description: BE-23H tenantService.total.
        openRequests:
          type: integer
          minimum: 0
          description: BE-23H tenantService.open.
        inProgressRequests:
          type: integer
          minimum: 0
          description: >
            CONVERTED requests whose linked BE-08 Work Order is IN_PROGRESS
            or ON_HOLD.
        completedRequests:
          type: integer
          minimum: 0
          description: BE-23H tenantService.completed.
        overdueRequests:
          type: integer
          minimum: 0
          description: >
            Outstanding requests older than overdueAfterDays by requestedAt;
            reporting state only.
        complaints:
          $ref: "#/components/schemas/ManagementTenantComplaintSummary"
        completionRate:
          type: number
          minimum: 0
          maximum: 100
          description: BE-23H tenantService.completionRate.

    ManagementTenantServiceSummary:
      type: object
      description: BE-24 PART 04C shared-contract Tenant Service projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementTenantServiceSummaryFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementTenantServiceSummaryData"

    ManagementAssetRegistryComplianceFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required: [expiringWithinDays]
          properties:
            expiringWithinDays:
              type: integer
              minimum: 0
              maximum: 365

    ManagementAssetLocationSummary:
      type: object
      required:
        [buildingId, locationType, functionalLocationId,
         functionalLocationCode, functionalLocationName,
         functionalLocationStatus, assetCount]
      properties:
        buildingId:
          $ref: "#/components/schemas/Uuid"
        locationType:
          type: string
          enum: [BUILDING, FUNCTIONAL_LOCATION]
        functionalLocationId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        functionalLocationCode:
          type: string
          nullable: true
        functionalLocationName:
          type: string
          nullable: true
        functionalLocationStatus:
          type: string
          nullable: true
        assetCount:
          type: integer
          minimum: 0

    ManagementAssetRegistryComplianceData:
      type: object
      required:
        [totalAssets, activeAssets, inactiveAssets, assetStatus, warranties,
         certifications, locations]
      properties:
        totalAssets:
          type: integer
          minimum: 0
        activeAssets:
          type: integer
          minimum: 0
        inactiveAssets:
          type: integer
          minimum: 0
        assetStatus:
          type: object
          required: [ACTIVE, INACTIVE, UNDER_MAINTENANCE, RETIRED]
          properties:
            ACTIVE:
              type: integer
              minimum: 0
            INACTIVE:
              type: integer
              minimum: 0
            UNDER_MAINTENANCE:
              type: integer
              minimum: 0
            RETIRED:
              type: integer
              minimum: 0
        warranties:
          type: object
          required:
            [totalRecords, active, expired, inactive,
             currentlyCoveredAssets, assetsWithoutCurrentCoverage]
          properties:
            totalRecords:
              type: integer
              minimum: 0
            active:
              type: integer
              minimum: 0
            expired:
              type: integer
              minimum: 0
            inactive:
              type: integer
              minimum: 0
            currentlyCoveredAssets:
              type: integer
              minimum: 0
            assetsWithoutCurrentCoverage:
              type: integer
              minimum: 0
        certifications:
          type: object
          required:
            [totalRecords, active, expired, inactive,
             currentlyEffectiveRecords, assetsWithEffectiveCertification,
             assetsWithoutEffectiveCertification, expiredComplianceCount,
             expiringComplianceCount]
          properties:
            totalRecords:
              type: integer
              minimum: 0
            active:
              type: integer
              minimum: 0
            expired:
              type: integer
              minimum: 0
            inactive:
              type: integer
              minimum: 0
            currentlyEffectiveRecords:
              type: integer
              minimum: 0
            assetsWithEffectiveCertification:
              type: integer
              minimum: 0
            assetsWithoutEffectiveCertification:
              type: integer
              minimum: 0
            expiredComplianceCount:
              type: integer
              minimum: 0
            expiringComplianceCount:
              type: integer
              minimum: 0
        locations:
          type: array
          items:
            $ref: "#/components/schemas/ManagementAssetLocationSummary"

    ManagementAssetRegistryCompliance:
      type: object
      description: BE-24 PART 05A shared-contract Asset registry/compliance projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementAssetRegistryComplianceFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementAssetRegistryComplianceData"

    ManagementAssetReliabilityWorkFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required: [graceMinutes]
          properties:
            graceMinutes:
              type: integer
              minimum: 0
              maximum: 10080

    ManagementAssetReliabilityWorkData:
      type: object
      required:
        [breakdowns, failures, activeCorrectiveWorkOrders,
         completedMaintenanceWork, overdueMaintenanceWork,
         assetAvailability, pmCompliance]
      properties:
        breakdowns:
          type: object
          required: [total, open, closed]
          properties:
            total:
              type: integer
              minimum: 0
            open:
              type: integer
              minimum: 0
            closed:
              type: integer
              minimum: 0
        failures:
          type: object
          required: [total, open, inProgress, resolved]
          properties:
            total:
              type: integer
              minimum: 0
            open:
              type: integer
              minimum: 0
            inProgress:
              type: integer
              minimum: 0
            resolved:
              type: integer
              minimum: 0
        activeCorrectiveWorkOrders:
          type: integer
          minimum: 0
          description: Breakdown-linked BE-08 Work Orders in active statuses.
        completedMaintenanceWork:
          type: integer
          minimum: 0
          description: Linked maintenance Work Orders in COMPLETED or CLOSED.
        overdueMaintenanceWork:
          type: integer
          minimum: 0
          description: Overdue tasks on ACTIVE maintenance bindings.
        assetAvailability:
          type: object
          required:
            [availableAssets, unavailableAssets, retiredAssets,
             availabilityRate]
          properties:
            availableAssets:
              type: integer
              minimum: 0
            unavailableAssets:
              type: integer
              minimum: 0
              description: INACTIVE plus UNDER_MAINTENANCE assets.
            retiredAssets:
              type: integer
              minimum: 0
            availabilityRate:
              type: number
              minimum: 0
              maximum: 100
              description: ACTIVE / non-retired assets using BE-23 rounding.
        pmCompliance:
          type: object
          required: [scheduled, completed, overdue, complianceRate]
          properties:
            scheduled:
              type: integer
              minimum: 0
            completed:
              type: integer
              minimum: 0
            overdue:
              type: integer
              minimum: 0
            complianceRate:
              type: number
              minimum: 0
              maximum: 100
              description: Completed/scheduled preventive tasks using BE-23 rounding.

    ManagementAssetReliabilityWork:
      type: object
      description: BE-24 PART 05B shared-contract Asset reliability/work projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementAssetReliabilityWorkFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementAssetReliabilityWorkData"

    ManagementUtilitySummaryFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required: [interval, includeSubMeters]
          properties:
            interval:
              type: string
              enum: [DAY, MONTH, YEAR]
            includeSubMeters:
              type: boolean

    ManagementUtilityConsumption:
      type: object
      required:
        [utilityType, totalConsumption, consumptionCount, meterCount, uomId]
      properties:
        utilityType:
          type: string
          enum: [ELECTRICITY, WATER, GAS]
        totalConsumption:
          type: number
        consumptionCount:
          type: integer
          minimum: 0
        meterCount:
          type: integer
          minimum: 0
        uomId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true

    ManagementUtilityAbnormalSummary:
      type: object
      required: [total, open, resolved, dismissed, byType]
      properties:
        total:
          type: integer
          minimum: 0
        open:
          type: integer
          minimum: 0
        resolved:
          type: integer
          minimum: 0
        dismissed:
          type: integer
          minimum: 0
        byType:
          type: object
          additionalProperties:
            type: integer

    ManagementUtilityVerificationSummary:
      type: object
      description: BE-23I verification of abnormal-consumption readings.
      required: [verified, pending, rejected, reworkRequired, unverified]
      properties:
        verified:
          type: integer
          minimum: 0
        pending:
          type: integer
          minimum: 0
        rejected:
          type: integer
          minimum: 0
        reworkRequired:
          type: integer
          minimum: 0
        unverified:
          type: integer
          minimum: 0

    ManagementTenantUtilityRow:
      type: object
      required:
        [buildingId, tenantCompanyId, tenantName, utilityType,
         totalConsumption, consumptionCount, meterCount, uomId,
         periodStart, periodEnd]
      properties:
        buildingId:
          $ref: "#/components/schemas/Uuid"
        tenantCompanyId:
          $ref: "#/components/schemas/Uuid"
        tenantName:
          type: string
          nullable: true
        utilityType:
          type: string
          enum: [ELECTRICITY, WATER, GAS]
        totalConsumption:
          type: number
        consumptionCount:
          type: integer
          minimum: 0
        meterCount:
          type: integer
          minimum: 0
        uomId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        periodStart:
          type: string
          format: date-time
          nullable: true
        periodEnd:
          type: string
          format: date-time
          nullable: true

    ManagementUtilityTrendPoint:
      type: object
      required: [intervalStart, totalConsumption, consumptionCount, meterCount]
      properties:
        intervalStart:
          type: string
          format: date-time
          nullable: true
        totalConsumption:
          type: number
        consumptionCount:
          type: integer
          minimum: 0
        meterCount:
          type: integer
          minimum: 0

    ManagementUtilitySummaryData:
      type: object
      required:
        [electricity, water, gas, abnormalConsumption,
         verifiedReadingSummary, tenantUtility, periodComparison]
      properties:
        electricity:
          $ref: "#/components/schemas/ManagementUtilityConsumption"
        water:
          $ref: "#/components/schemas/ManagementUtilityConsumption"
        gas:
          $ref: "#/components/schemas/ManagementUtilityConsumption"
        abnormalConsumption:
          $ref: "#/components/schemas/ManagementUtilityAbnormalSummary"
        verifiedReadingSummary:
          $ref: "#/components/schemas/ManagementUtilityVerificationSummary"
        tenantUtility:
          type: array
          items:
            $ref: "#/components/schemas/ManagementTenantUtilityRow"
        periodComparison:
          type: object
          description: Latest two BE-23I trend buckets; no alternate arithmetic.
          required: [interval, previous, current]
          properties:
            interval:
              type: string
              enum: [DAY, MONTH, YEAR]
            previous:
              allOf:
                - $ref: "#/components/schemas/ManagementUtilityTrendPoint"
              nullable: true
            current:
              allOf:
                - $ref: "#/components/schemas/ManagementUtilityTrendPoint"
              nullable: true

    ManagementUtilitySummary:
      type: object
      description: BE-24 PART 06A shared-contract Utility Summary projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementUtilitySummaryFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementUtilitySummaryData"

    ManagementCountAmount:
      type: object
      required: [count, amount]
      properties:
        count:
          type: integer
          minimum: 0
        amount:
          type: number

    ManagementFinancialSummaryBlock:
      type: object
      required:
        [tenantCharges, utilityBills, invoices, payments, receipts,
         vendorServiceCosts, basicExpenses, outstandingBalance,
         incomeVsOperationalCost]
      properties:
        tenantCharges:
          allOf:
            - $ref: "#/components/schemas/ManagementCountAmount"
            - type: object
              required: [cancelledCount]
              properties:
                cancelledCount:
                  type: integer
                  minimum: 0
        utilityBills:
          allOf:
            - $ref: "#/components/schemas/ManagementCountAmount"
            - type: object
              required: [cancelledCount]
              properties:
                cancelledCount:
                  type: integer
                  minimum: 0
        invoices:
          allOf:
            - $ref: "#/components/schemas/ManagementCountAmount"
            - type: object
              required: [draftCount, cancelledCount]
              properties:
                draftCount:
                  type: integer
                  minimum: 0
                cancelledCount:
                  type: integer
                  minimum: 0
        payments:
          type: object
          required:
            [paidAmount, unpaidAmount, overdueAmount, outstandingAmount,
             unpaidCount, partiallyPaidCount, paidCount, overdueCount]
          properties:
            paidAmount:
              type: number
            unpaidAmount:
              type: number
              description: Outstanding amount not yet overdue.
            overdueAmount:
              type: number
            outstandingAmount:
              type: number
            unpaidCount:
              type: integer
              minimum: 0
            partiallyPaidCount:
              type: integer
              minimum: 0
            paidCount:
              type: integer
              minimum: 0
            overdueCount:
              type: integer
              minimum: 0
        receipts:
          type: object
          required: [issued, void]
          properties:
            issued:
              $ref: "#/components/schemas/ManagementCountAmount"
            void:
              $ref: "#/components/schemas/ManagementCountAmount"
        vendorServiceCosts:
          type: object
          required: [finalized, draftCount, cancelledCount]
          properties:
            finalized:
              $ref: "#/components/schemas/ManagementCountAmount"
            draftCount:
              type: integer
              minimum: 0
            cancelledCount:
              type: integer
              minimum: 0
        basicExpenses:
          type: object
          required: [finalized, draftCount, cancelledCount]
          properties:
            finalized:
              $ref: "#/components/schemas/ManagementCountAmount"
            draftCount:
              type: integer
              minimum: 0
            cancelledCount:
              type: integer
              minimum: 0
        outstandingBalance:
          type: object
          required: [invoiceCount, amount]
          properties:
            invoiceCount:
              type: integer
              minimum: 0
            amount:
              type: number
        incomeVsOperationalCost:
          type: object
          required:
            [billedIncome, receivedIncome, operationalCost,
             netBilled, netReceived]
          properties:
            billedIncome:
              type: number
            receivedIncome:
              type: number
            operationalCost:
              type: number
            netBilled:
              type: number
            netReceived:
              type: number

    ManagementBuildingFinancialSummary:
      type: object
      required: [clientId, buildingId, summary]
      properties:
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          $ref: "#/components/schemas/Uuid"
        summary:
          $ref: "#/components/schemas/ManagementFinancialSummaryBlock"

    ManagementClientFinancialSummary:
      type: object
      required: [clientId, buildingIds, summary]
      properties:
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingIds:
          type: array
          items:
            $ref: "#/components/schemas/Uuid"
        summary:
          $ref: "#/components/schemas/ManagementFinancialSummaryBlock"

    ManagementFinancialSummaryData:
      type: object
      description: Client-partitioned monetary summaries; no cross-Client total.
      required: [clientSummaries, buildingSummaries]
      properties:
        clientSummaries:
          type: array
          items:
            $ref: "#/components/schemas/ManagementClientFinancialSummary"
        buildingSummaries:
          type: array
          items:
            $ref: "#/components/schemas/ManagementBuildingFinancialSummary"

    ManagementFinancialSummary:
      type: object
      description: BE-24 PART 06B shared-contract Financial Summary projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementReadScopeFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementFinancialSummaryData"

    # ------------------------------------------------------------------
    # CR-BE-FIN-01 — Operational Finance budget/binding/aggregation contract
    # ------------------------------------------------------------------
    OperationalBudgetStatus:
      type: string
      enum: [DRAFT, ACTIVE, CLOSED, CANCELLED]

    OperationalBudgetCurrency:
      type: string
      enum: [IDR, USD, SGD, MYR, AUD, EUR, GBP, JPY, CNY]

    OperationalBudgetPeriod:
      type: object
      required: [start, end]
      properties:
        start:
          type: string
          format: date
        end:
          type: string
          format: date

    OperationalBudgetOverspendPolicy:
      type: string
      description: >
        STRICT prohibits any commitment beyond available budget.
        ALLOW_WITH_OVERRIDE permits it ONLY through the separate
        operational_budget.override authority plus an explicit recorded reason.
        Overspend is never silently permitted.
      enum: [STRICT, ALLOW_WITH_OVERRIDE]

    OperationalBudget:
      type: object
      required:
        [id, budgetName, clientId, buildingId, budgetPeriod, currency,
         plannedAmount, status, overspendPolicy, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        budgetName: { type: string }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        budgetPeriod: { $ref: "#/components/schemas/OperationalBudgetPeriod" }
        currency: { $ref: "#/components/schemas/OperationalBudgetCurrency" }
        plannedAmount: { type: number, minimum: 0 }
        status: { $ref: "#/components/schemas/OperationalBudgetStatus" }
        overspendPolicy: { $ref: "#/components/schemas/OperationalBudgetOverspendPolicy" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateOperationalBudgetRequest:
      type: object
      required: [budgetPeriod, currency, plannedAmount]
      properties:
        budgetName: { type: string, maxLength: 160 }
        budgetPeriod: { $ref: "#/components/schemas/OperationalBudgetPeriod" }
        currency: { $ref: "#/components/schemas/OperationalBudgetCurrency" }
        plannedAmount: { type: number, minimum: 0 }
        overspendPolicy: { $ref: "#/components/schemas/OperationalBudgetOverspendPolicy" }

    UpdateOperationalBudgetRequest:
      type: object
      minProperties: 1
      description: >
        DRAFT-only. Changing the overspend policy of an ACTIVE budget uses the
        separately authorised POST /operational-budgets/{id}/overspend-policy.
      properties:
        budgetName: { type: string, maxLength: 160 }
        budgetPeriod: { $ref: "#/components/schemas/OperationalBudgetPeriod" }
        currency: { $ref: "#/components/schemas/OperationalBudgetCurrency" }
        plannedAmount: { type: number, minimum: 0 }
        overspendPolicy: { $ref: "#/components/schemas/OperationalBudgetOverspendPolicy" }

    OperationalBudgetCategory:
      type: object
      required: [id, budgetId, code, name, plannedAmount, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        budgetId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        plannedAmount: { type: number, minimum: 0 }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateOperationalBudgetCategoryRequest:
      type: object
      required: [code, name, plannedAmount]
      properties:
        code: { type: string, pattern: '^[A-Z][A-Z0-9_]{0,63}$' }
        name: { type: string, maxLength: 160 }
        plannedAmount: { type: number, minimum: 0 }

    UpdateOperationalBudgetCategoryRequest:
      type: object
      minProperties: 1
      properties:
        name: { type: string, maxLength: 160 }
        plannedAmount: { type: number, minimum: 0 }

    OperationalFinanceSourceType:
      type: string
      enum: [BASIC_EXPENSE, VENDOR_SERVICE_COST, VENDOR_INVOICE, PURCHASE_ORDER, PO_LINE, WORK_ORDER_MATERIAL]

    OperationalFinanceCurrencyStatus:
      type: string
      enum: [MATCHED, MISSING]

    OperationalFinanceBindingStatus:
      type: string
      enum: [ACTIVE, REMOVED]

    OperationalBudgetSourceBinding:
      type: object
      required:
        [id, budgetId, budgetCategoryId, clientId, buildingId, sourceType,
         currencyStatus, status, createdByUserId, removedByUserId, removedAt,
         createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        budgetId: { $ref: "#/components/schemas/Uuid" }
        budgetCategoryId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        sourceType: { $ref: "#/components/schemas/OperationalFinanceSourceType" }
        basicExpenseId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        vendorServiceCostId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        vendorInvoiceId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        purchaseOrderId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        purchaseOrderLineId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        workOrderMaterialUsageId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        currencyStatus: { $ref: "#/components/schemas/OperationalFinanceCurrencyStatus" }
        status: { $ref: "#/components/schemas/OperationalFinanceBindingStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        removedByUserId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        removedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateOperationalBudgetSourceBindingRequest:
      type: object
      description: Exactly one typed source reference is required; source amounts are never accepted.
      required: [budgetCategoryId, sourceType]
      properties:
        budgetCategoryId: { $ref: "#/components/schemas/Uuid" }
        sourceType: { $ref: "#/components/schemas/OperationalFinanceSourceType" }
        basicExpenseId: { $ref: "#/components/schemas/Uuid" }
        vendorServiceCostId: { $ref: "#/components/schemas/Uuid" }
        vendorInvoiceId: { $ref: "#/components/schemas/Uuid" }
        purchaseOrderId: { $ref: "#/components/schemas/Uuid" }
        purchaseOrderLineId: { $ref: "#/components/schemas/Uuid" }
        workOrderMaterialUsageId: { $ref: "#/components/schemas/Uuid" }

    OperationalBudgetAggregationTotals:
      type: object
      required: [plannedAmount, actualAmount, committedAmount, remainingAmount, varianceAmount]
      properties:
        plannedAmount: { type: number }
        actualAmount: { type: number }
        committedAmount: { type: number }
        remainingAmount: { type: number, description: May be negative. }
        varianceAmount: { type: number, description: May be negative. }

    OperationalBudgetAggregationCategory:
      type: object
      required:
        [budgetCategoryId, categoryCode, categoryName, plannedAmount,
         actualAmount, committedAmount, remainingAmount, varianceAmount,
         actualSourceCount, committedSourceCount]
      properties:
        budgetCategoryId: { $ref: "#/components/schemas/Uuid" }
        categoryCode: { type: string }
        categoryName: { type: string }
        plannedAmount: { type: number }
        actualAmount: { type: number }
        committedAmount: { type: number }
        remainingAmount: { type: number, description: May be negative. }
        varianceAmount: { type: number, description: May be negative. }
        actualSourceCount: { type: integer, minimum: 0 }
        committedSourceCount: { type: integer, minimum: 0 }

    OperationalBudgetAggregationExclusion:
      type: object
      required: [bindingId, budgetCategoryId, sourceType, sourceId, reason]
      properties:
        bindingId: { $ref: "#/components/schemas/Uuid" }
        budgetCategoryId: { $ref: "#/components/schemas/Uuid" }
        sourceType: { $ref: "#/components/schemas/OperationalFinanceSourceType" }
        sourceId: { $ref: "#/components/schemas/Uuid" }
        reason: { type: string }

    OperationalBudgetAggregationControls:
      type: object
      required: [failClosed, excludedContributionCount, exclusions]
      properties:
        failClosed: { type: boolean }
        excludedContributionCount: { type: integer, minimum: 0 }
        exclusions:
          type: array
          items: { $ref: "#/components/schemas/OperationalBudgetAggregationExclusion" }

    OperationalBudgetAggregation:
      type: object
      required:
        [budgetId, clientId, budgetName, buildingId, budgetPeriod, currency,
         status, totals, categories, controls, asOf]
      properties:
        budgetId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        budgetName: { type: string }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        budgetPeriod: { $ref: "#/components/schemas/OperationalBudgetPeriod" }
        currency: { $ref: "#/components/schemas/OperationalBudgetCurrency" }
        status: { $ref: "#/components/schemas/OperationalBudgetStatus" }
        totals: { $ref: "#/components/schemas/OperationalBudgetAggregationTotals" }
        categories:
          type: array
          items: { $ref: "#/components/schemas/OperationalBudgetAggregationCategory" }
        controls: { $ref: "#/components/schemas/OperationalBudgetAggregationControls" }
        asOf: { type: string, format: date-time }

    OperationalBudgetEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data: { $ref: "#/components/schemas/OperationalBudget" }

    OperationalBudgetListEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              type: array
              items: { $ref: "#/components/schemas/OperationalBudget" }

    OperationalBudgetCategoryEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data: { $ref: "#/components/schemas/OperationalBudgetCategory" }

    OperationalBudgetCategoryListEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              type: array
              items: { $ref: "#/components/schemas/OperationalBudgetCategory" }

    OperationalBudgetSourceBindingEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data: { $ref: "#/components/schemas/OperationalBudgetSourceBinding" }

    OperationalBudgetSourceBindingListEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              type: array
              items: { $ref: "#/components/schemas/OperationalBudgetSourceBinding" }

    OperationalCommitmentStatus:
      type: string
      description: >
        Derived from the ledger amounts. RELEASED and CANCELLED are terminal;
        a commitment is never cancellable once anything is actualized.
      enum: [COMMITTED, PARTIALLY_ACTUALIZED, ACTUALIZED, RELEASED, CANCELLED]

    OperationalCommitmentOrigin:
      type: string
      description: >
        MANUAL is an explicitly reasoned estimate; PO_LINE and PO_HEADER are
        derived from an ISSUED Purchase Order, the only priced obligation
        authority in the repository.
      enum: [MANUAL, PO_LINE, PO_HEADER]

    OperationalCommitmentEntryType:
      type: string
      enum:
        [CREATE, ADJUST_INCREASE, ADJUST_DECREASE, ACTUALIZE,
         ACTUALIZE_REVERSAL, RELEASE, CANCEL, OVERRIDE]

    OperationalCommitmentOverride:
      type: object
      nullable: true
      required: [reason, byUserId, at]
      properties:
        reason: { type: string }
        byUserId: { $ref: "#/components/schemas/Uuid" }
        at: { type: string, format: date-time }

    OperationalCommitment:
      type: object
      required:
        [id, clientId, buildingId, budgetId, budgetCategoryId, origin, currency,
         committedAmount, actualizedAmount, releasedAmount, openAmount, status,
         title, idempotencyKey, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        budgetId: { $ref: "#/components/schemas/Uuid" }
        budgetCategoryId: { $ref: "#/components/schemas/Uuid" }
        origin: { $ref: "#/components/schemas/OperationalCommitmentOrigin" }
        sourceType:
          type: string
          nullable: true
          enum: [PURCHASE_ORDER, PO_LINE, null]
        purchaseOrderId: { type: string, format: uuid, nullable: true }
        purchaseOrderLineId: { type: string, format: uuid, nullable: true }
        workOrderId: { type: string, format: uuid, nullable: true }
        vendorId: { type: string, format: uuid, nullable: true }
        materialRequestId: { type: string, format: uuid, nullable: true }
        currency: { $ref: "#/components/schemas/OperationalBudgetCurrency" }
        committedAmount: { type: number, minimum: 0 }
        actualizedAmount:
          type: number
          minimum: 0
          description: >
            Actualization moves value from openAmount to actualizedAmount and
            does NOT free budget, so an obligation is never counted twice.
        releasedAmount: { type: number, minimum: 0 }
        openAmount: { type: number, minimum: 0 }
        status: { $ref: "#/components/schemas/OperationalCommitmentStatus" }
        title: { type: string, maxLength: 160 }
        reason: { type: string, nullable: true }
        overspendOverride: { $ref: "#/components/schemas/OperationalCommitmentOverride" }
        idempotencyKey: { type: string }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        closedAt: { type: string, format: date-time, nullable: true }
        closedByUserId: { type: string, format: uuid, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    OperationalCommitmentEntry:
      type: object
      description: >
        Append-only. signedAmount is the effect on the commitment's open amount,
        so the entries of a commitment always sum to its openAmount. Entries are
        never updated or deleted; corrections are new entries.
      required:
        [id, commitmentId, entryType, signedAmount, currency, idempotencyKey,
         actorUserId, occurredAt, createdAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        commitmentId: { $ref: "#/components/schemas/Uuid" }
        entryType: { $ref: "#/components/schemas/OperationalCommitmentEntryType" }
        signedAmount: { type: number }
        currency: { $ref: "#/components/schemas/OperationalBudgetCurrency" }
        sourceBindingId: { type: string, format: uuid, nullable: true }
        idempotencyKey: { type: string }
        reason: { type: string, nullable: true }
        actorUserId: { $ref: "#/components/schemas/Uuid" }
        requestId: { type: string, format: uuid, nullable: true }
        occurredAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }

    OperationalCommitmentDetail:
      allOf:
        - $ref: "#/components/schemas/OperationalCommitment"
        - type: object
          required: [entries]
          properties:
            entries:
              type: array
              items: { $ref: "#/components/schemas/OperationalCommitmentEntry" }

    CreateOperationalCommitmentRequest:
      type: object
      description: >
        Manual/estimated commitment. Derived scope (client, building, budget,
        status and every ledger amount) must not be supplied.
      required: [budgetCategoryId, title, amount, currency, reason, idempotencyKey]
      properties:
        budgetCategoryId: { $ref: "#/components/schemas/Uuid" }
        title: { type: string, maxLength: 160 }
        amount:
          type: number
          exclusiveMinimum: 0
          description: Positive, at most two decimals, in the budget currency.
        currency: { $ref: "#/components/schemas/OperationalBudgetCurrency" }
        reason: { type: string, maxLength: 500 }
        idempotencyKey: { type: string, minLength: 8, maxLength: 120 }
        workOrderId: { type: string, format: uuid, nullable: true }
        vendorId: { type: string, format: uuid, nullable: true }
        materialRequestId: { type: string, format: uuid, nullable: true }
        overspendOverrideReason:
          type: string
          maxLength: 500
          nullable: true
          description: >
            Required to exceed available budget, and only honoured when the
            budget policy is ALLOW_WITH_OVERRIDE and the caller holds
            operational_budget.override.

    CreatePurchaseOrderLineCommitmentRequest:
      type: object
      description: >
        Amount, currency, vendor, Building and Material Request lineage are
        derived from the ISSUED Purchase Order line and must not be supplied.
      required: [purchaseOrderLineId, budgetCategoryId, idempotencyKey]
      properties:
        purchaseOrderLineId: { $ref: "#/components/schemas/Uuid" }
        budgetCategoryId: { $ref: "#/components/schemas/Uuid" }
        idempotencyKey: { type: string, minLength: 8, maxLength: 120 }
        overspendOverrideReason: { type: string, maxLength: 500, nullable: true }

    AdjustOperationalCommitmentRequest:
      type: object
      required: [amount, reason, idempotencyKey]
      properties:
        amount:
          type: number
          description: >
            Non-zero, at most two decimals. Positive increases the obligation,
            negative decreases it; a decrease below the actualized amount is
            rejected.
        reason: { type: string, maxLength: 500 }
        idempotencyKey: { type: string, minLength: 8, maxLength: 120 }
        overspendOverrideReason: { type: string, maxLength: 500, nullable: true }

    CloseOperationalCommitmentRequest:
      type: object
      required: [reason, idempotencyKey]
      properties:
        reason: { type: string, maxLength: 500 }
        idempotencyKey: { type: string, minLength: 8, maxLength: 120 }

    ChangeOperationalBudgetOverspendPolicyRequest:
      type: object
      required: [overspendPolicy, reason]
      properties:
        overspendPolicy: { $ref: "#/components/schemas/OperationalBudgetOverspendPolicy" }
        reason: { type: string, maxLength: 500 }

    OperationalBudgetOverspendPolicyResult:
      type: object
      required: [budgetId, overspendPolicy]
      properties:
        budgetId: { $ref: "#/components/schemas/Uuid" }
        overspendPolicy: { $ref: "#/components/schemas/OperationalBudgetOverspendPolicy" }

    OperationalBudgetVarianceTotals:
      type: object
      description: >
        openCommitment = ledger open + eligible legacy commitment.
        actual = ledger actualized + unallocated actual.
        consumed = (ledger committed - released) + eligible legacy commitment
        + unallocated actual. available = planned - consumed.
        variance = planned - actual. Percentages are null when the plan is zero.
      required:
        [plannedAmount, ledgerCommittedAmount, ledgerOpenAmount,
         ledgerActualizedAmount, ledgerReleasedAmount, legacyCommittedAmount,
         uncommittedMaterialActualAmount, uncommittedInvoiceActualAmount,
         unallocatedActualAmount, openCommitmentAmount, actualAmount,
         consumedAmount, availableAmount, varianceAmount, utilizationPercent,
         committedUtilizationPercent]
      properties:
        plannedAmount: { type: number }
        ledgerCommittedAmount: { type: number }
        ledgerOpenAmount: { type: number }
        ledgerActualizedAmount: { type: number }
        ledgerReleasedAmount: { type: number }
        legacyCommittedAmount: { type: number }
        uncommittedMaterialActualAmount: { type: number }
        uncommittedInvoiceActualAmount: { type: number }
        unallocatedActualAmount: { type: number }
        openCommitmentAmount: { type: number }
        actualAmount: { type: number }
        consumedAmount: { type: number }
        availableAmount: { type: number }
        varianceAmount: { type: number }
        utilizationPercent: { type: number, nullable: true }
        committedUtilizationPercent: { type: number, nullable: true }

    OperationalBudgetVarianceCategory:
      type: object
      required:
        [budgetCategoryId, categoryCode, categoryName, plannedAmount,
         ledgerCommittedAmount, ledgerOpenAmount, ledgerActualizedAmount,
         ledgerReleasedAmount, legacyCommittedAmount, openCommitmentAmount,
         actualAmount, consumedAmount, availableAmount, varianceAmount,
         utilizationPercent, committedUtilizationPercent, commitmentCount]
      properties:
        budgetCategoryId: { $ref: "#/components/schemas/Uuid" }
        categoryCode: { type: string }
        categoryName: { type: string }
        plannedAmount: { type: number }
        ledgerCommittedAmount: { type: number }
        ledgerOpenAmount: { type: number }
        ledgerActualizedAmount: { type: number }
        ledgerReleasedAmount: { type: number }
        legacyCommittedAmount: { type: number }
        openCommitmentAmount: { type: number }
        actualAmount: { type: number }
        consumedAmount: { type: number }
        availableAmount: { type: number }
        varianceAmount: { type: number }
        utilizationPercent: { type: number, nullable: true }
        committedUtilizationPercent: { type: number, nullable: true }
        commitmentCount: { type: integer, minimum: 0 }

    OperationalBudgetVarianceGap:
      type: object
      description: >
        A recorded gap is described, never valued. B-02: vendor_service_costs
        and basic_expenses with an unknown currency are excluded from
        currency-dependent figures. B-03: no approved vendor amount exists
        before a Purchase Order.
      required: [code, reference, message, affectedSourceCount]
      properties:
        code:
          type: string
          enum: [CURRENCYLESS_COST_AUTHORITY, VENDOR_ACTUAL_WITHOUT_COMMITMENT]
        reference: { type: string, enum: [B-02, B-03] }
        message: { type: string }
        affectedSourceCount: { type: integer, minimum: 0 }

    OperationalBudgetVariance:
      type: object
      required:
        [budgetId, clientId, buildingId, budgetName, budgetPeriod, currency,
         status, overspendPolicy, totals, categories, controls, gaps, asOf]
      properties:
        budgetId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        budgetName: { type: string }
        budgetPeriod: { $ref: "#/components/schemas/OperationalBudgetPeriod" }
        currency: { $ref: "#/components/schemas/OperationalBudgetCurrency" }
        status: { $ref: "#/components/schemas/OperationalBudgetStatus" }
        overspendPolicy: { $ref: "#/components/schemas/OperationalBudgetOverspendPolicy" }
        totals: { $ref: "#/components/schemas/OperationalBudgetVarianceTotals" }
        categories:
          type: array
          items: { $ref: "#/components/schemas/OperationalBudgetVarianceCategory" }
        controls:
          type: object
          required:
            [categoryScopeIncludesUnallocatedActual,
             legacyCommitmentSupersededCount, failClosed,
             excludedContributionCount, exclusions]
          properties:
            categoryScopeIncludesUnallocatedActual:
              type: boolean
              description: >
                Always false: an unmatched source carries no cost category and
                none is inferred.
            legacyCommitmentSupersededCount: { type: integer, minimum: 0 }
            failClosed: { type: boolean }
            excludedContributionCount: { type: integer, minimum: 0 }
            exclusions:
              type: array
              items: { $ref: "#/components/schemas/OperationalBudgetAggregationExclusion" }
        gaps:
          type: array
          items: { $ref: "#/components/schemas/OperationalBudgetVarianceGap" }
        asOf: { type: string, format: date-time }

    OperationalBudgetVarianceSummary:
      type: object
      required:
        [budgetId, clientId, buildingId, budgetName, budgetPeriod, currency,
         status, totals, failClosed, gapCount]
      properties:
        budgetId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        budgetName: { type: string }
        budgetPeriod: { $ref: "#/components/schemas/OperationalBudgetPeriod" }
        currency: { $ref: "#/components/schemas/OperationalBudgetCurrency" }
        status: { $ref: "#/components/schemas/OperationalBudgetStatus" }
        totals: { $ref: "#/components/schemas/OperationalBudgetVarianceTotals" }
        failClosed: { type: boolean }
        gapCount: { type: integer, minimum: 0 }

    OperationalBudgetTraceabilityRow:
      type: object
      required: [classification, sourceType, sourceId]
      properties:
        classification:
          type: string
          enum: [COMMITMENT, LEGACY_COMMITMENT, ACTUAL, EXCLUDED]
        sourceType: { type: string }
        sourceId: { $ref: "#/components/schemas/Uuid" }
        commitmentId: { type: string, format: uuid, nullable: true }
        bindingId: { type: string, format: uuid, nullable: true }
        budgetCategoryId: { type: string, format: uuid, nullable: true }
        amount: { type: number, nullable: true }
        openAmount: { type: number, nullable: true }
        actualizedAmount: { type: number, nullable: true }
        currency: { type: string, nullable: true }
        status: { type: string, nullable: true }
        description: { type: string, nullable: true }
        purchaseOrderId: { type: string, format: uuid, nullable: true }
        purchaseOrderLineId: { type: string, format: uuid, nullable: true }
        vendorId: { type: string, format: uuid, nullable: true }
        workOrderId: { type: string, format: uuid, nullable: true }
        materialRequestId: { type: string, format: uuid, nullable: true }
        vendorInvoiceId: { type: string, format: uuid, nullable: true }
        workOrderMaterialUsageId: { type: string, format: uuid, nullable: true }
        exclusionReason: { type: string, nullable: true }

    OperationalBudgetTraceability:
      type: object
      required:
        [budgetId, clientId, buildingId, budgetPeriod, currency, rowCount, rows, asOf]
      properties:
        budgetId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        budgetPeriod: { $ref: "#/components/schemas/OperationalBudgetPeriod" }
        currency: { $ref: "#/components/schemas/OperationalBudgetCurrency" }
        rowCount: { type: integer, minimum: 0 }
        rows:
          type: array
          items: { $ref: "#/components/schemas/OperationalBudgetTraceabilityRow" }
        asOf: { type: string, format: date-time }

    OperationalCommitmentEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data: { $ref: "#/components/schemas/OperationalCommitment" }

    OperationalCommitmentListEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              type: array
              items: { $ref: "#/components/schemas/OperationalCommitment" }

    OperationalCommitmentDetailEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data: { $ref: "#/components/schemas/OperationalCommitmentDetail" }

    OperationalBudgetOverspendPolicyEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data: { $ref: "#/components/schemas/OperationalBudgetOverspendPolicyResult" }

    OperationalBudgetVarianceEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data: { $ref: "#/components/schemas/OperationalBudgetVariance" }

    OperationalBudgetVarianceListEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data:
              type: array
              items: { $ref: "#/components/schemas/OperationalBudgetVarianceSummary" }

    OperationalBudgetTraceabilityEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data: { $ref: "#/components/schemas/OperationalBudgetTraceability" }

    OperationalBudgetAggregationEnvelope:
      allOf:
        - $ref: "#/components/schemas/SuccessEnvelope"
        - type: object
          properties:
            data: { $ref: "#/components/schemas/OperationalBudgetAggregation" }

    ManagementOperationalFinanceFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required: [budgetId]
          properties:
            budgetId:
              $ref: "#/components/schemas/Uuid"

    ManagementOperationalBudgetTotals:
      type: object
      required: [plannedAmount, actualAmount, committedAmount, remainingAmount, varianceAmount]
      properties:
        plannedAmount:
          type: number
        actualAmount:
          type: number
        committedAmount:
          type: number
        remainingAmount:
          type: number
          description: Planned amount minus actual and committed amounts; may be negative.
        varianceAmount:
          type: number
          description: Planned amount minus actual amount; may be negative.

    ManagementOperationalFinanceControls:
      type: object
      required: [failClosed, excludedContributionCount, exclusionReasons]
      properties:
        failClosed:
          type: boolean
        excludedContributionCount:
          type: integer
          minimum: 0
        exclusionReasons:
          type: array
          items:
            type: string
            enum:
              - SOURCE_NOT_ELIGIBLE
              - SOURCE_CURRENCY_UNPROVEN
              - SOURCE_CURRENCY_MISMATCH
              - SOURCE_PERIOD_OUTSIDE_BUDGET
              - SOURCE_AMOUNT_UNAVAILABLE
              - SOURCE_LINEAGE_AMBIGUOUS
              - SOURCE_REPRESENTED_BY_DOWNSTREAM_ACTUAL
              - PO_HEADER_SUPERSEDED_BY_PO_LINE
              - PO_COMMITMENT_REPLACED_BY_ACTUAL

    ManagementOperationalBudgetCategory:
      type: object
      required:
        [categoryId, categoryCode, categoryName, plannedAmount,
         actualAmount, committedAmount, remainingAmount, varianceAmount,
         actualSourceCount, committedSourceCount]
      properties:
        categoryId:
          $ref: "#/components/schemas/Uuid"
        categoryCode:
          type: string
        categoryName:
          type: string
        plannedAmount:
          type: number
        actualAmount:
          type: number
        committedAmount:
          type: number
        remainingAmount:
          type: number
          description: Planned amount minus actual and committed amounts; may be negative.
        varianceAmount:
          type: number
          description: Planned amount minus actual amount; may be negative.
        actualSourceCount:
          type: integer
          minimum: 0
        committedSourceCount:
          type: integer
          minimum: 0

    ManagementOperationalBudgetSummaryData:
      type: object
      required:
        [budgetId, clientId, buildingId, budgetName, periodStart, periodEnd,
         currency, status, totals, controls]
      properties:
        budgetId:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          $ref: "#/components/schemas/Uuid"
        budgetName:
          type: string
        periodStart:
          type: string
          format: date
        periodEnd:
          type: string
          format: date
        currency:
          type: string
          enum: [IDR, USD, SGD, MYR, AUD, EUR, GBP, JPY, CNY]
        status:
          type: string
          enum: [DRAFT, ACTIVE, CLOSED, CANCELLED]
        totals:
          $ref: "#/components/schemas/ManagementOperationalBudgetTotals"
        controls:
          $ref: "#/components/schemas/ManagementOperationalFinanceControls"

    ManagementOperationalBudgetCategoriesData:
      type: object
      required:
        [budgetId, clientId, buildingId, budgetName, periodStart, periodEnd,
         currency, status, categories, controls]
      properties:
        budgetId:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          $ref: "#/components/schemas/Uuid"
        budgetName:
          type: string
        periodStart:
          type: string
          format: date
        periodEnd:
          type: string
          format: date
        currency:
          type: string
          enum: [IDR, USD, SGD, MYR, AUD, EUR, GBP, JPY, CNY]
        status:
          type: string
          enum: [DRAFT, ACTIVE, CLOSED, CANCELLED]
        categories:
          type: array
          items:
            $ref: "#/components/schemas/ManagementOperationalBudgetCategory"
        controls:
          $ref: "#/components/schemas/ManagementOperationalFinanceControls"

    ManagementOperationalBudgetSummary:
      type: object
      description: Additive management contract delegated to PART 04 aggregation.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementOperationalFinanceFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementOperationalBudgetSummaryData"

    ManagementOperationalBudgetCategories:
      type: object
      description: Additive management category contract delegated to PART 04 aggregation.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementOperationalFinanceFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementOperationalBudgetCategoriesData"

    ManagementBuildingOperationalFinanceFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required: [buildingId, periodStart, periodEnd]
          properties:
            buildingId:
              $ref: "#/components/schemas/Uuid"
            periodStart:
              type: string
              format: date
            periodEnd:
              type: string
              format: date

    ManagementBuildingOperationalFinanceCategory:
      type: object
      required:
        [categoryId, categoryCode, categoryName, plannedAmount,
         actualAmount, committedAmount, remainingAmount, varianceAmount,
         budgetUtilizationPercent]
      properties:
        categoryId:
          $ref: "#/components/schemas/Uuid"
        categoryCode:
          type: string
        categoryName:
          type: string
        plannedAmount:
          type: number
        actualAmount:
          type: number
        committedAmount:
          type: number
        remainingAmount:
          type: number
          description: Planned amount minus actual and committed amounts; may be negative.
        varianceAmount:
          type: number
          description: Planned amount minus actual amount; may be negative.
        budgetUtilizationPercent:
          type: number
          nullable: true
          description: Actual divided by planned amount multiplied by 100; may exceed 100 percent.

    ManagementBuildingOperationalFinanceData:
      type: object
      required:
        [clientId, buildingId, periodStart, periodEnd, currency,
         plannedAmount, actualAmount, committedAmount, remainingAmount,
         varianceAmount, activeBudgetCount, categoryCount,
         categoriesOverBudget, categoriesWithCommitment, failClosed,
         excludedContributionCount, exclusionReasons, categories]
      properties:
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          $ref: "#/components/schemas/Uuid"
        periodStart:
          type: string
          format: date
        periodEnd:
          type: string
          format: date
        currency:
          type: string
          nullable: true
          enum: [IDR, USD, SGD, MYR, AUD, EUR, GBP, JPY, CNY]
        plannedAmount:
          type: number
        actualAmount:
          type: number
        committedAmount:
          type: number
        remainingAmount:
          type: number
          description: Planned amount minus actual and committed amounts; may be negative.
        varianceAmount:
          type: number
          description: Planned amount minus actual amount; may be negative.
        activeBudgetCount:
          type: integer
          minimum: 0
        categoryCount:
          type: integer
          minimum: 0
        categoriesOverBudget:
          type: integer
          minimum: 0
          description: Categories whose remaining amount is negative.
        categoriesWithCommitment:
          type: integer
          minimum: 0
        failClosed:
          type: boolean
        excludedContributionCount:
          type: integer
          minimum: 0
        exclusionReasons:
          type: array
          items:
            type: string
        categories:
          type: array
          items:
            $ref: "#/components/schemas/ManagementBuildingOperationalFinanceCategory"

    ManagementBuildingOperationalFinance:
      type: object
      description: >
        Read-only Building Operational Finance summary. The data block is
        composed from PART 05 management budget contracts, which delegate
        monetary calculations to PART 04.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementBuildingOperationalFinanceFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementBuildingOperationalFinanceData"

    ManagementOperationalKpiFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required: [graceMinutes]
          properties:
            graceMinutes:
              type: integer
              minimum: 0
              maximum: 10080

    ManagementOperationalKpiData:
      type: object
      required:
        [scheduled, completed, completionRate, overdue, overdueRate,
         workOrders, findings, incidentCount, criticalOperationalItems]
      properties:
        scheduled:
          type: integer
          minimum: 0
          description: BE-23G assignments.scheduled.
        completed:
          type: integer
          minimum: 0
          description: BE-23G assignments.completed.
        completionRate:
          type: number
          minimum: 0
          maximum: 100
          description: BE-23G assignments.completionRate.
        overdue:
          type: integer
          minimum: 0
          description: BE-23G assignments.overdue.
        overdueRate:
          type: number
          minimum: 0
          maximum: 100
          description: Overdue/scheduled using BE-23 percentage rounding.
        workOrders:
          type: object
          required: [open, closed]
          properties:
            open:
              type: integer
              minimum: 0
              description: Exact BE-08 OPEN count.
            closed:
              type: integer
              minimum: 0
              description: Exact BE-08 CLOSED count.
        findings:
          type: object
          required: [open, closed]
          properties:
            open:
              type: integer
              minimum: 0
              description: BE-23F2 outstanding status-set count.
            closed:
              type: integer
              minimum: 0
              description: Exact BE-09 CLOSED count.
        incidentCount:
          type: integer
          minimum: 0
        criticalOperationalItems:
          type: object
          required: [total, securityIncidents, tenantServiceRequests]
          properties:
            total:
              type: integer
              minimum: 0
            securityIncidents:
              type: integer
              minimum: 0
              description: BE-23F2 Security-relevant CRITICAL Incident count.
            tenantServiceRequests:
              type: integer
              minimum: 0
              description: BE-23H CRITICAL-priority Tenant Service Request count.

    ManagementOperationalKpi:
      type: object
      description: BE-24 PART 07 shared-contract Operational KPI projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementOperationalKpiFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementOperationalKpiData"

    ManagementBuildingPerformanceFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementReadScopeFilters"
        - type: object
          required:
            [graceMinutes, overdueAfterDays, expiringWithinDays,
             utilityInterval, includeSubMeters]
          properties:
            graceMinutes:
              type: integer
            overdueAfterDays:
              type: integer
            expiringWithinDays:
              type: integer
            utilityInterval:
              type: string
              enum: [DAY, MONTH, YEAR]
            includeSubMeters:
              type: boolean

    ManagementBuildingPerformanceRow:
      type: object
      required:
        [buildingId, operational, workOrders, findings,
         engineeringAssetHealth, housekeeping, security, utility,
         tenantService]
      properties:
        buildingId:
          $ref: "#/components/schemas/Uuid"
        operational:
          type: object
          required: [completionRate, overdueRate]
          properties:
            completionRate:
              type: number
            overdueRate:
              type: number
        workOrders:
          type: object
          required: [total, open, inProgress, completed, overdue, verified, closed]
          properties:
            total:
              type: integer
            open:
              type: integer
            inProgress:
              type: integer
            completed:
              type: integer
            overdue:
              type: integer
            verified:
              type: integer
            closed:
              type: integer
        findings:
          type: object
          required: [open, closed, critical]
          properties:
            open:
              type: integer
            closed:
              type: integer
            critical:
              type: integer
        engineeringAssetHealth:
          type: object
          required:
            [totalAssets, activeAssets, availabilityRate, breakdowns,
             failures, pmCompliance, expiredComplianceCount,
             expiringComplianceCount]
          properties:
            totalAssets:
              type: integer
            activeAssets:
              type: integer
            availabilityRate:
              type: number
            breakdowns:
              type: object
              additionalProperties:
                type: integer
            failures:
              type: object
              additionalProperties:
                type: integer
            pmCompliance:
              type: object
              additionalProperties:
                type: number
            expiredComplianceCount:
              type: integer
            expiringComplianceCount:
              type: integer
        housekeeping:
          type: object
          required:
            [cleaningTotal, cleaningCompleted, openFindings, reworkRequired,
             qualityAuditAverageScore, activeComplaints]
          properties:
            cleaningTotal:
              type: integer
            cleaningCompleted:
              type: integer
            openFindings:
              type: integer
            reworkRequired:
              type: integer
            qualityAuditAverageScore:
              type: number
              nullable: true
            activeComplaints:
              type: integer
        security:
          type: object
          required:
            [activePosts, patrolsScheduled, patrolsCompleted, openFindings,
             incidentReadiness]
          properties:
            activePosts:
              type: integer
            patrolsScheduled:
              type: integer
            patrolsCompleted:
              type: integer
            openFindings:
              type: integer
            incidentReadiness:
              type: object
              required: [notReady, partial, ready]
              properties:
                notReady:
                  type: integer
                partial:
                  type: integer
                ready:
                  type: integer
        utility:
          type: object
          required:
            [electricityConsumption, waterConsumption, gasConsumption,
             abnormalConsumption, verifiedReadings]
          properties:
            electricityConsumption:
              type: number
            waterConsumption:
              type: number
            gasConsumption:
              type: number
            abnormalConsumption:
              type: integer
            verifiedReadings:
              type: integer
        tenantService:
          type: object
          required:
            [totalRequests, openRequests, inProgressRequests,
             completedRequests, overdueRequests, completionRate, complaints]
          properties:
            totalRequests:
              type: integer
            openRequests:
              type: integer
            inProgressRequests:
              type: integer
            completedRequests:
              type: integer
            overdueRequests:
              type: integer
            completionRate:
              type: number
            complaints:
              $ref: "#/components/schemas/ManagementTenantComplaintSummary"

    ManagementBuildingPerformanceData:
      type: object
      description: Per-Building rows only; Portfolio aggregation is out of scope.
      required: [buildings]
      properties:
        buildings:
          type: array
          items:
            $ref: "#/components/schemas/ManagementBuildingPerformanceRow"

    ManagementBuildingPerformance:
      type: object
      description: BE-24 PART 08A shared-contract Building Performance projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementBuildingPerformanceFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementBuildingPerformanceData"

    ManagementPortfolioOverviewData:
      type: object
      required:
        [buildingCount, operational, workOrders, findings, assetEngineering,
         housekeeping, security, utility, tenantService, financial,
         buildingPerformance]
      properties:
        buildingCount:
          type: integer
          minimum: 0
        operational:
          type: object
          required: [completionRate, overdueRate]
          properties:
            completionRate:
              type: number
            overdueRate:
              type: number
        workOrders:
          type: object
          required: [total, open, inProgress, completed, overdue, verified, closed]
          additionalProperties:
            type: integer
        findings:
          type: object
          required: [open, closed, critical]
          additionalProperties:
            type: integer
        assetEngineering:
          type: object
          required:
            [totalAssets, activeAssets, availabilityRate, breakdowns,
             failures, pmCompliance, expiredComplianceCount,
             expiringComplianceCount]
          properties:
            totalAssets:
              type: integer
            activeAssets:
              type: integer
            availabilityRate:
              type: number
            breakdowns:
              type: object
              additionalProperties:
                type: integer
            failures:
              type: object
              additionalProperties:
                type: integer
            pmCompliance:
              type: object
              additionalProperties:
                type: number
            expiredComplianceCount:
              type: integer
            expiringComplianceCount:
              type: integer
        housekeeping:
          type: object
          required:
            [cleaningTotal, cleaningCompleted, openFindings,
             reworkRequired, activeComplaints]
          additionalProperties:
            type: integer
        security:
          type: object
          required:
            [activePosts, patrolsScheduled, patrolsCompleted,
             openFindings, incidentReadiness]
          properties:
            activePosts:
              type: integer
            patrolsScheduled:
              type: integer
            patrolsCompleted:
              type: integer
            openFindings:
              type: integer
            incidentReadiness:
              type: object
              required: [notReady, partial, ready]
              additionalProperties:
                type: integer
        utility:
          type: object
          required:
            [electricityConsumption, waterConsumption, gasConsumption,
             abnormalConsumption, verifiedReadings]
          additionalProperties:
            type: number
        tenantService:
          type: object
          required:
            [totalRequests, openRequests, inProgressRequests,
             completedRequests, overdueRequests, completionRate, complaints]
          properties:
            totalRequests:
              type: integer
            openRequests:
              type: integer
            inProgressRequests:
              type: integer
            completedRequests:
              type: integer
            overdueRequests:
              type: integer
            completionRate:
              type: number
            complaints:
              $ref: "#/components/schemas/ManagementTenantComplaintSummary"
        financial:
          type: object
          description: Client-partitioned; no cross-Client monetary total.
          required: [clientSummaries]
          properties:
            clientSummaries:
              type: array
              items:
                $ref: "#/components/schemas/ManagementClientFinancialSummary"
        buildingPerformance:
          type: array
          items:
            $ref: "#/components/schemas/ManagementBuildingPerformanceRow"

    ManagementPortfolioOverview:
      type: object
      description: BE-24 PART 08B shared-contract Portfolio Overview projection.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementBuildingPerformanceFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementPortfolioOverviewData"

    ManagementOperationsCommandCenterFilters:
      allOf:
        - $ref: "#/components/schemas/ManagementBuildingPerformanceFilters"
        - type: object
          required: [operationalDate]
          properties:
            operationalDate:
              type: string
              format: date
              description: UTC day delegated unchanged to PART 02A.

    ManagementOperationsCommandCenterData:
      type: object
      description: >
        Completed BE-24 component data without repeated component scope
        envelopes, alternate calculations, chart metadata, or UI metadata.
      required:
        [dailyOperations, pendingApprovals, criticalFindings,
         workOrderSummary, workforceSummary, vendorSummary,
         tenantServiceSummary, assetEngineeringHealth, utilitySummary,
         financialSummary, operationalKpi, buildingPerformance,
         portfolioContext]
      properties:
        dailyOperations:
          $ref: "#/components/schemas/ManagementDailyOperationsData"
        pendingApprovals:
          $ref: "#/components/schemas/ManagementPendingApprovalData"
        criticalFindings:
          $ref: "#/components/schemas/ManagementCriticalFindingsData"
        workOrderSummary:
          $ref: "#/components/schemas/ManagementWorkOrderSummaryData"
        workforceSummary:
          $ref: "#/components/schemas/ManagementWorkforceSummaryData"
        vendorSummary:
          $ref: "#/components/schemas/ManagementVendorSummaryData"
        tenantServiceSummary:
          $ref: "#/components/schemas/ManagementTenantServiceSummaryData"
        assetEngineeringHealth:
          type: object
          required: [assetRegistryCompliance, assetReliabilityWork]
          properties:
            assetRegistryCompliance:
              $ref: "#/components/schemas/ManagementAssetRegistryComplianceData"
            assetReliabilityWork:
              $ref: "#/components/schemas/ManagementAssetReliabilityWorkData"
        utilitySummary:
          $ref: "#/components/schemas/ManagementUtilitySummaryData"
        financialSummary:
          $ref: "#/components/schemas/ManagementFinancialSummaryData"
        operationalKpi:
          $ref: "#/components/schemas/ManagementOperationalKpiData"
        buildingPerformance:
          type: array
          description: Exact PART 08A rows composed by PART 08B.
          items:
            $ref: "#/components/schemas/ManagementBuildingPerformanceRow"
        portfolioContext:
          type: object
          description: Lightweight values copied from PART 08B.
          required: [buildingCount, operational]
          properties:
            buildingCount:
              type: integer
              minimum: 0
            operational:
              type: object
              required: [completionRate, overdueRate]
              properties:
                completionRate:
                  type: number
                overdueRate:
                  type: number

    ManagementOperationsCommandCenter:
      type: object
      description: BE-24 PART 09 shared-contract Operations Command Center facade.
      required: [scope, period, filters, asOf, data]
      properties:
        scope:
          $ref: "#/components/schemas/ManagementReadScope"
        period:
          $ref: "#/components/schemas/ManagementReadPeriod"
        filters:
          $ref: "#/components/schemas/ManagementOperationsCommandCenterFilters"
        asOf:
          type: string
          format: date-time
        data:
          $ref: "#/components/schemas/ManagementOperationsCommandCenterData"

    # ------------------------------------------------------------------
    # Authentication audit
    # ------------------------------------------------------------------
    # ------------------------------------------------------------------
    # Evidence file (CR-BE-API-01 PART 03)
    # ------------------------------------------------------------------
    FindingEvidenceRequest:
      type: object
      required: [evidenceType, fileReference, originalFileName, mimeType, fileSize]
      properties:
        evidenceType: { type: string, enum: [PHOTO, DOCUMENT, SIGNATURE] }
        evidenceRequirementId: { type: string, format: uuid, nullable: true }
        fileReference: { type: string }
        originalFileName: { type: string }
        mimeType: { type: string }
        fileSize: { type: integer, minimum: 0, maximum: 52428800 }
        capturedAt: { type: string, format: date-time, nullable: true }
    EvidenceResponse:
      allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { $ref: "#/components/schemas/EvidenceFileMetadata" } } }]
    EvidenceListResponse:
      allOf: [{ $ref: "#/components/schemas/SuccessEnvelope" }, { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/EvidenceFileMetadata" } } } }]
    EvidenceFileMetadata:
      type: object
      description: >
        An evidence submission is also the file metadata record. After an
        upload, `fileReference` holds the backend-generated storage key and
        `originalFileName` / `mimeType` / `fileSize` reflect the stored file.
      required: [id, evidenceType, fileReference, mimeType, fileSize, status]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        evidenceRequirementId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        executionType:
          type: string
          enum: [FORM_INSTANCE, CHECKLIST_EXECUTION, FINDING, FINDING_REWORK, FINDING_VERIFICATION]
        executionId:
          $ref: "#/components/schemas/Uuid"
        evidenceType:
          type: string
          enum: [PHOTO, DOCUMENT, SIGNATURE]
        fileReference:
          type: string
          description: Backend storage key (evidence/<uuid>) after upload.
        originalFileName:
          type: string
          nullable: true
        mimeType:
          type: string
          nullable: true
        fileSize:
          type: integer
          description: Bytes; capped at 52,428,800 (50 MB).
        capturedAt:
          type: string
          format: date-time
          nullable: true
        submittedByUserId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        status:
          type: string
          enum: [ACTIVE, REMOVED]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        # ------------------------------------------------------------
        # CR-BE-DOC-CONTROL-01 — additive integrity metadata (PART 01/02).
        # ------------------------------------------------------------
        contentSha256:
          type: string
          pattern: "^[0-9a-f]{64}$"
          nullable: true
          description: >
            Server-computed SHA-256 (lowercase hex) of the exact stored
            binary bytes, recorded during the authoritative upload flow.
            Null for legacy/unhashed evidence and metadata-contract
            submissions whose bytes never passed through the server — such
            rows are never assigned a fabricated hash. Callers can never
            submit this value; it is always computed server-side.
        contentHashedAt:
          type: string
          format: date-time
          nullable: true
        hashAlgorithm:
          type: string
          enum: [SHA-256]
          nullable: true
        lastIntegrityStatus:
          type: string
          enum: [VERIFIED, MISMATCH, NOT_HASHED, FILE_UNAVAILABLE]
          nullable: true
          description: Outcome of the most recent integrity verification; null when never verified.
        lastIntegrityCheckedAt:
          type: string
          format: date-time
          nullable: true
        # ------------------------------------------------------------
        # CR-BE-DOC-CONTROL-01 — additive retention snapshot (PART 03/04).
        # The snapshot is immutable: later policy edits never change these
        # values on already-governed evidence.
        # ------------------------------------------------------------
        retentionPolicyId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
          description: Governing retention policy identity; null = ungoverned (never purged).
        retentionPolicyCode:
          type: string
          nullable: true
        retentionDaysSnapshot:
          type: integer
          nullable: true
          description: Retention duration frozen at application time.
        retentionAppliedAt:
          type: string
          format: date-time
          nullable: true
        retainedUntil:
          type: string
          format: date-time
          nullable: true
          description: Anchor (capturedAt ?? createdAt) + retentionDaysSnapshot; null = ungoverned.
        retentionState:
          type: string
          enum: [ACTIVE, RETENTION_DUE, PURGED]
          description: >
            Retention lifecycle state. PURGED is a terminal metadata
            tombstone: the stored binary was disposed by retention, the row
            (hash, size, MIME, original file name, snapshot) survives so
            references never dangle. Purged evidence keeps serving metadata
            reads; file content returns 404, and re-upload, integrity
            verification, and hold requests return 400.
        retentionHold:
          type: boolean
          description: A held row is never purged while the hold is set.
        retentionHoldReason:
          type: string
          nullable: true
        purgedAt:
          type: string
          format: date-time
          nullable: true
          description: Set exactly when retentionState is PURGED.

    EvidenceIntegrityVerification:
      type: object
      description: >
        CR-BE-DOC-CONTROL-01 PART 02 — result of a server-side integrity
        verification: the stored binary is re-read through the storage
        abstraction and its SHA-256 recomputed against the persisted
        upload-time hash. Verification never mutates or deletes evidence;
        MISMATCH / FILE_UNAVAILABLE are auditable facts only.
      required: [evidenceId, outcome, checkedAt]
      properties:
        evidenceId:
          $ref: "#/components/schemas/Uuid"
        outcome:
          type: string
          enum: [VERIFIED, MISMATCH, NOT_HASHED, FILE_UNAVAILABLE]
        algorithm:
          type: string
          enum: [SHA-256]
          nullable: true
          description: Null when the evidence is NOT_HASHED.
        expectedSha256:
          type: string
          nullable: true
          description: Authoritative upload-time hash (null when NOT_HASHED).
        computedSha256:
          type: string
          nullable: true
          description: Hash recomputed from the currently stored bytes (null unless readable).
        checkedAt:
          type: string
          format: date-time

    EvidenceRetentionPolicy:
      type: object
      description: >
        CR-BE-DOC-CONTROL-01 PART 03 — Client-owned, optionally
        Building-scoped evidence retention policy over the existing evidence
        vocabularies. Deterministic application precedence scores
        building +4, executionType +2, evidenceType +1 (highest wins); a
        tied top score applies NO policy and records an
        EVIDENCE_RETENTION_AMBIGUOUS operational event. Governed evidence
        snapshots the applied rule immutably; editing a policy later never
        rewrites governed history.
      required: [id, clientId, code, name, retentionDays, status, effectiveFrom]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
          description: Null = whole Client. Building must belong to the Client.
        code:
          type: string
          pattern: "^[A-Z][A-Z0-9_.-]*$"
          description: Unique per Client.
        name:
          type: string
        evidenceType:
          type: string
          enum: [PHOTO, DOCUMENT, SIGNATURE]
          nullable: true
          description: Null = applies to any evidence type.
        executionType:
          type: string
          enum: [FORM_INSTANCE, CHECKLIST_EXECUTION, WORK_ORDER, VENDOR_WORK, UTILITY_METER_READING, PERMIT, FINDING, FINDING_REWORK, FINDING_VERIFICATION]
          nullable: true
          description: Null = applies to any execution (parent) kind.
        retentionDays:
          type: integer
          minimum: 1
        status:
          type: string
          enum: [ACTIVE, INACTIVE]
        effectiveFrom:
          type: string
          format: date-time
        effectiveTo:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    EvidenceRetentionPolicyCreateRequest:
      type: object
      required: [code, name, retentionDays, effectiveFrom]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string, pattern: "^[A-Z][A-Z0-9_.-]*$", maxLength: 100 }
        name: { type: string, maxLength: 200 }
        evidenceType: { type: string, enum: [PHOTO, DOCUMENT, SIGNATURE] }
        executionType: { type: string, enum: [FORM_INSTANCE, CHECKLIST_EXECUTION, WORK_ORDER, VENDOR_WORK, UTILITY_METER_READING, PERMIT, FINDING, FINDING_REWORK, FINDING_VERIFICATION] }
        retentionDays: { type: integer, minimum: 1 }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time }

    EvidenceRetentionPolicyUpdateRequest:
      type: object
      description: At least one field is required. Applicability fields accept null to widen to "any".
      properties:
        name: { type: string, maxLength: 200 }
        evidenceType: { type: string, enum: [PHOTO, DOCUMENT, SIGNATURE], nullable: true }
        executionType: { type: string, enum: [FORM_INSTANCE, CHECKLIST_EXECUTION, WORK_ORDER, VENDOR_WORK, UTILITY_METER_READING, PERMIT, FINDING, FINDING_REWORK, FINDING_VERIFICATION], nullable: true }
        retentionDays: { type: integer, minimum: 1 }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time, nullable: true }

    AuditEvent:
      type: object
      description: One persisted authentication audit event.
      required:
        [id, eventType, outcome, createdAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        eventType:
          type: string
          description: e.g. LOGIN, LOGOUT, LOGIN_FAILED, INVITATION_ACCEPTED.
        outcome:
          type: string
          enum: [SUCCESS, FAILURE]
        userId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        actorUserId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        sessionId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        requestId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        ipAddress:
          type: string
          nullable: true
        userAgent:
          type: string
          nullable: true
        metadata:
          type: object
          additionalProperties: true
        createdAt:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # BE-25A — Mobile execution surface schemas. Shapes mirror the
    # backend projections (src/modules/**/*.routes.ts `out()` helpers)
    # and must never invent fields.
    # ------------------------------------------------------------------
    MobilePaginationMeta:
      type: object
      description: >
        Pagination metadata emitted in `meta` for the shared page/pageSize
        shape. Mobile list endpoints may opt in under BE-25A; the enterprise
        operational-audit list uses this same shape with bounded pagination by
        default.
      required: [page, pageSize, total, totalPages]
      properties:
        page:
          type: integer
          description: 1-based page number requested.
        pageSize:
          type: integer
          description: Page size applied.
        total:
          type: integer
          description: Total number of items matching the filters.
        totalPages:
          type: integer
          description: Total number of pages (0 when total is 0).
    Task:
      type: object
      description: A generated task (generated_tasks projection).
      required: [id, clientId, occurrenceAt, targetType, targetId, status]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        scheduleDefinitionId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        occurrenceAt:
          type: string
          format: date-time
        targetType:
          type: string
        targetId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        status:
          type: string
          description: Task lifecycle status.
        startedAt:
          type: string
          format: date-time
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
        completedByUserId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        completionNotes:
          type: string
          nullable: true
        generatedAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    TaskAssignment:
      type: object
      description: A task assignment joined with its generated task.
      required: [id, taskId, assigneeType, status]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        taskId:
          $ref: "#/components/schemas/Uuid"
        assigneeType:
          type: string
          enum: [WORKFORCE, TEAM]
        workforceProfileId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        teamId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        assignedByUserId:
          $ref: "#/components/schemas/Uuid"
        assignedAt:
          type: string
          format: date-time
        status:
          type: string
          description: Assignment status (ACTIVE/INACTIVE).
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # CR-BE-MOB-CONTRACT-01 PART 03 — Work Order read schemas.
    # Shapes mirror the BE-08 public DTOs exactly.
    # ------------------------------------------------------------------
    WorkOrderStatus:
      type: string
      enum: [OPEN, ASSIGNED, IN_PROGRESS, ON_HOLD, COMPLETED, CANCELLED, CLOSED]

    WorkOrderPriority:
      type: string
      enum: [LOW, MEDIUM, HIGH, CRITICAL]

    WorkOrder:
      type: object
      description: >
        The authoritative Work Order record (BE-08). `status` is the backend
        lifecycle authority; clients must not infer transitions from it —
        per-caller actions come from `GET /mobile/assignments`. `assetId` /
        `functionalLocationId` are the optional BE-08D bindings (Location-only
        when null; a full resolved context is `GET /work-orders/:id/context`).
      required:
        [id, clientId, buildingId, workOrderNumber, workRequestId, title,
         description, workType, priority, status, bastRequirement,
         createdByUserId, assetId, functionalLocationId, assignedAt, startedAt,
         completedAt, completedByUserId, completionSummary, completionNotes,
         closedAt, cancelledAt, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        workOrderNumber: { type: string }
        workRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        title: { type: string }
        description: { type: string, nullable: true }
        workType: { type: string }
        priority: { $ref: "#/components/schemas/WorkOrderPriority" }
        status: { $ref: "#/components/schemas/WorkOrderStatus" }
        bastRequirement: { $ref: "#/components/schemas/BastRequirement" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        assetId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        functionalLocationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        assignedAt: { type: string, format: date-time, nullable: true }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        completedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        completionSummary: { type: string, nullable: true }
        completionNotes: { type: string, nullable: true }
        closedAt: { type: string, format: date-time, nullable: true }
        cancelledAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    WorkOrderAssigneeType:
      type: string
      enum: [WORKFORCE, TEAM, VENDOR, VENDOR_WORKFORCE]

    WorkOrderAssignmentStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    WorkOrderAssignment:
      type: object
      description: >
        A Work Order assignment (BE-08E). Exactly one ACTIVE assignment exists
        per Work Order; deactivation sets `status: INACTIVE` (never a delete).
        Exactly one of workforceProfileId / teamId / vendorId is populated for
        the assigneeType.
      required:
        [id, workOrderId, assigneeType, workforceProfileId, teamId, vendorId,
         assignedByUserId, assignedAt, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        assigneeType: { $ref: "#/components/schemas/WorkOrderAssigneeType" }
        workforceProfileId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        teamId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        vendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        assignedByUserId: { $ref: "#/components/schemas/Uuid" }
        assignedAt: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/WorkOrderAssignmentStatus" }

    WorkOrderActionType:
      type: string
      enum: [ACKNOWLEDGED, STARTED, ON_HOLD, RESUMED, NOTE_ADDED, CANCELLED]

    WorkOrderAction:
      type: object
      description: >
        An append-only Work Order execution action record (BE-08F). History,
        NOT an available-actions projection.
      required: [id, workOrderId, actionType, actorUserId, notes, occurredAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        actionType: { $ref: "#/components/schemas/WorkOrderActionType" }
        actorUserId: { $ref: "#/components/schemas/Uuid" }
        notes: { type: string, nullable: true }
        occurredAt: { type: string, format: date-time }

    WorkOrderContext:
      type: object
      description: >
        The authoritative resolved Work Order context (BE-08D): bound Asset,
        Functional Location and the projected operational hierarchy (BE-04H).
        Never stored — resolved on read.
      required:
        [workOrderId, clientId, buildingId, asset, functionalLocation,
         operationalContext]
      properties:
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        asset:
          allOf: [{ $ref: "#/components/schemas/WorkOrderContextAsset" }]
          nullable: true
        functionalLocation:
          allOf: [{ $ref: "#/components/schemas/WorkOrderContextFunctionalLocation" }]
          nullable: true
        operationalContext:
          type: object
          description: Projected Client → Property → Building hierarchy (BE-04H).
          nullable: true

    WorkOrderContextAsset:
      type: object
      required: [id, assetCode, assetName, status, functionalLocationId]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        assetCode: { type: string }
        assetName: { type: string }
        status: { type: string }
        functionalLocationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true

    WorkOrderContextFunctionalLocation:
      type: object
      required: [id, code, name, status, spaceId]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        status: { type: string }
        spaceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true

    WorkOrderCompletion:
      type: object
      description: The Work Order completion subset (BE-08H).
      required:
        [workOrderId, status, completedAt, completedByUserId,
         completionSummary, completionNotes]
      properties:
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        status: { type: string }
        completedAt: { type: string, format: date-time, nullable: true }
        completedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        completionSummary: { type: string, nullable: true }
        completionNotes: { type: string, nullable: true }

    # ------------------------------------------------------------------
    # CR-BE-MOB-CONTRACT-01 PART 07 — Work Order execution/evidence/
    # verification/history + Entitlement + Asset sub-resource schemas.
    # ------------------------------------------------------------------
    AssignTaskRequest:
      type: object
      required: [assigneeType]
      properties:
        assigneeType:
          type: string
          enum: [WORKFORCE, TEAM]
        workforceProfileId: { $ref: "#/components/schemas/Uuid" }
        teamId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }

    CreateWorkOrderRequest:
      type: object
      required: [clientId, workOrderNumber, title, workType]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        workOrderNumber: { type: string }
        title: { type: string }
        description: { type: string, nullable: true }
        workType: { type: string }

    UpdateWorkOrderRequest:
      type: object
      minProperties: 1
      properties:
        title: { type: string }
        description: { type: string, nullable: true }
        workType: { type: string }

    UpdateWorkOrderContextRequest:
      type: object
      properties:
        assetId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        functionalLocationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true

    AssignWorkOrderRequest:
      type: object
      required: [assigneeType]
      properties:
        assigneeType: { $ref: "#/components/schemas/WorkOrderAssigneeType" }
        workforceProfileId: { $ref: "#/components/schemas/Uuid" }
        teamId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }

    DeactivateWorkOrderAssignmentRequest:
      type: object
      required: [status]
      properties:
        status: { $ref: "#/components/schemas/WorkOrderAssignmentStatus" }

    WorkOrderActionRequest:
      type: object
      properties:
        notes: { type: string, nullable: true }

    CompleteWorkOrderRequest:
      type: object
      properties:
        completionSummary: { type: string, nullable: true }
        completionNotes: { type: string, nullable: true }

    WorkOrderEvidenceRequirement:
      type: object
      description: A BE-07 evidence requirement bound to a Work Order.
      required:
        [id, clientId, workOrderId, evidenceType, required, minimumCount,
         maximumCount, description, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        evidenceType:
          type: string
          enum: [PHOTO, DOCUMENT, SIGNATURE]
        required: { type: boolean }
        minimumCount: { type: integer }
        maximumCount: { type: integer, nullable: true }
        description: { type: string, nullable: true }
        status: { type: string }

    WorkOrderEvidence:
      type: object
      description: A BE-07 evidence submission bound to a Work Order.
      required:
        [id, clientId, workOrderId, evidenceRequirementId, evidenceType,
         fileReference, originalFileName, mimeType, fileSize, capturedAt,
         submittedByUserId, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        evidenceRequirementId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        evidenceType:
          type: string
          enum: [PHOTO, DOCUMENT, SIGNATURE]
        fileReference: { type: string }
        originalFileName: { type: string }
        mimeType: { type: string }
        fileSize: { type: integer }
        capturedAt: { type: string, format: date-time, nullable: true }
        submittedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        status: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    SubmitWorkOrderEvidenceRequest:
      type: object
      required: [evidenceType, fileReference, originalFileName, mimeType, fileSize]
      properties:
        evidenceType:
          type: string
          enum: [PHOTO, DOCUMENT, SIGNATURE]
        evidenceRequirementId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        fileReference: { type: string }
        originalFileName: { type: string }
        mimeType: { type: string }
        fileSize: { type: integer }
        capturedAt: { type: string, format: date-time, nullable: true }

    WorkOrderVerification:
      type: object
      description: A BE-07 review used as Work Order verification.
      required: [id, clientId, workOrderId, reviewerUserId, decision, notes, reviewedAt, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        reviewerUserId: { $ref: "#/components/schemas/Uuid" }
        decision: { $ref: "#/components/schemas/ReviewDecision" }
        notes: { type: string, nullable: true }
        reviewedAt: { type: string, format: date-time }
        status: { type: string }

    WorkOrderVerificationState:
      type: object
      description: The resolved Work Order verification state.
      required: [workOrderId, status, latestVerification, verifications]
      properties:
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        status: { type: string }
        latestVerification:
          allOf: [{ $ref: "#/components/schemas/WorkOrderVerification" }]
          nullable: true
        verifications:
          type: array
          items: { $ref: "#/components/schemas/WorkOrderVerification" }

    SubmitWorkOrderVerificationRequest:
      type: object
      required: [decision]
      properties:
        decision: { $ref: "#/components/schemas/ReviewDecision" }
        notes: { type: string, nullable: true }

    WorkOrderHistoryEvent:
      type: object
      description: A Work Order history event (BE-07 operational event).
      required:
        [id, workOrderId, clientId, buildingId, eventType, actorUserId,
         summary, metadata, occurredAt, createdAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        eventType: { type: string }
        actorUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        summary: { type: string }
        metadata: { type: object, additionalProperties: true }
        occurredAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }

    EntitlementStatus:
      type: string
      enum: [ACTIVE, SUSPENDED, EXPIRED, REVOKED]

    Entitlement:
      type: object
      description: A module entitlement (BE-02C).
      required: [id, subscriptionId, moduleId, status, startsAt, endsAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        subscriptionId: { $ref: "#/components/schemas/Uuid" }
        moduleId: { $ref: "#/components/schemas/Uuid" }
        status: { $ref: "#/components/schemas/EntitlementStatus" }
        startsAt: { type: string, format: date-time }
        endsAt: { type: string, format: date-time, nullable: true }

    AssetIdentifierType:
      type: string
      enum: [QR, TAG, BARCODE, LEGACY]

    AssetIdentifierStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    AssetIdentifier:
      type: object
      required: [id, assetId, identifierType, identifierValue, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        identifierType: { $ref: "#/components/schemas/AssetIdentifierType" }
        identifierValue: { type: string }
        status: { $ref: "#/components/schemas/AssetIdentifierStatus" }

    AssetHistoryEvent:
      type: object
      description: One asset history event (BE-05I).
      required: [id, assetId, eventType, actorUserId, summary, metadata, createdAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        eventType: { type: string }
        actorUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        summary: { type: string }
        metadata: { type: object, additionalProperties: true }
        createdAt: { type: string, format: date-time }

    AssetWarranty:
      type: object
      description: An asset warranty (BE-05F).
      required:
        [id, assetId, providerName, warrantyNumber, startDate, endDate,
         coverageDescription, status, isCurrentlyCovered]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        providerName: { type: string }
        warrantyNumber: { type: string }
        startDate: { type: string, format: date }
        endDate: { type: string, format: date }
        coverageDescription: { type: string, nullable: true }
        status: { type: string }
        isCurrentlyCovered: { type: boolean }

    AssetCertification:
      type: object
      description: An asset certification (BE-05G).
      required:
        [id, assetId, certificationType, certificateNumber, issuingAuthority,
         issueDate, expiryDate, status, notes, isCurrentlyEffective]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        certificationType: { type: string }
        certificateNumber: { type: string }
        issuingAuthority: { type: string }
        issueDate: { type: string, format: date }
        expiryDate: { type: string, format: date, nullable: true }
        status: { type: string }
        notes: { type: string, nullable: true }
        isCurrentlyEffective: { type: boolean }

    ChecklistExecution:
      type: object
      description: A checklist execution (checklist_executions projection).
      required: [id, clientId, checklistTemplateId, status]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        checklistTemplateId:
          $ref: "#/components/schemas/Uuid"
        status:
          type: string
          enum: [DRAFT, IN_PROGRESS, COMPLETED, CANCELLED]
        startedAt:
          type: string
          format: date-time
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
        completedByUserId:
          description: >
            Authenticated user who completed the execution (R06 PART 01
            completion actor). Null while not completed and for historical
            executions predating completion-actor attribution.
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        assigneeType:
          description: >
            Assignment snapshot captured at task-bound execution creation
            (R06 PART 02). WORKFORCE or TEAM when assigned; null when unassigned
            or the execution is not task-bound. Records WHO/WHAT WAS ASSIGNED —
            never an executor.
          type: string
          enum: [WORKFORCE, TEAM]
          nullable: true
        assignedWorkforceProfileId:
          description: >
            Workforce profile the task was assigned to at execution creation.
            Non-null only when assigneeType is WORKFORCE.
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        assignedTeamId:
          description: >
            Team the task was assigned to at execution creation. Non-null only
            when assigneeType is TEAM.
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        assignmentSnapshotAt:
          description: >
            Timestamp when the assignment facts were captured onto the
            execution (snapshot time, not assignment creation time).
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    ChecklistItemResponse:
      type: object
      description: >
        A saved checklist item response (raw joined projection:
        checklist_item_responses × checklist_items). When `is_na` is true,
        `value` and `result` are SQL NULL (JSON `null`) and `na_notes` may
        carry the explicit N/A reason.
      required: [id, checklist_execution_id, checklist_item_id, code, item_type, is_na]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        checklist_execution_id:
          $ref: "#/components/schemas/Uuid"
        checklist_item_id:
          $ref: "#/components/schemas/Uuid"
        value:
          description: Item value (boolean / number / string per item type). Null when is_na=true.
          nullable: true
        result:
          type: string
          nullable: true
        notes:
          type: string
          nullable: true
        is_na:
          type: boolean
          description: True when this response is an explicit N/A state.
        na_notes:
          type: string
          nullable: true
          description: N/A reason; populated only when is_na=true and a reason was recorded.
        last_responded_by_user_id:
          description: >
            Latest response writer (R06 PART 03B). Authenticated user whose
            successful response mutation most recently wrote this row. Null for
            historical rows predating response-actor attribution. Latest writer
            only — not the first responder and not full edit history.
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        code:
          type: string
          description: Checklist item code.
        item_type:
          type: string
          description: Checklist item type.
        required:
          type: boolean
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # CR-BE-MOB-CONTRACT-01 PART 04 — Checklist / Measurement / UOM /
    # Evidence Requirement schemas (mirror the BE-07 DTOs).
    # ------------------------------------------------------------------
    ChecklistResponsesRequest:
      type: object
      description: >
        One or more checklist item responses to save. May be a single object
        or an array of objects. Ordinary responses require `value` matching
        the item's `itemType` (CHECK/BOOLEAN → boolean, NUMBER → number,
        TEXT → string). Explicit N/A responses set `isNa: true` and MUST
        omit/null `value` and `result`; `naNotes` may be supplied and is
        required when the item's `naRequiresNote` policy is enabled.
      required: [itemId]
      properties:
        itemId:
          $ref: "#/components/schemas/Uuid"
        value:
          description: boolean / number / string per the item type. Must be null/absent when isNa=true.
          nullable: true
        isNa:
          type: boolean
          default: false
          description: When true, marks this entry as an explicit N/A response.
        naNotes:
          type: string
          nullable: true
          maxLength: 2048
          description: N/A reason; required when the item's naRequiresNote policy is enabled.
        result:
          type: string
          nullable: true
        notes:
          type: string
          nullable: true

    ChecklistTemplate:
      type: object
      required: [id, clientId, code, name, description, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/ChecklistTemplateStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    ChecklistTemplateStatus:
      type: string
      enum: [DRAFT, ACTIVE, INACTIVE]

    CreateChecklistTemplateRequest:
      type: object
      required: [code, name]
      properties:
        code: { type: string, description: Normalized to uppercase. }
        name: { type: string }
        description: { type: string }
        status: { $ref: "#/components/schemas/ChecklistTemplateStatus" }

    UpdateChecklistTemplateRequest:
      type: object
      minProperties: 1
      properties:
        name: { type: string }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/ChecklistTemplateStatus" }

    ChecklistItem:
      type: object
      required:
        [id, checklistTemplateId, code, label, itemType, required, displayOrder,
         status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        label: { type: string }
        itemType: { $ref: "#/components/schemas/ChecklistItemType" }
        required: { type: boolean }
        displayOrder: { type: integer }
        status: { $ref: "#/components/schemas/ChecklistItemStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    ChecklistItemType:
      type: string
      enum: [CHECK, BOOLEAN, TEXT, NUMBER, SELECT]

    ChecklistItemStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    CreateChecklistItemRequest:
      type: object
      required: [code, label, itemType, displayOrder]
      properties:
        code: { type: string, description: Normalized to uppercase. }
        label: { type: string }
        itemType: { $ref: "#/components/schemas/ChecklistItemType" }
        required: { type: boolean }
        displayOrder: { type: integer }
        status: { $ref: "#/components/schemas/ChecklistItemStatus" }

    UpdateChecklistItemRequest:
      type: object
      minProperties: 1
      properties:
        label: { type: string }
        required: { type: boolean }
        displayOrder: { type: integer }
        itemType: { $ref: "#/components/schemas/ChecklistItemType" }
        status: { $ref: "#/components/schemas/ChecklistItemStatus" }

    Uom:
      type: object
      required: [id, clientId, code, name, symbol, category, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        symbol: { type: string }
        category: { type: string }
        status: { $ref: "#/components/schemas/UomStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    UomStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    CreateUomRequest:
      type: object
      required: [code, name, symbol, category]
      properties:
        code: { type: string, description: Normalized to uppercase. }
        name: { type: string }
        symbol: { type: string }
        category: { type: string }

    UpdateUomRequest:
      type: object
      minProperties: 1
      properties:
        name: { type: string }
        symbol: { type: string }
        category: { type: string }
        status: { $ref: "#/components/schemas/UomStatus" }

    BindMeasurementRequest:
      type: object
      required: [uomId]
      properties:
        uomId: { $ref: "#/components/schemas/Uuid" }
        minimumValue: { type: number, nullable: true }
        maximumValue: { type: number, nullable: true }
        decimalPrecision: { type: integer, nullable: true }

    MeasurementBoundItem:
      type: object
      description: >
        The definition row (checklist item / form field) after a measurement
        binding, including the bound `uomId` and min/max/decimal precision.
      required: [id, uomId, minimumValue, maximumValue, decimalPrecision]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        uomId: { $ref: "#/components/schemas/Uuid" }
        minimumValue: { type: number, nullable: true }
        maximumValue: { type: number, nullable: true }
        decimalPrecision: { type: integer, nullable: true }

    EvidenceRequirementTargetType:
      type: string
      enum:
        [FORM_TEMPLATE, FORM_VERSION, FORM_SECTION, FORM_FIELD,
         CHECKLIST_TEMPLATE, CHECKLIST_ITEM]

    EvidenceRequirement:
      type: object
      required:
        [id, clientId, targetType, targetId, evidenceType, required,
         minimumCount, maximumCount, description, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        targetType: { $ref: "#/components/schemas/EvidenceRequirementTargetType" }
        targetId: { $ref: "#/components/schemas/Uuid" }
        evidenceType:
          type: string
          enum: [PHOTO, DOCUMENT, SIGNATURE]
        required: { type: boolean }
        minimumCount: { type: integer }
        maximumCount: { type: integer, nullable: true }
        description: { type: string, nullable: true }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateEvidenceRequirementRequest:
      type: object
      required: [targetType, targetId, evidenceType]
      properties:
        targetType: { $ref: "#/components/schemas/EvidenceRequirementTargetType" }
        targetId: { $ref: "#/components/schemas/Uuid" }
        evidenceType:
          type: string
          enum: [PHOTO, DOCUMENT, SIGNATURE]
        required: { type: boolean, description: Defaults to true. }
        minimumCount: { type: integer, description: Defaults to 0. }
        maximumCount: { type: integer, nullable: true }
        description: { type: string, nullable: true }

    UpdateEvidenceRequirementRequest:
      type: object
      minProperties: 1
      properties:
        required: { type: boolean }
        minimumCount: { type: integer }
        maximumCount: { type: integer, nullable: true }
        description: { type: string, nullable: true }
        status: { type: string, enum: [ACTIVE, INACTIVE] }

    SubmitEvidenceRequest:
      type: object
      required: [evidenceType, executionType, executionId, fileReference, originalFileName, mimeType, fileSize]
      properties:
        evidenceType:
          type: string
          enum: [PHOTO, DOCUMENT, SIGNATURE]
        executionType:
          type: string
          enum: [FORM_INSTANCE, CHECKLIST_EXECUTION, FINDING, FINDING_REWORK, FINDING_VERIFICATION]
          description: >-
            The authoritative parent kind. The exact upload permission is
            derived from it (MOB-C06 PART 01A): evidence.manage for
            FORM_INSTANCE / CHECKLIST_EXECUTION / FINDING / FINDING_REWORK;
            finding.review for FINDING_VERIFICATION.
        executionId: { $ref: "#/components/schemas/Uuid" }
        evidenceRequirementId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        fileReference:
          type: string
          description: Backend storage key reference (e.g. evidence/<uuid>).
        originalFileName: { type: string }
        mimeType: { type: string }
        fileSize:
          type: integer
          description: Bytes; ≤ 52,428,800 (50 MB).
        capturedAt:
          type: string
          format: date-time
          nullable: true

    Review:
      type: object
      description: A review of a form instance or checklist execution.
      required: [id, clientId, targetType, targetId, reviewerUserId, status]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        targetType:
          type: string
          enum: [FORM_INSTANCE, CHECKLIST_EXECUTION]
        targetId:
          $ref: "#/components/schemas/Uuid"
        reviewerUserId:
          $ref: "#/components/schemas/Uuid"
        decision:
          type: string
          nullable: true
        notes:
          type: string
          nullable: true
        reviewedAt:
          type: string
          format: date-time
          nullable: true
        status:
          type: string
          description: Review status (OPEN/COMPLETED).
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    OperationalEventSource:
      type: string
      description: Execution source persisted on new operational events; null is retained for historical rows.
      enum: [HTTP, SCHEDULER, SYSTEM]
    OperationalEvent:
      type: object
      description: >
        A scoped append-only operational audit event. `requestId` is the
        server-generated HTTP request UUID or governed background-run UUID;
        historical rows may return null. `source` identifies the execution
        boundary and is null only for historical/standalone rows without a
        governed context. Metadata is scrubbed and bounded; raw payloads,
        credentials, and provider secrets are never exposed.
      required: [id, clientId, eventType, entityType, entityId, requestId, source, occurredAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        eventType:
          type: string
        entityType:
          type: string
        entityId:
          $ref: "#/components/schemas/Uuid"
        actorUserId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        requestId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
          description: Server-generated request or governed background-run correlation UUID; null for historical/standalone rows.
        source:
          allOf:
            - $ref: "#/components/schemas/OperationalEventSource"
          nullable: true
        summary:
          type: string
          nullable: true
        metadata:
          type: object
          additionalProperties: true
          description: Scrubbed, bounded metadata only; secrets and raw payloads are omitted.
        occurredAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
    FindingAvailableActions:
      type: object
      description: >
        Backend-authoritative available actions for a finding. Mobile renders
        only these actions and never infers workflow authority.
      required: [findingId, state, availableActions]
      properties:
        findingId:
          $ref: "#/components/schemas/Uuid"
        state:
          type: string
          description: Current finding workflow state.
        availableActions:
          type: array
          items:
            type: string
          description: >
            Actions the calling user may perform now (ASSIGN, MARK_ASSIGNED,
            START, SUBMIT_FOR_REVIEW, OPEN_REVIEW, APPROVE, REJECT,
            REQUEST_REWORK, RESUBMIT, CLOSE, CANCEL).
    FindingState:
      type: object
      description: The current finding workflow state.
      required: [findingId, state, stateChangedAt]
      properties:
        findingId:
          $ref: "#/components/schemas/Uuid"
        state:
          type: string
        stateChangedAt:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # CR-BE-MOB-CONTRACT-01 PART 05 — Finding / Rework / Verification.
    # Shapes mirror the BE-09 DTOs exactly.
    # ------------------------------------------------------------------
    FindingStatus:
      type: string
      enum:
        [OPEN, ASSIGNED, IN_PROGRESS, PENDING_REVIEW, REJECTED,
         REWORK_REQUIRED, RESUBMITTED, VERIFIED, CLOSED, CANCELLED]

    FindingSourceType:
      type: string
      enum: [FORM_INSTANCE, CHECKLIST_EXECUTION, WORK_ORDER]

    Finding:
      type: object
      description: >
        The authoritative Finding record (BE-09). `status` is the backend
        lifecycle authority; clients must not infer transitions from it — the
        per-caller `availableActions` comes from
        `GET /findings/:id/available-actions`. `sourceType`/`sourceId` bind the
        finding to a checklist execution, form instance, or work order.
      required:
        [id, clientId, buildingId, findingNumber, title, description,
         classificationId, severityId, sourceType, sourceId, status,
         stateChangedAt, reportedByUserId, reportedAt, closedAt, closedByUserId,
         closureNotes, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        findingNumber: { type: string }
        title: { type: string }
        description: { type: string, nullable: true }
        classificationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        severityId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        sourceType:
          allOf: [{ $ref: "#/components/schemas/FindingSourceType" }]
          nullable: true
        sourceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        status: { $ref: "#/components/schemas/FindingStatus" }
        stateChangedAt: { type: string, format: date-time }
        reportedByUserId: { $ref: "#/components/schemas/Uuid" }
        reportedAt: { type: string, format: date-time }
        closedAt: { type: string, format: date-time, nullable: true }
        closedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        closureNotes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateFindingRequest:
      type: object
      required: [clientId, findingNumber, title]
      properties:
        clientId:
          $ref: "#/components/schemas/Uuid"
          description: The finding's Client (must match the Building's Client).
        findingNumber:
          type: string
          description: >-
            Normalized uppercase; 2-64 chars of uppercase letters, digits,
            hyphens, underscores.
        title: { type: string }
        description: { type: string, nullable: true }

    UpdateFindingRequest:
      type: object
      minProperties: 1
      properties:
        title: { type: string }
        description: { type: string, nullable: true }
        classificationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        severityId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true

    TransitionFindingStateRequest:
      type: object
      required: [state]
      properties:
        state: { $ref: "#/components/schemas/FindingStatus" }

    FindingSourceState:
      type: object
      description: >
        The finding's source binding and its resolved source context
        (reference type/id/code/title).
      required: [findingId, sourceType, sourceId, context]
      properties:
        findingId: { $ref: "#/components/schemas/Uuid" }
        sourceType:
          allOf: [{ $ref: "#/components/schemas/FindingSourceType" }]
          nullable: true
        sourceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        context:
          type: object
          nullable: true
          properties:
            sourceType: { $ref: "#/components/schemas/FindingSourceType" }
            sourceId: { $ref: "#/components/schemas/Uuid" }
            clientId: { $ref: "#/components/schemas/Uuid" }
            buildingId:
              allOf: [{ $ref: "#/components/schemas/Uuid" }]
              nullable: true
            status: { type: string }
            referenceType: { type: string }
            referenceId: { $ref: "#/components/schemas/Uuid" }
            referenceCode: { type: string, nullable: true }
            title: { type: string, nullable: true }

    UpdateFindingSourceRequest:
      type: object
      properties:
        sourceType:
          allOf: [{ $ref: "#/components/schemas/FindingSourceType" }]
          nullable: true
        sourceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true

    FindingReworkStatus:
      type: string
      enum: [REQUESTED, RESUBMITTED]

    FindingRework:
      type: object
      description: One finding rework cycle.
      required:
        [id, findingId, reviewId, requestedByUserId, reason, reworkNotes,
         resubmittedByUserId, requestedAt, resubmittedAt, status, createdAt,
         updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        findingId: { $ref: "#/components/schemas/Uuid" }
        reviewId: { $ref: "#/components/schemas/Uuid" }
        requestedByUserId: { $ref: "#/components/schemas/Uuid" }
        reason: { type: string }
        reworkNotes: { type: string, nullable: true }
        resubmittedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        requestedAt: { type: string, format: date-time }
        resubmittedAt: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/FindingReworkStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    FindingReworkContext:
      type: object
      description: The finding's rework context (current cycle + all cycles).
      required: [findingId, findingState, current, cycles]
      properties:
        findingId: { $ref: "#/components/schemas/Uuid" }
        findingState: { type: string }
        current:
          allOf: [{ $ref: "#/components/schemas/FindingRework" }]
          nullable: true
        cycles:
          type: array
          items: { $ref: "#/components/schemas/FindingRework" }

    RequestReworkRequest:
      type: object
      required: [reason]
      properties:
        reason:
          type: string
          description: Required rework/rejection reason (≤ 2000 chars).

    UpdateReworkNotesRequest:
      type: object
      required: [notes]
      properties:
        notes: { type: string, description: Required (≤ 2000 chars). }

    FindingReviewStatus:
      type: string
      enum: [PENDING, COMPLETED]

    ReviewDecision:
      type: string
      enum: [APPROVED, REJECTED, REWORK_REQUIRED]

    FindingReview:
      type: object
      description: A finding review / verification record.
      required:
        [id, clientId, findingId, reviewerUserId, decision, notes, reviewedAt,
         status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        findingId: { $ref: "#/components/schemas/Uuid" }
        reviewerUserId: { $ref: "#/components/schemas/Uuid" }
        decision:
          allOf: [{ $ref: "#/components/schemas/ReviewDecision" }]
          nullable: true
        notes: { type: string, nullable: true }
        reviewedAt: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/FindingReviewStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    FindingVerificationState:
      type: object
      description: The finding's supervisor verification state.
      required: [findingId, state, currentReview, latestVerification, reviews]
      properties:
        findingId: { $ref: "#/components/schemas/Uuid" }
        state: { type: string }
        currentReview:
          allOf: [{ $ref: "#/components/schemas/FindingReview" }]
          nullable: true
        latestVerification:
          allOf: [{ $ref: "#/components/schemas/FindingReview" }]
          nullable: true
        reviews:
          type: array
          items: { $ref: "#/components/schemas/FindingReview" }

    OpenFindingReviewRequest:
      type: object
      properties:
        notes: { type: string, nullable: true }

    SubmitFindingVerificationRequest:
      type: object
      required: [decision]
      properties:
        decision: { $ref: "#/components/schemas/ReviewDecision" }
        notes: { type: string, nullable: true }

    FindingAssigneeType:
      type: string
      enum: [WORKFORCE, TEAM, VENDOR, VENDOR_WORKFORCE]

    FindingAssignmentStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    FindingAssignment:
      type: object
      required:
        [id, findingId, assigneeType, workforceProfileId, teamId, vendorId,
         assignedByUserId, assignedAt, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        findingId: { $ref: "#/components/schemas/Uuid" }
        assigneeType: { $ref: "#/components/schemas/FindingAssigneeType" }
        workforceProfileId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        teamId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        vendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        assignedByUserId: { $ref: "#/components/schemas/Uuid" }
        assignedAt: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/FindingAssignmentStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    AssignFindingRequest:
      type: object
      required: [assigneeType]
      properties:
        assigneeType: { $ref: "#/components/schemas/FindingAssigneeType" }
        workforceProfileId: { $ref: "#/components/schemas/Uuid" }
        teamId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }

    DeactivateFindingAssignmentRequest:
      type: object
      required: [status]
      properties:
        status: { $ref: "#/components/schemas/FindingAssignmentStatus" }

    FindingHistoryEvent:
      type: object
      description: One append-only finding audit/history event.
      required:
        [id, findingId, clientId, buildingId, eventType, actorUserId, summary,
         metadata, occurredAt, createdAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        findingId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        eventType: { type: string }
        actorUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        summary: { type: string }
        metadata: { type: object, additionalProperties: true }
        occurredAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }

    FindingClosureInfo:
      type: object
      description: The finding closure readiness + provenance.
      required:
        [findingId, state, ready, latestVerificationId,
         latestVerificationDecision, reworkPending, closedAt, closedByUserId,
         closureNotes]
      properties:
        findingId: { $ref: "#/components/schemas/Uuid" }
        state: { type: string }
        ready: { type: boolean }
        latestVerificationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        latestVerificationDecision:
          allOf: [{ $ref: "#/components/schemas/ReviewDecision" }]
          nullable: true
        reworkPending: { type: boolean }
        closedAt: { type: string, format: date-time, nullable: true }
        closedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        closureNotes: { type: string, nullable: true }

    CloseFindingRequest:
      type: object
      properties:
        closureNotes: { type: string, nullable: true }

    AssetResolution:
      type: object
      description: Resolved asset identifier (QR-ready lookup).
      required: [identifier, asset]
      properties:
        identifier:
          type: object
          required: [id, identifierType, identifierValue]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
            identifierType:
              type: string
            identifierValue:
              type: string
        asset:
          type: object
          required: [id, buildingId, assetCode, assetName, status]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
            buildingId:
              $ref: "#/components/schemas/Uuid"
            assetCode:
              type: string
            assetName:
              type: string
            status:
              type: string
            functionalLocationId:
              allOf:
                - $ref: "#/components/schemas/Uuid"
              nullable: true

    # ------------------------------------------------------------------
    # CR-BE-MOB-CONTRACT-01 PART 06 — Location hierarchy, Asset/Equipment,
    # and operational context schemas. Shapes mirror the BE-04 / BE-05 DTOs
    # and the BE-04H read projections exactly.
    # ------------------------------------------------------------------
    StructureNode:
      type: object
      description: Minimal authoritative summary of one structure node.
      required: [id, code, name, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        status: { type: string }

    FloorNode:
      allOf:
        - $ref: "#/components/schemas/StructureNode"
        - type: object
          required: [levelNumber]
          properties:
            levelNumber: { type: integer }

    AreaNode:
      allOf:
        - $ref: "#/components/schemas/StructureNode"
        - type: object
          required: [type]
          properties:
            type: { $ref: "#/components/schemas/AreaType" }

    RoomNode:
      allOf:
        - $ref: "#/components/schemas/StructureNode"
        - type: object
          required: [roomType]
          properties:
            roomType:
              allOf: [{ $ref: "#/components/schemas/StructureNode" }]
              nullable: true

    FunctionalLocationNode:
      allOf:
        - $ref: "#/components/schemas/StructureNode"
        - type: object
          required: [spaceId]
          properties:
            spaceId:
              allOf: [{ $ref: "#/components/schemas/Uuid" }]
              nullable: true

    OperationalContext:
      type: object
      description: >
        The authoritative operational location context for one node. Only the
        levels that actually exist are present — missing hierarchy levels are
        never invented. `campus` appears only when the Building is grouped
        under one.
      required: [client, property, building]
      properties:
        client: { $ref: "#/components/schemas/StructureNode" }
        property: { $ref: "#/components/schemas/StructureNode" }
        campus: { $ref: "#/components/schemas/StructureNode" }
        building: { $ref: "#/components/schemas/StructureNode" }
        floor: { $ref: "#/components/schemas/FloorNode" }
        area: { $ref: "#/components/schemas/AreaNode" }
        room: { $ref: "#/components/schemas/RoomNode" }
        roomType: { $ref: "#/components/schemas/StructureNode" }
        space: { $ref: "#/components/schemas/StructureNode" }
        functionalLocation: { $ref: "#/components/schemas/FunctionalLocationNode" }

    SpaceHierarchy:
      allOf:
        - $ref: "#/components/schemas/StructureNode"
        - type: object
          required: [functionalLocations]
          properties:
            functionalLocations:
              type: array
              items: { $ref: "#/components/schemas/FunctionalLocationNode" }

    RoomHierarchy:
      allOf:
        - $ref: "#/components/schemas/RoomNode"
        - type: object
          required: [spaces]
          properties:
            spaces:
              type: array
              items: { $ref: "#/components/schemas/SpaceHierarchy" }

    AreaHierarchy:
      allOf:
        - $ref: "#/components/schemas/AreaNode"
        - type: object
          required: [rooms]
          properties:
            rooms:
              type: array
              items: { $ref: "#/components/schemas/RoomHierarchy" }

    FloorHierarchy:
      allOf:
        - $ref: "#/components/schemas/FloorNode"
        - type: object
          required: [areas]
          properties:
            areas:
              type: array
              items: { $ref: "#/components/schemas/AreaHierarchy" }

    BuildingHierarchy:
      type: object
      description: >
        The full authoritative digital structure of one Building (BE-04H):
        floors → areas → rooms (with Room Type) → spaces (with pinned
        Functional Locations), plus Building-level Functional Locations.
      required: [client, property, building, floors, functionalLocations]
      properties:
        client: { $ref: "#/components/schemas/StructureNode" }
        property: { $ref: "#/components/schemas/StructureNode" }
        campus: { $ref: "#/components/schemas/StructureNode" }
        building: { $ref: "#/components/schemas/StructureNode" }
        floors:
          type: array
          items: { $ref: "#/components/schemas/FloorHierarchy" }
        functionalLocations:
          type: array
          items: { $ref: "#/components/schemas/FunctionalLocationNode" }

    FloorStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    PublicFloor:
      type: object
      required: [id, buildingId, code, name, levelNumber, description, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        levelNumber: { type: integer }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/FloorStatus" }

    AreaType:
      type: string
      enum: [AREA, ZONE]

    AreaStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    PublicArea:
      type: object
      required: [id, floorId, code, name, type, description, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        floorId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        type: { $ref: "#/components/schemas/AreaType" }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/AreaStatus" }

    RoomStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    PublicRoom:
      type: object
      required: [id, areaId, roomTypeId, code, name, description, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        areaId: { $ref: "#/components/schemas/Uuid" }
        roomTypeId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/RoomStatus" }

    SpaceStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    PublicSpace:
      type: object
      required: [id, roomId, code, name, description, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        roomId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/SpaceStatus" }

    FunctionalLocationStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    PublicFunctionalLocation:
      type: object
      required: [id, buildingId, spaceId, code, name, description, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        spaceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/FunctionalLocationStatus" }

    AssetStatus:
      type: string
      enum: [ACTIVE, INACTIVE, UNDER_MAINTENANCE, RETIRED]

    PublicAsset:
      type: object
      required:
        [id, clientId, buildingId, assetCategoryId, assetTypeId,
         functionalLocationId, assetCode, assetName, description, manufacturer,
         model, serialNumber, status, previousStatus, statusChangedAt,
         statusReason]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        assetCategoryId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        assetTypeId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        functionalLocationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        assetCode: { type: string }
        assetName: { type: string }
        description: { type: string, nullable: true }
        manufacturer: { type: string, nullable: true }
        model: { type: string, nullable: true }
        serialNumber: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/AssetStatus" }
        previousStatus:
          allOf: [{ $ref: "#/components/schemas/AssetStatus" }]
          nullable: true
        statusChangedAt: { type: string, format: date-time, nullable: true }
        statusReason: { type: string, nullable: true }

    AssetLocation:
      type: object
      description: >
        The asset plus its authoritative resolved operational location context
        (BE-05C). `location` is the BE-04H projection, never stored.
      required: [asset, location]
      properties:
        asset: { $ref: "#/components/schemas/PublicAsset" }
        location: { $ref: "#/components/schemas/OperationalContext" }

    EquipmentProfileStatus:
      type: string
      enum: [ACTIVE, INACTIVE]

    PublicEquipmentProfile:
      type: object
      required:
        [id, assetId, equipmentCode, equipmentName, manufacturer, model,
         serialNumber, specification, capacity, unitOfMeasure,
         installationDate, commissioningDate, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        equipmentCode: { type: string }
        equipmentName: { type: string }
        manufacturer: { type: string, nullable: true }
        model: { type: string, nullable: true }
        serialNumber: { type: string, nullable: true }
        specification: { type: string, nullable: true }
        capacity: { type: number, nullable: true }
        unitOfMeasure: { type: string, nullable: true }
        installationDate: { type: string, format: date-time, nullable: true }
        commissioningDate: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/EquipmentProfileStatus" }

    # ------------------------------------------------------------------
    # BE-25C — Mobile Assignment Contract. Feed composition over the
    # authoritative BE-07 Task / BE-08 Work Order assignment tables; no
    # separate mobile assignment engine.
    # ------------------------------------------------------------------
    MobileAssignment:
      type: object
      description: >
        One mobile assignment feed item (discriminated by `type`). `id` is
        the assignment record id; `status` is the current authoritative work
        status; `availableActions` is backend-authoritative and gated on the
        caller's manage permissions.
      required:
        [id, type, status, assignee, context, schedule, reference, availableActions, shift]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
          description: Assignment record id (task_assignments / work_order_assignments).
        type:
          type: string
          enum: [TASK, WORK_ORDER]
        status:
          type: string
          description: Current authoritative status of the work itself.
        assignee:
          $ref: "#/components/schemas/MobileAssignmentAssignee"
        context:
          $ref: "#/components/schemas/MobileAssignmentContext"
        schedule:
          $ref: "#/components/schemas/MobileAssignmentSchedule"
        reference:
          $ref: "#/components/schemas/MobileAssignmentReference"
        availableActions:
          type: array
          description: >
            Actions the calling user may perform now (TASK: START/COMPLETE/
            CANCEL; WORK_ORDER: ACKNOWLEDGE/START/HOLD/RESUME/ADD_NOTE/CANCEL,
            plus CLOSE when completion, approval, and BAST readiness permit).
            Empty when the caller lacks the matching manage permission or the
            work is terminal.
          items:
            type: string
        shift:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/MobileAssignmentShift"
          description: >-
            MOB-C04 PART 01 — Shift context of this work item. Non-null only
            when the item's Building is the Building the authenticated worker
            is currently on shift at, resolved by reusing the authoritative
            `/mobile/current-shift` derivation (BE-03E roster + BE-02F/G
            Building scope + Building-timezone wall-clock) — never a
            client-computed "active shift" flag. Null when the work's Building
            has no current shift (worker not on shift there, no Building, or
            no valid timezone). A null shift never removes the item from the
            feed: scope isolation (BE-02G) is the only authority that filters
            work.
    MobileAssignmentShift:
      type: object
      description: >-
        MOB-C04 PART 01 — Minimal, execution-oriented shift context attached to
        one feed item when the assigned work's Building is the Building the
        worker is currently on shift at. `assignmentId` is the authoritative
        `workforce_shift_assignments.id`; `shiftId` is the authoritative
        `shifts.id`. Reuses the MOB-C03 PART 03B backend-assigned Security Post
        summary (nullable; does not grant authorization by itself).
      required:
        [assignmentId, shiftId, code, name, startTime, endTime, securityPost]
      properties:
        assignmentId:
          $ref: "#/components/schemas/Uuid"
          description: workforce_shift_assignments.id — the authoritative roster row.
        shiftId:
          $ref: "#/components/schemas/Uuid"
          description: shifts.id — the authoritative Shift id.
        code: { type: string }
        name: { type: string }
        startTime:
          type: string
          description: Wall-clock HH:MM:SS in the Building timezone (overnight shifts valid).
        endTime:
          type: string
          description: >-
            Wall-clock HH:MM:SS in the Building timezone. An `endTime` earlier
            than `startTime` is an overnight window; the Building IANA timezone
            is the authority that interprets these wall-clock times.
        securityPost:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/MobileSecurityPostSummary"
          description: >-
            Backend-assigned Security Post for this roster shift. Null means no
            valid assigned Security Post. Does not grant authorization by itself
            and is not attendance/patrol/handover state.
    MobileAssignmentAssignee:
      type: object
      description: Assignee / user / workforce context of the assignment.
      required:
        [assigneeType, workforceProfileId, teamId, assignedByUserId, assignedAt, assignmentStatus]
      properties:
        assigneeType:
          type: string
          enum: [WORKFORCE, TEAM]
        workforceProfileId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        teamId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        assignedByUserId:
          $ref: "#/components/schemas/Uuid"
        assignedAt:
          type: string
          format: date-time
        assignmentStatus:
          type: string
          const: ACTIVE
    MobileAssignmentContext:
      type: object
      description: Building / Location context of the assigned work.
      required: [clientId, buildingId, buildingCode, buildingName, locations]
      properties:
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        buildingCode:
          type: string
          nullable: true
        buildingName:
          type: string
          nullable: true
        locations:
          type: array
          description: Asset / functional-location references bound to the work.
          items:
            $ref: "#/components/schemas/MobileAssignmentLocation"
    MobileAssignmentLocation:
      type: object
      required: [type, id, code, name]
      properties:
        type:
          type: string
          enum: [ASSET, FUNCTIONAL_LOCATION]
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string
    MobileAssignmentSchedule:
      type: object
      description: >
        Due / schedule context. `occurrenceAt` carries the generated task's
        schedule context; `dueAt` is always null today (no authoritative due
        date exists for Work Orders) and stays reserved for future SLA
        configuration.
      required: [occurrenceAt, dueAt]
      properties:
        occurrenceAt:
          type: string
          format: date-time
          nullable: true
        dueAt:
          type: string
          format: date-time
          nullable: true
    MobileAssignmentReference:
      type: object
      description: >
        Type-specific work reference. TASK items populate the task fields;
        WORK_ORDER items populate the work-order fields (never both).
      properties:
        taskId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        scheduleDefinitionId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        targetType:
          type: string
          nullable: true
        targetId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        generatedAt:
          type: string
          format: date-time
          nullable: true
        workOrderId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        workOrderNumber:
          type: string
          nullable: true
        title:
          type: string
          nullable: true
        description:
          type: string
          nullable: true
        workType:
          type: string
          nullable: true
        priority:
          type: string
          nullable: true
        startedAt:
          type: string
          format: date-time
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
        completedByUserId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        completionSummary:
          type: string
          nullable: true
        completionNotes:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
          nullable: true

    # ------------------------------------------------------------------
    # BE-25D — Mobile Checklist Contract. Read-model composition over the
    # BE-07 Checklist Template / Execution / Measurement / Evidence
    # authorities; no separate mobile checklist engine.
    # ------------------------------------------------------------------
    MobileChecklistExecution:
      type: object
      description: >
        The mobile checklist execution contract. `id` is the checklist
        execution id; `status` is the authoritative execution status;
        `availableActions` is backend-authoritative (START / SAVE_RESPONSES /
        COMPLETE / CANCEL, matching the execution endpoints).
      required:
        [id, clientId, checklist, task, status, items, evidenceRequirements, availableActions]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        checklist:
          $ref: "#/components/schemas/MobileChecklistTemplateReference"
        task:
          allOf:
            - $ref: "#/components/schemas/MobileChecklistTaskReference"
          nullable: true
          description: Task reference when the execution's template is a task target; else null.
        status:
          type: string
          enum: [DRAFT, IN_PROGRESS, COMPLETED, CANCELLED]
        startedAt:
          type: string
          format: date-time
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        items:
          type: array
          description: Active template items (ordered) with response values/status.
          items:
            $ref: "#/components/schemas/MobileChecklistItem"
        evidenceRequirements:
          type: array
          description: Evidence requirements bound to the execution's template (template level).
          items:
            $ref: "#/components/schemas/MobileChecklistEvidenceRequirement"
        availableActions:
          type: array
          items:
            type: string
            enum: [START, SAVE_RESPONSES, COMPLETE, CANCEL]
    MobileChecklistExecutionUpdate:
      type: object
      description: >-
        MOB-C04 PART 02 — minimal execution-row response returned by the mobile
        online checklist execution commands. Mirrors the generic REST execution
        endpoint response shape.
      required:
        [id, clientId, checklistTemplateId, status, startedAt, completedAt, createdAt, updatedAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        checklistTemplateId:
          $ref: "#/components/schemas/Uuid"
        status:
          type: string
          enum: [DRAFT, IN_PROGRESS, COMPLETED, CANCELLED]
        startedAt:
          type: string
          format: date-time
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    MobileChecklistOpenReference:
      type: object
      description: >-
        MOB-C04 PART 02B — the minimum execution reference returned when a
        checklist execution is opened from an assigned generated task. `status`
        is the authoritative execution status (DRAFT on first creation; an
        existing bound execution is returned unchanged for later reuse).
      required:
        [executionId, generatedTaskId, checklistTemplateId, clientId, status]
      properties:
        executionId:
          $ref: "#/components/schemas/Uuid"
        generatedTaskId:
          $ref: "#/components/schemas/Uuid"
          description: The generated task this execution is authoritatively bound to.
        checklistTemplateId:
          $ref: "#/components/schemas/Uuid"
          description: The task's CHECKLIST_TEMPLATE target id (authoritative).
        clientId:
          $ref: "#/components/schemas/Uuid"
        status:
          type: string
          enum: [DRAFT, IN_PROGRESS, COMPLETED, CANCELLED]
    MobileFormInstanceOpenReference:
      type: object
      description: >-
        MOB-C07 PART 02 / PART 03 — the minimum Form Instance reference
        returned when a form instance is opened or started from an assigned
        FORM_VERSION generated task. `status` is the authoritative instance
        status (DRAFT on first creation; IN_PROGRESS after start).
      required:
        [id, status, formTemplateVersionId, generatedTaskId]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        status:
          type: string
          enum: [DRAFT, IN_PROGRESS, COMPLETED, CANCELLED]
        formTemplateVersionId:
          $ref: "#/components/schemas/Uuid"
          description: The task's FORM_VERSION target id (authoritative).
        generatedTaskId:
          $ref: "#/components/schemas/Uuid"
          description: The generated task this instance is authoritatively bound to.
        startedAt:
          type: string
          format: date-time
          nullable: true
        completedByUserId:
          description: >
            Authenticated user who completed the instance (R06 PART 04A
            completion actor). Null while not completed and for historical
            instances predating completion-actor attribution.
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        assigneeType:
          description: >
            Assignment snapshot captured at task-bound instance creation
            (R06 PART 04A). WORKFORCE or TEAM when assigned; null when
            unassigned or the instance is not task-bound. Records WHO/WHAT WAS
            ASSIGNED — never an executor.
          type: string
          enum: [WORKFORCE, TEAM]
          nullable: true
        assignedWorkforceProfileId:
          description: >
            Workforce profile the task was assigned to at instance creation.
            Non-null only when assigneeType is WORKFORCE.
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        assignedTeamId:
          description: >
            Team the task was assigned to at instance creation. Non-null only
            when assigneeType is TEAM.
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        assignmentSnapshotAt:
          description: >
            Timestamp when the assignment facts were captured onto the
            instance (snapshot time, not assignment creation time).
          type: string
          format: date-time
          nullable: true
    FormInstanceResponseWrite:
      type: object
      description: >-
        Generic Form Instance response write item (object form of PUT
        `/form-instances/{id}/responses`). `fieldId` is a pinned
        `form_template_version_fields.id`.
      required: [fieldId]
      properties:
        fieldId:
          $ref: "#/components/schemas/Uuid"
        value: {}
    FormInstanceResponsesWrite:
      description: >-
        Generic Form Instance response payload reused by mobile: a single
        object or an array of objects. Same semantics as PUT
        `/form-instances/{id}/responses`.
      oneOf:
        - $ref: "#/components/schemas/FormInstanceResponseWrite"
        - type: array
          items:
            $ref: "#/components/schemas/FormInstanceResponseWrite"
    CreateMobileChecklistFindingRequest:
      type: object
      description: >-
        MOB-C05 PART 03 — the only fields a field worker provides. All
        authoritative context (clientId, buildingId, findingNumber,
        reportedByUserId, generatedTaskId, checklistTemplateId,
        workforceProfileId, shift, source, status, assignee, reviewer) is
        derived server-side and must NOT be sent.
      required: [title]
      additionalProperties: false
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 200
        description:
          type: string
          maxLength: 2000
          description: Optional finding description.
    MobileChecklistFindingCreated:
      type: object
      description: >-
        MOB-C05 PART 03 — the minimal mobile acknowledgement after a Finding is
        created from an authoritative bound checklist execution.
      required: [findingId, findingNumber, status, source]
      properties:
        findingId:
          $ref: "#/components/schemas/Uuid"
        findingNumber:
          type: string
          description: Server-generated operational identifier (FND_<8 uppercase hex>).
        status:
          type: string
          enum: [OPEN]
        source:
          type: object
          required: [type, id]
          properties:
            type:
              type: string
              enum: [CHECKLIST_EXECUTION]
            id:
              $ref: "#/components/schemas/Uuid"
              description: The exact checklist execution the Finding is bound to.
    MobileChecklistTemplateReference:
      type: object
      required: [id, clientId, code, name, status]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string
        description:
          type: string
          nullable: true
        status:
          type: string
          enum: [DRAFT, ACTIVE, INACTIVE]
    MobileChecklistTaskReference:
      type: object
      description: Task reference of the execution (first generated task targeting the template).
      required: [taskId, occurrenceAt, targetId, taskStatus]
      properties:
        taskId:
          $ref: "#/components/schemas/Uuid"
        scheduleDefinitionId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        occurrenceAt:
          type: string
          format: date-time
        targetId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        taskStatus:
          type: string
    MobileChecklistItem:
      type: object
      description: One checklist item with its current response value/status.
      required:
        [id, code, label, itemType, required, displayOrder, status, itemStatus, measurement, evidenceRequirements]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        label:
          type: string
        itemType:
          type: string
          enum: [CHECK, BOOLEAN, TEXT, NUMBER, SELECT]
        required:
          type: boolean
        displayOrder:
          type: integer
        status:
          type: string
          enum: [ACTIVE, INACTIVE]
        itemStatus:
          type: string
          enum: [PENDING, ANSWERED]
          description: Response status within this execution.
        value:
          description: Stored response value (boolean / number / string per itemType); null when unanswered.
          nullable: true
        result:
          type: string
          nullable: true
        notes:
          type: string
          nullable: true
        measurement:
          allOf:
            - $ref: "#/components/schemas/MobileChecklistMeasurement"
          nullable: true
          description: Measurement/UOM for NUMBER items; null otherwise.
        evidenceRequirements:
          type: array
          items:
            $ref: "#/components/schemas/MobileChecklistEvidenceRequirement"
    MobileChecklistMeasurement:
      type: object
      required: [uom, minimumValue, maximumValue, decimalPrecision]
      properties:
        uom:
          allOf:
            - $ref: "#/components/schemas/MobileChecklistUom"
          nullable: true
        minimumValue:
          type: number
          nullable: true
        maximumValue:
          type: number
          nullable: true
        decimalPrecision:
          type: integer
          nullable: true
    MobileChecklistUom:
      type: object
      required: [id, code, name, symbol, category]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string
        symbol:
          type: string
        category:
          type: string
    MobileChecklistEvidenceRequirement:
      type: object
      required: [id, evidenceType, required, minimumCount, maximumCount, description]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        evidenceType:
          type: string
          enum: [PHOTO, DOCUMENT, SIGNATURE]
        required:
          type: boolean
        minimumCount:
          type: integer
        maximumCount:
          type: integer
          nullable: true
        description:
          type: string
          nullable: true

    # ------------------------------------------------------------------
    # BE-25E — Mobile Evidence Upload Contract. Composition over the BE-07
    # Evidence authority + CR-BE-API-01 PART 03 storage abstraction; no
    # separate mobile evidence engine, no bytes in PostgreSQL, no internal
    # storage paths exposed.
    # ------------------------------------------------------------------
    MobileEvidence:
      type: object
      description: >
        The mobile evidence contract. `id` is the evidence submission id;
        `uploadStatus` is UPLOADED when the bytes are storage-backed
        (PENDING for metadata-only rows); `fileReference` is deliberately
        NOT exposed — internal storage paths stay server-side (the download
        endpoint addresses files by evidence id).
      required:
        [id, clientId, building, evidenceRequirement, target, evidenceType, file, status, submittedByUserId, createdAt, updatedAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        building:
          allOf:
            - $ref: "#/components/schemas/MobileEvidenceBuilding"
          nullable: true
          description: >-
            Building context when resolvable — through the target task
            (CHECKLIST_EXECUTION) or, for the Finding-related kinds
            (MOB-C06 PART 02), through the parent Finding's building; else
            null.
        evidenceRequirement:
          allOf:
            - $ref: "#/components/schemas/MobileEvidenceRequirementReference"
          nullable: true
        target:
          $ref: "#/components/schemas/MobileEvidenceTarget"
        evidenceType:
          type: string
          enum: [PHOTO, DOCUMENT, SIGNATURE]
        file:
          $ref: "#/components/schemas/MobileEvidenceFile"
        status:
          type: string
          enum: [ACTIVE, REMOVED]
        submittedByUserId:
          $ref: "#/components/schemas/Uuid"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    MobileEvidenceBuilding:
      type: object
      required: [id, code, name]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string
    MobileEvidenceRequirementReference:
      type: object
      required: [id, evidenceType, required, minimumCount, maximumCount, description]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        evidenceType:
          type: string
          enum: [PHOTO, DOCUMENT, SIGNATURE]
        required:
          type: boolean
        minimumCount:
          type: integer
        maximumCount:
          type: integer
          nullable: true
        description:
          type: string
          nullable: true
    MobileEvidenceTarget:
      type: object
      description: >-
        Target task/checklist/work reference of the evidence. MOB-C06 PART 01
        added the FINDING / FINDING_REWORK / FINDING_VERIFICATION parent kinds
        (executionId = Finding id / rework cycle id / verification review id);
        MOB-C06 PART 02 enriches those kinds with the authoritative parent
        context derived server-side from the stored evidence: a minimal
        Finding reference, the rework/verification reference where
        applicable, and the Building of the parent Finding (top-level
        `building`). Non-Finding kinds keep their existing projections with
        finding/rework/verification null. A broken Finding-related parent
        fails closed (404); an inaccessible parent Building is rejected (403).
      required: [executionType, executionId, checklist, form, task, finding, rework, verification]
      properties:
        executionType:
          type: string
          enum: [FORM_INSTANCE, CHECKLIST_EXECUTION, FINDING, FINDING_REWORK, FINDING_VERIFICATION]
        executionId:
          $ref: "#/components/schemas/Uuid"
        checklist:
          type: object
          nullable: true
          required: [id, code, name]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
            code:
              type: string
            name:
              type: string
        form:
          type: object
          nullable: true
          required: [id, code, name]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
            code:
              type: string
            name:
              type: string
        task:
          type: object
          nullable: true
          required: [taskId, occurrenceAt, taskStatus, buildingId]
          properties:
            taskId:
              $ref: "#/components/schemas/Uuid"
            occurrenceAt:
              type: string
              format: date-time
            taskStatus:
              type: string
            buildingId:
              allOf:
                - $ref: "#/components/schemas/Uuid"
              nullable: true
        finding:
          type: object
          nullable: true
          description: >-
            MOB-C06 PART 02 — minimal authoritative Finding reference of the
            parent (FINDING / FINDING_REWORK / FINDING_VERIFICATION evidence;
            null otherwise). Server-derived from the stored evidence only.
          required: [id, findingNumber, title, status]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
            findingNumber:
              type: string
            title:
              type: string
            status:
              type: string
        rework:
          type: object
          nullable: true
          description: >-
            MOB-C06 PART 02 — minimal rework-cycle reference (FINDING_REWORK
            evidence only; null otherwise). The Finding relationship is
            resolved from the authoritative rework cycle row.
          required: [id, status]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
            status:
              type: string
        verification:
          type: object
          nullable: true
          description: >-
            MOB-C06 PART 02 — minimal verification-review reference
            (FINDING_VERIFICATION evidence only; null otherwise). The Finding
            relationship is resolved from the authoritative review row
            (target_type = FINDING).
          required: [id, status]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
            status:
              type: string
    MobileEvidenceFile:
      type: object
      description: File metadata + upload result of the submission.
      required: [originalFileName, mimeType, fileSize, capturedAt, uploadStatus, fileAvailable]
      properties:
        originalFileName:
          type: string
          nullable: true
        mimeType:
          type: string
          nullable: true
        fileSize:
          type: integer
          description: Bytes; capped at 52,428,800 (50 MB).
        capturedAt:
          type: string
          format: date-time
          nullable: true
        uploadStatus:
          type: string
          enum: [UPLOADED, PENDING]
        fileAvailable:
          type: boolean

    # ------------------------------------------------------------------
    # BE-25F — Mobile QR Resolution Contract. Composition over the BE-05H
    # Asset Identifier resolver + BE-02G access authority; no separate QR
    # engine.
    # ------------------------------------------------------------------
    MobileQrResolution:
      type: object
      description: >
        The mobile QR resolution contract. `identifier` is the opaque
        scanned value (with its type); `targetType` discriminates the
        resolved target (ASSET today); `asset` holds the Asset / Equipment
        context when applicable; `available.actions` are read-model hints —
        backend-authoritative workflow actions stay on the resource
        endpoints.
      required: [identifier, targetType, targetId, clientId, location, asset, available]
      properties:
        identifier:
          type: object
          required: [id, identifierType, identifierValue]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
            identifierType:
              type: string
              enum: [QR, TAG, BARCODE, LEGACY]
            identifierValue:
              type: string
        targetType:
          type: string
          enum: [ASSET]
        targetId:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        location:
          $ref: "#/components/schemas/MobileQrLocationContext"
        asset:
          allOf:
            - $ref: "#/components/schemas/MobileQrAssetContext"
          nullable: true
        available:
          $ref: "#/components/schemas/MobileQrAvailableActions"
    MobileQrLocationContext:
      type: object
      required: [building, functionalLocation]
      properties:
        building:
          allOf:
            - $ref: "#/components/schemas/MobileQrBuilding"
          nullable: true
        functionalLocation:
          allOf:
            - $ref: "#/components/schemas/MobileQrFunctionalLocation"
          nullable: true
    MobileQrBuilding:
      type: object
      required: [id, code, name]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string
    MobileQrFunctionalLocation:
      type: object
      required: [id, code, name, status]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        code:
          type: string
        name:
          type: string
        description:
          type: string
          nullable: true
        status:
          type: string
    MobileQrAssetContext:
      type: object
      description: Asset / Equipment context of the resolved target.
      required: [id, assetCode, assetName, status, equipment]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        assetCode:
          type: string
        assetName:
          type: string
        description:
          type: string
          nullable: true
        manufacturer:
          type: string
          nullable: true
        model:
          type: string
          nullable: true
        serialNumber:
          type: string
          nullable: true
        status:
          type: string
        equipment:
          type: object
          nullable: true
          required: [id, equipmentCode, equipmentName, status]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
            equipmentCode:
              type: string
            equipmentName:
              type: string
            manufacturer:
              type: string
              nullable: true
            model:
              type: string
              nullable: true
            serialNumber:
              type: string
              nullable: true
            status:
              type: string
    MobileQrAvailableActions:
      type: object
      description: Available mobile action/context hints for the resolved target.
      required: [actions]
      properties:
        actions:
          type: array
          description: >
            Read-model hints only (VIEW_DETAILS, START_FINDING for ACTIVE
            assets). Workflow authority stays on the resource endpoints.
          items:
            type: string

    # ------------------------------------------------------------------
    # BE-25G — Offline Sync Contract. Operations are executed through the
    # same shared services as the REST endpoints; no separate mobile
    # business engine. Idempotency/conflict handling are later parts.
    # ------------------------------------------------------------------
    MobileSyncBatchRequest:
      type: object
      description: >
        A batch of client-generated offline operations (1..100). Envelope
        violations are rejected with 400 VALIDATION_ERROR before any write;
        per-item business failures are returned as per-item FAILED results.
      required: [operations]
      properties:
        operations:
          type: array
          minItems: 1
          maxItems: 100
          items:
            $ref: "#/components/schemas/MobileSyncOperationItem"
    MobileSyncOperationItem:
      type: object
      description: One client-generated sync operation (idempotent in BE-25H).
      required: [operationId, resourceType, resourceId, operation, clientTimestamp, data]
      properties:
        operationId:
          type: string
          description: >
            Client-generated operation identifier (1-128 chars of letters,
            digits, dots, underscores, hyphens). Unique per user: a retry
            with the same operationId replays the stored result instead of
            executing the write again (BE-25H).
        resourceType:
          type: string
          description: >
            The synced resource kind. A kind exists here ONLY when an existing
            backend operation provides a faithful authoritative write target
            (authoritative id + published operation + same RBAC permission +
            same Building scope + explicit `data` contract). Offline records
            with no such target stay local and are deliberately absent — see
            `x-sync-unsupported-resource-types`.
          enum:
            [TASK_EXECUTION, CHECKLIST_RESPONSES, EVIDENCE_SUBMISSION,
             TASK_ASSIGNMENT, PATROL_EXECUTION, PATROL_POINT_VISIT,
             METER_READING]
        resourceId:
          type: string
          format: uuid
          description: >
            Authoritative resource reference — always an id the backend
            already owns, never client-generated: taskId (TASK_EXECUTION,
            TASK_ASSIGNMENT); checklist executionId (CHECKLIST_RESPONSES,
            EVIDENCE_SUBMISSION); patrol execution id, which IS the BE-07
            taskId (PATROL_EXECUTION, PATROL_POINT_VISIT); BE-07 form
            instance id started from a meter-reading binding (METER_READING).
        operation:
          type: string
          description: >
            PART 06 introduced no new verb. Valid combinations:
            TASK_EXECUTION START|COMPLETE|CANCEL; CHECKLIST_RESPONSES SAVE;
            EVIDENCE_SUBMISSION SUBMIT; TASK_ASSIGNMENT UPDATE;
            PATROL_EXECUTION START|COMPLETE; PATROL_POINT_VISIT SUBMIT;
            METER_READING SUBMIT. Any other pairing is rejected with 400
            VALIDATION_ERROR before any write.
          enum: [START, COMPLETE, CANCEL, SAVE, SUBMIT, UPDATE]
        clientTimestamp:
          type: string
          format: date-time
          description: >
            Required on every operation: the client timestamp of when the
            operation happened on-device. It is echoed on the result and is
            never used as an authoritative server time — the server records
            its own `serverTimestamp`.
        data:
          type: object
          description: >
            Type-specific payload: TASK_EXECUTION: { completionNotes? };
            CHECKLIST_RESPONSES: { responses: array|object }; 
            EVIDENCE_SUBMISSION: { evidenceType, executionType, executionId,
            evidenceRequirementId?, fileReference, originalFileName, mimeType,
            fileSize, capturedAt? }; TASK_ASSIGNMENT: { assignmentId,
            status? }; PATROL_EXECUTION: { completionNotes? } on COMPLETE;
            PATROL_POINT_VISIT: { patrolRoutePointId (required, authoritative
            BE-12B point id), notes? }; METER_READING: { value (required,
            finite number), notes? } — the UOM and the effective measurement
            range stay backend-owned and are NOT accepted from the client.
            Any operation may additionally carry `baseVersion` (BE-25I): the
            resource's `updatedAt` the client last saw; a stale baseVersion
            produces SYNC_CONFLICT. Evidence BYTES are never carried here —
            only metadata, uploaded through the evidence file API.
          additionalProperties: true
    MobileSyncBatchResponse:
      type: object
      description: The batch result; every operation gets its own result item.
      required: [batchId, receivedAt, results]
      properties:
        batchId:
          type: string
          format: uuid
          description: Server-generated batch id for correlation.
        receivedAt:
          type: string
          format: date-time
          description: Server timestamp of when the batch was received.
        results:
          type: array
          items:
            $ref: "#/components/schemas/MobileSyncResultItem"
    MobileSyncResultItem:
      type: object
      description: >
        Per-item success/failure result. `status` is the server result/status
        (SUCCESS | FAILED); `result` carries the written public resource on
        success; `error` carries the authoritative error code/message on
        failure; `serverTimestamp` is the server execution timestamp. On a
        duplicate replay, `result`/`error`/`serverTimestamp` are the ORIGINAL
        stored values (BE-25H).
      required: [operationId, clientTimestamp, success, status, result, error, serverTimestamp]
      properties:
        operationId:
          type: string
        clientTimestamp:
          type: string
          format: date-time
        success:
          type: boolean
        status:
          type: string
          enum: [SUCCESS, FAILED]
        result:
          description: The written result (task / evidence metadata / assignment) on success; null on failure.
          nullable: true
        error:
          type: object
          nullable: true
          required: [code, message]
          properties:
            code:
              type: string
              description: Authoritative ERROR_CODES value (e.g. PERMISSION_DENIED, BUILDING_ACCESS_DENIED, BAD_REQUEST, SYNC_CONFLICT).
            message:
              type: string
            conflict:
              type: object
              nullable: true
              description: BE-25I conflict payload (present on SYNC_CONFLICT failures).
              required: [current, guidance]
              properties:
                current:
                  description: Current authoritative server state/reference of the resource.
                guidance:
                  type: object
                  required: [action, reloadEndpoint, message]
                  properties:
                    action:
                      type: string
                      const: reload
                      description: Reload the resource and re-apply (the backend never merges automatically).
                    reloadEndpoint:
                      type: string
                      description: Endpoint to re-fetch the current state (e.g. /tasks/{taskId}).
                    message:
                      type: string
        serverTimestamp:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # BE-25J — Supervisor Verification Contract. Thin composition over the
    # shared BE-07 review authority and the BE-09 finding verification
    # workflow; no separate mobile verification engine.
    # ------------------------------------------------------------------
    MobileVerification:
      type: object
      description: >
        The mobile supervisor verification contract. `verification.state`
        reflects the current authoritative state (PENDING / VERIFIED /
        REJECTED / REWORK_REQUIRED / NOT_REVIEWABLE for review targets
        including WORK_ORDER; the finding status for FINDING).
        `availableActions` is backend-authoritative. A completed
        verification is immutable. WORK_ORDER delegates to the existing
        BE-08I lifecycle (CR-BE-MOB-03 PART 05).
      required:
        [targetType, targetId, clientId, buildingId, resource, verification, reviewer, availableActions, updatedAt]
      properties:
        targetType:
          type: string
          enum: [CHECKLIST_EXECUTION, FORM_INSTANCE, FINDING, WORK_ORDER]
        targetId:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        resource:
          $ref: "#/components/schemas/MobileVerificationResource"
        verification:
          $ref: "#/components/schemas/MobileVerificationState"
        reviewer:
          allOf:
            - $ref: "#/components/schemas/MobileVerificationReviewer"
          nullable: true
        availableActions:
          type: array
          description: >
            SUBMIT_DECISION for review targets when reviewable and not yet
            verified; the BE-09 verification subset (OPEN_REVIEW / APPROVE /
            REJECT / REQUEST_REWORK) for FINDING.
          items:
            type: string
        updatedAt:
          type: string
          format: date-time
          nullable: true
    MobileVerificationResource:
      type: object
      description: Reviewable resource reference (type-specific).
      required: [status, checklist, form, finding, workOrder]
      properties:
        status:
          type: string
        checklist:
          type: object
          nullable: true
          required: [id, code, name]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
            code:
              type: string
            name:
              type: string
        form:
          type: object
          nullable: true
          required: [id, code, name]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
            code:
              type: string
            name:
              type: string
        finding:
          type: object
          nullable: true
          required: [id, findingNumber, title, status, reportedAt]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
            findingNumber:
              type: string
            title:
              type: string
            status:
              type: string
            reportedAt:
              type: string
              format: date-time
        workOrder:
          type: object
          nullable: true
          required: [id, workOrderNumber, title, status]
          properties:
            id:
              $ref: "#/components/schemas/Uuid"
              description: Authoritative `work_orders.id`.
            workOrderNumber:
              type: string
            title:
              type: string
            status:
              type: string
    MobileVerificationState:
      type: object
      required: [state, decision, notes, verifiedAt, reviewId, reviewStatus]
      properties:
        state:
          type: string
          description: >
            NOT_REVIEWABLE / PENDING / VERIFIED / REJECTED / REWORK_REQUIRED
            (review targets including WORK_ORDER) or the authoritative
            finding status (FINDING).
        decision:
          type: string
          enum: [APPROVED, REJECTED, REWORK_REQUIRED]
          nullable: true
        notes:
          type: string
          nullable: true
        verifiedAt:
          type: string
          format: date-time
          nullable: true
        reviewId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        reviewStatus:
          type: string
          enum: [PENDING, COMPLETED]
          nullable: true
    MobileVerificationReviewer:
      type: object
      description: Supervisor / reviewer context (user + linked workforce profile).
      required: [userId, displayName, workforceProfileId, fullName, employeeCode]
      properties:
        userId:
          $ref: "#/components/schemas/Uuid"
        displayName:
          type: string
        workforceProfileId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        fullName:
          type: string
          nullable: true
        employeeCode:
          type: string
          nullable: true

    # ------------------------------------------------------------------
    # BE-25L — Push Token Registration. Bound to the authenticated
    # user/device context; refresh/rotation and deactivation supported.
    # No notification delivery engine exists yet (BE-26).
    # ------------------------------------------------------------------
    PushTokenRegistration:
      type: object
      description: >
        A mobile push token registration. `status` is ACTIVE while the token
        is usable; deactivation keeps the row as INACTIVE history, and a
        provider-rejected registration is retained as INVALID history. The
        listing returns every one of the user's registrations regardless of
        status, so all three values are observable. `registeredAt` is the
        first registration time; `lastSeenAt` refreshes on every
        registration/touch of the device.
      required:
        [id, userId, deviceId, pushToken, platform, status, registeredAt, lastSeenAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        userId:
          $ref: "#/components/schemas/Uuid"
        deviceId:
          type: string
        pushToken:
          type: string
        platform:
          type: string
          enum: [ANDROID, IOS]
        appVersion:
          type: string
          nullable: true
        deviceModel:
          type: string
          nullable: true
        deviceOsVersion:
          type: string
          nullable: true
        status:
          type: string
          enum: [ACTIVE, INACTIVE, INVALID]
          description: >-
            ACTIVE — usable registration. INACTIVE — the user or app
            deliberately unregistered the device. INVALID — the push provider
            rejected the registration (for example the token was unregistered
            on the device) and the backend retired it; the row is retained as
            history and is returned by this endpoint, so a client must treat
            INVALID as "re-register this device". The backend never deletes a
            registration row.
        registeredAt:
          type: string
          format: date-time
        lastSeenAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # BE-26A — Notification foundation (in-app inbox). Foundation only:
    # no push/email/WhatsApp/SMS delivery, no create endpoint.
    # ------------------------------------------------------------------
    Notification:
      type: object
      description: >
        A recipient-scoped in-app notification (the in-app delivery record).
        `channel` is always IN_APP in this foundation. `status` is UNREAD
        until `readAt` is stamped by the mark-read transition.
        `sourceEntityType` / `sourceEntityId` / `sourceEventType` reference
        the operational entity and event that produced the notification
        (operational modules remain the source of truth). `templateKey`
        references the BE-26B template that rendered it (null when created
        directly); `deliveredAt` is when the in-app delivery became visible.
        `metadata` is an opaque JSON object.
      required:
        [id, clientId, recipientUserId, type, channel, status, title,
         sourceEntityType, sourceEntityId, createdAt, deliveredAt, readAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        recipientUserId:
          $ref: "#/components/schemas/Uuid"
        type:
          type: string
          description: Notification type (data-driven, uppercase code).
        channel:
          type: string
          enum: [IN_APP]
        status:
          type: string
          enum: [UNREAD, READ]
        title:
          type: string
        body:
          type: string
          nullable: true
        sourceEntityType:
          type: string
          description: Domain entity type of the source (e.g. WORK_ORDER).
        sourceEntityId:
          $ref: "#/components/schemas/Uuid"
        sourceEventType:
          type: string
          nullable: true
          description: Originating operational event type (e.g. WORK_ORDER_ASSIGNED).
        templateKey:
          type: string
          nullable: true
          description: BE-26B template key that rendered this notification.
        metadata:
          type: object
          additionalProperties: true
        createdAt:
          type: string
          format: date-time
        deliveredAt:
          type: string
          format: date-time
          description: When the in-app delivery became visible to the recipient.
        readAt:
          type: string
          format: date-time
          nullable: true
        updatedAt:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # BE-26B — Notification template foundation. Platform configuration;
    # no recipient resolution and no sending.
    # ------------------------------------------------------------------
    NotificationTemplate:
      type: object
      description: >
        A reusable notification template (rendering recipe). `key` is the
        stable unique template identifier; `type` is the notification type
        stamped on rendered notifications. `subject` / `body` are templates
        with `{{variable}}` placeholders; `variables` lists the declared
        variable names. `channel` is IN_APP in this foundation.
      required: [id, key, type, channel, subject, variables, status, createdAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        key:
          type: string
        type:
          type: string
        channel:
          type: string
          enum: [IN_APP]
        subject:
          type: string
        body:
          type: string
          nullable: true
        variables:
          type: array
          items:
            type: string
        status:
          type: string
          enum: [ACTIVE, INACTIVE]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # BE-26K — Notification history read model. Unified chronological view
    # over in-app / email / WhatsApp deliveries; read-only.
    # ------------------------------------------------------------------
    NotificationHistoryItem:
      type: object
      description: >
        A unified delivery/history entry across channels. `channel` is
        IN_APP / EMAIL / WHATSAPP; `status` carries the channel-specific
        value (UNREAD/READ for in-app; SENT/FAILED for email/WhatsApp).
        Timestamps are populated where available: `sentAt`, `deliveredAt`,
        `failedAt`, and `readAt` (in-app only). `failureReason` is a
        sanitized error message (never contains credentials/tokens).
      required:
        [channel, id, clientId, recipientUserId, status, sentAt, deliveredAt,
         failedAt, readAt, createdAt]
      properties:
        channel:
          type: string
          enum: [IN_APP, EMAIL, WHATSAPP]
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        recipientUserId:
          $ref: "#/components/schemas/Uuid"
        type:
          type: string
          nullable: true
          description: Notification type (in-app only).
        templateKey:
          type: string
          nullable: true
          description: BE-26B template key.
        sourceEntityType:
          type: string
          nullable: true
          description: Source/resource domain type (in-app only).
        sourceEntityId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
          description: Source/resource id (in-app only).
        sourceEventType:
          type: string
          nullable: true
          description: Originating event type (in-app only).
        status:
          type: string
          enum: [UNREAD, READ, SENT, FAILED]
        sentAt:
          type: string
          format: date-time
          nullable: true
        deliveredAt:
          type: string
          format: date-time
          nullable: true
        failedAt:
          type: string
          format: date-time
          nullable: true
        readAt:
          type: string
          format: date-time
          nullable: true
        provider:
          type: string
          nullable: true
          description: Delivery provider discriminator (email/WhatsApp only).
        providerReference:
          type: string
          nullable: true
          description: Provider response reference (email/WhatsApp only).
        failureReason:
          type: string
          nullable: true
          description: Sanitized failure reason (email/WhatsApp only).
        deliveryId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
          description: >
            CR-BE-NOTIFY-PROV-01 PART 04 — the outbound delivery ledger row
            this attempt belongs to (null for in-app rows and pre-ledger
            attempts).
        providerFeedbackStatus:
          type: string
          nullable: true
          enum: [DELIVERED, BOUNCED, COMPLAINT, PROVIDER_FAILED]
          description: >
            CR-BE-NOTIFY-PROV-01 PART 07 — post-acceptance provider feedback
            from the linked ledger row (null when no feedback or no ledger
            linkage). Feedback never reopens the send lifecycle.
        feedbackAt:
          type: string
          format: date-time
          nullable: true
          description: When the provider feedback was accepted.
        createdAt:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # BE-26J — Notification secure link foundation. Opaque, recipient-bound,
    # single-purpose reference; no Notification History.
    # ------------------------------------------------------------------
    NotificationSecureLink:
      type: object
      description: >
        A secure notification link: an opaque, recipient-bound, single-purpose
        reference to a target resource + action. The token hash is never
        exposed; the raw token is returned exactly once at creation.
        `status` is ACTIVE until revoked (REVOKED), expired (EXPIRED), or
        exhausted (USED).
      required:
        [id, clientId, recipientUserId, targetEntityType, targetEntityId,
         action, expiresAt, maxUses, usedCount, status, createdAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        clientId:
          $ref: "#/components/schemas/Uuid"
        recipientUserId:
          $ref: "#/components/schemas/Uuid"
          description: The bound recipient (resolution requires this user).
        targetEntityType:
          type: string
          description: Target resource type.
        targetEntityId:
          $ref: "#/components/schemas/Uuid"
          description: Target resource id.
        action:
          type: string
          description: Target action (uppercase code).
        expiresAt:
          type: string
          format: date-time
        maxUses:
          type: integer
        usedCount:
          type: integer
        status:
          type: string
          enum: [ACTIVE, REVOKED, EXPIRED, USED]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # BE-26I — Notification escalation foundation. Time-deferred escalation
    # trigger; no scheduler engine, no workflow/approval engine, and no
    # business-resource state changes.
    # ------------------------------------------------------------------
    NotificationEscalation:
      type: object
      description: >
        A time-deferred escalation trigger: references the source/resource
        being escalated, the current (pre-escalation) recipient, and the
        escalation recipient rule (BE-26C). At `escalationAt` it renders its
        BE-26B template and triggers notification delivery. `status` is
        PENDING until triggered (TRIGGERED, with `triggeredAt`) or cancelled
        (CANCELLED). `reason` carries escalation notes.
      required:
        [id, key, clientId, sourceEntityType, sourceEntityId,
         escalationRule, templateKey, escalationAt, status, triggeredAt,
         createdAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        key:
          type: string
        clientId:
          $ref: "#/components/schemas/Uuid"
        sourceEntityType:
          type: string
          description: Source/resource domain type being escalated.
        sourceEntityId:
          $ref: "#/components/schemas/Uuid"
          description: Source/resource id being escalated.
        currentRecipientUserId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
          description: The current (pre-escalation) recipient.
        escalationRule:
          $ref: "#/components/schemas/RecipientRule"
        templateKey:
          type: string
          description: BE-26B notification template key.
        escalationAt:
          type: string
          format: date-time
        triggeredAt:
          type: string
          format: date-time
          nullable: true
        status:
          type: string
          enum: [PENDING, TRIGGERED, CANCELLED]
        reason:
          type: string
          nullable: true
          description: Escalation reason/notes.
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # BE-26H — Notification reminder foundation. Time-deferred notification
    # trigger; no scheduler engine and no escalation.
    # ------------------------------------------------------------------
    NotificationReminder:
      type: object
      description: >
        A time-deferred notification trigger: references the source/resource
        being reminded about, the recipients (BE-26C rule), and the BE-26B
        template rendered at dispatch time. `status` is PENDING until
        dispatched (SENT, with `sentAt`) or cancelled (CANCELLED).
      required:
        [id, key, clientId, sourceEntityType, sourceEntityId, recipientRule,
         templateKey, reminderAt, status, sentAt, createdAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        key:
          type: string
        clientId:
          $ref: "#/components/schemas/Uuid"
        sourceEntityType:
          type: string
          description: Source/resource domain type being reminded about.
        sourceEntityId:
          $ref: "#/components/schemas/Uuid"
          description: Source/resource id being reminded about.
        recipientRule:
          $ref: "#/components/schemas/RecipientRule"
        templateKey:
          type: string
          description: BE-26B notification template key.
        variables:
          type: object
          additionalProperties: true
          description: Template variable values snapshot.
        reminderAt:
          type: string
          format: date-time
        status:
          type: string
          enum: [PENDING, SENT, CANCELLED]
        sentAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    # ------------------------------------------------------------------
    # BE-26D — Notification event subscription foundation. Declaration
    # only; no delivery and no new domain events.
    # ------------------------------------------------------------------
    NotificationEventSubscription:
      type: object
      description: >
        Maps an existing backend domain event type to a notification trigger:
        a notification template key plus a recipient resolution rule, with an
        optional Client/Building context and an enabled status.
      required:
        [id, key, eventType, templateKey, recipientRule, status, createdAt]
      properties:
        id:
          $ref: "#/components/schemas/Uuid"
        key:
          type: string
        eventType:
          type: string
          description: Existing domain event type (e.g. WORK_ORDER_ASSIGNED).
        templateKey:
          type: string
          description: BE-26B notification template key.
        recipientRule:
          $ref: "#/components/schemas/RecipientRule"
        clientId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
          description: Optional Client context (null = any Client).
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
          description: Optional Building context (null = any Building).
        status:
          type: string
          enum: [ACTIVE, INACTIVE]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    RecipientRule:
      type: object
      description: >
        BE-26C recipient resolution rule: one or more recipient specs plus an
        optional Client/Building scope applied to the resolved users.
      required: [specs]
      properties:
        specs:
          type: array
          minItems: 1
          description: Recipient resolution specs (deduplicated at resolution).
          items:
            $ref: "#/components/schemas/RecipientSpec"
        scope:
          $ref: "#/components/schemas/RecipientScope"

    RecipientScope:
      type: object
      description: >
        Client/Building data scope applied to resolved recipients. Only users
        whose OWN accessible Building context satisfies the scope are kept
        (BE-02F/BE-02G).
      properties:
        clientId:
          $ref: "#/components/schemas/Uuid"
        buildingIds:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/Uuid"

    RecipientSpec:
      type: object
      description: >
        A single recipient resolution instruction (BE-26C). Exactly one
        variant applies per `kind`.
      oneOf:
        - type: object
          required: [kind, userId]
          properties:
            kind: { type: string, enum: [USER] }
            userId: { $ref: "#/components/schemas/Uuid" }
        - type: object
          required: [kind, roleCode]
          properties:
            kind: { type: string, enum: [ROLE] }
            roleCode: { type: string }
        - type: object
          required: [kind, permissionCode]
          properties:
            kind: { type: string, enum: [PERMISSION] }
            permissionCode: { type: string }
        - type: object
          required: [kind, workforceProfileId]
          properties:
            kind: { type: string, enum: [WORKFORCE] }
            workforceProfileId: { $ref: "#/components/schemas/Uuid" }
        - type: object
          required: [kind, teamId]
          properties:
            kind: { type: string, enum: [TEAM] }
            teamId: { $ref: "#/components/schemas/Uuid" }
        - type: object
          required: [kind]
          properties:
            kind: { type: string, enum: [TENANT_PIC] }
            tenantCompanyId: { $ref: "#/components/schemas/Uuid" }
            tenantPicId: { $ref: "#/components/schemas/Uuid" }
        - type: object
          required: [kind, vendorId]
          properties:
            kind: { type: string, enum: [VENDOR_PIC] }
            vendorId: { $ref: "#/components/schemas/Uuid" }

    # ------------------------------------------------------------------
    # BE-25M — App Version Metadata. Read-only contract; no update
    # delivery and no Flutter UI behavior.
    # ------------------------------------------------------------------
    MobileAppVersionMetadata:
      type: object
      description: >
        Per-platform mobile app version metadata. `updateRequired` is true
        when the caller's version is below `minimumSupportedVersion`;
        `updateAvailable` is true when it is below `currentVersion`.
        `release` carries optional release metadata (version, notes, date).
        Flags default to false when the caller omits `appVersion`.
      required:
        [platform, currentVersion, minimumSupportedVersion, updateRequired, updateAvailable, release]
      properties:
        platform:
          type: string
          enum: [ANDROID, IOS]
        currentVersion:
          type: string
          description: Latest supported version.
        minimumSupportedVersion:
          type: string
          description: Oldest still-supported version.
        updateRequired:
          type: boolean
          description: True when the caller's version is below the minimum supported version.
        updateAvailable:
          type: boolean
          description: True when the caller's version is below the current version.
        release:
          type: object
          nullable: true
          required: [version, notes, date]
          properties:
            version:
              type: string
            notes:
              type: string
              nullable: true
            date:
              type: string
              format: date-time
              nullable: true

    # ------------------------------------------------------------------
    # BE-25N — Mobile Observability. Lightweight diagnostics; correlation
    # via X-Request-ID; device/app metadata in logs only.
    # ------------------------------------------------------------------
    MobileDiagnostics:
      type: object
      description: >
        Basic mobile API health/diagnostic metadata. `requestId` is the
        correlation id of the request (same as the X-Request-ID response
        header); `device` echoes the sanitized mobile context headers when
        the client sent them.
      required: [status, requestId, serverTime, uptimeSeconds]
      properties:
        status:
          type: string
          const: OK
        requestId:
          type: string
        serverTime:
          type: string
          format: date-time
        uptimeSeconds:
          type: integer
        device:
          type: object
          required: []
          properties:
            deviceId:
              type: string
            platform:
              type: string
              enum: [ANDROID, IOS]
            appVersion:
              type: string

    # ------------------------------------------------------------------
    # CR-BE-COM-02 — Vendor Invoice domain schemas (PART 01–05)
    # ------------------------------------------------------------------

    # -------------------------------------------------------------------------
    # CR-BE-R2P-01 PART 07 — upstream chain schemas (pre-existing behaviour).
    # -------------------------------------------------------------------------

    ServiceRequestStatus:
      type: string
      enum: [OPEN, CANCELLED]

    ServiceRequest:
      type: object
      description: >
        BE-17C Service Request line on a Purchase Request. Client and Building
        are derived from the parent Purchase Request.
      required:
        [id, clientId, buildingId, purchaseRequestId, serviceType, title,
         status, requestedByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        purchaseRequestId: { $ref: "#/components/schemas/Uuid" }
        serviceType: { type: string }
        serviceCatalogId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            CR-BE-SVC-01 PART 02 — governed Service Catalog anchor. NULL means
            the request relies on its free-text `serviceType` only (historical
            requests). When set it references an ACTIVE same-Client
            `service_catalog` entry whose code equals `serviceType`.
        title: { type: string }
        description: { type: string, nullable: true }
        requiredDate: { type: string, format: date, nullable: true }
        functionalLocationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        vendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        notes: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/ServiceRequestStatus" }
        requestedByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateServiceRequestRequest:
      type: object
      required: [serviceType, title]
      properties:
        serviceType: { type: string }
        serviceCatalogId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            CR-BE-SVC-01 PART 02 — optional governed Service Catalog anchor
            (same-Client, ACTIVE, code equal to `serviceType`).
        title: { type: string }
        description: { type: string }
        requiredDate: { type: string, format: date, nullable: true }
        functionalLocationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        vendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        notes: { type: string }

    UpdateServiceRequestRequest:
      type: object
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 02A — BE-17C `PATCH
        /service-requests/{id}` body. Every field is optional; only the fields
        present are changed, and `null` clears a nullable field. Lifecycle
        (`status`), ownership (`clientId`, `buildingId`, `purchaseRequestId`)
        and `requestedByUserId` are never caller-supplied. The request must be
        OPEN.
      properties:
        serviceType:
          type: string
          minLength: 2
          maxLength: 64
          description: >
            Data-driven service type code (no hardcoded model). Trimmed and
            uppercased before validation; must start with a letter and contain
            only uppercase letters, digits, hyphens and underscores. When a
            `serviceCatalogId` is supplied its catalog code must equal this
            value; the free-text `serviceType` is never rewritten by the anchor.
        serviceCatalogId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            CR-BE-SVC-01 PART 02 — optional governed Service Catalog anchor
            (same-Client, ACTIVE, code equal to `serviceType`). `null` clears
            the anchor, leaving the request on its free-text `serviceType` only.
        title:
          type: string
          maxLength: 200
          description: Trimmed; must not be empty.
        description:
          type: string
          maxLength: 1000
          nullable: true
        requiredDate:
          type: string
          format: date
          nullable: true
          description: Valid ISO date string, or `null` to clear.
        functionalLocationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Optional Building-scoped location context; must belong to the same
            Building as the request. `null` clears it.
        vendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Optional BE-06 Vendor reference used by later PARTs for vendor
            selection; must belong to the request's Client. `null` clears it.
        notes:
          type: string
          maxLength: 1000
          nullable: true

    VendorSelectionRequestType:
      type: string
      enum: [PURCHASE_REQUEST, SERVICE_REQUEST]

    VendorSelectionReadinessStatus:
      type: string
      description: Backend-derived verdict; never caller-supplied.
      enum: [READY, NOT_READY, EXPIRED, INELIGIBLE]

    VendorSelection:
      type: object
      description: >
        BE-17E Vendor Selection Readiness — the deterministic verdict on
        whether a Vendor may be selected for a request, with its individual
        checks.
      required:
        [id, clientId, buildingId, requestType, requestId, vendorId, readiness,
         vendorActive, buildingRelationshipOk, capabilityMatch, complianceOk,
         licenseOk, approvalOk, evaluatedAt, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        requestType: { $ref: "#/components/schemas/VendorSelectionRequestType" }
        requestId: { $ref: "#/components/schemas/Uuid" }
        purchaseRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        serviceRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        vendorId: { $ref: "#/components/schemas/Uuid" }
        readiness:
          $ref: "#/components/schemas/VendorSelectionReadinessStatus"
        vendorActive: { type: boolean }
        buildingRelationshipOk: { type: boolean }
        capabilityMatch:
          type: boolean
          description: >
            CR-BE-SVC-01 PART 03 — when BOTH the request and a Vendor capability
            carry a governed `serviceCatalogId`, the match is identity-only (a
            mismatch is NOT eligible even when legacy codes match); otherwise
            normalized-code equality applies.
        complianceOk: { type: boolean }
        licenseOk: { type: boolean }
        approvalOk: { type: boolean }
        serviceCatalogId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            CR-BE-SVC-01 PART 03 — governed demand identity snapshot at
            evaluation time (NULL for un-governed / purchase requests).
        notes: { type: string, nullable: true }
        evaluatedAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateVendorSelectionRequest:
      type: object
      required: [requestType, requestId, vendorId]
      properties:
        requestType: { $ref: "#/components/schemas/VendorSelectionRequestType" }
        requestId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        notes: { type: string }

    POReadinessRequestType:
      type: string
      enum: [PURCHASE_REQUEST, SERVICE_REQUEST]

    POReadinessStatus:
      type: string
      description: >
        THE readiness verdict of the R2P chain. READY means "may commit"; an
        ISSUED Purchase Order means "committed". Downstream domains report this
        verdict verbatim and never re-decide it.
      enum: [READY, NOT_READY, BLOCKED]

    POReadiness:
      type: object
      description: >
        BE-17F Purchase Order Readiness — the sole readiness authority and a
        precondition for committing a Purchase Order.
      required:
        [id, clientId, buildingId, requestType, requestId, vendorId, readiness,
         approvalOk, vendorOk, materialContextOk, serviceContextOk,
         preparedByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        requestType: { $ref: "#/components/schemas/POReadinessRequestType" }
        requestId: { $ref: "#/components/schemas/Uuid" }
        purchaseRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        serviceRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        vendorId: { $ref: "#/components/schemas/Uuid" }
        readiness: { $ref: "#/components/schemas/POReadinessStatus" }
        approvalOk: { type: boolean }
        vendorOk: { type: boolean }
        materialContextOk: { type: boolean }
        serviceContextOk: { type: boolean }
        requiredDate: { type: string, format: date, nullable: true }
        notes: { type: string, nullable: true }
        preparedByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreatePOReadinessRequest:
      type: object
      required: [requestType, requestId, vendorId]
      properties:
        requestType: { $ref: "#/components/schemas/POReadinessRequestType" }
        requestId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        requiredDate: { type: string, format: date, nullable: true }
        notes: { type: string }

    UpdatePOReadinessRequest:
      type: object
      description: >
        Only editable detail; the readiness verdict and checks are
        backend-derived.
      minProperties: 1
      properties:
        requiredDate: { type: string, format: date, nullable: true }
        notes: { type: string, nullable: true }

    # -------------------------------------------------------------------------
    # CR-BE-R2P-01 — Request-to-Pay chain schemas (PART 01–06)
    # -------------------------------------------------------------------------

    PurchaseOrderStatus:
      type: string
      description: >
        Authoritative API lifecycle: DRAFT → ISSUED when issue-readiness passes,
        or DRAFT → CANCELLED before issuance. ISSUED and CANCELLED have no
        outgoing API command and are terminal for this contract.
      enum: [DRAFT, ISSUED, CANCELLED]

    PurchaseOrderAction:
      type: string
      description: >
        Existing Purchase Order command available to the calling user now.
        Tokens are upper-case and map one-to-one to registered command paths.
      enum: [ISSUE, CANCEL]

    PurchaseOrderAvailableActions:
      type: object
      description: >
        Backend-authoritative, caller-specific Purchase Order actions. An empty
        list is authoritative; clients must not infer transitions from state.
      required: [purchaseOrderId, state, availableActions]
      properties:
        purchaseOrderId: { $ref: "#/components/schemas/Uuid" }
        state: { $ref: "#/components/schemas/PurchaseOrderStatus" }
        availableActions:
          type: array
          items: { $ref: "#/components/schemas/PurchaseOrderAction" }

    PurchaseOrderRequestType:
      type: string
      description: >
        The source request behind the commitment. Mirrors the BE-17F PO
        Readiness vocabulary exactly — the PO never widens it.
      enum: [PURCHASE_REQUEST, SERVICE_REQUEST]

    PurchaseOrderCurrency:
      type: string
      description: Identical to the Vendor Invoice currency vocabulary.
      enum: [IDR, USD, SGD, MYR, AUD, EUR, GBP, JPY, CNY]

    PurchaseOrder:
      type: object
      description: >
        The authoritative COMMITMENT to a Vendor for an approved procurement
        request — the R2P link between "may commit" (BE-17F PO Readiness) and
        downstream execution. Carries NO quantity, NO ordered/received ledger,
        NO amount total and NO readiness verdict of its own.
      required:
        [id, clientId, buildingId, poNumber, poDate, vendorId, requestType,
         purchaseRequestId, serviceRequestId, poReadinessId, currency, status,
         vendorReference, requiredDate, notes, createdByUserId, issuedAt,
         issuedByUserId, cancelledAt, cancelledByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Derived from the qualifying PO Readiness.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Derived from the qualifying PO Readiness.
        poNumber:
          type: string
          description: PO identity foundation — unique within a Client.
        poDate: { type: string, format: date }
        vendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Derived from the qualifying PO Readiness.
        requestType: { $ref: "#/components/schemas/PurchaseOrderRequestType" }
        purchaseRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Exactly one of purchaseRequestId / serviceRequestId is set.
        serviceRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        poReadinessId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Required precondition — the BE-17F readiness that qualified this
            commitment. PO Readiness remains the sole readiness authority.
        currency: { $ref: "#/components/schemas/PurchaseOrderCurrency" }
        status: { $ref: "#/components/schemas/PurchaseOrderStatus" }
        vendorReference: { type: string, nullable: true }
        requiredDate: { type: string, format: date, nullable: true }
        notes: { type: string, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        issuedAt:
          type: string
          format: date-time
          nullable: true
          description: >
            CR-BE-R2P-01 PART 03 — set when the commitment was issued. NULL
            while DRAFT and on API-created CANCELLED rows. Historical/imported
            issued-cancelled rows may preserve this provenance even though the
            API exposes no ISSUED cancellation command.
        issuedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        cancelledAt: { type: string, format: date-time, nullable: true }
        cancelledByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreatePurchaseOrderRequest:
      type: object
      description: >
        `poReadinessId` is the ONLY context input — Client, Building, Vendor
        and the request reference are derived from it.
      required: [poReadinessId, poNumber, poDate, currency]
      properties:
        poReadinessId: { $ref: "#/components/schemas/Uuid" }
        poNumber:
          type: string
          minLength: 2
          maxLength: 64
          pattern: "^[A-Za-z0-9][A-Za-z0-9_\\-/]{1,63}$"
          description: >
            Normalized uppercase and unique within the Client.
        poDate: { type: string, format: date }
        currency: { $ref: "#/components/schemas/PurchaseOrderCurrency" }
        vendorReference: { type: string, nullable: true, maxLength: 255 }
        requiredDate: { type: string, format: date, nullable: true }
        notes: { type: string, nullable: true, maxLength: 2000 }

    UpdatePurchaseOrderRequest:
      type: object
      description: >
        DRAFT only. At least one field is required. Identity, commitment
        context and derived scope are immutable and must not be supplied.
      minProperties: 1
      properties:
        poDate: { type: string, format: date }
        currency: { $ref: "#/components/schemas/PurchaseOrderCurrency" }
        vendorReference: { type: string, nullable: true, maxLength: 255 }
        requiredDate: { type: string, format: date, nullable: true }
        notes: { type: string, nullable: true, maxLength: 2000 }

    IssuePurchaseOrderRequest:
      type: object
      description: >
        Optional issuance note only. Status and issuance provenance are
        backend-derived; a caller can neither declare readiness nor stamp
        `issuedAt` / `issuedByUserId`.
      properties:
        notes: { type: string, nullable: true, maxLength: 2000 }

    PurchaseOrderIssueBlocker:
      type: string
      description: >
        A deterministic reason a Purchase Order may not be issued right now.
        Resolved at evaluation time from existing authorities and never
        persisted. `PO_READINESS_NOT_READY` reports the BE-17F verdict — it
        does not re-decide it.
      enum:
        - PO_NOT_DRAFT
        - PO_READINESS_INVALID
        - PO_READINESS_NOT_READY
        - NO_PURCHASE_ORDER_LINES
        - REQUEST_LINKAGE_INVALID
        - REQUEST_LINE_INVALID
        - VENDOR_NOT_COMMITTABLE
        - SCOPE_INCONSISTENT

    PurchaseOrderIssueReadiness:
      type: object
      description: >
        Read-only issue-readiness projection. Evaluating it mutates nothing and
        creates no second readiness authority.
      required:
        [purchaseOrderId, status, issuable, poReadiness, lineCount, blockers,
         availableActions, evaluatedAt]
      properties:
        purchaseOrderId: { $ref: "#/components/schemas/Uuid" }
        status: { $ref: "#/components/schemas/PurchaseOrderStatus" }
        issuable: { type: boolean }
        poReadiness:
          type: string
          nullable: true
          description: >
            The BE-17F verdict reported VERBATIM (e.g. READY / NOT_READY /
            BLOCKED); null when the readiness record is unreadable.
        lineCount: { type: integer, minimum: 0 }
        blockers:
          type: array
          items: { $ref: "#/components/schemas/PurchaseOrderIssueBlocker" }
        availableActions:
          type: array
          description: >
            Contains `ISSUE` only when `issuable` is true and the caller holds
            `purchase_order.manage`; otherwise empty.
          items: { $ref: "#/components/schemas/PurchaseOrderAction" }
        evaluatedAt: { type: string, format: date-time }

    PurchaseOrderLineRequestType:
      type: string
      enum: [MATERIAL_REQUEST, SERVICE_REQUEST]

    PurchaseOrderLine:
      type: object
      description: >
        A commercial commitment over ONE originating MR/SR line. Material
        Request / Service Request remain the single quantity authority: this
        line snapshots the authoritative quantity and adds commercial terms
        only — it is never a competing quantity ledger.
      required:
        [id, purchaseOrderId, clientId, buildingId, lineNumber,
         requestLineType, materialRequestId, serviceRequestId, itemId, uomId,
         sourceServiceId, description, quantitySnapshot, unitPrice, lineAmount,
         notes, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        purchaseOrderId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        lineNumber:
          type: integer
          minimum: 1
          description: Deterministic ordering; unique within the Purchase Order.
        requestLineType:
          $ref: "#/components/schemas/PurchaseOrderLineRequestType"
        materialRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Exactly one of materialRequestId / serviceRequestId is set, in
            agreement with requestLineType.
        serviceRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        itemId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Set for MATERIAL lines only.
        uomId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: UOM snapshot; no conversion authority.
        sourceServiceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            CR-BE-SVC-01 PART 04 — governed SERVICE identity snapshot (NULL for
            MATERIAL_REQUEST and un-governed SERVICE lines).
        description: { type: string }
        quantitySnapshot:
          type: number
          nullable: true
          description: >
            Frozen READ of the request line's approved (or requested) quantity.
            Always NULL for a SERVICE line.
        unitPrice: { type: number }
        lineAmount:
          type: number
          description: >
            Always DERIVED — quantitySnapshot × unitPrice, or the unit price
            itself when there is no quantity. Never caller-supplied.
        notes: { type: string, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    AddPurchaseOrderLineRequest:
      type: object
      description: >
        Quantity, amount, item, UOM and line number are all backend-derived and
        must not be supplied.
      required: [requestLineType, requestLineId, unitPrice]
      properties:
        requestLineType:
          $ref: "#/components/schemas/PurchaseOrderLineRequestType"
        requestLineId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: The originating Material Request / Service Request line.
        unitPrice: { type: number, minimum: 0 }
        description:
          type: string
          maxLength: 500
          description: Defaults to the request line's own description.
        notes: { type: string, nullable: true, maxLength: 2000 }

    UpdatePurchaseOrderLineRequest:
      type: object
      description: >
        Parent PO must be DRAFT. At least one field is required; the line
        amount is always re-derived from the frozen snapshot.
      minProperties: 1
      properties:
        unitPrice: { type: number, minimum: 0 }
        description: { type: string, maxLength: 500 }
        notes: { type: string, nullable: true, maxLength: 2000 }

    RemovePurchaseOrderLineResult:
      type: object
      description: Compact acknowledgement for a successfully deleted PO line.
      required: [removed, purchaseOrderId, lineNumber]
      properties:
        removed:
          type: boolean
          enum: [true]
        purchaseOrderId: { $ref: "#/components/schemas/Uuid" }
        lineNumber: { type: integer, minimum: 1 }

    WorkContractStatus:
      type: string
      description: >
        SPK lifecycle. DRAFT → ACTIVE → COMPLETED, with CANCELLED reachable
        from DRAFT or ACTIVE. COMPLETED is reachable ONLY from ACTIVE, so a
        mandate can never be completed without having been authorized. Both
        COMPLETED and CANCELLED are terminal.
      enum: [DRAFT, ACTIVE, COMPLETED, CANCELLED]

    WorkContractAction:
      type: string
      description: >
        Existing Work Contract command available to the calling user now.
        Tokens are upper-case and map one-to-one to registered command paths.
      enum: [ACTIVATE, COMPLETE, CANCEL]

    WorkContractAvailableActions:
      type: object
      description: >
        Backend-authoritative, caller-specific Work Contract actions. An empty
        list is authoritative; clients must not infer transitions from state.
      required: [workContractId, state, availableActions]
      properties:
        workContractId: { $ref: "#/components/schemas/Uuid" }
        state: { $ref: "#/components/schemas/WorkContractStatus" }
        availableActions:
          type: array
          items: { $ref: "#/components/schemas/WorkContractAction" }

    WorkContract:
      type: object
      description: >
        The SPK (Surat Perintah Kerja / Work Contract) — the EXECUTION MANDATE
        following a committed Purchase Order. Its own entity: it does not
        duplicate the BE-17H Work Order Procurement Binding domain and holds no
        Work Order reference. Carries no quantity, amount ledger or readiness
        verdict.
      required:
        [id, clientId, buildingId, vendorId, purchaseOrderId, spkNumber,
         spkDate, title, scopeDescription, startDate, endDate, notes, status,
         createdByUserId, activatedAt, activatedByUserId, completedAt,
         completedByUserId, cancelledAt, cancelledByUserId, createdAt,
         updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Inherited from the Purchase Order.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Inherited from the Purchase Order.
        vendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Inherited from the Purchase Order.
        purchaseOrderId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: The ISSUED commitment this mandate executes.
        spkNumber:
          type: string
          description: SPK identity foundation — unique within a Client.
        spkDate: { type: string, format: date }
        title: { type: string }
        scopeDescription: { type: string, nullable: true }
        startDate: { type: string, format: date, nullable: true }
        endDate: { type: string, format: date, nullable: true }
        notes: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/WorkContractStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        activatedAt: { type: string, format: date-time, nullable: true }
        activatedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        completedAt: { type: string, format: date-time, nullable: true }
        completedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        cancelledAt: { type: string, format: date-time, nullable: true }
        cancelledByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateWorkContractRequest:
      type: object
      description: >
        `purchaseOrderId` is the ONLY context input — Client, Building and
        Vendor are inherited from that ISSUED Purchase Order.
      required: [purchaseOrderId, spkNumber, spkDate, title]
      properties:
        purchaseOrderId: { $ref: "#/components/schemas/Uuid" }
        spkNumber:
          type: string
          minLength: 2
          maxLength: 64
          pattern: "^[A-Za-z0-9][A-Za-z0-9_\\-/]{1,63}$"
          description: Normalized uppercase and unique within the Client.
        spkDate: { type: string, format: date }
        title: { type: string, maxLength: 255 }
        scopeDescription: { type: string, nullable: true, maxLength: 4000 }
        startDate: { type: string, format: date, nullable: true }
        endDate:
          type: string
          format: date
          nullable: true
          description: Must be the same as or after startDate when both are set.
        notes: { type: string, nullable: true, maxLength: 2000 }

    UpdateWorkContractRequest:
      type: object
      description: >
        DRAFT only. At least one field is required. Identity, the PO reference,
        the inherited scope and all lifecycle provenance are immutable.
      minProperties: 1
      properties:
        spkDate: { type: string, format: date }
        title: { type: string, maxLength: 255 }
        scopeDescription: { type: string, nullable: true, maxLength: 4000 }
        startDate: { type: string, format: date, nullable: true }
        endDate: { type: string, format: date, nullable: true }
        notes: { type: string, nullable: true, maxLength: 2000 }

    WOProcurementStatus:
      type: string
      description: >
        Backend-authoritative: BOUND on create, READY when a READY BE-17F PO
        Readiness exists for the request, RECEIVED when a Receiving is linked.
      enum: [BOUND, READY, RECEIVED]

    WorkOrderProcurementBinding:
      type: object
      description: >
        BE-17H binding of a Work Order to its procurement context, EXTENDED by
        CR-BE-R2P-01 PART 05 with the SPK chain. Exactly one binding exists per
        Work Order; there is no parallel SPK ↔ Work Order binding domain.
      required:
        [id, clientId, buildingId, workOrderId, purchaseRequestId,
         procurementStatus, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        purchaseRequestId: { $ref: "#/components/schemas/Uuid" }
        materialRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        serviceRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        receivingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        workContractId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            PART 05 — the ACTIVE SPK authorizing this Work Order. NULL together
            with purchaseOrderId / vendorId on a request-only binding; set
            together once the chain is bound.
        purchaseOrderId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: PART 05 — derived from the SPK, never caller-supplied.
        vendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: PART 05 — derived from the SPK, never caller-supplied.
        procurementStatus: { $ref: "#/components/schemas/WOProcurementStatus" }
        notes: { type: string, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        workOrder:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            workOrderNumber: { type: string }
            title: { type: string }
            status: { type: string }
        purchaseRequest:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            requestNumber: { type: string }
            title: { type: string }
            status: { type: string }
        materialRequest:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            itemId: { $ref: "#/components/schemas/Uuid" }
            quantity: { type: number }
            status: { type: string }
        serviceRequest:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            serviceType: { type: string }
            title: { type: string }
            status: { type: string }
        receiving:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            receivingType: { type: string }
            status: { type: string }
        workContract:
          type: object
          nullable: true
          description: PART 05 — the bound SPK, when the chain is present.
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            spkNumber: { type: string }
            title: { type: string }
            status: { $ref: "#/components/schemas/WorkContractStatus" }
        purchaseOrder:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            poNumber: { type: string }
            status: { $ref: "#/components/schemas/PurchaseOrderStatus" }

    CreateWorkOrderProcurementBindingRequest:
      type: object
      required: [workOrderId, purchaseRequestId]
      properties:
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        purchaseRequestId: { $ref: "#/components/schemas/Uuid" }
        materialRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        serviceRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        workContractId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            PART 05 — optional ACTIVE SPK to bind. The Purchase Order and
            Vendor are derived from it.
        notes: { type: string, maxLength: 1000 }

    BindWorkContractRequest:
      type: object
      description: >
        The SPK reference is the ONLY input; Purchase Order, Vendor, Client and
        Building are all derived from it.
      required: [workContractId]
      properties:
        workContractId: { $ref: "#/components/schemas/Uuid" }

    LinkReceivingRequest:
      type: object
      required: [receivingId]
      properties:
        receivingId: { $ref: "#/components/schemas/Uuid" }

    VendorInvoiceAction:
      type: string
      description: Existing Vendor Invoice command available to the caller now.
      enum: [FINALIZE, CANCEL, VERIFY, RECORD_PAYMENT]

    VendorInvoiceAvailableActions:
      type: object
      description: >
        Caller-specific action projection. An empty list is authoritative.
      required:
        [vendorInvoiceId, state, verificationStatus, matchingStatus,
         settlementReadiness, availableActions]
      properties:
        vendorInvoiceId: { $ref: "#/components/schemas/Uuid" }
        state: { type: string, enum: [DRAFT, FINALIZED, CANCELLED] }
        verificationStatus: { type: string, enum: [PENDING, VERIFIED, DISCREPANCY] }
        matchingStatus: { $ref: "#/components/schemas/VendorInvoiceMatchingStatus" }
        settlementReadiness:
          $ref: "#/components/schemas/VendorSettlementReadinessStatus"
        availableActions:
          type: array
          items: { $ref: "#/components/schemas/VendorInvoiceAction" }

    VendorInvoiceEligiblePurchaseOrderStatus:
      type: string
      description: Existing PO state eligible for Vendor Invoice linkage.
      enum: [ISSUED]

    VendorInvoiceEligibleWorkContractStatus:
      type: string
      description: Existing SPK states eligible for Vendor Invoice linkage.
      enum: [ACTIVE, COMPLETED]

    VendorInvoice:
      type: object
      description: >
        A Vendor Invoice — the canonical payable invoice from an external Vendor.
        Not a Tenant Invoice, not a vendor-service-cost, not a Purchase Order.
      required:
        [id, clientId, buildingId, vendorId, invoiceNumber, invoiceDate,
         receivedDate, currency, invoiceAmount, status, vendorReference,
         vendorWorkId, workOrderId, completionReportId, serviceReportId,
         bastDocumentId, purchaseOrderId, workContractId, notes,
         createdByUserId, verificationStatus, verifiedByUserId, verifiedAt,
         verificationNotes, discrepancyCodes, paymentStatus, paidAmount,
         outstandingAmount, lastPaymentDate, finalizedAt, finalizedByUserId,
         cancelledAt, cancelledByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        invoiceNumber: { type: string }
        invoiceDate: { type: string, format: date }
        receivedDate: { type: string, format: date }
        currency: { type: string, enum: [IDR, USD, SGD, MYR, AUD, EUR, GBP, JPY, CNY] }
        invoiceAmount: { type: number }
        status: { type: string, enum: [DRAFT, FINALIZED, CANCELLED] }
        vendorReference: { type: string, nullable: true }
        vendorWorkId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        workOrderId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        completionReportId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        serviceReportId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        bastDocumentId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        purchaseOrderId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            The ISSUED Purchase Order being invoiced against. Validated against
            the invoice's Vendor, Client and Building and immutable after
            creation; never a second commercial authority.
        workContractId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            The ACTIVE or COMPLETED SPK under which the work was executed.
            Requires purchaseOrderId, must belong to that Purchase Order, and
            is immutable after invoice creation.
        notes: { type: string, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        verificationStatus: { type: string, enum: [PENDING, VERIFIED, DISCREPANCY] }
        verifiedByUserId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        verifiedAt: { type: string, format: date-time, nullable: true }
        verificationNotes: { type: string, nullable: true }
        discrepancyCodes:
          type: array
          items: { $ref: "#/components/schemas/VendorInvoiceMatchingReason" }
        paymentStatus:
          type: string
          enum: [UNPAID, PARTIALLY_PAID, PAID]
          description: >
            UNPAID before any payment, PARTIALLY_PAID while cumulative
            paidAmount is below invoiceAmount, and PAID only when equal.
        paidAmount:
          type: number
          description: Cumulative payment amount persisted by the backend.
        outstandingAmount:
          type: number
          description: >
            Database-generated invoiceAmount minus paidAmount. Consumers must
            not submit, override, or independently recalculate this authority.
        lastPaymentDate: { type: string, format: date, nullable: true }
        finalizedAt: { type: string, format: date-time, nullable: true }
        finalizedByUserId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        cancelledAt: { type: string, format: date-time, nullable: true }
        cancelledByUserId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateVendorInvoiceRequest:
      type: object
      required: [buildingId, invoiceNumber, invoiceDate, receivedDate, currency, invoiceAmount]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        invoiceNumber:
          type: string
          minLength: 2
          maxLength: 64
          pattern: "^[A-Za-z0-9][A-Za-z0-9_\\-/]{1,63}$"
          description: Normalized uppercase and unique within the Client.
        invoiceDate: { type: string, format: date }
        receivedDate: { type: string, format: date }
        currency: { type: string, enum: [IDR, USD, SGD, MYR, AUD, EUR, GBP, JPY, CNY] }
        invoiceAmount: { type: number, minimum: 0 }
        vendorReference: { type: string, nullable: true, maxLength: 255 }
        vendorWorkId: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        completionReportId: { $ref: "#/components/schemas/Uuid" }
        serviceReportId: { $ref: "#/components/schemas/Uuid" }
        bastDocumentId: { $ref: "#/components/schemas/Uuid" }
        purchaseOrderId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            CR-BE-R2P-01 PART 06 — optional procurement linkage. The Purchase
            Order must be ISSUED (409
            `VENDOR_INVOICE_PURCHASE_ORDER_NOT_ISSUED`) and must belong to the
            invoice's Vendor, Client and Building (400
            `VENDOR_INVOICE_PROCUREMENT_VENDOR_MISMATCH` /
            `VENDOR_INVOICE_PROCUREMENT_SCOPE_MISMATCH`).
        workContractId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Optional SPK linkage. Requires `purchaseOrderId`, must belong to
            that PO (400 `VENDOR_INVOICE_WORK_CONTRACT_PO_MISMATCH`), and must
            be ACTIVE or COMPLETED (409
            `VENDOR_INVOICE_WORK_CONTRACT_NOT_ELIGIBLE`). Client, Building,
            Vendor, PO, and SPK references are authoritative and immutable.
        notes: { type: string, nullable: true, maxLength: 2000 }

    UpdateVendorInvoiceRequest:
      type: object
      description: >
        Existing DRAFT-only PATCH contract. At least one commercial field is
        required. Identity, scope, operational references, purchaseOrderId,
        workContractId, lifecycle, verification, and payment fields are
        immutable and rejected when supplied.
      minProperties: 1
      properties:
        invoiceDate: { type: string, format: date }
        receivedDate: { type: string, format: date }
        currency:
          type: string
          enum: [IDR, USD, SGD, MYR, AUD, EUR, GBP, JPY, CNY]
        invoiceAmount: { type: number, minimum: 0 }
        vendorReference: { type: string, nullable: true, maxLength: 255 }
        notes: { type: string, nullable: true, maxLength: 2000 }

    RecordVendorPaymentRequest:
      type: object
      required: [amount]
      properties:
        amount:
          type: number
          minimum: 0
          exclusiveMinimum: true
          maximum: 10000000000000000
          exclusiveMaximum: true
          multipleOf: 0.01
          description: >
            Positive finite payment amount compatible with NUMERIC(18,2): fewer
            than 10^16 and no more than two decimal places.
        paymentDate:
          type: string
          format: date
          description: >
            Optional YYYY-MM-DD payment date. When supplied it becomes
            lastPaymentDate; when omitted, any existing lastPaymentDate is
            preserved.
        notes:
          type: string
          nullable: true
          maxLength: 2000
          description: >
            Optional payment audit note. Non-empty text is retained in the
            operational payment event; null or blank text records no note.

    VendorInvoiceMatchingStatus:
      type: string
      description: >
        Derived matching outcome, not an invoice lifecycle. MISMATCH takes
        precedence over NOT_READY when both kinds of reason exist.
      enum: [MATCHED, MISMATCH, NOT_READY]

    VendorInvoiceMatchingReason:
      type: string
      enum:
        [VENDOR_MISMATCH, WORK_ORDER_NOT_FOUND, WORK_ORDER_SCOPE_MISMATCH,
         VENDOR_WORK_NOT_FOUND, VENDOR_WORK_VENDOR_MISMATCH,
         PURCHASE_ORDER_NOT_FOUND, PURCHASE_ORDER_NOT_ISSUED,
         PURCHASE_ORDER_SCOPE_MISMATCH, PURCHASE_ORDER_VENDOR_MISMATCH,
         WORK_CONTRACT_NOT_FOUND, WORK_CONTRACT_PO_MISMATCH,
         WORK_CONTRACT_SCOPE_MISMATCH, WORK_CONTRACT_VENDOR_MISMATCH,
         WORK_CONTRACT_NOT_ELIGIBLE, PURCHASE_ORDER_LINES_NOT_FOUND,
         CURRENCY_MISMATCH, INVOICE_PO_AMOUNT_MISMATCH,
         MATERIAL_REQUEST_NOT_FOUND, MATERIAL_RECEIVING_NOT_COMPLETE,
         MATERIAL_RECEIVING_OVER_APPROVED, SERVICE_RECEIVING_NOT_FINALIZED,
         COMPLETION_REPORT_NOT_FOUND, COMPLETION_REPORT_NOT_SUBMITTED,
         SERVICE_REPORT_NOT_FOUND, SERVICE_REPORT_NOT_FINALIZED,
         BAST_NOT_FOUND, BAST_REQUIRED_NOT_AVAILABLE, BAST_NOT_ACCEPTED,
         AMOUNT_INVALID]

    InvoiceMatchingCheck:
      type: object
      required: [check, applicable, status, matched, discrepancy]
      properties:
        check: { type: string }
        applicable:
          type: boolean
          description: False when the optional authoritative reference is absent.
        status: { $ref: "#/components/schemas/VendorInvoiceMatchingStatus" }
        matched:
          type: boolean
          description: Backward-compatible convenience; true iff status is MATCHED.
        discrepancy:
          allOf: [{ $ref: "#/components/schemas/VendorInvoiceMatchingReason" }]
          nullable: true

    InvoiceMatchingResult:
      type: object
      description: >
        One canonical, read-only Vendor Invoice matching result consumed by
        verification. It reuses PO/SPK, PO Lines, MR/SR/Receiving and existing
        completion evidence without persisting a second quantity authority.
      required:
        [invoiceId, status, verificationEligible, vendorMatch,
         purchaseOrderMatch, workContractMatch, amountMatch, receivingMatch,
         workOrderMatch, vendorWorkMatch, completionMatch, serviceMatch,
         bastMatch, allMatched, mismatchCodes, notReadyCodes,
         discrepancyCodes, evaluatedAt]
      properties:
        invoiceId: { $ref: "#/components/schemas/Uuid" }
        status: { $ref: "#/components/schemas/VendorInvoiceMatchingStatus" }
        verificationEligible: { type: boolean }
        vendorMatch: { $ref: "#/components/schemas/InvoiceMatchingCheck" }
        purchaseOrderMatch: { $ref: "#/components/schemas/InvoiceMatchingCheck" }
        workContractMatch: { $ref: "#/components/schemas/InvoiceMatchingCheck" }
        amountMatch: { $ref: "#/components/schemas/InvoiceMatchingCheck" }
        receivingMatch: { $ref: "#/components/schemas/InvoiceMatchingCheck" }
        workOrderMatch: { $ref: "#/components/schemas/InvoiceMatchingCheck" }
        vendorWorkMatch: { $ref: "#/components/schemas/InvoiceMatchingCheck" }
        completionMatch: { $ref: "#/components/schemas/InvoiceMatchingCheck" }
        serviceMatch: { $ref: "#/components/schemas/InvoiceMatchingCheck" }
        bastMatch: { $ref: "#/components/schemas/InvoiceMatchingCheck" }
        allMatched:
          type: boolean
          description: Backward-compatible convenience; true iff status is MATCHED.
        mismatchCodes:
          type: array
          items: { $ref: "#/components/schemas/VendorInvoiceMatchingReason" }
        notReadyCodes:
          type: array
          items: { $ref: "#/components/schemas/VendorInvoiceMatchingReason" }
        discrepancyCodes:
          type: array
          description: Combined mismatchCodes + notReadyCodes.
          items: { $ref: "#/components/schemas/VendorInvoiceMatchingReason" }
        evaluatedAt: { type: string, format: date-time }

    VendorInvoiceVerifyResult:
      type: object
      required: [invoice, matching]
      properties:
        invoice: { $ref: "#/components/schemas/VendorInvoice" }
        matching: { $ref: "#/components/schemas/InvoiceMatchingResult" }

    VendorSettlementReadinessStatus:
      type: string
      description: >
        Derived payment eligibility, not a parallel payment lifecycle. SETTLED
        is derived from the existing PAID state and zero outstanding amount.
      enum: [READY, NOT_READY, SETTLED]

    VendorSettlementReason:
      type: string
      enum:
        [INVOICE_NOT_FINALIZED, INVOICE_CANCELLED, INVOICE_NOT_VERIFIED,
         MATCHING_NOT_READY, MATCHING_MISMATCH, SCOPE_MISMATCH,
         INVALID_INVOICE_AMOUNT, INVALID_CURRENCY, INVALID_PAYMENT_STATE]

    VendorSettlementAction:
      type: string
      description: Existing payment command advertised only when READY.
      enum: [RECORD_PAYMENT]

    VendorSettlementReadinessResult:
      type: object
      description: >
        Canonical read-only payment eligibility over existing invoice,
        matching, consistency and payment authorities. No balance or settlement
        state is persisted by this projection.
      required:
        [invoiceId, readiness, invoiceStatus, verificationStatus,
         matchingStatus, paymentStatus, currency, invoiceAmount, paidAmount,
         outstandingAmount, reasons, blockers, matchingReasons,
         consistencyReasons, availableActions, evaluatedAt]
      properties:
        invoiceId: { $ref: "#/components/schemas/Uuid" }
        readiness: { $ref: "#/components/schemas/VendorSettlementReadinessStatus" }
        invoiceStatus: { type: string, enum: [DRAFT, FINALIZED, CANCELLED] }
        verificationStatus: { type: string, enum: [PENDING, VERIFIED, DISCREPANCY] }
        matchingStatus: { $ref: "#/components/schemas/VendorInvoiceMatchingStatus" }
        paymentStatus: { type: string, enum: [UNPAID, PARTIALLY_PAID, PAID] }
        currency: { type: string, enum: [IDR, USD, SGD, MYR, AUD, EUR, GBP, JPY, CNY] }
        invoiceAmount: { type: number }
        paidAmount: { type: number }
        outstandingAmount: { type: number }
        reasons:
          type: array
          items: { $ref: "#/components/schemas/VendorSettlementReason" }
        blockers:
          type: array
          deprecated: true
          description: Backward-compatible alias of reasons.
          items: { $ref: "#/components/schemas/VendorSettlementReason" }
        matchingReasons:
          type: array
          items: { $ref: "#/components/schemas/VendorInvoiceMatchingReason" }
        consistencyReasons:
          type: array
          items: { type: string }
        availableActions:
          type: array
          items: { $ref: "#/components/schemas/VendorSettlementAction" }
        evaluatedAt: { type: string, format: date-time }

    VendorConsistencyCheck:
      type: object
      required: [check, consistent, reason]
      properties:
        check: { type: string }
        consistent: { type: boolean }
        reason: { type: string, nullable: true }

    VendorConsistencyResult:
      type: object
      description: >
        Vendor consistency cross-check result. Validates consistency across
        the commercial-to-operational chain.
      required: [vendorWorkVendorMatch, workOrderAssignedVendorMatch, procurementVendorMatch, clientIsolationMatch, buildingIsolationMatch, allConsistent, inconsistencyCodes]
      properties:
        vendorWorkVendorMatch: { $ref: "#/components/schemas/VendorConsistencyCheck" }
        workOrderAssignedVendorMatch: { $ref: "#/components/schemas/VendorConsistencyCheck" }
        procurementVendorMatch: { $ref: "#/components/schemas/VendorConsistencyCheck" }
        clientIsolationMatch: { $ref: "#/components/schemas/VendorConsistencyCheck" }
        buildingIsolationMatch: { $ref: "#/components/schemas/VendorConsistencyCheck" }
        allConsistent: { type: boolean }
        inconsistencyCodes:
          type: array
          items:
            type: string
            enum:
              [INVOICE_VENDOR_WORK_MISMATCH, VENDOR_WORK_WORK_ORDER_MISMATCH,
               PROCUREMENT_VENDOR_MISMATCH, CROSS_CLIENT_REFERENCE,
               CROSS_BUILDING_REFERENCE]

    R2PTraceDocumentType:
      type: string
      enum:
        [PURCHASE_REQUEST, MATERIAL_REQUEST, SERVICE_REQUEST,
         PROCUREMENT_APPROVAL, PURCHASE_ORDER, PURCHASE_ORDER_LINE,
         WORK_CONTRACT, RECEIVING, WORK_ORDER, VENDOR_WORK,
         COMPLETION_REPORT, SERVICE_REPORT, BAST, VENDOR_INVOICE,
         INVOICE_MATCHING, INVOICE_VERIFICATION, PAYMENT_STATE,
         SETTLEMENT_READINESS]

    R2PTraceRelationshipType:
      type: string
      enum:
        [SOURCE_OF, APPROVED_BY, COMMITTED_BY_LINE, BELONGS_TO, MANDATES,
         RECEIVED_AS, BOUND_TO, EXECUTED_AS, REPORTED_BY, ACCEPTED_BY,
         INVOICED_BY, PROJECTS]

    R2PTraceDocument:
      type: object
      required: [documentType, documentId, documentNumber, status]
      properties:
        documentType: { $ref: "#/components/schemas/R2PTraceDocumentType" }
        documentId: { $ref: "#/components/schemas/Uuid" }
        documentNumber: { type: string, nullable: true }
        status: { type: string, nullable: true }

    R2PTraceRelationship:
      type: object
      required:
        [relationship, sourceDocumentType, sourceDocumentId,
         targetDocumentType, targetDocumentId]
      properties:
        relationship: { $ref: "#/components/schemas/R2PTraceRelationshipType" }
        sourceDocumentType: { $ref: "#/components/schemas/R2PTraceDocumentType" }
        sourceDocumentId: { $ref: "#/components/schemas/Uuid" }
        targetDocumentType: { $ref: "#/components/schemas/R2PTraceDocumentType" }
        targetDocumentId: { $ref: "#/components/schemas/Uuid" }

    VendorInvoiceTrace:
      type: object
      description: >
        End-to-end traceability references along the vendor commercial chain.
        Only identifiers/links — no large reporting data.
      required:
        [invoiceId, vendorId, clientId, buildingId, vendorWorkId, workOrderId,
         completionReportId, serviceReportId, bastDocumentId,
         purchaseOrderId, workContractId, purchaseOrderStatus,
         purchaseOrderNumber, workContractStatus, workContractSpkNumber,
         workOrderAssignedVendorId, workOrderBastRequirement,
         bastAcceptanceStatus, bastIsLegacyProjection,
         verificationStatus, paymentStatus, invoiceStatus, documents,
         relationships, matching, settlementReadiness, generatedAt]
      properties:
        invoiceId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        vendorWorkId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        workOrderId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        completionReportId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        serviceReportId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        bastDocumentId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        purchaseOrderId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: CR-BE-R2P-01 PART 06 — the linked Purchase Order.
        workContractId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: CR-BE-R2P-01 PART 06 — the linked Work Contract (SPK).
        purchaseOrderStatus:
          allOf: [{ $ref: "#/components/schemas/PurchaseOrderStatus" }]
          nullable: true
          description: Read VERBATIM from the Purchase Order authority.
        purchaseOrderNumber: { type: string, nullable: true }
        workContractStatus:
          allOf: [{ $ref: "#/components/schemas/WorkContractStatus" }]
          nullable: true
          description: Read VERBATIM from the Work Contract authority.
        workContractSpkNumber: { type: string, nullable: true }
        workOrderAssignedVendorId: { allOf: [{ $ref: "#/components/schemas/Uuid" }], nullable: true }
        workOrderBastRequirement: { type: string, nullable: true }
        bastAcceptanceStatus: { type: string, nullable: true }
        bastIsLegacyProjection: { type: boolean }
        verificationStatus: { type: string, enum: [PENDING, VERIFIED, DISCREPANCY] }
        paymentStatus: { type: string, enum: [UNPAID, PARTIALLY_PAID, PAID] }
        invoiceStatus: { type: string, enum: [DRAFT, FINALIZED, CANCELLED] }
        documents:
          type: array
          items: { $ref: "#/components/schemas/R2PTraceDocument" }
        relationships:
          type: array
          items: { $ref: "#/components/schemas/R2PTraceRelationship" }
        matching: { $ref: "#/components/schemas/InvoiceMatchingResult" }
        settlementReadiness:
          $ref: "#/components/schemas/VendorSettlementReadinessResult"
        generatedAt: { type: string, format: date-time }

    # -------------------------------------------------------------------------
    # CR-BE-MAT-01 — Material fulfilment chain schemas.
    # Traceable stable references: MaterialRequest.purchaseRequestId →
    # Receiving.materialRequestId → Receiving.stockMovementId (STOCK_IN) →
    # StockMovement → StockBalance (derived state) →
    # WorkOrderMaterialUsage.stockMovementId (STOCK_OUT) →
    # WorkOrderMaterialUsage.unitCost/totalCost (operational material cost).
    # -------------------------------------------------------------------------
    MaterialReservationStatus:
      type: string
      description: >
        ACTIVE allocation → RELEASED, CANCELLED, or CONSUMED. Partial issue
        consumption remains ACTIVE with a positive remaining allocation; full
        consumption becomes CONSUMED with zero remaining allocation. Reservation
        state is not a demand or stock authority; it references an existing
        Material Request.
      enum: [ACTIVE, RELEASED, CANCELLED, CONSUMED]
    MaterialReservation:
      type: object
      description: >
        CR-BE-INV-CONTROL-01 PART 01–02 allocation and controlled consumption against an existing approved
        Material Request. `reservedQuantity` is the original allocation;
        `consumedQuantity` and generated `remainingQuantity` track controlled
        issue consumption. Partial consumption remains ACTIVE; full consumption
        becomes CONSUMED. The existing
        `inventory_stock_balances.reserved_quantity` is updated transactionally
        and `quantityOnHand` is never changed by reservation progress.
      required:
        [id, clientId, buildingId, materialRequestId, warehouseId, itemId,
         uomId, reservedQuantity, consumedQuantity, remainingQuantity, status,
         createdByUserId, releasedByUserId, cancelledByUserId, consumedByUserId,
         notes, createdAt, releasedAt, cancelledAt, consumedAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        materialRequestId: { $ref: "#/components/schemas/Uuid" }
        warehouseId: { $ref: "#/components/schemas/Uuid" }
        itemId: { $ref: "#/components/schemas/Uuid" }
        uomId: { type: string, format: uuid, nullable: true }
        reservedQuantity: { type: number, exclusiveMinimum: 0 }
        consumedQuantity: { type: number, minimum: 0 }
        remainingQuantity: { type: number, minimum: 0 }
        status: { $ref: "#/components/schemas/MaterialReservationStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        releasedByUserId: { type: string, format: uuid, nullable: true }
        cancelledByUserId: { type: string, format: uuid, nullable: true }
        consumedByUserId: { type: string, format: uuid, nullable: true }
        notes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        releasedAt: { type: string, format: date-time, nullable: true }
        cancelledAt: { type: string, format: date-time, nullable: true }
        consumedAt: { type: string, format: date-time, nullable: true }
        updatedAt: { type: string, format: date-time }
        materialRequest:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            itemId: { $ref: "#/components/schemas/Uuid" }
            warehouseId: { type: string, format: uuid, nullable: true }
            quantity: { type: number }
            approvedQuantity: { type: number, nullable: true }
            status: { type: string }
        warehouse:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            buildingId: { $ref: "#/components/schemas/Uuid" }
        item:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            itemType: { type: string }
            uomId: { type: string, format: uuid, nullable: true }
    MaterialReservationCreateRequest:
      type: object
      required: [warehouseId, quantity]
      properties:
        warehouseId: { $ref: "#/components/schemas/Uuid" }
        itemId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Optional assertion; the item is derived from the Material Request.
        uomId:
          type: string
          format: uuid
          nullable: true
          description: Optional assertion; must match the Item/MR UOM exactly.
        quantity: { type: number, exclusiveMinimum: 0 }
        notes: { type: string, nullable: true, maxLength: 1000 }
    MaterialRequestStatus:
      type: string
      description: OPEN → APPROVED (PART 02 freeze) | CANCELLED (correction path).
      enum: [OPEN, APPROVED, CANCELLED]
    MaterialRequest:
      type: object
      description: >
        BE-17B Material Request line — the request authority. `quantity` is
        the preserved requested quantity; `approvedQuantity` (PART 02) is the
        authoritative fulfilment cap, defaulting to the requested quantity
        when approval does not explicitly change it, never above it, and NULL
        on historical/unapproved lines (downstream falls back to `quantity`).
        `uomId` is the UOM context captured from the Item Master at creation
        and frozen after approval. Detail reads derive `receivedQuantity` and
        `remainingQuantity`.
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        purchaseRequestId: { $ref: "#/components/schemas/Uuid" }
        itemId: { $ref: "#/components/schemas/Uuid" }
        warehouseId: { type: string, format: uuid, nullable: true }
        quantity: { type: number, description: Requested quantity (preserved). }
        approvedQuantity: { type: number, nullable: true }
        approvedAt: { type: string, format: date-time, nullable: true }
        approvedByUserId: { type: string, format: uuid, nullable: true }
        receivedQuantity:
          type: number
          description: Cumulative valid received quantity (detail reads only).
        remainingQuantity:
          type: number
          description: >
            Derived (approvedQuantity ?? quantity) − receivedQuantity (detail
            reads only). No separate remaining-quantity store exists.
        uomId: { type: string, format: uuid, nullable: true }
        requiredDate: { type: string, format: date-time, nullable: true }
        notes: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/MaterialRequestStatus" }
        requestedByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        purchaseRequest:
          type: object
          nullable: true
          description: Resolved Purchase Request context (detail reads).
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            requestNumber: { type: string }
            title: { type: string }
            status: { type: string }
        item:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            itemType: { type: string }
            uomId: { type: string, format: uuid, nullable: true }
        warehouse:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            buildingId: { $ref: "#/components/schemas/Uuid" }
    MaterialRequestCreateRequest:
      type: object
      required: [itemId, quantity]
      properties:
        itemId: { $ref: "#/components/schemas/Uuid" }
        quantity: { type: number, description: Must be > 0. }
        uomId:
          type: string
          format: uuid
          nullable: true
          description: Optional; must equal the item UOM exactly (no conversion).
        warehouseId: { type: string, format: uuid, nullable: true }
        requiredDate: { type: string, format: date-time, nullable: true }
        notes: { type: string, nullable: true }
    MaterialRequestUpdateRequest:
      type: object
      description: Only OPEN lines are editable (PART 02 freeze).
      properties:
        quantity: { type: number }
        uomId: { type: string, format: uuid, nullable: true }
        warehouseId: { type: string, format: uuid, nullable: true }
        requiredDate: { type: string, format: date-time, nullable: true }
        notes: { type: string, nullable: true }

    ProcurementApprovalRequestType:
      type: string
      enum: [PURCHASE_REQUEST, MATERIAL_REQUEST, SERVICE_REQUEST, RFQ]
    ProcurementApprovalStatus:
      type: string
      enum: [PENDING, APPROVED, REJECTED]
    ProcurementApproval:
      type: object
      description: >
        BE-17D approval binding. Decisions are append-only; an APPROVED
        MATERIAL_REQUEST binding establishes the line's approved quantity
        (CR-BE-MAT-01 PART 02).
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        requestType: { $ref: "#/components/schemas/ProcurementApprovalRequestType" }
        purchaseRequestId: { type: string, format: uuid, nullable: true }
        materialRequestId: { type: string, format: uuid, nullable: true }
        serviceRequestId: { type: string, format: uuid, nullable: true }
        rfqId: { type: string, format: uuid, nullable: true, description: Populated for RFQ award approval targets. }
        recommendationId: { type: string, format: uuid, nullable: true, description: Exact RFQ recommendation for RFQ approval targets. }
        requestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Convenience alias of the populated request reference.
        approvalType: { type: string }
        approverUserId: { $ref: "#/components/schemas/Uuid" }
        status: { $ref: "#/components/schemas/ProcurementApprovalStatus" }
        decidedAt: { type: string, format: date-time, nullable: true }
        decisionNotes: { type: string, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    ProcurementApprovalCreateRequest:
      type: object
      required: [requestType, requestId, approvalType, approverUserId]
      properties:
        requestType: { $ref: "#/components/schemas/ProcurementApprovalRequestType" }
        requestId: { $ref: "#/components/schemas/Uuid" }
        approvalType:
          type: string
          description: Existing data-driven approval type. RFQ awards require `RFQ_AWARD`.
        approverUserId: { $ref: "#/components/schemas/Uuid" }
        recommendationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Required with requestType RFQ; binds approval to the exact recommendation.
    ProcurementApprovalDecisionRequest:
      type: object
      properties:
        decisionNotes: { type: string, nullable: true }
        approvedQuantity:
          type: number
          nullable: true
          description: >
            CR-BE-MAT-01 PART 02 — only when APPROVING a MATERIAL_REQUEST
            binding; > 0 and ≤ the requested line quantity. Omitted →
            approved quantity defaults to the requested quantity.
    ProcurementApprovalAvailableActions:
      type: object
      properties:
        approvalId: { $ref: "#/components/schemas/Uuid" }
        state: { $ref: "#/components/schemas/ProcurementApprovalStatus" }
        availableActions:
          type: array
          items: { type: string, enum: [APPROVE, REJECT] }

    ReceivingType: { type: string, enum: [MATERIAL, SERVICE] }
    ReceivingStatus: { type: string, enum: [RECEIVED, FINALIZED] }
    Receiving:
      type: object
      description: >
        BE-17G Receiving — the receiving authority. `materialRequestId`
        (PART 01) is the stable binding to the fulfilled Material Request
        line; `stockMovementId` is the STOCK_IN ledger movement posted in the
        same transaction; `uomId` is the receipt-time UOM snapshot (PART 04,
        NULL on historical/service rows).
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        requestType: { type: string, enum: [PURCHASE_REQUEST, SERVICE_REQUEST] }
        purchaseRequestId: { type: string, format: uuid, nullable: true }
        serviceRequestId: { type: string, format: uuid, nullable: true }
        materialRequestId: { type: string, format: uuid, nullable: true }
        requestId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        receivingType: { $ref: "#/components/schemas/ReceivingType" }
        itemId: { type: string, format: uuid, nullable: true }
        warehouseId: { type: string, format: uuid, nullable: true }
        quantity: { type: number, nullable: true }
        uomId: { type: string, format: uuid, nullable: true }
        stockMovementId: { type: string, format: uuid, nullable: true }
        receivedByUserId: { $ref: "#/components/schemas/Uuid" }
        receivedAt: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/ReceivingStatus" }
        notes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        vendor:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            vendorCode: { type: string }
            vendorName: { type: string }
            status: { type: string }
        item:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            itemType: { type: string }
        warehouse:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            buildingId: { $ref: "#/components/schemas/Uuid" }
    ReceivingCreateRequest:
      type: object
      required: [requestType, requestId, vendorId, receivingType]
      properties:
        requestType: { type: string, enum: [PURCHASE_REQUEST, SERVICE_REQUEST] }
        requestId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        receivingType: { $ref: "#/components/schemas/ReceivingType" }
        materialRequestId:
          type: string
          format: uuid
          nullable: true
          description: >
            PART 01 — Material Request line being fulfilled (MATERIAL
            receiving on a PURCHASE_REQUEST only). Enables line identity
            enforcement and the cumulative over-receipt guard.
        itemId: { type: string, format: uuid, nullable: true, description: Required for MATERIAL receiving. }
        warehouseId: { type: string, format: uuid, nullable: true, description: Required for MATERIAL receiving. }
        quantity: { type: number, nullable: true, description: Required (> 0) for MATERIAL receiving. }
        uomId:
          type: string
          format: uuid
          nullable: true
          description: >
            PART 04 — optional explicit UOM; must match the resolved MR-line /
            item UOM exactly (`RECEIVING_UOM_INCOMPATIBLE` otherwise).
        notes: { type: string, nullable: true }
    ReceivingUpdateRequest:
      type: object
      properties:
        notes: { type: string, nullable: true }

    StockMovementType: { type: string, enum: [STOCK_IN, STOCK_OUT] }
    StockMovement:
      type: object
      description: >
        BE-16D append-only ledger entry — the stock-ledger authority
        (PART 03). Every balance change has one; `source` carries the
        originating authority (e.g. PROCUREMENT_RECEIVING, STOCK_ADJUSTMENT,
        BALANCE_INITIALIZATION, TRANSFER:{id}, WORK_ORDER:{id}). `uomId` is
        the movement-time UOM snapshot (PART 04, NULL on historical rows).
        `resultingQuantityOnHand`/`resultingAvailableQuantity` snapshot the
        balance after the movement.
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        warehouseId: { $ref: "#/components/schemas/Uuid" }
        itemId: { $ref: "#/components/schemas/Uuid" }
        movementType: { $ref: "#/components/schemas/StockMovementType" }
        quantity: { type: number }
        uomId: { type: string, format: uuid, nullable: true }
        movementDate: { type: string, format: date-time }
        reference: { type: string, nullable: true }
        source: { type: string, nullable: true }
        performedByUserId: { $ref: "#/components/schemas/Uuid" }
        notes: { type: string, nullable: true }
        resultingQuantityOnHand: { type: number }
        resultingAvailableQuantity: { type: number }
        createdAt: { type: string, format: date-time }
        warehouse:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            buildingId: { $ref: "#/components/schemas/Uuid" }
        item:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            itemType: { type: string }
        resultingBalance:
          type: object
          nullable: true
          properties:
            quantityOnHand: { type: number }
            reservedQuantity: { type: number }
            availableQuantity: { type: number }
    StockMovementCreateRequest:
      type: object
      required: [itemId, movementType, quantity]
      properties:
        warehouseId:
          type: string
          format: uuid
          nullable: true
          description: Required on the Client-scoped route; taken from the path otherwise.
        itemId: { $ref: "#/components/schemas/Uuid" }
        movementType: { $ref: "#/components/schemas/StockMovementType" }
        quantity: { type: number, description: Must be > 0. }
        movementDate: { type: string, format: date-time, nullable: true }
        reference: { type: string, nullable: true }
        source: { type: string, nullable: true }
        notes: { type: string, nullable: true }

    StockBalance:
      type: object
      description: >
        BE-16C derived/current stock state per warehouse + item. Mutated ONLY
        through ledger movements (PART 03); `availableQuantity` =
        quantityOnHand − reservedQuantity is DB-generated.
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        warehouseId: { $ref: "#/components/schemas/Uuid" }
        itemId: { $ref: "#/components/schemas/Uuid" }
        quantityOnHand: { type: number }
        reservedQuantity: { type: number }
        availableQuantity: { type: number }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    StockBalanceInitializeRequest:
      type: object
      required: [itemId]
      properties:
        warehouseId:
          type: string
          format: uuid
          nullable: true
          description: Required on the Client-scoped route; taken from the path otherwise.
        itemId: { $ref: "#/components/schemas/Uuid" }
        quantityOnHand:
          type: number
          nullable: true
          description: >
            Default 0. A non-zero seed enters through the ledger as a
            BALANCE_INITIALIZATION STOCK_IN (PART 03).
        reservedQuantity: { type: number, nullable: true, description: Default 0; ≤ quantityOnHand. }

    WorkOrderMaterialUsage:
      type: object
      description: >
        BE-16I Work Order Material Usage — the usage authority (append-only,
        immutable). `stockMovementId` (PART 06) is the stable link to the
        STOCK_OUT ledger movement committed in the same transaction; `uomId`
        (PART 04) and `unitCost`/`totalCost`/`currency`/`costSource`/
        `costReference` (PART 05 operational cost snapshot, `totalCost` =
        quantity × unitCost computed in NUMERIC) never change after issue.
        NULL snapshot fields on historical / cost-less rows.
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        warehouseId: { $ref: "#/components/schemas/Uuid" }
        itemId: { $ref: "#/components/schemas/Uuid" }
        materialRequestId: { type: string, format: uuid, nullable: true }
        reservationId: { type: string, format: uuid, nullable: true }
        quantity: { type: number }
        uomId: { type: string, format: uuid, nullable: true }
        unitCost: { type: number, nullable: true }
        totalCost: { type: number, nullable: true }
        currency:
          type: string
          nullable: true
          pattern: "^[A-Z]{3}$"
          description: >
            Currency snapshot at issue/use time. NULL remains UNKNOWN for
            historical / cost-less rows. Value is governed (ACTIVE Currency
            Master entry + Client-allowed) on new costed usage; historical
            NULL is never inferred, substituted, or converted.
        costSource: { type: string, nullable: true }
        costReference: { type: string, nullable: true }
        stockMovementId: { type: string, format: uuid, nullable: true }
        usedByUserId: { $ref: "#/components/schemas/Uuid" }
        usedAt: { type: string, format: date-time }
        reference: { type: string, nullable: true }
        notes: { type: string, nullable: true }
        resultingQuantityOnHand: { type: number }
        resultingAvailableQuantity: { type: number }
        createdAt: { type: string, format: date-time }
        workOrder:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            workOrderNumber: { type: string }
            title: { type: string }
            status: { type: string }
        warehouse:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            buildingId: { $ref: "#/components/schemas/Uuid" }
        item:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            itemType: { type: string }
        resultingBalance:
          type: object
          nullable: true
          properties:
            quantityOnHand: { type: number }
            reservedQuantity: { type: number }
            availableQuantity: { type: number }
    WorkOrderMaterialUsageCreateRequest:
      type: object
      required: [itemId, warehouseId, quantity]
      properties:
        itemId: { $ref: "#/components/schemas/Uuid" }
        warehouseId: { $ref: "#/components/schemas/Uuid" }
        materialRequestId:
          type: string
          format: uuid
          nullable: true
          description: Optional assertion; authoritative source is resolved from the Work Order procurement binding.
        reservationId:
          type: string
          format: uuid
          nullable: true
          description: Optional ACTIVE reservation allocation to consume.
        quantity: { type: number, description: Must be > 0 and within authorized demand and applicable stock. }
        uomId:
          type: string
          format: uuid
          nullable: true
          description: PART 06 — optional; must equal the item UOM (no conversion).
        unitCost:
          type: number
          nullable: true
          description: PART 05 — optional operational unit cost snapshot (≥ 0).
        currency:
          type: string
          nullable: true
          pattern: "^[A-Z]{3}$"
          description: >
            Required when unitCost is supplied (fail-closed with
            WO_MATERIAL_USAGE_CURRENCY_REQUIRED). Must be an ACTIVE Currency
            Master entry allowed for the resolved Client
            (CURRENCY_INACTIVE_OR_UNKNOWN / CLIENT_CURRENCY_NOT_ALLOWED).
            Optional/null for a non-costed usage. No IDR/default/base inference
            and no FX/conversion.
        costSource: { type: string, nullable: true, description: Defaults to MANUAL when unitCost is set. }
        costReference: { type: string, nullable: true }
        usedAt: { type: string, format: date-time, nullable: true }
        reference:
          type: string
          nullable: true
          description: Duplicate-issue protection — unique per Work Order.
        notes: { type: string, nullable: true }
    WorkOrderMaterialCostSummary:
      type: object
      description: >
        PART 05 — deterministic Work Order → Material Usage → total material
        cost aggregation over immutable snapshots. Material cost only.
      properties:
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        usageCount: { type: integer }
        costedUsageCount: { type: integer }
        totalMaterialCost: { type: number }
        byCurrency:
          type: array
          items:
            type: object
            properties:
              currency: { type: string, nullable: true }
              totalCost: { type: number }

    # ------------------------------------------------------------------
    # CR-BE-MOB-01 PART 01 — Housekeeping operational schemas (BE-11).
    # ------------------------------------------------------------------
    DailyCleaningStatus:
      type: string
      enum: [OPEN, ASSIGNED, IN_PROGRESS, COMPLETED, CANCELLED]
      description: BE-07 generated-task status projected as daily cleaning.
    DailyCleaning:
      type: object
      description: >-
        BE-11C operational view of a BE-07 generated task. `id` equals
        `taskId` and is the authoritative id for start/complete/cancel.
      required:
        [id, taskId, clientId, buildingId, cleaningAreaId, scheduleBindingId,
         scheduleDefinitionId, operationalDate, occurrenceAt, status,
         startedAt, completedAt, completedByUserId, completionNotes,
         cleaningArea, schedule, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        taskId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        scheduleBindingId: { $ref: "#/components/schemas/Uuid" }
        scheduleDefinitionId: { $ref: "#/components/schemas/Uuid" }
        operationalDate: { type: string, format: date }
        occurrenceAt: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/DailyCleaningStatus" }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        completedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        completionNotes: { type: string, nullable: true }
        cleaningArea:
          type: object
          required: [id, code, name, cleaningAreaType, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            cleaningAreaType: { type: string }
            status: { type: string }
            floorId: { type: string, format: uuid, nullable: true }
            areaId: { type: string, format: uuid, nullable: true }
            roomId: { type: string, format: uuid, nullable: true }
            spaceId: { type: string, format: uuid, nullable: true }
            functionalLocationId: { type: string, format: uuid, nullable: true }
        schedule:
          type: object
          required: [id, code, name, targetType, targetId, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            targetType: { type: string }
            targetId: { $ref: "#/components/schemas/Uuid" }
            status: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CleaningAssignmentStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    CleaningAssignment:
      type: object
      required:
        [id, taskId, assigneeType, workforceProfileId, teamId,
         assignedByUserId, assignedAt, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        taskId: { $ref: "#/components/schemas/Uuid" }
        assigneeType: { type: string, enum: [WORKFORCE, TEAM] }
        workforceProfileId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        teamId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        assignedByUserId: { $ref: "#/components/schemas/Uuid" }
        assignedAt: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/CleaningAssignmentStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        assignee:
          type: object
          nullable: true
          properties:
            type: { type: string, enum: [WORKFORCE, TEAM] }
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
    CreateCleaningAssignmentRequest:
      type: object
      required: [assigneeType]
      properties:
        assigneeType: { type: string, enum: [WORKFORCE, TEAM] }
        workforceProfileId:
          type: string
          format: uuid
          description: Required when assigneeType is WORKFORCE.
        teamId:
          type: string
          format: uuid
          description: Required when assigneeType is TEAM.

    HousekeepingBindingStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    ToiletInspectionBinding:
      type: object
      required:
        [id, clientId, buildingId, cleaningAreaId, checklistTemplateId,
         roomId, functionalLocationId, description, status, createdByUserId,
         createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        roomId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        functionalLocationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/HousekeepingBindingStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        cleaningArea:
          type: object
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            cleaningAreaType: { type: string }
            status: { type: string }
        checklistTemplate:
          type: object
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string }
        room:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
        functionalLocation:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string }
    CreateToiletInspectionBindingRequest:
      type: object
      required: [cleaningAreaId, checklistTemplateId]
      properties:
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        roomId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        description: { type: string, maxLength: 512, nullable: true }
        status: { $ref: "#/components/schemas/HousekeepingBindingStatus" }
    UpdateToiletInspectionBindingRequest:
      type: object
      properties:
        roomId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        description: { type: string, maxLength: 512, nullable: true }
        status: { $ref: "#/components/schemas/HousekeepingBindingStatus" }
    ToiletInspectionExecution:
      type: object
      required: [id, checklistTemplateId, toiletInspectionBindingId, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        toiletInspectionBindingId: { $ref: "#/components/schemas/Uuid" }
        status: { type: string }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    ToiletInspectionExecutionContext:
      type: object
      required: [execution, toiletInspectionBinding]
      properties:
        execution: { $ref: "#/components/schemas/ToiletInspectionExecution" }
        toiletInspectionBinding: { $ref: "#/components/schemas/ToiletInspectionBinding" }

    PublicAreaInspectionBinding:
      type: object
      required:
        [id, clientId, buildingId, cleaningAreaId, checklistTemplateId,
         floorId, areaId, roomId, functionalLocationId, description, status,
         createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        floorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        areaId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        roomId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        functionalLocationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/HousekeepingBindingStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        cleaningArea:
          type: object
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            cleaningAreaType: { type: string }
            status: { type: string }
        checklistTemplate:
          type: object
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string }
    CreatePublicAreaInspectionBindingRequest:
      type: object
      required: [cleaningAreaId, checklistTemplateId]
      properties:
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        floorId: { type: string, format: uuid, nullable: true }
        areaId: { type: string, format: uuid, nullable: true }
        roomId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/HousekeepingBindingStatus" }
    UpdatePublicAreaInspectionBindingRequest:
      type: object
      properties:
        floorId: { type: string, format: uuid, nullable: true }
        areaId: { type: string, format: uuid, nullable: true }
        roomId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/HousekeepingBindingStatus" }
    PublicAreaInspectionExecution:
      type: object
      required: [id, checklistTemplateId, publicAreaInspectionBindingId, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        publicAreaInspectionBindingId: { $ref: "#/components/schemas/Uuid" }
        status: { type: string }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    PublicAreaInspectionExecutionContext:
      type: object
      required: [execution, publicAreaInspectionBinding]
      properties:
        execution: { $ref: "#/components/schemas/PublicAreaInspectionExecution" }
        publicAreaInspectionBinding: { $ref: "#/components/schemas/PublicAreaInspectionBinding" }

    SupervisorInspectionTargetType:
      type: string
      enum: [DAILY_CLEANING, TOILET_INSPECTION, PUBLIC_AREA_INSPECTION]
    SupervisorInspectionStatus:
      type: string
      enum: [PENDING, COMPLETED]
    SupervisorInspectionDecision:
      type: string
      enum: [APPROVED, REJECTED, REWORK_REQUIRED]
    SupervisorInspection:
      type: object
      required:
        [id, clientId, buildingId, cleaningAreaId, targetType, targetId,
         reviewId, supervisorUserId, decision, status, notes, inspectedAt,
         createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        targetType: { $ref: "#/components/schemas/SupervisorInspectionTargetType" }
        targetId: { $ref: "#/components/schemas/Uuid" }
        reviewId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        supervisorUserId: { $ref: "#/components/schemas/Uuid" }
        decision:
          allOf: [{ $ref: "#/components/schemas/SupervisorInspectionDecision" }]
          nullable: true
        status: { $ref: "#/components/schemas/SupervisorInspectionStatus" }
        notes: { type: string, nullable: true }
        inspectedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        cleaningArea:
          type: object
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            cleaningAreaType: { type: string }
            status: { type: string }
        targetSummary:
          type: object
          nullable: true
          properties:
            status: { type: string }
            description: { type: string }
    CreateSupervisorInspectionRequest:
      type: object
      required: [targetType, targetId]
      properties:
        targetType: { $ref: "#/components/schemas/SupervisorInspectionTargetType" }
        targetId: { $ref: "#/components/schemas/Uuid" }
        notes: { type: string, maxLength: 1000, nullable: true }
    SubmitSupervisorInspectionDecisionRequest:
      type: object
      required: [decision]
      properties:
        decision: { $ref: "#/components/schemas/SupervisorInspectionDecision" }
        notes: { type: string, maxLength: 1000, nullable: true }

    QualityAuditSourceType:
      type: string
      enum: [DAILY_CLEANING, TOILET_INSPECTION, PUBLIC_AREA_INSPECTION, SUPERVISOR_INSPECTION, CLEANING_AREA]
    QualityAuditStatus:
      type: string
      enum: [DRAFT, COMPLETED]
    QualityAuditResult:
      type: string
      enum: [PASS, FAIL, REWORK_REQUIRED]
    QualityAudit:
      type: object
      required:
        [id, clientId, buildingId, cleaningAreaId, sourceType, sourceId,
         auditorUserId, score, result, status, notes, auditedAt, createdAt,
         updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        sourceType: { $ref: "#/components/schemas/QualityAuditSourceType" }
        sourceId: { $ref: "#/components/schemas/Uuid" }
        auditorUserId: { $ref: "#/components/schemas/Uuid" }
        score: { type: number, nullable: true }
        result:
          allOf: [{ $ref: "#/components/schemas/QualityAuditResult" }]
          nullable: true
        status: { $ref: "#/components/schemas/QualityAuditStatus" }
        notes: { type: string, nullable: true }
        auditedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        cleaningArea:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string }
    CreateQualityAuditRequest:
      type: object
      required: [sourceType, sourceId]
      properties:
        sourceType: { $ref: "#/components/schemas/QualityAuditSourceType" }
        sourceId: { $ref: "#/components/schemas/Uuid" }
        score: { type: number, minimum: 0, maximum: 100, nullable: true }
        result: { $ref: "#/components/schemas/QualityAuditResult" }
        notes: { type: string, maxLength: 1000, nullable: true }
    UpdateQualityAuditRequest:
      type: object
      properties:
        score: { type: number, minimum: 0, maximum: 100, nullable: true }
        result: { $ref: "#/components/schemas/QualityAuditResult" }
        notes: { type: string, maxLength: 1000, nullable: true }
    CompleteQualityAuditRequest:
      type: object
      required: [result]
      properties:
        result: { $ref: "#/components/schemas/QualityAuditResult" }
        score: { type: number, minimum: 0, maximum: 100, nullable: true }
        notes: { type: string, maxLength: 1000, nullable: true }

    HousekeepingFindingSourceType:
      type: string
      enum: [DAILY_CLEANING, TOILET_INSPECTION, PUBLIC_AREA_INSPECTION, SUPERVISOR_INSPECTION]
    HousekeepingFinding:
      type: object
      description: >-
        BE-11H binding. `id` is the HK link id; `findingId` is the BE-09
        Finding used for rework/verification.
      required:
        [id, clientId, buildingId, findingId, cleaningAreaId, sourceType,
         sourceId, floorId, areaId, roomId, functionalLocationId, notes,
         createdAt, updatedAt, finding]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        findingId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        sourceType: { $ref: "#/components/schemas/HousekeepingFindingSourceType" }
        sourceId: { $ref: "#/components/schemas/Uuid" }
        floorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        areaId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        roomId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        functionalLocationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        notes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        finding:
          type: object
          required: [id, findingNumber, title, status, reportedByUserId, reportedAt]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            findingNumber: { type: string }
            title: { type: string }
            description: { type: string, nullable: true }
            status: { type: string }
            state: { type: string }
            reportedByUserId: { $ref: "#/components/schemas/Uuid" }
            reportedAt: { type: string, format: date-time }
        cleaningArea:
          type: object
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            cleaningAreaType: { type: string }
            status: { type: string }
        availableActions:
          type: array
          items: { type: string }
          description: BE-09 backend-authoritative actions for the linked finding.
    CreateHousekeepingFindingRequest:
      type: object
      required: [buildingId, cleaningAreaId, sourceType, sourceId, title]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        sourceType: { $ref: "#/components/schemas/HousekeepingFindingSourceType" }
        sourceId: { $ref: "#/components/schemas/Uuid" }
        title: { type: string, maxLength: 160 }
        description: { type: string, maxLength: 1000, nullable: true }
        findingNumber: { type: string, maxLength: 64 }
        floorId: { type: string, format: uuid, nullable: true }
        areaId: { type: string, format: uuid, nullable: true }
        roomId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        notes: { type: string, maxLength: 1000, nullable: true }

    HousekeepingEvidenceSourceType:
      type: string
      enum: [daily-cleaning, toilet-inspections, public-area-inspections, supervisor-inspections, findings]
    HousekeepingEvidenceRequirement:
      type: object
      required:
        [id, clientId, targetType, targetId, evidenceType, required,
         minimumCount, maximumCount, description, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        targetType: { type: string }
        targetId: { $ref: "#/components/schemas/Uuid" }
        evidenceType: { type: string, enum: [PHOTO, DOCUMENT, SIGNATURE] }
        required: { type: boolean }
        minimumCount: { type: integer }
        maximumCount: { type: integer, nullable: true }
        description: { type: string, nullable: true }
        status: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    HousekeepingEvidenceSubmission:
      type: object
      required:
        [id, clientId, evidenceRequirementId, executionType, executionId,
         evidenceType, fileReference, originalFileName, mimeType, fileSize,
         capturedAt, submittedByUserId, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        evidenceRequirementId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        executionType: { type: string }
        executionId: { $ref: "#/components/schemas/Uuid" }
        evidenceType: { type: string, enum: [PHOTO, DOCUMENT, SIGNATURE] }
        fileReference: { type: string }
        originalFileName: { type: string }
        mimeType: { type: string }
        fileSize: { type: integer }
        capturedAt: { type: string, format: date-time, nullable: true }
        submittedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        status: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    SubmitHousekeepingEvidenceRequest:
      type: object
      required: [evidenceType, fileReference, originalFileName, mimeType, fileSize]
      properties:
        evidenceType: { type: string, enum: [PHOTO, DOCUMENT, SIGNATURE] }
        evidenceRequirementId: { type: string, format: uuid, nullable: true }
        fileReference: { type: string, maxLength: 512 }
        originalFileName: { type: string, maxLength: 256 }
        mimeType: { type: string, maxLength: 128 }
        fileSize: { type: integer, minimum: 0, maximum: 52428800 }
        capturedAt: { type: string, format: date-time }

    # ------------------------------------------------------------------
    # CR-BE-MOB-01 PART 02 — Security operational schemas
    # (BE-12A/B/C/D/E/F/H + BE-21A/B). Shapes mirror the implemented
    # public DTOs; no new field, status, or workflow is introduced.
    # ------------------------------------------------------------------
    SecurityPostStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    SecurityPostType:
      type: string
      enum:
        [GENERAL, LOBBY, GATE, PERIMETER, PARKING, CONTROL_ROOM, PATROL,
         STANDBY, OTHER]
    SecurityPost:
      type: object
      description: >-
        BE-12A Security Post — operational duty/post context anchored to the
        BE-04 structure. `clientId` is derived through Building → Property →
        Client.
      required:
        [id, clientId, buildingId, floorId, areaId, roomId, spaceId,
         functionalLocationId, code, name, description, postType, status,
         createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        floorId: { type: string, format: uuid, nullable: true }
        areaId: { type: string, format: uuid, nullable: true }
        roomId: { type: string, format: uuid, nullable: true }
        spaceId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        postType: { $ref: "#/components/schemas/SecurityPostType" }
        status: { $ref: "#/components/schemas/SecurityPostStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateSecurityPostRequest:
      type: object
      required: [code, name]
      properties:
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        postType: { $ref: "#/components/schemas/SecurityPostType" }
        status: { $ref: "#/components/schemas/SecurityPostStatus" }
        floorId: { type: string, format: uuid, nullable: true }
        areaId: { type: string, format: uuid, nullable: true }
        roomId: { type: string, format: uuid, nullable: true }
        spaceId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
    UpdateSecurityPostRequest:
      type: object
      description: "`code`, `clientId` and `buildingId` are immutable."
      properties:
        name: { type: string }
        description: { type: string, nullable: true }
        postType: { $ref: "#/components/schemas/SecurityPostType" }
        status: { $ref: "#/components/schemas/SecurityPostStatus" }
        floorId: { type: string, format: uuid, nullable: true }
        areaId: { type: string, format: uuid, nullable: true }
        roomId: { type: string, format: uuid, nullable: true }
        spaceId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }

    PatrolRouteStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    PatrolRoute:
      type: object
      description: >-
        BE-12B Patrol Route — an ordered patrol definition for one Building.
        Carries no schedule, task or execution semantics.
      required:
        [id, clientId, buildingId, startSecurityPostId, code, name,
         description, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/PatrolRouteStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreatePatrolRouteRequest:
      type: object
      required: [code, name]
      properties:
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/PatrolRouteStatus" }
        startSecurityPostId: { type: string, format: uuid, nullable: true }
    UpdatePatrolRouteRequest:
      type: object
      properties:
        name: { type: string }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/PatrolRouteStatus" }
        startSecurityPostId: { type: string, format: uuid, nullable: true }

    PatrolRoutePointStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    PatrolRoutePoint:
      type: object
      description: >-
        BE-12B Patrol Route Point — the Security checkpoint. It is a
        sequenced reference to an existing BE-04 location, not a separate
        checkpoint master and not a QR identifier.
      required:
        [id, clientId, buildingId, patrolRouteId, floorId, areaId, roomId,
         spaceId, functionalLocationId, sequence, notes, status, createdAt,
         updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        patrolRouteId: { $ref: "#/components/schemas/Uuid" }
        floorId: { type: string, format: uuid, nullable: true }
        areaId: { type: string, format: uuid, nullable: true }
        roomId: { type: string, format: uuid, nullable: true }
        spaceId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        sequence: { type: integer, minimum: 1 }
        notes: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/PatrolRoutePointStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreatePatrolRoutePointRequest:
      type: object
      required: [sequence]
      properties:
        sequence: { type: integer, minimum: 1 }
        notes: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/PatrolRoutePointStatus" }
        floorId: { type: string, format: uuid, nullable: true }
        areaId: { type: string, format: uuid, nullable: true }
        roomId: { type: string, format: uuid, nullable: true }
        spaceId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
    UpdatePatrolRoutePointRequest:
      type: object
      properties:
        sequence: { type: integer, minimum: 1 }
        notes: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/PatrolRoutePointStatus" }
        floorId: { type: string, format: uuid, nullable: true }
        areaId: { type: string, format: uuid, nullable: true }
        roomId: { type: string, format: uuid, nullable: true }
        spaceId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }

    PatrolScheduleBindingStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    PatrolScheduleBinding:
      type: object
      description: >-
        BE-12C binding between a Patrol Route and a shared BE-07 Schedule
        Definition. Recurrence and task generation stay in BE-07; the
        resolved `schedule` block is read-through context, never a copy.
      required:
        [id, clientId, buildingId, patrolRouteId, scheduleDefinitionId,
         startSecurityPostId, description, status, createdByUserId,
         createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        patrolRouteId: { $ref: "#/components/schemas/Uuid" }
        scheduleDefinitionId: { $ref: "#/components/schemas/Uuid" }
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        description: { type: string, nullable: true }
        status:
          $ref: "#/components/schemas/PatrolScheduleBindingStatus"
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        patrolRoute:
          type: object
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string }
        schedule:
          type: object
          description: Resolved BE-07 Schedule Definition + recurrence.
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            targetType: { type: string }
            targetId: { $ref: "#/components/schemas/Uuid" }
            startAt: { type: string, format: date-time }
            endAt: { type: string, format: date-time, nullable: true }
            timezone: { type: string }
            status: { type: string }
            recurrence:
              type: object
              nullable: true
              properties:
                id: { $ref: "#/components/schemas/Uuid" }
                frequency: { type: string }
                interval: { type: integer }
                daysOfWeek:
                  type: array
                  nullable: true
                  items: { type: integer }
                dayOfMonth: { type: integer, nullable: true }
                startDate: { type: string, format: date }
                endDate: { type: string, format: date, nullable: true }
                status: { type: string }
    CreatePatrolScheduleBindingRequest:
      type: object
      description: >-
        Either bind an existing BE-07 Schedule Definition
        (`scheduleDefinitionId`) or supply the inline schedule fields so the
        BE-07 schedule is created by the shared service. `patrolRouteId` is
        taken from the path.
      properties:
        scheduleDefinitionId: { type: string, format: uuid }
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        description: { type: string, nullable: true }
        status:
          $ref: "#/components/schemas/PatrolScheduleBindingStatus"
        code: { type: string }
        name: { type: string }
        targetType: { type: string }
        targetId: { type: string, format: uuid }
        startAt: { type: string, format: date-time }
        endAt: { type: string, format: date-time, nullable: true }
        timezone: { type: string }
    UpdatePatrolScheduleBindingRequest:
      type: object
      properties:
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        description: { type: string, nullable: true }
        status:
          $ref: "#/components/schemas/PatrolScheduleBindingStatus"

    PatrolExecutionStatus:
      type: string
      enum: [OPEN, ASSIGNED, IN_PROGRESS, COMPLETED, CANCELLED]
      description: >-
        The shared BE-07 generated-task status, re-exported. Security does
        not own a second patrol lifecycle.
    PatrolExecution:
      type: object
      description: >-
        BE-12D operational view of a BE-07 generated task bound to a Patrol
        Route. `id` equals `taskId`; assignment uses the published BE-07
        task-assignment operations on that id.
      required:
        [id, taskId, clientId, buildingId, patrolRouteId, scheduleBindingId,
         scheduleDefinitionId, startSecurityPostId, operationalDate,
         occurrenceAt, status, startedAt, completedAt, completedByUserId,
         completionNotes, patrolRoute, startSecurityPost, schedule,
         pointProgress, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        taskId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        patrolRouteId: { $ref: "#/components/schemas/Uuid" }
        scheduleBindingId: { $ref: "#/components/schemas/Uuid" }
        scheduleDefinitionId: { $ref: "#/components/schemas/Uuid" }
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        operationalDate: { type: string, format: date }
        occurrenceAt: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/PatrolExecutionStatus" }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        completedByUserId: { type: string, format: uuid, nullable: true }
        completionNotes: { type: string, nullable: true }
        patrolRoute:
          type: object
          required: [id, code, name, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string }
        startSecurityPost:
          type: object
          required: [id, code, name]
          properties:
            id: { type: string, format: uuid, nullable: true }
            code: { type: string, nullable: true }
            name: { type: string, nullable: true }
        schedule:
          type: object
          required: [id, code, name, targetType, targetId, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            targetType: { type: string }
            targetId: { $ref: "#/components/schemas/Uuid" }
            status: { type: string }
        pointProgress:
          type: object
          description: >-
            Backend-derived checkpoint progress over the route's ACTIVE
            points. The client must not recompute it.
          required: [totalPoints, visitedPoints, pendingPoints, nextSequence]
          properties:
            totalPoints: { type: integer }
            visitedPoints: { type: integer }
            pendingPoints: { type: integer }
            nextSequence: { type: integer, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    PatrolPointVisit:
      type: object
      description: BE-12D checkpoint confirmation (`patrol_point_visits`).
      required:
        [id, clientId, buildingId, taskId, patrolRouteId, patrolRoutePointId,
         sequence, visitedAt, visitedByUserId, notes, status, createdAt,
         updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        taskId: { $ref: "#/components/schemas/Uuid" }
        patrolRouteId: { $ref: "#/components/schemas/Uuid" }
        patrolRoutePointId: { $ref: "#/components/schemas/Uuid" }
        sequence: { type: integer }
        visitedAt: { type: string, format: date-time }
        visitedByUserId: { $ref: "#/components/schemas/Uuid" }
        notes: { type: string, nullable: true }
        status: { type: string, enum: [VISITED, INACTIVE] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    RecordPatrolPointVisitRequest:
      type: object
      description: >-
        The checkpoint is identified by the `pointId` path parameter; the
        body carries optional field notes only.
      properties:
        notes: { type: string, nullable: true, maxLength: 512 }
    UpdatePatrolPointVisitRequest:
      type: object
      properties:
        notes: { type: string, nullable: true, maxLength: 512 }
    CompletePatrolExecutionRequest:
      type: object
      properties:
        completionNotes: { type: string, nullable: true, maxLength: 512 }

    PatrolChecklistBindingStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    PatrolChecklistBinding:
      type: object
      description: >-
        BE-12E join row between a BE-07 Checklist Template and a Patrol
        Route (+ optional start post) in one Building.
      required:
        [id, clientId, buildingId, patrolRouteId, startSecurityPostId,
         checklistTemplateId, status, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        patrolRouteId: { $ref: "#/components/schemas/Uuid" }
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        status:
          $ref: "#/components/schemas/PatrolChecklistBindingStatus"
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreatePatrolChecklistBindingRequest:
      type: object
      required: [buildingId, patrolRouteId, checklistTemplateId]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        patrolRouteId: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        status:
          $ref: "#/components/schemas/PatrolChecklistBindingStatus"
    UpdatePatrolChecklistBindingRequest:
      type: object
      properties:
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        status:
          $ref: "#/components/schemas/PatrolChecklistBindingStatus"
    PatrolChecklistExecution:
      type: object
      description: >-
        The shared BE-07 checklist execution started from a patrol binding.
        Responses, completion, cancellation, evidence and verification use
        the published BE-07 checklist / evidence / verification operations
        on this `id`.
      required:
        [id, checklistTemplateId, patrolChecklistBindingId, status,
         startedAt, completedAt, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        patrolChecklistBindingId: { $ref: "#/components/schemas/Uuid" }
        status: { type: string }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    PatrolChecklistExecutionContext:
      type: object
      description: >-
        BE-12E projection of the authoritative Building / Template / Route /
        start post for a patrol checklist execution. Derived from the
        binding — never stored a second time.
      required: [execution, building, template, patrolRoute, startSecurityPost]
      properties:
        execution:
          $ref: "#/components/schemas/PatrolChecklistExecution"
        building:
          type: object
          required: [id, code, name]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
        template:
          type: object
          required: [id, code, name, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string }
        patrolRoute:
          type: object
          required: [id, code, name, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string }
        startSecurityPost:
          type: object
          required: [id, code, name]
          properties:
            id: { type: string, format: uuid, nullable: true }
            code: { type: string, nullable: true }
            name: { type: string, nullable: true }

    SecurityDailyActivitySummary:
      type: object
      required:
        [scheduledPatrols, inProgressPatrols, completedPatrols,
         cancelledPatrols, openChecklists, completedChecklists, openFindings]
      properties:
        scheduledPatrols: { type: integer }
        inProgressPatrols: { type: integer }
        completedPatrols: { type: integer }
        cancelledPatrols: { type: integer }
        openChecklists: { type: integer }
        completedChecklists: { type: integer }
        openFindings: { type: integer }
    SecurityDailyActivity:
      type: object
      description: >-
        BE-12F read model composing BE-12A/B/C/D/E + BE-07 + BE-09 for one
        Building and operational date. It stores nothing.
      required:
        [buildingId, operationalDate, shift, securityPost, summary, patrols,
         checklists, findings]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        operationalDate: { type: string, format: date }
        shift:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            startTime: { type: string }
            endTime: { type: string }
            status: { type: string, enum: [ACTIVE, INACTIVE] }
        securityPost:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            postType: { type: string }
            status: { type: string, enum: [ACTIVE, INACTIVE] }
        summary:
          $ref: "#/components/schemas/SecurityDailyActivitySummary"
        patrols:
          type: array
          items:
            type: object
            description: >-
              `id` is the BE-07 generated-task id (the Patrol Execution id).
            properties:
              id: { $ref: "#/components/schemas/Uuid" }
              patrolRouteId: { $ref: "#/components/schemas/Uuid" }
              patrolRoute:
                type: object
                properties:
                  id: { $ref: "#/components/schemas/Uuid" }
                  code: { type: string }
                  name: { type: string }
                  status: { type: string, enum: [ACTIVE, INACTIVE] }
                  startSecurityPostId:
                    { type: string, format: uuid, nullable: true }
              startSecurityPostId:
                { type: string, format: uuid, nullable: true }
              startSecurityPost:
                type: object
                nullable: true
                properties:
                  id: { $ref: "#/components/schemas/Uuid" }
                  code: { type: string }
                  name: { type: string }
                  postType: { type: string }
                  status: { type: string, enum: [ACTIVE, INACTIVE] }
              status:
                $ref: "#/components/schemas/PatrolExecutionStatus"
              operationalDate: { type: string, format: date }
              occurrenceAt: { type: string, format: date-time }
              startedAt: { type: string, format: date-time, nullable: true }
              completedAt: { type: string, format: date-time, nullable: true }
        checklists:
          type: array
          items:
            type: object
            properties:
              id: { $ref: "#/components/schemas/Uuid" }
              checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
              checklistTemplateCode: { type: string }
              checklistTemplateName: { type: string }
              patrolChecklistBindingId: { $ref: "#/components/schemas/Uuid" }
              patrolRouteId: { $ref: "#/components/schemas/Uuid" }
              status:
                type: string
                enum: [DRAFT, IN_PROGRESS, COMPLETED, CANCELLED]
              startedAt: { type: string, format: date-time, nullable: true }
              completedAt: { type: string, format: date-time, nullable: true }
              operationalDate: { type: string, format: date }
              createdAt: { type: string, format: date-time }
        findings:
          type: array
          items:
            type: object
            description: >-
              BE-09 finding projection; `availableActions` is sourced from
              BE-09 and is backend-authoritative.
            properties:
              id: { $ref: "#/components/schemas/Uuid" }
              findingNumber: { type: string }
              title: { type: string }
              status: { $ref: "#/components/schemas/FindingStatus" }
              classificationId:
                { type: string, format: uuid, nullable: true }
              severityId: { type: string, format: uuid, nullable: true }
              createdAt: { type: string, format: date-time }
              reportedByUserId: { $ref: "#/components/schemas/Uuid" }
              availableActions:
                type: array
                items: { type: string }

    SecurityFindingSourceType:
      type: string
      enum:
        [PATROL_EXECUTION, PATROL_CHECKLIST, SECURITY_DAILY_ACTIVITY,
         SHIFT_HANDOVER, SECURITY_POST]
      description: >-
        The Security operational sources. Intentionally separate from BE-09's
        own `FindingSourceType` (WORK_ORDER / CHECKLIST_EXECUTION /
        FORM_INSTANCE).
    SecurityFinding:
      type: object
      description: >-
        BE-12H Security context link for an authoritative BE-09 Finding.
        `id` is the link id; every workflow operation uses `findingId`.
      required:
        [id, clientId, buildingId, findingId, startSecurityPostId,
         patrolRouteId, sourceType, sourceId, createdAt, finding,
         availableActions]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        findingId: { $ref: "#/components/schemas/Uuid" }
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        patrolRouteId: { type: string, format: uuid, nullable: true }
        sourceType:
          $ref: "#/components/schemas/SecurityFindingSourceType"
        sourceId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        finding: { $ref: "#/components/schemas/Finding" }
        availableActions:
          type: array
          items: { type: string }
          description: >-
            BE-09 backend-authoritative actions for the linked finding —
            never re-derived by Security or by the client.
    CreateSecurityFindingRequest:
      type: object
      description: >-
        Creates the BE-09 Finding and the Security link in one call.
        `clientId` and the reporter are derived by the backend.
      required: [buildingId, sourceType, sourceId, title]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        sourceType:
          $ref: "#/components/schemas/SecurityFindingSourceType"
        sourceId: { $ref: "#/components/schemas/Uuid" }
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        patrolRouteId: { type: string, format: uuid, nullable: true }
        title: { type: string, maxLength: 160 }
        description: { type: string, nullable: true }
        classificationId: { type: string, format: uuid }
        severityId: { type: string, format: uuid }
        assigneeType:
          type: string
          enum: [WORKFORCE, TEAM, VENDOR, VENDOR_WORKFORCE]
        workforceProfileId: { type: string, format: uuid }
        teamId: { type: string, format: uuid }
        vendorId: { type: string, format: uuid }

    IncidentType:
      type: string
      enum: [OPERATIONAL, ASSET_FAILURE, FINDING_ESCALATION]
    IncidentSeverity:
      type: string
      enum: [LOW, MEDIUM, HIGH, CRITICAL]
    IncidentPriority:
      type: string
      enum: [LOW, MEDIUM, HIGH, CRITICAL]
    IncidentStatus:
      type: string
      enum: [REPORTED, CANCELLED, CLOSED]
      description: >-
        The BE-21A record lifecycle. `CLOSED` (BE-21K) is the verified
        resolution; `CANCELLED` is withdrawal. Both are terminal and
        mutually exclusive.
    IncidentLocationType:
      type: string
      enum: [FLOOR, AREA, ROOM, SPACE, FUNCTIONAL_LOCATION]
      description: >-
        An OPTIONAL refinement below the Building; omitting it means
        Building level.
    Incident:
      type: object
      description: >-
        BE-21A shared Incident foundation. `clientId` is derived from the
        Building and `reportedByUserId` is the authenticated actor.
      required:
        [id, clientId, buildingId, incidentNumber, incidentType, title,
         description, severity, priority, status, locationType, locationId,
         floorId, areaId, roomId, spaceId, functionalLocationId,
         reportedByUserId, reportedAt, cancelledAt, cancelledByUserId,
         closedAt, closedByUserId, closureNotes, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        incidentNumber: { type: string }
        incidentType: { $ref: "#/components/schemas/IncidentType" }
        title: { type: string }
        description: { type: string, nullable: true }
        severity: { $ref: "#/components/schemas/IncidentSeverity" }
        priority: { $ref: "#/components/schemas/IncidentPriority" }
        status: { $ref: "#/components/schemas/IncidentStatus" }
        locationType:
          allOf: [{ $ref: "#/components/schemas/IncidentLocationType" }]
          nullable: true
        locationId: { type: string, format: uuid, nullable: true }
        floorId: { type: string, format: uuid, nullable: true }
        areaId: { type: string, format: uuid, nullable: true }
        roomId: { type: string, format: uuid, nullable: true }
        spaceId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        reportedByUserId: { $ref: "#/components/schemas/Uuid" }
        reportedAt: { type: string, format: date-time }
        cancelledAt: { type: string, format: date-time, nullable: true }
        cancelledByUserId: { type: string, format: uuid, nullable: true }
        closedAt: { type: string, format: date-time, nullable: true }
        closedByUserId: { type: string, format: uuid, nullable: true }
        closureNotes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateIncidentRequest:
      type: object
      required: [buildingId, incidentNumber, incidentType, title]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        incidentNumber: { type: string }
        incidentType: { $ref: "#/components/schemas/IncidentType" }
        title: { type: string }
        description: { type: string, nullable: true }
        severity: { $ref: "#/components/schemas/IncidentSeverity" }
        priority: { $ref: "#/components/schemas/IncidentPriority" }
        locationType:
          allOf: [{ $ref: "#/components/schemas/IncidentLocationType" }]
          nullable: true
        locationId: { type: string, format: uuid, nullable: true }
        reportedAt: { type: string, format: date-time }
    UpdateIncidentRequest:
      type: object
      description: >-
        Only descriptive metadata is mutable — type, context, number and
        reporter are the identity of the incident.
      properties:
        title: { type: string }
        description: { type: string, nullable: true }
        severity: { $ref: "#/components/schemas/IncidentSeverity" }
        priority: { $ref: "#/components/schemas/IncidentPriority" }

    OperationalIncidentCategory:
      type: string
      enum:
        [UTILITY_FAILURE, ELECTRICAL, PLUMBING, HVAC, LIFT_ESCALATOR,
         FIRE_SAFETY, WATER_LEAK, STRUCTURAL, ENVIRONMENTAL, HOUSEKEEPING,
         SECURITY, SAFETY, ACCESS, OTHER]
    OperationalIncidentStatus:
      type: string
      enum: [OPEN, IN_PROGRESS, RESOLVED]
      description: >-
        BE-21B operational handling progression — distinct from the BE-21A
        record lifecycle. `RESOLVED` means handling finished, NOT verified
        or closed.
    OperationalIncidentAction:
      type: string
      enum: [START_PROGRESS, RESOLVE, REOPEN, UPDATE_DETAILS]
    OperationalIncident:
      type: object
      description: >-
        BE-21B specialization of the BE-21A foundation
        (`incidentType = OPERATIONAL`). `id` is the shared Incident id.
      required:
        [id, operationalIncidentId, clientId, buildingId, incidentNumber,
         incidentType, title, description, severity, priority,
         incidentStatus, operationalStatus, operationalCategory, occurredAt,
         statusChangedAt, notes, locationType, locationId, floorId, areaId,
         roomId, spaceId, functionalLocationId, reportedByUserId, reportedAt,
         createdByUserId, createdAt, updatedAt, availableActions]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        operationalIncidentId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        incidentNumber: { type: string }
        incidentType: { type: string, enum: [OPERATIONAL] }
        title: { type: string }
        description: { type: string, nullable: true }
        severity: { $ref: "#/components/schemas/IncidentSeverity" }
        priority: { $ref: "#/components/schemas/IncidentPriority" }
        incidentStatus: { $ref: "#/components/schemas/IncidentStatus" }
        operationalStatus:
          $ref: "#/components/schemas/OperationalIncidentStatus"
        operationalCategory:
          $ref: "#/components/schemas/OperationalIncidentCategory"
        occurredAt: { type: string, format: date-time }
        statusChangedAt: { type: string, format: date-time }
        notes: { type: string, nullable: true }
        locationType:
          allOf: [{ $ref: "#/components/schemas/IncidentLocationType" }]
          nullable: true
        locationId: { type: string, format: uuid, nullable: true }
        floorId: { type: string, format: uuid, nullable: true }
        areaId: { type: string, format: uuid, nullable: true }
        roomId: { type: string, format: uuid, nullable: true }
        spaceId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        reportedByUserId: { $ref: "#/components/schemas/Uuid" }
        reportedAt: { type: string, format: date-time }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        availableActions:
          type: array
          description: >-
            Backend-resolved per request from state × permission ×
            foundation lifecycle. The client must not recompute them.
          items:
            $ref: "#/components/schemas/OperationalIncidentAction"
    CreateOperationalIncidentRequest:
      type: object
      description: >-
        `incidentType` is absent by design (always OPERATIONAL); `clientId`
        is derived from the Building.
      required: [buildingId, incidentNumber, title, operationalCategory, occurredAt]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        incidentNumber: { type: string }
        title: { type: string }
        description: { type: string, nullable: true }
        severity: { $ref: "#/components/schemas/IncidentSeverity" }
        priority: { $ref: "#/components/schemas/IncidentPriority" }
        locationType:
          allOf: [{ $ref: "#/components/schemas/IncidentLocationType" }]
          nullable: true
        locationId: { type: string, format: uuid, nullable: true }
        operationalCategory:
          $ref: "#/components/schemas/OperationalIncidentCategory"
        occurredAt: { type: string, format: date-time }
        notes: { type: string, nullable: true }
        reportedByUserId: { type: string, format: uuid }
    UpdateOperationalIncidentRequest:
      type: object
      description: >-
        Foundation metadata and operational fields are applied atomically.
        `operationalStatus` must follow the closed BE-21B transition table.
      properties:
        title: { type: string }
        description: { type: string, nullable: true }
        severity: { $ref: "#/components/schemas/IncidentSeverity" }
        priority: { $ref: "#/components/schemas/IncidentPriority" }
        operationalCategory:
          $ref: "#/components/schemas/OperationalIncidentCategory"
        occurredAt: { type: string, format: date-time }
        notes: { type: string, nullable: true }
        operationalStatus:
          $ref: "#/components/schemas/OperationalIncidentStatus"

    # ------------------------------------------------------------------
    # CR-BE-MOB-01 PART 03 — Engineering field / reading schemas
    # (BE-10A/B/C/D/E/F/G/H/J/K). Shapes mirror the implemented public
    # DTOs; no new field, status, or workflow is introduced. Every
    # execution id is the authoritative BE-07 checklist-execution or
    # form-instance id; every work id is the BE-08 Work Order id; every
    # finding id is the BE-09 Finding id.
    # ------------------------------------------------------------------
    EngineeringBindingStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
      description: Shared ACTIVE/INACTIVE status of a BE-10 binding row.
    EngineeringAssetRef:
      type: object
      required: [id, assetCode, assetName, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        assetCode: { type: string }
        assetName: { type: string }
        status: { type: string }
    EngineeringBuildingRef:
      type: object
      required: [id, code, name]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
    EngineeringFunctionalLocationRef:
      type: object
      nullable: true
      required: [id, code, name, status]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        status: { type: string }

    InspectionBinding:
      type: object
      description: >-
        BE-10B equipment-inspection binding: BE-05 Asset × BE-07 Checklist
        Template (+ optional BE-04 Functional Location). Not an inspection
        engine.
      required:
        [id, clientId, buildingId, assetId, checklistTemplateId,
         functionalLocationId, status, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateInspectionBindingRequest:
      type: object
      required: [checklistTemplateId]
      properties:
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
    UpdateInspectionBindingRequest:
      type: object
      properties:
        functionalLocationId: { type: string, format: uuid, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
    InspectionExecution:
      type: object
      description: >-
        The shared BE-07 checklist execution started from an inspection
        binding. `id` is the authoritative checklist-execution id used by the
        published BE-07 checklist / evidence / verification operations.
      required:
        [id, checklistTemplateId, inspectionBindingId, status, startedAt,
         completedAt, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        inspectionBindingId: { $ref: "#/components/schemas/Uuid" }
        status: { type: string }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    InspectionExecutionContext:
      type: object
      required: [execution, asset, building, functionalLocation]
      properties:
        execution: { $ref: "#/components/schemas/InspectionExecution" }
        asset: { $ref: "#/components/schemas/EngineeringAssetRef" }
        building: { $ref: "#/components/schemas/EngineeringBuildingRef" }
        functionalLocation:
          $ref: "#/components/schemas/EngineeringFunctionalLocationRef"

    MeterReadingBinding:
      type: object
      description: >-
        BE-10C technical meter-reading definition: BE-05 Asset × numeric BE-07
        Form Field × BE-07 UOM (+ optional Functional Location), with an
        optional binding-level tightening of the field's measurement range.
      required:
        [id, clientId, buildingId, assetId, formFieldId, uomId,
         functionalLocationId, minimumValue, maximumValue, status,
         createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        formFieldId: { $ref: "#/components/schemas/Uuid" }
        uomId: { $ref: "#/components/schemas/Uuid" }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        minimumValue: { type: number, nullable: true }
        maximumValue: { type: number, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateMeterReadingBindingRequest:
      type: object
      required: [formFieldId]
      properties:
        formFieldId: { $ref: "#/components/schemas/Uuid" }
        uomId: { type: string, format: uuid }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        minimumValue: { type: number }
        maximumValue: { type: number }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
    UpdateMeterReadingBindingRequest:
      type: object
      properties:
        uomId: { type: string, format: uuid }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        minimumValue: { type: number }
        maximumValue: { type: number }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
    MeterReadingExecution:
      type: object
      description: >-
        The shared BE-07 form instance started from a meter-reading binding.
        `id` is the authoritative form-instance id.
      required:
        [id, formTemplateVersionId, meterReadingBindingId, status, startedAt,
         completedAt, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        formTemplateVersionId: { $ref: "#/components/schemas/Uuid" }
        meterReadingBindingId: { $ref: "#/components/schemas/Uuid" }
        status: { type: string }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    SubmitMeterReadingRequest:
      type: object
      required: [value]
      properties:
        value:
          type: number
          description: >-
            Validated against the effective range (binding range tightened
            over the BE-07 field range).
        notes: { type: string, nullable: true }
    MeterReading:
      type: object
      description: >-
        A submitted technical reading, stored in BE-07's own `form_responses`
        row — not in a second measurement table.
      required:
        [executionId, formInstanceId, versionFieldId, value, uom,
         minimumValue, maximumValue, notes, submittedAt]
      properties:
        executionId: { $ref: "#/components/schemas/Uuid" }
        formInstanceId: { $ref: "#/components/schemas/Uuid" }
        versionFieldId: { $ref: "#/components/schemas/Uuid" }
        value: { type: number }
        uom:
          type: object
          required: [id, code, symbol]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            symbol: { type: string }
        minimumValue: { type: number, nullable: true }
        maximumValue: { type: number, nullable: true }
        notes: { type: string, nullable: true }
        submittedAt: { type: string, format: date-time }
    MeterReadingContext:
      type: object
      required:
        [execution, asset, building, functionalLocation, uom, minimumValue,
         maximumValue, currentValue]
      properties:
        execution: { $ref: "#/components/schemas/MeterReadingExecution" }
        asset: { $ref: "#/components/schemas/EngineeringAssetRef" }
        building: { $ref: "#/components/schemas/EngineeringBuildingRef" }
        functionalLocation:
          $ref: "#/components/schemas/EngineeringFunctionalLocationRef"
        uom:
          type: object
          required: [id, code, name, symbol]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            symbol: { type: string }
        minimumValue: { type: number, nullable: true }
        maximumValue: { type: number, nullable: true }
        currentValue: { type: number, nullable: true }

    LogSheetBinding:
      type: object
      description: >-
        BE-10D equipment log-sheet definition: BE-05 Asset × BE-07 Form
        Template (+ optional pinned Template Version, optional Functional
        Location).
      required:
        [id, clientId, buildingId, assetId, formTemplateId,
         formTemplateVersionId, functionalLocationId, status, createdByUserId,
         createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        formTemplateId: { $ref: "#/components/schemas/Uuid" }
        formTemplateVersionId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateLogSheetBindingRequest:
      type: object
      required: [formTemplateId]
      properties:
        formTemplateId: { $ref: "#/components/schemas/Uuid" }
        formTemplateVersionId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
    UpdateLogSheetBindingRequest:
      type: object
      properties:
        formTemplateVersionId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
    LogSheetExecution:
      type: object
      description: >-
        One log-sheet row = one shared BE-07 form instance. `id` is the
        authoritative form-instance id.
      required:
        [id, formTemplateVersionId, logSheetBindingId, status, startedAt,
         completedAt, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        formTemplateVersionId: { $ref: "#/components/schemas/Uuid" }
        logSheetBindingId: { $ref: "#/components/schemas/Uuid" }
        status: { type: string }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    LogSheetExecutionContext:
      type: object
      required:
        [execution, asset, building, functionalLocation, template, version]
      properties:
        execution: { $ref: "#/components/schemas/LogSheetExecution" }
        asset: { $ref: "#/components/schemas/EngineeringAssetRef" }
        building: { $ref: "#/components/schemas/EngineeringBuildingRef" }
        functionalLocation:
          $ref: "#/components/schemas/EngineeringFunctionalLocationRef"
        template:
          type: object
          required: [id, code, name]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
        version:
          type: object
          required: [id, versionNumber, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            versionNumber: { type: integer }
            status: { type: string }

    EngineeringChecklistBinding:
      type: object
      description: >-
        BE-10E binding: BE-07 Checklist Template × Building (+ optional Asset
        and/or Functional Location target inside that Building).
      required:
        [id, clientId, buildingId, checklistTemplateId, assetId,
         functionalLocationId, status, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        assetId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateEngineeringChecklistBindingRequest:
      type: object
      required: [buildingId, checklistTemplateId]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        assetId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
    UpdateEngineeringChecklistBindingRequest:
      type: object
      properties:
        assetId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
    EngineeringChecklistExecution:
      type: object
      description: >-
        The shared BE-07 checklist execution started from an Engineering
        checklist binding.
      required:
        [id, checklistTemplateId, engineeringChecklistBindingId, status,
         startedAt, completedAt, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        checklistTemplateId: { $ref: "#/components/schemas/Uuid" }
        engineeringChecklistBindingId: { $ref: "#/components/schemas/Uuid" }
        status: { type: string }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    EngineeringChecklistExecutionContext:
      type: object
      required: [execution, building, template, asset, functionalLocation]
      properties:
        execution:
          $ref: "#/components/schemas/EngineeringChecklistExecution"
        building: { $ref: "#/components/schemas/EngineeringBuildingRef" }
        template:
          type: object
          required: [id, code, name, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string }
        asset:
          allOf: [{ $ref: "#/components/schemas/EngineeringAssetRef" }]
          nullable: true
        functionalLocation:
          $ref: "#/components/schemas/EngineeringFunctionalLocationRef"

    BreakdownStatus:
      type: string
      enum: [OPEN, CLOSED]
      description: >-
        The BE-10F breakdown record lifecycle. It is deliberately minimal —
        corrective progress lives on the linked BE-08 Work Order.
    BreakdownBinding:
      type: object
      description: >-
        BE-10F breakdown / corrective event on a BE-05 Asset. `corrective`
        projects the current BE-08 status of the linked Work Order and is
        never stored.
      required:
        [id, clientId, buildingId, assetId, functionalLocationId, category,
         description, reportedByUserId, reportedAt, status, createdAt,
         updatedAt, corrective]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        category: { type: string }
        description: { type: string }
        reportedByUserId: { $ref: "#/components/schemas/Uuid" }
        reportedAt: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/BreakdownStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        corrective:
          type: object
          nullable: true
          description: Live projection of the linked BE-08 Work Order.
          required: [workOrderId, workOrderNumber, title, status]
          properties:
            workOrderId: { $ref: "#/components/schemas/Uuid" }
            workOrderNumber: { type: string }
            title: { type: string }
            status: { type: string }
    CreateBreakdownRequest:
      type: object
      required: [category, description]
      properties:
        category: { type: string }
        description: { type: string }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        reportedAt: { type: string, format: date-time }
    CloseBreakdownRequest:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [CLOSED]
          description: CLOSED is the only accepted breakdown transition.
    LinkCorrectiveWorkOrderRequest:
      type: object
      description: >-
        Supply `workOrderId` to link an existing BE-08 Work Order, or `title`
        to have the shared BE-08 service create the corrective Work Order.
      properties:
        workOrderId: { type: string, format: uuid }
        title: { type: string }

    MaintenanceType:
      type: string
      enum: [PREVENTIVE, PREDICTIVE, CONDITION_BASED, CALIBRATION]
      description: >-
        BE-10G planned-maintenance classification. Corrective work is NOT a
        member: it is a BE-10F breakdown plus a BE-08 corrective Work Order.
    MaintenanceBinding:
      type: object
      description: >-
        BE-10G planned-maintenance context for a BE-05 Asset. `schedule`,
        `workOrder` and `tasks` are live projections of the authoritative
        BE-07 / BE-08 records, never stored copies.
      required:
        [id, clientId, buildingId, assetId, functionalLocationId, name,
         maintenanceType, description, status, createdByUserId, createdAt,
         updatedAt, schedule, workOrder, tasks]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        name: { type: string }
        maintenanceType: { $ref: "#/components/schemas/MaintenanceType" }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        schedule:
          type: object
          nullable: true
          description: Linked BE-07 Schedule Definition.
          required: [id, code, name, targetType, targetId, timezone, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            targetType: { type: string }
            targetId: { $ref: "#/components/schemas/Uuid" }
            timezone: { type: string }
            status: { type: string }
        workOrder:
          type: object
          nullable: true
          description: Linked BE-08 Work Order (authoritative status).
          required: [id, workOrderNumber, title, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            workOrderNumber: { type: string }
            title: { type: string }
            status: { type: string }
        tasks:
          type: array
          description: Linked BE-07 generated tasks, newest first.
          items:
            type: object
            required: [id, occurrenceAt, status]
            properties:
              id: { $ref: "#/components/schemas/Uuid" }
              occurrenceAt: { type: string, format: date-time }
              status: { type: string }
    CreateMaintenanceBindingRequest:
      type: object
      required: [name, maintenanceType]
      properties:
        name: { type: string }
        maintenanceType: { $ref: "#/components/schemas/MaintenanceType" }
        description: { type: string }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
    UpdateMaintenanceBindingRequest:
      type: object
      properties:
        name: { type: string }
        maintenanceType: { $ref: "#/components/schemas/MaintenanceType" }
        description: { type: string, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        status: { $ref: "#/components/schemas/EngineeringBindingStatus" }
    LinkMaintenanceScheduleRequest:
      type: object
      description: >-
        Supply `scheduleDefinitionId` to link an existing BE-07 schedule, or
        the creation fields so the shared BE-07 scheduler creates one.
      properties:
        scheduleDefinitionId: { type: string, format: uuid }
        targetType: { type: string }
        targetId: { type: string, format: uuid }
        code: { type: string }
        name: { type: string }
        startAt: { type: string, format: date-time }
        timezone: { type: string }
    LinkMaintenanceTaskRequest:
      type: object
      required: [taskId]
      properties:
        taskId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >-
            The authoritative BE-07 generated task id. Execution stays on the
            published task operations.
    LinkMaintenanceWorkOrderRequest:
      type: object
      description: >-
        Supply `workOrderId` to link an existing BE-08 Work Order, or `title`
        to have the shared BE-08 service create one.
      properties:
        workOrderId: { type: string, format: uuid }
        title: { type: string }

    EngineeringFindingOperationType:
      type: string
      enum:
        [EQUIPMENT_INSPECTION, METER_READING, EQUIPMENT_LOG_SHEET,
         ENGINEERING_CHECKLIST, BREAKDOWN, MAINTENANCE]
      description: >-
        Which Engineering operation produced the finding. Distinct from BE-09's
        own `FindingSourceType`, which records the underlying execution kind.
    EngineeringFinding:
      type: object
      description: >-
        BE-10H Engineering context link for an authoritative BE-09 Finding.
        `id` is the link id; every workflow operation uses `findingId`.
      required:
        [id, clientId, buildingId, findingId, assetId, functionalLocationId,
         operationType, sourceType, sourceId, createdAt, finding,
         availableActions]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        findingId: { $ref: "#/components/schemas/Uuid" }
        assetId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        operationType:
          $ref: "#/components/schemas/EngineeringFindingOperationType"
        sourceType:
          allOf: [{ $ref: "#/components/schemas/FindingSourceType" }]
          nullable: true
        sourceId: { type: string, format: uuid, nullable: true }
        createdAt: { type: string, format: date-time }
        finding: { $ref: "#/components/schemas/Finding" }
        availableActions:
          type: array
          items: { type: string }
          description: >-
            BE-09 backend-authoritative actions for the linked finding —
            never re-derived by Engineering or by the client.
    CreateEngineeringFindingRequest:
      type: object
      description: >-
        Supply `findingId` to link an existing BE-09 Finding, or the create
        fields (`title` …) to have the shared BE-09 service create one.
        `clientId` and the reporter are derived by the backend.
      required: [buildingId, operationType]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        operationType:
          $ref: "#/components/schemas/EngineeringFindingOperationType"
        findingId: { type: string, format: uuid }
        title: { type: string }
        description: { type: string }
        assetId: { type: string, format: uuid, nullable: true }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        sourceType: { $ref: "#/components/schemas/FindingSourceType" }
        sourceId: { type: string, format: uuid }
        classificationId: { type: string, format: uuid }
        severityId: { type: string, format: uuid }
        assigneeType:
          type: string
          enum: [WORKFORCE, TEAM, VENDOR, VENDOR_WORKFORCE]
        workforceProfileId: { type: string, format: uuid }
        teamId: { type: string, format: uuid }
        vendorId: { type: string, format: uuid }

    EngineeringShiftContext:
      type: object
      nullable: true
      description: BE-03 Shift context with the workforce bound to that Shift.
      required:
        [id, code, name, startTime, endTime, status, workforceCount, workforce]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        startTime: { type: string }
        endTime: { type: string }
        status: { type: string }
        workforceCount: { type: integer }
        workforce:
          type: array
          items:
            type: object
            required: [workforceProfileId, employeeCode, fullName]
            properties:
              workforceProfileId: { $ref: "#/components/schemas/Uuid" }
              employeeCode: { type: string }
              fullName: { type: string }
    EngineeringDailyOperationKind:
      type: string
      enum: [TASK, WORK_ORDER, FINDING]
    EngineeringDailyOperationsSummary:
      type: object
      required:
        [scheduled, inProgress, completed, openWorkOrders, openFindings]
      properties:
        scheduled: { type: integer }
        inProgress: { type: integer }
        completed: { type: integer }
        openWorkOrders: { type: integer }
        openFindings: { type: integer }
    EngineeringDailyOperation:
      type: object
      description: >-
        One consolidated daily operation referencing its authoritative source
        record. `id` is the BE-07 task id, BE-08 Work Order id or BE-09 Finding
        id depending on `kind`; `status` is the source domain's own status,
        untranslated.
      required:
        [kind, id, referenceNumber, title, status, occurredAt, completedAt,
         assignee]
      properties:
        kind: { $ref: "#/components/schemas/EngineeringDailyOperationKind" }
        id: { $ref: "#/components/schemas/Uuid" }
        referenceNumber: { type: string, nullable: true }
        title: { type: string }
        status: { type: string }
        occurredAt: { type: string, format: date-time }
        completedAt: { type: string, format: date-time, nullable: true }
        assignee:
          type: object
          nullable: true
          required: [type, workforceProfileId, teamId, vendorId]
          properties:
            type: { type: string }
            workforceProfileId: { type: string, format: uuid, nullable: true }
            teamId: { type: string, format: uuid, nullable: true }
            vendorId: { type: string, format: uuid, nullable: true }
        availableActions:
          type: array
          items: { type: string }
          description: >-
            BE-09 backend-authoritative actions. Present for FINDING rows
            only — Tasks and Work Orders expose no authoritative
            available-actions service in their foundations.
    EngineeringDailyOperations:
      type: object
      description: >-
        BE-10A read model for one Building + operational date (+ optional
        Shift). Composition only — it stores nothing.
      required: [buildingId, operationalDate, shift, summary, operations]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        operationalDate: { type: string, format: date }
        shift: { $ref: "#/components/schemas/EngineeringShiftContext" }
        summary:
          $ref: "#/components/schemas/EngineeringDailyOperationsSummary"
        operations:
          type: array
          items: { $ref: "#/components/schemas/EngineeringDailyOperation" }

    EngineeringOverview:
      type: object
      description: >-
        BE-10K supervisor read model derived from BE-10A operations, BE-09
        finding counts, BE-08 Work Orders and the BE-10J handover dataset.
      required:
        [buildingId, date, shift, summary, operations, findings, workOrders,
         handover]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        date: { type: string, format: date }
        shift: { $ref: "#/components/schemas/EngineeringShiftContext" }
        summary:
          type: object
          required:
            [scheduledOperations, inProgressOperations, completedOperations,
             activeWorkOrders, openFindings, pendingHandoverItems]
          properties:
            scheduledOperations: { type: integer }
            inProgressOperations: { type: integer }
            completedOperations: { type: integer }
            activeWorkOrders: { type: integer }
            openFindings: { type: integer }
            pendingHandoverItems: { type: integer }
        operations:
          $ref: "#/components/schemas/EngineeringDailyOperationsSummary"
        findings:
          type: object
          required: [open, rework, verified, closed]
          properties:
            open: { type: integer }
            rework: { type: integer }
            verified: { type: integer }
            closed: { type: integer }
        workOrders:
          type: object
          required: [active, completedInWindow]
          properties:
            active:
              type: array
              items:
                type: object
                required: [id, workOrderNumber, title, status]
                properties:
                  id: { $ref: "#/components/schemas/Uuid" }
                  workOrderNumber: { type: string }
                  title: { type: string }
                  status: { type: string }
            completedInWindow: { type: integer }
        handover:
          type: object
          required: [drafts, ready, acknowledged, pendingItems]
          properties:
            drafts: { type: integer }
            ready: { type: integer }
            acknowledged: { type: integer }
            pendingItems:
              type: array
              items: { $ref: "#/components/schemas/EngineeringHandoverItem" }

    EngineeringShiftHandoverStatus:
      type: string
      enum: [DRAFT, READY, ACKNOWLEDGED]
    EngineeringHandoverItem:
      type: object
      description: >-
        One unresolved operational item, referenced from its authoritative
        record. `status` is the source record's own status, never translated.
      required: [kind, id, referenceNumber, title, status, assetCode]
      properties:
        kind:
          type: string
          enum:
            [WORK_ORDER, BREAKDOWN, FINDING, INSPECTION, CHECKLIST,
             METER_READING, EQUIPMENT_LOG, MAINTENANCE, TASK]
        id: { $ref: "#/components/schemas/Uuid" }
        referenceNumber: { type: string, nullable: true }
        title: { type: string }
        status: { type: string }
        assetCode: { type: string, nullable: true }
    EngineeringHandoverDataset:
      type: object
      description: >-
        The live Engineering operational dataset for the handover's Building,
        resolved at read time from the authoritative records.
      required:
        [activeWorkOrders, openBreakdowns, openFindings, incompleteInspections,
         incompleteChecklists, incompleteMeterReadings, incompleteLogSheets,
         pendingMaintenance, scheduledTasks]
      properties:
        activeWorkOrders:
          type: array
          items: { $ref: "#/components/schemas/EngineeringHandoverItem" }
        openBreakdowns:
          type: array
          items: { $ref: "#/components/schemas/EngineeringHandoverItem" }
        openFindings:
          type: array
          items: { $ref: "#/components/schemas/EngineeringHandoverItem" }
        incompleteInspections:
          type: array
          items: { $ref: "#/components/schemas/EngineeringHandoverItem" }
        incompleteChecklists:
          type: array
          items: { $ref: "#/components/schemas/EngineeringHandoverItem" }
        incompleteMeterReadings:
          type: array
          items: { $ref: "#/components/schemas/EngineeringHandoverItem" }
        incompleteLogSheets:
          type: array
          items: { $ref: "#/components/schemas/EngineeringHandoverItem" }
        pendingMaintenance:
          type: array
          items: { $ref: "#/components/schemas/EngineeringHandoverItem" }
        scheduledTasks:
          type: array
          items: { $ref: "#/components/schemas/EngineeringHandoverItem" }
    EngineeringShiftHandover:
      type: object
      description: >-
        BE-10J shift handover. Only the summary and lifecycle fields are
        stored; `dataset` is resolved live from the authoritative Engineering
        records at read time.
      required:
        [id, clientId, buildingId, outgoingShift, incomingShift, handoverDate,
         preparedByUserId, acknowledgedByUserId, summary, status, preparedAt,
         acknowledgedAt, createdAt, updatedAt, dataset]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        outgoingShift:
          type: object
          required: [id, code, name, startTime, endTime]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            startTime: { type: string }
            endTime: { type: string }
        incomingShift:
          type: object
          required: [id, code, name, startTime, endTime]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            startTime: { type: string }
            endTime: { type: string }
        handoverDate: { type: string, format: date }
        preparedByUserId: { $ref: "#/components/schemas/Uuid" }
        acknowledgedByUserId: { type: string, format: uuid, nullable: true }
        summary: { type: string, nullable: true }
        status:
          $ref: "#/components/schemas/EngineeringShiftHandoverStatus"
        preparedAt: { type: string, format: date-time }
        acknowledgedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        dataset:
          $ref: "#/components/schemas/EngineeringHandoverDataset"
    CreateEngineeringShiftHandoverRequest:
      type: object
      required: [outgoingShiftId, incomingShiftId, handoverDate]
      properties:
        outgoingShiftId: { $ref: "#/components/schemas/Uuid" }
        incomingShiftId: { $ref: "#/components/schemas/Uuid" }
        handoverDate: { type: string, format: date }
        summary: { type: string }
    UpdateEngineeringShiftHandoverRequest:
      type: object
      properties:
        summary: { type: string }

    # ------------------------------------------------------------------
    # CR-BE-MOB-02 PART 07B — Security Shift Handover contract schemas
    # (BE-12G binding + BE-12M reporting dataset). Shapes mirror the
    # implemented public DTOs; the BE-10J handover lifecycle stays the only
    # authority on handover status.
    # ------------------------------------------------------------------
    SecurityShiftHandoverBindingStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
      description: >-
        The binding's own status. INACTIVE never blocks a re-bind and never
        affects the underlying BE-10J handover lifecycle.
    SecurityShiftHandoverBinding:
      type: object
      description: >-
        BE-12G binding attaching an existing BE-10J shift handover to a
        Security operational context (start Security Post + optional Patrol
        Route) under the same Building. `shiftHandoverId` references the
        authoritative BE-10J handover; `status` here is the binding's own
        ACTIVE/INACTIVE flag, never a duplicate of the handover lifecycle.
      required:
        [id, clientId, buildingId, shiftHandoverId, startSecurityPostId,
         patrolRouteId, status, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        shiftHandoverId: { $ref: "#/components/schemas/Uuid" }
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        patrolRouteId: { type: string, format: uuid, nullable: true }
        status:
          $ref: "#/components/schemas/SecurityShiftHandoverBindingStatus"
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateSecurityShiftHandoverBindingRequest:
      type: object
      required: [shiftHandoverId]
      properties:
        shiftHandoverId: { $ref: "#/components/schemas/Uuid" }
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        patrolRouteId: { type: string, format: uuid, nullable: true }
        status:
          $ref: "#/components/schemas/SecurityShiftHandoverBindingStatus"
    UpdateSecurityShiftHandoverBindingRequest:
      type: object
      description: >-
        Partial update. `shiftHandoverId`, `clientId`, `buildingId` and
        `createdByUserId` are immutable; at least one updatable field is
        required.
      properties:
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        patrolRouteId: { type: string, format: uuid, nullable: true }
        status:
          $ref: "#/components/schemas/SecurityShiftHandoverBindingStatus"
    SecurityShiftHandoverDatasetRow:
      type: object
      description: >-
        One BE-12M reporting row joining the BE-12G binding to the
        authoritative BE-10J handover. `bindingStatus` is the binding's own
        status; `handoverStatus` is the authoritative BE-10J lifecycle status.
      required:
        [bindingId, shiftHandoverId, startSecurityPostId, startSecurityPostCode,
         patrolRouteId, patrolRouteCode, bindingStatus, handoverStatus,
         handoverCreatedAt]
      properties:
        bindingId: { $ref: "#/components/schemas/Uuid" }
        shiftHandoverId: { $ref: "#/components/schemas/Uuid" }
        startSecurityPostId: { type: string, format: uuid, nullable: true }
        startSecurityPostCode: { type: string, nullable: true }
        patrolRouteId: { type: string, format: uuid, nullable: true }
        patrolRouteCode: { type: string, nullable: true }
        bindingStatus: { type: string }
        handoverStatus: { type: string }
        handoverCreatedAt: { type: string, format: date-time }

    # ------------------------------------------------------------------
    # CR-BE-MOB-02 PART 07C — Mobile Current Shift effective-context schemas
    # (BE-25M self-service). Shapes mirror the implemented DTOs; ids are the
    # authoritative BE-03E roster / shift ids, never invented.
    # ------------------------------------------------------------------
    MobileSecurityPostSummary:
      type: object
      description: >-
        MOB-C03 PART 03B — Minimal public-safe Security Post summary for mobile
        shift read projection. Only the authoritative backend-assigned post for
        this roster assignment is exposed; null means no valid assigned Security
        Post (no assignment, inactive post, or cross-building legacy/corrupt
        relation). This does not grant authorization by itself and is not
        attendance/patrol/handover state.
      required: [id, code, name]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
    MobileCurrentShift:
      type: object
      description: >-
        One Shift currently in effect for the authenticated user, resolved
        from the BE-03E Shift roster + BE-03C Workforce Profile + BE-02F/G
        Building access. `shiftId` is the authoritative `shifts.id`;
        `assignmentId` is the authoritative
        `workforce_shift_assignments.id`; `workforceProfileId` is the
        authenticated user's linked `workforce_profiles.id`.
        MOB-C03 PART 03B: includes the backend-assigned Security Post for this
        roster row as a nullable summary. It also carries no
        client-computed "active shift" boolean: a Shift is current only
        because the backend returned it in `shifts` at `asOf`; clients must
        not infer on-shift state from device time, a cached roster, a
        building selection, a role, or a previous shift.
      required:
        [assignmentId, shiftId, workforceProfileId, employeeCode, clientId,
         buildingId, buildingCode, buildingName, code, name, startTime, endTime,
         status, effectiveFrom, effectiveUntil, securityPost]
      properties:
        assignmentId: { $ref: "#/components/schemas/Uuid" }
        shiftId: { $ref: "#/components/schemas/Uuid" }
        workforceProfileId: { $ref: "#/components/schemas/Uuid" }
        employeeCode: { type: string }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        buildingCode: { type: string }
        buildingName: { type: string }
        code: { type: string }
        name: { type: string }
        startTime:
          type: string
          description: Wall-clock HH:MM:SS in the Building timezone.
        endTime:
          type: string
          description: >-
            Wall-clock HH:MM:SS in the Building timezone. An `endTime` earlier
            than `startTime` is an overnight window crossing midnight (the
            backend is overnight-aware); the Building IANA timezone is the
            authority that interprets these wall-clock times.
        status:
          type: string
          description: >-
            The Shift definition status. Only ACTIVE Shifts are returned here
            (an inactive Shift or roster assignment is excluded), so every
            item in this context is ACTIVE.
        effectiveFrom:
          type: string
          format: date-time
          nullable: true
          description: Absolute roster effective-window start; null = unbounded.
        effectiveUntil:
          type: string
          format: date-time
          nullable: true
          description: Absolute roster effective-window end; null = unbounded.
        securityPost:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/MobileSecurityPostSummary"
          description: >-
            MOB-C03 PART 03B — Backend-assigned Security Post for this roster
            assignment. Null means no valid assigned Security Post (no
            assignment, inactive post, or cross-building legacy/corrupt
            relation). This does not grant authorization by itself and is not
            attendance/patrol/handover state.
    MobileCurrentShiftContext:
      type: object
      description: >-
        The effective current-shift context of the authenticated user,
        evaluated at the backend instant `asOf`. `shifts` is the authoritative
        result: the caller is on shift exactly when it contains one or more
        items, and NOT on shift when it is empty. There is no separate
        "isActive"/"onShift" boolean — presence of an item is the only signal,
        and it is recomputed on every request from the live roster, shift,
        Building scope and Building timezone (assignment removed/inactive,
        profile inactive, Building scope removed, or a Building without an
        IANA timezone all yield exclusion rather than a guessed state).
      required: [asOf, shifts]
      properties:
        asOf:
          type: string
          format: date-time
          description: Instant the derivation was evaluated at (ISO-8601 UTC).
        shifts:
          type: array
          description: Shifts currently in effect; an empty array = not on shift at `asOf`.
          items: { $ref: "#/components/schemas/MobileCurrentShift" }

    # ------------------------------------------------------------------
    # CR-BE-MOB-05 PART 01 — Mobile Upcoming Shifts self-service schemas
    # (BE-25M family). The item shape deliberately reuses the authoritative
    # MobileCurrentShift roster-row shape; ids are the authoritative BE-03E
    # roster / shift ids, never invented.
    # ------------------------------------------------------------------
    MobileUpcomingShift:
      allOf:
        - $ref: "#/components/schemas/MobileCurrentShift"
      description: >-
        One current or future shift assignment of the authenticated user.
        Shape is identical to `MobileCurrentShift` (a BE-03E roster row):
        `shiftId` is the authoritative `shifts.id`; `assignmentId` is the
        authoritative `workforce_shift_assignments.id`. Only ACTIVE roster
        assignments of ACTIVE Shifts in accessible Buildings carrying a
        valid IANA timezone are returned; no attendance/clock/schedule state
        is invented. MOB-C03 PART 03B: includes the same nullable
        securityPost summary as MobileCurrentShift. The daily wall-clock
        window is not evaluated here; this is a roster schedule, not a live
        on-shift/clock state.
    MobileUpcomingShiftsContext:
      type: object
      description: >-
        The effective upcoming-shifts schedule of the authenticated user.
        `dateFrom` is the effective inclusive lower bound applied to the
        roster window (defaults to `asOf` when omitted); `dateTo` is the
        effective exclusive upper bound (null when unbounded; a date-only
        `dateTo` is inclusive of that whole UTC day). An empty `shifts`
        array means no matching roster in the queried window.
      required: [asOf, dateFrom, dateTo, shifts]
      properties:
        asOf: { type: string, format: date-time }
        dateFrom: { type: string, format: date-time }
        dateTo: { type: string, format: date-time, nullable: true }
        shifts:
          type: array
          items: { $ref: "#/components/schemas/MobileUpcomingShift" }

    # ------------------------------------------------------------------
    # CR-BE-MOB-05 PART 02 — Workforce Attendance schemas. Shapes mirror
    # the implemented DTOs; ids are authoritative (`attendance_records.id`,
    # `workforce_profiles.id`, `buildings.id`,
    # `workforce_shift_assignments.id`, `shifts.id`), never invented.
    # ------------------------------------------------------------------
    AttendanceStatus:
      type: string
      enum: [CLOCKED_IN, CLOCKED_OUT]
      description: >-
        Lifecycle of an attendance record. CLOCKED_IN is open; CLOCKED_OUT
        is terminal (preserved, never reopened or deleted).
    ClockInRequest:
      type: object
      description: >-
        Clock-in payload: exactly the Building the authenticated profile is
        clocking into. Identity and timestamps are never accepted from the
        client.
      required: [buildingId]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
    AttendanceRecord:
      type: object
      description: >-
        The authoritative workforce attendance record (CR-BE-MOB-05 PART 02).
        `id` is `attendance_records.id`; `workforceProfileId` is
        `workforce_profiles.id` (resolved from the authenticated session);
        `buildingId` is `buildings.id` (BE-02F/G access-asserted at
        clock-in); `workforceShiftAssignmentId` / `shiftId` are set together
        when an applicable ACTIVE roster row was bound (both null otherwise).
        `clockInAt` / `clockOutAt` are backend-set database timestamps —
        never client-supplied.
      required:
        [id, clientId, buildingId, buildingCode, buildingName,
         workforceProfileId, employeeCode, workforceShiftAssignmentId,
         shiftId, shiftCode, shiftName, clockInAt, clockOutAt, status,
         createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        buildingCode: { type: string }
        buildingName: { type: string }
        workforceProfileId: { $ref: "#/components/schemas/Uuid" }
        employeeCode: { type: string }
        workforceShiftAssignmentId:
          type: string
          format: uuid
          nullable: true
        shiftId: { type: string, format: uuid, nullable: true }
        shiftCode: { type: string, nullable: true }
        shiftName: { type: string, nullable: true }
        clockInAt: { type: string, format: date-time }
        clockOutAt: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/AttendanceStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    # ------------------------------------------------------------------
    # CR-BE-MOB-05 PART 03 — Security Operational Logbook schemas. Shapes
    # mirror the implemented DTOs; ids are authoritative
    # (`security_logbook_entries.id`, `workforce_profiles.id`,
    # `buildings.id`, `shift_handovers.id`), never invented.
    # ------------------------------------------------------------------
    SecurityLogbookStatus:
      type: string
      enum: [OPEN, CLOSED]
      description: >-
        Lifecycle of a logbook entry. OPEN entries accept updates; CLOSED is
        terminal (preserved, never reopened or deleted).
    SecurityLogbookCategory:
      type: string
      enum: [GENERAL, INCIDENT, PATROL, HANDOVER, FINDING]
      description: >-
        Fixed classification vocabulary only — the backend branches no
        behavior on a category; no patrol / incident / finding engine is
        implied.
    CreateSecurityLogbookEntryRequest:
      type: object
      description: >-
        Create payload: Building, category, summary, optional detail and an
        optional existing authoritative `shift_handovers.id` in the SAME
        Building. Identity and timestamps are never accepted from the
        client.
      required: [buildingId, category, summary]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        category: { $ref: "#/components/schemas/SecurityLogbookCategory" }
        summary: { type: string, maxLength: 500 }
        detail: { type: string, maxLength: 4000, nullable: true }
        shiftHandoverId: { type: string, format: uuid, nullable: true }
    UpdateSecurityLogbookEntryRequest:
      type: object
      description: >-
        Partial update payload (OPEN entries only). All fields optional;
        `shiftHandoverId: null` clears the binding. Timestamps are never
        accepted from the client.
      properties:
        category: { $ref: "#/components/schemas/SecurityLogbookCategory" }
        summary: { type: string, maxLength: 500 }
        detail: { type: string, maxLength: 4000, nullable: true }
        status: { $ref: "#/components/schemas/SecurityLogbookStatus" }
        shiftHandoverId: { type: string, format: uuid, nullable: true }
    SecurityLogbookEntry:
      type: object
      description: >-
        The authoritative Security logbook entry (CR-BE-MOB-05 PART 03).
        `id` is `security_logbook_entries.id`; `workforceProfileId` is
        `workforce_profiles.id` (resolved from the authenticated session);
        `buildingId` is `buildings.id` (BE-02F/G access-asserted);
        `shiftHandoverId` is `shift_handovers.id` when a valid same-Building
        handover was bound (null otherwise). `recordedAt` is a backend-set
        database timestamp — never client-supplied.
      required:
        [id, clientId, buildingId, buildingCode, buildingName,
         workforceProfileId, employeeCode, shiftHandoverId, category,
         summary, detail, status, recordedAt, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        buildingCode: { type: string }
        buildingName: { type: string }
        workforceProfileId: { $ref: "#/components/schemas/Uuid" }
        employeeCode: { type: string }
        shiftHandoverId: { type: string, format: uuid, nullable: true }
        category: { $ref: "#/components/schemas/SecurityLogbookCategory" }
        summary: { type: string }
        detail: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/SecurityLogbookStatus" }
        recordedAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    # ------------------------------------------------------------------
    # CR-BE-MOB-02 PART 07D — Mobile My Team effective-context schemas
    # (BE-25N self-service). Shapes mirror the implemented DTOs; ids are the
    # authoritative BE-03B/BE-03C hierarchy ids, never invented.
    # ------------------------------------------------------------------
    MobileTeamRef:
      type: object
      description: Minimal authoritative reference to a hierarchy node.
      required: [id, code, name]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
    MobileMyTeam:
      type: object
      description: >-
        The caller's team with its authoritative hierarchy resolved
        (Team → Department → Organization → Client). `id` is `teams.id`.
      required:
        [id, code, name, description, status, department, organization, client]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { type: string }
        department: { $ref: "#/components/schemas/MobileTeamRef" }
        organization: { $ref: "#/components/schemas/MobileTeamRef" }
        client: { $ref: "#/components/schemas/MobileTeamRef" }
    MobileTeamMember:
      type: object
      description: >-
        One ACTIVE workforce profile in the caller's team. `id` is
        `workforce_profiles.id`; `positionId` is `positions.id`; `userId` is
        `users.id` when a User account is linked.
      required:
        [id, employeeCode, fullName, workforceType, status, positionId, userId]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        employeeCode: { type: string }
        fullName: { type: string }
        workforceType: { type: string }
        status: { type: string }
        positionId: { $ref: "#/components/schemas/Uuid" }
        userId: { type: string, format: uuid, nullable: true }
    MobileMyTeamContext:
      type: object
      description: >-
        The effective my-team context of the authenticated user. `team` is
        null when the caller has no linked ACTIVE profile, no team, or a
        profile whose Client is outside the accessible scope; `members` lists
        ACTIVE profiles in that team (the caller included).
      required: [workforceProfile, team, members]
      properties:
        workforceProfile:
          type: object
          nullable: true
          required: [id, employeeCode, fullName]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            employeeCode: { type: string }
            fullName: { type: string }
        team:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/MobileMyTeam"
        members:
          type: array
          items: { $ref: "#/components/schemas/MobileTeamMember" }

    # ------------------------------------------------------------------
    # CR-BE-MOB-01 PART 04 — Work Order material context reference schemas
    # (BE-16A Item master, BE-16B Warehouse master, BE-16H Asset ↔ spare
    # part binding). Shapes mirror the implemented public DTOs. The usage,
    # stock, approved-quantity and verification schemas already exist and
    # are NOT redefined here.
    # ------------------------------------------------------------------
    InventoryItemStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
      description: INACTIVE items are preserved for history, never deleted.
    InventoryItemType:
      type: string
      enum: [SPARE_PART, MATERIAL, CONSUMABLE]
      description: >-
        Classification data only — the backend branches no behaviour on it.
    InventoryItem:
      type: object
      description: >-
        BE-16A Inventory Item master, owned by exactly one Client (`code` is
        unique per Client). `uomId` references the shared BE-07 UOM foundation
        and is the UOM snapshotted onto a Work Order material usage. Carries no
        stock, price or procurement state.
      required:
        [id, clientId, code, name, itemType, category, uomId, description,
         status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string }
        itemType: { $ref: "#/components/schemas/InventoryItemType" }
        category: { type: string, nullable: true }
        uomId: { type: string, format: uuid, nullable: true }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/InventoryItemStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        uom:
          type: object
          nullable: true
          description: Resolved BE-07 UOM context when available.
          required: [id, code, name, symbol]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            symbol: { type: string }

    InventoryWarehouseStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    InventoryWarehouse:
      type: object
      description: >-
        BE-16B Warehouse / store, operated within exactly one Building
        (`code` unique per Building). `clientId` is derived through
        Building → Property → Client and is never accepted from the caller.
        Stock lives on the published stock-balance / stock-movement surface.
      required:
        [id, clientId, buildingId, functionalLocationId, code, name,
         description, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        functionalLocationId: { type: string, format: uuid, nullable: true }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/InventoryWarehouseStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        functionalLocation:
          type: object
          nullable: true
          description: Resolved BE-04 Functional Location when bound.
          required: [id, code, name, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string }

    AssetSparePartStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    AssetSparePart:
      type: object
      description: >-
        BE-16H many-to-many binding between a BE-05 Asset and a SPARE_PART
        Inventory Item. `requiredQuantity` is a planning reference per asset —
        it is NOT an approved quantity (that is `MaterialRequest.approvedQuantity`)
        and NOT an issued quantity (that is `WorkOrderMaterialUsage.quantity`).
      required:
        [id, clientId, buildingId, assetId, itemId, requiredQuantity, status,
         notes, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        itemId: { $ref: "#/components/schemas/Uuid" }
        requiredQuantity: { type: number }
        status: { $ref: "#/components/schemas/AssetSparePartStatus" }
        notes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        asset:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            assetCode: { type: string }
            assetName: { type: string }
            buildingId: { $ref: "#/components/schemas/Uuid" }
            status: { type: string }
        item:
          type: object
          nullable: true
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            itemType: { type: string }
            uomId: { type: string, format: uuid, nullable: true }

    # ------------------------------------------------------------------
    # CR-BE-MOB-03 PART 01 — Workforce team workload & status schemas
    # (BE-23G KPI, BE-03I2 reporting, BE-03F reporting lines).
    # Mirrors the implemented DTOs; read-only projections over BE-03/BE-07.
    # ------------------------------------------------------------------
    WorkforceKpiHeadcount:
      type: object
      description: BE-03C headcount, scoped to the accessible Building set.
      required: [total, active, inactive, internal, outsourced, contract, withAssignments]
      properties:
        total: { type: integer, minimum: 0 }
        active: { type: integer, minimum: 0 }
        inactive: { type: integer, minimum: 0 }
        internal: { type: integer, minimum: 0 }
        outsourced: { type: integer, minimum: 0 }
        contract: { type: integer, minimum: 0 }
        withAssignments: { type: integer, minimum: 0, description: Distinct workforce profiles holding an assignment in range. }
    WorkforceKpiAssignments:
      type: object
      description: BE-07 assignment lifecycle counters in range (excluding CANCELLED occurrences from scheduled).
      required: [scheduled, completed, inProgress, open, cancelled, overdue, completionRate, byAssigneeType]
      properties:
        scheduled: { type: integer, minimum: 0 }
        completed: { type: integer, minimum: 0 }
        inProgress: { type: integer, minimum: 0 }
        open: { type: integer, minimum: 0 }
        cancelled: { type: integer, minimum: 0 }
        overdue: { type: integer, minimum: 0, description: Past due and not completed (grace-aware). }
        completionRate: { type: number, description: completed / scheduled as a percentage, 2dp; 0 when scheduled is 0. }
        byAssigneeType:
          type: object
          required: [workforce, team]
          properties:
            workforce: { type: integer, minimum: 0 }
            team: { type: integer, minimum: 0 }
    WorkforceKpiManHours:
      type: object
      description: Derived man-hours over completed assignments (completed_at - started_at); no stored timesheet field exists.
      required: [totalHours, measuredAssignments, unmeasuredAssignments, averageHoursPerAssignment, averageHoursPerWorkforce]
      properties:
        totalHours: { type: number }
        measuredAssignments: { type: integer, minimum: 0 }
        unmeasuredAssignments: { type: integer, minimum: 0, description: Completed with no recorded start — contribute zero hours. }
        averageHoursPerAssignment: { type: number }
        averageHoursPerWorkforce: { type: number }
    WorkforceKpiMember:
      type: object
      description: Per-workforce man-hour and completion breakdown, descending by hours.
      required: [workforceId, employeeCode, fullName, workforceType, status, scheduled, completed, overdue, completionRate, totalHours]
      properties:
        workforceId: { $ref: "#/components/schemas/Uuid" }
        employeeCode: { type: string }
        fullName: { type: string }
        workforceType: { type: string }
        status: { type: string }
        scheduled: { type: integer, minimum: 0 }
        completed: { type: integer, minimum: 0 }
        overdue: { type: integer, minimum: 0 }
        completionRate: { type: number }
        totalHours: { type: number }
    WorkforceKpi:
      type: object
      description: BE-23G workforce KPI projection (read-only).
      required:
        [buildingId, buildingScope, dateFrom, dateTo, graceMinutes, asOf,
         workforce, assignments, manHours, byWorkforce]
      properties:
        buildingId: { type: string, format: uuid, nullable: true, description: Null when the KPI is a multi-building rollup. }
        buildingScope: { type: array, items: { type: string, format: uuid }, description: Every Building actually included in the numbers. }
        dateFrom: { type: string, nullable: true }
        dateTo: { type: string, nullable: true }
        graceMinutes: { type: integer, minimum: 0 }
        asOf: { type: string, format: date-time, description: Evaluation instant used to decide overdue. }
        workforce: { $ref: "#/components/schemas/WorkforceKpiHeadcount" }
        assignments: { $ref: "#/components/schemas/WorkforceKpiAssignments" }
        manHours: { $ref: "#/components/schemas/WorkforceKpiManHours" }
        byWorkforce: { type: array, items: { $ref: "#/components/schemas/WorkforceKpiMember" } }
    CurrentSupervisor:
      type: object
      description: >-
        BE-03F — the Supervisor genuinely in force for a Workforce Profile at
        the current instant (ACTIVE line, ACTIVE supervisor, effective window).
      required:
        [reportingLineId, workforceProfileId, supervisorWorkforceProfileId,
         employeeCode, fullName, effectiveFrom, effectiveUntil]
      properties:
        reportingLineId: { $ref: "#/components/schemas/Uuid" }
        workforceProfileId: { $ref: "#/components/schemas/Uuid" }
        supervisorWorkforceProfileId: { $ref: "#/components/schemas/Uuid" }
        employeeCode: { type: string }
        fullName: { type: string }
        effectiveFrom: { type: string, format: date-time, nullable: true }
        effectiveUntil: { type: string, format: date-time, nullable: true }
    DirectReport:
      type: object
      description: BE-03F — one ACTIVE direct report of a Supervisor (profile fields denormalised).
      required:
        [reportingLineId, workforceProfileId, employeeCode, fullName, status,
         effectiveFrom, effectiveUntil]
      properties:
        reportingLineId: { $ref: "#/components/schemas/Uuid" }
        workforceProfileId: { $ref: "#/components/schemas/Uuid" }
        employeeCode: { type: string }
        fullName: { type: string }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        effectiveFrom: { type: string, format: date-time, nullable: true }
        effectiveUntil: { type: string, format: date-time, nullable: true }
    WorkforceReportingRecord:
      type: object
      description: >-
        BE-03I1 query-side projection of one Workforce Profile (and its
        resolved context) assembled from existing BE-03 source tables. The
        reporting module owns no source-of-truth data and writes nothing.
      required:
        [client, organization, department, team, position, profile, user,
         skills, shifts, supervisor, directReports, buildingAssignments,
         externalAffiliations]
      properties:
        client:
          type: object
          required: [id, code, name, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string, enum: [ACTIVE, INACTIVE] }
        organization:
          type: object
          required: [id, clientId, code, name, description, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            clientId: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            description: { type: string, nullable: true }
            status: { type: string, enum: [ACTIVE, INACTIVE] }
        department:
          type: object
          required: [id, organizationId, clientId, code, name, description, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            organizationId: { $ref: "#/components/schemas/Uuid" }
            clientId: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            description: { type: string, nullable: true }
            status: { type: string, enum: [ACTIVE, INACTIVE] }
        team:
          type: object
          nullable: true
          required: [id, departmentId, organizationId, clientId, code, name, description, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            departmentId: { $ref: "#/components/schemas/Uuid" }
            organizationId: { $ref: "#/components/schemas/Uuid" }
            clientId: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            description: { type: string, nullable: true }
            status: { type: string, enum: [ACTIVE, INACTIVE] }
        position:
          type: object
          required: [id, organizationId, departmentId, clientId, code, name, description, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            organizationId: { $ref: "#/components/schemas/Uuid" }
            departmentId: { type: string, format: uuid, nullable: true }
            clientId: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            description: { type: string, nullable: true }
            status: { type: string, enum: [ACTIVE, INACTIVE] }
        profile:
          type: object
          required: [id, employeeCode, fullName, workforceType, status, userId, createdAt, updatedAt]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            employeeCode: { type: string }
            fullName: { type: string }
            workforceType: { type: string, enum: [INTERNAL, OUTSOURCED, CONTRACT, EXTERNAL] }
            status: { type: string, enum: [ACTIVE, INACTIVE] }
            userId: { type: string, format: uuid, nullable: true }
            createdAt: { type: string, format: date-time }
            updatedAt: { type: string, format: date-time }
        user:
          type: object
          nullable: true
          required: [id, email, displayName, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            email: { type: string }
            displayName: { type: string }
            status: { type: string }
        skills:
          type: array
          items:
            type: object
            required: [id, skillId, code, name, category, status, assignmentStatus, proficiencyLevel, validFrom, validUntil]
            properties:
              id: { $ref: "#/components/schemas/Uuid" }
              skillId: { $ref: "#/components/schemas/Uuid" }
              code: { type: string }
              name: { type: string }
              category: { type: string, enum: [TECHNICAL, OPERATIONAL, SAFETY, SPECIALIST] }
              status: { type: string, enum: [ACTIVE, INACTIVE] }
              assignmentStatus: { type: string, enum: [ACTIVE, INACTIVE] }
              proficiencyLevel: { type: string, enum: [BASIC, INTERMEDIATE, ADVANCED, EXPERT] }
              validFrom: { type: string, format: date-time, nullable: true }
              validUntil: { type: string, format: date-time, nullable: true }
        shifts:
          type: array
          items:
            type: object
            required:
              [id, shiftId, code, name, startTime, endTime, buildingId,
               buildingCode, buildingName, shiftStatus, assignmentStatus,
               effectiveFrom, effectiveUntil]
            properties:
              id: { $ref: "#/components/schemas/Uuid" }
              shiftId: { $ref: "#/components/schemas/Uuid" }
              code: { type: string }
              name: { type: string }
              startTime: { type: string }
              endTime: { type: string }
              buildingId: { $ref: "#/components/schemas/Uuid" }
              buildingCode: { type: string }
              buildingName: { type: string }
              shiftStatus: { type: string, enum: [ACTIVE, INACTIVE] }
              assignmentStatus: { type: string, enum: [ACTIVE, INACTIVE] }
              effectiveFrom: { type: string, format: date-time, nullable: true }
              effectiveUntil: { type: string, format: date-time, nullable: true }
        supervisor:
          type: object
          nullable: true
          required:
            [reportingLineId, workforceProfileId, employeeCode, fullName,
             assignmentStatus, effectiveFrom, effectiveUntil]
          properties:
            reportingLineId: { $ref: "#/components/schemas/Uuid" }
            workforceProfileId: { $ref: "#/components/schemas/Uuid" }
            employeeCode: { type: string }
            fullName: { type: string }
            assignmentStatus: { type: string, enum: [ACTIVE, INACTIVE] }
            effectiveFrom: { type: string, format: date-time, nullable: true }
            effectiveUntil: { type: string, format: date-time, nullable: true }
        directReports:
          type: array
          items:
            type: object
            required:
              [reportingLineId, workforceProfileId, employeeCode, fullName,
               assignmentStatus, effectiveFrom, effectiveUntil]
            properties:
              reportingLineId: { $ref: "#/components/schemas/Uuid" }
              workforceProfileId: { $ref: "#/components/schemas/Uuid" }
              employeeCode: { type: string }
              fullName: { type: string }
              assignmentStatus: { type: string, enum: [ACTIVE, INACTIVE] }
              effectiveFrom: { type: string, format: date-time, nullable: true }
              effectiveUntil: { type: string, format: date-time, nullable: true }
        buildingAssignments:
          type: array
          items:
            type: object
            required:
              [id, buildingId, buildingCode, buildingName, propertyId,
               propertyCode, propertyName, clientId, buildingStatus,
               assignmentStatus, effectiveFrom, effectiveUntil]
            properties:
              id: { $ref: "#/components/schemas/Uuid" }
              buildingId: { $ref: "#/components/schemas/Uuid" }
              buildingCode: { type: string }
              buildingName: { type: string }
              propertyId: { $ref: "#/components/schemas/Uuid" }
              propertyCode: { type: string }
              propertyName: { type: string }
              clientId: { $ref: "#/components/schemas/Uuid" }
              buildingStatus: { type: string, enum: [ACTIVE, INACTIVE] }
              assignmentStatus: { type: string, enum: [ACTIVE, INACTIVE] }
              effectiveFrom: { type: string, format: date-time, nullable: true }
              effectiveUntil: { type: string, format: date-time, nullable: true }
        externalAffiliations:
          type: array
          items:
            type: object
            required:
              [id, externalOrganizationId, externalOrganizationCode,
               externalOrganizationName, externalOrganizationStatus,
               externalPersonnelCode, assignmentStatus, effectiveFrom,
               effectiveUntil]
            properties:
              id: { $ref: "#/components/schemas/Uuid" }
              externalOrganizationId: { $ref: "#/components/schemas/Uuid" }
              externalOrganizationCode: { type: string }
              externalOrganizationName: { type: string }
              externalOrganizationStatus: { type: string }
              externalPersonnelCode: { type: string }
              assignmentStatus: { type: string, enum: [ACTIVE, INACTIVE] }
              effectiveFrom: { type: string, format: date-time, nullable: true }
              effectiveUntil: { type: string, format: date-time, nullable: true }

    # ------------------------------------------------------------------
    # CR-BE-MOB-03 PART 02 — Housekeeping supervisor supporting schemas
    # (BE-11J consumable readiness, BE-16J consumable bindings, BE-11L
    # complaint bindings, BE-11M report datasets). Mirrors the implemented
    # DTOs; ids are the authoritative BE-11/BE-16 rows.
    # ------------------------------------------------------------------
    ConsumableRequirementStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    ReadinessStatus:
      type: string
      enum: [READY, LOW, NOT_READY, UNKNOWN]
      description: >-
        Derived operational readiness of a consumable requirement from
        authoritative BE-16 stock: READY when available >= required, LOW
        when 0 < available < required, NOT_READY when 0 or null,
        UNKNOWN when inactive.
    ConsumableRequirement:
      type: object
      description: BE-11J consumable requirement with latest readiness context.
      required:
        [id, clientId, buildingId, cleaningAreaId, code, name,
         requiredQuantity, unit, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaId: { type: string, format: uuid, nullable: true }
        code: { type: string }
        name: { type: string }
        requiredQuantity: { type: number, minimum: 0 }
        unit: { type: string }
        status: { $ref: "#/components/schemas/ConsumableRequirementStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        cleaningArea:
          type: object
          nullable: true
          required: [id, code, name, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string }
        currentReadiness:
          type: object
          nullable: true
          required: [readinessStatus, availableQuantity, checkedAt]
          properties:
            readinessStatus: { $ref: "#/components/schemas/ReadinessStatus" }
            availableQuantity: { type: number, nullable: true }
            checkedAt: { type: string, format: date-time }
    CreateConsumableRequirementRequest:
      type: object
      required: [buildingId, code, name, unit]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        code: { type: string }
        name: { type: string, maxLength: 160 }
        requiredQuantity: { type: number, minimum: 0, default: 1 }
        unit: { type: string, maxLength: 32 }
        status: { $ref: "#/components/schemas/ConsumableRequirementStatus" }
    UpdateConsumableRequirementRequest:
      type: object
      properties:
        name: { type: string, maxLength: 160 }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        requiredQuantity: { type: number, minimum: 0 }
        unit: { type: string, maxLength: 32 }
        status: { $ref: "#/components/schemas/ConsumableRequirementStatus" }
    RecordConsumableReadinessRequest:
      type: object
      required: [readinessStatus]
      properties:
        readinessStatus: { $ref: "#/components/schemas/ReadinessStatus" }
        availableQuantity: { type: number, minimum: 0, nullable: true, default: null }
        operationalDate: { type: string, description: YYYY-MM-DD operational date. }
        notes: { type: string, nullable: true }
    ConsumableReadiness:
      type: object
      description: BE-11J recorded readiness check.
      required:
        [id, clientId, buildingId, requirementId, operationalDate,
         readinessStatus, availableQuantity, checkedByUserId, checkedAt,
         notes, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        requirementId: { $ref: "#/components/schemas/Uuid" }
        operationalDate: { type: string }
        readinessStatus: { $ref: "#/components/schemas/ReadinessStatus" }
        availableQuantity: { type: number, nullable: true }
        checkedByUserId: { $ref: "#/components/schemas/Uuid" }
        checkedAt: { type: string, format: date-time }
        notes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        requirement:
          type: object
          nullable: true
          required: [id, code, name, requiredQuantity, unit]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            requiredQuantity: { type: number }
            unit: { type: string }
        cleaningArea:
          type: object
          nullable: true
          required: [id, code, name]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
    HkConsumableBindingStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    HkConsumableBinding:
      type: object
      description: >-
        BE-16J binding of a BE-11J requirement to a BE-16A CONSUMABLE item +
        BE-16B warehouse. `readiness` is derived from authoritative stock.
      required:
        [id, clientId, buildingId, cleaningAreaId, consumableRequirementId,
         itemId, warehouseId, requiredQuantity, status, notes, createdAt,
         updatedAt, readiness]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaId: { type: string, format: uuid, nullable: true }
        consumableRequirementId: { $ref: "#/components/schemas/Uuid" }
        itemId: { $ref: "#/components/schemas/Uuid" }
        warehouseId: { $ref: "#/components/schemas/Uuid" }
        requiredQuantity: { type: number, minimum: 0 }
        status: { $ref: "#/components/schemas/HkConsumableBindingStatus" }
        notes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        readiness: { $ref: "#/components/schemas/ReadinessStatus" }
        cleaningArea:
          type: object
          nullable: true
          required: [id, code, name]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
        requirement:
          type: object
          nullable: true
          required: [id, code, name, requiredQuantity, unit, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            requiredQuantity: { type: number }
            unit: { type: string }
            status: { type: string }
        item:
          type: object
          nullable: true
          required: [id, code, name, itemType, uomId]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            itemType: { type: string }
            uomId: { type: string, format: uuid, nullable: true }
        warehouse:
          type: object
          nullable: true
          required: [id, code, name, buildingId]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            buildingId: { $ref: "#/components/schemas/Uuid" }
        currentStock:
          type: object
          nullable: true
          required: [quantityOnHand, reservedQuantity, availableQuantity]
          properties:
            quantityOnHand: { type: number }
            reservedQuantity: { type: number }
            availableQuantity: { type: number }
    CreateHkConsumableBindingRequest:
      type: object
      required: [itemId, warehouseId]
      description: The consumable requirement id comes from the path.
      properties:
        itemId: { $ref: "#/components/schemas/Uuid" }
        warehouseId: { $ref: "#/components/schemas/Uuid" }
        requiredQuantity: { type: number, minimum: 0 }
        status: { $ref: "#/components/schemas/HkConsumableBindingStatus" }
        notes: { type: string, maxLength: 512 }
    UpdateHkConsumableBindingRequest:
      type: object
      properties:
        requiredQuantity: { type: number, minimum: 0 }
        status: { $ref: "#/components/schemas/HkConsumableBindingStatus" }
        notes: { type: string, maxLength: 512, nullable: true }
    HousekeepingComplaintBindingStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
    HousekeepingComplaintSourceType:
      type: string
      enum:
        [DAILY_CLEANING, TOILET_INSPECTION, PUBLIC_AREA_INSPECTION,
         SUPERVISOR_INSPECTION, CLEANING_AREA]
    HousekeepingComplaintBinding:
      type: object
      description: >-
        BE-11L binding of a customer complaint / service request to
        Housekeeping operations. Workflow stays on the referenced BE-08 /
        BE-09 / BE-07 authorities.
      required:
        [id, clientId, buildingId, complaintReference, workRequestId,
         cleaningAreaId, housekeepingSourceType, housekeepingSourceId,
         findingId, description, status, createdByUserId, createdAt,
         updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        complaintReference: { type: string }
        workRequestId: { type: string, format: uuid, nullable: true }
        cleaningAreaId: { type: string, format: uuid, nullable: true }
        housekeepingSourceType:
          type: string
          nullable: true
          $ref: "#/components/schemas/HousekeepingComplaintSourceType"
        housekeepingSourceId: { type: string, format: uuid, nullable: true }
        findingId: { type: string, format: uuid, nullable: true }
        description: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/HousekeepingComplaintBindingStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        cleaningArea:
          type: object
          nullable: true
          required: [id, code, name, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            code: { type: string }
            name: { type: string }
            status: { type: string }
        finding:
          type: object
          nullable: true
          required: [id, findingNumber, title, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            findingNumber: { type: string }
            title: { type: string }
            status: { type: string }
        workRequest:
          type: object
          nullable: true
          required: [id, requestNumber, title, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            requestNumber: { type: string }
            title: { type: string }
            status: { type: string }
    CreateHousekeepingComplaintBindingRequest:
      type: object
      required: [buildingId, complaintReference]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        complaintReference: { type: string }
        workRequestId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        housekeepingSourceType:
          $ref: "#/components/schemas/HousekeepingComplaintSourceType"
        housekeepingSourceId: { $ref: "#/components/schemas/Uuid" }
        findingId: { $ref: "#/components/schemas/Uuid" }
        description: { type: string }
        status: { $ref: "#/components/schemas/HousekeepingComplaintBindingStatus" }
    UpdateHousekeepingComplaintBindingRequest:
      type: object
      properties:
        description: { type: string, nullable: true }
        findingId: { $ref: "#/components/schemas/Uuid" }
        status: { $ref: "#/components/schemas/HousekeepingComplaintBindingStatus" }
    HousekeepingReportSummary:
      type: object
      description: BE-11M summary projection for one Building.
      required:
        [buildingId, dateRange, cleaningAreasCount, dailyCleaning,
         toiletInspections, publicAreaInspections, supervisorInspections,
         findings, consumables, qualityAudits, complaints]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        dateRange:
          type: object
          required: [dateFrom, dateTo]
          properties:
            dateFrom: { type: string, nullable: true }
            dateTo: { type: string, nullable: true }
        cleaningAreasCount: { type: integer, minimum: 0 }
        dailyCleaning:
          type: object
          required: [total, open, assigned, inProgress, completed, cancelled]
          properties:
            total: { type: integer, minimum: 0 }
            open: { type: integer, minimum: 0 }
            assigned: { type: integer, minimum: 0 }
            inProgress: { type: integer, minimum: 0 }
            completed: { type: integer, minimum: 0 }
            cancelled: { type: integer, minimum: 0 }
        toiletInspections:
          type: object
          required: [bindingsCount, executionsCount, completedCount]
          properties:
            bindingsCount: { type: integer, minimum: 0 }
            executionsCount: { type: integer, minimum: 0 }
            completedCount: { type: integer, minimum: 0 }
        publicAreaInspections:
          type: object
          required: [bindingsCount, executionsCount, completedCount]
          properties:
            bindingsCount: { type: integer, minimum: 0 }
            executionsCount: { type: integer, minimum: 0 }
            completedCount: { type: integer, minimum: 0 }
        supervisorInspections:
          type: object
          required: [total, pending, approved, rejected, reworkRequired]
          properties:
            total: { type: integer, minimum: 0 }
            pending: { type: integer, minimum: 0 }
            approved: { type: integer, minimum: 0 }
            rejected: { type: integer, minimum: 0 }
            reworkRequired: { type: integer, minimum: 0 }
        findings:
          type: object
          required: [total, open, inProgress, reworkRequired, verified, closed]
          properties:
            total: { type: integer, minimum: 0 }
            open: { type: integer, minimum: 0 }
            inProgress: { type: integer, minimum: 0 }
            reworkRequired: { type: integer, minimum: 0 }
            verified: { type: integer, minimum: 0 }
            closed: { type: integer, minimum: 0 }
        consumables:
          type: object
          required: [requirementsCount, readyCount, lowCount, notReadyCount]
          properties:
            requirementsCount: { type: integer, minimum: 0 }
            readyCount: { type: integer, minimum: 0 }
            lowCount: { type: integer, minimum: 0 }
            notReadyCount: { type: integer, minimum: 0 }
        qualityAudits:
          type: object
          required:
            [total, completed, passed, failed, reworkRequired, averageScore]
          properties:
            total: { type: integer, minimum: 0 }
            completed: { type: integer, minimum: 0 }
            passed: { type: integer, minimum: 0 }
            failed: { type: integer, minimum: 0 }
            reworkRequired: { type: integer, minimum: 0 }
            averageScore: { type: number, nullable: true }
        complaints:
          type: object
          required: [totalBindings, activeBindings]
          properties:
            totalBindings: { type: integer, minimum: 0 }
            activeBindings: { type: integer, minimum: 0 }
    HousekeepingCleaningReportRow:
      type: object
      description: BE-11M daily-cleaning dataset row.
      required:
        [taskId, operationalDate, occurrenceAt, status, cleaningAreaId,
         cleaningAreaCode, cleaningAreaName, cleaningAreaType, scheduleCode,
         scheduleName, assigneeType, workforceProfileId, teamId,
         completedByUserId, completedAt]
      properties:
        taskId: { $ref: "#/components/schemas/Uuid", description: BE-07 generated task id. }
        operationalDate: { type: string }
        occurrenceAt: { type: string, format: date-time }
        status: { type: string }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaCode: { type: string }
        cleaningAreaName: { type: string }
        cleaningAreaType: { type: string }
        scheduleCode: { type: string }
        scheduleName: { type: string }
        assigneeType: { type: string, nullable: true }
        workforceProfileId: { type: string, format: uuid, nullable: true }
        teamId: { type: string, format: uuid, nullable: true }
        completedByUserId: { type: string, format: uuid, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
    HousekeepingInspectionReportRow:
      type: object
      description: BE-11M toilet / public-area inspection dataset row.
      required:
        [inspectionType, bindingId, executionId, executionStatus,
         cleaningAreaId, cleaningAreaCode, cleaningAreaName, templateCode,
         templateName, roomCode, functionalLocationCode, startedAt,
         completedAt]
      properties:
        inspectionType: { type: string, enum: [TOILET, PUBLIC_AREA] }
        bindingId: { $ref: "#/components/schemas/Uuid" }
        executionId: { type: string, format: uuid, nullable: true, description: BE-07 checklist execution id. }
        executionStatus: { type: string, nullable: true }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaCode: { type: string }
        cleaningAreaName: { type: string }
        templateCode: { type: string }
        templateName: { type: string }
        roomCode: { type: string, nullable: true }
        functionalLocationCode: { type: string, nullable: true }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
    HousekeepingSupervisorInspectionReportRow:
      type: object
      description: BE-11M supervisor-inspection dataset row.
      required:
        [inspectionId, targetType, targetId, cleaningAreaId, cleaningAreaCode,
         supervisorUserId, decision, status, inspectedAt, notes]
      properties:
        inspectionId: { $ref: "#/components/schemas/Uuid" }
        targetType: { type: string }
        targetId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaCode: { type: string }
        supervisorUserId: { $ref: "#/components/schemas/Uuid" }
        decision: { type: string, nullable: true }
        status: { type: string }
        inspectedAt: { type: string, format: date-time, nullable: true }
        notes: { type: string, nullable: true }
    HousekeepingFindingReportRow:
      type: object
      description: BE-11M housekeeping finding dataset row.
      required:
        [findingId, findingNumber, title, status, sourceType, sourceId,
         cleaningAreaId, cleaningAreaCode, reportedByUserId, reportedAt]
      properties:
        findingId: { $ref: "#/components/schemas/Uuid", description: BE-09 finding id. }
        findingNumber: { type: string }
        title: { type: string }
        status: { type: string }
        sourceType: { type: string }
        sourceId: { type: string, format: uuid }
        cleaningAreaId: { $ref: "#/components/schemas/Uuid" }
        cleaningAreaCode: { type: string }
        reportedByUserId: { $ref: "#/components/schemas/Uuid" }
        reportedAt: { type: string, format: date-time }
    HousekeepingConsumableReportRow:
      type: object
      description: BE-11M consumable dataset row.
      required:
        [requirementId, requirementCode, requirementName, unit,
         requiredQuantity, cleaningAreaId, cleaningAreaCode, readinessStatus,
         availableQuantity, checkedAt]
      properties:
        requirementId: { $ref: "#/components/schemas/Uuid" }
        requirementCode: { type: string }
        requirementName: { type: string }
        unit: { type: string }
        requiredQuantity: { type: number }
        cleaningAreaId: { type: string, format: uuid, nullable: true }
        cleaningAreaCode: { type: string, nullable: true }
        readinessStatus: { type: string, nullable: true }
        availableQuantity: { type: number, nullable: true }
        checkedAt: { type: string, format: date-time, nullable: true }
    HousekeepingQualityAuditReportRow:
      type: object
      description: BE-11M quality-audit dataset row.
      required:
        [auditId, sourceType, sourceId, cleaningAreaId, cleaningAreaCode,
         auditorUserId, score, result, status, auditedAt]
      properties:
        auditId: { $ref: "#/components/schemas/Uuid" }
        sourceType: { type: string }
        sourceId: { type: string, format: uuid }
        cleaningAreaId: { type: string, format: uuid, nullable: true }
        cleaningAreaCode: { type: string, nullable: true }
        auditorUserId: { $ref: "#/components/schemas/Uuid" }
        score: { type: number, nullable: true }
        result: { type: string, nullable: true }
        status: { type: string }
        auditedAt: { type: string, format: date-time, nullable: true }
    HousekeepingComplaintReportRow:
      type: object
      description: BE-11M complaint-binding dataset row.
      required:
        [bindingId, complaintReference, workRequestId, cleaningAreaId,
         cleaningAreaCode, housekeepingSourceType, housekeepingSourceId,
         findingId, status, createdAt]
      properties:
        bindingId: { $ref: "#/components/schemas/Uuid" }
        complaintReference: { type: string }
        workRequestId: { type: string, format: uuid, nullable: true }
        cleaningAreaId: { type: string, format: uuid, nullable: true }
        cleaningAreaCode: { type: string, nullable: true }
        housekeepingSourceType: { type: string, nullable: true }
        housekeepingSourceId: { type: string, format: uuid, nullable: true }
        findingId: { type: string, format: uuid, nullable: true }
        status: { type: string }
        createdAt: { type: string, format: date-time }

    # ------------------------------------------------------------------
    # CR-BE-MOB-03 PART 03 — Engineering & corrective-action verification
    # schemas (BE-10I report datasets, BE-21J corrective-action
    # verification). Mirrors the implemented DTOs; ids are the authoritative
    # BE-10/BE-21/BE-07 rows.
    # ------------------------------------------------------------------
    EngineeringTechnicalSummary:
      type: object
      description: BE-10I technical summary projection for one Building.
      required:
        [buildingId, dateFrom, dateTo, operations, inspections,
         meterReadings, equipmentLogs, checklists, breakdowns, maintenance,
         findings]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        dateFrom: { type: string, nullable: true }
        dateTo: { type: string, nullable: true }
        operations:
          type: object
          required: [scheduled, inProgress, completed, openWorkOrders, openFindings]
          properties:
            scheduled: { type: integer, minimum: 0 }
            inProgress: { type: integer, minimum: 0 }
            completed: { type: integer, minimum: 0 }
            openWorkOrders: { type: integer, minimum: 0 }
            openFindings: { type: integer, minimum: 0 }
        inspections:
          type: object
          required: [bindingCount, executionCount]
          properties:
            bindingCount: { type: integer, minimum: 0 }
            executionCount: { type: integer, minimum: 0 }
        meterReadings:
          type: object
          required: [bindingCount, readingCount]
          properties:
            bindingCount: { type: integer, minimum: 0 }
            readingCount: { type: integer, minimum: 0 }
        equipmentLogs:
          type: object
          required: [bindingCount, executionCount]
          properties:
            bindingCount: { type: integer, minimum: 0 }
            executionCount: { type: integer, minimum: 0 }
        checklists:
          type: object
          required: [bindingCount, executionCount]
          properties:
            bindingCount: { type: integer, minimum: 0 }
            executionCount: { type: integer, minimum: 0 }
        breakdowns:
          type: object
          required: [open, closed]
          properties:
            open: { type: integer, minimum: 0 }
            closed: { type: integer, minimum: 0 }
        maintenance:
          type: object
          required: [activeBindings, linkedWorkOrders]
          properties:
            activeBindings: { type: integer, minimum: 0 }
            linkedWorkOrders: { type: integer, minimum: 0 }
        findings:
          type: object
          required: [open, verified, closed]
          properties:
            open: { type: integer, minimum: 0 }
            verified: { type: integer, minimum: 0 }
            closed: { type: integer, minimum: 0 }
    EngineeringInspectionReportRow:
      type: object
      description: BE-10I equipment-inspection binding dataset row.
      required:
        [bindingId, assetId, assetCode, assetName, templateId, templateCode,
         functionalLocationCode, status, executionCount, lastExecutionAt]
      properties:
        bindingId: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        assetCode: { type: string }
        assetName: { type: string }
        templateId: { $ref: "#/components/schemas/Uuid" }
        templateCode: { type: string }
        functionalLocationCode: { type: string, nullable: true }
        status: { type: string }
        executionCount: { type: integer, minimum: 0 }
        lastExecutionAt: { type: string, format: date-time, nullable: true }
    EngineeringMeterReadingReportRow:
      type: object
      description: BE-10I meter-reading binding dataset row (BE-07 UOM + range).
      required:
        [bindingId, assetId, assetCode, assetName, fieldCode, uomId, uomCode,
         uomSymbol, minimumValue, maximumValue, status, readingCount,
         lastReadAt]
      properties:
        bindingId: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        assetCode: { type: string }
        assetName: { type: string }
        fieldCode: { type: string, nullable: true }
        uomId: { $ref: "#/components/schemas/Uuid" }
        uomCode: { type: string }
        uomSymbol: { type: string }
        minimumValue: { type: number, nullable: true }
        maximumValue: { type: number, nullable: true }
        status: { type: string }
        readingCount: { type: integer, minimum: 0 }
        lastReadAt: { type: string, format: date-time, nullable: true }
    EngineeringEquipmentLogReportRow:
      type: object
      description: BE-10I equipment log-sheet binding dataset row.
      required:
        [bindingId, assetId, assetCode, assetName, templateId, templateCode,
         status, executionCount, lastExecutionAt]
      properties:
        bindingId: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        assetCode: { type: string }
        assetName: { type: string }
        templateId: { $ref: "#/components/schemas/Uuid" }
        templateCode: { type: string }
        status: { type: string }
        executionCount: { type: integer, minimum: 0 }
        lastExecutionAt: { type: string, format: date-time, nullable: true }
    EngineeringChecklistReportRow:
      type: object
      description: BE-10I engineering-checklist binding dataset row.
      required:
        [bindingId, templateId, templateCode, assetId, assetCode,
         functionalLocationCode, bindingStatus, executionCount,
         lastExecutionAt]
      properties:
        bindingId: { $ref: "#/components/schemas/Uuid" }
        templateId: { $ref: "#/components/schemas/Uuid" }
        templateCode: { type: string }
        assetId: { type: string, format: uuid, nullable: true }
        assetCode: { type: string, nullable: true }
        functionalLocationCode: { type: string, nullable: true }
        bindingStatus: { type: string }
        executionCount: { type: integer, minimum: 0 }
        lastExecutionAt: { type: string, format: date-time, nullable: true }
    EngineeringBreakdownReportRow:
      type: object
      description: BE-10I breakdown dataset row with linked corrective Work Order context.
      required:
        [breakdownId, assetId, assetCode, assetName, category, description,
         reportedByUserId, reportedAt, status, workOrderId, workOrderNumber,
         workOrderStatus]
      properties:
        breakdownId: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        assetCode: { type: string }
        assetName: { type: string }
        category: { type: string }
        description: { type: string }
        reportedByUserId: { $ref: "#/components/schemas/Uuid" }
        reportedAt: { type: string, format: date-time }
        status: { type: string }
        workOrderId: { type: string, format: uuid, nullable: true }
        workOrderNumber: { type: string, nullable: true }
        workOrderStatus: { type: string, nullable: true }
    EngineeringMaintenanceReportRow:
      type: object
      description: BE-10I planned-maintenance binding dataset row.
      required:
        [bindingId, assetId, assetCode, assetName, name, maintenanceType,
         status, scheduleCode, scheduleStatus, workOrderNumber,
         workOrderStatus, taskCount]
      properties:
        bindingId: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        assetCode: { type: string }
        assetName: { type: string }
        name: { type: string }
        maintenanceType: { type: string }
        status: { type: string }
        scheduleCode: { type: string, nullable: true }
        scheduleStatus: { type: string, nullable: true }
        workOrderNumber: { type: string, nullable: true }
        workOrderStatus: { type: string, nullable: true }
        taskCount: { type: integer, minimum: 0 }
    EngineeringFindingReportRow:
      type: object
      description: BE-10I engineering finding dataset row (BE-09 finding via the BE-10H link).
      required:
        [linkId, findingId, findingNumber, title, status, operationType,
         sourceType, sourceId, assetId, assetCode, classificationName,
         severityName, reportedAt]
      properties:
        linkId: { $ref: "#/components/schemas/Uuid", description: BE-10H link id. }
        findingId: { $ref: "#/components/schemas/Uuid", description: BE-09 finding id. }
        findingNumber: { type: string }
        title: { type: string }
        status: { type: string }
        operationType: { type: string }
        sourceType: { type: string, nullable: true }
        sourceId: { type: string, format: uuid, nullable: true }
        assetId: { type: string, format: uuid, nullable: true }
        assetCode: { type: string, nullable: true }
        classificationName: { type: string, nullable: true }
        severityName: { type: string, nullable: true }
        reportedAt: { type: string, format: date-time }
    CorrectiveActionVerificationStatus:
      type: string
      enum: [PENDING, COMPLETED]
    CorrectiveActionVerification:
      type: object
      description: >-
        BE-21J — a shared BE-07 `reviews` row projected for the
        CORRECTIVE_ACTION target type. `decision` reuses the shared
        ReviewDecision vocabulary; the lifecycle outcome (VERIFIED /
        IN_PROGRESS) is derived by the backend, never asserted by the caller.
      required:
        [id, clientId, correctiveActionId, reviewerUserId, decision, notes,
         reviewedAt, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        correctiveActionId: { $ref: "#/components/schemas/Uuid" }
        reviewerUserId: { $ref: "#/components/schemas/Uuid", description: Always the authenticated reviewer. }
        decision: { $ref: "#/components/schemas/ReviewDecision" }
        notes: { type: string, nullable: true }
        reviewedAt: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/CorrectiveActionVerificationStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CorrectiveActionStatus:
      type: string
      description: BE-21G corrective action lifecycle status.
      enum:
        [PROPOSED, APPROVED, IN_PROGRESS, COMPLETED, VERIFIED, REJECTED, CANCELLED]
    CorrectiveActionVerificationContext:
      type: object
      description: >-
        BE-21J backend-authoritative verification context: everything a
        caller needs to decide whether a verification can be opened or
        submitted, computed by the backend. Only COMPLETED actions are
        verifiable.
      required:
        [correctiveActionId, incidentId, clientId, buildingId,
         correctiveActionStatus, verifiable, blockers, currentVerification,
         latestVerification, finalized]
      properties:
        correctiveActionId: { $ref: "#/components/schemas/Uuid" }
        incidentId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        correctiveActionStatus: { $ref: "#/components/schemas/CorrectiveActionStatus" }
        verifiable: { type: boolean }
        blockers:
          type: array
          items: { type: string }
          description: Why the action cannot be verified, when verifiable is false.
        currentVerification:
          type: object
          nullable: true
          allOf:
            - $ref: "#/components/schemas/CorrectiveActionVerification"
          description: The open PENDING verification, if one exists.
        latestVerification:
          type: object
          nullable: true
          allOf:
            - $ref: "#/components/schemas/CorrectiveActionVerification"
          description: The most recent COMPLETED verification — the authoritative result.
        finalized: { type: boolean, description: True once an APPROVED decision has been recorded. }
    OpenCorrectiveActionVerificationRequest:
      type: object
      description: Opening a verification takes an optional note and nothing else.
      properties:
        notes: { type: string, maxLength: 4000, nullable: true }
    SubmitCorrectiveActionVerificationRequest:
      type: object
      description: >-
        The decision payload. `decision` is validated against the shared
        review vocabulary; derived fields (status, reviewer, timestamps) are
        refused.
      required: [decision]
      properties:
        decision: { $ref: "#/components/schemas/ReviewDecision" }
        notes: { type: string, maxLength: 4000, nullable: true }
    SlaDefinition:
      type: object
      description: SLA configuration only; it is not an applied SLA or clock.
      required: [id, clientId, buildingId, code, name, operationalType, workType, priority, responseTargetMinutes, resolutionTargetMinutes, status, effectiveFrom, effectiveTo, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        code: { type: string }
        name: { type: string }
        operationalType: { type: string, enum: [WORK_ORDER] }
        workType: { type: string, nullable: true }
        priority: { type: string, enum: [LOW, MEDIUM, HIGH, CRITICAL], nullable: true }
        responseTargetMinutes: { type: integer, minimum: 1, nullable: true }
        resolutionTargetMinutes: { type: integer, minimum: 1, nullable: true }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateSlaDefinitionRequest:
      type: object
      required: [code, name, operationalType, effectiveFrom]
      anyOf: [{ required: [responseTargetMinutes] }, { required: [resolutionTargetMinutes] }]
      properties:
        code: { type: string }
        name: { type: string }
        operationalType: { type: string, enum: [WORK_ORDER] }
        buildingId: { type: string, format: uuid }
        workType: { type: string }
        priority: { type: string, enum: [LOW, MEDIUM, HIGH, CRITICAL] }
        responseTargetMinutes: { type: integer, minimum: 1 }
        resolutionTargetMinutes: { type: integer, minimum: 1 }
        status: { type: string, enum: [ACTIVE, INACTIVE], default: ACTIVE }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time }
    UpdateSlaDefinitionRequest:
      type: object
      properties:
        name: { type: string }
        workType: { type: string }
        priority: { type: string, enum: [LOW, MEDIUM, HIGH, CRITICAL] }
        responseTargetMinutes: { type: integer, minimum: 1 }
        resolutionTargetMinutes: { type: integer, minimum: 1 }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time }
    SlaEscalationClockType:
      type: string
      enum: [RESPONSE, RESOLUTION, ANY]
      description: Which SLA clock a policy applies to; ANY matches either clock.
    SlaEscalationDerivedTarget:
      type: object
      description: >
        Configuration vocabulary for a recipient target that can only be derived from the
        breached Work Order. Stored and validated here; expansion and resolution are out of
        scope for this contract.
      required: [kind]
      properties:
        kind:
          type: string
          enum: [WORK_ORDER_ASSIGNEE, WORK_ORDER_ASSIGNEE_SUPERVISOR, WORK_ORDER_CREATOR, BUILDING_ROLE, BUILDING_PERMISSION]
        roleCode: { type: string, description: Required when kind is BUILDING_ROLE. }
        permissionCode: { type: string, description: Required when kind is BUILDING_PERMISSION. }
    SlaEscalationRecipientRule:
      type: object
      description: >
        Stored recipient configuration. `specs` reuses the existing recipient rule
        vocabulary; `derived` carries Work-Order-derived targets. At least one static spec
        or one derived target is required. No recipient is resolved by this API.
      properties:
        specs:
          type: array
          items: { type: object }
        derived:
          type: array
          items: { $ref: "#/components/schemas/SlaEscalationDerivedTarget" }
        scope:
          type: object
          properties:
            clientId: { $ref: "#/components/schemas/Uuid" }
            buildingIds: { type: array, items: { $ref: "#/components/schemas/Uuid" } }
    SlaEscalationPolicy:
      type: object
      description: SLA escalation configuration only; it schedules nothing and sends nothing.
      required: [id, clientId, buildingId, code, name, description, operationalType, clockType, workType, priority, status, effectiveFrom, effectiveTo, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { type: string, format: uuid, nullable: true }
        code: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        operationalType: { type: string, enum: [WORK_ORDER] }
        clockType: { $ref: "#/components/schemas/SlaEscalationClockType" }
        workType: { type: string, nullable: true }
        priority: { type: string, enum: [LOW, MEDIUM, HIGH, CRITICAL], nullable: true }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateSlaEscalationPolicyRequest:
      type: object
      required: [code, name, operationalType, clockType, effectiveFrom]
      properties:
        code: { type: string }
        name: { type: string }
        description: { type: string }
        buildingId: { type: string, format: uuid }
        operationalType: { type: string, enum: [WORK_ORDER] }
        clockType: { $ref: "#/components/schemas/SlaEscalationClockType" }
        workType: { type: string }
        priority: { type: string, enum: [LOW, MEDIUM, HIGH, CRITICAL] }
        status: { type: string, enum: [ACTIVE, INACTIVE], default: ACTIVE }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time }
    UpdateSlaEscalationPolicyRequest:
      type: object
      properties:
        name: { type: string }
        description: { type: string, nullable: true }
        clockType: { $ref: "#/components/schemas/SlaEscalationClockType" }
        workType: { type: string, nullable: true }
        priority: { type: string, enum: [LOW, MEDIUM, HIGH, CRITICAL], nullable: true }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time }
    SlaEscalationLevel:
      type: object
      description: An ordered escalation step of a policy, expressed as minutes after the breach.
      required: [id, policyId, level, offsetMinutes, templateKey, recipientRule, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        policyId: { $ref: "#/components/schemas/Uuid" }
        level: { type: integer, minimum: 1 }
        offsetMinutes: { type: integer, minimum: 0 }
        templateKey: { type: string }
        recipientRule: { $ref: "#/components/schemas/SlaEscalationRecipientRule" }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateSlaEscalationLevelRequest:
      type: object
      required: [level, templateKey, recipientRule]
      properties:
        level: { type: integer, minimum: 1 }
        offsetMinutes: { type: integer, minimum: 0, default: 0 }
        templateKey: { type: string }
        recipientRule: { $ref: "#/components/schemas/SlaEscalationRecipientRule" }
        status: { type: string, enum: [ACTIVE, INACTIVE], default: ACTIVE }
    UpdateSlaEscalationLevelRequest:
      type: object
      properties:
        level: { type: integer, minimum: 1 }
        offsetMinutes: { type: integer, minimum: 0 }
        templateKey: { type: string }
        recipientRule: { $ref: "#/components/schemas/SlaEscalationRecipientRule" }
        status: { type: string, enum: [ACTIVE, INACTIVE] }
    SlaEscalationAction:
      type: object
      description: >
        One materialized escalation level for one breached SLA clock, with its policy/level
        provenance snapshotted at materialization time. Read-only. Recipient identities are
        deliberately absent: the snapshotted recipient rule is never published, and only the
        aggregate resolved-recipient and created-notification counts are returned. Escalation
        delivery is in-app only, so no provider, transport, or delivery-receipt data exists here.
      required:
        [
          id,
          workOrderId,
          appliedSlaId,
          slaClockId,
          clientId,
          buildingId,
          clockType,
          policyId,
          escalationLevelId,
          level,
          templateKey,
          breachedAt,
          dueAt,
          status,
          triggeredAt,
          cancelledAt,
          cancelReason,
          recipientsResolved,
          notificationsCreated,
          failureReason,
          createdAt,
          updatedAt,
        ]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        appliedSlaId: { $ref: "#/components/schemas/Uuid" }
        slaClockId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        clockType: { $ref: "#/components/schemas/SlaEscalationClockType" }
        policyId: { $ref: "#/components/schemas/Uuid" }
        escalationLevelId: { $ref: "#/components/schemas/Uuid" }
        level: { type: integer, minimum: 1 }
        templateKey: { type: string, description: Snapshotted; later policy edits do not rewrite it. }
        breachedAt: { type: string, format: date-time, description: The persisted breach instant this action descends from. }
        dueAt: { type: string, format: date-time, description: Absolute fire time computed once as breachedAt plus the level offset. }
        status: { type: string, enum: [PENDING, TRIGGERED, CANCELLED, SKIPPED] }
        triggeredAt: { type: string, format: date-time, nullable: true }
        cancelledAt: { type: string, format: date-time, nullable: true }
        cancelReason: { type: string, enum: [CLOCK_SATISFIED, CLOCK_TERMINATED], nullable: true }
        recipientsResolved: { type: integer, minimum: 0, description: Count only; recipient identities are never exposed. }
        notificationsCreated: { type: integer, minimum: 0 }
        failureReason: { type: string, nullable: true, maxLength: 500, description: Operator diagnostic; contains no recipient identities. }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    SlaClock:
      type: object
      description: Neutral PART 02 clock state; no pause, deadline, or breach fields exist.
      required: [id, appliedSlaId, clockType, targetMinutes, startedAt, status, satisfiedAt, terminatedAt, breachedAt, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        appliedSlaId: { $ref: "#/components/schemas/Uuid" }
        clockType: { type: string, enum: [RESPONSE, RESOLUTION] }
        targetMinutes: { type: integer, minimum: 1 }
        startedAt: { type: string, format: date-time }
        status: { type: string, enum: [RUNNING, SATISFIED, TERMINATED] }
        satisfiedAt: { type: string, format: date-time, nullable: true }
        terminatedAt: { type: string, format: date-time, nullable: true }
        breachedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        isPaused: { type: boolean }
        totalPausedMilliseconds: { type: integer, minimum: 0 }
        effectiveElapsedMilliseconds: { type: integer, minimum: 0 }
        pauseIntervals:
          type: array
          items: { $ref: "#/components/schemas/SlaClockPauseInterval" }
    SlaClockPauseInterval:
      type: object
      description: Auditable interval created only by Work Order ON_HOLD and closed by RESUMED or terminal lifecycle.
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        slaClockId: { $ref: "#/components/schemas/Uuid" }
        pausedAt: { type: string, format: date-time }
        resumedAt: { type: string, format: date-time, nullable: true }
        pauseSource: { type: string, enum: [WORK_ORDER_ON_HOLD] }
        pauseActorUserId: { type: string, format: uuid, nullable: true }
        resumeActorUserId: { type: string, format: uuid, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    AppliedSla:
      type: object
      description: Immutable definition snapshot applied prospectively to one Work Order.
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        slaDefinitionId: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        definitionCode: { type: string }
        operationalType: { type: string, enum: [WORK_ORDER] }
        definitionWorkType: { type: string, nullable: true }
        definitionPriority: { type: string, nullable: true }
        workOrderWorkType: { type: string }
        workOrderPriority: { type: string }
        responseTargetMinutes: { type: integer, nullable: true }
        resolutionTargetMinutes: { type: integer, nullable: true }
        definitionEffectiveFrom: { type: string, format: date-time }
        definitionEffectiveTo: { type: string, format: date-time, nullable: true }
        appliedAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }
        clocks: { type: array, items: { $ref: "#/components/schemas/SlaClock" } }

    # -------------------------------------------------------------------
    # CR-BE-INTEG-01 PART 06 — Integration Webhook schemas
    # -------------------------------------------------------------------
    IntegrationWebhookEndpoint:
      type: object
      description: >
        Client-scoped webhook endpoint configuration — the SECRET-FREE
        projection every read/list/update response uses. The signing secret
        is structurally absent from this schema and is disclosed only
        through `IntegrationWebhookEndpointWithSecret` on create/rotate.
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
          description: Optional Building narrowing (null = whole Client).
        name: { type: string }
        url: { type: string, description: HTTPS receiver URL. }
        eventTypes:
          type: array
          items: { type: string }
          description: Subscribed BE-07 event-type codes (uppercase).
        status: { type: string, enum: [ACTIVE, INACTIVE] }
        secretRotatedAt:
          type: string
          format: date-time
          nullable: true
          description: Last rotate-secret time (null = never rotated).
        timeoutMs: { type: integer, minimum: 1000, maximum: 30000 }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    IntegrationWebhookEndpointWithSecret:
      allOf:
        - $ref: "#/components/schemas/IntegrationWebhookEndpoint"
        - type: object
          required: [signingSecret]
          properties:
            signingSecret:
              type: string
              description: >
                ONE-TIME disclosure of the server-generated HMAC signing
                secret (`whsec_` + 32 random bytes, base64url). Returned
                ONLY by create (201) and rotate-secret (200); it can never
                be read back through any other operation. Store it securely
                on the receiver side.
    IntegrationWebhookDelivery:
      type: object
      description: >
        One per-endpoint delivery ledger row (read-only history). Exposes
        delivery state, attempt accounting, timing, the last HTTP response
        status and a sanitized error — never signing secrets, never payload
        bodies. `id` is the receiver-facing idempotency key sent as
        `X-Asentra-Delivery-Id` (stable across retry attempts).
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        outboxEventId: { $ref: "#/components/schemas/Uuid" }
        endpointId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId:
          allOf:
            - $ref: "#/components/schemas/Uuid"
          nullable: true
        eventType: { type: string, description: Source BE-07 event type. }
        status:
          type: string
          enum: [PENDING, SENDING, DELIVERED, RETRY_SCHEDULED, FAILED_PERMANENT, EXHAUSTED]
          description: >
            Lifecycle: PENDING → SENDING → DELIVERED (receiver 2xx only) /
            RETRY_SCHEDULED (retryable failure, durable retry window) /
            FAILED_PERMANENT (non-retryable) / EXHAUSTED (attempt budget
            spent). DELIVERED, FAILED_PERMANENT and EXHAUSTED are terminal.
        attemptCount: { type: integer, minimum: 0 }
        maxAttempts: { type: integer, minimum: 1 }
        nextRetryAt:
          type: string
          format: date-time
          nullable: true
          description: Due time of the next retry (RETRY_SCHEDULED only).
        lastAttemptAt: { type: string, format: date-time, nullable: true }
        lastResponseStatus:
          type: integer
          nullable: true
          description: >
            HTTP status of the latest attempt (null for network errors and
            timeouts, where no response was received).
        lastError:
          type: string
          nullable: true
          description: Sanitized failure description (never response bodies).
        deliveredAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    # ------------------------------------------------------------------
    # CR-BE-PRO-02 PART 01–06 — RFQ procurement contract
    # ------------------------------------------------------------------
    RfqSourceMode:
      type: string
      enum: [MATERIAL, SERVICE]
    RfqStatus:
      type: string
      enum: [DRAFT, OPEN, CLOSED, CANCELLED]
    RfqCurrency:
      type: string
      enum: [IDR, USD, SGD, MYR, AUD, EUR, GBP, JPY, CNY]
    Rfq:
      type: object
      description: >
        Client/Building-scoped sourcing record. RFQ, quotation, comparison,
        recommendation, award, and DRAFT PO values do not create a financial
        commitment.
      required: [id, clientId, buildingId, purchaseRequestId, sourceMode, rfqNumber, title, currency, status, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        purchaseRequestId: { $ref: "#/components/schemas/Uuid" }
        sourceMode: { $ref: "#/components/schemas/RfqSourceMode" }
        rfqNumber: { type: string }
        title: { type: string }
        description: { type: string, nullable: true }
        currency: { $ref: "#/components/schemas/RfqCurrency" }
        requiredDate: { type: string, format: date-time, nullable: true }
        responseDeadline: { type: string, format: date-time, nullable: true }
        sourceRequestNumber: { type: string }
        sourceRequestTitle: { type: string }
        sourceRequestStatus: { type: string }
        status: { $ref: "#/components/schemas/RfqStatus" }
        openedAt: { type: string, format: date-time, nullable: true }
        openedByUserId: { $ref: "#/components/schemas/Uuid" }
        closedAt: { type: string, format: date-time, nullable: true }
        closedByUserId: { $ref: "#/components/schemas/Uuid" }
        cancelledAt: { type: string, format: date-time, nullable: true }
        cancelledByUserId: { $ref: "#/components/schemas/Uuid" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateRfqRequest:
      type: object
      required: [purchaseRequestId, sourceMode, rfqNumber, title, currency, idempotencyKey]
      properties:
        purchaseRequestId: { $ref: "#/components/schemas/Uuid" }
        sourceMode: { $ref: "#/components/schemas/RfqSourceMode" }
        rfqNumber: { type: string, maxLength: 64 }
        title: { type: string, maxLength: 200 }
        description: { type: string, nullable: true }
        currency: { $ref: "#/components/schemas/RfqCurrency" }
        requiredDate: { type: string, format: date-time, nullable: true }
        responseDeadline: { type: string, format: date-time, nullable: true }
        idempotencyKey: { type: string, maxLength: 200 }
    UpdateRfqRequest:
      type: object
      minProperties: 1
      properties:
        title: { type: string }
        description: { type: string, nullable: true }
        currency: { $ref: "#/components/schemas/RfqCurrency" }
        requiredDate: { type: string, format: date-time, nullable: true }
        responseDeadline: { type: string, format: date-time, nullable: true }
    RfqAvailableActions:
      type: object
      required: [rfqId, state, availableActions]
      properties:
        rfqId: { $ref: "#/components/schemas/Uuid" }
        state: { $ref: "#/components/schemas/RfqStatus" }
        availableActions:
          type: array
          items: { type: string, enum: [OPEN, CLOSE, CANCEL] }
    RfqLine:
      type: object
      required: [id, rfqId, sourceMode, clientId, buildingId, purchaseRequestId, lineNumber, sourceDescription, sourceServiceId, quantitySnapshot, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        rfqId: { $ref: "#/components/schemas/Uuid" }
        sourceMode: { $ref: "#/components/schemas/RfqSourceMode" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        purchaseRequestId: { $ref: "#/components/schemas/Uuid" }
        lineNumber: { type: integer, minimum: 1 }
        materialRequestId: { $ref: "#/components/schemas/Uuid", nullable: true }
        serviceRequestId: { $ref: "#/components/schemas/Uuid", nullable: true }
        sourceDescription: { type: string }
        sourceItemId: { $ref: "#/components/schemas/Uuid", nullable: true }
        sourceUomId: { $ref: "#/components/schemas/Uuid", nullable: true }
        sourceServiceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            CR-BE-SVC-01 PART 04 — governed SERVICE identity snapshot derived
            from the Service Request (NULL for MATERIAL and un-governed lines;
            never introduces item/UOM/quantity).
        quantitySnapshot: { type: number, nullable: true }
        sourceRequiredDate: { type: string, format: date-time, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateRfqLineRequest:
      type: object
      required: [sourceLineType, sourceLineId]
      properties:
        sourceLineType: { type: string, enum: [MATERIAL_REQUEST, SERVICE_REQUEST] }
        sourceLineId: { $ref: "#/components/schemas/Uuid" }

    RfqInvitationStatus:
      type: string
      enum: [INVITED, ACCEPTED, DECLINED, NO_BID, EXPIRED, REVOKED, QUOTATION_SUBMITTED]
    RfqInvitation:
      type: object
      required: [id, rfqId, vendorId, clientId, buildingId, attemptNumber, status, responseDeadline, tokenExpiresAt, contactSourceType, contactSourceId, contactNameSnapshot, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        rfqId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        attemptNumber: { type: integer, minimum: 1 }
        status: { $ref: "#/components/schemas/RfqInvitationStatus" }
        responseDeadline: { type: string, format: date-time }
        tokenExpiresAt: { type: string, format: date-time }
        tokenConsumedAt: { type: string, format: date-time, nullable: true }
        contactSourceType: { type: string, enum: [VENDOR] }
        contactSourceId: { $ref: "#/components/schemas/Uuid" }
        contactNameSnapshot: { type: string }
        recipientEmailSnapshot: { type: string, format: email, nullable: true }
        viewedAt: { type: string, format: date-time, nullable: true }
        acceptedAt: { type: string, format: date-time, nullable: true }
        declinedAt: { type: string, format: date-time, nullable: true }
        noBidAt: { type: string, format: date-time, nullable: true }
        expiredAt: { type: string, format: date-time, nullable: true }
        revokedAt: { type: string, format: date-time, nullable: true }
        revokedByUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        responseReason: { type: string, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    RfqInvitationIssued:
      allOf:
        - $ref: "#/components/schemas/RfqInvitation"
        - type: object
          properties:
            invitationToken:
              type: string
              nullable: true
              description: One-time disclosure only; never returned on replay or reads.
    CreateRfqInvitationRequest:
      type: object
      required: [vendorId, idempotencyKey]
      properties:
        vendorId: { $ref: "#/components/schemas/Uuid" }
        idempotencyKey: { type: string, maxLength: 200 }
    IdempotencyRequest:
      type: object
      required: [idempotencyKey]
      properties:
        idempotencyKey: { type: string, maxLength: 200 }
    RfqTokenExchangeRequest:
      type: object
      required: [token]
      properties:
        token: { type: string, description: One-time raw invitation token, accepted only in the POST body. }
    RfqResponseReasonRequest:
      type: object
      properties:
        reason: { type: string, nullable: true, maxLength: 2000 }
        responseReason: { type: string, nullable: true, maxLength: 2000 }
    RfqSession:
      type: object
      required: [sessionId, invitationId, rfqId, vendorId]
      properties:
        sessionId: { $ref: "#/components/schemas/Uuid" }
        invitationId: { $ref: "#/components/schemas/Uuid" }
        rfqId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        status: { type: string, enum: [ACTIVE, REVOKED, EXPIRED] }
        sessionToken: { type: string, nullable: true, description: Returned only at exchange. }
    RfqSessionExchange:
      type: object
      required: [sessionToken, session, access]
      properties:
        sessionToken: { type: string }
        session: { $ref: "#/components/schemas/RfqSession" }
        access:
          type: object
          required: [invitationId, rfqId, vendorId]
          properties:
            invitationId: { $ref: "#/components/schemas/Uuid" }
            rfqId: { $ref: "#/components/schemas/Uuid" }
            vendorId: { $ref: "#/components/schemas/Uuid" }
    VendorSafeRfqLine:
      type: object
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        lineNumber: { type: integer }
        sourceMode: { $ref: "#/components/schemas/RfqSourceMode" }
        description: { type: string }
        quantity: { type: number, nullable: true, description: Null for SERVICE lines. }
        requiredDate: { type: string, format: date-time, nullable: true }
    VendorSafeInvitation:
      type: object
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        status: { $ref: "#/components/schemas/RfqInvitationStatus" }
        responseDeadline: { type: string, format: date-time }
        contactName: { type: string }
    VendorSafeRfq:
      type: object
      description: Vendor invitation-scoped RFQ projection; no peers, comparison, award, PO, or commitment data.
      properties:
        rfq:
          type: object
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            rfqNumber: { type: string }
            title: { type: string }
            description: { type: string, nullable: true }
            sourceMode: { $ref: "#/components/schemas/RfqSourceMode" }
            currency: { $ref: "#/components/schemas/RfqCurrency" }
            requiredDate: { type: string, format: date-time, nullable: true }
            responseDeadline: { type: string, format: date-time, nullable: true }
            status: { $ref: "#/components/schemas/RfqStatus" }
            lines: { type: array, items: { $ref: "#/components/schemas/VendorSafeRfqLine" } }
        invitation: { $ref: "#/components/schemas/VendorSafeInvitation" }

    VendorQuotationStatus:
      type: string
      enum: [DRAFT, SUBMITTED, WITHDRAWN]
    VendorQuotationRevisionStatus:
      type: string
      enum: [DRAFT, SUBMITTED, SUPERSEDED]
    TechnicalCompliance:
      type: string
      enum: [COMPLIANT, NON_COMPLIANT, NOT_STATED]
    VendorQuotationLine:
      type: object
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        quotationRevisionId: { $ref: "#/components/schemas/Uuid" }
        quotationId: { $ref: "#/components/schemas/Uuid" }
        rfqId: { $ref: "#/components/schemas/Uuid" }
        rfqLineId: { $ref: "#/components/schemas/Uuid" }
        sourceMode: { $ref: "#/components/schemas/RfqSourceMode" }
        lineNumberSnapshot: { type: integer }
        description: { type: string, nullable: true }
        requiredQuantitySnapshot: { type: number, nullable: true }
        requiredUomId: { $ref: "#/components/schemas/Uuid", nullable: true }
        sourceServiceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            CR-BE-SVC-01 PART 04 — governed SERVICE identity propagated from
            the RFQ line (NULL for MATERIAL and un-governed SERVICE lines; the
            Vendor cannot set it).
        quotedQuantity: { type: number, nullable: true, description: Null for SERVICE lines. }
        unitPrice: { type: number, minimum: 0, format: double }
        lineTotal: { type: number, minimum: 0, format: double }
        technicalCompliance: { $ref: "#/components/schemas/TechnicalCompliance" }
        deviationNotes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    QuotationAttachment:
      type: object
      properties:
        supportingDocumentId: { $ref: "#/components/schemas/Uuid" }
        documentId: { $ref: "#/components/schemas/Uuid" }
        parentType: { type: string, enum: [QUOTATION_REVISION] }
        parentId: { $ref: "#/components/schemas/Uuid" }
        documentNumber: { type: string }
        documentType: { type: string }
        title: { type: string }
        description: { type: string, nullable: true }
        fileReference: { type: string, nullable: true, description: Opaque storage reference, never binary content. }
        status: { type: string }
    VendorQuotationRevision:
      type: object
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        quotationId: { $ref: "#/components/schemas/Uuid" }
        rfqId: { $ref: "#/components/schemas/Uuid" }
        invitationId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        revisionNumber: { type: integer, minimum: 1 }
        status: { $ref: "#/components/schemas/VendorQuotationRevisionStatus" }
        currency: { $ref: "#/components/schemas/RfqCurrency" }
        validUntil: { type: string, format: date, nullable: true }
        leadTimeDays: { type: integer, minimum: 0, nullable: true }
        deliveryTerms: { type: string, nullable: true }
        serviceTerms: { type: string, nullable: true }
        notes: { type: string, nullable: true }
        submittedAt: { type: string, format: date-time, nullable: true }
        supersededAt: { type: string, format: date-time, nullable: true }
        totalAmount: { type: number, minimum: 0 }
        lines: { type: array, items: { $ref: "#/components/schemas/VendorQuotationLine" } }
        attachments: { type: array, items: { $ref: "#/components/schemas/QuotationAttachment" } }
    VendorQuotation:
      type: object
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        rfqId: { $ref: "#/components/schemas/Uuid" }
        invitationId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        quotationNumber: { type: string }
        status: { $ref: "#/components/schemas/VendorQuotationStatus" }
        currentRevision: { allOf: [{ $ref: "#/components/schemas/VendorQuotationRevision" }], nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateVendorQuotationRequest:
      allOf:
        - $ref: "#/components/schemas/CreateVendorQuotationRevisionRequest"
        - type: object
          properties: { quotationNumber: { type: string, maxLength: 128 } }
    CreateVendorQuotationRevisionRequest:
      type: object
      required: [currency, idempotencyKey, lines]
      properties:
        currency: { $ref: "#/components/schemas/RfqCurrency" }
        validUntil: { type: string, format: date, nullable: true }
        leadTimeDays: { type: integer, minimum: 0, nullable: true }
        deliveryTerms: { type: string, nullable: true }
        serviceTerms: { type: string, nullable: true }
        notes: { type: string, nullable: true }
        idempotencyKey: { type: string, maxLength: 200 }
        lines: { type: array, items: { $ref: "#/components/schemas/CreateVendorQuotationLineRequest" } }
    UpdateVendorQuotationRevisionRequest:
      type: object
      properties:
        currency: { $ref: "#/components/schemas/RfqCurrency" }
        validUntil: { type: string, format: date, nullable: true }
        leadTimeDays: { type: integer, minimum: 0, nullable: true }
        deliveryTerms: { type: string, nullable: true }
        serviceTerms: { type: string, nullable: true }
        notes: { type: string, nullable: true }
    CreateVendorQuotationLineRequest:
      type: object
      required: [rfqLineId, unitPrice]
      properties:
        rfqLineId: { $ref: "#/components/schemas/Uuid" }
        quotedQuantity: { type: number, minimum: 0, nullable: true }
        unitPrice: { type: number, minimum: 0 }
        description: { type: string, nullable: true }
        technicalCompliance: { $ref: "#/components/schemas/TechnicalCompliance" }
        deviationNotes: { type: string, nullable: true }
    UpdateVendorQuotationLineRequest:
      type: object
      properties:
        quotedQuantity: { type: number, minimum: 0, nullable: true }
        unitPrice: { type: number, minimum: 0 }
        description: { type: string, nullable: true }
        technicalCompliance: { $ref: "#/components/schemas/TechnicalCompliance" }
        deviationNotes: { type: string, nullable: true }
    CreateQuotationAttachmentRequest:
      type: object
      required: [documentNumber, documentType, title, fileReference]
      properties:
        documentNumber: { type: string, maxLength: 128 }
        documentType: { type: string, maxLength: 128 }
        title: { type: string, maxLength: 500 }
        description: { type: string, nullable: true }
        fileReference: { type: string, maxLength: 512, description: Opaque storage reference only. }

    PriceCatalogCurrency:
      type: string
      description: >
        Exact-match currency vocabulary; no FX conversion exists anywhere in
        the price-authority domain.
      enum: [IDR, USD, SGD, MYR, AUD, EUR, GBP, JPY, CNY]

    PriceCatalogEntryStatus:
      type: string
      description: >
        API lifecycle: DRAFT (mutable, never effective) → ACTIVE (selected by
        window) → INACTIVE (terminal; closed by replacement, correction or
        deactivation). Facts on ACTIVE/INACTIVE rows are never updated.
      enum: [DRAFT, ACTIVE, INACTIVE]

    PriceCatalogEntryKind:
      type: string
      description: >
        Derived state: `VENDOR_CONTRACT` exactly when a Vendor tier key is
        set, otherwise `REFERENCE`. Never caller-editable.
      enum: [REFERENCE, VENDOR_CONTRACT]

    PriceCatalogScopeTier:
      type: string
      description: >
        Provenance tier of a matched authority. Selection precedence is fixed:
        VENDOR_BUILDING → VENDOR → BUILDING → CLIENT_WIDE.
      enum: [VENDOR_BUILDING, VENDOR, BUILDING, CLIENT_WIDE]

    PriceCatalogSourceMode:
      type: string
      description: >
        The price subject discriminator (CR-BE-SVC-01 PART 05). MATERIAL
        prices an Inventory Item per 1 UOM; SERVICE prices a governed
        service_catalog entry per 1 service (no item, no UOM, no quantity).
        The subject shape is enforced by a discriminated-union DB CHECK.
      enum: [MATERIAL, SERVICE]

    ServiceCatalogStatus:
      type: string
      description: >
        CR-BE-SVC-01 PART 01 — reference-master lifecycle. INACTIVE is
        terminal (no reactivation lane in v1); the row is retained for
        history and references stay valid.
      enum: [ACTIVE, INACTIVE]

    ServiceCatalogEntry:
      type: object
      description: >
        CR-BE-SVC-01 PART 01 — governed Service master identity: a
        Client-scoped reference master (the SERVICE analog of
        `inventory_items`). `code` is unique per Client and immutable; it is
        byte-compatible with the legacy free-text `service_type` grammar so
        existing codes are valid catalog codes. Identity only — no
        demand/price/quantity/UOM surface.
      required:
        [id, clientId, code, name, description, category, status,
         createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        code:
          type: string
          description: >
            Stable machine-readable identity, unique per Client, immutable.
            Uppercase `/^[A-Z][A-Z0-9_-]*$/`, 2–64 chars.
        name: { type: string }
        description: { type: string, nullable: true }
        category:
          type: string
          description: Free classification (e.g. ENGINEERING, HOUSEKEEPING).
        status: { $ref: "#/components/schemas/ServiceCatalogStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateServiceCatalogEntryRequest:
      type: object
      description: >
        CR-BE-SVC-01 PART 01. `code` is normalized to uppercase and is
        immutable once created. Created ACTIVE.
      required: [clientId, code, name, category]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        code:
          type: string
          description: Uppercase `/^[A-Z][A-Z0-9_-]*$/`, 2–64 chars.
        name: { type: string }
        category: { type: string }
        description: { type: string, nullable: true, maxLength: 1000 }

    UpdateServiceCatalogEntryRequest:
      type: object
      description: >
        CR-BE-SVC-01 PART 01. `code` and `clientId` are immutable; only
        `name`, `description`, `category` are editable. `status` is not
        editable here — deactivation is the governed lifecycle transition.
      properties:
        name: { type: string }
        description: { type: string, nullable: true, maxLength: 1000 }
        category: { type: string }

    PriceCatalogLookupResolution:
      type: string
      description: >
        Total, typed outcome of the §8 resolver. Non-MATCHED outcomes are
        explicit absence classifications — never fabricated prices. SERVICE
        never emits UOM_INCOMPATIBLE (no UOM).
      enum:
        [MATCHED, NO_REFERENCE_PRICE, UOM_INCOMPATIBLE, CURRENCY_INCOMPATIBLE,
         AMBIGUOUS]

    PriceCatalogEntry:
      type: object
      description: >
        One governed reference price authority: either a MATERIAL Inventory
        Item priced per 1 unit of an explicit UOM, or a SERVICE governed
        service_catalog entry priced per 1 service (no item/UOM/quantity —
        CR-BE-SVC-01 PART 05), inside one scope tier (nullable keys at most
        one of Building and/or Vendor), with a half-open effective window
        `[effectiveFrom, effectiveTo)`. Change is replace/correct — never
        in-place mutation; `replacedByEntryId` links the closing lineage.
      required:
        [id, clientId, buildingId, vendorId, sourceMode, entryKind, itemId,
         uomId, serviceId, currency, unitPrice, effectiveFrom, effectiveTo,
         status, activatedAt, activatedByUserId, deactivatedAt,
         deactivatedByUserId, replacedByEntryId, sourceType, sourceReference,
         notes, approvedByUserId, approvedAt, createdByUserId, createdAt,
         updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Null marks a non-Building tier.
        vendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Null marks a general (non-Vendor) tier.
        sourceMode: { $ref: "#/components/schemas/PriceCatalogSourceMode" }
        entryKind: { $ref: "#/components/schemas/PriceCatalogEntryKind" }
        itemId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: MATERIAL subject (NULL for SERVICE).
        uomId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: MATERIAL UOM (NULL for SERVICE).
        serviceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            SERVICE subject — a governed service_catalog entry (NULL for
            MATERIAL).
        currency: { $ref: "#/components/schemas/PriceCatalogCurrency" }
        unitPrice:
          type: number
          exclusiveMinimum: 0
          description: >
            MATERIAL: price per 1 UOM unit. SERVICE: price per 1 service
            (the service amount; at most two decimals).
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/PriceCatalogEntryStatus" }
        activatedAt: { type: string, format: date-time, nullable: true }
        activatedByUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        deactivatedAt: { type: string, format: date-time, nullable: true }
        deactivatedByUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        replacedByEntryId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Successor link when closed by replace/correct.
        sourceType:
          type: string
          enum: [MANUAL]
          description: v1 provenance is MANUAL only; no backfill/adoption.
        sourceReference: { type: string, nullable: true }
        notes: { type: string, nullable: true }
        approvedByUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        approvedAt: { type: string, format: date-time, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreatePriceCatalogEntryRequest:
      type: object
      description: >
        DRAFT creation facts. `entryKind` is never supplied (derived);
        approved-by attribution is optional human metadata, not an engine.
        `sourceMode` defaults to MATERIAL when omitted. MATERIAL requires
        `itemId` + `uomId`; SERVICE requires `serviceId` (no item/UOM) —
        CR-BE-SVC-01 PART 05.
      required: [clientId, currency, unitPrice, effectiveFrom]
      properties:
        clientId: { $ref: "#/components/schemas/Uuid" }
        sourceMode: { $ref: "#/components/schemas/PriceCatalogSourceMode" }
        buildingId: { $ref: "#/components/schemas/Uuid", nullable: true }
        vendorId: { $ref: "#/components/schemas/Uuid", nullable: true }
        itemId: { $ref: "#/components/schemas/Uuid", nullable: true }
        uomId: { $ref: "#/components/schemas/Uuid", nullable: true }
        serviceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        currency: { $ref: "#/components/schemas/PriceCatalogCurrency" }
        unitPrice: { type: number, exclusiveMinimum: 0 }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time, nullable: true }
        sourceReference: { type: string, nullable: true, maxLength: 200 }
        notes: { type: string, nullable: true, maxLength: 1000 }
        approvedByUserId: { $ref: "#/components/schemas/Uuid", nullable: true }

    UpdatePriceCatalogEntryRequest:
      type: object
      description: >
        DRAFT-only field edits; at least one field required. Re-scoping a
        DRAFT re-derives `entryKind` from the final Vendor key.
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid", nullable: true }
        vendorId: { $ref: "#/components/schemas/Uuid", nullable: true }
        itemId: { $ref: "#/components/schemas/Uuid", nullable: true }
        uomId: { $ref: "#/components/schemas/Uuid", nullable: true }
        serviceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        currency: { $ref: "#/components/schemas/PriceCatalogCurrency" }
        unitPrice: { type: number, exclusiveMinimum: 0 }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time, nullable: true }
        sourceReference: { type: string, nullable: true, maxLength: 200 }
        notes: { type: string, nullable: true, maxLength: 1000 }
        approvedByUserId: { $ref: "#/components/schemas/Uuid", nullable: true }

    ReplacePriceCatalogEntryRequest:
      type: object
      description: >
        Successor facts for the managed-change lane. Subject, tier keys, UOM
        and currency are inherited from the predecessor — never supplied.
        `effectiveFrom` must not open before the predecessor's window start
        (that retroactive repair belongs to `/correct`).
      required: [unitPrice, effectiveFrom]
      properties:
        unitPrice: { type: number, exclusiveMinimum: 0 }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time, nullable: true }
        sourceReference: { type: string, nullable: true, maxLength: 200 }
        notes: { type: string, nullable: true, maxLength: 1000 }
        approvedByUserId: { $ref: "#/components/schemas/Uuid", nullable: true }

    CorrectPriceCatalogEntryRequest:
      type: object
      description: >
        Corrective successor facts for the override lane
        (`price_catalog.override`). The `reason` is MANDATORY governance
        evidence: required, non-blank, ≤ 1000 chars; it is recorded in the
        `PRICE_CATALOG_ENTRY_OVERRIDE_CORRECTED` event and joins the
        idempotency fingerprint, so replaying a key with a different reason
        conflicts instead of silently reusing earlier evidence. The successor
        window MAY open before the predecessor's start (retroactive
        correction); subject, scope, UOM and currency remain inherited.
      required: [unitPrice, effectiveFrom, reason]
      properties:
        unitPrice: { type: number, exclusiveMinimum: 0 }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time, nullable: true }
        reason:
          type: string
          minLength: 1
          maxLength: 1000
          description: Mandatory, non-blank governance reason.
        sourceReference: { type: string, nullable: true, maxLength: 200 }
        notes: { type: string, nullable: true, maxLength: 1000 }
        approvedByUserId: { $ref: "#/components/schemas/Uuid", nullable: true }

    PriceCatalogLookupResult:
      type: object
      description: >
        Typed §8 resolver outcome with the full request echo. `scopeTier` and
        `entry` are present only when `resolution` is `MATCHED`. For a SERVICE
        lookup, `itemId`/`uomId` are null and `serviceId` carries the subject
        (CR-BE-SVC-01 PART 05); SERVICE never resolves to UOM_INCOMPATIBLE.
      required:
        [resolution, clientId, buildingId, vendorId, sourceMode, itemId,
         uomId, serviceId, currency, asOf, scopeTier, entry]
      properties:
        resolution: { $ref: "#/components/schemas/PriceCatalogLookupResolution" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid", nullable: true }
        sourceMode: { $ref: "#/components/schemas/PriceCatalogSourceMode" }
        itemId: { $ref: "#/components/schemas/Uuid", nullable: true }
        uomId: { $ref: "#/components/schemas/Uuid", nullable: true }
        serviceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        currency: { $ref: "#/components/schemas/PriceCatalogCurrency" }
        asOf: { type: string, format: date-time }
        scopeTier:
          allOf: [{ $ref: "#/components/schemas/PriceCatalogScopeTier" }]
          nullable: true
        entry:
          allOf: [{ $ref: "#/components/schemas/PriceCatalogEntry" }]
          nullable: true

    PurchaseOrderPriceDeviationResolution:
      type: string
      description: >
        Advisory deviation outcome vocabulary (§8 outcomes + `NOT_REQUESTED`
        for SERVICE lines). Non-MATCHED outcomes carry null facts — explicit,
        never fabricated.
      enum:
        [MATCHED, NO_REFERENCE_PRICE, UOM_INCOMPATIBLE, CURRENCY_INCOMPATIBLE,
         AMBIGUOUS, NOT_REQUESTED]

    PriceDeviationPosition:
      type: string
      description: Coarse advisory comparison of the PO unit price vs reference.
      enum: [ABOVE, BELOW, EQUAL]

    PriceCatalogLinePriceReference:
      type: object
      description: >
        Advisory reference-resolution + variance facts for one PO line. All
        fact fields are null unless `resolution` is `MATCHED`. Computed at
        read time; never persisted; never an issuance gate.
      required:
        [resolution, priceEntryId, scopeTier, scopeVendor, scopeBuilding,
         unitPrice, currency, uomId, effectiveFrom, effectiveTo,
         referenceTotal, unitVariance, totalVariance, variancePercent,
         position]
      properties:
        resolution:
          $ref: "#/components/schemas/PurchaseOrderPriceDeviationResolution"
        priceEntryId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Winning price-authority row when MATCHED.
        scopeTier:
          allOf: [{ $ref: "#/components/schemas/PriceCatalogScopeTier" }]
          nullable: true
        scopeVendor: { type: boolean, nullable: true }
        scopeBuilding: { type: boolean, nullable: true }
        unitPrice:
          type: number
          nullable: true
          description: Reference unit price per 1 reference UOM.
        currency:
          allOf: [{ $ref: "#/components/schemas/PriceCatalogCurrency" }]
          nullable: true
        uomId: { $ref: "#/components/schemas/Uuid", nullable: true }
        effectiveFrom: { type: string, format: date-time, nullable: true }
        effectiveTo: { type: string, format: date-time, nullable: true }
        referenceTotal:
          type: number
          nullable: true
          description: Reference unit price × frozen PO line quantity.
        unitVariance:
          type: number
          nullable: true
          description: PO unit price − reference unit price (2dp).
        totalVariance:
          type: number
          nullable: true
          description: PO line amount − reference total (2dp).
        variancePercent:
          type: number
          nullable: true
          description: (unitVariance / reference unitPrice) × 100 (2dp).
        position:
          allOf: [{ $ref: "#/components/schemas/PriceDeviationPosition" }]
          nullable: true

    PurchaseOrderPriceDeviationLine:
      type: object
      description: >
        One PO line's committed commercial facts (echoed, immutable) plus its
        advisory reference deviation.
      required:
        [purchaseOrderLineId, lineNumber, requestLineType, itemId, uomId,
         quantitySnapshot, unitPrice, lineAmount, reference]
      properties:
        purchaseOrderLineId: { $ref: "#/components/schemas/Uuid" }
        lineNumber: { type: integer }
        requestLineType:
          $ref: "#/components/schemas/PurchaseOrderLineRequestType"
        itemId: { $ref: "#/components/schemas/Uuid", nullable: true }
        uomId: { $ref: "#/components/schemas/Uuid", nullable: true }
        quantitySnapshot:
          type: number
          nullable: true
          description: Frozen request-line quantity; never recomputed.
        unitPrice: { type: number }
        lineAmount: { type: number }
        reference: { $ref: "#/components/schemas/PriceCatalogLinePriceReference" }

    PurchaseOrderPriceDeviation:
      type: object
      description: >
        COMPUTED advisory projection (§13.3): PO line prices vs the current
        reference authority, resolved as-of `asOf` (request time). Warn-only —
        it never gates PO creation/approval/acknowledgement/issuance, never
        rewrites PO facts, never touches RFQ comparison snapshots and is not
        settlement behavior. Available only to holders of BOTH
        `purchase_order.read` and `price_catalog.read`.
      required:
        [purchaseOrderId, poNumber, purchaseOrderStatus, clientId, buildingId,
         vendorId, currency, asOf, advisoryOnly, lines]
      properties:
        purchaseOrderId: { $ref: "#/components/schemas/Uuid" }
        poNumber: { type: string }
        purchaseOrderStatus: { $ref: "#/components/schemas/PurchaseOrderStatus" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        currency: { $ref: "#/components/schemas/PurchaseOrderCurrency" }
        asOf: { type: string, format: date-time }
        advisoryOnly:
          type: boolean
          enum: [true]
          description: Constant posture marker — advisory, never a gate.
        lines:
          type: array
          items: { $ref: "#/components/schemas/PurchaseOrderPriceDeviationLine" }

    RfqComparisonLineReference:
      type: object
      description: >
        CR-BE-PRICE-01 PART 04 advisory reference-price snapshot frozen into a
        comparison line offer at run creation (`reference_*` columns).
        Advisory, non-financial: never a rejection reason, never a winner
        signal. All fact fields are null unless `resolution` is `MATCHED`;
        SERVICE lines freeze as `NOT_REQUESTED`. Immutable once written —
        later catalog replace/correct never recomputes or mutates it.
      properties:
        priceEntryId: { $ref: "#/components/schemas/Uuid", nullable: true }
        unitPrice: { type: number, nullable: true }
        currency: { type: string, nullable: true }
        uomId: { $ref: "#/components/schemas/Uuid", nullable: true }
        scopeVendor: { type: boolean, nullable: true }
        scopeBuilding: { type: boolean, nullable: true }
        effectiveFrom: { type: string, format: date-time, nullable: true }
        resolution:
          type: string
          nullable: true
          enum:
            [MATCHED, NO_REFERENCE_PRICE, UOM_INCOMPATIBLE,
             CURRENCY_INCOMPATIBLE, NOT_REQUESTED, null]
        referenceTotal: { type: number, nullable: true }
        unitVariance:
          type: number
          nullable: true
          description: Quotation unit price − reference unit price (2dp).
        totalVariance: { type: number, nullable: true }
        variancePercent: { type: number, nullable: true }
        position: { type: string, nullable: true, enum: [ABOVE, BELOW, EQUAL, null] }

    RfqComparison:
      type: object
      description: >
        Immutable factual comparison snapshot. Lowest price fields are
        comparison facts only and never constitute a recommendation, winner, or
        award.
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        rfqId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        sourceMode: { $ref: "#/components/schemas/RfqSourceMode" }
        currency: { $ref: "#/components/schemas/RfqCurrency" }
        rfqNumber: { type: string }
        status: { type: string, enum: [SNAPSHOT] }
        snapshotAt: { type: string, format: date-time }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        evidence: { type: array, items: { $ref: "#/components/schemas/RfqComparisonEvidence" } }
        lines: { type: array, items: { $ref: "#/components/schemas/RfqComparisonLine" } }
        commercial: { $ref: "#/components/schemas/RfqComparisonCommercial" }
        evaluations: { type: array, items: { $ref: "#/components/schemas/RfqComparisonEvaluation" } }
    RfqComparisonEvidence:
      type: object
      properties:
        evidenceId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        invitationId: { $ref: "#/components/schemas/Uuid" }
        quotationId: { $ref: "#/components/schemas/Uuid" }
        quotationRevisionId: { $ref: "#/components/schemas/Uuid" }
        quotationNumber: { type: string }
        revisionNumber: { type: integer }
        submittedAt: { type: string, format: date-time }
        currency: { $ref: "#/components/schemas/RfqCurrency" }
        validUntil: { type: string, format: date, nullable: true }
        leadTimeDays: { type: integer, nullable: true }
        deliveryTerms: { type: string, nullable: true }
        serviceTerms: { type: string, nullable: true }
        notes: { type: string, nullable: true }
        totalAmount: { type: number, minimum: 0 }
        lines: { type: array, items: { $ref: "#/components/schemas/RfqComparisonEvidenceLine" } }
        attachments: { type: array, items: { $ref: "#/components/schemas/ComparisonAttachmentReference" } }
    RfqComparisonEvidenceLine:
      type: object
      properties:
        quotationLineId: { $ref: "#/components/schemas/Uuid" }
        rfqLineId: { $ref: "#/components/schemas/Uuid" }
        lineNumber: { type: integer }
        sourceMode: { $ref: "#/components/schemas/RfqSourceMode" }
        description: { type: string }
        offeredDescription: { type: string, nullable: true }
        requiredQuantity: { type: number, nullable: true }
        requiredUomId: { $ref: "#/components/schemas/Uuid", nullable: true }
        quotedQuantity: { type: number, nullable: true }
        unitPrice: { type: number }
        lineTotal: { type: number }
        technicalCompliance: { $ref: "#/components/schemas/TechnicalCompliance" }
        deviationNotes: { type: string, nullable: true }
        lineStatus: { type: string, enum: [QUOTED, MISSING] }
    ComparisonAttachmentReference:
      type: object
      properties:
        supportingDocumentId: { $ref: "#/components/schemas/Uuid" }
        documentId: { $ref: "#/components/schemas/Uuid" }
    RfqComparisonLine:
      type: object
      properties:
        rfqLineId: { $ref: "#/components/schemas/Uuid" }
        lineNumber: { type: integer }
        sourceMode: { $ref: "#/components/schemas/RfqSourceMode" }
        description: { type: string }
        requiredQuantity: { type: number, nullable: true, description: Null is authoritative for SERVICE lines. }
        requiredUomId: { $ref: "#/components/schemas/Uuid", nullable: true }
        offers: { type: array, items: { $ref: "#/components/schemas/RfqComparisonLineOffer" } }
        lowestQuotedUnitPrice: { type: number, nullable: true }
        highestQuotedUnitPrice: { type: number, nullable: true }
        priceDelta: { type: number, nullable: true }
        percentageDelta: { type: number, nullable: true, description: Null when the lowest price is zero or undefined. }
    RfqComparisonLineOffer:
      type: object
      properties:
        evidenceId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        invitationId: { $ref: "#/components/schemas/Uuid" }
        quotationId: { $ref: "#/components/schemas/Uuid" }
        quotationRevisionId: { $ref: "#/components/schemas/Uuid" }
        revisionNumber: { type: integer }
        submittedAt: { type: string, format: date-time }
        quotedQuantity: { type: number, nullable: true }
        unitPrice: { type: number }
        lineTotal: { type: number }
        technicalCompliance: { $ref: "#/components/schemas/TechnicalCompliance" }
        deviationNotes: { type: string, nullable: true }
        reference:
          allOf: [{ $ref: "#/components/schemas/RfqComparisonLineReference" }]
          description: >
            CR-BE-PRICE-01 PART 04 advisory reference snapshot, present ONLY
            when the caller holds both `rfq.read` and `price_catalog.read`
            (conjunctive default) — callers without `price_catalog.read` never
            see this key at all, and RFQ Vendor sessions never receive it.
    RfqComparisonCommercial:
      type: object
      properties:
        lowestTotalQuotations: { type: array, items: { $ref: "#/components/schemas/RfqComparisonTotalFact" } }
        lowestTotalAmount: { type: number, nullable: true }
        highestTotalAmount: { type: number, nullable: true }
        totalPriceSpread: { type: number, nullable: true }
    RfqComparisonTotalFact:
      type: object
      properties:
        evidenceId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        totalAmount: { type: number }
    CreateComparisonRequest:
      type: object
      required: [idempotencyKey]
      properties: { idempotencyKey: { type: string, maxLength: 200 } }
    CreateComparisonEvaluationRequest:
      type: object
      required: [evidenceId]
      properties:
        evidenceId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        commercialObservation: { type: string, nullable: true }
        technicalObservation: { type: string, nullable: true }
        complianceObservation: { type: string, nullable: true }
        evaluatorNote: { type: string, nullable: true }
    UpdateComparisonEvaluationRequest:
      type: object
      properties:
        commercialObservation: { type: string, nullable: true }
        technicalObservation: { type: string, nullable: true }
        complianceObservation: { type: string, nullable: true }
        evaluatorNote: { type: string, nullable: true }
    RfqComparisonEvaluation:
      type: object
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        comparisonRunId: { $ref: "#/components/schemas/Uuid" }
        evidenceId: { $ref: "#/components/schemas/Uuid" }
        rfqId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        quotationRevisionId: { $ref: "#/components/schemas/Uuid" }
        commercialObservation: { type: string, nullable: true }
        technicalObservation: { type: string, nullable: true }
        complianceObservation: { type: string, nullable: true }
        evaluatorNote: { type: string, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        updatedByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    RfqRecommendationOutcome:
      type: string
      enum: [VENDOR, NO_AWARD]
    RfqRecommendationStatus:
      type: string
      enum: [PENDING_APPROVAL, APPROVED, REJECTED, AWARDED]
    RfqRecommendation:
      type: object
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        rfqId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        comparisonRunId: { $ref: "#/components/schemas/Uuid" }
        evidenceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        quotationId: { $ref: "#/components/schemas/Uuid", nullable: true }
        quotationRevisionId: { $ref: "#/components/schemas/Uuid", nullable: true }
        invitationId: { $ref: "#/components/schemas/Uuid", nullable: true }
        vendorId: { $ref: "#/components/schemas/Uuid", nullable: true }
        outcome: { $ref: "#/components/schemas/RfqRecommendationOutcome" }
        status: { $ref: "#/components/schemas/RfqRecommendationStatus" }
        reason: { type: string }
        notes: { type: string, nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateRfqRecommendationRequest:
      type: object
      required: [comparisonRunId, reason]
      properties:
        comparisonRunId: { $ref: "#/components/schemas/Uuid" }
        evidenceId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        quotationRevisionId: { $ref: "#/components/schemas/Uuid" }
        outcome: { $ref: "#/components/schemas/RfqRecommendationOutcome" }
        reason: { type: string, minLength: 1, maxLength: 4000 }
        notes: { type: string, nullable: true, maxLength: 4000 }
    RfqAward:
      type: object
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        rfqId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        recommendationId: { $ref: "#/components/schemas/Uuid" }
        approvalId: { $ref: "#/components/schemas/Uuid" }
        comparisonRunId: { $ref: "#/components/schemas/Uuid" }
        evidenceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        quotationId: { $ref: "#/components/schemas/Uuid", nullable: true }
        quotationRevisionId: { $ref: "#/components/schemas/Uuid", nullable: true }
        invitationId: { $ref: "#/components/schemas/Uuid", nullable: true }
        vendorId: { $ref: "#/components/schemas/Uuid", nullable: true }
        outcome: { $ref: "#/components/schemas/RfqRecommendationOutcome" }
        awardReason: { type: string, nullable: true }
        awardedByUserId: { $ref: "#/components/schemas/Uuid" }
        awardedAt: { type: string, format: date-time }
    CreateRfqAwardRequest:
      type: object
      properties:
        awardReason: { type: string, nullable: true, maxLength: 4000 }
    CreateRfqPoConversionRequest:
      type: object
      required: [poReadinessId, poNumber, poDate, idempotencyKey]
      properties:
        poReadinessId: { $ref: "#/components/schemas/Uuid" }
        poNumber: { type: string, maxLength: 128 }
        poDate: { type: string, format: date }
        idempotencyKey: { type: string, maxLength: 200 }
        notes: { type: string, nullable: true, maxLength: 2000 }
    RfqPoLineProvenance:
      type: object
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        conversionId: { $ref: "#/components/schemas/Uuid" }
        purchaseOrderId: { $ref: "#/components/schemas/Uuid" }
        purchaseOrderLineId: { $ref: "#/components/schemas/Uuid" }
        rfqId: { $ref: "#/components/schemas/Uuid" }
        rfqLineId: { $ref: "#/components/schemas/Uuid" }
        quotationLineId: { $ref: "#/components/schemas/Uuid" }
        quotationRevisionId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
    RfqPoConversion:
      type: object
      description: >
        Typed lineage only: RFQ → Award → Vendor → Quotation → Revision →
        existing PO → existing PO Lines. It stores no duplicate amount or
        commitment ledger.
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        awardId: { $ref: "#/components/schemas/Uuid" }
        recommendationId: { $ref: "#/components/schemas/Uuid" }
        rfqId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        comparisonRunId: { $ref: "#/components/schemas/Uuid" }
        evidenceId: { $ref: "#/components/schemas/Uuid" }
        quotationId: { $ref: "#/components/schemas/Uuid" }
        quotationRevisionId: { $ref: "#/components/schemas/Uuid" }
        invitationId: { $ref: "#/components/schemas/Uuid" }
        vendorId: { $ref: "#/components/schemas/Uuid" }
        poReadinessId: { $ref: "#/components/schemas/Uuid" }
        purchaseOrderId: { $ref: "#/components/schemas/Uuid" }
        convertedByUserId: { $ref: "#/components/schemas/Uuid" }
        convertedAt: { type: string, format: date-time }
        purchaseOrder: { $ref: "#/components/schemas/PurchaseOrder" }
        purchaseOrderLines: { type: array, items: { $ref: "#/components/schemas/PurchaseOrderLine" } }
        lineProvenance: { type: array, items: { $ref: "#/components/schemas/RfqPoLineProvenance" } }

# CR-BE-ESG-01 PART 06 — implemented ESG surfaces (no inferred endpoints)
    /esg/metric-definitions:
      get:
        operationId: listEsgMetricDefinitions
        security: [{ bearerAuth: [] }]
        description: Client-scoped ESG definitions. Requires esg.read.
        parameters: [{ name: status, in: query, schema: { type: string, enum: [ACTIVE, INACTIVE] } }]
        responses: { '200': { description: ESG metric definitions } }
      post:
        operationId: createEsgMetricDefinition
        security: [{ bearerAuth: [] }]
        description: Client-scoped definition lifecycle. Requires esg.manage.
        responses: { '201': { description: Created } }
    /esg/metric-definitions/{id}:
      get:
        operationId: getEsgMetricDefinition
        security: [{ bearerAuth: [] }]
        description: Requires esg.read.
        parameters: [{ name: id, in: path, required: true, schema: { type: string, format: uuid } }]
        responses: { '200': { description: Definition } }
      patch:
        operationId: updateEsgMetricDefinition
        security: [{ bearerAuth: [] }]
        description: Requires esg.manage.
        parameters: [{ name: id, in: path, required: true, schema: { type: string, format: uuid } }]
        responses: { '200': { description: Updated } }
    /esg/metric-definitions/{id}/deactivate:
      post:
        operationId: deactivateEsgMetricDefinition
        security: [{ bearerAuth: [] }]
        description: Requires esg.manage; historical records remain preserved.
        parameters: [{ name: id, in: path, required: true, schema: { type: string, format: uuid } }]
        responses: { '200': { description: Deactivated } }
    /esg/waste-records:
      get:
        operationId: listEsgWasteRecords
        security: [{ bearerAuth: [] }]
        description: Building-scoped waste records. Requires esg.read. Quantity is not converted between UOMs.
        responses: { '200': { description: Waste records } }
      post:
        operationId: createEsgWasteRecord
        security: [{ bearerAuth: [] }]
        description: Requires esg.manage.
        responses: { '201': { description: Created } }
    /esg/waste-records/{id}:
      get:
        operationId: getEsgWasteRecord
        security: [{ bearerAuth: [] }]
        description: Requires esg.read.
        parameters: [{ name: id, in: path, required: true, schema: { type: string, format: uuid } }]
        responses: { '200': { description: Waste record } }
    /esg/metric-values:
      get:
        operationId: listEsgMetricValues
        security: [{ bearerAuth: [] }]
        description: Building-scoped periodized values. Requires esg.read. ACTUAL, ESTIMATED, and MISSING remain visible.
        responses: { '200': { description: Metric values } }
      post:
        operationId: createEsgMetricValue
        security: [{ bearerAuth: [] }]
        description: Requires esg.manage.
        responses: { '201': { description: Created } }
    /esg/metric-values/{id}:
      get:
        operationId: getEsgMetricValue
        security: [{ bearerAuth: [] }]
        description: Requires esg.read.
        parameters: [{ name: id, in: path, required: true, schema: { type: string, format: uuid } }]
        responses: { '200': { description: Metric value } }
    /esg/kpi:
      get:
        operationId: getEsgKpi
        security: [{ bearerAuth: [] }]
        description: Building/date scoped read model with provenance. Requires esg.read.
        responses: { '200': { description: ESG KPI read model } }
    /management/esg-summary:
      get:
        operationId: getManagementEsgSummary
        security: [{ bearerAuth: [] }]
        description: Management ESG summary over accessible buildings intersected with requested buildings; includes period provenance. Requires esg.read.
        responses: { '200': { description: Management ESG summary } }


    # ------------------------------------------------------------------
    # CR-BE-FX-01 PART 02 — FX Rate Authority + Client FX Policy
    #
    # CANONICAL CONVENTION (frozen): 1 BASE = RATE x QUOTE.
    # No conversion schema exists yet: conversion is PART 03.
    # ------------------------------------------------------------------
    FxRateStatus:
      type: string
      description: >
        Frozen lifecycle. `PENDING_APPROVAL` -> `ACTIVE` | `REJECTED`;
        `ACTIVE` -> `SUPERSEDED` | `INACTIVE`. `REJECTED`, `SUPERSEDED` and
        `INACTIVE` are terminal.
      enum: [PENDING_APPROVAL, ACTIVE, REJECTED, SUPERSEDED, INACTIVE]
    FxRateType:
      type: string
      description: >
        FX-01 supports exactly one rate type. Selection is by effective date,
        not by rate type. A new type may only be added by a CR proving a
        consumer that effective-date selection cannot serve.
      enum: [REFERENCE]
    FxRateSource:
      type: string
      description: >
        The provider seam. FX-01 ships `MANUAL_TREASURY` only — no external
        provider ingestion exists. A future provider may PROPOSE a rate but may
        never activate one.
      enum: [MANUAL_TREASURY]
    FxRateEventType:
      type: string
      description: The append-only FX rate audit vocabulary.
      enum:
        - FX_RATE_CREATED
        - FX_RATE_APPROVED
        - FX_RATE_REJECTED
        - FX_RATE_SUPERSEDED
        - FX_RATE_DEACTIVATED
    FxRate:
      type: object
      description: >
        One FX rate. Convention: 1 BASE = RATE x QUOTE, so
        `baseCurrencyCode: USD`, `quoteCurrencyCode: IDR`, `rate: "16500"`
        means 1 USD = 16,500 IDR.
      required:
        - id
        - baseCurrencyCode
        - quoteCurrencyCode
        - rateType
        - rate
        - effectiveFrom
        - status
        - source
        - createdByUserId
        - createdAt
        - updatedAt
      properties:
        id: { type: string, format: uuid }
        baseCurrencyCode:
          type: string
          pattern: "^[A-Z]{3}$"
          description: One unit of this currency is the left-hand side of the convention.
        quoteCurrencyCode:
          type: string
          pattern: "^[A-Z]{3}$"
          description: The currency the rate is expressed in. Never equal to the base.
        rateType: { $ref: "#/components/schemas/FxRateType" }
        rate:
          type: string
          description: >
            QUOTE units per ONE unit of BASE, stored as NUMERIC(24,12) and
            returned as a string so a client never sees float drift. Always
            strictly positive.
          example: "16500.000000000000"
        effectiveFrom:
          type: string
          format: date-time
          description: Inclusive start of the effective window.
        effectiveTo:
          type: string
          format: date-time
          nullable: true
          description: Exclusive end of the window; null means still open.
        status: { $ref: "#/components/schemas/FxRateStatus" }
        source: { $ref: "#/components/schemas/FxRateSource" }
        sourceReference:
          type: string
          nullable: true
          description: Treasury rate-sheet or bulletin reference.
        ingestedAt:
          type: string
          format: date-time
          nullable: true
          description: Reserved for future automated ingestion; null for manual entry.
        supersedesRateId:
          type: string
          format: uuid
          nullable: true
          description: The rate this one corrects.
        supersededByRateId:
          type: string
          format: uuid
          nullable: true
          description: The rate that corrected this one.
        createdByUserId:
          type: string
          format: uuid
          description: The maker.
        approvedByUserId:
          type: string
          format: uuid
          nullable: true
          description: The checker. Never equal to `createdByUserId`.
        approvedAt: { type: string, format: date-time, nullable: true }
        rejectedByUserId: { type: string, format: uuid, nullable: true }
        rejectedAt: { type: string, format: date-time, nullable: true }
        rejectedReason: { type: string, nullable: true }
        supersededByUserId: { type: string, format: uuid, nullable: true }
        supersededAt: { type: string, format: date-time, nullable: true }
        deactivatedByUserId: { type: string, format: uuid, nullable: true }
        deactivatedAt: { type: string, format: date-time, nullable: true }
        deactivationReason: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateFxRateRequest:
      type: object
      required: [baseCurrencyCode, quoteCurrencyCode, rate, effectiveFrom, source]
      properties:
        baseCurrencyCode: { type: string, pattern: "^[A-Z]{3}$", example: USD }
        quoteCurrencyCode: { type: string, pattern: "^[A-Z]{3}$", example: IDR }
        rate:
          type: string
          description: >
            Positive plain decimal. QUOTE units per ONE unit of BASE. Exponent
            notation, separators and signed literals are rejected.
          example: "16500"
        effectiveFrom: { type: string, format: date-time }
        effectiveTo:
          type: string
          format: date-time
          nullable: true
          description: Must be strictly later than `effectiveFrom`; null = open.
        source: { $ref: "#/components/schemas/FxRateSource" }
        sourceReference:
          type: string
          maxLength: 200
          nullable: true
    SupersedeFxRateRequest:
      type: object
      description: >
        A correction. The currency pair is inherited from the incumbent and
        cannot be changed, so no currency field exists here.
      required: [effectiveFrom]
      properties:
        rate:
          type: string
          description: >
            Omit for a window-only correction, which reuses the incumbent's
            exact stored decimal.
          example: "16600"
        effectiveFrom: { type: string, format: date-time }
        effectiveTo: { type: string, format: date-time, nullable: true }
        sourceReference: { type: string, maxLength: 200, nullable: true }
    FxRateReasonRequest:
      type: object
      description: Optional free-text reason for a rejection or deactivation.
      properties:
        reason:
          type: string
          maxLength: 500
          nullable: true
          description: Blank or absent means no reason given.
    FxRateSupersessionResult:
      type: object
      required: [superseded, successor]
      properties:
        superseded:
          allOf:
            - $ref: "#/components/schemas/FxRate"
          description: The incumbent, now `SUPERSEDED`.
        successor:
          allOf:
            - $ref: "#/components/schemas/FxRate"
          description: The correction, now `ACTIVE`.
    FxRateEvent:
      type: object
      description: >
        One row of the append-only rate audit ledger. Contains no Client
        ownership: an FX rate is platform-global.
      required: [id, fxRateId, eventType, occurredAt, createdAt]
      properties:
        id: { type: string, format: uuid }
        fxRateId: { type: string, format: uuid }
        eventType: { $ref: "#/components/schemas/FxRateEventType" }
        actorUserId: { type: string, format: uuid, nullable: true }
        requestId: { type: string, nullable: true }
        metadata:
          type: object
          description: >
            Provenance copied at the time of the event: pair, rate, convention,
            window, source, status and the lineage/actor fields relevant to the
            transition.
          additionalProperties: true
        occurredAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }
    ClientFxPolicy:
      type: object
      description: >
        Client-scoped FX governance. FAIL CLOSED: `fxEnabled` and
        `inversePermitted` default to false, and an empty `permittedSources`
        permits nothing. No Building override exists.
      required:
        - clientId
        - fxEnabled
        - reportingCurrencyCode
        - permittedSources
        - inversePermitted
        - createdAt
        - updatedAt
      properties:
        clientId: { type: string, format: uuid }
        fxEnabled:
          type: boolean
          description: false means FX is unavailable for this Client.
        reportingCurrencyCode:
          type: string
          pattern: "^[A-Z]{3}$"
          description: Must equal the Client base currency in FX-01.
        permittedSources:
          type: array
          description: Empty array means no rate source may be used.
          items: { $ref: "#/components/schemas/FxRateSource" }
        inversePermitted:
          type: boolean
          description: >
            Whether the governed INVERSE path may be used. Defaults to false.
            The inverse is never stored as a rate and is always recorded as an
            inverse when PART 03 applies it.
        maxStalenessDays:
          type: integer
          nullable: true
          minimum: 1
          description: null means no staleness bound beyond the effective window.
        createdByUserId: { type: string, format: uuid }
        updatedByUserId: { type: string, format: uuid }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    SetClientFxPolicyRequest:
      type: object
      required: [reportingCurrencyCode, permittedSources]
      properties:
        fxEnabled:
          type: boolean
          default: false
          description: Omitted means false — FX fails closed.
        reportingCurrencyCode:
          type: string
          pattern: "^[A-Z]{3}$"
          description: >
            Must be stated explicitly and must equal the Client base currency.
            It is never inferred or injected.
        permittedSources:
          type: array
          items: { $ref: "#/components/schemas/FxRateSource" }
        inversePermitted:
          type: boolean
          default: false
        maxStalenessDays:
          type: integer
          nullable: true
          minimum: 1
    ClientFxPolicyReadModel:
      type: object
      required: [clientId, policy, fxAvailable]
      properties:
        clientId: { type: string, format: uuid }
        policy:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/ClientFxPolicy"
          description: null when the Client has no policy. null means FX is unavailable.
        fxAvailable:
          type: boolean
          description: >
            Resolved availability. true only when a policy exists, is enabled,
            and permits at least one rate source. Absence is never permission.
        baseCurrencyCode:
          type: string
          pattern: "^[A-Z]{3}$"
          nullable: true
          description: The Client base currency, for display only.

    # ------------------------------------------------------------------
    # CR-BE-FIN-RPT-01 — Currency-safe basic financial reporting schemas
    # ------------------------------------------------------------------
    BasicFinancialCurrencyBucket:
      type: object
      required: [count, amount]
      properties:
        count:
          type: integer
          minimum: 0
          description: Count of monetary rows for this currency bucket.
        amount:
          type: number
          description: Exact-currency total amount for this bucket.

    BasicFinancialPaymentBucket:
      type: object
      required:
        [paidAmount, unpaidAmount, overdueAmount, outstandingAmount,
         unpaidCount, partiallyPaidCount, paidCount, overdueCount]
      properties:
        paidAmount:
          type: number
          description: Sum of paid amounts for this exact currency.
        unpaidAmount:
          type: number
          description: Outstanding but not yet overdue for this currency.
        overdueAmount:
          type: number
        outstandingAmount:
          type: number
        unpaidCount:
          type: integer
          minimum: 0
        partiallyPaidCount:
          type: integer
          minimum: 0
        paidCount:
          type: integer
          minimum: 0
        overdueCount:
          type: integer
          minimum: 0

    BasicFinancialByCurrencyEntry:
      type: object
      required:
        [currencyCode, tenantCharges, utilityBills, invoices,
         paidAmount, unpaidAmount, overdueAmount, outstandingAmount,
         receiptsIssued, receiptsVoid, vendorServiceCosts, basicExpenses,
         unrepresentedVendorCosts, billedIncome, receivedIncome,
         operationalCost, netBilled, netReceived]
      properties:
        currencyCode:
          type: string
          pattern: "^[A-Z]{3}$"
          description: Exact currency_code, e.g., IDR, USD.
        tenantCharges:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        utilityBills:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        invoices:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        paidAmount:
          type: number
          description: Paid amount for this exact invoice currency.
        unpaidAmount:
          type: number
        overdueAmount:
          type: number
        outstandingAmount:
          type: number
        receiptsIssued:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        receiptsVoid:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        vendorServiceCosts:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        basicExpenses:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        unrepresentedVendorCosts:
          type: object
          required: [amount]
          properties:
            amount:
              type: number
              description: Vendor costs finalized but not represented as basic expense, for this currency.
        billedIncome:
          type: number
          description: Billed income for this currency (invoices finalized).
        receivedIncome:
          type: number
          description: Received income for this currency (receipts issued).
        operationalCost:
          type: number
          description: Operational cost for this currency (basicExpenses + unrepresentedVendorCosts, exact same-currency addition).
        netBilled:
          type: number
          description: netBilled(C) = billedIncome(C) - operationalCost(C), exact same-currency subtraction.
        netReceived:
          type: number
          description: netReceived(C) = receivedIncome(C) - operationalCost(C).

    BasicFinancialUnknownByTenant:
      type: object
      required: [tenantCompanyId, tenantChargeAmount, utilityBillAmount, invoiceAmount, paidAmount, outstandingAmount]
      properties:
        tenantCompanyId:
          $ref: "#/components/schemas/Uuid"
        tenantChargeAmount:
          type: number
        utilityBillAmount:
          type: number
        invoiceAmount:
          type: number
        paidAmount:
          type: number
        outstandingAmount:
          type: number

    BasicFinancialUnknown:
      type: object
      description: >
        Historical monetary data without a governed currency snapshot. It is
        not IDR, not Client base/default currency, not reporting currency,
        excluded from byCurrency arithmetic and net calculations, never
        inferred. Per-source traceable.
      required:
        [tenantCharges, utilityBills, invoices, paidAmount, outstandingAmount,
         receiptsIssued, receiptsVoid, vendorServiceCosts, basicExpenses,
         unrepresentedVendorCosts, byTenant]
      properties:
        tenantCharges:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        utilityBills:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        invoices:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        paidAmount:
          type: object
          required: [amount, count]
          properties:
            amount: { type: number }
            count: { type: integer, minimum: 0 }
        outstandingAmount:
          type: object
          required: [amount, count]
          properties:
            amount: { type: number }
            count: { type: integer, minimum: 0 }
        receiptsIssued:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        receiptsVoid:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        vendorServiceCosts:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        basicExpenses:
          $ref: "#/components/schemas/BasicFinancialCurrencyBucket"
        unrepresentedVendorCosts:
          type: object
          required: [amount]
          properties:
            amount: { type: number }
        byTenant:
          type: array
          items:
            $ref: "#/components/schemas/BasicFinancialUnknownByTenant"

    BasicFinancialMonetarySummary:
      type: object
      required: [byCurrency, unknown, distinctKnownCurrencies, hasUnknown, singleCurrencyCode]
      properties:
        byCurrency:
          type: object
          description: Exact-currency grouped authoritative totals.
          additionalProperties:
            $ref: "#/components/schemas/BasicFinancialByCurrencyEntry"
        unknown:
          $ref: "#/components/schemas/BasicFinancialUnknown"
        distinctKnownCurrencies:
          type: array
          description: Union of all known currencies appearing in any monetary source.
          items:
            type: string
            pattern: "^[A-Z]{3}$"
        hasUnknown:
          type: boolean
          description: True when any monetary source has historical NULL currency.
        singleCurrencyCode:
          type: string
          nullable: true
          pattern: "^[A-Z]{3}$"
          description: >
            Convenience indicator only, not base/default currency. Non-null only
            when exactly one known currency and zero UNKNOWN.

    BasicFinancialSummary:
      type: object
      description: >
        CR-BE-FIN-RPT-01 currency-safe basic financial summary. Authoritative
        totals are in monetarySummary.byCurrency per exact currency. Legacy
        scalar monetary fields are nullable per single-currency convenience
        rule: single known currency + zero UNKNOWN → populated from that
        single currency; multiple currencies OR any UNKNOWN → null and
        monetarySummary.byCurrency is authoritative. No FX conversion in base
        report; FX remains read-side optional additive only. Payment/outstanding
        amounts inherit invoice currency and are grouped by that currency.
        UNKNOWN is historical data without governed snapshot, not IDR, not
        base/default/reporting currency, excluded from byCurrency arithmetic
        and net.
      required:
        [buildingId, periodFrom, periodTo, tenantCompanyId, monetarySummary,
         tenantBilling, invoicePayment, receipts, vendorServiceCosts,
         basicExpenses, outstandingBalance, incomeVsOperationalCost]
      properties:
        buildingId:
          $ref: "#/components/schemas/Uuid"
        periodFrom:
          type: string
          nullable: true
          format: date
        periodTo:
          type: string
          nullable: true
          format: date
        tenantCompanyId:
          type: string
          nullable: true
          format: uuid
        monetarySummary:
          $ref: "#/components/schemas/BasicFinancialMonetarySummary"
        tenantBilling:
          type: object
          required: [tenantCharges, utilityBills, byTenant]
          properties:
            tenantCharges:
              type: object
              required: [count, amount, cancelledCount]
              properties:
                count: { type: integer, minimum: 0 }
                amount:
                  type: number
                  nullable: true
                  description: >
                    Nullable per single-currency convenience rule. Null when
                    multi-currency or UNKNOWN exists; otherwise single currency total.
                cancelledCount: { type: integer, minimum: 0 }
            utilityBills:
              type: object
              required: [count, amount, cancelledCount]
              properties:
                count: { type: integer, minimum: 0 }
                amount:
                  type: number
                  nullable: true
                  description: Nullable per single-currency convenience rule.
                cancelledCount: { type: integer, minimum: 0 }
            byTenant:
              type: array
              items:
                type: object
                required: [tenantCompanyId, tenantChargeAmount, utilityBillAmount, invoiceAmount, paidAmount, outstandingAmount]
                properties:
                  tenantCompanyId: { $ref: "#/components/schemas/Uuid" }
                  tenantChargeAmount:
                    type: number
                    nullable: true
                    description: Nullable when tenant has multi-currency or UNKNOWN.
                  utilityBillAmount:
                    type: number
                    nullable: true
                  invoiceAmount:
                    type: number
                    nullable: true
                  paidAmount:
                    type: number
                    nullable: true
                  outstandingAmount:
                    type: number
                    nullable: true
        invoicePayment:
          type: object
          required: [invoices, payments]
          properties:
            invoices:
              type: object
              required: [count, amount, draftCount, cancelledCount]
              properties:
                count: { type: integer, minimum: 0 }
                amount:
                  type: number
                  nullable: true
                  description: Nullable per single-currency convenience rule.
                draftCount: { type: integer, minimum: 0 }
                cancelledCount: { type: integer, minimum: 0 }
            payments:
              type: object
              required:
                [paidAmount, unpaidAmount, overdueAmount, outstandingAmount,
                 unpaidCount, partiallyPaidCount, paidCount, overdueCount]
              properties:
                paidAmount:
                  type: number
                  nullable: true
                  description: Nullable when multi-currency or UNKNOWN.
                unpaidAmount:
                  type: number
                  nullable: true
                overdueAmount:
                  type: number
                  nullable: true
                outstandingAmount:
                  type: number
                  nullable: true
                unpaidCount: { type: integer, minimum: 0 }
                partiallyPaidCount: { type: integer, minimum: 0 }
                paidCount: { type: integer, minimum: 0 }
                overdueCount: { type: integer, minimum: 0 }
        receipts:
          type: object
          required: [issued, void]
          properties:
            issued:
              type: object
              required: [count, amount]
              properties:
                count: { type: integer, minimum: 0 }
                amount:
                  type: number
                  nullable: true
                  description: Nullable per single-currency convenience rule.
            void:
              type: object
              required: [count, amount]
              properties:
                count: { type: integer, minimum: 0 }
                amount:
                  type: number
                  nullable: true
        vendorServiceCosts:
          type: object
          required: [finalized, draftCount, cancelledCount]
          properties:
            finalized:
              type: object
              required: [count, amount]
              properties:
                count: { type: integer, minimum: 0 }
                amount:
                  type: number
                  nullable: true
                  description: Nullable per single-currency convenience rule.
            draftCount: { type: integer, minimum: 0 }
            cancelledCount: { type: integer, minimum: 0 }
        basicExpenses:
          type: object
          required: [finalized, draftCount, cancelledCount]
          properties:
            finalized:
              type: object
              required: [count, amount]
              properties:
                count: { type: integer, minimum: 0 }
                amount:
                  type: number
                  nullable: true
                  description: Nullable per single-currency convenience rule.
            draftCount: { type: integer, minimum: 0 }
            cancelledCount: { type: integer, minimum: 0 }
        outstandingBalance:
          type: object
          required: [invoiceCount, amount]
          properties:
            invoiceCount: { type: integer, minimum: 0 }
            amount:
              type: number
              nullable: true
              description: Nullable when multi-currency or UNKNOWN.
        incomeVsOperationalCost:
          type: object
          required: [billedIncome, receivedIncome, operationalCost, netBilled, netReceived]
          properties:
            billedIncome:
              type: number
              nullable: true
              description: Nullable per single-currency convenience rule. Null when multi-currency or UNKNOWN.
            receivedIncome:
              type: number
              nullable: true
            operationalCost:
              type: number
              nullable: true
              description: Operational cost for single currency (basicExpenses + unrepresented), null when multi-currency or UNKNOWN.
            netBilled:
              type: number
              nullable: true
              description: netBilled = billedIncome - operationalCost per exact currency, null when multi-currency or UNKNOWN.
            netReceived:
              type: number
              nullable: true

    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 01A - J09 Visitor Core (BE-13A/C/D).
    VisitorStatus:
      type: string
      description: >
        BE-13A Visitor identity lifecycle. Identities are never deleted:
        they are deactivated (`INACTIVE`) or blocked (`BLOCKED`).
      enum: [ACTIVE, INACTIVE, BLOCKED]

    VisitorIdentityType:
      type: string
      description: >
        BE-13A identity document class. `NONE` declares the absence of a
        document - an `identityNumber` requires a concrete type instead.
      enum: [NATIONAL_ID, PASSPORT, DRIVER_LICENSE, EMPLOYEE_BADGE, OTHER, NONE]

    Visitor:
      type: object
      description: >
        BE-13A `PublicVisitor` - the single shared Client-scoped identity
        master of the whole BE-13 Front Desk domain. Invitation, Expected
        Visitor, Walk-In, Contractor and Delivery/Courier records all
        reference this identity; there is no visitor deletion.
      required: [id, clientId, fullName, identityType, identityNumber, phone,
                 email, organizationName, notes, status, createdByUserId,
                 createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        fullName: { type: string, maxLength: 256 }
        identityType: { $ref: "#/components/schemas/VisitorIdentityType" }
        identityNumber: { type: string, nullable: true, maxLength: 128 }
        phone: { type: string, nullable: true, maxLength: 32 }
        email: { type: string, nullable: true, maxLength: 254 }
        organizationName: { type: string, nullable: true, maxLength: 256 }
        notes: { type: string, nullable: true, maxLength: 4096 }
        status: { $ref: "#/components/schemas/VisitorStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateVisitorRequest:
      type: object
      description: >
        BE-13A create payload. `status` defaults to `ACTIVE` when omitted.
        `identityNumber` requires a concrete `identityType` (400
        `VISITOR_IDENTITY_NUMBER_REQUIRES_TYPE` otherwise). Duplicate
        identity document within the Client is rejected 409
        `VISITOR_IDENTITY_ALREADY_EXISTS`. `clientId` comes from the path.
      required: [fullName]
      properties:
        fullName: { type: string, maxLength: 256 }
        identityType: { $ref: "#/components/schemas/VisitorIdentityType" }
        identityNumber: { type: string, nullable: true, maxLength: 128 }
        phone: { type: string, nullable: true, maxLength: 32 }
        email: { type: string, nullable: true, maxLength: 254 }
        organizationName: { type: string, nullable: true, maxLength: 256 }
        notes: { type: string, nullable: true, maxLength: 4096 }
        status: { $ref: "#/components/schemas/VisitorStatus" }

    UpdateVisitorRequest:
      type: object
      description: >
        BE-13A edit payload. At least one updatable field is required;
        empty bodies are rejected 400. `clientId`/id are immutable.
      properties:
        fullName: { type: string, maxLength: 256 }
        identityType: { $ref: "#/components/schemas/VisitorIdentityType" }
        identityNumber: { type: string, nullable: true, maxLength: 128 }
        phone: { type: string, nullable: true, maxLength: 32 }
        email: { type: string, nullable: true, maxLength: 254 }
        organizationName: { type: string, nullable: true, maxLength: 256 }
        notes: { type: string, nullable: true, maxLength: 4096 }
        status: { $ref: "#/components/schemas/VisitorStatus" }

    ExpectedVisitorStatus:
      type: string
      description: BE-13C front-desk expectation lifecycle; CANCELLED is terminal.
      enum: [EXPECTED, CANCELLED]

    ExpectedVisitor:
      type: object
      description: >
        BE-13C `PublicExpectedVisitor` - the front-desk "who is expected"
        record. References the shared BE-13A visitor identity and,
        optionally, the PENDING BE-13B invitation it was planned from. No
        Walk-In / Check-In / Pass semantics.
      required: [id, clientId, buildingId, visitorId, visitorInvitationId,
                 hostUserId, hostWorkforceId, hostName, expectedArrivalAt,
                 expectedDepartureAt, purpose, notes, status,
                 createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        visitorId: { $ref: "#/components/schemas/Uuid" }
        visitorInvitationId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostWorkforceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostName: { type: string, nullable: true, maxLength: 256 }
        expectedArrivalAt: { type: string, format: date-time }
        expectedDepartureAt: { type: string, format: date-time, nullable: true }
        purpose: { type: string, maxLength: 512 }
        notes: { type: string, nullable: true, maxLength: 4096 }
        status: { $ref: "#/components/schemas/ExpectedVisitorStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateExpectedVisitorRequest:
      type: object
      description: >
        BE-13C create payload. Standalone creation requires `buildingId`,
        `visitorId`, `expectedArrivalAt`, `purpose` and a resolvable Host
        (400 `EXPECTED_VISITOR_CONTEXT_REQUIRED` /
        `EXPECTED_VISITOR_HOST_REQUIRED` otherwise); when
        `visitorInvitationId` is supplied those fields default from the
        PENDING invitation and explicitly supplied values must stay
        consistent with it (400 mismatch / 400 cancelled invitation / 409
        `EXPECTED_VISITOR_INVITATION_ALREADY_USED`).
        `expectedDepartureAt`, when supplied, must be after
        `expectedArrivalAt`. A BLOCKED or INACTIVE visitor identity is
        rejected 400.
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        visitorId: { $ref: "#/components/schemas/Uuid" }
        visitorInvitationId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostWorkforceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostName: { type: string, nullable: true, maxLength: 256 }
        expectedArrivalAt: { type: string, format: date-time }
        expectedDepartureAt: { type: string, format: date-time, nullable: true }
        purpose: { type: string, maxLength: 512 }
        notes: { type: string, nullable: true, maxLength: 4096 }

    UpdateExpectedVisitorRequest:
      type: object
      description: >
        BE-13C edit payload. At least one updatable field is required.
        A CANCELLED record can no longer be modified (409
        `EXPECTED_VISITOR_ALREADY_CANCELLED`). Building/Visitor/invitation
        linkage are immutable.
      properties:
        hostUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostWorkforceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostName: { type: string, nullable: true, maxLength: 256 }
        expectedArrivalAt: { type: string, format: date-time }
        expectedDepartureAt: { type: string, format: date-time, nullable: true }
        purpose: { type: string, maxLength: 512 }
        notes: { type: string, nullable: true, maxLength: 4096 }

    WalkInVisitStatus:
      type: string
      description: BE-13D guest-book lifecycle; CANCELLED is terminal.
      enum: [REGISTERED, CANCELLED]

    WalkInNewVisitorInput:
      type: object
      description: >
        BE-13D inline identity registration payload, delegated verbatim to
        the BE-13A visitor service (same validation, duplicate handling and
        Client scoping). Exactly one of `visitorId` / `newVisitor` must be
        supplied on create.
      required: [fullName]
      properties:
        fullName: { type: string, maxLength: 256 }
        identityType: { $ref: "#/components/schemas/VisitorIdentityType" }
        identityNumber: { type: string, nullable: true, maxLength: 128 }
        phone: { type: string, nullable: true, maxLength: 32 }
        email: { type: string, nullable: true, maxLength: 254 }
        organizationName: { type: string, nullable: true, maxLength: 256 }
        notes: { type: string, nullable: true, maxLength: 4096 }

    WalkInVisit:
      type: object
      description: >
        BE-13D `PublicWalkInVisit` - the front-desk guest-book entry for a
        visitor arriving without a prior invitation, referencing the shared
        BE-13A identity. Host context is optional.
      required: [id, clientId, buildingId, visitorId, hostUserId,
                 hostWorkforceId, hostName, purpose, arrivedAt,
                 frontDeskNotes, status, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        visitorId: { $ref: "#/components/schemas/Uuid" }
        hostUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostWorkforceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostName: { type: string, nullable: true, maxLength: 256 }
        purpose: { type: string, maxLength: 512 }
        arrivedAt: { type: string, format: date-time }
        frontDeskNotes: { type: string, nullable: true, maxLength: 4096 }
        status: { $ref: "#/components/schemas/WalkInVisitStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateWalkInVisitRequest:
      type: object
      description: >
        BE-13D create payload. Exactly ONE of `visitorId` (reuse an
        existing matched identity) or `newVisitor` (register a new identity
        via BE-13A) is required (400
        `WALK_IN_VISIT_VISITOR_REFERENCE_REQUIRED` otherwise). `arrivedAt`
        defaults to the current time and must not be in the future (400
        `WALK_IN_VISIT_ARRIVAL_IN_FUTURE`). A BLOCKED or INACTIVE visitor
        identity is rejected 400; one open (REGISTERED) entry per
        (building, visitor) - 409 `WALK_IN_VISIT_ALREADY_REGISTERED`.
      required: [buildingId, purpose]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        visitorId: { $ref: "#/components/schemas/Uuid" }
        newVisitor: { $ref: "#/components/schemas/WalkInNewVisitorInput" }
        hostUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostWorkforceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostName: { type: string, nullable: true, maxLength: 256 }
        purpose: { type: string, maxLength: 512 }
        arrivedAt: { type: string, format: date-time }
        frontDeskNotes: { type: string, nullable: true, maxLength: 4096 }

    UpdateWalkInVisitRequest:
      type: object
      description: >
        BE-13D edit payload. At least one updatable field is required. A
        CANCELLED entry can no longer be modified (409
        `WALK_IN_VISIT_ALREADY_CANCELLED`). Building/Visitor linkage are
        immutable.
      properties:
        hostUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostWorkforceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostName: { type: string, nullable: true, maxLength: 256 }
        purpose: { type: string, maxLength: 512 }
        arrivedAt: { type: string, format: date-time }
        frontDeskNotes: { type: string, nullable: true, maxLength: 4096 }

    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 01B1 - J09 Visit Check-In (BE-13G/H).
    VisitCheckInStatus:
      type: string
      description: >
        BE-13G/H controlled visit lifecycle. CHECKED_IN -> CHECKED_OUT
        (visit closure) or -> CANCELLED (mistake reversal); both terminals
        are final. At most one active check-in per visit.
      enum: [CHECKED_IN, CHECKED_OUT, CANCELLED]

    VisitCheckIn:
      type: object
      description: >
        BE-13G/H `PublicVisitCheckIn` - the backend-authoritative check-in
        over an existing BE-13 visit context (Expected Visitor or Walk-In).
        The visitor identity travels through the visit reference, never
        duplicated. Check-Out preserves the original check-in history on
        the same row (`checkedIn*` untouched).
      required: [id, clientId, buildingId, visitorId, expectedVisitorId,
                 walkInVisitId, checkedInAt, checkedInByUserId, entryNotes,
                 checkedOutAt, checkedOutByUserId, exitNotes, status,
                 createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        visitorId: { $ref: "#/components/schemas/Uuid" }
        expectedVisitorId: { $ref: "#/components/schemas/Uuid", nullable: true }
        walkInVisitId: { $ref: "#/components/schemas/Uuid", nullable: true }
        checkedInAt: { type: string, format: date-time }
        checkedInByUserId: { $ref: "#/components/schemas/Uuid" }
        entryNotes: { type: string, nullable: true, maxLength: 4096 }
        checkedOutAt: { type: string, format: date-time, nullable: true }
        checkedOutByUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        exitNotes: { type: string, nullable: true, maxLength: 4096 }
        status: { $ref: "#/components/schemas/VisitCheckInStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateVisitCheckInRequest:
      type: object
      description: >
        BE-13G create payload. Exactly ONE of `expectedVisitorId` /
        `walkInVisitId` is required (400
        `VISIT_CHECK_IN_VISIT_REFERENCE_REQUIRED` otherwise); Client,
        Building and visitor identity derive from the visit. Host
        confirmation gate and one-active-check-in-per-visit apply (see
        operation description). `checkedInAt` defaults to now and must not
        be in the future (400 `VISIT_CHECK_IN_TIME_IN_FUTURE`).
      properties:
        expectedVisitorId: { $ref: "#/components/schemas/Uuid" }
        walkInVisitId: { $ref: "#/components/schemas/Uuid" }
        checkedInAt: { type: string, format: date-time }
        entryNotes: { type: string, nullable: true, maxLength: 4096 }

    CheckOutVisitRequest:
      type: object
      description: >
        BE-13H check-out payload over an active check-in row; the body is
        optional. `checkedOutAt` defaults to now, must not be in the future
        (400 `VISIT_CHECK_OUT_TIME_IN_FUTURE`) and must not precede the
        original check-in (400 `VISIT_CHECK_OUT_BEFORE_CHECK_IN`).
      properties:
        checkedOutAt: { type: string, format: date-time }
        exitNotes: { type: string, nullable: true, maxLength: 4096 }

    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 01B2 - J09 Visitor Pass (BE-13I).
    VisitorPassStatus:
      type: string
      description: BE-13I administrative pass lifecycle; both terminals are final.
      enum: [ACTIVE, RETURNED, CANCELLED]

    VisitorPass:
      type: object
      description: >
        BE-13I `PublicVisitorPass` - an administrative visitor credential
        bound to one BE-13G/H visit lifecycle (`visitCheckInId`). Physical
        access-control hardware is outside this boundary.
      required: [id, clientId, buildingId, visitCheckInId, passCode,
                 issuedAt, issuedByUserId, status, returnedAt,
                 returnedByUserId, cancelledAt, cancelledByUserId,
                 createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        visitCheckInId: { $ref: "#/components/schemas/Uuid" }
        passCode: { type: string, minLength: 1, maxLength: 64 }
        issuedAt: { type: string, format: date-time }
        issuedByUserId: { $ref: "#/components/schemas/Uuid" }
        status: { $ref: "#/components/schemas/VisitorPassStatus" }
        returnedAt: { type: string, format: date-time, nullable: true }
        returnedByUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        cancelledAt: { type: string, format: date-time, nullable: true }
        cancelledByUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    IssueVisitorPassRequest:
      type: object
      description: >
        BE-13I issue payload. `passCode` is normalized to trimmed
        uppercase and must be 1-64 letters / digits / hyphens /
        underscores, unique per Building. `issuedAt` defaults to now.
      required: [buildingId, visitCheckInId, passCode]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        visitCheckInId: { $ref: "#/components/schemas/Uuid" }
        passCode: { type: string, minLength: 1, maxLength: 64 }
        issuedAt: { type: string, format: date-time }

    ReturnVisitorPassRequest:
      type: object
      description: >
        BE-13I return payload; the body is optional. `returnedAt` defaults
        to now and must be a valid time (400
        `VISITOR_PASS_RETURN_TIME_INVALID`).
      properties:
        returnedAt: { type: string, format: date-time }

    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 01B3 - J09 Visitor Photo (BE-13E).
    VisitorPhotoType:
      type: string
      description: BE-13E attachment class of the referenced file.
      enum: [VISITOR_PHOTO, IDENTITY_DOCUMENT]

    VisitorPhotoOcrStatus:
      type: string
      description: >
        BE-13E OCR readiness lifecycle (metadata only):
        NOT_REQUESTED -> PENDING -> PROCESSED | FAILED.
      enum: [NOT_REQUESTED, PENDING, PROCESSED, FAILED]

    VisitorPhotoReviewStatus:
      type: string
      description: >
        BE-13E review boundary over staged OCR output:
        UNREVIEWED -> APPLIED | REJECTED.
      enum: [UNREVIEWED, APPLIED, REJECTED]

    VisitorPhotoStatus:
      type: string
      description: BE-13E readiness record lifecycle; REMOVED is a soft removal, the row is retained.
      enum: [ACTIVE, REMOVED]

    VisitorPhoto:
      type: object
      description: >
        BE-13E `PublicVisitorPhoto` - a photo-evidence / OCR-readiness row
        attached to the shared BE-13A visitor identity. BE-07 evidence
        convention: safe file reference + metadata only; no image binary
        is stored in PostgreSQL and extracted OCR values are STAGED until
        an explicit review routes them through the BE-13A visitor service.
      required: [id, clientId, visitorId, photoType, fileReference,
                 originalFileName, mimeType, fileSize, capturedAt,
                 ocrStatus, ocrProvider, ocrError, ocrProcessedAt,
                 extractedFullName, extractedIdentityType,
                 extractedIdentityNumber, reviewStatus, reviewedByUserId,
                 reviewedAt, status, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        visitorId: { $ref: "#/components/schemas/Uuid" }
        photoType: { $ref: "#/components/schemas/VisitorPhotoType" }
        fileReference: { type: string, maxLength: 1024 }
        originalFileName: { type: string, maxLength: 256 }
        mimeType: { type: string, maxLength: 128 }
        fileSize: { type: integer, maximum: 52428800 }
        capturedAt: { type: string, format: date-time, nullable: true }
        ocrStatus: { $ref: "#/components/schemas/VisitorPhotoOcrStatus" }
        ocrProvider: { type: string, nullable: true, maxLength: 128 }
        ocrError: { type: string, nullable: true, maxLength: 2048 }
        ocrProcessedAt: { type: string, format: date-time, nullable: true }
        extractedFullName: { type: string, nullable: true, maxLength: 256 }
        extractedIdentityType: { $ref: "#/components/schemas/VisitorIdentityType", nullable: true }
        extractedIdentityNumber: { type: string, nullable: true, maxLength: 128 }
        reviewStatus: { $ref: "#/components/schemas/VisitorPhotoReviewStatus" }
        reviewedByUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        reviewedAt: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/VisitorPhotoStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateVisitorPhotoRequest:
      type: object
      description: >
        BE-13E attach payload - file REFERENCE plus metadata only, never a
        binary body. `fileSize` mirrors the BE-07 evidence limit (50 MB).
        `requestOcr: true` queues the photo for external OCR
        (`ocrStatus` = PENDING, optional `ocrProvider`); otherwise
        NOT_REQUESTED.
      required: [photoType, fileReference, originalFileName, mimeType, fileSize]
      properties:
        photoType: { $ref: "#/components/schemas/VisitorPhotoType" }
        fileReference: { type: string, maxLength: 1024 }
        originalFileName: { type: string, maxLength: 256 }
        mimeType: { type: string, maxLength: 128 }
        fileSize: { type: integer, maximum: 52428800 }
        capturedAt: { type: string, format: date-time, nullable: true }
        requestOcr: { type: boolean }
        ocrProvider: { type: string, nullable: true, maxLength: 128 }

    RecordVisitorPhotoOcrResultRequest:
      type: object
      description: >
        BE-13E external OCR result metadata. PROCESSED carries the
        extracted identity fields, FAILED carries the error text;
        `ocrProcessedAt` defaults to now for PROCESSED / FAILED. Requires
        OCR to have been requested (409 `VISITOR_PHOTO_OCR_NOT_REQUESTED`
        otherwise). Staged values never touch the visitor master.
      required: [ocrStatus]
      properties:
        ocrStatus:
          type: string
          description: External processor outcome reported for this photo.
          enum: [PENDING, PROCESSED, FAILED]
        ocrProvider: { type: string, nullable: true, maxLength: 128 }
        ocrError: { type: string, nullable: true, maxLength: 2048 }
        ocrProcessedAt: { type: string, format: date-time, nullable: true }
        extractedFullName: { type: string, nullable: true, maxLength: 256 }
        extractedIdentityType: { $ref: "#/components/schemas/VisitorIdentityType", nullable: true }
        extractedIdentityNumber: { type: string, nullable: true, maxLength: 128 }

    ReviewVisitorPhotoRequest:
      type: object
      description: >
        BE-13E explicit review decision over staged OCR output. APPLY
        routes staged identity fields through the BE-13A visitor service -
        by default all non-null staged fields, optionally narrowed by
        `applyFullName` / `applyIdentity`. REJECT discards the staged
        output. Requires OCR PROCESSED (409
        `VISITOR_PHOTO_OCR_NOT_PROCESSED` otherwise) and at least one
        staged value to apply (400 `VISITOR_PHOTO_NOTHING_TO_APPLY`).
      required: [decision]
      properties:
        decision:
          type: string
          enum: [APPLY, REJECT]
        applyFullName: { type: boolean }
        applyIdentity: { type: boolean }

    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C1 - J09 Front Desk Log / Visitor Invitation (BE-13L/B).
    FrontDeskActivityType:
      type: string
      description: BE-13L projected activity kinds over the authoritative BE-13 records.
      enum: [INVITATION_CREATED, EXPECTED_VISITOR_REGISTERED, WALK_IN_REGISTERED,
             HOST_CONFIRMATION_REQUESTED, HOST_CONFIRMATION_CONFIRMED,
             HOST_CONFIRMATION_REJECTED, VISIT_CHECKED_IN, VISIT_CHECKED_OUT,
             VISITOR_PASS_ISSUED, VISITOR_PASS_RETURNED, VISITOR_PASS_CANCELLED,
             CONTRACTOR_VISITOR_REGISTERED, DELIVERY_COURIER_ARRIVED,
             DELIVERY_COURIER_RECEIVED, DELIVERY_COURIER_REJECTED,
             DELIVERY_COURIER_CANCELLED]

    FrontDeskSourceType:
      type: string
      description: BE-13L authoritative source domain of the projected entry.
      enum: [VISITOR_INVITATION, EXPECTED_VISITOR, WALK_IN_VISIT,
             HOST_CONFIRMATION, VISIT_CHECK_IN, VISITOR_PASS,
             CONTRACTOR_VISITOR, DELIVERY_COURIER]

    FrontDeskLog:
      type: object
      description: >
        BE-13L `PublicFrontDeskLog` - chronological read projection over
        authoritative BE-13 activities. Read-only by design; source
        references (`sourceId` plus the related BE-13 ids) stay with the
        authoritative records.
      required: [id, activityType, sourceType, sourceId, clientId,
                 buildingId, visitorId, expectedVisitorId, walkInVisitId,
                 visitCheckInId, actorUserId, occurredAt, notes]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        activityType: { $ref: "#/components/schemas/FrontDeskActivityType" }
        sourceType: { $ref: "#/components/schemas/FrontDeskSourceType" }
        sourceId: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        visitorId: { $ref: "#/components/schemas/Uuid", nullable: true }
        expectedVisitorId: { $ref: "#/components/schemas/Uuid", nullable: true }
        walkInVisitId: { $ref: "#/components/schemas/Uuid", nullable: true }
        visitCheckInId: { $ref: "#/components/schemas/Uuid", nullable: true }
        actorUserId: { $ref: "#/components/schemas/Uuid" }
        occurredAt: { type: string, format: date-time }
        notes: { type: string, nullable: true }

    VisitorInvitationStatus:
      type: string
      description: BE-13B invitation lifecycle; CANCELLED is terminal and never re-activated.
      enum: [PENDING, CANCELLED]

    VisitorInvitation:
      type: object
      description: >
        BE-13B `PublicVisitorInvitation` - a visit-planning record over the
        shared BE-13A visitor identity. Distinct from the BE-01 auth user
        onboarding `invitations` module. At least one host reference is
        present.
      required: [id, clientId, buildingId, visitorId, hostUserId,
                 hostWorkforceId, hostName, expectedArrivalAt,
                 expectedDepartureAt, purpose, notes, status,
                 createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        visitorId: { $ref: "#/components/schemas/Uuid" }
        hostUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostWorkforceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostName: { type: string, nullable: true, maxLength: 256 }
        expectedArrivalAt: { type: string, format: date-time }
        expectedDepartureAt: { type: string, format: date-time, nullable: true }
        purpose: { type: string, maxLength: 512 }
        notes: { type: string, nullable: true, maxLength: 4096 }
        status: { $ref: "#/components/schemas/VisitorInvitationStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateVisitorInvitationRequest:
      type: object
      description: >
        BE-13B create payload. At least one host reference (`hostUserId`,
        `hostWorkforceId` or `hostName`) is required (400
        `VISITOR_INVITATION_HOST_REQUIRED`); a BLOCKED or INACTIVE visitor
        identity is rejected 400 and `expectedDepartureAt`, when supplied,
        must be after `expectedArrivalAt` (400
        `VISITOR_INVITATION_INVALID_TIME_WINDOW`). No delivery / token
        semantics are part of this contract.
      required: [buildingId, visitorId, expectedArrivalAt, purpose]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        visitorId: { $ref: "#/components/schemas/Uuid" }
        hostUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostWorkforceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostName: { type: string, nullable: true, maxLength: 256 }
        expectedArrivalAt: { type: string, format: date-time }
        expectedDepartureAt: { type: string, format: date-time, nullable: true }
        purpose: { type: string, maxLength: 512 }
        notes: { type: string, nullable: true, maxLength: 4096 }

    UpdateVisitorInvitationRequest:
      type: object
      description: >
        BE-13B edit payload. At least one updatable field is required. A
        CANCELLED invitation is terminal (409
        `VISITOR_INVITATION_ALREADY_CANCELLED`). Building / Visitor /
        Client linkage are immutable.
      properties:
        hostUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostWorkforceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostName: { type: string, nullable: true, maxLength: 256 }
        expectedArrivalAt: { type: string, format: date-time }
        expectedDepartureAt: { type: string, format: date-time, nullable: true }
        purpose: { type: string, maxLength: 512 }
        notes: { type: string, nullable: true, maxLength: 4096 }

    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C2 - J09 Contractor Visitor / Host Confirmation (BE-13J/F).
    ContractorVisitorStatus:
      type: string
      description: BE-13J contractor metadata lifecycle; CANCELLED is terminal.
      enum: [REGISTERED, CANCELLED]

    ContractorVisitor:
      type: object
      description: >
        BE-13J `PublicContractorVisitor` - contractor-specific metadata
        bound to exactly one existing BE-13 visit (Expected Visitor or
        Walk-In). Visitor identity and operational entry/exit stay owned
        by the BE-13A / BE-13G/H / BE-13I modules. No permit-to-work,
        vendor workforce or contractor-master administration semantics.
      required: [id, clientId, buildingId, visitorId, expectedVisitorId,
                 walkInVisitId, contractorCompany, contractorPurpose,
                 responsibleHostUserId, responsibleHostWorkforceId,
                 responsibleHostName, functionalLocationId, workLocation,
                 notes, status, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        visitorId: { $ref: "#/components/schemas/Uuid" }
        expectedVisitorId: { $ref: "#/components/schemas/Uuid", nullable: true }
        walkInVisitId: { $ref: "#/components/schemas/Uuid", nullable: true }
        contractorCompany: { type: string, maxLength: 255 }
        contractorPurpose: { type: string, maxLength: 1024 }
        responsibleHostUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        responsibleHostWorkforceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        responsibleHostName: { type: string, nullable: true, maxLength: 255 }
        functionalLocationId: { $ref: "#/components/schemas/Uuid", nullable: true }
        workLocation: { type: string, nullable: true, maxLength: 512 }
        notes: { type: string, nullable: true, maxLength: 4096 }
        status: { $ref: "#/components/schemas/ContractorVisitorStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateContractorVisitorRequest:
      type: object
      description: >
        BE-13J create payload. Exactly ONE of `expectedVisitorId` /
        `walkInVisitId` is required (400
        `CONTRACTOR_VISITOR_VISIT_REFERENCE_REQUIRED` otherwise); Building
        and visitor identity derive from the visit - optional `buildingId`
        / `visitorId` are consistency assertions only (400 mismatch). One
        contractor record per visit (409
        `CONTRACTOR_VISITOR_ALREADY_EXISTS`). At least one responsible-host
        reference must resolve (400 `CONTRACTOR_VISITOR_HOST_REQUIRED`).
      required: [contractorCompany, contractorPurpose]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        visitorId: { $ref: "#/components/schemas/Uuid" }
        expectedVisitorId: { $ref: "#/components/schemas/Uuid" }
        walkInVisitId: { $ref: "#/components/schemas/Uuid" }
        contractorCompany: { type: string, maxLength: 255 }
        contractorPurpose: { type: string, maxLength: 1024 }
        responsibleHostUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        responsibleHostWorkforceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        responsibleHostName: { type: string, nullable: true, maxLength: 255 }
        functionalLocationId: { $ref: "#/components/schemas/Uuid", nullable: true }
        workLocation: { type: string, nullable: true, maxLength: 512 }
        notes: { type: string, nullable: true, maxLength: 4096 }

    UpdateContractorVisitorRequest:
      type: object
      description: >
        BE-13J edit payload. At least one updatable field is required.
        `status: CANCELLED` performs the lifecycle cancellation
        (REGISTERED -> CANCELLED, terminal).
      properties:
        contractorCompany: { type: string, maxLength: 255 }
        contractorPurpose: { type: string, maxLength: 1024 }
        responsibleHostUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        responsibleHostWorkforceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        responsibleHostName: { type: string, nullable: true, maxLength: 255 }
        functionalLocationId: { $ref: "#/components/schemas/Uuid", nullable: true }
        workLocation: { type: string, nullable: true, maxLength: 512 }
        notes: { type: string, nullable: true, maxLength: 4096 }
        status: { $ref: "#/components/schemas/ContractorVisitorStatus" }

    HostConfirmationStatus:
      type: string
      description: >
        BE-13F confirmation lifecycle. PENDING -> CONFIRMED | REJECTED is a
        single, terminal decision - a REJECTED visit can never proceed as
        confirmed.
      enum: [PENDING, CONFIRMED, REJECTED]

    HostConfirmation:
      type: object
      description: >
        BE-13F `PublicHostConfirmation` - backend-authoritative host
        confirmation over one BE-13 visit context (Expected Visitor or
        Walk-In). Host references are defaulted from the visit and are the
        smallest safe host/tenant reference (User / Workforce / free-form
        name) - no Tenant Management semantics.
      required: [id, clientId, buildingId, expectedVisitorId,
                 walkInVisitId, hostUserId, hostWorkforceId, hostName,
                 status, confirmedByUserId, confirmedAt, rejectionReason,
                 notes, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        expectedVisitorId: { $ref: "#/components/schemas/Uuid", nullable: true }
        walkInVisitId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostWorkforceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostName: { type: string, nullable: true, maxLength: 256 }
        status: { $ref: "#/components/schemas/HostConfirmationStatus" }
        confirmedByUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        confirmedAt: { type: string, format: date-time, nullable: true }
        rejectionReason: { type: string, nullable: true, maxLength: 1024 }
        notes: { type: string, nullable: true, maxLength: 4096 }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateHostConfirmationRequest:
      type: object
      description: >
        BE-13F create payload. Exactly ONE of `expectedVisitorId` /
        `walkInVisitId` is required (400
        `HOST_CONFIRMATION_VISIT_REFERENCE_REQUIRED` otherwise); one
        confirmation per visit (409 `HOST_CONFIRMATION_ALREADY_EXISTS`).
        Host references default from the visit; at least one must resolve
        (400 `HOST_CONFIRMATION_HOST_REQUIRED`).
      properties:
        expectedVisitorId: { $ref: "#/components/schemas/Uuid" }
        walkInVisitId: { $ref: "#/components/schemas/Uuid" }
        hostUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostWorkforceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        hostName: { type: string, nullable: true, maxLength: 256 }
        notes: { type: string, nullable: true, maxLength: 4096 }

    ConfirmHostConfirmationRequest:
      type: object
      description: BE-13F confirm payload; the body is optional.
      properties:
        notes: { type: string, nullable: true, maxLength: 4096 }

    RejectHostConfirmationRequest:
      type: object
      description: >
        BE-13F reject payload; `rejectionReason` is mandatory. An already
        decided record is rejected 409 `HOST_CONFIRMATION_ALREADY_DECIDED`.
      required: [rejectionReason]
      properties:
        rejectionReason: { type: string, maxLength: 1024 }
        notes: { type: string, nullable: true, maxLength: 4096 }

    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 01C3 - J09 Delivery / Security Binding (BE-13K/BE-12J).
    DeliveryCourierType:
      type: string
      description: BE-13K arrival class.
      enum: [DELIVERY, COURIER]

    DeliveryCourierStatus:
      type: string
      description: BE-13K delivery lifecycle; status updates run ARRIVED -> RECEIVED | REJECTED | CANCELLED (terminals).
      enum: [ARRIVED, RECEIVED, REJECTED, CANCELLED]

    DeliveryCourier:
      type: object
      description: >
        BE-13K `PublicDeliveryCourier` - delivery / courier arrival
        metadata, optionally bound to the shared Visitor identity and one
        existing visit. Check-In / Check-Out stay owned by the BE-13 visit
        lifecycle.
      required: [id, clientId, buildingId, visitorId, expectedVisitorId,
                 walkInVisitId, deliveryType, courierCompany, courierName,
                 recipientUserId, recipientWorkforceId, recipientName,
                 arrivedAt, referenceNumber, notes, status,
                 statusUpdatedAt, statusUpdatedByUserId, createdByUserId,
                 createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        visitorId: { $ref: "#/components/schemas/Uuid", nullable: true }
        expectedVisitorId: { $ref: "#/components/schemas/Uuid", nullable: true }
        walkInVisitId: { $ref: "#/components/schemas/Uuid", nullable: true }
        deliveryType: { $ref: "#/components/schemas/DeliveryCourierType" }
        courierCompany: { type: string, nullable: true, maxLength: 255 }
        courierName: { type: string, nullable: true, maxLength: 255 }
        recipientUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        recipientWorkforceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        recipientName: { type: string, nullable: true, maxLength: 255 }
        arrivedAt: { type: string, format: date-time }
        referenceNumber: { type: string, nullable: true, maxLength: 255 }
        notes: { type: string, nullable: true, maxLength: 4096 }
        status: { $ref: "#/components/schemas/DeliveryCourierStatus" }
        statusUpdatedAt: { type: string, format: date-time, nullable: true }
        statusUpdatedByUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateDeliveryCourierRequest:
      type: object
      description: >
        BE-13K create payload. Without a visit reference `buildingId` is
        required (400 `DELIVERY_COURIER_CONTEXT_REQUIRED`); courier
        identity (company and/or name) and a resolvable recipient are
        required (400 otherwise). `arrivedAt` defaults to now and must not
        be in the future (400 `DELIVERY_COURIER_ARRIVAL_IN_FUTURE`).
      required: [deliveryType]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        visitorId: { $ref: "#/components/schemas/Uuid", nullable: true }
        expectedVisitorId: { $ref: "#/components/schemas/Uuid" }
        walkInVisitId: { $ref: "#/components/schemas/Uuid" }
        deliveryType: { $ref: "#/components/schemas/DeliveryCourierType" }
        courierCompany: { type: string, nullable: true, maxLength: 255 }
        courierName: { type: string, nullable: true, maxLength: 255 }
        recipientUserId: { $ref: "#/components/schemas/Uuid", nullable: true }
        recipientWorkforceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        recipientName: { type: string, nullable: true, maxLength: 255 }
        arrivedAt: { type: string, format: date-time }
        referenceNumber: { type: string, nullable: true, maxLength: 255 }
        notes: { type: string, nullable: true, maxLength: 4096 }

    UpdateDeliveryCourierStatusRequest:
      type: object
      description: >
        BE-13K resolve payload. `status` must be a terminal target - only
        ARRIVED records can transition (409
        `DELIVERY_COURIER_INVALID_TRANSITION` otherwise); REJECTED /
        CANCELLED additionally require no ACTIVE Check-In for the bound
        visit (409 `DELIVERY_COURIER_ACTIVE_VISIT`).
      required: [status]
      properties:
        status:
          type: string
          enum: [RECEIVED, REJECTED, CANCELLED]
        referenceNumber: { type: string, nullable: true, maxLength: 255 }
        notes: { type: string, nullable: true, maxLength: 4096 }

    SecurityVisitorBindingStatus:
      type: string
      description: BE-12J binding status; independent of any future Visitor lifecycle - INACTIVE never blocks a re-bind.
      enum: [ACTIVE, INACTIVE]

    SecurityVisitorBinding:
      type: object
      description: >
        BE-12J `PublicSecurityVisitorBinding` - the Security operational
        context for an OPAQUE `externalVisitReference` (caller-supplied
        future-link hook, intentionally NOT a foreign key). Stores no
        visitor personal data: client/building, optional Security Post and
        Security Workforce anchors, the external reference, free-form
        context and its own status only.
      required: [id, clientId, buildingId, securityPostId,
                 securityWorkforceId, externalVisitReference,
                 securityContext, status, createdByUserId, createdAt,
                 updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        securityPostId: { $ref: "#/components/schemas/Uuid", nullable: true }
        securityWorkforceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        externalVisitReference: { type: string, maxLength: 256 }
        securityContext: { type: string, nullable: true, maxLength: 4096 }
        status: { $ref: "#/components/schemas/SecurityVisitorBindingStatus" }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateSecurityVisitorBindingRequest:
      type: object
      description: >
        BE-12J create payload. `buildingId` and `externalVisitReference`
        are required; optional anchors must belong to the same Building
        and be ACTIVE (400). `status` defaults to ACTIVE. One ACTIVE
        binding per (building, externalVisitReference) - 409
        `SECURITY_VISITOR_BINDING_ALREADY_EXISTS`.
      required: [buildingId, externalVisitReference]
      properties:
        buildingId: { $ref: "#/components/schemas/Uuid" }
        securityPostId: { $ref: "#/components/schemas/Uuid", nullable: true }
        securityWorkforceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        externalVisitReference: { type: string, maxLength: 256 }
        securityContext: { type: string, nullable: true, maxLength: 4096 }
        status: { $ref: "#/components/schemas/SecurityVisitorBindingStatus" }

    UpdateSecurityVisitorBindingRequest:
      type: object
      description: >
        BE-12J edit payload. At least one updatable field is required.
        `externalVisitReference` and `status` may be edited; an update
        that would collide with another ACTIVE binding is rejected 409.
        Client / Building are immutable.
      properties:
        securityPostId: { $ref: "#/components/schemas/Uuid", nullable: true }
        securityWorkforceId: { $ref: "#/components/schemas/Uuid", nullable: true }
        externalVisitReference: { type: string, maxLength: 256 }
        securityContext: { type: string, nullable: true, maxLength: 4096 }
        status: { $ref: "#/components/schemas/SecurityVisitorBindingStatus" }

    # ------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 02B - J10 Tenant Service Request
    # (BE-14E) schemas. Shapes mirror the runtime public DTOs exactly.
    # ------------------------------------------------------------------

    TenantServiceRequestStatus:
      type: string
      description: >
        BE-14E Tenant Service Request lifecycle. `OPEN` is the intake state;
        `CONVERTED` is reached when the Work Request binding is created;
        `CANCELLED` is reached via the cancel command. Only `OPEN` is mutable or
        cancellable. There is no re-open and no approval state.
      enum: [OPEN, CANCELLED, CONVERTED]

    TenantServiceRequestAction:
      type: string
      description: >
        Upper-case command token of the backend-authoritative, caller-specific
        action set. The subset actually offered to a caller is resolved by
        `GET /tenant-service-requests/{id}/available-actions`; a command run
        without being offered yields 403
        `TENANT_SERVICE_REQUEST_ACTION_NOT_ALLOWED`.
      enum: [UPDATE, CANCEL, CREATE_WORK_REQUEST, CREATE_WORK_ORDER]

    TenantServiceRequest:
      type: object
      description: >
        BE-14E Tenant Service Request. `clientId` is derived from the Tenant
        Company and `tenantCompanyId` from the intake path - neither is
        caller-supplied. `workRequestId` / `workOrderId` are the BE-08
        conversion bindings and are null until the corresponding command runs.
      required:
        [id, clientId, tenantCompanyId, tenantPicId, buildingId, spaceId,
         requestNumber, requestType, title, description, priority, status,
         requestedAt, workRequestId, workOrderId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        tenantCompanyId: { $ref: "#/components/schemas/Uuid" }
        tenantPicId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: The ACTIVE Tenant PIC who raised the request.
        buildingId: { $ref: "#/components/schemas/Uuid" }
        spaceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Optional Space context. When set it must have an ACTIVE relationship
            to the tenant in this Building.
        requestNumber:
          type: string
          description: >
            Trimmed and uppercased on intake; must start with a letter and
            contain only uppercase letters, digits, hyphens and underscores
            (2-64 characters). Unique per Client.
        requestType:
          type: string
          description: >
            Data-driven request type code (no hardcoded model). Trimmed and
            uppercased on intake; must start with a letter and contain only
            uppercase letters, digits, hyphens and underscores (2-64
            characters). Reused as the Work Order `workType` on conversion.
        title: { type: string, maxLength: 200 }
        description: { type: string, nullable: true, maxLength: 1000 }
        priority: { $ref: "#/components/schemas/WorkOrderPriority" }
        status: { $ref: "#/components/schemas/TenantServiceRequestStatus" }
        requestedAt: { type: string, format: date-time }
        workRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            BE-08 Work Request binding, set by
            `POST /tenant-service-requests/{id}/work-request` (which also moves
            `status` to `CONVERTED`). Null before conversion.
        workOrderId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            BE-08 Work Order binding, set by
            `POST /tenant-service-requests/{id}/work-order`. Requires an
            existing `workRequestId`; setting it does not change `status`. Null
            before that command runs.
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    TenantServiceRequestAvailableActions:
      type: object
      description: >
        Backend-authoritative, caller-specific Tenant Service Request actions,
        using the established `{ serviceRequestId, state, availableActions }`
        shape. An empty list is authoritative; clients must not infer
        transitions from `state`.
      required: [serviceRequestId, state, availableActions]
      properties:
        serviceRequestId: { $ref: "#/components/schemas/Uuid" }
        state: { $ref: "#/components/schemas/TenantServiceRequestStatus" }
        availableActions:
          type: array
          items: { $ref: "#/components/schemas/TenantServiceRequestAction" }

    CreateTenantServiceRequestRequest:
      type: object
      description: >
        BE-14E intake payload for
        `POST /tenant-companies/{tenantCompanyId}/service-requests`.
        `tenantCompanyId` comes from the path and `clientId` is derived from the
        Tenant Company - neither may be supplied here. `priority` defaults to
        `MEDIUM` when omitted. The created record is always `OPEN`.
      required: [tenantPicId, buildingId, requestNumber, requestType, title]
      properties:
        tenantPicId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Must be an ACTIVE PIC of the path Tenant Company, else 400
            `TENANT_SERVICE_REQUEST_REQUESTER_INVALID`.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Must be a Building the caller can access and must have an ACTIVE
            tenant-building context effective now, else 400
            `TENANT_SERVICE_REQUEST_CONTEXT_INVALID`.
        spaceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Optional. Must have an ACTIVE relationship to the tenant in this
            Building, else 400 `TENANT_SERVICE_REQUEST_SPACE_MISMATCH`.
        requestNumber:
          type: string
          minLength: 2
          maxLength: 64
          description: >
            Trimmed and uppercased; must start with a letter and contain only
            uppercase letters, digits, hyphens and underscores. Unique per
            Client, else 409
            `TENANT_SERVICE_REQUEST_NUMBER_ALREADY_EXISTS`.
        requestType:
          type: string
          minLength: 2
          maxLength: 64
          description: >
            Data-driven code. Trimmed and uppercased; must start with a letter
            and contain only uppercase letters, digits, hyphens and underscores.
        title:
          type: string
          maxLength: 200
          description: Required; trimmed and must not be empty.
        description:
          type: string
          maxLength: 1000
        priority: { $ref: "#/components/schemas/WorkOrderPriority" }

    UpdateTenantServiceRequestRequest:
      type: object
      description: >
        BE-14E edit payload for `PATCH /tenant-service-requests/{id}`. Only
        these four fields are mutable and all are optional; only the fields
        present are changed. Supplying `clientId`, `tenantCompanyId`,
        `tenantPicId`, `buildingId`, `spaceId`, `requestNumber`, `status`,
        `requestedAt`, `workRequestId` or `workOrderId` is rejected 400 as an
        immutable field. The request must be `OPEN` and the caller must hold the
        resolved `UPDATE` action.
      properties:
        requestType:
          type: string
          minLength: 2
          maxLength: 64
          description: >
            Data-driven code. Trimmed and uppercased; must start with a letter
            and contain only uppercase letters, digits, hyphens and underscores.
        title:
          type: string
          maxLength: 200
          description: Trimmed and must not be empty.
        description:
          type: string
          maxLength: 1000
          nullable: true
          description: "`null` clears the description."
        priority: { $ref: "#/components/schemas/WorkOrderPriority" }

    CreateTenantServiceRequestWorkOrderRequest:
      type: object
      description: >
        BE-14E Work Order binding payload for
        `POST /tenant-service-requests/{id}/work-order`. The caller supplies
        only the Work Order number - `title`, `description`, `workType` (from
        `requestType`) and `priority` are all derived from the Tenant Service
        Request and its bound Work Request.
      required: [workOrderNumber]
      properties:
        workOrderNumber:
          type: string
          minLength: 2
          maxLength: 64
          description: >
            Trimmed and uppercased; must start with a letter and contain only
            uppercase letters, digits, hyphens and underscores.

    # ------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 02C - J10 Tenant Complaint (BE-14F)
    # schemas. Shapes mirror the runtime public DTOs exactly.
    # ------------------------------------------------------------------

    TenantComplaintStatus:
      type: string
      description: >
        BE-14F Tenant Complaint lifecycle. `OPEN` is the intake state;
        `ESCALATED` is reached when the Finding binding is created; `CANCELLED`
        is reached via the cancel command. Only `OPEN` is mutable or cancellable.
        There is no re-open and no approval state.
      enum: [OPEN, CANCELLED, ESCALATED]

    TenantComplaintAction:
      type: string
      description: >
        The full complaint action vocabulary: the BE-14F intake commands
        (`UPDATE`, `CANCEL`, `CREATE_FINDING`), the BE-09 Finding lifecycle
        tokens of the bound Finding, and `CREATE_WORK_ORDER`. The subset actually
        offered to a caller is resolved by `GET
        /tenant-complaints/{id}/available-actions`; a command run without being
        offered yields 403 `TENANT_COMPLAINT_ACTION_NOT_ALLOWED`. The Finding
        tokens are reported only - this route surface has no endpoint that
        executes them.
      enum:
        [UPDATE, CANCEL, CREATE_FINDING, ASSIGN, MARK_ASSIGNED, START,
         SUBMIT_FOR_REVIEW, OPEN_REVIEW, APPROVE, REJECT, REQUEST_REWORK,
         RESUBMIT, CLOSE, CREATE_WORK_ORDER]

    TenantComplaint:
      type: object
      description: >
        BE-14F Tenant Complaint. `clientId` is derived from the Tenant Company
        and `tenantCompanyId` from the intake path - neither is caller-supplied.
        `findingId` / `workOrderId` are the BE-09 / BE-08 bindings and are null
        until the corresponding command runs.
      required:
        [id, clientId, tenantCompanyId, tenantPicId, buildingId, spaceId,
         complaintNumber, complaintType, title, description, severity, status,
         reportedAt, findingId, workOrderId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        tenantCompanyId: { $ref: "#/components/schemas/Uuid" }
        tenantPicId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: The ACTIVE Tenant PIC who raised the complaint.
        buildingId: { $ref: "#/components/schemas/Uuid" }
        spaceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Optional Space context. When set it must have an ACTIVE relationship
            to the tenant in this Building, effective now.
        complaintNumber:
          type: string
          description: >
            Trimmed and uppercased on intake; must start with a letter and
            contain only uppercase letters, digits, hyphens and underscores
            (2-64 characters). Unique per Client. Reused as the bound Finding's
            number on escalation.
        complaintType:
          type: string
          description: >
            Data-driven complaint type code (no hardcoded model). Trimmed and
            uppercased on intake; must start with a letter and contain only
            uppercase letters, digits, hyphens and underscores (2-64
            characters). Reused as the Work Order `workType` on conversion.
        title: { type: string, maxLength: 200 }
        description: { type: string, nullable: true, maxLength: 2000 }
        severity: { $ref: "#/components/schemas/WorkOrderPriority" }
        status: { $ref: "#/components/schemas/TenantComplaintStatus" }
        reportedAt: { type: string, format: date-time }
        findingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            BE-09 Finding binding, set by
            `POST /tenant-complaints/{id}/finding` (which also moves `status` to
            `ESCALATED`). Null before escalation.
        workOrderId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            BE-08 Work Order binding, set by
            `POST /tenant-complaints/{id}/work-order`. Requires an existing
            `findingId`; setting it does not change `status`. Null before that
            command runs.
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    TenantComplaintAvailableActions:
      type: object
      description: >
        Backend-authoritative, caller-specific Tenant Complaint actions, using
        the established `{ complaintId, state, findingId, availableActions }`
        shape. An empty list is authoritative; clients must not infer
        transitions from `state`.
      required: [complaintId, state, findingId, availableActions]
      properties:
        complaintId: { $ref: "#/components/schemas/Uuid" }
        state: { $ref: "#/components/schemas/TenantComplaintStatus" }
        findingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: The bound BE-09 Finding, or null when not yet escalated.
        availableActions:
          type: array
          items: { $ref: "#/components/schemas/TenantComplaintAction" }

    CreateTenantComplaintRequest:
      type: object
      description: >
        BE-14F intake payload for
        `POST /tenant-companies/{tenantCompanyId}/complaints`. `tenantCompanyId`
        comes from the path and `clientId` is derived from the Tenant Company -
        neither may be supplied here. `severity` defaults to `MEDIUM` when
        omitted. The created record is always `OPEN`.
      required: [tenantPicId, buildingId, complaintNumber, complaintType, title]
      properties:
        tenantPicId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Must be an ACTIVE PIC of the path Tenant Company, else 400
            `TENANT_COMPLAINT_COMPLAINANT_INVALID`.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Must be a Building the caller can access and must have an ACTIVE
            tenant-building context effective now, else 400
            `TENANT_COMPLAINT_CONTEXT_INVALID`.
        spaceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Optional. Must have an ACTIVE relationship to the tenant in this
            Building, effective now, else 400
            `TENANT_COMPLAINT_SPACE_MISMATCH`.
        complaintNumber:
          type: string
          minLength: 2
          maxLength: 64
          description: >
            Trimmed and uppercased; must start with a letter and contain only
            uppercase letters, digits, hyphens and underscores. Unique per
            Client, else 409 `TENANT_COMPLAINT_NUMBER_ALREADY_EXISTS`.
        complaintType:
          type: string
          minLength: 2
          maxLength: 64
          description: >
            Data-driven code. Trimmed and uppercased; must start with a letter
            and contain only uppercase letters, digits, hyphens and underscores.
        title:
          type: string
          maxLength: 200
          description: Required; trimmed and must not be empty.
        description:
          type: string
          maxLength: 2000
        severity: { $ref: "#/components/schemas/WorkOrderPriority" }

    UpdateTenantComplaintRequest:
      type: object
      description: >
        BE-14F edit payload for `PATCH /tenant-complaints/{id}`. Only these four
        fields are mutable and all are optional; only the fields present are
        changed. Supplying `clientId`, `tenantCompanyId`, `tenantPicId`,
        `buildingId`, `spaceId`, `complaintNumber`, `status`, `reportedAt`,
        `findingId` or `workOrderId` is rejected 400 as an immutable field. The
        complaint must be `OPEN` and the caller must hold the resolved `UPDATE`
        action.
      properties:
        complaintType:
          type: string
          minLength: 2
          maxLength: 64
          description: >
            Data-driven code. Trimmed and uppercased; must start with a letter
            and contain only uppercase letters, digits, hyphens and underscores.
        title:
          type: string
          maxLength: 200
          description: Trimmed and must not be empty.
        description:
          type: string
          maxLength: 2000
          nullable: true
          description: "`null` clears the description."
        severity: { $ref: "#/components/schemas/WorkOrderPriority" }

    CreateTenantComplaintWorkOrderRequest:
      type: object
      description: >
        BE-14F Work Order binding payload for
        `POST /tenant-complaints/{id}/work-order`. The caller supplies only the
        Work Order number - `title`, `description`, `workType` (from
        `complaintType`) and the priority (from `severity`) are all derived from
        the Tenant Complaint.
      required: [workOrderNumber]
      properties:
        workOrderNumber:
          type: string
          minLength: 2
          maxLength: 64
          description: >
            Trimmed and uppercased; must start with a letter and contain only
            uppercase letters, digits, hyphens and underscores.

    # ------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 02D - J10 Tenant Utility Request
    # (BE-14G) schemas. Shapes mirror the runtime public DTOs exactly. This is
    # an intake + conversion-binding surface: no meter, tariff, reading,
    # consumption or monetary field exists on it.
    # ------------------------------------------------------------------

    TenantUtilityRequestStatus:
      type: string
      description: >
        BE-14G Tenant Utility Request lifecycle. The runtime aliases the Work
        Request status vocabulary exactly - this module does not widen or
        narrow it. `OPEN` -> `CONVERTED` happens when the Work Request binding
        is created; `OPEN` -> `CANCELLED` happens via the cancel command. Only
        `OPEN` is mutable or cancellable. There is no re-open and no approval
        state.
      enum: [OPEN, CANCELLED, CONVERTED]

    TenantUtilityRequestAction:
      type: string
      description: >
        Upper-case command token of the backend-authoritative, caller-specific
        action set. The subset actually offered to a caller is resolved by `GET
        /tenant-utility-requests/{id}/available-actions`; a command run without
        being offered yields 403 `TENANT_UTILITY_REQUEST_ACTION_NOT_ALLOWED`.
      enum: [UPDATE, CANCEL, CREATE_WORK_REQUEST, CREATE_WORK_ORDER]

    TenantUtilityRequest:
      type: object
      description: >
        BE-14G Tenant Utility Request. `clientId` is derived from the Tenant
        Company and `tenantCompanyId` from the intake path - neither is
        caller-supplied. `spaceId` is REQUIRED and non-nullable. `workRequestId`
        / `workOrderId` are the BE-08 conversion bindings and are null until the
        corresponding command runs.
      required:
        [id, clientId, tenantCompanyId, tenantPicId, buildingId, spaceId,
         utilityType, requestNumber, requestDetails, requestedAt, status, notes,
         workRequestId, workOrderId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        tenantCompanyId: { $ref: "#/components/schemas/Uuid" }
        tenantPicId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: The ACTIVE Tenant PIC who raised the request.
        buildingId: { $ref: "#/components/schemas/Uuid" }
        spaceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            REQUIRED, non-nullable Space context. Must have an ACTIVE
            relationship to the tenant in this Building.
        utilityType:
          type: string
          description: >
            Data-driven utility type code (no hardcoded model). Trimmed and
            uppercased on intake; must start with a letter and contain only
            uppercase letters, digits, hyphens and underscores (2-64
            characters). Reused as the Work Request `requestType` and the Work
            Order `workType` on conversion.
        requestNumber:
          type: string
          description: >
            Trimmed and uppercased on intake; must start with a letter and
            contain only uppercase letters, digits, hyphens and underscores
            (2-64 characters). Unique per Client. Reused as the bound Work
            Request's number on conversion.
        requestDetails:
          type: string
          maxLength: 2000
          description: >
            REQUIRED free-form detail of what the tenant is asking for. This
            module has no `title` field; the Work Request / Work Order title and
            description are derived server-side.
        requestedAt: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/TenantUtilityRequestStatus" }
        notes:
          type: string
          nullable: true
          maxLength: 1000
        workRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            BE-08 Work Request binding, set by
            `POST /tenant-utility-requests/{id}/work-request` (which also moves
            `status` to `CONVERTED`). Null before conversion.
        workOrderId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            BE-08 Work Order binding, set by
            `POST /tenant-utility-requests/{id}/work-order`. Requires an
            existing `workRequestId`; setting it does not change `status`. Null
            before that command runs.
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    TenantUtilityRequestAvailableActions:
      type: object
      description: >
        Backend-authoritative, caller-specific Tenant Utility Request actions,
        using the established `{ utilityRequestId, state, availableActions }`
        shape. An empty list is authoritative; clients must not infer
        transitions from `state`.
      required: [utilityRequestId, state, availableActions]
      properties:
        utilityRequestId: { $ref: "#/components/schemas/Uuid" }
        state: { $ref: "#/components/schemas/TenantUtilityRequestStatus" }
        availableActions:
          type: array
          items: { $ref: "#/components/schemas/TenantUtilityRequestAction" }

    CreateTenantUtilityRequestRequest:
      type: object
      description: >
        BE-14G intake payload for
        `POST /tenant-companies/{tenantCompanyId}/utility-requests`.
        `tenantCompanyId` comes from the path and `clientId` is derived from the
        Tenant Company - neither may be supplied here. `spaceId` is REQUIRED.
      required:
        [tenantPicId, buildingId, spaceId, utilityType, requestNumber,
         requestDetails]
      properties:
        tenantPicId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Must be an ACTIVE PIC of the path Tenant Company, else 400
            `TENANT_UTILITY_REQUEST_REQUESTER_INVALID`.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Must have an ACTIVE tenant building context, else 400
            `TENANT_UTILITY_REQUEST_CONTEXT_INVALID`.
        spaceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            REQUIRED. Must have an ACTIVE relationship to the tenant in this
            Building, else 400 `TENANT_UTILITY_REQUEST_SPACE_INVALID`.
        utilityType:
          type: string
          minLength: 2
          maxLength: 64
          description: >
            Data-driven code. Trimmed and uppercased; must start with a letter
            and contain only uppercase letters, digits, hyphens and underscores.
        requestNumber:
          type: string
          minLength: 2
          maxLength: 64
          description: >
            Trimmed and uppercased; must start with a letter and contain only
            uppercase letters, digits, hyphens and underscores. Unique per
            Client, else 409 `TENANT_UTILITY_REQUEST_NUMBER_ALREADY_EXISTS`.
        requestDetails:
          type: string
          maxLength: 2000
          description: Required; trimmed and must not be empty.
        notes:
          type: string
          maxLength: 1000

    UpdateTenantUtilityRequestRequest:
      type: object
      description: >
        BE-14G edit payload for `PATCH /tenant-utility-requests/{id}`. Only
        these three fields are mutable and all are optional; only the fields
        present are changed. Supplying `clientId`, `tenantCompanyId`,
        `tenantPicId`, `buildingId`, `spaceId`, `requestNumber`, `requestedAt`,
        `status`, `workRequestId` or `workOrderId` is rejected 400 as an
        immutable field. The request must be `OPEN` and the caller must hold the
        resolved `UPDATE` action.
      properties:
        utilityType:
          type: string
          minLength: 2
          maxLength: 64
          description: >
            Data-driven code. Trimmed and uppercased; must start with a letter
            and contain only uppercase letters, digits, hyphens and underscores.
        requestDetails:
          type: string
          maxLength: 2000
          description: Trimmed and must not be empty.
        notes:
          type: string
          maxLength: 1000
          nullable: true
          description: "`null` clears the notes."

    CreateTenantUtilityRequestWorkOrderRequest:
      type: object
      description: >
        BE-14G Work Order binding payload for
        `POST /tenant-utility-requests/{id}/work-order`. The caller supplies
        only the Work Order number - the title and description are derived
        server-side and `workType` comes from `utilityType`.
      required: [workOrderNumber]
      properties:
        workOrderNumber:
          type: string
          minLength: 2
          maxLength: 64
          description: >
            Trimmed and uppercased; must start with a letter and contain only
            uppercase letters, digits, hyphens and underscores.

    # ------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 02E1 - J10 Tenant Communication
    # (BE-14K) schemas. Shapes mirror the runtime public DTOs exactly. This is
    # an operational RECORD surface: it carries no delivery transport, receipt,
    # thread, attachment, provider or notification field.
    # ------------------------------------------------------------------

    TenantCommunicationStatus:
      type: string
      description: >
        BE-14K Tenant Communication status vocabulary. `status` is
        server-assigned and is never caller-supplied or client-mutable:
        `DRAFT` -> `SENT` happens only via
        `POST /tenant-communications/{id}/send`, and `SENT` -> `READ` only via
        `POST /tenant-communications/{id}/read`. There is no re-open, recall or
        deletion state.
      enum: [DRAFT, SENT, READ]

    TenantCommunicationRelatedType:
      type: string
      description: >
        Type of the Tenant record a communication is related to. The concrete
        related record id is exposed as the single derived `relatedId` field;
        `relatedType` and `relatedId` are set together at creation and are
        immutable afterwards. This is a relationship link only - it does not
        copy, move or generate anything on the related record.
      enum: [SERVICE_REQUEST, COMPLAINT, UTILITY_REQUEST, DOCUMENT]

    TenantCommunication:
      type: object
      description: >
        BE-14K Tenant Communication record. `clientId` is derived from the
        Tenant Company and `senderUserId` from the authenticated session -
        neither is caller-supplied. `relatedId` is the derived convenience form
        of the four explicit bindings below: the runtime resolves the first
        non-null of `serviceRequestId`, `complaintId`, `utilityRequestId`,
        `documentId`. This DTO contains no delivery, receipt, read-receipt,
        thread or attachment field.
      required:
        [id, clientId, tenantCompanyId, buildingId, senderUserId,
         recipientTenantPicId, recipientUserId, communicationType, subject,
         messageBody, relatedType, serviceRequestId, complaintId,
         utilityRequestId, documentId, status, relatedId, sentAt, readAt,
         createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        tenantCompanyId: { $ref: "#/components/schemas/Uuid" }
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Optional Building context supplied at creation.
        senderUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: The authenticated User who created the record.
        recipientTenantPicId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Addressed Tenant PIC recipient. At least one of
            `recipientTenantPicId` / `recipientUserId` is non-null on every
            record.
        recipientUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Addressed internal User recipient.
        communicationType:
          type: string
          description: >
            Data-driven communication type code (no hardcoded model), validated
            on intake with the shared data-driven code rules.
        subject:
          type: string
          maxLength: 255
          description: Required at creation; trimmed and must not be empty.
        messageBody:
          type: string
          maxLength: 5000
          description: >
            Required at creation; trimmed and must not be empty. This is the
            recorded body only - nothing is delivered anywhere.
        relatedType:
          allOf: [{ $ref: "#/components/schemas/TenantCommunicationRelatedType" }]
          nullable: true
          description: Null unless the communication was related at creation.
        serviceRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Binding to the related Service Request when `relatedType` is
            `SERVICE_REQUEST`.
        complaintId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Binding to the related Complaint when `relatedType` is `COMPLAINT`.
        utilityRequestId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Binding to the related Utility Request when `relatedType` is
            `UTILITY_REQUEST`.
        documentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Binding to the related Document when `relatedType` is `DOCUMENT`.
        status: { $ref: "#/components/schemas/TenantCommunicationStatus" }
        relatedId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Derived read-only id of the related record (first non-null of the
            four bindings above). Not accepted on any payload.
        sentAt:
          type: string
          format: date-time
          nullable: true
          description: >
            Recorded by `POST /tenant-communications/{id}/send`; null until then.
        readAt:
          type: string
          format: date-time
          nullable: true
          description: >
            Recorded by `POST /tenant-communications/{id}/read`; null until then.
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateTenantCommunicationRequest:
      type: object
      description: >
        BE-14K payload for
        `POST /tenant-companies/{tenantCompanyId}/communications`.
        `tenantCompanyId` comes from the path, `clientId` is derived from the
        Tenant Company and `senderUserId` from the session - none may be
        supplied here. `status`, `sentAt` and `readAt` are server-assigned and
        are not accepted. At least one of `recipientTenantPicId` /
        `recipientUserId` is required, and `relatedType` and `relatedId` must be
        supplied together.
      required: [communicationType, subject, messageBody]
      properties:
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Optional. Must be an ACTIVE Building context of the Tenant, else 400
            `TENANT_COMMUNICATION_CONTEXT_INVALID`.
        recipientTenantPicId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Addressed Tenant PIC recipient; must belong to the Tenant and the
            requested context, else 400
            `TENANT_COMMUNICATION_RECIPIENT_INVALID`.
        recipientUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Addressed internal User recipient; must belong to the Tenant and the
            requested context, else 400
            `TENANT_COMMUNICATION_RECIPIENT_INVALID`. At least one recipient
            field is required, else 400 `VALIDATION_ERROR`.
        communicationType:
          type: string
          description: >
            Required data-driven communication type code, validated with the
            shared data-driven code rules; an invalid value yields 400
            `VALIDATION_ERROR`.
        subject:
          type: string
          maxLength: 255
          description: Required; trimmed and must not be empty.
        messageBody:
          type: string
          maxLength: 5000
          description: Required; trimmed and must not be empty.
        relatedType:
          allOf: [{ $ref: "#/components/schemas/TenantCommunicationRelatedType" }]
          description: >
            Optional relation type. Must be supplied together with `relatedId`,
            else 400 `VALIDATION_ERROR`; the related record must belong to the
            same context, else 400 `TENANT_COMMUNICATION_RELATED_INVALID`.
        relatedId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Optional related record id; required to be supplied together with
            `relatedType`.

    UpdateTenantCommunicationRequest:
      type: object
      description: >
        BE-14K edit payload for `PATCH /tenant-communications/{id}`. Only these
        three fields are mutable and all are optional; only the fields present
        are changed. The communication must be `DRAFT`, else 400
        `TENANT_COMMUNICATION_NOT_DRAFT`. Supplying `clientId`,
        `tenantCompanyId`, `buildingId`, `senderUserId`, `recipientTenantPicId`,
        `recipientUserId`, `relatedType`, `relatedId`, `status`, `sentAt` or
        `readAt` is rejected with 400 `VALIDATION_ERROR` as an immutable field.
      properties:
        communicationType:
          type: string
          description: >
            Data-driven communication type code, validated with the shared
            data-driven code rules; an invalid value yields 400
            `VALIDATION_ERROR`.
        subject:
          type: string
          maxLength: 255
          description: Trimmed and must not be empty.
        messageBody:
          type: string
          maxLength: 5000
          description: Trimmed and must not be empty.

    # ------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 02E2 - J10 Work Request (BE-08A)
    # schemas. Shapes mirror the runtime public DTOs exactly. This is the
    # intake channel that exists before a formal Work Order: it carries no
    # Asset, priority, assignee, execution, evidence, approval or SLA field.
    # ------------------------------------------------------------------

    WorkRequestStatus:
      type: string
      description: >
        BE-08A Work Request status vocabulary. A Work Request is created `OPEN`;
        `OPEN` -> `CANCELLED` happens via `POST /work-requests/{id}/cancel`.
        `CONVERTED` means a Work Order has been created from the request in the
        Work Order domain (BE-08B) and is terminal for this channel - no route on
        this surface sets or reverses it. There is no re-open and no delete.
      enum: [OPEN, CANCELLED, CONVERTED]

    WorkRequest:
      type: object
      description: >
        BE-08A Work Request. `buildingId` comes from the intake path and
        `requestedByUserId` from the authenticated session - the requester is
        never caller-supplied. `status` and `requestedAt` are server-assigned.
        `requestType` is a data-driven code; `description` is nullable.
      required:
        [id, clientId, buildingId, requestNumber, title, description, requestType,
         requestedByUserId, requestedAt, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            The Client supplied on intake; it must resolve to the Building's own
            Client, else the create is rejected.
        buildingId: { $ref: "#/components/schemas/Uuid" }
        requestNumber:
          type: string
          description: >
            Trimmed and uppercased on intake; must start with a letter and
            contain only uppercase letters, digits, hyphens and underscores
            (2-64 characters). Unique per Client.
        title:
          type: string
          maxLength: 200
          description: Required; trimmed and must not be empty.
        description:
          type: string
          maxLength: 1000
          nullable: true
          description: >
            Optional free text; null when not supplied. Cleared by sending
            `null` on update.
        requestType:
          type: string
          description: >
            Data-driven request type code (no hardcoded model). Trimmed and
            uppercased on intake; must start with a letter and contain only
            uppercase letters, digits, hyphens and underscores (2-64
            characters).
        requestedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            The authenticated User who created the request; taken from the
            session.
        requestedAt: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/WorkRequestStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateWorkRequestRequest:
      type: object
      description: >
        BE-08A intake payload for
        `POST /buildings/{buildingId}/work-requests`. `buildingId` comes from the
        path and `requestedByUserId` from the session - neither may be supplied
        here. `clientId` IS supplied by the caller and must match the Building's
        Client through its Property, else 400
        `WORK_REQUEST_BUILDING_CLIENT_MISMATCH`.
      required: [clientId, requestNumber, title, requestType]
      properties:
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Required. Must resolve, through the path Building's Property, to the
            same Client, else 400 `WORK_REQUEST_BUILDING_CLIENT_MISMATCH`.
        requestNumber:
          type: string
          minLength: 2
          maxLength: 64
          description: >
            Required. Trimmed and uppercased; must start with a letter and
            contain only uppercase letters, digits, hyphens and underscores.
            Unique per Client, else 409 `WORK_REQUEST_NUMBER_ALREADY_EXISTS`.
        title:
          type: string
          maxLength: 200
          description: Required; trimmed and must not be empty.
        description:
          type: string
          maxLength: 1000
          description: >
            Optional; trimmed. An empty or whitespace-only value is treated as
            absent, and `null` is accepted and stored as absent.
        requestType:
          type: string
          minLength: 2
          maxLength: 64
          description: >
            Required. Data-driven code, trimmed and uppercased; must start with a
            letter and contain only uppercase letters, digits, hyphens and
            underscores.

    UpdateWorkRequestRequest:
      type: object
      description: >
        BE-08A edit payload for `PATCH /work-requests/{id}`. Only these three
        fields are mutable and all are optional; only the fields present are
        changed. The request must be `OPEN`, else 400 `WORK_REQUEST_NOT_OPEN`.
        `requestNumber`, `clientId`, `buildingId`, `requestedByUserId`,
        `requestedAt` and `status` are not part of this DTO; any other field
        present in the payload is ignored by the update parser.
      properties:
        title:
          type: string
          maxLength: 200
          description: Trimmed and must not be empty.
        description:
          type: string
          maxLength: 1000
          nullable: true
          description: "`null` clears the description."
        requestType:
          type: string
          minLength: 2
          maxLength: 64
          description: >
            Data-driven code, trimmed and uppercased; must start with a letter
            and contain only uppercase letters, digits, hyphens and underscores.

    # ------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03A - J11 Permit (BE-20A)
    # schemas. Shapes mirror the runtime public DTOs exactly. This is the
    # foundation Permit record only - no application, approval, safety,
    # validity, worker, equipment, evidence or lifecycle field beyond
    # DRAFT/CANCELLED.
    # ------------------------------------------------------------------

    PermitStatus:
      type: string
      description: >
        BE-20A Permit status vocabulary. A Permit is created `DRAFT`;
        `DRAFT` -> `CANCELLED` happens via `POST /permits/{id}/cancel`.
        Only a `DRAFT` Permit can be updated or cancelled. There is no
        re-open, approval or execution state on this surface.
      enum: [DRAFT, CANCELLED]

    PermitContractorContextType:
      type: string
      description: >
        BE-20A contractor context discriminator. `VENDOR_CONTRACTOR` is a
        directly engaged Vendor; `TENANT_CONTRACTOR` is a Vendor engaged
        through a BE-14I Tenant Contractor relationship.
      enum: [VENDOR_CONTRACTOR, TENANT_CONTRACTOR]

    PermitWorkLocationType:
      type: string
      description: >
        BE-20D work-context location type. Present here only as a read-only
        filter value for `GET /permits?locationType=`; no work-context
        mutation is performed by the Permit foundation.
      enum: [BUILDING, FLOOR, AREA, ROOM, FUNCTIONAL_LOCATION]

    PermitValidityStatus:
      type: string
      description: >
        BE-20G current-validity status. Present here only as a read-only
        filter value for `GET /permits?validityStatus=` and `?validAt=`; no
        validity mutation is performed by the Permit foundation.
      enum: [PENDING, VALID, EXPIRED, REVOKED]

    Permit:
      type: object
      description: >
        BE-20A Permit - the single shared foundation for Vendor and Tenant
        Contractor permits. `clientId` and `buildingId` are resolved by the
        BE-20B contractor resolver and are never caller-supplied.
        `contractorContextId` is the source id supplied on create; the
        resolved `contractorVendorId` and `tenantContractorRelationshipId`
        are authoritative. `status` and `requestedAt` are server-assigned;
        `cancelledAt`/`cancelledByUserId` are set only on cancellation.
      required:
        [id, clientId, buildingId, permitNumber, permitType, title,
         workDescription, applicantReference, contractorContextType,
         contractorContextId, contractorVendorId,
         tenantContractorRelationshipId, status, requestedAt,
         createdByUserId, cancelledAt, cancelledByUserId, createdAt,
         updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Resolved Client from the contractor context.
        buildingId: { $ref: "#/components/schemas/Uuid" }
        permitNumber:
          type: string
          description: >
            Trimmed and uppercased on intake; must match
            `^[A-Z0-9][A-Z0-9_\-/]{0,99}$` (1-100 chars, first char
            alphanumeric). Unique per Client.
        permitType:
          type: string
          description: >
            Data-driven permit type code. Trimmed and uppercased; must match
            `^[A-Z][A-Z0-9_-]{0,99}$` (1-100 chars, starts with a letter).
        title:
          type: string
          maxLength: 200
          description: Required; trimmed and must not be empty.
        workDescription:
          type: string
          maxLength: 4000
          description: Required; trimmed and must not be empty.
        applicantReference:
          type: string
          maxLength: 200
          nullable: true
          description: Optional free text; null when not supplied. Clearable on update with `null`.
        contractorContextType: { $ref: "#/components/schemas/PermitContractorContextType" }
        contractorContextId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Source-specific context id supplied on create. Echoed as
            `tenantContractorRelationshipId` when `contractorContextType` is
            `TENANT_CONTRACTOR`, otherwise as `contractorVendorId`.
        contractorVendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Resolved Vendor id from the contractor context.
        tenantContractorRelationshipId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            BE-14I relationship id when the contractor is tenant-sponsored;
            null for `VENDOR_CONTRACTOR`.
        status: { $ref: "#/components/schemas/PermitStatus" }
        requestedAt: { type: string, format: date-time }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        cancelledAt:
          type: string
          format: date-time
          nullable: true
          description: Set by `POST /permits/{id}/cancel`; null until then.
        cancelledByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Actor who cancelled the Permit; null until then.
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreatePermitRequest:
      type: object
      description: >
        BE-20A create payload for `POST /permits`. `buildingId` is the
        intended Building and must be consistent with the resolved contractor
        context; `clientId`, `status`, `requestedAt`, `createdByUserId`,
        `cancelledAt`, `cancelledByUserId`, `createdAt` and `updatedAt`
        must not be supplied (400 `VALIDATION_ERROR`). The resolved
        `clientId`/`buildingId`/`contractorVendorId`/
        `tenantContractorRelationshipId` are server-derived.
      required:
        [buildingId, permitNumber, permitType, title, workDescription,
         contractorContextType, contractorContextId]
      properties:
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Required. Building where the work will be performed; must be an
            ACTIVE Building and must match the contractor context's Building.
        permitNumber:
          type: string
          minLength: 1
          maxLength: 100
          description: >
            Required. Trimmed and uppercased; must match
            `^[A-Z0-9][A-Z0-9_\-/]{0,99}$`. Unique per Client, else 409
            `PERMIT_NUMBER_ALREADY_EXISTS`.
        permitType:
          type: string
          minLength: 1
          maxLength: 100
          description: >
            Required. Data-driven code; trimmed and uppercased; must match
            `^[A-Z][A-Z0-9_-]{0,99}$` (starts with a letter).
        title:
          type: string
          maxLength: 200
          description: Required; trimmed and must not be empty.
        workDescription:
          type: string
          maxLength: 4000
          description: Required; trimmed and must not be empty.
        applicantReference:
          type: string
          maxLength: 200
          nullable: true
          description: Optional; trimmed. `null` is accepted.
        contractorContextType: { $ref: "#/components/schemas/PermitContractorContextType" }
        contractorContextId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Required. Vendor id for `VENDOR_CONTRACTOR`; BE-14I relationship
            id for `TENANT_CONTRACTOR`.

    UpdatePermitRequest:
      type: object
      description: >
        BE-20A edit payload for `PATCH /permits/{id}`. Only these four
        fields are mutable and all are optional; only the fields present are
        changed. At least one must be supplied, else 400 `VALIDATION_ERROR`.
        `permitNumber`, `clientId`, `buildingId`, contractor context fields,
        `status`, `requestedAt`, `createdByUserId`, `cancelledAt`,
        `cancelledByUserId`, `createdAt` and `updatedAt` are immutable -
        supplying them yields 400 `VALIDATION_ERROR`. The Permit must be
        `DRAFT`, else 400 `PERMIT_UPDATE_NOT_ALLOWED`.
      minProperties: 1
      properties:
        permitType:
          type: string
          minLength: 1
          maxLength: 100
          description: >
            Data-driven code; trimmed and uppercased; must match
            `^[A-Z][A-Z0-9_-]{0,99}$` (starts with a letter).
        title:
          type: string
          maxLength: 200
          description: Trimmed and must not be empty.
        workDescription:
          type: string
          maxLength: 4000
          description: Trimmed and must not be empty.
        applicantReference:
          type: string
          maxLength: 200
          nullable: true
          description: "`null` clears the reference."

    # ------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03B - J11 Permit Application (BE-20C)
    # schemas. Shapes mirror the runtime public DTOs exactly. This is the
    # application layer onto a BE-20A Permit - no approval, safety, validity
    # or execution field is carried here.
    # ------------------------------------------------------------------

    PermitApplicationStatus:
      type: string
      description: >
        BE-20C Permit Application status vocabulary. An Application is created
        `DRAFT`; `DRAFT` -> `SUBMITTED` happens via `POST
        /permit-applications/{id}/submit`; `DRAFT`|`SUBMITTED` ->
        `CANCELLED` happens via `POST /permit-applications/{id}/cancel`.
        Only a `DRAFT` Application can be updated or submitted.
      enum: [DRAFT, SUBMITTED, CANCELLED]

    PermitApplication:
      type: object
      description: >
        BE-20C Permit Application with its authoritative BE-20A Permit
        projection. Permit, applicant, contractor, Client, Building and
        work-description fields are selected through the Permit reference and
        are not duplicated in the application row. `permitReference` is the
        Permit's `permitNumber`; `workDescription` is the Permit's resolved
        description projected into the application.
      required:
        [id, permitId, permitReference, permitStatus, clientId, buildingId,
         applicantReference, contractorContextType, contractorContextId,
         contractorVendorId, tenantContractorRelationshipId, workDescription,
         requestedWorkAt, status, submittedAt, submittedByUserId, notes,
         createdByUserId, cancelledAt, cancelledByUserId, createdAt,
         updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        permitId: { $ref: "#/components/schemas/Uuid" }
        permitReference:
          type: string
          description: The Permit `permitNumber` projected from BE-20A.
        permitStatus: { $ref: "#/components/schemas/PermitStatus" }
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Authoritative Client from the Permit.
        buildingId: { $ref: "#/components/schemas/Uuid" }
        applicantReference:
          type: string
          nullable: true
          maxLength: 200
          description: Projected from the Permit.
        contractorContextType: { $ref: "#/components/schemas/PermitContractorContextType" }
        contractorContextId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Source-specific context id projected from the Permit.
        contractorVendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Resolved Vendor id projected from the Permit.
        tenantContractorRelationshipId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        workDescription:
          type: string
          maxLength: 4000
          description: Resolved work description projected from the Permit.
        requestedWorkAt:
          type: string
          format: date-time
          description: Future instant when the work is requested to start.
        status: { $ref: "#/components/schemas/PermitApplicationStatus" }
        submittedAt:
          type: string
          format: date-time
          nullable: true
          description: Set by `POST /permit-applications/{id}/submit`; null until then.
        submittedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Actor who submitted the Application.
        notes:
          type: string
          maxLength: 2000
          nullable: true
          description: Optional free text; null when not supplied.
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        cancelledAt:
          type: string
          format: date-time
          nullable: true
          description: Set by `POST /permit-applications/{id}/cancel`; null until then.
        cancelledByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Actor who cancelled the Application.
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreatePermitApplicationRequest:
      type: object
      description: >
        BE-20C create payload for `POST /permit-applications`. `permitId` is
        the only required reference - `requestedWorkAt` is required and must be
        a future ISO-8601 date-time with timezone; optional `notes`,
        `applicantReference`, `workDescription`, `contractorContextType` and
        `contractorContextId` are assertions validated against the Permit
        (mismatch yields 400 `PERMIT_APPLICATION_CONTRACTOR_INVALID`).
        `contractorContextType` and `contractorContextId` must be supplied
        together (400 `VALIDATION_ERROR` otherwise). Server-controlled fields
        `clientId`, `buildingId`, `permitReference`, `permitStatus`, `status`,
        `submittedAt`, `submittedByUserId`, `cancelledAt`,
        `cancelledByUserId`, `createdByUserId`, `createdAt`, `updatedAt` must
        not be supplied.
      required: [permitId, requestedWorkAt]
      properties:
        permitId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Required. Must reference an existing BE-20A Permit.
        requestedWorkAt:
          type: string
          format: date-time
          description: >
            Required. Must be a future ISO-8601 date-time with timezone
            (e.g. 2026-09-16T09:00:00Z). Must be a valid calendar date.
        notes:
          type: string
          maxLength: 2000
          nullable: true
          description: Optional; trimmed. Empty is stored as null.
        applicantReference:
          type: string
          maxLength: 200
          nullable: true
          description: >
            Optional assertion. When supplied it is validated against the
            Permit's `applicantReference`.
        workDescription:
          type: string
          maxLength: 4000
          description: >
            Optional assertion. When supplied it is validated against the
            Permit's `workDescription`.
        contractorContextType:
          allOf: [{ $ref: "#/components/schemas/PermitContractorContextType" }]
          description: >
            Optional assertion. Must be supplied together with
            `contractorContextId`; validated against the Permit's contractor
            context.
        contractorContextId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Optional assertion. Must be supplied together with
            `contractorContextType`.

    UpdatePermitApplicationRequest:
      type: object
      description: >
        BE-20C edit payload for `PATCH /permit-applications/{id}`. Only
        `requestedWorkAt` and `notes` are mutable and at least one must be
        supplied (400 `VALIDATION_ERROR` otherwise). `requestedWorkAt` must
        be a future ISO-8601 date-time with timezone. `permitId`,
        `permitReference`, `clientId`, `buildingId`, `applicantReference`,
        contractor context fields, `workDescription`, `status`,
        `submittedAt`, `submittedByUserId`, `cancelledAt`,
        `cancelledByUserId`, `createdByUserId`, `createdAt` and `updatedAt`
        are inherited/controlled and must not be supplied. The Application must
        be `DRAFT`, else 400 `PERMIT_APPLICATION_UPDATE_NOT_ALLOWED`.
      minProperties: 1
      properties:
        requestedWorkAt:
          type: string
          format: date-time
          description: >
            Optional. When supplied it must be a future ISO-8601 date-time with
            timezone; past or present instants yield 400
            `PERMIT_APPLICATION_INVALID_WORK_DATE` on submit.
        notes:
          type: string
          maxLength: 2000
          nullable: true
          description: "`null` clears the notes."

    # ------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03C - J11 Permit Approval (BE-20F)
    # schemas. Shapes mirror the runtime public DTOs exactly. Thin binding
    # over shared reviews; no generic approval engine.
    # ------------------------------------------------------------------

    PermitApprovalStatus:
      type: string
      description: >
        BE-20F Permit Approval status vocabulary. Derived from the shared
        review decision. `PENDING` (review PENDING), `APPROVED` (decision
        APPROVED), `REJECTED` (REJECTED), `REWORK_REQUIRED` (REWORK_REQUIRED).
        Only `PENDING` may be decided.
      enum: [PENDING, APPROVED, REJECTED, REWORK_REQUIRED]

    PermitApprovalAction:
      type: string
      description: >
        BE-20F caller-specific approval action token. Resolved by
        `GET /permit-approvals/{id}/available-actions`; an empty list means
        nothing is offered. `APPROVE` needs optional notes, `REJECT` and
        `REQUEST_REWORK` need required notes.
      enum: [APPROVE, REJECT, REQUEST_REWORK]

    PermitApproval:
      type: object
      description: >
        BE-20F Permit Approval - thin Permit binding over a shared `reviews`
        row. `permitApplicationId`/`permitId`/`permitReference`/`clientId`/
        `buildingId`/`applicationStatus` are projections from the Application/Permit;
        `approvalStage`/`approvalType` (data-driven codes) are unique per
        Application; `approverUserId` is the assigned approver; `reviewId` is
        the shared review; `reviewStatus` PENDING|COMPLETED and `decision`
        APPROVED|REJECTED|REWORK_REQUIRED live on the review. `approvalStatus`
        is the derived enum (PENDING/APPROVED/REJECTED/REWORK_REQUIRED).
      required:
        [id, reviewId, permitApplicationId, permitId, permitReference,
         clientId, buildingId, contractorContextType, contractorVendorId,
         applicationStatus, permitStatus, approvalStage, approvalType,
         approverUserId, reviewStatus, decision, approvalStatus, decisionAt,
         decisionNotes, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        reviewId: { $ref: "#/components/schemas/Uuid" }
        permitApplicationId: { $ref: "#/components/schemas/Uuid" }
        permitId: { $ref: "#/components/schemas/Uuid" }
        permitReference:
          type: string
          description: Permit `permitNumber` projected from BE-20A.
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        contractorContextType:
          type: string
          description: Projected contractor context type.
        contractorVendorId: { $ref: "#/components/schemas/Uuid" }
        applicationStatus: { $ref: "#/components/schemas/PermitApplicationStatus" }
        permitStatus: { $ref: "#/components/schemas/PermitStatus" }
        approvalStage:
          type: string
          description: >
            Data-driven stage code (trimmed uppercased, 2-64 chars). Unique
            with `approvalType` per Application.
        approvalType:
          type: string
          description: >
            Data-driven type code (trimmed uppercased, 2-64 chars). Unique
            with `approvalStage` per Application.
        approverUserId: { $ref: "#/components/schemas/Uuid" }
        reviewStatus:
          type: string
          enum: [PENDING, COMPLETED]
          description: Shared review status.
        decision:
          type: string
          nullable: true
          enum: [APPROVED, REJECTED, REWORK_REQUIRED]
          description: Shared review decision; null when `reviewStatus` is PENDING.
        approvalStatus: { $ref: "#/components/schemas/PermitApprovalStatus" }
        decisionAt:
          type: string
          format: date-time
          nullable: true
          description: Review decision instant; null until decided.
        decisionNotes:
          type: string
          nullable: true
          maxLength: 4000
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreatePermitApprovalRequest:
      type: object
      description: >
        BE-20F create payload for
        `POST /permit-applications/{applicationId}/approvals`. Requires
        `approvalStage`, `approvalType` (data-driven codes, trimmed uppercased,
        2-64 chars, must match valid request-type pattern, unique per
        Application) and `approverUserId` (must already hold `permit.approve`
        and have ACTIVE Building assignment). Optional `notes`.
      required: [approvalStage, approvalType, approverUserId]
      properties:
        approvalStage:
          type: string
          minLength: 2
          maxLength: 64
          description: >
            Required data-driven stage code. Trimmed uppercased; must start with
            a letter and contain only uppercase letters, digits, hyphens and
            underscores.
        approvalType:
          type: string
          minLength: 2
          maxLength: 64
          description: >
            Required data-driven type code. Same rules as `approvalStage`.
        approverUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Required. Must be an active User who holds `permit.approve` and has
            an ACTIVE assignment to the Application's Building.
        notes:
          type: string
          maxLength: 4000
          nullable: true
          description: Optional; trimmed. Empty is stored as null.

    PermitApprovalDecisionRequest:
      type: object
      description: >
        BE-20F decision payload for approve/reject/request-rework. `approve`
        accepts an empty body or optional `decisionNotes`/`notes` (alias);
        `reject` and `request-rework` require `decisionNotes`/`notes`
        (400 `VALIDATION_ERROR` when absent). Values are trimmed; empty
        becomes null except when required.
      properties:
        decisionNotes:
          type: string
          maxLength: 4000
          nullable: true
          description: >
            Optional for approve, required for reject/request-rework. Alias
            `notes` is accepted and must equal `decisionNotes` when both are
            sent; trimmed; empty becomes null (or 400 when required).
        notes:
          type: string
          maxLength: 4000
          nullable: true
          description: Alias for `decisionNotes`.

    PermitApprovalAvailableActions:
      type: object
      description: >
        Backend-authoritative available actions for one Permit Approval.
        `availableActions` is the caller-specific subset of
        `[APPROVE, REJECT, REQUEST_REWORK]` offered now; an empty list is
        authoritative. `safetyReadinessStatus` reflects BE-20E readiness
        (`readinessStatus` from the BE-20E read model, e.g. `READY` when the
        Application's Permit is ready for approval).
      required: [approvalId, approvalStatus, applicationStatus, safetyReadinessStatus, availableActions]
      properties:
        approvalId: { $ref: "#/components/schemas/Uuid" }
        approvalStatus: { $ref: "#/components/schemas/PermitApprovalStatus" }
        applicationStatus: { $ref: "#/components/schemas/PermitApplicationStatus" }
        safetyReadinessStatus:
          type: string
          description: >
            BE-20E readiness status for the Application's Permit (e.g. `READY`).
            Exact READY vs not-ready vocabulary is owned by BE-20E.
        availableActions:
          type: array
          items: { $ref: "#/components/schemas/PermitApprovalAction" }

    PermitApprovalContext:
      type: object
      description: >
        BE-20F approval context for one Permit Application. Returns the
        Application's `permitId`/`permitReference`/`buildingId`,
        `applicationStatus`, the computed `safetyReadiness` read model and the
        full list of `approvals` bound to the Application.
      required: [permitId, permitApplicationId, permitReference, buildingId, applicationStatus, safetyReadiness, approvals]
      properties:
        permitId: { $ref: "#/components/schemas/Uuid" }
        permitApplicationId: { $ref: "#/components/schemas/Uuid" }
        permitReference:
          type: string
          description: Permit `permitNumber`.
        buildingId: { $ref: "#/components/schemas/Uuid" }
        applicationStatus: { $ref: "#/components/schemas/PermitApplicationStatus" }
        safetyReadiness:
          type: object
          description: >
            BE-20E safety readiness read model projected for the Application.
            Exact fields beyond `readinessStatus` are owned by BE-20E and are
            not re-typed here.
          required: [readinessStatus]
          properties:
            readinessStatus:
              type: string
              description: >
                BE-20E readiness status (e.g. `READY`). Exact vocabulary is
                owned by BE-20E.
        approvals:
          type: array
          items: { $ref: "#/components/schemas/PermitApproval" }

    # ------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D1 - J11 Permit Equipment (BE-20I)
    # schemas. Shapes mirror the runtime public DTOs exactly. Permit-bound
    # binding over existing Asset / Equipment foundations only.
    # ------------------------------------------------------------------

    PermitEquipmentStatus:
      type: string
      description: >
        BE-20I Permit Equipment status vocabulary. Created `ACTIVE`;
        `ACTIVE` -> `INACTIVE` via deactivation; `INACTIVE` is terminal for
        that entry. Only `ACTIVE` entries are eligible for active-work
        resolution when Permit validity is `VALID`.
      enum: [ACTIVE, INACTIVE]

    PermitEquipment:
      type: object
      description: >
        BE-20I Permit Equipment - permit-bound binding over an existing
        Asset (BE-05) and optional Equipment Profile / Asset Identifier /
        Asset Certification / Inspection binding-execution foundations.
        `permitApplicationId`/`permitId`/`permitReference`/`clientId`/
        `buildingId`/`contractorContextType`/`contractorVendorId` are
        projections from the Permit/Application; `assetId`/`assetCode`/
        `assetName`/`assetStatus`/`equipmentCode`/`equipmentName`/
        `identifierReference`/`certificationReference`/`inspectionStatus` are
        projections from the referenced foundations. `validFrom`/`validUntil`
        are the equipment window contained within the Permit validity.
        `eligibleForActiveWork` is the runtime-computed eligibility for
        `GET /permits/{permitId}/equipment/active`.
      required:
        [id, permitApplicationId, permitId, permitReference, clientId,
         buildingId, contractorContextType, contractorContextId,
         contractorVendorId, assetId, assetCode, assetName, assetStatus,
         assetTypeId, assetTypeCode, assetTypeName, equipmentProfileId,
         equipmentCode, equipmentName, equipmentProfileStatus, equipmentType,
         assetIdentifierId, identifierType, identifierReference,
         identifierStatus, assetCertificationId, certificationType,
         certificationReference, certificationStatus, certificationIssueDate,
         certificationExpiryDate, inspectionBindingId, inspectionBindingStatus,
         inspectionExecutionId, inspectionStatus, status, validFrom,
         validUntil, permitValidityStatus, notes, equipmentAssetReference,
         eligibleForActiveWork, certificationCurrentlyValid,
         inspectionCurrentlyValid, createdByUserId, updatedByUserId,
         deactivatedAt, deactivatedByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        permitApplicationId: { $ref: "#/components/schemas/Uuid" }
        permitId: { $ref: "#/components/schemas/Uuid" }
        permitReference:
          type: string
          description: Permit `permitNumber` projected from BE-20A.
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        contractorContextType: { $ref: "#/components/schemas/PermitContractorContextType" }
        contractorContextId: { $ref: "#/components/schemas/Uuid" }
        contractorVendorId: { $ref: "#/components/schemas/Uuid" }
        assetId: { $ref: "#/components/schemas/Uuid" }
        assetCode: { type: string }
        assetName: { type: string }
        assetStatus: { type: string, description: Asset status projection (e.g. ACTIVE) }
        assetTypeId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        assetTypeCode: { type: string, nullable: true }
        assetTypeName: { type: string, nullable: true }
        equipmentProfileId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        equipmentCode: { type: string, nullable: true }
        equipmentName: { type: string }
        equipmentProfileStatus: { type: string, nullable: true }
        equipmentType: { type: string }
        assetIdentifierId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        identifierType: { type: string, nullable: true }
        identifierReference: { type: string }
        identifierStatus: { type: string, nullable: true }
        assetCertificationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        certificationType: { type: string, nullable: true }
        certificationReference: { type: string, nullable: true }
        certificationStatus: { type: string, nullable: true }
        certificationIssueDate:
          type: string
          format: date
          nullable: true
          description: Certification issue date (date key) or null.
        certificationExpiryDate:
          type: string
          format: date
          nullable: true
          description: Certification expiry date (date key) or null.
        inspectionBindingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        inspectionBindingStatus: { type: string, nullable: true }
        inspectionExecutionId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        inspectionStatus: { type: string, nullable: true }
        status: { $ref: "#/components/schemas/PermitEquipmentStatus" }
        validFrom: { type: string, format: date-time, description: Equipment valid-from (ISO with timezone, contained within Permit validity) }
        validUntil: { type: string, format: date-time, description: Equipment valid-until (ISO with timezone, contained within Permit validity) }
        permitValidityStatus:
          allOf: [{ $ref: "#/components/schemas/PermitValidityStatus" }]
          nullable: true
          description: Current Permit validity status at read time.
        notes:
          type: string
          nullable: true
          maxLength: 2000
        equipmentAssetReference:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Convenience alias for `assetId`.
        eligibleForActiveWork:
          type: boolean
          description: Whether this entry is eligible for active-work resolution.
        certificationCurrentlyValid:
          type: boolean
          nullable: true
          description: Certification validity at read time, null when no certification.
        inspectionCurrentlyValid:
          type: boolean
          nullable: true
          description: Inspection validity at read time, null when no inspection.
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        updatedByUserId: { $ref: "#/components/schemas/Uuid" }
        deactivatedAt:
          type: string
          format: date-time
          nullable: true
          description: Set when deactivated; null until then.
        deactivatedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    AddPermitEquipmentRequest:
      type: object
      description: >
        BE-20I add payload for `POST /permits/{permitId}/equipment`. Requires
        `permitApplicationId`, `buildingId`, `contractorContextType`
        (`TENANT_CONTRACTOR`|`VENDOR_CONTRACTOR`), `contractorContextId` and
        `assetId` (alias `equipmentAssetReference` accepted for `assetId`).
        Optional `equipmentProfileId`, `assetIdentifierId`,
        `assetCertificationId`, `inspectionBindingId`+`inspectionExecutionId`
        (must be supplied together), `validFrom`/`validUntil` (ISO-8601 with
        timezone, ordered and contained within Permit validity; defaults to
        Permit validity when omitted) and `notes`. Backend-resolved fields
        `equipmentName`, `equipmentType`, `identifierReference`, `status`,
        `deactivatedAt`, `deactivatedByUserId`, `createdByUserId`, `createdAt`,
        `updatedAt` must not be supplied.
      required: [permitApplicationId, buildingId, contractorContextType, contractorContextId, assetId]
      properties:
        permitApplicationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Required. Must reference the Permit Application that is `SUBMITTED` and belongs to the path Permit.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Required. Must be the Permit's Building.
        contractorContextType:
          allOf: [{ $ref: "#/components/schemas/PermitContractorContextType" }]
          description: Required. Must match the Permit's contractor context.
        contractorContextId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Required. Must be the Permit's resolved contractor context id.
        assetId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Required. Asset to bind; alias `equipmentAssetReference` is accepted
            by the runtime as an alias for this field.
        equipmentAssetReference:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Alias for `assetId`; when both are supplied they reference the same Asset.
        equipmentProfileId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Optional. When supplied it must match the Asset's Equipment Profile.
        assetIdentifierId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Optional. Must belong to the Asset and be ACTIVE when supplied.
        assetCertificationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Optional. Must be ACTIVE, belong to the Asset and cover the equipment validity window.
        inspectionBindingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Optional. Must be supplied together with `inspectionExecutionId`;
            binding must be ACTIVE and belong to the same Asset/Building.
        inspectionExecutionId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Optional. Must be supplied together with `inspectionBindingId`;
            execution must belong to the binding/Asset and be `COMPLETED`.
        validFrom:
          type: string
          format: date-time
          description: >
            Optional ISO-8601 date-time with timezone; defaults to Permit
            validity `validFrom`. Must be ordered and contained.
        validUntil:
          type: string
          format: date-time
          description: >
            Optional ISO-8601 date-time with timezone; defaults to Permit
            validity `validUntil`. Must be after `validFrom` and within Permit
            validity.
        notes:
          type: string
          maxLength: 2000
          nullable: true
          description: Optional; trimmed. Empty is stored as null.

    UpdatePermitEquipmentRequest:
      type: object
      description: >
        BE-20I edit payload for `PATCH /permit-equipment/{id}`. At least one
        updatable field is required (400 `VALIDATION_ERROR` otherwise). Updatable
        fields are `assetIdentifierId` (nullable, null clears), `assetCertificationId`
        (nullable, null clears), `inspectionBindingId` (nullable, null clears),
        `inspectionExecutionId` (nullable, null clears), `validFrom`,
        `validUntil` (ISO-8601 with timezone, ordered and contained) and `notes`
        (nullable; null clears). The entry must be `ACTIVE` and on an open Permit
        (`PENDING` or `VALID` validity) - otherwise 400
        `PERMIT_EQUIPMENT_UPDATE_NOT_ALLOWED`. Immutable context fields
        (`permitId`, `permitApplicationId`, `buildingId`, `contractorContextType`,
        `contractorContextId`, `contractorVendorId`, `assetId`,
        `equipmentProfileId`, `status`, `deactivatedAt`, `deactivatedByUserId`)
        must not be supplied.
      minProperties: 1
      properties:
        assetIdentifierId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Optional; `null` clears the identifier binding.
        assetCertificationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Optional; `null` clears the certification binding.
        inspectionBindingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Optional; `null` clears the inspection binding.
        inspectionExecutionId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Optional; `null` clears the inspection execution.
        validFrom:
          type: string
          format: date-time
          description: Optional ISO-8601 date-time with timezone.
        validUntil:
          type: string
          format: date-time
          description: Optional ISO-8601 date-time with timezone; must be after `validFrom` when both are present.
        notes:
          type: string
          maxLength: 2000
          nullable: true
          description: "`null` clears the notes."

    PermitActiveEquipmentList:
      type: object
      description: >
        BE-20I active equipment view for one Permit, returned by
        `GET /permits/{permitId}/equipment/active`. `validityStatus` is the
        current Permit validity status (or null); `active` is true only when
        `VALID`. When not `VALID` `equipment` is empty; when `VALID` only
        `eligibleForActiveWork` entries are included.
      required: [permitId, permitReference, buildingId, contractorContextType, contractorVendorId, validityStatus, active, equipmentCount, equipment]
      properties:
        permitId: { $ref: "#/components/schemas/Uuid" }
        permitReference:
          type: string
          description: Permit `permitNumber`.
        buildingId: { $ref: "#/components/schemas/Uuid" }
        contractorContextType: { $ref: "#/components/schemas/PermitContractorContextType" }
        contractorVendorId: { $ref: "#/components/schemas/Uuid" }
        validityStatus:
          allOf: [{ $ref: "#/components/schemas/PermitValidityStatus" }]
          nullable: true
          description: Current Permit validity status, or null when none.
        active:
          type: boolean
          description: True only when `validityStatus` is `VALID`.
        equipmentCount:
          type: integer
          minimum: 0
          description: Number of equipment entries in `equipment`.
        equipment:
          type: array
          items: { $ref: "#/components/schemas/PermitEquipment" }

    # ------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03D2 - J11 Permit Evidence (BE-20J)
    # schemas. Shapes mirror the runtime public DTOs exactly. Permit-bound
    # evidence over BE-07 foundations, fileReference metadata only.
    # ------------------------------------------------------------------

    PermitEvidenceType:
      type: string
      description: >
        BE-20J Permit Evidence type vocabulary. `PHOTO`, `DOCUMENT` or
        `SIGNATURE`. One ACTIVE requirement per (`permitId`, `evidenceType`).
      enum: [PHOTO, DOCUMENT, SIGNATURE]

    PermitEvidenceStatus:
      type: string
      description: >
        BE-20J Permit Evidence status. Submissions are created `ACTIVE` and
        flipped to `REMOVED` via `PATCH /permit-evidence/{id}`; `REMOVED` is
        terminal and an already `REMOVED` entry yields 409
        `PERMIT_EVIDENCE_ALREADY_REMOVED`.
      enum: [ACTIVE, REMOVED]

    PermitEvidenceRequirement:
      type: object
      description: >
        BE-20J Permit Evidence Requirement - per-Permit, per-`evidenceType`
        requirement bound to the Permit's resolved contractor Building context.
        `minimumCount`/`maximumCount` define the required window (required
        `minimumCount` >=1, `maximumCount` null = unbounded, otherwise
        >= `minimumCount`). `required` flag determines readiness.
      required:
        [id, clientId, permitId, permitReference, buildingId,
         contractorContextType, contractorVendorId, evidenceType, required,
         minimumCount, maximumCount, description, status, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        permitId: { $ref: "#/components/schemas/Uuid" }
        permitReference:
          type: string
          description: Permit `permitNumber` projected from BE-20A.
        buildingId: { $ref: "#/components/schemas/Uuid" }
        contractorContextType: { $ref: "#/components/schemas/PermitContractorContextType" }
        contractorVendorId: { $ref: "#/components/schemas/Uuid" }
        evidenceType: { $ref: "#/components/schemas/PermitEvidenceType" }
        required: { type: boolean }
        minimumCount:
          type: integer
          minimum: 0
        maximumCount:
          type: integer
          minimum: 0
          nullable: true
          description: Null means unbounded.
        description:
          type: string
          nullable: true
          maxLength: 2000
        status:
          type: string
          description: Requirement status (runtime `ACTIVE`).
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreatePermitEvidenceRequirementRequest:
      type: object
      description: >
        BE-20J create-requirement payload for
        `POST /permits/{permitId}/evidence-requirements`. Requires
        `buildingId`, `contractorContextType` (`TENANT_CONTRACTOR`|
        `VENDOR_CONTRACTOR`), `contractorContextId` and `evidenceType`.
        `required` defaults to `true`, `minimumCount` defaults to `1` when
        required and `0` otherwise; `maximumCount` null = unbounded otherwise
        must be >= `minimumCount`. `description` optional (2000 chars).
      required: [buildingId, contractorContextType, contractorContextId, evidenceType]
      properties:
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Required. Must be the Permit's Building.
        contractorContextType:
          allOf: [{ $ref: "#/components/schemas/PermitContractorContextType" }]
          description: Required. Must match the Permit's contractor context.
        contractorContextId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Required. Must be the Permit's resolved contractor context id.
        evidenceType: { $ref: "#/components/schemas/PermitEvidenceType" }
        required:
          type: boolean
          description: Defaults to `true` when omitted.
        minimumCount:
          type: integer
          minimum: 0
          description: Defaults to `1` when `required` true else `0`.
        maximumCount:
          type: integer
          minimum: 0
          nullable: true
          description: Null or omitted means unbounded; when set must be >= `minimumCount`.
        description:
          type: string
          maxLength: 2000
          nullable: true
          description: Optional; trimmed. Empty is stored as null.

    PermitEvidence:
      type: object
      description: >
        BE-20J Permit Evidence submission - file-reference metadata bound to
        one Requirement and one Permit. `fileReference` is a storage reference
        string (not a multipart upload), plus `originalFileName`, `mimeType`,
        `fileSize` and optional `capturedAt`. `evidenceType` must match the
        Requirement.
      required:
        [id, clientId, permitId, permitReference, buildingId,
         contractorContextType, contractorVendorId, evidenceRequirementId,
         evidenceType, fileReference, originalFileName, mimeType, fileSize,
         capturedAt, submittedByUserId, submittedAt, status, createdAt,
         updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        permitId: { $ref: "#/components/schemas/Uuid" }
        permitReference:
          type: string
          description: Permit `permitNumber`.
        buildingId: { $ref: "#/components/schemas/Uuid" }
        contractorContextType: { $ref: "#/components/schemas/PermitContractorContextType" }
        contractorVendorId: { $ref: "#/components/schemas/Uuid" }
        evidenceRequirementId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Requirement binding; null when requirement was removed.
        evidenceType: { $ref: "#/components/schemas/PermitEvidenceType" }
        fileReference:
          type: string
          maxLength: 512
          description: Storage reference string (metadata only, not a binary body).
        originalFileName:
          type: string
          maxLength: 255
        mimeType:
          type: string
          maxLength: 255
        fileSize:
          type: integer
          minimum: 0
          maximum: 52428800
        capturedAt:
          type: string
          format: date-time
          nullable: true
          description: Optional ISO-8601 date-time with timezone.
        submittedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        submittedAt: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/PermitEvidenceStatus" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    SubmitPermitEvidenceRequest:
      type: object
      description: >
        BE-20J submit payload for `POST /permits/{permitId}/evidence`.
        Requires `buildingId`, `contractorContextType`,
        `contractorContextId`, `evidenceRequirementId`, `evidenceType`,
        `fileReference` (512 chars), `originalFileName` (255), `mimeType`
        (255) and `fileSize` (0..52428800 integer). Optional `capturedAt`
        ISO-8601 with timezone. `evidenceType` must match the Requirement and
        the count must not exceed `maximumCount`.
      required:
        [buildingId, contractorContextType, contractorContextId,
         evidenceRequirementId, evidenceType, fileReference,
         originalFileName, mimeType, fileSize]
      properties:
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Required. Must be the Permit's Building.
        contractorContextType:
          allOf: [{ $ref: "#/components/schemas/PermitContractorContextType" }]
        contractorContextId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Required contractor context id.
        evidenceRequirementId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Required. Must belong to the Permit.
        evidenceType: { $ref: "#/components/schemas/PermitEvidenceType" }
        fileReference:
          type: string
          maxLength: 512
          description: Required storage reference string.
        originalFileName:
          type: string
          maxLength: 255
          description: Required original file name.
        mimeType:
          type: string
          maxLength: 255
          description: Required MIME type.
        fileSize:
          type: integer
          minimum: 0
          maximum: 52428800
          description: Required file size in bytes (0..52428800).
        capturedAt:
          type: string
          format: date-time
          description: Optional ISO-8601 date-time with timezone.

    PermitEvidenceReadinessDetail:
      type: object
      description: >
        BE-20J per-requirement readiness detail for the evidence readiness
        view. `activeCount` counts `ACTIVE` submissions for the requirement;
        `satisfied` is true when `required` is false or
        `activeCount >= minimumCount`.
      required: [evidenceRequirementId, evidenceType, required, minimumCount, maximumCount, activeCount, satisfied]
      properties:
        evidenceRequirementId: { $ref: "#/components/schemas/Uuid" }
        evidenceType: { $ref: "#/components/schemas/PermitEvidenceType" }
        required: { type: boolean }
        minimumCount:
          type: integer
          minimum: 0
        maximumCount:
          type: integer
          nullable: true
          minimum: 0
        activeCount:
          type: integer
          minimum: 0
        satisfied: { type: boolean }

    PermitEvidenceReadiness:
      type: object
      description: >
        BE-20J Permit Evidence readiness view for one Permit, returned by
        `GET /permits/{permitId}/evidence-readiness`. `configured` is false
        when no requirements exist; `ready` is true only when every `required`
        requirement is `satisfied`; `missingEvidenceTypes` lists the
        `evidenceType`s that are still unsatisfied.
      required: [permitId, permitReference, buildingId, contractorContextType, contractorVendorId, configured, ready, missingEvidenceTypes, requirements]
      properties:
        permitId: { $ref: "#/components/schemas/Uuid" }
        permitReference: { type: string }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        contractorContextType: { $ref: "#/components/schemas/PermitContractorContextType" }
        contractorVendorId: { $ref: "#/components/schemas/Uuid" }
        configured: { type: boolean }
        ready: { type: boolean }
        missingEvidenceTypes:
          type: array
          items: { $ref: "#/components/schemas/PermitEvidenceType" }
        requirements:
          type: array
          items: { $ref: "#/components/schemas/PermitEvidenceReadinessDetail" }

    # ------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03E1 - J11 Permit Safety Requirements (BE-20E)
    # schemas. Shapes mirror the runtime public DTOs exactly. PTW
    # prerequisites only; not a general HSE platform.
    # ------------------------------------------------------------------

    PermitSafetyReadinessStatus:
      type: string
      description: >
        BE-20E Permit Safety Requirement readiness vocabulary. Requirements are
        created with `readinessStatus` default `PENDING`; individual readiness
        is updated via `PATCH /safety-requirements/{id}/readiness`. The view
        `GET /permits/{permitId}/safety-readiness` aggregates to
        `NOT_CONFIGURED`|`PENDING`|`READY`|`NOT_READY`.
      enum: [PENDING, READY, NOT_READY, NOT_REQUIRED]

    PermitSafetyRequirement:
      type: object
      description: >
        BE-20E Permit Safety Requirement - a PTW prerequisite bound to a
        `permitApplicationId`/`permitId`/`permitReference`/`clientId`/
        `buildingId`/`workType` and a data-driven `requirementType` (unique
        per Application) with `requirementDescription`. `required` defaults to
        true; `readinessStatus` is the stored readiness (`PENDING` etc) and
        `resolvedReadinessStatus` is the same value resolved with shared-control
        fallbacks; `checklistStatus` and `evidenceReady` are nullable derived
        from the shared controls when present. `notes`, `reference`,
        `checklistTemplateId`, `checklistExecutionId`, `evidenceRequirementId`
        are nullable bindings revalidated as shared controls.
      required:
        [id, permitApplicationId, permitId, permitReference, clientId,
         buildingId, applicationStatus, workType, currentWorkType,
         requirementType, requirementDescription, required, readinessStatus,
         resolvedReadinessStatus, notes, reference, checklistTemplateId,
         checklistExecutionId, evidenceRequirementId, checklistStatus,
         evidenceReady, createdByUserId, updatedByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        permitApplicationId: { $ref: "#/components/schemas/Uuid" }
        permitId: { $ref: "#/components/schemas/Uuid" }
        permitReference:
          type: string
          description: Permit `permitNumber`.
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        applicationStatus: { $ref: "#/components/schemas/PermitApplicationStatus" }
        workType:
          type: string
          description: Controlled work-type code (uppercased).
        currentWorkType:
          type: string
          nullable: true
          description: Current work type projection, null when unchanged.
        requirementType:
          type: string
          description: Data-driven requirement type code (2-64, uppercased).
        requirementDescription:
          type: string
          maxLength: 2000
        required: { type: boolean }
        readinessStatus: { $ref: "#/components/schemas/PermitSafetyReadinessStatus" }
        resolvedReadinessStatus: { $ref: "#/components/schemas/PermitSafetyReadinessStatus" }
        notes:
          type: string
          nullable: true
          maxLength: 2000
        reference:
          type: string
          nullable: true
          maxLength: 500
        checklistTemplateId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        checklistExecutionId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        evidenceRequirementId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        checklistStatus:
          type: string
          nullable: true
          description: Derived checklist status when a checklist control is bound.
        evidenceReady:
          type: boolean
          nullable: true
          description: Derived evidence readiness when an evidence control is bound.
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        updatedByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreatePermitSafetyRequirementRequest:
      type: object
      description: >
        BE-20E create payload for `POST
        /permit-applications/{applicationId}/safety-requirements` and
        `POST /permits/{permitId}/safety-requirements`. Requires `buildingId`,
        `workType`, `requirementType` and `requirementDescription`. Optional
        `required`, `readinessStatus`, `notes`, `reference`,
        `checklistTemplateId`, `checklistExecutionId`, `evidenceRequirementId`.
        `buildingId` must match the Application/Permit Building and `workType`
        must be a valid controlled code; otherwise 400
        `PERMIT_SAFETY_CONTEXT_MISMATCH`. Duplicate `requirementType` per
        Application yields 409 `PERMIT_SAFETY_REQUIREMENT_ALREADY_EXISTS`.
      required: [buildingId, workType, requirementType, requirementDescription]
      properties:
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Required. Must match the Application/Permit Building.
        workType:
          type: string
          minLength: 2
          maxLength: 64
          description: Required controlled work-type code (uppercased).
        requirementType:
          type: string
          minLength: 2
          maxLength: 64
          description: Required data-driven code (uppercased, must match request-type pattern).
        requirementDescription:
          type: string
          maxLength: 2000
          description: Required non-empty description.
        required:
          type: boolean
          description: Optional, default `true`.
        readinessStatus: { $ref: "#/components/schemas/PermitSafetyReadinessStatus" }
        notes:
          type: string
          maxLength: 2000
          nullable: true
          description: Optional; empty -> null.
        reference:
          type: string
          maxLength: 500
          nullable: true
          description: Optional; empty -> null.
        checklistTemplateId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        checklistExecutionId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        evidenceRequirementId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true

    UpdatePermitSafetyReadinessRequest:
      type: object
      description: >
        BE-20E patch payload for `PATCH /safety-requirements/{id}/readiness`.
        Requires `readinessStatus`; optional `notes`, `reference`,
        `checklistTemplateId`, `checklistExecutionId`, `evidenceRequirementId`
        (nullable; null clears). Immutable fields `permitId`,
        `permitApplicationId`, `buildingId`, `workType`, `requirementType`,
        `requirementDescription`, `required` must not be supplied. Trying to
        set `READY` when checklist/evidence prerequisites are incomplete yields
        400 `PERMIT_SAFETY_PREREQUISITE_NOT_MET`; updating a requirement on a
        `CANCELLED` Application yields 400 `PERMIT_SAFETY_UPDATE_NOT_ALLOWED`;
        shared-control invalid yields 400
        `PERMIT_SAFETY_SHARED_CONTROL_INVALID`.
      required: [readinessStatus]
      properties:
        readinessStatus: { $ref: "#/components/schemas/PermitSafetyReadinessStatus" }
        notes:
          type: string
          maxLength: 2000
          nullable: true
          description: Optional; empty -> null.
        reference:
          type: string
          maxLength: 500
          nullable: true
          description: Optional; empty -> null.
        checklistTemplateId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Null clears the binding.
        checklistExecutionId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Null clears the binding.
        evidenceRequirementId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Null clears the binding.

    PermitSafetyReadiness:
      type: object
      description: >
        BE-20E Permit Safety readiness view for one Permit (or its
        Application), returned by `GET /permits/{permitId}/safety-readiness`.
        `workType` is the current work type or null; `ready` is true only
        when every `required` requirement is `READY`; `readinessStatus` is
        `NOT_CONFIGURED` when no requirements exist, otherwise aggregated
        `PENDING`|`READY`|`NOT_READY`; `missingRequirementTypes` lists the
        requirement types that are still not `READY`.
      required: [permitId, permitApplicationId, permitReference, buildingId, workType, ready, readinessStatus, requiredCount, readyCount, missingRequirementTypes, requirements]
      properties:
        permitId: { $ref: "#/components/schemas/Uuid" }
        permitApplicationId: { $ref: "#/components/schemas/Uuid" }
        permitReference: { type: string }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        workType:
          type: string
          nullable: true
          description: Current work type or null.
        ready: { type: boolean }
        readinessStatus:
          type: string
          enum: [NOT_CONFIGURED, PENDING, READY, NOT_READY]
          description: Aggregated readiness status for the Permit.
        requiredCount:
          type: integer
          minimum: 0
        readyCount:
          type: integer
          minimum: 0
        missingRequirementTypes:
          type: array
          items: { type: string }
        requirements:
          type: array
          items: { $ref: "#/components/schemas/PermitSafetyRequirement" }

    # ------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03E2 - J11 Permit Validities (BE-20G)
    # schemas. Shapes mirror the runtime public DTOs exactly. Validity
    # authority only; not a renewal/approval engine.
    # ------------------------------------------------------------------

    PermitValidity:
      type: object
      description: >
        BE-20G Permit Validity - a time-bounded authority window for a Permit.
        `permitApplicationId`/`permitId`/`permitReference`/`clientId`/
        `buildingId`/`contractorContextType`/`contractorVendorId` are
        projections from the Permit/Application; `validFrom`/`validUntil` are
        the window (ISO-8601 with timezone); `status` is
        `PENDING`|`VALID`|`EXPIRED`|`REVOKED` (backend-controlled) plus derived
        `active` (true only when `status` is `VALID` and now within window);
        `activatedAt`/`expiredAt`/`revokedAt`/`revokedByUserId`/`revocationNotes`
        are lifecycle timestamps.
      required:
        [id, permitApplicationId, permitId, permitReference, clientId,
         buildingId, contractorContextType, contractorVendorId,
         applicationStatus, validFrom, validUntil, status, active,
         activatedAt, expiredAt, revokedAt, revokedByUserId, notes,
         revocationNotes, createdByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        permitApplicationId: { $ref: "#/components/schemas/Uuid" }
        permitId: { $ref: "#/components/schemas/Uuid" }
        permitReference:
          type: string
          description: Permit `permitNumber`.
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        contractorContextType: { $ref: "#/components/schemas/PermitContractorContextType" }
        contractorVendorId: { $ref: "#/components/schemas/Uuid" }
        applicationStatus: { $ref: "#/components/schemas/PermitApplicationStatus" }
        validFrom: { type: string, format: date-time }
        validUntil: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/PermitValidityStatus" }
        active:
          type: boolean
          description: True only when `status` is `VALID` and now is within `[validFrom, validUntil)`.
        activatedAt:
          type: string
          format: date-time
          nullable: true
          description: Set when the validity becomes `VALID`.
        expiredAt:
          type: string
          format: date-time
          nullable: true
          description: Set when the validity has `EXPIRED`.
        revokedAt:
          type: string
          format: date-time
          nullable: true
          description: Set when `REVOKED`.
        revokedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        notes:
          type: string
          nullable: true
          maxLength: 2000
        revocationNotes:
          type: string
          nullable: true
          maxLength: 2000
          description: Notes supplied on revocation (alias `notes` accepted).
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    SetPermitValidityRequest:
      type: object
      description: >
        BE-20G set payload for `POST /permits/{permitId}/validity`. Requires
        `permitApplicationId` (must be the Permit's `SUBMITTED` Application),
        `buildingId` (must match Permit Building), `validFrom` and `validUntil`
        (ISO-8601 with timezone, calendar-valid, `validUntil` > `validFrom`).
        Optional `notes` (2000, empty->null). Backend-controlled fields `status`,
        `activatedAt`, `expiredAt`, `revokedAt`, `revokedByUserId`,
        `createdByUserId`, `createdAt`, `updatedAt` must not be supplied.
      required: [permitApplicationId, buildingId, validFrom, validUntil]
      properties:
        permitApplicationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Required. Must be the Permit's Application.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Required. Must match the Permit Building.
        validFrom:
          type: string
          format: date-time
          description: Required ISO-8601 with timezone.
        validUntil:
          type: string
          format: date-time
          description: Required ISO-8601 with timezone, must be after `validFrom`.
        notes:
          type: string
          maxLength: 2000
          nullable: true
          description: Optional; empty -> null.

    RevokePermitValidityRequest:
      type: object
      description: >
        BE-20G revoke payload for `POST /permit-validities/{id}/revoke`.
        Optional `revocationNotes` (alias `notes`, 2000 chars, empty->null).
        An empty body is also accepted. Only `PENDING` or `VALID` can be
        revoked.
      properties:
        revocationNotes:
          type: string
          maxLength: 2000
          nullable: true
          description: Optional; alias `notes` is accepted.
        notes:
          type: string
          maxLength: 2000
          nullable: true
          description: Alias for `revocationNotes`.

    PermitValidityState:
      type: object
      description: >
        BE-20G Permit Validity state for one Permit, returned by
        `GET /permits/{permitId}/validity` and alias
        `GET /permits/{permitId}/validity/current`. `currentValidity` is the
        current `PENDING`|`VALID`|`EXPIRED`|`REVOKED` window or null when none;
        `history` is the full validity history for the Permit (plain array,
        ordered).
      required: [permitId, permitReference, buildingId, currentValidity, history]
      properties:
        permitId: { $ref: "#/components/schemas/Uuid" }
        permitReference:
          type: string
          description: Permit `permitNumber`.
        buildingId: { $ref: "#/components/schemas/Uuid" }
        currentValidity:
          allOf: [{ $ref: "#/components/schemas/PermitValidity" }]
          nullable: true
          description: Current validity window or null when none.
        history:
          type: array
          items: { $ref: "#/components/schemas/PermitValidity" }

    # ------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03F1 - J11 Permit Work Contexts (BE-20D)
    # schemas. Shapes mirror the runtime public DTOs exactly. Work context
    # only; not an execution lifecycle.
    # ------------------------------------------------------------------

    PermitWorkContext:
      type: object
      description: >
        BE-20D Permit Work Context - the permit-bound work-location + work-type
        assignment over the authoritative BE-04 structure. `permitApplicationId`/
        `permitId`/`permitReference`/`clientId`/`buildingId`/`applicationStatus`
        are projections from the Permit/Application; `locationType`+`locationId`
        resolve to `buildingLocationId`/`floorId`/`areaId`/`roomId`/
        `functionalLocationId` with `locationCode`/`locationName` projections;
        `workType` is the controlled code, `workDescription` is the resolved
        description and `workDescriptionOverride` is the caller override (or
        null); `plannedStartAt`/`plannedEndAt` are the planned window
        (ISO-8601 with timezone) and `accessRestrictionNotes` are optional
        restriction notes. `createdByUserId`/`updatedByUserId`/`createdAt`/
        `updatedAt` are server-assigned.
      required:
        [id, permitApplicationId, permitId, permitReference, clientId,
         buildingId, applicationStatus, locationType, locationId, locationCode,
         locationName, buildingLocationId, floorId, areaId, roomId,
         functionalLocationId, workType, workDescription,
         workDescriptionOverride, plannedStartAt, plannedEndAt,
         accessRestrictionNotes, createdByUserId, updatedByUserId, createdAt,
         updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        permitApplicationId: { $ref: "#/components/schemas/Uuid" }
        permitId: { $ref: "#/components/schemas/Uuid" }
        permitReference:
          type: string
          description: Permit `permitNumber`.
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        applicationStatus: { $ref: "#/components/schemas/PermitApplicationStatus" }
        locationType:
          allOf: [{ $ref: "#/components/schemas/PermitWorkLocationType" }]
          nullable: true
          description: null when no location has been assigned.
        locationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Concrete BE-04 id, null when no location assigned.
        locationCode:
          type: string
          nullable: true
          description: Code projection from the resolved BE-04 row.
        locationName:
          type: string
          nullable: true
          description: Name projection from the resolved BE-04 row.
        buildingLocationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        floorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        areaId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        roomId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        functionalLocationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        workType:
          type: string
          nullable: true
          description: Controlled work-type code, null when not assigned.
        workDescription:
          type: string
          maxLength: 4000
          description: Resolved work description (from Permit or override).
        workDescriptionOverride:
          type: string
          nullable: true
          maxLength: 4000
          description: Caller override for work description, empty->null.
        plannedStartAt:
          type: string
          format: date-time
          nullable: true
          description: Planned start (ISO-8601 with timezone), null when not assigned.
        plannedEndAt:
          type: string
          format: date-time
          nullable: true
          description: Planned end (ISO-8601 with timezone), null when not assigned.
        accessRestrictionNotes:
          type: string
          nullable: true
          maxLength: 2000
          description: Optional restriction notes, empty->null.
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        updatedByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    AssignPermitWorkLocationRequest:
      type: object
      description: >
        BE-20D assign-location payload for
        `PUT /permit-applications/{id}/work-location`. Requires
        `locationType` (BUILDING|FLOOR|AREA|ROOM|FUNCTIONAL_LOCATION),
        `locationId` (BE-04 id), `plannedStartAt` and `plannedEndAt`
        (ISO-8601 with timezone, calendar-valid, future start > now and
        end > start). Optional `accessRestrictionNotes` (2000, empty->null)
        and `workDescription` (4000, empty->null). Must be DRAFT.
      required: [locationType, locationId, plannedStartAt, plannedEndAt]
      properties:
        locationType: { $ref: "#/components/schemas/PermitWorkLocationType" }
        locationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Required BE-04 location id.
        plannedStartAt:
          type: string
          format: date-time
          description: Required ISO-8601 with timezone, must be a future instant.
        plannedEndAt:
          type: string
          format: date-time
          description: Required ISO-8601 with timezone, must be after `plannedStartAt`.
        accessRestrictionNotes:
          type: string
          maxLength: 2000
          nullable: true
          description: Optional; empty -> null.
        workDescription:
          type: string
          maxLength: 4000
          nullable: true
          description: Optional; empty -> null.

    AssignPermitWorkTypeRequest:
      type: object
      description: >
        BE-20D assign-work-type payload for
        `PUT /permit-applications/{id}/work-type`. Requires `workType`
        (controlled data code, normalized, must be valid). Optional
        `workDescription` (4000, empty->null). Must be DRAFT.
      required: [workType]
      properties:
        workType:
          type: string
          description: Required controlled work-type code (e.g. uppercased, validated).
        workDescription:
          type: string
          maxLength: 4000
          nullable: true
          description: Optional; empty -> null.

    UpdatePermitWorkContextRequest:
      type: object
      description: >
        BE-20D patch payload for
        `PATCH /permit-applications/{id}/work-context`. At least one field is
        required. Optional `locationType`+`locationId` (must be supplied
        together, same validation as assign), `workType` (controlled code),
        `workDescription` (4000, empty->null, `null` clears), `plannedStartAt`/
        `plannedEndAt` (ISO-8601 with timezone, calendar-valid; when supplied
        the resulting window must be future start > now and end > start),
        `accessRestrictionNotes` (2000, empty->null, `null` clears). Immutable
        fields `permitId`, `permitApplicationId`, `permitReference`, `clientId`,
        `buildingId`, `applicationStatus`, `createdByUserId`, `updatedByUserId`,
        `createdAt`, `updatedAt` must not be supplied. Must be DRAFT.
      minProperties: 1
      properties:
        locationType: { $ref: "#/components/schemas/PermitWorkLocationType" }
        locationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: When supplied `locationType` must also be supplied.
        workType:
          type: string
          description: Controlled work-type code.
        workDescription:
          type: string
          maxLength: 4000
          nullable: true
          description: Optional; `null` clears the override.
        plannedStartAt:
          type: string
          format: date-time
          nullable: true
          description: Optional ISO-8601 with timezone.
        plannedEndAt:
          type: string
          format: date-time
          nullable: true
          description: Optional ISO-8601 with timezone; must be after `plannedStartAt` when both present.
        accessRestrictionNotes:
          type: string
          maxLength: 2000
          nullable: true
          description: Optional; `null` clears.

    # ------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03F2 - J11 Permit Work Lifecycle (BE-20K)
    # schemas. Shapes mirror the runtime public DTOs exactly. BE-09-style
    # authority: READY -> IN_PROGRESS -> CLOSED, CANCELLED derived.
    # ------------------------------------------------------------------

    PermitWorkStatus:
      type: string
      description: >
        BE-20K Permit Work status vocabulary. `READY` means no lifecycle row
        exists yet and the Permit is not CANCELLED; `IN_PROGRESS` means work
        has been started; `CLOSED` means work has been closed; `CANCELLED` is
        derived when the BE-20A Permit itself is `CANCELLED` (no lifecycle row
        transition produces `CANCELLED` directly). Only `READY`->`IN_PROGRESS` via
        `POST /permits/{permitId}/work-start` and `IN_PROGRESS`->`CLOSED` via
        `POST /permits/{permitId}/work-close` are exposed as state transitions.
      enum: [READY, IN_PROGRESS, CLOSED, CANCELLED]

    PermitWorkAction:
      type: string
      description: >
        BE-20K Permit Work action vocabulary for the BE-09-style authority.
        `START` corresponds to `POST /permits/{permitId}/work-start` (READY->IN_PROGRESS)
        and `CLOSE` to `POST /permits/{permitId}/work-close` (IN_PROGRESS->CLOSED).
        Available actions are filtered by `resolvePermitWorkAuthority` to those
        allowed now for the caller (START requires READY+ready+permit.read+permit.manage,
        CLOSE requires IN_PROGRESS+permit.read+permit.manage).
      enum: [START, CLOSE]

    PermitWorkLifecycle:
      type: object
      description: >
        BE-20K Permit Work Lifecycle record. `permitApplicationId`/`permitId`/
        `permitReference`/`clientId`/`buildingId`/`contractorContextType`/
        `contractorVendorId` are projections from the Permit/Application;
        `status` is READY|IN_PROGRESS|CLOSED|CANCELLED (CANCELLED derived),
        `startedAt`/`startedByUserId`/`startNotes` set on START,
        `closedAt`/`closedByUserId`/`closeNotes` set on CLOSE, `createdAt`/
        `updatedAt` server-assigned. When no lifecycle row exists and Permit
        is not CANCELLED the status is READY.
      required:
        [id, permitApplicationId, permitId, permitReference, clientId,
         buildingId, contractorContextType, contractorVendorId, status,
         startedAt, startedByUserId, startNotes, closedAt, closedByUserId,
         closeNotes, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        permitApplicationId: { $ref: "#/components/schemas/Uuid" }
        permitId: { $ref: "#/components/schemas/Uuid" }
        permitReference:
          type: string
          description: Permit `permitNumber`.
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        contractorContextType: { $ref: "#/components/schemas/PermitContractorContextType" }
        contractorVendorId: { $ref: "#/components/schemas/Uuid" }
        status: { $ref: "#/components/schemas/PermitWorkStatus" }
        startedAt:
          type: string
          format: date-time
          nullable: true
          description: Set on START, null until then.
        startedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Actor who started work.
        startNotes:
          type: string
          nullable: true
          maxLength: 2000
          description: Notes supplied on START (trimmed, empty->null).
        closedAt:
          type: string
          format: date-time
          nullable: true
          description: Set on CLOSE, null until then.
        closedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Actor who closed work.
        closeNotes:
          type: string
          nullable: true
          maxLength: 2000
          description: Notes supplied on CLOSE (trimmed, empty->null).
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    PermitWorkStatusView:
      type: object
      description: >
        BE-20K Permit Work Status view for one Permit, returned by
        `GET /permits/{permitId}/work-status`. `lifecycleId` is the lifecycle
        row id or null when still READY; `status` is the derived work status
        (READY|IN_PROGRESS|CLOSED|CANCELLED), timestamps/notes are projections
        from the lifecycle row, `availableActions` is the BE-09-style filtered
        list ([START] when READY+ready, [CLOSE] when IN_PROGRESS, [] otherwise)
        for the caller.
      required:
        [lifecycleId, permitId, permitApplicationId, permitReference,
         buildingId, contractorContextType, contractorVendorId, status,
         startedAt, startedByUserId, startNotes, closedAt, closedByUserId,
         closeNotes, availableActions]
      properties:
        lifecycleId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Lifecycle row id or null when READY.
        permitId: { $ref: "#/components/schemas/Uuid" }
        permitApplicationId: { $ref: "#/components/schemas/Uuid" }
        permitReference:
          type: string
          description: Permit `permitNumber`.
        buildingId: { $ref: "#/components/schemas/Uuid" }
        contractorContextType: { $ref: "#/components/schemas/PermitContractorContextType" }
        contractorVendorId: { $ref: "#/components/schemas/Uuid" }
        status: { $ref: "#/components/schemas/PermitWorkStatus" }
        startedAt:
          type: string
          format: date-time
          nullable: true
        startedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        startNotes:
          type: string
          nullable: true
          maxLength: 2000
        closedAt:
          type: string
          format: date-time
          nullable: true
        closedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        closeNotes:
          type: string
          nullable: true
          maxLength: 2000
        availableActions:
          type: array
          items: { $ref: "#/components/schemas/PermitWorkAction" }

    PermitWorkStartReadiness:
      type: object
      description: >
        BE-20K Permit Work Start Readiness view for one Permit, returned by
        `GET /permits/{permitId}/work-start-readiness`. `workStatus` is the
        derived work status (READY|IN_PROGRESS|CLOSED|CANCELLED); `ready` is
        true only when every prerequisite is ready; `approvalReady`,
        `validityStatus` (PENDING|VALID|EXPIRED|REVOKED or null),
        `safetyReady`/`safetyReadinessStatus`, `workerListReady`/
        `activeWorkerCount`/`configuredWorkerCount`, `equipmentRequired`/
        `equipmentReady`/`activeEquipmentCount`/`configuredEquipmentCount`,
        `evidenceConfigured`/`evidenceReady` are the prerequisite projections;
        `blockers` lists the blocker codes (e.g. WORK_ALREADY_TRANSITIONED) and
        `availableActions` is [START] when READY+ready+authorized else [].
      required:
        [permitId, permitApplicationId, permitReference, buildingId,
         contractorContextType, contractorVendorId, workStatus, ready,
         approvalReady, validityStatus, safetyReady, safetyReadinessStatus,
         workerListReady, activeWorkerCount, configuredWorkerCount,
         equipmentRequired, equipmentReady, activeEquipmentCount,
         configuredEquipmentCount, evidenceConfigured, evidenceReady, blockers,
         availableActions]
      properties:
        permitId: { $ref: "#/components/schemas/Uuid" }
        permitApplicationId: { $ref: "#/components/schemas/Uuid" }
        permitReference:
          type: string
          description: Permit `permitNumber`.
        buildingId: { $ref: "#/components/schemas/Uuid" }
        contractorContextType: { $ref: "#/components/schemas/PermitContractorContextType" }
        contractorVendorId: { $ref: "#/components/schemas/Uuid" }
        workStatus: { $ref: "#/components/schemas/PermitWorkStatus" }
        ready: { type: boolean }
        approvalReady: { type: boolean }
        validityStatus:
          allOf: [{ $ref: "#/components/schemas/PermitValidityStatus" }]
          nullable: true
          description: Current validity status or null.
        safetyReady: { type: boolean }
        safetyReadinessStatus:
          type: string
          description: BE-20E safety readiness status (e.g. READY, NOT_READY).
        workerListReady: { type: boolean }
        activeWorkerCount:
          type: integer
          minimum: 0
        configuredWorkerCount:
          type: integer
          minimum: 0
        equipmentRequired: { type: boolean }
        equipmentReady: { type: boolean }
        activeEquipmentCount:
          type: integer
          minimum: 0
        configuredEquipmentCount:
          type: integer
          minimum: 0
        evidenceConfigured: { type: boolean }
        evidenceReady: { type: boolean }
        blockers:
          type: array
          items: { type: string }
          description: Blocker codes when not ready.
        availableActions:
          type: array
          items: { $ref: "#/components/schemas/PermitWorkAction" }

    PermitWorkNotesRequest:
      type: object
      description: >
        BE-20K notes payload for `POST /permits/{permitId}/work-start` and
        `POST /permits/{permitId}/work-close`. Optional `notes` (2000 chars,
        trimmed, empty->null, `null` clears). An empty body is also accepted
        (no notes).
      properties:
        notes:
          type: string
          maxLength: 2000
          nullable: true
          description: Optional notes; trimmed, empty -> null.

    # ------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03G1 - J11 Permit Workers (BE-20H)
    # schemas. Shapes mirror the runtime public DTOs exactly. Workers are
    # bindings over Workforce masters only.
    # ------------------------------------------------------------------

    PermitWorkerStatus:
      type: string
      description: >
        BE-20H Permit Worker status vocabulary. Created `ACTIVE`;
        `ACTIVE` -> `INACTIVE` via `POST /permit-workers/{id}/deactivate`;
        `INACTIVE` is terminal for that entry. Only `ACTIVE` entries are
        eligible for active-work resolution when Permit validity is `VALID`.
      enum: [ACTIVE, INACTIVE]

    PermitWorker:
      type: object
      description: >
        BE-20H Permit Worker - permit-bound binding over Workforce masters.
        `permitApplicationId`/`permitId`/`permitReference`/`clientId`/
        `buildingId`/`contractorContextType`/`contractorContextId`/
        `contractorVendorId` are projections from the Permit/Application;
        `vendorWorkforceBindingId`/`workforceProfileId`/`workerName`/
        `employeeCode`/`vendorPersonnelCode`/`workforceType`/`workforceStatus`/
        `vendorWorkforceStatus`/`vendorWorkforceEffectiveFrom`/
        `vendorWorkforceEffectiveUntil`/`roleTrade` are resolved from Workforce/
        Vendor Workforce. `validFrom`/`validUntil` are the worker window
        contained within Permit validity, `permitValidityStatus` is the current
        Permit validity status at read time, `eligibleForActiveWork` is the
        runtime-computed eligibility for `GET /permits/{permitId}/workers/active`.
        `workerPersonReference`/`identificationReference` are convenience aliases
        for `workforceProfileId`. `notes` is optional.
      required:
        [id, permitApplicationId, permitId, permitReference, clientId,
         buildingId, contractorContextType, contractorContextId,
         contractorVendorId, vendorWorkforceBindingId, workforceProfileId,
         workerName, employeeCode, vendorPersonnelCode, workforceType,
         workforceStatus, vendorWorkforceStatus,
         vendorWorkforceEffectiveFrom, vendorWorkforceEffectiveUntil,
         roleTrade, status, validFrom, validUntil, permitValidityStatus,
         workerPersonReference, identificationReference, eligibleForActiveWork,
         notes, createdByUserId, updatedByUserId, deactivatedAt,
         deactivatedByUserId, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        permitApplicationId: { $ref: "#/components/schemas/Uuid" }
        permitId: { $ref: "#/components/schemas/Uuid" }
        permitReference:
          type: string
          description: Permit `permitNumber`.
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        contractorContextType: { $ref: "#/components/schemas/PermitContractorContextType" }
        contractorContextId: { $ref: "#/components/schemas/Uuid" }
        contractorVendorId: { $ref: "#/components/schemas/Uuid" }
        vendorWorkforceBindingId: { $ref: "#/components/schemas/Uuid" }
        workforceProfileId: { $ref: "#/components/schemas/Uuid" }
        workerName: { type: string }
        employeeCode: { type: string }
        vendorPersonnelCode: { type: string }
        workforceType: { type: string }
        workforceStatus: { type: string }
        vendorWorkforceStatus: { type: string }
        vendorWorkforceEffectiveFrom:
          type: string
          format: date-time
          nullable: true
        vendorWorkforceEffectiveUntil:
          type: string
          format: date-time
          nullable: true
        roleTrade:
          type: string
          description: Data-driven role/trade code (normalized, valid code).
        status: { $ref: "#/components/schemas/PermitWorkerStatus" }
        validFrom: { type: string, format: date-time }
        validUntil: { type: string, format: date-time }
        permitValidityStatus:
          allOf: [{ $ref: "#/components/schemas/PermitValidityStatus" }]
          nullable: true
        workerPersonReference:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Alias for `workforceProfileId`.
        identificationReference:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Alias/projection for identification.
        eligibleForActiveWork:
          type: boolean
          description: Whether this entry is eligible for active-work resolution.
        notes:
          type: string
          nullable: true
          maxLength: 2000
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        updatedByUserId: { $ref: "#/components/schemas/Uuid" }
        deactivatedAt:
          type: string
          format: date-time
          nullable: true
        deactivatedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    AddPermitWorkerRequest:
      type: object
      description: >
        BE-20H add payload for `POST /permits/{permitId}/workers`. Requires
        `permitApplicationId`, `buildingId`, `workforceProfileId` (alias
        `workerPersonReference` accepted) and `roleTrade` (data-driven code).
        Optional `vendorWorkforceBindingId`, `validFrom`/`validUntil`
        (ISO-8601 with timezone, ordered and contained within Permit validity;
        defaults to Permit validity when omitted) and `notes` (2000, empty->null).
        Backend-resolved fields `workerName`, `employeeCode`,
        `vendorPersonnelCode`, `identificationReference`, `status`,
        `deactivatedAt`, `deactivatedByUserId`, `createdByUserId`, `createdAt`,
        `updatedAt` must not be supplied.
      required: [permitApplicationId, buildingId, workforceProfileId, roleTrade]
      properties:
        permitApplicationId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Required. Must reference the Permit Application that is `SUBMITTED` and belongs to the path Permit.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Required. Must be the Permit's Building.
        workforceProfileId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Required workforce profile; alias `workerPersonReference` accepted.
        workerPersonReference:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Alias for `workforceProfileId`.
        vendorWorkforceBindingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Optional Vendor Workforce binding id.
        roleTrade:
          type: string
          minLength: 2
          maxLength: 64
          description: Required data-driven role/trade code (normalized, must be valid).
        validFrom:
          type: string
          format: date-time
          description: Optional ISO-8601 with timezone; defaults to Permit validity `validFrom`.
        validUntil:
          type: string
          format: date-time
          description: Optional ISO-8601 with timezone; defaults to Permit validity `validUntil`; must be after `validFrom`.
        notes:
          type: string
          maxLength: 2000
          nullable: true
          description: Optional; trimmed. Empty is stored as null.

    UpdatePermitWorkerRequest:
      type: object
      description: >
        BE-20H edit payload for `PATCH /permit-workers/{id}`. At least one
        updatable field is required (400 otherwise). Updatable fields are
        `roleTrade` (data-driven code), `validFrom`/`validUntil`
        (ISO-8601 with timezone, ordered and contained) and `notes` (nullable;
        `null` clears). The entry must be `ACTIVE` and on an open Permit
        (`PENDING`|`VALID` validity) - otherwise 400
        `PERMIT_WORKER_UPDATE_NOT_ALLOWED`. Immutable context fields
        (`permitId`, `permitApplicationId`, `buildingId`, `contractorContextType`,
        `contractorContextId`, `contractorVendorId`, `workforceProfileId`,
        `vendorWorkforceBindingId`, `status`, `deactivatedAt`) must not be
        supplied.
      minProperties: 1
      properties:
        roleTrade:
          type: string
          minLength: 2
          maxLength: 64
          description: Optional data-driven role/trade code.
        validFrom:
          type: string
          format: date-time
          description: Optional ISO-8601 with timezone.
        validUntil:
          type: string
          format: date-time
          description: Optional ISO-8601 with timezone; must be after `validFrom` when both present.
        notes:
          type: string
          maxLength: 2000
          nullable: true
          description: "`null` clears the notes."

    PermitActiveWorkerList:
      type: object
      description: >
        BE-20H active worker view for one Permit, returned by
        `GET /permits/{permitId}/workers/active`. `validityStatus` is the
        current Permit validity status (or null); `active` is true only when
        `validityStatus` is `VALID`. When not `VALID` `workers` is empty; when
        `VALID` only `eligibleForActiveWork` workers are included.
      required: [permitId, permitReference, buildingId, contractorContextType, contractorVendorId, validityStatus, active, workerCount, workers]
      properties:
        permitId: { $ref: "#/components/schemas/Uuid" }
        permitReference:
          type: string
          description: Permit `permitNumber`.
        buildingId: { $ref: "#/components/schemas/Uuid" }
        contractorContextType: { $ref: "#/components/schemas/PermitContractorContextType" }
        contractorVendorId: { $ref: "#/components/schemas/Uuid" }
        validityStatus:
          allOf: [{ $ref: "#/components/schemas/PermitValidityStatus" }]
          nullable: true
          description: Current Permit validity status, or null when none.
        active:
          type: boolean
          description: True only when `validityStatus` is `VALID`.
        workerCount:
          type: integer
          minimum: 0
          description: Number of workers in `workers`.
        workers:
          type: array
          items: { $ref: "#/components/schemas/PermitWorker" }

    # ------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 03G2 - J11 Work Permit Readiness (BE-15D)
    # schemas. Shapes mirror the runtime public DTOs exactly. Readiness is a
    # binding/projection layer only; not a PtW engine.
    # ------------------------------------------------------------------

    WorkPermitReadinessStatus:
      type: string
      description: >
        BE-15D derived readiness status. `READY` means the permit requirement
        is satisfied and within validity; `NOT_READY` means the permit is
        missing, not ISSUED or has wrong status; `EXPIRED` means validUntil is
        in the past (time-aware); `NOT_REQUIRED` means the `permitRequirementType`
        is `NOT_REQUIRED` (no permit needed). Derived backend-side, never
        client-supplied.
      enum: [READY, NOT_READY, EXPIRED, NOT_REQUIRED]

    WorkPermitReadinessPermitStatus:
      type: string
      description: >
        BE-15D permit status vocabulary for readiness. `DRAFT`/`PENDING`/
        `ISSUED`/`SUSPENDED`/`REJECTED`/`REVOKED`/`EXPIRED`. The status is
        supplied by the caller on create/update or defaults server-side and is
        part of the readiness derivation.
      enum: [DRAFT, PENDING, ISSUED, SUSPENDED, REJECTED, REVOKED, EXPIRED]

    WorkPermitReadiness:
      type: object
      description: >
        BE-15D Work Permit Readiness record. `clientId`/`vendorWorkId`/
        `buildingId`/`workOrderId` are resolved from the BE-15B Vendor Work and
        its Building; `permitRequirementType` (uppercased 1..100) is the
        requirement type (`NOT_REQUIRED` reserved), `permitReference` is the
        permit reference (or null), `permitStatus` is the permit status,
        `validFrom`/`validUntil` are the validity window (ISO date strings,
        nullable, derived to ISO strings), `readinessStatus` is the derived
        readiness (READY|NOT_READY|EXPIRED|NOT_REQUIRED), `notes` is optional
        (2000, empty->null). `id`/`createdByUserId`/`createdAt`/`updatedAt`
        are server-assigned.
      required:
        [id, clientId, vendorWorkId, buildingId, workOrderId,
         permitRequirementType, permitReference, permitStatus, validFrom,
         validUntil, readinessStatus, notes, createdByUserId, createdAt,
         updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        vendorWorkId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        workOrderId: { $ref: "#/components/schemas/Uuid" }
        permitRequirementType:
          type: string
          maxLength: 100
          description: Upperscased permit requirement type (e.g. HOT_WORK), `NOT_REQUIRED` reserved.
        permitReference:
          type: string
          nullable: true
          maxLength: 200
          description: Permit reference or null.
        permitStatus: { $ref: "#/components/schemas/WorkPermitReadinessPermitStatus" }
        validFrom:
          type: string
          format: date-time
          nullable: true
          description: Validity start (ISO date string, nullable).
        validUntil:
          type: string
          format: date-time
          nullable: true
          description: Validity end (ISO date string, nullable).
        readinessStatus: { $ref: "#/components/schemas/WorkPermitReadinessStatus" }
        notes:
          type: string
          nullable: true
          maxLength: 2000
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    CreateWorkPermitReadinessRequest:
      type: object
      description: >
        BE-15D create payload for `POST /permit-readiness`. Requires
        `vendorWorkId` (Vendor Work id) and `permitRequirementType`
        (uppercased 1..100, `NOT_REQUIRED` allowed). Optional
        `permitReference` (200, empty->null), `permitStatus`
        (DRAFT|PENDING|ISSUED|SUSPENDED|REJECTED|REVOKED|EXPIRED), `validFrom`/
        `validUntil` (ISO date strings, nullable, invalid window yields 400
        `PERMIT_READINESS_INVALID_VALIDITY`) and `notes` (2000, empty->null).
        `readinessStatus` is not accepted (derived).
      required: [vendorWorkId, permitRequirementType]
      properties:
        vendorWorkId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Required Vendor Work id.
        permitRequirementType:
          type: string
          maxLength: 100
          description: Required requirement type (uppercased, 1..100 chars).
        permitReference:
          type: string
          maxLength: 200
          description: Optional permit reference; trimmed, empty->null.
        permitStatus: { $ref: "#/components/schemas/WorkPermitReadinessPermitStatus" }
        validFrom:
          type: string
          format: date-time
          nullable: true
          description: Optional validity start (ISO date string).
        validUntil:
          type: string
          format: date-time
          nullable: true
          description: Optional validity end (ISO date string); must be after `validFrom` when both set.
        notes:
          type: string
          maxLength: 2000
          description: Optional notes; trimmed, empty->null.

    UpdateWorkPermitReadinessRequest:
      type: object
      description: >
        BE-15D update payload for `PATCH /permit-readiness/{readinessId}`.
        At least one field is required. Optional `permitReference` (200,
        empty->null, `null` clears), `permitStatus`
        (DRAFT|PENDING|ISSUED|SUSPENDED|REJECTED|REVOKED|EXPIRED),
        `validFrom`/`validUntil` (ISO date strings, nullable, `null` clears,
        invalid window yields 400 `PERMIT_READINESS_INVALID_VALIDITY`), `notes`
        (2000, empty->null, `null` clears). `permitRequirementType` and
        identity fields are immutable; `readinessStatus` remains derived.
      minProperties: 1
      properties:
        permitReference:
          type: string
          maxLength: 200
          nullable: true
          description: Optional; `null` clears.
        permitStatus: { $ref: "#/components/schemas/WorkPermitReadinessPermitStatus" }
        validFrom:
          type: string
          format: date-time
          nullable: true
          description: Optional; `null` clears.
        validUntil:
          type: string
          format: date-time
          nullable: true
          description: Optional; `null` clears; must be after `validFrom` when both present.
        notes:
          type: string
          maxLength: 2000
          nullable: true
          description: Optional; `null` clears.

    ResolvedPermitReadiness:
      type: object
      description: >
        BE-15D time-aware resolved readiness of one permit requirement,
        embedded in `VendorWorkPermitReadiness`. Mirrors `WorkPermitReadiness`
        but re-evaluates `readinessStatus` time-awarely (EXPIRED when
        validUntil in past).
      required:
        [id, permitRequirementType, permitReference, permitStatus, validFrom,
         validUntil, readinessStatus, notes]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        permitRequirementType:
          type: string
          maxLength: 100
        permitReference:
          type: string
          nullable: true
          maxLength: 200
        permitStatus: { $ref: "#/components/schemas/WorkPermitReadinessPermitStatus" }
        validFrom:
          type: string
          format: date-time
          nullable: true
        validUntil:
          type: string
          format: date-time
          nullable: true
        readinessStatus: { $ref: "#/components/schemas/WorkPermitReadinessStatus" }
        notes:
          type: string
          nullable: true
          maxLength: 2000

    VendorWorkPermitReadiness:
      type: object
      description: >
        BE-15D authoritative, time-aware current permit readiness of a Vendor
        Work, returned by `GET /permit-readiness/current`. `vendorWorkId` is
        the Vendor Work, `buildingId` is the resolved Building, `ready` is true
        only when every permit `readinessStatus` is READY or NOT_REQUIRED and
        none is EXPIRED/NOT_READY, `readinessStatus` is the aggregate
        (READY|NOT_READY|EXPIRED|NOT_REQUIRED) and `permits` is the array of
        `ResolvedPermitReadiness`.
      required: [vendorWorkId, buildingId, ready, readinessStatus, permits]
      properties:
        vendorWorkId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        ready: { type: boolean }
        readinessStatus: { $ref: "#/components/schemas/WorkPermitReadinessStatus" }
        permits:
          type: array
          items: { $ref: "#/components/schemas/ResolvedPermitReadiness" }

    # ------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 04A - J12 Vendor Assignment (BE-15A)
    # ------------------------------------------------------------------
    VendorAssignmentStatus:
      type: string
      enum: [ACTIVE, INACTIVE]
      description: >
        BE-15A assignment lifecycle: ACTIVE -> INACTIVE. INACTIVE is terminal
        (no reactivation endpoint exists); deactivation never deletes the row
        and reassignment produces a NEW ACTIVE assignment.
    VendorAssignment:
      type: object
      description: >
        BE-15A `PublicVendorAssignment` projection returned by every Vendor
        Assignment operation. It carries the assignment record only: the Vendor
        and the Work Order are referenced by id and never embedded, and the
        persisted `createdAt`/`updatedAt` are not exposed.
      required:
        [id, vendorId, workOrderId, buildingId, status, notes,
         assignedByUserId, assignedAt]
      properties:
        id:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Server-assigned Vendor Assignment id.
        vendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Referenced BE-06A Vendor (never mutated by this module).
        workOrderId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Referenced BE-08B Work Order (never mutated by this module).
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Building resolved from the Work Order at assignment time; also the
            BE-02 isolation scope of every read/update of this assignment.
        status: { $ref: "#/components/schemas/VendorAssignmentStatus" }
        notes:
          type: string
          nullable: true
          maxLength: 2000
          description: Optional notes; null when never supplied or supplied empty.
        assignedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Authenticated User that performed the assignment; server-assigned
            from the session and never client-supplied.
        assignedAt:
          type: string
          format: date-time
          description: ISO-8601 assignment timestamp.
    CreateVendorAssignmentRequest:
      type: object
      description: >
        BE-15A assign payload for `POST /vendor-assignments` and for the
        reassign form of `PATCH /vendor-assignments/{assignmentId}`. Only
        `vendorId` and `workOrderId` are accepted plus optional `notes`;
        `id`, `buildingId`, `status`, `assignedByUserId` and `assignedAt` are
        server-assigned and unassignable by the caller.
      required: [vendorId, workOrderId]
      properties:
        vendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Required Vendor id; must belong to the Work Order's Client and Building.
        workOrderId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Required Work Order id; must exist and be in an assignable state.
        notes:
          type: string
          maxLength: 2000
          description: Optional notes; trimmed, empty string or null is accepted as absent.
    UpdateVendorAssignmentRequest:
      type: object
      description: >
        BE-15A PATCH payload for `PATCH /vendor-assignments/{assignmentId}`.
        Exactly one of the two runtime forms is supplied: the DEACTIVATION form
        (only `status: "INACTIVE"` is accepted) or the REASSIGNMENT form (no
        `status` key, `vendorId` and `workOrderId` required, `notes` optional).
        When `status` is present it takes precedence and the remaining fields
        are ignored. A body with neither form fails 400 `VALIDATION_ERROR`;
        `status: "ACTIVE"` is rejected because INACTIVE is terminal and no
        reactivation is exposed. There is no delete and no other field is
        mutable.
      properties:
        status:
          type: string
          enum: [INACTIVE]
          description: >
            Deactivation form: mark the assignment INACTIVE (row preserved, never
            deleted). Any other value - including `ACTIVE` - yields 400
            `VALIDATION_ERROR`.
        vendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: "Reassignment form: new Vendor id (required in that form)."
        workOrderId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: "Reassignment form: new Work Order id (required in that form)."
        notes:
          type: string
          maxLength: 2000
          description: "Reassignment form: optional notes carried onto the NEW assignment."

    # ------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 04B1 - J12 Vendor Work (BE-15B)
    # ------------------------------------------------------------------
    VendorWorkStatus:
      type: string
      enum: [NOT_STARTED, IN_PROGRESS, ON_HOLD, COMPLETED]
      description: >
        BE-15B Vendor Work lifecycle, enforced by the explicit backend
        transition table and never client-assigned: NOT_STARTED -> IN_PROGRESS;
        IN_PROGRESS -> ON_HOLD | COMPLETED; ON_HOLD -> IN_PROGRESS;
        COMPLETED is terminal for BE-15B. `COMPLETED` means execution
        completion only - it is not acceptance, sign-off, verification or
        handover.
    VendorWork:
      type: object
      description: >
        BE-15B `PublicVendorWork` projection returned by every Vendor Work
        operation. It carries the execution record only: the Vendor Assignment,
        Vendor, Work Order and Building are referenced by id and never embedded
        or mutated, and the persisted `createdAt`/`updatedAt` are not exposed.
      required:
        [id, vendorAssignmentId, vendorId, workOrderId, buildingId, status,
         notes, startedAt, completedAt]
      properties:
        id:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Server-assigned Vendor Work id.
        vendorAssignmentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            The BE-15A Vendor Assignment this work executes. At most one Vendor
            Work exists per assignment (database unique constraint).
        vendorId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Vendor copied from the assignment at resolve time.
        workOrderId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Work Order copied from the assignment at resolve time.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Building copied from the assignment at resolve time; also the BE-02
            isolation scope of every read and status transition of this work.
        status: { $ref: "#/components/schemas/VendorWorkStatus" }
        notes:
          type: string
          nullable: true
          maxLength: 2000
          description: >
            Execution notes; null when never supplied. Replaced by a non-empty
            `notes` on a status transition, never clearable through the API.
        startedAt:
          type: string
          format: date-time
          nullable: true
          description: >
            ISO-8601 timestamp set by the backend the first time the work
            leaves NOT_STARTED; null while still NOT_STARTED.
        completedAt:
          type: string
          format: date-time
          nullable: true
          description: >
            ISO-8601 timestamp set by the backend exactly when the work reaches
            COMPLETED; null otherwise. Never a completion report, BAST,
            acceptance or invoice reference.
    ResolveVendorWorkRequest:
      type: object
      description: >
        BE-15B optional body of `POST /vendor-assignments/{assignmentId}/work`.
        The whole body may be omitted; only `notes` is accepted and it is
        applied only when a new Vendor Work is created - a repeat call returns
        the existing record unchanged. `vendorId`, `workOrderId`, `buildingId`,
        `status` and both timestamps are derived or backend-set and are not
        accepted here.
      properties:
        notes:
          type: string
          maxLength: 2000
          description: Optional notes; trimmed, empty string or null is accepted as absent.
    UpdateVendorWorkStatusRequest:
      type: object
      description: >
        BE-15B PATCH payload of `/vendor-works/{workId}/status`. Only `status`
        and `notes` are accepted. `status` must be a target the backend
        transition table allows from the current status; anything else -
        including repeating the current status, moving out of COMPLETED or
        jumping NOT_STARTED -> COMPLETED - is 400
        `VENDOR_WORK_INVALID_TRANSITION`. The client cannot set `startedAt`,
        `completedAt` or any other field.
      required: [status]
      properties:
        status:
          allOf: [{ $ref: "#/components/schemas/VendorWorkStatus" }]
          description: >
            Required target status (NOT_STARTED is never a valid target - the
            table exposes no transition into it).
        notes:
          type: string
          maxLength: 2000
          description: >
            Optional notes replaced on the accepted transition; trimmed, and
            empty string or null is accepted as absent (it does not clear the
            stored notes).

    # ------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 04B2 - J12 Vendor Work Evidence
    # (BE-15E) + Vendor Work History (BE-15K)
    # ------------------------------------------------------------------
    VendorWorkEvidenceType:
      type: string
      enum: [PHOTO, DOCUMENT, SIGNATURE]
      description: >
        BE-15E evidence type - the only types a Vendor Work submission may
        carry. The shared BE-07 engine may know other types; none is accepted or
        returned through this surface. Each type has a fixed accepted MIME set
        enforced on submit: PHOTO = image/jpeg, image/png, image/webp;
        DOCUMENT = application/pdf, image/jpeg, image/png; SIGNATURE =
        image/png, image/svg+xml.
    VendorWorkEvidenceRequirement:
      type: object
      description: >
        BE-15E projection of one ACTIVE BE-07 `evidence_requirements` row bound
        to a Vendor Work (`target_type = 'VENDOR_WORK'`). It is a read-only
        requirement definition: it carries no fulfilment verdict, no submission
        counters and no retention, integrity or approval state. Deliberately
        distinct from the generic BE-07 `EvidenceRequirement` schema, which
        addresses a row by `targetType`/`targetId` and exposes
        `createdAt`/`updatedAt`.
      required:
        [id, clientId, vendorWorkId, evidenceType, required, minimumCount,
         maximumCount, description, status]
      properties:
        id:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Requirement id; accepted as `evidenceRequirementId` on submit.
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Client of the requirement (BE-07 ownership).
        vendorWorkId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Vendor Work this requirement is bound to.
        evidenceType: { $ref: "#/components/schemas/VendorWorkEvidenceType" }
        required:
          type: boolean
          description: Whether the requirement is mandatory for the Vendor Work.
        minimumCount:
          type: integer
          description: >
            Minimum number of ACTIVE submissions the requirement expects. Not
            evaluated, enforced or reported as satisfied by this API surface.
        maximumCount:
          type: integer
          nullable: true
          description: >
            Maximum number of ACTIVE submissions allowed; null means uncapped.
            Only an uncapped-free value makes a submit fail with 400
            `VENDOR_WORK_EVIDENCE_COUNT_VIOLATION`.
        description:
          type: string
          nullable: true
          description: Requirement description.
        status:
          type: string
          description: >
            BE-07 requirement status string carried through unchanged; only
            ACTIVE requirements are resolved for a Vendor Work.
    VendorWorkEvidence:
      type: object
      description: >
        BE-15E projection of one evidence submission bound to a Vendor Work
        (`evidence_submissions` with `execution_type = 'VENDOR_WORK'`). The
        payload is file METADATA only - the response never embeds file content,
        a signed URL, a scan verdict, a hash, a retention state or an approval
        decision, and it never implies that the Vendor Work is complete,
        accepted or verified.
      required:
        [id, clientId, vendorWorkId, evidenceRequirementId, evidenceType,
         fileReference, originalFileName, mimeType, fileSize, capturedAt,
         submittedByUserId, status, createdAt, updatedAt]
      properties:
        id:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Submission id; the `evidenceId` accepted by the remove route.
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Client resolved by the backend from the Vendor Work's Work Order.
        vendorWorkId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Vendor Work the evidence is bound to.
        evidenceRequirementId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Requirement the submission was bound to; null when submitted
            without one.
        evidenceType: { $ref: "#/components/schemas/VendorWorkEvidenceType" }
        fileReference:
          type: string
          maxLength: 512
          description: >
            Opaque reference of the stored object as kept by the shared engine;
            not a URL contract, not a download endpoint and not a document id.
        originalFileName:
          type: string
          maxLength: 255
          description: Caller-supplied original file name (trimmed).
        mimeType:
          type: string
          maxLength: 255
          description: MIME type; validated against the submitted evidenceType's set.
        fileSize:
          type: integer
          minimum: 0
          maximum: 52428800
          description: Declared size in bytes (0..52428800, matching BE-07).
        capturedAt:
          type: string
          format: date-time
          nullable: true
          description: >
            Capture timestamp as supplied on submit and serialized by the
            engine; null when not supplied.
        submittedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Session User recorded by the backend as the submitter; never client-supplied.
        status:
          type: string
          description: >
            BE-07 submission status string. Soft removal sets it to `REMOVED`
            (the row is never deleted); per-work and requirement listings
            return ACTIVE rows only.
        createdAt:
          type: string
          format: date-time
          description: Creation timestamp.
        updatedAt:
          type: string
          format: date-time
          description: Last mutation timestamp.
    SubmitVendorWorkEvidenceRequest:
      type: object
      description: >
        BE-15E submit payload for `POST /vendor-works/{id}/evidence`. JSON file
        metadata only - there is no multipart, binary or chunked upload route
        registered for Vendor Work evidence. `vendorWorkId` comes from the path,
        `clientId` is resolved by the backend from the Work Order and
        `submittedByUserId` from the session, so none of the three is accepted
        here; `status`, `id`, `createdAt` and `updatedAt` are likewise
        server-assigned. Unknown body fields are ignored.
      required:
        [evidenceType, fileReference, originalFileName, mimeType, fileSize]
      properties:
        evidenceType:
          allOf: [{ $ref: "#/components/schemas/VendorWorkEvidenceType" }]
          description: Required evidence type; must match the bound requirement when one is given.
        evidenceRequirementId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Optional requirement to bind to. It must belong to this Vendor Work
            and share the same `evidenceType`, otherwise 400
            `VENDOR_WORK_EVIDENCE_REQUIREMENT_MISMATCH`. Omitting it submits
            unbound evidence.
        fileReference:
          type: string
          maxLength: 512
          description: Required non-empty reference of the already-stored object (trimmed).
        originalFileName:
          type: string
          maxLength: 255
          description: Required non-empty original file name (trimmed).
        mimeType:
          type: string
          maxLength: 255
          description: >
            Required non-empty MIME type; must be in the accepted set for
            `evidenceType` (PHOTO: image/jpeg, image/png, image/webp;
            DOCUMENT: application/pdf, image/jpeg, image/png; SIGNATURE:
            image/png, image/svg+xml), otherwise 400
            `VENDOR_WORK_EVIDENCE_REQUIREMENT_MISMATCH`.
        fileSize:
          type: integer
          minimum: 0
          maximum: 52428800
          description: Required integer size in bytes (0..52428800 = 50 MB, matching BE-07).
        capturedAt:
          type: string
          maxLength: 64
          description: >
            Optional capture timestamp text (trimmed, at most 64 characters;
            empty or null is accepted as absent).
    VendorWorkHistoryEvent:
      type: object
      description: >
        BE-15K projection of one BE-07 operational event scoped to a Vendor Work
        or to its BE-15A assignment. Read-only and append-oriented: nothing here
        is mutable through the API. `metadata` and `related` are opaque
        server-generated objects whose keys depend on `eventType`, and
        `related` holds the operational references resolved from that metadata.
      required:
        [id, vendorWorkId, clientId, buildingId, eventType, entityType,
         entityId, actorUserId, summary, metadata, related, occurredAt, createdAt]
      properties:
        id:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Event id.
        vendorWorkId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Vendor Work the history was read for.
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Client the event belongs to.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Building context of the event when it carries one.
        eventType:
          type: string
          description: >
            Open, stable event-type string written by the BE-15 domain services
            (e.g. `VENDOR_WORK_STATUS_CHANGED`, `VENDOR_WORK_EVIDENCE_ADDED`).
            Unknown values must be tolerated; this API never defines a closed
            set and exposes no transition rules of its own.
        entityType:
          type: string
          description: Entity kind the event refers to.
        entityId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Identifier of the entity the event refers to.
        actorUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Session User that caused the event; null for system-written events.
        summary:
          type: string
          description: Human-readable one-line summary.
        metadata:
          type: object
          description: Server-generated event payload; credentials and sensitive values are scrubbed by the shared BE-07 writer.
        related:
          type: object
          description: Operational references resolved from the event metadata; keys depend on `eventType`.
        occurredAt:
          type: string
          format: date-time
          description: When the event occurred; the chronological ordering key.
        createdAt:
          type: string
          format: date-time
          description: When the event row was written.

    # ------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 04C1 - J12 Vendor Completion Reports
    # (BE-15F)
    # ------------------------------------------------------------------
    VendorCompletionReportStatus:
      type: string
      enum: [DRAFT, SUBMITTED]
      description: >
        BE-15F report lifecycle, the only one this module owns: `DRAFT` ->
        `SUBMITTED`. `SUBMITTED` is final and immutable - no reversion to
        `DRAFT`, no resubmission, no delete and no re-open is exposed. It is a
        reporting state, not an acceptance or approval state, and it is
        independent of the Vendor Work status enum and of any BAST lifecycle.
    VendorCompletionReport:
      type: object
      description: >
        BE-15F `PublicVendorCompletionReport` projection returned by all five
        Vendor Completion Report operations. It reports on a Vendor Work and its
        Work Order by reference only - no Vendor Work, Work Order, evidence,
        BAST or acceptance object is embedded, and `vendorId` /
        `vendorAssignmentId` are not part of this representation at all.
      required:
        [id, clientId, vendorWorkId, workOrderId, buildingId, completionStatus,
         summary, notes, completedByUserId, completedAt, evidenceReady,
         missingEvidenceTypes, createdByUserId, createdAt, updatedAt]
      properties:
        id:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Server-assigned completion report id (the `reportId` path value).
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Client resolved by the backend from the Work Order; never client-supplied.
        vendorWorkId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: The BE-15B Vendor Work this report is about (one report per work).
        workOrderId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Work Order referenced through the Vendor Work; read-only linkage, not a closure target.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Building copied from the resolved Work Order and validated against
            the Vendor Work (400 `COMPLETION_REPORT_BUILDING_MISMATCH` on
            divergence); it is also the BE-02 isolation scope of every read and
            write of this report.
        completionStatus: { $ref: "#/components/schemas/VendorCompletionReportStatus" }
        summary:
          type: string
          nullable: true
          maxLength: 4000
          description: Completion summary; null when never supplied or cleared.
        notes:
          type: string
          nullable: true
          maxLength: 4000
          description: Additional notes; null when never supplied or cleared.
        completedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            User whose submission finalized the report; null while DRAFT. A
            completion-report actor, not an approver, verifier or acceptor.
        completedAt:
          type: string
          format: date-time
          nullable: true
          description: >
            Finalization timestamp set by the backend at submission; null while
            DRAFT. It is not an acceptance date, a handover date or an invoice
            date.
        evidenceReady:
          type: boolean
          description: >
            Snapshot of the BE-07 / BE-15E required-evidence readiness: computed
            at create, refreshed on each DRAFT edit, and true after a successful
            submission. A stored `false` never blocks reading or drafting, and
            `true` is not an evidence-verification verdict.
        missingEvidenceTypes:
          type: array
          description: >
            Evidence types whose bound BE-15E requirement is below its
            `minimumCount` at snapshot time; empty when ready. Values are BE-15E
            evidence types (`PHOTO` | `DOCUMENT` | `SIGNATURE`), and this list is
            data only - it grants no requirement waiver and triggers no rework.
          items: { type: string }
        createdByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Session User that created the draft; never client-supplied.
        createdAt:
          type: string
          format: date-time
          description: Creation timestamp.
        updatedAt:
          type: string
          format: date-time
          description: Last mutation timestamp (DRAFT edit or submission).
    CreateVendorCompletionReportRequest:
      type: object
      description: >
        BE-15F create payload for `POST /vendor-completion-reports`. Only
        `vendorWorkId` plus the two free-text fields are accepted: the report
        always starts as `DRAFT`, so `completionStatus` is not settable, and
        `id`, `clientId`, `workOrderId`, `buildingId`, `completedByUserId`,
        `completedAt`, `evidenceReady`, `missingEvidenceTypes`,
        `createdByUserId`, `createdAt` and `updatedAt` are derived or
        server-assigned. Unknown body fields are ignored.
      required: [vendorWorkId]
      properties:
        vendorWorkId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Required Vendor Work id. It must exist, its Work Order must exist and
            their Buildings must match, and the report must not already exist for
            it. A caller cannot target a Work Order or a Building directly.
        summary:
          type: string
          nullable: true
          maxLength: 4000
          description: >
            Optional completion summary (trimmed, at most 4000 characters). An
            empty/whitespace string or null is stored as null, not rejected.
        notes:
          type: string
          nullable: true
          maxLength: 4000
          description: Optional notes; trimmed, empty/whitespace or null stored as null.
    UpdateVendorCompletionReportRequest:
      type: object
      description: >
        BE-15F update payload for `PATCH /vendor-completion-reports/{reportId}`,
        accepted only while the report is `DRAFT` (409
        `COMPLETION_REPORT_ALREADY_SUBMITTED` otherwise). At least one of
        `summary` or `notes` must be present (400 `VALIDATION_ERROR`, validation
        detail field `body`). An omitted key keeps its stored value; sending
        null or an empty string clears it to null. No other field is mutable:
        `completionStatus`, the linkage ids, the actor fields, the readiness
        snapshot and the timestamps are rejected by omission - they are silently
        ignored and never applied - and submission is not a PATCH concern.
      properties:
        summary:
          type: string
          nullable: true
          maxLength: 4000
          description: Replacement completion summary (trimmed, at most 4000 characters).
        notes:
          type: string
          nullable: true
          maxLength: 4000
          description: Replacement notes (trimmed, at most 4000 characters).

    # ------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 04C2 - J12 Vendor Rework (BE-15J)
    # + Vendor Work Verification (BE-15I)
    # ------------------------------------------------------------------
    VendorReworkStatus:
      type: string
      enum: [REQUESTED, RESUBMITTED]
      description: >
        BE-15J rework cycle lifecycle: `REQUESTED` -> `RESUBMITTED`. Only a
        `REQUESTED` cycle is the "current" one and only it is editable; a
        `RESUBMITTED` cycle is immutable forever, is never deleted and is never
        overwritten by a later cycle. It is a rework-cycle state, not a Vendor
        Work state, and it never implies acceptance or Work Order closure.
    VendorRework:
      type: object
      description: >
        BE-15J `PublicVendorRework` projection - one rework cycle of one Vendor
        Work. It links to the verification review that produced it and to the
        work by id only: no verification, completion report, evidence, BAST,
        acceptance or Work Order object is embedded.
      required:
        [id, vendorWorkId, reviewId, requestedByUserId, reason, reworkNotes,
         resubmittedByUserId, requestedAt, resubmittedAt, status, createdAt,
         updatedAt]
      properties:
        id:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Server-assigned rework cycle id.
        vendorWorkId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Vendor Work the cycle belongs to; taken from the path, never the body.
        reviewId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            The BE-07 review row of the `REWORK_REQUIRED` verification this
            cycle is anchored to; assigned by the backend from the latest
            verification, and one cycle per review is enforced.
        requestedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Session User that requested rework; never client-supplied.
        reason:
          type: string
          maxLength: 2000
          description: >
            Why the work must be reworked (trimmed, 1..2000). Immutable after
            creation on every documented route.
        reworkNotes:
          type: string
          nullable: true
          maxLength: 2000
          description: >
            Working notes of the cycle; the only mutable field while
            `REQUESTED`. Null until first written and never clearable.
        resubmittedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: User who resubmitted; null while `REQUESTED`.
        requestedAt:
          type: string
          format: date-time
          description: Backend timestamp of the rework request.
        resubmittedAt:
          type: string
          format: date-time
          nullable: true
          description: Backend timestamp of the resubmission; null while `REQUESTED`.
        status: { $ref: "#/components/schemas/VendorReworkStatus" }
        createdAt:
          type: string
          format: date-time
          description: Creation timestamp.
        updatedAt:
          type: string
          format: date-time
          description: Last mutation timestamp (notes edit or resubmission).
    VendorReworkContext:
      type: object
      description: >
        BE-15J resolved rework state of a Vendor Work returned by
        `GET /vendor-works/{id}/rework`. Read-only bookkeeping: it mirrors the
        work's own `vendorWorkStatus` without changing it and exposes no
        acceptance, BAST, invoice or closure state.
      required: [vendorWorkId, vendorWorkStatus, buildingId, current, cycles]
      properties:
        vendorWorkId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: The Vendor Work this context describes.
        vendorWorkStatus:
          type: string
          description: >
            Current BE-15B Vendor Work status as found (`NOT_STARTED` /
            `IN_PROGRESS` / `ON_HOLD` / `COMPLETED`); echoed, never mutated by
            this module.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Building resolved from the Work Order, and the BE-02 isolation scope.
        current:
          allOf: [{ $ref: "#/components/schemas/VendorRework" }]
          nullable: true
          description: >
            The most recently requested cycle still in `REQUESTED` - the only
            cycle `PATCH .../rework` and `POST .../resubmit` act on - or null
            when there is none.
        cycles:
          type: array
          description: >
            All rework cycles of the work, append-preserving, ordered by
            `requestedAt` then `id` ascending.
          items: { $ref: "#/components/schemas/VendorRework" }
    RequestVendorReworkRequest:
      type: object
      description: >
        BE-15J payload of `POST /vendor-works/{id}/rework`. Only `reason` is
        accepted; the anchor `reviewId`, `vendorWorkId`, `requestedByUserId`,
        `status` and all timestamps are derived by the backend, and unknown body
        fields are ignored.
      required: [reason]
      properties:
        reason:
          type: string
          maxLength: 2000
          description: >
            Required, non-empty after trimming, at most 2000 characters. A
            blank or whitespace-only value is 400 `VALIDATION_ERROR`.
    VendorReworkNotesRequest:
      type: object
      description: >
        BE-15J payload shared by the two note-taking routes -
        `PATCH /vendor-works/{id}/rework` (notes on the current cycle) and
        `POST /vendor-works/{id}/resubmit` (notes recorded with the
        resubmission) - because both parse the identical runtime shape. Only
        `notes` is accepted; status and timestamps cannot be sent, and `notes`
        cannot be cleared or omitted.
      required: [notes]
      properties:
        notes:
          type: string
          maxLength: 2000
          description: >
            Required, non-empty after trimming, at most 2000 characters.
    VendorVerificationDecision:
      type: string
      enum: [APPROVED, REJECTED, REWORK_REQUIRED]
      description: >
        BE-15I verification decision - the shared BE-07 review vocabulary, not a
        Vendor-specific enum. `APPROVED` is final for the work (it blocks any
        later verification) and records a verification outcome only: it is not
        acceptance, sign-off, handover approval, invoice approval or Work Order
        closure. `REWORK_REQUIRED` only records the decision - it neither opens
        nor mutates a rework cycle. No value changes the Vendor Work `status`.
    VendorVerification:
      type: object
      description: >
        BE-15I `PublicVendorVerification` projection - one appended BE-07 review
        row used as a Vendor Work verification. Rows are immutable through this
        contract: no edit, withdrawal or delete is exposed, and every submission
        creates a new one.
      required:
        [id, clientId, vendorWorkId, reviewerUserId, decision, notes,
         reviewedAt, status]
      properties:
        id:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Review row id; it is the `reviewId` a later rework cycle anchors to.
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Client resolved by the backend from the Work Order.
        vendorWorkId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Vendor Work the decision was recorded against.
        reviewerUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Session User that submitted the decision; never client-supplied.
        decision: { $ref: "#/components/schemas/VendorVerificationDecision" }
        notes:
          type: string
          nullable: true
          maxLength: 4000
          description: Reviewer notes; null when absent, empty or null in the request.
        reviewedAt:
          type: string
          format: date-time
          description: Backend review timestamp.
        status:
          type: string
          description: >
            Status string of the underlying BE-07 review row, carried through as
            stored. It is a review state only - never a Vendor Work, acceptance,
            BAST, invoice or closure state.
    VendorVerificationState:
      type: object
      description: >
        BE-15I resolved verification state returned by
        `GET /vendor-works/{id}/verification`: the verification history plus the
        read-only completion / service / BAST context a reviewer sees. Every
        context member is a reference projection, not an authority: reading this
        state neither accepts, closes, signs off nor approves anything, and the
        listed records are never created or mutated by this module.
      required:
        [vendorWorkId, vendorWorkStatus, buildingId, completionReport,
         serviceReport, bast, latestVerification, verifications]
      properties:
        vendorWorkId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: The Vendor Work this state describes.
        vendorWorkStatus:
          type: string
          description: >
            Current BE-15B Vendor Work status as found (`NOT_STARTED` /
            `IN_PROGRESS` / `ON_HOLD` / `COMPLETED`); verification is only
            accepted while `COMPLETED` and never changes this value.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Building of the Work Order, and the BE-02 isolation scope.
        completionReport:
          type: object
          nullable: true
          description: >
            The BE-15F completion report of this work, or null when none exists.
            A `DRAFT` report blocks verification with 400
            `VENDOR_VERIFICATION_COMPLETION_NOT_SUBMITTED`; reading it here
            grants nothing.
          required: [id, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            status: { $ref: "#/components/schemas/VendorCompletionReportStatus" }
        serviceReport:
          type: object
          nullable: true
          description: >
            The vendor service report reference of this work, or null when none
            exists. Its status is carried through as stored and belongs to a
            surface not documented by this contract; it is never written here.
          required: [id, status]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            status:
              type: string
        bast:
          type: object
          nullable: true
          description: >
            The BAST reference of this work, or null when none exists.
            `acceptanceStatus` is a read-only projection of the BAST surface: it
            is not written, changed or evaluated by BE-15I, a verification
            decision never sets it, and a `null` here does not mean the work is
            unaccepted.
          required: [id, acceptanceStatus]
          properties:
            id: { $ref: "#/components/schemas/Uuid" }
            acceptanceStatus:
              type: string
        latestVerification:
          allOf: [{ $ref: "#/components/schemas/VendorVerification" }]
          nullable: true
          description: >
            The last row of the `verifications` ordering (the most recently
            created review), or null when nothing has been verified. This is the
            row a following `POST /vendor-works/{id}/rework` anchors to when its
            decision is `REWORK_REQUIRED`.
        verifications:
          type: array
          description: >
            All verification rows of the work, ordered by their creation time
            ascending.
          items: { $ref: "#/components/schemas/VendorVerification" }
    SubmitVendorVerificationRequest:
      type: object
      description: >
        BE-15I payload of `POST /vendor-works/{id}/verification`. Only
        `decision` and optional `notes` are accepted: `vendorWorkId` comes from
        the path and `id`, `clientId`, `reviewerUserId`, `reviewedAt` and
        `status` are server-assigned, so no reviewer identity, timestamp, review
        status or evidence reference can be asserted by the caller. Unknown body
        fields are ignored.
      required: [decision]
      properties:
        decision:
          allOf: [{ $ref: "#/components/schemas/VendorVerificationDecision" }]
          description: Required verification decision.
        notes:
          type: string
          maxLength: 4000
          description: >
            Optional reviewer notes (trimmed, at most 4000 characters). An empty
            string or null is accepted as absent and stored as null; a
            non-string value is 400 `VALIDATION_ERROR`.

    # ------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 04D1 - J12 Acceptance Sign-Offs
    # (BE-22E)
    # ------------------------------------------------------------------
    SignOffDecision:
      type: string
      enum: [ACCEPTED, REJECTED]
      description: >
        BE-22E recorded decision - the only decision vocabulary of this module.
        It is case-insensitive on input (trimmed and upper-cased before
        validation) and stored upper-cased. Neither value mutates the target
        document: `ACCEPTED` is not BAST acceptance, Work Order closure, Vendor
        Work completion, invoice or payment approval, nor handover completion,
        and `REJECTED` revokes, reverses and invalidates nothing. There is no
        third decision kind, no conditional approval and no score.
    AcceptanceSignOff:
      type: object
      description: >
        BE-22E `PublicAcceptanceSignOff` projection of one recorded sign-off. It
        is an immutable audit row, not the live state of any other surface: the
        target documents are referenced by id only and no BAST, Handover,
        document-version, Work Order, Vendor Work or approval object is
        embedded. There is no `status`, no approval stage and no second actor -
        `signerUserId` is the only actor field and it always equals the user who
        submitted the request.
      required:
        [id, bastDocumentId, handoverDocumentId, bastSubmissionAttemptId,
         documentVersionId, clientId, buildingId, contextType, decision,
         signerUserId, notes, signedAt, createdAt, updatedAt]
      properties:
        id:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Sign-off row id, generated by the backend at insert.
        bastDocumentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Canonical BAST document the row is linked to, or null for a
            Handover-linked sign-off. Always null on rows created through this
            contract (BAST targets are refused on submit); BAST-linked rows exist
            only as history and stay readable via the list filter.
        handoverDocumentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: Handover document the row is linked to; exactly one of the two target ids is set.
        bastSubmissionAttemptId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Reserved link to a BAST submission attempt. No registered route
            populates or clears it, so it is always null through this surface.
        documentVersionId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Reserved link to a document version. No registered route populates or
            clears it, so it is always null through this surface even though the
            submit path reads the target document's status.
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Client copied from the resolved target document.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Building copied from the resolved target document; it is also the
            isolation scope of every read of this row and the target of the
            signer authorization check.
        contextType:
          type: string
          enum: [INTERNAL, TENANT, VENDOR]
          description: >
            Sign-off context declared by this module and copied from the target
            document. It states whose acceptance perspective the row records; it
            grants no authority and is not a lifecycle state of the target.
        decision: { $ref: "#/components/schemas/SignOffDecision" }
        signerUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            The user whose signature this row records. The submitting actor is
            always the signer - there is no delegated, countersigning or named
            approver field in the request, and no actor is inferred from the
            Vendor Work, Work Order or verification records.
        notes:
          type: string
          nullable: true
          maxLength: 4000
          description: Free-text note stored with the decision; null when absent or blank.
        signedAt:
          type: string
          format: date-time
          description: >
            Backend insert-time timestamp of the signature. It is never
            client-supplied and never rewritten, and it is not an acceptance date
            of the underlying document.
        createdAt:
          type: string
          format: date-time
          description: Row creation timestamp.
        updatedAt:
          type: string
          format: date-time
          description: >
            Row update timestamp. No route updates a sign-off, so in practice it
            matches `createdAt` for rows created through this contract.
    SubmitAcceptanceSignOffRequest:
      type: object
      description: >
        BE-22E payload of `POST /acceptance-sign-offs`. Exactly one of
        `bastDocumentId` / `handoverDocumentId` must be present (an explicit
        null counts as absent); supplying both or neither is 400
        `VALIDATION_ERROR` reported on field `bastDocumentId`. Despite accepting
        the shape, the runtime refuses BAST targets outright: any
        `bastDocumentId` is answered with 400 `ACCEPTANCE_INVALID_CONTEXT`
        directing callers to the canonical BAST decision command, so a
        successful submission here is always Handover-linked. No other field is
        accepted: `clientId`, `buildingId`, `contextType`, `signerUserId`,
        `signedAt` and the reserved links are derived or null, and unknown body
        fields are ignored.
      required: [decision]
      properties:
        bastDocumentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Canonical BAST document id. Accepted by request validation, then
            rejected by the service with 400 `ACCEPTANCE_INVALID_CONTEXT`; use
            the canonical BAST decision command instead.
        handoverDocumentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Target Handover document id. It must exist, its underlying document
            row must exist and must not be `ARCHIVED`, and its Building must be
            accessible to the caller.
        decision:
          allOf: [{ $ref: "#/components/schemas/SignOffDecision" }]
          description: >
            Required decision (`ACCEPTED` or `REJECTED`, case-insensitive on
            input). A blank or unknown value is 400 `VALIDATION_ERROR`.
        notes:
          type: string
          nullable: true
          maxLength: 4000
          description: >
            Optional note (trimmed, at most 4000 characters). Empty string or
            null is stored as null; a non-string value is 400
            `VALIDATION_ERROR` ("notes must be a string or null").
    DocumentContextType:
      type: string
      enum: [INTERNAL, TENANT, VENDOR]
      description: >
        Shared BE-22A document context (`DOCUMENT_CONTEXT_TYPES`), re-used
        verbatim by the Work Completion Document module. It states whose
        document this is; it is not a lifecycle state and grants no authority
        over the linked Work Order or Vendor Work. Input is trimmed and
        upper-cased before the enum check.
    DocumentSourceType:
      type: string
      enum: [TENANT_COMPANY, VENDOR, INTERNAL]
      description: >
        Shared BE-22A source kind (`DOCUMENT_SOURCE_TYPES`). It must be paired
        with `sourceId` and with `contextType`: `INTERNAL` context allows only
        `INTERNAL` or no source, `TENANT` requires `TENANT_COMPANY` and `VENDOR`
        requires `VENDOR`. A nullable, informational origin marker - it does not
        select a workflow, an approver or a downstream module.
    DocumentStatus:
      type: string
      enum: [DRAFT, ACTIVE, INACTIVE, ARCHIVED]
      description: >
        Shared BE-22A document status (`DOCUMENT_STATUSES`). On this contract the
        status is settable only at creation - default `DRAFT`, `ARCHIVED` refused
        - and otherwise filterable and readable; no route here approves,
        activates, deactivates or archives a document.
    Document:
      type: object
      description: >
        The shared BE-22A document projection (`PublicDocument`) exactly as the
        Work Completion Document routes embed it. It is the only document shape
        exposed by this contract: no standalone `/documents` endpoint, search,
        version list, upload or download route is claimed here. Every field is a
        stored value or a backend-assigned timestamp - there is no checksum,
        hash, size, mime type, storage bucket, object key, OCR result,
        retention or expiry policy and no document version/revision number in
        this representation, and `fileReference` is only an opaque pointer whose
        retrieval is out of scope.
      required:
        [id, clientId, buildingId, documentNumber, documentType, contextType,
         sourceType, sourceId, title, description, fileReference, status,
         expiryDate, archivedAt, archivedByUserId, archiveReason,
         statusBeforeArchive, createdByUserId, createdAt, updatedAt]
      properties:
        id:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Shared document row id; it is also this binding's `documentId`.
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Client that owns the document.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Building the document belongs to. Nullable in the shared shape; on
            this route it is always the Building the binding was created with,
            because both the request and the work context must agree on it.
        documentNumber:
          type: string
          maxLength: 64
          description: >
            Human document number, trimmed and stored as supplied; unique inside
            `clientId` (409 `DOCUMENT_NUMBER_ALREADY_EXISTS`).
        documentType:
          type: string
          maxLength: 64
          description: >
            Free-form type code validated only by shape - 2 to 64 characters of
            `[A-Z][A-Z0-9_-]*` after upper-casing. There is no registered
            allow-list, so no document type carries special behaviour.
        contextType: { $ref: "#/components/schemas/DocumentContextType" }
        sourceType:
          allOf: [{ $ref: "#/components/schemas/DocumentSourceType" }]
          nullable: true
          description: Originating source kind; null when the context carries no source.
        sourceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Resolved Tenant Company or Vendor id for TENANT / VENDOR contexts;
            null for an INTERNAL document with no source. It is a reference only -
            no tenant or vendor object is embedded and no vendor state is changed.
        title:
          type: string
          maxLength: 255
          description: Document title, trimmed.
        description:
          type: string
          nullable: true
          maxLength: 2000
          description: Optional free-text description; null when absent or blank.
        fileReference:
          type: string
          nullable: true
          maxLength: 512
          description: >
            Opaque pointer to the stored file, exactly as supplied. The runtime
            neither uploads, lists, downloads nor verifies it here, so no
            provider, bucket, key, size or content type is exposed.
        status: { $ref: "#/components/schemas/DocumentStatus" }
        expiryDate:
          type: string
          format: date
          nullable: true
          description: >
            Optional document expiry date held by the shared foundation. It is
            not accepted by this contract's request body, so documents created
            through these routes always carry null.
        archivedAt:
          type: string
          format: date-time
          nullable: true
          description: >
            Archive timestamp of the shared foundation. No archive action is
            registered on this route, so it is always null here.
        archivedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: User who archived the document; always null through this surface.
        archiveReason:
          type: string
          nullable: true
          description: Free-text archive reason; always null through this surface.
        statusBeforeArchive:
          type: string
          enum: [DRAFT, ACTIVE, INACTIVE]
          nullable: true
          description: >
            Status the shared foundation restores on un-archive. Always null on
            documents created and read through these routes.
        createdByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Authenticated actor recorded by the backend as the document creator.
        createdAt:
          type: string
          format: date-time
          description: Document creation timestamp.
        updatedAt:
          type: string
          format: date-time
          description: >
            Document update timestamp as stored. Nothing on this contract edits a
            document, so for rows created here it matches `createdAt`.
    WorkCompletionDocument:
      type: object
      description: >
        BE-22B `PublicWorkCompletionDocument` projection: the binding row with
        its identifiers plus the shared document it points at. No Work Order,
        Vendor Work, BAST, Acceptance Sign-Off, Handover Document or Vendor
        Completion Report object is embedded, and no completion, acceptance or
        closure state of any adjacent module is mirrored here - the work status
        is read only to validate creation. There is no version, no signature and
        no approval field, and no `notes` field on this binding.
      required:
        [id, documentId, workOrderId, vendorWorkId, clientId, buildingId,
         contextType, createdByUserId, createdAt, updatedAt, document]
      properties:
        id:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Binding row id, generated by the backend at insert.
        documentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            The shared BE-22A document this binding points at; the same id as
            `document.id`. One binding per document is enforced by the database
            (409 `WORK_COMPLETION_ALREADY_EXISTS`), and it is the only
            uniqueness guarantee on this table.
        workOrderId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Work Order the completion document is attached to. It is a reference
            copied from the validated request - the Work Order is not mutated,
            not closed and not re-opened by anything on this contract.
        vendorWorkId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Vendor Work narrowed to, or null when the document covers the whole
            Work Order. Optional at creation; a supplied one must already be
            `COMPLETED` and belong to the Work Order.
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Client copied from the validated request and checked against the work context.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Building copied from the validated request; it is also the isolation
            scope of every read of this row and the target of the access check.
        contextType: { $ref: "#/components/schemas/DocumentContextType" }
        createdByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Authenticated actor recorded by the backend as the creator. It is the
            only actor field on this surface: there is no submitter, executor,
            completer, signer or approver field, and no actor is inferred from
            the Work Order, Vendor Work, report or acceptance records.
        createdAt:
          type: string
          format: date-time
          description: >
            Binding creation timestamp; also the sole ordering key of the list
            route (descending). It is not a completion date of the work.
        updatedAt:
          type: string
          format: date-time
          description: >
            Row update timestamp. No registered route updates or deletes a
            binding, so in practice it matches `createdAt`.
        document: { $ref: "#/components/schemas/Document" }
    CreateWorkCompletionDocumentRequest:
      type: object
      description: >
        BE-22B payload of `POST /work-completion-documents`. Every UUID field is
        trimmed, lower-cased and must be a valid UUID; every string field is
        trimmed and blank-but-supplied optional strings are stored as null.
        `sourceType` and `sourceId` must be supplied together and must pair with
        `contextType`. Unknown properties are ignored, and no actor, tenant,
        timestamp, expiry or file-metadata field is accepted.
      required:
        [clientId, buildingId, documentNumber, documentType, contextType, title,
         workOrderId]
      properties:
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Owner Client; must exist and must be the Work Order's Client.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Owner Building; must exist, must belong to `clientId`, must be the
            Work Order's (and any Vendor Work's) Building, and must be accessible
            to the actor.
        documentNumber:
          type: string
          maxLength: 64
          description: Document number, unique inside the Client.
        documentType:
          type: string
          minLength: 2
          maxLength: 64
          pattern: "^[A-Z][A-Z0-9_-]{1,63}$"
          description: >
            Type code. Upper-cased before validation, so lower-case input is
            accepted and stored upper-cased; anything outside the pattern is 400
            `VALIDATION_ERROR`.
        contextType: { $ref: "#/components/schemas/DocumentContextType" }
        sourceType:
          allOf: [{ $ref: "#/components/schemas/DocumentSourceType" }]
          nullable: true
          description: >
            Optional source kind. Omitted or null means no source, which is only
            valid for an `INTERNAL` context; it is reported on field
            `sourceType` when unpaired.
        sourceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Tenant Company id (TENANT) or Vendor id (VENDOR). Required together
            with `sourceType`, must resolve and must belong to `clientId`. Not a
            vendor assignment or approval - it only labels the document source.
        title:
          type: string
          maxLength: 255
          description: Document title; required and non-blank.
        description:
          type: string
          maxLength: 2000
          nullable: true
          description: Optional free-text description.
        fileReference:
          type: string
          maxLength: 512
          nullable: true
          description: >
            Opaque pointer to an already-stored file. This route performs no
            upload: there is no multipart body, no binary field, no storage
            provider, no size or content-type check and no download or deletion
            of the referenced file.
        status:
          allOf: [{ $ref: "#/components/schemas/DocumentStatus" }]
          description: >
            Initial status of the created document; `DRAFT` when omitted, and
            `ARCHIVED` refused (409 `DOCUMENT_UPDATE_NOT_ALLOWED`). Null is not
            accepted. It labels the document only - it does not complete, accept,
            verify or close the linked work.
        workOrderId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Work Order to attach the document to. It must already be `COMPLETED`;
            this route never transitions it.
        vendorWorkId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Optional Vendor Work narrowing. It must belong to `workOrderId`,
            share its Building and already be `COMPLETED`; supplying it neither
            marks the Vendor Work complete nor mutates its history.
    HandoverStatus:
      type: string
      enum: [DRAFT, HANDED_OVER]
      description: >
        Handover row status (`HANDOVER_STATUSES`), separate from and not
        interchangeable with the shared document `status`. It states where the
        handover record itself stands; it is not an acceptance, approval,
        verification or completion state of the Work Order, the Vendor Work, the
        Work Completion Document or the BAST, and it never feeds an invoice,
        payment or customer-satisfaction decision. Input is trimmed and
        upper-cased before the enum check on the list filter; the create route
        does not accept this field at all, and every row it writes starts as
        `DRAFT`.
    HandoverDocument:
      type: object
      description: >
        BE-22D `PublicHandoverDocument` projection: the handover row, its
        references and the shared document it points at. The work references are
        ids only - no Work Order, Vendor Work, Work Completion Document, BAST,
        Acceptance Sign-Off or Vendor Completion Report object is embedded, and
        no acceptance, verification or closure state is mirrored here. There is
        no version number, no signature image, no approval stage and no second
        approver: `preparedByUserId` is the only populated actor, and the
        handover-execution pair stays null on this contract.
      required:
        [id, documentId, workOrderId, vendorWorkId, workCompletionDocumentId,
         bastDocumentId, clientId, buildingId, contextType, handoverNumber,
         handoverDate, handoverStatus, notes, fileReference, preparedByUserId,
         handedOverByUserId, handedOverAt, createdAt, updatedAt, document]
      properties:
        id:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Handover row id, generated by the backend at insert.
        documentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            The shared BE-22A document this handover was created with; the same
            id as `document.id`. Exactly one handover may point at a document -
            the column is unique - and that is the only document-level guarantee
            on this table.
        workOrderId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Work Order the handover is attached to, copied from the validated
            request. The Work Order is not mutated, closed or re-opened by
            anything on this contract.
        vendorWorkId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Vendor Work narrowed to, or null when the handover covers the whole
            Work Order. Supplying one requires that Vendor Work to be
            `COMPLETED` and blocks a second handover for the same Vendor Work
            (409 `HANDOVER_ALREADY_EXISTS`).
        workCompletionDocumentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Optional reference to the BE-22B Work Completion Document that
            documents this work. It is stored and filterable, nothing more: the
            completion document is not read back, edited or created here, and a
            handover may exist without one.
        bastDocumentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Optional reference to a canonical BAST document. Creation only
            checks that the BAST belongs to the same Work Order, Building, Client
            and (where both name one) Vendor Work; it never reads or writes BAST
            acceptance state, and the pre-existing BAST decision command stays
            the single BAST authority.
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Client copied from the validated request; also the namespace of `handoverNumber`.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Building copied from the validated request; it is also the isolation
            scope of every read of this row and the target of the access check.
        contextType: { $ref: "#/components/schemas/DocumentContextType" }
        handoverNumber:
          type: string
          maxLength: 100
          description: >
            Human handover number, trimmed and stored as supplied (no case
            folding) and unique inside `clientId`.
        handoverDate:
          type: string
          format: date
          description: >
            Declared handover date as stored, always serialised `YYYY-MM-DD`. It
            is a written-down date on the record, not a system confirmation that
            a handover took place: it says nothing about `handedOverAt`, which
            stays null on this contract.
        handoverStatus: { $ref: "#/components/schemas/HandoverStatus" }
        notes:
          type: string
          nullable: true
          maxLength: 4000
          description: >
            Free-text note held on the handover row only; null when absent or
            blank. It is not a shared-document field and is not a decision,
            remark log or signature.
        fileReference:
          type: string
          nullable: true
          maxLength: 512
          description: >
            Opaque pointer to the stored file, taken from the same request field
            the shared document receives - so `fileReference` here and
            `document.fileReference` always carry the identical value, and the
            handover row keeps no independent copy that could diverge. No
            upload, download, size, content-type or integrity check exists on
            this contract.
        preparedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Authenticated actor recorded by the backend as the preparer. It is
            the only actor field these routes populate: it is not an approver,
            acceptor or executor, and no actor is inferred from the Work Order,
            Vendor Work, Work Completion Document, BAST or sign-off records.
        handedOverByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Reserved handover-execution actor. No registered route writes it, so
            it is always null through this surface.
        handedOverAt:
          type: string
          format: date-time
          nullable: true
          description: >
            Reserved handover-execution timestamp. No registered route writes it,
            so it is always null here and must not be read as a delivery,
            acceptance or receipt time.
        createdAt:
          type: string
          format: date-time
          description: >
            Row creation timestamp; also the sole ordering key of the list route
            (descending).
        updatedAt:
          type: string
          format: date-time
          description: >
            Row update timestamp. No registered route updates or deletes a
            handover row, so in practice it matches `createdAt`.
        document: { $ref: "#/components/schemas/Document" }
    CreateHandoverDocumentRequest:
      type: object
      description: >
        BE-22D payload of `POST /handover-documents`. Every UUID field is
        trimmed, lower-cased and must be a valid UUID; every string field is
        trimmed, and blank-but-supplied optional strings are stored as null.
        `sourceType` and `sourceId` must be supplied together and must pair with
        `contextType`. Unknown properties are ignored. `handoverStatus`,
        `handedOverByUserId`, `handedOverAt`, `preparedByUserId`, all
        timestamps and the shared document's expiry/archive fields are not
        accepted - so a request cannot mark a handover as completed, and no
        file-metadata or signature field exists.
      required:
        [clientId, buildingId, contextType, title, documentNumber, documentType,
         workOrderId, handoverNumber, handoverDate]
      properties:
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: Owner Client; must exist and must be the Work Order's Client.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Owner Building; must exist, must belong to `clientId`, must match the
            Work Order and every supplied reference, and must be accessible to
            the actor.
        contextType: { $ref: "#/components/schemas/DocumentContextType" }
        sourceType:
          allOf: [{ $ref: "#/components/schemas/DocumentSourceType" }]
          nullable: true
          description: >
            Optional source kind for the shared document. Omitted or null means
            no source, valid only for an `INTERNAL` context; it is reported on
            field `sourceType` when unpaired.
        sourceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Tenant Company id (TENANT) or Vendor id (VENDOR). Required together
            with `sourceType`, must resolve, must belong to `clientId`, and for a
            `VENDOR` context with a `vendorWorkId` must be that Vendor Work's
            vendor. It labels the document source only - it assigns no vendor
            work and approves nothing.
        title:
          type: string
          maxLength: 255
          description: Shared-document title; required and non-blank.
        description:
          type: string
          maxLength: 2000
          nullable: true
          description: Optional free-text description held on the shared document.
        fileReference:
          type: string
          maxLength: 512
          nullable: true
          description: >
            Opaque pointer to an already-stored file; copied onto both the shared
            document and the handover row. This route performs no upload: there is
            no multipart body, no binary field, no storage provider, no size or
            content-type check and no download or deletion.
        status:
          allOf: [{ $ref: "#/components/schemas/DocumentStatus" }]
          description: >
            Initial status of the created shared document; `DRAFT` when omitted
            and `ARCHIVED` refused (409 `DOCUMENT_UPDATE_NOT_ALLOWED`). Null is
            not accepted. It labels the document only and is not the handover
            status - `handoverStatus` cannot be supplied.
        documentNumber:
          type: string
          maxLength: 64
          description: Shared-document number, unique inside the Client.
        documentType:
          type: string
          minLength: 2
          maxLength: 64
          pattern: "^[A-Z][A-Z0-9_-]{1,63}$"
          description: >
            Shared-document type code, upper-cased before validation; free-form
            with no registered allow-list, so no document type carries special
            handover behaviour.
        workOrderId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Work Order to attach the handover record to. It must already be
            `COMPLETED`; this route never transitions or closes it.
        vendorWorkId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Optional Vendor Work narrowing. It must belong to `workOrderId`,
            share its Building and already be `COMPLETED`; supplying it neither
            marks the Vendor Work complete nor mutates its history, and it limits
            that Vendor Work to one handover row.
        workCompletionDocumentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Optional BE-22B Work Completion Document reference. It must resolve
            and match the same work context; it is stored as a pointer and is
            never required, created or modified here.
        bastDocumentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Optional canonical BAST document reference. It must resolve and match
            the same work context; no BAST status or acceptance is read or
            written, and BAST decisions stay on their own canonical command.
        handoverNumber:
          type: string
          maxLength: 100
          description: >
            Handover number, trimmed and stored as supplied; unique inside
            `clientId` (409 `HANDOVER_NUMBER_ALREADY_EXISTS`).
        handoverDate:
          type: string
          format: date
          pattern: "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"
          description: >
            Declared handover date, `YYYY-MM-DD` (validated as a real date by the
            shared date-format helper). Required; there is no default and no
            server-side substitution of "today".
        notes:
          type: string
          maxLength: 4000
          nullable: true
          description: Optional free-text note stored on the handover row.

    # -------------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B1 - J19 Documents (BE-22A).
    #
    # The generic Document foundation surface. `Document` (PART 04D2) is the
    # same 20-field runtime projection seen through a Work Completion binding,
    # and its field descriptions and `expiryDate` format are written for that
    # surface, so the foundation's own projection is documented separately
    # instead of rewriting the existing component. `DocumentContextType`,
    # `DocumentSourceType` and `DocumentStatus` are re-used verbatim because the
    # foundation module is the authority that defines those three enums.
    # -------------------------------------------------------------------------
    DocumentFoundation:
      type: object
      description: >
        The generic BE-22A document row exactly as the shared foundation
        serialises it (`PublicDocument` in `document.types.ts`): stored values
        plus backend-assigned actor and timestamp fields, nothing derived. There
        is no checksum, hash, file size, mime type, storage bucket or object key,
        no OCR result, no retention or integrity state, no version number and no
        approval or sign-off field in this representation - `fileReference` is
        only an opaque pointer whose retrieval is out of scope. The Work
        Completion Document, Handover Document, BAST Document and Acceptance
        Sign-Off aggregates are separate journeys that reference this row; no
        such object is embedded here.
      required:
        [id, clientId, buildingId, documentNumber, documentType, contextType,
         sourceType, sourceId, title, description, fileReference, status,
         expiryDate, archivedAt, archivedByUserId, archiveReason,
         statusBeforeArchive, createdByUserId, createdAt, updatedAt]
      properties:
        id:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Document row id - the `id` path key of every route in this part and
            the `documentId` other journeys reference.
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Client that owns the document. It fixes the `documentNumber` unique
            space and is the isolation boundary when `buildingId` is null.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Building the document belongs to, or null for a Client-level
            document. It decides which isolation rule applies: Building access
            when set, Client access when null.
        documentNumber:
          type: string
          maxLength: 64
          description: >
            Human document number, trimmed and stored as supplied, unique inside
            `clientId`. Immutable after creation - a patch that names it is 400
            `VALIDATION_ERROR`, and renumbering does not exist on any route of
            this part.
        documentType:
          type: string
          maxLength: 64
          description: >
            Free-form type code validated only by shape: 2 to 64 characters of
            `[A-Z][A-Z0-9_-]*` after upper-casing. There is no registered
            allow-list, so no type code carries special behaviour, and no type
            selects an approver, a workflow or an adjacent module.
        contextType: { $ref: "#/components/schemas/DocumentContextType" }
        sourceType:
          allOf: [{ $ref: "#/components/schemas/DocumentSourceType" }]
          nullable: true
          description: >
            Originating source kind; null when the context carries no source. It
            is a label stored on the document only - the referenced Tenant
            Company or Vendor object is not embedded and is not read back here.
        sourceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Resolved Tenant Company or Vendor id for `TENANT` / `VENDOR`
            contexts; null for an `INTERNAL` document without a source. A
            reference only: no tenant or vendor state is created or changed.
        title:
          type: string
          maxLength: 255
          description: Document title, trimmed.
        description:
          type: string
          nullable: true
          maxLength: 2000
          description: >
            Optional free-text description. A blank or null value is stored as
            null, and a patch may clear it again with an explicit null.
        fileReference:
          type: string
          nullable: true
          maxLength: 512
          description: >
            Opaque pointer to an already-stored file, exactly as supplied. None of
            the six routes in this part uploads, lists, downloads, verifies or
            deletes that file, so no provider, bucket, key, size or content type
            is exposed.
        status: { $ref: "#/components/schemas/DocumentStatus" }
        expiryDate:
          type: string
          format: date-time
          nullable: true
          description: >
            Expiry held by the shared foundation. The stored column is a date and
            the runtime serialises it through `toISOString()`, so a value appears
            as a full RFC3339 timestamp; null when unset, which is always the
            case for a document created here because neither create nor patch
            accepts this field - Document Expiry (another J19 part) owns it.
        archivedAt:
          type: string
          format: date-time
          nullable: true
          description: >
            Server timestamp written by `POST /documents/{id}/archive` and cleared
            again by `POST /documents/{id}/restore`. Not caller-supplied.
        archivedByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Actor recorded by the archive action; null while unarchived and
            cleared on restore. It is an audit label, not a holder, approver or
            custodian identity.
        archiveReason:
          type: string
          nullable: true
          description: >
            Free-text reason stored at archive time - trimmed, at most 1000
            characters, null when the request omitted it. Cleared on restore. It
            is not a comment channel: no reply, thread or notification is created
            from it.
        statusBeforeArchive:
          type: string
          enum: [DRAFT, ACTIVE, INACTIVE]
          nullable: true
          description: >
            The status the archive action snapshotted and that restore returns to;
            null while the document is unarchived (and after a restore). `ARCHIVED`
            can never appear here.
        createdByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Authenticated actor the backend recorded as the document creator at
            insert.
        createdAt:
          type: string
          format: date-time
          description: Document creation timestamp.
        updatedAt:
          type: string
          format: date-time
          description: >
            Last stored modification timestamp. Only a patch that actually writes
            a field, or an archive / restore write, moves it; an empty-object patch
            leaves it untouched and a freshly created document has `updatedAt`
            equal to `createdAt`.
    CreateDocumentRequest:
      type: object
      description: >
        BE-22A payload of `POST /documents`. Every UUID field is trimmed,
        lower-cased and must be a valid UUID; every string field is trimmed, and
        a blank-but-supplied optional string is stored as null. `documentType`,
        `contextType`, `sourceType` and `status` are upper-cased before the
        pattern / enum check, so lower-case input is accepted and stored
        normalised. `sourceType` and `sourceId` must be supplied together and
        must pair with `contextType`: `INTERNAL` allows `INTERNAL` or no source,
        `TENANT` requires `TENANT_COMPANY` and `VENDOR` requires `VENDOR`.
        Unknown properties are ignored - neither rejected nor stored - and no
        actor, tenant, vendor, timestamp, expiry, retention or file-metadata
        field is accepted.
      required: [clientId, documentNumber, documentType, contextType, title]
      properties:
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Owner Client. It must exist and the actor must be able to act for it
            (404 `CLIENT_NOT_FOUND`, 403 `BUILDING_ACCESS_DENIED`), and it fixes
            the `documentNumber` unique space.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Optional Building scope. Omitted or null keeps the document at Client
            level. When supplied the Building must exist, its Property must
            belong to `clientId` and it must be accessible to the actor (404
            `BUILDING_NOT_FOUND`, 400 `DOCUMENT_BUILDING_MISMATCH`, 403
            `BUILDING_ACCESS_DENIED`).
        documentNumber:
          type: string
          maxLength: 64
          description: >
            Human document number, required and non-blank, unique inside
            `clientId` (409 `DOCUMENT_NUMBER_ALREADY_EXISTS`). It is never
            generated, substituted or renumbered by the backend.
        documentType:
          type: string
          minLength: 2
          maxLength: 64
          pattern: "^[A-Z][A-Z0-9_-]{1,63}$"
          description: >
            Type code. Upper-cased before validation, so lower-case input is
            accepted and stored upper-cased; anything outside the pattern is 400
            `VALIDATION_ERROR`. It is a label only - it selects no engine, no
            approval chain and no journey.
        contextType: { $ref: "#/components/schemas/DocumentContextType" }
        sourceType:
          allOf: [{ $ref: "#/components/schemas/DocumentSourceType" }]
          nullable: true
          description: >
            Optional source kind. Omitted or null means no source, which is valid
            only for an `INTERNAL` context; a `TENANT` / `VENDOR` context without
            its source is reported on field `sourceType`.
        sourceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Tenant Company id (`TENANT`) or Vendor id (`VENDOR`). It must resolve
            (404 `DOCUMENT_SOURCE_NOT_FOUND`) and must sit under `clientId` (400
            `DOCUMENT_SOURCE_CLIENT_MISMATCH`). It labels the origin only - it
            does not assign, approve or notify anyone.
        title:
          type: string
          maxLength: 255
          description: Document title; required and non-blank.
        description:
          type: string
          maxLength: 2000
          nullable: true
          description: Optional free-text description; blank or null stores null.
        fileReference:
          type: string
          maxLength: 512
          nullable: true
          description: >
            Opaque pointer to an already-stored file. This route performs no
            upload: there is no multipart body, no binary field, no storage
            provider, no size or content-type check, and no download or deletion
            of the referenced file.
        status:
          allOf: [{ $ref: "#/components/schemas/DocumentStatus" }]
          description: >
            Initial status; `DRAFT` when omitted. Null is not accepted, and
            `ARCHIVED` is refused with 409 `DOCUMENT_UPDATE_NOT_ALLOWED` because
            only the dedicated archive action may enter it. It labels the
            document only - it approves, activates or closes nothing elsewhere.
      examples:
        - clientId: "6f7b1b70-0000-4000-8000-000000000001"
          buildingId: null
          documentNumber: "DOC-2026-0001"
          documentType: "CONTRACT"
          contextType: "TENANT"
          sourceType: "TENANT_COMPANY"
          sourceId: "6f7b1b70-0000-4000-8000-000000000002"
          title: "Signed service contract"
          description: null
          fileReference: "clients/6f7b1b70/documents/contract.pdf"
          status: "ACTIVE"
    UpdateDocumentRequest:
      type: object
      description: >
        BE-22A payload of `PATCH /documents/{id}`. Partial by construction: every
        field is optional, only supplied keys are written, an omitted key is never
        cleared or defaulted, and a body with no mutable field is a legal no-op
        that returns the document unchanged. Explicit `null` clears
        `description`, `fileReference` or `buildingId`. Strings are trimmed and
        `documentType` / `status` are upper-cased before validation. Unknown
        properties are ignored. Identity and context cannot be patched: the
        runtime rejects `clientId`, `documentNumber`, `contextType`, `sourceType`
        and `sourceId` whenever they appear at all - even with the unchanged
        value - and `expiryDate` is not a field of this surface.
      properties:
        documentType:
          type: string
          minLength: 2
          maxLength: 64
          pattern: "^[A-Z][A-Z0-9_-]{1,63}$"
          description: >
            New type code, same shape rule as on create. Refused with 409
            `DOCUMENT_UPDATE_NOT_ALLOWED` when the document is the canonical
            document behind a BAST binding, and whenever the document is
            `INACTIVE` or `ARCHIVED`.
        title:
          type: string
          maxLength: 255
          description: New title; must be non-blank, and there is no null form.
        description:
          type: string
          maxLength: 2000
          nullable: true
          description: New description; `null` or a blank string clears it to null.
        fileReference:
          type: string
          maxLength: 512
          nullable: true
          description: >
            New opaque file pointer, stored as supplied; `null` clears it. The
            route never uploads, fetches or verifies whatever it points at.
        status:
          allOf: [{ $ref: "#/components/schemas/DocumentStatus" }]
          description: >
            New status among `DRAFT`, `ACTIVE` or `INACTIVE` (case-insensitive on
            input; null is rejected). `ARCHIVED` is refused with 409
            `DOCUMENT_UPDATE_NOT_ALLOWED`, as is any patch while the document is
            already `INACTIVE` or `ARCHIVED`. Changing it never archives,
            approves, expires or reopens anything else.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Re-scope the document to another Building, or to none with `null`. A
            supplied Building must exist, must belong to the document's own
            Client (its immutability is what is checked, not a Client change) and
            must be accessible to the actor (404 `BUILDING_NOT_FOUND`, 400
            `DOCUMENT_BUILDING_MISMATCH`, 403 `BUILDING_ACCESS_DENIED`); moving it
            does not move any linked journey record.
    ArchiveDocumentRequest:
      type: object
      description: >
        BE-22A payload of `POST /documents/{id}/archive`. `reason` is the only
        accepted key and this route - unlike create and patch - rejects any other
        property outright with a "<field> is not allowed." detail. The body itself
        is optional: an absent, empty or null body archives with a null reason.
      additionalProperties: false
      properties:
        reason:
          type: string
          maxLength: 1000
          nullable: true
          description: >
            Free-text archive reason, trimmed and stored verbatim; blank,
            whitespace-only or null stores null, and a whitespace-only string is
            rejected as "reason must be a non-empty string." It is recorded for
            audit only - no notification, ticket, approval or workflow is created
            from it.

    # -------------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B2 - J19 Document Versions (BE-22G).
    #
    # `Document` (PART 04D2) and `DocumentFoundation` (PART 05B1) are left
    # untouched on purpose: a Document Version embeds neither of them - it copies
    # six snapshot fields into its own flat row - so no existing component is
    # reused, narrowed or widened here. `DocumentVersionStatus` exists because the
    # append route accepts only the three writable foundation statuses, which is
    # narrower than `DocumentStatus` and must not retroactively change what the
    # two existing document components state.
    # -------------------------------------------------------------------------
    DocumentVersionStatus:
      type: string
      enum: [DRAFT, ACTIVE, INACTIVE]
      description: >
        Status writable onto a new Document Version - the set enforced by
        `readStatus` in the version parser. Input is trimmed and upper-cased
        before the check; null or a blank string is rejected, and any value
        outside these three - `ARCHIVED` included - is 400 `VALIDATION_ERROR`
        with detail "status must be one of: DRAFT, ACTIVE, INACTIVE." It labels
        the snapshot only: it approves, activates, deactivates, archives or
        expires nothing, and it does not relax the parent document's own
        lifecycle guards.
    DocumentVersion:
      type: object
      description: >
        BE-22G `PublicDocumentVersion` projection exactly as the version module
        serialises it: a flat snapshot of the six versioned fields plus the row's
        own identity, number and timestamps. Neither `Document` nor
        `DocumentFoundation` is embedded - the parent's `clientId`,
        `documentNumber`, `contextType`, `sourceType` and `sourceId` are not
        duplicated here and cannot be read through this projection - and there is
        no checksum, hash, file size, mime type, storage bucket or object key, no
        OCR result, no retention or integrity state, no approval or sign-off
        field and no "current version" flag. `fileReference` is only an opaque
        pointer carried as text.
      required:
        [id, documentId, versionNumber, title, description, fileReference,
         documentType, status, expiryDate, createdByUserId, createdAt, updatedAt]
      properties:
        id:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Version row id - the `versionId` path key of
            `GET /document-versions/{versionId}`. It is not the parent document id.
        documentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Parent generic Document row this version belongs to. It is fixed at
            insert, is never re-pointed by any registered route, and is the only
            isolation path to this row: access is always asserted on the parent,
            never on the version itself.
        versionNumber:
          type: integer
          minimum: 1
          description: >
            Backend-assigned position in the document's history: one more than the
            highest number already stored for that document, starting at 1 when it
            has none. Unique per document through the
            `document_versions_document_version_unique` constraint, immutable,
            never renumbered and never reused. No client can choose it and no
            route fills a gap or re-sequences the history, so contiguity is a
            consequence of "always append one", not a guarantee this contract
            promises.
        title:
          type: string
          maxLength: 255
          description: >
            Title as snapshotted - the supplied override, or the parent document's
            title at that moment. The version row has no null title and no
            `documentNumber` field, so nothing here restates the document number.
        description:
          type: string
          nullable: true
          maxLength: 2000
          description: >
            Description as snapshotted; null when the parent document had none or
            when the append request explicitly cleared it with null.
        fileReference:
          type: string
          nullable: true
          maxLength: 512
          description: >
            Opaque pointer as snapshotted. No file is copied, uploaded, downloaded
            or deleted when a version is appended, so an older and a newer row may
            point at the same reference, and nothing verifies that the reference
            still resolves.
        documentType:
          type: string
          maxLength: 64
          description: >
            Type code as snapshotted, already upper-cased at input (2-64 characters
            of `[A-Z][A-Z0-9_-]*`). It labels the snapshot only; the parent
            document's own type is aligned by this module's snapshot write and is
            never re-validated through the generic Documents patch guards here.
        status:
          type: string
          description: >
            Status as snapshotted. It is typed as a stored string rather than as
            `DocumentStatus` because the row carries whatever the parent document
            held at snapshot time - including `ARCHIVED`, which can appear only on
            the implicit version-1 baseline the list and latest routes create for an
            archived document, since the append route accepts `DRAFT` / `ACTIVE` /
            `INACTIVE` only and refuses an archived parent. It is a historical
            label: no reader may treat it as a live approval, activation or
            archive decision.
        expiryDate:
          type: string
          format: date-time
          nullable: true
          description: >
            Expiry copied into the snapshot from the parent document - no version
            route accepts, computes or writes a new expiry value. As on the
            foundation projection the stored column is a date serialised through
            `toISOString()`, so a value appears as a full RFC3339 timestamp and
            null means the document had none. Expiry itself belongs to the Document
            Expiry journey; this field is a snapshot, not a capability.
        createdByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Actor recorded at insert: the authenticated caller for an appended
            version, or the parent document's own `createdByUserId` when the list
            or latest route created the implicit version-1 baseline. It is not a
            version author, approver or editor identity, and it is never supplied
            by a caller.
        createdAt:
          type: string
          format: date-time
          description: >
            Version row creation timestamp. Ordering is not derived from it:
            listing sorts by `versionNumber` ascending and "latest" is the highest
            number, so an inserted row is placed by number even if its timestamp
            ordering differs.
        updatedAt:
          type: string
          format: date-time
          description: >
            Version row update timestamp as stored. No registered route edits a
            version row, so for a row created here it matches `createdAt`, and an
            existing row is never rewritten in place.
    CreateDocumentVersionRequest:
      type: object
      description: >
        BE-22G payload of `POST /documents/{documentId}/versions`. Every field is
        optional and a body is never required: an absent, empty or null body is
        legal and appends a full duplicate snapshot of the parent document's
        current state under the next number. Each field is either an override or
        an inheritance - a key that is absent takes the document's current value.
        Strings are trimmed, and on `title` / `description` / `fileReference` a
        blank string is normalised to null without a validation detail;
        `documentType` and `status` are upper-cased first and reject a null or a
        blank value with 400. Unknown
        properties are ignored, neither rejected nor stored. Nothing
        backend-derived is accepted: `id`, `documentId`, `versionNumber`,
        `createdByUserId`, `createdAt` and `updatedAt` are not fields of this
        request, and `expiryDate` cannot be set here even though the projection
        carries it - it is inherited from the document.
      minProperties: 0
      properties:
        title:
          type: string
          maxLength: 255
          description: >
            New title for the snapshot: trimmed, at most 255 characters, and a
            non-blank string. A blank or whitespace-only value is not reported as
            a validation detail - the parser normalises it (and an explicit null)
            to null - while a version row has no null title, so blank is never a
            way to clear or skip the field; omit the key to inherit the parent
            document's title. This request does not patch the parent document
            itself: the parent row is aligned with the snapshot afterwards by this
            module's own follow-up write.
        description:
          type: string
          maxLength: 2000
          nullable: true
          description: >
            New description for the snapshot. Unlike `title`, null and a blank
            string are meaningful overrides: both store a null description on the
            version row instead of inheriting the document's.
        fileReference:
          type: string
          maxLength: 512
          nullable: true
          description: >
            New opaque file pointer for the snapshot; null or blank stores a null
            pointer, and an omitted key inherits the document's current reference.
            The route performs no upload, download, copy or verification - the
            value is stored exactly as supplied, and no storage provider, size,
            content type or checksum is involved.
        documentType:
          type: string
          minLength: 2
          maxLength: 64
          pattern: "^[A-Z][A-Z0-9_-]{1,63}$"
          description: >
            New type code for the snapshot, upper-cased before validation (so
            lower-case input is accepted and stored upper-cased); anything outside
            the 2-64 character shape is 400 `VALIDATION_ERROR`. A null or a blank
            value is not "no change" but a 400 as well ("documentType must be a
            non-empty string."), so only an omitted key inherits the document's
            type. It selects no engine, workflow or journey.
        status:
          allOf: [{ $ref: "#/components/schemas/DocumentVersionStatus" }]
          description: >
            New status for the snapshot; an omitted key inherits the document's
            current status and an explicit null is rejected. `ARCHIVED` cannot be
            requested here (400 at the parser, and the module separately refuses
            to append to a document that is already `ARCHIVED` with 409
            `DOCUMENT_UPDATE_NOT_ALLOWED`). It records what the document looked
            like at snapshot time - it does not approve, activate, deactivate,
            archive or supersede anything.
      examples:
        - {}
        - title: "Signed service contract (rev 2)"
          description: "Re-issued after the appendix was attached."
          fileReference: "clients/6f7b1b70/documents/contract-v2.pdf"
          documentType: "CONTRACT"
          status: "ACTIVE"

    # -------------------------------------------------------------------------
    # CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B3 - J19 Document Expiry (BE-22H).
    #
    # `Document`, `DocumentFoundation`, `DocumentVersion`, `DocumentStatus`,
    # `DocumentVersionStatus`, `DocumentContextType` and `DocumentSourceType` are
    # left exactly as their own contract parts wrote them: an expiry projection
    # embeds none of them, and the derived expiry vocabulary has no existing
    # component that could be widened without changing what those parts state. Only
    # the three expiry-specific components below are added, on top of the shared
    # `Uuid`, envelope and `Unauthorized` components this file already defines.
    # -------------------------------------------------------------------------
    ExpiryState:
      type: string
      enum: [ACTIVE, EXPIRING, EXPIRED]
      description: >
        Expiry state DERIVED from the stored date inside each request. It is not a
        column, not a stored flag and not a lifecycle field of the document or of a
        version row. The runtime rule is exact: a null date is `ACTIVE`; a date
        strictly earlier than the moment of the request is `EXPIRED`; a date at
        most 30 days ahead - the millisecond difference divided by 86400000 and
        compared with `<=`, so day boundaries are not truncated - is `EXPIRING`;
        and anything further ahead is `ACTIVE`. Because it is recomputed per
        request, the same row moves from `EXPIRING` to `EXPIRED` with no write and
        no callback: nothing is scheduled, notified, locked, archived, retained or
        deleted when the date passes. It is not a retention state, not an approval
        state and not a document or version `status`.
    DocumentExpiry:
      type: object
      description: >
        BE-22H `PublicDocumentExpiry` projection - the whole success payload of all
        four expiry routes. It carries the addressed identity, the effective date
        and the derived state only: no actor field, no `createdAt` / `updatedAt`, no
        document or version `status`, no title, no `fileReference`, no checksum,
        hash, size or mime type, no retention or integrity field and no approval
        field. Neither `Document`, `DocumentFoundation` nor `DocumentVersion` is
        embedded, so the parent's `documentNumber`, `documentType`, `contextType`,
        `sourceType` and `sourceId` cannot be read through this response, and no
        history list is carried - reading history means calling the Document
        Versions contract. `clientId` and `buildingId` are declared optional on the
        runtime projection type yet every one of the four routes returns them, so
        they are required here; only `versionId` is genuinely absent, and only on
        the document-scoped pair.
      required: [documentId, expiryDate, expiryState, clientId, buildingId]
      properties:
        documentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            The parent generic Document row. On the version-scoped pair it is
            derived from the addressed version row, not supplied by the caller.
        versionId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Present only on the two version-scoped routes, and never null: on the
            read it is the addressed version row, while on the write it is the NEW
            version row that now carries the value - which is why the id in the
            request path and the id in the response can differ. On the
            document-scoped pair the key is absent from the object entirely, not
            emitted as null.
        expiryDate:
          type: string
          format: date-time
          nullable: true
          description: >
            The expiry in force, serialised through `toISOString()`, so a value
            always appears as a full RFC3339 timestamp and never as a bare date;
            null means no expiry is set and pairs with `ACTIVE`. A read returns the
            stored value, whereas a write returns the instant that request
            submitted without re-reading it - the two writes are issued before the
            response is built and are not a re-projection of the stored row, so if
            persistence narrows the value the write response and a later read can
            differ. There is no time-of-day component in the semantics of expiry:
            the comparison above is made against the raw instants.
        expiryState: { $ref: "#/components/schemas/ExpiryState" }
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Client that owns the parent document, taken from the parent row on all
            four routes. It is context, not a filter, and it is never accepted from
            callers.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Building of the parent document, or null for a Client-level document.
            It is the isolation key the request actually asserted - Building access
            when set, Client access when null - and it does not change because of
            an expiry write.
    SetDocumentExpiryRequest:
      type: object
      description: >
        BE-22H payload of both PUT routes. `expiryDate` is the only field and it is
        mandatory in the strictest sense: an omitted key is 400 `VALIDATION_ERROR`
        with detail "expiryDate is required (use null to clear).", while an explicit
        null is a valid clear - the two are not interchangeable, and because no
        DELETE route exists, PUT with null is the only way to remove an expiry. A
        string value is trimmed and converted with the JavaScript `Date` parser, so
        a date-only `YYYY-MM-DD` (which resolves to UTC midnight) is the
        representation the module is built around without any format restriction
        beyond what that parser accepts, a full ISO-8601 timestamp is likewise
        accepted, and anything unparseable is 400 "expiryDate must be a valid
        date.". An empty string is reported as "expiryDate must be an ISO-8601 date
        string or null." rather than as a date error. Unknown properties are ignored,
        not rejected. There is no validation against the past, no minimum horizon, no
        maximum, and no field for a timezone, grace period, reminder, notification,
        retention rule or archive instruction.
      required: [expiryDate]
      additionalProperties: true
      properties:
        expiryDate:
          type: string
          nullable: true
          description: >
            The new expiry value, replacing whatever the target carried - never
            merged with it and never incremented. Null clears the expiry, which
            reads back as `ACTIVE` with a null date. A date already behind the
            current instant is accepted and simply answers `EXPIRED`; there is no
            renewal, extension or comparison guard, and setting this value neither
            archives nor deletes nor retains anything.
      examples:
        - expiryDate: "2026-12-31"
        - expiryDate: null
    DocumentApprovalStatus:
      type: string
      enum:
        - PENDING
        - APPROVED
        - REJECTED
      description: >
        Public approval state of one review row, derived at read time from the
        stored status/decision pair: `PENDING` while the review is open, and
        thereafter the decision itself (`APPROVED` or `REJECTED`). The stored
        "completed" marker is never exposed as a status, so there is no
        `COMPLETED` value and no fourth state; `EXPIRED`, `SUPERSEDED`,
        `CANCELLED`, `DELEGATED` and `RETURNED` do not exist because the runtime
        implements none of them. This is an approval outcome only: it says nothing
        about the target document's `status`, version, expiry or archive state, and
        `DocumentStatus`, `DocumentVersionStatus` and `ExpiryState` are separate
        vocabularies that none of these routes mutate.
    DocumentApprovalDecision:
      type: string
      enum:
        - APPROVED
        - REJECTED
      description: >
        Decision recorded on a review row by its named reviewer. It is null until a
        decision exists, and both values are terminal: the runtime cannot replace,
        retract or re-record one, so `APPROVED` never becomes `REJECTED` (or the
        reverse) on the same row. It is a two-valued outcome on a single reviewer -
        there is no request-changes, abstain, conditional-approval or partial value,
        no aggregate of several reviewers and no decision on behalf of someone else.
        This vocabulary belongs to document approvals only; it is not the `ACCEPTED`
        / `REJECTED` decision of an Acceptance Sign-Off (J12) and not a generic
        workflow action, and neither `decision` nor `approvalStatus` may be written
        by a request body.
    DocumentApproval:
      type: object
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B4 - the public projection of one
        document approval (runtime `PublicDocumentApproval`). It is a single
        reviewer's pending or decided review of one target: a generic BE-22A
        Document or one of its Document Versions. Exactly these fields are
        serialized; the underlying review row's storage columns (its `targetType`
        and `targetId`) are not exposed and only appear translated into
        `documentId` plus `versionId`.

        `required` lists all fourteen keys because the projection always emits
        them: `versionId`, `buildingId`, `decision`, `notes`, `reviewedAt` and
        `createdByUserId` are nullable rather than optional, and on a
        document-scoped review `versionId` is null, not absent. A client must test
        those keys for null, never for presence, and must not read `notes` as a
        required field on either side - the runtime never rejects a request for
        leaving `notes` out, so notes are not proof of anything.

        The two identity fields are deliberately separate: `documentId` is the
        governing document (resolved through the version when the review targets
        one) while `versionId` is the snapshot the reviewer was asked to judge; a
        null `versionId` means the review addresses the document as a whole.
        `clientId` and `buildingId` are copied from the target document for
        isolation and are not independent authorisation state - they are never
        accepted from a request. `contextType` is the resolved source-domain string
        of the target document (never derived from a request and never resolved
        from the version by itself, falling back to `INTERNAL` when the context row
        cannot be resolved), and it is a distinct vocabulary from the `Document`
        `DocumentContextType` projection with which it must not be conflated.
      required:
        - id
        - documentId
        - versionId
        - clientId
        - buildingId
        - contextType
        - approvalStatus
        - decision
        - reviewerUserId
        - notes
        - reviewedAt
        - createdByUserId
        - createdAt
        - updatedAt
      properties:
        id:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Approval row id (the review record's own identity), returned by the
            submission response and the only handle the single-row read,
            `/approve` and `/reject` accept. It is not the document id, not a
            version id and not a `documentNumber`.
        documentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Target Document id. For a version-targeted review it is the document
            the version belongs to, resolved through that version row, so it is
            never independently settable and never inferred from the request path
            alone.
        versionId:
          type: string
          format: uuid
          nullable: true
          description: >
            Version id when the review targets a Document Version, and null when it
            targets the generic Document itself. It is never a version NUMBER, and
            this projection carries no `versionNumber` field - a client that needs
            the ordinal reads the Document Versions surface. A null here does not
            mean "the latest version" or "all versions": it means the review was
            raised against the document as a whole.
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Owning client, taken from the target document at submission time; the
            isolation predicate this approval is read and written under. For a
            document without a client linkage it is whatever the document row
            carries - this surface never creates, repairs or re-points that
            linkage, and an approval cannot address a client, building or document
            of its own.
        buildingId:
          type: string
          format: uuid
          nullable: true
          description: >
            Building scope of the target document, null when that document has
            none. It governs the Building isolation asserted on every route here,
            is resolved through the version for a version-targeted review, and is
            never caller-supplied, never defaulted and never a capability.
        contextType:
          type: string
          description: >
            Source-domain label resolved from the target document (`INTERNAL` or
            `EXTERNAL` in the current document projection) and stored on the review
            row at submission time, defaulting to `INTERNAL` when it cannot be
            resolved. It is descriptive only: it selects no engine, workflow,
            stage, policy or permission and does not change any approval rule - a
            `PRODUCTION` context, an external contractor or a special review path
            is NOT modelled here.
        approvalStatus:
          allOf: [{ $ref: "#/components/schemas/DocumentApprovalStatus" }]
          description: >
            Derived public state of the review (`PENDING`, `APPROVED`, `REJECTED`).
            It belongs to this row alone: it is not a Document lifecycle state, so
            an approved review never implies an active, published, accepted or
            locked document, and a rejected one never implies an archived or
            expired one.
        decision:
          allOf: [{ $ref: "#/components/schemas/DocumentApprovalDecision" }]
          nullable: true
          description: >
            Recorded decision, null while pending. It is the raw stored value and
            is deliberately not merged into `approvalStatus`: it is the source the
            derived status falls back to, and a client that needs the difference
            between "open" and "decided" reads both fields.
        reviewerUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Designated approver - the only user allowed to decide this row. It is
            supplied at submission, validated against the user store and the
            approver-authority check there, and re-checked at decision time; it is
            immutable afterwards and cannot be reassigned, delegated or widened to
            a group, role or queue.
        notes:
          type: string
          maxLength: 4000
          nullable: true
          description: >
            Free text, capped at 4000 characters: the submitter's reason on the
            `POST`, and the reviewer's comment on `/approve` or `/reject`. A blank
            or whitespace-only value is normalised to null rather than rejected,
            and a decision that omits `notes` (or sends null) KEEPS the submitted
            notes through the `COALESCE` on the write - so notes cannot be cleared
            once written. It is stored on this row only and never becomes a
            document field, a version note or a notification body.
        reviewedAt:
          type: string
          format: date-time
          nullable: true
          description: >
            Backend decision timestamp, null while the review is pending. It is
            written by the same statement that records the decision and cannot be
            supplied, corrected or back-dated by a client; there is no per-step
            history, so one row carries one review instant and an older decided
            row's `reviewedAt` never moves when a newer review is submitted.
        createdByUserId:
          type: string
          format: uuid
          nullable: true
          description: >
            Submitter. The submission response echoes the acting user; the review
            row persists no submitter column, so every later read of the same row
            - the single-row read, the history list and both decision responses,
            which are re-reads - reports null. A client must therefore treat this
            field as absent-by-design rather than as evidence of who submitted
            what, and must not infer the submitter from the document's or version's
            `updatedByUserId`, which is unrelated state.
        createdAt:
          type: string
          format: date-time
          description: >
            Row creation instant (the `created_at` column on a read row; the
            request clock on the submission response). The history list is ordered
            by this value ascending, and it is never client-settable.
        updatedAt:
          type: string
          format: date-time
          description: >
            Row update instant (the `updated_at` column on a read row; the request
            clock on the submission response). It moves once, when the review is
            decided, and is never an optimistic-lock token: it cannot be echoed back
            to force, order or retry a decision, and there is no concurrency token
            on this surface.
    SubmitDocumentApprovalRequest:
      type: object
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B4 - the parsed create payload of
        `parseCreateDocumentApprovalBody`. It is required (an absent, empty or
        non-object body is 400 `VALIDATION_ERROR` with detail "Request body must be
        a JSON object."). Only these three keys are read; unknown properties are
        ignored and neither rejected nor stored.

        Nothing else is accepted, because nothing else is client-owned:
        `documentId` comes from the path (never from the body), and `id`,
        `clientId`, `buildingId`, `contextType`, `approvalStatus`, `decision`,
        `reviewerUserId`-as-caller, `reviewedAt`, `createdAt` and `updatedAt` are
        resolved or asserted by the backend. There is deliberately no priority,
        due date, SLA, routing, stage, delegate, group or multi-reviewer field, no
        attachment, and no field that could bundle an approval for several versions
        or documents at once.
      required:
        - approverUserId
      minProperties: 1
      properties:
        approverUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Required approver - trimmed and lower-cased, and a non-UUID value is
            400 `VALIDATION_ERROR` (detail "approverUserId must be a valid UUID.").
            A missing key is a separate 400 ("approverUserId is required.") and an
            explicit null is treated as missing rather than as "nobody". The
            service then requires that user to exist and be ACTIVE, to reach the
            same Building (or the same Client, when the document has none), and to
            hold `document.approve` or `document.manage`; otherwise 403
            `DOCUMENT_APPROVAL_UNAUTHORIZED`. It is one single user id - no array,
            role, group, queue or email - it cannot be the target document's owner
            by right, and it cannot be changed later: a wrong approver means this
            row stays pending for that reviewer until it is decided.
        versionId:
          type: string
          format: uuid
          description: >
            Optional version scope: when present the review addresses that
            `document_versions` row instead of the whole document, and the service
            requires the version to exist AND to belong to the addressed document,
            otherwise 400 `DOCUMENT_APPROVAL_CONTEXT_INVALID` (it is never silently
            re-pointed at another document, and an unknown version is not a 404 on
            this route). A non-UUID value is 400. An explicit null is read as
            ABSENT - it does not store a null version and does not select "the
            latest version": omitting the key and sending null both submit a
            document-level review. It is an id only, never a version number, and it
            binds neither future versions nor the parent document's lifecycle.
        notes:
          type: string
          maxLength: 4000
          nullable: true
          description: >
            Optional free text for the approver: trimmed, at most 4000 characters
            (400 `VALIDATION_ERROR` with a detail naming the cap), and normalised to
            null when blank, whitespace-only or explicitly null - so an empty string
            never stores a blank note and is not a validation error. It is optional:
            no note is required to submit, and this field is not a reason code, an
            attachment, a comment thread or a rejection justification.
    DocumentApprovalDecisionRequest:
      type: object
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B4 - the parsed payload shared by
        `/approve` and `/reject` (`parseDocumentApprovalDecisionBody`). A body is
        never required: an absent, empty or null body is accepted and read as an
        empty object, so a decision can always be recorded with no comment. The
        only accepted field is `notes`; a non-object body is 400 `VALIDATION_ERROR`
        (detail "Request body must be a JSON object.") and unknown properties are
        ignored, not rejected.

        The decision VALUE is not a field of this request: `/approve` records
        `APPROVED` and `/reject` records `REJECTED` from the route alone, so
        neither `decision`, `status`, `approvalStatus` nor any
        `approve`/`reject` flag may be sent, and an empty body cannot flip an
        outcome. There is no reason code, no comment required for a rejection, no
        `notify` / `email` flag (no mail is sent by either route), no reassign or
        `delegateTo` field, no attachment, no due date, no
        conditional-approval / partial-approval shape, no rework-request action and
        no idempotency key - the transition is guarded by the row's `PENDING`
        predicate, not by a caller-supplied token.
      minProperties: 0
      properties:
        notes:
          type: string
          maxLength: 4000
          nullable: true
          description: >
            Reviewer's comment: trimmed, at most 4000 characters (400
            `VALIDATION_ERROR` with a detail naming the cap; a non-string value is
            rejected with "notes must be a string or null."), and blank,
            whitespace-only or explicitly null arrive as null - which the decision
            write then reads as "keep what is there" (`notes = COALESCE($3,
            notes)`), never as "clear it". A decision therefore cannot blank an
            existing note, cannot append a second comment and cannot edit its own
            comment afterwards.
    SupportingParentType:
      type: string
      enum:
        - WORK_COMPLETION
        - BAST
        - HANDOVER
        - SIGN_OFF
        - TENANT_COMPANY
        - VENDOR
        - DOCUMENT
        - RFQ
        - QUOTATION_REVISION
      description: >
        The closed set of targets a Supporting Document may be bound to
        (`SUPPORTING_PARENT_TYPES`), exactly as the runtime enumerates it - input is
        trimmed and upper-cased before the check, and anything else is 400
        `VALIDATION_ERROR` with a detail naming these nine. Each value selects which
        store `parentId` is resolved against: the Work Completion, BAST, Handover and
        Acceptance Sign-Off records, a Tenant Company, a Vendor, a generic
        `documents` row, an RFQ row and a vendor quotation revision row
        respectively. It is a REFERENCE KIND, nothing more: it is not a document
        type code, not a workflow, stage or owner, not a permission and not a
        capability - naming a target never reads, writes, approves, verifies or
        unlocks anything inside that target's own module, and no other business
        object can be addressed. `DOCUMENT` binds the new document to another
        generic document as a peer; it does not adopt that document, create a
        version of it or make the two rows one. The same vocabulary is accepted as a
        list filter, where it must be paired with `parentId`.
    SupportingDocument:
      type: object
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B5 - the public projection of one
        supporting-document binding (runtime `PublicSupportingDocument`). It is the
        binding row itself - identity, the bound document, the target pair, the
        copied context, the submitter and the row timestamps - plus the bound
        Document embedded whole under `document` as the shared BE-22A projection
        exactly as the foundation serialises it. Nothing else of the pair is
        flattened: the document's `title`, `documentNumber`, `status`,
        `fileReference`, `expiryDate` and archive fields are readable only through
        the embedded object, are never duplicated onto the binding, and are never
        kept in step with it afterwards.

        Exactly these eleven keys are serialised and all of them are always present,
        so only `buildingId` is nullable rather than optional and a client must test
        it for null instead of for presence. The parent target is NOT embedded -
        `parentType` and `parentId` are identifiers that this surface never resolves
        back, so a deleted parent leaves the pair readable as stored - and there is
        no parent title, number, status or existence flag, no `available_actions`,
        no approval or verification state, no evidence, retention, legal-hold or
        integrity field, no file size, mime type, checksum, bucket, object key, page
        count or preview, and no version, expiry or download field of its own.
      required:
        - id
        - documentId
        - parentType
        - parentId
        - clientId
        - buildingId
        - contextType
        - createdByUserId
        - createdAt
        - updatedAt
        - document
      properties:
        id:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Binding row id - the `id` path key of the single-row read and the
            `entityId` of the operational event this route records. It is neither the
            document id nor the parent id, and it is generated by the backend at
            insert.
        documentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            The generic BE-22A Document row this binding created and points at. It
            is the only document linkage this contract has: the row can be read and
            managed through the Documents routes with its own permissions, but it
            cannot be swapped, re-bound or supplied by a caller here, and deleting or
            archiving it elsewhere leaves this binding holding the stale id (the
            single-row read then answers 404 with the supporting-document code).
        parentType:
          allOf: [{ $ref: "#/components/schemas/SupportingParentType" }]
          description: >
            Which store `parentId` belongs to, fixed at insert and immutable - there
            is no re-targeting route. It states what the binding was ATTACHED to, not
            what kind of document was created: the document's own kind is
            `document.documentType` and its context is `contextType`, and none of
            the three is derived from the others.
        parentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Target row id inside that store. Only its existence, Client, Building,
            context and - where applicable - archived state were consulted at
            creation; the id is stored verbatim afterwards, never re-validated, never
            cascade-checked and never expanded into the parent object.
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Owning Client, taken from the request and required to equal the target's
            own Client at creation. It is the isolation boundary when `buildingId`
            is null and it is what makes `documentNumber` unique; it is never
            re-derived from the parent afterwards, so moving the parent changes
            nothing here.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          nullable: true
          description: >
            Building scope of the binding - the one the caller supplied, or the
            target's own when the caller named none (the inheritance the module
            performs on write), or null for a genuinely building-less Client-level
            binding. It decides which isolation rule applies on read and is never a
            capability over that Building.
        contextType:
          allOf: [{ $ref: "#/components/schemas/DocumentContextType" }]
          description: >
            The shared document context recorded on the binding row and copied from
            the request (compared against the target's own context only for the five
            compared parent kinds). It is descriptive: it selects no engine,
            workflow, approval path, storage location or permission, and it is a
            snapshot - a later change to the parent's or the document's context is
            not reflected here.
        createdByUserId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            The authenticated caller who created the binding, persisted on the row
            and returned by every route here. It is never accepted from a request,
            never inferred from the bound document's creator or the target's owner,
            and there is no updated-by field: this module writes the row once and
            never touches it again.
        createdAt:
          type: string
          format: date-time
          description: >
            Binding row creation instant, produced by the row's own insert. The list
            is ordered by this value descending (newest first) and it is never
            client-settable.
        updatedAt:
          type: string
          format: date-time
          description: >
            Row update instant. No registered route updates a binding row, so on
            this contract it carries the same instant as `createdAt`; it is not an
            optimistic-lock token, cannot be echoed back to force or order a write,
            and is not evidence that anything changed.
        document:
          allOf: [{ $ref: "#/components/schemas/DocumentFoundation" }]
          description: >
            The bound generic document as the shared BE-22A foundation serialises it:
            the object returned by the creation on the `POST`, and a fresh read of
            the row on both reads. Its `status`, `expiryDate` and archive fields are
            the document's own state and are NOT this binding's state - reading or
            creating a binding never approves, archives, expires or versions
            anything, and no field of this projection is kept in step with the
            document afterwards.
    CreateSupportingDocumentRequest:
      type: object
      description: >
        CR-BE-FE-JOURNEY-CONTRACT-01 PART 05B5 - the parsed create payload of
        `parseCreateSupportingDocumentBody`, a body that is always required (an
        absent, empty or non-object body is 400 `VALIDATION_ERROR` with detail
        "Request body must be a JSON object."). It carries BOTH halves of the write:
        the metadata of the Document to create and the (`parentType`, `parentId`)
        pair to bind it to. Strings are trimmed - and `contextType`, `sourceType`,
        `status` and `documentType` upper-cased - before validation; unknown
        properties are ignored, neither rejected nor stored; and a missing or blank
        value for any of the eight required fields is a validation detail naming that
        field, all of which are reported together.

        Nothing identity- or actor-shaped is accepted: no `id`, no `documentId` (the
        Document is always newly created and can never be an existing row), no
        `createdByUserId`, `createdAt` or `updatedAt`. Nor is anything out of scope
        here: no `expiryDate` (that belongs to the Document Expiry route of the
        created document), no version or supersedes field, no `notes`, reason, title
        of the parent or target object, no priority, due date or owner, no
        `targetType` free-form kind, no arrays, no `documentIds` bundle and no
        storage-shaped field - `fileReference` is text, and there is no upload,
        size, mime type, checksum, retention or evidence field to fill.
      required:
        - clientId
        - contextType
        - title
        - fileReference
        - documentNumber
        - documentType
        - parentType
        - parentId
      minProperties: 8
      properties:
        clientId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Required owning Client - trimmed and lower-cased, malformed is 400
            `VALIDATION_ERROR` (detail "clientId must be a valid UUID."). The row must
            exist (404 `CLIENT_NOT_FOUND`), the actor must reach it - its Building
            when one is named, otherwise the Client itself (403) - and it must be the
            target's own Client, or 400 `SUPPORTING_DOCUMENT_CONTEXT_MISMATCH`. It is
            also the unique space for `documentNumber`. Nothing is defaulted into it
            and it is never inherited from the target.
        buildingId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Optional Building for the document being created. When supplied it must
            exist (404 `BUILDING_NOT_FOUND`), its Property must belong to `clientId`
            (400 `SUPPORTING_DOCUMENT_BUILDING_MISMATCH`) and the actor must have
            access to it (403); when omitted - or sent as null, which the parser
            treats as absent rather than as a value - the target's Building is
            INHERITED when it has one, with the actor's access re-checked on the
            inherited value, and a target-less binding simply stays
            Client-scoped. It is never a claim of authority over the Building and it
            never moves any parent.
        contextType:
          allOf: [{ $ref: "#/components/schemas/DocumentContextType" }]
          description: >
            Required shared document context (`INTERNAL`, `TENANT` or `VENDOR`),
            upper-cased before the check; anything else is 400 with a detail naming
            those three. It is not decorative: it constrains the source pair (a
            `TENANT` or `VENDOR` context must name its source, `INTERNAL` may not
            name another kind) and, for the five compared parent kinds, it must
            equal the target's own context. It selects no workflow, approver,
            storage location or permission.
        sourceType:
          allOf: [{ $ref: "#/components/schemas/DocumentSourceType" }]
          description: >
            Optional origin kind for the created document - `TENANT_COMPANY`,
            `VENDOR` or `INTERNAL`, upper-cased, and an explicit null is read as
            "no source". It must be provided together with `sourceId` (400 detail
            "sourceType and sourceId must be provided together." on `sourceType`) and
            must be consistent with `contextType`: `INTERNAL` context only allows an
            `INTERNAL` source or none, `TENANT` requires `TENANT_COMPANY`, `VENDOR`
            requires `VENDOR`. It is a marker on the created document only - it never
            creates, mutates or unlocks the source record it names.
        sourceId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Optional source row id, paired with `sourceType`. For a `TENANT` context
            it must resolve to a Tenant Company and for a `VENDOR` context to a
            Vendor, both existing (404 `DOCUMENT_SOURCE_NOT_FOUND`) and both sitting
            under `clientId` (400 `DOCUMENT_SOURCE_CLIENT_MISMATCH`) - these two
            stores are the only sources this route validates. It has nothing to do
            with `parentId`: naming the same row in both places is not cross-checked
            and creates no link between the two.
        title:
          type: string
          minLength: 1
          maxLength: 255
          description: >
            Required document title for the created Document: trimmed, non-blank (a
            blank or whitespace-only value is "title is required.", not an empty
            title) and at most 255 characters. It belongs to the document, not to
            the binding, and there is no null form of it.
        description:
          type: string
          maxLength: 2000
          nullable: true
          description: >
            Optional document description; blank, whitespace-only or explicitly null
            all store null, so an empty string never saves a blank note. It is not a
            comment about the binding, not a reason and not an approval note, and it
            is not required to justify the attachment.
        fileReference:
          type: string
          minLength: 1
          maxLength: 512
          description: >
            Required opaque pointer for the created document: trimmed, at most 512
            characters, and this module re-checks it after parsing, so a blank value
            is 400 `SUPPORTING_DOCUMENT_CONTEXT_MISMATCH` ("fileReference is required
            for supporting documents.") and an over-long one is the same code
            ("fileReference too long.") rather than a `VALIDATION_ERROR`. It is a
            string the caller is trusted to fill: the route performs no upload,
            download, copy, deletion, size check, mime detection, hashing, scanning or
            existence test of any kind, and nothing verifies that the reference still
            resolves.
        status:
          allOf: [{ $ref: "#/components/schemas/DocumentStatus" }]
          description: >
            Optional initial status for the created Document, upper-cased, with
            `DRAFT` applied when the key is absent; a null is rejected as a value
            outside the enum and a blank string as well. This route imposes no status
            rule of its own and no status transition: it hands the value to the
            shared BE-22A creation path, whose documented creation rules therefore
            govern it, and the binding row has no status field at all. It is not an
            approval, a verification, a submission or a completion of anything on the
            target.
        documentNumber:
          type: string
          minLength: 1
          maxLength: 64
          description: >
            Required human reference for the created Document: trimmed, non-blank, at
            most 64 characters, and unique within `clientId` (409
            `DOCUMENT_NUMBER_ALREADY_EXISTS` on a clash). It is never generated,
            renumbered, suffixed or cleared, is not a version number, and carries no
            format requirement beyond length; the binding's own row stores none.
        documentType:
          type: string
          minLength: 2
          maxLength: 64
          pattern: "^[A-Z][A-Z0-9_-]{1,63}$"
          description: >
            Required type code for the created Document, upper-cased first and then
            required to be 2-64 characters of `[A-Z][A-Z0-9_-]*` (detail "documentType
            must start with a letter and contain only uppercase letters, digits,
            hyphens, and underscores (2-64 characters)."). It is a free label - no
            registry, no enum and no per-type behaviour: it selects no template,
            workflow, approval chain, storage path or permission, and it is unrelated
            to `parentType`.
        parentType:
          allOf: [{ $ref: "#/components/schemas/SupportingParentType" }]
          description: >
            Required target kind, upper-cased before the closed nine-value check
            (missing or blank is "parentType is required."; an unknown value is
            rejected with a detail naming all nine, and no fuzzy or case-insensitive
            match is performed). It fixes which store `parentId` is resolved against
            and which guards apply - the archived check and the context comparison
            only reach the kinds whose records carry them - and it is the only way a
            target can be named: no path, no query and no other field can select a
            parent, and `parentId` may never accompany a kind it was not created
            under.
        parentId:
          allOf: [{ $ref: "#/components/schemas/Uuid" }]
          description: >
            Required target row id - trimmed, lower-cased, must be a UUID. The row
            must exist in the store `parentType` selects or the request is 404
            `SUPPORTING_DOCUMENT_PARENT_NOT_FOUND`; beyond existence its Client,
            Building, context and archived state are read for the alignment guards
            above, and nothing else: the parent is never returned, never written,
            never re-linked, never duplicated and never made dependent on this
            binding. An explicit null is treated as missing rather than as "no
            parent", and no parent-less binding can be created.

    HandymanRequest:
      type: object
      description: >
        CR-HM-BE-01 — Building-scoped Handyman Request intake record. `clientId`
        is derived from the Building, `createdByUserId` from the authenticated
        actor, `requestNumber` is server-generated, and `status` /
        `operationalSurface` are server-managed. The internal idempotency
        fingerprint is never returned.
      required: [id, clientId, buildingId, spaceId, customerName, createdByUserId, operationalSurface, inboundChannel, requestNumber, title, priority, status, requestedAt, createdAt, updatedAt]
      properties:
        id: { $ref: "#/components/schemas/Uuid" }
        clientId: { $ref: "#/components/schemas/Uuid" }
        buildingId: { $ref: "#/components/schemas/Uuid" }
        spaceId: { $ref: "#/components/schemas/Uuid" }
        tenantCompanyId: { $ref: "#/components/schemas/Uuid", nullable: true }
        tenantPicId: { $ref: "#/components/schemas/Uuid", nullable: true }
        customerName: { type: string, maxLength: 200 }
        customerPhone: { type: string, nullable: true, maxLength: 50 }
        customerEmail: { type: string, nullable: true, maxLength: 254 }
        createdByUserId: { $ref: "#/components/schemas/Uuid" }
        operationalSurface: { type: string, enum: [BM_SUPER_APP] }
        inboundChannel: { type: string, enum: [WHATSAPP, PHONE, WALK_IN, OTHER] }
        requestNumber: { type: string }
        title: { type: string, maxLength: 200 }
        description: { type: string, nullable: true, maxLength: 2000 }
        priority: { type: string, enum: [LOW, MEDIUM, HIGH, CRITICAL] }
        status: { type: string, enum: [SUBMITTED, CANCELLED] }
        idempotencyKey: { type: string, nullable: true, maxLength: 255 }
        requestedAt: { type: string, format: date-time }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateHandymanRequest:
      type: object
      description: >
        CR-HM-BE-01 RUN 3 — allowed intake fields ONLY. `buildingId` comes from
        the route, the actor from the session, and `clientId`,
        `createdByUserId`, `requestNumber`, `status`, `operationalSurface`,
        `idempotencyKey` (header-only), `idempotencyFingerprint` and
        `requestedAt` are never accepted from the caller — supplying any of
        them, or any other field, is rejected with 400 `VALIDATION_ERROR`.
      required: [spaceId, customerName, inboundChannel, title]
      properties:
        spaceId: { $ref: "#/components/schemas/Uuid" }
        tenantCompanyId: { $ref: "#/components/schemas/Uuid", nullable: true }
        tenantPicId: { $ref: "#/components/schemas/Uuid", nullable: true }
        customerName: { type: string, maxLength: 200 }
        customerPhone: { type: string, nullable: true, maxLength: 50 }
        customerEmail: { type: string, nullable: true, maxLength: 254 }
        inboundChannel: { type: string, enum: [WHATSAPP, PHONE, WALK_IN, OTHER] }
        title: { type: string, maxLength: 200 }
        description: { type: string, nullable: true, maxLength: 2000 }
        priority: { type: string, enum: [LOW, MEDIUM, HIGH, CRITICAL], default: MEDIUM }
