import type {PoolClient} from 'pg';import type {Migration} from './types';export const migration0074CreateReviews:Migration={id:'0074_create_reviews',async up(c:PoolClient){await c.query(`CREATE TABLE reviews(id UUID PRIMARY KEY,client_id UUID NOT NULL REFERENCES clients(id),target_type TEXT NOT NULL,target_id UUID NOT NULL,reviewer_user_id UUID NOT NULL REFERENCES users(id),decision TEXT,status TEXT NOT NULL DEFAULT 'PENDING',notes TEXT,reviewed_at TIMESTAMPTZ,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),CONSTRAINT review_target CHECK(target_type IN ('FORM_INSTANCE','CHECKLIST_EXECUTION')),CONSTRAINT review_decision CHECK(decision IS NULL OR decision IN ('APPROVED','REJECTED','REWORK_REQUIRED')),CONSTRAINT review_status CHECK(status IN ('PENDING','COMPLETED')),CONSTRAINT review_completed CHECK((status='COMPLETED' AND decision IS NOT NULL AND reviewed_at IS NOT NULL) OR status='PENDING'))`);await c.query('CREATE INDEX reviews_target_idx ON reviews(target_type,target_id,created_at);CREATE INDEX reviews_client_idx ON reviews(client_id,status)')},async down(c:PoolClient){await c.query('DROP TABLE IF EXISTS reviews')}};
