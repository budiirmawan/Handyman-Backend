import type { PoolClient } from 'pg'; import type { Migration } from './types';
export const migration0065CreateFormSectionsFields:Migration={id:'0065_create_form_sections_fields',async up(c:PoolClient){await c.query(`CREATE TABLE form_sections (id UUID PRIMARY KEY, form_template_id UUID NOT NULL REFERENCES form_templates(id) ON DELETE CASCADE, code TEXT NOT NULL, title TEXT NOT NULL, description TEXT, display_order INTEGER NOT NULL, status TEXT NOT NULL DEFAULT 'ACTIVE', created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), CONSTRAINT form_sections_template_code_unique UNIQUE(form_template_id,code), CONSTRAINT form_sections_order_check CHECK(display_order>=0), CONSTRAINT form_sections_status_check CHECK(status IN ('ACTIVE','INACTIVE')))`);await c.query(`CREATE TABLE form_fields (id UUID PRIMARY KEY, form_section_id UUID NOT NULL REFERENCES form_sections(id) ON DELETE CASCADE, code TEXT NOT NULL, label TEXT NOT NULL, field_type TEXT NOT NULL, required BOOLEAN NOT NULL DEFAULT FALSE, display_order INTEGER NOT NULL, placeholder TEXT, help_text TEXT, status TEXT NOT NULL DEFAULT 'ACTIVE', created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), CONSTRAINT form_fields_section_code_unique UNIQUE(form_section_id,code), CONSTRAINT form_fields_order_check CHECK(display_order>=0), CONSTRAINT form_fields_status_check CHECK(status IN ('ACTIVE','INACTIVE')), CONSTRAINT form_fields_type_check CHECK(field_type IN ('TEXT','TEXTAREA','NUMBER','DATE','DATETIME','BOOLEAN','SELECT')))`);await c.query('CREATE INDEX form_sections_template_order_idx ON form_sections(form_template_id,display_order); CREATE INDEX form_fields_section_order_idx ON form_fields(form_section_id,display_order)')},async down(c:PoolClient){await c.query('DROP TABLE IF EXISTS form_fields; DROP TABLE IF EXISTS form_sections')}};
