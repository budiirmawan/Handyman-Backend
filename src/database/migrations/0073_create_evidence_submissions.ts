import type {PoolClient} from 'pg';import type {Migration} from './types';export const migration0073CreateEvidenceSubmissions:Migration={id:'0073_create_evidence_submissions',async up(c:PoolClient){await c.query(`CREATE TABLE evidence_submissions(id UUID PRIMARY KEY,client_id UUID NOT NULL REFERENCES clients(id),evidence_requirement_id UUID REFERENCES evidence_requirements(id),execution_type TEXT NOT NULL,execution_id UUID NOT NULL,evidence_type TEXT NOT NULL,file_reference TEXT NOT NULL,original_file_name TEXT NOT NULL,mime_type TEXT NOT NULL,file_size BIGINT NOT NULL,captured_at TIMESTAMPTZ,submitted_by_user_id UUID REFERENCES users(id),status TEXT NOT NULL DEFAULT 'ACTIVE',created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),CONSTRAINT evidence_submission_type CHECK(evidence_type IN ('PHOTO','DOCUMENT','SIGNATURE')),CONSTRAINT evidence_submission_execution CHECK(execution_type IN ('FORM_INSTANCE','CHECKLIST_EXECUTION')),CONSTRAINT evidence_submission_status CHECK(status IN ('ACTIVE','REMOVED')),CONSTRAINT evidence_submission_size CHECK(file_size>=0 AND file_size<=52428800))`);await c.query('CREATE INDEX evidence_submissions_execution_idx ON evidence_submissions(execution_type,execution_id,status);CREATE INDEX evidence_submissions_requirement_idx ON evidence_submissions(evidence_requirement_id,status);CREATE INDEX evidence_submissions_client_idx ON evidence_submissions(client_id)')},async down(c:PoolClient){await c.query('DROP TABLE IF EXISTS evidence_submissions')}};
