import type {PoolClient} from 'pg';import type {Migration} from './types';export const migration0070CreateChecklistExecutions:Migration={id:'0070_create_checklist_executions',async up(c:PoolClient){await c.query(`CREATE TABLE checklist_executions(id UUID PRIMARY KEY,client_id UUID NOT NULL REFERENCES clients(id),checklist_template_id UUID NOT NULL REFERENCES checklist_templates(id),status TEXT NOT NULL DEFAULT 'DRAFT',started_at TIMESTAMPTZ,completed_at TIMESTAMPTZ,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),CONSTRAINT checklist_executions_status CHECK(status IN ('DRAFT','IN_PROGRESS','COMPLETED','CANCELLED')));CREATE INDEX checklist_executions_client_idx ON checklist_executions(client_id,created_at);CREATE INDEX checklist_executions_template_idx ON checklist_executions(checklist_template_id)`);await c.query(`CREATE TABLE checklist_item_responses(id UUID PRIMARY KEY,checklist_execution_id UUID NOT NULL REFERENCES checklist_executions(id) ON DELETE CASCADE,checklist_item_id UUID NOT NULL REFERENCES checklist_items(id),value JSONB,result TEXT,notes TEXT,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),CONSTRAINT checklist_response_unique UNIQUE(checklist_execution_id,checklist_item_id));CREATE INDEX checklist_responses_execution_idx ON checklist_item_responses(checklist_execution_id)`)},async down(c:PoolClient){await c.query('DROP TABLE IF EXISTS checklist_item_responses;DROP TABLE IF EXISTS checklist_executions')}};
