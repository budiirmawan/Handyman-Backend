import type {PoolClient} from 'pg';import type {Migration} from './types';export const migration0069CreateChecklistTemplates:Migration={id:'0069_create_checklist_templates',async up(c:PoolClient){await c.query(`CREATE TABLE checklist_templates(id UUID PRIMARY KEY,client_id UUID NOT NULL REFERENCES clients(id),code TEXT NOT NULL,name TEXT NOT NULL,description TEXT,status TEXT NOT NULL DEFAULT 'DRAFT',created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),CONSTRAINT checklist_templates_client_code UNIQUE(client_id,code),CONSTRAINT checklist_templates_status CHECK(status IN ('DRAFT','ACTIVE','INACTIVE')));CREATE INDEX checklist_templates_client_status ON checklist_templates(client_id,status)`);await c.query(`CREATE TABLE checklist_items(id UUID PRIMARY KEY,checklist_template_id UUID NOT NULL REFERENCES checklist_templates(id) ON DELETE CASCADE,code TEXT NOT NULL,label TEXT NOT NULL,item_type TEXT NOT NULL,required BOOLEAN NOT NULL DEFAULT FALSE,display_order INTEGER NOT NULL,status TEXT NOT NULL DEFAULT 'ACTIVE',created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),CONSTRAINT checklist_items_code UNIQUE(checklist_template_id,code),CONSTRAINT checklist_items_order CHECK(display_order>=0),CONSTRAINT checklist_items_status CHECK(status IN ('ACTIVE','INACTIVE')),CONSTRAINT checklist_items_type CHECK(item_type IN ('CHECK','BOOLEAN','TEXT','NUMBER')));CREATE INDEX checklist_items_template_order ON checklist_items(checklist_template_id,display_order)`)},async down(c:PoolClient){await c.query('DROP TABLE IF EXISTS checklist_items;DROP TABLE IF EXISTS checklist_templates')}};
