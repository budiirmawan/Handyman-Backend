import type{PoolClient}from'pg';import type{Migration}from'./types';
/** BE-19H lightweight operational expense only; no GL, COA, AP, tax, payroll, or accounting. */
export const migration0202CreateBasicExpenses:Migration={id:'0202_create_basic_expenses',async up(c:PoolClient){await c.query(`CREATE TABLE basic_expenses(id UUID PRIMARY KEY,client_id UUID NOT NULL REFERENCES clients(id),building_id UUID NOT NULL REFERENCES buildings(id),expense_number TEXT NOT NULL,expense_category TEXT NOT NULL,description TEXT NOT NULL,amount NUMERIC NOT NULL,expense_date DATE NOT NULL,vendor_service_cost_id UUID REFERENCES vendor_service_costs(id),status TEXT NOT NULL DEFAULT 'DRAFT',notes TEXT,created_by_user_id UUID NOT NULL REFERENCES users(id),finalized_at TIMESTAMPTZ,finalized_by_user_id UUID REFERENCES users(id),cancelled_at TIMESTAMPTZ,cancelled_by_user_id UUID REFERENCES users(id),created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),CONSTRAINT basic_expenses_number_unique UNIQUE(client_id,expense_number),CONSTRAINT basic_expenses_amount_check CHECK(amount>0),CONSTRAINT basic_expenses_status_check CHECK(status IN('DRAFT','FINALIZED','CANCELLED')),CONSTRAINT basic_expenses_state_check CHECK((status='DRAFT' AND finalized_at IS NULL AND cancelled_at IS NULL)OR(status='FINALIZED' AND finalized_at IS NOT NULL AND finalized_by_user_id IS NOT NULL AND cancelled_at IS NULL)OR(status='CANCELLED' AND cancelled_at IS NOT NULL AND cancelled_by_user_id IS NOT NULL)))`);await c.query(`CREATE INDEX basic_expenses_building_idx ON basic_expenses(building_id,expense_date DESC);CREATE INDEX basic_expenses_category_idx ON basic_expenses(expense_category,expense_date DESC);CREATE INDEX basic_expenses_status_idx ON basic_expenses(status,expense_date DESC);CREATE INDEX basic_expenses_vendor_cost_idx ON basic_expenses(vendor_service_cost_id)`);await c.query(`CREATE TABLE basic_expense_history(id UUID PRIMARY KEY,basic_expense_id UUID NOT NULL REFERENCES basic_expenses(id),action TEXT NOT NULL,expense_category TEXT NOT NULL,description TEXT NOT NULL,amount NUMERIC NOT NULL,expense_date DATE NOT NULL,status TEXT NOT NULL,notes TEXT,changed_by_user_id UUID NOT NULL REFERENCES users(id),changed_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),CONSTRAINT basic_expense_history_action_check CHECK(action IN('CREATED','UPDATED','FINALIZED','CANCELLED')))`);await c.query('CREATE INDEX basic_expense_history_expense_idx ON basic_expense_history(basic_expense_id,changed_at)')},async down(c:PoolClient){await c.query('DROP TABLE IF EXISTS basic_expense_history');await c.query('DROP TABLE IF EXISTS basic_expenses')}};
