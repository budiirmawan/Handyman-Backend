import type {PoolClient} from 'pg';import type {Migration} from './types';export const migration0067CreateFormInstancesResponses:Migration={id:'0067_create_form_instances_responses',async up(c:PoolClient){await c.query(`CREATE TABLE form_instances(id UUID PRIMARY KEY,client_id UUID NOT NULL REFERENCES clients(id),form_template_version_id UUID NOT NULL REFERENCES form_template_versions(id),status TEXT NOT NULL DEFAULT 'DRAFT',started_at TIMESTAMPTZ,completed_at TIMESTAMPTZ,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),CONSTRAINT form_instances_status CHECK(status IN ('DRAFT','IN_PROGRESS','COMPLETED','CANCELLED')))`);await c.query(`CREATE TABLE form_responses(id UUID PRIMARY KEY,form_instance_id UUID NOT NULL REFERENCES form_instances(id) ON DELETE CASCADE,version_field_id UUID NOT NULL REFERENCES form_template_version_fields(id),value JSONB,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),CONSTRAINT form_responses_unique UNIQUE(form_instance_id,version_field_id))`);await c.query('CREATE INDEX form_instances_client_idx ON form_instances(client_id,created_at);CREATE INDEX form_instances_version_idx ON form_instances(form_template_version_id);CREATE INDEX form_responses_instance_idx ON form_responses(form_instance_id)')},async down(c:PoolClient){await c.query('DROP TABLE IF EXISTS form_responses;DROP TABLE IF EXISTS form_instances')}};
