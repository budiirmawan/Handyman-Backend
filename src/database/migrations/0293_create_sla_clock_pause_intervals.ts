import type{PoolClient}from'pg';import type{Migration}from'./types';
/** CR-BE-SLA-01 PART 03 — auditable Work Order hold intervals only. */
export const migration0293CreateSlaClockPauseIntervals:Migration={id:'0293_create_sla_clock_pause_intervals',async up(c){await c.query(`CREATE TABLE sla_clock_pause_intervals(id UUID PRIMARY KEY,sla_clock_id UUID NOT NULL REFERENCES sla_clocks(id),paused_at TIMESTAMPTZ NOT NULL,resumed_at TIMESTAMPTZ,pause_source TEXT NOT NULL,pause_actor_user_id UUID REFERENCES users(id),resume_actor_user_id UUID REFERENCES users(id),created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),CONSTRAINT sla_pause_source_check CHECK(pause_source='WORK_ORDER_ON_HOLD'),CONSTRAINT sla_pause_range_check CHECK(resumed_at IS NULL OR resumed_at>=paused_at));CREATE UNIQUE INDEX sla_clock_one_open_pause_idx ON sla_clock_pause_intervals(sla_clock_id) WHERE resumed_at IS NULL;CREATE INDEX sla_clock_pause_history_idx ON sla_clock_pause_intervals(sla_clock_id,paused_at)`)} ,async down(c){await c.query('DROP TABLE IF EXISTS sla_clock_pause_intervals')}};
