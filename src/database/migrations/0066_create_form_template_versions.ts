import type {PoolClient} from 'pg';import type {Migration} from './types';export const migration0066CreateFormTemplateVersions:Migration={id:'0066_create_form_template_versions',async up(c:PoolClient){await c.query(`CREATE TABLE form_template_versions(id UUID PRIMARY KEY,form_template_id UUID NOT NULL REFERENCES form_templates(id) ON DELETE CASCADE,version_number INTEGER NOT NULL,status TEXT NOT NULL DEFAULT 'DRAFT',published_at TIMESTAMPTZ,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),CONSTRAINT form_template_versions_unique UNIQUE(form_template_id,version_number),CONSTRAINT form_template_versions_number CHECK(version_number>0),CONSTRAINT form_template_versions_status CHECK(status IN ('DRAFT','PUBLISHED','RETIRED')),CONSTRAINT form_template_versions_published_at CHECK((status='PUBLISHED' AND published_at IS NOT NULL) OR status<>'PUBLISHED'))`);await c.query(`CREATE TABLE form_template_version_sections(id UUID PRIMARY KEY,version_id UUID NOT NULL REFERENCES form_template_versions(id) ON DELETE CASCADE,section_id UUID NOT NULL,code TEXT NOT NULL,title TEXT NOT NULL,description TEXT,display_order INTEGER NOT NULL,status TEXT NOT NULL);CREATE TABLE form_template_version_fields(id UUID PRIMARY KEY,version_section_id UUID NOT NULL REFERENCES form_template_version_sections(id) ON DELETE CASCADE,field_id UUID NOT NULL,code TEXT NOT NULL,label TEXT NOT NULL,field_type TEXT NOT NULL,required BOOLEAN NOT NULL,display_order INTEGER NOT NULL,placeholder TEXT,help_text TEXT,status TEXT NOT NULL)`);await c.query('CREATE INDEX form_template_versions_template_idx ON form_template_versions(form_template_id,version_number);CREATE INDEX form_template_versions_status_idx ON form_template_versions(status)')},async down(c:PoolClient){await c.query('DROP TABLE IF EXISTS form_template_version_fields;DROP TABLE IF EXISTS form_template_version_sections;DROP TABLE IF EXISTS form_template_versions')}};
