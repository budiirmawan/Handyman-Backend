import type{PoolClient}from'pg';import type{Migration}from'./types';export const migration0077CreateTasks:Migration={id:'0077_create_tasks',async up(c:PoolClient){await c.query(`CREATE TABLE generated_tasks(id UUID PRIMARY KEY,client_id UUID NOT NULL REFERENCES clients(id),schedule_definition_id UUID NOT NULL REFERENCES schedule_definitions(id),occurrence_at TIMESTAMPTZ NOT NULL,target_type TEXT NOT NULL,target_id UUID NOT NULL,building_id UUID REFERENCES buildings(id),status TEXT NOT NULL DEFAULT 'OPEN',generated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),CONSTRAINT task_status CHECK(status IN ('OPEN')),CONSTRAINT task_unique_occurrence UNIQUE(schedule_definition_id,occurrence_at))`);await c.query('CREATE INDEX tasks_client_idx ON generated_tasks(client_id,created_at);CREATE INDEX tasks_schedule_idx ON generated_tasks(schedule_definition_id,occurrence_at);CREATE INDEX tasks_target_idx ON generated_tasks(target_type,target_id)')},async down(c:PoolClient){await c.query('DROP TABLE IF EXISTS generated_tasks')}};
