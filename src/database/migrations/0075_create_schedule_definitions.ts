import type {PoolClient}from'pg';import type{Migration}from'./types';export const migration0075CreateScheduleDefinitions:Migration={id:'0075_create_schedule_definitions',async up(c:PoolClient){await c.query(`CREATE TABLE schedule_definitions(id UUID PRIMARY KEY,client_id UUID NOT NULL REFERENCES clients(id),code TEXT NOT NULL,name TEXT NOT NULL,target_type TEXT NOT NULL,target_id UUID NOT NULL,building_id UUID REFERENCES buildings(id),start_at TIMESTAMPTZ NOT NULL,end_at TIMESTAMPTZ,timezone TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'ACTIVE',created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),CONSTRAINT schedule_target CHECK(target_type IN ('FORM_TEMPLATE','FORM_VERSION','CHECKLIST_TEMPLATE')),CONSTRAINT schedule_range CHECK(end_at IS NULL OR start_at<=end_at),CONSTRAINT schedule_status CHECK(status IN ('ACTIVE','INACTIVE')),CONSTRAINT schedule_active_unique UNIQUE(client_id,target_type,target_id,building_id,status,code))`);await c.query('CREATE INDEX schedules_client_status ON schedule_definitions(client_id,status);CREATE INDEX schedules_target ON schedule_definitions(target_type,target_id);CREATE INDEX schedules_building ON schedule_definitions(building_id)')},async down(c:PoolClient){await c.query('DROP TABLE IF EXISTS schedule_definitions')}};
