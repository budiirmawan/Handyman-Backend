import { foundationAccessSeed } from './foundation-access.seed';
import type { Seed } from './types';

export type { Seed } from './types';

export const seeds: readonly Seed[] = [foundationAccessSeed];
