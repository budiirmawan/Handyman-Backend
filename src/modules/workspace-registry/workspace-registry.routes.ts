import{Router}from'express';import{authenticationMiddleware}from'../auth/authentication.middleware';import{requirePermission}from'../auth/rbac.middleware';import{createBuildingWorkspace,createClientWorkspace,effectiveBuildingWorkspaces,effectiveClientWorkspaces,getWorkspace,listBuildingWorkspaces,listClientWorkspaces,updateWorkspace}from'./workspace-registry.controller';export function createWorkspaceRegistryRouter(){const r=Router(),a=authenticationMiddleware,rd=requirePermission('workspace_registry.read'),m=requirePermission('workspace_registry.manage');r.post('/clients/:clientId/workspaces',a,m,createClientWorkspace);r.get('/clients/:clientId/workspaces/effective',a,rd,effectiveClientWorkspaces);r.get('/clients/:clientId/workspaces',a,rd,listClientWorkspaces);r.post('/buildings/:buildingId/workspaces',a,m,createBuildingWorkspace);r.get('/buildings/:buildingId/workspaces/effective',a,rd,effectiveBuildingWorkspaces);r.get('/buildings/:buildingId/workspaces',a,rd,listBuildingWorkspaces);r.get('/workspaces/:id',a,rd,getWorkspace);r.patch('/workspaces/:id',a,m,updateWorkspace);return r}
