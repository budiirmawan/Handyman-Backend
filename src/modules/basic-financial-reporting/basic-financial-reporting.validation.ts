import{AppError}from'../../shared/errors';import{isValidUuid}from'../clients';import type{FinancialReportFilters}from'./basic-financial-reporting.types';type D={field:string;message:string};const DATE=/^\d{4}-\d{2}-\d{2}$/;function rec(v:unknown):v is Record<string,unknown>{return typeof v==='object'&&v!==null&&!Array.isArray(v)}function fail(d:D[]):never{throw AppError.validation('Request validation failed.',d)}export function parseFinancialReportBuildingId(raw:string){const v=raw.trim().toLowerCase();if(!isValidUuid(v))fail([{field:'buildingId',message:'buildingId must be a valid UUID.'}]);return v}export function parseFinancialReportFilters(q:unknown):FinancialReportFilters{if(!rec(q))return{};const d:D[]=[];let tenantCompanyId:string|undefined;if(q.tenantCompanyId!==undefined){if(typeof q.tenantCompanyId!=='string'||!isValidUuid(q.tenantCompanyId.trim()))d.push({field:'tenantCompanyId',message:'tenantCompanyId must be a valid UUID.'});else tenantCompanyId=q.tenantCompanyId.trim().toLowerCase()}const periodFrom=date(q.periodFrom,'periodFrom',d),periodTo=date(q.periodTo,'periodTo',d);if(periodFrom&&periodTo&&periodTo<periodFrom)d.push({field:'periodTo',message:'periodTo must be the same as or after periodFrom.'});if(d.length)fail(d);return{...(tenantCompanyId?{tenantCompanyId}:{}),...(periodFrom?{periodFrom}:{}),...(periodTo?{periodTo}:{})}}function date(v:unknown,f:string,d:D[]){if(v===undefined)return undefined;if(typeof v!=='string'||!DATE.test(v)||!calendar(v)){d.push({field:f,message:`${f} must be a valid YYYY-MM-DD date.`});return undefined}return v}function calendar(v:string){const[y,m,day]=v.split('-').map(Number),x=new Date(Date.UTC(y,m-1,day));return x.getUTCFullYear()===y&&x.getUTCMonth()===m-1&&x.getUTCDate()===day}
