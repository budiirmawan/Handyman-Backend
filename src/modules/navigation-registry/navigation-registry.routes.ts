import{Router}from'express';import{authenticationMiddleware}from'../auth/authentication.middleware';import{requirePermission}from'../auth/rbac.middleware';import{createBuildingNavigation,createClientNavigation,effectiveBuildingNavigation,effectiveClientNavigation,getNavigation,listBuildingNavigation,listClientNavigation,updateNavigation}from'./navigation-registry.controller';export function createNavigationRegistryRouter(){const r=Router(),a=authenticationMiddleware,rd=requirePermission('navigation_registry.read'),m=requirePermission('navigation_registry.manage');r.post('/clients/:clientId/navigation-items',a,m,createClientNavigation);r.get('/clients/:clientId/navigation-items',a,rd,listClientNavigation);r.get('/clients/:clientId/navigation/effective',a,rd,effectiveClientNavigation);r.post('/buildings/:buildingId/navigation-items',a,m,createBuildingNavigation);r.get('/buildings/:buildingId/navigation-items',a,rd,listBuildingNavigation);r.get('/buildings/:buildingId/navigation/effective',a,rd,effectiveBuildingNavigation);r.get('/navigation-items/:id',a,rd,getNavigation);r.patch('/navigation-items/:id',a,m,updateNavigation);return r}
