import{AppError}from'../../shared/errors';import{isValidUuid}from'../clients';import{isPermitContractorContextType}from'../permits/permit.types';import type{PermitWorkFilters,PermitWorkNotesInput}from'./permit-work-lifecycle.types';type D={field:string;message:string};function rec(v:unknown):v is Record<string,unknown>{return typeof v==='object'&&v!==null&&!Array.isArray(v)}function fail(d:D[]):never{throw AppError.validation('Request validation failed.',d)}export function parsePermitWorkPermitIdParam(r:string){const v=r.trim().toLowerCase();if(!isValidUuid(v))fail([{field:'permitId',message:'permitId must be a valid UUID.'}]);return v}export function parsePermitWorkNotesBody(b:unknown):PermitWorkNotesInput{if(b===undefined||b===null)return{};if(!rec(b))fail([{field:'body',message:'Request body must be a JSON object.'}]);if(b.notes===undefined||b.notes===null)return b.notes===null?{notes:null}:{};if(typeof b.notes!=='string')fail([{field:'notes',message:'notes must be a string.'}]);const notes=b.notes.trim();if(notes.length>2000)fail([{field:'notes',message:'notes is too long.'}]);return{notes:notes||null}}export function parsePermitWorkFilters(q:unknown):PermitWorkFilters{if(!rec(q))return{};const d:D[]=[],buildingId=id(q.buildingId,'buildingId',d),contractorVendorId=id(q.contractorVendorId??q.contractorId,'contractorVendorId',d);let contractorContextType:string|undefined;if(q.contractorContextType!==undefined){const n=typeof q.contractorContextType==='string'?q.contractorContextType.trim().toUpperCase():q.contractorContextType;if(!isPermitContractorContextType(n))d.push({field:'contractorContextType',message:'contractorContextType is invalid.'});else contractorContextType=n}let status:'IN_PROGRESS'|'CLOSED'|undefined;if(q.status!==undefined){const n=typeof q.status==='string'?q.status.trim().toUpperCase():q.status;if(n!=='IN_PROGRESS'&&n!=='CLOSED')d.push({field:'status',message:'status must be IN_PROGRESS or CLOSED.'});else status=n}if(d.length)fail(d);return{...(buildingId?{buildingId}:{}),...(contractorContextType?{contractorContextType}:{}),...(contractorVendorId?{contractorVendorId}:{}),...(status?{status}:{})}}function id(v:unknown,f:string,d:D[]){if(v===undefined)return undefined;if(typeof v!=='string'||!isValidUuid(v.trim())){d.push({field:f,message:`${f} must be a valid UUID.`});return undefined}return v.trim().toLowerCase()}
